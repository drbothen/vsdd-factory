# AC-002b Evidence — Architecture Compliance: hooks.json Zero Entries + .sh Deleted

**Story:** S-8.08 — Native port: track-agent-start (PreToolUse:Agent)
**BC trace:** Architecture Compliance concern (hook path lifecycle); not a BC-7.03.079 invariant test
**Status:** PASS

## Acceptance Criterion

Verify `hooks.json.*` platform files and `hooks.json.template` contain ZERO
`track-agent-start` command entries (post-DRIFT-004 baseline; verification-only step).
Delete `plugins/vsdd-factory/hooks/track-agent-start.sh` from the repository.

## Evidence

### hooks.json Zero-Entry Verification

Command run: `grep -rn "track-agent-start" plugins/vsdd-factory/hooks/hooks.json*`

Result: no output — zero matches across all platform files and template.

This is the expected post-DRIFT-004 baseline. Native WASM plugins dispatch via
`hooks-registry.toml` exclusively; they do not register in `hooks.json.*`. The
`track-agent-start` hook never had a `hooks.json` entry following DRIFT-004.

Architecture Compliance rule (E-8 D-7 / DRIFT-004):
> "Dispatcher routing: native WASM plugins do NOT register in hooks.json"

Verification confirms this rule holds. No deletion was required — this was a
read-only verification step.

### Bash Script Deletion

File `plugins/vsdd-factory/hooks/track-agent-start.sh`: **DELETED**

Verification command: `ls plugins/vsdd-factory/hooks/track-agent-start.sh`
Result: `No such file or directory`

The bash script is fully replaced by the native WASM crate at
`crates/hook-plugins/track-agent-start/`. All hook logic now executes natively
via `serde_json` (replacing `jq`) and `vsdd_hook_sdk::host::emit_event`
(replacing `bin/emit-event` subprocess call).

### Bats Test

```
ok 4 AC-002b: plugins/vsdd-factory/hooks/track-agent-start.sh is deleted
```

Test assertion: `[ ! -f "${REPO_ROOT}/plugins/vsdd-factory/hooks/track-agent-start.sh" ]`
Result: **PASS**

### bin/emit-event Not Removed (Compliance Check)

Per Architecture Compliance rule (E-8 D-10 + R-8.07):
> "bin/emit-event must NOT be removed until S-8.29"

The `bin/emit-event` binary was not touched. Only the hook plugin's internal
implementation changed from bash subprocess calls to native host function calls.
The shared binary remains available for other hooks still using the legacy-bash-adapter.
