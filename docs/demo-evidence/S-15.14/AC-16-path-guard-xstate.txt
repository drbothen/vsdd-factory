# AC-16 Demo Evidence: Path /some/dir/xSTATE.md does NOT trigger STATE.md validation
# BC-5.39.006 invariant 3; EC-019
#
# Command:
#   cd plugins/vsdd-factory/tests/validate-dispatch-advance
#   bats --tap pass-xstate-md-not-target.bats
#
# Fixture: pass-xstate-md-not-target/ — STATE.md content at path named xSTATE.md
#   dispatcher envelope uses file_path ending in "xSTATE.md"
#   is_state_md_target() uses Path::file_name() == Some("STATE.md") — STRICT
#   "xSTATE.md".file_name() == "xSTATE.md" != "STATE.md" => is_state_md_target returns false
#
# Expected: exit 0 (Continue; hook does not run STATE.md validation path)

$ bats --tap pass-xstate-md-not-target.bats
1..1
ok 1 AC-16 PASS: path-component-strict guard passes when file is xSTATE.md (not STATE.md)
