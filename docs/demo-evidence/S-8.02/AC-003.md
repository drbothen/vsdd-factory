# AC-003 Evidence — STEP_COMPLETE counting >=8 passes (BC-7.03.046 + BC-2.02.012 P6)

**AC statement:** When `agent_type` contains `pr-manager` or `pr_manager` AND the result
text (extracted via BC-2.02.012 Postcondition 6 canonical typed-projection fallback chain:
`payload.last_assistant_message.as_deref().or(payload.result.as_deref()).unwrap_or("")`)
contains >= 8 *lines containing* `STEP_COMPLETE:` (line-occurrence semantics, matching
`grep -c`): WASM plugin exits 0 (all steps complete, no block).

**BC traces:** BC-7.03.046 postcondition 1; BC-2.02.012 postcondition 6

---

## Implementation (src/lib.rs)

**BC-2.02.012 Postcondition 6 canonical chain (lines 188-193):**
```rust
// Per BC-2.02.012 Postcondition 6:
let result: &str = payload
    .last_assistant_message
    .as_deref()
    .or(payload.result.as_deref())
    .unwrap_or("");
```

**STEP_COMPLETE line counting (lines 103-107):**
```rust
pub fn count_step_complete_lines(text: &str) -> u64 {
    text.lines()
        .filter(|line| line.contains("STEP_COMPLETE:"))
        .count() as u64
}
```
Line-occurrence semantics: one line with multiple `STEP_COMPLETE:` tokens counts as 1
(mirrors `grep -c` behavior). This matches bash line behavior exactly.

**Pass guard (line 196-198):**
```rust
let step_count = count_step_complete_lines(result);
if step_count >= 8 {
    return HookResult::Continue;
}
```

---

## Bats test evidence

```
ok 10 AC-006(b): 9 STEP_COMPLETE lines → exit 0, no hook.block
ok 11 AC-006(b EC-005): exactly 8 STEP_COMPLETE lines → exit 0, no hook.block
```

Both dispatched through live factory-dispatcher with WASM plugin.
Exit code 0 verified; `assert_no_hook_block` confirmed no hook.block in dispatcher log.

---

## Unit test evidence

```
test tests::test_BC_7_03_046_count_step_complete_lines_empty_text_returns_zero ... ok
test tests::test_BC_7_03_046_count_step_complete_lines_eight_lines_returns_eight ... ok
test tests::test_BC_7_03_046_count_step_complete_lines_nine_lines_returns_nine ... ok
test tests::test_BC_7_03_046_count_step_complete_lines_one_line_two_tokens_counts_as_one ... ok
test tests::test_BC_7_03_046_exactly_eight_steps_passes ... ok
test tests::test_BC_7_03_046_nine_steps_complete_passes ... ok
test tests::test_BC_2_02_012_pr_manager_result_fallback_to_result_field ... ok
```

32 unit tests total, all pass (`cargo test -p pr-manager-completion-guard`).

---

## EC-005 edge case (exactly 8 lines)

The `>= 8` threshold is inclusive. With exactly 8 STEP_COMPLETE lines, the hook exits 0.
Bats test `AC-006(b EC-005)` and unit test `test_BC_7_03_046_exactly_eight_steps_passes`
both verify this off-by-one boundary.

---

## Result

AC-003: PASS. STEP_COMPLETE line counting uses `grep -c` semantics; >= 8 lines yields
exit 0; BC-2.02.012 Postcondition 6 canonical typed-projection chain used for result
extraction; 19/19 bats + 32/32 unit tests green.
