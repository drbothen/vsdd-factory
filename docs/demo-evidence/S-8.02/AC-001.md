# AC-001 Evidence — WASM crate built; registry migration (BC-7.03.045)

**AC statement:** WASM crate `crates/hook-plugins/pr-manager-completion-guard/` exists
with `Cargo.toml` targeting `wasm32-wasip1`, implements vsdd_hook_sdk hook interface,
builds successfully. Registry entry updated to native plugin path; `script_path` and
`shell_bypass_acknowledged` removed. Binding preserved: `event = "SubagentStop"`,
`priority = 920`, `on_error = "block"`, `timeout_ms = 5000`.

**BC trace:** BC-7.03.045 postcondition 1 (identity & registry binding)

---

## Crate structure

```
crates/hook-plugins/pr-manager-completion-guard/
├── Cargo.toml
└── src/
    ├── lib.rs   (pure logic: hint table, step counting, BLOCKED detection, guard logic)
    └── main.rs  (WASI entry point: wires logic to host::emit_event + eprint)
```

## Cargo.toml (key fields)

```toml
[package]
name = "pr-manager-completion-guard"
version = "0.0.1"
description = "WASM hook plugin: guards pr-manager SubagentStop; blocks early stop before
  all 9 PR lifecycle steps complete (FM4 prevention)"

[lib]
path = "src/lib.rs"

[[bin]]
name = "pr-manager-completion-guard"
path = "src/main.rs"

[dependencies]
vsdd-hook-sdk = { path = "../../hook-sdk" }
serde_json = { workspace = true }
regex = { workspace = true }
```

## WASM artifact

```
plugins/vsdd-factory/hook-plugins/pr-manager-completion-guard.wasm
```
Confirmed present at: `plugins/vsdd-factory/hook-plugins/pr-manager-completion-guard.wasm`

## hooks-registry.toml migration diff

```diff
 [[hooks]]
 name = "pr-manager-completion-guard"
 event = "SubagentStop"
-plugin = "hook-plugins/legacy-bash-adapter.wasm"
+plugin = "hook-plugins/pr-manager-completion-guard.wasm"
 priority = 920
 timeout_ms = 5000
 on_error = "block"

-[hooks.config]
-script_path = "hooks/pr-manager-completion-guard.sh"
-
-[hooks.capabilities]
-env_allow = ["PATH", "HOME", "TMPDIR", "CLAUDE_PROJECT_DIR", "CLAUDE_PLUGIN_ROOT", "VSDD_SESSION_ID"]
-
-[hooks.capabilities.exec_subprocess]
-binary_allow = ["bash", "gh", "jq"]
-shell_bypass_acknowledged = "legacy-bash-adapter runs unported hooks"
-env_allow = ["PATH", "HOME", "TMPDIR", "CLAUDE_PROJECT_DIR", "CLAUDE_PLUGIN_ROOT", "VSDD_SESSION_ID"]
```

Registry binding preserved:
- `event = "SubagentStop"` — correct trigger
- `priority = 920` — unchanged
- `timeout_ms = 5000` — unchanged
- `on_error = "block"` — preserved (dispatcher-level crash behavior)

## Workspace root Cargo.toml

`"crates/hook-plugins/pr-manager-completion-guard"` added to `[workspace] members`.

## Result

AC-001: PASS. WASM crate created, builds to `.wasm` artifact, registry migrated to
native plugin path, all binding fields preserved, legacy adapter fields removed.
