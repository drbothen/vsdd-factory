# AC-003 Evidence: hooks-registry.toml Preamble Comment

**Story:** S-19.04 — Registry/bundle hygiene: orphan WASM removal + tool-filter regex anchoring convention + lint check
**AC:** AC-003 — `hooks-registry.toml` contains a preamble comment documenting regex SEARCH semantics and anchoring convention

## Claim

`plugins/vsdd-factory/hooks-registry.toml` contains a preamble comment section
(before the first `[[hooks]]` entry) that documents: (1) `tool` field uses regex
SEARCH semantics (not fullmatch); (2) the `^(ToolA|ToolB)$` anchoring convention;
(3) why anchoring matters (e.g., `Edit` without anchoring matches `MultiEdit`).

## Evidence

### Gate: preamble markers present

```
$ grep -n "regex SEARCH\|fullmatch\|anchoring" plugins/vsdd-factory/hooks-registry.toml
16:# The `tool` field in [[hooks]] entries is matched using regex SEARCH semantics
17:# (substring match), NOT fullmatch.  This means an unanchored value such as
26:# Why anchoring matters:
27:#   Without anchoring,  tool = "Edit"       matches MultiEdit, MultiEdit*, etc.
28:#   Without anchoring,  tool = "Write"      would match a hypothetical MultiWrite.
29:#   Without anchoring,  tool = "Bash"       would match a hypothetical BashAsync.
```

Exit 0 — all three required keywords (`regex SEARCH`, `fullmatch`, `anchoring`) are
present in the preamble comment at lines 16-29 of `hooks-registry.toml`.

## VP Trace

VP-099 — preamble comment documents tool-filter regex semantics; prevents silent
fullmatch assumption by future registry editors.

## Pass / Fail

PASS — `grep -q "regex SEARCH\|fullmatch\|anchoring"` exits 0. Preamble comment
present at the top of `hooks-registry.toml` covering all three required topics.
