# AC-005 Evidence: Bats Lint Suite — registry-tool-filter-anchoring.bats (7/7)

**Story:** S-19.04 — Registry/bundle hygiene: orphan WASM removal + tool-filter regex anchoring convention + lint check
**AC:** AC-005 — `registry-tool-filter-anchoring.bats` exits 0; T-011/T-012 reject-fixtures flagged; verify-factory-lock classified as fully anchored

## Claim

`bats plugins/vsdd-factory/tests/registry-tool-filter-anchoring.bats` exits 0
(7/7 tests passing). The suite includes T-011 (prefix-only-anchor reject-fixture)
and T-012 (comment-injection reject-fixture). It confirms verify-factory-lock's
`^(Edit|Write|MultiEdit|Agent)$` pattern is correctly classified as fully anchored.

## Evidence

```
$ bats plugins/vsdd-factory/tests/registry-tool-filter-anchoring.bats
1..7
ok 1 T-001 AC-004/AC-005: unanchored fixture entry detected by lint
ok 2 T-002 AC-004/AC-005: anchored fixture entry passes lint
ok 3 T-003 AC-005: intent-comment exemption passes lint (EC-001)
ok 4 T-004 AC-004/AC-005: actual hooks-registry.toml has no unanchored tool entries
ok 5 T-005 AC-004: verify-factory-lock tool pattern is anchored and includes MultiEdit
ok 6 T-011 AC-004/AC-005: prefix-only anchor (^Bash, no trailing $) detected as violation
ok 7 T-012 AC-004/AC-005: comment-injection fixture (^Edit with $ in comment) flagged as violation
```

Exit 0 — 7/7 tests pass.

### Test-to-AC mapping

| Test | AC | Description |
|------|----|-------------|
| T-001 | AC-004/AC-005 | Unanchored fixture (`tool = "Edit\|Write"`) → lint exits non-zero |
| T-002 | AC-004/AC-005 | Anchored fixture (`tool = "^(Edit\|Write)$"`) → lint exits 0 |
| T-003 | AC-005 | Intent-comment exemption (`# intent:`) → lint exits 0 (EC-001) |
| T-004 | AC-004/AC-005 | Live `hooks-registry.toml` post-fix → zero violations, exits 0 (RG-004) |
| T-005 | AC-004 | `verify-factory-lock` pattern `^(Edit\|Write\|MultiEdit\|Agent)$` is fully anchored and MultiEdit-inclusive |
| T-011 | AC-004/AC-005 | `prefix-only-anchor.toml` (`tool = "^Bash"`) → exits non-zero (RG-006/F6-1) |
| T-012 | AC-004/AC-005 | `comment-inject.toml` (`tool = "^Edit" # note "$"`) → exits non-zero (RG-007/F7-1) |

## VP Trace

VP-099 — bats suite is the primary lint gate for tool-filter anchoring; T-011/T-012
close the prefix-only-anchor and comment-injection bypass classes respectively.

## Pass / Fail

PASS — 7/7 tests pass. T-011 and T-012 both flag their respective reject-fixtures.
T-004 confirms zero violations against the live registry. T-005 confirms positive-scope
verify-factory-lock classification.
