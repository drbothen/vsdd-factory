# AC-007 Evidence: stage_release_bundle Staging Simulation (T-010)

**Story:** S-19.04 — Registry/bundle hygiene: orphan WASM removal + tool-filter regex anchoring convention + lint check
**AC:** AC-007 — `stage_release_bundle` mirrors release.yml `*_*.wasm` outer-arm underscore-glob; zero dual-registry-orphan WASMs in staged artifact/; `some_new_stub_lib.wasm` absent (glob-proof)

## Claim

The `stage_release_bundle` Rust function mirrors the release.yml `*_*.wasm` outer-arm
underscore-glob semantics. A pre-staging fixture containing:
- `vsdd_context_resolvers.wasm` (underscore orphan — skipped by `*_*.wasm` arm)
- `wasm_resolver_export.wasm` (underscore orphan — skipped by `*_*.wasm` arm)
- `some_new_stub_lib.wasm` (underscore, NOT in inner legacy denylist — glob-proof fixture)
- `vsdd-context-resolvers.wasm` (hyphen, keep-assertion: survives staging)

`hello-hook.wasm` is ABSENT from the fixture — its exclusion is upstream via
BUILD-OMISSION (AC-001), not the staging glob.

After staging: zero dual-registry-orphan WASMs; `vsdd-context-resolvers.wasm` present;
`some_new_stub_lib.wasm` absent (proves outer `*_*.wasm` governs ALL underscore names,
not only a hardcoded denylist).

## Evidence

```
$ cargo test --test bundle_orphan_check test_S_19_04_ac007_T010_release_staging_underscore_glob_excludes_orphans -- --nocapture
    Finished `test` profile [unoptimized + debuginfo] target(s) in 0.17s
     Running tests/bundle_orphan_check.rs (target/debug/deps/bundle_orphan_check-511188a9d6df476c)

running 1 test
test test_S_19_04_ac007_T010_release_staging_underscore_glob_excludes_orphans ... ok

test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.00s
```

### Assertions verified by T-010

| Assertion | Status |
|-----------|--------|
| Zero dual-registry-orphan WASMs in staged `artifact/` (EAC-005) | PASS |
| `vsdd-context-resolvers.wasm` present in `artifact/` (keep-assertion i) | PASS |
| `some_new_stub_lib.wasm` absent from `artifact/` (glob-semantics proof) | PASS |
| `resolvers-registry.toml` still contains `hook-plugins/vsdd-context-resolvers.wasm` (keep-assertion ii) | PASS |

### Glob-semantics proof rationale

`some_new_stub_lib.wasm` is NOT in the inner legacy-documentation name list
(`vsdd_context_resolvers.wasm|wasm_resolver_export.wasm`). Its absence from `artifact/`
proves the outer `*_*.wasm` arm governs ALL underscore-named WASMs — a
hardcoded-denylist implementation would erroneously copy `some_new_stub_lib.wasm`
(and T-010 would fail). The outer glob is the correct mechanism.

## Epic Trace

EAC-005 — zero dual-registry-orphan WASMs in rc.23 bundle; T-010 is the
load-bearing leg of this epic acceptance criterion.

## Pass / Fail

PASS — T-010 exits 0. All four assertions verified. Outer `*_*.wasm` underscore-glob
semantics are correctly implemented in `stage_release_bundle` and mirror release.yml.
