# AC-001 Evidence: hello-hook.wasm Build-Omission from release.yml

**Story:** S-19.04 — Registry/bundle hygiene: orphan WASM removal + tool-filter regex anchoring convention + lint check
**AC:** AC-001 — After this story, release.yml no longer copies `hello-hook.wasm` into the release artifact

## Claim

The explicit `cargo build --example hello-hook` build step AND any `hello-hook.wasm`
copy step are REMOVED from `.github/workflows/release.yml`. Keep-assertions confirm
the live WaveContextResolver (`vsdd-context-resolvers.wasm`, hyphen) is untouched.

## Evidence

### Gate 1: `--example hello-hook` build step absent

```
$ ! grep -q 'example hello-hook' .github/workflows/release.yml && echo "BUILD_OMISSION_OK (no --example hello-hook in release.yml)"
BUILD_OMISSION_OK (no --example hello-hook in release.yml)
```

Exit 0 — no `--example hello-hook` build step in release.yml.

### Gate 2: `hello-hook.wasm` copy step absent

```
$ ! grep -q 'hello-hook.wasm' .github/workflows/release.yml && echo "BUILD_OMISSION_OK (no hello-hook.wasm copy step in release.yml)"
BUILD_OMISSION_OK (no hello-hook.wasm copy step in release.yml)
```

Exit 0 — no `hello-hook.wasm` copy step in release.yml.

### Keep-assertion (i): live WaveContextResolver present

```
$ test -f plugins/vsdd-factory/hook-plugins/vsdd-context-resolvers.wasm && echo "KEEP_ASSERTION_OK (i): vsdd-context-resolvers.wasm present (git-tracked live WaveContextResolver)"
KEEP_ASSERTION_OK (i): vsdd-context-resolvers.wasm present (git-tracked live WaveContextResolver)
```

Exit 0 — `vsdd-context-resolvers.wasm` (hyphen) is present and untouched.

### Keep-assertion (ii): registry reference intact

```
$ grep -q "hook-plugins/vsdd-context-resolvers.wasm" plugins/vsdd-factory/resolvers-registry.toml && echo "KEEP_ASSERTION_OK (ii): registry reference intact"
KEEP_ASSERTION_OK (ii): registry reference intact
```

Exit 0 — `resolvers-registry.toml` reference to the live resolver is intact.

## Pass / Fail

PASS — Both BUILD-OMISSION gates exit 0 (patterns absent from release.yml). Both
keep-assertions exit 0 (live WaveContextResolver present and registered).
