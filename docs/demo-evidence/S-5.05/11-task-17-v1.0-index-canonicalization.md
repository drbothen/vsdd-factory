# Task 17 — v1.0-index.md S-5.7 → S-5.07 canonicalization

**Task statement:** Update `docs/guide/v1.0-index.md` lines 6 + 40 to use
canonical `S-5.07` form (legacy short-form ID drift carryforward).

**Evidence type:** before/after diff

## Line 6 — before (HEAD~1)

```
> the 1.0.0 release gate (S-5.7).
```

## Line 6 — after (HEAD)

```
> the 1.0.0 release gate (S-5.07).
```

## Line 40 — before (HEAD~1)

```
`docs/guide/` for any remaining `TODO(S-` markers and blocks the 1.0.0
tag if any are found, so doc completeness rides with code completeness.
```

(The bare `S-5.7` was in the sentence that read: "The release gate at S-5.7 grep-checks")

## Line 40 — after (HEAD)

```
The release gate at S-5.07 grep-checks
`docs/guide/` for any remaining `TODO(S-` markers and blocks the 1.0.0
tag if any are found, so doc completeness rides with code completeness.
```

## Verification command

```
grep "S-5.7" docs/guide/v1.0-index.md
```

**Result:** (no output — zero matches)

```
grep "S-5.07" docs/guide/v1.0-index.md
```

**Result:**
```
> the 1.0.0 release gate (S-5.07).
The release gate at S-5.07 grep-checks
```

## Commentary

Both occurrences of the bare `S-5.7` legacy short-form canonicalized to
`S-5.07`. Same fix class as README `S-5.5 → S-5.05` in Task 16.
