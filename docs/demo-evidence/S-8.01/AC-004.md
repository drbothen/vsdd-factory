# AC-004 Evidence: Short Result Warning Path

**Story:** S-8.01 — Native port: handoff-validator (SubagentStop)
**AC:** AC-004 (traces to BC-7.03.044 postcondition 1)

## Claim

When trimmed non-whitespace result length is 1-39 characters: native WASM
plugin calls `host::emit_event` with
`type=hook.block hook=handoff-validator matcher=SubagentStop
reason=subagent_truncated_result severity=warn subagent=<agent_type>
result_len=<N>`, writes canonical stderr warning, exits 0.

## Evidence

### Implementation (src/lib.rs — Short branch)

```rust
ResultClassification::Short(len) => {
    // AC-004 / BC-7.03.044 postcondition 1.
    let len_str = len.to_string(); // bind temporary before slice
    emit(
        "hook.block",
        &[
            ("hook",       "handoff-validator"),
            ("matcher",    "SubagentStop"),
            ("reason",     "subagent_truncated_result"),
            ("severity",   "warn"),
            ("subagent",   agent),
            ("result_len", len_str.as_str()),
        ],
    );
    let msg = format!(
        "handoff-validator: subagent '{}' returned only {} non-whitespace characters.\n  Suspiciously short — verify the subagent completed its task.\n",
        agent, len
    );
    warn_stderr(&msg);
}
```

### Classification logic

```rust
pub fn classify_result(result: &str) -> ResultClassification {
    let trimmed_len: usize = result.chars().filter(|c| !c.is_whitespace()).count();
    match trimmed_len {
        0 => ResultClassification::Empty,
        n if n < 40 => ResultClassification::Short(n),
        _ => ResultClassification::Sufficient,
    }
}
```

Threshold is `n < 40` (not `<= 40`), matching bash `(( LEN < 40 ))` exactly.
`result_len` carries the non-whitespace character count, not the total string length.

### Unit test evidence (cargo test)

Test `test_BC_7_03_044_short_result_emits_truncated_event`:
- Input: `last_assistant_message = "hello"` (5 non-whitespace chars)
- Assert `emitted_type == "hook.block"`
- Assert field `reason == "subagent_truncated_result"`
- Assert field `result_len == "5"`
- Assert stderr contains `"non-whitespace characters"` and `"5"`
- Result: PASS

Test `test_BC_7_03_044_classify_short_5_chars`:
- Input: `"hello"` → `Short(5)`
- Result: PASS

Test `test_BC_7_03_044_classify_counts_nonwhitespace_only`:
- Input: `"a b c"` (5 chars total, 3 non-whitespace) → `Short(3)`
- Confirms only non-whitespace chars are counted, matching bash `tr -d '[:space:]'`
- Result: PASS

Test `test_BC_7_03_044_classify_exactly_39_chars_is_short`:
- Input: `"a" * 39` → `Short(39)`
- Result: PASS (39 < 40, warning emitted)

Test `test_BC_7_03_044_exactly_39_chars_emits_warning`:
- Input: 39-char message
- Assert `reason == "subagent_truncated_result"` emitted
- Result: PASS

Test `test_BC_7_03_044_classify_exactly_40_chars_is_sufficient`:
- Input: `"a" * 40` → `Sufficient`
- Result: PASS (40 is NOT < 40; no warning)

Test `test_BC_7_03_044_exactly_40_chars_no_warning`:
- Input: 40-char message
- Assert no event emitted
- Result: PASS

### Bats integration tests (cases b, d)

Bats test `AC-005(b)` — 5-char result:
```
input: {"last_assistant_message":"hello"}
status: 0
hook.block with reason=subagent_truncated_result: PASS
stderr contains "non-whitespace characters": PASS
```

Bats test `AC-005(d)` — LEN=39:
```
input: {"last_assistant_message":"aaa...a"} (39 'a' chars)
status: 0
hook.block with reason=subagent_truncated_result: PASS
stderr contains "non-whitespace characters": PASS
```

### Off-by-one boundary: LEN=40 (bats case e)

Bats test `AC-005(e)` — LEN=40 (at threshold, NO warning):
```
input: {"last_assistant_message":"aaa...a"} (40 'a' chars)
status: 0
no hook.block event: PASS
no "non-whitespace" in stderr: PASS
```

This confirms the `< 40` threshold (not `<= 40`) matches the bash
`(( LEN < 40 ))` behavior precisely.

## Pass / Fail

PASS — short result path (1-39 non-whitespace chars) emits correct
`hook.block` event with `reason=subagent_truncated_result` and `result_len=N`,
writes canonical stderr, exits 0. Off-by-one boundary (LEN=39 warns,
LEN=40 does not) matches bash parity.
