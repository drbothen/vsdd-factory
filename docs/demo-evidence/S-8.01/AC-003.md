# AC-003 Evidence: Empty Result Warning Path

**Story:** S-8.01 — Native port: handoff-validator (SubagentStop)
**AC:** AC-003 (traces to BC-7.03.043 postcondition 1; BC-2.02.012 postconditions 5-6 + EC-004)

## Claim

When the assistant message resolves to empty or whitespace-only: native WASM
plugin calls `host::emit_event` with
`type=hook.block hook=handoff-validator matcher=SubagentStop
reason=subagent_empty_result severity=warn subagent=<agent_type>`,
writes the canonical stderr warning, exits 0.

Agent identity resolved via BC-2.02.012 Postcondition 5 canonical chain:
`payload.agent_type.as_deref().or(payload.subagent_name.as_deref()).unwrap_or("unknown")`.

Assistant message resolved via BC-2.02.012 Postcondition 6 canonical 2-stage chain:
`payload.last_assistant_message.as_deref().or(payload.result.as_deref()).unwrap_or("")`.

EC-004 3rd-arm `.output` fallback intentionally dropped; divergence documented.

## Evidence

### Implementation (src/lib.rs — Empty branch)

```rust
ResultClassification::Empty => {
    // AC-003 / BC-7.03.043 postcondition 1.
    emit(
        "hook.block",
        &[
            ("hook",     "handoff-validator"),
            ("matcher",  "SubagentStop"),
            ("reason",   "subagent_empty_result"),
            ("severity", "warn"),
            ("subagent", agent),
        ],
    );
    let msg = format!(
        "handoff-validator: subagent '{}' returned an empty result.\n  This is a silent-failure risk — verify before continuing.\n",
        agent
    );
    warn_stderr(&msg);
}
```

### Agent identity fallback chain (BC-2.02.012 Postcondition 5)

```rust
// BC-2.02.012 Postcondition 5: canonical agent identity fallback chain.
let agent: &str = payload
    .agent_type
    .as_deref()
    .or(payload.subagent_name.as_deref())
    .unwrap_or("unknown");
```

### Assistant message 2-stage chain (BC-2.02.012 Postcondition 6 + EC-004)

```rust
// BC-2.02.012 Postcondition 6: canonical 2-stage assistant-message chain.
// Handoff-validator divergence per BC-2.02.012 EC-004: the bash 3-stage
// chain `.last_assistant_message // .result // .output // empty` becomes
// this 2-stage chain. The `.output` 3rd-arm is intentionally dropped
// (BC-2.02.012 does not model `output` in HookPayload; behavior parity
// is preserved for all observed envelope shapes).
let result: &str = payload
    .last_assistant_message
    .as_deref()
    .or(payload.result.as_deref())
    .unwrap_or("");
```

EC-004 divergence: the bash 3-stage chain included `.output` as a third
fallback arm (`handoff-validator.sh:28`). The WASM port drops this arm. Parity
is preserved because Claude Code emits `last_assistant_message` canonically;
`result` and `output` are bash-era defensive fallbacks. When all three bash
arms are absent, both the bash chain and the 2-stage Rust chain produce `""`.

### Unit test evidence (cargo test)

Test `test_BC_7_03_043_empty_result_emits_event_and_stderr`:
- Input: `{"agent_type":"test-agent","last_assistant_message":""}`
- Assert `emitted_type == "hook.block"`
- Assert field `reason == "subagent_empty_result"`
- Assert field `severity == "warn"`
- Assert field `subagent == "test-agent"`
- Assert `stderr_msg` contains `"empty result"`
- Result: PASS

Test `test_BC_7_03_043_whitespace_only_result_is_empty`:
- Input: `last_assistant_message` = `"   \t\n   "`
- Assert `reason == "subagent_empty_result"`
- Result: PASS

Test `test_BC_7_03_043_agent_identity_fallback_to_subagent_name`:
- Input: no `agent_type`, `subagent_name = "fallback-agent"`, empty message
- Assert `subagent == "fallback-agent"` in event
- Result: PASS

Test `test_BC_7_03_043_agent_identity_defaults_to_unknown`:
- Input: neither `agent_type` nor `subagent_name`
- Assert `subagent == "unknown"` in event
- Result: PASS

Test `test_BC_7_03_043_message_fallback_to_result_field`:
- Input: no `last_assistant_message`, `result = ""`
- Assert `reason == "subagent_empty_result"` (fallback chain resolved to empty)
- Result: PASS

### Bats integration test (case a, case f)

Bats test `AC-005(a)`:
```
input: {"event_name":"SubagentStop","session_id":"s","dispatcher_trace_id":"t",
        "agent_type":"test-agent","last_assistant_message":""}
status: 0
hook.block event in dispatcher log with reason=subagent_empty_result: PASS
stderr contains "empty result": PASS
```

Bats test `AC-005(f)`:
```
input: {"event_name":"SubagentStop","session_id":"s","dispatcher_trace_id":"t",
        "agent_type":"test-agent"}  [no last_assistant_message]
status: 0
hook.block event with reason=subagent_empty_result: PASS
stderr contains "empty result": PASS
```

Both cases pass. 7/7 bats tests pass overall.

## Pass / Fail

PASS — empty result path emits correct `hook.block` event, writes canonical
stderr, exits 0. Agent identity and message fallback chains match BC-2.02.012
Postconditions 5 and 6. EC-004 `.output` arm drop documented in source.
