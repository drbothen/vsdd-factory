---
document_type: verification-property
level: L4
vp_id: VP-096
version: "1.0"
status: draft
producer: architect
timestamp: 2026-07-06T00:00:00Z
phase: F5
last_amended: "2026-07-06 (v1.0) — E-19 VP package (architect): Initial creation. extract_frontmatter purity and determinism proptest per BC-4.13.001 v1.5 Invariant 9 (ADR-025 Decision 14 frontmatter-only parse mandate)."
title: "extract_frontmatter Purity — Output Byte-Equals File Prefix Through Second --- Delimiter; Deterministic for Any Input"
type: invariant
proof_method: proptest
scope: SS-04
domain_invariants: []
source_bc: "BC-4.13.001 v1.5 Invariant 9 (frontmatter-only parse: factory_lock_parse functions abort scanning after second --- fence; no content past closing frontmatter delimiter is read or evaluated)"
anchor_story: "S-19.02"
traces_to: VP-INDEX.md
bcs:
  - BC-4.13.001
inputs:
  - .factory/specs/behavioral-contracts/ss-04/BC-4.13.001.md
input-hash: "4875d5c"
module: crates/factory-lock-parse/src/lib.rs
feasibility: feasible-pending-harness
verification_lock: false
proof_completed_date: null
proof_file_hash: null
lifecycle_status: active
introduced: "v1.0-feature-engine-discipline-pass-1"
modified: []
deprecated: null
deprecated_by: null
replacement: null
retired: null
withdrawn: null
withdrawal_reason: null
removed: null
removal_reason: null
---

# VP-096: extract_frontmatter Purity — Output Byte-Equals File Prefix Through Second --- Delimiter; Deterministic for Any Input

## Property Statement

`factory_lock_parse::extract_frontmatter(input: &[u8]) -> &[u8]` MUST be a pure deterministic
function satisfying four invariants:

1. **Byte-exact prefix:** for any input that contains a second `---\n` delimiter, the returned
   slice equals the bytes of the input from byte 0 through (and including) that second delimiter
   line — `&input[..second_fence_end]`.
2. **No content beyond fence:** no byte past the second `---\n` line is included in the output.
3. **Determinism:** calling `extract_frontmatter` twice on the same input MUST return
   byte-identical slices.
4. **No-fence fallback:** if fewer than two `---` delimiters are present, returns the entire
   input unchanged (fail-open: callers see the full content and may fail to parse, but no panic
   occurs).

These four properties close the assertion that `parse_factory_lock` and
`extract_yaml_string_value` never read past the frontmatter block, eliminating body-size scaling
(ADR-025 Decision 14; BC-4.13.001 v1.5 Invariant 9).

## Source Contract

- **BC:** BC-4.13.001 v1.5 Invariant 9 — `factory_lock_parse` functions MUST abort scanning
  after second `---` fence; no content past closing frontmatter delimiter is read or evaluated.
- **ADR:** ADR-025 Decision 14 — frontmatter-only parse mandate; body-size scaling elimination
  rationale.

## Proof Method

| Method | Tool | Bounded? | Coverage |
|--------|------|----------|----------|
| proptest | Rust proptest crate | yes — 1024 trials per run, arbitrary byte input | byte-exact prefix; no-fence fallback; determinism; boundary: delimiter at position 0, 1, last byte |

### Why proptest

`extract_frontmatter` is a pure function over `&[u8]` input. All four properties are checkable
as pure postconditions over arbitrary byte inputs, making proptest the natural choice. No
filesystem I/O; no WASM runtime. Kani is a viable upgrade candidate once the function is stable.

## Proof Harness Skeleton

```rust
// File: crates/factory-lock-parse/tests/proptest_extract_frontmatter.rs
use factory_lock_parse::extract_frontmatter;
use proptest::prelude::*;

/// Find end position of second `---\n` delimiter in input (exclusive of bytes after).
fn find_second_fence_end(input: &[u8]) -> Option<usize> {
    let fence = b"---\n";
    let first = input.windows(fence.len()).position(|w| w == fence)?;
    let rest_start = first + fence.len();
    input[rest_start..].windows(fence.len())
        .position(|w| w == fence)
        .map(|pos| rest_start + pos + fence.len())
}

proptest! {
    #![proptest_config(ProptestConfig::with_cases(1024))]

    #[test]
    fn prop_byte_exact_prefix(input in proptest::collection::vec(any::<u8>(), 0..4096)) {
        let result = extract_frontmatter(&input);
        match find_second_fence_end(&input) {
            Some(end) => {
                // Invariant 1: output equals prefix through second fence
                prop_assert_eq!(result, &input[..end],
                    "extract_frontmatter output must equal input[..second_fence_end]");
                // Invariant 2: no byte past fence
                prop_assert!(result.len() <= end);
            }
            None => {
                // Invariant 4: no-fence fallback returns entire input
                prop_assert_eq!(result, input.as_slice(),
                    "extract_frontmatter must return full input when fewer than 2 fences");
            }
        }
    }

    #[test]
    fn prop_deterministic(input in proptest::collection::vec(any::<u8>(), 0..4096)) {
        // Invariant 3: determinism
        let r1 = extract_frontmatter(&input);
        let r2 = extract_frontmatter(&input);
        prop_assert_eq!(r1, r2, "extract_frontmatter must be deterministic");
    }

    #[test]
    fn prop_no_panic_on_arbitrary_bytes(input in proptest::collection::vec(any::<u8>(), 0..65536)) {
        // Safety: no panic on any input up to 64 KiB
        let _ = extract_frontmatter(&input);
    }
}
```

## Feasibility Assessment

| Factor | Assessment | Notes |
|--------|-----------|-------|
| Artifact | `crates/factory-lock-parse/src/lib.rs` pure function | No I/O; pure `&[u8]→&[u8]` function |
| Proof complexity | Low — pure function, proptest pattern | 1024 trials; fast (<1s per run) |
| Tool support | High — proptest is a workspace dependency | No new tooling |
| Estimated proof time | 1 hour | Pattern from VP-059 ID-monotonicity proptest |

## Lifecycle

| Event | Date | Actor |
|-------|------|-------|
| Created | 2026-07-06 | architect (E-19 VP package) |
| Harness instantiated | — | test-writer |
| First proof passed | — | test-writer |
| Locked (VERIFIED) | — | formal-verifier |

## Traceability

- **BC:** BC-4.13.001 v1.5 Invariant 9 — frontmatter-only parse abort-after-second-fence
- **ADR:** ADR-025 Decision 14 — frontmatter-only parse mandate (body-size scaling elimination)
- **Function anchors:** `factory_lock_parse::extract_frontmatter`, `factory_lock_parse::parse_factory_lock`,
  `factory_lock_parse::extract_yaml_string_value` (per TD-VSDD-091 stable symbol anchors)
- **Subsystem:** SS-04 (Plugin Ecosystem — factory-lock-parse pure library crate)
- **Anchor story:** S-19.02
- **Risk closed:** Body-size scaling in frontmatter parsers; STATE.md body content never read by
  guard logic
