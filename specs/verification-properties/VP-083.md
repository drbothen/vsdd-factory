---
document_type: verification-property
level: L4
vp_id: VP-083
version: "1.9"
status: draft
producer: architect
timestamp: 2026-06-15T00:00:00Z
last_amended: "2026-06-15 (v1.9) — fix burst (architect): (F-P32-002 MEDIUM tautology-fix) Discriminating fixture added to the proof harness: `wave_1_epic_complete_blocks_malformed_base_field`. This fixture proves EPIC-COMPLETE evaluation order (step 2 per ADR-026 §Decision 9 Inv3) fires BEFORE the wave-1 no-op (step 3) by using wave_id==1 + next_wave_stories:[] (EPIC-COMPLETE) + a malformed base field (last_verified_develop_sha: '' empty string, treated as malformed per BC-4.14.001 EC-004/PC7) → MUST block with HandoffIncomplete on the base-field failure. If the gate had short-circuited on the wave-1 no-op first it would return Continue — so a block result is a direct witness that the EPIC-COMPLETE branch was reached and full validation ran. The prior two EPIC-COMPLETE fixtures (wave_1_epic_complete_validates_epic_status_present and wave_1_epic_complete_blocks_missing_epic_status) are not tautology-proofs: they test epic_status presence/absence on a fully-valid or epic_status-only-absent payload, which could pass even if the gate coincidentally checked epic_status without actually reaching the EPIC-COMPLETE branch via the canonical step-2 path. The new discriminating fixture eliminates that ambiguity. Aligns VP-083 with BC-4.14.001 PC7 (empty scalar = malformed, per F-P32-001 parallel product-owner fix). Refs: F-P32-002, ADR-026 §Decision 9 Inv3, BC-4.14.001 PC7/EC-004. [Prior: 2026-06-15 (v1.8) — fix burst (architect): (F-P25-002 MEDIUM carve-out) Property §1 wave-1 no-op refined: the no-op at wave_id==1 is now CONDITIONAL on NOT being in EPIC-COMPLETE context (i.e., NOT `next_wave_stories: []`). ADR-026 §Decision 9 canonical evaluation order (F-P25-002) places EPIC-COMPLETE detection (step 2) BEFORE the wave_id==1 no-op (step 3). When wave_id==1 AND `next_wave_stories: []` (single-wave EPIC-COMPLETE), the gate evaluates PC2a and validates `epic_status: complete` — it does NOT return Continue unconditionally. The prior Property §1 stated 'Wave-1 no-op unconditional' and formal postcondition 'Continue. No block. Wave-1 no-op unconditional.' — this is now incorrect for the EPIC-COMPLETE intersection. Both property body and formal postconditions updated to reflect the conditional nature of the wave-1 no-op. Proof harness: added `wave_1_epic_complete_validates_epic_status` test fixture; existing `wave_1_no_op_even_with_handoff_present` fixture annotated to clarify it applies to NON-EPIC-COMPLETE payloads only (next_wave_stories non-empty). Refs: F-P25-002, ADR-026 §Decision 9 (v1.16), BC-4.14.001 PC2a/PC3/Inv3. [Prior: 2026-06-15 (v1.7) — fix burst (architect): (F-P20-005 LOW) Property §2 over-narrow qualifier removed: the '(wave > 1)' parenthetical in §2 incorrectly scoped the non-HANDOFF.md no-op leg to wave_id > 1 only. BC-4.14.001 PC4/Inv3 specify the non-HANDOFF.md-target check is performed BEFORE any wave_id evaluation — a non-HANDOFF.md write no-ops at ANY wave including wave 1 and wave_id-absent. The VP-083 H1/title connective ('Wave-1 OR Non-HANDOFF.md') was already correct; only the body §2 '(wave > 1)' parenthetical was wrong. Fix: '(wave > 1)' qualifier removed from §2 body text; description changed from 'when the gate fires on a Write/Edit event whose target file is NOT HANDOFF.md (wave > 1)' to 'when the gate fires on a Write/Edit event whose target file is NOT HANDOFF.md'. Formal Postconditions and unit-test harness are unchanged (they already correctly describe the two independent sufficient conditions). Refs: F-P20-005, BC-4.14.001 PC4/Inv3, ADR-026 §Decision 9. [Prior: 2026-06-15 (v1.6) — fix burst (architect): (F-P19-003 LOW adjudicated YES) domain_invariant field populated: source BC BC-4.14.001 enforces DI-020; field changed from singular 'domain_invariant: —' to plural array form 'domain_invariants: [DI-020]' per POLICY 2 bidirectional traceability. [Prior: 2026-06-15 (v1.5) — F2 adversarial-pass-16 fix burst (architect; F-P16-003 MEDIUM): Title logical-connective corrected from 'and' to 'or' in both `title:` frontmatter and H1 heading — 'Completeness Gate Is No-Op on Wave-1 or Non-HANDOFF.md Writes'. The two no-op conditions in Property §1 and §2 are independent sufficient conditions: the gate no-ops if (wave_id == 1) OR if (target != HANDOFF.md). Either condition alone is sufficient; no conjunction of both is required. 'and' (as in 'both conditions must hold simultaneously') is logically incorrect and contradicts Property §1 which grants a no-op on wave-1 regardless of target file. VP-INDEX row already carries 'or'; this fix aligns the body to the index. No behavioral change to Property §1/§2 postconditions, which correctly describe the two independent sufficient conditions. [Prior: 2026-06-15 (v1.4) — F2 adversarial-pass-15 fix burst (architect): (F-P15-004 LOW) Title corrected from 'Completeness Gate Is No-Op on Wave-1 and HANDOFF.md Absent' to 'Completeness Gate Is No-Op on Wave-1 and Non-HANDOFF.md Writes' — the WASM gate fires on a Write/Edit payload and cannot detect HANDOFF.md file-absence; Property §2 no-op condition is a non-HANDOFF.md target write (different target file), not file-absence. H1 heading and title frontmatter aligned to corrected framing. Property §2 prose aligned to describe 'a Write/Edit event whose target file is NOT HANDOFF.md' (not 'HANDOFF.md absent'). Postconditions, feasibility assessment, and traceability are unchanged. (F-P15-003 LOW) No VP-INDEX version pin was found in VP-083 normative body — confirmed no action needed for this file on F-P15-003. [Prior: v1.3 — F2 adversarial-pass-11 fix burst (architect): (F-P11-001 MAJOR) Property §1 and all GateContext/fixture comments rewritten to PAYLOAD-ONLY discriminator per BC-4.14.001 v1.8 (F-P10-001): `is_first_wave = (payload.wave_id == 1)` computed FROM the parsed HANDOFF payload — NO prior-HANDOFF.md filesystem read, NO sprint-state.yaml topo-sort ordinal. Added missing fail-closed postcondition: `wave_id` ABSENT from payload → gate FAILS CLOSED (proceeds to full validation, blocking with HandoffIncomplete: ['wave_id']); unit-test fixture `wave_id_absent_fails_closed` added. VP-083 v1.2→v1.3. [Prior: v1.2 — F2 adversarial-pass-2 revision: (F-P2-002) Property text and all Rust test fixture `current_wave` struct fields replaced with canonical wave-identity substrate per ADR-026 v1.2 §Wave-Identity Derivation — `GateContext` uses `is_first_wave: bool` (derived from prior-HANDOFF.md absence or topo-sort ordinal=1), not `current_wave: u32`; inputs: path corrected to real ADR-026 slug (F-P2-003). [Prior: v1.1 — bcs[] resolved; source_bc resolved.]]]"
title: "Completeness Gate Is No-Op on Wave-1 or Non-HANDOFF.md Writes"
type: invariant
proof_method: unit-test
scope: SS-04
domain_invariants: [DI-020]
source_bc: "BC-4.14.001 (validate-wave-handoff-completeness WASM gate — wave-1 no-op leg and non-HANDOFF.md no-op)"
anchor_story: "S-18.02"
traces_to: VP-INDEX.md
bcs:
  - BC-4.14.001
inputs:
  - .factory/specs/architecture/decisions/ADR-026-wave-boundary-checkpoint-reset-and-lossless-intra-wave-compaction.md
---

# VP-083: Completeness Gate Is No-Op on Wave-1 or Non-HANDOFF.md Writes

## Property

The `validate-wave-handoff-completeness` WASM gate MUST return `Continue` (not block)
in two specific conditions that must not create false-positive blocking:

1. **Wave-1 no-op (PAYLOAD-ONLY) — CONDITIONAL on NOT EPIC-COMPLETE (F-P25-002):** When
   `payload.wave_id == 1` AND the payload is NOT in EPIC-COMPLETE context (i.e.,
   `next_wave_stories` is non-empty or absent) — the gate reads `wave_id` directly from the
   HANDOFF Write/Edit payload (`is_first_wave = (payload.wave_id == 1)`). No filesystem, git,
   sprint-state.yaml, or factory-artifacts read is performed to determine first-wave status.
   The shell wave-handoff skill (BC-5.41.001) is responsible for deriving `wave_id` from real
   substrate and embedding it in the payload before writing.

   **Single-wave EPIC-COMPLETE exception:** When `payload.wave_id == 1` AND
   `next_wave_stories: []` (EPIC-COMPLETE context) — the wave-1 no-op does NOT apply. The gate
   evaluates the EPIC-COMPLETE branch (PC2a) and validates `epic_status: complete` regardless
   of `wave_id`. This is because a single-wave epic whose only wave is wave 1 can legitimately
   reach terminal status without proceeding to wave 2. On such a write, skipping the EPIC-COMPLETE
   branch would leave `epic_status` never validated — the completeness gate's purpose (preventing
   an incomplete handoff from being committed) would be a no-op on the single-wave-final case.
   The EPIC-COMPLETE discriminator is PAYLOAD-ONLY (`next_wave_stories: []` from the current
   payload), so evaluating it at wave_id==1 is sound and preserves pure-parse purity.
   Per ADR-026 §Decision 9, the canonical evaluation order is: (1) non-HANDOFF.md → no-op;
   (2) EPIC-COMPLETE detection → validate epic_status if true, even at wave_id==1; (3) wave_id==1
   AND NOT EPIC-COMPLETE → no-op; (4) wave_id>1 → full validation.

2. **Non-HANDOFF.md target write (wave-agnostic):** When the gate fires on a Write/Edit event
   whose target file is NOT `HANDOFF.md` (e.g., other `factory-artifacts` file writes such
   as STATE.md, wave-state.yaml, etc.). The gate performs no field validation and returns
   `Continue` unconditionally for non-HANDOFF.md targets at ANY wave — including wave 1 and
   wave_id-absent — because the non-HANDOFF.md check precedes any wave_id evaluation
   (BC-4.14.001 PC4/Inv3). The WASM gate is a pure-parse gate operating on the Write/Edit
   payload; it cannot perform a filesystem probe to determine HANDOFF.md file-absence.

The gate blocks ONLY when:
- The target file being written IS `HANDOFF.md`, AND one or more required fields are
  missing or malformed; OR
- `wave_id` is ABSENT from the payload → gate FAILS CLOSED: proceeds to full validation,
  blocking with `HandoffIncomplete: ["wave_id"]` (absent `wave_id` is NOT treated as wave-1); OR
- EPIC-COMPLETE context is detected (`next_wave_stories: []`) AND `epic_status` is absent or
  invalid — this applies even when `wave_id == 1` (single-wave EPIC-COMPLETE case).

Formally:
- **Postcondition (payload.wave_id == 1, NOT EPIC-COMPLETE):** `Continue`. No block. Wave-1 no-op (applies when `next_wave_stories` is non-empty or the payload does not assert EPIC-COMPLETE).
- **Postcondition (payload.wave_id == 1, EPIC-COMPLETE — `next_wave_stories: []`):** Gate evaluates PC2a. If `epic_status: complete` present and valid → `Continue`. If `epic_status` absent → `MissingEpicStatus` block. If `epic_status` present but not `complete` → `HandoffIncomplete: epic_status malformed` block.
- **Postcondition (payload.wave_id > 1, non-HANDOFF.md write):** `Continue`. No block.
- **Postcondition (payload.wave_id > 1, HANDOFF.md write, all fields present):** `Continue`.
- **Postcondition (payload.wave_id > 1, HANDOFF.md write, field missing):** `HandoffIncomplete(<field_name>)` block.
- **Postcondition (wave_id ABSENT from payload):** Gate FAILS CLOSED — proceeds to full validation; blocks with `HandoffIncomplete: ["wave_id"]` (and any other missing required fields). Does NOT return `Continue`.

## Invariant Classification: Invariant (No False-Positive Block)

A false-positive block on wave-1 or on normal file writes would block the factory
from operating at all. This invariant ensures the gate is safe to deploy without
disrupting existing pipelines.

## Proof Harness Skeleton (Unit Test)

```rust
// File: crates/hook-plugins/validate-wave-handoff-completeness/tests/unit_tests.rs
// Anchor: no_false_positive_on_wave_1

use validate_wave_handoff_completeness::{check_handoff_completeness, GateContext, GateResult};

// NOTE: GateContext uses `is_first_wave: bool` computed PAYLOAD-ONLY by the gate itself:
// `is_first_wave = (payload.wave_id == 1)`. The gate reads `wave_id` from the HANDOFF
// Write/Edit payload — no prior-HANDOFF.md filesystem read, no sprint-state.yaml read,
// no git access. The shell wave-handoff skill (BC-5.41.001) derives `wave_id` from real
// substrate and embeds it in the payload before writing. The WASM gate is a CONSUMER of
// `wave_id`, not a DERIVER. Absent `wave_id` is NOT treated as wave-1: it is a fail-closed
// condition (see `wave_id_absent_fails_closed` test below).

#[test]
fn wave_1_no_op_no_handoff() {
    let ctx = GateContext {
        // is_first_wave=true because payload.wave_id == 1 (caller embedded wave_id=1)
        is_first_wave: true,
        file_path: "factory-artifacts/SOME_OTHER_FILE.md".to_string(),
        handoff_content: None,
        close_wave_mode: false,
    };
    assert_eq!(check_handoff_completeness(&ctx), GateResult::Continue);
}

#[test]
fn wave_1_no_op_even_with_handoff_present() {
    // NOTE (F-P25-002): this no-op applies ONLY when the payload is NOT in EPIC-COMPLETE context
    // (i.e., next_wave_stories is non-empty or not present as an empty list). When wave_id==1
    // AND next_wave_stories:[] (EPIC-COMPLETE), the gate evaluates PC2a — see
    // `wave_1_epic_complete_validates_epic_status` below.
    let ctx = GateContext {
        // is_first_wave=true because payload.wave_id == 1 (caller embedded wave_id=1)
        // next_wave_stories is non-empty (not EPIC-COMPLETE) — wave-1 no-op applies
        is_first_wave: true,
        file_path: "factory-artifacts/HANDOFF.md".to_string(),
        handoff_content: Some("wave_id: 1\nnext_wave_stories:\n  - {id: S-02.01, status: pending}\n".to_string()),
        close_wave_mode: false,
    };
    assert_eq!(check_handoff_completeness(&ctx), GateResult::Continue);
}

#[test]
fn wave_1_epic_complete_validates_epic_status_present() {
    // F-P25-002: single-wave EPIC-COMPLETE — wave_id==1 AND next_wave_stories:[] (empty list).
    // The wave-1 no-op does NOT apply. The gate evaluates PC2a (EPIC-COMPLETE branch) and
    // validates epic_status: complete. With epic_status: complete present → Continue.
    let ctx = GateContext {
        is_first_wave: true,
        file_path: "factory-artifacts/HANDOFF.md".to_string(),
        // payload has wave_id=1 + next_wave_stories:[] (EPIC-COMPLETE) + epic_status:complete
        handoff_content: Some(valid_handoff_fixture_epic_complete_wave_1()),
        close_wave_mode: false,
    };
    assert_eq!(check_handoff_completeness(&ctx), GateResult::Continue);
}

#[test]
fn wave_1_epic_complete_blocks_missing_epic_status() {
    // F-P25-002: single-wave EPIC-COMPLETE with epic_status ABSENT → MissingEpicStatus block.
    // The wave-1 no-op does NOT apply when next_wave_stories:[] — PC2a demands epic_status.
    let ctx = GateContext {
        is_first_wave: true,
        file_path: "factory-artifacts/HANDOFF.md".to_string(),
        // payload has wave_id=1 + next_wave_stories:[] but epic_status is absent
        handoff_content: Some(valid_handoff_fixture_wave_1_epic_complete_no_epic_status()),
        close_wave_mode: false,
    };
    assert!(matches!(
        check_handoff_completeness(&ctx),
        GateResult::Block { code: "MissingEpicStatus", .. }
    ));
}

#[test]
fn wave_1_epic_complete_blocks_malformed_base_field() {
    // F-P32-002: DISCRIMINATING fixture proving EPIC-COMPLETE branch (step 2 per ADR-026 §Decision 9 Inv3)
    // fires BEFORE the wave-1 no-op (step 3).
    //
    // Setup: wave_id==1 + next_wave_stories:[] (EPIC-COMPLETE) + last_verified_develop_sha is an
    // empty string (treated as a malformed/missing base field per BC-4.14.001 PC7 / EC-004).
    //
    // Expected: gate BLOCKS with HandoffIncomplete on the empty last_verified_develop_sha.
    //
    // Why this is discriminating: if the gate had short-circuited on the wave-1 no-op (step 3)
    // before reaching full validation, it would return Continue (no block). A Block result is
    // therefore a direct witness that:
    //   (a) the gate DID NOT short-circuit on wave-1 no-op, AND
    //   (b) the EPIC-COMPLETE branch (step 2) was entered and full base-field validation ran.
    //
    // The prior two EPIC-COMPLETE fixtures test epic_status presence/absence on otherwise-valid
    // payloads; they do not distinguish "gate reached EPIC-COMPLETE branch" from "gate happened
    // to check epic_status by coincidence". This fixture eliminates that ambiguity.
    //
    // Also corroborates BC-4.14.001 PC7/EC-004 (F-P32-001): empty scalar = malformed base field.
    let ctx = GateContext {
        is_first_wave: true,
        file_path: "factory-artifacts/HANDOFF.md".to_string(),
        // wave_id:1 + next_wave_stories:[] (EPIC-COMPLETE) + last_verified_develop_sha is empty
        // (malformed base field per BC-4.14.001 EC-004) + epic_status: complete (present and valid)
        handoff_content: Some(
            "wave_id: 1\nnext_wave_stories: []\nepic_status: complete\nlast_verified_develop_sha: \"\"\n".to_string()
        ),
        close_wave_mode: false,
    };
    let result = check_handoff_completeness(&ctx);
    assert!(matches!(
        result,
        GateResult::Block { code: "HandoffIncomplete", .. }
    ), "Expected HandoffIncomplete block: gate must enter EPIC-COMPLETE branch (step 2) and \
        run full base-field validation; empty last_verified_develop_sha is malformed (EC-004). \
        If gate returned Continue, it short-circuited on wave-1 no-op (step 3) — evaluation \
        order violation per ADR-026 §Decision 9 Inv3.");
    // Additionally verify the block message names last_verified_develop_sha as the failing field
    if let GateResult::Block { message, .. } = result {
        assert!(message.contains("last_verified_develop_sha"),
            "HandoffIncomplete block must name 'last_verified_develop_sha' as the malformed field");
    }
}

#[test]
fn wave_gt_1_non_handoff_write_no_block() {
    let ctx = GateContext {
        // is_first_wave=false because payload.wave_id > 1 (caller embedded wave_id=2)
        is_first_wave: false,
        file_path: "factory-artifacts/STATE.md".to_string(),
        handoff_content: None,
        close_wave_mode: false,
    };
    assert_eq!(check_handoff_completeness(&ctx), GateResult::Continue);
}

#[test]
fn wave_gt_1_handoff_write_missing_sha_blocks() {
    let ctx = GateContext {
        // is_first_wave=false because payload.wave_id == 3 (caller embedded wave_id=3)
        is_first_wave: false,
        file_path: "factory-artifacts/HANDOFF.md".to_string(),
        handoff_content: Some("wave_id: 3\n# missing last_verified_develop_sha\n".to_string()),
        close_wave_mode: false,
    };
    assert!(matches!(
        check_handoff_completeness(&ctx),
        GateResult::Block { code: "HandoffIncomplete", .. }
    ));
}

#[test]
fn wave_gt_1_handoff_write_all_fields_present_continues() {
    let ctx = GateContext {
        // is_first_wave=false because payload.wave_id == 3 (caller embedded wave_id=3)
        is_first_wave: false,
        file_path: "factory-artifacts/HANDOFF.md".to_string(),
        handoff_content: Some(valid_handoff_fixture()),
        close_wave_mode: false,
    };
    assert_eq!(check_handoff_completeness(&ctx), GateResult::Continue);
}

#[test]
fn wave_id_absent_fails_closed() {
    // EC-010: when wave_id is ABSENT from the payload, the gate does NOT return Continue.
    // It FAILS CLOSED: proceeds to full validation and blocks with HandoffIncomplete: ["wave_id"].
    // Absent wave_id is NOT treated as wave-1. The gate's is_first_wave check requires wave_id
    // to be present and equal to 1; absence is a missing-required-field condition.
    let ctx = GateContext {
        // wave_id absent from payload — gate receives is_first_wave=false (absence is not wave-1)
        is_first_wave: false,
        file_path: "factory-artifacts/HANDOFF.md".to_string(),
        // payload omits wave_id field entirely
        handoff_content: Some("last_verified_develop_sha: abc123\n# wave_id is absent\n".to_string()),
        close_wave_mode: false,
    };
    let result = check_handoff_completeness(&ctx);
    assert!(matches!(
        result,
        GateResult::Block { code: "HandoffIncomplete", .. }
    ), "Expected HandoffIncomplete block when wave_id is absent; gate must fail closed");
    // Additionally verify the block message names wave_id as a missing field
    if let GateResult::Block { message, .. } = result {
        assert!(message.contains("wave_id"),
            "HandoffIncomplete block message must name 'wave_id' when it is absent from payload");
    }
}
```

## Feasibility Assessment

**Feasible as Rust unit test.** The gate logic is a pure function over a `GateContext`
struct containing the file path, `is_first_wave` flag (computed PAYLOAD-ONLY as
`payload.wave_id == 1`), HANDOFF.md content, and mode flag. No filesystem or git access
at the unit level — the `wave_id` value is read from the HANDOFF payload, and
`is_first_wave` is a derived boolean the gate computes before any field validation.
The fail-closed path (absent `wave_id` → HandoffIncomplete) is equally testable as
a unit test: supply a payload with wave_id absent and assert the Block result.
The integration-level git-SHA cross-check is tested separately in VP-081 integration harness.

WASM fuel consumption is bounded: the gate parses only HANDOFF.md frontmatter fields
(not the body) and does not perform I/O beyond `host::read_file` on the target path.

## Traceability

- **Feature:** issue #173 (E-18 context-durability)
- **ADR:** ADR-026 §Decision 9 (wave-1 no-op rule)
- **Anchor story:** S-18.02 (validate-wave-handoff-completeness WASM crate)
- **Subsystems:** SS-04 (plugin ecosystem — WASM crate)
- **Risk closed:** F1 regression risk §4.1 R3 (false-positive on wave-1 / short-run)
