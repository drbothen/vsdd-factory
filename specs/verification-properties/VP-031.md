---
document_type: verification-property
level: L4
version: "1.0"
status: draft
producer: architect
timestamp: 2026-04-25T00:00:00
phase: 1.6b
inputs:
  - .factory/specs/domain-spec/invariants.md
  - .factory/specs/behavioral-contracts/BC-INDEX.md
input-hash: "424d55f"
traces_to: ARCH-INDEX.md
source_bc: BC-3.02.010
module: crates/sink-file/src/lib.rs
proof_method: unit-test
feasibility: feasible
verification_lock: false
proof_completed_date: null
proof_file_hash: null
lifecycle_status: active
introduced: 1.6b
modified: []
deprecated: null
deprecated_by: null
replacement: null
retired: null
withdrawn: null
withdrawal_reason: null
removed: null
removal_reason: null
vp_id: VP-031
type: invariant
scope: SS-03
bcs: [BC-3.02.010, BC-3.02.011]
domain_invariants: [DI-012]
nfrs: [NFR-OBS-009]
test_evidence: crates/sink-file/src/lib.rs::tests::tag_enrichment_writes_tags_onto_every_event
---

# VP-031: Tag Enrichment Does Not Overwrite Producer Fields

## Property Statement

Static tags configured for a sink (e.g., env=prod, team=factory) are merged onto every written event. A tag whose key conflicts with a reserved producer field (type, ts, ts_epoch, etc.) does NOT overwrite the producer field. Non-conflicting tags are always added.

## Source Contract

- **BC:** BC-3.02.010 — tag_enrichment_writes_tags_onto_every_event
- **Postcondition/Invariant:** BC-3.02.011 — tags do not overwrite producer fields

## Proof Method

| Method | Tool | Bounded? | Coverage |
|--------|------|----------|----------|
| unit-test | Rust unit tests in sink-file/lib.rs | yes — tag=type conflict + normal tags | enrichment + conflict avoidance |

## Proof Harness Skeleton

```rust
#[test]
fn verify_vp_031_tags_enriched_no_overwrite() {
    let config = SinkConfig::with_tags([("env", "prod"), ("type", "injected")]);
    let event = make_event_with_type("plugin.invoked");

    let enriched = enrich_with_tags(event, &config.tags);

    // Normal tags added
    assert_eq!(enriched.get("env").and_then(|v| v.as_str()), Some("prod"));

    // Producer field NOT overwritten
    assert_eq!(enriched.get("type").and_then(|v| v.as_str()), Some("plugin.invoked"));
}
```

## Feasibility Assessment

| Factor | Assessment | Notes |
|--------|-----------|-------|
| Input space size | Fixed tag set + event | Representative |
| Proof complexity | Low | Map merge with key priority |
| Tool support | High | Unit tests exist |
| Estimated proof time | Already passing | BC-3.02.010 + BC-3.02.011 unit tests |

## Lifecycle

| Event | Date | Actor |
|-------|------|-------|
| Created | 2026-04-25 | architect |
| Proof harness committed | — | formal-verifier |
| Proof first passed | — | formal-verifier |
| Locked (VERIFIED) | — | formal-verifier |

## Traceability

- BCs: BC-3.02.010 (tags written), BC-3.02.011 (no overwrite)
- Domain Invariant: DI-012
- Architecture: SS-03-observability-sinks.md
- Stories: TBD

## Test Evidence (brownfield only)

`crates/sink-file/src/lib.rs::tests::tag_enrichment_writes_tags_onto_every_event` — env=prod and team=factory tags on every event line.

`crates/sink-file/src/lib.rs::tests::tag_enrichment_does_not_overwrite_producer_fields` — tag with key=type does NOT clobber the event's type field.

## Notes

The merge semantics (producer fields win over tags) must be consistent across all sink drivers. Currently only file sink implements tag enrichment; otel-grpc uses resource_attributes instead. When additional sinks are added, this VP should be extended to cover them.
