---
document_type: verification-property
level: L4
version: "1.0"
status: draft
producer: architect
timestamp: 2026-04-25T00:00:00
phase: 1.6b
inputs:
  - .factory/specs/domain-spec/invariants.md
  - .factory/specs/behavioral-contracts/BC-INDEX.md
input-hash: "424d55f"
traces_to: ARCH-INDEX.md
source_bc: BC-1.03.013
module: crates/factory-dispatcher/src/executor.rs
proof_method: integration
feasibility: feasible
verification_lock: false
proof_completed_date: null
proof_file_hash: null
lifecycle_status: active
introduced: 1.6b
modified: []
deprecated: null
deprecated_by: null
replacement: null
retired: null
withdrawn: null
withdrawal_reason: null
removed: null
removal_reason: null
vp_id: VP-002
type: invariant
scope: SS-01
bcs: [BC-1.03.002, BC-1.03.013, BC-1.03.014]
domain_invariants: [DI-002]
nfrs: [NFR-REL-003]
test_evidence: crates/factory-dispatcher/tests/executor_integration.rs::crash_does_not_affect_siblings
---

# VP-002: Plugin Crash or Timeout Does Not Block Sibling Plugins

## Property Statement

When one plugin in a tier produces a Crashed or Timeout result, all other plugins in the same tier continue execution to completion. The final exit_code is determined by the on_error policy, not by the crashing plugin's existence.

## Source Contract

- **BC:** BC-1.03.013 — crash_does_not_affect_siblings
- **Postcondition/Invariant:** DI-002 — A plugin crash or timeout does not block sibling plugins

## Proof Method

| Method | Tool | Bounded? | Coverage |
|--------|------|----------|----------|
| integration | wasmtime executor, real WASM plugins | no | crash isolation + timeout non-cascade across sibling plugins |

## Proof Harness Skeleton

```rust
// Integration test: crates/factory-dispatcher/tests/executor_integration.rs
#[tokio::test(flavor = "current_thread")]
async fn verify_vp_002_crash_isolation() {
    // Three plugins: ok-a, crash (unreachable), ok-b — all same priority
    let summary = execute_tiers(inputs, tiers).await.unwrap();

    // Sibling results must be Ok
    let ok_results: Vec<_> = summary.per_plugin_results.iter()
        .filter(|r| r.plugin_name != "crash")
        .collect();
    assert!(ok_results.iter().all(|r| matches!(r.result, PluginResult::Ok { .. })));

    // Final exit_code not blocked by crash (on_error=continue)
    assert_eq!(summary.exit_code, 0);
}
```

## Feasibility Assessment

| Factor | Assessment | Notes |
|--------|-----------|-------|
| Input space size | Small — 3-plugin scenario | Representative of the invariant |
| Proof complexity | Low | Existing integration test already passes |
| Tool support | High — tokio + wasmtime | Real isolation via spawn_blocking |
| Estimated proof time | Already passing | Brownfield: test is the proof |

## Lifecycle

| Event | Date | Actor |
|-------|------|-------|
| Created | 2026-04-25 | architect |
| Proof harness committed | — | formal-verifier |
| Proof first passed | — | formal-verifier |
| Locked (VERIFIED) | — | formal-verifier |

## Traceability

- BCs: BC-1.03.002 (fuel exhaustion test), BC-1.03.013 (crash isolation), BC-1.03.014 (timeout non-cascade)
- Domain Invariant: DI-002
- Architecture: SS-01-hook-dispatcher.md
- Stories: TBD

## Test Evidence (brownfield only)

`crates/factory-dispatcher/tests/executor_integration.rs::crash_does_not_affect_siblings` — one Crashed plugin, siblings are Ok, exit_code=0.

`crates/factory-dispatcher/tests/executor_integration.rs::parallel_timeout_does_not_cascade` — hanger times out at 120ms, siblings complete, wall < 2s.

## Notes

Isolation is achieved by running each plugin in its own wasmtime Store via spawn_blocking. JoinError (task panic) is also caught and converted to PluginResult::Crashed per NFR-REL-014. No additional proptest coverage needed; the integration tests exercise the domain invariant directly.
