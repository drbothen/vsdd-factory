---
document_type: behavioral-contract
level: L3
version: "1.21"
status: draft
producer: product-owner
timestamp: 2026-06-14T00:00:00Z
last_amended: "2026-06-18 (v1.21) — clarity amendment (product-owner): PC2 `active_bcs` field definition expanded to state explicitly that semantics are existence-only (NOT lifecycle-filtered); `find -name '*.md'` is the correct implementation; 'active' in the field name means BCs present in the spec corpus at handoff time, not BCs with lifecycle_status:active. Disposition for O-P10-002 (pass-10 adversary observation). BC-5.41.001 v1.20→v1.21. [Prior: 2026-06-18 (v1.20) — explicit named preconditions P-WAVE-BARRIER-INVARIANT + P-SPRINT-STATE-WAVE-ORDER added to PC2 wave_id derivation block (O-P8-001 research disposition, product-owner). Leading-contiguous-terminal-run derivation model documented as intended algorithm under these preconditions; Kahn-DAG-level derivation documented as a design boundary (not deferred work, not a TD entry). BC-5.41.001 v1.19→v1.20. [Prior: 2026-06-18 (v1.19) — new edge case (product-owner): EC-015 added — idempotent EPIC-COMPLETE re-invocation with byte-identical HANDOFF.md content (F-P7-002 MEDIUM). Skill MUST detect empty staged-diff via `git -C <ARTIFACTS_WT> diff --cached --quiet` after staging, skip the commit, STILL emit the canonical PC8 / AC-012 3-line EPIC-COMPLETE stdout announcement, and exit 0. Anchored to PC8. BC-5.41.001 v1.18→v1.19. [Prior: 2026-06-18 (v1.18) — BC conflict reconciliation (product-owner): (F-S1801-P3-003) PC8 EPIC-COMPLETE stdout message corrected — lines 2 and 3 were divergent from BC-5.41.002 PC7; canonical text is now verbatim-identical in both BCs. Line 2: 'No next-wave handoff required.' → 'No wave-state.yaml written for next wave.' Line 3: 'HANDOFF.md written with epic_status: complete. wave-state.yaml NOT written.' → 'HANDOFF.md committed to factory-artifacts with epic_status: complete.' The BC-5.41.002 PC7 formulation was adopted as canonical: it is more precise (names the exact artifact in line 2; uses 'committed to factory-artifacts' in line 3 which aligns with PC6 language; eliminates the redundant repeat of wave-state.yaml NOT written). BC-5.41.001 v1.17→v1.18. [Prior: 2026-06-15 (v1.17) — fix burst (product-owner): (F-P32-003 LOW) §Changelog: defensive skip-marker annotation added for v1.5 absence — v1.5 was a coordinated-burst skip (only behaviorally-changed BCs were bumped that burst); the gap v1.4→v1.6 is deliberate, not a lost row; future readers can distinguish deliberate-skip from a lost entry (mirrors capabilities.md 'All versions vX–vY confirmed present' guard pattern). [Prior: 2026-06-15 (v1.15) — fix burst (product-owner): (F-P30-002 LOW) §Postconditions PC5: added explicit one-line clause stating that a non-null `precompact_flush_sha` when the log is absent or corrupt is VALID (no corroboration possible — anti-fabrication only HARD-BLOCKs on a positive mismatch against a present+valid log); previously implicit from the three-state rule, now explicit per ADR-026 §Decision 2 and EC-001/EC-006. (O-P29-002 LOW intent) §Architecture Anchors: added one-line justification note for the SS-05/SS-06 subsystem-split between the orchestration logic BC (SS-05) and the skill file artifact (SS-06). BC-5.41.001 v1.14→v1.15. [Prior: 2026-06-15 (v1.14) — F-P25-001: EC-002/003/004 error-code typo fixed: `AntiShabricationFailed` → `AntiFabricationFailed` (3 occurrences; no other files affected). [Prior: 2026-06-15 (v1.13) — F-P22-003: §Related BCs, §Traceability DI-021, §Traceability ADR: replaced obsolete 'side-channel file' framing with 'precompact-flush-log (append-only)' terminology consistent with PC5/EC-011/Architecture Anchors. [Prior: 2026-06-15 (v1.12) — F-P19-001-sibling (BC-5.41.001 §VP Anchors VP-081 title cite synced to VP-081 current H1): added missing `(wave_id > 1)` qualifier to VP-081 dash-list title in §VP Anchors (line ~176); trailing behavioral prose preserved verbatim. Stale-VP-title-cite class per POLICY 19 stable-anchor cites. [Prior: 2026-06-15 (v1.11) — F-P18-001: HandoffMissing error-code contract added to shell wave-gate (PC9 + EC-014 + test vector handoff-missing-hard-block); HandoffMissing defined as HANDOFF.md absent on factory-artifacts at wave-close — semantically distinct from HandoffIncomplete (WASM gate, BC-4.14.001); boundary made explicit. [Prior: 2026-06-15 (v1.10) — F-P17-001: wave-1 unconditional-null carve-out closed; three-state rule now wave-agnostic for log-present+valid case (PC2/PC5/EC-001/EC-011 + test vectors); F-P17-002: EC table display order reordered to ascending EC-NNN. [Prior: 2026-06-15 (v1.9) — CV-P14-002 follow-up: EC-011 hard-block mechanism aligned to PC5 FIELD-4 corroboration; removed live `git cat-file -t` implication inconsistent with pure-parse capability. [Prior: 2026-06-15 (v1.8) — F2 pass-14 fix-burst: (F-P14-001) PC2 precompact_flush_sha null-permitted rule rewritten: blanket wave>1-null prohibition removed; null now permitted for wave_id=1 OR for wave_id>1 ONLY when precompact-flush-log is genuinely absent (advisory per PC5); null is a HARD BLOCK (PrecompactShaMismatch) when log exists and contains a valid commit SHA. PC2, PC5, EC-006, and test vector wave-n-null-sha-advisory are now mutually coherent. [Prior: 2026-06-14 (v1.7) — F2 pass-13 fix-burst: (F-P13-004) ADR Traceability cell: load-bearing inline `per v1.6` version token removed from §Decision 2 parenthetical; rewritten as non-load-bearing informational cite per POLICY 19 anti-volatile-pin. [Prior: 2026-06-14 (v1.6) — F2 pass-6 fix-burst: (F-P6-001/002) PC5 stale-SHA case (b) label: 'write-before-push crash' → 'log corruption or truncation' (re-grounding per ADR-026 v1.6 F-P5-002; mechanically impossible write-before-push justification removed); wave-gate is a SHELL skill so it MAY exec git, but canonical corroboration is structured field read (FIELD-2 SHA + FIELD-4 `commit` token from last-line of precompact-flush-log); git-exec is a permitted shell-context capability, NOT the sole method. (F-P6-004) PC2 heading and body: 'All 9 required fields' → 'All 9 base required fields present; `epic_status: complete` additionally required on EPIC-COMPLETE wave (must be ABSENT on non-final waves)'; `epic_status` conditional field added to PC2 list; H1 updated to reflect 9-base+epic_status conditionality; EC-012/EC-013 added (non-final+epic_status → UnexpectedEpicStatus; final+epic_status:complete → Continue; final missing epic_status → MissingEpicStatus). (E-18) ADR cite convention: v1.4 version token dropped per ADR-026 §BC Traceability Cite Convention (TD-VSDD-091 anti-volatile-pin); stable §Decision anchors adopted. [Prior: 2026-06-14 (v1.4) — F2 pass-4 fix-burst: (F-P4-003) ADR cite v1.3→v1.4 (cite-only). [Prior: 2026-06-14 (v1.3) — F2 pass-3 fix-burst: (O-P3-002) PC8 added: EPIC-COMPLETE operator surfacing — when EPIC-COMPLETE triggers (all stories terminal), wave-gate announces completion via stdout with concrete message format before exiting. ADR cite v1.1→v1.3. [Prior: 2026-06-14 (v1.2) — F2 pass-2 fix-burst: (F-P2-001) PC2 wave_id field def updated: phantom `current_wave:` field → derived from sprint-state.yaml topo-sort ordinal (product) or STATE.md current_step: (engine). PC3 anti-fabrication cross-check updated: wave_id matches derived value from sprint-state.yaml/current_step: (no stored current_wave: field). (F-12 append-log) PC5 last-precompact-flush-sha side-channel → precompact-flush-log append-log (last line + git cat-file -t validation); stale-SHA-in-log edge case added (write-before-push crash → skip); EC-006 + EC-011 + Architecture Anchors updated to precompact-flush-log. [Prior: 2026-06-14 (v1.1) — F2 pass-1 fix-burst: (F-1) Precondition 3 re-anchored: phantom `current_wave:` field removed; wave identity derives from sprint-state.yaml dependency-order (product pipelines) or STATE.md `current_step:` (engine). (F-2) Precondition 4 downgraded: factory lock held OR absent; `factory_lock_holder` nullable; Postcondition 2 and anti-fabrication cross-check Postcondition 3 updated for null case. EC-010 (factory_lock absent) added. (F-12) precompact_flush_sha now a HARD cross-check against `.factory/hooks/last-precompact-flush-sha` when that file exists; advisory-override for wave>1 null SHA tightened. (DI) TBD-DI replaced with DI-020+DI-021+DI-023.]]"
phase: F2
inputs:
  - .factory/feature-delta/issue-173/F1-delta-analysis.md
  - .factory/specs/architecture/decisions/ADR-026-wave-boundary-checkpoint-reset-and-lossless-intra-wave-compaction.md
input-hash: "c2426d5"
traces_to: .factory/specs/prd.md
origin: greenfield
extracted_from: null
subsystem: "SS-05"
capability: "CAP-032"
lifecycle_status: draft
introduced: v1.0-feature-context-durability-E18
modified:
  - "2026-06-18 (v1.21) — clarity amendment (product-owner): (O-P10-002 disposition) PC2 `active_bcs` field definition expanded — field semantics are existence-only (all BC .md files resolvable in bc-dir via find -name '*.md'), NOT lifecycle-filtered. 'Active' in the name means present in the corpus at handoff time. PC3 cross-check remains file-existence-only. If lifecycle filtering is ever required, it requires an explicit postcondition amendment."
  - "2026-06-18 (v1.20) — (O-P8-001 research disposition, product-owner) PC2 wave_id derivation: explicit named preconditions P-WAVE-BARRIER-INVARIANT + P-SPRINT-STATE-WAVE-ORDER added; leading-contiguous-terminal-run algorithm documented as intended model under these preconditions; Kahn-DAG-level derivation documented as design boundary (not deferred work, not a TD entry). Cross-links BC-4.10.001 and validate-wave-gate-completeness/validate-wave-gate-prerequisite hooks. No behavioral change to operative wave_id derivation logic."
  - "2026-06-18 (v1.19) — new edge case (product-owner): (F-P7-002 MEDIUM) EC-015 added — idempotent EPIC-COMPLETE re-invocation; byte-identical HANDOFF.md content; skill detects empty staged-diff, skips commit, still emits PC8/AC-012 3-line EPIC-COMPLETE announcement, exits 0. Anchored to PC8. Closes pass-7 F-P7-002."
  - "2026-06-18 (v1.18) — BC conflict reconciliation (product-owner): (F-S1801-P3-003) PC8 EPIC-COMPLETE stdout message lines 2–3 corrected to be verbatim-identical with BC-5.41.002 PC7 (canonical text adopted from BC-5.41.002; see changelog v1.18)."
  - "2026-06-15 (v1.17) — fix burst (product-owner): (F-P35-002) §Changelog v1.5 skip-marker: de-enumerated explanatory clause (canonical de-enumerated wording applied; robust against premise errors)."
  - "2026-06-15 (v1.16) — fix burst (product-owner): (F-P32-003) §Changelog: defensive skip-marker annotation added for v1.5 absence — coordinated-burst skip (only behaviorally-changed BCs bumped that burst); gap v1.4→v1.6 is deliberate, not a lost row."
  - "2026-06-15 (v1.15) — fix burst (product-owner): (F-P30-002) PC5: added explicit non-null-when-log-absent VALID clause per ADR-026 §Decision 2. (O-P29-002) §Architecture Anchors: SS-05/SS-06 subsystem-split justification note added."
  - "2026-06-15 (v1.14) — F-P25-001: EC-002/003/004 error-code typo fixed: `AntiShabricationFailed` → `AntiFabricationFailed` (3 occurrences). No semantic change; load-bearing identifier corrected to match PC3 prose ('Anti-fabrication cross-checks')."
  - "2026-06-15 (v1.13) — F-P22-003: §Related BCs, §Traceability DI-021, §Traceability ADR: replaced obsolete 'side-channel file' framing with 'precompact-flush-log (append-only)' terminology consistent with PC5/EC-011/Architecture Anchors."
  - "2026-06-15 (v1.12) — F-P19-001-sibling: §VP Anchors VP-081 title cite synced to VP-081 current H1 (added missing `(wave_id > 1)` qualifier); trailing behavioral prose preserved verbatim. Stale-VP-title-cite class per POLICY 19 stable-anchor cites."
  - "2026-06-15 (v1.11) — F-P18-001: HandoffMissing error-code contract added to shell wave-gate: PC9 defines HARD BLOCK when wave-close is attempted but no current/verified HANDOFF.md is present on factory-artifacts; EC-014 added (absent HANDOFF.md at wave-close → HandoffMissing); canonical test vector handoff-missing-hard-block added; boundary with HandoffIncomplete (WASM gate BC-4.14.001, which validates payload fields, NOT file presence) made explicit in PC9 and EC-014."
  - "2026-06-15 (v1.10) — F-P17-001: three-state rule made wave-agnostic for log-present+valid-FIELD-4 case; wave-1 unconditional-null carve-out removed from PC2/PC5/EC-001/EC-011 and test vectors; wave-1+present-valid-log+null now HARD BLOCK PrecompactShaMismatch. F-P17-002: EC table display rows reordered to ascending EC-NNN order (IDs unchanged, display-only reorder)."
  - "2026-06-15 (v1.9) — CV-P14-002 follow-up: EC-011 hard-block mechanism aligned to PC5 FIELD-4 corroboration; removed live `git cat-file -t` implication inconsistent with pure-parse capability constraint."
  - "2026-06-15 (v1.8) — F2 pass-14 fix-burst: (F-P14-001) PC2 precompact_flush_sha null-permitted rule rewritten: blanket wave>1-null prohibition removed; null permitted for wave_id=1 OR for wave_id>1 ONLY when precompact-flush-log is genuinely absent (advisory per PC5, no hard block); null is HARD BLOCK (PrecompactShaMismatch) when log exists and contains a valid commit SHA. PC2/PC5/EC-006/test-vector wave-n-null-sha-advisory mutually coherent."
  - "2026-06-14 (v1.7) — F2 pass-13 fix-burst: (F-P13-004) ADR Traceability cell: load-bearing inline `per v1.6` version token removed from §Decision 2 parenthetical; rewritten as non-load-bearing informational cite per POLICY 19 anti-volatile-pin."
  - "2026-06-14 (v1.6) — F2 pass-6 fix-burst: (F-P6-001/002) PC5 stale-SHA label corrected + corroboration mechanism documented; (F-P6-004) H1+PC2 field-count 9→9-base+epic_status conditional; EC-012/EC-013 added; ADR cite convention: stable §Decision anchors (TD-VSDD-091)."
  - "2026-06-14 (v1.4) — F2 pass-4 fix-burst: (F-P4-003) ADR cite v1.3→v1.4 (cite-only)."
  - "2026-06-14 (v1.3) — F2 pass-3 fix-burst: PC8 added (EPIC-COMPLETE stdout surfacing per O-P3-002); ADR cite v1.1→v1.3."
  - "2026-06-14 (v1.2) — F2 pass-2 fix-burst: PC2 wave_id phantom-field → derived value (sprint-state.yaml/current_step:); PC3 cross-check updated to derived value; PC5 side-channel file → precompact-flush-log append-log (last line + git cat-file -t); stale-SHA edge case (write-before-push crash); EC-006+EC-011+Arch Anchors updated."
  - "2026-06-14 (v1.1) — F2 pass-1 fix-burst: Precondition 3 re-anchored (sprint-state.yaml/current_step:; no phantom current_wave:); Precondition 4 lock-held-or-absent; PC2 factory_lock_holder nullable; PC3 cross-check updated for null holder; PC5 precompact_flush_sha hard cross-check against last-precompact-flush-sha side-channel; EC-010 factory_lock absent edge case added; TBD-DI replaced with DI-020+DI-021+DI-023; ADR cite v1.0→v1.1."
deprecated: null
deprecated_by: null
replacement: null
retired: null
removed: null
removal_reason: null
---

# BC-5.41.001: wave-gate writes verified HANDOFF.md with all 9 base required fields (+epic_status on EPIC-COMPLETE wave) and anti-fabrication cross-checks before declaring wave closed

## Description

When the `wave-gate` skill is invoked to close wave N (where N > 0), it must produce a `HANDOFF.md` artifact on the `factory-artifacts` branch before the wave is considered closed. Every field in `HANDOFF.md` must be cross-checked against an external verifiable source (git or filesystem) — no field may rely solely on in-context memory. Missing or unverifiable fields cause `wave-gate` to block and surface the specific gap to the operator. This BC closes the fabricated-SHA failure class documented in issues #170 and #173.

## Preconditions

1. The `wave-gate` skill is invoked with intent to close wave N (N >= 1).
2. The `factory-artifacts` orphan branch is accessible via `git`.
3. Wave identity is determinable from real substrate: for product pipelines, `sprint-state.yaml` wave-group ordering (derived by `wave-scheduling` skill dependency-order); for the self-referential engine, STATE.md frontmatter `current_step:` field. No phantom `current_wave:` frontmatter field is referenced — this field does not exist.
4. The factory lock is held by the invoking session OR is absent. Lock held: `STATE.md` `factory_lock.holder` matches invoking session identity per BC-5.40.001 PC4. Lock absent: `factory_lock:` block is absent/null in STATE.md — wave-gate may proceed without a lock guard; `factory_lock_holder` field in HANDOFF.md is set to `null`. Both states are valid preconditions for wave-gate invocation.
5. Harness version >= v2.1.105 (for PreCompact/PostCompact event support in downstream hooks).

## Postconditions

1. **HANDOFF.md written**: A `HANDOFF.md` file is written to the `factory-artifacts` branch root (or `.factory/` path on that branch) containing all 9 base required fields specified in ADR-026 §Decision 2 (plus `epic_status: complete` conditionally on the EPIC-COMPLETE wave).

2. **All 9 base required fields present; `epic_status: complete` additionally required on EPIC-COMPLETE wave**: The following 9 base fields are present and non-empty (unless explicitly allowed to be null):
   - `wave_id` — integer derived from sprint-state.yaml dependency-order topo-sort ordinal (product pipelines) OR STATE.md `current_step:` pass number (engine) — there is no `current_wave:` field.

     **Algorithm: leading-contiguous-terminal-run model.** `wave_id` is derived as: scan sprint-state.yaml entries in file order; count the leading contiguous run of entries whose status is terminal (merged / withdrawn / cancelled); the current wave ordinal is that run length + 1. This model is correct under the two preconditions below.

     **P-WAVE-BARRIER-INVARIANT** *(explicit precondition — previously implicit)*: Wave_id derivation assumes the wave-gate barrier invariant holds — all stories in wave k reach terminal status (merged / withdrawn / cancelled) before any story in wave k+1 leaves draft or pending. This invariant is enforced upstream by the `validate-wave-gate-completeness` and `validate-wave-gate-prerequisite` hooks (registered in `hooks-registry.toml`) and by BC-4.10.001 ("validate-per-story-adversary-convergence WASM hook MUST block wave-gate dispatch when any story lacks convergence clearance"). The leading-contiguous-terminal-run derivation is sound under this invariant because a non-terminal entry cannot appear before a terminal entry in a valid wave sequence.

     **P-SPRINT-STATE-WAVE-ORDER** *(explicit precondition — previously implicit)*: sprint-state.yaml entries are ordered by wave (file order == dependency-wave order). If per-entry wave-level information is present (e.g., a `wave:` field on each entry), derivation SHOULD use it directly — taking the minimum wave number among non-terminal stories — rather than relying on file order. If only statuses and file order are present (no per-entry wave field), file-order==wave-order is a precondition of the algorithm and violations MUST fail loud (hard error surfaced to operator), not silently return a wrong ordinal.

     **Documented design boundary (not deferred work):** A full Kahn-DAG-level derivation — computing wave ordinals from explicit dependency edges, with no reliance on file order or the barrier invariant — would make `wave_id` derivation order-free and invariant-free. This is a known optional design evolution. It is NOT required while VSDD enforces the wave-gate barrier invariant (P-WAVE-BARRIER-INVARIANT above). This boundary is documented here to distinguish "soundly scoped given invariants" from "deferred work." No tech-debt-register entry is needed; no future-story anchor is required.
   - `last_verified_develop_sha` — 40-character lowercase hex string
   - `active_bcs` — non-empty list of strings (each a BC file path). **Semantics: existence-only, NOT lifecycle-filtered.** The field lists all BC `.md` files resolvable in the BC directory at handoff time (`find "$BC_DIR" -name "*.md"`). The name "active" refers to the set of BCs present/available in the spec corpus at wave close — it does NOT mean only BCs with `lifecycle_status: active`. The anti-fabrication cross-check (PC3) enforces file existence only: each path must resolve to a real file. Retired, deprecated, or removed BCs whose files are still present in the directory are included; BCs whose files have been physically deleted from the directory are not (they would fail the PC3 existence check). If a lifecycle-filtered subset is ever required, that must be a future behavioral change with an explicit postcondition amendment.
   - `next_wave_stories` — list of objects with `{id, status}` keys
   - `open_decisions` — list of objects with `{id, anchor_type, anchor_ref}` keys (may be empty list)
   - `pending_fixes` — list of objects with `{finding_id, pr_or_issue_ref}` keys (may be empty list)
   - `process_gaps` — list (may be empty)
   - `precompact_flush_sha` — 40-char hex OR explicit `null`. Three-state rule (wave-agnostic for log-present+valid case):
     1. **Log genuinely absent** (`test -f` false): null permitted for wave_id=1 (silent, normal first-wave); null permitted for wave_id>1 with advisory warning only (no hard block per PC5).
     2. **Log present but last-line corrupt** (FIELD-4 absent/empty/≠`commit`): stale entry; treat as log-absent per DI-025; null permitted any wave_id.
     3. **Log present + last-line FIELD-4 == `commit`**: null is a HARD BLOCK via `PrecompactShaMismatch` for **any wave_id including wave_id=1**; non-null must equal FIELD-2 SHA, mismatch is also a HARD BLOCK. The wave_id=1 carve-out does NOT override a present+valid log.
   - `factory_lock_holder` — string matching `factory_lock.holder` in `STATE.md` when lock is held; `null` when `factory_lock:` block is absent or holder is absent/null (lock not held)
   - `epic_status` — string `"complete"`; **CONDITIONAL**: required ONLY on the final/EPIC-COMPLETE wave (when all sprint-state.yaml stories have terminal status AND `next_wave_stories: []`); MUST be ABSENT (not `null`, not any value — genuinely absent) on all non-final waves. Presence of `epic_status` on a non-final wave is a malformed HANDOFF.md (causes `UnexpectedEpicStatus` from the validate-wave-handoff-completeness gate).

3. **Anti-fabrication cross-checks pass**:
   - `wave_id` matches the value computed by wave-handoff from sprint-state.yaml topo-sort ordinal (product) or STATE.md `current_step:` (engine) — no `current_wave:` field exists; cross-check is against the derived value, not a stored field
   - `last_verified_develop_sha` == output of `git rev-parse origin/develop` at handoff time
   - Each path in `active_bcs` resolves to an existing file under `.factory/specs/behavioral-contracts/`
   - Each `id` in `next_wave_stories` exists in `STORY-INDEX.md`
   - `factory_lock_holder` == `STATE.md` `factory_lock.holder` (read from git) when lock is held; null in HANDOFF.md is correct when STATE.md `factory_lock:` block is absent/null

4. **Block on missing/failing fields**: If any required field is absent or any anti-fabrication check fails, `wave-gate` blocks wave close, surfaces the specific failing fields and checks to the operator, and does NOT write a partial `HANDOFF.md`.

5. **`precompact_flush_sha` hard cross-check (F-12)**: `wave-gate` MUST cross-check `precompact_flush_sha` against the append-log file `.factory/hooks/precompact-flush-log` — specifically the LAST LINE of that file. The log line has four space-separated fields: `<ISO-timestamp> <SHA> <cycle>/<step> commit`. The canonical/primary corroboration is a structured field read: wave-gate reads FIELD-2 (the SHA) and FIELD-4 (the `commit` token) from the last line. wave-gate is a SHELL skill and therefore MAY exec `git cat-file -t <SHA>` to independently verify the object type, but this git exec is a permitted shell-context capability, NOT the sole or mandatory method — the FIELD-4 pre-embedded token provides sufficient corroboration (consistent with BC-5.41.003 and DI-025 which specify FIELD-4 as the canonical corroboration for WASM/shell contexts alike):
   - **File exists; last-line FIELD-4 == `commit` and FIELD-2 SHA is valid**: `precompact_flush_sha` MUST equal FIELD-2 of the LAST LINE of `.factory/hooks/precompact-flush-log` (read via literal filesystem read, not operator attestation). A mismatch or a null `precompact_flush_sha` when the log contains a valid commit SHA is a HARD BLOCK — `wave-gate` rejects the handoff with `PrecompactShaMismatch`. This rule applies to **any wave_id including wave_id=1**; the wave_id=1 carve-out does not override a present+valid log. Fabricated or null SHAs that contradict the log MUST block regardless of wave number.
   - **SHA in log but FIELD-4 is absent, empty, or not the literal string `commit`** (indicating log corruption or truncation): treat the log entry as stale and skip it; fall through to the genuinely-absent case below.
   - **File genuinely absent** (verified by `test -f .factory/hooks/precompact-flush-log` returning false): `precompact_flush_sha: null` is permitted for wave_id=1 (silent, normal first-wave case; no log means no PreCompact ever fired). For wave_id > 1 with no flush log: advisory warning only; operator must confirm no PreCompact fired; `wave-gate` surfaces the warning but does not hard-block (the absence is mechanically verified, not attested).
   - **Non-null SHA when log is absent or corrupt is VALID**: A non-null `precompact_flush_sha` value when the log is genuinely absent or its last line is corrupt (FIELD-4 absent/empty/≠`commit`) is VALID — no corroboration is possible in those cases, so anti-fabrication only HARD-BLOCKs on a positive mismatch against a present+valid log (per ADR-026 §Decision 2). A non-null SHA with no log to check against is not a violation; it is simply unverifiable and permitted.
   - **Rationale**: An unchecked null SHA is exploitable at any wave number — a hallucinating session could write `precompact_flush_sha: null` to bypass the flush guarantee even on wave_id=1 if a PreCompact fired mid-wave. The log file is the mechanical ground truth; wave_id is a discriminator ONLY in the log-absent case.

6. **Commit to factory-artifacts**: After all checks pass, `wave-gate` commits `HANDOFF.md` to the `factory-artifacts` branch with a commit message: `HANDOFF wave-<N> <ISO-timestamp>`.

7. **validate-wave-handoff-completeness gate passes**: After writing `HANDOFF.md`, the `validate-wave-handoff-completeness` WASM gate (BC-4.14.001) is invoked as a PostToolUse gate and must return `Continue` before the wave is declared closed.

8. **EPIC-COMPLETE operator surfacing**: When `wave-gate` detects EPIC-COMPLETE (all entries in `sprint-state.yaml` have terminal status — merged, withdrawn, or cancelled — and no pending/draft entries remain), before exiting 0 it MUST write the following message to stdout so the operator is explicitly notified:
   ```
   EPIC-COMPLETE: All stories in sprint-state.yaml have reached terminal status.
   Epic <epic-id> is complete. No wave-state.yaml written for next wave.
   HANDOFF.md committed to factory-artifacts with epic_status: complete.
   ```
   Where `<epic-id>` is derived from the cycle identifier in `STATE.md` `current_cycle:` field. A silent exit 0 on EPIC-COMPLETE is a specification violation — the operator must receive explicit confirmation that the epic has ended, not merely the absence of an error.

9. **HandoffMissing — block wave-close when no HANDOFF.md is present on factory-artifacts**: If `wave-gate` is invoked to close a wave but `HANDOFF.md` is absent from the `factory-artifacts` branch (i.e., no prior verified HANDOFF.md exists at the root or `.factory/` path of that branch), `wave-gate` MUST:
   - Return `Error("HandoffMissing")` and surface a human-readable message: `HandoffMissing: no verified HANDOFF.md found on factory-artifacts at wave-close; write HANDOFF.md via wave-handoff before closing the wave`.
   - NOT declare the wave closed.
   - NOT write any partial HANDOFF.md in this state (the file must be produced by the full wave-handoff flow, not patched in-place).

   **Semantic boundary with HandoffIncomplete (BC-4.14.001):**
   - `HandoffMissing` is a SHELL wave-gate error code. It fires when `HANDOFF.md` is **absent** on `factory-artifacts` at wave-close time. This check is performed by the shell `wave-gate` skill before invoking any WASM gate.
   - `HandoffIncomplete` is a WASM gate error code (BC-4.14.001 / `validate-wave-handoff-completeness`). It fires when `HANDOFF.md` IS present but required fields are missing or malformed. Per BC-4.14.001 EC-011, the WASM gate is NOT triggered on HANDOFF.md absence — it validates a write payload, not file presence. The WASM gate therefore can never emit `HandoffMissing`.
   - The two codes are disjoint: `HandoffMissing` = file absent; `HandoffIncomplete` = file present but incomplete. Neither subsumes the other.

   **Integration fixture**: The integration test `test_wave_close_blocked_without_handoff` (exercising VP-081 Postcondition A) asserts that the shell wave-gate returns `Error("HandoffMissing")` when wave-close is attempted with no verified HANDOFF.md present on factory-artifacts. This BC is the defining source for that error code.

## Invariants

1. **No in-context memory assertions**: Every `HANDOFF.md` field value must be derivable from an external source (git, filesystem, index file). A field whose value cannot be cross-checked must be omitted or flagged with an explicit `TBD` + operator action item — not silently populated from in-context reasoning.

2. **Wave cannot be closed without a passing HANDOFF.md**: The wave-gate's "declare wave closed" step is gated behind this BC's Postconditions. A wave-gate invocation that skips HANDOFF.md production is a specification violation.

3. **HANDOFF.md is a singleton per wave boundary**: There is one `HANDOFF.md` on `factory-artifacts` (not wave-scoped files). Each wave close overwrites the previous HANDOFF.md with the new wave's verified state. Git history preserves prior HANDOFF.md versions.

4. **Anti-fabrication is not optional**: The cross-check against `git rev-parse origin/develop` for `last_verified_develop_sha` must execute as a literal shell command, not be inferred from in-context knowledge of the SHA. Same for filesystem existence checks on `active_bcs` paths.

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | Wave_id = 1 (first wave ever); no prior HANDOFF.md exists; `.factory/hooks/precompact-flush-log` is genuinely absent | `precompact_flush_sha: null` is valid (log absent — no PreCompact ever fired). All other fields still required. If precompact-flush-log IS present and its last line has FIELD-4==`commit`, null is a HARD BLOCK (PrecompactShaMismatch) even at wave_id=1 per PC5 three-state rule. |
| EC-002 | `last_verified_develop_sha` mismatch (`git rev-parse` returns different value than operator supplied) | Block with `AntiFabricationFailed: last_verified_develop_sha mismatch`; surface both values to operator |
| EC-003 | An `active_bcs` path does not resolve to an existing file | Block with `AntiFabricationFailed: BC path not found: <path>`; operator must correct the path |
| EC-004 | A `next_wave_stories` story ID not found in STORY-INDEX.md | Block with `AntiFabricationFailed: story ID not in STORY-INDEX: <id>` |
| EC-005 | `factory_lock_holder` does not match `STATE.md` factory_lock.holder (when lock IS held) | Block; lock mismatch indicates stale handoff data or another holder |
| EC-006 | `precompact_flush_sha` is null for wave_id > 1; `.factory/hooks/precompact-flush-log` file genuinely absent (mechanically verified) | Advisory warning only; operator must confirm no PreCompact fired; wave-gate does not hard-block |
| EC-007 | HANDOFF.md written but `validate-wave-handoff-completeness` gate fails | Wave close is blocked; operator must correct the failing fields |
| EC-008 | `open_decisions` list is empty | Valid; no open decisions is a legitimate wave-close state |
| EC-009 | `process_gaps` list is non-empty (carry-forward from issue #171) | Valid; gaps are carried forward explicitly; wave closes after other checks pass |
| EC-010 | `STATE.md` `factory_lock:` block is absent/null (no lock held) | `factory_lock_holder: null` in HANDOFF.md; wave-gate proceeds without lock validation; this is valid per ADR-025 opt-in model |
| EC-011 | `precompact_flush_sha: null` in HANDOFF.md but `.factory/hooks/precompact-flush-log` last line contains a valid commit SHA (FIELD-4 == `commit` per PC5's FIELD-4 corroboration method — not by a live `git cat-file` call); applies to **any wave_id including wave_id=1** | HARD BLOCK: `PrecompactShaMismatch`; operator must populate `precompact_flush_sha` with the SHA (FIELD-2) from the last line of the log; the wave_id=1 carve-out does not apply when the log is present and valid |
| EC-012 | HANDOFF.md write on non-final wave includes `epic_status: complete` | HARD BLOCK: `UnexpectedEpicStatus`; `epic_status` MUST be absent on non-final waves; wave-gate rejects the handoff |
| EC-013 | HANDOFF.md write on EPIC-COMPLETE final wave (all stories terminal, `next_wave_stories: []`) is missing `epic_status` | HARD BLOCK: `MissingEpicStatus`; `epic_status: complete` is required on EPIC-COMPLETE waves |
| EC-014 | `wave-gate` is invoked to close a wave but `HANDOFF.md` is entirely absent from `factory-artifacts` (not merely incomplete — the file does not exist at the branch root or `.factory/` path) | HARD BLOCK: `HandoffMissing`; human-readable message: `HandoffMissing: no verified HANDOFF.md found on factory-artifacts at wave-close; write HANDOFF.md via wave-handoff before closing the wave`; wave is NOT declared closed; no partial HANDOFF.md written. Distinct from HandoffIncomplete (BC-4.14.001 WASM gate) which fires only when the file IS present but fields are missing — the WASM gate is NOT triggered on absence per BC-4.14.001 EC-011. |
| EC-015 | The EPIC-COMPLETE path (PC8) is re-invoked with no changes since the prior wave-close commit — HANDOFF.md content is byte-identical to the previously committed version; `git -C <ARTIFACTS_WT> diff --cached --quiet` returns 0 after staging | The skill MUST detect the empty staged-diff condition after staging (e.g., `git -C <ARTIFACTS_WT> diff --cached --quiet`), treat it as a successful idempotent re-invocation, skip the commit (do NOT run `git commit`), STILL emit the canonical PC8 / AC-012 EPIC-COMPLETE 3-line stdout announcement, and exit 0. It MUST NOT exit non-zero, suppress the announcement, or attempt a commit that would fail with 'nothing to commit'. Anchored to PC8: the 3-line EPIC-COMPLETE stdout announcement is mandatory regardless of whether a new commit was produced. |

## Canonical Test Vectors

| Input | Expected Output | Category |
|-------|----------------|----------|
| wave-gate close (non-final wave) with all 9 base fields correct + all cross-checks pass; `epic_status` absent | HANDOFF.md committed to factory-artifacts; wave declared closed | happy-path-non-final |
| wave-gate close (EPIC-COMPLETE final wave) with all 9 base fields + `epic_status: complete`; all stories terminal; `next_wave_stories: []` | HANDOFF.md committed; EPIC-COMPLETE stdout announcement; wave declared complete | happy-path-epic-complete |
| wave-gate close; non-final wave; HANDOFF.md includes `epic_status: complete` | Block; `UnexpectedEpicStatus`; no HANDOFF.md committed | unexpected-epic-status |
| wave-gate close; EPIC-COMPLETE wave; HANDOFF.md missing `epic_status` | Block; `MissingEpicStatus`; no HANDOFF.md committed | missing-epic-status |
| wave-gate close; `last_verified_develop_sha` does not match `git rev-parse origin/develop` | Block; error message names the mismatch; no HANDOFF.md committed | anti-fabrication-sha |
| wave-gate close; `active_bcs` contains path `.factory/specs/behavioral-contracts/BC-99.99.999.md` (non-existent) | Block; error names the missing path; no HANDOFF.md committed | anti-fabrication-fs |
| wave_id = 1; `precompact_flush_sha: null`; precompact-flush-log genuinely absent (`test -f` false) | Valid; HANDOFF.md accepted (log absent — first wave, no PreCompact fired) | wave-1-null-sha-log-absent |
| wave_id = 1; `precompact_flush_sha: null`; precompact-flush-log present with valid last-line (FIELD-4==`commit`) | HARD BLOCK: `PrecompactShaMismatch`; wave-1 carve-out does not override present+valid log | wave-1-null-sha-log-present-hard-block |
| wave_id = 2; `precompact_flush_sha: null`; precompact-flush-log genuinely absent (`test -f` false) | Advisory warning; operator confirmation required; not a hard block | wave-n-null-sha-advisory |
| wave-gate close; `factory_lock_holder` mismatch | Block; lock mismatch surfaced | lock-holder-mismatch |
| wave-gate close invoked; `HANDOFF.md` absent from `factory-artifacts` branch entirely (no file at root or `.factory/` path); `validate-wave-handoff-completeness` WASM gate not invoked | HARD BLOCK: `Error("HandoffMissing")`; message `HandoffMissing: no verified HANDOFF.md found on factory-artifacts at wave-close; write HANDOFF.md via wave-handoff before closing the wave`; wave NOT declared closed; no partial HANDOFF.md written | handoff-missing-hard-block |

## Related BCs

- BC-4.14.001 — depends on: validate-wave-handoff-completeness WASM gate verifies HANDOFF.md completeness post-write
- BC-5.41.002 — sibling: wave-state.yaml is written atomically alongside HANDOFF.md at wave close (same wave-gate invocation)
- BC-5.40.001 — depends on: factory_lock must be held; factory-lock-write.sh renew invoked before HANDOFF.md commit per ADR-025 D11 Mechanism 1
- BC-7.07.001 — composes with: precompact-flush.sh appends `precompact_flush_sha` to the precompact-flush-log (append-only); wave-gate reads it for HANDOFF.md field population

## Architecture Anchors

- `plugins/vsdd-factory/skills/wave-gate/SKILL.md` — wave-gate skill; wave-close step extended with HANDOFF.md production obligation
- `plugins/vsdd-factory/skills/wave-handoff/SKILL.md` — NEW skill that encapsulates HANDOFF.md production logic (S-18.01 deliverable)
- `.factory/hooks/precompact-flush-log` — append-log written by precompact-flush.sh; wave-gate reads the LAST LINE for `precompact_flush_sha` field; canonical corroboration is FIELD-2 (SHA) + FIELD-4 (`commit` token) from the last line; wave-gate MAY additionally exec `git cat-file -t <SHA>` (shell context permits it) but this is not the sole or mandatory method
- **Subsystem SS-05/SS-06 split justification (O-P29-002):** This BC's `subsystem: SS-05` (Pipeline Orchestration) anchors the orchestration LOGIC and contract (wave-close decisions, HANDOFF.md schema); the skill FILE artifacts (`wave-gate/SKILL.md`, `wave-handoff/SKILL.md`) reside in SS-06 (Skill Catalog) per ADR-026 §Deliverables. This split is intentional: the BC governs orchestration behavior; the Architecture Anchors above locate the skill implementation files. SS-05 is the correct subsystem assignment for this behavioral contract.

## Story Anchor

S-18.01 (HANDOFF.md schema + wave-handoff skill)

## VP Anchors

- VP-081 — Wave Cannot Close Without Verified Handoff (wave_id > 1): shell wave-gate returns `Error("HandoffMissing")` when HANDOFF.md is absent on factory-artifacts at wave-close (PC9 + EC-014); `validate-wave-handoff-completeness` WASM gate additionally blocks close unless HANDOFF.md exists with all required ADR-026 §D2 fields (PC7)

## Verification Properties

| VP-NNN | Property | Proof Method |
|--------|----------|-------------|
| VP-081 | Wave cannot close without verified HANDOFF.md: shell wave-gate returns `Error("HandoffMissing")` when HANDOFF.md is absent on factory-artifacts (PC9/EC-014); WASM gate blocks when HANDOFF.md present but incomplete (PC7); integration fixture `test_wave_close_blocked_without_handoff` covers the absence path | integration |

## Traceability

| Field | Value |
|-------|-------|
| L2 Capability | CAP-032 ("Guarantee lossless context-window transitions via wave-boundary checkpoint and PreCompact flush") per capabilities.md §CAP-032 |
| Capability Anchor Justification | CAP-032 ("Guarantee lossless context-window transitions via wave-boundary checkpoint and PreCompact flush") per capabilities.md §CAP-032 — this BC specifies the verified HANDOFF.md wave-close checkpoint that is the primary cross-wave continuity mechanism (ADR-026 Decision 1 + Decision 2); it directly closes the fabricated-SHA failure class documented in issues #170 and #173 |
| L2 Domain Invariants | DI-020 (Wave/phase boundary transitions must not lose load-bearing pipeline state — enforced by requiring HANDOFF.md with all 9 fields before wave close); DI-021 (Handoff claims must be cross-checked against verifiable external ground truth — enforced by anti-fabrication cross-checks in PC3, including SHA verification against git and precompact_flush_sha against precompact-flush-log (append-only)); DI-023 (Wave/phase identity and next-wave story lists derive from real persisted substrate — enforced by wave_id derivation from sprint-state.yaml or STATE.md current_step:, and next_wave_stories from sprint-state.yaml status entries) |
| Architecture Module | SS-05 (Pipeline Orchestration) — wave-gate and wave-handoff skills live in `plugins/vsdd-factory/skills/` |
| ADR | ADR-026 §Decision 2 (HANDOFF.md schema + anti-fabrication cross-checks; wave_id from real substrate; factory_lock_holder nullable; precompact_flush_sha hard cross-check against precompact-flush-log (append-only); 9 base required fields + epic_status conditional on EPIC-COMPLETE wave — informational, non-load-bearing), §Decision 9 (wave-1 no-op), §Decision 1 (wave-boundary reset is primary mechanism) |
| Stories | S-18.01 |
| Cycle | v1.0-feature-context-durability-E18 (F2) |
| Feature | issue #173 / E-18 |

## Changelog

| Version | Date | Author | Change |
|---------|------|--------|--------|
| v1.21 | 2026-06-18 | product-owner | (O-P10-002 disposition) PC2 `active_bcs` field definition: clarity amendment stating field semantics are existence-only (all BC `.md` files present in bc-dir at handoff time via `find -name '*.md'`), NOT lifecycle-filtered. "Active" in the field name means resolvable in the corpus at handoff time — not filtered to `lifecycle_status: active`. PC3 cross-check remains file-existence-only. Future lifecycle filtering requires explicit postcondition amendment. |
| v1.20 | 2026-06-18 | product-owner | (O-P8-001 research disposition) PC2 wave_id derivation: explicit named preconditions P-WAVE-BARRIER-INVARIANT and P-SPRINT-STATE-WAVE-ORDER added. Leading-contiguous-terminal-run algorithm documented as the intended derivation model under these preconditions; cross-links BC-4.10.001 and validate-wave-gate-completeness / validate-wave-gate-prerequisite hooks (stable anchor refs, no volatile line numbers per POLICY 5 / TD-VSDD-091). Kahn-DAG-level order-free derivation documented as a design boundary — not deferred work, not a TD entry. No behavioral change to operative wave_id derivation logic. |
| v1.19 | 2026-06-18 | product-owner | (F-P7-002 MEDIUM) EC-015 added: idempotent EPIC-COMPLETE re-invocation — when the EPIC-COMPLETE path is re-invoked with byte-identical HANDOFF.md content and `git diff --cached --quiet` returns 0 after staging, the skill MUST detect the empty staged-diff, skip the commit, STILL emit the canonical PC8 / AC-012 EPIC-COMPLETE 3-line stdout announcement, and exit 0. MUST NOT exit non-zero or suppress the announcement. Anchored to PC8. Closes pass-7 F-P7-002. |
| v1.18 | 2026-06-18 | product-owner | (F-S1801-P3-003) PC8 EPIC-COMPLETE stdout message reconciled with BC-5.41.002 PC7. Line 2 corrected: "No next-wave handoff required." → "No wave-state.yaml written for next wave." Line 3 corrected: "HANDOFF.md written with epic_status: complete. wave-state.yaml NOT written." → "HANDOFF.md committed to factory-artifacts with epic_status: complete." BC-5.41.002 PC7 formulation adopted as canonical: more precise artifact naming in line 2; 'committed to factory-artifacts' in line 3 aligns with PC6 language; eliminates redundant repeat of wave-state.yaml NOT written. PC8 ≡ PC7 verbatim after this change. |
| v1.17 | 2026-06-15 | product-owner | (F-P35-002) §Changelog v1.5 skip-marker: de-enumerated explanatory clause — reworded to be self-contained without referencing which BCs changed at pass-5 (canonical de-enumerated form makes it robust against premise errors); consistent with canonical skip-marker wording applied to sibling E-18 BCs under F-P35-002. |
| v1.16 | 2026-06-15 | product-owner | (F-P32-003) §Changelog: defensive skip-marker annotation added for v1.5 gap — v1.5 was a coordinated-burst skip (only behaviorally-changed BCs were bumped in that burst); the jump v1.4→v1.6 is intentional, not a lost row. This annotation distinguishes deliberate-skip from a missing entry for future readers (mirrors capabilities.md guard pattern). |
| v1.15 | 2026-06-15 | product-owner | (F-P30-002) PC5: explicit non-null-when-log-absent/corrupt VALID clause added — a non-null `precompact_flush_sha` when the log is absent or corrupt is VALID (no corroboration possible); anti-fabrication only HARD-BLOCKs on positive mismatch against a present+valid log. Previously implicit; now explicit per ADR-026 §Decision 2 and EC-001/EC-006. (O-P29-002) §Architecture Anchors: SS-05/SS-06 split justification note added. |
| v1.14 | 2026-06-15 | product-owner | (F-P25-001) EC-002/003/004 error-code typo fixed: `AntiShabricationFailed` → `AntiFabricationFailed` (3 occurrences). Load-bearing identifier corrected to match PC3 prose ("Anti-fabrication cross-checks"). No semantic change to behavior. |
| v1.13 | 2026-06-15 | product-owner | (F-P22-003) §Related BCs, §Traceability DI-021, §Traceability ADR: replaced obsolete 'side-channel file' framing with 'precompact-flush-log (append-only)' terminology; consistent with load-bearing PC5/EC-011/Architecture Anchors. |
| v1.12 | 2026-06-15 | product-owner | (F-P19-001-sibling) §VP Anchors VP-081 title cite synced to VP-081 current H1: added missing `(wave_id > 1)` qualifier; trailing behavioral prose preserved verbatim. Stale-VP-title-cite class per POLICY 19 stable-anchor cites. |
| v1.11 | 2026-06-15 | product-owner | (F-P18-001) HandoffMissing error-code contract added: PC9 defines the SHELL wave-gate HARD BLOCK when wave-close is attempted but HANDOFF.md is absent on factory-artifacts. EC-014 added (absent HANDOFF.md → `HandoffMissing`). Canonical test vector `handoff-missing-hard-block` added. Semantic boundary with HandoffIncomplete (WASM gate BC-4.14.001 EC-011 — fires on present-but-incomplete file, NOT on absence) made explicit in PC9 body and EC-014. BC-5.41.001 is now the defining source for `Error("HandoffMissing")` asserted by VP-081 Postcondition A and integration fixture `test_wave_close_blocked_without_handoff`. |
| v1.10 | 2026-06-15 | product-owner | (F-P17-001) Closed wave-1 unconditional-null carve-out: three-state rule is now wave-agnostic for log-present+valid-FIELD-4 case. PC2 `precompact_flush_sha` bullet rewritten to enumerate all three states explicitly; state 3 (log present + FIELD-4==`commit`) is a HARD BLOCK for any wave_id including wave_id=1. PC5 first bullet updated to declare wave-agnostic scope. EC-001 updated: null valid ONLY when log genuinely absent; if log present+valid → HARD BLOCK even at wave_id=1. EC-011 updated: scope declared wave-agnostic including wave_id=1. Test vectors: `wave-1-null-sha` split into `wave-1-null-sha-log-absent` (valid) and `wave-1-null-sha-log-present-hard-block` (HARD BLOCK); `wave-n-null-sha-advisory` conditioned on log-absent. (F-P17-002) EC table display rows reordered to ascending EC-NNN sequence (EC-001..EC-013); IDs unchanged per append-only policy. |
| v1.9 | 2026-06-15 | product-owner | (CV-P14-002) EC-011 hard-block mechanism aligned to PC5 FIELD-4 corroboration; removed live `git cat-file -t` implication inconsistent with pure-parse capability constraint. Hard-block determination is now stated as FIELD-4 of last precompact-flush-log line equalling `commit` (per PC5), not a live `git cat-file` call. |
| v1.8 | 2026-06-15 | product-owner | (F-P14-001) PC2 precompact_flush_sha null-permitted rule rewritten: blanket wave>1-null prohibition removed; null permitted for wave_id=1 OR for wave_id>1 ONLY when precompact-flush-log is genuinely absent (advisory per PC5, no hard block); null is HARD BLOCK (PrecompactShaMismatch) when log exists and contains a valid commit SHA. PC2, PC5, EC-006, and test vector wave-n-null-sha-advisory are now mutually coherent. |
| v1.7 | 2026-06-14 | product-owner | (F-P13-004) ADR Traceability cell: load-bearing inline `per v1.6` version token removed from §Decision 2 parenthetical; rewritten as non-load-bearing informational cite per POLICY 19 anti-volatile-pin. |
| v1.6 | 2026-06-14 | product-owner | (F-P6-001/002) PC5 stale-SHA label corrected + corroboration mechanism documented; (F-P6-004) H1+PC2 field-count 9→9-base+epic_status conditional; EC-012/EC-013 added; ADR cite convention: stable §Decision anchors (TD-VSDD-091). |
| v1.4 | 2026-06-14 | product-owner | (F-P4-003) ADR cite v1.3→v1.4 (cite-only). |
| ~~v1.5~~ | — | — | **[SKIP — coordinated-burst skip (F-P32-003):** v1.5 was not produced for this BC; this BC received no behavioral change at the pass-5 (F-P5-*) coordinated fix-burst; the v1.4→v1.6 jump is intentional, not a lost changelog row. This row distinguishes a deliberate skip from a lost entry.] |
| v1.3 | 2026-06-14 | product-owner | PC8 added (EPIC-COMPLETE stdout surfacing per O-P3-002); ADR cite v1.1→v1.3. |
| v1.2 | 2026-06-14 | product-owner | PC2 wave_id phantom-field → derived value (sprint-state.yaml/current_step:); PC3 cross-check updated; PC5 side-channel → precompact-flush-log append-log; stale-SHA edge case; EC-006+EC-011+Arch Anchors updated. |
| v1.1 | 2026-06-14 | product-owner | PC3 re-anchored (sprint-state.yaml/current_step:; no phantom current_wave:); PC4 lock-held-or-absent; PC2 factory_lock_holder nullable; PC3 null-holder cross-check; PC5 precompact_flush_sha hard cross-check; EC-010 added; TBD-DI replaced with DI-020+DI-021+DI-023. |
| v1.0 | 2026-06-14 | product-owner | Initial creation (E-18 context-durability feature). |
