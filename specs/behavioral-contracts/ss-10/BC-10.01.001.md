---
document_type: behavioral-contract
level: L3
version: "1.0"
status: draft
producer: codebase-analyzer
timestamp: 2026-04-25T00:00:00
phase: "1.4b"
inputs:
  - .factory/specs/behavioral-contracts/bc-id-mapping.md
  - .factory/phase-0-ingestion/pass-3-deep-templates-tools-rules.md
input-hash: "4a5e2b8"
traces_to: .factory/specs/behavioral-contracts/bc-id-mapping.md
origin: brownfield
extracted_from: "pass-3-deep-templates-tools-rules.md:1214"
subsystem: "SS-10"
capability: "TBD"
lifecycle_status: active
introduced: v1.0.0-beta.4
modified: []
deprecated: null
deprecated_by: null
replacement: null
retired: null
removed: null
removal_reason: null
---

# Behavioral Contract BC-10.01.001: compute-input-hash: input-hash drift detection + remediation tool

> Section: compute-input-hash CLI
> Source BC (audit ID): BC-AUDIT-2100

## Description

Computes a 7-char short MD5 of the concatenated content of an artifact's `inputs:` frontmatter file list, used for drift detection between an artifact and its sources. Per-file commands: `<file>` (print hash), `--update` (write into frontmatter), `--check` (compare stored vs computed; exit 2 on mismatch), `--resolve` (list inputs with FOUND/MISSING). Scan commands: `--scan <dir>` (walk dir + report drift summary), `--scan <dir> --update` (auto-fix stale), `--scan <dir> --resolve` (find unresolvable inputs).

## Preconditions

1. TBD — preconditions not explicitly stated in source pass-3 entry.

## Postconditions

1. Computes a 7-char short MD5 of the concatenated content of an artifact's `inputs:` frontmatter file list, used for drift detection between an artifact and its sources. Per-file commands: `<file>` (print hash), `--update` (write into frontmatter), `--check` (compare stored vs computed; exit 2 on mismatch), `--resolve` (list inputs with FOUND/MISSING). Scan commands: `--scan <dir>` (walk dir + report drift summary), `--scan <dir> --update` (auto-fix stale), `--scan <dir> --resolve` (find unresolvable inputs).

## Invariants

1. Tool present at path; `--help` enumerates the 7 modes.

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | TBD — Phase 1.6b will enumerate edge cases from source review | TBD |

## Canonical Test Vectors

> Test vectors will be pulled from the pinned test case in Phase 1.6b.

| Input | Expected Output | Category |
|-------|----------------|----------|
| TBD | TBD | happy-path |
| TBD | TBD | edge-case |
| TBD | TBD | error |

## Verification Properties

| VP-NNN | Property | Proof Method |
|--------|----------|-------------|
| VP-001 | Computes a 7-char short MD5 of the concatenated content of an artifact's `inputs:` frontmatter file list, used for drift | manual (existing test: `plugins/vsdd-factory/bin/compute-input-hash`) |

## Traceability

| Field | Value |
|-------|-------|
| L2 Capability | TBD (subsystem L2 spec pending) |
| L2 Domain Invariants | TBD |
| Architecture Module | SS-10 (compute-input-hash CLI) |
| Stories | TBD (Phase 2 story-writer pass) |

## Related BCs (Recommended)

- TBD — cross-references will be filled in Phase 1.6b after all per-BC files exist.

## Architecture Anchors (Recommended)

- `architecture/SS-10-cli-tools.md` — section: compute-input-hash CLI

---

### Brownfield-Specific Sections

> This BC was extracted during Phase 0d brownfield ingestion (BC-AUDIT pass) and migrated to canonical one-per-file BC-S.SS.NNN format in Phase 1.4b.

#### Source Evidence

| Property | Value |
|----------|-------|
| **Path** | `plugins/vsdd-factory/bin/compute-input-hash` |
| **Source line(s)** | 1–21 (docstring) |
| **Confidence** | high |
| **Original audit ID** | BC-AUDIT-2100 |
| **Pass-3 source** | `pass-3-deep-templates-tools-rules.md:1214` |
| **Extraction Date** | 2026-04-25 |
| **Used by** | Used by `validate-input-hash.sh` hook; called by skills `check-input-drift`, `state-burst`, plus state-manager batch sweeps. |
| **Pinned test** | ``plugins/vsdd-factory/bin/compute-input-hash`` |

#### Evidence Types Used

- **assertion**: pinned by Rust unit/integration test
- **documentation**: stated in source-file docstring or pass-3 narrative

#### Purity Classification

| Property | Assessment |
|----------|-----------|
| **I/O operations** | TBD — Phase 1.6b will classify |
| **Global state access** | TBD |
| **Deterministic** | TBD |
| **Thread safety** | TBD |
| **Overall classification** | TBD |

#### Refactoring Notes

TBD — Phase 1.6b will produce refactoring guidance.

