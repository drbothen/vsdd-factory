---
document_type: behavioral-contract
level: L3
version: "1.3"
status: active
producer: state-manager
timestamp: 2026-09-03T10:43:17Z
phase: F2
cycle: v1.0-feature-engine-discipline-pass-1
inputs:
  - .factory/specs/architecture/decisions/ADR-049-last-amended-write-path-durable-fix-current-entry-plus-changelog-sequence.md
  - .factory/stories/S-15.03-index-cite-refresh-hook.md
  - .factory/cycles/v1.0-brownfield-backfill/decision-log.md
  - .factory/policies.yaml
input-hash: "5d7ec46"
traces_to: .factory/specs/architecture/decisions/ADR-049-last-amended-write-path-durable-fix-current-entry-plus-changelog-sequence.md
origin: greenfield
extracted_from: null
subsystem: "SS-10"
capability: "CAP-042"
lifecycle_status: active
introduced: v1.0-feature-engine-discipline-pass-1
modified:
  - "2026-09-02 (v1.1)"
  - "2026-09-02 (v1.2)"
  - "2026-09-03 (v1.3)"
deprecated: null
deprecated_by: null
replacement: null
retired: null
removed: null
removal_reason: null
bc_id: BC-10.13.001
section: "10.13"
last_amended: "2026-09-03 (v1.3) — POL-14 auto-promotion: draft→active (state-manager; S-15.03 PR #805 squash-merged `b4ff2383` 2026-09-03T10:43:17Z into develop); BC-INDEX v5.42→v5.43; D-1152."
---

# BC-10.13.001: last-amended-migrate — Sanctioned Rust CLI for `last_amended`/`changelog:` Migration, Full-Recovery Split, and Changelog Rotation (POLICY 21-Compliant)

## Description

A committed, platform-agnostic Rust `bin/` binary (POLICY 21 `no_new_shell_scripts` — explicitly
NOT a new `.sh` script) that performs three functions mandated by ADR-049/S-15.03 AC-006 and this
amendment: (1) migration bringing `STORY-INDEX.md`, `BC-INDEX.md`, `ARCH-INDEX.md`, `VP-INDEX.md`,
and `STATE.md` fully into the ADR-049 shape (adding `changelog:` to `STORY-INDEX.md`, confirming/
enforcing `last_amended` is strictly current-entry-only on all five, and remediating the
pre-existing D-1144 unescaped-double-quote defect in `BC-INDEX.md`/`ARCH-INDEX.md`/`STATE.md`);
(2) **full-recovery split** (this amendment, human-directed) — where a target file's
`last_amended` instead holds an inline `[Prior: ...]` bracket chain (the pre-D-1149-style
unbounded-growth shape, at any scale up to and beyond the historical 323K-350K-char calibration
ceiling), the migration subcommand SPLITS the current entry from the chain and relocates every
chained entry into `changelog:` as new sequence items, newest-first (see Postcondition PC7),
instead of refusing the file; and (3) as a safety-net utility, rotation of an over-long
`changelog:` sequence into a per-cycle archive under `.factory/cycles/<cycle-name>/`, mirroring
how `burst-log.md`/`decision-log.md` already archive per cycle. With function (2), this tool is a
complete, robust replacement for D-1149's one-time human-authorized POL-3 exception — not merely a
maintainer of the already-migrated shape — satisfying S-15.03 AC-010's "no future POL-3 exception
needed for this class" without qualification. Invoking the tool is the sanctioned path going
forward for every occurrence of this failure class, present or future, removing the need for any
future POL-3/TD-FACTORY-HOOK-BYPASS-001 ad hoc exception request.

## Preconditions

1. The tool is invoked against one of the five ADR-049-governed files (or, for the migration
   subcommand, against all five in one run) by an operator or an orchestrator-dispatched agent
   with filesystem write access to `.factory/`.

2. For the migration subcommand: the target file has a `last_amended:` field whose raw value
   parses as one of two recognized shapes — **(a)** already in D-1149-slim current-entry form (a
   single dated entry, optionally with a trailing `[Prior history → <file>-amendment-history.md]`
   pointer note), for which Postcondition PC2 applies as a verified no-op; or **(b)** a current
   entry followed by one or more inline `[Prior: <date> (vX.Y) — ...]` bracket-chain entries — the
   pre-D-1149-style unbounded-growth shape, at any scale up to and beyond the historical
   323K-350K-char calibration ceiling — for which Postcondition PC7's **full-recovery split**
   applies. **Both shapes are ELIGIBLE for migration** (superseding this BC's v1.0 Precondition 2,
   which excluded shape (b) as `NotEligible`): an inline-chain file is no longer out of scope; the
   tool is a full replacement for the one-time D-1149 human-authorized POL-3 exception, not merely
   a shape-completion/escape-remediation utility for already-slim files. The tool's only remaining
   `NotEligible` outcome is a `last_amended:` field that cannot be located at all, or whose raw
   value does not parse as a YAML scalar (e.g. a corrupted frontmatter delimiter) — see EC-008; a
   malformed-frontmatter condition, not a chain-length or chain-presence condition.

3. For the rotation subcommand: the target file's `changelog:` sequence has grown past an
   operator-configurable or tool-default size threshold, and a destination cycle name (matching
   an existing or newly-created `.factory/cycles/<cycle-name>/` directory) is supplied.

## Postconditions

### PC1 — migration completes `changelog:` coverage across all five files

Running the migration subcommand against `STORY-INDEX.md` adds a `changelog:` top-level YAML
sequence to its frontmatter if absent (completing the D-448(b)/D-414(c) deferral already recorded
in `STORY-INDEX.md`'s own header blockquote). Running it against `ARCH-INDEX.md`, `BC-INDEX.md`,
or `VP-INDEX.md` — which already carry `changelog:` — is a verified no-op on that field (the tool
confirms presence and structural validity rather than re-adding it). Running it against
`STATE.md` never adds a `changelog:` field (ADR-049 Decision 4 — `STATE.md` relies on its
body-level `## Decisions Log`/`## Phase Progress` instead).

### PC2 — migration confirms current-entry-only `last_amended`, or performs a full-recovery split into that shape

For each of the five files, the tool brings `last_amended` into a single dated current-entry-only
form with no nested `[Prior: <date> (vX.Y) — ...]` bracket referencing a different dated entry.
Where the file is already compliant (Precondition 2(a)), this is a verified no-op (identical to
the pre-amendment v1.0 behavior). Where the file's `last_amended` instead holds an inline
`[Prior: ...]` bracket chain (Precondition 2(b)), the tool performs the **full-recovery split**
defined in PC7, which also brings the field into this same current-entry-only shape as a side
effect of relocating the historical entries to `changelog:`.

### PC3 — D-1144 escape remediation on the three defective files (and on any relocated split entry)

For `BC-INDEX.md`, `ARCH-INDEX.md`, and `STATE.md` — the three files carrying the pre-existing
D-1144 unescaped-literal-double-quote defect in their current `last_amended` entry — the tool
rewrites the offending entry with every embedded literal `"` escaped to `\"`, producing a value
that parses cleanly under strict YAML `safe_load`. The entry's substantive text (every character
other than the escape insertions) is preserved verbatim. `VP-INDEX.md` and `STORY-INDEX.md` are
unaffected by this postcondition on their OWN current entry (not part of the D-1144 defect set).
This same escaping discipline also applies, as needed, to every entry PC7's full-recovery split
relocates into `changelog:` — regardless of which of the five files is being split — since a
historical bracket-chain entry inherited from the pre-D-1149 write path may itself carry an
unescaped literal quote.

### PC4 — migration is idempotent

Running the migration subcommand a second time (or any subsequent time) against an
already-migrated file produces a verified-clean report with zero file mutations — the tool
detects the already-compliant shape and takes no destructive or duplicating action (it never
double-adds a `changelog:` field, never double-escapes an already-escaped quote, and never
re-wraps an already-current-entry-only `last_amended`). This includes the case where PC7's
full-recovery split has just run: the freshly-split file is now in the Precondition 2(a) shape, so
an immediate re-invocation is governed by this postcondition, not by PC7 again — PC7 fires at most
once per pre-existing chain, never re-detects a chain where none remains, and never re-splits.

### PC5 — rotation archives without data loss

Running the rotation subcommand against a file whose `changelog:` sequence exceeds the size
threshold moves the oldest N items (operator-specified count, or all-but-the-most-recent-K by
tool default) into a new or appended `.factory/cycles/<cycle-name>/<file-basename>-changelog-archive.md`
file, verbatim, and removes exactly those items from the source file's `changelog:` sequence,
leaving a discoverability pointer (e.g. a comment or a `changelog_archive:` frontmatter field
naming the archive path) in the source file. No `changelog:` item's `date:`/`change:` text content
is altered by rotation — only its location changes.

### PC6 — the tool never touches the frozen D-1149 sidecars

Neither the migration subcommand (including its PC7 full-recovery split path), the rotation
subcommand, ever writes to, reads for mutation, or deletes any of the five
`*-amendment-history.md` sidecar files. They remain a frozen pre-migration archive per
S-15.03 §Migration and ADR-049 Decision 5. The tool MAY read them read-only for reporting purposes
(e.g., confirming they exist and are referenced by a pointer note) but never mutates them. PC7
operates exclusively on the live target file's own `last_amended`/`changelog:` fields; recovering
an inline chain is never sourced from, nor written to, a sidecar.

### PC7 — full-recovery split (inline `[Prior:]` chain migration)

When the migration subcommand encounters a `last_amended` value containing one or more inline
`[Prior: <date> (vX.Y) — ...]` bracket-chain entries (Precondition 2(b)), the tool performs a
full-recovery split in the same run, making itself a complete, robust replacement for the one-time
D-1149/POL-3 human-authorized surgery (S-15.03 AC-010 — no future POL-3 exception needed for this
class):

1. **Split point.** The tool scans the raw `last_amended` value for the FIRST occurrence of the
   literal marker ` [Prior:` (a space followed by `[Prior:`), distinguishing it from the
   unrelated, non-growing `[Prior history → ...]` pointer note (never split; see BC-5.45.001 PC4
   parenthetical / EC-005). Everything from byte 0 up to (not including) that marker is the
   CURRENT entry.
2. **Current entry stays in `last_amended`.** The CURRENT entry is re-emitted as the new
   `last_amended` value, current-entry-only, D-1144-escaped per PC3 — identical in effect to the
   confirm/no-op path in PC2.
3. **Tail parsing.** Everything from the first ` [Prior:` marker to the end of the value is the
   TAIL. The tool splits the TAIL on every subsequent ` [Prior:` marker (a single linear
   left-to-right scan; see the bounded-scan requirement in step 7 below), yielding one substring
   per historical entry, ordered from newest-of-the-priors to oldest (the order in which they
   appear in the nested chain).
4. **Verbatim preservation.** Each split entry's text is preserved character-for-character, except
   for the strict-YAML escaping already required by PC3/Invariant 3 (e.g. escaping an embedded
   literal `"`). No entry's substantive text is truncated, reworded, or reordered relative to its
   neighbors.
5. **Destination: `changelog:` sequence, newest-first.** Each parsed historical entry becomes one
   new `changelog:` sequence list item (`- date: ... / change: "..."` — the same shape PC2/
   BC-5.45.001 PC2 already use for a single displaced entry). The N entries are PREPENDED to the
   existing `changelog:` sequence in newest-first order — the entry nested immediately inside the
   CURRENT entry's `[Prior: ...]` bracket becomes the first (topmost) new `changelog:` item, the
   next-nested entry becomes the second, and so on. Every pre-existing `changelog:` item is left
   byte-for-byte untouched, appearing after all N newly-inserted items. This is the ADR-049 Option
   2 destination for history — full-recovery split produces the exact same canonical
   current-entry-only-plus-`changelog:`-sequence shape as every other write under BC-5.45.001; it
   does not introduce a new history location.
6. **`STORY-INDEX.md` bootstrap case.** If the target file has no `changelog:` field yet (only
   `STORY-INDEX.md`, pre-migration), the tool creates the `changelog:` sequence in the same write,
   seeded with the N split entries in newest-first order (consistent with EC-002's initial-seed
   convention).
7. **Bounded/streaming scan — mandatory for arbitrarily long input.** The marker scan (steps 1 and
   3) and the escape pass (step 4) are each a single linear (`O(n)`) pass over the raw scalar
   bytes, performed via a bounded/streaming scanner (e.g. a byte-cursor forward scan, never a
   repeated substring re-scan or a backtracking regex) so the tool safely processes a chain of
   arbitrary length and arbitrary entry count — including the D-1149 calibration case
   (`STORY-INDEX.md`'s pre-surgery 323,499-char frontmatter scalar; the general class extends up
   to ~350K chars) — without unbounded memory growth or non-linear runtime blowup, satisfying
   Invariant 3. This is precisely the operation Edit/Write-tool-mediated manual editing cannot
   perform safely at this scale, which is why this capability — not just the maintain-only
   shape-confirmation in PC2 — is what makes the tool a full replacement for D-1149's one-time
   human-authorized POL-3 exception.
8. **Idempotent.** Once PC7 has run against a file, the file's `last_amended` is
   current-entry-only with no inline chain remaining (Precondition 2(a) shape) — re-running the
   migration subcommand against it is therefore a PC4 no-op; it does not re-detect a chain (there
   is none left) and does not re-split anything.

## Invariants

1. **Rust `bin/` binary, not a shell script (POLICY 21).** The tool is delivered as a compiled
   Rust binary under `plugins/vsdd-factory/bin/` (or an equivalent Rust workspace crate producing
   such a binary), never as a new `.sh` script, satisfying `no_new_shell_scripts`.

2. **Idempotent on every subcommand.** Both migration and rotation are safe to re-run; neither
   subcommand's second invocation against an unchanged target produces a different result or a
   file mutation (see PC4; the same property extends to rotation — re-running rotation
   immediately after a successful rotation, before the threshold is exceeded again, is a
   verified-clean no-op). It further extends to the full-recovery split (PC7): once a chain has
   been split, re-running migration against the now-slim file is a PC4 no-op — it does not attempt
   to re-split, because there is no longer a chain to find.

3. **Safe on arbitrarily long input, including full-recovery split of a chain of any length.** The
   tool processes files up to and including the pre-migration mega-line scale (D-1149's
   323,499-char `STORY-INDEX.md` frontmatter scalar as the calibration ceiling, with the general
   class extending to ~350K chars) without unbounded memory growth or non-linear runtime blowup —
   via a bounded/streaming, single-linear-pass marker scan (PC7 step 7), never a repeated
   substring re-scan or a backtracking-regex implementation. It is the sanctioned mechanism
   precisely because Edit/Write-tool-mediated manual editing cannot safely handle content at this
   scale (per the D-1149 justification for the original one-time POL-3 exception) — and, as of
   this amendment, the tool actively SPLITS such content rather than merely tolerating or refusing
   it (superseding this BC's v1.0 framing, which scoped this invariant to read-only scale
   tolerance for a maintain-only tool).

4. **Every write the tool performs satisfies BC-5.45.001's write-path invariants — and an existing
   chain the tool encounters is always resolved, never left in place.** The tool is itself a
   conforming writer under BC-5.45.001 — its own migration/rotation/split writes never introduce a
   NEW inline `[Prior: ...]` bracket chain, and every value it emits parses under strict YAML
   `safe_load`. Per PC7, an EXISTING inline chain the tool encounters on input is always resolved
   into the current-entry-only-plus-`changelog:` shape in the same run; it is never left in place,
   never re-wrapped, and never deferred to a future ad hoc exception.

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | Migration invoked against a file already fully compliant with ADR-049 shape | Verified-clean report; zero mutations (PC4). |
| EC-002 | Migration invoked against `STORY-INDEX.md` for the first time (no `changelog:` field exists) | `changelog:` sequence added (initially empty, or seeded with the current `last_amended` entry as its first item per the migration's own initial-seed convention — tool documentation MUST state which); `last_amended` unchanged if already current-entry-only. |
| EC-003 | Migration invoked against a file whose `last_amended` contains an inline `[Prior: ...]` bracket chain (pre-D-1149 style, or any future recurrence), at any scale | **ELIGIBLE (v1.1 amendment — supersedes v1.0's refusal behavior).** The tool performs the PC7 full-recovery split: the current entry stays in `last_amended`; each chained entry becomes a new `changelog:` item, newest-first, verbatim (PC3-escaped); report states the file as SPLIT (distinct from both "already migrated" and "NotEligible") with a count of entries recovered. No POL-3/TD-FACTORY-HOOK-BYPASS-001 exception is invoked or needed (S-15.03 AC-010). |
| EC-004 | Rotation invoked against a file whose `changelog:` sequence is below the size threshold | No-op; tool reports current size vs. threshold and takes no action. |
| EC-005 | Rotation invoked with a `<cycle-name>` that does not yet exist under `.factory/cycles/` | Tool creates the target directory (consistent with how `burst-log.md`/`decision-log.md` archives are created per cycle) before writing the archive file. |
| EC-006 | Migration invoked against `STATE.md` | `changelog:` field is never added (PC1); only `last_amended` current-entry-only confirmation or full-recovery split (PC2/PC7) and, if applicable, D-1144 escape remediation (PC3) apply. `STATE.md` has no `changelog:` field, so a chain found on `STATE.md` is split with the recovered entries superseded by its body-level Decisions Log rather than relocated to a frontmatter sequence, per ADR-049 Decision 4 / BC-5.45.001 PC3. |
| EC-007 | A `*-amendment-history.md` sidecar file is missing or unreadable when the tool runs a reporting pass | Tool reports the missing sidecar as an informational finding but does not fail the migration/rotation/split operation on the file itself — sidecar presence is not a precondition for PC1-PC5/PC7 (PC6 governs write-avoidance, not read-availability). |
| EC-008 | Migration invoked against a file where `last_amended` cannot be located at all, or its raw value does not parse as a YAML scalar (e.g. a corrupted frontmatter delimiter) | The ONLY remaining `NotEligible` outcome (v1.1 amendment narrows this from "any inline chain" to genuinely-unparseable frontmatter). Tool reports the file as `NotEligible`, distinct from both "already migrated" and "SPLIT" (EC-003), and takes no mutating action. |
| EC-009 | Migration invoked against a file whose inline `[Prior:]` chain reaches or exceeds the D-1149 calibration scale (a single physical `last_amended` scalar of ~323K-350K chars, potentially dozens of chained entries) | The PC7 bounded/streaming linear scan completes the split without unbounded memory growth or non-linear runtime blowup — behavior is identical in kind to a short chain (EC-003), differing only in scan duration, which remains linear in input size (Invariant 3). This is the calibration case the "arbitrarily long input" requirement exists to cover. |

## Canonical Test Vectors

| Input | Expected Output | Category |
|-------|----------------|----------|
| Migrate `STORY-INDEX.md` (no `changelog:`, current-entry-only `last_amended`, no D-1144 defect) | `changelog:` sequence added; `last_amended` unchanged; report: 1 file mutated, 0 escape fixes | happy-path |
| Migrate `BC-INDEX.md` (has `changelog:`; `last_amended` has an unescaped `"` per D-1144) | `changelog:` unchanged; `last_amended` rewritten with the `"` escaped to `\"`; report: 1 file mutated, 1 escape fix | edge-case |
| Migrate a `STORY-INDEX.md` fixture with NO pre-existing `changelog:`, whose `last_amended` = current entry + 2 inline `[Prior: ...]` bracket entries | **(v1.1 — full-recovery split, replaces the v1.0 "never split" framing.)** `last_amended` becomes current-entry-only (the current entry, unchanged text); `changelog:` is CREATED and seeded with the 2 split entries in newest-first order, verbatim; report: 1 file mutated, 1 split (2 entries recovered) | edge-case |
| Migrate a `BC-INDEX.md` fixture with M pre-existing `changelog:` items, whose `last_amended` = current entry + 1 inline `[Prior: ...]` entry containing an unescaped `"` | `last_amended` becomes current-entry-only; `changelog:` gains 1 new item at position 0 with the `"` escaped to `\"`; the M pre-existing items are unchanged, byte-for-byte, now at positions 1..M; report: 1 file mutated, 1 split (1 entry recovered), 1 escape fix | edge-case (split + D-1144 escaping combined) |
| Migrate a fixture matching the D-1149 calibration scale (~323K-350K-char inline `[Prior:]` chain, N historical entries) | `last_amended` becomes current-entry-only; `changelog:` gains N new items, newest-first, verbatim; tool completes within bounded time/memory via the PC7 linear scan (no non-linear blowup); report: 1 file mutated, 1 split (N entries recovered) | edge-case (mega-line calibration) |
| Re-run migration on the now-slim, post-split file from any of the 3 split vectors above | 0 file mutations; verified-clean report (PC4/PC7 step-8 idempotency — no chain remains to re-split) | error (would be a false-positive re-split if the tool were not idempotent) |
| Migrate an already-fully-compliant file a second time | 0 file mutations; verified-clean report | error (would be a false-positive mutation if the tool were not idempotent) |

## Verification Properties

| VP-NNN | Property | Proof Method |
|--------|----------|-------------|
| [VP-111](../../verification-properties/VP-111.md) | Migration subcommand is idempotent, including immediately after a PC7 full-recovery split (PC4, Invariant 2) | unit-test: `crates/last-amended-migrate/tests/bc_10_13_001_pc4_idempotency_test.rs` — already-compliant no-op, post-bootstrap no-op, check-mode zero-mutation, and PC7-rerun-is-noop fixtures |
| [VP-112](../../verification-properties/VP-112.md) | Rotation preserves every `changelog:` item's text content verbatim across the move (PC5) | unit-test: `crates/last-amended-migrate/tests/bc_10_13_001_pc5_rotation_test.rs` — oldest-items-moved-verbatim, below-threshold no-op, missing-cycle-dir creation, rerun-is-noop, and check-mode-never-writes fixtures |
| [VP-113](../../verification-properties/VP-113.md) | D-1144 escape remediation produces strictly-valid YAML (PC3) | unit-test: `crates/last-amended-migrate/tests/bc_10_13_001_pc3_escape_test.rs` — `needs_escaping`/`escape_value` unit coverage plus a `migrate_file` end-to-end BC-INDEX.md-shaped quote-defect fixture and idempotent-re-escape fixture |
| [VP-109](../../verification-properties/VP-109.md) | Full-recovery split (PC7) recovers every chained entry with zero data loss and correct newest-first ordering | unit-test: `crates/last-amended-migrate/tests/bc_10_13_001_pc2_eligibility_test.rs` — 2-entry split-and-bootstrap, split+D-1144-escape-combo, pointer-note-is-not-a-chain negative control, and mega-line (EC-009) split fixtures |
| [VP-110](../../verification-properties/VP-110.md) | Full-recovery split completes in bounded time/memory for arbitrarily long input (linear-scan property, Invariant 3) | unit-test: `crates/last-amended-migrate/tests/bc_10_13_001_pc2_eligibility_test.rs` — `test_BC_10_13_001_invariant3_mega_line_eligibility_scan_is_bounded` (eligibility classification) and `test_BC_10_13_001_EC009_mega_line_file_migrate_file_splits_within_bounded_time` (end-to-end split) assert bounded wall-clock time against the D-1149 calibration-scale fixture; `split_tail_entries`'s single forward-scanning cursor (never re-scanning from index 0) is the O(n) structural guarantee `check_eligibility`'s single `str::contains` call relies on |

## Traceability

| Field | Value |
|-------|-------|
| L2 Capability | CAP-042 |
| Capability Anchor Justification | CAP-042 ("`last_amended` Write-Path Durable Fix: current-entry-only scalar, `changelog:` prepend discipline, sanctioned migration/rotation tooling, and bash-adapter fuel-budget relief") per `.factory/specs/domain-spec/capabilities.md` §CAP-042. BC-10.13.001 is the tooling-implementing BC for CAP-042 (BC-5.45.001 implements the write-path-invariant clause; BC-4.18.001 implements the fuel-relief clause). |
| L2 Domain Invariants | none (operational infrastructure) |
| Architecture Module | new Rust `bin/` binary under `plugins/vsdd-factory/bin/` (exact crate/binary name is an implementer decision at delivery time, per S-15.03 §Files Affected); output paths (and the existing D-1149 `*-amendment-history.md` sidecars) to be registered in `plugins/vsdd-factory/config/artifact-path-registry.yaml` as part of delivery (S-15.03 AC-006) |
| Stories | S-15.03 |
| Source Issues | D-1149; `L-BB-D1149`; STATE.md v9.64 Drift Item (a) — unregistered sidecar path; v1.1 human-directed FULL-RECOVERY/SPLIT amendment (2026-09-02), making the tool AC-010-complete |
| ADR Reference | ADR-049 §Decision 6, §Decision 7 |
| ADR-049 Alignment (v1.1) | Full-recovery split (PC7) is WITHIN ADR-049's ratified Option 2 design, not a departure from it — it produces the exact same current-entry-only-plus-`changelog:`-sequence canonical shape as every other write under BC-5.45.001/ADR-049 §Decision 1-2. It is a new INPUT path (recovering an existing inline-chain file) to an unchanged OUTPUT shape. **ADR-049 does NOT require re-ratification for this amendment** — no Decision, Rationale, or Consequences clause is contradicted, relaxed, or extended beyond what §Decision 1-2/6 already specifies for the migration tool's role. |

## Related BCs

- BC-5.45.001 — the write-path invariant this tool's migration output must conform to going
  forward (depends on)
- BC-4.18.001 — the fuel-budget-relief regression that this tool's migration is a prerequisite
  for (composes with)
- BC-10.01.001 — `compute-input-hash`, a sibling SS-10 CLI tool with a similar drift-detection/
  remediation shape; distinct concern (input-hash drift vs. `last_amended`/`changelog:` shape)
  (related to)

## Architecture Anchors

- `plugins/vsdd-factory/bin/` — POLICY 21-compliant home for the new binary.
- `plugins/vsdd-factory/config/artifact-path-registry.yaml` — registration target for the tool's
  own output paths and the 5 existing D-1149 sidecar paths (S-15.03 AC-006 delivery requirement).
- `.factory/cycles/<cycle-name>/` — rotation-archive destination pattern, mirroring
  `burst-log.md`/`decision-log.md` per-cycle archival.
- `.factory/specs/architecture/decisions/ADR-049-last-amended-write-path-durable-fix-current-entry-plus-changelog-sequence.md` — §Decision 6, §Decision 7.

## Story Anchor

S-15.03 (E-12 Engine Governance — `last_amended` Write-Path Durable Fix, Scope Extension
AC-006/AC-010)

## VP Anchors

VP-109 (PC7 split correctness/ordering), VP-110 (PC7/Invariant 3 bounded-scan linearity),
VP-111 (PC4 idempotency incl. PC7 rerun), VP-112 (PC5 rotation losslessness), VP-113
(PC3 D-1144 escape validity) — registered in VP-INDEX.md per
`vp_index_is_vp_catalog_source_of_truth` (architect; S-15.03 spec-package pre-PR VP
registration burst). All 5 cite real, already-passing
`crates/last-amended-migrate/tests/*.rs` unit tests.

## Changelog

| Version | Date | Description |
|---------|------|-------------|
| 1.3 | 2026-09-03 | **POL-14 auto-promotion** (state-manager): `lifecycle_status`/`status` draft→active on S-15.03 PR #805 squash-merge `b4ff2383` (2026-09-03T10:43:17Z) into develop. BC-INDEX v5.42→v5.43. No PC/Invariant/EC/test-vector substance change. D-1152. |
| 1.2 | 2026-09-02 | **VP registration (architect; S-15.03 pre-PR spec-package completion).** All 5 flagged VP candidates registered in VP-INDEX.md: PC7 split-correctness/ordering -> VP-109; PC7/Invariant 3 bounded-scan linearity -> VP-110; PC4 idempotency (incl. PC7 rerun) -> VP-111; PC5 rotation losslessness -> VP-112; PC3 D-1144 escape validity -> VP-113. Verification Properties table and VP Anchors section updated TBD -> VP-NNN; proof-method column corrected from aspirational proptest/benchmark framing to the actual implemented unit-test evidence in `crates/last-amended-migrate/tests/*.rs`. No PC/Invariant/EC/test-vector substance change. POLICY 9: verification-architecture.md + verification-coverage-matrix.md propagated same-burst. |
| 1.1 | 2026-09-02 | **FULL-RECOVERY / SPLIT capability added (product-owner; human-directed amendment).** Precondition 2 reframed: an inline `[Prior: ...]` chain is now ELIGIBLE for migration (split), not `NotEligible`; `NotEligible` narrowed to genuinely-unparseable frontmatter (new EC-008). New Postcondition PC7 (full-recovery split): splits at the first ` [Prior:` marker, current entry stays in `last_amended`, tail is parsed into individual entries (split on subsequent ` [Prior:` markers) and prepended newest-first as `changelog:` items, verbatim (PC3-escaped), via a bounded/streaming linear scan safe on arbitrarily long input (D-1149 323K-350K-char calibration ceiling). PC2/PC4/PC6 amended to cross-reference PC7 (split is also idempotent and never touches the frozen sidecars). Invariants 2-4 extended to cover split idempotency, bounded-scan safety at any chain length, and always-resolve-not-just-never-introduce chain semantics. EC-003 rewritten (split, not refusal); new EC-008 (malformed-frontmatter `NotEligible`) and EC-009 (mega-line calibration split). 3 new canonical test vectors (small split, split+escape combo, mega-line-scale split) plus a re-run idempotency vector, replacing the v1.0 "never split" framing. 2 new VP candidates (split correctness/ordering; bounded-scan linearity). This makes the tool a complete, robust replacement for D-1149's one-time POL-3 exception, satisfying S-15.03 AC-010 without qualification. ADR-049 alignment: the split's output is the same current-entry-only-plus-`changelog:` canonical shape ADR-049 §Decision 1-2 already ratifies (Option 2) — a new INPUT path to an unchanged OUTPUT shape; ADR-049 does not require re-ratification for this amendment. |
| 1.0 | 2026-09-02 | Initial authoring (product-owner; ADR-049 Phase B; S-15.03 AC-006). PC1 changelog-coverage completion; PC2 current-entry-only confirmation; PC3 D-1144 escape remediation on the 3 defective files; PC4 idempotency; PC5 lossless rotation; PC6 frozen-sidecar non-mutation. 4 invariants (POLICY 21 Rust bin/, idempotent, bounded-resource-safe, BC-5.45.001-conforming writer). 7 edge cases EC-001..EC-007. 3 test vectors. 3 VP candidates flagged for architect. lifecycle_status: draft. |
