---
document_type: behavioral-contract
level: L3
version: "1.0"
status: draft
producer: codebase-analyzer
timestamp: 2026-04-25T00:00:00
phase: "1.4b"
inputs:
  - .factory/specs/behavioral-contracts/bc-id-mapping.md
  - .factory/phase-0-ingestion/pass-3-deep-templates-tools-rules.md
input-hash: "9d62203"
traces_to: .factory/specs/behavioral-contracts/bc-id-mapping.md
origin: brownfield
extracted_from: "pass-3-deep-templates-tools-rules.md:1390"
subsystem: "SS-10"
capability: "TBD"
lifecycle_status: active
introduced: v1.0.0-beta.4
modified: []
deprecated: null
deprecated_by: null
replacement: null
retired: null
removed: null
removal_reason: null
---

# Behavioral Contract BC-10.05.002: factory-query: 6 subcommands (top, recent, grep, hooks, stats, reasons, help)

> Section: factory-query CLI
> Source BC (audit ID): BC-AUDIT-2124

## Description

`top` (top reason codes + hooks by count); `recent` (latest events); `grep <reason_code>` (filter by reason); `hooks` (block counts per hook); `stats` (aggregate block/warn/action counts + unique reasons + unique hooks); `reasons` (list all reason codes with counts).

## Preconditions

1. TBD — preconditions not explicitly stated in source pass-3 entry.

## Postconditions

1. `top` (top reason codes + hooks by count); `recent` (latest events); `grep <reason_code>` (filter by reason); `hooks` (block counts per hook); `stats` (aggregate block/warn/action counts + unique reasons + unique hooks); `reasons` (list all reason codes with counts).

## Invariants

1. All 6 subcommands functional.

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | TBD — Phase 1.6b will enumerate edge cases from source review | TBD |

## Canonical Test Vectors

> Test vectors will be pulled from the pinned test case in Phase 1.6b.

| Input | Expected Output | Category |
|-------|----------------|----------|
| TBD | TBD | happy-path |
| TBD | TBD | edge-case |
| TBD | TBD | error |

## Verification Properties

| VP-NNN | Property | Proof Method |
|--------|----------|-------------|
| VP-001 | `top` (top reason codes + hooks by count); `recent` (latest events); `grep <reason_code>` (filter by reason); `hooks` (b | manual (existing test: `plugins/vsdd-factory/bin/factory-query`) |

## Traceability

| Field | Value |
|-------|-------|
| L2 Capability | TBD (subsystem L2 spec pending) |
| L2 Domain Invariants | TBD |
| Architecture Module | SS-10 (factory-query CLI) |
| Stories | TBD (Phase 2 story-writer pass) |

## Related BCs (Recommended)

- TBD — cross-references will be filled in Phase 1.6b after all per-BC files exist.

## Architecture Anchors (Recommended)

- `architecture/SS-10-cli-tools.md` — section: factory-query CLI

---

### Brownfield-Specific Sections

> This BC was extracted during Phase 0d brownfield ingestion (BC-AUDIT pass) and migrated to canonical one-per-file BC-S.SS.NNN format in Phase 1.4b.

#### Source Evidence

| Property | Value |
|----------|-------|
| **Path** | `plugins/vsdd-factory/bin/factory-query` |
| **Source line(s)** | 7–14 (docstring), 342–349 (dispatch) |
| **Confidence** | high |
| **Original audit ID** | BC-AUDIT-2124 |
| **Pass-3 source** | `pass-3-deep-templates-tools-rules.md:1390` |
| **Extraction Date** | 2026-04-25 |
| **Used by** | Operator CLI / factory-dashboard |
| **Pinned test** | ``plugins/vsdd-factory/bin/factory-query`` |

#### Evidence Types Used

- **assertion**: pinned by Rust unit/integration test
- **documentation**: stated in source-file docstring or pass-3 narrative

#### Purity Classification

| Property | Assessment |
|----------|-----------|
| **I/O operations** | TBD — Phase 1.6b will classify |
| **Global state access** | TBD |
| **Deterministic** | TBD |
| **Thread safety** | TBD |
| **Overall classification** | TBD |

#### Refactoring Notes

TBD — Phase 1.6b will produce refactoring guidance.

