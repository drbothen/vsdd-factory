---
document_type: behavioral-contract
level: L3
version: "1.1"
status: draft
producer: product-owner
timestamp: 2026-05-03T00:00:00Z
last_amended: 2026-05-08
phase: 1.4b
inputs: [gap-analysis-w16-subprocess.md]
input-hash: "[pending-recompute]"
traces_to: gap-analysis-w16-subprocess.md
origin: brownfield
extracted_from: ".factory/architecture/gap-analysis-w16-subprocess.md:Section 5"
subsystem: "SS-01"
capability: "CAP-TBD"
lifecycle_status: active
introduced: v1.0.0
modified: [D-293-pass-50, D-295-pass-51, D-296-pass-52, D-305-pass-60]
deprecated: null
deprecated_by: null
replacement: null
retired: null
removed: null
removal_reason: null
---

# Behavioral Contract BC-1.05.036: factory-dispatcher::host::exec_subprocess::emits_completed_event_on_success — host.exec_subprocess.completed event on every successful subprocess completion

## Description

**ADR-015 Awareness (added per E-9 v1.7 Post-Audit Amendment, propagated to BC at v1.21 per TD-VSDD-074):**
The success-path event name `host.exec_subprocess.completed` SHOWN IN THIS BC IS INTERIM. Per ADR-015 D-15.2 reverse-DNS naming requirement, the canonical event name resolves to one of two binary options (per OQ-W16-001 in `.factory/specs/open-questions.md`):
- (a) If `vsdd.host.*` is added to ADR-015 D-15.2 registry → `vsdd.host.exec_subprocess.completed.v1` (category to be assigned per OQ-W16-001 acceptance criterion (a) — ADR-015 D-15.2 registry has 5 categories: lifecycle, domain, audit, error, unknown).
- (b) Otherwise → `vsdd.dispatcher.subprocess_completed.v1` (lifecycle category per existing `vsdd.dispatcher.*` registry entry; OQ-W16-001 acceptance criterion (b)).
S-9.07 implementer MUST close OQ-W16-001 before merging the host-emit-fix story.

On successful subprocess completion (any exit code, including non-zero), the dispatcher MUST emit a `host.exec_subprocess.completed` event through `ctx.emit_internal` to the single-stream `FileSink` per ADR-015 D-15.1 (multi-sink stanza model removed; Router/SinkRegistry retired). This closes an observability gap identified in gap-analysis-w16-subprocess.md Section 5: `exec_subprocess.rs::execute_bounded` (success-return block) currently has no emit call on success. The new event is success-path only; existing denial-path events are preserved unchanged.

## Preconditions

1. Plugin calls `vsdd::exec_subprocess`.
2. Capability check passes (binary is on allow-list, no traversal, no shell bypass violation).
3. Subprocess spawns and completes (any exit code).

## Postconditions

1. On successful subprocess termination (i.e., `child.wait()` returns `Ok(Some(status))` within timeout AND output cap; see Postcondition 5 for error-path reality and EC-009 for signal-death substitution), exactly one `host.exec_subprocess.completed` event is emitted via `ctx.emit_internal`. **Footnote on signal-death:** when `child.wait()` reports a status without an exit code (Unix signal termination), the dispatcher substitutes `exit_code = -1` per `status.code().unwrap_or(-1)` at `exec_subprocess.rs::execute_bounded`. See EC-009 for v1 known limitation; OQ-W16-002 tracks v2 disambiguation. **Footnote on best-effort emit:** if `internal_log.write_inner` encounters an IO failure (ENOSPC, missing directory, permissions), `write` emits an eprintln to stderr and returns `()`; the event is NOT silently dropped — it is logged to stderr AND still pushed to the in-memory `events` queue; see EC-010 and Postcondition 6.
2. Event payload includes all 8 fields: `{plugin_id: String, binary: String /* canonicalized full path */, args_count: u32, exit_code: i32, duration_ms: u64, stdout_bytes: u64, stderr_bytes: u64, truncated: bool /* reserved for future ABI break: always false in v1; truncation currently returns Err(OUTPUT_TOO_LARGE -3); see gap-analysis Section 5 'fundamentally insufficient' Gap 1 */}`. Per ADR-015 D-15.2:270, the host-enrichment layer additionally stamps an `outcome` enum field on this event using the canonical taxonomy (`success | failure | error | timeout | skipped | blocked`). For `host.exec_subprocess.completed` events the mapping is: `exit_code == 0 → 'success'`; `exit_code != 0 → 'failure'`. (TIMEOUT path emits no event per Postcondition 5; OUTPUT_TOO_LARGE and INTERNAL_ERROR likewise emit no event.) The `outcome` field is host-stamped per ADR-015 D-15.3, not part of the 8-field domain payload listed above. **`stdout_bytes`/`stderr_bytes` semantic:** defined as the count of bytes returned in the envelope AFTER any potential truncation — equal to the bytes encoded into the envelope. **v1 best-effort-read qualifier:** `stdout_bytes` and `stderr_bytes` reflect the number of bytes placed in the envelope buffer, which equals the bytes successfully read from the pipe via `stdout.read_to_end` and `stderr.read_to_end` at `exec_subprocess.rs::execute_bounded`. Both calls use `let _ = ...` which silently discards any `io::Result::Err`. If a kernel pipe IO error occurs mid-read (e.g., EIO on a tmpfs-backed pipe or disk-backed swap failure), the read halts with a partial buffer and returns `Ok(partial_bytes_read)` or an `Err` that is discarded — in either case `stdout_buf` may contain fewer bytes than the subprocess actually wrote to its stdout pipe. The emitted `stdout_bytes`/`stderr_bytes` counts therefore represent a **best-effort read** of pipe output, not a guaranteed-complete count. A silent pipe IO error is indistinguishable from a subprocess that wrote fewer bytes. The `outcome` field is stamped `'success'` regardless of whether the read was complete. See §Edge Cases EC-015 and OQ-W16-009. At any future ABI break that introduces a real `truncated: true` semantic, `stdout_bytes` MAY exceed `max_output_bytes` if pre-truncate counting is adopted; until then the field's invariant is `stdout_bytes ≤ max_output_bytes` (where the count is subject to best-effort-read semantics above). Same definition applies to `stderr_bytes`. See EC-006 cross-ref.
3. `duration_ms` is measured from `Instant::now()` at `Command::spawn()` to process exit; implementer adds a `let started = Instant::now();` capture immediately before `command.spawn()` at `exec_subprocess.rs::execute_bounded` (which is the actual spawn point); the existing deadline `Instant` at `exec_subprocess.rs::execute_bounded` (deadline assignment) is post-spawn and is NOT the duration reference.
4. Event is emitted via `ctx.emit_internal` (same code path as the existing four `emit_denial` calls). The `emit_internal` implementation at `host/mod.rs::HostContext::emit_internal` has a bifurcation: **If `ctx.internal_log.is_some()`** (production path — `InternalLog` wired by dispatcher main), `log.write(&event)` is called first, writing to `dispatcher-internal-*.jsonl`; this is INTERIM per ADR-015 D-15.1 (migration target: single-stream FileSink to `events-*.jsonl`). **If `ctx.internal_log` is `None`** (test-context path — `HostContext::new` at `host/mod.rs::HostContext::new` sets `internal_log: None`), the `log.write` call is SKIPPED entirely. In both cases, `events.lock().push(event)` at `host/mod.rs::HostContext::emit_internal` runs independently and the event is pushed to the in-memory `events` queue regardless of whether `internal_log` is wired. Tests using `HostContext::new` therefore only observe the event through the `events` queue (via `drain_events`), not through any file on disk. ADR-015 D-15.1 mandates the implementation be migrated to write through the single-stream `FileSink` to `events-*.jsonl`; BC-1.05.036's implementation MUST include rewiring `emit_internal` to the single-stream FileSink BEFORE the success-path event is consumer-visible per ADR-015 D-15.1. Track ADR-015 routing migration as part of E-9 Wave 1 implementation scope (see E-9 epic frontmatter). **NOTE (ADR-015 FileSink rewire CTV gap, LOW-P52-001):** The Canonical Test Vectors in this BC witness the **post-rewire spec-frame state** — i.e., the state in which the consumer reads events from `events-*.jsonl` via the single-stream FileSink per ADR-015 D-15.1. The **pre-rewire INTERIM state** — in which the event lands in `dispatcher-internal-*.jsonl` (not `events-*.jsonl`) — is acknowledged here but is NOT separately CTV-witnessed in this BC; CTV coverage of the INTERIM routing dichotomy ("event present in `dispatcher-internal-*.jsonl` but absent from `events-*.jsonl`") is deferred to E-9 Wave 1 implementation completion. Cross-reference: OQ-W16-001 (ADR-015 D-15.2 reverse-DNS naming resolution) and the INTERIM caveat above.
5. **Error-path event reality (per gap-analysis §1):** Once BC-1.05.035 Postcondition 3 is implemented, `internal.capability_denied` is emitted on 5 denial paths: the 4 pre-existing source paths (`no_exec_subprocess_capability` per `exec_subprocess.rs::run` (no-capability arm), `binary_not_on_allow_list` per `exec_subprocess.rs::run` (allow-list miss arm), `shell_bypass_not_acknowledged` per `exec_subprocess.rs::run` (shell-bypass arm), `setuid_or_setgid_binary` per `exec_subprocess.rs::run` (setuid arm)) PLUS the new `binary_canonicalize_failed` path inserted before the `binary_allowed` call per BC-1.05.035 Postcondition 3. **In current source (pre-S-9.07 merge):** only the 4 paths in `exec_subprocess.rs::run` exist; `binary_canonicalize_failed` is not yet implemented. This BC specifies the post-S-9.07 spec-frame state (5 denial paths) consistent with EC-003. Note: `env_allow` names absent from `ctx.env_view` are silently omitted (no event; see EC-014) and `cwd_allow` is currently unenforced (no denial event for cwd outside the allow-list; see EC-012 and OQ-W16-007). TIMEOUT (-2) and OUTPUT_TOO_LARGE (-3) paths return error codes WITHOUT emitting any event (two distinct OUTPUT_TOO_LARGE paths exist: EC-005A subprocess-output-overflow at `exec_subprocess.rs::execute_bounded` (truncated check) and EC-005B result_buf_cap-overflow at `exec_subprocess.rs::register` (envelope-length check) — both return -3 and emit no event). The success-path event introduced by this BC closes the success-path observability gap. Adding TIMEOUT/OUTPUT_TOO_LARGE error-path events is OUT OF SCOPE for this BC and may be tracked in a future OQ if needed. Additionally, INTERNAL_ERROR (-99) paths return error codes WITHOUT emitting any event: spawn failure (`exec_subprocess.rs::execute_bounded` spawn call), stdin take/write failure (`exec_subprocess.rs::execute_bounded` stdin-take and write_all arms), stdout/stderr take failure (`exec_subprocess.rs::execute_bounded` stdout/stderr-take arms), try_wait error (`exec_subprocess.rs::execute_bounded` Err(_) arm). `INTERNAL_ERROR` constant defined at `crates/factory-dispatcher/src/host/mod.rs::codes::INTERNAL_ERROR` as `i32 = -99`. All three no-event error paths (TIMEOUT, OUTPUT_TOO_LARGE, INTERNAL_ERROR) are out-of-scope for this BC's success-path event contract. **TIMEOUT enforcement scope footnote (v1 known limitation):** TIMEOUT enforcement covers the deadline check at `exec_subprocess.rs::execute_bounded` (deadline check) only (`if Instant::now() >= deadline`). Once the deadline fires, the cleanup sequence at `exec_subprocess.rs::execute_bounded` (kill+wait on TIMEOUT) calls `let _ = child.kill()` and `let _ = child.wait()` — both discard their `io::Result` via `let _ =`. If `child.kill()` returns `Err` (e.g., child already exited, kernel error, or permission denied) AND `child.wait()` subsequently blocks (e.g., child in NFS D-state or kernel-level uninterruptible wait), the `execute_bounded` function hangs inside `child.wait()` with no secondary deadline. The host call never returns to the wasm caller; no TIMEOUT (-2) is reported. The same `let _ = child.kill(); let _ = child.wait()` pattern applies to the stdin-fail branch at `exec_subprocess.rs::execute_bounded` (kill+wait on stdin fail). Both are v1 known limitations. See §Edge Cases EC-016 and OQ-W16-010.
6. **Best-effort emit semantics (internal_log path):** `internal_log.write(&event)` has return type `()` — it NEVER returns an error to its caller (`emit_internal` at `host/mod.rs::HostContext::emit_internal`). On internal IO failure, `write_inner` returns `std::io::Result<()>` (`internal_log.rs::InternalLog::write_inner`); `write` (`internal_log.rs::InternalLog::write`) swallows the `Err` via `eprintln!("factory-dispatcher: internal_log write failed ({}): {}", event.type_, e)` to stderr and returns `()`. The `emit_internal` call site at `host/mod.rs::HostContext::emit_internal` therefore sees no error regardless of IO outcome. Separately, `events.lock().push(event)` at `host/mod.rs::HostContext::emit_internal` is an independent step — it runs whether or not the internal_log write succeeded. The host call's return value (`Ok(envelope)` or `Err(code)`) is NOT affected by emit-side IO failure. **v1 known limitation:** Internal_log IO failures are reported to stderr (not silent) but are not propagated to callers, not retried, and not exposed via fallback counter. Track in **OQ-W16-003** if observability of emit-side failures becomes a security requirement. See EC-010. (Witnessed by Test Vector row 11 — best-effort-emit-witness — and EC-010.)

**Input bounds (implicit):** `args_len`, `stdin_len`, and total argv+envp size are NOT explicitly validated by this BC. Bounds enforcement is implicit: (a) `read_wasm_bytes` at `host/memory.rs::read_wasm_bytes` returns `HostCallError::OutOfBounds` for `len` exceeding guest memory size; the caller at `exec_subprocess.rs::register` (args-reading block) maps the error to `codes::INVALID_ARGUMENT` (-4); (b) `command.spawn()` at `exec_subprocess.rs::execute_bounded` returns Err on kernel-level limits (e.g., ARG_MAX exceedance) → INTERNAL_ERROR (-99). No pre-spawn argv-length check is performed.

## Invariants

1. Success-path telemetry is emitted for every subprocess completion regardless of the subprocess's own exit code (exit code 0 and non-zero both trigger the event).

## Related BCs

- BC-1.05.001 — exec_subprocess capability check (depends on: this event fires only after the capability check passes)
- BC-1.05.032 — timeout enforcement (sibling: timeout path returns `Err(TIMEOUT -2)` and emits NO event in v1; future error-path emit is out-of-scope per BC-1.05.036 Postcondition 5)
- BC-1.05.005 — OUTPUT_TOO_LARGE path (sibling: output-too-large path returns `Err(OUTPUT_TOO_LARGE -3)` and emits NO event in v1; aligns with BC-1.05.036 Postcondition 5; NOTE: two distinct OUTPUT_TOO_LARGE paths exist — EC-005A subprocess-output-overflow at `exec_subprocess.rs::execute_bounded` (truncated check) and EC-005B result_buf_cap-overflow at `exec_subprocess.rs::register` (envelope-length check))
- BC-1.05.035 — path canonicalization guard (sibling extension from same gap analysis). NOTE: BC-1.05.035 adds canonicalization for TOCTOU prevention. Symlink-resolved targets that fall outside `binary_allow` reach the existing CAPABILITY_DENIED path via emit_denial('binary_not_on_allow_list'). No novel error-code pairing introduced. All denial paths (including symlink-miss) return CAPABILITY_DENIED (-1), consistent with the 4 existing denial paths. Cross-dependency: BC-1.05.036's success-path event payload `binary` field (per Postcondition 2: `binary: String /* canonicalized full path */`) depends on BC-1.05.035 Postcondition 1 propagating the canonical path through to `Command::new(...)` at line 230. (NOTE: EC-006 lists field types but does not carry the 'canonicalized full path' annotation; Postcondition 2 is the canonical schema source-of-truth for that semantic.) If BC-1.05.035 Postcondition 1 is misimplemented (canonical at allow-check only, raw at spawn), BC-1.05.036's `binary` field will be the raw cmd — a contract violation in BC-1.05.036.

## Architecture Anchors

- `crates/factory-dispatcher/src/host/exec_subprocess.rs::execute_bounded` (success-return block) — current success path with no emit call; `emit_event!("host.exec_subprocess.completed", ...)` added here
- `crates/factory-dispatcher/src/host/exec_subprocess.rs::run` (allow-list miss arm) — representative existing `emit_denial(ctx, cmd, "binary_not_on_allow_list", ...)` call site (four denial call sites exist in `run`; new emit-on-success call follows the same `ctx.emit_internal(...)` pattern). The fn definition is at `exec_subprocess.rs::emit_denial` for reference.
- `.factory/architecture/gap-analysis-w16-subprocess.md` Section 5 — authority for this extension

## Story Anchor

S-9.07 (validate-wave-gate-prerequisite WASM port) — implementation task

## VP Anchors

(TBD — to be assigned in Phase 1.6b)

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | Subprocess exits 0 | `host.exec_subprocess.completed` emitted with `exit_code=0` |
| EC-002 | Subprocess exits non-zero (e.g., 1) | `host.exec_subprocess.completed` emitted with `exit_code=1` |
| EC-003 | Capability check fails (one of: `no_exec_subprocess_capability`, `binary_not_on_allow_list`, `shell_bypass_not_acknowledged`, `setuid_or_setgid_binary` per `exec_subprocess.rs::run` (four denial arms); OR `binary_canonicalize_failed` per BC-1.05.035 Postcondition 3 NEW path inserted before the `binary_allowed` call; env_allow + cwd_allow violations do NOT trigger this EC) | `internal.capability_denied` emitted; `host.exec_subprocess.completed` NOT emitted |
| EC-004 | Subprocess times out | Returns `Err(TIMEOUT -2)`; **NO event emitted in v1** (per Postcondition 5; future error-path emit is out-of-scope); `host.exec_subprocess.completed` NOT emitted |
| EC-005A | Subprocess stdout or stderr output **strictly exceeds** (`>`, not `>=`) `max_output_bytes` (`exec_subprocess.rs::execute_bounded` truncated check): after subprocess exits, `stdout_buf.len() > max_output_bytes as usize OR stderr_buf.len() > max_output_bytes as usize` per source — the `truncated` flag fires ONLY when `len > max_output_bytes`; the boundary case `len == max_output_bytes` is the SUCCESS path (no truncation; see boundary CTV row "stdout_buf.len() == max_output_bytes"). Source-of-truth operator: strict `>` in `exec_subprocess.rs::execute_bounded`. | Returns `Err(OUTPUT_TOO_LARGE -3)`; **NO event emitted in v1** (per Postcondition 5; future error-path emit is out-of-scope); `host.exec_subprocess.completed` NOT emitted. This is the subprocess-output-overflow path. [error] |
| EC-005B | Encoded result envelope size exceeds `result_buf_cap` (`exec_subprocess.rs::register` envelope-length check): after subprocess completes and output is within cap, `encode_envelope(exit_code, stdout, stderr).len() > result_buf_cap` — envelope too large for the guest-provided result buffer | Returns `Err(OUTPUT_TOO_LARGE -3)` (same code as EC-005A); **NO event emitted in v1** (per Postcondition 5); `host.exec_subprocess.completed` NOT emitted. Distinct from EC-005A: subprocess output WAS within `max_output_bytes`, but the encoded envelope (12-byte header overhead + stdout + stderr) does not fit in the guest's `result_buf_cap`. **Operator note:** plugins MUST size result buffers to accommodate at least `stdout_len + stderr_len + 12` bytes — the `encode_envelope` overhead at `exec_subprocess.rs::encode_envelope` is exactly 12 bytes (4 bytes exit_code + 4 bytes stdout_len + 4 bytes stderr_len). Sized correctly: `result_buf_cap >= max_output_bytes * 2 + 12`. [error] |
| EC-006 | Payload field type check | All 8 fields present with declared types (`plugin_id: String`, `binary: String`, `args_count: u32`, `exit_code: i32`, `duration_ms: u64`, `stdout_bytes: u64`, `stderr_bytes: u64`, `truncated: bool /* reserved for future ABI break: always false in v1; truncation currently returns Err(OUTPUT_TOO_LARGE -3); see gap-analysis Section 5 'fundamentally insufficient' Gap 1 */`). `stdout_bytes`/`stderr_bytes` semantic in v1: post-truncate (envelope bytes); see Postcondition 2 field description. |
| EC-007 | Subprocess spawn fails / pipe take/write fails / try_wait error. **Spawn io::Error cause erasure (MED-P50-001):** at `exec_subprocess.rs::execute_bounded` (spawn call), `command.spawn().map_err(\|_\| codes::INTERNAL_ERROR)?` discards the `io::Error` type via the `\|_\|` closure before returning INTERNAL_ERROR (-99). Kernel-level spawn failure causes — ENOENT (rare post-canonicalize, e.g., binary removed between canonicalize and spawn), EACCES (permission race), ETXTBSY (binary being written), ENOMEM (out of memory), EAGAIN (process limit) — all collapse to INTERNAL_ERROR (-99) with no diagnostic. The io::Error type, message, and OS error code are discarded. **v1 known limitation:** no pre-spawn `is_file()` check or error classification is performed. Note: the most common prior ENOENT case (missing binary at spawn time) is now intercepted earlier by BC-1.05.035 Postcondition 3 (`Path::canonicalize` fails → `CAPABILITY_DENIED` -1 with `binary_canonicalize_failed` emit), reducing the collision surface for this erasure — but a TOCTOU-narrow window between canonicalize success and spawn remains for ENOENT, and EACCES/ETXTBSY/ENOMEM/EAGAIN are unaffected. **Stdin write_all cause erasure (LOW-P51-003):** at `exec_subprocess.rs::execute_bounded` (write_all arm), `if child_stdin.write_all(stdin_bytes).is_err()` converts the `Result<(), io::Error>` to a plain `bool`, discarding the `io::Error` entirely — this is **more lossy than the spawn `map_err(\|_\|)` pattern** (which at least preserves the Err variant for closure-chaining), since `.is_err()` collapses the Result to a boolean with no access to the original error at all. Kernel-level stdin write failure causes — EPIPE (child closed its stdin end / broken pipe before reading, e.g., child exited early), ENOSPC (write buffer exhausted on certain kernel configurations), kernel pipe buffer exhaustion — all collapse to INTERNAL_ERROR (-99) with no diagnostic. The io::Error type (BrokenPipe, WriteZero, etc.), the OS error code, and the error message are all discarded. **v1 known limitation.** (Parallel to MED-P50-001 spawn cause erasure; analogous silent-cause-collapse pattern per TD-VSDD-092 BC-SOUL4-coverage.) **try_wait Err cause erasure (LOW-P51-004):** at `exec_subprocess.rs::execute_bounded` (Err(_) arm), `Err(_) => return Err(codes::INTERNAL_ERROR)` discards the `io::Error` from `child.try_wait()` before returning INTERNAL_ERROR (-99). Kernel-level wait failure causes — ECHILD (no such child process, e.g., child already reaped by another thread or signal handler), EINTR (system call interrupted by a signal), kernel-level wait failures — all collapse to INTERNAL_ERROR (-99) with no diagnostic. The io::Error type, message, and OS error code are discarded. **v1 known limitation.** (Parallel to MED-P50-001 spawn cause erasure; same silent-cause-collapse pattern per TD-VSDD-092 BC-SOUL4-coverage.) | Returns `Err(INTERNAL_ERROR -99)`; **NO event emitted in v1** (per Postcondition 5; spawn, stdin take/write, stdout/stderr take, and try_wait failure arms all in `exec_subprocess.rs::execute_bounded`); `host.exec_subprocess.completed` NOT emitted |
| EC-008 | Outcome enum stamping | exit_code=0 → host stamps `outcome='success'` per ADR-015 D-15.3 (mapping per Postcondition 2); exit_code≠0 → `outcome='failure'`. TIMEOUT/OUTPUT_TOO_LARGE/INTERNAL_ERROR paths emit no event so no outcome stamping applies. |
| EC-009 | Subprocess terminated by signal (kernel SIGKILL/SIGSEGV/SIGINT/external `kill -9`) — `status.code()` returns `None`; no exit code | `host.exec_subprocess.completed` IS emitted with `exit_code = -1` (from `status.code().unwrap_or(-1)` in `exec_subprocess.rs::execute_bounded`), `outcome = 'failure'`. **v1 known limitation:** `exit_code = -1` is indistinguishable from a literal `_exit(-1)` from C — signal-vs-exit collision is NOT disambiguated in v1. **Future:** track in **OQ-W16-002** if security observability needs to distinguish SIGSEGV (potential exploit) from SIGKILL (OOM-killer) from real exit `-1`. POSIX 128+signum convention (e.g., 139 for SIGSEGV) considered for v2 ABI break. [known-limitation] |
| EC-010 | `internal_log.write_inner` encounters IO error (e.g., log directory missing, ENOSPC, permissions denied): `write_inner` returns `Err(io::Error)`; `write` (`internal_log.rs::InternalLog::write`) catches it via `if let Err(e) = self.write_inner(event)` and emits `eprintln!("factory-dispatcher: internal_log write failed ({}): {}", event.type_, e)` to stderr, then returns `()`. The `events.lock().push(event)` at `host/mod.rs::HostContext::emit_internal` runs independently and SUCCEEDS (in-memory queue is unaffected by internal_log IO failure). Host call return value unaffected (still `Ok(envelope)` for success path). **Test-harness note:** to exercise this path, tests MUST set `ctx.internal_log = Some(Arc::new(InternalLog::new(path_that_will_fail_io)))` — e.g., a directory that does not exist and cannot be created, or a read-only filesystem path. Tests using `HostContext::new` (which sets `internal_log: None`) do NOT exercise this path; they skip `log.write` entirely (see Postcondition 4 bifurcation). Postcondition 1 "exactly one event emitted" is best-effort: emitted-to-`emit_internal`-API, NOT guaranteed-delivered-to-`dispatcher-internal-*.jsonl`. See Postcondition 6 and **OQ-W16-003**. [best-effort-emit-witness] |
| EC-011 | `events: Mutex<Vec<InternalEvent>>` is poisoned by panic in another concurrent host call | `emit_internal` silently drops (`host/mod.rs::HostContext::emit_internal` `if let Ok(mut events) = self.events.lock()` arm); subsequent `drain_events` (`host/mod.rs::HostContext::drain_events` `.expect("events mutex poisoned")`) panics dispatcher. Asymmetric write-vs-read poison handling. Track in **OQ-W16-004**. **emit_internal poison vs internal_log IO asymmetry (LOW-P51-006):** The two failure modes on the write side of `emit_internal` are handled asymmetrically: (a) **`events` Mutex poison (this EC):** the `if let Ok(mut events) = self.events.lock()` arm in `host/mod.rs::HostContext::emit_internal` silently drops the event with **no eprintln, no stderr fallback, and no observable signal** — the event is lost without any indication; (b) **`internal_log.write` IO failure (EC-010):** `write` at `internal_log.rs::InternalLog::write` explicitly catches the `Err` from `write_inner` and emits `eprintln!("factory-dispatcher: internal_log write failed ({}): {}", event.type_, e)` to stderr before returning. The asymmetry means: an IO failure on the file-backed log path IS observable (stderr output), while a Mutex poison on the in-memory events queue is **completely silent** — the calling host function returns `Ok(envelope)` as if nothing failed. A monitoring system watching stderr will see log-write failures but will not see any signal for poison-triggered drops. [known-limitation] |
| EC-012 | `caps.cwd_allow` is populated (e.g., `cwd_allow = ["/tmp/safe"]`) but subprocess `cwd` is set to a path NOT in `cwd_allow` (e.g., `ctx.cwd = "/etc"`) | **v1 known limitation:** `cwd_allow` is declared in `ExecSubprocessCaps` (`registry.rs::ExecSubprocessCaps::cwd_allow`) but NOT enforced in `execute_bounded` (`exec_subprocess.rs::execute_bounded` cwd block uses `ctx.cwd` directly — `if !cwd.as_os_str().is_empty() { command.current_dir(cwd); }` — without consulting `caps.cwd_allow` at all). Subprocess `cwd` is `ctx.cwd` regardless of `cwd_allow` content. No denial event is emitted for a cwd outside the allow-list. Track enforcement decision in **OQ-W16-007** (v1: no-op as documented; v2 candidate: enforce with `cwd_not_on_allow_list` denial reason). [known-limitation] |
| EC-013A | `timeout_ms = 0` or small `timeout_ms` (e.g., `timeout_ms = 1`); **symmetric upper bound: `timeout_ms = u32::MAX`** | **Lower-bound (zero/small):** Deadline is set to `Instant::now() + Duration::from_millis(0)` at `exec_subprocess.rs::execute_bounded` (deadline assignment). On the first poll iteration at the `if Instant::now() >= deadline` check (`exec_subprocess.rs::execute_bounded` deadline check), the deadline has already expired (or expires within the same microsecond); subprocess is killed via `child.kill()` and `child.wait()` at `exec_subprocess.rs::execute_bounded` (kill+wait on TIMEOUT); returns `Err(TIMEOUT -2)`. No subprocess output is collected. **v1 has no 'unlimited timeout' mode** — `timeout_ms = 0` means "kill immediately", not "no timeout". **5ms busy-poll granularity (LOW-P51-005):** The polling loop at `exec_subprocess.rs::execute_bounded` (thread::sleep) sleeps `std::thread::sleep(Duration::from_millis(5))` on each `Ok(None)` (child still running) iteration before rechecking the deadline. This hardcoded 5ms interval imposes a **minimum effective timeout granularity** of approximately 5ms for any nonzero `timeout_ms` value. A caller specifying `timeout_ms = 1` will not observe a timeout at 1ms — the first deadline check fires only AFTER the first 5ms sleep completes. The effective timeout is therefore `max(timeout_ms, ~5ms)` in the running-child case. More precisely: the deadline check in `exec_subprocess.rs::execute_bounded` runs before each sleep, so a subprocess that exits naturally within the first poll cycle will be reaped correctly; only the kill-on-timeout path is affected by the 5ms floor. The 5ms value is not configurable in v1. Track in **OQ-W16-010** if sub-5ms timeout precision becomes a requirement. **Upper-bound (`timeout_ms = u32::MAX`, LOW-P52-002):** At `exec_subprocess.rs::execute_bounded` (deadline assignment), `Duration::from_millis(timeout_ms as u64)` accepts `u32::MAX` (4,294,967,295) cast to `u64` without overflow. This produces a deadline approximately 49.7 days in the future (`u32::MAX ms ÷ 86,400,000 ms/day ≈ 49.71 days`). The subprocess is effectively unbounded by the timeout mechanism; it will run until it exits naturally, hits the `max_output_bytes` cap, or the host process is killed. **v1 known limitation:** there is no maximum timeout cap enforced by the dispatcher — the operator allow-list (which governs which plugins can call `exec_subprocess` at all) is the only policy gate for preventing excessively long-running subprocesses. A plugin specifying `timeout_ms = u32::MAX` does not cause an error; it simply sets a ~49.7-day deadline. OS process limits (e.g., `ulimit -t`) may independently bound CPU time. [edge-case, known-limitation] |
| EC-013B | `max_output_bytes = 0` | After subprocess exits, `stdout_buf.len() > 0 as usize` is true if subprocess produces ANY output (even 1 byte); `truncated = true` fires immediately; returns `Err(OUTPUT_TOO_LARGE -3)`. If subprocess produces exactly zero bytes of stdout AND zero bytes of stderr, `truncated = false` and the success path proceeds with empty buffers. **v1 effectively means 'any output → OUTPUT_TOO_LARGE'** when `max_output_bytes = 0`. [edge-case] |
| EC-014 | `caps.env_allow` lists a variable name (e.g., `"MY_VAR"`) that is absent from `ctx.env_view` | At `exec_subprocess.rs::execute_bounded` (env_allow loop): `for name in &caps.env_allow { if let Some(val) = env_view.get(name) { command.env(name, val); } }` — the name is silently omitted from the subprocess environment; no event emitted; no error returned. Subprocess does NOT receive `MY_VAR`. **v1 best-effort env-forwarding:** a plugin cannot distinguish "name was set to empty value in dispatcher env" from "name was absent from dispatcher env". Track alongside `cwd_allow` enforcement decision in **OQ-W16-007**. [known-limitation] |
| EC-015 | Kernel pipe IO error occurs during `stdout.read_to_end(&mut stdout_buf)` or `stderr.read_to_end(&mut stderr_buf)` at `exec_subprocess.rs::execute_bounded` (read_to_end calls) (e.g., EIO on a tmpfs-backed pipe, disk-backed swap failure, or kernel pipe buffer corruption) — `read_to_end` returns `Err(io::Error)` after reading partial bytes | **v1 known limitation — silent pipe IO error swallow:** Both calls use `let _ = stdout.read_to_end(...)` and `let _ = stderr.read_to_end(...)` which discard the `io::Result` regardless of outcome. If `read_to_end` returns `Err` after reading N partial bytes, `stdout_buf` contains those N bytes. Execution continues: the `truncated` check at `exec_subprocess.rs::execute_bounded` (truncated check) evaluates against the partial buffer (`stdout_buf.len() > max_output_bytes`). If N < `max_output_bytes`, `truncated = false`; the success path proceeds; `encode_envelope` is called with the partial buffer; `host.exec_subprocess.completed` is emitted with `stdout_bytes = N` (under-counted) and `outcome = 'success'`. The plugin caller cannot distinguish a complete read from an IO-truncated read — both produce `outcome='success'` with differing byte counts. The emitted `stdout_bytes`/`stderr_bytes` values are therefore best-effort counts, not guaranteed-complete counts (see Postcondition 2 best-effort-read qualifier). Track in **OQ-W16-009**. [known-limitation] |
| EC-016 | `child.kill()` or `child.wait()` fails after TIMEOUT deadline fires (`exec_subprocess.rs::execute_bounded` kill+wait on TIMEOUT) or after stdin write failure (`exec_subprocess.rs::execute_bounded` kill+wait on stdin fail) — specifically: `child.kill()` returns `Err` (e.g., child already exited, kernel error) AND `child.wait()` subsequently blocks (e.g., child in NFS D-state or kernel-level uninterruptible wait) | **v1 known limitation — cleanup-phase hang with no secondary deadline:** Both cleanup sequences (`let _ = child.kill(); let _ = child.wait()` in `exec_subprocess.rs::execute_bounded` TIMEOUT and stdin-fail cleanup branches) discard their `io::Result` via `let _ =`. If `child.kill()` returns `Err` AND `child.wait()` blocks indefinitely, `execute_bounded` hangs inside `child.wait()` with no secondary timeout. The host call never returns to the wasm caller; no error code is delivered; no event is emitted. TIMEOUT enforcement (Postcondition 5 footnote) covers the deadline check in `exec_subprocess.rs::execute_bounded` only — post-TIMEOUT cleanup has no secondary deadline. Same hazard applies to the stdin-fail branch. Track in **OQ-W16-010**. [known-limitation] |

## Canonical Test Vectors

| Input | Expected Output | Category |
|-------|----------------|----------|
| Capability passes; subprocess exits 0 | Exactly one `host.exec_subprocess.completed` event; `exit_code=0` | happy-path |
| Capability passes; subprocess exits 1 | Exactly one `host.exec_subprocess.completed` event; `exit_code=1` | happy-path |
| Outcome enum (success): exit_code=0 invocation | Exactly one event; payload exit_code=0; host-stamped outcome='success' (per Postcondition 2 mapping; ADR-015 D-15.3 enrichment) | host-stamping |
| Outcome enum (failure): exit_code=1 invocation | Exactly one event; payload exit_code=1; host-stamped outcome='failure' | host-stamping |
| Capability check fails | `internal.capability_denied` emitted; `host.exec_subprocess.completed` NOT emitted | error |
| Subprocess timeout | Returns `Err(TIMEOUT -2)`; NO event emitted in v1 per Postcondition 5; `host.exec_subprocess.completed` NOT emitted | error |
| Subprocess output exceeds `max_output_bytes` (EC-005A path: stdout or stderr > max_output_bytes after subprocess exits; `exec_subprocess.rs::execute_bounded` truncated check) | Returns `Err(OUTPUT_TOO_LARGE -3)`; **NO event emitted in v1** per Postcondition 5; `host.exec_subprocess.completed` NOT emitted | error |
| **Boundary case (EC-005A strict-`>` boundary, MED-P52-001):** `stdout_buf.len() == max_output_bytes` exactly (e.g., `max_output_bytes = 1024`; subprocess writes exactly 1024 bytes of stdout; stderr empty); capability passes; subprocess exits 0 | At `exec_subprocess.rs::execute_bounded` (truncated check): `stdout_buf.len() > max_output_bytes as usize` evaluates to `1024 > 1024 = false`; `truncated = false`; success path proceeds; `encode_envelope` called; exactly one `host.exec_subprocess.completed` event emitted with `stdout_bytes = 1024` (= `max_output_bytes`), `outcome = 'success'`. Returns `Ok(envelope)`. **No `OUTPUT_TOO_LARGE` error.** Witnesses EC-005A strict-`>` boundary: `len == max_output_bytes` is NOT a truncation trigger per source in `exec_subprocess.rs::execute_bounded`. | boundary-success-witness [MED-P52-001] |
| Subprocess spawn fails (or pipe take/write fails or try_wait error) | Returns `Err(INTERNAL_ERROR -99)`; NO event emitted in v1 per Postcondition 5 (all failure paths in `exec_subprocess.rs::execute_bounded`); `host.exec_subprocess.completed` NOT emitted. **By-design cause-collapse NOTE (LOW-P60-001 / TD-VSDD-092 v1 known-limitation):** This single TV row witnesses the OUTCOME (INTERNAL_ERROR -99 + no event emission) for ALL 4 cause-erasure paths enumerated in EC-007: spawn call (`map_err(\|_\|)` per MED-P50-001), stdin write_all arm (`.is_err()` per LOW-P51-003), try_wait Err(_) arm (per LOW-P51-004), and stdout/stderr pipe take arms — all in `exec_subprocess.rs::execute_bounded`. The 4 paths produce IDENTICAL observable outcomes (no test caller can distinguish them); per TD-VSDD-092 BC-SOUL4-coverage v1 known-limitation framing, the collapsed witness is intentional. Mock-injection-required individual-path witnesses are deferred to v2 (consistent with TV-19 EC-016 mock-injection precedent). | error |
| Successful completion | Event payload contains all 8 fields with correct types | edge-case |
| Subprocess terminated by SIGKILL: `command = "bash"`, `args = ["-c", "kill -9 $$"]`; capability-passes; subprocess receives SIGKILL from itself → `child.wait()` returns `Ok(Some(status))` with `status.code() == None` (signal-death) | `host.exec_subprocess.completed` event emitted with `exit_code = -1`, `outcome = 'failure'`, `terminated_by_signal: None` (v1 schema does not expose signal field per OQ-W16-002 deferral); host call returns `Ok(envelope)`. Witnesses EC-009. | signal-death-witness |
| `internal_log.write_inner` IO error: `ctx.internal_log = Some(InternalLog::new(<directory that does not exist and cannot be created>))`; capability passes; subprocess exits 0 | `write_inner` returns `Err(io::Error)`; `write` (`internal_log.rs::InternalLog::write`) emits `eprintln!("factory-dispatcher: internal_log write failed (host.exec_subprocess.completed): ...")` to stderr; `events.lock().push(event)` at `host/mod.rs::HostContext::emit_internal` SUCCEEDS; host call returns `Ok(envelope)`. The event IS in the in-memory queue; it is absent from `dispatcher-internal-*.jsonl`. Witnesses EC-010 + Postcondition 6. **Test setup:** requires `internal_log: Some(InternalLog::new(missing_dir))` — NOT `HostContext::new` which sets `None`. | best-effort-emit-witness |
| `ctx.internal_log = None` (test-context via `HostContext::new`); capability passes; subprocess exits 0 | `emit_internal` at `host/mod.rs::HostContext::emit_internal` skips `log.write` entirely (None branch); `events.lock().push(event)` at `host/mod.rs::HostContext::emit_internal` runs; exactly one event in in-memory queue; no file written. Witnesses Postcondition 4 bifurcation (internal_log: None path). | test-context-no-log-witness |
| Subprocess output within `max_output_bytes`; encoded envelope size (`stdout_len + stderr_len + 12`) exceeds guest `result_buf_cap` | Returns `Err(OUTPUT_TOO_LARGE -3)` at `exec_subprocess.rs::register` (envelope-length check); NO event emitted (Postcondition 5 / EC-005B). Host call returns -3. `host.exec_subprocess.completed` NOT emitted. Witnesses EC-005B result_buf_cap-overflow path. | error |
| `events` Mutex poisoned by concurrent panic in another host call | `emit_internal` silently drops the event (`host/mod.rs::HostContext::emit_internal` `if let Ok` short-circuits to `else => no-op`); subsequent `drain_events` (`host/mod.rs::HostContext::drain_events`) panics; host call return value `Ok(envelope)` UNCHANGED for the emit-side. Witnesses EC-011. | mutex-poison-witness |
| `caps.cwd_allow = ["/tmp/safe"]`; `ctx.cwd = "/etc"`; capability passes (binary on allow-list); subprocess spawns successfully | Subprocess receives `cwd = "/etc"` — NOT restricted to `/tmp/safe`. No denial event emitted. No error returned. `host.exec_subprocess.completed` IS emitted. **v1 known limitation per EC-012:** `cwd_allow` is declared but not enforced at `exec_subprocess.rs::execute_bounded` (cwd block). Test MUST assert NO `internal.capability_denied` event with reason `cwd_not_on_allow_list`. Witnesses EC-012 silent-no-op behavior. | cwd-allow-no-op-witness [LOW-P42-001] |
| `caps.env_allow = ["MY_VAR"]`; `ctx.env_view` does NOT contain `"MY_VAR"`; capability passes; subprocess spawns and exits 0 | Subprocess environment does NOT include `MY_VAR` — name is silently omitted at `exec_subprocess.rs::execute_bounded` (env_allow loop, `if let Some(val) = env_view.get(name)` branch not taken). No event emitted; no error returned. `host.exec_subprocess.completed` IS emitted. Test MUST assert subprocess did not receive `MY_VAR` (e.g., subprocess echoes env and `MY_VAR` is absent from stdout). Witnesses EC-014 silent-no-op behavior. | env-allow-absent-no-op-witness [LOW-P42-001] |
| Simulated partial pipe read: inject a mock `stdout` reader that returns `Ok(5)` after writing 5 bytes then returns `Err(io::Error::from(io::ErrorKind::BrokenPipe))` on the next read; `max_output_bytes = 1024`; subprocess exits 0 | `read_to_end` discards the `Err` via `let _ =`; `stdout_buf` contains 5 bytes; `truncated = false` (5 < 1024); `host.exec_subprocess.completed` emitted with `stdout_bytes = 5`, `outcome = 'success'`. No error code returned. Plugin caller cannot distinguish from a subprocess that wrote exactly 5 bytes. Witnesses EC-015 silent pipe IO error swallow / best-effort-read-witness. | best-effort-read-witness [HIGH-P50-001] |
| Simulated `child.kill()` Err + `child.wait()` block: inject a mock child where `kill()` returns `Err(io::Error::from(io::ErrorKind::PermissionDenied))` and `wait()` blocks indefinitely; TIMEOUT deadline fires in `exec_subprocess.rs::execute_bounded` | `let _ = child.kill()` discards the Err; `let _ = child.wait()` blocks; `execute_bounded` never returns; no TIMEOUT (-2) is delivered to the wasm caller; no event is emitted. Witnesses EC-016 cleanup-phase hang / no-secondary-deadline-witness. **Note:** this test requires mock injection or a specialized test harness; real subprocess integration tests cannot easily trigger `kill()` Err + `wait()` block simultaneously. | no-secondary-deadline-witness [HIGH-P50-002] |
| `command = "sleep"`, `args = ["0.5"]`, `timeout_ms = 5000`, `max_output_bytes = 1024`; capability passes (sleep on allow-list with PATH forwarded); subprocess exits 0 after ~500ms wall-clock | Subprocess exits 0 after ~500ms; `host.exec_subprocess.completed` event emitted with `duration_ms ∈ [490, 600]` (range covers kernel scheduling jitter on typical Linux/macOS systems). **PC-3 active timing witness:** `duration_ms` is computed from a `let started = Instant::now()` captured **immediately before `command.spawn()`** at `exec_subprocess.rs::execute_bounded` (spawn call) — NOT from the deadline `Instant` at `exec_subprocess.rs::execute_bounded` (deadline assignment) which is set post-spawn. A misimplementation that uses the post-spawn deadline Instant would produce a `duration_ms` value near zero (deadline-Instant is set after spawn; delta to exit is close to 500ms which matches, so sub-case: if deadline is captured at the deadline assignment and measured to exit, result would also be ~500ms — but semantic is wrong: a misimplementation might capture duration from deadline construction to exit, which for a 5000ms deadline would produce ~500ms coincidentally; the distinguishing test is `timeout_ms = 500` with `sleep 0.4` — a misimplementation using the deadline Instant would produce near-zero duration, while the correct started-before-spawn Instant produces ~400ms). This row exercises the PC-3 normative timing semantic that TV-10 only passively asserts (type-presence, not timing-correctness). | [normative] [MED-P60-002] |
| `command = "/usr/bin/true"`, `args = []`, `timeout_ms = 0`, `max_output_bytes = 1024`; capability passes | Deadline = `Instant::now() + Duration::from_millis(0)` at `exec_subprocess.rs::execute_bounded` (deadline assignment); `true` exits immediately but the busy-poll loop in `exec_subprocess.rs::execute_bounded` calls `child.try_wait()` — if `true` has already exited, `Ok(Some(status))` is returned and success path fires (zero output, exit 0, `host.exec_subprocess.completed` emitted). If `true` is still running on first poll (race: spawn + first try_wait nanoseconds), `Ok(None)` fires the `if Instant::now() >= deadline` check in `exec_subprocess.rs::execute_bounded` — deadline already expired → `child.kill(); child.wait(); return Err(TIMEOUT -2)`. **In practice:** `/usr/bin/true` exits fast enough that `Ok(Some(status))` typically fires first; however the normative contract for `timeout_ms = 0` is that it is operationally equivalent to "kill immediately if child is still running at first poll." The dominant test-observable outcome is `Err(TIMEOUT -2)` on any subprocess that does not exit before the first `try_wait()` call. Witnesses EC-013A lower-bound semantic: `timeout_ms = 0` does NOT mean "unlimited" — it means "deadline is already expired at first poll." NO event emitted on TIMEOUT path per Postcondition 5. | [edge-case] [MED-P60-003a] |
| `command = "sleep"`, `args = ["0.01"]` (subprocess runs ~10ms), `timeout_ms = 1` (1ms budget — below 5ms floor), `max_output_bytes = 1024`; capability passes | Subprocess starts and runs; `try_wait()` in `exec_subprocess.rs::execute_bounded` poll loop returns `Ok(None)` (still running) on first poll; the `Ok(None)` branch in `exec_subprocess.rs::execute_bounded` checks `if Instant::now() >= deadline` (deadline was set 1ms in the past) — but `Instant::now()` may not yet have reached deadline if spawn+try_wait took <1ms (race); if deadline already expired, TIMEOUT fires immediately without sleeping; if NOT yet expired, `std::thread::sleep(Duration::from_millis(5))` at `exec_subprocess.rs::execute_bounded` (thread::sleep) yields control for ~5ms before second poll. On second poll, 5ms+ has elapsed; `Instant::now() >= deadline` is true; TIMEOUT fires: `child.kill(); child.wait(); return Err(TIMEOUT -2)`. Total wall-clock: ~5-6ms (from spawn). **Witnesses 5ms granularity floor (LOW-P51-005 / EC-013A):** the effective timeout for any running-child case with `timeout_ms < 5ms` is `max(timeout_ms, ~5ms)` due to the hardcoded 5ms sleep in `exec_subprocess.rs::execute_bounded`. Operator setting `timeout_ms = 1` does not yield 1ms TIMEOUT detection. No event emitted on TIMEOUT path. | [edge-case, known-limitation] [MED-P60-003b] |
| `command = "/usr/bin/true"`, `args = []`, `timeout_ms = 4_294_967_295` (`u32::MAX`), `max_output_bytes = 1024`; capability passes | `Duration::from_millis(timeout_ms as u64)` at `exec_subprocess.rs::execute_bounded` (deadline assignment) accepts `u32::MAX` cast to `u64` = 4,294,967,295ms without overflow; deadline = `Instant::now() + Duration::from_millis(4_294_967_295)` ≈ 49.7 days in the future; `/usr/bin/true` exits in <10ms; `Ok(Some(status))` fires on first or second poll; `host.exec_subprocess.completed` event emitted with `exit_code = 0`, `outcome = 'success'`, `duration_ms ≈ 0–10`. Witnesses LOW-P52-002 upper-bound semantic: `timeout_ms = u32::MAX` is operationally equivalent to "effectively unbounded" (~49.7 days). No TIMEOUT error. No overflow or panic from the `u32 as u64` cast. | [edge-case, known-limitation] [MED-P60-003c] |
| `command = "/bin/echo"`, `args = ["x"]` (1 byte stdout: "x" + newline = 2 bytes total), `timeout_ms = 1000`, `max_output_bytes = 0`; capability passes | Subprocess exits 0; `read_to_end(&mut stdout_buf)` in `exec_subprocess.rs::execute_bounded` populates stdout_buf with 2 bytes ("x\n"); `stdout_buf.len() > max_output_bytes as usize` in `exec_subprocess.rs::execute_bounded` (truncated check) evaluates `2 > 0 = true`; `truncated = true` fires; returns `Err(OUTPUT_TOO_LARGE -3)` via `exec_subprocess.rs::execute_bounded` (OUTPUT_TOO_LARGE return); NO event emitted (PC-5 error-path, Postcondition 5). Witnesses EC-013B "any output → OUTPUT_TOO_LARGE" branch: when `max_output_bytes = 0`, any subprocess producing ≥1 byte triggers OUTPUT_TOO_LARGE. | [edge-case] [MED-P60-004a] |
| `command = "/usr/bin/true"`, `args = []` (zero bytes stdout, zero bytes stderr), `timeout_ms = 1000`, `max_output_bytes = 0`; capability passes | Subprocess exits 0; both `stdout_buf` and `stderr_buf` are empty after `read_to_end` in `exec_subprocess.rs::execute_bounded`; `stdout_buf.len() > max_output_bytes as usize` in `exec_subprocess.rs::execute_bounded` (truncated check) evaluates `0 > 0 = false` (boundary case `==` is SUCCESS per the strict-`>` operator); `truncated = false`; success path fires; `encode_envelope(0, &[], &[])` called via `exec_subprocess.rs::execute_bounded` (success return); `host.exec_subprocess.completed` emitted with `stdout_bytes = 0`, `stderr_bytes = 0`, `outcome = 'success'`. Witnesses EC-013B zero-byte success branch and reinforces the MED-P52-001 boundary semantic at `max_output_bytes = 0`: the boundary value itself (`0 > 0`) is the success path, not OUTPUT_TOO_LARGE. | [edge-case] [MED-P60-004b] |

## Verification Properties

| VP-NNN | Property | Proof Method |
|--------|----------|-------------|
| (TBD — to be assigned in Phase 1.6b) | | |

## Traceability

| Field | Value |
|-------|-------|
| L2 Capability | CAP-TBD (anchor in Phase 1.5) |
| L2 Domain Invariants | TBD |
| Architecture Module | SS-01 — `crates/factory-dispatcher/src/host/exec_subprocess.rs` |
| Stories | S-9.07 |
| Capability Anchor Justification | CAP-TBD — capability anchor to be confirmed in Phase 1.5; this BC governs exec_subprocess success-path observability in factory-dispatcher |

### Source Evidence

| Property | Value |
|----------|-------|
| **Path** | `.factory/architecture/gap-analysis-w16-subprocess.md` Section 5 item 2; `crates/factory-dispatcher/src/host/exec_subprocess.rs::execute_bounded` (success-return block) |
| **Confidence** | HIGH (gap explicitly identified by architect gap analysis; analogous pattern exists in `exec_subprocess.rs::emit_denial`) |
| **Extraction Date** | 2026-05-03 |
| **Extracted from** | `.factory/architecture/gap-analysis-w16-subprocess.md` Section 5 |

#### Evidence Types Used

- gap-analysis (architect-identified missing emit call in `exec_subprocess.rs::execute_bounded` success-return block)
- assertion (analogous emit_denial pattern at `exec_subprocess.rs::emit_denial` confirms implementation pattern)

#### Purity Classification

| Property | Assessment |
|----------|-----------|
| **I/O operations** | YES — emits event via `ctx.emit_internal`: synchronous `Mutex::lock` + `Vec::push` to events queue per `host/mod.rs::HostContext::emit_internal`, then host writes to single-stream `FileSink` per ADR-015 D-15.1 (no channel send; not async) |
| **Global state access** | No |
| **Deterministic** | YES — event emission is deterministic given subprocess completion |
| **Thread safety** | YES on the success-path (same write pattern as existing emit_denial calls). KNOWN LIMITATION: Mutex poison asymmetry — `emit_internal` silently drops on poison (`host/mod.rs::HostContext::emit_internal`), while `drain_events` panics on poison (`host/mod.rs::HostContext::drain_events`). See EC-011. |
| **Overall classification** | Deterministic with best-effort sink I/O |

#### Refactoring Notes

(TBD — to be assessed in Phase 1.6b verification properties pass)

## Changelog

- v1.1 (2026-05-08): TD-VSDD-091 stable-anchor migration sweep (Chunk 1) — 44 cites migrated from `file.ext:NNN` line-number form to stable symbol anchors (`::function`, `::method`, `::constant`).
