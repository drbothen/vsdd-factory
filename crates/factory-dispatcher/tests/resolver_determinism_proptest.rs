// S-12.03 VP-075 proptest — Step 3 test-writer fills this in.
// Covers: prop_resolve_is_deterministic, prop_merge_is_deterministic,
// prop_merge_preserves_base_config_fields (200 trials, 5s timeout).
