---
document_type: dispatch-package
level: ops
title: "S-15.03 Wave — Milestone 1 Dispatch Package"
producer: state-manager
timestamp: 2026-05-15T00:00:00Z
phase: section-12-step-3-m1-dispatch
cycle: brownfield-backfill
inputs:
  - .factory/cycles/v1.0-brownfield-backfill/s-15.03-wave-plan-2026-05-15.md
  - .factory/STATE.md
input-hash: "ad1c745"
traces_to: .factory/stories/S-15.03-index-cite-refresh-hook.md
related_stories: [S-15.06, S-15.08, S-15.16]
develop_head_at_authoring: "224fa18421214b30dacf1cdd606152294cd33bd6"
factory_artifacts_head_at_authoring: "bcc1e9ed"
---

# S-15.03 Wave — Milestone 1 Dispatch Package
## Self-Contained Dispatch Package (2026-05-15)

> **Post-CLEAR entry point.** Read this file top-to-bottom to dispatch the M1 stories with zero prior context.
> Verified against develop@224fa184 and factory-artifacts@bcc1e9ed.

---

## Goal

Deliver the three Milestone 1 stories of the S-15.03 PRIORITY-A wave:

1. **S-15.06** — E-10 Pass-14 Retroactive Spec Gap Closures (state-manager fix burst, sub-day)
2. **S-15.08** — Dim-2 Gate Template Library — 10 canonical bash scripts (multi-day, per-story-delivery)
3. **S-15.16 Part A** — lessons.md Compaction (state-manager-led content reorganization)

These three stories have no WASM compilation, no research gates, and zero remaining gates to entry. M1 is the lowest-risk, highest-visible-progress segment of the S-15.03 wave.

---

## Why This Exists

The S-15.03 wave plan was produced by the architect on 2026-05-15 in a session that will be CLEARed. The full wave plan is persisted to `.factory/cycles/v1.0-brownfield-backfill/s-15.03-wave-plan-2026-05-15.md`. This dispatch package extracts the M1 execution instructions so a post-CLEAR orchestrator can dispatch S-15.06, S-15.08, and S-15.16 Part A without reading the full 95-item plan document.

**Pattern:** This is the same durability-burst pattern used for `td-74-dispatch.md` (commit 543338f1) after a prior /clear in the same session.

---

## Scope

| Story | Routing | Effort | Branch |
|-------|---------|--------|--------|
| S-15.06 | state-manager (NOT story-writer + implementer) | Sub-day | factory-artifacts only (no develop branch) |
| S-15.08 | story-writer → test-writer → implementer → pr-manager → state-manager | Multi-day | `feature/S-15.08-dim2-gates-bash-templates` |
| S-15.16 Part A | state-manager-led (content reorganization in .factory/) | Multi-day | factory-artifacts only (no develop branch) |

---

## Pre-Flight Gate (before dispatching any M1 story)

```bash
# Verify factory-artifacts worktree health
git -C .factory rev-parse --git-dir && git -C .factory branch --show-current
# Expected: factory-artifacts

# Verify develop HEAD
git rev-parse origin/develop
# Expected: 224fa18421214b30dacf1cdd606152294cd33bd6 (or newer if PRs merged)

# Verify no in-flight PRs or dirty worktrees
git -C .factory status --porcelain
# Expected: empty (clean)
```

---

## Story 1: S-15.06 — E-10 Pass-14 Retroactive Spec Gap Closures

### Goal

Close 4 deferred findings from E-10 pass-14 adversarial review that were excluded from D-471 asymptotic-acceptance seal: F-PASS14-003, F-PASS14-005, F-PASS14-007, F-PASS14-008. These are pure content edits in `.factory/` — no source code changes.

### Why These Were Deferred

E-10 pass-14 closed only F-PASS14-001 (CRITICAL) and F-PASS14-002 (HIGH) as mandatory. The remaining 6 findings were deferred to S-15.03 PRIORITY-A automation wave per D-471 asymptotic-acceptance seal (1e810021). F-PASS14-006 is separately tracked as a human decision (three-digit POLICY IDs, RESOLVED 2026-05-15). F-PASS14-004 is deferred to S-15.15. The 4 findings addressed here are one-time retroactive content fixes below story threshold for separate PRs.

### Routing

**state-manager** directly. This is a `.factory/` content edit bundle, NOT a source code delivery. The per-story-delivery workflow (story-writer → test-writer → implementer → pr-manager) does NOT apply to state-manager fix bursts on factory-artifacts.

### Sequence

1. **Add D-NNN row to brownfield-backfill decision-log** for the POLICY 13-18 registration event at commit `b8909832` (F-PASS14-003). The D-NNN must be the next sequential ID after D-471. Check decision-log.md for the current highest D-NNN first.

2. **Annotate E-10 INDEX.md pass-13 status cell** to reference the new D-NNN codification (F-PASS14-003 closure).

3. **Update `.factory/policies.yaml`** to add D-NNN field to POLICY 13-18 entries, documenting when each policy was codified (matching any existing D-NNN annotation convention in POLICY 1-12 rows; establish if absent).

4. **Propagate VP-014 v1.2.1 NN-2 parity update** to `verification-architecture.md` and `verification-coverage-matrix.md` (F-PASS14-005). Before editing, verify whether the existing `validate-vp-consistency` hook (priority 493 in hooks-registry.toml) would already catch this going forward — if it does, document that fact in the commit message. If it does not cover this case, note that S-15.11 will extend it.

5. **Expand E-10 INDEX.md Convergence Status paragraph** to mention POLICY 13-18 (F-PASS14-007). One to two sentences sufficient.

6. **Update E-10 Concurrent Cycles brownfield row tally** to current accurate count (F-PASS14-008).

7. **Single factory-artifacts commit.** Subject: `state: S-15.06 SHIPPED — E-10 pass-14 retroactive spec gap closures (F-PASS14-003/005/007/008)`. No multi-commit chain. No "backfill" or "Stage N" in subject.

8. **Post-merge:** Register S-15.06 in STORY-INDEX as status=merged. Bump STORY-INDEX v3.25 → v3.26.

### Dependency Verification

No new dependencies introduced. No cargo audit required.

---

## Story 2: S-15.08 — Dim-2 Gate Template Library (10 Bash Scripts)

### Goal

Write 10 production-grade bash scripts to `plugins/vsdd-factory/hooks/dim2-gates/` that serve as canonical Dim-2 attestation templates for fix bursts. These are author-time tools, not dispatcher-invoked hooks. They close D-453(e), D-454(a/c), and D-450(b/c/d/e), D-451(a), D-452(a/b).

### Why This Exists

The dim2-gates directory was scaffolded in PR #137 (21d444d8) with a README documenting 5 PLANNED templates. No script bodies were written. S-15.08 instantiates all 10 templates (5 README-planned + 5 additional from D-450/451/452 codifications), moves them from PLANNED to ACTIVE, and provides bats test coverage for each.

### Pre-existing Scaffolding

```
plugins/vsdd-factory/hooks/dim2-gates/README.md  (already exists — PR #137)
```

The 5 PLANNED entries in the README are:
- `trajectory-tail-cell-grep.sh`
- `freshness-literal-stdout.sh`
- `block-label-canonical-form.sh`
- `banner-wc-l-gate.sh`
- `propagation-count-gate.sh`

### Scripts to Author

Write all 10 to `plugins/vsdd-factory/hooks/dim2-gates/`:

| Script | Closes | Description |
|--------|--------|-------------|
| `trajectory-tail-cell-grep.sh` | D-454(a) | Per-cell grep verifying trajectory-tail value at each prescribed site |
| `freshness-literal-stdout.sh` | D-454(b) | Literal stdout capture for freshness re-execution evidence |
| `block-label-canonical-form.sh` | D-454(d) | Verifies burst-log block labels match D-453(c) canonical 9 block types |
| `banner-wc-l.sh` | D-450(d) | STATE.md banner "actual N lines" vs `wc -l` arithmetic gate |
| `propagation-completeness.sh` | D-452(a) | Post-derivation propagation-completeness — derived value appears at all prescribed sites |
| `dim7-dispatched-count-sweep.sh` | D-450(b) | Dim-7 sibling-sweep for dispatched-count across all burst-log Dim-7 entries |
| `dim1-file-count-arithmetic.sh` | D-450(c) | Dim-1 headline cardinality vs enumerated list count arithmetic gate |
| `active-branches-sha-currency.sh` | D-450(d) | `git rev-parse origin/<branch>` currency check for Active Branches table rows |
| `decision-log-monotonic-rows.sh` | D-450(e) | Decision-log D-NNN ascending-order enforcement |
| `layer-ordinal-dual-direction.sh` | D-452(b) | Layer-N dual-direction sweep — positive `Nth-layer` form + negative `(N±1)th-layer` drift |
| `meta-level-ack-grep.sh` | D-451(a) | META-LEVEL acknowledgment literal-shell grep evidence |

Note: 11 scripts listed — the banner-wc-l gate is split from active-branches-sha-currency (separate concerns; both close D-450(d) sub-clauses). Deliver all 11.

### Script Requirements (ALL scripts)

Each bash script MUST:
- Start with `#!/usr/bin/env bash` and `set -euo pipefail`
- Accept positional arguments: at minimum the factory root path (default `.`)
- Exit 0 on PASS with human-readable success message to stdout
- Exit 1 on FAIL with human-readable failure message to stdout (specific: what was expected vs what was found)
- Include a usage block (`--help` flag) documenting expected arguments and return values
- Be executable (`chmod +x`)
- Reference the D-NNN it implements in a header comment

### Bats Coverage

Write bats tests in `plugins/vsdd-factory/tests/dim2-gates/`:
- 2 test cases per script: PASS (well-formed fixture input) and FAIL (broken fixture input)
- Fixture `.factory/` directories in `plugins/vsdd-factory/tests/fixtures/dim2-gates/`
- ~22 bats test cases total (11 scripts × 2 scenarios)

### dim2-gates README Update

Update `plugins/vsdd-factory/hooks/dim2-gates/README.md`:
- Move the 5 PLANNED entries to ACTIVE
- Add the 6 additional scripts from D-450/451/452 (marking them ACTIVE)
- Add a "How to Use" section explaining these are author-time tools invoked manually during Dim-2 attestation

### Pre-Flight 4-Gate

```bash
cargo fmt --check --all
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace --all-targets
cd plugins/vsdd-factory/tests && ./run-all.sh
```

All four must PASS before pushing. Expected: cargo gates PASS unchanged (bash-only delivery). Bats suite must include the new dim2-gates tests.

### Branch + PR Strategy

- **Branch:** `feature/S-15.08-dim2-gates-bash-templates` off `origin/develop` (currently `224fa184`)
- **Target:** develop
- **Conventional Commits subject:** `feat(hooks): dim2-gate template library — 11 canonical bash scripts for fix-burst attestation (S-15.08)`
- **PR type:** bash-script + bats. Standard 9-step pr-manager lifecycle.
- **Squash-merge** to develop.

### Dispatch Sequence

1. **story-writer** reads the architect wave plan (`s-15.03-wave-plan-2026-05-15.md` Section 2 S-15.08 entry) and authors a full S-15.08 story file at `.factory/stories/S-15.08-dim2-gates-bash-templates.md` with acceptance criteria, tasks, and implementation detail.
2. **test-writer** writes failing bats tests (22 test cases in `plugins/vsdd-factory/tests/dim2-gates/`).
3. **implementer** writes the 11 bash scripts + updates README. Runs pre-flight 4-gate.
4. **implementer** commits + pushes `feature/S-15.08-dim2-gates-bash-templates`.
5. **pr-manager** dispatches 9-step PR lifecycle: open PR → AI-review → security-review (light, bash-only) → CI wait → merge decision → squash-merge.
6. **state-manager** post-merge burst: closes S-15.08 in STORY-INDEX; updates factory-artifacts.

### Post-Merge Next Steps (within M1)

After S-15.08 merges, dispatch S-15.16 Part A immediately. S-15.16 Part A (lessons.md compaction) reduces fuel exhaustion risk before any hook stories that write lessons.md (M2+M3).

---

## Story 3: S-15.16 Part A — lessons.md Compaction

### Goal

Compact `cycles/v1.0-feature-engine-discipline-pass-1/lessons.md` from ~3600+ lines to under 3000 lines by archiving pre-pass-50 lesson detail (L-EDP1-001..049) to a new file. This closes D-442(e) structural risk (WASM fuel exhaustion on PostToolUse writes to lessons.md when the file exceeds the hook sandbox's fuel budget).

### Why This Exists

D-442(e) flags that lessons.md exceeding 3500 lines (soft) / 4000 lines (hard) triggers WASM fuel exhaustion on PostToolUse validators. The file is currently ~3600+ lines per STATE.md Section 6. This manifests as advisory-level blocks during the session (known false-positive pattern documented in CLAUDE.md Factory Hook Diagnostics). Part B (the size-gate hook) ships in M3. Part A ships in M1 to prevent any M2/M3 hook story from triggering exhaustion during its own delivery.

### Routing

**state-manager-led.** This is a `.factory/` content reorganization — no source code changes. The per-story-delivery workflow does NOT apply.

### Sequence

1. **Read `lessons.md` to determine exact line count and lesson boundaries.** Identify where L-EDP1-049 ends and L-EDP1-050 begins.

2. **Write `lessons-archive-pass1-49.md`** at `.factory/cycles/v1.0-feature-engine-discipline-pass-1/lessons-archive-pass1-49.md` with:
   - Frontmatter: `document_type: lessons-archive`, `cycle: v1.0-feature-engine-discipline-pass-1`, `archived_range: L-EDP1-001..049`, `archived_date: 2026-05-15`, `parent_file: lessons.md`
   - Full content of L-EDP1-001 through L-EDP1-049 (verbatim from lessons.md — do NOT summarize)

3. **Edit `lessons.md`** to:
   - Delete L-EDP1-001 through L-EDP1-049 content
   - Add at the top of the active lessons section: "Historical lessons L-EDP1-001..049 archived at `lessons-archive-pass1-49.md`. Active lessons start at L-EDP1-050."
   - Preserve all other content unchanged (header, frontmatter, L-EDP1-050 onward)

4. **Verify final line count** of lessons.md is under 3000 lines.

5. **Single factory-artifacts commit.** Subject: `state: S-15.16 Part A SHIPPED — lessons.md compaction (L-EDP1-001..049 archived; active lessons start L-EDP1-050)`. No multi-commit chain.

6. **Post-merge:** Register S-15.16 in STORY-INDEX (Part A only; mark as merged with note "Part B deferred to M3"). Bump STORY-INDEX v3.26 → v3.27 (after S-15.06 has already bumped v3.25 → v3.26; if S-15.06 has not shipped yet, this becomes v3.26 → v3.27 sequentially).

### Dependency Verification

No new dependencies. No cargo audit required.

---

## Delivery Sequence Within M1

Execute in this order (per architect wave plan Section 4 Milestone 1 rationale):

1. **S-15.06 first** — closes E-10 outstanding spec gaps; factory-artifacts commit only; sub-day
2. **S-15.08 second** — instantiates bash templates that M2/M3 hook stories reference in Dim-2 attestations; standard per-story-delivery; multi-day
3. **S-15.16 Part A third** — compacts lessons.md below WASM fuel threshold before M2 hook stories trigger exhaustion; factory-artifacts commit only

S-15.06 and S-15.16 Part A can be dispatched in the same orchestrator session (both are factory-artifacts-only). S-15.08 requires the full per-story-delivery pipeline.

---

## Effort Estimates

| Story | Effort class | Calendar estimate |
|-------|-------------|-------------------|
| S-15.06 | Sub-day | 1-2 hours state-manager time |
| S-15.08 | Multi-day | 1 full orchestrator session (story-writer + test-writer + implementer + pr-manager) |
| S-15.16 Part A | Multi-day (content audit) | 2-4 hours state-manager time |

Total M1 calendar estimate: ~1-2 orchestrator sessions.

---

## Resumption Gate (after M1 closes)

**M2 entry gate:** All 3 M1 stories merged + CI green + S-15.16 Part A lessons.md verified under 3000 lines.

**M2 dispatch:** Architect confirms M2 readiness. PO/story-writer authors 4 M2 story files (S-15.07, S-15.09, S-15.11, S-15.14). Each M2 story is its own per-story-delivery cycle (multi-day each, WASM Rust crates).

**Recommended:** Pause for human direction before M2 launch given M2 wall-clock estimate ~4-8 days.

---

## Hard Constraints

- **No AI attribution** in any commit message or file content.
- **Single factory-artifacts commit per burst** per TD-VSDD-053. No "Stage 1/2/backfill" subjects.
- **No `--no-verify`** — hook chain must run; investigate any blocks per CLAUDE.md Factory Hook Diagnostics.
- **S-15.06 and S-15.16 Part A are factory-artifacts-only** — no develop branch PRs, no CI, no pr-manager.
- **S-15.08 requires the full per-story-delivery pipeline** — story-writer → test-writer → implementer → pr-manager → state-manager.

---

## Post-Merge Next Steps (Section 12 Strict Ordering)

After M1 closes, the strict engine-discipline ordering in STATE.md Section 12 governs:

| Step | Item | Gate |
|------|------|------|
| 3 (M1) | S-15.06 + S-15.08 + S-15.16 Part A | ACTIVE (this document) |
| 3 (M2) | S-15.07 + S-15.09 + S-15.11 + S-15.14 (4 WASM hooks) | M1 complete |
| 3 (M3) | S-15.10 + S-15.12 + S-15.13 + S-15.15 + S-15.16 Part B | M2 complete |
| 4 | E-10 sub-cycle resumption (pass-15 forward) | Step 3 (all milestones) complete |
| 5 | F5 cycle resumption (pass-75 forward) | Step 3 complete + explicit human direction |
