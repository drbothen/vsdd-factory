# Red Gate Log — S-19.06 (host::read_prefix bounded partial read: head-c semantics, NEVER OUTPUT_TOO_LARGE, additive FFI entry point)

**Date:** 2026-07-15
**Branch:** feature/S-19.06 @ cf0b3d3b (T-009f commit; tests base a8aaf281; stubs base e422a30e)
**Test Writer:** vsdd-factory:test-writer
**Status:** RED_GATE_VERIFIED

## Summary

| Story | New Tests Written | All New Tests Fail (Red)? | Pre-existing Tests | Gate |
|-------|------------------|--------------------------|-------------------|------|
| S-19.06 | 9 unit + 1 bats (T-009f) | YES — 9 unit FAIL, T-009f FAIL; T-009a-e PASS at red gate by stub design (regression guards) | 180 PASS | PASSED |

## Red Gate Verification

**Command:** `cargo test -p factory-dispatcher read_prefix`

**Result:** RED GATE PASSED. 9 new unit tests FAIL via `panic-at-todo!()` in `read_prefix::prepare` stub; 180 pre-existing tests PASS; 0 new tests pass against unmodified stubs.

### Unit Tests (crates/factory-dispatcher/src/host/read_prefix.rs `#[cfg(test)]`)

| Test | AC | BC Trace | Failure Reason | Status |
|------|----|----------|----------------|--------|
| T-001 (`test_S19_06_T001_bounded_prefix_returns_exactly_max_bytes`) | AC-001 | BC-1.17.001 v1.6 PC-1 + PC-6 | `panic at 'not yet implemented: S-19.06: implement read_prefix prepare function'` — 100-byte file, max_bytes=50; `prepare()` is `todo!()` | FAIL (expected) |
| T-002 (`test_S19_06_T002_byte_exact_no_utf8_trimming_at_boundary`) | AC-001 | BC-1.17.001 v1.6 PC-6 | `panic at todo!()` — partial multi-byte UTF-8 at 50-byte boundary, no trimming required; `prepare()` is `todo!()` | FAIL (expected) |
| T-003 (`test_S19_06_T003_short_file_returns_full_content_no_padding`) | AC-002 | BC-1.17.001 v1.6 PC-2 | `panic at todo!()` — 30-byte file, max_bytes=100; full content, no padding; `prepare()` is `todo!()` | FAIL (expected) |
| T-004 (`test_S19_06_T004_never_returns_output_too_large`) | AC-003 | BC-1.17.001 v1.6 PC-3 | `panic at todo!()` — 10000-byte file, max_bytes=50; must NEVER return OUTPUT_TOO_LARGE (-3); `prepare()` is `todo!()` | FAIL (expected) |
| T-005 (`test_S19_06_T005_no_capability_block_returns_capability_denied`) | AC-004 | BC-1.17.001 v1.6 PC-4 | `panic at todo!()` — no capabilities.read_prefix block → CAPABILITY_DENIED (-1) before filesystem access; `prepare()` is `todo!()` | FAIL (expected) |
| T-006 (`test_S19_06_T006_read_file_cap_only_returns_capability_denied`) | AC-004 | BC-1.17.001 v1.6 Invariant 3 | `panic at todo!()` — capabilities.read_file only (not read_prefix) → CAPABILITY_DENIED (-1); independent capability defense-in-depth; `prepare()` is `todo!()` | FAIL (expected) |
| T-007 (`test_S19_06_T007_absent_allowlisted_file_returns_not_found_and_emits_event`) | AC-005 | BC-1.17.001 v1.6 PC-5 + Invariant 5 | `panic at todo!()` — absent allowlisted file → NOT_FOUND (-5) + `internal.file_not_found` event, zero `internal.capability_denied` events; `prepare()` is `todo!()` | FAIL (expected) |
| T-008 (`test_S19_06_T008_max_bytes_zero_returns_empty_payload_exit_0`) | AC-006 | BC-1.17.001 v1.6 EC-001 | `panic at todo!()` — max_bytes=0 → empty payload (0 bytes), exit 0, file NOT opened; `prepare()` is `todo!()` | FAIL (expected) |
| T-010 (`test_S19_06_T010_path_outside_allowlist_returns_capability_denied_with_event`) | AC-007 (EC-004) | BC-1.17.001 v1.6 EC-004 | `panic at todo!()` — capabilities.read_prefix present but path outside path_allow → CAPABILITY_DENIED (-1) + `internal.capability_denied` event with reason=path_not_allowed; `prepare()` is `todo!()` | FAIL (expected) |

### Bats Integration Tests (plugins/vsdd-factory/tests/read-prefix-wasm.bats)

| Test | AC | BC Trace | Status | Notes |
|------|----|----------|--------|-------|
| T-009a (Gate 1: safe wrapper signature in hook-sdk/src/host.rs) | AC-007 | BC-1.17.001 v1.6 §(a) layering parenthetical | PASS at Red Gate | Stub at e422a30e carries correct `pub fn read_prefix(path: &str, max_bytes: u32, timeout_ms: u32) -> Result<Vec<u8>, HostError>` signature; regression guard only |
| T-009b (Gate 2(i): raw extern 6-param shape in ffi.rs) | AC-007 | BC-1.17.001 v1.6 §(a) | PASS at Red Gate | Stub carries correct `pub safe fn read_prefix(` with 6-param pointer/length wire ABI; regression guard only |
| T-009c (Gate 2(ii): `#[link(wasm_import_module = "vsdd")]` in ffi.rs) | AC-007 | BC-1.17.001 v1.6 §(a) | PASS at Red Gate | vsdd namespace attribute present in existing extern block; regression guard only |
| T-009d (Gate 2(iii): read_prefix in wasm32 extern AND host_stubs blocks) | AC-007 | BC-1.17.001 v1.6 §Architecture Anchors | PASS at Red Gate | Both `cfg(target_arch = "wasm32")` extern block and `pub mod host_stubs` block contain `fn read_prefix`; regression guard only |
| T-009e (Gate 3: `read_prefix::register` in dispatcher dispatch table) | AC-007 | BC-1.17.001 v1.6 §Architecture Anchors | PASS at Red Gate | `read_prefix::register(&mut linker)` call added to `setup_linker` in stub commit e422a30e; regression guard only |
| T-009f (Gate 4: fixture WASM compile/link — `read-prefix-fixture` crate) | AC-007 | BC-1.17.001 v1.6 §(a) + §Architecture Anchors | FAIL (expected — Red Gate) | `cargo build -p read-prefix-fixture --target wasm32-wasip1` exits 101: package ID specification did not match any packages (crates/hook-plugins/read-prefix-fixture/ does not exist yet) |

## Regression Check

| Existing Tests | Status |
|---------------|--------|
| 180 pre-existing tests | all pass |

Zero regressions.

## Red Gate Density

- Unit (cargo test): 9 failing / 9 new unit tests = **100% RED**
- Bats T-009f (fixture compile/link): 1 failing / 1 = **100% RED**
- Bats T-009a-e (static signature greps): 5 PASS at Red Gate by stub design — these are regression guards, not behavioral tests; pass/fail at Red Gate is expected per stub architecture
- Combined behavioral: 9 unit + 1 bats T-009f = 10 failing / 10 meaningful new tests = **100% RED** (target: ≥50% RED per BC-8.30.001 Postcondition 3; EXCEEDED)

## Failure Mode Verification

All 9 unit tests fail via `panic-at-todo!()` — the message is `"not yet implemented: S-19.06: implement read_prefix prepare function"` from the `prepare()` stub body. The failure mechanism is correct per the stub design: all behavioral tests call `prepare()`, which is `todo!()`. The `register()` linker stub also contains `todo!("S-19.06: implement read_prefix host function body")` but this path is not exercised by unit tests (unit tests call `prepare()` directly).

T-009f fails via non-zero cargo exit code (101): the `read-prefix-fixture` crate at `crates/hook-plugins/read-prefix-fixture/` does not yet exist. This is the correct Red Gate form for AC-007 Gate 4 — the fixture crate is created by the implementer as part of Tasks 10–11.

No `#[should_panic]` masking. No vacuously-passing new tests. T-009a-e pass at Red Gate by design (static grep on stubs that already carry correct signatures and registrations per stub-architect discipline) and are documented as regression guards, not behavioral Red Gate evidence.

## Stubs Created

### S-19.06 stub commit (e422a30e)

Five files modified:

- `crates/factory-dispatcher/src/host/read_prefix.rs` — new file; two stubs:
  - `pub fn register(linker: &mut Linker<HostContext>) -> Result<(), HostCallError>` — inner closure body is `todo!("S-19.06: implement read_prefix host function body")`; correct 6-parameter wire ABI shape `(path_ptr, path_len, max_bytes, timeout_ms, out_ptr_out, out_len_out) -> i32`
  - `pub(crate) fn prepare(_ctx: &HostContext, _path: &str, _max_bytes: u32) -> Result<(Vec<u8>, u32), i32>` — body is `todo!("S-19.06: implement read_prefix prepare function")`
- `crates/factory-dispatcher/src/host/mod.rs` — `read_prefix` module declared + `read_prefix::register(&mut linker)?` called in `setup_linker`
- `crates/factory-dispatcher/src/registry.rs` — `ReadPrefixCaps { path_allow: Vec<String> }` struct + `read_prefix: Option<ReadPrefixCaps>` field added to `Capabilities`; `Default` derived
- `crates/hook-sdk/src/ffi.rs` — `pub safe fn read_prefix(path_ptr, path_len, max_bytes, timeout_ms, out_ptr_out, out_len_out) -> i32` added to both the `#[cfg(target_arch = "wasm32")]` extern block and the `pub mod host_stubs` block; `#[link(wasm_import_module = "vsdd")]` attribute already present from prior `read_file` extern
- `crates/hook-sdk/src/host.rs` — `pub fn read_prefix(path: &str, max_bytes: u32, timeout_ms: u32) -> Result<Vec<u8>, HostError>` safe wrapper stub added; calls `ffi::read_prefix(...)` via the wire ABI

## Hand-Off to Implementer

- Story ready for implementation: S-19.06
- Implementation tasks (from story spec v1.19):

  1. **`prepare()` body** in `crates/factory-dispatcher/src/host/read_prefix.rs` — implement all 7 responsibilities per the doc comment: capability check (`capabilities.read_prefix`), path resolution (`resolve_path_for_allowlist` + `check_path_allowed`), existence check (NOT_FOUND + `internal.file_not_found` event), bounded read (open, read ≤ max_bytes, max_bytes=0 short-circuit), timeout (TIMEOUT -2), directory/OS error (INTERNAL_ERROR -99), NEVER emit OUTPUT_TOO_LARGE (-3). Unblocks T-001..T-008+T-010.
  2. **`register()` inner closure** — wire `prepare()` into the linker closure using `guest_ptr_read` + `write_bytes_to_guest` patterns from `read_file::register`; replace the inner `todo!()`.
  3. **`hooks-registry.toml` capabilities.read_prefix schema** — add DISTINCT "Capability Schemas" preamble block (separate from S-19.04's tool-filter-anchoring block per story v1.15 ordering-only dependency fix).
  4. **fixture crate** `crates/hook-plugins/read-prefix-fixture/` — minimum: `Cargo.toml` with `name = "read-prefix-fixture"` + `vsdd-hook-sdk` dep, `src/main.rs` calling `vsdd_hook_sdk::host::read_prefix("", 0, 0)` in a no-op hook body; add to workspace `Cargo.toml` members. Unblocks T-009f.
  5. **`crates/hook-sdk/src/host.rs` safe wrapper** — complete the `read_prefix` wrapper body (currently stub); reads guest output via the `out_ptr_out`/`out_len_out` pattern from `read_file` wrapper.

  Parity reference: S-19.03 (PR merged, develop `091ce499`) implemented `read_file` NOT_FOUND + `internal.file_not_found` event; S-19.08 (PR merged, develop `1304d280`) implemented the cap-raise pattern for `verify-state-timestamp-refresh`. Patterns are directly applicable.
