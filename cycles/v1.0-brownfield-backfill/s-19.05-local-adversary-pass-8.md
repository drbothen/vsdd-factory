# S-19.05 LOCAL Adversary Pass-8 — NOT-CLEAN

**Date:** 2026-07-13
**Story:** S-19.05 — Observability gaps: async plugin completion telemetry + VSDD_SINK_FILE release-mode opt-in
**Pass:** 8 of BC-5.39.001 cascade
**Result:** NOT-CLEAN B0/H0/M1/L0
**Streak:** 0/3 (unchanged)
**Source HEAD:** b9dea7ce
**Prior Pass:** pass-7 NOT-CLEAN B0/H0/M2/L0 (both findings CLOSED; streak 0/3)

## Summary

1 MEDIUM finding. F-P8-001 MEDIUM: story propagation gap — AC-004 gate specification and §File Structure in S-19.05 v1.17 did not reflect the new `#[cfg(any(not(test), feature = "test-support"))]` pattern introduced by the pass-7 fix (b9dea7ce). Story-writer dispatched; S-19.05 v1.18 CLOSED same-session. Streak 0/3 — pass-9 required.

## Part A — Findings

### F-P8-001 — MEDIUM — Story propagation gap: AC-004 gate spec + §File Structure not reflecting test-support cfg(any()) (CLOSED same-session)

**POLICY:** POLICY 4 (description-bearing anchor-prose derives from target SoT); POLICY 8 (story must reflect implementation accurately)
**Location:** S-19.05 AC-004 gate specification; §File Structure vsdd_sink.rs row
**Issue:** The pass-7 implementer fix (b9dea7ce) introduced `#[cfg(any(not(test), feature = "test-support"))]` on the VSDD_ASYNC_DRAIN_WINDOW_MS read site and added `test-support` as a Cargo feature. The story (at v1.17) was authored before this cfg(any()) pattern existed. Consequently:
(a) AC-004 static gate still read `grep -q '#\[cfg(not(test))\]'` which would match the OLD form but not the NEW `cfg(any())` form. The gate would pass on a regression to the old form.
(b) §File Structure vsdd_sink.rs row described the cfg as `#[cfg(debug_assertions)]` (from the original BC-3.08.001 derivation), not the post-fix `cfg(any(not(test), feature = "test-support"))` form.
**Fix:** story-writer; S-19.05 v1.18: AC-004 static gate updated to `grep -q '#\[cfg(any'` form that matches cfg(any()); §File Structure vsdd_sink.rs row updated to reflect post-fix cfg form; test-support feature Cargo.toml row added to §File Structure.
**Status:** CLOSED same-session — S-19.05 v1.18 (story-writer)
**Routing:** story-writer

## Fix Chain

| Leg | Agent | SHA | Change |
|-----|-------|-----|--------|
| story-writer | story-writer | — | S-19.05 v1.18: AC-004 gate → cfg(any()) form; §File Structure vsdd_sink.rs + Cargo.toml test-support feature rows updated |

## Cascade Trajectory

pass-1 BLOCKER(1B+2H+2M+0L) [streak 0/3] → pass-2 CLEAN(0) [streak 1/3] → pass-3 NOT-CLEAN(0B+0H+1M+3L) [streak 0/3] → pass-4 CLEAN [streak 1/3] → pass-5 NOT-CLEAN(0B+0H+1M+2L) [streak 0/3] → pass-6 NOT-CLEAN(0B+0H+1M+0L) [streak 0/3] → pass-7 NOT-CLEAN(0B+0H+2M+0L) [streak 0/3] → pass-8 NOT-CLEAN(0B+0H+1M+0L) [streak 0/3]

## Next Step

Pass-9 required. Source HEAD: b9dea7ce (unchanged — only story updated; code HEAD unchanged). F-P8-001 CLOSED same-session by S-19.05 v1.18.
