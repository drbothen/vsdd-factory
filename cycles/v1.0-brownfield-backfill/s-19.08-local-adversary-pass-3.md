# S-19.08 LOCAL Adversary Pass-3 — NOT-CLEAN

**Date:** 2026-07-14
**Story:** S-19.08 — verify-state-timestamp-refresh: raise 64 KiB byte cap to 256 KiB + wire extract_frontmatter
**Pass:** 3 of BC-5.39.001 cascade
**Result:** NOT-CLEAN B0/H0/M0/L1
**Streak:** 0/3 UNCHANGED
**Source HEAD:** 7f8d8b7b
**Branch:** feature/S-19.08 (base 7b35c8e4)
**Commits reviewed (since pass-2):** 78e77910 (test-writer: 5 tests incl. 3 body-target reds) / 7f8d8b7b (implementer: two-variable split on_disk_field_content vs on_disk_reconstruction_base)
**Prior Pass:** pass-2 NOT-CLEAN B0/H1/M1/L2-accepted (streak 1/3→0/3 RESET)

## Summary

F-P2-001 HIGH and F-P2-002 MEDIUM are CLOSED-VERIFIED. The implementer introduced a structural two-variable split: `on_disk_reconstruction_base` carries the full file content for Edit/MultiEdit match reconstruction; `on_disk_field_content` carries the frontmatter-only slice for timestamp and lock-subfield extraction. Adversary grep-verified every callsite: no truncated-slice reconstruction path remains; no full-content field-extraction path violates Invariant 7. The three body-target negative tests added by test-writer flip Block→Continue under the pre-fix base, confirming they are load-bearing regression guards.

1 new LOW finding: F-P3-001 is a fix-introduced coverage gap (TD-VSDD-059 class). The non-UTF-8-body fallback in the guard_logic Step 3 fallback arm (`on_disk_reconstruction_base → on_disk_field_content.clone()`) is not exercised by any Edit/MultiEdit reconstruction test. Behavior is correct by tracing; the fallback is unreachable under normal conditions. The orchestrator applied production-grade fix-in-scope over the adversary's offered acceptance and routed test-writer same-session for a fallback regression-lock test with mutation-check evidence (in flight at time of record). Note: volatile line-cites from the adversary report were NOT carried into this record — stable anchor used (guard_logic Step 3 fallback arm) per TD-VSDD-091.

2 LOWs from prior passes (F-P1-001, F-P1-002) independently re-derived with the same analysis; both carried accepted-with-record unchanged.

Streak stays at 0/3 on F-P3-001 LOW. F-P3-001 closure verify required then pass-4.

## Part A — Findings

### F-P2-001 — HIGH — [regression class: guard-bypass reintroduction] extract_frontmatter reconstruction-base truncation — CLOSED-VERIFIED

**Location:** `crates/hook-plugins/verify-state-timestamp-refresh/src/lib.rs` — two-variable split in Edit/MultiEdit dispatch paths (guard_logic Step 3–7)
**Fix verified:** Implementer commit 7f8d8b7b introduces a structural two-variable split. `on_disk_reconstruction_base` is populated with full file content and passed exclusively to `extract_edit_proposed`/`extract_multiedit_proposed` as the match base. `on_disk_field_content` is populated with the frontmatter-only slice and used only for `last_modified` timestamp extraction (Step 5) and lock-subfield extraction (Step 7). Adversary grep-verified every callsite in lib.rs:
- Edit/MultiEdit reconstruction reads `on_disk_reconstruction_base` (full content) — confirmed.
- Timestamp extraction and lock-subfield extraction read `on_disk_field_content` (frontmatter only) — confirmed.
- No path reconstructs from the truncated slice; no path passes full content to field-extraction — confirmed.
- Invariant 7 compliance restored: field extraction reads frontmatter only; reconstruction base is unrestricted.
**Load-bearing regression guard:** The three body-target negative tests added in 78e77910 assert `Block` for body-targeting edits with stale timestamps. Adversary confirmed these tests fail (produce `Continue` instead of `Block`) when run against the pre-fix reconstruction-base — establishing them as load-bearing regression guards, not tautologies.
**Status:** CLOSED-VERIFIED

---

### F-P2-002 — MEDIUM — [test-coverage gap] no body-targeting Edit/MultiEdit reconstruction test — CLOSED-VERIFIED

**Location:** `crates/hook-plugins/verify-state-timestamp-refresh/tests/integration_t006_no_output_too_large.rs` — Edit/MultiEdit reconstruction test variants
**Fix verified:** Test-writer commit 78e77910 adds 5 new tests including 3 body-target negative tests (Edit/MultiEdit `old_string` anchored to body content on delimiter-present files), boundary-spanning variants, and mixed-MultiEdit positives. The body-target negatives are load-bearing regression guards per the F-P2-001 closure verification above. The boundary-spanning and mixed-MultiEdit positives close the remaining coverage gap identified in pass-2.
**Status:** CLOSED-VERIFIED

---

### F-P3-001 — LOW — [fix-introduced coverage gap; TD-VSDD-059 class] non-UTF-8-body fallback not exercised by reconstruction tests — OPEN (routed test-writer, fix-in-scope in flight)

**Location:** `crates/hook-plugins/verify-state-timestamp-refresh/src/lib.rs` — guard_logic Step 3 fallback arm
**Observation:** The two-variable split introduced in 7f8d8b7b includes a fallback arm: when frontmatter extraction fails (non-UTF-8 body or parse error), `on_disk_reconstruction_base` is set to `on_disk_field_content.clone()`. This fallback is not exercised by any Edit/MultiEdit reconstruction test. The fallback path is unreachable under normal well-formed input (delimiter-present UTF-8 files), and behavior is correct by tracing: falling back to field_content is safe — it is a conservative path that may produce a FailOpen rather than a false Block. However, the absence of a test means a future refactor could silently break the fallback without a test-suite signal.
**Why production-grade, not accepted:** The fallback arm is new code introduced by the current fix burst. An uncovered new code path is a TD-VSDD-059 class gap (paper-fix detection: every claimed closure should have a load-bearing test). The adversary offered acceptance-with-record; the orchestrator applied the production-grade default and routed fix-in-scope.
**Routing:** test-writer (same-session; fallback regression-lock test with mutation-check evidence; in flight at time of record)
**Status:** OPEN — test-writer dispatch in flight

---

### F-P1-001 — LOW — T-001 const-pin question (carried accepted-with-record; independently re-derived pass-3)

**Location:** `crates/hook-plugins/verify-state-timestamp-refresh/tests/integration_t006_no_output_too_large.rs` — `test_BC_`-prefixed const-pin test
**Observation (re-derived):** T-001 asserts a const value equal to the production `READ_BOUNDED_CAP_BYTES` constant without calling a production function through the public API. Pattern superficially resembles a tautological const-echo test.
**Why accepted (unchanged):** AC-001-mandated pin; references production symbol directly; byte-identical to S-19.02 merged sibling pattern accepted by same reasoning. Not a tautology.
**Status:** Carried accepted-with-record; open follow-up: none.

---

### F-P1-002 — LOW — [process-gap] BC-5.40.001 VP Anchors placeholder text (carried accepted-with-record; independently re-derived pass-3)

**Location:** `.factory/specs/behavioral-contracts/ss-05/BC-5.40.001.md` — "VP Anchors" / "Verification Properties" section
**Observation (re-derived):** BC-5.40.001 states S-19.08 unit-test VP IDs are "to be assigned by state-manager after S-19.08 VP authoring pass." VP-096 reuse is already active in the VP-INDEX; the placeholder text is a bookkeeping gap.
**Why accepted (unchanged):** Outside code perimeter; no effect on compilation, test execution, or AC gate. Correct target: state-manager VP-authoring bookkeeping step at S-19.08 post-merge/convergence burst.
**Status:** Carried accepted-with-record; routing: product-owner/state-manager — post-story burst.

---

## Could-Not-Verify (recorded without routing)

- **Live wasmtime sandbox runtime:** AC-004 integration test exercises cap-enforcement through an in-process mock comparator, not a live wasmtime harness. Consistent with story test design and S-19.02 sibling precedent. Recorded without routing.
- **Operator-level dispatcher logs:** Adversary cannot access the running dispatcher telemetry for this crate's hook invocations. Recorded without routing.
- **Invariant 8 u64 fields via log_warn:** Invariant 8 boundary fields are delivered through a `log_warn`-formatted string rather than a structured type. This is parity with the verify-factory-lock sibling plugin and is a pre-accepted design; no routing implied.

## Coverage

Adversary examined the following artifacts at HEAD 7f8d8b7b (diff base 7b35c8e4→7f8d8b7b):

- S-19.08 story v1.1 (all ACs, gate forms, task list, Architecture Compliance Rules)
- BC-5.40.001 v1.2 (all invariants, preconditions, acceptance criteria, VP anchors section)
- `crates/hook-plugins/verify-state-timestamp-refresh/src/lib.rs` (full file; two-variable split: on_disk_reconstruction_base vs on_disk_field_content, all callsite paths)
- `crates/hook-plugins/verify-state-timestamp-refresh/tests/integration_t006_no_output_too_large.rs` (full integration test; all AC-gate rows including 5 new tests)
- `crates/factory-lock-parse/src/lib.rs` (unmodified confirmed — no scope bleed from fix commits)
- POLICY 21 (cap-raise registration) — hooks-registry.toml cap-value entry confirmed consistent with `READ_BOUNDED_CAP_BYTES`

Diff scope: touches only `lib.rs` and `integration_t006_no_output_too_large.rs` — no unintended scope bleed confirmed.

## Exit Gates (adversary independently verified)

- Crate test suite: **48+2 green** (original suite + 5 test-writer additions)
- `cargo clippy --workspace --all-targets -- -D warnings`: **clean**
- `cargo fmt --check --all`: **clean**
- `factory-lock-parse`: **UNMODIFIED** — no behavioral regression in unchanged sibling plugin
- POLICY 21 satisfied: hooks-registry.toml cap-value consistent with production constant

## Cascade Trajectory

| Pass | Date | Result | B | H | M | L | Streak |
|------|------|--------|---|---|---|---|--------|
| 1 | 2026-07-14 | CLEAN | 0 | 0 | 0 | 2-accepted | 1/3 |
| 2 | 2026-07-14 | NOT-CLEAN | 0 | 1 | 1 | 2-accepted | 0/3 RESET |
| 3 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 1+2-accepted | 0/3 UNCHANGED |

## Next Step

F-P3-001 closure verify (test-writer fallback regression-lock test in flight) → pass-4.
