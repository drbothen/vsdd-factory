# S-19.09 LOCAL Adversary Pass-13 — CLEAN

**Date:** 2026-07-15
**Story:** S-19.09 — post-E-19 host ABI fixes: read_prefix production path registration, timeout_ms framing, telemetry hygiene
**Pass:** 13 of BC-5.39.001 cascade
**Result:** CLEAN B0/H0/M0/L0
**Streak:** 3/3 **BC-5.39.001 CONVERGED**
**Source HEAD reviewed:** 923ebff4
**Branch:** feature/S-19.09
**Story artifact reviewed at:** v1.5
**BC reviewed at:** BC-3.08.001 v1.24 (PO a15dbba9)

## Summary

Pass-13 reviewed story artifact at v1.5 and implementation at HEAD 923ebff4 against BC-3.08.001 v1.24, BC-1.17.001 v1.7, and ADR-025 v1.16 §D16-19 — all three normative specs read directly (`.factory/` worktree mounted). Full workspace gate run directly by the adversary: `cargo test --workspace --all-targets` — 185 suites ok, 0 failures; `cargo clippy --workspace --all-targets -- -D warnings` — clean; `cargo fmt --check --all` — clean; `bats host-abi-hygiene.bats` — 9/9 green.

ZERO new findings. No new blockers, highs, mediums, or lows.

All eleven cascade findings confirmed closed with pointed evidence:
- **F-P1-001 MEDIUM CLOSED-VERIFIED** — two-linker comment in `read_file.rs` rewritten at 4ecba42d (both paths yield `Vec::new()` via shared ptr==0 guard; `read_prefix.rs` sibling swept — no matching comment).
- **F-P1-002 LOW CLOSED-VERIFIED** — `plugin.timeout` and `plugin.completed` production emitters migrated to named consts at 8913c2e9; crate-wide scan confirmed zero remaining production bare literals for all four event names.
- **F-P2-001 MEDIUM CLOSED-VERIFIED** — T-002 extended to extract `out_len` from `memory[4:8]` and assert byte-equality; mutation-kill confirmed; T-002b `head_c` bound variant present.
- **F-P2-002 LOW CLOSED-VERIFIED** — T-002 temp-file migrated to `tempdir`-based isolation; T-001/T-003 sibling sweep confirmed file-free.
- **F-P3-001 LOW CLOSED-VERIFIED** — four `assert_eq!` value-pin tests in `internal_log.rs` `#[cfg(test)]` for `PLUGIN_COMPLETED`/`PLUGIN_TIMEOUT`; mutation-kill confirmed.
- **F-P4-001 LOW CLOSED-VERIFIED** — story v1.2 (00fd8a70): seven `lines 670–744 of invoke.rs` volatile pins replaced with stable anchor `the \`read_file\` \`func_wrap\` block in \`setup_host_on_store_data\` (\`invoke.rs\`)`; 10 stable-anchor hits verified; zero body volatile pins remain.
- **F-P5-001 LOW CLOSED-VERIFIED** — story v1.3 (26e9f677): two §Narrative D20 singular `line NN` pins (`read_file.rs line 38`, `read_prefix.rs line 53`) replaced with stable anchors; full-story `line [0-9]+` general-pattern sweep confirmed exactly two body sites; residual matches exclusively historical-by-construction in frontmatter/changelog.
- **F-P6-001 LOW CLOSED-VERIFIED** — T-015 empty-body branch present at HEAD 923ebff4; Assert 2 mutation-kill confirmed; coverage load-bearing.
- **F-P8-001 LOW CLOSED** — BC leg: BC-3.08.001 v1.24 adds `timestamp` to Event 6 §Mandatory-fields + wire example. Story leg: via F-P9-001 closure → v1.5.
- **F-P9-001 LOW CLOSED-VERIFIED** — story v1.5 (dcc0c7cc): five §Common Fields timestamp claims corrected to §Postconditions Event 6 Mandatory-fields; semantic-class sweep (`grep -n "Common Fields"`) over all in-scope artifacts confirms seven residual hits all historical-by-construction (changelog prose, BC table formatting notes, Changelog entries); zero load-bearing claims remain.
- **F-P10-001 LOW CLOSED-VERIFIED** — emit_event.rs three §Common Fields timestamp code-comment sites corrected at HEAD 923ebff4 (D22 production comment, T-013 header comment, T-013 assertion message); cross-artifact gate re-run at widest scope confirms §Common Fields timestamp claim-class PROVABLY DEAD across all in-scope artifacts.

Thirteenth-perspective marshalling audit: `needed == out_cap` precondition sound — `prepare()` returns body bounded by `max_bytes` (explicit `take(max_bytes as u64)` on the reader); no silent-skip path for oversized reads. Truncation bound: `out_ptr + body.len()` never exceeds allocated memory (grow pages cover `body.len()`). WAT layout non-clobbering: T-002/T-002b arg slots at 0..8, body at 131072+ (after 2-page grow); no overlap. Timestamp field in `plugin.completed` async events passes through the `emit_internal` pipeline without hitting the `RESERVED_FIELDS` filter — `timestamp` is not in the filter list; filter confirmed by adversary direct read of filter logic at HEAD 923ebff4.

Four accepted-with-record observations carried forward to human merge gate (pre-existing, untouched by S-19.09, outside diff perimeter):
- `emit_dispatcher_schema_mismatch` doc/code field drift: doc comment lists `got`/`expected` field names but code emits `found_version`/`expected_version`/`error_code`; micro-cleanup candidate.
- darwin `mktemp` non-trailing-Xs template bug in `resolver-integration.bats`: BSD mktemp rejects non-trailing-Xs template string; pre-existing test-infra defect; CI-Linux unaffected; candidate future story rider.
- `memory.grow`-failure branch untested for `read_file` + `read_prefix`: template inheritance; testable via `(memory 1 1)` + oversized file; out-of-AC-scope for S-19.09; candidate follow-on hardening story.

Four run-all.sh failures confirmed environmental (identical on develop base; none trace to the S-19.09 diff):
1. `pass-real-state-md-snapshot` — `.factory/` worktree unmounted in feature worktrees.
2. `resolver-integration` — BSD/darwin mktemp non-trailing-Xs (see above).
3. `check-harness-version` — passes individually; run-all.sh state pollution.
4. `codify-lessons` — passes individually; same run-all.sh state pollution.

**BC-5.39.001 CONVERGED.** Three consecutive CLEAN passes: 11, 12, 13. Streak 2/3 → 3/3. Story ready for status promotion draft→ready and PR creation.

## Part A — Findings

No findings. CLEAN pass.

---

## Closed Findings (Carried — Full Attestation at HEAD 923ebff4 / Story v1.5 / BC v1.24)

### F-P1-001 — MEDIUM — CLOSED-VERIFIED at 4ecba42d

Two-linker comment in `read_file.rs` rewritten at 4ecba42d. Both the test-path (`Linker<HostContext>` / `setup_linker`) and production-path (`Linker<StoreData>` / `setup_host_on_store_data`) register calls write body bytes to guest memory at address 0 via `write_wasm_bytes`, then return `out_ptr=0`. The `ptr==0` guard in `read_owned_bytes` yields `Vec::new()` on both paths — bytes discarded in both cases. ADR-025 §Decision 17 architectural claim (two-linker duality) is accurate; the comment now faithfully describes the shared discard semantics. `read_prefix.rs` sibling: no matching comment added (nothing to correct; no such duality comment exists there).

### F-P1-002 — LOW — CLOSED-VERIFIED at 8913c2e9

Production emitters for `plugin.timeout` (`emit_plugin_timeout_async`) and `plugin.completed` (`emit_plugin_completed_async`) in `emit_event.rs` migrated to named consts `PLUGIN_TIMEOUT` / `PLUGIN_COMPLETED` at 8913c2e9. Crate-wide scan at HEAD 923ebff4: remaining `"plugin.timeout"` / `"plugin.completed"` occurrences are const definitions or `#[cfg(test)]` fixtures — zero production bare literals for all four event names.

### F-P2-001 — MEDIUM — CLOSED-VERIFIED at HEAD following pass-2 closure

T-002 (`bytes_correct`) now: (1) extracts `out_len` from `memory[4:8]` as LE-u32; (2) reads `memory[out_ptr..out_ptr+out_len]`; (3) asserts byte-equality against fixture content. T-002b (`head_c`) applies PC-2 clamp semantics. Mutation-kill: adversary confirmed test fails when fixture content is altered. End-to-end D19 production marshalling coverage sound.

### F-P2-002 — LOW — CLOSED-VERIFIED at HEAD following pass-2 closure

T-002 temp-file migrated to `tempdir`-based isolation. T-001 and T-003 sibling sweep: both are file-free (no temp-file artifacts). Parallel execution collision risk eliminated.

### F-P3-001 — LOW — CLOSED-VERIFIED at HEAD following pass-3 closure

Four `assert_eq!` value-pin tests present in `internal_log.rs` `#[cfg(test)]`: two for `PLUGIN_COMPLETED` (`plugin.completed`) and two for `PLUGIN_TIMEOUT` (`plugin.timeout`). Mutation-kill: test fails when either constant's string value is altered. Spec-mandated T-009/T-010 artifact form satisfied.

### F-P4-001 — LOW — CLOSED-VERIFIED at story v1.2 (commit 00fd8a70)

Story v1.2 replaced seven `lines 670–744 of invoke.rs` volatile pins with stable anchor `the \`read_file\` \`func_wrap\` block in \`setup_host_on_store_data\` (\`invoke.rs\`)`. Independent verification: 10 stable-anchor hits; `grep -E "invoke\.rs:670|invoke\.rs lines|670[–-]744"` on story body → 0 hits; residual 670/744 digit-string matches exclusively in `last_amended`, `closes[]`, `modified[]`, and Changelog v1.2 row — all historical-by-construction. Closure is load-bearing.

### F-P5-001 — LOW — CLOSED-VERIFIED at story v1.3 (commit 26e9f677)

Story v1.3 replaced two §Narrative D20 singular `line NN` pins with stable anchors on the `let _ = timeout_ms;` drop line. Full-story general-pattern sweep `grep -nE "line [0-9]+"` confirmed exactly two body sites (both corrected); remaining matches exclusively in `last_amended`, `closes[]`, `modified[]`, and Changelog v1.3 row — all historical-by-construction.

### F-P6-001 — LOW — CLOSED-VERIFIED at HEAD 0e466d79 (carried)

T-015 empty-body branch present and load-bearing: Assert 2 (`out_ptr == 0` for empty body) mutation-kill confirmed at pass-7 (test fails when empty-body branch is removed). Coverage sound through HEAD 923ebff4 (no change to this path).

### F-P8-001 — LOW — CLOSED (BC leg v1.24; story leg via F-P9-001 → v1.5)

BC-3.08.001 v1.24 (PO a15dbba9): `timestamp` added to Event 6 §Mandatory-fields list and wire example, byte-consistent with Events 1-5. Story leg closed via F-P9-001 at v1.5 — see F-P9-001 below.

### F-P9-001 — LOW — CLOSED-VERIFIED at story v1.5 (dcc0c7cc)

Story v1.5 corrected all five body §Common Fields timestamp claims to §Postconditions Event 6 Mandatory-fields: (1) §Narrative D22 final sentence, (2) §Behavioral Contracts BC-3.08.001 Story-ACs cell, (3) §Token Budget BC-3.08.001 row, (4) §Architecture Compliance Rules timestamp Source cell, (5) AC-009 BC Trace cell (corrected at v1.4). Semantic-class gate (`grep -n "Common Fields"` over story): seven residual hits verified historical-by-construction — v1.4 last_amended record, F-P8-001 closes[] rationale, v1.4 modified[] entry, Changelog v1.4/v1.5 rows, and BC table formatting column header. Zero load-bearing §Common Fields timestamp claims remain.

### F-P10-001 — LOW — CLOSED-VERIFIED at HEAD 923ebff4

Three §Common Fields timestamp code-comment sites in `emit_event.rs` corrected: (1) D22 production comment, (2) T-013 test-header comment, (3) T-013 assertion message — all now cite §Postconditions Event 6 Mandatory-fields. Cross-artifact gate re-run at widest scope (`grep -n "Common Fields"` over all six in-scope changed files: invoke.rs, internal_log.rs, read_file.rs, read_prefix.rs, emit_event.rs, host-abi-hygiene.bats):
- `emit_event.rs:372` — `plugin_version §Common Fields` — GENUINELY-CORRECT: BC v1.24 §Common Fields prose covers Event 6 sync-path plugin_version parity.
- `emit_event.rs:384`, `:623`, `:654` — three §Common Fields timestamp citations — CORRECTED to §Postconditions Event 6 Mandatory-fields at HEAD 923ebff4.
- No hits in invoke.rs, internal_log.rs, read_file.rs, read_prefix.rs, host-abi-hygiene.bats.

§Common Fields timestamp claim-class PROVABLY DEAD across all in-scope artifacts.

---

## Accepted-with-Record (Carried)

**run-all.sh 4 failing suites — environmental, root-caused, outside diff perimeter (pass-13 confirmation):**
1. `pass-real-state-md-snapshot` — `.factory/` worktree unmounted in feature worktrees; fixture reads fail by construction. Pre-existing.
2. `resolver-integration` — BSD/darwin `mktemp` non-trailing-Xs template; pre-existing test-infra defect; CI-Linux green.
3. `check-harness-version` — passes individually; run-all.sh state pollution. Pre-existing.
4. `codify-lessons` — passes individually; same run-all.sh state pollution. Pre-existing.
All four confirmed identical on develop base; none trace to the S-19.09 diff.

**emit_dispatcher_schema_mismatch doc/code field drift (anchored, pending human merge gate):** doc comment lists `got`/`expected` field names; code emits `found_version`/`expected_version`/`error_code`. Pre-existing; outside diff perimeter; micro-cleanup candidate for future story.

**darwin mktemp non-trailing-Xs template bug in resolver-integration.bats (anchored, pending human merge gate):** Pre-existing test-infra defect; CI-Linux unaffected. Candidate story rider.

**memory.grow-failure branch untested for read_file + read_prefix (anchored, pending human merge gate):** Template inheritance; testable via `(memory 1 1)` + oversized file; out-of-AC-scope. Candidate follow-on hardening story.

**plugin_version §Common Fields citation retained-correct:** emit_event.rs:372 is load-bearing correct — BC v1.24 §Common Fields line-69 prose covers Event 6 sync-path plugin_version parity.

**D20 framing confirmed correct:** timeout_ms framing per D20 design brief present and correct at HEAD 923ebff4.

**S-19.07 scope boundary clean:** No premature ReadPrefixCaps code at HEAD 923ebff4.

**Zero new production unwrap/expect/println (pass-13 confirmation):** Static scan at HEAD 923ebff4 — zero new unwrap/expect/println in production-scoped code attributable to S-19.09.

**Sink-crate and tests/*.rs bare string literals:** Standing adjudication maintained. Cross-crate routing keys and test fixtures exempt.

**Epoch-deadline harness-only:** Standing adjudication maintained. Production dispatch always invokes `timeout_ms_to_epochs` with configured value.

**S-19.05-era "v1.21" provenance tags:** Non-load-bearing historical versioning annotation; TD-VSDD-091 code-comment exemption applies.

---

## Coverage

Adversary examined the following artifacts at review state:

- S-19.09 story v1.5 (whole-artifact integrity; all eleven finding closures re-verified; changelog monotonic v1.0→v1.5)
- BC-3.08.001 v1.24 (direct read: §Common Fields lines 53-72; Event 6 §Mandatory-fields + wire example; v1.24 changelog; POLICY 14 legs coherent; active status confirmed)
- BC-1.17.001 v1.7 (direct read: PC-1 correct bytes + non-zero out_ptr; PC-2 max_bytes clamp; PC-4 CAPABILITY_DENIED; AC-001/002/003 grounding; active status confirmed)
- ADR-025 v1.16 §D16-19 (direct read: D16 production-path registration CRITICAL; D17 two-linker out_ptr=0 protocol; D18 timeout_ms framing retraction; D19 -4 schema omission upheld)
- `emit_event.rs` at HEAD 923ebff4 (cross-artifact gate: per-hit adjudication of all "Common Fields" occurrences; F-P10-001 closure sustained; D22 timestamp persistence confirmed; emit_plugin_completed_async marshalling sound)
- invoke.rs, internal_log.rs, read_file.rs, read_prefix.rs at HEAD 923ebff4 (cross-artifact gate sweep — zero "Common Fields" hits; all sustained)
- `plugins/vsdd-factory/tests/host-abi-hygiene.bats` (T-004..T-012; 9/9 green; adversary-run)
- AC-001..AC-010 re-derived against code at HEAD 923ebff4 (all satisfied; unchanged)
- S-19.09 design brief D19-D22 per-deliverable (faithful mirror; no new divergence)

Full workspace test gate (adversary-run, widest scope):
- `cargo test --workspace --all-targets`: 185 suites ok, 0 failures
- `cargo clippy --workspace --all-targets -- -D warnings`: clean
- `cargo fmt --check --all`: clean
- `bats host-abi-hygiene.bats`: 9/9 green

Marshalling audit (thirteenth-perspective):
- `needed == out_cap` precondition: `prepare()` enforces `take(max_bytes as u64)` — no silent-skip path
- Truncation bound: grow pages cover `body.len()` exactly via `div_ceil(65536)`
- WAT layout: T-002/T-002b arg slots at 0..8; body at 131072+ post-grow; no overlap
- `timestamp` field not in `RESERVED_FIELDS` filter: direct read of filter logic confirmed

## Exit Gates (at HEAD 923ebff4, story v1.5 + BC v1.24 + BC v1.7 + ADR-025 v1.16)

- Cargo workspace test suite (all-targets): **185 suites ok, 0 failures**
- Bats T-004..T-012: **9/9 green**
- AC-008 static gate: **PASS**
- D22 timestamp persistence: **confirmed (emit_internal pipeline; RESERVED_FIELDS filter clear)**
- F-P10-001 cross-artifact gate: **PASS (§Common Fields timestamp class PROVABLY DEAD)**
- Design brief D19-D22 per-deliverable diff: **PASS**
- fmt/clippy: **clean**
- BC-1.17.001 v1.7 direct read: **PASS**
- BC-3.08.001 v1.24 direct read: **PASS**
- ADR-025 v1.16 §D16-19 direct read: **PASS**

## 4-Index at Persist Time

POLICY 14 gate (literal shell):

```
$ for f in \
    ".factory/specs/behavioral-contracts/BC-INDEX.md" \
    ".factory/specs/verification-properties/VP-INDEX.md" \
    ".factory/stories/STORY-INDEX.md" \
    ".factory/specs/architecture/ARCH-INDEX.md"; do
    name=$(echo "$f" | grep -oE '[^/]+\.md$')
    ver=$(grep -m1 '^version:' "$f" | grep -oE '"[0-9]+\.[0-9]+"' | tr -d '"')
    la_ver=$(grep -m1 '^last_amended:' "$f" | grep -oE '\(v[0-9]+\.[0-9]+\)' | head -1 | grep -oE '[0-9]+\.[0-9]+')
    [ "$ver" = "$la_ver" ] && echo "PASS $name  version=$ver  last_amended_v=$la_ver" \
                           || echo "DRIFT $name  version=$ver  last_amended_v=$la_ver"
  done
PASS BC-INDEX.md  version=4.06  last_amended_v=4.06
PASS VP-INDEX.md  version=2.68  last_amended_v=2.68
PASS STORY-INDEX.md  version=4.197  last_amended_v=4.197
PASS ARCH-INDEX.md  version=3.02  last_amended_v=3.02
```

BC-INDEX v4.06 / VP-INDEX v2.68 / STORY-INDEX v4.197 (pre-convergence-burst) / ARCH-INDEX v3.02

## Cascade Trajectory

| Pass | Date | Result | B | H | M | L | Streak |
|------|------|--------|---|---|---|---|--------|
| 1 | 2026-07-15 | NOT-CLEAN | 0 | 0 | 1 | 1 | 0/3 |
| 2 | 2026-07-15 | NOT-CLEAN | 0 | 0 | 1 | 1 | 0/3 |
| 3 | 2026-07-15 | NOT-CLEAN | 0 | 0 | 0 | 1 | 0/3 |
| 4 | 2026-07-15 | NOT-CLEAN | 0 | 0 | 0 | 1 | 0/3 |
| 5 | 2026-07-15 | NOT-CLEAN | 0 | 0 | 0 | 1 | 0/3 |
| 6 | 2026-07-15 | NOT-CLEAN | 0 | 0 | 0 | 1 | 0/3 |
| 7 | 2026-07-15 | CLEAN | 0 | 0 | 0 | 0 | 1/3 |
| 8 | 2026-07-15 | NOT-CLEAN | 0 | 0 | 0 | 1 | 0/3 |
| 9 | 2026-07-15 | NOT-CLEAN | 0 | 0 | 0 | 1 | 0/3 |
| 10 | 2026-07-15 | NOT-CLEAN | 0 | 0 | 0 | 1 | 0/3 |
| 11 | 2026-07-15 | CLEAN | 0 | 0 | 0 | 0 | 1/3 |
| 12 | 2026-07-15 | CLEAN | 0 | 0 | 0 | 0 | 2/3 |
| 13 | 2026-07-15 | CLEAN | 0 | 0 | 0 | 0 | **3/3 CONVERGED** |

## BC-5.39.001 Convergence Declaration

Three consecutive CLEAN passes (11 + 12 + 13) at HEAD 923ebff4, story v1.5, BC-3.08.001 v1.24.

**BC-5.39.001 strict 3-CLEAN PROTOCOL SATISFIED. S-19.09 LOCAL adversarial cascade CONVERGED.**

Next: state-manager convergence burst (story v1.5→v1.6 draft→ready; STORY-INDEX v4.197→v4.198; 4 lessons codified; STATE.md SRC refresh). Post-convergence: devops-engineer PR creation → human merge gate.
