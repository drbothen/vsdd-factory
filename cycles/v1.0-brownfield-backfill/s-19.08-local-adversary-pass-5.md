# S-19.08 LOCAL Adversary Pass-5 — NOT-CLEAN

**Date:** 2026-07-14
**Story:** S-19.08 — verify-state-timestamp-refresh: raise 64 KiB byte cap to 256 KiB + wire extract_frontmatter
**Pass:** 5 of BC-5.39.001 cascade
**Result:** NOT-CLEAN B0/H0/M0/L1
**Streak:** 1/3 → 0/3 RESET
**Source HEAD:** 7fc9bb7f
**Branch:** feature/S-19.08 (base 7b35c8e4)
**Commits reviewed (since pass-4):** none (static analysis pass; same HEAD)
**Prior Pass:** pass-4 CLEAN B0/H0/M0/L0 (streak 1/3)

## Summary

One LOW finding (F-P5-001) identified: integration_t006 AC-004 assertion is vacuous — the filter greps the `log_warn` stream for strings ("output_too_large", "outputtoolarge", "StateReadError") that `guard_logic` never emits on the fail-open path. The real fail-open path emits "fail-open read-error (STATE.md unreadable)" and the soft-warn path emits "state_md_approaching_cap"; the actual `output_too_large` event is host-emitted (read_file.rs TooLarge arm) and is unobservable in the mocked harness. The AC-004 string-match assertion therefore cannot fail due to missing guard logic — it is a dead assertion providing false confidence that the output_too_large guard fires. Mitigation acknowledged: the Block(TimestampStale)-at-70KiB assertion is a genuine operational proxy (Red-Gates at old cap); shipped code correct. Finding ROUTED test-writer fix-in-scope (live fail-open read-error assertion + operational-proxy doc note; in flight per orchestrator production-grade override of offered acceptance).

Additional in-scope sweep (orchestrator-directed, same session): lib.rs top doc comment omits the ≤262144 upper bound of the soft-warn range (twice-noted accepted-with-record) — ROUTED implementer for one-line precision fix after test-writer commit.

Pass-5 positive coverage confirmed: hooks-registry.toml entry conformance verified (PreToolUse; tool regex `^(Edit|Write|MultiEdit)$`; on_error=continue; path_allow .factory/STATE.md; no max_bytes field — correct plugin-side-const design); cross-plugin parity with verify-factory-lock exact (cap constant, soft-warn expression, event format); synthetic delimiter re-append traced safe (no-trailing-newline, mid-line ---, CRLF, duplicate-key first-wins/last-wins consistency); Invariant 7 proposed-content scope adjudicated satisfied.

2 LOWs from prior passes (F-P1-001, F-P1-002) independently re-derived; both carried accepted-with-record unchanged. Streak resets 1/3 → 0/3. Pass-6 required after F-P5-001 closure.

## Part A — Findings

### F-P5-001 — LOW — [test-integrity; vacuous assertion] AC-004 filter greps log_warn stream for strings guard_logic never emits

**Location:** `crates/hook-plugins/verify-state-timestamp-refresh/tests/integration_t006_no_output_too_large.rs` — AC-004 gate assertions
**Observation:** The AC-004 integration test filters the `log_warn` captured stream for the strings "output_too_large", "outputtoolarge", and "StateReadError". None of these strings are emitted by `guard_logic` on the fail-open path. The actual fail-open path (STATE.md unreadable) emits "fail-open read-error (STATE.md unreadable)"; the soft-warn path emits "state_md_approaching_cap". The real `output_too_large` event is host-emitted (read_file.rs TooLarge arm) and is unobservable in the mocked harness. The AC-004 string-match assertion is therefore a dead assertion: it cannot fail even if the guard logic were entirely removed or mutated, providing false confidence.
**Mitigation acknowledged:** The Block(TimestampStale)-at-70KiB assertion is a genuine operational proxy (Red-Gates at old cap); the shipped code itself is correct. The vacuous assertion does not affect production behavior.
**Routing:** ROUTED test-writer fix-in-scope — live fail-open read-error assertion (asserting that the fail-open path emits the correct string) plus operational-proxy doc note clarifying the Block(TimestampStale)-at-70KiB as an intentional proxy for the host-side cap event. In flight per orchestrator production-grade override of offered acceptance.
**Status:** OPEN — test-writer fix in flight

---

### F-P1-001 — LOW — T-001 const-pin question (carried accepted-with-record; independently re-derived pass-5)

**Location:** `crates/hook-plugins/verify-state-timestamp-refresh/tests/integration_t006_no_output_too_large.rs` — `test_BC_`-prefixed const-pin test
**Observation (re-derived):** T-001 asserts a const value equal to the production `READ_BOUNDED_CAP_BYTES` constant without calling a production function through the public API. Pattern superficially resembles a tautological const-echo test.
**Why accepted (unchanged):** AC-001-mandated pin; references production symbol directly; byte-identical to S-19.02 merged sibling pattern accepted by same reasoning. Not a tautology.
**Status:** Carried accepted-with-record; open follow-up: none.

---

### F-P1-002 — LOW — [process-gap] BC-5.40.001 VP Anchors placeholder text (carried accepted-with-record; independently re-derived pass-5)

**Location:** `.factory/specs/behavioral-contracts/ss-05/BC-5.40.001.md` — "VP Anchors" / "Verification Properties" section
**Observation (re-derived):** BC-5.40.001 states S-19.08 unit-test VP IDs are "to be assigned by state-manager after S-19.08 VP authoring pass." VP-096 reuse is already active in the VP-INDEX; the placeholder text is a bookkeeping gap.
**Why accepted (unchanged):** Outside code perimeter; no effect on compilation, test execution, or AC gate. Correct target: state-manager VP-authoring bookkeeping step at S-19.08 post-merge/convergence burst.
**Status:** Carried accepted-with-record; routing: product-owner/state-manager — post-story burst.

---

## Could-Not-Verify

- **Cargo suite execution this pass:** Adversary did not run the cargo suite this pass (static analysis only). Orchestrator to confirm all gates green before merge.
- **Real dispatcher-level capability_denied absence:** VSDD_SINK_FILE run out of harness scope; capability_denied path cannot be verified without live dispatcher invocation.

## Pass-5 Positive Coverage

Adversary verified the following at HEAD 7fc9bb7f (static analysis):

- `plugins/vsdd-factory/hooks-registry.toml` — S-19.08 plugin entry: trigger=PreToolUse; tool regex `^(Edit|Write|MultiEdit)$`; on_error=continue; path_allow=`.factory/STATE.md`; no max_bytes field (correct plugin-side-const design, consistent with POLICY 21 and verify-factory-lock sibling)
- Cross-plugin parity with verify-factory-lock: cap constant (`READ_BOUNDED_CAP_BYTES = 262144`), soft-warn expression, event format strings — all consistent
- Synthetic delimiter re-append edge cases traced safe: no-trailing-newline (newline appended before ---), mid-line --- (delimiter search anchored to line-start), CRLF (normalised), duplicate-key (first-wins/last-wins consistency documented)
- Invariant 7 proposed-content scope: `extract_top_level_field` / `extract_lock_subfields` self-bound to frontmatter region; BC Invariant 7 text ties `extract_frontmatter` to `host::read_file` bytes — adjudicated satisfied
- POLICY 5 (function-name anchors only in new comments): PASS
- POLICY 11: PASS

## In-Scope Sweep (orchestrator-directed)

- lib.rs top doc comment omits the ≤262144 upper bound of the soft-warn range. Twice-noted accepted-with-record. ROUTED implementer for one-line precision fix after test-writer commit (doc-precision, not a logic defect).

## Accepted-With-Record (re-affirmed pass-5)

- Invariant 8 log_warn formatted string (parity design with verify-factory-lock sibling; pre-accepted)
- Story Test Plan T-001..T-007 vs larger suite (regression-lock convention; pre-accepted)
- F-P1-001 / F-P1-002 per above

## Cascade Trajectory

| Pass | Date | Result | B | H | M | L | Streak |
|------|------|--------|---|---|---|---|--------|
| 1 | 2026-07-14 | CLEAN | 0 | 0 | 0 | 2-accepted | 1/3 |
| 2 | 2026-07-14 | NOT-CLEAN | 0 | 1 | 1 | 2-accepted | 0/3 RESET |
| 3 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 1+2-accepted | 0/3 UNCHANGED |
| 4 | 2026-07-14 | CLEAN | 0 | 0 | 0 | 0+2-accepted | 1/3 |
| 5 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 1+2-accepted | 0/3 RESET |

## Next Step

F-P5-001 closure (test-writer: live fail-open read-error assertion + operational-proxy doc note) + lib.rs doc sweep (implementer: one-line soft-warn range precision fix) → pass-6.
