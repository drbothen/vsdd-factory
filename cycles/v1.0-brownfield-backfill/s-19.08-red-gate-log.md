# Red Gate Log — S-19.08 (verify-state-timestamp-refresh: raise 64 KiB byte cap to 256 KiB + wire extract_frontmatter)

**Date:** 2026-07-14
**Branch:** feature/S-19.08 @ 1781c0d4 (test commit; stubs base 7b35c8e4)
**Test Writer:** vsdd-factory:test-writer
**Status:** RED_GATE_VERIFIED

## Summary

| Story | New Tests Written | All New Tests Fail (Red)? | Pre-existing Tests | Gate |
|-------|------------------|--------------------------|-------------------|------|
| S-19.08 | 8 | YES — 8 FAIL, 0 pass | 37 PASS | PASSED |

## Red Gate Verification

**Command:** `cargo test -p verify-state-timestamp-refresh --all-targets`

**Result:** RED GATE PASSED. 8 new tests FAIL with assertion errors; 37 pre-existing tests PASS; 0 new tests pass against unmodified code.

### Unit Tests (lib.rs `#[cfg(test)]`)

| Test | AC | BC Trace | Failure Reason | Status |
|------|----|----------|----------------|--------|
| T-001 | AC-001 | BC-5.40.001 v1.2 Precondition 6 | `assert_eq!(STATE_MD_MAX_BYTES, 262144)` fails: left:65536 right:262144 | FAIL (expected) |
| T-002 | AC-002 | BC-5.40.001 v1.2 PC4 | `STATE_MD_MAX_BYTES (65536) must be >= 70000` | FAIL (expected) |
| T-003 | AC-002 | BC-5.40.001 v1.2 PC4 | `STATE_MD_MAX_BYTES (65536) must be >= 70000` | FAIL (expected) |
| T-004 | AC-003 | BC-5.40.001 v1.2 Precondition 6 + Invariant 7 | `extract_frontmatter NOT wired — String::from_utf8 fails on full bytes → fail-open Continue` (structural red via non-UTF-8 body fixture) | FAIL (expected) |
| T-005 | AC-003 | BC-5.40.001 v1.2 Invariant 7 | cap precondition fails (behavioral assertion is regression-preservation; red via cap gate) | FAIL (expected) |
| T-007 (sub-tests A–E) | AC-005 | BC-5.40.001 v1.2 Invariant 8 | cap assertion fails; soft-warn boundary sub-tests A–E gated on cap = 262144 | FAIL (expected) |

### Integration Tests (tests/integration_t006_no_output_too_large.rs)

| Test Function | AC | BC Trace | Failure Reason | Status |
|--------------|----|----------|----------------|--------|
| t006_zero_output_too_large_on_70kib_state_md | AC-004 | BC-5.40.001 v1.2 PC4 | cap precondition fails | FAIL (expected) |
| t006_companion_advanced_timestamp_70kib_continues | AC-004 | BC-5.40.001 v1.2 PC4 | cap precondition fails | FAIL (expected) |

## Regression Check

| Existing Tests | Status |
|---------------|--------|
| 37 pre-existing tests | all pass |

Zero regressions.

## Red Gate Density

- Unit: 6 failing / 6 new unit tests = **100% RED**
- Integration: 2 failing / 2 new integration tests = **100% RED**
- Combined: 8 failing / 8 total new tests = **100% RED** (target: ≥50% RED per BC-8.30.001 Postcondition 3; EXCEEDED)

## Failure Mode Verification

All 8 new tests fail via assertion errors — not via `todo!()` panics. The failure mechanism is correct per the stub design: the T-001 constant assertion fails on the existing value (65536 ≠ 262144), and all other tests gate on the same cap constant via a precondition check before reaching behavioral logic. T-004 uses a non-UTF-8 body fixture that causes `String::from_utf8` to fail, producing a structural red (fail-open Continue) that distinguishes the wired-`extract_frontmatter` path from the un-wired path.

No `#[should_panic]` masking. No vacuously-passing new tests.

## Hand-Off to Implementer

- Story ready for implementation: S-19.08
- Implementation tasks (in order):
  1. Raise `STATE_MD_MAX_BYTES` from 65536 to 262144 in `crates/hook-plugins/verify-state-timestamp-refresh/src/lib.rs` — unblocks T-001 and cascades T-002/T-003/T-005/T-007/t006 off cap-precondition failures
  2. Add `use factory_lock_parse::extract_frontmatter;` and call `extract_frontmatter(bytes)` before existing YAML parsing logic — unblocks T-004/T-005
  3. Confirm `factory-lock-parse` dep in `Cargo.toml` (expected already present per plugin doc comment; grep before assuming)
  4. Add soft-warn emission: `bytes_read` strictly > 200000 and ≤ 262144 → emit `state_md_approaching_cap` event with `bytes_read: u64` and `cap_bytes: u64` — unblocks T-007 sub-tests A–E
  5. Parity reference: S-19.02 (MERGED PR #610) implemented the identical fix for `verify-factory-lock`; patterns are directly applicable
