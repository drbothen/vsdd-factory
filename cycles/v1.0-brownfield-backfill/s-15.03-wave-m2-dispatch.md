---
document_type: dispatch-package
level: ops
title: "S-15.03 Wave — Milestone 2 Dispatch Package"
producer: state-manager
timestamp: 2026-05-16T00:00:00Z
phase: section-12-step-3-m2-dispatch
cycle: brownfield-backfill
inputs:
  - .factory/cycles/v1.0-brownfield-backfill/s-15.03-wave-plan-2026-05-15.md
  - .factory/STATE.md
input-hash: "ad1c745"
traces_to: .factory/cycles/v1.0-brownfield-backfill/s-15.03-wave-plan-2026-05-15.md
related_stories: [S-15.07, S-15.09, S-15.11, S-15.14]
develop_head_at_authoring: "c62f952c7307febcc65b6ab722ff02688dfe8c90"
factory_artifacts_head_at_authoring: "f1e88045"
---

# S-15.03 Wave — Milestone 2 Dispatch Package
## Self-Contained Dispatch Package (2026-05-16)

> **Post-CLEAR entry point.** Read this file top-to-bottom to dispatch the M2 stories with zero prior context.
> Verified against develop@c62f952c and factory-artifacts@f1e88045.

---

## Goal

Deliver the four Milestone 2 stories of the S-15.03 PRIORITY-A wave — all WASM Rust hook implementations:

1. **S-15.07** — validate-index-cite-refresh WASM Hook (Cross-Index Version Staleness Gate)
2. **S-15.09** — validate-state-structure WASM Hook Phase 1 (Banner wc-l, Dual-Margin, Trajectory-Tail Cardinality)
3. **S-15.11** — validate-burst-log WASM Hook (8-Block Completeness, h2 Heading Form, Dim-1 Cardinality)
4. **S-15.14** — validate-dispatch-advance WASM Hook (Verbatim current_step, 4-Index Completeness, Trajectory-Tail Derivation)

These four stories each require a new Rust WASM crate, hooks-registry.toml registration, WASM compilation, and full per-story-delivery pipeline (story-writer → test-writer → implementer → LOCAL adversary 3-CLEAN → pr-manager → state-manager post-merge). Total M2 calendar: ~4-8 days at continuous high agent throughput.

---

## Why This Exists

The S-15.03 wave plan was produced by the architect on 2026-05-15 and persisted to `.factory/cycles/v1.0-brownfield-backfill/s-15.03-wave-plan-2026-05-15.md`. M1 (S-15.06 + S-15.16 Part A + S-15.08) SHIPPED on 2026-05-16 — all three M1 stories are merged to develop. This dispatch package extracts the M2 execution instructions so a post-CLEAR orchestrator can dispatch S-15.07, S-15.09, S-15.11, and S-15.14 without reading the full 95-item plan document.

**Pattern:** This mirrors the SESSION-END DURABILITY BURST 2026-05-15 pattern that produced `s-15.03-wave-m1-dispatch.md` (commit in factory-artifacts at `bcc1e9ed`) after a prior /clear in that session.

---

## Scope

| Story | Title | Routing | Effort | Branch |
|-------|-------|---------|--------|--------|
| S-15.07 | validate-index-cite-refresh | story-writer → test-writer → implementer → pr-manager → state-manager | Multi-day | `feature/S-15.07-index-cite-refresh-hook` |
| S-15.09 | validate-state-structure Phase 1 | story-writer → test-writer → implementer → pr-manager → state-manager | Multi-day | `feature/S-15.09-validate-state-structure-p1` |
| S-15.11 | validate-burst-log | story-writer → test-writer → implementer → pr-manager → state-manager | Multi-day | `feature/S-15.11-validate-burst-log` |
| S-15.14 | validate-dispatch-advance | story-writer → test-writer → implementer → pr-manager → state-manager | Multi-day | `feature/S-15.14-validate-dispatch-advance` |

---

## Pre-Flight Gate (before dispatching any M2 story)

```bash
# Verify factory-artifacts worktree health
git -C .factory rev-parse --git-dir && git -C .factory branch --show-current
# Expected: factory-artifacts

# Verify develop HEAD (c62f952c = S-15.08 merge; or newer if subsequent PRs merged)
git rev-parse origin/develop
# Expected: c62f952c7307febcc65b6ab722ff02688dfe8c90 or newer

# Verify M1 complete — S-15.08 merged
git log origin/develop --oneline | grep -i "S-15.08\|dim2-gate"
# Expected: c62f952c feat(hooks): dim2-gate template library...

# Verify no in-flight PRs or dirty factory-artifacts worktree
git -C .factory status --porcelain
# Expected: empty (clean)
```

**First substantive action after worktree health: dispatch architect for M2 inter-story order adjudication (see section below).**

---

## Architect Dispatch (First Action)

Before dispatching any story-writer for M2, the orchestrator MUST dispatch the architect to adjudicate M2 inter-story order. The architect decision document produced here gates all M2 story-writer dispatches.

### Architect Task Prompt

Read the following files:
1. `.factory/cycles/v1.0-brownfield-backfill/s-15.03-wave-plan-2026-05-15.md` — Section 2 (story specs for S-15.07, S-15.09, S-15.11, S-15.14), Section 4 (Milestone 2 rationale and sequencing), Section 5 (Open Questions — all resolved).
2. `.factory/STATE.md` — Section 12 (Strict Engine-Discipline Ordering), Session Resume Checkpoint Section 4 (Active Tier-A).
3. Current `crates/` directory structure to identify any existing shared Rust crate patterns that M2 should follow.

Adjudicate the following questions and produce a decision document at `.factory/cycles/v1.0-brownfield-backfill/architect-m2-2026-05-NN.md` (substitute actual date). Mirror the structure of `.factory/cycles/v1.0-brownfield-backfill/architect-2026-05-15-td-66-67-split.md` as the format reference.

**Question 1: Shared schema crate for burst-log-consuming stories.**

S-15.09, S-15.11, and S-15.14 all consume STATE.md or burst-log.md structure. S-15.09 and S-15.14 both parse STATE.md. S-15.11 parses burst-log.md. The architect's wave plan (Section 2, S-15.09/S-15.14) notes "S-15.14 shares STATE.md domain with S-15.09; independent enough to parallel-develop."

Should the 3 STATE.md/burst-log parsing stories share a common schema crate (e.g., `crates/lint-hook-schema/`) that defines shared markdown parsing utilities? If yes:
- Propose a new sub-story identifier (e.g., S-15.07b or S-15.17 if sequential slot available) for a "shared lint-hook schema crate" that ships before S-15.09/S-15.11/S-15.14.
- Estimate effort for the shared crate (likely sub-day: struct definitions + regex patterns + no WASM compilation required for the schema crate itself).
- Confirm whether S-15.07 (index-cite-refresh) also benefits or whether it is sufficiently isolated.

If no shared crate is warranted (each story is independent enough): state the rationale explicitly.

**Question 2: M2 wave-1 story selection.**

Per the architect wave plan (Section 4, Milestone 2): "S-15.07 (index-cite-refresh) is the original story motivation and the highest-visibility deliverable. Recommend shipping first within Milestone 2." Confirm whether S-15.07 should be wave-1 (or if the shared schema crate ships first), and which story should be wave-2.

**Question 3: Parallel vs serial sequencing within M2.**

Can S-15.09 + S-15.11 + S-15.14 be developed in parallel (separate feature branches, no shared file conflicts in hooks-registry.toml)? The architect wave plan notes: "S-15.09 + S-15.14 timing: orchestrator coordinates timing so their hooks-registry registrations land in separate PRs without overlapping development windows. No simultaneous in-flight PRs touching the same hooks-registry.toml section" (per OQ-5 resolution).

Clarify: does OQ-5 mean these stories must ship *sequentially* (one at a time, no parallel in-flight PRs), or does it mean the hooks-registry.toml sections for each story are distinct enough that parallel branches are safe as long as merge ordering is managed? Recommend an operational strategy for the orchestrator.

**Question 4: Crate naming conventions.**

Confirm the canonical Rust crate naming pattern for M2 hook crates. S-15.08's templates landed in `plugins/vsdd-factory/hooks/dim2-gates/` (bash). For Rust WASM crates, confirm whether the pattern is `crates/lint-hook-<name>/` (e.g., `crates/lint-hook-index-cite-refresh/`) or `crates/<name>-hook/` or another pattern. This decision must be consistent across all 4 M2 stories.

### Expected Architect Output

A decision document at `.factory/cycles/v1.0-brownfield-backfill/architect-m2-2026-05-NN.md` containing:
- Adjudication for each of the 4 questions above
- Final M2 dispatch order (e.g., "S-15.07 first, then S-15.09, then S-15.11 and S-15.14 in parallel if safe, else serial")
- Any new sub-story proposals with story IDs and scope
- Confirmed crate naming convention for M2

The orchestrator uses this document to set the dispatch sequence for M2 story-writer dispatches.

---

## Per-Story Dispatch Template (after architect order confirmed)

Apply this template to each M2 story in the architect-confirmed order.

### Step 1: story-writer

Author a full story spec at `.factory/stories/S-15.NN-<slug>.md` with:
- Frontmatter: `status: draft`, `epic: E-12`, `effort: multi-day`, `bcs: [<new-BC-ID>]`
- Acceptance criteria mapped to D-NNN sub-clauses being closed (see architect wave plan Section 2 for each story's sub-item list)
- Tasks section enumerating: Rust crate scaffold, WASM target compilation, hooks-registry.toml entry, bats integration tests, LOCAL adversary cascade
- Implementation detail: cite hook-sdk ContextResolver + dispatcher registration patterns from existing WASM crate examples in `crates/`

Reference story format: `.factory/stories/S-15.08-dim2-gates-bash-templates.md` (the M1 story-writer product at factory-artifacts).

After authoring: register in STORY-INDEX (bump version); single factory-artifacts commit.

### Step 2: test-writer

Author failing bats integration tests in `plugins/vsdd-factory/tests/<story-slug>/`:
- Mirror full-stack plugin invocation pattern from `plugins/vsdd-factory/tests/full_stack_plugin_invocation.rs` (Rust integration test harness)
- Alternatively: bats test files in `plugins/vsdd-factory/tests/` triggering the PreToolUse/PostToolUse hook chain
- At minimum: PASS test (well-formed input → pass) + FAIL test (malformed input → block expected)
- Test must invoke the actual compiled WASM binary (not a mock); binary is compiled in Step 3

Since WASM binary does not exist yet, use a test-stub approach: write the bats test spec and mark as `@pending` until the implementer compiles the WASM. The test-writer and implementer may iterate.

### Step 3: implementer

Author the WASM Rust crate at `crates/<name-per-architect-convention>/`:
- `Cargo.toml`: workspace member, `crate-type = ["cdylib"]`, depends on `hook-sdk` workspace crate
- `src/lib.rs`: implement the hook logic per story acceptance criteria; use `host::read_file` for file access; emit `block_with_fix` on violations
- Target: `wasm32-wasip1` (or per existing WASM crate target pattern in workspace)
- Compiled output: `.wasm` binary to `plugins/vsdd-factory/hook-plugins/<name>.wasm`

Update `plugins/vsdd-factory/hooks-registry.toml`:
- Add plugin entry: `name`, `wasm_path`, `trigger_event` (PostToolUse), `file_pattern` (path pattern for the monitored file), `tier: sync`, `on_error: block` (unless architect specifies advisory-only)
- Confirm no overlap with existing plugin entries on same file-pattern

Run pre-flight 4-gate:
```bash
cargo fmt --check --all
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace --all-targets
cd plugins/vsdd-factory/tests && ./run-all.sh
```

All 4 must PASS. Expected: cargo gates PASS with new crate; bats suite includes new story tests (previously pending, now active).

**Pre-existing known non-failures to accept without investigation:**
- `sink-http bc_3_07_001_backoff` bats test failure — pre-existing, NOT M2's concern
- `F-P3-008` timing flake (ubuntu CI) — RESOLVED by S-15.05; if it reappears flag it but do not block M2

### Step 4: LOCAL adversary cascade (BC-5.39.001 3-CLEAN protocol)

Dispatch fresh-context adversary per `vsdd-factory:adversarial-review`. The adversary reads ONLY the story spec + diff vs develop base. Minimum 3 consecutive CLEAN or NITPICK-only passes (0 MEDIUM+ findings) for convergence. Any finding ≥ LOW resets streak to 0/3.

Mirror S-15.08 cascade trajectory tracking: record each pass result in factory-artifacts. Target: converge in ≤6 passes (S-15.08 took 6 passes + 4 fix-bursts; M2 WASM stories may require more due to Rust logic complexity).

**Special notes for WASM stories:**
- Adversary must verify the hook-sdk API surface is used correctly (no raw FFI where safe wrappers exist)
- Adversary must verify `block_with_fix` emitted with actionable remediation text (not just "FAIL")
- Adversary must verify bats tests cover both PASS and FAIL branches

### Step 5: pr-manager 9-step PR lifecycle

Standard 9-step pr-manager cycle to develop. Target branch: develop. Squash-merge.

PR body must include:
- Stories closed (S-15.NN)
- D-NNN sub-clauses closed (per story acceptance criteria)
- WASM compilation evidence (cargo build success + binary size)
- Pre-flight 4-gate results (cargo fmt/clippy/test/bats all PASS)
- LOCAL adversary cascade trajectory and convergence declaration

### Step 6: state-manager post-merge burst

After squash-merge to develop:
- Update STORY-INDEX: status → merged; bump version
- Update STATE.md: Phase Progress row for the story; Active Branches develop SHA update; Concurrent Cycles M2 progress update; Session Resume Checkpoint refresh
- Single factory-artifacts commit per TD-VSDD-053

---

## Pre-Flight 4-Gate (for each M2 story before pushing)

```bash
cargo fmt --check --all
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace --all-targets
cd plugins/vsdd-factory/tests && ./run-all.sh
```

All 4 must PASS. Pre-existing `sink-http bc_3_07_001_backoff` bats failure and resolved `F-P3-008` timing behavior are NOT M2's concern.

**WASM compilation gate (additional, per story):**

```bash
# Confirm WASM target is installed
rustup target list --installed | grep wasm32-wasip1

# Compile WASM crate (replace <name> with actual crate name per architect decision)
cargo build --release --target wasm32-wasip1 -p lint-hook-<name>

# Confirm output binary exists
ls -la plugins/vsdd-factory/hook-plugins/lint-hook-<name>.wasm
```

Expected: binary size in range 100KB-2MB (similar to existing WASM plugins); no linker errors.

---

## Story-by-Story Details (from architect wave plan Section 2)

### S-15.07 — validate-index-cite-refresh

**D-NNN sub-clauses closed:**
- D-405(c): ARCH-INDEX cites of BC/VP/STORY index versions must not be stale
- D-429(b): Cross-cell sibling sweep — STATE.md and INDEX.md cells citing same 4-index versions must cross-validate

**Hook trigger:** PostToolUse Edit/Write on `ARCH-INDEX.md`

**Core logic:** Read ARCH-INDEX, extract cited BC-INDEX/VP-INDEX/STORY-INDEX version strings (regex `BC-INDEX v\d+\.\d+`). Read each index file's frontmatter `version:` field via `host::read_file`. If any cited version < actual version, emit `block_with_fix` naming the stale cite. Extension: validate STATE.md trajectory-bearing cells and INDEX.md Convergence Status cells agree on same 4-index versions (D-429(b)).

**TDD seam:** bats — Edit ARCH-INDEX with stale `BC-INDEX v1.05` cite when BC-INDEX actual is v2.24 → expect block. Edit with current version → expect pass.

**Dependencies per architect wave plan:** None (can start immediately after architect adjudication). S-15.07 is the original story motivation and highest-visibility M2 deliverable; architect recommends shipping first within M2 (Section 4 Milestone 2 rationale).

**New BC required:** Anchor E-12 (Engine Governance).

---

### S-15.09 — validate-state-structure Phase 1

**D-NNN sub-clauses closed:**
- D-421(c) + D-422(c) + D-424(b) + D-428(d) + D-438(a) + D-440(d) + D-442(d): Banner wc-l discipline
- D-446(c): Dual-margin form — both margins must be present in banner
- D-433(e) + D-439(c) + D-451(c) extension: Trajectory-tail LENGTH=4
- D-432(b): Trajectory-tail canonical form

**Hook trigger:** PostToolUse Edit/Write on `STATE.md`

**Core logic:** Read STATE.md content, extract: (a) banner line-count claim vs newline count in content; (b) dual-margin values arithmetic check (both "from soft-target" and "from actual" present); (c) trajectory-tail string, validate `→(\d+)` match count equals 4. Emit `block_with_fix` per violation.

**TDD seam:** bats — Write STATE.md with banner claiming "399 lines" when content is 400 lines → expect block. Write STATE.md with 3-component tail → expect block. Write STATE.md with correct values → expect pass.

**Dependencies per architect wave plan:** Best started after S-15.07 (to share crate structural lessons). S-15.10 depends on S-15.09 crate scaffolding (M3). S-15.14 shares STATE.md domain but is independent enough to parallel-develop per OQ-5.

**New BC required:** Anchor E-12.

---

### S-15.11 — validate-burst-log

**D-NNN sub-clauses closed:**
- D-421(e) + D-438(d) + D-439(a): Burst-log h2 heading form
- D-444(c) + D-446(a): 8-block completeness gate — all 9 block types (Parent-commit, Adversary verdict, Files touched (Dim-1), Codifications, Dim-2 Attestation, Dim-5 Attestation, Dim-6 Attestation, Dim-7 Attestation, Closes) must be present
- D-432(e) + D-448(d)(i): Dim-1 cardinality — headline "N unique files" must match enumerated list count
- D-443(e)(ii): Own-burst h2 real-time at Commit A

**Hook trigger:** PostToolUse Edit/Write on `burst-log.md`

**Core logic:** Extract latest h2 heading, validate format (`## Burst: F5 pass-N fix burst (YYYY-MM-DD)` or equivalent). Count distinct block-type headings (regex `\*\*(Parent-commit|Adversary verdict|Files touched \(Dim-1\)|Codifications|Dim-2|Dim-5|Dim-6|Dim-7|Closes)\*\*`). Parse Dim-1 headline integer and list cardinality, assert equality. Emit `block_with_fix` on any violation.

**TDD seam:** bats — Write burst-log entry with only 6 blocks → expect block. Write burst-log with Dim-1 headline 5 but list of 7 files → expect block. Write burst-log with correctly formed entry → expect pass.

**Dependencies per architect wave plan:** None (independent of STATE.md validator crates).

**New BC required:** Anchor E-12.

---

### S-15.14 — validate-dispatch-advance

**D-NNN sub-clauses closed:**
- D-440(a): Dispatch-advance must cite full D-382..D-<latest> chain in current_step
- D-441(a) + D-442(a): Verbatim-strict current_step — no meta-commentary labels, prescribed clause order
- D-443(a): Clause-completeness gate — every prescribed clause must appear
- D-439(b): All 4 index versions cited in dispatch current_step
- D-441(b): 6-column INDEX.md adversary-pass row schema
- D-451(c): Trajectory-tail derived from INDEX.md canonical string

**Hook trigger:** PostToolUse Edit/Write on `STATE.md` (frontmatter field `current_step:`) and PostToolUse Edit/Write on `INDEX.md`

**Core logic:** Validate `current_step:` frontmatter field: (a) forbidden meta-commentary patterns (regex: `META-LEVEL-N WATCH|self-app TEST|expected verdict`); (b) all 4 index version patterns present; (c) trajectory-tail length = 4; (d) D-chain cite currency. Also validates INDEX.md adversary-pass rows are 6 columns on writes to INDEX.md.

**TDD seam:** bats — Write STATE.md frontmatter with `current_step: "META-LEVEL-N WATCH..."` → block. Write INDEX.md with 5-column row → block. Write STATE.md frontmatter omitting ARCH-INDEX version cite → block.

**Dependencies per architect wave plan:** S-15.09 crate patterns recommended as reference (same validate-state-structure domain). Per OQ-5: no simultaneous in-flight PRs touching same hooks-registry.toml section as S-15.09. Architect adjudication (Question 3 above) clarifies whether parallel development is safe.

**New BC required:** Anchor E-12.

---

## Hard Constraints

- **No AI attribution** in any commit message or file content.
- **Single factory-artifacts commit per burst** per TD-VSDD-053. No "Stage 1/2/backfill" subjects.
- **No `--no-verify`** — hook chain must run; investigate any blocks per CLAUDE.md Factory Hook Diagnostics.
- **All M2 stories require the full per-story-delivery pipeline** — story-writer → test-writer → implementer → LOCAL adversary 3-CLEAN → pr-manager → state-manager. No shortcuts.
- **No simultaneous in-flight PRs touching hooks-registry.toml** per OQ-5 resolution (architect wave plan Section 5). Confirm with architect adjudication document before attempting parallel development.
- **BC-5.39.001 3-CLEAN protocol:** Every M2 story requires 3 consecutive CLEAN or NITPICK-only adversary passes before PR dispatch. A single finding ≥ LOW resets streak to 0/3.
- **Squash-merge to develop** (same as M1; all prior stories used squash-merge).

---

## Post-M2 Next Steps (Section 12 Strict Ordering)

After M2 closes (all 4 stories merged + CI green + regression suite green):

| Step | Item | Gate |
|------|------|------|
| 3 (M1) | S-15.06 + S-15.08 + S-15.16 Part A | **COMPLETE 2026-05-16** |
| 3 (M2) | S-15.07 + S-15.09 + S-15.11 + S-15.14 | **ACTIVE (this document)** |
| 3 (M3) | S-15.10 + S-15.12 + S-15.13 + S-15.15 + S-15.16 Part B | M2 complete |
| 4 | E-10 sub-cycle resumption (pass-15 forward) | Step 3 (all milestones) complete |
| 5 | F5 cycle resumption (pass-75 forward) | Step 3 complete + explicit human direction |

M3 dispatch package should be authored in the same durability-burst pattern after M2 closes.

---

## Effort Estimates (per architect wave plan Section 6)

| Story | Effort class | Calendar estimate | ADR gating |
|-------|-------------|-------------------|------------|
| S-15.07 | Multi-day | ~1 orchestrator session (story-writer + test-writer + implementer + adversary + pr-manager) | None |
| S-15.09 | Multi-day | ~1 orchestrator session | None |
| S-15.11 | Multi-day | ~1 orchestrator session (simplest new crate in M2) | None |
| S-15.14 | Multi-day | ~1 orchestrator session | None |

Total M2 calendar estimate: **4-8 days** at continuous high agent throughput (one story per orchestrator session; each story requires full per-story adversary convergence loop).

Note: If architect adjudication introduces a shared schema sub-story (Question 1 above), add ~0.5 orchestrator sessions for that crate before the main M2 stories begin.
