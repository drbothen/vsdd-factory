---
document_type: dispatch-package
level: ops
title: "S-15.03 Wave — Milestone 2 Wave-3 Dispatch Package"
producer: state-manager
timestamp: 2026-05-17T23:59:00Z
phase: section-12-step-3-m2-wave-3-dispatch
cycle: brownfield-backfill
inputs:
  - .factory/cycles/v1.0-brownfield-backfill/s-15.03-wave-m2-dispatch.md
  - .factory/cycles/v1.0-brownfield-backfill/s-15.03-wave-m2-wave-2-dispatch.md
  - .factory/cycles/v1.0-brownfield-backfill/architect-m2-2026-05-16.md
  - .factory/cycles/v1.0-brownfield-backfill/architect-m2-q5-tool-attribute-2026-05-16.md
  - .factory/cycles/v1.0-brownfield-backfill/architect-m2-q6-bats-registry-scope-2026-05-16.md
  - .factory/stories/S-15.07-validate-index-cite-refresh.md
  - .factory/stories/S-15.11-validate-burst-log.md
  - .factory/stories/S-15.09-validate-state-structure-phase-1.md
  - .factory/specs/behavioral-contracts/ss-05/BC-5.39.005.md
  - .factory/cycles/v1.0-brownfield-backfill/s-15.03-wave-plan-2026-05-15.md
input-hash: "5af355e"
traces_to: .factory/cycles/v1.0-brownfield-backfill/s-15.03-wave-m2-dispatch.md
related_stories: [S-15.09]
develop_head_at_authoring: "6e0d54070eca90e897b60fb51996305fac45b60b"
factory_artifacts_head_at_authoring: "26490be7"
---

# S-15.03 Wave — Milestone 2 Wave-3 Dispatch Package

## Self-Contained Dispatch Package (2026-05-17 post-S-15.11-ship)

> **Post-CLEAR entry point.** Read this file top-to-bottom to dispatch S-15.09 with zero prior context.
> Verified against develop@6e0d5407 and factory-artifacts@26490be7.

---

## Goal

Deliver S-15.09 (validate-state-structure Phase 1 WASM hook), wave-3 of Milestone 2 of S-15.03
PRIORITY-A wave. After S-15.09 SHIPS, M2 wave-4 (S-15.14 validate-dispatch-advance) becomes
dispatch-ready.

---

## Why This Exists

S-15.11 (validate-burst-log WASM hook, M2 wave-2) SHIPPED 2026-05-17 via PR #146 squash-merge
at 6e0d5407. Per architect-m2-2026-05-16.md (factory-artifacts 624e9fab) D-473 fully-serial
inter-story order, S-15.09 is the next wave. This dispatch package extracts S-15.09-specific
execution instructions so a post-CLEAR orchestrator can dispatch the full per-story-delivery
cycle without reading the full M2 wave plan or having any prior conversation context.

**Parent dispatch document:** `s-15.03-wave-m2-dispatch.md` (factory-artifacts, input-hash ad1c745)
remains authoritative for wave-4 (S-15.14). This document is a wave-3 refinement, not a
replacement.

**RETROACTIVELY-COMPLETE NOTE (Step 1):** The S-15.09 story spec was already authored at
factory-artifacts commit `26490be7` by the story-writer dispatch at session-end 2026-05-17.
Step 1 of the per-story dispatch template (story-writer) is RETROACTIVELY-COMPLETE. Proceed
directly to Step 2 (test-writer).

---

## S-15.11 SHIPPED Summary (M2 wave-2 canonical reference)

- PR #146 squash-merge `6e0d5407` on develop 2026-05-17
- LOCAL adversary: **7-pass cascade CONVERGED 3/3**
  - Trajectory: LOW→HIGH→LOW→MEDIUM→CLEAN→CLEAN→CLEAN; 4 fix-bursts
  - Real defects caught: F-P2-001 HIGH (production `**` glob neuters hook via `canonicalize()`
    failure → silent CapabilityDenied → fail-open); F-P4-001 MEDIUM (UTF-8 char-boundary
    panic in `validate_h2_heading` on em-dash); 4 additional MEDIUM/LOW narrative drift
- BC-5.39.004 POL-14 auto-promoted draft→active; STORY-INDEX v3.38; BC-INDEX v2.29
- D-475 codified in brownfield decision-log
- S-7.02 SATISFIED: PG-S-15.11-bats-prod-registry-parity-gate codified in lessons.md +
  Drift Items entry OPEN target=S-15.03 PRIORITY-A
- S-15.11 is the **canonical template story** for all M2 WASM stories (alongside S-15.07);
  its spec + BC + cascade reports are reference for S-15.09's test-writer and implementer

---

## Scope

| Story | Title | Routing | Effort | Branch |
|-------|-------|---------|--------|--------|
| S-15.09 | validate-state-structure Phase 1 | (story-writer DONE) → test-writer → implementer → LOCAL adversary 3-CLEAN → pr-manager → state-manager | Multi-day (8 pts) | `feature/S-15.09-validate-state-structure-p1` |

---

## Pre-Flight Gate

Run these before dispatching test-writer:

```bash
# Verify factory-artifacts worktree health
git -C /Users/jmagady/Dev/vsdd-factory/.factory rev-parse --git-dir
git -C /Users/jmagady/Dev/vsdd-factory/.factory branch --show-current
# Expected: factory-artifacts

# Verify develop HEAD (6e0d5407 = S-15.11 merge; or newer if subsequent PRs merged)
git rev-parse origin/develop
# Expected: 6e0d54070eca90e897b60fb51996305fac45b60b or newer

# Verify S-15.11 in develop log
git log origin/develop --oneline | grep -i "S-15.11\|validate-burst-log"
# Expected: 6e0d5407 ...S-15.11...

# Verify S-15.09 spec exists at factory-artifacts
ls /Users/jmagady/Dev/vsdd-factory/.factory/stories/S-15.09-validate-state-structure-phase-1.md
# Expected: file exists (authored at 26490be7)

# Verify factory-artifacts clean (excluding log files which are ephemeral)
git -C /Users/jmagady/Dev/vsdd-factory/.factory status --porcelain | grep -v "^.M logs/"
# Expected: empty (clean, modulo log files)
```

---

## Per-Story Dispatch Template (Step-by-Step for S-15.09)

### Step 1: story-writer — RETROACTIVELY-COMPLETE

**Status: COMPLETE.** Story spec authored at factory-artifacts commit `26490be7` (2026-05-17).
- Spec at: `.factory/stories/S-15.09-validate-state-structure-phase-1.md`
- BC-5.39.005 at: `.factory/specs/behavioral-contracts/ss-05/BC-5.39.005.md`
- STORY-INDEX v3.39 + BC-INDEX v2.30 + ARCH-INDEX v2.06 bumped in same commit
- All 8 cascade lessons (from S-15.07 + S-15.11) applied preemptively in spec authoring

**Proceed directly to Step 2.**

### Step 2: test-writer

Author failing bats integration tests under `plugins/vsdd-factory/tests/validate-state-structure/`.

**Branch:** Create `feature/S-15.09-validate-state-structure-p1` off `origin/develop@6e0d5407`.

**Bats structure (mirror S-15.11 pattern exactly):**

Fixture directories (one per violation class):
- `pass-all-valid/` — STATE.md where banner line-count claim matches actual newline count,
  dual-margin present, trajectory-tail `→9→9→9→9` (4 components)
- `fail-banner-wc-off-by-one/` — STATE.md where banner claims N-1 lines but file has N newlines
- `fail-no-dual-margin/` — correct line-count and tail; banner has only one margin value
- `fail-tail-3-components/` — correct line-count and dual-margin; tail `→9→9→9` (3 components)
- `fail-tail-5-components/` — correct line-count and dual-margin; tail `→9→9→9→9→9` (5 components)
- `fail-all-three/` — all three violations simultaneously
- `fail-open-unreadable/` — empty dir; test harness arranges unreadable file path

Bats files (one per AC):
- `fail-banner-wc-off-by-one.bats` (AC-1)
- `fail-no-dual-margin.bats` (AC-2)
- `fail-tail-3-components.bats` (AC-3)
- `fail-tail-5-components.bats` (AC-4)
- `pass-all-valid.bats` (AC-5)
- `fail-all-three.bats` (AC-6)
- `fail-open-unreadable.bats` (AC-7)
- `integration-production-registry.bats` (preemptive per S-15.11 F-P2-001 lesson; **INCLUDED FROM DAY 1**)

**CRITICAL: canonical `tool = "Edit|Write"` per architect Q5/Q6 from day 1** in every
`_write_registry()` heredoc in every bats file. NOT `Write|Edit`. 5 reference classes must all
have `Edit|Write`:
1. Production hooks-registry.toml entry (maintained by implementer)
2. Bats inline `_write_registry()` heredocs (each bats file)
3. Story spec body references (already correct at 26490be7)
4. AC verification predicates
5. Doc-comment narrative in Rust source

**`integration-production-registry.bats` requirements (preemptive per S-15.11 F-P2-001):**
- Extracts `path_allow` verbatim from production `hooks-registry.toml` entry for
  `validate-state-structure`
- Asserts hook actually blocks for `fail-banner-wc-off-by-one` fixture using production registry
- Confirms no `**` glob or other capability-denying form that would cause `canonicalize()` to fail-open
- Production `path_allow` MUST use bare paths (no `**` glob) — this is lesson #1 from S-15.11

All tests marked `@pending` — binary does not exist yet. Confirm red state with `./run-all.sh`.

### Step 3: implementer

Author WASM crate at `crates/hook-plugins/validate-state-structure/`:

**Cargo.toml:**
- `name = "validate-state-structure"`; `version = "0.0.1"`; workspace member registration
- `[lib] crate-type = ["cdylib"]` + `[[bin]]` dual-target pattern per S-15.11 + S-15.07 template
- Depends on `hook-sdk` workspace crate

**src/lib.rs — core logic (all cascade lessons applied preemptively):**

```rust
// CANONICAL in-plugin guard per architect Q4/Q5/Q6:
fn is_state_md_target(file_path: &str) -> bool {
    Path::new(file_path).file_name() == Some(OsStr::new("STATE.md"))
}
// NOT ends_with("STATE.md") — lesson #3 (path-component-strict filename guard)

// All banner parsing MUST use is_char_boundary() guards on any byte-index slice — lesson #4
// cited_raw: String field populated from raw body-literal at violation site — lesson #5
```

Three validation functions:
1. `validate_banner_wc` — extract declared line-count from banner `wc-l` narrative integer;
   count actual `\n` in content; diverge → BlockWithFix naming claimed/actual counts + D-421(c)
2. `validate_dual_margin` — regex scan banner block for BOTH margin expressions (per D-446(c));
   absent either → BlockWithFix citing D-446(c)
3. `validate_trajectory_tail` — global regex `→(\d+)` on trajectory-tail line; count matches;
   != 4 → BlockWithFix naming actual count, required 4, citing D-433(e)+D-439(c)

Multiple violations → single BlockWithFix enumerating all; fail-open on read errors.

**WASM compilation target:** `wasm32-wasip1`
**Compiled output:** `plugins/vsdd-factory/hook-plugins/validate-state-structure.wasm`
**src/main.rs:** CLI entrypoint (dual-target pattern per existing crates)

**hooks-registry.toml entry (production `path_allow` MUST be bare path, no `**` glob — lesson #1):**
```toml
[[plugins]]
name = "validate-state-structure"
wasm = "hook-plugins/validate-state-structure.wasm"
trigger_event = "PostToolUse"
tool = "Edit|Write"
tier = "sync"
on_error = "block"
path_allow = [".factory/STATE.md"]
```

**Pre-flight 4-gate (all must PASS before LOCAL adversary):**
```bash
cargo fmt --check --all
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace --all-targets
cd plugins/vsdd-factory/tests && ./run-all.sh
```

**Pre-existing known non-failures (do NOT investigate):**
- `sink-http bc_3_07_001_backoff` bats test failure — pre-existing
- `F-P3-008` ubuntu timing flake — RESOLVED by S-15.05; if reappears flag but do not block

### Step 4: LOCAL adversary cascade (BC-5.39.001 3-CLEAN protocol)

- Dispatch fresh-context adversary per `vsdd-factory:adversarial-review`
- Adversary reads ONLY story spec + diff vs develop base (strict information asymmetry)
- Minimum 3 consecutive CLEAN or NITPICK-only passes for convergence; any finding >= LOW resets
  streak to 0/3
- Persist each pass at `.factory/code-delivery/S-15.09/adv-local-pass-N.md` (canonical pattern)
- **Expected trajectory:** Given preemptive application of all 8 cascade lessons, budget 4-6 passes
  and 2-4 fix-bursts (compressed vs S-15.11's 7 passes + 4 fix-bursts)
- WASM-specific adversary checks: `is_state_md_target` path-component-strict guard correct;
  is_char_boundary() guard present on all byte-index slices; `block_with_fix` messages are
  actionable; bats cover PASS and FAIL branches; `tool = "Edit|Write"` canonical in all 5
  reference classes; `integration-production-registry.bats` passes with production config;
  `cited_raw: String` plumbed structurally at all violation sites

### Step 5: pr-manager 9-step PR lifecycle

- Standard 9-step pr-manager cycle; target branch: develop; squash-merge
- PR body must include: D-NNN sub-clauses closed (AC-1 through AC-10 + AC-11 + AC-12);
  WASM compilation evidence; pre-flight 4-gate results; LOCAL adversary cascade trajectory
  and convergence declaration (with streak count)

### Step 6: state-manager post-merge burst

After squash-merge to develop:
- STORY-INDEX: S-15.09 status → merged; bump version (v3.39 → v3.40)
- BC-5.39.005: POL-14 draft → active; BC-INDEX bump
- STATE.md: Phase Progress row; Active Branches develop SHA advance; Concurrent Cycles
  M2-wave-3-SHIPPED; D-476 Decisions Log preamble annotation; Session Resume full refresh;
  Last Updated; Current Phase; Story Status 67→68; frontmatter advance per D-443(a)+D-444(a)
- Single factory-artifacts commit per TD-VSDD-053
- SHA-patch follow-up commit per D-447(c)+D-449(e)

---

## Story-by-Story Details — S-15.09

### S-15.09 — validate-state-structure Phase 1

**D-NNN sub-clauses closed (12 total):**
- D-421(c) + D-422(c) + D-424(b) + D-428(d) + D-438(a) + D-440(d) + D-442(d): Banner line-count
  discipline (7 sub-clauses; passes 41-62 codifications). Hook enforces wc-l claim matches actual.
- D-446(c): Dual-margin form (pass 66 codification). Hook enforces both margins present in banner.
- D-433(e) + D-439(c) + D-451(c): Trajectory-tail LENGTH=4 (passes 53/59/71). Hook enforces
  exactly 4 `→(\d+)` matches.
- D-432(b): Trajectory-tail canonical form `→N→N→N→N` (pass 52 codification). Covered by same
  cardinality check.

**Anchor:** SS-05 (Engine Governance) + E-12 (Engine Governance capability class)

**Hook trigger:** PostToolUse Edit/Write on STATE.md (path-component-strict via `is_state_md_target`)

**New BC required:** BC-5.39.005 — **ALREADY AUTHORED** at factory-artifacts `26490be7`.

**Core TDD seam (bats test cases):**
- Write STATE.md where banner claims 399 lines but file has 400 newlines → expect block (banner wc-l)
- Write STATE.md where banner has single margin but missing second margin → expect block (dual-margin)
- Write STATE.md where trajectory-tail is `→9→9→9` (3 components) → expect block (tail LENGTH)
- Write STATE.md where trajectory-tail is `→9→9→9→9→9` (5 components) → expect block (tail LENGTH)
- All three violations simultaneously → expect single block enumerating all 3
- File unreadable → expect Continue (fail-open)
- Correct STATE.md with accurate banner + dual-margin + `→9→9→9→9` → expect pass
- Production registry config (integration-production-registry.bats) → expect block on fail fixture

**Dependencies per architect M2 decision (624e9fab):**
Zero blocking dependencies. S-15.09 spec already authored. S-15.07 + S-15.11 merged — proceed.

---

## Hard Constraints (S-15.09-specific)

All constraints from `s-15.03-wave-m2-dispatch.md` §Hard Constraints apply. Additional constraints
established during S-15.07 + S-15.11 cascades:

1. **Production `path_allow` MUST use bare paths (no `**` glob).** Dispatcher `canonicalize()`
   fails on glob → silent CapabilityDenied → fail-open neuters hook. S-15.11 F-P2-001 HIGH.
   Verified by `integration-production-registry.bats` Scenario B.

2. **`Path::file_name() == Some("STATE.md")` (path-component-strict).** NEVER bare
   `ends_with("STATE.md")` on raw path — false-positive on `xSTATE.md`. S-15.11 F-P1-002 +
   F-P3-001 cross-application.

3. **`is_char_boundary()` guard on ALL byte-index slice expressions where multi-byte UTF-8 input
   is possible.** Banner narrative text may contain em-dash, en-dash, NBSP. S-15.11 F-P4-001
   MEDIUM (UTF-8 panic in `validate_h2_heading`).

4. **`cited_raw: String` structural plumbing from day 1.** Populated with `.trim_end().to_string()`
   of raw body-literal at every violation site. TD-VSDD-059 paper-fix avoidance.

5. **Canonical `tool = "Edit|Write"` from day 1 in all 5 reference classes**: production registry
   + bats inline `_write_registry()` heredocs + spec body + AC verification predicates +
   doc-comment narrative. Architect Q5/Q6 lock.

6. **Single-source-of-truth helper functions** (`is_state_md_target`): makes canonical guard
   testable + greppable. S-15.11 pattern.

7. **AC verification predicates MUST grep load-bearing code, not doc-comments.** S-15.11 F-P3-001
   caught an AC predicate matching a doc-comment-only `ends_with` reference. Predicates must point
   at production guards.

8. **`integration-production-registry.bats` from day 1.** Not a fix-burst addition. This is the
   canonical preemptive gate from S-15.11 onward.

9. **No simultaneous in-flight PRs touching hooks-registry.toml** per OQ-5 resolution (architect
   M2 adjudication Q3 fully-serial). Wait for S-15.09 to merge before authoring S-15.14.

10. **BC-5.39.001 3-CLEAN protocol:** 3 consecutive CLEAN/NITPICK passes before pr-manager opens
    develop PR. One finding >= LOW resets streak to 0/3.

---

## Lessons from S-15.07 + S-15.11 Cascades (Apply Preemptively in S-15.09)

These lessons accumulated across the two prior M2 cascades. Apply ALL eight from day 1.

1. **Production `path_allow` MUST use bare paths (no `**` glob).** Dispatcher `canonicalize()`
   fails on glob → silent CapabilityDenied → fail-open neuters hook. S-15.11 F-P2-001 HIGH.
   Proven load-bearing: `integration-production-registry.bats` Scenario B caught the gap.

2. **Bats inline `_write_registry()` heredocs MUST mirror production registry shape byte-for-byte.**
   S-15.11 added `integration-production-registry.bats` as the canonical pattern. Apply from day 1.

3. **Path-component-strict filename guard via `Path::file_name() == Some("<canonical>.md")`.** Never
   bare `ends_with("<canonical>.md")` (false-positive on `x<canonical>.md`). S-15.11 F-P1-002 +
   F-P3-001.

4. **`is_char_boundary()` guard on ALL byte-index slice expressions where multi-byte UTF-8 input
   is possible.** S-15.11 F-P4-001 MEDIUM (UTF-8 panic in `validate_h2_heading`).

5. **`cited_raw: String` structural plumbing from day 1.** Populated with `.trim_end().to_string()`
   of raw body-literal at every violation site. TD-VSDD-059 paper-fix avoidance.

6. **Canonical `tool = "Edit|Write"` in all 5 reference classes from day 1**: production registry
   + bats inline `_write_registry()` heredocs + spec body + AC verification predicates +
   doc-comment narrative. Architect Q5/Q6 lock.

7. **Single-source-of-truth helper functions** (`is_state_md_target`, etc.) — makes canonical guard
   testable + greppable.

8. **AC verification predicates MUST grep load-bearing code, not doc-comments.** S-15.11 F-P3-001
   caught an AC predicate that matched a doc-comment-only `ends_with` reference. Predicates must
   point at production guards.

---

## Post-S-15.09 Next Steps

After S-15.09 SHIPS:

| M2 Wave | Story | Status after S-15.09 |
|---------|-------|----------------------|
| Wave-1 | S-15.07 validate-index-cite-refresh | **SHIPPED 2026-05-16 PR #145 6fe7de4c** |
| Wave-2 | S-15.11 validate-burst-log | **SHIPPED 2026-05-17 PR #146 6e0d5407** |
| Wave-3 | S-15.09 validate-state-structure Phase 1 | **THIS WAVE** |
| Wave-4 | S-15.14 validate-dispatch-advance | dispatch-ready (blocked on this wave SHIPS; hooks-registry.toml serial constraint per architect Q3 + D-473) |

After M2 closes (all 4 stories — S-15.07 + S-15.11 + S-15.09 + S-15.14 merged):
- M3 (S-15.10 + S-15.12 + S-15.13 + S-15.15 + S-15.16 Part B + ADR-021/022 gating) becomes
  dispatch-ready
- M3 dispatch package to be authored in the same durability-burst pattern

Section 12 Step 3 M2 wave table:

| Step | Item | Gate |
|------|------|------|
| 3 (M1) | S-15.06 + S-15.08 + S-15.16 Part A | **COMPLETE 2026-05-16** |
| 3 (M2 wave-1) | S-15.07 validate-index-cite-refresh | **SHIPPED 2026-05-16 PR #145 6fe7de4c** |
| 3 (M2 wave-2) | S-15.11 validate-burst-log | **SHIPPED 2026-05-17 PR #146 6e0d5407** |
| 3 (M2 wave-3) | S-15.09 validate-state-structure Phase 1 | **ACTIVE (this document)** |
| 3 (M2 wave-4) | S-15.14 validate-dispatch-advance | M2 wave-3 SHIPS |
| 3 (M3) | S-15.10 + S-15.12 + S-15.13 + S-15.15 + S-15.16 Part B | M2 complete |
| 4 | E-10 sub-cycle resumption (pass-15 forward) | Step 3 (all milestones) complete |
| 5 | F5 cycle resumption (pass-75 forward) | Step 3 complete + explicit human direction |
