---
document_type: f2-prd-delta
level: analysis
cycle: v1.0-brownfield-backfill
story_id: "S-25.02"
epic_id: "E-25"
producer: product-owner
timestamp: 2026-09-05T00:00:00Z
phase: F2
status: draft
---

<!-- Source artifacts consulted (not declared as a hash-drift-tracked `inputs:` list; this is an
     F2 PRD-delta narrative mirroring the F1/F2 doc placement convention, not a spec artifact):
     .factory/cycles/v1.0-brownfield-backfill/S-25.02-f1-delta-analysis.md
     .factory/cycles/v1.0-brownfield-backfill/S-25.02-f2-architecture-delta.md
     .factory/specs/architecture/decisions/ADR-051-layer-2-two-mechanism-size-triggered-shard-rotation-append-logs-and-bc-index-sharding.md
     .factory/specs/architecture/decisions/ADR-047-indeterminate-outcome-model-durable-mutation-marker-next-advance-gate.md (v1.6)
     .factory/specs/architecture/decisions/ADR-039-validator-failure-policy-resource-exhaustion-fail-closed.md
     .factory/specs/architecture/ARCH-INDEX.md
     .factory/specs/domain-spec/capabilities.md
     .factory/specs/behavioral-contracts/ss-01/BC-1.18.001.md..BC-1.18.004.md
     .factory/specs/behavioral-contracts/BC-INDEX.md
     .factory/stories/S-25.02-artifact-sharding-layer2.md
     plugins/vsdd-factory/hooks-registry.toml
-->

# S-25.02 F2 PRD Delta: Artifact Sharding Layer 2 — BC Authorship

## 0. Summary

This burst authors 7 new Behavioral Contracts (BC-1.18.005 through BC-1.18.010 under SS-01, and
BC-7.08.001 under SS-07) implementing the architect's ADR-051 two-mechanism Layer-2 shard-rotation
design, registers a new capability (CAP-043) in `capabilities.md`, and inserts the corresponding
rows into `BC-INDEX.md` (v5.50 / 1,997 BCs → v5.51 / 2,004 BCs). No VPs are authored (formal-verifier
follows). The S-25.02 story body is NOT rewritten (story-writer's domain at F3) — this doc records
the BC-authorship burst and the inputs story-writer needs at F3.

**§9 (added, fix-burst) — v1.1 fix-burst amendment.** A fresh-context adversary reviewed the
authored BC set and found a BLOCKER + HIGHs, resolved by architect (ADR-051 v1.1) and then by
product-owner in this fix-burst: BC-1.18.009's double-actor rotation contract rewritten to
single-actor block-and-retry (BLOCKER); a NEW BC-1.18.011 authored (governed one-time B2 migration);
BC-1.18.005 gained an item-count-trigger postcondition; VP-method columns reconciled across all 8
BCs to VP-INDEX v3.02; `prd-supplements/error-taxonomy.md` materialized; SDK Grounding Evidence
added to all 8 BCs; a volatile ADR-047 v1.6 version pin removed from CAP-043/BC-7.08.001. See §9
below for the full accounting. `BC-INDEX.md` now v5.52 / 2,005 BCs (SS-01 133→134).

## 1. New Behavioral Contracts

| BC ID | Subsystem | Capability | Status | One-line postcondition summary |
|---|---|---|---|---|
| [BC-1.18.005](../../specs/behavioral-contracts/ss-01/BC-1.18.005.md) | SS-01 | CAP-043 | draft | Native `stat()`-only PreToolUse size-trigger; byte-denominated cap formula `shard_cap_bytes <= (PRACTICAL_FUEL_CEILING/WORST_CASE_FUEL_PER_BYTE) - MAX_SINGLE_RECORD_BYTES - SAFETY_MARGIN`, evaluated as the MINIMUM across every Cohort B validator reading the artifact (Cross-Validator Minimum Rule); all five constants explicitly PROVISIONAL until the F4 synthetic calibration harness locks them. |
| [BC-1.18.006](../../specs/behavioral-contracts/ss-01/BC-1.18.006.md) | SS-01 | CAP-043 | draft | Roll-before-write is seal→create→atomic-index-publish THEN `HookResult::Block` with an explicit `Edit`-vs-`Write`-specific retry instruction — never a silent transparent redirect (structurally impossible under `HookResult`'s three-variant contract); shard-index TOML schema; stable-current-filename addressing falls out of the seal mechanism for zero-code-change shard-unaware reads. |
| [BC-1.18.007](../../specs/behavioral-contracts/ss-01/BC-1.18.007.md) | SS-01 | CAP-043 | draft | Configurable `retention_count` (default 10); same-invocation archival move to `archive/<stem>/`, `path_allow`-visible but excluded from the default whole-corpus glob — honest `O(active shards)` accounting, never `O(all shards ever)`. |
| [BC-1.18.008](../../specs/behavioral-contracts/ss-01/BC-1.18.008.md) | SS-01 | CAP-043 | draft | **NEW, not in F1's original 4-BC enumeration.** Mandatory one-time backfill-split of the four pre-existing oversized cycle append-logs (908,938 / 806,198 / 234,731 / 830,621 bytes as of 2026-09-05, all 5-19× over cap) into `ceil(bytes/cap)` sealed shards + fresh current file, record-boundary-safe, with mandatory content-preservation verification before the original file's role is retired. |
| [BC-1.18.009](../../specs/behavioral-contracts/ss-01/BC-1.18.009.md) | SS-01 | CAP-043 | draft | **NEW.** Mechanism B1 — BC-INDEX frontmatter `changelog:` array auto-rotation via the SAME native gate, reusing the already-shipped `rotate_changelog`/`prepend_changelog_item` primitives (no new rotation logic); diverges from mechanism A by returning `Continue` after rotation (rotation always makes room for a bounded-size item prepend, unlike mechanism A's arbitrarily-large append blocks). |
| [BC-1.18.010](../../specs/behavioral-contracts/ss-01/BC-1.18.010.md) | SS-01 | CAP-043 | draft | **NEW.** Mechanism B2 — per-subsystem `### SS-NN` body-table sharding; first-level addressing is a pure function of the BC-ID prefix via ARCH-INDEX's authoritative `BC-S Prefix`→`SS-NN` mapping (zero lookup); SS-05/SS-06 require immediate second-level manifest-keyed sub-sharding (both already over cap on section size alone: ~88,695 / ~85,407 bytes measured 2026-09-05). |
| [BC-7.08.001](../../specs/behavioral-contracts/ss-07/BC-7.08.001.md) | SS-07 | CAP-041 | draft | Cohort B `failure_policy = fail-closed` flip for the CORRECTED plugin identity `validate-burst-log` (PostToolUse `^(Edit\|Write\|MultiEdit)$` arm only — its separate `^Bash$` chain-detection arm is explicitly excluded), `regression-gate`, `convergence-tracker`; hard-gated on BC-1.18.005/006/008's postconditions holding (cap enforced, gate live, AND the pre-existing oversized files already backfill-split) per ADR-051 Decision 9's corrected sequencing. |

### BC ID allocation and collision check

The architect's F1/F2 enumeration carried BC-1.18.005–007 forward as provisional IDs and flagged
BC-7.NN.001 for confirmation against BC-INDEX at F2 close. Confirmed at authoring time:

- **BC-1.18.005–010**: no collision. The `ss-01/` directory and `BC-INDEX.md` both showed
  `BC-1.18.004` as the last-registered member of the BC-1.18 family; BC-1.18.005–010 are the next
  six free sequential slots. (BC-1.18.008/009/010 were NOT in the original F1 enumeration — F1 only
  proposed 4 BCs for SS-01 — but their numbers were still the next free slots after the F2
  architecture-elaboration burst identified the three additional required BCs.)
- **BC-7.08.001**: the F1/F2 docs left the `SS` slot as "BC-7.NN.001, architect to confirm." Direct
  inspection of the live `ss-07/` directory listing (`BC-7.01.001` through `BC-7.07.002`, confirmed
  via `ls` at authoring time) and a `BC-INDEX.md` grep for `BC-7.08`/`BC-7.09` (zero hits) show
  `BC-7.08` is the next free family slot. Allocated as **BC-7.08.001** — no collision, no
  reconciliation needed against the architect's provisional numbering (the architect explicitly
  deferred the exact number to product-owner/BC-authorship time).

## 2. New Capability

**CAP-043 — Artifact Sharding Layer 2: Size-Triggered Shard Rotation for Cycle Append-Logs and
BC-INDEX Structured-Catalog Sharding**, registered in `.factory/specs/domain-spec/capabilities.md`
(v1.16 → v1.17, CAP count 42 → 43). Justification: no existing capability covers artifact-size-
triggered shard rotation. Distinguished from CAP-041 (INDETERMINATE outcome DETECTION-and-
quarantine, which this capability's root-cause PREVENTION extends — CAP-041 explicitly anticipates
this as its own "Ratified Future Phase Layer 2" per ADR-047 §Decision 8b) and CAP-042 (one specific
`last_amended`-field write-path fix, whose `rotate_changelog` primitive CAP-043 reuses as a
component). BC-7.08.001 (the Cohort B flip) is anchored to CAP-041, not CAP-043, because the
registry `failure_policy` assignment itself is squarely CAP-041's domain (extending the existing
Cohort mechanism to a second human-confirmed set) — CAP-043's shard-bounding postconditions are a
cited dependency/precondition for BC-7.08.001, not its own capability anchor.

## 3. New Edge Cases (by BC)

| BC | New EC IDs | Summary |
|---|---|---|
| BC-1.18.005 | EC-001..EC-007 | Zero-cost bypass for unmatched paths; inclusive/exclusive cap-boundary behavior; first-write-ever (0-byte current shard); net-negative `MultiEdit` payload; superlinear-fuel harness finding; `DEFAULT_FUEL_CAP` change requiring harness re-run (not naive proportional scale). |
| BC-1.18.006 | EC-001..EC-006 | Roll-before-write for an oversized single block; blind-retry `Edit` after a block; seal-rename failure (fail-loud `Error`, not `Block`/`Continue`); concurrent-dispatch race; a single record alone exceeding cap post-roll; `/compact-state`'s free shard-awareness. |
| BC-1.18.007 | EC-001..EC-005 | Retention-limit-exactly-reached; `retention_count` lowered mid-flight (multi-shard archival in one invocation); default-glob exclusion of archived shards; unsupported manual un-archival; corrupt/missing shard-index (fail-loud). |
| BC-1.18.008 | EC-001..EC-005 | Under-cap artifact (1-shard trivial case, still index-registered); a single record itself exceeding cap (`oversized_record: true` flag, not truncated); crash-mid-backfill atomicity; content-preservation-verification failure (fail-loud abort); stale illustrative byte counts (re-measurement required at actual F4 execution). |
| BC-1.18.009 | EC-001..EC-005 | Rotation-triggered-at-N-item-boundary; under-N no-op; `rotate_changelog` failure (fail-loud); a single oversized changelog item still counting as ONE item toward the item-count trigger (distinguishing B1's item-count trigger from mechanism A's byte-size trigger); concurrent-session race. |
| BC-1.18.010 | EC-001..EC-006 | Sub-sharded-subsystem lookup (two manifest reads); non-sub-sharded lookup (zero manifest reads); a currently-under-cap subsystem growing into sub-sharding later; whole-corpus scan iterating the manifest; out-of-scope cross-subsystem BC renumbering; ARCH-INDEX-amendment/BC-INDEX-lookup race (bounded by existing factory-lock discipline). |
| BC-7.08.001 | EC-001..EC-005 | Flip attempted before backfill-split completes (rejected — reproduces the exact DoS Cohort A/B partition was designed to prevent); Bash-arm mistakenly flipped (scope violation); per-validator calibration divergence (partial flip permitted); unauthorized Cohort B expansion (rejected, needs its own BC); S-25.02's own stale AC-006 citation (flagged for story-writer, not fixed here). |

## 4. New Error-Taxonomy Variants

The project's existing `E-xxx-NNN` convention (`E-REG`, `E-PAY`, `E-CAP`, `E-PLG`, `E-SNK`, `E-ACT`,
`E-HK`, per `prd.md` §5.1) is extended with a new category for shard-management failures. Per
CLAUDE.md's production-grade default, this is defined in-scope now rather than deferred:

| Prefix | Category | Severity | Exit code / HookResult | Examples |
|---|---|---|---|---|
| `E-SHD-NNN` | Shard management errors | broken → fail-loud | `HookResult::Error` (native gate, no dispatcher exit-code semantics apply — this is a PreToolUse hook outcome, not a process exit code) | `E-SHD-001`: seal-rename failure (disk full / permission error) mid-roll (BC-1.18.006 EC-003); `E-SHD-002`: shard-index file missing or corrupt at dispatch time (BC-1.18.007 EC-005, story's own EC-005); `E-SHD-003`: backfill-split content-preservation verification failure (BC-1.18.008 EC-004, Postcondition 6); `E-SHD-004`: `rotate_changelog` invocation failure during B1 rotation (BC-1.18.009 EC-003) |

**RESOLVED in the fix-burst (F-S2502-F2-006):** `.factory/specs/prd-supplements/error-taxonomy.md`
has now been materialized by product-owner, containing the `E-SHD-NNN` row above (extended with a
NEW `E-SHD-005` for BC-1.18.011's migration-verification-failure case) alongside the existing seven
categories mirrored verbatim from `prd.md` §5.1. This also required registering a new
`prd-supplement` artifact-type entry in `plugins/vsdd-factory/config/artifact-path-registry.yaml`
(a mechanical prerequisite — the write was blocked `ARTIFACT_PATH_UNREGISTERED` with no prior entry
for `.factory/specs/prd-supplements/*.md`). See §9 below for the full fix-burst accounting.

## 5. F3 Input for Story-Writer (NOT actioned in this burst)

**AC-006 body-text correction.** S-25.02's own AC-006 (`.factory/stories/S-25.02-artifact-sharding-layer2.md`)
cites the non-existent plugin name `validate-burst-log-structure`. Per ADR-051's OQ-5 resolution
(already applied to ADR-047 v1.6) and this burst's BC-7.08.001, the correct name is
`validate-burst-log` (PostToolUse `^(Edit|Write|MultiEdit)$` arm only). Story-writer should correct
AC-006's citation at F3, and update the story's `behavioral_contracts:` frontmatter array (currently
`[]`) to list all 7 new BC IDs: `[BC-1.18.005, BC-1.18.006, BC-1.18.007, BC-1.18.008, BC-1.18.009,
BC-1.18.010, BC-7.08.001]`, per the `bc_array_changes_propagate_to_body_and_acs` policy (product-owner
does not touch story body/frontmatter `bcs:`-array propagation directly — that is story-writer's
domain per CLAUDE.md routing).

**Stories affected by this burst's BC changes:** S-25.02 (behavioral_contracts frontmatter array
population; AC-006 citation correction; §Behavioral Contracts section population — currently a
"TBD" placeholder per the story's own §Behavioral Contracts note). Story-writer must propagate under
`bc_array_changes_propagate_to_body_and_acs` policy.

## 6. VP Citation Handoff (for architect/formal-verifier)

Every new BC above cites `(pending)` VP rows in its own `## Verification Properties` /
`## VP Anchors` sections, following the existing project convention (e.g. BC-5.39.006's
`(pending)` rows). **VP citations changed in: BC-1.18.005, BC-1.18.006, BC-1.18.007, BC-1.18.008,
BC-1.18.009, BC-1.18.010, BC-7.08.001.** Architect/formal-verifier must propagate to VP-INDEX,
`verification-architecture.md`, and `verification-coverage-matrix.md` under the
`vp_index_is_vp_catalog_source_of_truth` policy when VPs are authored for these BCs (formal-verifier
follows product-owner per the CLAUDE.md routing table — not actioned in this burst).

## 7. Inconsistency Not Resolved In-Scope (routing flag for F2 close)

1. **`prd.md` §5.1/§5b reference a `.factory/specs/prd-supplements/` directory that does not exist
   on disk** (confirmed via directory listing at authoring time). This predates S-25.02 and is not
   introduced by this burst; flagged for orchestrator routing to spec-steward/technical-writer to
   either materialize the supplements directory or correct `prd.md`'s references. Not fixed here —
   out of S-25.02's scope, and product-owner does not unilaterally decide the supplements-directory
   materialization timeline.
2. **`prd.md` §2.1 (SS-01 BC index) does not list ANY `BC-1.18.*` entries**, confirming the
   established S-25.01 precedent that incremental Feature-Mode BC additions are NOT back-filled
   into the monolithic `prd.md` — `BC-INDEX.md` plus the cycle-dir F2-delta doc are the living
   record for this class of change (as they were for S-25.01's BC-1.18.001–004). This burst follows
   that same precedent rather than silently deviating from it; flagged here only for visibility, not
   as a defect requiring a fix.

## 9. Fix-Burst Amendments (product-owner, adversary pass-1 findings routed via architect's F-S2502-F2 dispatch)

This section records the v1.1 fix-burst that consumed architect's ADR-051 v1.1 corrections
(§4a above) and directly resolved the product-owner-routed findings.

| Finding | Resolution |
|---|---|
| F-S2502-F2-001 (BLOCKER) | BC-1.18.009 Postconditions 2/6 + Invariant 1 rewritten to the single-actor block-and-retry contract (ADR-051 v1.1 Decision 7 CORRECTED). The gate performs ONLY `rotate_changelog`'s trim step, then returns `HookResult::Block`; the new `changelog:` item's prepend is exclusively the responsibility of whichever `Edit`/`Write`/`MultiEdit` call ultimately lands (original or retried). The withdrawn v1.0 "gate rotates AND prepends, then Continues" design is explicitly superseded, not silently replaced (POLICY 1 append-only history preserved in the v1.0 changelog row). BC-1.18.009's H1 gained "— Single-Actor Block-and-Retry" per BC H1 Title Authority; BC-INDEX title column updated same-burst. |
| F-S2502-F2-002 (HIGH) | New BC-1.18.011 authored — the next free SS-01 slot, confirmed against the live `ss-01/` directory (BC-1.18.001–010 pre-existing) and BC-INDEX.md v5.51/2,004 BCs at authoring time; no collision. Encodes all 8 postcondition obligations from ADR-051 §Decision 10: content-preservation, independent census, crash-atomicity, rollback, idempotency, SS-05/SS-06 second-level coverage in the same operation, no new Cohort-B dependency, and `depends_on` BC-1.18.010 + BC-1.18.006. `status: draft`; VP citations left `(pending)` for formal-verifier. New error code `E-SHD-005` added to the taxonomy for its rollback/census failure path. |
| F-S2502-F2-005 (MEDIUM) | BC-1.18.005 gained Postcondition 8 (+ Invariant 5, EC-008/EC-009): the item-count-denominated trigger for the `"frontmatter-changelog-array"` artifact shape, dispatched via a `[[shard]]` config `shape` field. BC-1.18.005 now explicitly owns BOTH trigger shapes (byte-size `stat()`-only for mechanism A; item-count/bounded-frontmatter-parse for mechanism B1). |
| F-S2502-F2-003 (HIGH) | VP-method columns reconciled to the authoritative VP-INDEX v3.02 catalog across all 8 BCs: BC-1.18.005 VP-116 unit-test→kani-proof, VP-117 byte-denomination row proptest→unit-test; BC-1.18.008 VP-124 idempotency row unit-test→integration (both VP-124 rows now read "integration test"); BC-1.18.010 VP-128 single-authoritative-row "consistency-validator scan"→"integration test (consistency-validator scan...)"; BC-7.08.001 VP-129 closed-cohort "config audit"→"static-check (config audit...)". BC-1.18.006/007/009 swept and confirmed already correct (no change). |
| F-S2502-F2-006 (MEDIUM) | `.factory/specs/prd-supplements/error-taxonomy.md` materialized (8 categories: the existing REG/PAY/CAP/PLG/SNK/ACT/HK mirrored from `prd.md` §5.1, plus SHD/`E-SHD-NNN` extended with the new `E-SHD-005`). Resolves the pre-existing dangling `prd-supplements/` reference in `prd.md` §5.1/§5b (that reference now resolves; the sibling `interface-definitions.md`/`nfr-catalog.md`/`test-vectors.md` supplements remain unmaterialized — out of this burst's scope, a separate pre-existing gap). Required a companion `prd-supplement` artifact-type registration in `artifact-path-registry.yaml` (mechanical prerequisite). §4 above updated to point to the now-materialized canonical file. |
| F-S2502-F2-007 (MEDIUM, POLICY 5) | `## SDK Grounding Evidence` added to all 8 BCs (005–011, 7.08.001), each with literal stable-anchor grep stdout (no `grep -n`, no file:line citations, per TD-VSDD-091) substantiating `HookResult` variants, `rotate_changelog`/`prepend_changelog_item`/`rewrite_source_after_rotation`, `block_if_marker_check`/`write_indeterminate_marker`/`write_atomic`, the three Cohort B registry entries' priorities, and CAP-041/042/043 existence. |
| F-S2502-F2-009 (LOW, POLICY 19) | BC-7.08.001 §Traceability ADR cell and the mirrored CAP-043 §Source in `capabilities.md` (v1.17→v1.18) de-loaded of the volatile `ADR-047 v1.6` version pin — the stable `§Decision 8a` section anchor already resolves the citation without the version token, mirroring the CAP-032/`ADR-026` precedent. |

**BC-INDEX.md updated to v5.52 / 2,005 BCs** (SS-01 133→134): new BC-1.18.011 row inserted after
BC-1.18.010; version-chain cells for BC-1.18.005/006/007/008/009/010 and BC-7.08.001 all bumped to
note their v1.1 amendment. **capabilities.md updated to v1.18** (CAP-043 §Source citation-list
extension + v1.6 pin removal; no CAP count change, still 43).

**Not actioned in this burst (explicitly out of product-owner's write scope, per dispatch
constraints):** VP bodies (VP-125/126 flagged for formal-verifier re-review against BC-1.18.009's
corrected contract; BC-1.18.011's `(pending)` VPs are formal-verifier's allocation); ADR content
(architect's domain — already amended to v1.1 by architect in the preceding burst); STATE.md;
the verification-delta doc's §7.3 E-SHD routing note (explicitly left to formal-verifier's own
fix-burst per this dispatch's instructions); the S-25.02 story body (story-writer's domain at F3).

**Stories affected by BC changes (unchanged from §5 above, now also covering BC-1.18.011):**
S-25.02 — `behavioral_contracts:` frontmatter array population must now list all 8 new BC IDs:
`[BC-1.18.005, BC-1.18.006, BC-1.18.007, BC-1.18.008, BC-1.18.009, BC-1.18.010, BC-1.18.011,
BC-7.08.001]`. Story-writer must propagate under `bc_array_changes_propagate_to_body_and_acs`
policy (per §5's existing routing note, extended to include the new BC).

**VP citation change handoff (extends §6 above):** VP citations changed in BC-1.18.005 (new
Postcondition 8), BC-1.18.008 (VP-124 method only, no property-content change), BC-1.18.009
(Postconditions 2/6 + Invariant 1 rewritten — VP-125/126 bodies need re-review), BC-1.18.010
(VP-128 method only), BC-7.08.001 (VP-129 method only), and the new BC-1.18.011 (all VPs
`(pending)`). Architect/formal-verifier must propagate to VP-INDEX, `verification-architecture.md`,
and `verification-coverage-matrix.md` under `vp_index_is_vp_catalog_source_of_truth` policy.

## Changelog

| Version | Date | Author | Summary |
|---|---|---|---|
| 1.1 | 2026-09-05 | product-owner | Fix-burst amendment resolving adversary pass-1 findings routed via architect's ADR-051 v1.1 dispatch (§4a): added §9 (Fix-Burst Amendments) documenting BC-1.18.009's BLOCKER single-actor block-and-retry rewrite, new BC-1.18.011 (governed B2 migration BC), BC-1.18.005's item-count-trigger postcondition, VP-method reconciliation across all 8 BCs, `prd-supplements/error-taxonomy.md` materialization (+ companion artifact-path-registry.yaml entry), SDK Grounding Evidence additions, and the CAP-043/BC-7.08.001 ADR-047 v1.6 pin removal. `BC-INDEX.md` v5.51/2,004→v5.52/2,005 (SS-01 133→134); `capabilities.md` v1.17→v1.18 (no CAP count change). §0 Summary and the §4/§4a error-taxonomy note updated accordingly. |
| 1.0 | 2026-09-05 | product-owner | Initial F2 PRD-delta for S-25.02. Authored BC-1.18.005–010 (SS-01, CAP-043) and BC-7.08.001 (SS-07, CAP-041); registered CAP-043 in capabilities.md (v1.16→v1.17); inserted all 7 rows into BC-INDEX.md (v5.50/1,997→v5.51/2,004). No VPs authored (formal-verifier follows). Story body not modified (story-writer's domain at F3) — AC-006 correction and `behavioral_contracts:` array population flagged as F3 inputs. Two pre-existing, out-of-scope inconsistencies flagged for routing (prd-supplements directory absence; prd.md §2.1 BC-1.18.* omission, following established S-25.01 precedent).|
