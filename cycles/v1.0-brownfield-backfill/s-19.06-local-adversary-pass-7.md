# S-19.06 LOCAL Adversary Pass-7 — NOT-CLEAN

**Date:** 2026-07-14
**Story:** S-19.06 — host::read_prefix bounded partial read
**Pass:** 7 of BC-5.39.001 cascade
**Result:** NOT-CLEAN B0/H0/M0/L2
**Streak:** 0/3 UNCHANGED
**Source HEAD:** abde30f3
**Remediation HEAD:** 3b2abd45
**Branch:** feature/S-19.06 (base 1304d280)
**Prior Pass:** pass-6 NOT-CLEAN B0/H0/M0/L1 (streak 0/3 RESET)

## Summary

Two new LOW findings (F-P7-001, F-P7-002) raised at source HEAD abde30f3 — the first commit produced by the pass-6 implementer fix for F-P6-001. Both are doc-accuracy only; no production code affected. Streak holds at 0/3 UNCHANGED (was already 0/3 after pass-6 reset; no further regression).

F-P7-001: the bats file header at `plugins/vsdd-factory/tests/read-prefix-wasm.bats` claimed "no WASM compilation harness" / "CANNOT be expressed" / "out of scope" for compilation/link testing while T-009f in the same file IS the passing compile/link harness — stale gap-documentation from the pre-T-009f era that survived the F-P6-001 sweep. CLOSED: test-writer at 3b2abd45 (header rewritten; full-file doc sweep corrected 5 stale statements including per-test comments + bottom status summary; runtime execution correctly noted as the genuinely-out-of-scope remainder).

F-P7-002: the fixture crate module doc in the F-P6-001 fix (abde30f3) corrected the CapabilityDenied claim but introduced a new misattribution — claiming the `host_stubs` `−1` path "is exercised by `cargo test -p factory-dispatcher -- host::read_prefix`". That command exercises `prepare()`, never the hook-sdk stub; the stub exists solely for non-wasm compilation (dead_code; no test calls it; the fixture is never executed via that path). CLOSED: implementer at 8b3f40b0 (paragraph corrected: stub exists solely for non-wasm compilation; not exercised by any test).

Orchestrator full-gate verified at remediation HEAD 3b2abd45: bats 8/8 green; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo fmt --check --all` clean; diff abde30f3..3b2abd45 is comment-only (+32/−24 lines, 2 files — no production code modified).

---

## Part A — Prior-Pass Finding Verification

### F-P6-001 — CONFIRMED CLOSED

**Prior status:** OPEN at pass-6 source HEAD e6a69a4d — fixture crate `main.rs` module doc claimed wasm-target standalone execution returns `HostError::CapabilityDenied` (structurally impossible under standalone instantiation without host linkage).

**Closure verification:** At abde30f3 (implementer fix), the module doc no longer claims CapabilityDenied on standalone instantiation. The corrected text accurately describes instantiation failure as the outcome when host functions are absent, and correctly scopes the `−1/CapabilityDenied` mapping to the non-wasm `host_stubs` path only.

**Result:** CONFIRMED CLOSED. No residual.

---

## Part A — New Findings

### F-P7-001 — LOW — [doc-accuracy; stale header] — bats file header claims no WASM compilation harness while T-009f in the same file IS that harness

**Severity:** LOW
**Class:** doc-accuracy
**Scope:** `plugins/vsdd-factory/tests/read-prefix-wasm.bats` (file header block + per-test comments + bottom status summary)
**Production impact:** None. Test file documentation only; T-009f itself is correct and passing.

**Description:** At source HEAD abde30f3 the bats file retained header language stating that compilation/link testing "CANNOT be expressed" in the bats suite and is "out of scope." T-009f — present in the same file and passing at this HEAD — IS the compile/link harness: it runs `cargo build -p read-prefix-fixture --target wasm32-unknown-unknown` and asserts exit 0 and presence of the output artifact. The header claim was accurate before T-009f was added (it documented a genuine gap); after T-009f landed the header was not updated, leaving a false statement that a test axis cannot be expressed when it already is.

Additionally, five individual stale statements were present in per-test comments and the bottom status summary block that carried forward the same pre-T-009f framing (e.g., describing runtime execution as the only remaining gap when the compile/link gap was already closed).

**Why it matters:** A reader of the test file header would incorrectly conclude the compile/link axis is untested, potentially treating T-009f as redundant or misclassifying it during review. Under the production-grade default, doc comments that misrepresent test coverage posture are correctness defects — they corrupt the cognitive model of the test suite.

**Routing decision:** Test-writer fix-in-scope. The fix is a doc-only sweep: rewrite the header to reflect T-009f's presence; correct the 5 stale per-test/summary statements; retain the accurate observation that runtime WASM execution (actually invoking the compiled artifact under wasmtime) remains the genuinely-out-of-scope remainder.

**Status:** CLOSED — test-writer at 3b2abd45. Header rewritten; full-file doc sweep completed; runtime execution correctly scoped as the remaining untested axis.

---

### F-P7-002 — LOW — [doc-accuracy; misattribution] — fixture module doc claims host_stubs stub is exercised by `cargo test -p factory-dispatcher -- host::read_prefix`

**Severity:** LOW
**Class:** doc-accuracy
**Scope:** `crates/hook-plugins/read-prefix-fixture/src/main.rs` (module-level doc comment paragraph introduced by abde30f3)
**Production impact:** None. Fixture doc only; the stub itself is correctly marked `#[allow(dead_code)]`.

**Description:** The F-P6-001 fix at abde30f3 correctly removed the CapabilityDenied claim and added a paragraph explaining the `host_stubs` path. That paragraph stated the non-wasm `host_stubs` `−1` path "is exercised by `cargo test -p factory-dispatcher -- host::read_prefix`." This claim is incorrect. The `cargo test` invocation runs the `host` module tests in the dispatcher crate, which exercise `prepare()` / `build_event()` / `register()` on the dispatcher-side. The hook-sdk stub function (`host_stubs::read_prefix`) is never called from any test — it exists solely to satisfy the wasm32 compilation target's import expectations (dead_code in non-wasm builds). The fixture is never instantiated via `cargo test`.

**Why it matters:** A reader would incorrectly believe the `host_stubs` path has live test coverage, potentially deferring a future test-gap analysis on the assumption it is covered. Under the production-grade default, attribution of test coverage to a code path that has none is a correctness defect.

**Routing decision:** Implementer fix-in-scope. The fix is a doc-only change: replace the misattribution paragraph with an accurate statement that the stub exists solely for non-wasm compilation and is not exercised by any test.

**Status:** CLOSED — implementer at 8b3f40b0. Paragraph corrected to accurately state stub exists solely for non-wasm compilation; not exercised by any test.

---

## Accepted-with-Record (carried from prior passes)

**out_ptr=0 sentinel — read_file-verbatim parity (re-confirmed):** The `out_ptr=0` / `write_bytes_to_guest` null-guard in `read_prefix::register` replicates `read_file::register`'s pre-existing wasm-boundary pattern verbatim per Invariant 2. Non-empty boundary read untested for both; coverage posture inherited. No new risk. Accepted-with-record; no routing.

**EC-006 timeout non-enforcement — SYSTEMIC host-ABI gap (re-confirmed):** `read_prefix::register` does not enforce the timeout precondition at the wasm-boundary. Synchronous `func_wrap` closures in wasmtime cannot be preempted by epoch interruption while blocked on a host call (blocking `read_to_end`); the `−2` timeout code in the registry preamble is a code `read_prefix` structurally never returns. Gap spans `read_file` as well and is NOT introduced by S-19.06. Invariant 2 forbids modifying `read_file.rs`. Accepted-with-record. ROUTED: architect follow-up story recommendation anchored at the S-19.06 merge gate and convergence/post-merge burst per Canonical Principle Rule 3.

**Story Test Plan lag vs test inventory (re-confirmed):** Tests T-013a/b (max_bytes=0 denial axes added by remediation in pass-4) and 2 MUTANT_VERIFY witnesses (`prepare_mutant_hoisted` T-013) plus T-011 (hook-sdk from_code mapping) appear in the test suite but are not listed in the story Test Plan section. Accepted per cascade-remediation convention — cascade-driven tests added mid-stream need not block the cascade; optional story-writer sweep non-blocking. Accepted-with-record.

**FFI adversarial-input surface swept clean (re-confirmed):** Adversary re-examined the wasm-boundary FFI surface for adversarial input handling: NUL byte in path → `DeniedResolutionFailed` (CString conversion check fires before host dispatch); non-UTF-8 path bytes → `INVALID_ARGUMENT −4` (from_utf8 Err branch); u32::MAX for max_bytes → `with_capacity` uses `min(max_bytes, actual_len)` bound + `take()` enforced at read; empty path string → directory stat returns `INTERNAL_ERROR` or EC-001 short-circuit (path normalization rejects empty). No adversarial input produces undefined behavior or a bypass of the deny-by-default ordering. Accepted-with-record; no routing.

**EC-007 directory-read + T-004 Err-branch vacuous-by-construction (re-confirmed):** Directory-read (EC-007) error path not covered by unit tests. Parity with `read_file` (same gap accepted there). Story Test Plan does not require this path. T-004 Err-branch coverage is vacuous-by-construction: `fs::read` returns the only possible Err variant, covered statically by T-009g. Accepted-with-record; no routing.

**T-009a..T-009e as static regression guards, not red-gates (re-confirmed):** Tests T-009a..T-009e are static grep-based regression guards written post-implementation to lock specification properties. Not TDD red-gate tests. Honestly documented in the test suite. Accepted-with-record; no routing.

**Story v1.21 ↔ BC v1.6 consistency (re-confirmed):** All 10 body-site citations in S-19.06 v1.21 reference BC-1.17.001 v1.6. Task renumbering through v1.17..v1.21 revisions consistent with Test Plan rows and actual test names at remediation HEAD 3b2abd45.

**Commit hygiene (re-confirmed):** 16 commits from base 1304d280 to remediation HEAD 3b2abd45. Conventional-commit prefixes consistent (feat/test/fix/docs); no `Co-Authored-By` AI attribution; single-purpose per commit; no unintended file-scope bleed confirmed.

**POLICY 10 — n/a pre-Step-5:** PR merge gate not yet triggered.

**Invariant 4 chain naming (adjudicated NOT a defect):** `check_path_allowed` calls `resolve_path_for_allowlist` internally. Module doc naming was reviewed; naming is internally consistent and not a doc-accuracy defect. Accepted-with-record; no routing.

---

## Pass-7 Fresh Positive Coverage (at remediation HEAD 3b2abd45)

The following gates were freshly re-verified in pass-7:

**Unit test live-run:** 14/14 unit tests passing (factory-dispatcher lib target). No regressions.

**Ordering invariants — mutation witnesses:** Both mutation witnesses (`prepare_mutant_hoisted` T-013 variants) confirm the deny-by-default ordering is load-bearing — hoisting `prepare()` before `check_path_allowed()` produces the wrong result. Invariants confirmed.

**Invariant 4 chain verification:** `check_path_allowed → resolve_path_for_allowlist` chain verified. Module doc naming adjudicated NOT a defect (internal consistency confirmed).

**Bats `read-prefix-wasm.bats`:** 8/8 tests green. T-009f (compile/link harness) confirmed present and passing.

**bundle_orphan_check:** 6/6 hook plugin entries in `hooks-registry.toml` resolve to present WASM artifacts. No orphan entries.

**hook-sdk suite:** All hook-sdk library tests passing. `from_code` mapping confirmed: `−5 → NotFound`, `−1 → CapabilityDenied`, `−2 → Timeout`, `−3 → InternalError`, `−4 → InvalidArgument`, `−99/unknown → Other`.

**Registry/preamble/story/BC consistency:** Token Budget confirmed at 22,000 tokens. Step-order match between story and BC confirmed. No inconsistencies.

**Diff scope at remediation HEAD:** diff abde30f3..3b2abd45 is comment-only: +32/−24 lines across 2 files (`plugins/vsdd-factory/tests/read-prefix-wasm.bats` + `crates/hook-plugins/read-prefix-fixture/src/main.rs`). No production code modified; no test logic modified.

---

## Exit Gates (at remediation HEAD 3b2abd45 — orchestrator-verified)

- `cargo clippy --workspace --all-targets -- -D warnings`: **clean** (exit 0)
- `cargo fmt --check --all`: **clean** (exit 0)
- Bats `read-prefix-wasm.bats` T-009a..T-009h: **8/8 green**
- diff abde30f3..3b2abd45: **comment-only** (+32/−24, 2 files — no production code modified)
- F-P6-001: **CONFIRMED CLOSED** (abde30f3)
- F-P7-001: **CLOSED** — test-writer 3b2abd45
- F-P7-002: **CLOSED** — implementer 8b3f40b0
- 4-index at commit: BC-INDEX v4.04 / VP-INDEX v2.68 / STORY-INDEX v4.191 / ARCH-INDEX v3.01

---

## Cascade Trajectory

| Pass | Date | Result | B | H | M | L | Streak |
|------|------|--------|---|---|---|---|--------|
| 1 | 2026-07-14 | NOT-CLEAN | 0 | 1 | 1 | 0 | 0/3 |
| 2 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 2 | 0/3 |
| 3 | 2026-07-14 | NOT-CLEAN | 1 | 0 | 0 | 0 | 0/3 |
| 4 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 1 | 0 | 0/3 |
| 5 | 2026-07-14 | **CLEAN** | 0 | 0 | 0 | 0 | **1/3** |
| 6 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 1 | 0/3 RESET |
| 7 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 2 | 0/3 UNCHANGED |

## Next Step

Both F-P7-001 and F-P7-002 CLOSED at remediation HEAD 3b2abd45. Pass-8 commences from HEAD 3b2abd45. No story version change. 4-index at commit: BC-INDEX v4.04 / VP-INDEX v2.68 / STORY-INDEX v4.191 / ARCH-INDEX v3.01.
