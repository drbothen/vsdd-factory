# S-19.06 LOCAL Adversary Pass-4 — NOT-CLEAN

**Date:** 2026-07-14
**Story:** S-19.06 — host::read_prefix bounded partial read
**Pass:** 4 of BC-5.39.001 cascade
**Result:** NOT-CLEAN B0/H0/M1/L0
**Streak:** 0/3 (unchanged — prior streak 0/3 from pass-3 NOT-CLEAN)
**Source HEAD:** 3ac997b8
**Branch:** feature/S-19.06 (base 1304d280)
**Prior Pass:** pass-3 NOT-CLEAN B1/H0/M0/L0 (F-P3-001 CLOSED same-session: test-writer 3ac997b8)
**Remediation HEAD:** pending (T-013 pair in flight — test-writer)

## Summary

Pass-3 finding F-P3-001 (BLOCKER) CLOSED-VERIFIED at source HEAD 3ac997b8. 1 new MEDIUM finding: F-P4-001 harness-liveness gap — `prepare()`'s capability and path checks correctly precede the max_bytes=0 short-circuit (mandated by BC-1.17.001 Invariant 3 / PC-4), but no test exercises max_bytes=0 against a denied capability or a denied path. All denial tests use max_bytes=1024; all max_bytes=0 tests use fully-authorized callers. A mutation that hoists the short-circuit above the deny checks would return `Ok(Vec::new())` to unauthorized callers with the entire suite green — structurally symmetric to the reorder-witness pattern established by T-012 (which showed hoisting the existence-check above the max_bytes=0 short-circuit produces NOT_FOUND where EC-001 requires empty return). ROUTED test-writer fix-in-scope: T-013 pair covering (a) no-capability + max_bytes=0 → CAPABILITY_DENIED and (b) out-of-allowlist path + max_bytes=0 → CAPABILITY_DENIED reason=path_not_allowed, plus a permanent hoisted-mutant witness.

Adversary independently re-ran full gate suite at HEAD 3ac997b8: clippy clean, fmt clean, 11/11 unit tests green, 8/8 bats green, AC-003 static gate direct-run PASS.

Positive coverage re-confirmed: story v1.21 ↔ BC-1.17.001 v1.6 cite consistency verified across all 10 body sites; task renumbering from v1.17..v1.21 revisions consistent with current Test Plan rows and actual test names; commit hygiene across 13 commits faithful.

Streak: 0/3. F-P4-001 remediation in flight (test-writer T-013 pair). Pass-5 required.

## Part A — Prior-Pass Finding Verification

### F-P3-001 Closure Verification — BLOCKER — [fix-introduced; CI clippy] T-012 prepare_mutant uses ok_or_else with bare-constant closure: CLOSED-VERIFIED

**Prior finding:** The `prepare_mutant` mutation-witness helper added by test-writer babce0be used `ok_or_else(|| codes::CAPABILITY_DENIED)` — a bare-constant closure triggering clippy lint `unnecessary_lazy_evaluations` under `-D warnings`. `cargo clippy --workspace --all-targets -- -D warnings` exited 101; CI-red; branch unmergeable.
**Verification method:** Adversary executed `cargo clippy --workspace --all-targets -- -D warnings` against source HEAD 3ac997b8. Exit code 0 — lint-clean. Adversary inspected the `prepare_mutant` helper at 3ac997b8: the call site now reads `ok_or(codes::CAPABILITY_DENIED)` (bare-constant form, no closure). Rustfmt consolidations at 3ac997b8 are semantics-neutral whitespace changes only (verified via `cargo fmt --check --all` exit 0). Production `prepare()` retains `ok_or_else(|| { emit_denial(...); codes::CAPABILITY_DENIED })` — the closure captures an `emit_denial` side-effect and is not a bare-constant; `ok_or_else` is the correct form there. TD-VSDD-060 sibling sweep confirmed no other `ok_or_else` sites in `read_prefix.rs` that would trigger the lint. 11/11 unit tests green at 3ac997b8.
**Status:** CLOSED-VERIFIED

---

## Part A — New Findings

### F-P4-001 — MEDIUM — [harness-liveness class; deny-by-default ordering unlocked] max_bytes=0 + denial interaction not tested

**Location:** `crates/factory-dispatcher/src/host/read_prefix.rs` — `prepare()` implementation; unit test module T-001..T-012
**Observation:** `prepare()` enforces deny-by-default ordering per BC-1.17.001 Invariant 3 / PC-4: capability check and path allow-list check precede the `max_bytes=0` short-circuit. This ordering is correct and mandated. However, the test suite does not exercise the deny-path at max_bytes=0. All tests that exercise denial (CAPABILITY_DENIED or path_not_allowed) use max_bytes=1024; all tests that exercise the max_bytes=0 path (T-012 and variants) use fully-authorized callers. The two denial axes are tested only in isolation.

**Mutation exposure:** A mutation that hoists the `if max_bytes == 0 { return Ok(Vec::new()); }` short-circuit above the capability and path checks would return `Ok(Vec::new())` to an unauthorized caller. With the current suite, this mutation is not caught: all denial assertions operate at max_bytes=1024 (so the short-circuit would not fire), and all max_bytes=0 assertions use authorized callers (so the hoisted position would also return correctly). The full suite would remain green under this mutation — exactly the harness-liveness class established by the T-012 witnesses (which showed that hoisting existence-check above max_bytes=0 short-circuit leaks NOT_FOUND).

**Why MEDIUM:** This is a structural test coverage gap permitting a load-bearing behavioral mutation to go undetected. The implementation is correct at HEAD; the gap is in the test harness. Classification MEDIUM (not BLOCKER) because the correct behavior is implemented and BC-1.17.001 Invariant 3 / PC-4 are satisfied by production code; the defect is harness-liveness, not a production defect.

**Fix required:** T-013 pair: (a) no-capability caller + max_bytes=0 → must return `Err(CAPABILITY_DENIED)`, not `Ok(Vec::new())`; (b) out-of-allowlist path + max_bytes=0 → must return `Err(CAPABILITY_DENIED)` with reason=path_not_allowed, not `Ok(Vec::new())`. Additionally, add a permanent hoisted-short-circuit mutant witness (`prepare_mutant_hoisted`) analogous to `prepare_mutant` in T-012, confirming that hoisting the short-circuit above deny-checks produces `Ok(Vec::new())` where the correct implementation returns `Err(CAPABILITY_DENIED)`.

**Routing:** test-writer (T-013 pair + hoisted-mutant witness; no story version change — test-only remediation)
**Status:** OPEN — test-writer remediation in flight

---

## Accepted-with-Record

**EC-006 timeout non-enforcement — SYSTEMIC host-ABI gap (ROUTED, re-confirmed):** `read_prefix::register` does not enforce the timeout precondition at the wasm-boundary. This is a SYSTEMIC host-ABI gap: synchronous `func_wrap` closures in wasmtime cannot be preempted by epoch interruption while blocked on a host call; the `-2` timeout code documented in the registry preamble is a code `read_prefix` structurally never returns. This gap spans `read_file` as well and is NOT introduced by S-19.06. Invariant 2 forbids modifying `read_file.rs`. Accepted-with-record. ROUTED: architect follow-up story recommendation to be surfaced at the S-19.06 merge gate and anchored at the convergence/post-merge burst per Canonical Principle Rule 3.

**wasm-boundary out_ptr=0 byte-transfer pattern (re-confirmed):** The `out_ptr=0` / `write_bytes_to_guest` null-guard pattern in `read_prefix::register` replicates `read_file::register`'s pre-existing wasm-boundary pattern verbatim per Invariant 2 (byte-unmodified replication). Non-empty boundary read is untested for both `read_prefix` and `read_file`; coverage posture inherited. No new risk introduced. Accepted-with-record; no routing.

**EC-007 directory-read + T-004 Err-branch vacuous-by-construction (re-confirmed):** Directory-read (EC-007) error path is not covered by unit tests. Parity with `read_file` (same gap accepted there). Story Test Plan does not require this path. T-004 Err-branch coverage is vacuous-by-construction: the `fs::read` call returns the only possible Err variant, and that variant is covered statically by T-009g (AC-003 gate confirms no OUTPUT_TOO_LARGE path exists in production code). Accepted-with-record; no routing.

**T-009a..T-009e as static regression guards, not red-gates (re-confirmed):** Tests T-009a..T-009e are static grep-based regression guards written post-implementation to lock specification properties. They are not TDD red-gate tests. Honestly documented in the test suite. Accepted-with-record; no routing.

## Coverage

Adversary examined the following artifacts at source HEAD 3ac997b8:

- S-19.06 story v1.21 (all ACs AC-001..AC-007, §Architecture Mapping 8-step execution order, gate forms, task list, Architecture Compliance Rules; cite consistency: BC-1.17.001 v1.6 across all 10 body sites verified; task renumbering consistent through v1.17..v1.21 revisions; Test Plan rows match actual test names at HEAD 3ac997b8)
- BC-1.17.001 v1.6 (all invariants, preconditions, acceptance criteria — Invariant 3 deny-by-default ordering, PC-4 authorization precondition, EC-001 "The file is not read", EC-001 composite: max_bytes=0 + file absent → empty return, no path check)
- `crates/factory-dispatcher/src/host/read_prefix.rs` (full file: prepare + register implementations, unit test module T-001..T-012 including T-012 regression pin, `prepare_mutant` mutation witness, ok_or_else → ok_or fix at 3ac997b8; deny-path matrix analyzed against max_bytes=0 axis)
- `crates/factory-dispatcher/src/host/mod.rs` (read_prefix module declaration + register call in setup_linker)
- `crates/factory-dispatcher/src/registry.rs` (ReadPrefixCaps struct + Capabilities.read_prefix field; serde deny_unknown_fields confirmed)
- `crates/hook-sdk/src/host.rs` (read_prefix safe wrapper)
- `crates/hook-sdk/src/ffi.rs` (6-param extern + host_stubs block; wasm32 and non-wasm32 cfg branches)
- `crates/hook-plugins/read-prefix-fixture/src/main.rs` + `Cargo.toml` (fixture crate)
- `plugins/vsdd-factory/hooks-registry.toml` (DISTINCT Capability Schemas preamble block + read_prefix schema; registry preamble -2 timeout code note confirmed)
- `plugins/vsdd-factory/tests/read-prefix-wasm.bats` (T-009a..T-009h at source HEAD 3ac997b8; count-COUPLING awk + dual-mutation fixtures confirmed present)
- `.github/workflows/ci.yml` (wasm build steps + staging loops; confirmed unmodified at 3ac997b8 diff)
- `crates/factory-dispatcher/src/host/read_file.rs` (sibling parity reference; Invariant 2 byte-unmodified re-check; EC-006 systemic-gap reference)

Commit hygiene review (13 commits from base 1304d280 to HEAD 3ac997b8): feat/test/fix conventional-commit prefixes consistent; no Co-Authored-By AI attribution; single-purpose per commit; no unintended file scope bleed confirmed via `git diff --name-only` per-commit inspection.

## Exit Gates (at source HEAD 3ac997b8 — adversary-verified)

- `cargo clippy --workspace --all-targets -- -D warnings`: **clean** (exit 0; F-P3-001 ok_or fix confirmed)
- `cargo fmt --check --all`: **clean** (exit 0)
- Cargo test suite (read_prefix unit): **11/11 passing** (T-001..T-012 green including T-012 regression pin and `prepare_mutant` witness at 3ac997b8)
- Bats T-009a..T-009h: **8/8 green**
- AC-003 static gate direct-run: **PASS** (zero OUTPUT_TOO_LARGE matches in production-scoped code at 3ac997b8)

## Cascade Trajectory

| Pass | Date | Result | B | H | M | L | Streak |
|------|------|--------|---|---|---|---|--------|
| 1 | 2026-07-14 | NOT-CLEAN | 0 | 1 | 1 | 0 | 0/3 |
| 2 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 2 | 0/3 |
| 3 | 2026-07-14 | NOT-CLEAN | 1 | 0 | 0 | 0 | 0/3 |
| 4 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 1 | 0 | 0/3 |

## Next Step

Pass-5 required (streak 0/3). F-P4-001 remediation in flight: test-writer T-013 pair (no-cap+max0 → CAPABILITY_DENIED; out-of-allowlist+max0 → CAPABILITY_DENIED reason=path_not_allowed) + hoisted-short-circuit mutant witness. No story version change (test-only remediation). Source HEAD for pass-5: remediation commit from test-writer (pending). 4-index at commit: BC-INDEX v4.04 / VP-INDEX v2.68 / STORY-INDEX v4.191 / ARCH-INDEX v3.01.
