# S-19.06 LOCAL Adversary Pass-9 — CLEAN

**Date:** 2026-07-14
**Story:** S-19.06 — host::read_prefix bounded partial read
**Pass:** 9 of BC-5.39.001 cascade
**Result:** CLEAN B0/H0/M0/L0
**Streak:** 0/3 → 1/3
**Source HEAD:** 7156a4c3
**Remediation HEAD:** N/A — CLEAN pass
**Branch:** feature/S-19.06 (base 1304d280)
**Prior Pass:** pass-8 NOT-CLEAN B0/H0/M0/L2 (streak 0/3 UNCHANGED)

## Summary

CLEAN pass at remediation HEAD 7156a4c3 — the full-file comment-accuracy sweep produced by the test-writer fix burst responding to F-P8-001 and F-P8-002. Streak advances from 0/3 to 1/3.

The sweep committed 17 corrections across `crates/factory-dispatcher/src/host/read_prefix.rs` and `plugins/vsdd-factory/tests/read-prefix-wasm.bats`, accompanied by a per-claim verification table attesting each correction site against live code. Full re-derivation of AC-001..AC-007 (14/14 unit tests + 8/8 bats executed live; AC-007 four gates pass), BC PC-1..PC-6 + Invariants 2/3/4/5 + EC-001 honored, and a freshly executed EC-007 directory-read probe — all returned no findings.

Sweep-completeness falsification attempt: 6+ corrected sites and 6+ attested-accurate sites spot-verified against shipped code, including the hooks-registry preamble error-code table, the `prepare()` 7-step docstring, and T-009h coupled-count claims re-grepped against the live test. All verified accurate. No survivors found.

Zero new findings (B0/H0/M0/L0).

---

## Part A — Prior-Pass Finding Verification

### F-P8-001 — CONFIRMED CLOSED

**Prior status:** OPEN at source HEAD 3b2abd45 — test-module banner in `read_prefix.rs` enumerated 9 of 14 tests and claimed all listed tests panic at `todo!()` Red Gate; false post-implementation given T-012 and T-013 are fully implemented.

**Closure verification:** At remediation HEAD 7156a4c3 the banner has been rewritten to enumerate all 14 tests with accurate state descriptions. Stub-era `todo!()` framing removed throughout. The banner now accurately reflects the cascade-lock partition (T-012 and T-013 note groups carry real assertions; no `todo!()` stubs remain). Verified vs live `cargo test -p factory-dispatcher` output: all 14 tests pass; no stub-era framing survives in any comment block in the file.

**Result:** CONFIRMED CLOSED. No residual.

---

### F-P8-002 — CONFIRMED CLOSED

**Prior status:** OPEN at source HEAD 3b2abd45 — T-009f inline comment in `read-prefix-wasm.bats` described the `read-prefix-fixture` as a "no-op hook body compiled for wasm32"; the fixture is a plain `fn main()` WASI-command binary with no hook macro and no hook-body logic.

**Closure verification:** At remediation HEAD 7156a4c3 the T-009f comment has been corrected to accurately describe the fixture as a plain WASI-command binary. The "hook body" framing is removed. The comment now correctly distinguishes the fixture's role (minimal WASM compilation target, no `__hook_entry` macro path) from an actual hook body, and accurately characterizes why runtime WASM dispatch remains the genuinely-out-of-scope axis. Verified vs shipped `crates/read-prefix-fixture/src/main.rs`: plain `fn main()` confirmed; no hook-sdk macro invocation present.

**Result:** CONFIRMED CLOSED. No residual.

---

## Part A — New Findings

None. Zero new findings at remediation HEAD 7156a4c3 (B0/H0/M0/L0).

---

## Sweep-Completeness Falsification Attempt

A full re-derivation sweep was performed at 7156a4c3 with deliberate adversarial probing of the most common survivor patterns. Six or more corrected sites and six or more attested-accurate sites were spot-verified against shipped code:

- **hooks-registry preamble error-code table:** `read_prefix` entry fields (plugin name, WASM path, trigger events, tier, on-error behavior, error codes `−1`/`−2`/`−3`/`−4`/`−99`) re-verified against `crates/factory-dispatcher/src/host/read_prefix.rs` implementation. All accurate.
- **`prepare()` 7-step docstring:** Steps 1–7 re-verified against the `prepare()` implementation body. All accurate; no aspirational or stub-era framing survives.
- **T-009h coupled-count claim:** Re-grepped live; the coupled-count assertion in T-009h matches the actual hook registration count enforced by the coupling gate. Accurate.
- **T-012/T-013 note group inline comments:** Verified post-sweep that framing accurately describes each test's scope and the cascade-lock partition. Accurate.
- **read-prefix-wasm.bats T-009a..T-009e static guard comments:** All five describe their role as static grep-based regression guards (post-implementation). Accurate and consistent with accepted-with-record item.
- **Fixture module doc (updated by F-P6-001 fix):** Re-verified that the paragraph introduced by the F-P6-001 sweep still accurately states the stub exists for non-wasm compilation and is not exercised by any test. No misattribution revived.

No survivors found. Sweep completeness corroborated by the per-claim verification table committed at 7156a4c3.

---

## Fresh Positive Coverage (at remediation HEAD 7156a4c3)

**Full diff scope:** 14 files across full diff from base 1304d280 to source HEAD 7156a4c3. No files excluded from review.

**Unit tests live-run:** 14/14 unit tests passing (factory-dispatcher lib target). No regressions.

**Bats `read-prefix-wasm.bats`:** 8/8 tests green. T-009f confirmed present and passing.

**AC-001..AC-007 re-derivation:**
- AC-001 (bounded read with max_bytes cap): T-001..T-003 — pass.
- AC-002 (deny-by-default allowlist ordering): T-008, T-012 — pass; mutation witnesses T-013 confirm ordering is load-bearing.
- AC-003 (error taxonomy mapping from PrefixReadErr variants): T-005..T-007, T-011 — pass.
- AC-004 (WIT-interface registration / coupled-count gate): T-009a..T-009h, T-010, T-013b — pass.
- AC-005 (path resolution honoring allowlist policy): T-004, T-009c..T-009e — pass.
- AC-006 (max_bytes=0 denial): T-013a, T-013b — pass.
- AC-007 (four gates — prepare succeeds, plugin registers, bats green, ordering locked): all four gates pass at 7156a4c3.

**BC PC-1..PC-6 and Invariants 2/3/4/5 honored:**
- PC-1..PC-6: all precondition checks (path non-empty, allowlist membership, max_bytes > 0, host-filesystem read, wasm-boundary FFI, error-code mapping) confirmed operational by live test runs.
- Invariant 2 (`read_file` sibling unmodified): verified — `read_file.rs` and `path_util.rs` have no modifications since base 1304d280.
- Invariant 3 (deny-by-default ordering locked): T-012/T-013 + mutation witnesses confirm.
- Invariant 4 (chain naming internally consistent; adjudicated NOT a defect in pass-8): no change.
- Invariant 5 (error codes non-overlapping with sibling `read_file`): verified — `read_prefix` uses `−99` for Other; `read_file` uses same convention; no overlap introduced.

**EC-001 honored:** Ordering locked by T-012/T-013 + live mutation witnesses. `PrefixReadErr::DeniedNotAllowed` fires before any read attempt.

**Story EC map confirmed:**
- EC-001 → T-008 / T-012 (allowlist-deny before read)
- EC-002 → T-003 (max_bytes cap enforced)
- EC-003 → T-007 (path resolution failure maps to DENIED_RESOLUTION_FAILED)
- EC-004 → T-010 / T-013b (max_bytes=0 denial gate)
- EC-005 → T-006 (non-UTF-8 path → INVALID_ARGUMENT)
- EC-008 → T-009f (WASM compile/link verified)

**EC-007 directory-read probe (new empirical evidence, pass-9):** Executed live at 7156a4c3. `File::open` on a directory path succeeds on the test host; subsequent `take().read_to_end()` returns `IsADirectory` (OS error 21); this maps through `PrefixReadErr::Other` to `INTERNAL_ERROR (−99)`. Behavior is correct — the host-ABI contract does not require EC-007 to be separately enumerated in the Test Plan; parity with `read_file` sibling (same gap, same accepted-with-record posture) holds. No new defect.

**Bundle orphan check:** 6/6 hook plugin entries in `hooks-registry.toml` resolve to present WASM artifacts. No orphan entries.

**Workflow POLICY 20 comments:** Reviewed and accurate. No mischaracterizations.

---

## Accepted-with-Record (carried from prior passes)

**out_ptr=0 sentinel — read_file-verbatim parity:** The `out_ptr=0` / `write_bytes_to_guest` null-guard in `read_prefix::register` replicates `read_file::register`'s pre-existing wasm-boundary pattern verbatim per Invariant 2. Non-empty boundary read untested for both; coverage posture inherited. No new risk. Accepted-with-record; no routing.

**EC-006 timeout non-enforcement — SYSTEMIC host-ABI gap:** `read_prefix::register` does not enforce the timeout precondition at the wasm-boundary. Synchronous `func_wrap` closures in wasmtime cannot be preempted by epoch interruption while blocked on a blocking `read_to_end`; the `−2` timeout code is a code `read_prefix` structurally never returns. Gap spans `read_file` as well and is NOT introduced by S-19.06. Invariant 2 forbids modifying `read_file.rs`. Accepted-with-record. ROUTED: architect follow-up story recommendation anchored at the S-19.06 merge gate.

**Story Test Plan lag vs test inventory:** Tests T-013a/b, `prepare_mutant_hoisted` T-013 mutation witnesses, and T-011 appear in the suite but are not listed in the story Test Plan. Accepted per cascade-remediation convention. Accepted-with-record.

**FFI adversarial-input surface swept clean:** NUL byte in path, non-UTF-8 path bytes, u32::MAX for max_bytes, empty path string — all produce correct error codes with no undefined behavior or allowlist bypass. Accepted-with-record; no routing.

**EC-007 directory-read + T-004 Err-branch vacuous-by-construction:** Directory-read error path not covered by unit tests. Parity with `read_file`. T-004 Err-branch statically covered by T-009g. EC-007 now supported by new empirical live probe (see Fresh Positive Coverage above); behavior confirmed correct. Accepted-with-record; no routing.

**T-009a..T-009e as static regression guards, not red-gates:** Honestly documented post-implementation. Accepted-with-record; no routing.

**Story v1.21 ↔ BC v1.6 consistency:** All 10 body-site citations in S-19.06 v1.21 reference BC-1.17.001 v1.6. Task renumbering consistent through v1.17..v1.21 revisions. Confirmed accurate at 7156a4c3.

**Commit hygiene:** 17 commits from base 1304d280 to remediation HEAD 7156a4c3. Conventional-commit prefixes consistent (feat/test/fix/docs); no `Co-Authored-By` AI attribution; single-purpose per commit; no unintended file-scope bleed confirmed.

**POLICY 10 — n/a pre-Step-5:** PR merge gate not yet triggered.

**Invariant 4 chain naming (adjudicated NOT a defect):** Confirmed; no change at 7156a4c3.

---

## Could-Not-Verify

- **S-19.04 internal AC cross-ref numbering:** Out of perimeter for this cascade; not reviewed.
- **Runtime WASM dispatch:** Documented extent of coverage = compile/link (T-009f). Actual in-process WASM execution path not exercised by the test suite. Consistent with accepted-with-record EC-008 posture across all prior passes.
- **Full-workspace clippy rerun:** Not re-run this pass. Orchestrator ran `cargo clippy --workspace --all-targets -- -D warnings` clean at 3ac997b8; CI enforces on every push. No clippy-touching changes introduced by the 7156a4c3 sweep.

---

## Exit Gates (at remediation HEAD 7156a4c3 — status at pass-9 close)

- `cargo clippy --workspace --all-targets -- -D warnings`: **clean** (verified clean at 3ac997b8; no clippy-touching changes at 7156a4c3)
- `cargo fmt --check --all`: **clean**
- Bats `read-prefix-wasm.bats` T-009a..T-009h: **8/8 green**
- Unit tests: **14/14 green**
- Full diff (14 files): **reviewed — zero findings**
- F-P8-001: **CONFIRMED CLOSED** (7156a4c3)
- F-P8-002: **CONFIRMED CLOSED** (7156a4c3)
- New findings: **none (B0/H0/M0/L0)**
- 4-index at commit: BC-INDEX v4.04 / VP-INDEX v2.68 / STORY-INDEX v4.191 / ARCH-INDEX v3.01

---

## Cascade Trajectory

| Pass | Date | Result | B | H | M | L | Streak |
|------|------|--------|---|---|---|---|--------|
| 1 | 2026-07-14 | NOT-CLEAN | 0 | 1 | 1 | 0 | 0/3 |
| 2 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 2 | 0/3 |
| 3 | 2026-07-14 | NOT-CLEAN | 1 | 0 | 0 | 0 | 0/3 |
| 4 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 1 | 0 | 0/3 |
| 5 | 2026-07-14 | **CLEAN** | 0 | 0 | 0 | 0 | **1/3** |
| 6 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 1 | 0/3 RESET |
| 7 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 2 | 0/3 UNCHANGED |
| 8 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 2 | 0/3 UNCHANGED |
| 9 | 2026-07-14 | **CLEAN** | 0 | 0 | 0 | 0 | **1/3** |

## Next Step

Pass-9 is CLEAN B0/H0/M0/L0. Streak advances to 1/3. Pass-10 commences from the same source HEAD 7156a4c3 — no remediation needed before next pass. Two consecutive CLEAN passes required for convergence (BC-5.39.001 3-CLEAN protocol). No story version change. 4-index at commit: BC-INDEX v4.04 / VP-INDEX v2.68 / STORY-INDEX v4.191 / ARCH-INDEX v3.01.
