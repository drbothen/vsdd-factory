# W-16 S-9.30 pass-1 adversarial review

Date: 2026-05-03
Adversary agentId: add95faf2f5e0986d
Target: .factory/stories/S-9.30-host-run-subprocess-sdk.md (698L)
Verdict: SUBSTANTIVE (20 findings: 8 HIGH, 9 MEDIUM, 3 LOW)
Pass: 1 of N

AC↔MUST trace verification: All 24 MUSTs nominally traced across 8 ACs, but I-2.6 double-traced, I-7.21+I-7.23 trace to nonexistent HostError variants (F-01).

> **Note:** Full 20-finding table in agent transcript at agentId add95faf2f5e0986d.
> This file captures the 8 HIGH severities verbatim; MEDIUM and LOW summaries follow.
> Story-writer fix-burst MUST consult the full transcript before applying fixes.
> Architect fix-burst MUST address F-01/F-02/F-03 (architectural decisions) BEFORE story-writer applies remaining fixes.

---

## Findings Table

| ID | Severity | Category | Location | Description | Suggested Fix |
|----|----------|----------|----------|-------------|---------------|
| F-01 | HIGH | error-semantics | AC-8b/d vs hook-sdk/src/host.rs:80-94 | AC mandates HostError::BinaryNotFound + HostError::IoError variants that DO NOT EXIST in the current HostError enum. I-7.21 and I-7.23 trace to these nonexistent variants. AC-8(e) "no new error codes" creates a self-contradiction: you cannot trace to variants that don't exist AND forbid adding new ones. | Architect decision required: (a) Add task to extend HostError with BinaryNotFound + IoError variants (most correct); (b) OR remap ACs to existing HostError variants and document the semantic loss; (c) OR relax AC-8(e) to allow exactly these 2 new variants. Pick one and propagate to all BC-2.02.013 MUST traces. |
| F-02 | HIGH | scope | registry.rs:54-88 ExecSubprocessCaps vs S-9.30 SubprocessCaps | Legacy ExecSubprocessCaps (BC-2.02.005) already exists in the codebase with a different field layout. S-9.30 introduces a parallel SubprocessCaps with different fields (added allowed_envs, removed legacy fields). Coexistence is undefined: do both structs live in the SDK? Does ExecSubprocessCaps become an alias? Is BC-2.02.005 deprecated? The implementer has no guidance. | Architect decision required: (a) Supersede ExecSubprocessCaps with SubprocessCaps and retire BC-2.02.005; (b) keep both as separate types with a documented coexistence contract; (c) make ExecSubprocessCaps a type alias for SubprocessCaps. Pick one; update BC-2.02.005 lifecycle field + BC-INDEX; add task to S-9.30. |
| F-03 | HIGH | scope (protocol) | S-9.30 lines 385-386, 448-454 | JSON-pointer FFI protocol lacks a size-out pointer. When the host serializes SubprocessResult (which includes stdout/stderr as variable-length byte buffers), the SDK guest plugin cannot know the buffer size in advance. The read_file pattern uses a size-out pointer; run_subprocess doesn't. AC-8(e) explicitly excludes OutputTooLarge as an error — so there is no error path for buffer overflow. This creates undefined behavior on large subprocess output. | Architect decision required: (a) Add `size_out: *mut u32` parameter to `run_subprocess_impl` (mirrors read_file pattern); (b) OR re-introduce HostError::OutputTooLarge (contradicts AC-8(e)); (c) OR fix max envelope size to a compile-time constant and document the cap. Pick one; update ABI signature in SS-02, AC-8, and all related BCs. |
| F-04 | HIGH | cross-doc | BC-2.02.013 EC count vs S-9.30 §EC table | BC-2.02.013 lists 20 Edge Cases (EC-001..EC-020). S-9.30 §EC table lists 18 rows. Delta = 2. Two ECs present in BC-2.02.013 are absent from S-9.30's own EC section. The story is supposed to implement the full BC. | Identify the 2 missing ECs; either add them to S-9.30 §EC or explicitly carve them to a separate story with a cross-reference. |
| F-05 | HIGH | subsystem boundary | S-9.30 §subsystems field vs tasks | S-9.30 frontmatter declares `subsystems: [SS-02]` only. But the tasks modify `factory-dispatcher::host` module (which lives in SS-01 per SS-01 architecture doc) and `crates/hook-sdk` (SS-02). The story touches both SS-01 and SS-02 modules. SS-01 is not declared. | Add SS-01 to `subsystems` list. Verify disclosure pattern is consistent with S-8.10 / S-8.30 (prior analogous stories). |
| F-06 | HIGH | security | S-9.30 §AC-4 + SubprocessCaps.allowed_paths | Path canonicalization is not required. AC-4 asserts that `allowed_paths` entries are enforced, but no task or AC requires that paths be canonicalized before comparison. A plugin could pass `../../../etc/passwd` and evade an `allowed_paths = [".factory/"]` check if the host compares raw string values. | Add AC or task requiring path canonicalization (e.g., `std::fs::canonicalize`) before allowed_paths comparison. Cross-ref BC-1.05.001 (CAPABILITY_DENIED invariant). |
| F-07 | HIGH | security | S-9.30 §AC-5 + tasks — subprocess FD inheritance | No task or AC requires closing file descriptors before exec. If factory-dispatcher holds open FDs (sinks, log files), the subprocess inherits them. CAPABILITY_DENIED for file access is meaningless if the subprocess inherits open FDs by a different path. | Add task: use `close_range(3, RLIMIT_NOFILE, CLOSE_RANGE_CLOEXEC)` or equivalent before exec, OR document the threat model and accept the residual risk explicitly. |
| F-08 | HIGH | AC-quality | AC-2 timeout enforcement | AC-2 requires that the subprocess is killed after `timeout_ms`. But no task specifies WHICH signal (SIGTERM then SIGKILL? SIGKILL immediately?), the grace period, or behavior on kill failure. If the subprocess ignores SIGTERM, the factory-dispatcher worker thread could block indefinitely. | Add task specifying the kill strategy (e.g., SIGTERM → 100ms grace → SIGKILL) or reference an existing BC that defines this contract. |
| F-09 | MEDIUM | cross-doc | S-9.30 §inputs[] | inputs[] does not list BC-2.02.013 as an input. S-9.30 was authored specifically to implement BC-2.02.013, but the frontmatter `inputs` array only references E-9 and SS-02. | Add `BC-2.02.013` to inputs[]. |
| F-10 | MEDIUM | AC-quality | AC-6 memory safety | AC-6 asserts "no unsafe blocks in S-9.30 implementation." However, the FFI boundary (extern "C" host functions, raw pointer arithmetic for size-out) inherently requires unsafe. Either the AC is wrong, or the implementation strategy (no raw pointers) needs to be stated explicitly. | Clarify: either unsafe is permitted in FFI glue code and AC-6 scopes to "no unsafe in business logic," or document the encapsulation strategy that avoids unsafe at the FFI layer (e.g., wit-bindgen-generated code not counted). |
| F-11 | MEDIUM | scope | S-9.30 §tasks — signal handling | No task covers the child process's signal disposition. If factory-dispatcher is itself receiving SIGTERM during a subprocess run, the child may become an orphan. | Add a task or explicit out-of-scope note for orphan process handling. If out-of-scope, reference a TD item. |
| F-12 | MEDIUM | AC-quality | AC-3 environment isolation | AC-3 requires that `allowed_envs` filter the environment. But no task specifies the DEFAULT environment when `allowed_envs = []` (empty list). Is it an empty env, or a minimal set (PATH, HOME, TMPDIR)? | Add an EC row or task defining behavior for `allowed_envs = []`. |
| F-13 | MEDIUM | scope | S-9.30 §tasks — working directory | No task specifies the subprocess working directory. A plugin that shells out to a path-relative command will have unpredictable behavior depending on where factory-dispatcher was launched. | Add task: set working directory to a well-defined location (e.g., project root, or a per-invocation temp dir). |
| F-14 | MEDIUM | cross-doc | S-9.30 §AC-7 + SS-02 §telemetry | AC-7 requires a `subprocess.completed` telemetry event. SS-02 §telemetry does not list this event. Either SS-02 is stale, or AC-7 is introducing a new event that needs to be declared in SS-02. | Architect to add `subprocess.completed` (and related `subprocess.failed`, `subprocess.timeout`) to SS-02 §events; story-writer to cross-reference. |
| F-15 | MEDIUM | AC-quality | AC-8 — overall | AC-8 is a grab-bag of 5 sub-items (a)-(e). Item (e) "no new error codes" directly conflicts with F-01 (missing HostError variants). This finding supersedes F-01 in scope but it is worth noting that resolving F-01 necessarily invalidates AC-8(e) or requires it to be scoped more narrowly. | After architect resolves F-01, re-examine all of AC-8 for internal consistency. |
| F-16 | MEDIUM | library-pinning | S-9.30 §lib-table — wit-bindgen | wit-bindgen listed without pinned version. POLICY 4 violation (same as E-9 F-8/F-10). | Pin to the version in use. |
| F-17 | MEDIUM | formatting | S-9.30 §MUST-trace table | MUST I-2.6 appears twice in the trace table (double-traced as noted in the header). Remove the duplicate and verify the correct AC mapping. | Remove duplicate I-2.6 row; confirm the intended single AC target. |
| F-18 | LOW | editorial | S-9.30 frontmatter `status` | `status: draft` correct. Confirm `wave: 0` matches E-9 topology once E-9 F-17 is resolved. | Verify after E-9 fix-burst. |
| F-19 | LOW | cross-doc | S-9.30 §success-criteria | Success criteria do not reference ADR-013 convergence requirement. Same pattern as E-9 F-16 and S-9.00 F-10. | Add convergence note. |
| F-20 | LOW | scope | S-9.30 §background | Background says "analogous to S-8.10 and S-8.30" but does not call out the key DIFFERENCE: S-8.10/S-8.30 added read-only host functions; S-9.30 adds a WRITE/EXEC capability (subprocess) with a fundamentally different threat model. | Add a sentence in Background distinguishing the threat model from S-8.10/S-8.30 and cross-referencing ADR-014 §security-rationale. |

---

## Convergence Status

- **Pass 1:** SUBSTANTIVE — 20 findings (8 HIGH, 9 MEDIUM, 3 LOW)
- **Next action (architect):** F-01 (HostError variants architectural decision), F-02 (ExecSubprocessCaps coexistence decision), F-03 (JSON-pointer protocol size-out decision) — these must be resolved BEFORE story-writer applies fixes, as their resolutions ripple into SS-02, BC-2.02.013, and the MUST-trace table
- **Next action (story-writer, after architect):** Remaining 17 findings in severity order
- **Convergence clock:** 0 of 3 NITPICK_ONLY passes
