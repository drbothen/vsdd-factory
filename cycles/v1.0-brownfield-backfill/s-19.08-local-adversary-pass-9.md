# S-19.08 LOCAL Adversary Pass-9 — CLEAN

**Date:** 2026-07-14
**Story:** S-19.08 — verify-state-timestamp-refresh: raise 64 KiB byte cap to 256 KiB + wire extract_frontmatter
**Pass:** 9 of BC-5.39.001 cascade
**Result:** CLEAN B0/H0/M0/L0
**Streak:** 1/3 → 2/3
**Source HEAD:** fea77eea
**Branch:** feature/S-19.08 (base 7b35c8e4)
**Commits reviewed (since pass-8):** none — pass-9 is a re-check of HEAD fea77eea; no code changes since pass-8
**Prior Pass:** pass-8 CLEAN B0/H0/M0/L0 (streak 1/3)

## Summary

Zero findings. No code changes since pass-8. Adversary performed an independent re-derivation pass against HEAD fea77eea with five targeted load-bearing re-checks: (1) Invariant 7 wiring genuinely load-bearing — T-004 non-UTF-8-after-delimiter fixture fails without extract_frontmatter, confirming the integration is not dead code; (2) soft-warn boundary matches BC Invariant 8 table exactly — sub-tests A/B/C/D/E aligned with the spec-tabulated threshold/cap alignment; (3) hostile-payload/panic sweep clean — no unwrap/expect/panic reachable from hostile input, unreachable!() sites provably cold, reconstruction subtraction cannot underflow (guarded by prior find/contains); (4) trigger-path correctness — absolute paths fire, evil.factory/STATE.md and .bak suffixes do not fire, non-firing is fail-open; (5) spec↔code↔test parity — story frontmatter ↔ BC-5.40.001 v1.2 ↔ T-001..T-007 consistent, Cargo dep present, diff boundary clean.

Accepted-with-record re-affirmed. NEW adjudication note added for Invariant 8 log_warn formatted string: this item is at the production-grade ceiling under ADR-025 HOST_ABI_VERSION=1 no-new-host-functions constraint; log_warn(&str) is the only observability primitive available in the WASM hook ABI, so u64 field-type spec compliance via structured logging is architecturally blocked until a future host extension. Additionally: cap-enforcement mocks cannot detect a hypothetical production read_bounded comparator regression, but that comparator is covered by read_file.rs's own unit tests and the S-19.02 merged sibling (F-P7-001 closure holds independently); body-only Edits leaving timestamp unchanged yielding Block is correct PC4 enforcement (timestamp unchanged → not a genuine refresh → block is the correct decision), not over-blocking. All other accepted-with-record items carried unchanged from prior passes.

Streak advances 1/3 → 2/3. Pass-10 required.

## Part A — Findings

No new findings at HEAD fea77eea. The three carried accepted-with-record items (F-P1-001, F-P1-002, Invariant 8 log_warn) are re-affirmed below; none rise to findings under the production-grade lens.

---

### F-P1-001 — LOW — T-001 const-pin question (carried accepted-with-record; independently re-derived pass-9)

**Location:** `crates/hook-plugins/verify-state-timestamp-refresh/tests/integration_t006_no_output_too_large.rs` — `test_BC_`-prefixed const-pin test
**Observation (re-derived):** T-001 asserts a const value equal to the production `READ_BOUNDED_CAP_BYTES` constant without calling a production function through the public API. Pattern superficially resembles a tautological const-echo test.
**Why accepted (unchanged):** AC-001-mandated pin; references production symbol directly; byte-identical to S-19.02 merged sibling pattern accepted by same reasoning. Not a tautology.
**Status:** Carried accepted-with-record; open follow-up: none.

---

### F-P1-002 — LOW — [process-gap] BC-5.40.001 VP Anchors placeholder text (carried accepted-with-record; independently re-derived pass-9)

**Location:** `.factory/specs/behavioral-contracts/ss-05/BC-5.40.001.md` — "VP Anchors" / "Verification Properties" section
**Observation (re-derived):** BC-5.40.001 states S-19.08 unit-test VP IDs are "to be assigned by state-manager after S-19.08 VP authoring pass." VP-096 reuse is already active in the VP-INDEX; the placeholder text is a bookkeeping gap.
**Why accepted (unchanged):** Outside code perimeter; no effect on compilation, test execution, or AC gate. Correct target: state-manager VP-authoring bookkeeping step at S-19.08 post-merge/convergence burst.
**Status:** Carried accepted-with-record; routing: product-owner/state-manager — post-story burst.

---

## Could-Not-Verify

- **Live host-level capability_denied event capture:** VSDD_SINK_FILE dispatcher run is out of harness scope; capability_denied path cannot be verified without live dispatcher invocation. Accepted operational proxy: verify-factory-lock parity pattern. Carried from prior passes unchanged.

## Part B — Load-Bearing Re-Checks (pass-9 independent re-derivation)

Adversary independently re-derived the following five load-bearing properties at HEAD fea77eea:

**1. Invariant 7 wiring genuinely load-bearing.** T-004 fixture (non-UTF-8 bytes after delimiter) exercises the extract_frontmatter code path. Without extract_frontmatter wired in the production plugin, T-004 would return a parsing error or incorrect Continue outcome. Adversary confirms the integration is not dead code: removing the extract_frontmatter call from the production path would cause T-004 to fail. Load-bearing confirmed.

**2. Soft-warn boundary matches BC Invariant 8 table exactly.** BC-5.40.001 Invariant 8 specifies a threshold table with five sub-cases (A: below soft-warn; B: at soft-warn; C: between soft-warn and cap; D: at cap exact; E: above cap). Adversary verified alignment: sub-test A < 200_000 → Continue no-warn; sub-test B = 200_000 → Continue + log_warn; sub-test C between 200_000 and 262144 → Continue + log_warn; sub-test D = 262144 → Continue (inclusive cap boundary); sub-test E = 262145 → Block TooLarge. All five sub-cases aligned with the BC Invariant 8 table. No drift detected.

**3. Hostile-payload/panic sweep clean.** Adversary traced all reachable code paths from external file input through to plugin return. No unwrap() or expect() calls reachable from hostile input; all user-controlled bytes go through explicit match/? error propagation. unreachable!() sites are provably cold (preceded by exhaustive match arms or type-guaranteed branches). Reconstruction subtraction (used for Write payload reconstruction) cannot underflow: the subtraction occurs after a confirmed find/contains guard that establishes the subtracted offset is within bounds. Panic-free under hostile input confirmed.

**4. Trigger-path correctness.** Adversary confirmed: absolute paths matching the VSDD_SINK_FILE pattern fire the plugin hook; paths containing "evil.factory/STATE.md" (wrong prefix) do not fire; paths with ".bak" suffixes do not fire. Non-firing is fail-open (passes through to the tool call unchanged). No false-positive blocking of legitimate tool calls confirmed; no false-negative blocking of malicious paths confirmed under the trigger specification.

**5. Spec↔code↔test parity.** Story frontmatter ACs, BC-5.40.001 v1.2 acceptance criteria, and T-001..T-007 test assertions are mutually consistent: every AC in the story maps to at least one test; every BC criterion is exercised; no AC appears in the BC without a corresponding test assertion. Cargo.toml dependency on hook-sdk is present. Cumulative diff boundary (lib.rs +1262/-19; integration tests +260; hooks-registry unmodified; reuse refs unmodified) consistent with prior passes. No drift.

## Accepted-With-Record (re-affirmed pass-9)

- **Invariant 8 log_warn formatted string:** NEW adjudication note — this item is at the production-grade ceiling under ADR-025 HOST_ABI_VERSION=1 no-new-host-functions constraint. log_warn(&str) is the only observability primitive available in the WASM hook ABI; structured u64 field-type logging would require a new host function not yet registered in the ABI. Compliance with the BC u64 field-type spec is architecturally blocked until a future host extension is defined and registered. Not a production shortcut — it is the correct implementation under the current ABI contract.
- **Cap-enforcement mocks cannot detect a hypothetical production read_bounded comparator regression:** The mock-based harness uses an inline comparator, not the production read_bounded function. A regression in the production comparator would not be caught by T-007 mock sub-tests. However, the production read_bounded comparator is covered by read_file.rs's own unit tests and by the S-19.02 merged sibling (which tests the same comparator in a shipped context). F-P7-001 closure holds independently of T-007 mock fidelity.
- **Body-only Edits leaving timestamp unchanged → Block is correct PC4 enforcement:** A file Edit that modifies body content but leaves the frontmatter timestamp field unchanged is correctly blocked. PC4 states that a timestamp-unchanged Edit is not a genuine state refresh; blocking is the intended behavior, not over-blocking. Confirmed aligned with BC-5.40.001 PC4 semantics.
- **Defensively-redundant `<=` upper bound in soft-warn condition:** Always-true on success path; matches BC table note. Mutation-tested in pass-8 (the `<=` bound is load-bearing as an inclusive boundary marker). Cosmetic, not a coverage gap.
- **F-P2 positive-direction tests as over-block guards:** Tests that verify Block outcomes for valid AC inputs provide over-block regression coverage. Accepted as structurally sound.
- **Soft-warn 200_000 inline literal:** Single-use, comment-cited, cosmetic only.
- **Reconstruction base computed for Write payloads:** Bounded O(n), not a fuel risk. Pre-accepted.
- **F-P1-001 / F-P1-002 per above.**

## Coverage

Adversary examined the following artifacts at HEAD fea77eea (no new diff; re-derivation pass from prior committed state):

- S-19.08 story (all ACs, gate forms, task list, Architecture Compliance Rules, EC-001..EC-005)
- BC-5.40.001 v1.2 (all invariants, preconditions, acceptance criteria; Precondition 6 / Invariants 7+8 / PC4 / EC-010 verified)
- `crates/hook-plugins/verify-state-timestamp-refresh/src/lib.rs` (full file; cumulative lib.rs +1262/-19 scope confirmed; main.rs wiring confirmed; no scope bleed)
- `crates/hook-plugins/verify-state-timestamp-refresh/tests/` — all test files including T-007 family; 52 assertions enumerated
- `plugins/vsdd-factory/hooks-registry.toml` — confirmed unmodified (reuse refs intact)
- POLICY 21 (cap-raise registration) — hooks-registry.toml cap-value consistent with `READ_BOUNDED_CAP_BYTES`
- POLICY 11 — PASS
- TD-VSDD-091 — function-name anchors only in comments; no line-number citations — PASS
- Demo-evidence absence — PASS (pre-Step-5; no demo required)

## Exit Gates (adversary independently verified)

- Crate test suite: **52 assertions green** (50 lib + 2 integration at HEAD fea77eea; unchanged from pass-8)
- `cargo clippy --workspace --all-targets -- -D warnings`: **clean** (unchanged from pass-8)
- `cargo fmt --check --all`: **clean** (unchanged from pass-8)
- Diff boundary: lib.rs +1262/-19 (cumulative); integration test +260 (cumulative); hooks-registry unmodified; reuse refs unmodified
- POLICY 21 satisfied: hooks-registry.toml cap-value consistent with production constant

## Cascade Trajectory

| Pass | Date | Result | B | H | M | L | Streak |
|------|------|--------|---|---|---|---|--------|
| 1 | 2026-07-14 | CLEAN | 0 | 0 | 0 | 2-accepted | 1/3 |
| 2 | 2026-07-14 | NOT-CLEAN | 0 | 1 | 1 | 2-accepted | 0/3 RESET |
| 3 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 1+2-accepted | 0/3 UNCHANGED |
| 4 | 2026-07-14 | CLEAN | 0 | 0 | 0 | 0+2-accepted | 1/3 |
| 5 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 1+2-accepted | 0/3 RESET |
| 6 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 1+2-accepted | 0/3 UNCHANGED |
| 7 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 1+2-accepted | 0/3 UNCHANGED |
| 8 | 2026-07-14 | CLEAN | 0 | 0 | 0 | 0+2-accepted | 1/3 |
| 9 | 2026-07-14 | CLEAN | 0 | 0 | 0 | 0+2-accepted | 2/3 |

## Next Step

Pass-10 required (streak 2/3; need 3/3 for CONVERGED). Source HEAD: fea77eea (no fix required; CLEAN pass).
