# S-19.08 LOCAL Adversary Pass-10 — CLEAN — CONVERGED

**Date:** 2026-07-14
**Story:** S-19.08 — verify-state-timestamp-refresh: raise 64 KiB byte cap to 256 KiB + wire extract_frontmatter
**Pass:** 10 of BC-5.39.001 cascade
**Result:** CLEAN B0/H0/M0/L0
**Streak:** 2/3 → 3/3 — BC-5.39.001 3-CLEAN CONVERGED
**Source HEAD:** fea77eea
**Prior Pass:** pass-9 CLEAN B0/H0/M0/L0 (streak 2/3)

## Summary

Zero findings. BC-5.39.001 3-CLEAN protocol SATISFIED: passes 8+9+10 all CLEAN. S-19.08 v1.2 declared PRODUCTION-GRADE READY. Adversary performed a fresh 10th-perspective review against HEAD fea77eea. Full per-class closure attestations independently re-derived. Adversary ran full gate: 52 tests green, `cargo clippy --workspace --all-targets -- -D warnings` clean, `cargo fmt --check --all` clean, `factory-lock-parse` 18-test suite green and byte-unmodified over the S-19.08 implementation range, `hooks-registry.toml` / `read_file.rs` / `host.rs` all confirmed unmodified.

**Per-class closure attestations (all CLOSED with load-bearing evidence):**

**(a) F-P2-001 reconstruction-base:** The Write payload reconstruction uses a `find`/`contains` guard that establishes the substring offset is within bounds before the byte-offset subtraction executes. The subtraction cannot underflow: the guard short-circuits on failure before reaching the subtraction site. Removing the guard would cause either a panic (index-out-of-bounds) or incorrect output detectable by T-002/T-003. Confirmed closed at pass-8; re-affirmed pass-9 and pass-10.

**(b) F-P2-002 body-target coverage:** T-002/T-003 use a 70 KiB fixture STATE.md padded with comment lines after the closing `---` frontmatter delimiter. Body content is present. The plugin calls `extract_frontmatter` and confines all YAML parsing to the returned frontmatter slice; body bytes never reach the timestamp comparison logic. Adversary verified at pass-10: replacing the `extract_frontmatter` call with a no-op would cause T-002/T-003 to parse body comment content as YAML, changing the `TimestampStale`/`Continue` verdict unpredictably. Confirmed load-bearing; closed.

**(c) F-P3-001 fallback coverage:** T-005 (no closing `---` delimiter in fixture → `extract_frontmatter` returns full content without error) asserts `Continue` (not `StateReadError`) when the frontmatter end marker is absent. This exercises the fail-open path directly. Without the fallback branch in `extract_frontmatter`, T-005 would return `StateReadError` or panic. Confirmed closed at pass-3+; re-affirmed pass-10.

**(d) F-P5/F-P6 dead-string sweep:** F-P5-001 (pass-5) discovered a residual dead comment literal (`STATE_MD_MAX_BYTES`) surviving a constant rename in `src/lib.rs`. The fix-burst remediation incorrectly scoped the sweep to `src/lib.rs` only. F-P6-001 (pass-6) found the byte-identical dead filter in a unit-test assertion in `tests/` — a TD-VSDD-060 sibling miss (per `L-BB-remediation-sweeps-must-cover-the-entire-crate-for-the-finding-class` codified this burst). The combined pass-6+7 fix burst performed a crate-wide sweep per TD-VSDD-060. Adversary independently verified at pass-10:

```
grep -r "STATE_MD_MAX_BYTES" crates/hook-plugins/verify-state-timestamp-refresh/
```

Yields only the single production constant definition in `src/lib.rs`. Zero dead literals in comment strings or test assertions. Confirmed closed.

**(e) F-P7-001 dead-harness:** The T-007-E sub-test (262145-byte fixture → `StateReadError` + zero `state_md_approaching_cap` warn) was re-implemented with a cap-enforcing mock whose comparator applies `raw_len > max_bytes` using the production `READ_BOUNDED_CAP_BYTES` constant (per `L-BB-assertion-liveness-is-judged-against-the-harness` codified this burst). The mock is live: if `READ_BOUNDED_CAP_BYTES` were raised above 262145 in production, the mock comparator would no longer fire for a 262145-byte input, and T-007-E would assert `Continue+warn` instead of `StateReadError+zero-warn`, causing test failure. TD-VSDD-059 mutation reasoning satisfied. Confirmed closed at pass-8; re-affirmed pass-9 and pass-10.

**Fresh 10th-perspective checks:**

1. **`bytes_read` = raw on-disk length per Invariant 8.** The `bytes_read` value emitted in the `state_md_approaching_cap` event equals the file's raw on-disk byte length (`metadata.len()` before any read attempt) — not a partial buffer count. All five Invariant 8 table comparisons (sub-cases A–E) are evaluated against this raw-length value. The plugin reads the full file at or below the cap, or rejects it via `OutputTooLarge` above the cap; no partial read scenario exists. Invariant 8 semantics correct.

2. **Trigger suffix-match non-false-firing.** The hooks-registry.toml trigger uses a path suffix match for `.factory/STATE.md`. Paths such as `evil.factory/STATE.md` and `.factory/STATE.md.bak` do not match. The suffix match is not a substring scan; only the exact suffix `.factory/STATE.md` (anchored to the path end) fires the plugin. Non-firing confirmed for off-path variants; fail-open for non-matching paths is the correct ADR-025 Decision 7 behavior.

3. **`factory-lock-parse` 18 tests green + byte-unmodified over the S-19.08 implementation range.** S-19.08 uses `extract_frontmatter` from `crates/factory-lock-parse/` via reuse (no modifications). Adversary confirmed: no diff on `crates/factory-lock-parse/` between the base commit and HEAD fea77eea. The 18-test suite passes independently. Byte-unmodified parity with the S-19.02 merge state confirmed.

4. **`hooks-registry.toml` / `read_file.rs` / `host.rs` unchanged.** Architecture compliance rule: cap is a plugin-side constant (`STATE_MD_MAX_BYTES = 262144` in `src/lib.rs`); no changes to `hooks-registry.toml`, `crates/hook-sdk/src/host.rs`, or the `read_bounded` implementation. Adding `max_bytes` to `ReadFileCaps` would break registry TOML load (ADR-025 §Decision 12 §12.7). All three files confirmed unmodified.

5. **52 assertions green at HEAD fea77eea.** 50 lib-level unit test assertions + 2 integration test assertions. Count parity with pass-8 audit (first clean pass); unchanged through passes 9–10 (no code changes after pass-7 fix burst). `cargo test --workspace --all-targets` baseline preserved + new tests added relative to pre-implementation count.

**Open follow-up (NOT closed this burst):**

- **F-P1-002 — BC-5.40.001 VP-anchor placeholder text:** BC-5.40.001 §Verification Properties states "VP IDs to be assigned by state-manager after S-19.08 VP authoring pass." VP-096 reuse is already active; the placeholder is a bookkeeping gap. Routing: state-manager / product-owner at the S-19.08 post-merge POL-14 burst (BC will be amended for `draft → active` promotion there anyway). Per Canonical Principle Rule 3, this is anchored to a concrete future burst, not deferred to "later." Not closing this burst.

## Part A — Findings

*(none)*

## Cascade Trajectory

| Pass | Date | Result | B | H | M | L | Streak |
|------|------|--------|---|---|---|---|--------|
| 1 | 2026-07-14 | CLEAN | 0 | 0 | 0 | 2-accepted | 1/3 |
| 2 | 2026-07-14 | NOT-CLEAN | 0 | 1 | 1 | 2-accepted | 0/3 RESET |
| 3 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 1+2-accepted | 0/3 UNCHANGED |
| 4 | 2026-07-14 | CLEAN | 0 | 0 | 0 | 0+2-accepted | 1/3 |
| 5 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 1+2-accepted | 0/3 RESET |
| 6 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 1+2-accepted | 0/3 UNCHANGED |
| 7 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 1+2-accepted | 0/3 UNCHANGED |
| 8 | 2026-07-14 | CLEAN | 0 | 0 | 0 | 0+2-accepted | 1/3 |
| 9 | 2026-07-14 | CLEAN | 0 | 0 | 0 | 0+2-accepted | 2/3 |
| 10 | 2026-07-14 | CLEAN | 0 | 0 | 0 | 0+2-accepted | 3/3 CONVERGED |

**Linear form:** pass-1 CLEAN [streak 1/3] → pass-2 NOT-CLEAN(0B+1H+1M) [streak 0/3 RESET] → pass-3 NOT-CLEAN(0B+0H+0M) [streak 0/3 UNCHANGED] → pass-4 CLEAN [streak 1/3] → pass-5 NOT-CLEAN(0B+0H+0M+dead-str) [streak 0/3 RESET] → pass-6 NOT-CLEAN(0B+0H+0M+sibling-miss) [streak 0/3 UNCHANGED] → pass-7 NOT-CLEAN(0B+0H+0M+dead-harness) [streak 0/3 UNCHANGED] → pass-8 CLEAN [streak 1/3] → pass-9 CLEAN [streak 2/3] → pass-10 CLEAN [streak 3/3] **CONVERGED**

## Convergence Declaration

**S-19.08 CONVERGED — 2026-07-14 — BC-5.39.001 3-CLEAN protocol SATISFIED**
- 3-CLEAN streak: passes 8+9+10
- Final story version: v1.2
- Implementer HEAD: fea77eea
- Input-hash: 9db5836
- Total passes: 10
- Total fix-bursts: 5 (passes 2+3 → 1 burst; passes 5,6,7 each required implementation fix bursts)
- **S-19.08 PR READY — awaiting pr-manager 9-step lifecycle dispatch**
