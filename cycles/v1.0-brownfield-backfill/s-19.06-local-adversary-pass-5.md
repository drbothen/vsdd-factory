# S-19.06 LOCAL Adversary Pass-5 — CLEAN

**Date:** 2026-07-14
**Story:** S-19.06 — host::read_prefix bounded partial read
**Pass:** 5 of BC-5.39.001 cascade
**Result:** CLEAN B0/H0/M0/L0
**Streak:** 1/3 (advanced from 0/3)
**Source HEAD:** e6a69a4d
**Branch:** feature/S-19.06 (base 1304d280)
**Prior Pass:** pass-4 NOT-CLEAN B0/H0/M1/L0 (F-P4-001 OPEN; test-writer T-013 pair + hoisted-mutant witness in flight)
**Remediation HEAD:** e6a69a4d (test-writer T-013 pair + hoisted-mutant witness — LANDED)

## Summary

Pass-4 finding F-P4-001 (MEDIUM — max_bytes=0 + denial interaction not tested; harness-liveness gap) CLOSED-VERIFIED at source HEAD e6a69a4d. Zero new findings. Full re-derivation clean: AC-001..AC-007 load-bearing coverage enumerated; BC-1.17.001 PC-1..PC-6 + Invariants 2/3/4/5 held; `read_file.rs` / `path_util.rs` UNMODIFIED re-confirmed; event byte-parity correct by construction (shared `denial_event` / `emit_internal`; `function` field differs correctly); hook-sdk `from_code` mapping complete (−1/−2/−3/−4/−5 named + −99/unknown → Other); registry loader backward-compatible (serde default, comment-only preamble, no live entries); POLICY 20 dual-layer verified; suite self-falsifying (2 MUTANT_VERIFY witnesses + 2 bats mutation fixtures).

Adversary ran all gates at source HEAD e6a69a4d: `cargo clippy --workspace --all-targets -- -D warnings` clean (dispatcher + hook-sdk + fixture); `cargo fmt --check --all` clean; factory-dispatcher lib 194/194; bats 8/8. Streak advances to 1/3. Pass-6 required for 2/3.

## Part A — Prior-Pass Finding Verification

### F-P4-001 Closure Verification — MEDIUM — [harness-liveness class] max_bytes=0 + denial interaction not tested: CLOSED-VERIFIED

**Prior finding:** `prepare()` enforced deny-by-default ordering (capability check + path allow-list check precede the `max_bytes == 0` short-circuit) correctly, but no test exercised the deny-path at `max_bytes=0`. All denial assertions (CAPABILITY_DENIED / path_not_allowed) used `max_bytes=1024`; all `max_bytes=0` tests used fully-authorized callers. A mutation hoisting the short-circuit above the deny checks would leak `Ok(Vec::new())` to unauthorized callers undetected.

**Closure analysis:** Three closure axes evaluated.

*Axis A — no-capability + max_bytes=0 (T-013a):* Test asserts `prepare()` returns `Err(CAPABILITY_DENIED)` when called with a caller lacking the read_prefix capability and `max_bytes=0`. Under the hoisted mutant, the short-circuit fires before the capability check and returns `Ok(Vec::new())`; T-013a catches this — confirmed MUTANT_VERIFY witness asserts `Ok(Vec::new())` on the mutant path.

*Axis B — out-of-allowlist path + max_bytes=0 (T-013b):* Test asserts `prepare()` returns `Err(CAPABILITY_DENIED)` with `reason=path_not_allowed` when called with a valid capability but a path outside `caps.path_allow` and `max_bytes=0`. Route T-013b confirmed: `caps.path_allow` only exists after the capability unwrap — the path check is a hard data dependency on the capability check. A mutation hoisting the short-circuit above the capability check still fires before both checks; T-013b catches it.

*Third permutation — path-before-capability:* Adversary evaluated whether a mutation could hoist only the path check above the capability check (leaving the short-circuit in place). Adjudication: STRUCTURALLY INEXPRESSIBLE. The path check operates on `caps.path_allow`, which is a field of the unwrapped capability struct; it cannot be evaluated before the capability unwrap. No pinning test needed for this permutation — the data dependency is a structural impossibility.

**Verification method:** Adversary inspected `prepare()` at source HEAD e6a69a4d. T-013a/b confirmed present in the unit test module. MUTANT_VERIFY witnesses (`prepare_mutant` from T-012 + `prepare_mutant_hoisted` from T-013) confirmed present, each asserting the mutant-path divergence. `cargo clippy --workspace --all-targets -- -D warnings` exit 0; `cargo fmt --check --all` exit 0; 194/194 factory-dispatcher lib tests passing (14-test unit module + prior 180 tests confirmed non-regressed).

**Status:** CLOSED-VERIFIED

---

## Part A — New Findings

*None.* Zero new findings at source HEAD e6a69a4d. Full re-derivation produced no unresolved observations.

---

## Accepted-with-Record (carried from prior passes — unchanged)

**EC-006 timeout non-enforcement — SYSTEMIC host-ABI gap (re-confirmed):** `read_prefix::register` does not enforce the timeout precondition at the wasm-boundary. Synchronous `func_wrap` closures in wasmtime cannot be preempted by epoch interruption while blocked on a host call; the `−2` timeout code in the registry preamble is a code `read_prefix` structurally never returns. Gap spans `read_file` as well and is NOT introduced by S-19.06. Invariant 2 forbids modifying `read_file.rs`. Accepted-with-record. ROUTED: architect follow-up story recommendation anchored at the S-19.06 merge gate and convergence/post-merge burst per Canonical Principle Rule 3.

**wasm-boundary out_ptr=0 sentinel parity (re-confirmed):** The `out_ptr=0` / `write_bytes_to_guest` null-guard in `read_prefix::register` replicates `read_file::register`'s pre-existing wasm-boundary pattern verbatim per Invariant 2. Non-empty boundary read untested for both; coverage posture inherited. No new risk. Accepted-with-record; no routing.

**EC-007 directory-read + T-004 Err-branch vacuous-by-construction (re-confirmed):** Directory-read (EC-007) error path not covered by unit tests. Parity with `read_file` (same gap accepted there). Story Test Plan does not require this path. T-004 Err-branch coverage is vacuous-by-construction: `fs::read` returns the only possible Err variant, covered statically by T-009g. Accepted-with-record; no routing.

**T-009a..T-009e as static regression guards, not red-gates (re-confirmed):** Tests T-009a..T-009e are static grep-based regression guards written post-implementation to lock specification properties. Not TDD red-gate tests. Honestly documented in the test suite. Accepted-with-record; no routing.

**Story v1.21 ↔ BC v1.6 consistency (re-confirmed):** All 10 body-site citations in S-19.06 v1.21 reference BC-1.17.001 v1.6. Task renumbering through v1.17..v1.21 revisions consistent with Test Plan rows and actual test names at e6a69a4d.

**Commit hygiene (re-confirmed):** 14 commits from base 1304d280 to HEAD e6a69a4d. Conventional-commit prefixes consistent (feat/test/fix); no `Co-Authored-By` AI attribution; single-purpose per commit; no unintended file-scope bleed confirmed.

**POLICY 10 — n/a pre-Step-5:** PR merge gate not yet triggered.

---

## Full Re-derivation — Coverage at Source HEAD e6a69a4d

Adversary enumerated load-bearing coverage against all ACs and invariants:

**AC-001 (read_prefix capability present → honor max_bytes):** T-001 exact-max_bytes read; T-002 read larger than max_bytes truncates; T-003 empty file at max_bytes=1024 → empty vec. Covered.

**AC-002 (capability absent → CAPABILITY_DENIED):** T-005 no-capability caller; T-013a no-capability + max_bytes=0. Covered; T-013a closes the max_bytes=0 axis per F-P4-001.

**AC-003 (output size cannot exceed max_bytes → no OUTPUT_TOO_LARGE):** T-009g static grep gate; AC-003 direct-run PASS confirmed. Covered.

**AC-004 (file absent → NOT_FOUND):** T-006 absent-file path. Covered.

**AC-005 (path not in allow-list → CAPABILITY_DENIED reason=path_not_allowed):** T-007 out-of-allowlist path at max_bytes=1024; T-013b out-of-allowlist path + max_bytes=0. Covered; T-013b closes the max_bytes=0 axis per F-P4-001.

**AC-006 (max_bytes=0 → Ok(empty) regardless of file state):** T-012 max_bytes=0 authorized caller → Ok(empty). Covered.

**AC-007 (event emission on denial):** T-008 capability-denied event byte-parity; T-010 path-denied event byte-parity. Covered. Event byte-parity correct by construction: shared `denial_event` helper + `emit_internal`; `function` field set per-variant correctly.

**BC-1.17.001 Invariants (2/3/4/5):**
- Invariant 2 (read_file.rs + path_util.rs unmodified): `git diff 1304d280..e6a69a4d -- crates/factory-dispatcher/src/host/read_file.rs crates/factory-dispatcher/src/host/path_util.rs` → empty diff. CONFIRMED.
- Invariant 3 (deny-by-default ordering — capability + path precede max_bytes=0 short-circuit): Production `prepare()` code order re-inspected at e6a69a4d. CONFIRMED.
- Invariant 4 (hook-sdk from_code mapping complete): `−1/−2/−3/−4/−5` named; `−99/unknown → Other`. CONFIRMED.
- Invariant 5 (registry loader backward-compatible): `read_prefix` schema behind serde default; comment-only preamble; no live registry entries exercise the new schema in CI fixtures. CONFIRMED.

**POLICY 20 dual-layer:** Capability gate at `prepare()` (application layer) + WASM sandbox capability-schema gate at registry load (infra layer). CONFIRMED.

**Suite self-falsifying:** 2 MUTANT_VERIFY witnesses in unit tests (`prepare_mutant` T-012, `prepare_mutant_hoisted` T-013) assert mutant-path divergence. 2 bats mutation fixtures in `read-prefix-wasm.bats` exercise fixture crate. CONFIRMED.

**Artifacts inspected at e6a69a4d:**

- S-19.06 story v1.21
- BC-1.17.001 v1.6 (all invariants, PCs, ECs)
- `crates/factory-dispatcher/src/host/read_prefix.rs` (full file: prepare + register, T-001..T-013 including T-013a/b + prepare_mutant_hoisted witness)
- `crates/factory-dispatcher/src/host/mod.rs`
- `crates/factory-dispatcher/src/registry.rs`
- `crates/hook-sdk/src/host.rs`
- `crates/hook-sdk/src/ffi.rs`
- `crates/hook-plugins/read-prefix-fixture/src/main.rs` + `Cargo.toml`
- `plugins/vsdd-factory/hooks-registry.toml` (DISTINCT Capability Schemas preamble + read_prefix schema)
- `plugins/vsdd-factory/tests/read-prefix-wasm.bats`
- `.github/workflows/ci.yml`
- `crates/factory-dispatcher/src/host/read_file.rs` (Invariant 2 sibling parity re-check)
- `crates/factory-dispatcher/src/host/path_util.rs` (Invariant 2 re-check)

## Exit Gates (at source HEAD e6a69a4d — adversary-verified)

- `cargo clippy --workspace --all-targets -- -D warnings`: **clean** (exit 0)
- `cargo fmt --check --all`: **clean** (exit 0)
- Cargo test suite (factory-dispatcher lib): **194/194 passing**
- Bats `read-prefix-wasm.bats` T-009a..T-009h: **8/8 green**
- AC-003 static gate direct-run: **PASS** (zero OUTPUT_TOO_LARGE matches in production-scoped code)

## Cascade Trajectory

| Pass | Date | Result | B | H | M | L | Streak |
|------|------|--------|---|---|---|---|--------|
| 1 | 2026-07-14 | NOT-CLEAN | 0 | 1 | 1 | 0 | 0/3 |
| 2 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 2 | 0/3 |
| 3 | 2026-07-14 | NOT-CLEAN | 1 | 0 | 0 | 0 | 0/3 |
| 4 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 1 | 0 | 0/3 |
| 5 | 2026-07-14 | **CLEAN** | 0 | 0 | 0 | 0 | **1/3** |

## Next Step

Pass-6 required (streak 1/3 → 2/3). No story version change (test-only remediation; pass-5 is test-harness closure only). 4-index at commit: BC-INDEX v4.04 / VP-INDEX v2.68 / STORY-INDEX v4.191 / ARCH-INDEX v3.01.
