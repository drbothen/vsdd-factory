# S-19.06 LOCAL Adversary Pass-8 — NOT-CLEAN

**Date:** 2026-07-14
**Story:** S-19.06 — host::read_prefix bounded partial read
**Pass:** 8 of BC-5.39.001 cascade
**Result:** NOT-CLEAN B0/H0/M0/L2
**Streak:** 0/3 UNCHANGED
**Source HEAD:** 3b2abd45
**Remediation HEAD:** (in progress — findings ROUTED to test-writer)
**Branch:** feature/S-19.06 (base 1304d280)
**Prior Pass:** pass-7 NOT-CLEAN B0/H0/M0/L2 (streak 0/3 UNCHANGED)

## Summary

Two new LOW findings (F-P8-001, F-P8-002) raised at source HEAD 3b2abd45 — the remediation HEAD produced by the pass-7 fix burst. Both are doc-accuracy only; no production code affected. Streak holds at 0/3 UNCHANGED (was already 0/3 after pass-7; no further regression).

F-P7-001 and F-P7-002 confirmed closed: the bats file header accurately reflects T-009f's presence; the fixture module doc no longer misattributes `host_stubs` test coverage to `cargo test -p factory-dispatcher -- host::read_prefix`.

F-P8-001: the `read_prefix.rs` test-module banner (stub-era framing carried forward from the Red Gate phase) enumerates 9 of 14 tests and states that all listed tests panic at `todo!()`. This is false post-implementation — T-012 and T-013 note groups are fully implemented; `todo!()` Red Gate stubs no longer exist. The banner contradicts the T-012/T-013 inline notes visible two lines below it. ROUTED: test-writer full-file comment-accuracy sweep.

F-P8-002: the bats test file's T-009f comment describes the `read-prefix-fixture` as a "no-op hook body" compiled for wasm32. The fixture is a plain `fn main()` WASI-command binary; it contains no hook macro and no hook-body logic. The "hook body" framing misrepresents the nature of the artifact. ROUTED: test-writer full-file comment-accuracy sweep.

**Remediation directive:** Both F-P8-001 and F-P8-002 are the 5th and 6th doc-accuracy findings in this cascade (F-P6-001, F-P7-001/002, F-P8-001/002), with a 7th of the same class found in the S-19.08 sibling cascade. Per L-BB-remediation-sweeps-must-cover-the-entire-crate-for-the-finding-class, the fix burst MUST perform a FULL-FILE comment-accuracy sweep of both affected files — not per-instance line fixes. Line-at-a-time remediation demonstrably leaves survivors: three consecutive passes have each found doc-accuracy survivors in the same two files.

**[Process-gap] Lesson candidate:** doc-accuracy findings MUST trigger a full-perimeter comment sweep at FIRST occurrence, not per-instance fixes. The first doc-accuracy finding appeared in pass-6 (F-P6-001); passes 7 and 8 both found survivors in the same files from the same stub-era framing class. A full-perimeter sweep at pass-6 would have closed the entire class in one burst.

---

## Part A — Prior-Pass Finding Verification

### F-P7-001 — CONFIRMED CLOSED

**Prior status:** OPEN at source HEAD abde30f3 — bats file header claimed no WASM compilation harness existed ("CANNOT be expressed" / "out of scope") while T-009f in the same file IS the passing compile/link harness.

**Closure verification:** At remediation HEAD 3b2abd45 the bats file header no longer contains the stale gap-documentation. The header accurately reflects T-009f's presence as the compile/link harness; runtime WASM execution is correctly scoped as the remaining untested axis. Five accompanying stale per-test and summary-block statements were also corrected in the full-file sweep.

**Result:** CONFIRMED CLOSED. No residual.

---

### F-P7-002 — CONFIRMED CLOSED

**Prior status:** OPEN at source HEAD abde30f3 — fixture module doc paragraph (introduced by the F-P6-001 fix) stated the `host_stubs` `−1` path "is exercised by `cargo test -p factory-dispatcher -- host::read_prefix`" — a misattribution; that command exercises `prepare()` in the dispatcher crate, not the stub.

**Closure verification:** At remediation HEAD 3b2abd45 the paragraph accurately states the stub exists solely for non-wasm compilation and is not exercised by any test. No residual misattribution remains.

**Result:** CONFIRMED CLOSED. No residual.

---

## Part A — New Findings

### F-P8-001 — LOW — [doc-accuracy; stale stub-era banner] — read_prefix.rs test-module banner enumerates 9 of 14 tests and claims all panic at todo!() Red Gate

**Severity:** LOW
**Class:** doc-accuracy
**Scope:** `crates/factory-dispatcher/src/host/read_prefix.rs` (test-module banner comment block)
**Production impact:** None. Test module comment only; the tests themselves are correctly implemented.

**Description:** At source HEAD 3b2abd45 the test-module banner in `read_prefix.rs` carries framing from the Red Gate stub phase. It enumerates 9 of the 14 tests present in the module and claims that all listed tests panic at `todo!()` as a Red Gate. This is false post-implementation: the T-012 and T-013 note groups are fully implemented with real assertions; the `todo!()` stubs no longer exist in any of the listed tests. The banner's claim directly contradicts the T-012 and T-013 inline notes visible within the same module, which describe the actual implemented behavior. Additionally, the banner enumerates only 9 of 14 tests, omitting T-009a..T-009h and the mutation-witness variants (T-013 `prepare_mutant_hoisted`), giving a false impression that the module's test inventory is smaller than it is.

**Why it matters:** A reader of the test-module header would incorrectly conclude that a significant portion of the test suite is still at the Red Gate stub stage, potentially treating implemented tests as unverified stubs or misclassifying the implementation status of the module. Under the production-grade default, comments that misrepresent implementation state are correctness defects — they corrupt the cognitive model used during adversarial review and future maintenance.

**Routing decision:** Test-writer fix-in-scope. Per the full-perimeter sweep directive, the fix burst must audit every comment in the file, not only the banner block. The banner must be rewritten to enumerate all 14 tests with accurate state descriptions; stub-era `todo!()` framing must be removed throughout.

**Status:** OPEN — ROUTED test-writer; full-file comment-accuracy sweep required.

---

### F-P8-002 — LOW — [doc-accuracy; mischaracterization] — bats T-009f comment calls the fixture a "no-op hook body"

**Severity:** LOW
**Class:** doc-accuracy
**Scope:** `plugins/vsdd-factory/tests/read-prefix-wasm.bats` (T-009f inline comment)
**Production impact:** None. Bats test comment only; T-009f itself is correct and passing.

**Description:** At source HEAD 3b2abd45 the T-009f comment in `read-prefix-wasm.bats` describes the `read-prefix-fixture` artifact as a "no-op hook body compiled for wasm32." The fixture is not a hook body. It is a plain `fn main()` WASI-command binary with no hook macro invocation and no hook-body logic — it exists as a minimal WASM compilation target to verify that the wasm32 build of the fixture crate succeeds. Calling it a "hook body" misrepresents the artifact's nature: a hook body is a WASM module that registers a hook via the hook-sdk macro; this fixture does neither. A reader relying on this comment would form a false model of what T-009f actually verifies (that a hook body compiles) versus what it truly verifies (that the wasm32 target compiles without link errors).

**Why it matters:** The fixture's role as a plain WASI-command binary (not a hook body) is directly relevant to understanding why it does not exercise the `__hook_entry` macro path and why runtime WASM execution remains the genuinely-out-of-scope axis. An incorrect "hook body" framing obscures this distinction and undermines the accuracy of the pass-7 sweep's "runtime execution out of scope" rationale.

**Routing decision:** Test-writer fix-in-scope. Per the full-perimeter sweep directive, the fix burst must audit every comment in the file. The T-009f comment must be corrected to accurately describe the fixture as a plain WASI-command binary; "hook body" framing must be removed.

**Status:** OPEN — ROUTED test-writer; full-file comment-accuracy sweep required.

---

## Accepted-with-Record (carried from prior passes)

**out_ptr=0 sentinel — read_file-verbatim parity (re-confirmed):** The `out_ptr=0` / `write_bytes_to_guest` null-guard in `read_prefix::register` replicates `read_file::register`'s pre-existing wasm-boundary pattern verbatim per Invariant 2. Non-empty boundary read untested for both; coverage posture inherited. No new risk. Accepted-with-record; no routing.

**EC-006 timeout non-enforcement — SYSTEMIC host-ABI gap (re-confirmed):** `read_prefix::register` does not enforce the timeout precondition at the wasm-boundary. Synchronous `func_wrap` closures in wasmtime cannot be preempted by epoch interruption while blocked on a host call (blocking `read_to_end`); the `−2` timeout code in the registry preamble is a code `read_prefix` structurally never returns. Gap spans `read_file` as well and is NOT introduced by S-19.06. Invariant 2 forbids modifying `read_file.rs`. Accepted-with-record. ROUTED: architect follow-up story recommendation anchored at the S-19.06 merge gate and convergence/post-merge burst per Canonical Principle Rule 3.

**Story Test Plan lag vs test inventory (re-confirmed):** Tests T-013a/b (max_bytes=0 denial axes added by remediation in pass-4) and 2 MUTANT_VERIFY witnesses (`prepare_mutant_hoisted` T-013) plus T-011 (hook-sdk from_code mapping) appear in the test suite but are not listed in the story Test Plan section. Accepted per cascade-remediation convention — cascade-driven tests added mid-stream need not block the cascade; optional story-writer sweep non-blocking. Accepted-with-record.

**FFI adversarial-input surface swept clean (re-confirmed):** Adversary re-examined the wasm-boundary FFI surface for adversarial input handling: NUL byte in path → `DeniedResolutionFailed` (CString conversion check fires before host dispatch); non-UTF-8 path bytes → `INVALID_ARGUMENT −4` (from_utf8 Err branch); u32::MAX for max_bytes → `with_capacity` uses `min(max_bytes, actual_len)` bound + `take()` enforced at read; empty path string → directory stat returns `INTERNAL_ERROR` or EC-001 short-circuit (path normalization rejects empty). No adversarial input produces undefined behavior or a bypass of the deny-by-default ordering. Accepted-with-record; no routing.

**EC-007 directory-read + T-004 Err-branch vacuous-by-construction (re-confirmed):** Directory-read (EC-007) error path not covered by unit tests. Parity with `read_file` (same gap accepted there). Story Test Plan does not require this path. T-004 Err-branch coverage is vacuous-by-construction: `fs::read` returns the only possible Err variant, covered statically by T-009g. Accepted-with-record; no routing.

**T-009a..T-009e as static regression guards, not red-gates (re-confirmed):** Tests T-009a..T-009e are static grep-based regression guards written post-implementation to lock specification properties. Not TDD red-gate tests. Honestly documented in the test suite. Accepted-with-record; no routing.

**Story v1.21 ↔ BC v1.6 consistency (re-confirmed):** All 10 body-site citations in S-19.06 v1.21 reference BC-1.17.001 v1.6. Task renumbering through v1.17..v1.21 revisions consistent with Test Plan rows and actual test names at source HEAD 3b2abd45.

**Commit hygiene (re-confirmed):** 16 commits from base 1304d280 to source HEAD 3b2abd45. Conventional-commit prefixes consistent (feat/test/fix/docs); no `Co-Authored-By` AI attribution; single-purpose per commit; no unintended file-scope bleed confirmed.

**POLICY 10 — n/a pre-Step-5:** PR merge gate not yet triggered.

**Invariant 4 chain naming (adjudicated NOT a defect):** `check_path_allowed` calls `resolve_path_for_allowlist` internally. Module doc naming was reviewed; naming is internally consistent and not a doc-accuracy defect. Accepted-with-record; no routing.

---

## Pass-8 Fresh Positive Coverage (at source HEAD 3b2abd45)

The following gates were freshly re-verified in pass-8:

**Full diff scope:** 14 files examined across the full diff from base 1304d280 to source HEAD 3b2abd45. No files excluded from review.

**Unit test live-run:** 14/14 unit tests passing (factory-dispatcher lib target). No regressions from pass-7.

**Bundle orphan check:** 6/6 hook plugin entries in `hooks-registry.toml` resolve to present WASM artifacts. No orphan entries.

**Bats `read-prefix-wasm.bats`:** 8/8 tests green. T-009f (compile/link harness) confirmed present and passing.

**Ordering invariants — mutation witnesses:** Both mutation witnesses (`prepare_mutant_hoisted` T-013 variants) confirm the deny-by-default ordering is load-bearing. Invariants confirmed.

**hooks-registry preamble:** Line-by-line review of the `read_prefix` entry in `hooks-registry.toml`. All fields (plugin name, WASM path, trigger events, tier, on-error behavior) accurate against implementation. No stale or aspirational preamble claims found.

**Workflow POLICY 20 comments:** Workflow file POLICY 20 annotations reviewed. Comments accurately reflect current implementation behavior; no mischaracterizations found.

**Coupled-count gate:** Confirmed via live invocation that the `read_prefix` plugin coupled-count gate fires correctly on boundary conditions. Live run output consistent with documented behavior.

---

## Exit Gates (at source HEAD 3b2abd45 — status at pass-8 close)

- `cargo clippy --workspace --all-targets -- -D warnings`: **clean** (exit 0)
- `cargo fmt --check --all`: **clean** (exit 0)
- Bats `read-prefix-wasm.bats` T-009a..T-009h: **8/8 green**
- Full diff (14 files): **reviewed — no production correctness defects found**
- F-P7-001: **CONFIRMED CLOSED** (3b2abd45)
- F-P7-002: **CONFIRMED CLOSED** (3b2abd45)
- F-P8-001: **OPEN** — ROUTED test-writer; full-file comment-accuracy sweep required
- F-P8-002: **OPEN** — ROUTED test-writer; full-file comment-accuracy sweep required
- 4-index at commit: BC-INDEX v4.04 / VP-INDEX v2.68 / STORY-INDEX v4.191 / ARCH-INDEX v3.01

---

## Cascade Trajectory

| Pass | Date | Result | B | H | M | L | Streak |
|------|------|--------|---|---|---|---|--------|
| 1 | 2026-07-14 | NOT-CLEAN | 0 | 1 | 1 | 0 | 0/3 |
| 2 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 2 | 0/3 |
| 3 | 2026-07-14 | NOT-CLEAN | 1 | 0 | 0 | 0 | 0/3 |
| 4 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 1 | 0 | 0/3 |
| 5 | 2026-07-14 | **CLEAN** | 0 | 0 | 0 | 0 | **1/3** |
| 6 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 1 | 0/3 RESET |
| 7 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 2 | 0/3 UNCHANGED |
| 8 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 2 | 0/3 UNCHANGED |

## Next Step

F-P8-001 and F-P8-002 are OPEN and ROUTED to test-writer for full-file comment-accuracy sweep of both `crates/factory-dispatcher/src/host/read_prefix.rs` and `plugins/vsdd-factory/tests/read-prefix-wasm.bats`. Remediation directive: full-perimeter sweep of both files for all doc-accuracy defects of the stub-era and mischaracterization classes — per-instance fixes are insufficient given three consecutive passes finding survivors in these files. Pass-9 commences from the remediation HEAD produced by the test-writer fix burst. No story version change. 4-index at commit: BC-INDEX v4.04 / VP-INDEX v2.68 / STORY-INDEX v4.191 / ARCH-INDEX v3.01.
