# S-19.04 LOCAL Adversary Pass-1 — NOT-CLEAN

**Date:** 2026-07-13  
**Story:** S-19.04 — Registry/bundle hygiene: orphan WASM removal + tool-filter regex anchoring convention + lint check  
**Pass:** 1 of BC-5.39.001 cascade  
**Result:** NOT-CLEAN B0/H2/M2/L3 (F-1 classified HIGH-security per dispatch)  
**Streak:** 0/3  
**Source HEAD:** TBD (implementer fix HEAD = 2c6bd1ca post-fix)  
**Fix chain:** architect ruling + test-writer 298389b0 + implementer 2c6bd1ca. All 8 findings CLOSED same-session.

## Summary

Pass-1 revealed MultiEdit guard regression (security-class finding), real-bundle gate gaps (T-009/T-010 missing), preamble inversion in story, and 3 observation-level items. All closed same-session via 3-leg fix chain. Streak 0/3 — pass-2 required.

## Part A — Findings

### F-1 — HIGH-security — MultiEdit guard regression (CLOSED same-session)

**POLICY:** Security class — tool-filter guard omitted MultiEdit from anchored regex  
**Issue:** S-19.04 implementer produced tool-filter regex `^(Edit|Write|Agent)$` omitting `MultiEdit`, creating a regression: MultiEdit tool calls would bypass the guard. BC-4.13.001 Invariant 5 requires `^(Edit|Write|MultiEdit|Agent)$`.  
**Fix:** Architect ruling confirmed ^(Edit|Write|MultiEdit|Agent)$ form. Implementer 2c6bd1ca corrected to 39 entries with anchored ^(Edit|Write|MultiEdit|Agent)$ form; 3 orphan WASMs deleted; stage_release_bundle implemented.  
**Status:** CLOSED same-session — implementer 2c6bd1ca  
**Routing:** architect adjudication + implementer  

### F-2 — HIGH — Real-bundle gate T-009 missing (CLOSED same-session)

**Issue:** AC-004 real-bundle gate T-009 was absent from test file. Gate required to verify the ^(Edit|Write|MultiEdit|Agent)$ pattern is present in the actual release bundle registry.  
**Fix:** test-writer 298389b0 added T-009 row.  
**Status:** CLOSED same-session — test-writer 298389b0  
**Routing:** test-writer  

### F-3 — MEDIUM — AC-004/AC-005 preamble inversion (CLOSED same-session)

**Issue:** AC-004 gate preamble described incorrect invocation order; AC-005 preamble had inverted precondition logic.  
**Fix:** Implementer 2c6bd1ca corrected preamble ordering.  
**Status:** CLOSED same-session — implementer 2c6bd1ca  
**Routing:** implementer  

### F-4 — MEDIUM — Real-bundle gate T-010 missing (CLOSED same-session)

**Issue:** T-010 row for AC-005 negative-control (verify no unanchored patterns remain) was absent.  
**Fix:** test-writer 298389b0 added T-010 row.  
**Status:** CLOSED same-session — test-writer 298389b0  
**Routing:** test-writer  

### O-1 — LOW — Observation (accepted-with-record)

Task-13 count field did not reflect count=5 distinct tool-values. Accepted-with-record; corrected in S-19.04 v1.14.

### O-2 — LOW — Observation (accepted-with-record)

3 orphan WASM delete rows missing from story File-Structure. Accepted-with-record; corrected in S-19.04 v1.14.

### O-3 — LOW — Observation (accepted-with-record)

stage_release_bundle implementation not surfaced in story task list. Accepted-with-record; implementer 2c6bd1ca implemented.

## Fix Chain

| Leg | Agent | SHA | Change |
|-----|-------|-----|--------|
| Architect ruling | architect | (D-a ruling) | `^(Edit\|Write\|MultiEdit\|Agent)$` confirmed canonical; `^Agent$` for BC-7.03.079 Invariant 1 |
| test-writer | test-writer | 298389b0 | T-009 + T-010 rows added |
| implementer | implementer | 2c6bd1ca | 39 entries `^(Edit\|Write\|MultiEdit\|Agent)$`; 3 orphan WASMs deleted; stage_release_bundle implemented |

## Cascade Trajectory

pass-1 NOT-CLEAN(0B+2H+2M+3L) [streak 0/3]

## Next Step

Pass-2 required. Source HEAD: 2c6bd1ca (post-fix). All 8 findings CLOSED same-session.
