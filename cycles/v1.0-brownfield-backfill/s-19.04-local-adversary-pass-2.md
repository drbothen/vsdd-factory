# S-19.04 LOCAL Adversary Pass-2 — NOT-CLEAN

**Date:** 2026-07-13
**Story:** S-19.04 — Registry/bundle hygiene: orphan WASM removal + tool-filter regex anchoring convention + lint check
**Pass:** 2 of BC-5.39.001 cascade
**Result:** NOT-CLEAN B0/H0/M2/L0
**Streak:** 0/3 (unchanged — prior streak 0/3 from pass-1 NOT-CLEAN)
**Source HEAD:** 2c6bd1ca
**Prior Pass:** pass-1 NOT-CLEAN B0/H2/M2/L3 (all 8 findings CLOSED same-session)

## Summary

2 MEDIUM findings. F-NEW-2 (T-009 hermeticity gap: git ls-files required for bundle membership test) and F-NEW-3 (ci.yml contamination: underscore-glob staging pattern). Both CLOSED same-session by implementer commits 2bd3c898 + d9502701. Streak 0/3 — pass-3 required.

## Part A — Findings

### F-NEW-2 — MEDIUM — T-009 hermeticity gap: git ls-files required (CLOSED same-session)

**POLICY:** AC-004 real-bundle gate (S-7.02 exhaustive verification)
**Location:** S-19.04 AC-004 gate, T-009 test row
**Issue:** T-009 real-bundle membership test was written to check for file presence in the artifact directory but did not invoke `git ls-files` to verify the file is actually tracked in the release bundle. A file present on disk but not git-tracked would pass T-009 vacuously. The correct hermeticity check requires: `git ls-files --error-unmatch <path>` exits 0 for bundle members.
**Fix:** Implementer commit 2bd3c898 rewrote T-009 to use `git ls-files` for bundle membership verification.
**Status:** CLOSED same-session — implementer 2bd3c898
**Routing:** implementer

### F-NEW-3 — MEDIUM — ci.yml contamination: underscore-glob staging pattern (CLOSED same-session)

**POLICY:** POLICY 20 (release_bundle_no_dev_samples) — bundle staging must not include underscore-prefixed dev artifacts
**Location:** release.yml staging step
**Issue:** The underscore-glob staging step `*_*.wasm` in release.yml inadvertently staged underscore-variant WASM files (e.g., `vsdd_context_resolvers.wasm`) alongside release bundle artifacts. This contaminated the release bundle with orphan dev artifacts. The case-arm guard `*_*) echo "skip..."` was intended as defense-in-depth but was in the wrong position relative to the staging step.
**Fix:** Implementer commit d9502701 corrected the staging step ordering: orphan-guard case-arm moved BEFORE the staging glob, ensuring underscore-variants are skipped before any staging occurs.
**Status:** CLOSED same-session — implementer d9502701
**Routing:** implementer

## Fix Chain

| Leg | Agent | SHA | Change |
|-----|-------|-----|--------|
| implementer | implementer | 2bd3c898 | T-009 hermeticity: git ls-files bundle membership check |
| implementer | implementer | d9502701 | ci.yml underscore-glob staging: orphan-guard moved before staging glob |

## Cascade Trajectory

pass-1 NOT-CLEAN(0B+2H+2M+3L) [streak 0/3] → pass-2 NOT-CLEAN(0B+0H+2M+0L) [streak 0/3]

## Next Step

Pass-3 required. Source HEAD: d9502701 (post-fix). Both findings CLOSED same-session.
