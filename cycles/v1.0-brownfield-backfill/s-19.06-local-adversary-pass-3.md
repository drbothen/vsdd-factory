# S-19.06 LOCAL Adversary Pass-3 — NOT-CLEAN

**Date:** 2026-07-14
**Story:** S-19.06 — host::read_prefix bounded partial read
**Pass:** 3 of BC-5.39.001 cascade
**Result:** NOT-CLEAN B1/H0/M0/L0
**Streak:** 0/3 (unchanged — prior streak 0/3 from pass-2 NOT-CLEAN)
**Source HEAD:** babce0be
**Branch:** feature/S-19.06 (base 1304d280)
**Prior Pass:** pass-2 NOT-CLEAN B0/H0/M0/L2 (all 2 findings CLOSED same-session: story-writer c63bd39a + test-writer babce0be)
**Remediation HEAD:** 3ac997b8

## Summary

Pass-2 findings F-P2-001 (LOW) and F-P2-002 (LOW) both CLOSED-VERIFIED at source HEAD babce0be. 1 new BLOCKER finding: F-P3-001 fix-introduced CI clippy failure — `prepare_mutant` helper added by test-writer babce0be in T-012 used `ok_or_else(|| codes::CAPABILITY_DENIED)` with a bare-constant closure, triggering clippy::unnecessary_lazy_evaluations. `cargo clippy --workspace --all-targets -- -D warnings` exited 101 on this pattern; CI-red; unmergeable. Adversary reproduced the exact gate. CLOSED same session: test-writer 3ac997b8 (changed to `ok_or(codes::CAPABILITY_DENIED)`; rustfmt consolidations; TD-VSDD-060 sweep confirmed the sole remaining `ok_or_else` at production `prepare()` is legitimate — the closure captures an `emit_denial` side-effect and is not a bare-constant). Orchestrator full-gate verified at 3ac997b8: clippy clean, fmt clean, workspace 2042/2042 green, bats 8/8.

Process observation: orchestrator post-remediation verification after babce0be ran unit tests + bats but did NOT run `cargo clippy`. The fix-introduced clippy failure survived one verification round because the exit-gate subset used was narrower than the full CI gate. Lesson candidate: remediation commits MUST re-run the FULL exit gate (clippy + fmt + workspace cargo test), not only the subset that appears relevant to the change scope.

Positive coverage: full AC-001..AC-007 re-derivation correct; numeric/boundary sweep clean (u32/usize lossless on wasm32; `Vec::with_capacity` min-capped — no huge-max_bytes DoS vector); `ReadPrefixCaps` serde `deny_unknown_fields` matches preamble schema; read_file.rs/path_util.rs byte-unmodified re-confirmed.

Accepted-with-record: EC-006 timeout non-enforcement is a SYSTEMIC host-ABI gap spanning `read_file` (synchronous `func_wrap` closures; epoch interruption cannot preempt blocked host calls; registry preamble documents a -2 code `read_prefix` never returns) — NOT S-19.06-introduced; Invariant 2 forbids touching read_file; ROUTED to architect as follow-up story recommendation, to be surfaced at the S-19.06 merge gate and anchored at the convergence/post-merge burst per Canonical Principle Rule 3. Also carried from prior passes: out_ptr=0 parity; EC-007 untested-parity.

Streak: 0/3. Remediation landed same session. Pass-4 required.

## Part A — Prior-Pass Finding Verification

### F-P2-001 Closure Verification — LOW — [spec-impl ordering divergence] §Architecture Mapping steps out of order: CLOSED-VERIFIED

**Prior finding:** S-19.06 story v1.20 §Architecture Mapping listed existence-check before max_bytes=0 short-circuit; implementation short-circuits first; EC-001 composite case was underspecified.
**Verification method:** Adversary read story v1.21 at source HEAD babce0be. §Architecture Mapping now leads with the max_bytes=0 short-circuit as step 1 (returns `Ok(Vec::new())` immediately; no file read and no path check). EC-001 composite case is pinned: "max_bytes=0 + file absent → empty return, no path check." T-012 in `read_prefix.rs` unit test module confirms: `prepare_mutant` (reorder witness) genuinely returns `NOT_FOUND` + 1 `file_not_found` count when existence-check precedes short-circuit; real `prepare()` returns `Ok(Vec::new())` with zero file-not-found. Story v1.21 byte-consistent with implementation execution order.
**Status:** CLOSED-VERIFIED

---

### F-P2-002 Closure Verification — LOW — [test-comment overstatement + real gap] T-009h count-COUPLING absent; stale inline line references: CLOSED-VERIFIED-with-recorded-edge

**Prior finding:** T-009h comment overstated new-build coverage guarantee; exact-count could not structurally couple to wasm32-wasip1 build step count; stale inline line-number citations violated TD-VSDD-091.
**Verification method:** Adversary independently re-derived anchor counts at source HEAD babce0be. Exclusion-anchor count equals `wasm32-wasip1 --workspace` build count per file via adjacency-anchor awk (layer-1 coupling). Stale line-number citations confirmed dropped. Dual-direction mutation fixtures present: deletion of an exclusion anchor causes diverge to fire; addition of a synthetic new build step without a corresponding exclusion causes diverge to fire. Both mutation directions confirmed live. Staging-loop skip counts kept in exact-count form per adjudication.
**Recorded edge:** Layer-1 awk adjacency anchor produces a false-PASS on non-canonically-formatted future builds where `--workspace` appears inline on the same command line rather than as a standalone flag. This edge is backstopped by layer-2 exact-count staging skips + T-011 (Rust reimplementation of exclusion logic), so the artifact-shipping outcome remains protected even if the layer-1 count miscounts.
**Status:** CLOSED-VERIFIED-with-recorded-edge

---

## Part A — New Findings

### F-P3-001 — BLOCKER — [fix-introduced; CI clippy] T-012 prepare_mutant uses ok_or_else with bare-constant closure

**Location:** `crates/factory-dispatcher/src/host/read_prefix.rs` — `prepare_mutant` helper, T-012 test module (added by test-writer babce0be)
**Observation:** The `prepare_mutant` helper function added by babce0be uses `ok_or_else(|| codes::CAPABILITY_DENIED)` — a closure that returns a bare constant. Clippy lint `unnecessary_lazy_evaluations` fires on this pattern: when the error value is a constant expression (not a closure with side-effects), `ok_or` should be used directly instead of `ok_or_else`. The lint is enabled under `clippy::all` and promoted to an error under `-D warnings`. `cargo clippy --workspace --all-targets -- -D warnings` exits with code 101 on the babce0be tree; CI fails; the branch is unmergeable.
**Adversary reproduction:** Gate reproduced verbatim — `cargo clippy --workspace --all-targets -- -D warnings` against babce0be tree exits 101 with `error: unnecessary closure used with ok_or_else` at the `prepare_mutant` call site.
**Why BLOCKER:** CI-red gate violation. The branch cannot pass the `develop` merge gate until this is resolved. A clippy failure under `-D warnings` is a hard CI block per `.github/workflows/ci.yml`.
**TD-VSDD-060 sweep:** Test-writer 3ac997b8 confirmed the sole remaining `ok_or_else` in `read_prefix.rs` is at the production `prepare()` function, where the closure captures an `emit_denial` call — a side-effectful operation, not a bare-constant — making `ok_or_else` the correct form. The sweep found no additional bare-constant `ok_or_else` sites.
**Fix required:** Replace `ok_or_else(|| codes::CAPABILITY_DENIED)` with `ok_or(codes::CAPABILITY_DENIED)` in `prepare_mutant`; run rustfmt consolidations; re-verify full exit gate.
**Routing:** test-writer
**Status:** CLOSED — test-writer 3ac997b8 (`ok_or(codes::CAPABILITY_DENIED)` substitution + rustfmt consolidations; TD-VSDD-060 sweep confirmed sole remaining `ok_or_else` at production `prepare()` is legitimate; orchestrator full-gate verified: clippy clean, fmt clean, workspace 2042/2042 green, bats 8/8)

---

## Accepted-with-Record

**EC-006 timeout non-enforcement — SYSTEMIC host-ABI gap (ROUTED):** `read_prefix::register` does not enforce the timeout precondition at the wasm-boundary. This is a SYSTEMIC host-ABI gap: synchronous `func_wrap` closures in wasmtime cannot be preempted by epoch interruption while blocked on a host call; the `-2` timeout code documented in the registry preamble is a code `read_prefix` structurally never returns. This gap spans `read_file` as well and is NOT introduced by S-19.06. Invariant 2 forbids modifying `read_file.rs`. Accepted-with-record. ROUTED: architect follow-up story recommendation to be surfaced at the S-19.06 merge gate and anchored at the convergence/post-merge burst per Canonical Principle Rule 3.

**wasm-boundary out_ptr=0 byte-transfer pattern (re-confirmed):** The `out_ptr=0` / `write_bytes_to_guest` null-guard pattern in `read_prefix::register` replicates `read_file::register`'s pre-existing wasm-boundary pattern verbatim per Invariant 2 (byte-unmodified replication). Non-empty boundary read is untested for both `read_prefix` and `read_file`; coverage posture inherited. No new risk introduced. Accepted-with-record; no routing.

**EC-007 directory-read untested (re-confirmed):** Directory-read (EC-007) error path is not covered by unit tests. Parity with `read_file` (same gap accepted there). Story Test Plan does not require this path. Accepted-with-record; no routing.

**T-009a..T-009e as static regression guards, not red-gates (re-confirmed):** Tests T-009a..T-009e are static grep-based regression guards written post-implementation to lock specification properties. They are not TDD red-gate tests. Honestly documented in the test suite. Accepted-with-record; no routing.

## Coverage

Adversary examined the following artifacts at source HEAD babce0be:

- S-19.06 story v1.21 (all ACs AC-001..AC-007, §Architecture Mapping 8-step execution order, gate forms, task list, Architecture Compliance Rules; per-AC re-derivation: AC-001 open-return OK vec, AC-002 max_bytes cap, AC-003 OUTPUT_TOO_LARGE unreachable, AC-004 path allow-list, AC-005 POLICY 20 presence gate, AC-006 registry preamble DISTINCT, AC-007 T-012 EC-001 composite regression)
- BC-1.17.001 v1.6 (all invariants, preconditions, acceptance criteria — including EC-001 "The file is not read"; Invariant 2 byte-unmodified; Invariant 3 no-fallback)
- `crates/factory-dispatcher/src/host/read_prefix.rs` (full file: prepare + register implementations, unit test module T-001..T-012 including T-012 regression pin and `prepare_mutant` mutation witness at babce0be)
- `crates/factory-dispatcher/src/host/mod.rs` (read_prefix module declaration + register call in setup_linker)
- `crates/factory-dispatcher/src/registry.rs` (ReadPrefixCaps struct + Capabilities.read_prefix field; serde deny_unknown_fields confirmed)
- `crates/hook-sdk/src/host.rs` (read_prefix safe wrapper)
- `crates/hook-sdk/src/ffi.rs` (6-param extern + host_stubs block; wasm32 and non-wasm32 cfg branches)
- `crates/hook-plugins/read-prefix-fixture/src/main.rs` + `Cargo.toml` (fixture crate)
- `plugins/vsdd-factory/hooks-registry.toml` (DISTINCT Capability Schemas preamble block + read_prefix schema; registry preamble -2 timeout code note confirmed)
- `plugins/vsdd-factory/tests/read-prefix-wasm.bats` (T-009a..T-009h at source HEAD babce0be; count-COUPLING adjacency awk + dual-mutation fixtures added by pass-2 remediation)
- `.github/workflows/release.yml` (exclusion line + bundle orphan gate; post devops 47a81d4e)
- `.github/workflows/ci.yml` (all wasm build steps + all staging loops; post devops be91d5b1 + 47a81d4e)
- `crates/factory-dispatcher/src/host/read_file.rs` (sibling parity reference; Invariant 2 byte-unmodified re-check; EC-006 systemic-gap reference)
- `crates/factory-dispatcher/src/host/path_util.rs` (sibling parity reference; Invariant 2 re-check)

Shell gap closed by adversary: `git diff --name-only 29df1cba..babce0be` = test files only (`crates/factory-dispatcher/src/host/read_prefix.rs` test module, `plugins/vsdd-factory/tests/read-prefix-wasm.bats`); zero unintended scope bleed. Numeric/boundary sweep: u32/usize lossless on wasm32 (max u32 fits usize on 32-bit and 64-bit targets); `Vec::with_capacity` min-capped at 0 — no huge-max_bytes DoS vector.

## Exit Gates (at source HEAD babce0be)

- Cargo test suite (workspace unit): **passing** (T-001..T-012 green; T-012 regression pin + `prepare_mutant` witness green per test-writer babce0be verification)
- `cargo clippy --workspace --all-targets -- -D warnings`: **FAIL exit 101** — F-P3-001: `prepare_mutant` `ok_or_else(|| codes::CAPABILITY_DENIED)` triggers clippy::unnecessary_lazy_evaluations; CI-red; unmergeable
- `cargo fmt --check --all`: **clean**
- Bats T-009a..T-009h: **8/8 green**

## Exit Gates (at remediation HEAD 3ac997b8 — orchestrator-verified)

- Cargo test suite (workspace): **2042/2042 passing**
- `cargo clippy --workspace --all-targets -- -D warnings`: **clean**
- `cargo fmt --check --all`: **clean**
- Bats T-009a..T-009h: **8/8 green**
- TD-VSDD-060 sweep: **confirmed** — sole remaining `ok_or_else` in `read_prefix.rs` at production `prepare()` is legitimate (emit_denial side-effect)

## Cascade Trajectory

| Pass | Date | Result | B | H | M | L | Streak |
|------|------|--------|---|---|---|---|--------|
| 1 | 2026-07-14 | NOT-CLEAN | 0 | 1 | 1 | 0 | 0/3 |
| 2 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 2 | 0/3 |
| 3 | 2026-07-14 | NOT-CLEAN | 1 | 0 | 0 | 0 | 0/3 |

## Next Step

Pass-4 required (streak 0/3). Remediation landed same session: test-writer 3ac997b8 (feature/S-19.06, `ok_or` substitution + rustfmt consolidations; full-gate clean). Source HEAD for pass-4: 3ac997b8.
