# S-19.09 LOCAL Adversary Pass-7 — CLEAN

**Date:** 2026-07-15
**Story:** S-19.09 — post-E-19 host ABI fixes: read_prefix production path registration, timeout_ms framing, telemetry hygiene
**Pass:** 7 of BC-5.39.001 cascade
**Result:** CLEAN B0/H0/M0/L0
**Streak:** 0/3 → 1/3
**Source HEAD reviewed:** 0e466d79
**Branch:** feature/S-19.09
**Story artifact reviewed at:** v1.3 (no version change; test-only remediation at HEAD)

## Summary

Pass-7 reviewed story artifact at v1.3 with source HEAD 0e466d79. F-P6-001 confirmed CLOSED-VERIFIED: T-015 independently re-derived at HEAD — real 0-byte file, production linker, four assertions covering the D19 empty-body branch. Mutation reasoning independently validated: branch removal gives `grow(0) + write_offset = current_bytes = 131072`, Assert 2 (`assert_eq!(current_bytes, body_buf.len())`) flips. NOT a paper-fix. Note: Assert 4 alone would NOT catch this mutation — Assert 2 is the load-bearing kill.

ZERO new findings. Six finding classes swept dry: (1) doc-accuracy; (2) const/sibling-sweep (crate-wide, all residue test-scoped); (3) harness-liveness; (4) spec-conformance; (5) TD-VSDD-091 general-pattern; (6) enumerated-EC coverage — EC-001→T-003, EC-002→T-015, EC-003→AC-010, EC-004→AC-005+T-002, EC-005→T-009/T-010/T-012, EC-006→T-013 — all six enumerated edge cases mapped to concrete tests.

Adversary ran full test suite directly: crate all-targets 204+ green; full workspace `cargo test` green; clippy/fmt clean; hygiene bats 9/9; AC-004/AC-006/AC-007/AC-008 static gates re-executed with captured stdout.

Seventh-perspective observation: AC-008 awk-scoping soundness independently proven — each of the five target files contains exactly one column-0 `#[cfg(test)]` marker, with only the test module continuing to EOF. No path exists for the awk `end` block to double-count a non-test-scoped struct.

Accepted-with-record items carried forward: Token Budget `(NNNN lines)` entries adjudicated as documentary estimates (not TD-VSDD-091 volatile pins); T-002b + T-011 5-file perimeter stricter-than-spec (additive, no violation); test-scoped string literals exempt per standing adjudication; two pre-existing out-of-scope "unused variable" warnings in crates outside factory-dispatcher noted and confirmed not attributable to S-19.09.

Cascade result: CLEAN. Streak advances to 1/3. Pass-8 required for 2/3.

Cascade Trajectory: P1 M1/L1 → P2 M1/L1 → P3 L1 → P4 L1 → P5 L1 → P6 L1 → P7 CLEAN 1/3.

Next: pass-8 (fresh-context). Streak 1/3 — two consecutive CLEAN passes required for convergence.

## Part A — Findings

No findings. CLEAN.

---

## Closed Findings

### F-P6-001 — LOW — CLOSED-VERIFIED at HEAD 0e466d79

**Prior status:** OPEN at pass-6; routed test-writer for T-015
**Closure evidence:** T-015 added at HEAD 0e466d79 exercises the D19 empty-body branch of `prepare()` in `invoke.rs`. Independent adversary re-derivation confirms: test uses a real 0-byte file (not a mock), exercises the production linker path, and asserts four invariants: (1) OK return, (2) `assert_eq!(current_bytes, body_buf.len())` — the no-grow assertion, (3) `write_offset == 0` post-call, (4) `body_buf` unchanged. Mutation reasoning: removing the `if ptr.is_null() || len == 0 { return Ok(()); }` early-return guard causes `grow(0)` execution followed by `write_offset` advancing to `current_bytes = 131072`; Assert 2 catches this — `131072 != 0`. Assert 4 (`body_buf` unchanged) would NOT independently catch the mutation since `grow(0)` leaves the buffer content intact. Assert 2 is the load-bearing kill. NOT a paper-fix: a code mutant that removes the guard flips Assert 2. Closure is load-bearing.
**Status:** CLOSED-VERIFIED

### F-P5-001 — LOW — CLOSED-VERIFIED at story v1.3 (commit 26e9f677)

Carried from pass-6: see pass-6 closed findings.

### F-P4-001 — LOW — CLOSED-VERIFIED at story v1.2 (commit 00fd8a70)

Carried from pass-6: see pass-6 closed findings.

---

## Accepted-with-Record

**Sink-crate and tests/*.rs bare string literals (pass-7 re-confirmation):** Standing adjudication maintained. `PLUGIN_COMPLETED` and `PLUGIN_TIMEOUT` consts defined in `crates/factory-dispatcher/src/internal_log.rs`; bare string literals in the sink crate and `plugins/vsdd-factory/tests/*.rs` are cross-crate routing keys and test fixtures. TD-VSDD-060 does not require replacement.

**Epoch-deadline harness-only (pass-7 re-confirmation):** `timeout_ms_to_epochs(5000)` alignment (740f01d8) is in test harness code only. Production dispatch always invokes the same helper with the configured `timeout_ms` value; no production masking present.

**Token Budget `(NNNN lines)` entries (pass-7 re-confirmation):** Documentary line-count approximations for human readers. Not TD-VSDD-091-class volatile pins. Entries are slightly stale post-T-015 addition but adjudicated as documentary estimates; no behavioral assertion depends on them. Accepted-with-record standing maintained.

**T-002b + T-011 5-file perimeter stricter-than-spec (pass-7 adjudication):** T-002b and T-011 enforce a 5-file membership constraint on the hygiene test perimeter. Story v1.3 AC-002/AC-011 do not enumerate files; the 5-file perimeter is stricter-than-spec (additive), not looser. No violation; no story amendment required.

**Pre-existing out-of-scope "unused variable" warnings (pass-7 observation):** Two `#[allow(unused_variables)]` suppressions or warnings observed in crates outside factory-dispatcher. Confirmed pre-existing and not attributable to S-19.09 changes at HEAD 0e466d79. Not a S-19.09 defect.

**Code deliverable production-grade — fifth consecutive pass (passes 3, 4, 5, 6, 7):** Passes 3 through 7 each independently attest that the code deliverable at HEAD is production-grade. All 10 ACs of story v1.3 conform to the implementation. Pass-7 is CLEAN with zero findings.

---

## Coverage

Adversary examined the following artifacts at review state:

- S-19.09 story v1.3 (whole-artifact integrity: changelog monotonic v1.0→v1.1→v1.2→v1.3; BC↔AC parity confirmed; no version change at pass-7)
- BC-1.17.001 v1.7 (active; Inv3 no-fallback, Inv4 shared-path algorithm, PC-2 head_c clamp, PC-3 take() semantics)
- BC-3.08.001 v1.23 (AC-010 timestamp field; Event 6 plugin.completed async path)
- ADR-025 v1.16 §Decisions 16–22 (D19 production-path registration; D20 doc comments; D21 named constants; D22 timestamp persistence)
- `crates/factory-dispatcher/src/invoke.rs` at HEAD 0e466d79 (D19 production-path registration block; T-015 new test; F-P6-001 closure confirmed)
- `crates/factory-dispatcher/src/host/read_file.rs` (D20 corrected drop comment; drop line at 41; F-P5-001 confirmed stable)
- `crates/factory-dispatcher/src/host/read_prefix.rs` (D20 corrected drop comment; drop line at 56; F-P5-001 confirmed stable)
- `crates/factory-dispatcher/src/internal_log.rs` (const definitions + value-pin tests; const-clean)
- `crates/factory-dispatcher/src/executor.rs` (D20 epoch-interruption grep: 0 matches; D21 named-constant site confirmed)
- `plugins/vsdd-factory/tests/host-abi-hygiene.bats` (T-004..T-012; 9/9 green)

Crate test suite: `cargo test -p factory-dispatcher --all-targets` — 204+ passing at HEAD 0e466d79.
Full workspace: `cargo test --workspace --all-targets` — green.
AC-004/AC-006/AC-007/AC-008 static gates: all PASS with captured stdout.
Clippy: `cargo clippy --workspace --all-targets -- -D warnings` — clean.
Fmt: `cargo fmt --check --all` — clean.
Bats hygiene: 9/9 green.

## Exit Gates (at HEAD 0e466d79, story v1.3)

- Cargo unit test suite (factory-dispatcher all-targets): **204+ passing**
- Full workspace `cargo test --workspace --all-targets`: **green**
- `cargo clippy --workspace --all-targets -- -D warnings`: **clean**
- `cargo fmt --check --all`: **clean**
- Bats T-004..T-012: **9/9 green**
- T-002 byte-equality assertion (AC-002 mandate): **confirmed**
- T-015 empty-body no-grow assertion (F-P6-001 closure): **CLOSED-VERIFIED**
- T-009/T-010 assert_eq! value-pins in internal_log.rs cfg(test) (AC-006/AC-007 mandate): **confirmed**
- T-013 timestamp-persistence load-bearing: **confirmed**
- D22 timestamp persistence: **confirmed**
- D19 base-0/HEAD-42 grep gate: **confirmed**
- F-P5-001 general-pattern sweep (body lines 60–324, zero file:line citations): **CLOSED-VERIFIED**
- F-P6-001 empty-body branch coverage (T-015 with mutation evidence): **CLOSED-VERIFIED**
- Enumerated EC coverage (EC-001..EC-006 all mapped): **complete**

## Cascade Trajectory

| Pass | Date | Result | B | H | M | L | Streak |
|------|------|--------|---|---|---|---|--------|
| 1 | 2026-07-15 | NOT-CLEAN | 0 | 0 | 1 | 1 | 0/3 |
| 2 | 2026-07-15 | NOT-CLEAN | 0 | 0 | 1 | 1 | 0/3 |
| 3 | 2026-07-15 | NOT-CLEAN | 0 | 0 | 0 | 1 | 0/3 |
| 4 | 2026-07-15 | NOT-CLEAN | 0 | 0 | 0 | 1 | 0/3 |
| 5 | 2026-07-15 | NOT-CLEAN | 0 | 0 | 0 | 1 | 0/3 |
| 6 | 2026-07-15 | NOT-CLEAN | 0 | 0 | 0 | 1 | 0/3 |
| 7 | 2026-07-15 | CLEAN | 0 | 0 | 0 | 0 | 1/3 |

## Next Step

Pass-8 (fresh-context). Streak 1/3 — two consecutive CLEAN passes required for convergence per BC-5.39.001. No findings carried forward.
