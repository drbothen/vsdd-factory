# S-19.09 LOCAL Adversary Pass-4 — NOT-CLEAN

**Date:** 2026-07-15
**Story:** S-19.09 — post-E-19 host ABI fixes: read_prefix production path registration, timeout_ms framing, telemetry hygiene
**Pass:** 4 of BC-5.39.001 cascade
**Result:** NOT-CLEAN B0/H0/M0/L1
**Streak:** 0/3 UNCHANGED
**Source HEAD reviewed:** edc3b384
**Branch:** feature/S-19.09

## Summary

Pass-4 reviewed HEAD edc3b384 following pass-3 open item (F-P3-001) and test-writer remediation (value-pin unit tests for `PLUGIN_COMPLETED` and `PLUGIN_TIMEOUT` in `internal_log.rs`). F-P3-001 confirmed closed with independent end-to-end verification; one new LOW spec-conformance finding surfaced under TD-VSDD-091.

F-P3-001 LOW: CLOSED-VERIFIED. Four `assert_eq!` value-pin tests are present in `internal_log.rs` `#[cfg(test)]` — two for `PLUGIN_COMPLETED` (`plugin.completed`) and two for `PLUGIN_TIMEOUT` (`plugin.timeout`). Naming parity between const symbol and string value confirmed. Adversary ran the full crate test suite (`cargo test -p factory-dispatcher`) independently: all tests pass. Closure is load-bearing: a mutant that changes either constant's string value now fails the corresponding `assert_eq!` pin.

F-P4-001 LOW — [TD-VSDD-091 volatile-pin self-contradiction]: Story v1.1 cites "lines 670–744 of invoke.rs" at seven body sites (AC-001 verification note, AC-003 verification note, AC-004 verification note, AC-008 verification note, AC-009 verification note, Task T-7 note, and the Test Plan §T-001 table cell) while the story's own Architecture Compliance Rules table row for TD-VSDD-091 explicitly declares "no line-number citations." The accuracy of the cited range is currently coincidental — the `read_prefix` registration block begins at approximately line 670 in the current tree — but any insertion to `invoke.rs` before that block will silently invalidate all seven citations without triggering a spec-failure. ROUTED story-writer (in flight): stable-anchor sweep replacing all seven `lines 670–744` cites with stable function-name anchors (e.g., `setup_host_on_store_data` or the `register!` macro call block), verified with literal-shell gate per POLICY 5 v1.3.4.

Pass-4 positive coverage: adversary ran the full crate suite independently (all tests exit 0); `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo fmt --check --all` clean; bats hygiene suite 9/9 green. Four finding-classes independently re-swept CLEAN: (1) doc-accuracy — `read_prefix.rs` duality comment, `executor.rs` D20 epoch-interruption grep (0 matches), D21 structurally-unenforced comment (1 match at expected location); (2) const/sibling-sweep — `PLUGIN_COMPLETED`/`PLUGIN_TIMEOUT` production occurrences are the const definitions only; no TD-VSDD-060 violation; (3) harness-liveness — T-002/T-002b/T-003 remain load-bearing; T-013 timestamp-persistence confirmed; (4) spec-conformance — full T-001..T-014 suite + File Structure rows map to the actual tree. Base-0-hit/HEAD-42-hit D19 grep reproduction confirmed. Whole-crate four-literal sweep (T-011) clean. Direct re-reads of ADR-025 v1.16, BC-1.17.001 v1.7, and BC-3.08.001 v1.23 confirm no spec drift.

Code deliverable attested production-grade at edc3b384: all 10 ACs conform. D19 mirrors `read_file.rs` template faithfully. D22 sibling parity (`plugin.completed` timestamp field absent from `RESERVED_FIELDS`). D21 named-constant discipline satisfied (four literals replaced with `PLUGIN_COMPLETED`/`PLUGIN_TIMEOUT` const refs). T-009/T-010 value-pin gap closed by F-P3-001 remediation.

Accepted-with-record (no present defect, no streak impact):

- Sink-crate and `tests/*.rs` bare string literals are cross-crate routing keys and test fixtures, not intra-crate const violations. `PLUGIN_COMPLETED` and `PLUGIN_TIMEOUT` are not exported across those crate boundaries; no TD-VSDD-060 violation.
- Epoch-deadline remains harness-only (consistent with pass-1 through pass-3): `timeout_ms_to_epochs(5000)` alignment is in test harness code only; production dispatch always invokes the same helper with the configured `timeout_ms` value; no production masking.

Cascade Trajectory: P1 M1/L1 0/3 → P2 M1/L1 0/3 → P3 L1 0/3 → P4 L1 0/3.

Next: F-P4-001 closure via story-writer dispatch (stable-anchor sweep with literal-shell gate). Pass-5 to proceed after F-P4-001 closure verified at new HEAD. Streak remains 0/3.

## Part A — Findings

### F-P4-001 — LOW — [TD-VSDD-091 volatile-pin self-contradiction] Story v1.1 cites line ranges in 7 body sites while its own ACR table forbids line-number citations

**Location:** `S-19.09` story v1.1 body — AC-001 verification note; AC-003 verification note; AC-004 verification note; AC-008 verification note; AC-009 verification note; Task T-7 note; Test Plan §T-001 table cell
**Observation:** The S-19.09 story v1.1 contains the phrase "lines 670–744 of invoke.rs" (or equivalent `invoke.rs:670–744`) at seven distinct body sites across the ACs and Test Plan section. The story's own Architecture Compliance Rules (ACR) table includes a TD-VSDD-091 row that explicitly prohibits line-number citations in spec artifacts ("no line-number citations in spec narrative"). This is a direct self-contradiction: the spec body violates a rule the same spec document codifies. The cited range is currently accurate in the HEAD tree (the `read_prefix` registration block beginning with `setup_host_on_store_data` does reside near line 670 of `invoke.rs`), but that accuracy is coincidental and will silently decay on any upstream insertion. Under TD-VSDD-091, stable function-name anchors (`setup_host_on_store_data`, the `register!` macro call, or the `fn register_read_prefix_host` symbol) must replace all seven line-range cites.
**Severity rationale:** LOW because: (1) the referenced behavior and the actual implementation are correctly aligned at this HEAD — the line-range cites accurately point to the production-path registration block; (2) no test or gate depends on the line numbers; (3) the defect is spec-form only, not behavioral. However, TD-VSDD-091 is a standing production-grade rule with no waiver for "currently accurate" volatile pins.
**Fix required:** Story-writer sweep: replace all seven `lines 670–744 of invoke.rs` (or `invoke.rs:670–744`) cites with stable function-name anchors. Execute literal-shell gate per POLICY 5 v1.3.4 confirming zero residual line-range cites remain in non-historical body content after sweep. Capture stdout in commit body.
**Routing:** story-writer (in flight per pass-4 dispatch)
**Status:** OPEN — story-writer dispatched (in flight)

---

## Closed Findings

### F-P3-001 — LOW — CLOSED-VERIFIED at HEAD edc3b384

**Prior status:** OPEN at pass-3 HEAD 53f5a63c
**Closure evidence:** `internal_log.rs` `#[cfg(test)]` module now contains four `assert_eq!` value-pin tests: two for `PLUGIN_COMPLETED` (asserting the string value `"plugin.completed"`) and two for `PLUGIN_TIMEOUT` (asserting the string value `"plugin.timeout"`). The const names and their string values are confirmed to match the production values used by the executor path and the bats event-value assertions. Adversary executed `cargo test -p factory-dispatcher` independently at HEAD edc3b384: all tests pass. A mutant that changes either constant's string value will now fail the corresponding unit-test pin — closure is load-bearing. Symmetry adjudication per story v1.1 scope confirmed: both `PLUGIN_COMPLETED` and `PLUGIN_TIMEOUT` received value-pins.
**Mutation check:** Confirmed. A change to `PLUGIN_COMPLETED = "plugin.completed"` would fail the `assert_eq!(PLUGIN_COMPLETED, "plugin.completed")` pin test. Equivalent for `PLUGIN_TIMEOUT`. Both pins are in the same `#[cfg(test)]` module as specified by AC-006/AC-007.

---

## Accepted-with-Record

**Sink-crate and tests/*.rs bare string literals are cross-crate routing keys/fixtures — not TD-VSDD-060 violations:** `PLUGIN_COMPLETED` and `PLUGIN_TIMEOUT` consts are defined in `crates/factory-dispatcher/src/internal_log.rs`. The bare string literals in the sink crate and in `plugins/vsdd-factory/tests/*.rs` are cross-crate routing keys and test fixtures; those crates do not import or depend on the consts. TD-VSDD-060 requires a sibling-sweep for callers of a const within the scope of a crate that exports it — it does not require every string matching the const value to be replaced. No violation.

**Epoch-deadline remains harness-only (pass-4 re-confirmation):** `timeout_ms_to_epochs(5000)` alignment (740f01d8) is in test harness code only. Production dispatch always invokes the same `timeout_ms_to_epochs` helper with the configured `timeout_ms` value; no production masking present. Re-confirmed at HEAD edc3b384. Accepted-with-record standing from pass-1 (740f01d8) maintained.

## Coverage

Adversary examined the following artifacts at HEAD edc3b384:

- S-19.09 story v1.1 (all ACs; Architecture Compliance Rules table; Test Plan §T-001..§T-014; File Structure; Task T-7 note)
- BC-1.17.001 v1.7 (active; Inv3 no-fallback, Inv4 shared-path algorithm, PC-2 head_c clamp, PC-3 take() semantics)
- BC-3.08.001 v1.23 (AC-010 timestamp field; Event 6 plugin.completed async path)
- ADR-025 v1.16 §Decisions 16–22 (D19 production-path registration; D20 doc comments; D21 named constants; D22 timestamp persistence)
- `crates/factory-dispatcher/src/invoke.rs` (T-001..T-014 suite; D19 production-path registration block; base-0-hit/HEAD-42-hit D19 grep reproduction; four-literal sweep T-011)
- `crates/factory-dispatcher/src/internal_log.rs` (const definitions + new `#[cfg(test)]` value-pin tests T-009/T-010; MUTANT-fixture literal at :708 exempted)
- `crates/factory-dispatcher/src/host/read_prefix.rs` (sibling sweep: confirmed clean)
- `crates/factory-dispatcher/src/host/read_file.rs` (reference pattern: two-linker duality comment; ptr==0 guard; write_wasm_bytes semantics)
- `crates/factory-dispatcher/src/executor.rs` (D20 epoch-interruption grep: 0 matches; D21 structurally-unenforced: 1 match at expected location)
- `plugins/vsdd-factory/src/vsdd_sink.rs` (D21 cfg(test) boundary: line 120 production boundary verified)
- `plugins/vsdd-factory/tests/host-abi-hygiene.bats` (T-004..T-012 suite including T-009/T-010 grep-gates and T-011 four-literal gate; T-013 load-bearing)
- `plugins/vsdd-factory/hooks-registry.toml` (scope check: unmodified)
- hook-sdk `src/host.rs` + `src/ffi.rs` (scope check: unmodified; ABI unchanged)

Crate test suite: `cargo test -p factory-dispatcher` — all tests pass at HEAD edc3b384.
Clippy: `cargo clippy --workspace --all-targets -- -D warnings` — clean.
Fmt: `cargo fmt --check --all` — clean.
Bats hygiene: 9/9 green.
AC grep gates: all 10 ACs verified against code at edc3b384.
D19 grep: base=0 hits (`HEAD~`), HEAD=42 hits (production-path registration block present; mirrors read_file.rs pattern).
Four-literal sweep (T-011): whole-crate scan confirms no residual bare string literals for `PLUGIN_COMPLETED`/`PLUGIN_TIMEOUT` in production code outside the const definitions.

## Exit Gates (at HEAD edc3b384)

- Cargo unit test suite (factory-dispatcher): **passing** (T-009/T-010 value-pins now present; all tests green)
- `cargo clippy --workspace --all-targets -- -D warnings`: **clean**
- `cargo fmt --check --all`: **clean**
- Bats T-004..T-012: **9/9 green** (T-011 four-literal gate included; T-009/T-010 grep-gates passing)
- T-002 byte-equality assertion (AC-002 mandate): **confirmed** (F-P2-001 CLOSED; load-bearing)
- T-002 tempdir isolation: **confirmed** (F-P2-002 CLOSED; structural)
- T-009/T-010 assert_eq! value-pins in internal_log.rs cfg(test) (AC-006/AC-007 mandate): **CLOSED-VERIFIED** (F-P3-001)
- T-013 load-bearing: **confirmed**
- D22 timestamp persistence: **confirmed**
- TD-VSDD-091 line-range cites (story v1.1 body): **OPEN** — story-writer dispatched (F-P4-001)

## Cascade Trajectory

| Pass | Date | Result | B | H | M | L | Streak |
|------|------|--------|---|---|---|---|--------|
| 1 | 2026-07-15 | NOT-CLEAN | 0 | 0 | 1 | 1 | 0/3 |
| 2 | 2026-07-15 | NOT-CLEAN | 0 | 0 | 1 | 1 | 0/3 |
| 3 | 2026-07-15 | NOT-CLEAN | 0 | 0 | 0 | 1 | 0/3 |
| 4 | 2026-07-15 | NOT-CLEAN | 0 | 0 | 0 | 1 | 0/3 |

## Next Step

F-P4-001 in flight via story-writer dispatch: replace all seven `lines 670–744 of invoke.rs` cites in story v1.1 body with stable function-name anchors, with literal-shell gate per POLICY 5 v1.3.4 confirming zero residual line-range cites in non-historical body content. Pass-5 to proceed after F-P4-001 closure verified at new HEAD. Streak remains 0/3.
