# W-16 S-9.00 pass-1 adversarial review

Date: 2026-05-03
Adversary agentId: a8507fb0333a2ebe0
Target: .factory/stories/S-9.00-perf-baseline-and-bundle-ceiling.md (330L)
Verdict: SUBSTANTIVE (12 findings: 3 HIGH, 5 MEDIUM, 4 LOW)
Pass: 1 of N

> **Note:** Full 12-finding table in agent transcript at agentId a8507fb0333a2ebe0.
> This file captures the 3 HIGH severities verbatim; MEDIUM and LOW summaries follow.
> Story-writer fix-burst MUST consult the full transcript before applying fixes.

---

## Findings Table

| ID | Severity | Category | Location | Description | Suggested Fix |
|----|----------|----------|----------|-------------|---------------|
| F-1 | HIGH | deliverable | AC-1 + EC-003 | Platform-coverage drift: story targets 3 platforms but vsdd-factory ships 5 (darwin-arm64, darwin-x64, linux-arm64, linux-x64, windows-x64). darwin-x64 and linux-arm64 are missing from AC-1 platform list and from EC-003's measurement table. Baseline recorded on fewer platforms than CI tests. | Update AC-1, EC-003, and A.3 (appendix/measurement script) to all 5 platforms; add darwin-x64 + linux-arm64 rows. |
| F-2 | HIGH | ceiling-rationale | Goal Responsibility B + B.1 | Ceiling baseline mismatch with E-8 R-8.09. R-8.09 anchors bundle ceiling to v1.0.0 GA artifact sizes. S-9.00 anchors to post-rc.4 (current workspace), creating cumulative drift that is uncapped. If W-16 delivers multiple stories that each grow the bundle, the GA ceiling reference is already stale before W-16 even begins. | AC-3 must explicitly address the relationship: either (a) the ceiling is set vs GA and W-16 must not exceed it, (b) a per-wave growth budget applies, or (c) the GA ceiling reference in R-8.09 is superseded. Pick one; document in AC-3. |
| F-3 | HIGH | POLICY 11 / AC-quality | AC-1..AC-7 | Tautology risk — ACs assert that specific numeric values are recorded in specific files, but none require that the committed measurement script be EXECUTED to reproduce those values. An implementer could hand-write numbers into the committed file without running any tooling, and every AC would pass. POLICY 11 (no_test_tautologies) applies here. | Add AC requiring `measure-bundle-sizes.sh` (or equivalent) to be run in CI and its output compared to the committed baseline file. OR add an AC with a bats test that runs the script and asserts the output format/columns match the committed file structure. |
| F-4 | MEDIUM | cross-doc | S-9.00 §tasks vs S-9.30 | S-9.30 adds SubprocessCaps schema and host::run_subprocess to the SDK. S-9.00 does not account for the bundle-size contribution of the new host fn bindings. If S-9.30 ships in the same wave (Wave 0), the baseline measurement window is ambiguous: measure before or after S-9.30? | Clarify ordering: is the S-9.00 baseline measured on current main/develop (pre-S-9.30), or on the branch that includes S-9.30? Document in AC-1 pre-conditions. |
| F-5 | MEDIUM | AC-quality | AC-2 | AC-2 requires a `per-plugin budget` table, but does not specify the unit (bytes? kibibytes?). EC-003 uses kB in its column header, but "kB" is ambiguous (1000 vs 1024). | Standardize on a unit (e.g., KiB = 1024-byte kibibytes). Propagate to AC-2, AC-3, EC-003, and the measurement script output. |
| F-6 | MEDIUM | scope | AC-3 ceiling definition | AC-3 commits to a per-plugin ceiling but does not define what happens when a plugin is NEW (added in W-16). A new plugin has no prior baseline; any value would satisfy the ceiling trivially. | Add an AC or EC row specifying the ceiling policy for new plugins added in W-16 (e.g., MUST be ≤ the median existing plugin size, or a fixed max). |
| F-7 | MEDIUM | library-pinning | S-9.00 §tasks — measurement tooling | `du` (coreutils) version is not pinned. On macOS `du -sb` uses BSD semantics; on Linux GNU coreutils. The `-b` flag (byte-count) is GNU-only; macOS `du` uses `-k` for kibibytes. Cross-platform measurements may be inconsistent. | Script must use `wc -c < file` or `stat -c %s` for portable byte counts, or call out platform-specific flag differences explicitly. |
| F-8 | MEDIUM | AC-quality | AC-5 | AC-5 references "all 5 CI platforms" but AC-1 only lists 3 platforms. Contradiction within the same story. | Resolve by aligning AC-1 to 5 platforms (per F-1 fix) so AC-5 is consistent. |
| F-9 | LOW | editorial | S-9.00 frontmatter | `status: draft` is appropriate for the authoring phase. Confirm `wave: 0` matches E-9 topology table once F-17 (E-9 cross-doc finding) is resolved. | Verify `wave: 0` field present and consistent with E-9 after E-9 fix-burst. |
| F-10 | LOW | cross-doc | S-9.00 §success-criteria | Success criteria do not reference the ADR-013 convergence requirement. Same pattern as E-9 F-16. | Add convergence note: "Adversarial convergence per ADR-013 (3 NITPICK_ONLY) required before S-9.00 is implementation-ready." |
| F-11 | LOW | formatting | S-9.00 §A.3 appendix | Appendix A.3 has a shell snippet but no indication of expected output format. An implementer reading the story doesn't know what the script should print. | Add a one-line example output comment in the snippet showing expected format (e.g., `# Expected: plugin_name size_kib`). |
| F-12 | LOW | scope | S-9.00 §goal-B — per-plugin budget | Goal B commits to a budget table at story completion, but does not indicate whether the budget is HARD (CI gate blocks on overage) or SOFT (advisory warning). | Clarify in AC-3 or a new EC row: is the per-plugin ceiling a CI gate or an advisory? |

---

## Convergence Status

- **Pass 1:** SUBSTANTIVE — 12 findings (3 HIGH, 5 MEDIUM, 4 LOW)
- **Next action:** Story-writer fix-burst; priority order: F-1 (5-platform coverage), F-3 (POLICY 11 script-execution AC), F-2 (ceiling baseline vs GA R-8.09), F-4 (S-9.30 ordering ambiguity), F-7 (du portability)
- **Convergence clock:** 0 of 3 NITPICK_ONLY passes
