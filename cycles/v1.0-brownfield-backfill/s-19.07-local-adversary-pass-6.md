# S-19.07 LOCAL Adversary Pass-6 — NOT-CLEAN

**Date:** 2026-07-16
**Story:** S-19.07 — verify-factory-lock read_prefix migration
**Pass:** 6 of BC-5.39.001 cascade
**Result:** NOT-CLEAN B0/H1/M1/L0
**Streak:** 0/3 UNCHANGED
**Source HEAD:** d6fdcdc8 (adversary review baseline; implementer c24b77a4 closes F-P6-001 post-review)
**Branch:** feature/S-19.07 (base 9787c056)
**Story version reviewed:** v1.22 (factory-artifacts 4927102a)

## Summary

2 findings: 1 HIGH + 1 MEDIUM. F-P5-001 CLOSED-VERIFIED: fixture pads body content so total file size reaches ≥262144 bytes at d6fdcdc8; real-shape FP1002 tests now exercise the read_prefix truncation path (fixture total confirmed ≥262144; dual-bound mutation slice independent per upstream verification; all four offset-form sites updated consistently with fixture arithmetic). F-P5-002 CLOSED-VERIFIED: story-writer v1.22 (4927102a) corrected all three sibling sites (Task 2 / RG-003 / T-003) from absolute "~27 KB" offset form to delta-parenthetical form ("~27 KB beyond the retracted 8192-byte window"); claim-class gate run: 8 hits adjudicated ACCEPTABLE (delta parenthetical or historical changelog); zero absolute-offset form remaining.

F-P6-001 HIGH [NEW — shipped-artifact build mode]: the committed verify-factory-lock.wasm at d6fdcdc8 was a DEBUG build (6,058,566 bytes; full DWARF sections; 114 registry paths vs develop's 223,517-byte release); rebuild commands omitted `--release`; hot-path guard on a security path → fuel-exhaustion risk + DWARF/source-path leakage. Prior passes verified path + imports but never build mode. CLOSED: implementer c24b77a4 (release rebuild 224,681 bytes; zero .debug_ sections; read_prefix-only imports; bats 5/5 against the committed artifact).

F-P6-002 MEDIUM [NEW — reverse-traceability]: File Structure table omitted the integration_ac004 delete row (introduced in a prior burst but not reflected in the story's File Structure inventory); the v1.20 audit that closed F-P2-002→F-P3-001 ran only forward-only direction (rows→inventory); reverse direction (inventory→rows) was never run, so new deletions silently fall through. CLOSED — story-writer v1.23 (ca64dcce): delete row added for `crates/hook-plugins/verify-factory-lock/tests/integration_ac004_no_output_too_large.rs` (Phase-A VP-095 harness, superseded, deleted at worktree 0cbf913e); REVERSE audit executed confirming all 5 branch-diff entries have File Structure rows (lib.rs M, integration_ac004_no_output_too_large.rs D, verify-factory-lock.wasm M, hooks-registry.toml M, verify-factory-lock-read-prefix.bats A); bonus correction: wasm row build target corrected wasm32-unknown-unknown→wasm32-wasip1 (matches actual build command; F-P6-001 context); input-hash daa1363 unchanged.

Accepted-with-record carried: READ_TIMEOUT_MS/§D18; 262144 coherence; VP-095 dual-anchor; portable Gate B (F-P4-002).

Cascade Trajectory: P1 B1/H1 → P2 H1/M1/L1 → P3 M1 → P4 M2/L1 → P5 M1/L1 → P6 H1/M1, all 0/3.

[process-gap note]: This is the first instance in the S-19.07 cascade of shipped-artifact build-mode verification failing. DEBUG builds of hook plugins are a distinct defect class from path/import verification: size envelope (>20× release size), DWARF section presence, and fuel-exhaustion risk on security paths are all invisible to path+imports-only gates. Standing lesson: adversary verification of any committed WASM artifact MUST include (i) size envelope check against the release-mode envelope for that plugin, (ii) zero `.debug_` section assertion (`wasm-objdump -h` or equivalent), (iii) imports consistent with release-mode capability surface — not just path + capability name presence.

## Part A — Findings

### F-P1-001 — BLOCKER CARRY-FORWARD — CODE-CLOSED-VERIFIED

**Original finding:** read_prefix max_bytes=8192 premise-false; guard silently inert on production STATE.md
**Status at implementer HEAD c24b77a4:** CLOSED-VERIFIED — `crates/hook-plugins/verify-factory-lock/src/lib.rs` call site confirmed at 262144. WASM rebuilt in release mode at c24b77a4 (224,681 bytes; zero .debug_ sections). Live-proof stands.

---

### F-P1-002 — HIGH CARRY-FORWARD — CLOSED-VERIFIED

**Original finding:** fixture coverage gap — no test exercises factory_lock block past the 8192-byte frontmatter boundary
**Status at implementer HEAD c24b77a4:** CLOSED-VERIFIED — fixture padded to ≥262144 total bytes at d6fdcdc8 (F-P5-001 closure); real-shape FP1002 tests exercise the truncation path. Dual-bound mutation proof confirmed.

---

### F-P2-001 — HIGH CARRY-FORWARD — CLOSED-VERIFIED

**Original finding:** POLICY 9 propagation miss: VA/VCM VP-095 rows carried v1.2 stale title and proof_method
**Status at implementer HEAD c24b77a4 (factory-artifacts at 4927102a):** CLOSED-VERIFIED — VA v1.11 §VP-095 row: "read_prefix(262144) structural guarantee" + proof_method "unit+static". VCM v1.8 §SS-04 VP-095 row: 262144 + proof_method "unit+static". Six-artifact claim-class gate: zero active normative 8192 occurrences.

---

### F-P2-002 through F-P4-002 — CARRY-FORWARD — CLOSED-VERIFIED

All carry-forward closures from passes 2–5 re-verified at c24b77a4 / story v1.22 (4927102a). Specifically: F-P2-002→F-P3-001 Test Plan phantom corrected; F-P2-003 bats BC version-token gate clean; F-P4-001 `verification_properties: ["VP-095"]` frontmatter present; F-P4-002 AC-001 Gate B portable `-e` form at both sites. No regressions detected.

---

### F-P4-003 — LOW CARRY-FORWARD — PARTIALLY-CLOSED → F-P5-002 → CLOSED-VERIFIED

**Original finding (F-P4-003→F-P5-002):** AC-003 main-site corrected; Task 2 / RG-003 / T-003 retained "~27 KB" absolute-offset phrasing
**Status at story v1.22 (SW 4927102a):** CLOSED-VERIFIED — story-writer swept all three sibling sites and corrected to delta-parenthetical form ("~27 KB beyond the retracted 8192-byte window, immediately before the closing --- delimiter at ~34.87 KB / byte 35,174"). Claim-class gate run: `grep -nE "27 ?KB|27,?000"` → 8 hits; all adjudicated ACCEPTABLE (parenthetical delta form or historical changelog); zero absolute-offset characterization remaining. TD-VSDD-060 claim-class sweep confirmed exhaustive.

---

### F-P5-001 — MEDIUM CARRY-FORWARD — CLOSED-VERIFIED

**Original finding:** fixture body pads to 45,000 bytes; story spec requires ≥262144 total; FP1002 tests do not exercise truncation path
**Status at worktree HEAD d6fdcdc8:** CLOSED-VERIFIED — test-writer padded the real-shape fixture body so total file size reaches ≥262144 bytes. T-003/T-004 now exercise the `read_prefix` truncation path under real-shape 35KB-frontmatter conditions. Mutation slice (EC-001 boundary unit test) confirmed independent — fixture expansion does not overlap with the EC-001 coverage domain. All four offset-form sites in the story updated consistently with the fixture arithmetic.

---

### F-P6-001 — HIGH — Shipped-artifact build mode: DEBUG WASM (6,058,566 bytes; full DWARF) at review HEAD d6fdcdc8

**Location:** `plugins/vsdd-factory/hooks/hook-plugins/verify-factory-lock.wasm` committed at d6fdcdc8
**Observation:** The committed verify-factory-lock.wasm was a DEBUG build: 6,058,566 bytes (>26× the develop-branch release-mode artifact at 223,517 bytes); full DWARF debug sections present (`wasm-objdump -h` shows `.debug_info`, `.debug_str`, `.debug_loc`, etc.); 114 import entries vs the release-mode's read_prefix-only capability surface. Prior passes verified path registration and capability name but did not check build mode. For a hook plugin that gates a security-critical path (factory-lock enforcement), a DEBUG build carries dual risk: (a) fuel exhaustion — DWARF sections consume disproportionate WASM fuel on the hot path, creating a silent pass-through failure mode identical to the original F-P1-001 class; (b) source-path leakage — DWARF embeds developer filesystem paths and symbol names into the shipped plugin.
**Routing:** implementer — rebuild `verify-factory-lock` with `cargo build --release --target wasm32-wasip1 -p verify-factory-lock` and commit the release-mode artifact.
**Status:** CLOSED — implementer c24b77a4 committed the release-mode rebuild: 224,681 bytes; `wasm-objdump -h` shows zero `.debug_` sections; imports reduced to `read_prefix` only; bats suite 5/5 green against the committed artifact.

---

### F-P6-002 — MEDIUM — Reverse-traceability gap: File Structure table missing integration_ac004 delete row

**Location:** `S-19.07-verify-factory-lock-read-prefix-migration.md` v1.22 — §File Structure table
**Observation:** The story's File Structure table lists files added/modified by the implementation but omits the deletion of `integration_ac004` (a test file removed in a prior burst as part of the read_prefix migration cleanup). The v1.20 exhaustive audit (F-P2-002→F-P3-001) ran only in the forward direction: verifying that every row in the File Structure table has a corresponding implementation artifact. The reverse direction — verifying that every implementation artifact change (including deletions) has a corresponding row in the File Structure table — was not run. This asymmetric audit methodology means that any file added to or deleted from the implementation without a corresponding File Structure row silently passes the forward audit. Under the VSDD spec-wins rule the File Structure table is the authoritative inventory; it must be complete in both directions.
**Routing:** story-writer v1.23 — run a REVERSE audit: enumerate every file touched by the implementation (adds, modifies, deletes) and verify each has a corresponding File Structure row; add any missing rows (including the integration_ac004 delete row); then re-run the forward audit to confirm parity in both directions.
**Status:** CLOSED — story-writer v1.23 (ca64dcce): delete row added for `crates/hook-plugins/verify-factory-lock/tests/integration_ac004_no_output_too_large.rs` (Phase-A VP-095 harness superseded; deleted at worktree 0cbf913e); REVERSE audit executed (all 5 branch-diff entries confirmed: lib.rs M, integration_ac004_no_output_too_large.rs D, verify-factory-lock.wasm M, hooks-registry.toml M, verify-factory-lock-read-prefix.bats A); wasm row build target corrected wasm32-unknown-unknown→wasm32-wasip1; forward audit re-confirmed clean; input-hash daa1363 unchanged.

---

## Coverage

Adversary examined the following artifacts at worktree HEAD d6fdcdc8 (factory-artifacts at story v1.22 / 4927102a):

- S-19.07 story v1.22 (all AC gates; §File Structure table forward + reverse audit; claim-class gate for "~27 KB" / "27,000" / "27 KB" residues; `verification_properties:` frontmatter; fixture body padding vs spec ≥262144; AC-001 Gate B portable `-e` form both sites; WASM artifact size + sections + imports)
- BC-4.13.001 v1.20 (Phase-B test inventory; preconditions; invariants; postconditions)
- VP-095 v1.3 (stories: frontmatter array S-19.07 entry; proof_method unit+static; structural guarantee title)
- verification-architecture.md v1.11 (§VP-095 row)
- verification-coverage-matrix.md v1.8 (§SS-04 VP-095 column)
- `plugins/vsdd-factory/tests/verify-factory-lock-read-prefix.bats` at d6fdcdc8 (5 tests; portable `-e` sed form; BC anchor form clean)
- `plugins/vsdd-factory/tests/verify-factory-lock/verify-factory-lock.bats` at d6fdcdc8 (fixture total size vs 262144 spec; T-003/T-004 truncation path coverage)
- `crates/hook-plugins/verify-factory-lock/src/lib.rs` at d6fdcdc8 (read_prefix call site; 262144 bound; EC-001 unit test)
- `plugins/vsdd-factory/hooks/hook-plugins/verify-factory-lock.wasm` at d6fdcdc8 (size: 6,058,566 bytes; sections: full DWARF; imports: 114 entries — DEBUG build confirmed)
- `plugins/vsdd-factory/hooks/hook-plugins/verify-factory-lock.wasm` at c24b77a4 (size: 224,681 bytes; sections: zero .debug_*; imports: read_prefix only — release build confirmed)
- `plugins/vsdd-factory/hooks-registry.toml` (registry entries; capabilities.read_prefix presence; path_allow)

## Exit Gates (at implementer HEAD c24b77a4 / story v1.22)

- Phase-B bats suite (verify-factory-lock-read-prefix.bats): **5/5 green**
- read-prefix-wasm.bats: **8/8 green**
- verify-factory-lock/verify-factory-lock.bats: **13/13 green** (including ≥262144-total real-shape regression fixture)
- sprint-state-format.bats: **14/14 green**
- validate-state-structure --lib: **65/65 green**
- `cargo clippy --workspace --all-targets -- -D warnings`: **clean**
- `cargo fmt --check --all`: **clean**
- AC-001 Gate A: `grep -q "read_prefix" crates/hook-plugins/verify-factory-lock/src/lib.rs` → **exits 0**
- AC-001 Gate B (symbol-absence): portable `-e` form confirmed at story v1.22; F-P4-002 CLOSED → **exits non-zero (zero non-comment symbol uses)**
- `verification_properties:` frontmatter gate: `grep -q 'verification_properties:.*VP-095' S-19.07-verify-factory-lock-read-prefix-migration.md` → **exits 0**
- Claim-class gate (F-P5-002): `grep -nE "27 ?KB|27,?000"` → **8 hits, all ACCEPTABLE (delta parenthetical or historical changelog); zero absolute-offset form**
- WASM release-mode gate (F-P6-001): `wasm-objdump -h verify-factory-lock.wasm | grep -c '\.debug_'` → **0 (zero debug sections at c24b77a4)**
- WASM size gate (F-P6-001): artifact size 224,681 bytes (within release-mode envelope; >26× DEBUG artifact at d6fdcdc8)
- F-P6-002 closure gate: story v1.23 (ca64dcce) — REVERSE audit 5/5 entries covered; delete row present; wasm target corrected → **CLOSED**

## POLICY 14 4-Index Gate (pre-commit)

Executed before commit per POLICY 14 literal-shell gate requirement:

```bash
cd /Users/zious/Documents/GITHUB/vsdd-factory/.factory/specs && \
for IDX_PATH in behavioral-contracts/BC-INDEX.md verification-properties/VP-INDEX.md ../stories/STORY-INDEX.md architecture/ARCH-INDEX.md; do
  V=$(grep -E '^version:' "$IDX_PATH" | grep -oE '"[0-9]+\.[0-9]+"' | tr -d '"')
  LA=$(grep -E '^last_amended:' "$IDX_PATH" | grep -oE '\(v[0-9]+\.[0-9]+\)' | head -1 | tr -d '()v')
  [ "$V" = "$LA" ] && echo "PASS $IDX_PATH ($V)" || echo "FAIL $IDX_PATH (version=$V la_prefix=$LA)"
done
```

Gate stdout (pre-update):
```
PASS behavioral-contracts/BC-INDEX.md (4.08)
PASS verification-properties/VP-INDEX.md (2.69)
PASS ../stories/STORY-INDEX.md (4.203)
PASS architecture/ARCH-INDEX.md (3.04)
```

Zero FAIL lines. STORY-INDEX bumped v4.203→v4.205 (S-19.07 story v1.21→v1.23 leg-5 entry; v1.22 SW 4927102a + TW d6fdcdc8 + impl c24b77a4; v1.23 SW ca64dcce F-P6-002 close). BC-INDEX/VP-INDEX/ARCH-INDEX unchanged (all burst closures complete).

## Cascade Trajectory

| Pass | Date | Result | B | H | M | L | Streak |
|------|------|--------|---|---|---|---|--------|
| 1 | 2026-07-16 | NOT-CLEAN | 1 | 1 | 0 | 0 | 0/3 |
| 2 | 2026-07-16 | NOT-CLEAN | 0 | 1 | 1 | 1 | 0/3 |
| 3 | 2026-07-16 | NOT-CLEAN | 0 | 0 | 1 | 0 | 0/3 |
| 4 | 2026-07-16 | NOT-CLEAN | 0 | 0 | 2 | 1 | 0/3 |
| 5 | 2026-07-16 | NOT-CLEAN | 0 | 0 | 1 | 1 | 0/3 |
| 6 | 2026-07-16 | NOT-CLEAN | 0 | 1 | 1 | 0 | 0/3 |

## Next Step

All pass-6 findings CLOSED. F-P1-001 through F-P5-002 carry-forward closures re-verified at c24b77a4 / story v1.22. F-P6-001 HIGH (DEBUG WASM build mode): CLOSED by implementer c24b77a4 (release rebuild 224,681 bytes; zero .debug_ sections; bats 5/5). F-P6-002 MEDIUM (reverse-traceability File Structure audit gap): CLOSED by story-writer v1.23 ca64dcce (delete row added; REVERSE audit 5/5 entries confirmed; wasm target corrected wasm32-unknown-unknown→wasm32-wasip1). Pass-7 proceeds from HEAD c24b77a4 / story v1.23 (ca64dcce). Streak 0/3.
