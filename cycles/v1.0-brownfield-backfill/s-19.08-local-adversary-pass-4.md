# S-19.08 LOCAL Adversary Pass-4 — CLEAN

**Date:** 2026-07-14
**Story:** S-19.08 — verify-state-timestamp-refresh: raise 64 KiB byte cap to 256 KiB + wire extract_frontmatter
**Pass:** 4 of BC-5.39.001 cascade
**Result:** CLEAN B0/H0/M0/L0
**Streak:** 0/3 → 1/3
**Source HEAD:** 7fc9bb7f
**Branch:** feature/S-19.08 (base 7b35c8e4)
**Commits reviewed (since pass-3):** 7fc9bb7f (test-writer: fallback regression-lock test +142/-0 test-only diff)
**Prior Pass:** pass-3 NOT-CLEAN B0/H0/M0/L1 (streak 0/3 UNCHANGED)

## Summary

Zero new findings. F-P3-001 LOW is CLOSED-VERIFIED. The test-writer added a fallback regression-lock test (delimiter-present + non-UTF-8 body) that traces the guard_logic Step 3 fallback arm through production. Adversary independently re-derived AC-001..AC-005 + BC-5.40.001 Precondition 6 / Invariants 7+8 / PC4 at HEAD 7fc9bb7f — all paths correct. The adversarial shapes swept clean: empty file; non-UTF-8 frontmatter; over-cap; dual-occurrence old_string; MultiEdit later-body-edit sequential semantics. Prior could-not-verify on AC-005 sub-test D RESOLVED: production `read_bounded` uses strict `>` comparator (`metadata.len() as usize > max_bytes`), so a 262144-byte-exact read succeeds in production, not only under the mock. AC-005 sub-test D holds in production.

2 LOWs from prior passes (F-P1-001, F-P1-002) independently re-derived; both carried accepted-with-record unchanged. Streak advances 0/3 → 1/3. Pass-5 required.

## Part A — Findings

### F-P3-001 — LOW — [fix-introduced coverage gap; TD-VSDD-059 class] non-UTF-8-body fallback not exercised by reconstruction tests — CLOSED-VERIFIED

**Location:** `crates/hook-plugins/verify-state-timestamp-refresh/src/lib.rs` — guard_logic Step 3 fallback arm
**Fix verified:** Test-writer commit 7fc9bb7f adds a fallback regression-lock test (+142/-0 test-only diff). The test constructs a delimiter-present file with a non-UTF-8 body and submits an Edit with a well-formed old_string anchor in the frontmatter. Adversary traced the path through production: frontmatter extraction succeeds (the delimiter is present and the frontmatter bytes are valid UTF-8); the body beyond the delimiter is non-UTF-8 but is not read for field-extraction. Under the fallback arm specifically (parse failure or non-UTF-8 body path), `on_disk_reconstruction_base` is set to `on_disk_field_content.clone()`. The test with non-UTF-8 body genuinely routes the fallback arm; the reconstruction decision consumes the fallback base.
**Mutation claim validated analytically:** A `String::new()` mutation of the fallback assignment (`on_disk_reconstruction_base = String::new()`) would cause `extract_edit_proposed` to find no match for the old_string anchor, returning `None`, and the guard would FailOpen (Continue) rather than correctly proceeding. The test asserts a determinate Block/Continue outcome that would change under this mutation — establishing it as a load-bearing regression guard, not a tautological path exercise.
**Diff scope:** +142/-0, test file only. No production source modified. No unintended scope bleed.
**Status:** CLOSED-VERIFIED

---

### F-P1-001 — LOW — T-001 const-pin question (carried accepted-with-record; independently re-derived pass-4)

**Location:** `crates/hook-plugins/verify-state-timestamp-refresh/tests/integration_t006_no_output_too_large.rs` — `test_BC_`-prefixed const-pin test
**Observation (re-derived):** T-001 asserts a const value equal to the production `READ_BOUNDED_CAP_BYTES` constant without calling a production function through the public API. Pattern superficially resembles a tautological const-echo test.
**Why accepted (unchanged):** AC-001-mandated pin; references production symbol directly; byte-identical to S-19.02 merged sibling pattern accepted by same reasoning. Not a tautology.
**Status:** Carried accepted-with-record; open follow-up: none.

---

### F-P1-002 — LOW — [process-gap] BC-5.40.001 VP Anchors placeholder text (carried accepted-with-record; independently re-derived pass-4)

**Location:** `.factory/specs/behavioral-contracts/ss-05/BC-5.40.001.md` — "VP Anchors" / "Verification Properties" section
**Observation (re-derived):** BC-5.40.001 states S-19.08 unit-test VP IDs are "to be assigned by state-manager after S-19.08 VP authoring pass." VP-096 reuse is already active in the VP-INDEX; the placeholder text is a bookkeeping gap.
**Why accepted (unchanged):** Outside code perimeter; no effect on compilation, test execution, or AC gate. Correct target: state-manager VP-authoring bookkeeping step at S-19.08 post-merge/convergence burst.
**Status:** Carried accepted-with-record; routing: product-owner/state-manager — post-story burst.

---

## Could-Not-Verify (resolved from pass-3)

- **AC-005 sub-test D in production — RESOLVED:** Pass-3 recorded a could-not-verify on whether the 262144-byte-exact read succeeds in production (not only under the mock). Adversary inspected the production `read_bounded` comparator and confirmed it uses `metadata.len() as usize > max_bytes` (strict `>`, not `>=`). A 262144-byte-exact file satisfies `262144 > 262144 == false`, so the read proceeds successfully. AC-005 sub-test D holds in production.
- **Live wasmtime sandbox runtime:** AC-004 integration test exercises cap-enforcement through an in-process mock comparator, not a live wasmtime harness. Consistent with story test design and S-19.02 sibling precedent. Recorded without routing.
- **Operator-level dispatcher logs:** Adversary cannot access the running dispatcher telemetry for this crate's hook invocations. Recorded without routing.
- **Invariant 8 u64 fields via log_warn:** Invariant 8 boundary fields are delivered through a `log_warn`-formatted string rather than a structured type. This is parity with the verify-factory-lock sibling plugin and is a pre-accepted design; no routing implied.

## Coverage

Adversary examined the following artifacts at HEAD 7fc9bb7f (diff base 7f8d8b7b→7fc9bb7f):

- S-19.08 story v1.1 (all ACs, gate forms, task list, Architecture Compliance Rules)
- BC-5.40.001 v1.2 (all invariants, preconditions, acceptance criteria, VP anchors section)
- `crates/hook-plugins/verify-state-timestamp-refresh/src/lib.rs` (full file; two-variable split confirmed unmodified from pass-3 closure)
- `crates/hook-plugins/verify-state-timestamp-refresh/tests/integration_t006_no_output_too_large.rs` (full integration test; all AC-gate rows including fallback regression-lock test from 7fc9bb7f)
- `crates/factory-lock-parse/src/lib.rs` (unmodified confirmed — no scope bleed from test-only diff)
- POLICY 21 (cap-raise registration) — hooks-registry.toml cap-value entry confirmed consistent with `READ_BOUNDED_CAP_BYTES`
- POLICY 5 (function-name anchors only in new comments) — PASS
- POLICY 10 (no demo evidence required yet) — PASS
- POLICY 11 — PASS

Diff scope: +142/-0 test file only — no production source modified; no unintended scope bleed confirmed.

## Exit Gates (adversary independently verified)

- Crate test suite: **50 lib + 2 integration green**
- `cargo clippy --workspace --all-targets -- -D warnings`: **clean**
- `cargo fmt --check --all`: **clean**
- `factory-lock-parse`: **UNMODIFIED** — no behavioral regression in unchanged sibling plugin
- POLICY 21 satisfied: hooks-registry.toml cap-value consistent with production constant

## Cascade Trajectory

| Pass | Date | Result | B | H | M | L | Streak |
|------|------|--------|---|---|---|---|--------|
| 1 | 2026-07-14 | CLEAN | 0 | 0 | 0 | 2-accepted | 1/3 |
| 2 | 2026-07-14 | NOT-CLEAN | 0 | 1 | 1 | 2-accepted | 0/3 RESET |
| 3 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 1+2-accepted | 0/3 UNCHANGED |
| 4 | 2026-07-14 | CLEAN | 0 | 0 | 0 | 0+2-accepted | 1/3 |

## Next Step

Pass-5 required (streak 1/3; need 2/3 for next advance). Source HEAD: 7fc9bb7f (no fix required; CLEAN pass).
