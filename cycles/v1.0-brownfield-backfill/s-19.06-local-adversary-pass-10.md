# S-19.06 LOCAL Adversary Pass-10 — CLEAN

**Date:** 2026-07-14
**Story:** S-19.06 — host::read_prefix bounded partial read
**Pass:** 10 of BC-5.39.001 cascade
**Result:** CLEAN B0/H0/M0/L0
**Streak:** 1/3 → 2/3
**Source HEAD:** 7156a4c3
**Remediation HEAD:** N/A — CLEAN pass
**Branch:** feature/S-19.06 (base 1304d280)
**Prior Pass:** pass-9 CLEAN B0/H0/M0/L0 (streak 1/3)

## Summary

CLEAN pass at source HEAD 7156a4c3 — no code changes since pass-9. Streak advances from 1/3 to 2/3.

The pass-9 could-not-verify item regarding the hooks-registry "S-19.04 AC-003" cross-reference has been resolved: the reference is semantically correct — tool-filter anchoring is S-19.04 AC-003; the Capability Schemas block is distinct and self-cites S-19.06 AC-007. No discrepancy.

A new adversarial probe of the registry capability-schema error-code table identified that wire code `-4` (INVALID_ARGUMENT — guest-memory marshalling failure in register closure) is absent from the documented error-code set (`0 / −1 / −2 / −5 / −99`). This was adjudicated a NON-finding: the BC-1.17.001 contract surface omits `-4`; the FFI-marshalling error is not a capability or filesystem outcome; `read_file` parity is identical. Recorded as an OPTIONAL completeness item for human review at the merge gate.

Full workspace gate re-run by the adversary at 7156a4c3: `cargo clippy --workspace --all-targets -- -D warnings` exit 0; `cargo fmt --check --all` exit 0; `cargo test --workspace` 0 failures; 14/14 unit tests green; 8/8 bats green.

All per-class sweep attestations hold:
- (a) POLICY 20 two-layer defense (coupled counts + dual-direction mutation-liveness + T-011 orphan proof + publish=false);
- (b) AC-003/T-009g executable + mutation-live;
- (c) spec↔impl ordering exact (steps 1–8; T-012/T-013 locks);
- (d) both MUTANT_VERIFY witnesses genuine (TD-VSDD-059 paper-fix detection);
- (e) doc-accuracy spot-checks all match code.

Zero new findings (B0/H0/M0/L0).

---

## Part A — Prior-Pass Finding Verification

No open findings carried from pass-9. Pass-9 was CLEAN B0/H0/M0/L0 with zero open items requiring closure verification.

---

## Part A — Could-Not-Verify Resolution

### Pass-9 CNV: hooks-registry "S-19.04 AC-003" cross-ref — RESOLVED

**Prior status (pass-9 CNV):** The hooks-registry contains a cross-reference to "S-19.04 AC-003"; could not verify whether this referred to the correct AC or was a numbering error.

**Resolution at 7156a4c3:** Verified against the hooks-registry and story specs. The cross-reference is semantically correct: the tool-filter anchoring mechanism is indeed specified in S-19.04 AC-003. The Capability Schemas block in the S-19.06 section is distinct and self-cites S-19.06 AC-007. No numbering error; no discrepancy. Item closed.

---

## Part A — New Findings

None. Zero new findings at source HEAD 7156a4c3 (B0/H0/M0/L0).

---

## New Adversarial Probe — Wire Code `-4` Error-Code Table Gap

**Probe:** Registry capability-schema error-code table for `read_prefix` documents codes `0 / −1 / −2 / −5 / −99`. Wire code `-4` (INVALID_ARGUMENT — returned when the guest-memory marshalling fails in the `register` closure) is absent.

**Adjudication:** NON-finding.

Rationale:
1. BC-1.17.001 contract surface omits `-4` from its enumerated error-code set; the table reflects the BC, not an undocumented extension of it.
2. The `-4` path is an FFI-layer marshalling failure, not a capability-layer or filesystem-layer outcome. It is triggered by a malformed WASM caller violating the ABI, not by any valid host-ABI invocation path.
3. `read_file` parity: identical gap present in the `read_file` sibling. This is a systemic FFI-marshalling documentation posture inherited from the host-ABI design, not introduced by S-19.06.
4. No BC violation; no test coverage gap for reachable behavior; no user-observable correctness impact.

**Disposition:** Recorded as OPTIONAL completeness item for human review at the S-19.06 merge gate. Not routed; not a defect. Human may elect to add a documentation note to the error-code table covering the marshalling-failure path, or defer to a systemic host-ABI documentation story.

---

## Fresh Positive Coverage (at source HEAD 7156a4c3)

**Full workspace gate re-run (adversary-executed):**
- `cargo clippy --workspace --all-targets -- -D warnings`: **exit 0**
- `cargo fmt --check --all`: **exit 0**
- `cargo test --workspace`: **0 failures**
- Unit tests: **14/14 green**
- Bats `read-prefix-wasm.bats`: **8/8 green**

**Full diff scope:** 14 files across full diff from base 1304d280 to source HEAD 7156a4c3. No files excluded from review.

**Must-be-unmodified re-confirmed:** `git diff` on `read_file.rs` and `path_util.rs` returns empty (no modifications since base 1304d280). Invariant 2 holds.

**AC-001..AC-007 sweep (HOLD):**
- AC-001 (bounded read with max_bytes cap): T-001..T-003 — pass.
- AC-002 (deny-by-default allowlist ordering): T-008, T-012 — pass; mutation witnesses T-013 confirm ordering is load-bearing.
- AC-003 (error taxonomy mapping from PrefixReadErr variants): T-005..T-007, T-011 — pass. T-009g executable and mutation-live confirmed.
- AC-004 (WIT-interface registration / coupled-count gate): T-009a..T-009h, T-010, T-013b — pass.
- AC-005 (path resolution honoring allowlist policy): T-004, T-009c..T-009e — pass.
- AC-006 (max_bytes=0 denial): T-013a, T-013b — pass.
- AC-007 (four gates — prepare succeeds, plugin registers, bats green, ordering locked): all four gates pass at 7156a4c3.

**POLICY 20 two-layer defense sweep (HOLD):**
- Layer 1 (coupled counts): T-009h coupled-count assertion re-grepped; matches live hook registration count. Accurate.
- Layer 2 (dual-direction mutation-liveness): T-013 mutation witnesses (`prepare_mutant_hoisted` + `prepare_mutant_lowered`) both confirm genuine liveness — neither is a vacuous assertion (TD-VSDD-059 paper-fix detection passes).
- T-011 orphan proof: confirms no phantom hook entries exist beyond the registered set.
- `publish = false`: present and accurate in `hooks-registry.toml` entry.

**Spec↔impl ordering exact (steps 1–8; HOLD):**
- All 8 prepare-steps in the story spec are implemented in order. T-012 and T-013 cascade-lock the ordering. No reordering introduced.

**Doc-accuracy spot-checks (HOLD):**
- `prepare()` 7-step docstring: re-verified against implementation body. Accurate; no stub-era framing.
- hooks-registry preamble error-code table: fields re-verified against implementation. Accurate (with OPTIONAL completeness note for `-4` above).
- T-009h coupled-count claim: re-grepped live; matches actual count.
- T-012/T-013 note group inline comments: accurately describe scope and cascade-lock partition.
- T-009a..T-009e static guard comments: accurately describe post-implementation static regression guard role.

---

## Accepted-with-Record (carried from prior passes)

**out_ptr=0 sentinel — read_file-verbatim parity:** The `out_ptr=0` / `write_bytes_to_guest` null-guard in `read_prefix::register` replicates `read_file::register`'s pre-existing wasm-boundary pattern verbatim per Invariant 2. Non-empty boundary read untested for both; coverage posture inherited. No new risk. Accepted-with-record; no routing.

**EC-006 timeout non-enforcement — SYSTEMIC host-ABI gap:** `read_prefix::register` does not enforce the timeout precondition at the wasm-boundary. Synchronous `func_wrap` closures in wasmtime cannot be preempted by epoch interruption while blocked on a blocking `read_to_end`; the `−2` timeout code is a code `read_prefix` structurally never returns. Gap spans `read_file` as well and is NOT introduced by S-19.06. Invariant 2 forbids modifying `read_file.rs`. Accepted-with-record. ROUTED: architect follow-up story recommendation anchored at the S-19.06 merge gate.

**Wire code `-4` gap — OPTIONAL completeness item (NEW, this pass):** See New Adversarial Probe section above. Adjudicated NON-finding; recorded for human review at merge gate.

**Story Test Plan lag vs test inventory:** Tests T-013a/b, `prepare_mutant_hoisted` T-013 mutation witnesses, and T-011 appear in the suite but are not listed in the story Test Plan. Accepted per cascade-remediation convention. Accepted-with-record.

**FFI adversarial-input surface swept clean:** NUL byte in path, non-UTF-8 path bytes, u32::MAX for max_bytes, empty path string — all produce correct error codes with no undefined behavior or allowlist bypass. Accepted-with-record; no routing.

**EC-007 directory-read + T-004 Err-branch vacuous-by-construction:** Directory-read error path not covered by unit tests. Parity with `read_file`. T-004 Err-branch statically covered by T-009g. EC-007 supported by empirical live probe (pass-9); behavior confirmed correct. Accepted-with-record; no routing.

**T-009a..T-009e as static regression guards, not red-gates:** Honestly documented post-implementation. Accepted-with-record; no routing.

**Story v1.21 ↔ BC v1.6 consistency:** All 10 body-site citations in S-19.06 v1.21 reference BC-1.17.001 v1.6. Task renumbering consistent through v1.17..v1.21 revisions. Confirmed accurate at 7156a4c3.

**Commit hygiene:** 17 commits from base 1304d280 to source HEAD 7156a4c3. Conventional-commit prefixes consistent (feat/test/fix/docs); no `Co-Authored-By` AI attribution; single-purpose per commit; no unintended file-scope bleed confirmed.

**POLICY 10 — n/a pre-Step-5:** PR merge gate not yet triggered.

**Invariant 4 chain naming (adjudicated NOT a defect):** Confirmed; no change at 7156a4c3.

---

## Could-Not-Verify

- **S-19.04 internal AC cross-ref numbering:** Pass-9 CNV resolved (see above). No outstanding CNV on this item.
- **Runtime WASM dispatch:** Documented extent of coverage = compile/link (T-009f). Actual in-process WASM execution path not exercised by the test suite. Consistent with accepted-with-record EC-008 posture across all prior passes.

---

## Exit Gates (at source HEAD 7156a4c3 — status at pass-10 close)

- `cargo clippy --workspace --all-targets -- -D warnings`: **exit 0** (adversary re-run this pass)
- `cargo fmt --check --all`: **exit 0** (adversary re-run this pass)
- `cargo test --workspace`: **0 failures** (adversary re-run this pass)
- Unit tests: **14/14 green**
- Bats `read-prefix-wasm.bats` T-009a..T-009h: **8/8 green**
- Full diff (14 files): **reviewed — zero findings**
- Prior-pass findings: **none open (pass-9 CLEAN)**
- New findings: **none (B0/H0/M0/L0)**
- 4-index at commit: BC-INDEX v4.04 / VP-INDEX v2.68 / STORY-INDEX v4.191 / ARCH-INDEX v3.01

---

## Cascade Trajectory

| Pass | Date | Result | B | H | M | L | Streak |
|------|------|--------|---|---|---|---|--------|
| 1 | 2026-07-14 | NOT-CLEAN | 0 | 1 | 1 | 0 | 0/3 |
| 2 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 2 | 0/3 |
| 3 | 2026-07-14 | NOT-CLEAN | 1 | 0 | 0 | 0 | 0/3 |
| 4 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 1 | 0 | 0/3 |
| 5 | 2026-07-14 | **CLEAN** | 0 | 0 | 0 | 0 | **1/3** |
| 6 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 1 | 0/3 RESET |
| 7 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 2 | 0/3 UNCHANGED |
| 8 | 2026-07-14 | NOT-CLEAN | 0 | 0 | 0 | 2 | 0/3 UNCHANGED |
| 9 | 2026-07-14 | **CLEAN** | 0 | 0 | 0 | 0 | **1/3** |
| 10 | 2026-07-14 | **CLEAN** | 0 | 0 | 0 | 0 | **2/3** |

## Next Step

Pass-10 is CLEAN B0/H0/M0/L0. Streak advances to 2/3. Pass-11 commences from the same source HEAD 7156a4c3 — no remediation needed before next pass. One additional CLEAN pass required for convergence (BC-5.39.001 3-CLEAN protocol). No story version change. 4-index at commit: BC-INDEX v4.04 / VP-INDEX v2.68 / STORY-INDEX v4.191 / ARCH-INDEX v3.01.
