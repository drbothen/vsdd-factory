---
document_type: cycle-decision-log
producer: state-manager
cycle: v1.0-feature-engine-discipline-pass-1
version: "1.0"
---

# v1.0-feature-engine-discipline-pass-1 Cycle Decision Log

Seed decisions recorded at cycle open (2026-05-06). All subsequent decisions will be
appended here per POLICY 1 (append-only, immutable IDs).

| ID | Decision | Rationale | Phase | Date | Made By |
|----|----------|-----------|-------|------|---------|
| D-336 | Cycle opened with two-cluster scope: (a) per-story adversarial convergence loop (documented in orchestrator MANDATORY STEPS but unimplemented in `per-story-delivery.md`), and (b) artifact path governance (WASM hook + path registry + relocation skill). Scope was user-decided after a `feature-deltas/` path-invention error during F1 dispatch surfaced the path-validation need. | Two governance gaps independently identified; co-locating in one cycle reduces release overhead and shares WASM toolchain work. Path-invention error was the triggering event that confirmed the need for path governance enforcement. | F1 | 2026-05-06 | user + orchestrator |
| D-337 | WASM-only for new hooks introduced by this cycle. Tier E Bash→WASM migration is in flight; new hooks MUST NOT introduce additional Bash hook debt. Any hook authored in this cycle must be a WASM binary. | Reduces long-term maintenance burden and aligns with Tier E migration trajectory. Bash hook debt is the root cause being addressed; adding more Bash hooks would worsen the problem. | F1 | 2026-05-06 | user |
| D-338 | Cycle name `v1.0-feature-engine-discipline-pass-1` chosen with `pass-1` suffix anticipating future engine-discipline cycles. Naming anticipates that engine governance improvements will recur across multiple cycles as the engine matures. | Pattern established by brownfield-backfill wave numbering (Wave 1..N). Engine discipline work is ongoing; `pass-1` suffix reserves the naming namespace for continuations. | F1 | 2026-05-06 | user + state-manager |
| D-339 | F1 architect agent recommended 3-story decomposition + 2 epics: S-A (per-story adversary workflow + agent docs), S-B (per-story adversary WASM hook), S-C (artifact path governance bundle). Epics: Engine Governance (S-A + S-B) and Artifact Integrity (S-C). F2 to confirm story boundaries and assign epic IDs. | 3-story split aligns with natural subsystem boundaries: agent-document changes (S-A) are low-blast-radius and shippable independently; WASM hook (S-B) requires Rust implementation; path governance (S-C) is a cross-cutting bundle. | F1 | 2026-05-06 | architect (F1 output) |
| D-340 | F2 integration burst commit — single atomic commit per TD-VSDD-053. 6 BCs (BC-5.39.001/002 SS-05, BC-4.10.001/002 + BC-4.11.001 SS-04, BC-6.22.001 SS-06), 2 ADRs (ADR-016 path registry SoT, ADR-017 per-story adversary phasing), 4 VPs (VP-069 proptest, VP-070 kani, VP-071 kani, VP-072 integration/bats), PRD 1.0→1.1 (FR-047), all indexes updated (BC-INDEX 1.14→1.15, ARCH-INDEX 1.7→1.8, VP-INDEX 1.0→1.1), total_bcs 1931→1937, current_cycle flipped. | Single atomic commit is the TD-VSDD-053 protocol for integration bursts. All index updates atomically bundled with spec artifacts to prevent count drift. | F2 | 2026-05-07 | state-manager |
| D-341 | OQ resolution from human gate — OQ1: serde_yaml acceptable; OQ3: per-story state file at .factory/cycles/<cycle>/<story-id>/adversary-convergence-state.json with schema {passes_clean, last_finding_count, last_classification, last_timestamp, deferred_findings[]}; OQ5: immediate block enforcement, no warn phase, relocation preflight gates hook registration; OQ6: adversary agent owns convergence assessment, no new agent. | Human gate on the F2 open questions resolved all 4 blocking OQs in a single decision. OQ3 schema is now codified in BC-5.39.001 invariants. OQ5 enforcement model (immediate block, no grace) is the simplest safe default. OQ6 (no new agent) avoids agent proliferation. | F2 | 2026-05-07 | user |
| D-342 | F2 specs produced — 6 BCs, 2 ADRs, 4 VPs, PRD 1.0→1.1. Per-story adversary state schema codified in BC-5.39.001 invariants. | F2 spec evolution phase complete; artifacts sufficient to begin F3 story decomposition. | F2 | 2026-05-07 | product-owner + architect |
| D-343 | E-10 paused by user. current_cycle flipped from v1.0-brownfield-backfill to v1.0-feature-engine-discipline-pass-1. E-10 pass-9 (NITPICK_ONLY counter: 0; trend: 22→11→16→16→12→2→1→4) remains queued for resumption after feature cycle completes or at user direction. | User prioritized engine discipline pass-1 over E-10 continuation. E-10 work is preserved; no artifacts lost; resumption is low-friction (adversary pass-9 is the next dispatch). | F2 | 2026-05-07 | user |
| D-344 | Group C decomposition resolved — 1 BC for hook+registry SoT (BC-4.11.001), 1 BC for relocation skill+sequencing (BC-6.22.001). F1's split of 'registry schema' as separate BC collapsed; registry schema lives in ADR-016 architectural anchors, not as a behavioral contract. | ADR-016 is the appropriate vehicle for the registry YAML schema (architectural decision); BCs govern the runtime behaviors of the WASM hook and relocation skill. Collapsing the registry-schema BC reduces contract count without losing coverage. | F2 | 2026-05-07 | architect |
| D-345 | F3 epics created — E-12 Engine Governance (Cluster A: workflow + WASM hook) and E-13 Artifact Integrity (Cluster B: registry + skills + relocation). Pattern parallel to E-7 process-codification (CONVERGED, not reopened). | Two-epic split maps cleanly to delivery sequence: E-13 ships first (path governance is a sequencing prerequisite); E-12 ships second (convergence loop depends on clean path governance). Parallel to E-7 prevents scope creep into already-converged process-codification work. | F3 | 2026-05-07 | product-owner |
| D-346 | F3 stories authored — S-13.01 (path governance bundle, 24 files, ships first), S-12.01 (workflow + agent docs, 4-6 files, ships second), S-12.02 (per-story adversary WASM hook, 6 files, ships third). Linear chain: S-13.01 → S-12.01 → S-12.02. All strict TDD mode. | 3-story split aligns with natural sequencing: S-13.01 establishes the path registry and relocate-artifact preflight; S-12.01 wires the workflow and agent docs that describe the new loop; S-12.02 implements the WASM convergence hook that enforces it. Strict TDD mode throughout. | F3 | 2026-05-07 | story-writer |
| D-347 | Dependency rationale — S-13.01 ships first because path-validation hook activates immediately in `block` mode (OQ5 resolution); registering before .factory/ is clean would brick subsequent agents. relocate-artifact preflight is the safety mechanism. | Immediate block enforcement (no warn-then-block phase) was the OQ5 resolution chosen by user. This means S-13.01 must reach a merged + validated state before S-12.01 or S-12.02 can be dispatched. relocate-artifact provides the escape hatch for agents that create files in the wrong location. | F3 | 2026-05-07 | orchestrator |
| D-348 | Open questions surfaced for F4 — OQ-9 (VP-071 advisory-block vs BC-4.10.001 block_with_fix discrepancy) is a SPEC contradiction that needs resolution before S-12.02 implementation. Other 8 OQs are F4 implementer concerns (path verifications, parser choice, etc.). OQ-9 may warrant a hot-fix BC amendment or VP correction prior to F4 dispatch. | VP-071 specifies advisory-block behavior while BC-4.10.001 mandates block_with_fix. These are contradictory enforcement levels that will produce divergent test vectors. F4 dispatch of S-12.02 must be gated on OQ-9 resolution to avoid implementing to a contradictory spec. | F3 | 2026-05-07 | story-writer |
| D-349 | OQ-9 resolved — VP-071 v1.0 specified deprecated advisory-block-mode pattern (`HookResult::Continue` + stdout outcome line); BC-4.10.001 specified canonical `block_with_fix(hook, reason, recommendation, code)` form. Authoritative source HOST_ABI.md lines 168-170 confirms advisory pattern is restricted to the 3 named v1.0 plugins (handoff-validator, pr-manager-completion-guard, validate-pr-review-posted) and lines 421-460 establish `block_with_fix` as the canonical form for new WASM hooks (active migration per slices 1+2 commits c94f8bf, 4cf59bc). VP-071 amended to v1.1 with kani harness updated: 4 block-path proofs now assert `BlockWithFix { .. }` not `Continue`; new proof E verifies `block_with_fix` fields are non-empty. F4 entry gate clear. | User chose Path A: quick fix-PR delivery to reconcile VP-071 to match BC-4.10.001. BC-4.10.001 is authoritative (canonical form per HOST_ABI.md). The advisory-block-mode pattern is restricted to v1.0 legacy plugins; new WASM hooks adopt block_with_fix per the active Tier A migration. VP amendment (not BC amendment) is the correct direction since BC-4.10.001 correctly specified the canonical form. | F3→F4 | 2026-05-07 | user + architect |
| D-350 | S-13.01 (Path Governance Bundle, E-13) merged to develop at 2c97cb0 via PR #97 on 2026-05-07. validate-artifact-path WASM hook live in block mode on develop. relocate-artifact preflight confirmed zero violations during Step 4 Chunk 6. 26 files delivered (7 new + 18 modified + 1 build output). Test gate: 50/50 cargo + 11/11 relocate-artifact bats + 28/28 vp-072 bats green at merge. State updated: sprint-state.yaml S-13.01 → completed; STORY-INDEX v2.25→v2.26 (S-13.01 draft→completed); STATE.md current_step + Phase Progress updated to reflect merge. | S-13.01 was the first delivery in the engine-discipline-pass-1 cycle and the path governance prerequisite for S-12.01 and S-12.02. Merge gate confirmed all test suites green; zero path violations in preflight. State bookkeeping captured atomically per TD-VSDD-053 single-commit burst. | F4 | 2026-05-07 | state-manager |
| D-351 | Cycle structural milestone — first cycle delivery completed. S-12.01 (per-story adversary workflow, E-12) and S-12.02 (per-story adversary convergence WASM hook, E-12) are now unblocked from the path governance dependency (S-13.01 merged). Linear delivery chain active: S-13.01 complete → S-12.01 in-flight (Step 5) → S-12.02 in-flight (Step 4→5). validate-artifact-path hook is now a live enforcement gate on develop; any subsequent agent creating files in unregistered paths will be blocked immediately per OQ5 resolution (D-341). | The path governance hook being live before S-12.01 and S-12.02 are delivered was the key sequencing constraint (D-347). With S-13.01 merged, the safety mechanism is in place. S-12.01 and S-12.02 can now proceed without risk of path-invention errors being silently introduced during their implementation. | F4 | 2026-05-07 | orchestrator |
| D-352 | S-12.01 merged via PR #98 at 2e9b670 on 2026-05-07. Per-story adversary workflow + agent doc updates live. 31/31 bats green. CLEAN security review. 1-cycle pr-reviewer convergence. | S-12.01 was the second story in the engine-discipline-pass-1 delivery chain. Per-story-delivery.md now has Step 4.5 (per-story adversary convergence loop). Agent docs (orchestrator.md, adversary.md, wave-gate.md) reconciled. CLEAN security review confirms no sensitive data leakage in workflow changes. 1-cycle convergence validates the agent doc changes were well-scoped. | F4 | 2026-05-07 | state-manager |
| D-353 | S-12.02 merged via PR #99 at e2fd3d4 on 2026-05-07. validate-per-story-adversary-convergence WASM hook live (148KB, SubagentStop priority 960). 30/30 cargo + 11+1skip bats green. Required merge-conflict resolution against develop (Cargo.toml workspace members + Cargo.lock regenerated + hooks-registry.toml auto-merged) — resolution at 7100431. | S-12.02 was the third and final story in the cycle delivery chain. The WASM hook enforces per-story convergence at SubagentStop, gating wave-gate dispatch if any story has not reached NITPICK_ONLY status. The 148KB binary is within WASM size budget. The merge conflict arose because develop had moved forward from S-12.01 merge while S-12.02 branch was still in review; conflict resolution was straightforward (Cargo workspace additions + lock regeneration). | F4 | 2026-05-07 | state-manager |
| D-354 | F4 COMPLETE for cycle v1.0-feature-engine-discipline-pass-1. All 3 cycle stories shipped (S-13.01 → S-12.01 → S-12.02). 11 PR commits across the cycle (97, 98, 99). Engine discipline now codified end-to-end: artifact path governance + per-story adversarial convergence + three-perimeter scope contract. Bootstrap exception acknowledged: the 3 stories of THIS cycle did not themselves go through Step 4.5 (chicken-and-egg); future stories from 2026-05-07T05:30Z onward inherit the new workflow. | The bootstrap exception is intentional and documented: the convergence enforcement hook (S-12.02) could not enforce Step 4.5 on the stories that created it, since it was not yet merged when those stories were authored and delivered. This is a one-time exception. All future stories are subject to the full Step 4.5 loop from story start. | F4 | 2026-05-07 | orchestrator |
| D-355 | Next phase: F5 scoped adversarial review on cycle delta. Entry conditions met: all stories merged, all tests green at merge, no open spec contradictions (OQ-9 resolved D-349). F5 scope includes: 6 BCs (BC-4.10.001/002, BC-4.11.001, BC-5.39.001/002, BC-6.22.001), 2 ADRs (ADR-016, ADR-017), 4 VPs (VP-069..VP-072), per-story-delivery.md Step 4.5 additions, adversary.md three-perimeter scope contract, wave-gate Gate 3 narrowing, orchestrator.md MANDATORY STEPS reconciliation, validate-artifact-path WASM hook implementation, validate-per-story-adversary-convergence WASM hook implementation. | F5 is the scoped adversarial review on the cycle delta — all artifacts introduced or modified during this cycle. Entry conditions are clean: all 3 stories merged with green tests, OQ-9 resolved, no open blockers. The adversarial review will apply information-asymmetry methodology to find gaps, contradictions, and missing edge cases in the cycle's spec and implementation artifacts. | F4→F5 | 2026-05-07 | orchestrator |
| D-355-AMEND | Amendment to D-355 (F5 entry): the "no open spec contradictions" claim was overstated. F5 pass-1 adversarial review (adv-cycle-pass-1.md) found that VP-071 frontmatter lines 30-31 still contained placeholder BC IDs (`BC-5.NN.002`, "see PO output") that were never resolved after F2. These were open spec inconsistencies present at F5 entry. D-355 should be read as "no open BLOCKING spec contradictions at the story/epic level (OQ-9 resolved)" — unresolved VP/BC frontmatter placeholders are a pre-F5 lint gap (F-LOW-4 / PG-3). A pre-F5 placeholder lint check has been proposed as story PG-3 to prevent recurrence. | F5 pass-1 adversarial review surfaced that VP-071 placeholder BC IDs constituted open spec inconsistencies at F5 entry, contradicting D-355's "no open spec contradictions" claim. Root cause: no automated lint gate runs before F5 to detect placeholder frontmatter strings. The D-355 claim was accurate at the OQ/story level but missed the VP frontmatter gap. Retrospective note to accurately reflect F5 entry state. | F5 | 2026-05-07 | state-manager |
| D-356 | F5 pass-1 adversarial review complete. Classification: CRITICAL. 29 findings (4 CRITICAL / 14 HIGH / 6 MEDIUM / 5 LOW). Top 3 concerns: (1) Lobster workflow references non-existent step-d5-adversary-convergence.md — dispatch will fail; (2) 3 story specs reference wrong ADR file path slug — broken story-to-decision traceability; (3) VP-071 kani harness uses HookResult::Block but spec proof harness uses HookResult::BlockWithFix — verifier tests wrong variant. Review persisted at adv-cycle-pass-1.md (65KB, 704 lines). Process-gap surfaced: adversary dispatch assumes write access contradicting read-only tool profile; state-manager persisted review from chat output. Next: route all 29 findings via fix-pr-delivery before pass-2 dispatch. Two [process-gap] observations flagged for follow-up story authorship (F5 dispatch write-access gap; per-story convergence state files missing for bootstrap cohort). | CRITICAL classification triggers mandatory fix routing per F5 protocol. All 4 CRITICAL findings block pass-2 entry. Fix-pr-delivery workflow applies: separate fix PR per finding cluster, each with security review and CI gate, then pass-2 re-dispatch for convergence. Process-gaps documented as follow-up stories per cycle-closing checklist. | F5 | 2026-05-07 | state-manager |
| D-357 | F5 pass-1 fix burst B1 spec amendments. (1) VP-071 v1.1→v1.2 corrects BlockWithFix enum-variant assumption to match actual SDK HookResult::Block (F-CRIT-3 + F-HIGH-5 + F-MED-7 redux of OQ-9). (2) BC-4.11.001 v1.0→v1.1 codifies NC-1 user-decided single-segment placeholder semantics (was 'sequence of segments'). (3) 6 BC input-hash placeholders replaced with content hash 40a6fb6 (F-LOW-5): BC-4.10.001/002, BC-4.11.001, BC-5.39.001/002, BC-6.22.001. (4) Story spec ADR-017 slug fixes in S-12.01, S-12.02, E-12 (F-CRIT-2). BC-INDEX 1.15→1.16 (BC-4.11.001 v1.1; Summary table SS-04 31→34 + total 1931→1937 corrected). VP-INDEX 1.2→1.3 (VP-071 v1.2). | Single atomic commit per TD-VSDD-053. Spec amendments correspond to the B1 implementer changes on fix/F5-B1-critical-blockers branch (PR in flight). This factory-artifacts commit lands the spec side; source/code/workflow changes are on the feature branch. The B1 source branch pre-dates this commit; the spec amendments are the authoritative source of truth alignment. | F5 | 2026-05-07 | state-manager |
| D-358 | F5 pass-1 fix burst B2 spec amendments. (1) BC-4.10.002 v1.0→v1.1 corrects PC3 from `host::log_debug` (which doesn't exist in SDK) to `host::log_info` (F-HIGH-4). (2) VP-070 v1.0→v1.1 corrects `match_path`→`matches_canonical` terminology, resolves placeholder BC IDs to BC-4.11.001, corrects type names (PathDecision→MatchResult, ArtifactRegistry→PathRegistry) (F-HIGH-10). (3) S-13.01 + S-12.02 story spec terminology aligned with implementation (F-HIGH-9, F-HIGH-12). All amendments triggered by F5 pass-1 fix-plan B2. BC-INDEX 1.16→1.17 (BC-4.10.002 v1.1). VP-INDEX 1.3→1.4 (VP-070 v1.1). | Single atomic commit per TD-VSDD-053. Spec amendments correspond to the B2 implementer changes on fix/F5-B2-spec-impl-alignment branch (PR in flight). This factory-artifacts commit lands the corresponding spec amendments on factory-artifacts. | F5 | 2026-05-07 | state-manager |
| D-359 | B6 process-gap follow-up stories committed. New epic E-14 (Engine Discipline Pass-2) created with 5 stories: S-14.01 P0 (bootstrap backfill, PG-2), S-14.02 P1 (F5 persist, PG-1), S-14.03 P2 (placeholder lint, PG-3), S-14.04 P1 (policy rubric, PG-4), S-14.05 P1 (pr-manager early-exit, PG-6 orchestrator-added). PG-2 inline backfill: 3 convergence-state.json files created for S-13.01, S-12.01, S-12.02 with bootstrap_annotation field (exception_type: cycle_self_introduction; rationale: these 3 stories introduced Step 4.5 itself — F5 pass-1 cycle-level review covered them collectively; fix_batches_completed: B1/B2/B5; fix_batches_pending: B3/B4). F7 close-out unblocked from CONVERGENCE_STATE_MISSING risk. STORY-INDEX 2.27→2.28. | Single atomic commit per TD-VSDD-053. E-14 stories address process gaps identified in F5 pass-1 fix-plan [process-gap] annotations. PG-2 was disposed Option C (inline backfill + formal story) — the inline backfill executes immediately; the formal story (S-14.01) codifies the bootstrap exception protocol so future cycles have a documented procedure. Path for convergence-state files confirmed against artifact-path-registry.yaml post-B1 fix: `.factory/cycles/{cycle-id}/{story-id}/adversary-convergence-state.json` (F-HIGH-13 registry fix merged via PR #101). | F5 | 2026-05-07 | state-manager |
| D-360 | F5 pass-2 adversarial review complete. Classification: CRITICAL. 15 findings — significant novelty decay from pass-1 (29→15). Top concerns: (1) F-P2-001 CRITICAL — convergence hook still operationally inert in production; pass-1 F-HIGH-3 'fix' added consumer plumbing but no producer (no wave-state→plugin_config wiring); (2) F-P2-002 CRITICAL — BC-4.10.001 + BC-5.39.001 VP-071 traceability rows still have deprecated 'advisory-block' wording (sibling-file regression of F-CRIT-3); (3) Multiple partial-fix gaps where pass-1 closed one occurrence but missed siblings (F-P2-003 F1 line 391, F-P2-004 S-13.01 types). Review at adv-cycle-pass-2.md. Next: human review of F-P2-001 fix architecture before pass-2 fix burst. | CRITICAL classification again — convergence loop still requires further fix routing. F-P2-001 is an architectural gap (not a documentation error): the convergence hook reads plugin_config but nothing writes the wave-state→plugin_config pipeline; fix options involve either wiring wave-gate state into the hook configuration at dispatch time or redesigning the hook to read wave-state directly. Human review needed before fix burst to avoid rework. 3 consecutive NITPICK_ONLY passes required for convergence; pass-2 CRITICAL means at minimum 5 more passes assuming immediate convergence after this fix burst. | F5 | 2026-05-07 | state-manager |
| D-361 | MID-CYCLE SCOPE EXPANSION — WASM-plugin Context Resolver platform. User-authorized 2026-05-07 mid-cycle scope expansion of cycle `v1.0-feature-engine-discipline-pass-1` to include a generic, factory-agnostic, WASM-plugin-based context-resolver platform. Triggering finding: F-P2-001 (validate-per-story-adversary-convergence operationally inert in production despite pass-1 F-HIGH-3 "fix"; consumer plumbing added in B3 but no producer populates plugin_config.stories at dispatch). Architectural choice (user-decided): γ-generic with WASM-plugin resolvers. Rejected alternatives: α (hook reads wave-state.yaml directly — concern conflation, doesn't generalize); β (skill writes manifest — side-channel coordination); γ-compile-time (factory-specific resolvers compiled into dispatcher — doesn't generalize across factories). Generality requirement (user-directed): the dispatcher must be agnostic and usable by ALL factory types (vsdd-factory, future content-factory, future PR-review-factory, etc.). Per-factory resolvers ship in separate per-factory crates. Dispatcher core knows nothing about wave/story/cycle/article/PR vocabulary. Cycle structure (orchestrator-recommended, user-approved): full structured mid-cycle amendment. F1-amendment (delta analysis) → F2-amendment (new BCs/ADR/VPs/PRD-FR) → F3-amendment (5-7 new stories under existing E-12 Engine Governance) → F4 platform delivery (per-story TDD, with the cycle's own Step 4.5 active — bootstrap pattern flipping right-side-up) → F5 resumption (F-P2-001 closes via new platform; remaining 14 pass-2 findings fix burst; passes-3+ until 3 consecutive NITPICK_ONLY). Acknowledged scope reality: original cycle scope was 3 stories (S-12.01, S-12.02, S-13.01). Amended scope adds ~6 stories + a new per-factory crate + extensions to dispatcher, hook-sdk, hooks-registry schema, and HOST_ABI documentation. Cycle effort approx 3-4x original. No corner-cutting: the platform is treated as a properly-designed architectural addition with full F2/F3 spec/story discipline, not crammed into the F5 fix-burst as a hot-fix. First concrete resolver: WaveContextResolver (new vsdd-context-resolvers crate). Future factory deployments would ship their own resolver crates (ArticleContextResolver, PRContextResolver, etc.) without touching dispatcher core. Next: dispatch architect for F1-amendment delta analysis (in progress in parallel with this commit). | User authorized 2026-05-07. F-P2-001 is an architectural gap: the convergence hook reads plugin_config.stories but nothing in the dispatch pipeline writes it. Three architectural options considered: α reads wave-state directly (conflation, non-generalizable), β uses skill-written manifest (side-channel coordination), γ-generic WASM-plugin resolvers (user choice — factory-agnostic, sandboxed, generalizable). The γ-generic choice requires a structured amendment cycle (F1/F2/F3/F4) rather than a hot-fix, which is correct: doing it right now avoids α-stopgap technical debt and makes the dispatcher usable across all factory types without modification. Step 4.5 (the convergence loop introduced in S-12.02) will be active during F4 platform delivery, which closes the bootstrap irony: the new stories that build the context resolver will themselves go through the per-story convergence loop that depends on the context resolver — but the loop degrades gracefully when the resolver is absent (reads empty plugin_config), so delivery is feasible. | F5 | 2026-05-07 | user + orchestrator |
| D-362 | F2-amendment specs produced. 6 new BCs codify WASM-plugin Context Resolver platform: resolver-registry loading + pre-dispatch context injection (BC-1.13.001 SS-01); resolver lifecycle invariant (BC-4.12.001 SS-04, mtime-cache load-at-startup per OD-1); resolver ABI/payload schema (BC-4.12.002 SS-04, distinct ResolverInput/ResolverOutput types per OD-3, RESOLVER_ABI_VERSION=1); resolver capability model (BC-4.12.003 SS-04, path_allow per resolver in resolvers-registry.toml per OD-2); resolver error/crash isolation (BC-4.12.004 SS-04); context-injection merging contract (BC-4.12.005 SS-04). ADR-018 codifies all 6 OD-N decisions with rejected alternatives. 4 VPs cover load purity, error isolation, injection determinism, capability confinement. PRD FR-048 added. Critical pre-condition encoded: BC-1.13.001 PC1 + Inv-2 explicitly state absent resolvers-registry.toml = zero resolvers configured (NOT startup error). | F2-amendment integration burst executed as single atomic commit per TD-VSDD-053. All index updates, spec files, and state records committed atomically. | F2 | 2026-05-07 | product-owner + architect + state-manager |
| D-363 | F-P2-002 pre-flight fix applied (sibling regression of pass-1 F-CRIT-3). BC-4.10.001 v1.0→v1.1 and BC-5.39.001 v1.0→v1.1: VP-071 traceability row description aligned to v1.2 canonical 'Block Invariant — kani harness verifies HookResult::Block on non-converged input'. Both BCs now consistent with VP-071 v1.2. | F-P2-002 was a sibling-file regression: when VP-071 was corrected in F5 pass-1 B1 (D-357), the traceability row text in BC-4.10.001 and BC-5.39.001 was not simultaneously updated. The F2-amendment integration burst provides the correct atomic commit window to apply this fix alongside the new F2 specs. | F2 | 2026-05-07 | state-manager |
| D-364 | F2-amendment scope-correction in PRD: overview BC count was stale at 1,911 (pre-E-10 amendments); corrected to 1,943 alongside FR-048 delta. FR count §2 preamble was stale at 45; corrected to 48. Per architect anomaly G-2/G-3 — opportunistic propagation, not a separate spec amendment. | PRD §1 overview text contained stale counts from before E-10 BC authorship (D-313/D-321). With the F2-amendment bumping version 1.1→1.2, the opportunistic correction avoids carrying stale counts into the next adversary pass. Propagation sweep confirmed only PRD was stale (ARCH-INDEX, BC-INDEX counts already corrected). | F2 | 2026-05-07 | state-manager |
| D-365 | Next: F3-amendment dispatches story-writer to author S-12.03 through S-12.08 under E-12. Architect F1-amendment recommended dependency chain: S-12.06 (HOST_ABI docs) → S-12.03+S-12.05 parallel (trait + SDK) → S-12.04 (WASM loading) → S-12.07 (vsdd-context-resolvers crate) → S-12.08 (F-P2-001 migration). | F3-amendment is the next phase of the mid-cycle amendment cycle (D-361). The dependency chain ensures HOST_ABI documentation is established before the Rust trait/SDK work begins, and the vsdd-context-resolvers crate is complete before the migration story (S-12.08) that wires WaveContextResolver into production. Step 4.5 (per-story adversary convergence loop) will be active for all 6 new stories from S-12.03 onward. | F2→F3 | 2026-05-07 | orchestrator |
| D-366 | F3-amendment stories authored. 6 new platform stories under E-12: S-12.03 (ContextResolver trait + ResolverRegistry, generic dispatcher core, 12 ACs), S-12.04 (WASM resolver loading + lifecycle + error isolation, 13 ACs), S-12.05 (hook-sdk resolver-authoring extensions, 10 ACs), S-12.06 (HOST_ABI context-injection contract docs, factory-agnostic, ships first, 10 ACs), S-12.07 (vsdd-context-resolvers crate with first concrete resolver WaveContextResolver, 10 ACs), S-12.08 (migrate validate-per-story-adversary-convergence to consume plugin_config.wave_context.stories, closes F-P2-001, 10 ACs). 65 ACs total. All tdd_mode strict. STORY-INDEX 2.28→2.29 (90 stories). E-12 epic v1.0→v1.1 (story_count 2→8; scope widened to engine governance platform; new BCs/ADR/VPs anchored). | Single atomic commit per TD-VSDD-053. 6 story files + STORY-INDEX + E-12 epic + STATE.md + decision-log committed atomically. | F3 | 2026-05-07 | state-manager |
| D-367 | Bootstrap pattern flipping right-side-up: S-12.03 through S-12.08 are the FIRST stories in cycle history to be subject to Step 4.5 per-story adversary convergence in their delivery (S-13.01/S-12.01/S-12.02 were authored before Step 4.5 existed; D-354 bootstrap exception). The cycle's own infrastructure now governs its later stories — engine discipline applied to engine itself. | Step 4.5 was introduced by S-12.01 (merged 2026-05-07). The first 3 cycle stories (S-13.01, S-12.01, S-12.02) could not be subject to Step 4.5 because they authored it (bootstrap exception, D-354). S-12.03 onward are the first stories where the convergence loop is available AND mandatory. The engine now eats its own cooking from the first story of F4-platform delivery. | F3 | 2026-05-07 | orchestrator |
| D-368 | E-12 epic scope widened from 'per-story adversary workflow' (original 2 stories) to 'engine governance platform' (8 stories: original 2 + 6 platform expansion). Anchored artifacts updated to include 6 new BCs (BC-1.13.001 SS-01 + BC-4.12.001-005 SS-04) + ADR-018 + 4 VPs (VP-073-076). subsystems_affected expanded to include SS-01. | E-12 was originally scoped to the per-story adversarial convergence loop only (2 stories, SS-04 + SS-05). The F3-amendment adds the WASM-plugin Context Resolver platform (D-361 authorization), which primarily lands in SS-01 (dispatcher pre-dispatch injection) and SS-04 (resolver lifecycle, ABI, capability). The epic title is amended to reflect the full platform scope, not just the convergence loop. | F3 | 2026-05-07 | state-manager |
| D-369 | Next: F4-platform delivery. Order per dependency graph: S-12.06 first (foundation, doc-only; HOST_ABI context-injection contract); then S-12.03 + S-12.05 in parallel (ContextResolver trait + hook-sdk extensions); then S-12.04 (WASM loading + lifecycle); then S-12.07 (vsdd-context-resolvers crate + WaveContextResolver); then S-12.08 (closes F-P2-001 → enables F5 pass-2 fix burst resumption for the 14 remaining pass-2 findings + pass-3+ adversary cycles to convergence). | S-12.06 is a documentation-only story (HOST_ABI additions) and is the shortest path to unblocking the parallel S-12.03 + S-12.05 tracks. S-12.08 is the critical-path terminus: it wires WaveContextResolver into the convergence hook so F-P2-001 is closed in production, which is the precondition for F5 resumption. | F3→F4 | 2026-05-07 | orchestrator |
| D-370 | S-12.06 per-story adversary pass-1 complete. Classification LOW. 5 within-story findings (4 LOW + 1 NITPICK_ONLY). 3 deferred findings (1 architectural BC↔ADR drift to wave-gate, 1 system-level BC consolidation to wave-gate, 1 integration concurrency-model docs to phase-5). Per-story convergence state file initialized at .factory/cycles/v1.0-feature-engine-discipline-pass-1/S-12.06/adversary-convergence-state.json. Pass-2 dispatched after orchestrator routes the 4 LOW fixes through implementer. | S-12.06 is the first real application of Step 4.5 in cycle history (the cycle's own infrastructure now governs its own later stories — D-367 bootstrap pattern flipping right-side-up). LOW classification means passes_clean=0; convergence counter does not increment until a NITPICK_ONLY pass. The 4 LOW findings include: (1) bats test filename drift from story spec (breaks DoD literal invocation); (2) BC-4.12.002 INV4/OD-5 no inter-resolver dependencies invariant missing from section prose; (3) BC-4.12.001 PC6 fail_closed registry field undocumented in registration table; (4) three undocumented HOST_ABI functions in resolver context (load_resolver, reload_all, context_snapshot). NITPICK: vsdd-hook-sdk crate name references are structurally unavoidable (not a true domain leak). 3 deferred findings captured in convergence state: BC-4.12.005 PC4 vs ADR-018 collision-order contradiction (architectural, wave-gate target); BC-4.12.003 PC2 vs BC-4.12.004 PC2 duplicate event naming (system-level, wave-gate target); concurrency model silent on resolver invocation (integration, phase-5 target). | F4 | 2026-05-07 | state-manager |
| D-371 | S-12.06 per-story adversary pass-2 complete. Classification LOW. 3 within-story findings (2 LOW + 1 NITPICK_ONLY). Novelty decay: pass-1 had 5 within-story findings, pass-2 has 3 — no regressions, all pass-1 LOWs CLOSED. New findings: HOST_ABI internal log function naming inconsistency (LOW), event_type enumeration unspecified (LOW), name vs context_key terminology drift (NITPICK_ONLY). 1 new deferred finding (BC↔HOST_ABI log API). passes_clean stays at 0 (LOW != NITPICK_ONLY). Pass-3 dispatched after pass-2 fix burst. | LOW classification means passes_clean remains 0; convergence counter does not increment until a NITPICK_ONLY pass. The 2 new LOW findings: (1) HOST_ABI.md Capability Model table cites host::log_info/log_warn/log_error (3 functions) but the existing host-functions section defines only log(level,...) — internal inconsistency within the same file; (2) event_type field example uses "SubagentStop" without specifying the allowed value space. NITPICK: name vs context_key terminology drift is implicit-equivalent per BC-4.12.005 PC6 but a parenthetical would make it explicit. 4th deferred finding added to convergence state: BC-4.12.003 PC4 host log triplet vs HOST_ABI single log() API — wave-gate to adjudicate whether BC should be amended after implementer chooses SDK wrapper or single-export model. Review persisted at .factory/cycles/v1.0-feature-engine-discipline-pass-1/S-12.06/adversary-pass-2.md. | F4 | 2026-05-07 | state-manager |
| D-372 | S-12.06 per-story adversary pass-3 complete. Classification LOW. 2 within-story findings (1 LOW + 1 NITPICK). Novelty decay continues: pass-1 (5) → pass-2 (3) → pass-3 (2). 1 NEW LOW (resolver.capability_denied 3-field BC mandate vs 2-field doc), 1 NEW NITPICK (dispatcher log entry side-effect documentation). 1 new deferred (BC-4.12.001 INV3/PC4 to phase-5/S-12.04). passes_clean stays at 0. Pass-4 dispatched after fix burst. | LOW classification (1 LOW finding present) means passes_clean does not increment. The 1 LOW: resolver.capability_denied event documented with only 2 of 3 BC-4.12.003 PC2 mandated fields — omits "resolved path that was attempted" (distinct from denied path post-canonicalization); S-12.04 implementer reading HOST_ABI.md as canonical reference would emit a 2-field event, weakening security telemetry and producing a downstream finding. NITPICK: BC-4.12.004 PC7 dispatcher log-entry side-effect on resolver failure not documented in Error and Crash Isolation subsection — minor, at author discretion. 5th deferred added: BC-4.12.001 INV3 (same Engine instance) + PC4 (determinism) not surfaced in HOST_ABI section — dispatcher-impl territory, flagged for S-12.04 author to read BC-4.12.001 directly. Review persisted at .factory/cycles/v1.0-feature-engine-discipline-pass-1/S-12.06/adversary-pass-3.md. | F4 | 2026-05-07 | state-manager |
| D-373 | S-12.06 per-story adversary pass-4 NITPICK_ONLY. passes_clean increments 0→1 (first clean pass). Novelty trajectory: pass-1 (5) → pass-2 (3) → pass-3 (2) → pass-4 (0). Adversary explicitly verified pass-3 fixes are present and BC-source-of-truth correct. No regressions. No within-story content gaps. Need 2 more consecutive NITPICK_ONLY passes (pass-5, pass-6) for convergence. | NITPICK_ONLY classification — first pass with zero within-story findings. Both pass-3 fixes verified: (1) resolver.capability_denied 3-field documentation at HOST_ABI.md:723-734 matches BC-4.12.003 PC2 exactly; (2) Dispatcher log entry side-effect at HOST_ABI.md:768 matches BC-4.12.004 PC7 exactly. One pass-4 candidate (BC-4.12.001 PC5 startup log "Compiled N resolver modules") demoted symmetrically per pass-3 classification rule (dispatcher-internal observability, same category as pass-3 INV3/PC4 deferral) — folded into DEFER-5 in convergence-state.json. 5 deferred findings unchanged. Review persisted at .factory/cycles/v1.0-feature-engine-discipline-pass-1/S-12.06/adversary-pass-4.md. | F4 | 2026-05-07 | state-manager |
| D-374 | S-12.06 per-story adversary pass-5 NITPICK_ONLY. passes_clean increments 1→2 (second consecutive clean pass). Pass-5 found 2 new sub-NITPICK findings: (1) story spec line 275 verification command typo `resolver_impl` → `resolve_impl` (bats test uses correct token; CI gate unaffected); (2) bats header line 23 `EC-004 -> BC-4.12.005 EC-005` orphan mapping (test scope absorbed by AC-006e). Both classified NITPICK_ONLY, doc-annotation level, not gating convergence per adversary explicit recommendation. Pass-6 confirms steady state for convergence. | NITPICK_ONLY classification — second consecutive clean pass. Novelty trajectory: pass-1 (5) → pass-2 (3) → pass-3 (2) → pass-4 (0) → pass-5 (2 NITPICK_ONLY, no LOW+). Both pass-5 NITPICKs are genuinely novel (not raised in prior passes) but are documentation-annotation level with no implementation or CI-gate impact. The section, BC↔HOST_ABI surface, and bats test gates remain mutually consistent and complete. Adversary recommended not gating convergence on either finding. 5 deferred findings carry forward unchanged. Review persisted at .factory/cycles/v1.0-feature-engine-discipline-pass-1/S-12.06/adversary-pass-5.md. | F4 | 2026-05-07 | state-manager |
| D-375 | S-12.06 PER-STORY ADVERSARY CONVERGENCE REACHED (3 consecutive NITPICK_ONLY passes). Pass-6 NITPICK_ONLY. passes_clean transitions 2→3. CONVERGED. Decay trajectory across 6 passes: 5 → 3 → 2 → 0 → 0 → 0. Three independent clean angles (pass-4 lifecycle/ABI/capability/error subsections, pass-5 typo/header annotation, pass-6 PC5/PC6/PC7/cross-ref/factory-agnostic-grep replication) all returned zero within-story findings. S-12.06 is the FIRST story in cycle history to complete Step 4.5 per-story adversary convergence (the cycle's own infrastructure governing its own story — bootstrap pattern flipping right-side-up). 5 deferred findings (DEFER-1 through DEFER-5) remain routed to wave-gate or phase-5; none are within-story-scope. S-12.06 advances to Step 5 (demo recording). | Three independent angles across passes 4/5/6 confirm BC-source-of-truth coherence with all 6 anchored BCs (BC-1.13.001 + BC-4.12.001-005) and ADR-018. The §Context Injection Contract section in HOST_ABI.md has reached within-story convergence. Per D-354 bootstrap exception, S-13.01/S-12.01/S-12.02 authored Step 4.5 itself and carried the exception; S-12.06 is the first story that ran Step 4.5 normally. convergence_reached: true, convergence_date: 2026-05-07 recorded in adversary-convergence-state.json. | F4 | 2026-05-07 | state-manager |
| D-376 | S-12.06 (HOST_ABI Context Injection Contract) MERGED via PR #105 at 15432c6 on 2026-05-07. First platform story of E-12 expansion. First story in cycle history to complete Step 4.5 per-story adversary convergence (3 consecutive NITPICK_ONLY passes; decay 5→3→2→0→0→0 across 6 passes). 5 deferred findings (DEFER-1-5) routed to wave-gate / phase-5 (BC↔ADR collision-order, BC↔BC capability-denied event-naming, concurrency model, BC-4.12.001 INV3/PC4 dispatcher-internal, BC-4.12.003 PC4 host log triplet). Section is 393 lines, factory-agnostic, BC-source-of-truth coherent. Next: S-12.03 + S-12.05 in parallel (both unblocked by S-12.06's foundation). | S-12.06 merge closes the foundational doc layer of the E-12 platform expansion. S-12.03 (ContextResolver trait + ResolverRegistry) and S-12.05 (hook-sdk resolver extensions) are now unblocked and can proceed in parallel. sprint-state.yaml + STORY-INDEX 2.29→2.30 + STATE.md + decision-log updated atomically per TD-VSDD-053. | F4 | 2026-05-07 | state-manager |
| D-377 | Session-resume checkpoint document authored for compact-session handoff. SESSION-CHECKPOINT.md written at `.factory/cycles/v1.0-feature-engine-discipline-pass-1/SESSION-CHECKPOINT.md` (comprehensive; captures full cycle state for fresh-session resumption without conversation re-read). Document covers: γ-generic WASM-plugin resolver architecture (OD-1 through OD-6 all resolved), 9 PRs merged, 4 of 9 stories delivered (S-13.01 + S-12.01 + S-12.02 + S-12.06), 2 stories in-flight locally (S-12.03 at 7f37f5a; S-12.05 at bbc936e; both NOT PUSHED), 3 stories not started (S-12.04, S-12.07, S-12.08), F5 pass-2 CRITICAL (15 findings; F-P2-002 pre-fixed; 14 pending; fix burst blocked on S-12.08), no pass-2 architect-triage authored yet, E-14 5 process-gap stories, plugin-async-semantics concurrent cycle at F1 awaiting human gate, estimated 32-55 hours remaining agent work to close cycle. | Compact-session handoff. Fresh session reads SESSION-CHECKPOINT.md and immediately understands state without re-reading conversation. D-377 is the final decision of this session. | F4 | 2026-05-07 | state-manager |
| D-378 | F-P5-003 triage: disposition of 9 accumulated MEDIUM findings from passes 2–3. F-P2-003 (F1-delta-analysis.md line 391 stale "convergence tracker" alias): CLOSE — verify and fix if still present in source; if gone, mark closed. F-P2-004 (S-13.01 terminology "sequence of segments" vs single-segment): CLOSE — BC-4.11.001 v1.1 (D-357) already corrected this; verify S-13.01 story body reflects v1.1 terminology. F-P2-007 (VP-072 line 48 "wave_context not in scope" comment stale): WON'T FIX — VP-072 scope note is accurate for the VP's original context; comment is advisory, not a spec violation. F-P2-009 (BC-4.12.001 invariant numbering gap INV2→INV4): FOLLOW-UP STORY — file as S-14.xx cleanup under E-14; not a runtime bug. F-P2-010 (HOST_ABI resolver-input schema missing error-response field): FOLLOW-UP STORY — file as S-14.xx under E-14; requires HOST_ABI amendment + BC-4.12.002 update. F-P2-011 (ADR-018 §Implementation Plan stale after S-12.06 merge): CLOSE — add note "S-12.06 merged 2026-05-07" in ADR-018 Implementation Plan to mark first story complete. F-P3-009 (forward-compat test for unknown resolver output fields): CLOSE — add unit test in vsdd-context-resolvers asserting unknown fields in ResolverOutput are ignored (serde flatten or deny_unknown_fields). F-P3-010 (non-UTF8 path rejection test): WON'T FIX — requires WAT fixture construction disproportionately complex for MEDIUM finding; defer indefinitely; document here as won't-fix. F-P3-011 (plugin.log observability for resolver load events): FOLLOW-UP STORY — file as S-14.xx under E-14; requires extending bats to grep dispatcher-internal log for resolver.loaded events. | F-P5-003 triage policy: CLOSE when fix is trivial or already done; WON'T FIX when complexity exceeds finding severity; FOLLOW-UP STORY when the fix requires implementation work that is not in scope of F-P5. Three WON'T FIX items are documented here as the authoritative record; they need not recur in future adversarial passes for this cycle. | F5 | 2026-05-10 | implementer |
| D-379 | CI-green-signal rule for CRITICAL CI-class finding closures. Rule: when closing any CRITICAL finding tagged as CI-class (touches .github/workflows/, ci.yml, release.yml, or any test infrastructure that affects CI behavior), the implementer MUST demonstrate a CI green signal on a PR branch BEFORE recording the finding as CLOSED. Closure record (commit message OR decision-log entry) MUST include a CI run URL showing GREEN status. Closures lacking the CI-green link are subject to immediate reopening. Initial application: F-P6-001 closure MUST cite a CI-green URL on the F5-pass-3 PR. | Rationale: F-P2-001 → F-P3-001 → F-P4-002 → F-P5-001 → F-P6-001 documents a 5-pass recurrence of fix-burst-introduced CI false-greens. The pass-5 review (F-P5-008) identified the pattern and recommended this rule. The pass-6 review (F-P6-001/F-P6-007) confirmed the rule was authored but operationally inert — the pass-5 fix burst itself violated the advisory because no CI-green verification was performed before declaring F-P5-001 CLOSED. D-379 codifies the rule with enforcement teeth: the CI-green URL is the closure evidence, not the implementer's declaration. | F5 | 2026-05-10 | implementer |
| D-380 | D-379 initial application: F-P6-001 CLOSED. CI run https://github.com/drbothen/vsdd-factory/actions/runs/25651192161 on PR #124 (feature/F5-pass-3-cycle-hardening) shows cargo-host (macos-latest) PASS (12m58s) and cargo-host (ubuntu-latest) PASS (16m36s). These are the two CI jobs that exercise perf-baseline.bats with the WASM staging order fix. The macos run required a second fix (ae4778c4): F-P3-008 upper bound 3000ms → 8000ms due to loaded runner variance (observed 5382ms). First CI run (25650765890) failed on macOS with "TIMEOUT EXCEEDED: dispatch took 5382ms (threshold 3000ms)"; second run (25651192161) passed after the bound was raised. F-P6-001 is now CLOSED with CI evidence per D-379. | D-379 enforcement: this record IS the CI-green URL required by D-379. The finding F-P6-001 (WASM staging step positioned after perf-baseline.bats — false-green CI vector) is closed. The staging step now runs BEFORE perf-baseline.bats AND before resolver-integration.bats/capability-confinement.bats/E-8-integration in the cargo-host CI job. CI run 25651192161 is the authoritative verification. | F5 | 2026-05-11 | implementer |
| D-381 | Adversary fix-burst MUST update STATE.md sibling fields. Rule: every adversary fix-burst (cycle-level OR per-story) MUST update STATE.md's current_step, phase, Phase Progress, and Session Resume Checkpoint fields to reflect the post-burst state. Pass-N closure is incomplete until STATE.md is consistent with the new pass-N+1 baseline. Closures lacking the STATE.md update are subject to reopening. Initial application: F-P8-003 closure in the same fix burst as this decision. Lint rule TBD (see S-15.03 scope expansion). | F-P8-003 found STATE.md stale across 6 consecutive passes (passes 3-8 missed the update). A fresh session resuming from STATE.md would re-dispatch pass-3, duplicating work. This is the same sibling-file-discipline gap that L-P20-002 codified for ARCH-INDEX but STATE.md was not in L-P20-002 scope. D-381 explicitly extends sibling-file discipline to STATE.md. | F5 pass-8 | 2026-05-11 | state-manager |
| D-382 | Cycle-level sibling-file discipline (extends D-381 + L-P20-002). Rule: every adversary fix-burst (cycle-level OR per-story) MUST update ALL cycle-level state-tracking sibling files. The complete mandatory set is: (1) STATE.md — current_step, phase, Phase Progress, Session Resume Checkpoint per D-381; (2) cycles/<cycle>/burst-log.md — append burst narrative per state-manager.md line 134; (3) cycles/<cycle>/INDEX.md — Adversarial Reviews table, add new pass row; (4) cycles/<cycle>/lessons.md — append when a new lesson is identified per state-manager.md line 136; (5) cycles/<cycle>/decision-log.md — append if new decision authored. Pass-N closure is incomplete until ALL applicable sibling files are updated. Closures missing any file from the set are subject to reopening. Enforcement: closure record (commit message OR fix burst summary) MUST list which sibling files were updated. Future lint hook (S-15.03) MUST verify all sibling files were touched if STATE.md was touched in the burst. Initial application: F-P9-001 closure in this same fix burst — updates burst-log.md (pass-8 entry + pass-9 entry) + INDEX.md (passes 3-9 table rows) + STATE.md + decision-log.md (this entry). Rationale: F-P9-001 demonstrated that D-381's narrow scope (STATE.md only) allowed the pass-8 fix burst — which authored D-381 — to simultaneously violate the discipline class for burst-log.md and INDEX.md. Enumerating the full set eliminates the "not in scope" omission path. | F-P9-002 finding; F-P9-001 immediate application. D-381 codified STATE.md; D-382 completes the sibling-file enumeration. POLICY 1: D-381 is not amended; D-382 extends it. | F5 pass-9 | 2026-05-11 | state-manager |
| D-383 | Intra-file content audit + sibling-pattern sweep (extends D-382). Rule: every adversary fix-burst MUST apply BOTH: (1) File-touch discipline (D-382): update all applicable cycle-level sibling files. (2) Intra-file content audit (NEW): within EACH touched file, verify: (a) arithmetic consistency — stated totals match breakdown sums; (b) stale-phrase scan — "passes 3-N" citations, version strings, verdict labels match current state; (c) cross-reference verification — forward/backward references resolve correctly, no retroactive annotations added to immutable rows. (3) Sibling-pattern sweep (NEW): when fixing a finding of a specific defect class (e.g., row ordering, missing entry, stale citation), audit ALL same-class sites in the affected file, not just the cited instance. Enforcement: closure record MUST attest intra-file content audit was performed on each touched file. Sibling-pattern sweep MUST be cited for any pattern-class finding. Lint rule S-15.03 expanded scope to mechanize. Rationale: F-P10-001 through F-P10-006 demonstrate that the pass-9 fix burst satisfied D-382 file-touch enforcement but introduced 4 content defects within the touched files. F-P10-002 demonstrates F-P9-005 row-reordering was applied narrowly (D-379..D-382 range only), missing the D-377/D-378 inversion in the adjacent range. F-P10-005 demonstrates F-P9-006 retroactive-annotation removal was asymmetric (removed from burst-log but introduced in D-381 NOTE). The L-EDP1-003 recursive-violation pattern migrated one layer: from "missed file" to "touched file with content defect" / "fix applied narrowly within file". Initial application: this fix burst (F-P10-001..006) applies intra-file content audit on INDEX.md (row arithmetic + Convergence Status), decision-log.md (row ordering full sweep + retroactive-reference scan + D-383 append), STATE.md (phase + current_step + trajectory), lessons.md (Source line completeness). | F-P10-008 process-gap finding. D-382 mandates file-touch; D-383 mandates content correctness within each touched file. POLICY 1: D-382 is not amended; D-383 extends it. | F5 pass-10 | 2026-05-11 | state-manager |
| D-384 | D-383 enforcement clarifications: self-referential N clause, cardinality cross-check, attestation specificity (extends D-383). Rule: D-383 (intra-file content audit + sibling-pattern sweep) is extended with three sub-rules that the pass-10 fix burst violated in its own initial application: (1) Self-referential pass-N clause (F-P11-005): when a fix burst writes a "passes 3-N" phrase, N MUST equal the CURRENT burst's pass number, not the prior pass. Pass-10's "passes 3-9" was wrong because pass-10 was itself the current burst; the phrase should have been "passes 3-10". (2) External trajectory cardinality cross-check (F-P11-006): when a file contains BOTH per-row counts (e.g., INDEX.md Adversarial Reviews table rows) AND a trajectory shorthand (e.g., 29→15→11→…), the audit MUST verify: (a) cardinality: trajectory value count == total pass count; (b) per-position match: each value at position N == per-row count for pass N. The pass-10 burst applied the D-383 arithmetic check to row counts but NOT to trajectory shorthand cardinality. (3) Audit attestation specificity (F-P11-007): closure records (commit messages + burst-log entries) MUST cite specific phrases/values verified with pre/post pairs, NOT category-level claims. "Convergence Status stale-phrase scan performed" is a category claim; a compliant attestation names exact phrases and pre/post values. Enforcement: all three sub-rules apply to every fix burst that touches INDEX.md or any file containing a trajectory shorthand. Rationale: F-P11-001 + F-P11-002 demonstrate L-EDP1-003 (recursive discipline violation) recurred at the D-383 layer: the pass-10 burst codified D-383 with three sub-rules but applied them partially. D-384 closes the L-EDP1-003 recursive pattern at the D-383 layer. Initial application — this burst (F-P11-001..007 closure) attests: trajectory pre: "29→15→11→9→9→8→7→5→6→6→6" (11 values, stale); trajectory post: "29→15→11→9→8→7→5→6→6→6→4" (11 values for 11 passes, correct); "passes 3-N" phrase pre: "passes 3-9 fix bursts applied"; "passes 3-N" phrase post: "passes 3-11 fix bursts applied" (current burst is pass-11); prior-findings-count pre: "29" (wrong, was pass-2 count 15); prior-findings-count post: "15". Cardinality cross-check: 29,15,11,9,8,7,5,6,6,6,4 = 11 values = 11 passes. | F-P11-005/006/007 process-gap findings. POLICY 1: D-383 is not amended; D-384 extends it. | F5 pass-11 | 2026-05-11 | state-manager |
| D-385 | D-383/D-384 enforcement clarifications: sub-trajectory sibling enumeration, immutable-row scope, attestation completeness (extends D-383 + D-384). Rule: Three clarifications to D-383 + D-384 that the pass-11 fix burst violated in its own initial application: (1) Sub-trajectory sibling enumeration (PG-12-001): when fixing a canonical trajectory shorthand (e.g., `29→15→11→…`), the audit MUST enumerate ALL N-tuple sub-trajectories in the same file (Phase Progress rows, Current Phase Steps rows, summary tables, etc.) and verify each sub-trajectory is consistent with the canonical full-length trajectory. The pass-11 burst fixed the canonical 11-value trajectory but left two `9→9→8→7→5` sub-trajectories stale at STATE.md:63,78 (which should be `11→9→8→7→5` per F-P10-001 pass-3 correction). (2) Immutable-row scope (PG-12-002): D-383 rule 2(c) "no retroactive annotations on immutable rows" applies to: decision-log.md rows (D-NNN entries), burst-log.md entries (per-pass burst narratives), adversary-review files (adv-cycle-pass-N.md, including frontmatter and body), lessons.md entries (L-EDP1-NNN). The rule does NOT apply to: STATE.md (live state — fields are designed to be updated), INDEX.md (live navigation — table is designed to extend), story specs (versioned via CHANGELOG + frontmatter version bump), BC/VP/ADR (versioned via CHANGELOG). The pass-11 burst violated rule 2(c) by injecting NOTE annotations into the pass-10 burst-log entry; the corrections were already documented in the pass-11 burst entry below. (3) Per-position attestation completeness (PG-12-003): D-384 sub-rule 2(b) per-position match attestations MUST enumerate EVERY position from P1 to Pn (where n = total pass count). Format: `P1=v1✓ P2=v2✓ ... Pn=vn✓`. The pass-11 burst attestation listed only P4-P11, omitting P1-P3 (which did match but were not formally attested). Rationale: F-P12-001/002/003 demonstrate the L-EDP1-003 recursive-discipline-violation pattern recurring at the D-384 layer (4th consecutive). D-385 closes the 3 sub-rule ambiguities so D-383+D-384 are fully specified. Enforcement: S-15.03 (automated enforcement lint hook) is the structural remedy. Until S-15.03 ships, fix bursts must check D-385's three clarifications during the intra-file audit step. Initial application — this burst (F-P12-001/002/003 closure) applies: (1) sub-trajectory sibling enumeration on STATE.md: identified 2 stale sub-trajectories at lines 63+78 (`9→9→8→7→5` → `11→9→8→7→5`); fixed. (2) immutable-row scope verification on burst-log.md: removed retroactive NOTE annotations from pass-10 entry. (3) per-position attestation completeness: extended burst-log:102 attestation from P4-P11 to P1-P11. | F-P12-001/002/003 + PG-12-001/002/003. POLICY 1: D-383 and D-384 are not amended; D-385 extends them. | F5 pass-12 | 2026-05-11 | state-manager |
| D-386 | F5 convergence path selected (Option C). Continue F5 — accept asymptotic L-EDP1-003 limit; S-15.03 elevation deferred to next cycle. Pass-14 dispatched 2026-05-11. Closes F-P14-011 process-gap (dispatch-while-pending-decision). F-P14-012 process-gap acknowledged: S-15.03 DRAFT with no scheduled implementation; acceptance of asymptotic convergence is explicit per user directive. D-NNN-AMEND convention (F-P14-013) deferred — not blocking this cycle. | User dispatch (path C). Per-D-381: STATE.md must reflect post-dispatch state (done in this burst). Per-D-382: all sibling files updated (burst-log, INDEX.md, lessons.md, decision-log, STATE.md, STORY-INDEX). | F5 pass-14 | 2026-05-11 | user + state-manager |
| D-387 | D-385 sub-rule 2 structural-correction exception codified. When an adversary-review file's frontmatter contains a defect that violates the cycle's own canonical rules (e.g., undefined verdict label, undocumented schema variant, empty required field), a single structural correction is permitted to the frontmatter ONLY (body remains strictly immutable). Each such correction MUST: (a) be cited in the fixing burst's burst-log with explicit file:line and rule citation; (b) be propagated to ALL sibling sites in the same burst (sibling-pattern sweep mandatory per D-383); (c) when applied to a back-reference field (e.g., prior-pass-classification), update consistently in all sibling back-reference fields. Body content of adversary-review files remains immutable; corrections to body content require a non-amending corrigendum line OR a new same-cycle review pass with the corrected analysis. Lessons.md L-EDP1-NNN entries: corrigenda are permitted at the END of an entry (delimited by a "**Corrigendum**:" prefix, on a new line before `---` separator); structural body edits remain forbidden. Self-application attestation: this rule retroactively legalizes pass-14's F-P14-004 frontmatter edit (verdict: MEDIUM-HIGH → HIGH on adv-cycle-pass-9.md) AND the F-P14-003 corrigendum appended to L-EDP1-006 in lessons.md. Both were permissible structural corrections under D-387's framework; the absence of D-387 at the time of those edits was the procedural defect, not the edits themselves. | F-P15-001 (D-385 vs F-P14-004 rule conflict, F-P15-PG1). POLICY 1: D-385 is not amended; D-387 adds a structural-correction exception pathway. | F5 pass-15 | 2026-05-11 | state-manager |
| D-388 | Forward-reference cycle: convention codified. Stories MAY carry `cycle:` frontmatter references to cycles whose directories do not yet exist if and only if: (a) STORY-INDEX last_amended narrative explicitly notes the forward reference as a deferred-cycle entry; (b) the parent epic's body documents the forward-reference convention. Cycle directories MUST be opened (with INDEX.md scaffold) BEFORE any per-story-delivery cycle dispatch targeting that cycle. This constraint prevents delivery dispatch from failing because the artifact directory structure does not exist. Historical application: S-14.06..S-14.09 carry `cycle: v1.0-feature-engine-discipline-pass-2` — this is the first use of the convention; D-388 codifies it retroactively. | F-P15-011 (forward-reference cycle: convention not formally codified). Closes F-P15-011. | F5 pass-15 | 2026-05-11 | state-manager |
| D-389 | BC/VP/story input-hash placeholder convention codified. The literal string `"[pending-recompute]"` is the canonical placeholder value for `input-hash:` frontmatter on BC, VP, and story files PENDING the S-14.03 placeholder-lint automation. The literal string `"[live-state]"` is the canonical placeholder for STATE.md and similar live-state files. Reviewers MUST NOT flag these literal strings as content defects in cycle reviews; they are placeholders awaiting S-14.03 computation. S-14.03 will replace placeholders with concrete hashes computed from artifact content. Until S-14.03 ships, both literal strings are intentional and conformant. This rule retroactively closes F-LOW-4 (pass-1), F-P14-009 (pass-14), F-P16-004 (pass-16), and any subsequent recurrence of this defect class. | F-P16-004 recurring recurrence across passes. Closes F-LOW-4 (residual), F-P14-009 (residual), F-P16-004. | F5 pass-16 | 2026-05-11 | state-manager |
| D-390 | CHANGELOG row addition propagates to frontmatter housekeeping fields. When appending a CHANGELOG row to a BC/VP/story file (or any CHANGELOG-versioned artifact): (a) `last_amended:` frontmatter MUST be updated to match the new row's date; (b) if `version:` frontmatter is bumped, it must match the new row's version; (c) `input-hash:` frontmatter MAY be left as canonical placeholder `"[pending-recompute]"` until S-14.03 ships. Items (a) and (b) are sibling-pattern propagation under D-383. Violations of this rule are MEDIUM severity. This rule closes F-P16-PG2 (no formal CHANGELOG↔last_amended/input-hash propagation rule). | F-P16-PG2. Closes F-P16-PG2. | F5 pass-16 | 2026-05-11 | state-manager |
| D-391 | Sibling-pattern sweep extent enumeration source codified. When performing a sibling-pattern sweep under D-383/D-387/D-390, the fix-burst's burst-log entry MUST cite the *enumeration source* used to derive the sweep extent BEFORE stating the cardinality. Valid enumeration sources: (a) project policy rubric (artifact-list section in policies.yaml or adversary-prompt rubric); (b) file glob result with the glob pattern named (e.g., `.factory/cycles/<cycle>/adv-cycle-pass-*.md`); (c) an explicit BC-INDEX/VP-INDEX/STORY-INDEX query (e.g., "all BCs with subsystem == SS-04"); (d) explicit per-file enumeration. Format: "Sweep dimension X — enumeration source: <source>. Extent: <N> files. Audited: <list>. Action: <result>." Burst-log claims of the form "N files audited" without an enumeration source are NON-COMPLIANT under L-EDP1-009. Retroactively closes F-P17-PG1. Scopes future bursts. Self-application attestation: this rule is codified BY the pass-17 fix burst; the pass-17 burst-log entry MUST itself apply D-391 to all sweep dimensions documented therein, demonstrating self-application before declaring D-391 active. | F5 pass-17 fix burst. Closes F-P17-PG1. | F5 pass-17 | 2026-05-11 | state-manager |
| D-392 | VP Lifecycle table recognized as equivalent to BC CHANGELOG for D-390 purposes. VP files use a `## Lifecycle` event table (event \| version \| date) instead of BC's `## Changelog` markdown rows. D-390's "CHANGELOG row addition propagates to frontmatter housekeeping fields" rule applies equivalently to VP Lifecycle event-row additions. `last_amended:` on a VP file MUST match the most-recent Lifecycle event's date. The Lifecycle table's most-recent event row is the authoritative source for `last_amended:` on VP files, parallel to the CHANGELOG table's most-recent row for BC files. | F5 pass-17 fix burst. Closes F-P17-008. | F5 pass-17 | 2026-05-11 | state-manager |
| D-393 | Sibling-pattern sweep independent re-derivation requirement codified. Extends D-391. The enumeration-source citation in a burst-log sweep attestation MUST be re-derivable by an executable query (Grep command, file glob, jq query against index file, etc.) that yields the SAME cardinality as the inlined enumeration. The burst-log attestation MUST record ALL THREE of: (a) the inlined per-file list AND (b) the second-source executable query AND (c) arithmetic confirmation that \|list\| == \|query result\|. Discrepancies between the inlined list and the query result BLOCK the sweep-completion claim until reconciled. Violations of this rule are MEDIUM severity, equivalent to D-390/D-391. This rule closes the gap that F-P18-001 exposed: the pass-17 dim-1 attestation used "project policy rubric" as enumeration source without an executable query, and the inlined list was wrong (3 BCs missing; canonical Grep yields 12 BCs, not 13). Retroactively closes F-P18-PG1, F-P18-003. | F5 pass-18 fix burst. Closes F-P18-PG1, F-P18-003. | F5 pass-18 | 2026-05-11 | state-manager |
| D-394 | D-391 severity + D-381 dispatch-side phase update clarification. (a) D-391 violations are MEDIUM severity, equivalent to D-390 (matches the inferred-but-unstated convention; now explicit). (b) D-381 STATE.md update obligation extends to ADVERSARY DISPATCH: at adversary-pass dispatch time, the orchestrator (or dispatching agent) MUST update STATE.md `phase:` and `current_step:` to reflect pass-N-adversary-in-progress, BEFORE the adversary returns its review. This prevents STATE.md `phase:` field from lagging the cycle state. Going forward: when dispatching pass-19, the orchestrator MUST update STATE.md `phase:` to `engine-discipline-F5-pass-19` before the adversary runs. Retroactively closes F-P18-004, F-P18-006. | F5 pass-18 fix burst. Closes F-P18-004, F-P18-006. | F5 pass-18 | 2026-05-11 | state-manager |
| D-395 | File-state grep-back verification of burst-log Action claims codified. Extends D-393. For every "Action: \<file\> \<field\> \<verb\>" item in a burst-log sweep attestation, the agent MUST, AFTER applying the action, re-grep the target file to verify the field appears (or has the expected value). The grep evidence MUST be inlined into the burst-log entry as a sibling of the Action claim. Format: `Action: <file> <field> <verb> \| Verification: <grep command> → <expected result> ✓`. A "✓" mark without a verification command is non-compliant under D-395. Violations of this rule are MEDIUM severity, equivalent to D-393. This rule closes the gap that F-P19-001 exposed: the pass-18 dim-3 attestation listed "Action: VP-INDEX last_amended added ✓" without a post-action grep verification, and the action was never executed — producing a false-true attestation. Retroactively closes F-P19-PG1. | F5 pass-19 fix burst. Closes F-P19-PG1. | F5 pass-19 | 2026-05-11 | state-manager |
| D-396 | Story frontmatter ↔ STORY-INDEX body-table sibling-pattern sweep codified. When a fix burst changes story frontmatter (`status:`, `merged_at:`, `merged_in:`, `merge_sha:`, `wave:`, `points:`), the burst MUST also sweep STORY-INDEX.md body-table cells for matching story IDs and propagate the change same-burst. Executable query (D-393-compliant): `grep -E '\| S-[0-9.]+\s*\|.*\| (draft\|in-flight\|partial) \|' STORY-INDEX.md` cross-referenced against `grep -l 'status: merged' .factory/stories/S-*.md`. Set-difference yields propagation gap. Violations MEDIUM severity. This rule closes the gap that F-P19-002 exposed: S-12.03/04/05/07/08 story frontmatter was marked `status: merged` (PRs #119-123 merged 2026-05-10) but STORY-INDEX.md body-table cells still showed `draft` across multiple passes. Retroactively closes F-P19-PG2 and F-P19-002. | F5 pass-19 fix burst. Closes F-P19-PG2, F-P19-002. | F5 pass-19 | 2026-05-11 | state-manager |
| D-397 | D-395 intent-match sub-clause codified. Extends D-395. When a burst-log Action verb writes to a narrative/log field for pass-N, the written content MUST reference pass-N (current burst), not a prior pass. The Verification grep target string MUST contain "pass-N" (or the canonical pass-N marker) — verifying that the current-burst end-state was achieved, not merely that a substring exists. Example: pass-19 burst updating STATE.md "Last Updated" MUST write `F5 pass-19 fix burst COMPLETE...` and `grep -c 'pass-19 fix burst COMPLETE' STATE.md` MUST yield 1. A Verification grep targeting prior-pass substrings is a D-397 violation (false-green). Violations MEDIUM severity. Retroactively closes F-P20-PG1 and F-P20-001 (as the canonical exemplar). | F5 pass-20 fix burst. Closes F-P20-PG1, F-P20-001. | F5 pass-20 | 2026-05-11 | state-manager |
| D-398 | Lessons.md Layer-N "Same-burst Violation" awaiting-audit convention codified. The "Same-burst Violation" column in L-EDP1-NNN layer-history tables MUST be filled by the FOLLOWING adversary pass (pass-(N+1) fresh-context audit), not by the codifying burst itself. The codifying burst CANNOT self-diagnose its own in-burst defect — that requires fresh-context. Until pass-(N+1) runs, the field MUST read `(awaiting pass-(N+1) adversary fresh-context audit)` rather than `—` (which falsely claims absence of violation). Pass-(N+1)'s fix burst, after running the adversary, updates the field via D-387 corrigendum format. This pattern has recurred twice: L-EDP1-010 Layer-9 "—" corrected by F-P19-005 corrigendum; L-EDP1-011 Layer-10 "—" corrected by F-P20-004 corrigendum. D-398 makes the convention explicit so future layers use the correct placeholder from the start. Retroactively closes F-P20-PG2. | F5 pass-20 fix burst. Closes F-P20-PG2. | F5 pass-20 | 2026-05-11 | state-manager |
| D-399 | "Canonical pass-N marker" defined for D-397 intent-match. A "canonical pass-N marker" in a D-397 Verification grep target string is: (a) the literal substring `pass-N` where N is the current burst number; OR (b) a content marker AUTHORED in pass-N referencing a pass-N-codified rule/finding/decision (e.g., for pass-21 fix burst: substrings `D-399`, `D-400`, `L-EDP1-013`, `F-P21-NNN`); OR (c) a date-stamp marker matching the current burst's date. Markers from prior passes (e.g., D-395 cited in a Verification grep during pass-20 burst) are NOT canonical pass-N markers and trigger D-397 violations. Closes F-P21-PG1, F-P21-004. | F5 pass-21 fix burst. Closes F-P21-PG1, F-P21-004. | F5 pass-21 | 2026-05-11 | state-manager |
| D-400 | Lessons.md Layer-N row update protocol codified (D-385/D-398 reconciliation). When pass-(N+1) audits a Layer-N row whose "Same-burst Violation" column reads `(awaiting pass-(N+1) adversary fresh-context audit)` per D-398, the next-pass fix burst MUST update the column INLINE (replacing the awaiting-text with the actual finding description). This is NOT a D-385 sub-rule 2 violation because the awaiting-text was provisional by D-398 codification and the inline update is the documented closure mechanism. Body content of L-EDP1-NNN entries OTHER than the awaiting-audit row remains immutable per D-385. Codifying-burst rules: the burst that ORIGINALLY authored the L-EDP1-NNN entry uses `(awaiting pass-(N+1) ...)` placeholder; the NEXT pass's fix burst inline-edits ONLY the awaiting-text cell. Closes F-P21-006. | F5 pass-21 fix burst. Closes F-P21-006. | F5 pass-21 | 2026-05-11 | state-manager |
| D-401 | Cross-index sync + D-394 ownership + counting-basis. Three sub-decisions: (a) Cross-index sync: when a cycle codifies ≥3 governance decisions (D-NNN) in a fix burst, ALL 4 indexes (BC-INDEX, VP-INDEX, STORY-INDEX, ARCH-INDEX) MUST append a changelog row referencing the decision range in the same burst; silence in any sibling index is a MEDIUM severity finding. Closes F-P22-PG1, F-P22-002. (b) D-394 ownership: the dispatch-side STATE.md `phase:` and `current_step:` update (before adversary returns) is the ORCHESTRATOR's responsibility; the fix-burst-side STATE.md phase update (at pass-N-COMPLETE) is the STATE-MANAGER's responsibility. Both obligations remain from D-394; D-401 clarifies the ownership boundary. Closes F-P22-006. (c) Trajectory counting-basis: the trajectory shorthand records CONTENT-ONLY counts (CRITICAL+HIGH+MEDIUM+LOW+NITPICK); process-gap findings are tracked separately in the `process_gap_count` frontmatter field and the INDEX.md "+NPG" notation. Retroactively closes F-P22-005. | F5 pass-22 fix burst. Closes F-P22-PG1, F-P22-002, F-P22-005, F-P22-006. | F5 pass-22 | 2026-05-11 | state-manager |
| D-402 | D-399 actual-count exactness sub-clause. Extends D-395+D-399. Verification grep cardinality MUST report the EXACT integer returned by the `-c` flag. Lower-bound (≥N), upper-bound (≤N), and ranged (N–M) claims are non-conformant under D-395+D-397+D-399. Acceptable form: `Verification: <grep_command> → <integer> ✓`. Non-conformant forms (all rejected): `→ ≥3 ✓`, `→ ≤5 ✓`, `→ 3-5 ✓`. Rationale: the exact integer is the only form that provides deterministic, independently reproducible evidence. Lower-bound forms conceal incorrect counts that happen to satisfy the lower bound. Retroactively closes F-P22-PG2, F-P22-008, F-P22-011. | F5 pass-22 fix burst. Closes F-P22-PG2, F-P22-008, F-P22-011. | F5 pass-22 | 2026-05-11 | state-manager |
| D-403 | D-401(a) self-application enforcement + D-402 regex-precision clarification + D-394 dispatch-side recurrence acknowledgment. Three sub-decisions: (a) D-401(a) self-application: when a fix burst codifies cycle-governance decisions, ALL FOUR indexes (BC, VP, STORY, ARCH) MUST acknowledge the decision range AT THE TIME the burst lands, NOT inherited from prior bursts. The burst MAY bump multiple indexes in the same burst if the decision count threshold (≥3 governance decisions) is met OR if the prior burst's coverage was partial. Retroactively closes F-P23-001, F-P23-004, F-P23-008, F-P23-009. (b) D-402 regex precision: Verification grep patterns SHOULD match the actual file string form (e.g., `version: "1.42"` matches via regex `"1\.42"` not `v1.42`); regex authors MUST consider quoted-frontmatter vs body-narrative substring differences. The exact-integer obligation per D-402 stands unchanged. Retroactively closes F-P23-002, F-P23-PG2. (c) D-394+D-401(b) dispatch-side phase update: continues to recur at each pass dispatch boundary per L-EDP1-007 Option C asymptotic. The orchestrator workflow update is the structural remedy; pass-by-pass tactical fixes are acceptable. F-P23-006 closure: this fix burst inline-updates STATE.md phase to pass-23 (tactical). Recurrence at pass-24 dispatch is acknowledged as the asymptotic limit, not a defect. | F5 pass-23 fix burst. Closes F-P23-001, F-P23-002, F-P23-004, F-P23-006, F-P23-008, F-P23-009, F-P23-PG2. | F5 pass-23 | 2026-05-11 | state-manager |
| D-404 | D-403(a) literal acknowledgment enforcement — each codified D-NNN MUST appear by ID in ALL 4 indexes' enumerations within the codifying burst. Extends D-401(a) + D-403(a). When a fix burst codifies D-NNN, the same burst MUST update ALL 4 indexes (BC, VP, STORY, ARCH) to include D-NNN BY ID in their changelog enumeration (not just as procedural rationale). A reference of the form "per D-NNN(x)" is procedural rationale (rule reference); a reference of the form "Acknowledges D-NNN" or "decision range D-AAA..D-NNN" is literal acknowledgment. D-404 requires literal acknowledgment in all 4 indexes. The version-bump may be a no-content-change increment if the only purpose is to add the literal acknowledgment. Violations MEDIUM severity per D-394+D-401 convention. Retroactively closes F-P24-001, F-P24-003, F-P24-004; scopes D-404 closure obligation to F-P24-PG1 (the automation gap remains for S-15.03). | F5 pass-24 fix burst. Closes F-P24-001, F-P24-003, F-P24-004. | F5 pass-24 | 2026-05-11 | state-manager |
