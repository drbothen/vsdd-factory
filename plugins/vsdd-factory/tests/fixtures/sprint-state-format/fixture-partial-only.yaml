# sprint-state-format fixture: partial-only entries — no draft/pending/terminal
# Used by: test_consumer_partial_only_raises_broken_sprint_state
# BC-5.41.002 PC3: partial is classified as BROKEN_STORY_IDS (not terminal, not next-wave).
# When has_broken=1 AND has_next_wave=0, consumer raises BrokenSprintState.
# This confirms partial is neither terminal (does not complete a wave) nor next-wave
# (does not select stories for the upcoming wave).
#
# Wave topology (all partial — no terminal, no draft):
#   S-1.01: partial — broken (non-terminal, non-next-wave)
#   S-1.02: partial — broken (non-terminal, non-next-wave)
# Expected consumer outcome: exit 0, CLASSIFY_RESULT=broken-sprint-state

stories:
  - id: S-1.01
    status: partial
  - id: S-1.02
    status: partial
