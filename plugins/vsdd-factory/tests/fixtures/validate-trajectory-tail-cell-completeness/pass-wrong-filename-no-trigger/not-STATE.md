This file has a filename that does NOT match any target basename.
Path-component-strict guard: file_name() == "not-STATE.md" is not "STATE.md".
Hook must emit Continue immediately without any validation.
Even though this content has no trajectory tail, the hook must not fire.
