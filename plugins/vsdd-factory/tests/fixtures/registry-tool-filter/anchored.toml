# Fixture: anchored tool= entry (leading ^ present).
# Used by T-002 / RG-002: lint must accept this as compliant.
# Represents the post-fix canonical form per D-a table.
schema_version = 2

[[hooks]]
name = "test-anchored-hook"
event = "PostToolUse"
tool = "^(Edit|Write)$"
plugin = "hook-plugins/test-anchored-hook.wasm"
timeout_ms = 5000
on_error = "continue"
