# Fixture: dim1-file-count-arithmetic / PASS

**Scenario:** The Dim-1 headline says "3 unique files" and the comma-delimited
list on the following line contains exactly 3 file paths.

**Expected exit code:** 0 (PASS)

**Script invocation:**
```
dim1-file-count-arithmetic.sh burst-log.md
```
