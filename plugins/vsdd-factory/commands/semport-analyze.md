---
description: Semantic code porting — scan a reference codebase, extract behavioral intent, and design a translation strategy to the target language.
argument-hint: "[source-path] [target-language] [--incremental module-name]"
---

Use the `semport-analyze` skill via the Skill tool.

Arguments: $ARGUMENTS
