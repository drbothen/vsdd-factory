---
description: "Wire Claude Code's native OpenTelemetry export into the vsdd-factory local observability stack. Writes OTEL env vars to .claude/settings.local.json. Default: on."
argument-hint: "[on|off|status]"
---

Use the `vsdd-factory:claude-telemetry` skill via the Skill tool. Pass $ARGUMENTS to the skill as the subcommand (default: `on`).
