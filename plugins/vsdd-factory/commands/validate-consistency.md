---
description: Cross-file consistency validation — verify spec IDs, anchor links, counts, naming, and traceability across planning artifacts.
---

Use the `validate-consistency` skill via the Skill tool.
