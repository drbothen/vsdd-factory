---
description: Wire the current project into the local observability stack — factory-obs register + claude-telemetry env vars — in one step.
---

Use the `vsdd-factory:onboard-observability` skill via the Skill tool.
