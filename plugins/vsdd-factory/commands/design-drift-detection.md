---
description: Detect design system drift during maintenance — token overrides, component misuse, pattern violations, emergent patterns.
---

Use the `vsdd-factory:design-drift-detection` skill via the Skill tool.
