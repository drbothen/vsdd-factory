---
document_type: story
level: implementation
story_id: S-19.01
epic_id: "E-19"
version: "1.0"
title: "pr-manager hardening: READY verdict HEAD-SHA pinning + release-PR merge-strategy guard + shell-dialect simulation discipline"
status: draft
producer: story-writer
timestamp: "2026-07-04T00:00:00Z"
last_amended: "2026-07-04 (v1.0) — initial draft authored by story-writer; post-rc.22 smoke gate authorization (2026-07-04); closes L-BB-merge-race-ready-report-stale-head (D-749), L-BB-release-pr-squash-merge-not-mechanically-enforced (D-750), L-BB-simulation-shell-dialect-gap (D-750)."
phase: F3
cycle: v1.0-feature-engine-discipline-pass-1
inputs:
  - plugins/vsdd-factory/agents/pr-manager.md
  - .factory/cycles/v1.0-brownfield-backfill/lessons.md
  - .github/workflows/release.yml
input-hash: "1165dbf"
traces_to:
  - .factory/stories/epics/E-19-post-rc22-operator-hardening.md
wave: 1
priority: P1
points: 8
track: feature
effort_class: multi-day
depends_on: []
blocks: []
behavioral_contracts: []
verification_properties: []
domain_invariants: []
tdd_mode: strict
target_module: "plugins/vsdd-factory/skills/pr-manager"
subsystems:
  - "SS-05"
  - "SS-06"
estimated_days: 2.0
assumption_validations: []
risk_mitigations: []
anchored_adrs: []
closes:
  - "lesson L-BB-merge-race-ready-report-stale-head (D-749): READY verdicts must pin the exact covered HEAD SHA; later pushes uncovered"
  - "lesson L-BB-release-pr-squash-merge-not-mechanically-enforced (D-750): release PR merge strategy not mechanically enforced; squash-merge could silently strip develop ancestry"
  - "lesson L-BB-simulation-shell-dialect-gap (D-750): release.yml darwin-leg scripts validated under /bin/bash 3.2.57 not arbitrary bash"
feature: "post-rc.22-smoke"
modified:
  - "2026-07-04 v1.0: initial draft"
---

# S-19.01 — pr-manager hardening: READY verdict HEAD-SHA pinning + release-PR merge-strategy guard + shell-dialect simulation discipline

**Epic:** E-19 — Post-rc.22 Operator Hardening
**Wave:** 1 (no hard predecessor in E-19; can begin immediately)
**Depends on:** none
**Priority:** P1 (process-gap hardening; prevents silent merge-race data loss and erroneous squash-merges of release PRs)
**Subsystem anchor:** SS-05 (Pipeline Orchestration) owns pr-manager and orchestrator agent behavior. SS-06 (Skill Catalog) is the secondary anchor for skill SKILL.md changes.

## Narrative

Three lessons codified from the rc.22 brownfield-backfill cycle expose gaps in the pr-manager workflow that create silent failure modes in production:

**L-BB-merge-race-ready-report-stale-head (D-749):** The pr-manager issues a READY verdict after reviewing a PR, but does not record the HEAD SHA it evaluated. If a subsequent push lands before the orchestrator issues the merge instruction, the READY verdict silently covers a different commit than the one that was actually merged. The operator has no way to detect that the reviewed commit and the merged commit differ.

**L-BB-release-pr-squash-merge-not-mechanically-enforced (D-750):** Release PRs (branches matching `release/v*`) MUST be merged with `--merge` per RELEASING.md (TD #69). The pr-manager has no explicit merge-strategy gate; a squash-merge could strip develop's commits from being ancestors of main, violating the release invariant. The pr-manager should detect and block squash-merge attempts on release PRs.

**L-BB-simulation-shell-dialect-gap (D-750):** When validating release.yml darwin-leg scripts (e.g., bash scripts in CI that run on macOS runners with Bash 3.2.57), the test harness must invoke those scripts explicitly under `/bin/bash`, not the operator's default `bash` (which on macOS Ventura/Sonoma may be Bash 5.x from Homebrew). The gap was discovered when a script failed on the macOS CI runner in production but appeared to pass in local testing because the validator used Homebrew's `bash` instead of `/bin/bash`.

## Acceptance Criteria

| ID | Criterion | Gate |
|----|-----------|------|
| AC-001 | `pr-manager` READY verdict output includes `covered_sha: <40-char-hex>` recording the PR's HEAD SHA at the time READY was assessed. If `gh pr view --json headRefOid` fails or returns empty, the pr-manager exits non-zero with message `READY_SHA_FETCH_FAILED: cannot pin covered HEAD SHA`. | `grep -q "covered_sha:" <READY_output>` exits 0; `covered_sha` value matches `git rev-parse HEAD` on the PR branch at assessment time. Non-exit test: a READY verdict missing `covered_sha:` must cause the downstream merge gate to fail. |
| AC-002 | Before issuing the merge instruction, the orchestrator fetches the current PR HEAD SHA and compares it to `covered_sha`. If they differ (stale verdict), execution halts with exit code 1 and message `STALE_READY_VERDICT: HEAD <current> has advanced past covered_sha <pinned>; re-review required.` | Shell exit code ≠ 0 when HEAD ≠ covered_sha. Positive control: same SHA → merge proceeds. Negative control: advanced SHA → exits 1 with required message text. |
| AC-003 | Merge instruction gate: when the source branch matches `^release/v`, the merge instruction uses `--merge` (not `--squash`). If a squash flag would be issued, the pr-manager exits non-zero with `RELEASE_PR_SQUASH_FORBIDDEN: release branches must merge with --merge per RELEASING.md`. | Shell exit code ≠ 0 on squash attempt for release branch. Positive control: `--merge` proceeds. Negative control: `--squash` on `release/v*` exits 1. |
| AC-004 | Darwin-leg CI script validation in the test harness uses `/bin/bash` (the macOS system Bash, version 3.2.x) as the interpreter, not `bash` from PATH (which may be Bash 5.x). A preflight assertion verifies `/bin/bash --version` contains `version 3.2` on macOS (darwin) test runners; on Linux the preflight is skipped. Violations exit non-zero with `DARWIN_LEG_WRONG_INTERPRETER: expected /bin/bash 3.2.x, got <actual>`. | On a macOS environment: `/bin/bash --version` substring match `version 3.2`. Bats test that invokes a release.yml darwin-leg shell fragment uses `#!/bin/bash` as the shebang in generated temp scripts. Preflight assertion in bats setup_file exits 1 on wrong interpreter. |

## Exceptional Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | `gh pr view` network failure during covered_sha fetch | pr-manager exits non-zero with `READY_SHA_FETCH_FAILED`; does not issue a READY verdict without the SHA. |
| EC-002 | Merge gate check: PR branch has been force-pushed (HEAD SHA changed, PR still open) | `STALE_READY_VERDICT` message includes both the covered_sha and the current HEAD so the operator can trace the divergence. |
| EC-003 | Darwin-leg preflight: test running on Linux | Preflight skipped gracefully; Linux tests use `bash` from PATH (Linux typically ships Bash 5.x; darwin-leg shims apply only on macOS). |
| EC-004 | Release PR merge with `--merge` flag explicitly provided | Gate passes; no false positive. |

## Architecture Mapping

This story modifies existing skills/agents in SS-05:
- `plugins/vsdd-factory/skills/pr-manager/SKILL.md` — adds covered_sha pinning to READY verdict format, adds merge-strategy gate specification for release PRs
- Orchestrator-per-story delivery workflow — adds stale-verdict check before merge instruction
- Test harness (bats files in `plugins/vsdd-factory/tests/`) — adds darwin-leg preflight discipline for release script validation

No new WASM crates. No hooks-registry.toml changes.

## Red Gate Test Plan

| Test ID | Description | Expected Outcome |
|---------|-------------|------------------|
| RG-001 | pr-manager produces READY verdict — assert `covered_sha:` line present in output | PASS: `covered_sha: <40-char-hex>` in stdout |
| RG-002 | Merge gate: covered_sha matches current HEAD — assert merge proceeds | PASS: exit 0 |
| RG-003 | Merge gate: covered_sha does NOT match current HEAD — assert stale verdict error | FAIL with exit 1 + `STALE_READY_VERDICT` message |
| RG-004 | Merge instruction on `release/v1.2.3` branch with `--squash` — assert blocked | FAIL with exit 1 + `RELEASE_PR_SQUASH_FORBIDDEN` |
| RG-005 | Merge instruction on `release/v1.2.3` branch with `--merge` — assert allowed | PASS: exit 0 |
| RG-006 | Darwin-leg preflight on macOS: `/bin/bash --version` matches `3.2` — assert pass | PASS: exit 0 |
| RG-007 | Darwin-leg preflight on macOS: wrong interpreter — assert non-zero exit | FAIL with exit 1 + `DARWIN_LEG_WRONG_INTERPRETER` |

## Test Plan

| Test ID | Story AC | Test File | Description |
|---------|----------|-----------|-------------|
| T-001 | AC-001 | plugins/vsdd-factory/tests/pr-manager-hardening.bats | READY verdict includes covered_sha field |
| T-002 | AC-001 | plugins/vsdd-factory/tests/pr-manager-hardening.bats | READY_SHA_FETCH_FAILED on gh failure |
| T-003 | AC-002 | plugins/vsdd-factory/tests/pr-manager-hardening.bats | Stale verdict detected: covered_sha ≠ current HEAD |
| T-004 | AC-002 | plugins/vsdd-factory/tests/pr-manager-hardening.bats | Fresh verdict passes: covered_sha = current HEAD |
| T-005 | AC-003 | plugins/vsdd-factory/tests/pr-manager-hardening.bats | Release branch + squash → RELEASE_PR_SQUASH_FORBIDDEN |
| T-006 | AC-003 | plugins/vsdd-factory/tests/pr-manager-hardening.bats | Release branch + merge → allowed |
| T-007 | AC-004 | plugins/vsdd-factory/tests/pr-manager-hardening.bats | Darwin-leg preflight: wrong interpreter exits 1 |
| T-008 | AC-004 | plugins/vsdd-factory/tests/pr-manager-hardening.bats | Darwin-leg preflight: Linux skips gracefully |

## File Structure Requirements

| File | Action | Notes |
|------|--------|-------|
| `plugins/vsdd-factory/skills/pr-manager/SKILL.md` | modify | Add §READY Verdict Format (covered_sha field), §Merge-Strategy Gate (release branch check), §Darwin-Leg Validation Discipline |
| `plugins/vsdd-factory/tests/pr-manager-hardening.bats` | create | New bats file: 8 tests covering AC-001..AC-004 + EC-001..EC-004 |
| `plugins/vsdd-factory/tests/fixtures/pr-manager/` | create | Fixture scripts for darwin-leg validation tests (stub gh mock + bash version mock) |
