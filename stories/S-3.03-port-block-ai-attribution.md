---
document_type: story
level: ops
story_id: "S-3.03"
epic_id: "E-3"
version: "1.2"
status: draft
producer: story-writer
timestamp: 2026-04-25T00:00:00
phase: 1.8
inputs:
  - .factory/stories/v1.0/S-3.3-port-block-ai-attribution.md
  - .factory/specs/architecture/ARCH-INDEX.md
input-hash: "956ce76"
traces_to: .factory/specs/prd.md#FR-013
functional_requirements: ["FR-013", "FR-032"]
cycle: v1.0.0-greenfield
points: "3"
depends_on: ["S-2.08", "S-3.04"]
blocks: ["S-4.07", "S-4.08"]
behavioral_contracts:
  - BC-2.01.002
verification_properties:
  - VP-044
capabilities: ["CAP-008"]
priority: "P1"
wave: 11
target_module: crates/hook-plugins/block-ai-attribution
subsystems: ["SS-02", "SS-04"]
estimated_days: 2
assumption_validations: []
risk_mitigations: []
---

# S-3.03: Port block-ai-attribution to WASM

**Epic:** E-3 — WASM Port: High-Value Hooks
**Phase:** Phase 3 — High-value port to native WASM
**Milestone:** `1.0.0-rc.1`
**Tier:** E
**Blocks:** S-4.07, S-4.08
**Blocked by:** S-2.08, S-3.04
**Estimated effort:** S

## Capability Anchor Justification

Primary: CAP-008 ("Gate tool calls with pre-execution behavioral checks (PreToolUse hooks)") per capabilities.md §CAP-008. The block-ai-attribution plugin is a PreToolUse gate — it blocks `git commit` commands containing AI attribution patterns before execution, directly implementing the behavioral gating capability.

FR anchor: **Dual — FR-013 (primary) + FR-032 (secondary).** FR-013 ("Legacy bash adapter plugin") per PRD §2.4 covers the bash-to-WASM plugin bridging. FR-032 ("PreToolUse gate hooks") also applies because block-ai-attribution is a PreToolUse gate that implements the gating contract directly — the blocking decision logic is within scope of FR-032. Primary trace is FR-013 (the plugin production story); FR-032 is the functional gating requirement this plugin satisfies. Both are listed in `functional_requirements`. Primary `traces_to` is PRD §FR-013 (F-004 fix).

Note: architecture compliance rules reference BC-1.03.001 (plugin execution lifecycle) and BC-1.01.001 (registry schema) — these are cross-subsystem informational references, not SS-04 anchors.

## Behavioral Contracts

| BC ID | Title | Scope Reason |
|-------|-------|--------------|
| BC-2.01.002 | HookResult exit codes Continue=0 / Block=2 / Error=1 | This is the SS-02 SDK contract that block-ai-attribution must satisfy: the native WASM plugin returns a HookResult variant; BC-2.01.002 contracts the canonical exit-code mapping that the dispatcher uses to interpret the plugin's return value. The plugin's Block return triggers the HookResult::Block path. |

Note: BC-4.02.002 (adapter exit-code mapping) and BC-4.02.003 (script_path validation ordering) were removed per F-002 adjudication — those BCs are scoped to the legacy bash-adapter (SS-04/SS-07 boundary); a native WASM plugin does not go through the adapter. BC-2.01.002 at SS-02 is the correct anchor for the HookResult contract this story must satisfy.

## Verification Properties

| VP ID | Title | Relevance |
|-------|-------|-----------|
| VP-044 | Legacy Bash Adapter Exit Code Mapping Is Correct | Verifies that exit 2 from any plugin (including block-ai-attribution) maps to HookResult::Block |

## Narrative
- **As a** repository maintainer
- **I want to** have `block-ai-attribution` running as native WASM in the PreToolUse/Bash hook
- **So that** commits containing AI attribution strings are blocked before they reach git

## Goal

Rewrite `block-ai-attribution` from bash to native WASM. This is a
PreToolUse/Bash gate — it must return `HookResult::Block` to prevent
the Bash tool from running if the commit message contains AI attribution patterns.

## Acceptance criteria

- [ ] `block-ai-attribution.wasm` compiled and registered as PreToolUse hook
  - _trace: [process-gap] — no SS-04 BC contracts block-ai-attribution as a distinct plugin (BC-4.02.003 removed per F-002; scoped to bash-adapter); v1.1 BC candidate BC-4.05.001_
- [ ] Scans `tool_input.command` for `git commit` with AI attribution patterns (Co-Authored-By, Generated with Claude, etc.)
  - _trace: CAP-008 ("blocks dangerous commands before execution: … AI attribution injection")_ [process-gap: no SS-04 BC contracts the attribution pattern detection logic — v1.1 BC candidate]
- [ ] Returns `HookResult::Block(reason)` if patterns found
  - _trace: BC-2.01.002 postcondition 2 (Block.exit_code() == 2) — native WASM plugin returns HookResult::Block; the SDK maps this to exit code 2 which Claude Code interprets as a block_
- [ ] Returns `HookResult::Continue` for clean commits or non-git commands
  - _trace: BC-2.01.002 postcondition 1 (Continue.exit_code() == 0) + story EC-001 (non-git command → Continue)_
- [ ] Block message is human-readable and includes the matched pattern
  - _trace: [process-gap] — no SS-04 BC contracts "human-readable block message with matched pattern"; v1.1 BC candidate BC-4.05.001_
- [ ] `hooks-registry.toml` updated; `on_error = "block"` set for this entry
  - _trace: BC-1.01.001 (registry schema at SS-01 scope; on_error=block is a registry config field)_ [process-gap: no SS-04 BC contracts the on_error=block setting for this plugin — v1.1 BC candidate]
- [ ] Existing bats tests for block-ai-attribution still pass
  - _trace: BC-2.01.002 (exit code contract unchanged; bats tests verify exit code 2 → block outcome)_

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| block-ai-attribution/src/lib.rs | crates/hook-plugins/block-ai-attribution/ | pure-core (pattern matching) + effectful (emit_event) |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | Non-git command | Return Continue |
| EC-002 | `git commit --allow-empty` with attribution | Block |
| EC-003 | Attribution in commit body (not subject) | Block |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| pattern matching logic | pure-core | String search; no I/O |
| main plugin entry | effectful-shell | emit_event for blocked commit |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~900 |
| block-ai-attribution/src/lib.rs | ~700 |
| Test files | ~700 |
| Tool outputs overhead | ~300 |
| **Total** | **~2,600** |
| Agent context window | 200K for Sonnet |
| **Budget usage** | **~1.3%** |

## Tasks (MANDATORY)

1. [ ] Create `crates/hook-plugins/block-ai-attribution/`
2. [ ] Implement AI attribution pattern detection (pure core)
3. [ ] Return Block with human-readable message on match
4. [ ] Update `hooks-registry.toml` with `on_error = "block"`
5. [ ] Verify bats tests pass

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-3.01 | PostToolUse plugin pattern | PreToolUse uses same HookPayload shape | PreToolUse `tool_response` is always None |
| S-3.02 | Command parsing from `tool_input.command` | Extract command from payload | |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| PreToolUse blocking plugins must return Block (not error) to halt | BC-1.03.001 (plugin execution lifecycle at SS-01) — Block is the only result that halts Bash tool execution | Unit test: attribution found → Block result |
| `on_error = "block"` required in registry | BC-1.01.001 (registry schema at SS-01) — on_error governs the fail-safe behavior when plugin errors | Registry validation |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| vsdd-hook-sdk | workspace-pinned | Plugin macro and types |

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| crates/hook-plugins/block-ai-attribution/Cargo.toml | create | Plugin crate |
| crates/hook-plugins/block-ai-attribution/src/lib.rs | create | Attribution pattern gate |
| plugins/vsdd-factory/hooks-registry.toml | modify | Update to native WASM with on_error=block |

## v1.1 BC Candidates (pre-emptive)

| Candidate ID | Description | AC that triggers it | Suggested subsystem |
|---|---|---|---|
| BC-4.05.001 | block-ai-attribution: pure-core pattern scan detects "Co-Authored-By", "Generated with Claude" (and variants) in `tool_input.command` for git commit commands; returns Block(reason includes matched pattern) | AC "Scans tool_input.command for AI attribution patterns" + "Block message is human-readable and includes matched pattern" | SS-04 |
| BC-4.05.002 | block-ai-attribution: non-git command or attribution-free commit → HookResult::Continue | AC "Returns Continue for clean commits or non-git commands" + EC-001 | SS-04 |
| BC-4.05.003 | block-ai-attribution: `git commit --allow-empty` with attribution in body → Block (EC-002/EC-003 coverage) | AC EC-002 (allow-empty with attribution) + EC-003 (attribution in body not subject) | SS-04 |
| BC-4.05.004 | block-ai-attribution pure-core module: `detect_attribution(command: &str) -> Option<&str>` is a pure function with no I/O, returning the first matched pattern string or None (F-002 pure-core pattern-detection candidate) | Purity classification table ("pattern matching logic = pure-core") — a dedicated pure-core BC enables isolated unit testing of the detection logic separate from the effectful plugin shell | SS-04 |
