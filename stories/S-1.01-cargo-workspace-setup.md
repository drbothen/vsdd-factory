---
document_type: story
level: ops
story_id: "S-1.01"
epic_id: "E-1"
version: "1.1"
status: merged
producer: story-writer
timestamp: 2026-04-25T00:00:00
phase: 1.8
inputs:
  - .factory/stories/v1.0/S-1.1-cargo-workspace-setup.md
  - .factory/specs/architecture/ARCH-INDEX.md
input-hash: "3d85b42"
traces_to: .factory/specs/domain-spec/capabilities.md
cycle: v1.0.0-greenfield
points: "5"
depends_on: []
blocks: ["S-1.02", "S-1.03", "S-1.04", "S-1.05", "S-1.06", "S-1.07", "S-1.08", "S-1.09"]
behavioral_contracts: []
verification_properties: []
priority: "P0"
wave: 1
target_module: crates/factory-dispatcher
subsystems: ["SS-01", "SS-02"]
estimated_days: 2
assumption_validations: []
risk_mitigations: []
---

# S-1.01: Cargo workspace + CI scaffolding

**Epic:** E-1 — Dispatcher Foundation
**Phase:** Phase 1 — Dispatcher foundation
**Milestone:** `1.0.0-beta.1`
**Tier:** B.0 (BLOCKER — gates all of Tier B.x)
**Blocks:** S-1.02, S-1.03, S-1.04, S-1.05, S-1.06, S-1.07, S-1.08, S-1.09
**Blocked by:** none
**Estimated effort:** M

## Narrative
- **As a** Rust developer contributing to vsdd-factory
- **I want to** have a properly scaffolded Cargo workspace with CI
- **So that** all dispatcher, SDK, and plugin crates share a coherent build graph and can be developed in parallel

## Goal

Establish the Cargo workspace at the repo root so all Rust crates
(dispatcher, SDK, hook-plugins, sink drivers) share a coherent build
graph, dependency set, and CI pipeline. This is a pure-plumbing story —
no business logic — but it's the single blocker preventing Tier B.x from
fanning out in parallel.

## Acceptance criteria

- [ ] `Cargo.toml` at repo root declares a workspace with members
  `crates/factory-dispatcher`, `crates/hook-sdk`, and a stub plugin
  `crates/hook-plugins/capture-commit-activity`
- [ ] Each member crate has a minimal `Cargo.toml` + placeholder `lib.rs`
  or `main.rs` that compiles cleanly
- [ ] Workspace uses `resolver = "2"`
- [ ] Shared dependency versions pinned in `[workspace.dependencies]` —
  minimum: `serde`, `serde_json`, `toml`, `anyhow`, `thiserror`,
  `tokio`, `tracing`, `tracing-subscriber`, `wasmtime`, `clap`
- [ ] `rust-toolchain.toml` pins a specific stable Rust version (e.g., `1.83.0`)
- [ ] `.github/workflows/ci.yml` gains a `cargo-check` job that runs
  `cargo check --workspace --all-targets` + `cargo fmt --check` +
  `cargo clippy --workspace --all-targets -- -D warnings`
- [ ] Existing bats CI (1177 tests) continues to run unchanged
- [ ] `target/` is gitignored
- [ ] Workspace builds clean on darwin-arm64, linux-x64 at minimum

## Tasks

1. Create `Cargo.toml` at repo root with workspace declaration.
2. Create `crates/factory-dispatcher/Cargo.toml` with a placeholder `src/main.rs`.
3. Create `crates/hook-sdk/Cargo.toml` with `src/lib.rs`.
4. Create `crates/hook-plugins/capture-commit-activity/Cargo.toml` with
   `crate-type = ["cdylib"]`, target = `wasm32-wasip1`.
5. Add `rust-toolchain.toml` pinning stable Rust version.
6. Add `[workspace.dependencies]` block with version pins.
7. Update `.gitignore` — ensure `target/` is handled.
8. Add `.github/workflows/ci.yml` job: cargo fmt + clippy + check.
9. Verify `cargo check --workspace` passes locally on macOS.

## Artifacts

- `Cargo.toml` (root) — workspace manifest
- `rust-toolchain.toml` (root) — Rust version pin
- `crates/factory-dispatcher/{Cargo.toml,src/main.rs}` — stub
- `crates/hook-sdk/{Cargo.toml,src/lib.rs}` — stub
- `crates/hook-plugins/capture-commit-activity/{Cargo.toml,src/lib.rs}` — stub
- `.github/workflows/ci.yml` — new `cargo-check` job
- `.gitignore` — updates
- `Cargo.lock` — committed

## Contracts

**Workspace layout contract (consumed by S-1.02 through S-1.09):**

```
Cargo.toml                                  # workspace root
crates/
  factory-dispatcher/                       # the binary
  hook-sdk/                                 # the SDK
  hook-plugins/
    <per-plugin>/                           # each a separate crate
```

**Rust version pin:** all crates target the same stable Rust version
declared in `rust-toolchain.toml`.

**Dependency version contract:** shared deps have versions pinned in
`[workspace.dependencies]`. Member crates reference them with `workspace = true`.

## Test plan

- `cargo check --workspace --all-targets` passes
- `cargo fmt --check` passes
- `cargo clippy --workspace --all-targets -- -D warnings` passes
- `cargo build --workspace` succeeds on darwin-arm64 and linux-x64
- Existing bats suite still green

## Notes

`crate-type = ["cdylib"]` + `wasm32-wasip1` target for plugin crates is
non-negotiable — required by wasmtime to instantiate them as modules.

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| Cargo.toml | / (workspace root) | pure (build config) |
| ci.yml cargo-check job | .github/workflows/ci.yml | effectful-shell (runs CI) |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | Member crate compilation fails | `cargo check --workspace` fails; CI red |
| EC-002 | bats suite broken by new Cargo.toml | Must be pre-checked; existing tests must pass |
| EC-003 | `wasm32-wasip1` target not installed | CI setup step must `rustup target add wasm32-wasip1` |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| Cargo.toml | pure | Build configuration only |
| ci.yml | effectful-shell | Runs build and test tools |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~1,200 |
| Cargo.toml template | ~400 |
| ci.yml additions | ~500 |
| Tool outputs overhead | ~300 |
| **Total** | **~2,400** |
| Agent context window | 200K for Sonnet |
| **Budget usage** | **~1.2%** |

## Tasks (MANDATORY)

1. [ ] Create root `Cargo.toml` with workspace declaration
2. [ ] Create stub crates for dispatcher, hook-sdk, capture-commit-activity
3. [ ] Add `rust-toolchain.toml`
4. [ ] Add `[workspace.dependencies]` version pins
5. [ ] Update `.gitignore` for `target/`
6. [ ] Add `cargo-check` CI job
7. [ ] Verify all bats tests still pass

## Previous Story Intelligence (MANDATORY)

N/A — first story in epic E-1.

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| `resolver = "2"` in workspace | SS-01 Rust edition requirement | Cargo.toml validation |
| Plugin crates use `crate-type = ["cdylib"]` | SS-02 WASM ABI requirement | Build check |
| Commit `Cargo.lock` (binary-producing workspace) | SS-01 reproducibility | CI enforces presence |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| Rust | 1.83.0 (pinned) | Stable compiler via rust-toolchain.toml |
| wasmtime | workspace-pinned | WASM runtime (added to workspace deps) |
| tokio | workspace-pinned | Async runtime |
| serde | workspace-pinned | Serialization |

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| Cargo.toml | create | Workspace root manifest |
| rust-toolchain.toml | create | Rust version pin |
| crates/factory-dispatcher/Cargo.toml | create | Dispatcher crate manifest |
| crates/factory-dispatcher/src/main.rs | create | Placeholder entry point |
| crates/hook-sdk/Cargo.toml | create | SDK crate manifest |
| crates/hook-sdk/src/lib.rs | create | Placeholder lib |
| crates/hook-plugins/capture-commit-activity/Cargo.toml | create | Plugin crate manifest |
| crates/hook-plugins/capture-commit-activity/src/lib.rs | create | Empty on_hook stub |
| .github/workflows/ci.yml | modify | Add cargo-check job |
| .gitignore | modify | Add target/ entry |
| Cargo.lock | create | Committed for reproducibility |
