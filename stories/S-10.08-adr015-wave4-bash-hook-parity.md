---
document_type: story
level: ops
story_id: "S-10.08"
epic_id: "E-10"
version: "1.1"
status: draft
producer: story-writer
timestamp: 2026-05-04T00:00:00Z
phase: 2
inputs:
  - .factory/specs/architecture/decisions/ADR-015-single-stream-otel-schema.md
  - .factory/stories/epics/E-10-single-stream-otel-event-emission.md
input-hash: ""
traces_to: .factory/stories/epics/E-10-single-stream-otel-event-emission.md
functional_requirements: []
cycle: v1.0-brownfield-backfill
points: "5"
wave: 17
depends_on: ["S-10.07"]
blocks: ["S-10.09"]
behavioral_contracts: []
# BC status: pending PO authorship
verification_properties: []
priority: "P1"
tdd_mode: strict
target_module: "bin/"
subsystems: ["SS-01", "SS-07"]
estimated_days: 2
assumption_validations: []
risk_mitigations: []
anchored_adr: ADR-015
---

# S-10.08: ADR-015 Wave 4 — Bash hook parity (bin/emit-event schema alignment)

**Epic:** E-10 — Single-stream OTel-aligned event emission (ADR-015)
**Wave:** Wave 4
**Depends on:** S-10.07
**Blocks:** S-10.09
**Estimated effort:** M (5 pts, ~2 days)

## Narrative

- **As a** vsdd-factory operator using bash hooks that call `bin/emit-event`
- **I want** bash-sourced events to route through the dispatcher's `host::emit_event`
  host function via the legacy-bash-adapter, receiving the same Resource enrichment
  and identity stamping as native WASM plugins
- **So that** every emitted event (WASM-native or bash-via-adapter) routes through
  the host's `emit_event` and lands in `events-*.jsonl` via a single code path

> **Cross-reference TD-014:** S-10.08 coordinates with TD-014 (full native WASM
> migration of remaining bash hooks). S-10.08 migrates the emit-event call path to
> the dispatcher; TD-014 handles the broader bash-hook retirement scope.
> S-10.08 MUST NOT conflict with TD-014's approach — coordinate with the TD-014
> tracking issue before implementation begins.
>
> **Cross-reference TD-007:** S-10.08 (and S-10.09 for the physical removal step)
> are the authoritative trackers for the AC-003 deferral originally captured by
> TD-007 (S-3.04 AC-003 — bash bin/emit-event retirement). TD-007 narrowed to a
> breadcrumb on 2026-05-04; this story owns the migration work.

## Acceptance Criteria

### AC-001: bin/emit-event adds all Resource attributes before writing (traces to ADR-015 D-15.3 §Bash hook parity)

`bin/emit-event` (or an equivalent enhanced binary) reads the Resource attributes
that the dispatcher would stamp at startup and includes them in every event it writes
to `events-*.jsonl`. The minimum required Resource attributes are:

`service.name` | `service.namespace` | `service.instance.id` | `service.version` |
`deployment.environment.name` | `host.name` | `host.id` (cascade) | `os.type` |
`process.pid` | `vcs.repository.url.full` | `vcs.repository.name` | `vcs.owner.name` |
`vcs.provider.name` | `worktree.id` | `project.id` | `schema_url`

All fallback cascade logic from S-10.03 applies equally to bash events.

**Falsifiable test:** A test bash hook invocation via `bin/emit-event` produces a
record in `events-*.jsonl` that contains all 16 Resource fields listed above with
non-empty values.

### AC-002: bin/emit-event adds per-event identity fields (traces to ADR-015 D-15.3 §Bash hook parity + D-15.2 Per-event attributes)

`bin/emit-event` stamps the same per-event identity fields that the WASM dispatcher
stamps at `host::emit_event` time:

`timestamp` | `observed_timestamp` | `event.id` (UUIDv4) | `event.category` |
`event.schema_url` | `event.source = "bash-adapter"` | `severity_number` |
`severity_text` | `trace_id` (from `VSDD_TRACE_ID` env if set) |
`span_id` (new UUIDv4 per invocation) | `parent_span_id` (from `VSDD_PARENT_SPAN_ID` env)

**Falsifiable test:** A test bash event record contains all fields listed above.
`event.source` is exactly `"bash-adapter"` (this marker identifies thin events
during the migration window per ADR-015 D-15.3).

### AC-003: All bash hooks that called bin/emit-event now route through dispatcher host::emit_event via legacy-bash-adapter (traces to ADR-015 D-15.3 §Bash hook parity — Q5 adjudication 2026-05-04)

All bash hooks that previously called `bin/emit-event` are migrated to call the
dispatcher's `host::emit_event` host function via the legacy-bash-adapter. The bash
side gets the same Resource enrichment and identity stamping as native WASM plugins.

Under this model, EVERY emitted event (WASM-native or bash-via-adapter) routes through
the host's `emit_event` and lands in `events-*.jsonl` — there is no separate code path.

**Falsifiable test:** After this story merges:
(a) bash hook events in `events-*.jsonl` contain a non-empty `service.namespace` field
(impossible with the old thin format that bypassed the dispatcher).
(b) The legacy-bash-adapter call path is exercised by at least one bats integration test
that verifies end-to-end routing through `host::emit_event`.

### AC-004: event.source = "bash-adapter" marker identifies thin events during migration (traces to ADR-015 D-15.3 §Bash hook parity)

Until all bash hooks are ported to native WASM, `bin/emit-event`-sourced events
carry `event.source = "bash-adapter"`. This allows dashboards to identify and
optionally exclude thin events during the migration window.

**Falsifiable test:** All events written by `bin/emit-event` (before this story's
enhancement lands) had `event.source = "bash-adapter"` OR were missing the field
entirely. After this story, enhanced bash events carry `event.source = "bash-adapter"`
AND the full Resource attribute set.

### AC-005: bin/emit-event VSDD_TRACE_ID and VSDD_PARENT_SPAN_ID propagation (traces to ADR-015 D-15.4 §Trace propagation)

`bin/emit-event` reads `VSDD_TRACE_ID` from the environment (if set) and uses it
as the `trace_id` for the emitted event. `VSDD_PARENT_SPAN_ID` is read and used as
`parent_span_id`. This allows bash events to participate in the dispatcher's
cross-boundary trace chain.

**Falsifiable test:** Setting `VSDD_TRACE_ID=test-trace-001` before invoking
`bin/emit-event` results in an event record with `trace_id = "test-trace-001"`.

### AC-006: Legacy bash hooks do not regress (traces to ADR-015 Wave 4 scope)

All existing bash hooks that previously called `bin/emit-event` continue to work
after Wave 4. The enhanced `bin/emit-event` is backward-compatible with existing
call sites — no existing bash hook requires modification.

**Falsifiable test:** Full test suite (including bats tests for bash hooks) passes
after Wave 4 changes.

## Architecture Mapping

| Component | Module | Pure/Effectful | ADR-015 Reference |
|-----------|--------|---------------|-------------------|
| legacy-bash-adapter | `crates/legacy-bash-adapter/` or `crates/factory-dispatcher/src/` | Effectful (receives bash call → host::emit_event) | Wave 4; D-15.3; Q5 adjudication |
| `bin/emit-event` (deprecated after T-11) | `bin/emit-event` | Effectful — deprecated; routes to dispatcher | Wave 4; marked deprecated, physical removal deferred to TD-014 v1.1 |
| Bash hooks (existing) | `plugins/vsdd-factory/hooks/*.sh` | Effectful (call legacy-bash-adapter) | Wave 4 — backward compat required |
| `host::emit_event` | `crates/factory-dispatcher/src/main.rs` | Effectful — THE single emit path | D-15.3; Q5 adjudication |

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | `bin/emit-event` called outside a dispatcher context (no VSDD_TRACE_ID) | Generates a new per-invocation trace_id (same as WASM dispatcher behavior when no VSDD_TRACE_ID is set) |
| EC-002 | Bash hook runs in environment without CLAUDE_PROJECT_DIR set | `service.namespace` falls back to `basename(cwd)` (same cascade as S-10.03 EC-001) |
| EC-003 | `bin/emit-event` was deleted in commit 818fb95 (per ADR-015 Context) | Restore and enhance; or implement the preferred routing-through-dispatcher path |
| EC-004 | Bash hook already sets `event.source` to a custom value | `event.source = "bash-adapter"` overrides the plugin-supplied value (host wins per D-15.3); `event.host_overrides` is stamped |

## Tasks

- [ ] T-0: STOP CHECK — confirm S-10.07 Wave 3 announcement delivered; confirm Wave 3 hard gates passed; confirm TD-014 tracking issue reviewed for overlap
- [ ] T-1: Migrate all bash hooks that currently call `bin/emit-event` to instead call the dispatcher's `host::emit_event` host function via the legacy-bash-adapter; bash side gets the same Resource enrichment + identity stamping as native WASM plugins
- [ ] T-2: Verify that every emitted event (WASM-native or bash-via-adapter) routes through `host::emit_event` and lands in `events-*.jsonl` — confirm no separate code path exists
- [ ] T-3: Implement Resource attribute stamping in the legacy-bash-adapter path (reuse S-10.03 cascade logic; do NOT duplicate enrichment logic)
- [ ] T-4: Implement per-event identity field stamping (event.id UUIDv4, timestamps, category lookup)
- [ ] T-5: Implement `event.source = "bash-adapter"` marker in the adapter path
- [ ] T-6: Implement VSDD_TRACE_ID / VSDD_PARENT_SPAN_ID propagation from env through the adapter
- [ ] T-7: Write bats test: bash event routed through dispatcher contains all 16 Resource fields (AC-001)
- [ ] T-8: Write test: bash event contains all per-event identity fields (AC-002)
- [ ] T-9: Write test: VSDD_TRACE_ID propagation through adapter (AC-005)
- [ ] T-10: Run full bats test suite; confirm backward compat (AC-006)
- [ ] T-11: Mark `bin/emit-event` as deprecated with a runtime warning + log entry once all bash hooks route through the dispatcher; physical removal of `bin/emit-event` is deferred to v1.1 under TD-014's broader bash-hook retirement scope

## Previous Story Intelligence

S-10.03 implemented the Resource attribute cascade for the WASM dispatcher. The same
cascade logic should be reused (extracted to a shared function or reimplemented) in
`bin/emit-event`. Confirm with the S-10.03 implementer whether a shared library is
available or if bash replication is the simpler path.

S-10.07 confirmed Wave 3 hard gates passed. The fact that bash events are now the
only remaining source of thin events makes this story the final parity gap to close.

## Architecture Compliance Rules

Per ADR-015 Wave 4, D-15.3, and Q5 adjudication (2026-05-04):
- The route-through-dispatcher path is REQUIRED, not optional. `bin/emit-event` is
  migrated to a wrapper that calls the dispatcher's `host::emit_event` via the
  legacy-bash-adapter. The standalone-binary enhancement path is NOT chosen.
- `event.source = "bash-adapter"` is the official marker for bash-sourced events
  until they are ported to native WASM.
- Resource attribute cascades from S-10.03 apply equally to bash events.
- EVERY emitted event (WASM-native or bash-via-adapter) MUST route through
  `host::emit_event` and land in `events-*.jsonl`. No separate code path.
- `bin/emit-event` is deprecated (runtime warning + log entry) after T-11 completes;
  physical removal is a v1.1 task tracked under TD-014.
- Coordinate with TD-014 tracking issue before dispatch to avoid conflicting approaches.

**Forbidden:** Bash events that write directly to `events-*.jsonl` without routing
through `host::emit_event`. After Wave 4, there is exactly one emit path.

## Library and Framework Requirements

| Library | Version | Notes |
|---------|---------|-------|
| `uuid` (if Rust binary) | workspace | UUIDv4 for event.id and span_id |
| bash standard library | system | If bin/emit-event remains a bash script |
| `hostname`, `date`, platform commands | system | Resource cascade implementation in bash |

## File Structure Requirements

| File | Action | Notes |
|------|--------|-------|
| `bin/emit-event` | MODIFY or CREATE | Enhanced with Resource + per-event fields; backward-compatible |
| `plugins/vsdd-factory/hooks/*.sh` | NO CHANGE (AC-006 backward compat) | Existing bash hooks unchanged |
| `tests/bash/emit-event-*.bats` | CREATE | bats tests for enhanced emit-event |

## Token Budget Estimate

| Context Source | Estimated Tokens |
|----------------|-----------------|
| This story spec | ~5,500 |
| ADR-015 Wave 4 + D-15.3 bash parity | ~3,000 |
| S-10.03 enrichment implementation (reference) | ~4,000 |
| `bin/emit-event` source | ~2,000 |
| bash hook sources (scan) | ~3,000 |
| Test file context | ~3,000 |
| **Total** | **~20,500** |

Well within 20% of a 200k context window.

## CHANGELOG

| Version | Date | Change |
|---------|------|--------|
| v1.0 | 2026-05-04 | Initial authoring from ADR-015 Wave 4 decomposition. |
| v1.1 | 2026-05-04 | Q5 adjudication: route-through-dispatcher path required (not optional); AC-003 rewritten; T-1 STOP CHECK replaced with dispatcher migration task; T-11 added (bin/emit-event deprecation warning + deferred physical removal to TD-014 v1.1); TD-014 coordination cross-reference added; architecture mapping updated; story_id renamed S-T.08 → S-10.08. |
