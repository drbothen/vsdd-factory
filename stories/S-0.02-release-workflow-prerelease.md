---
document_type: story
level: ops
story_id: "S-0.02"
epic_id: "E-0"
version: "1.2"
status: merged
producer: product-owner
timestamp: 2026-04-27T00:00:00
phase: 1.8
inputs:
  - .factory/stories/v1.0/S-0.2-release-workflow-prerelease.md
  - .factory/specs/architecture/ARCH-INDEX.md
input-hash: "ca6f8e7"
traces_to: .factory/specs/domain-spec/capabilities.md
cycle: v1.0.0-greenfield
points: "2"
depends_on: ["S-0.01"]
blocks: ["S-2.08", "S-4.08", "S-5.07"]
behavioral_contracts: ["BC-9.01.001", "BC-9.01.003"]
verification_properties: []
functional_requirements: ["FR-037"]
priority: "P0"
wave: 1
target_module: .github/workflows/Release.yml
subsystems: ["SS-10"]
estimated_days: 1
assumption_validations: []
risk_mitigations: []
---

# S-0.02: Release workflow prerelease handling

**Epic:** E-0 — Infrastructure Prep
**Phase:** Phase 0 — Infrastructure prep
**Milestone:** `1.0.0-beta.1`
**Tier:** A
**Blocks:** S-2.08, S-4.08, S-5.07 (release gate stories)
**Blocked by:** none (parallel with S-0.01)
**Estimated effort:** S

## Narrative
- **As a** release engineer
- **I want to** push a prerelease tag and have GitHub Release automatically marked as prerelease
- **So that** marketplace users stay on stable and early adopters can opt in to betas

## Goal

The `.github/workflows/Release.yml` workflow currently assumes stable
versions. Extend it to handle prerelease tags (`1.0.0-beta.1`, `1.0.0-rc.2`)
by marking GitHub Releases as pre-release, so users can distinguish stable
from prereleases and marketplace defaults to stable.

## Acceptance criteria

- [ ] Pushing a tag `1.0.0-beta.1` creates a GitHub Release with
  `prerelease: true`
- [ ] Pushing a tag `1.0.0-rc.1` creates a GitHub Release with
  `prerelease: true`
- [ ] Pushing a tag `1.0.0` creates a GitHub Release with `prerelease: false`
  (no regression)
- [ ] Pushing a tag `0.79.5` creates a GitHub Release with
  `prerelease: false` (no regression on existing release stream)
- [ ] Release notes body includes a note about prerelease status at the top
  when applicable
- [ ] "Latest Release" pointer on GitHub continues to point to the most
  recent stable, not prereleases (verified behavior of GH's `latest`
  semantics)

## Tasks

1. Read current `.github/workflows/Release.yml` to identify the
   `softprops/action-gh-release@v2` (or similar) step.
2. Add a conditional computation: `is_prerelease=$(echo "$TAG" | grep -q '-' && echo true || echo false)`
3. Pass `prerelease: ${{ env.IS_PRERELEASE }}` to the action
4. Optionally prepend a prerelease banner to the release body when
   `is_prerelease=true`
5. Test on a throwaway prerelease tag (`0.79.5-test.1` or similar) against a
   fork or a dry-run.

## Artifacts

- `.github/workflows/Release.yml` — conditional prerelease detection + flag

## Contracts

- **Prerelease detection**: any tag matching `*-*` is treated as prerelease.
  This matches semver where prerelease is signaled by a hyphen after the
  version core.
- **GH Release "prerelease" field**: set to `true` for prerelease tags,
  `false` for stable tags. Marketplace tools reading from GH Releases rely
  on this field.

## Test plan

- Manual: create a test tag on a fork, observe the resulting Release is
  marked prerelease.
- Manual after S-0.01 and S-2.08: first `1.0.0-beta.1` tag produces a
  correctly-flagged prerelease on GH.
- No bats coverage needed (YAML workflow; tested via real-run observation).

## Notes

`softprops/action-gh-release@v2` supports `prerelease: true` per its docs.
If we're using a different action, adjust accordingly.

GH Release semantics: "Latest" is the most recent non-prerelease, regardless
of publish order. This is exactly what we want — marketplace `/plugin install`
gets 0.79.4 until 1.0.0 is tagged, even as prereleases ship in between.

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| Release.yml | .github/workflows/Release.yml | effectful-shell (triggers GH API) |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | Tag `1.0.0-` (trailing hyphen only) | Treated as prerelease via `*-*` match |
| EC-002 | Stable tag after prerelease | Latest pointer moves to stable |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| .github/workflows/Release.yml | effectful-shell | Calls GitHub API |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~900 |
| Release.yml | ~600 |
| Tool outputs overhead | ~200 |
| **Total** | **~1,700** |
| Agent context window | 200K for Sonnet |
| **Budget usage** | **~0.9%** |

## Tasks (MANDATORY)

1. [ ] Read `.github/workflows/Release.yml`
2. [ ] Add `IS_PRERELEASE` environment variable computation
3. [ ] Pass prerelease flag to `action-gh-release` step
4. [ ] Add optional prerelease banner to release body
5. [ ] Validate with test tag

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-0.01 | Semver regex extended with `-[0-9A-Za-z.-]+` suffix | Prerelease detection via hyphen | None at this stage |

## Capability Anchor Justification

Anchoring to CAP-028: "Install and update the plugin via Claude Code marketplace" per capabilities.md §CAP-028.
Justification: S-0.02 extends Release.yml to mark GH Releases as prerelease when the pushed tag contains a hyphen. This directly implements the release-channel labeling that allows marketplace users to distinguish stable from prerelease installs — which is the core CAP-028 delivery-channel concern. The prerelease flag is the mechanism by which `/plugin install vsdd-factory` stays on stable while early adopters can opt into betas.

## Stretch-Anchor Disclosure

This story applies the **F-007 (Wave 3) / F-002 (Wave 5) / F-005 (Wave 6) sanctioned-template-anchor pattern**:

- **Story `subsystems[]`**: declares `SS-10` (target_module is `.github/workflows/Release.yml` which lives under SS-10 implementing-module wildcards per ARCH-INDEX line 83).
- **Story `behavioral_contracts[]`**: anchors to `BC-9.01.001` and `BC-9.01.003` BCs (subsystem: SS-09 per BC frontmatter).
- **Pattern**: BC-subsystem (SS-09) != story.subsystems[] (SS-10). This is intentional — `.github/workflows/Release.yml` is invoked by SS-10 as a CLI/release-tooling surface but its semantic contract (semver format / atomic bot commit) is owned by SS-09 BCs that codify the release-tooling discipline.
- **Cross-subsystem ACs**: AC-1 [BC-9.01.001 prerelease tag format recognized; BC-9.01.003 bot commit triggered] and AC-2 [BC-9.01.001 postcondition 1] exercise SS-09 BCs directly. AC-3, AC-4, AC-5, AC-6 are [process-gap] — uncontracted SS-10 behaviors.
- **Codification path**: 11 v1.1 BC candidates (BC-10.13.001..011) registered for currently-uncontracted SS-10 behaviors (Release.yml prerelease flag, stable-tag false branch, prerelease banner, GH "latest" semantics, gate-dependency-closure, shakedown-stability, documentation completeness, SDK-publish gate, marketplace default switch). When SS-10 BC backfill closes, primary anchors will move SS-10 → SS-10 BCs.

## Behavioral Contracts

| BC ID | Title | Trace |
|-------|-------|-------|
| BC-9.01.001 | bump-version.sh accepts semver prerelease format (1.0.0-beta.N, 1.0.0-rc.N) | Complementary cross-wave anchor: the prerelease tag format detected by Release.yml (`*-*`) is the same tag produced by bump-version.sh; the two BCs implement the same prerelease signaling from two sides (tag creation vs release labeling) |
| BC-9.01.003 | release workflow's bot commit atomically writes binaries + plugin.json + marketplace.json | AC: `prerelease: true` GH Release created; bot commit triggered by Release.yml includes the prerelease-flagged metadata |

## Verification Properties

None directly exercised by this story. The prerelease detection logic is verified via manual GH Release observation (no bats coverage needed for YAML workflow per test plan).

## Acceptance Criteria with BC Traces

| AC | Description | BC Trace |
|----|-------------|----------|
| AC-1 | Pushing `1.0.0-beta.1` creates GH Release with `prerelease: true` | BC-9.01.001 (prerelease tag format recognized); BC-9.01.003 (bot commit triggered with correct metadata) |
| AC-2 | Pushing `1.0.0-rc.1` creates GH Release with `prerelease: true` | BC-9.01.001 postcondition 1 (rc.N format accepted) |
| AC-3 | Pushing `1.0.0` creates GH Release with `prerelease: false` (no regression) | [process-gap] (stable release path — no existing SS-10 BC contracts the `prerelease: false` guard; Release.yml is not in any SS-10 BC's module scope). v1.1 BC candidate: BC-10.13.001-release-yml-stable-prerelease-false. |
| AC-4 | Pushing `0.79.5` creates GH Release with `prerelease: false` (no regression on existing stream) | [process-gap] (same gap as AC-3 for legacy version format). v1.1 BC candidate: BC-10.13.001. |
| AC-5 | Release notes body includes prerelease status note at top when applicable | [process-gap] (release notes banner — no existing BC contracts the prerelease banner content). v1.1 BC candidate: BC-10.13.002-release-yml-prerelease-banner. |
| AC-6 | "Latest Release" pointer continues to point to most recent stable | [process-gap] (GH Release "latest" semantics — GitHub API behavior, not contracted by any current BC). v1.1 BC candidate: BC-10.13.003-gh-release-latest-semantics. |

## v1.1 BC/VP Candidates

Process gaps in this story's AC traces that require new BCs in v1.1:

| Candidate ID | Proposed Title | Type | Source AC | Gap Description |
|-------------|---------------|------|-----------|----------------|
| BC-10.13.001-release-yml-stable-prerelease-false | Release.yml: tag without hyphen produces `prerelease: false` GH Release | BC | AC-3, AC-4 | No SS-10 BC contracts the stable-release path in Release.yml; the `*-*` pattern check is uncontracted for the false branch |
| BC-10.13.002-release-yml-prerelease-banner | Release.yml: prerelease releases include status banner in release notes body | BC | AC-5 | No BC contracts the release notes banner content for prerelease releases |
| BC-10.13.003-gh-release-latest-semantics | Release.yml: GH "latest" pointer does not advance on prerelease tags | BC | AC-6 | GitHub API behavior for `latest` is an invariant of the prerelease flag; uncontracted in current BC set |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| Any `*-*` tag treated as prerelease | SS-10 release conventions | CI workflow |
| Cross-wave complementary anchor: BC-9.01.001 (SS-09) + BC-9.01.003 (SS-09) provide version-labeling backing | Wave 6 SS-09 (S-0.01, S-2.04, S-2.08) | BC traceability |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| softprops/action-gh-release | v2 | GitHub Release creation |

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| .github/workflows/Release.yml | modify | Add prerelease detection and flag |
