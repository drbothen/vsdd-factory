---
document_type: story
level: implementation
story_id: S-18.02
epic_id: "E-18"
version: "1.3"
title: "validate-wave-handoff-completeness WASM Gate Crate"
status: draft
producer: story-writer
timestamp: "2026-06-16T00:00:00Z"
last_amended: "2026-06-17 (v1.3) — (C-P6-006) blocks: [] corrected to blocks: [S-18.08]; S-18.08 lists S-18.02 in its depends_on making S-18.08 a direct successor; direct-successor-only blocks convention confirmed across E-18 corpus."
phase: "F3"
level: implementation
cycle: v1.0-feature-context-durability-E18
inputs:
  - .factory/specs/behavioral-contracts/ss-04/BC-4.14.001.md
  - .factory/specs/verification-properties/VP-081.md
  - .factory/specs/verification-properties/VP-083.md
input-hash: fd98182
traces_to:
  - CAP-032
  - .factory/stories/epics/E-18-factory-context-durability.md
wave: 3
priority: P0
points: 8
track: feature
effort_class: medium
depends_on:
  - S-18.01
blocks:
  - S-18.08
behavioral_contracts:
  - BC-4.14.001
verification_properties:
  - VP-081
  - VP-083
domain_invariants: []
tdd_mode: strict
target_module: crates/hook-plugins/validate-wave-handoff-completeness
subsystems:
  - SS-04
estimated_days: 3
assumption_validations: []
risk_mitigations: []
anchored_adrs: []
closes: "issue #173 (partial — validate-wave-handoff-completeness WASM gate)"
modified:
  - "2026-06-17 (v1.3) — (C-P6-006) blocks: [] corrected to blocks: [S-18.08]; S-18.08 explicitly lists S-18.02 in depends_on; direct-successor-only blocks convention confirmed."
  - "2026-06-16 (v1.2) — (F-SP3-002 MAJOR) AC↔PC mis-traces corrected against BC-4.14.001 v1.13: AC-001→PC4, AC-002→PC2a, AC-004→PC7+INV3, AC-005→PC3+PC8+INV3, AC-006→INV2. Red Gate table + Traceability table updated. (M-005) traces_to: added E-18 epic path."
---

# S-18.02 — validate-wave-handoff-completeness WASM Gate Crate

## Narrative

As a factory-dispatcher hook runtime,
I want a WASM PostToolUse gate that validates HANDOFF.md completeness whenever
the file is written or edited,
so that malformed or incomplete wave-handoff artifacts are caught immediately
at write-time rather than discovered later during wave-open.

## Behavioral Contracts

| BC ID | Title | Version | PCs Exercised |
|-------|-------|---------|---------------|
| BC-4.14.001 | validate-wave-handoff-completeness WASM gate blocks HandoffIncomplete on PostToolUse HANDOFF.md writes | v1.13 | PC1, PC2, PC2a, PC3, PC4, PC5, PC6, PC7, PC8, INV1, INV2, INV3 |

## Acceptance Criteria

### AC-001 (traces to BC-4.14.001 postcondition 4 — non-HANDOFF.md no-op)
The WASM gate is triggered on `PostToolUse` events for `Write` and `Edit` tools.
File paths that do NOT match `**/HANDOFF.md` result in immediate no-op (exit 0).

### AC-002 (traces to BC-4.14.001 postcondition 2a — EPIC-COMPLETE detection)
When the written HANDOFF.md contains `next_wave_stories: []`, the gate detects
EPIC-COMPLETE. In EPIC-COMPLETE, it validates only that `epic_status: complete`
is present. All other field-presence checks are skipped. If `epic_status` is
absent, gate emits `HandoffIncomplete: epic_status must be 'complete' when
next_wave_stories is empty list` and exits non-zero.

### AC-003 (traces to BC-4.14.001 postcondition 3 — wave_id=1 no-op)
When `wave_id: 1` is present AND the file is NOT in EPIC-COMPLETE state, the
gate exits 0 (no-op). Wave-1 handoffs are not subject to completeness gating.

### AC-004 (traces to BC-4.14.001 postcondition 7 + invariant 3 step 4 — wave_id > 1 full validation)
When `wave_id > 1` (and not EPIC-COMPLETE), the gate validates all 9 base
required fields are present and non-empty (for scalar fields) or present (for
list fields which may be empty lists):
- Scalar fields (must be non-empty/non-null where noted):
  `wave_id`, `last_verified_develop_sha`, `precompact_flush_sha` (may be null),
  `factory_lock_holder` (may be null)
- List fields (empty list is valid):
  `active_bcs`, `next_wave_stories`, `open_decisions`, `pending_fixes`, `process_gaps`

### AC-005 (traces to BC-4.14.001 postcondition 3 + postcondition 8 + invariant 3 step 5 — wave_id absent fail-closed)
When `wave_id` field is absent from the HANDOFF.md, the gate fails closed:
emits `HandoffIncomplete: wave_id field is required` and exits non-zero.

### AC-006 (traces to BC-4.14.001 invariant 2 — fail-all-at-once)
When multiple fields are missing or malformed, the gate emits a single
`HandoffIncomplete` message listing ALL failing field names in one output,
not one message per field. (BC-4.14.001 INV2: completeness error names all
failing fields.)

### AC-007 (traces to BC-4.14.001 postcondition 7 — scalar vs list distinction)
List fields (`active_bcs`, `next_wave_stories`, `open_decisions`, `pending_fixes`,
`process_gaps`) accept an empty YAML list `[]` as valid. A missing list field
(field absent from YAML) is invalid. A scalar field with value `""` (empty string)
is invalid.

### AC-008 (traces to BC-4.14.001 invariant 1)
The WASM gate is pure-parse: it reads only the written file content. It does NOT
shell out, does NOT read other files, does NOT query git. All validation is
performed on the in-memory YAML parse result.

### AC-009 (traces to BC-4.14.001 invariant 2)
`HandoffMissing` is NEVER emitted by the WASM gate. HandoffMissing is a shell-side
error emitted when HANDOFF.md is absent. The WASM gate is only triggered when
the file IS present (PostToolUse Write/Edit). These are disjoint error classes.

### AC-010 (traces to BC-4.14.001 invariant 3 — 5-step evaluation order)
The gate evaluates in strict order:
1. Non-HANDOFF.md path → no-op (exit 0)
2. `next_wave_stories: []` present → EPIC-COMPLETE check (AC-002)
3. `wave_id == 1` AND NOT EPIC-COMPLETE → no-op (exit 0)
4. `wave_id > 1` → full validation (AC-004)
5. `wave_id` absent → fail-closed (AC-005)
No step is skipped; step 2 must be evaluated before step 3.

### AC-011 (traces to BC-4.14.001 invariant 3 — VP-083 discriminating fixture)
A HANDOFF.md with `wave_id: 1` AND `epic_status: complete` AND malformed
`last_verified_develop_sha: ""` (empty string) MUST be blocked by the gate
(EPIC-COMPLETE path validates epic_status but malformed base scalar is still
an error — wave_id=1 no-op only applies when NOT EPIC-COMPLETE).
This is the F-P32-002 tautology proof fixture from VP-083.

### AC-012 (traces to BC-4.14.001 postcondition 5 — fuel budget / 200-line advisory)
The gate completes within `timeout_ms = 5000` on any HANDOFF.md body up to 200 lines.
When the body exceeds 200 lines, the gate emits a `plugin.log` advisory record with
`level: warn` and `message: "HANDOFF.md body exceeds 200-line advisory cap"` — but
it continues to parse and validate all fields normally. The advisory does NOT cause
the gate to return a block. (BC-4.14.001 INV5 documents this as advisory: the 200-line
cap is a soft cap; the gate never hard-fails on length alone.)

### AC-013 (traces to BC-4.14.001 postcondition 6 + invariant 1 — on_error=continue; fail-open on crash)
When the WASM gate crashes (WASM panic, fuel exhaustion, ABI violation), the dispatcher
`on_error = "continue"` setting causes the gate to fail open (Continue). The tool call
is NOT blocked. The dispatcher internal log records a `plugin.crashed` event. This
behavior is independent of the gate's own logic — it is enforced by the dispatcher
per the registry `on_error = "continue"` field.

### AC-014 (traces to BC-4.14.001 registered config)
The gate is registered in `hooks-registry.toml` as:
- `event = "PostToolUse"`
- `tool = "Write|Edit"`
- `on_error = "continue"`
- `async = false`
- `timeout_ms = 5000`

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| WASM gate crate | `crates/hook-plugins/validate-wave-handoff-completeness/` | Pure (YAML parse only) |
| YAML parser | `serde_yaml` or `serde-yaml` crate (existing workspace dep) | Pure |
| 5-step evaluation logic | `src/lib.rs` | Pure |
| WASM binary output | `target/wasm32-wasi/release/validate_wave_handoff_completeness.wasm` | Pure |
| hooks-registry.toml entry | `plugins/vsdd-factory/hooks-registry.toml` | Pure (config) |

## Red Gate Test Table

| Test name | File | AC | BC clause | Red Gate condition |
|-----------|------|----|-----------|-------------------|
| `test_non_handoff_path_is_noop` | `crates/hook-plugins/validate-wave-handoff-completeness/tests/unit.rs` | AC-001 | BC-4.14.001 PC4 | Fails because crate does not exist |
| `test_epic_complete_valid` | `crates/hook-plugins/validate-wave-handoff-completeness/tests/unit.rs` | AC-002 | BC-4.14.001 PC2a | Fails because crate does not exist |
| `test_epic_complete_missing_epic_status_fails` | `crates/hook-plugins/validate-wave-handoff-completeness/tests/unit.rs` | AC-002 | BC-4.14.001 PC2a | Fails because crate does not exist |
| `test_wave_id_1_noop_when_not_epic_complete` | `crates/hook-plugins/validate-wave-handoff-completeness/tests/unit.rs` | AC-003 | BC-4.14.001 PC3 | Fails because crate does not exist |
| `test_wave_id_gt1_full_validation_all_fields_present` | `crates/hook-plugins/validate-wave-handoff-completeness/tests/unit.rs` | AC-004 | BC-4.14.001 PC7 + INV3 step 4 | Fails because crate does not exist |
| `test_wave_id_absent_fails_closed` | `crates/hook-plugins/validate-wave-handoff-completeness/tests/unit.rs` | AC-005 | BC-4.14.001 PC3 + PC8 + INV3 step 5 | Fails because crate does not exist |
| `test_all_failing_fields_named_in_one_message` | `crates/hook-plugins/validate-wave-handoff-completeness/tests/unit.rs` | AC-006 | BC-4.14.001 INV2 | Fails because crate does not exist |
| `test_empty_list_is_valid_for_list_fields` | `crates/hook-plugins/validate-wave-handoff-completeness/tests/unit.rs` | AC-007 | BC-4.14.001 PC7 | Fails because crate does not exist |
| `test_missing_list_field_is_invalid` | `crates/hook-plugins/validate-wave-handoff-completeness/tests/unit.rs` | AC-007 | BC-4.14.001 PC7 | Fails because crate does not exist |
| `test_five_step_eval_order_step2_before_step3` | `crates/hook-plugins/validate-wave-handoff-completeness/tests/unit.rs` | AC-010 | BC-4.14.001 INV3 | Fails because crate does not exist |
| `test_vp083_f_p32_002_wave1_epic_complete_malformed_base` | `crates/hook-plugins/validate-wave-handoff-completeness/tests/unit.rs` | AC-011 | BC-4.14.001 INV3 + VP-083 | Fails because crate does not exist |
| `test_body_over_200_lines_emits_advisory_but_continues` | `crates/hook-plugins/validate-wave-handoff-completeness/tests/unit.rs` | AC-012 | BC-4.14.001 PC5 + INV5 | Fails because crate does not exist |
| `test_on_error_continue_crash_is_fail_open` | `plugins/vsdd-factory/tests/validate-wave-handoff-completeness.bats` | AC-013 | BC-4.14.001 PC6 | Fails because crate does not exist |

## Tasks

- T-1: Write failing Rust unit tests for all ACs (Red Gate — tests must not compile before crate exists)
- T-2: Create `crates/hook-plugins/validate-wave-handoff-completeness/Cargo.toml` with wasm32-wasi target
- T-3: Implement `src/lib.rs` with 5-step evaluation logic (pure YAML parse)
- T-4: Implement `src/main.rs` WASM entrypoint (reads stdin JSON from dispatcher, writes stdout JSON response)
- T-5: Add `wasm32-wasi` build target to Cargo workspace if not already present
- T-6: Build WASM binary: `cargo build --target wasm32-wasi --release -p validate-wave-handoff-completeness`
- T-7: Copy WASM binary to `plugins/vsdd-factory/hook-plugins/validate-wave-handoff-completeness.wasm`
- T-8: Add hooks-registry.toml entry (PostToolUse, Write|Edit, on_error=continue, async=false, timeout_ms=5000)
- T-9: Verify all Rust unit tests pass: `cargo test -p validate-wave-handoff-completeness`
- T-10: Verify full workspace: `cargo test --workspace --all-targets`

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | HANDOFF.md with malformed YAML (parse error) | Gate exits non-zero with `HandoffIncomplete: YAML parse error: <msg>` |
| EC-002 | HANDOFF.md with `wave_id: 0` | Treated as wave_id > 1 path? No — wave_id=0 is malformed; fail with `HandoffIncomplete: wave_id must be a positive integer` |
| EC-003 | HANDOFF.md with `precompact_flush_sha:` (explicit null) | Valid — null is permitted for precompact_flush_sha |
| EC-004 | HANDOFF.md with `precompact_flush_sha: ""` | Invalid — empty string is not null; scalar field rules apply |
| EC-005 | HANDOFF.md with `active_bcs: []` | Valid — empty list is valid for list fields (PC7) |
| EC-006 | HANDOFF.md with `active_bcs` key absent | Invalid — missing list field is an error (AC-007) |
| EC-007 | PostToolUse Write on `.factory/STATE.md` | Non-HANDOFF.md path → no-op (AC-001) |
| EC-008 | Dispatcher timeout (>5000ms for parse) | `on_error=continue` → advisory only; no block |
| EC-009 | wave_id=1 + EPIC-COMPLETE + malformed last_verified_develop_sha | EPIC-COMPLETE path engaged (step 2 before step 3); epic_status validated; remaining scalar-field malformation is NOT caught by gate (VP-083 F-P32-002 tautology: step 2 short-circuits before step 4 full-validation) |

**Correction to AC-011**: VP-083 F-P32-002 is the discriminating fixture that proves the tautology — when wave_id=1 AND EPIC-COMPLETE, step 2 runs first (EPIC-COMPLETE detection), validates epic_status, and exits. Malformed `last_verified_develop_sha` is NOT checked in EPIC-COMPLETE path. The fixture verifies this ordering is correct, not that it should block. The gate EXITS 0 in this case (epic_status valid). This is correct behavior per 5-step eval order.

## Forbidden Dependencies

`crates/hook-plugins/validate-wave-handoff-completeness` MUST NOT:
- Shell out to any external process (pure WASM, no `std::process::Command`)
- Read from the filesystem directly (all input via dispatcher stdin JSON)
- Import `crates/factory-dispatcher` (would create circular dependency)
- Use `unwrap()` or `expect()` in non-test code paths

## Token Budget

| Item | Estimated tokens |
|------|-----------------|
| This story file | ~3 500 |
| BC-4.14.001 | ~4 000 |
| VP-081 | ~2 000 |
| VP-083 | ~2 000 |
| Rust unit tests (new) | ~4 000 |
| `src/lib.rs` (new) | ~3 000 |
| `src/main.rs` (new) | ~1 500 |
| hooks-registry.toml section | ~500 |
| Tool outputs (cargo test) | ~2 000 |
| **Total** | **~22 500** |

Budget is ~22.5k tokens, within 20-30% of a 200k-token context window. Safe for one dispatch.

## Dependencies

- **Depends on:** S-18.01 (HANDOFF.md schema must be defined before gate is implemented; gate validates the schema produced by wave-handoff skill)
- **Blocks:** none in this epic

## Traceability

| Source | ID | Coverage |
|--------|----|---------|
| Behavioral Contract | BC-4.14.001 PC1 | AC-010 (happy-path final result when all fields valid) |
| Behavioral Contract | BC-4.14.001 PC2 | AC-013 (HandoffIncomplete block — on_error=continue is fail-open on crash, distinct from intentional block) |
| Behavioral Contract | BC-4.14.001 PC2a | AC-002 |
| Behavioral Contract | BC-4.14.001 PC3 | AC-003, AC-005 (wave_id=1 no-op; wave_id absent fail-closed clause) |
| Behavioral Contract | BC-4.14.001 PC4 | AC-001 (non-HANDOFF.md no-op) |
| Behavioral Contract | BC-4.14.001 PC5 | AC-012 (fuel budget / 200-line advisory) |
| Behavioral Contract | BC-4.14.001 PC6 | AC-013 (on_error=continue; fail-open on crash) |
| Behavioral Contract | BC-4.14.001 PC7 | AC-004, AC-007 (scalar vs list field scope) |
| Behavioral Contract | BC-4.14.001 PC8 | AC-005 (wave-1 no-op conditional on NOT EPIC-COMPLETE) |
| Behavioral Contract | BC-4.14.001 INV1 | AC-008 |
| Behavioral Contract | BC-4.14.001 INV2 | AC-006, AC-009 |
| Behavioral Contract | BC-4.14.001 INV3 | AC-004 (step 4), AC-005 (step 5), AC-010, AC-011 |
| Behavioral Contract | BC-4.14.001 INV5 | AC-012 (200-line cap is advisory; never hard-fails) |
| Behavioral Contract | BC-4.14.001 registered config | AC-014 |
| Verification Property | VP-081 | AC-001, AC-004, AC-010 (wave cannot close without verified handoff) |
| Verification Property | VP-083 | AC-003, AC-010, AC-011 (no-op on wave-1 or non-HANDOFF.md writes; F-P32-002 tautology proof) |
| Domain Capability | CAP-032 | All ACs (context-durability feature) |

## Previous Story Intelligence

S-18.01 defines the HANDOFF.md schema. This story implements the gate that validates
it. Key architecture lesson from existing hook-plugin crates (e.g.,
`validate-burst-log.wasm`): the WASM main entrypoint reads a JSON dispatch packet
from stdin and writes a JSON response to stdout. The dispatcher passes the tool
call context (including file path and written content) in the dispatch packet.
Implementer must read the dispatcher protocol schema from
`crates/factory-dispatcher/src/invoke.rs` before implementing `src/main.rs`.

## Architecture Compliance Rules

- Pure WASM: no filesystem access, no process spawning (BC-4.14.001 INV1)
- `wasm32-wasi` target: use `wasm32-wasip1` if `wasm32-wasi` is deprecated in toolchain
- Crate must be added to `Cargo.toml` workspace members
- All WASM hook-plugin crates follow the pattern in `crates/hook-plugins/` — examine
  existing crates for the exact dispatcher JSON protocol before implementing
- `#[deny(warnings)]` in lib.rs (CI enforces `-D warnings`)
- No `unwrap()` in non-test code

## Library & Framework Requirements

- `serde` + `serde_yaml` (or `serde-yaml`) — check existing workspace Cargo.toml for pinned version
- `serde_json` for dispatcher protocol IO — pinned version per workspace
- Rust toolchain: pinned per `rust-toolchain.toml`
- No additional crate dependencies without architect review

## File Structure Requirements

| File | Action | Notes |
|------|--------|-------|
| `crates/hook-plugins/validate-wave-handoff-completeness/Cargo.toml` | Create | wasm32-wasi target crate |
| `crates/hook-plugins/validate-wave-handoff-completeness/src/lib.rs` | Create | 5-step evaluation pure logic |
| `crates/hook-plugins/validate-wave-handoff-completeness/src/main.rs` | Create | WASM entrypoint (dispatcher protocol) |
| `crates/hook-plugins/validate-wave-handoff-completeness/tests/unit.rs` | Create | Red Gate unit tests |
| `plugins/vsdd-factory/hook-plugins/validate-wave-handoff-completeness.wasm` | Create (built) | Compiled WASM binary |
| `plugins/vsdd-factory/hooks-registry.toml` | Modify | Add PostToolUse entry |
| `Cargo.toml` (workspace root) | Modify | Add crate to workspace members |

## Changelog

- v1.3 (2026-06-17): (C-P6-006) blocks: [] corrected to blocks: [S-18.08]; S-18.08 explicitly lists S-18.02 in depends_on (direct successor); direct-successor-only blocks convention confirmed across E-18 corpus (blocks = only stories that list this story in their depends_on).
- v1.2 (2026-06-16): (F-SP3-002 MAJOR) AC↔PC mis-traces corrected against BC-4.14.001 v1.13 actual PC numbering. (F-SP3-006 adjudicated) BC table descriptive-Title column is editorial; no POLICY-7 verbatim violation found in formal traceability cites (all formal cites use BC ID + clause only, not `BC-X — <title>` form). AC-001: postcondition 1 → postcondition 4 (non-HANDOFF.md no-op is PC4, not PC1 happy-path). AC-002: postcondition 2 → postcondition 2a (EPIC-COMPLETE is the conditional 2a branch). AC-004: postcondition 4 → postcondition 7 + invariant 3 step 4 (wave_id>1 full validation is field-scope PC7 + INV3 step 4; PC4 is the non-HANDOFF.md no-op). AC-005: postcondition 5 → postcondition 3 + postcondition 8 + invariant 3 step 5 (wave_id absent fail-closed is in PC3 body + PC8 + INV3 step 5; PC5 is fuel budget). AC-006: postcondition 6 → invariant 2 (fail-all-at-once is INV2; PC6 is on_error=continue). BC table title updated to H1-verbatim from BC file. Red Gate table BC clause column corrected for AC-001, AC-002, AC-004, AC-005. Traceability table rebuilt with correct PC↔AC mapping.
- v1.1 (2026-06-16): Frontmatter normalization (epic_id "E-18", phase "F3", level implementation). Fix 3: added AC-012 (BC-4.14.001 PC5/INV5 — 200-line fuel-budget advisory) and AC-013 (BC-4.14.001 PC6 — on_error=continue fail-open crash handling). AC-014 inherits old AC-012 registered-config content. Two new Red Gate test rows added. Traceability table updated (PC5, PC6, INV5 rows).
- v1.0 (2026-06-16): Initial authoring by story-writer for E18 context-durability feature
