---
document_type: story
level: implementation
story_id: S-18.04a
epic_id: "E-18"
version: "1.10"
title: "precompact-flush Native WASM Plugin Core"
status: draft
producer: story-writer
timestamp: "2026-06-16T00:00:00Z"
last_amended: "(v1.10) — (ADR-028 v1.2; F-NW2-005/006/007/008/009 + consistency ISSUE-2/4) AC-011 uniform git -C summary fix (steps 5/6/7/10 bare git corrected); renew_lock pure RenewOutcome signature; read-absent-log-as-empty; NoOp clean-state exit0; malformed-fence-without-lock NoOp parity; DURABILITY DEGRADED advisory (two-form); phantom F-NW-010 label → stable anchor; +Red Gates (B13); BC cite v1.16→v1.17."
phase: "F3"
cycle: v1.0-feature-context-durability-E18
inputs:
  - .factory/specs/behavioral-contracts/ss-07/BC-7.07.001.md
  - .factory/specs/verification-properties/VP-082.md
  - .factory/specs/verification-properties/VP-085.md
  - .factory/specs/architecture/decisions/ADR-028-e18-precompact-flush-native-wasm-migration.md
input-hash: 449dcc4
traces_to:
  - CAP-032
  - .factory/stories/epics/E-18-factory-context-durability.md
wave: 2
priority: P0
points: 13
track: feature
effort_class: large
depends_on:
  - S-18.00
  - S-17.04
blocks:
  - S-18.03
  - S-18.04b
  - S-18.07
  - S-18.08
behavioral_contracts:
  - BC-7.07.001
verification_properties:
  - VP-082
  - VP-085
domain_invariants: []
tdd_mode: strict
target_module: crates/hook-plugins/precompact-flush/src/main.rs
target_module_secondary:
  - crates/factory-lock/src/lib.rs
subsystems:
  - SS-07
  - SS-04
estimated_days: 5
assumption_validations: []
risk_mitigations: []
anchored_adrs:
  - ADR-028
closes: "issue #173 (partial — precompact-flush native WASM plugin core implementation)"
modified:
  - "2026-06-20 (v1.10) — (ADR-028 v1.2; F-NW2-005/006/007/008/009 + ISSUE-2/4) AC-011 bare git steps 5/6/7/10 → -C <wt> form; phantom F-NW-010 → stable anchor; AC-017 DURABILITY DEGRADED two-form advisory; AC-018 renew_lock pure RenewOutcome signature + caller pattern + presence-pre-check; AC-007 absent-log-as-empty; AC-011 step3/step5 RenewOutcome+INV5; +5 Red Gates (B13); File Structure lib.rs signature; EC-011/012/013/014/015 updates; BC cite v1.16→v1.17."
  - "2026-06-20 (v1.9) — (ADR-028 v1.1; spec-re-convergence F-NW-001/003/008/009/010) AC-001 env_allow +PATH/+SSH_AUTH_SOCK; uniform git -C <wt> on AC-004/005/006/008/009; AC-007 read-modify-write + single-writer note; AC-018 expires_at format pin + renew Err/caller-downgrade + LF/mode; +6 Red Gate rows; +Precondition-4/Decision-3 traceability rows; BC cite v1.15→v1.16; behavioral ACs preserved."
  - "2026-06-20 (v1.8) — (ADR-028) mechanism rewrite bash→native WASM plugin: target_module → crates/hook-plugins/precompact-flush/src/main.rs; target_module_secondary [crates/factory-lock/src/lib.rs]; subsystems: [SS-07, SS-04]; file list → Rust crate deliverables; registry stanza → native WASM (binary_allow=[git] only); AC-001/003/007/010/011/013/014 mechanism-updated; AC-017+AC-018 added (worktree discovery + native renewal); Red Gate → cargo unit + bats integration; BC-7.07.001 cite v1.14→v1.15; behavioral ACs preserved."
  - "2026-06-20 (v1.7) — (adversary S-18.04a LOCAL F-ADV-003) added reset-failure-block + diverged-no-reset Red Gate Test Table rows for the append-failure branch matrix (BC-7.07.001 PC8 step 3b/step 4 canonical TVs); test-count note refreshed."
  - "2026-06-20 (v1.6) — (adversary S-18.04a LOCAL pass-1 F-PF1-001) corrected factory-lock-write.sh dependency anchor hooks/ → bin/ + BC-7.07.001 cite v1.13→v1.14; precompact-flush.sh location unchanged (hooks/)."
  - "2026-06-17 (v1.5) — (C-P7-002) S-18.03 and S-18.07 added to blocks: both list S-18.04a in depends_on (direct successors); reverse edges were missing."
  - "2026-06-17 (v1.4) — (C-P6-006 sibling-sweep) S-18.08 added to blocks; S-18.08 explicitly lists S-18.04a in depends_on (direct successor per direct-successor-only blocks convention)."
  - "2026-06-17 (v1.3) — (F-P5-001) BC-7.07.001 version cite v1.12→v1.13 in Behavioral Contracts table."
  - "2026-06-16 (v1.2) — (M-005) traces_to: added E-18-factory-context-durability.md epic path."
---

# S-18.04a — precompact-flush Native WASM Plugin Core

## Narrative

As the factory-dispatcher runtime receiving a PreCompact event,
I want a native WASM plugin (`crates/hook-plugins/precompact-flush/`) that commits
STATE.md to the factory-artifacts branch BEFORE compaction proceeds,
so that context-window transitions do not lose uncommitted pipeline state,
and the precompact-flush-log provides an auditable record of every pre-compaction
commit with SHA, timestamp, cycle, and step.

The plugin is implemented as a native Rust WASM crate (not a bash script routed
through legacy-bash-adapter.wasm) per ADR-014 standing policy and ADR-028 migration
decision. Lock renewal is performed natively via `crates/factory-lock::renew_lock()`
with no bash subprocess. The factory-artifacts worktree path is discovered at runtime
via `git worktree list --porcelain` (ADR-028 §Decision 1).

## Behavioral Contracts

| BC ID | Title | Version | PCs Exercised |
|-------|-------|---------|---------------|
| BC-7.07.001 | precompact-flush native WASM plugin fires synchronously on PreCompact; hermetic (STATE.md + git only); renews factory lock natively when held via crates/factory-lock::renew_lock() (no-op when absent); commits (local) with `PreCompact flush <cycle>/<step>` message; appends commit SHA to precompact-flush-log; pushes to remote; on append failure: SHA-pinned reset (`git reset --soft SHA_B^` when HEAD==SHA_B; no-reset + human-intervention exit 2 when HEAD!=SHA_B); exit 2 on commit failure (local; no network) or push failure (network); fail-open on crash | v1.17 | PC1–PC8, INV1–INV7, Precondition 4 |

## Acceptance Criteria

### AC-001 (traces to BC-7.07.001 precondition 1 — hooks-registry.toml registration)
`precompact-flush.wasm` is registered in `hooks-registry.toml` as a native WASM
plugin with the following stanza (exact TOML; no `[hooks.config]` block; no `shell_bypass_acknowledged`):

```toml
[[hooks]]
name = "precompact-flush"
event = "PreCompact"
plugin = "hook-plugins/precompact-flush.wasm"
priority = 100
timeout_ms = 30000
on_error = "continue"
async = false

[hooks.capabilities.read_file]
path_allow = [".factory/"]

[hooks.capabilities.write_file]
path_allow = [".factory/"]

[hooks.capabilities.exec_subprocess]
binary_allow = ["git"]
env_allow = ["HOME", "GIT_CONFIG_GLOBAL", "XDG_CONFIG_HOME", "PATH", "SSH_AUTH_SOCK"]
```

`binary_allow = ["git"]` ONLY — `"bash"` is explicitly absent (ADR-028 §Decision 2).
No `shell_bypass_acknowledged`. No `legacy-bash-adapter.wasm`.
`PATH` is required for git to locate `ssh` and credential helpers (needed by `git push`).
`SSH_AUTH_SOCK` is required for SSH agent communication. Without these, `git push` over
SSH will fail with authentication errors (the dispatcher's `exec_subprocess` runs with
`env_clear` followed by re-injection of only the names listed in `env_allow`).
(traces to BC-7.07.001 Precondition 1 + ADR-028 §Decision 1 F-NW-001)

### AC-002 (traces to BC-7.07.001 postcondition 7 — STATE.md unreadable: exit 0 + warn)
On invocation, the plugin reads `STATE.md` from the factory-artifacts worktree
(discovered via AC-017). If STATE.md is absent or unreadable, the plugin exits 0
(fail-open) and writes a warning to stderr:
`precompact-flush: STATE.md unreadable; flush skipped.`
(No commit, no log append, no push on this path.)

### AC-003 (traces to BC-7.07.001 postcondition 3 + ADR-028 §Decision 2 — factory lock renewal: native Rust, no bash)
The plugin checks the `factory_lock:` block in STATE.md. If `factory_lock:` is absent
or `factory_lock.holder` is absent/null, the lock is not held and the lock-renewal step
is skipped entirely (no-op per ADR-025 opt-in model). If the lock IS held, the plugin
natively updates `factory_lock.expires_at = now + 2700s` (UTC ISO-8601) via
`crates/factory-lock::renew_lock()` — a pure Rust in-process update with NO
`exec_subprocess("bash", ...)` call for renewal. The `holder` and `locked_at` fields
are preserved unchanged. Lock renewal failure is non-fatal (advisory warning to stderr;
flush proceeds). Lock renewal failure does NOT cause exit 2 — per BC-7.07.001 PC3
and EC-004.

### AC-004 (traces to BC-7.07.001 postcondition 4 — factory-artifacts commit: git add sub-step)
The plugin stages STATE.md (and any other factory-artifacts files with pending changes)
via `exec_subprocess("git", &["-C", &wt, "add", "-u"])` in the factory-artifacts
worktree (where `wt` is the absolute worktree path discovered in AC-017). The `-u` flag
stages only tracked files (no untracked files pulled in). This is step 4 of the canonical
execution order per BC-7.07.001 INV3.
Note: `wt` is the absolute worktree path discovered in AC-017. ALL git subprocesses
except the initial `git worktree list --porcelain` MUST use `-C <wt>` (ADR-028 §Decision 3
F-NW-003). Bare `git` commands without `-C` would operate on the main repo, not the
factory-artifacts worktree.

### AC-005 (traces to BC-7.07.001 postcondition 4 + postcondition 1 + invariant 4 — LOCAL commit with canonical message)
The plugin creates a LOCAL git commit (does not push yet) via `exec_subprocess("git",
&["-C", &wt, "commit", "-m", msg])` (where `wt` is the absolute worktree path from
AC-017). Context identity (`<cycle>` and `<step>`) is derived from
the `current_cycle:` and `current_step:` fields in STATE.md frontmatter (BC-7.07.001
PC1 — filesystem read via host `read_file`; NOT in-context reasoning, NOT
`current_wave:` phantom field). The commit message MUST be:
`PreCompact flush <cycle>/<step> <ISO-8601-timestamp>` (BC-7.07.001 INV4 — canonical
format). The subject MUST begin with the literal prefix `PreCompact flush ` (capital
P, capital C, one space between words, trailing space — case-sensitive per BC-5.41.003
INV3). If there are no pending changes on factory-artifacts, the plugin exits 0
silently without creating an empty commit (BC-7.07.001 INV5).

### AC-005b (traces to BC-7.07.001 postcondition 6 — exit 2 on git commit LOCAL failure)
If the `git commit` subprocess exits non-zero (lock contention, permission error, disk
full, or other LOCAL filesystem error), the plugin exits 2. This signals the dispatcher
to propagate `block_intent=true` and block compaction. A diagnostic message is written
to stderr before exiting 2. At this point: no precompact-flush-log entry has been
appended and no push has been attempted. `git commit` is a purely LOCAL operation —
network errors cannot originate here.

### AC-006 (traces to BC-7.07.001 postcondition 8 — SHA_B capture immediately after commit)
IMMEDIATELY after the local commit succeeds, the plugin captures
`SHA_B` via `exec_subprocess("git", &["-C", &wt, "rev-parse", "HEAD"])` (where `wt` is
the absolute worktree path from AC-017). This capture happens
BEFORE any append to the precompact-flush-log (BC-7.07.001 INV3 step 6: SHA_B capture
MUST precede the append attempt so SHA_B is available on append failure).

### AC-007 (traces to BC-7.07.001 postcondition 8 — precompact-flush-log append, newline-termination MANDATORY)
The plugin appends a 4-field log entry to `.factory/hooks/precompact-flush-log`
via host `read_file` + string concatenation + host `write_file` (read-modify-write;
NOT OS-atomic append — `write_file` calls `std::fs::write` which is a full overwrite).
The implementation reads existing log content via `read_file`, concatenates the new
`\n`-terminated 4-field line `<ISO-timestamp> <SHA_B> <cycle>/<step> commit\n`, and
writes the combined result back via `write_file`.
The appended line has the form `<ISO-timestamp> <SHA_B> <cycle>/<step> commit\n` where
the trailing `\n` is part of the string written. The append MUST produce a `\n`-terminated
line as the final byte of the file. Field-4 is the literal string `commit` (never a
variable). The file is created if absent (first write creates it). The implementation
uses host `write_file` — NOT `exec_subprocess("echo", ...)` or `exec_subprocess("printf", ...)`.

**Absent-log treatment (F-NW2-008 / ADR-028 §Decision 12):** If `host::read_file` on
`.factory/hooks/precompact-flush-log` returns an error — including `CAPABILITY_DENIED`
when the file does not yet exist (`read_file` requires the file to exist for
`canonicalize()` to succeed) — the plugin MUST treat the error as EMPTY prior content
(`""`). The new entry is concatenated to `""` and written via `host::write_file` (which
supports creating new files per BC-2.02.011 EC-005). This is NOT a failure; it is the
intended first-flush behavior. Do NOT treat a `read_file` error on the log path as an
append failure (which would trigger the SHA-pinned reset guard). Only the STATE.md
read error is treated as unrecoverable (AC-002); the log-path read error is always
treated as an empty baseline.

Single-writer guarantee: under normal factory operation (factory-lock held), only one
session performs PreCompact flushes (the `precompact-flush` plugin) and one session
performs pruning (the `precompact-flush-prune.sh` invoked by `check-state-health`). The
dispatcher processes hooks synchronously within a plugin tier, preventing concurrent
`precompact-flush` invocations in the same session. A theoretical race between
`precompact-flush` and `precompact-flush-prune.sh` across concurrent sessions could lose
a log entry, but the factory-lock mechanism serializes typical usage. The bounded
data-loss window is acceptable: the flush COMMIT to factory-artifacts is the durability
guarantee; the log is the secondary audit record. The next successful flush will append
its own entry.

### AC-008 (traces to BC-7.07.001 postcondition 8 — append failure SHA-pinned concurrent-commit guard)
If the log append FAILS (host `write_file` returns an error), the plugin compares
`exec_subprocess("git", &["-C", &wt, "rev-parse", "HEAD"])` (CURRENT_HEAD) to `SHA_B`
(where `wt` is the absolute worktree path from AC-017):
- If CURRENT_HEAD == SHA_B (no intervening commits): run
  `exec_subprocess("git", &["-C", &wt, "reset", "--soft", &format!("{}^", sha_b)])` to
  undo the local commit (SHA-PINNED to `SHA_B^`, NOT `HEAD~1`), then exit 2 (block
  compaction). Write diagnostic: `precompact-flush: SHA append to precompact-flush-log failed; orphan commit reverted (SHA_B=<sha>); blocking compaction`.
- If CURRENT_HEAD != SHA_B (intervening commits existed): exit 2 immediately with
  message: `precompact-flush: append failed; concurrent commit advanced HEAD; SHA_B=<sha_b>; current HEAD=<current_head>; human intervention required`.
  Do NOT reset (SOUL.md Rule 4: no silent data loss).
- If the reset itself fails (step 3a reset exits non-zero): exit 2; human intervention
  required.

### AC-009 (traces to BC-7.07.001 postcondition 6b — push failure exit 2; postcondition 5 — push success exit 0)
If the log append SUCCEEDS, the plugin runs
`exec_subprocess("git", &["-C", &wt, "push", "origin", "factory-artifacts"])` (where
`wt` is the absolute worktree path from AC-017) on the factory-artifacts worktree.
- If push fails: exit 2 with message `precompact-flush: git push failed; local commit <SHA_B> and log entry intact; retry is push-only`. Compaction is blocked. Local commit and log entry are retained (NOT rolled back).
- If push succeeds: exit 0.

### AC-010 (traces to BC-7.07.001 invariant 6 — plugin error hardening)
The plugin uses idiomatic Rust error propagation (`Result<T, E>`) throughout. All
fallible operations return `Result` and propagate errors via the `?` operator. No
`unwrap()` or `expect()` in non-test code paths. Panics are treated as crashes
triggering `on_error = "continue"` fail-open behavior (BC-7.07.001 PC9 / INV6).
`set -euo pipefail` does NOT apply (this is a Rust binary, not a shell script).

### AC-011 (traces to BC-7.07.001 invariant 3 — canonical execution order)
The canonical execution order is strictly (per BC-7.07.001 INV3 — no step may be
reordered):
1. Discover factory-artifacts worktree path via `git worktree list --porcelain` (AC-017) — fail-open if not found
2. Read STATE.md via host `read_file` — extract `current_cycle:` + `current_step:` fields (PC1)
3. Check `factory_lock:` block via `renew_lock(state_md_content)` (ADR-028 §Decision 9):
   - `Ok(RenewOutcome::NoOp)` — lock key absent (regardless of fence shape) OR lock null/absent → skip step 4 (no write_file call); proceed to step 5
   - `Ok(RenewOutcome::Renewed(content))` — lock held and expires_at updated → proceed to step 4 (write_file with content)
   - `Err(Malformed)` — factory_lock: key IS present but block is malformed → advisory warn to stderr; skip step 4; proceed to step 5
4. If `RenewOutcome::Renewed`: call `host::write_file(".factory/STATE.md", content)` — writes renewed STATE.md; native Rust; no bash exec; updates `expires_at = now + 2700s`; preserves `holder` and `locked_at`
5. `exec_subprocess("git", &["-C", &wt, "add", "-u"])` — stage tracked changes (PC4). After staging, check `exec_subprocess("git", &["-C", &wt, "diff", "--cached"])`: if step 3 returned `RenewOutcome::NoOp` AND staged diff is empty → apply INV5 (exit 0 silently; do NOT create empty commit). If step 3 returned `RenewOutcome::Renewed`, the renewal write guarantees at least STATE.md is staged; INV5 does not apply.
6. `exec_subprocess("git", &["-C", &wt, "commit", "-m", msg])` (LOCAL — no network); exit 2 immediately on commit failure (PC6 / AC-005b)
7. `SHA_B = exec_subprocess("git", &["-C", &wt, "rev-parse", "HEAD"])` — IMMEDIATELY after commit, BEFORE append (PC8)
8. Append 4-field line to precompact-flush-log via host write_file — MUST be `\n`-terminated; treat absent-log read error as empty baseline (AC-007; PC8)
9. If append fails: compare CURRENT_HEAD to SHA_B → if equal: `git -C <wt> reset --soft SHA_B^` (SHA-PINNED) then exit 2; if not equal: exit 2 with human-intervention message (do NOT reset) (PC8)
10. If append succeeds: `exec_subprocess("git", &["-C", &wt, "push", "origin", "factory-artifacts"])` (NETWORK) (PC6b)
11. If push fails: exit 2 with diagnostic; local commit + log entry retained; retry is push-only (PC6b)
12. If push succeeds: exit 0 (PC5)

Note: steps are numbered 1–12 here to reflect the addition of step 1 (worktree
discovery per ADR-028 §Decision 1). BC-7.07.001 INV3 labels them 1–11 with discovery
implicit in "read STATE.md". No behavioral reordering.

**Uniform `-C <wt>` rule (ADR-028 §Decision 3 / B7):** Every git subprocess in steps 5, 6, 7, 9, 10 MUST use
`exec_subprocess("git", &["-C", &wt, ...])` where `wt` is the absolute worktree path
discovered in step 1 (AC-017). The ONLY git command that does NOT use `-C` is the initial
`git worktree list --porcelain` in step 1, which intentionally targets the main repo.
Bare `git` commands without `-C` would operate on the main repo (develop branch) and
corrupt the SHA guard or commit to the wrong branch.

### AC-012 (traces to BC-7.07.001 invariant 7 — log pruning deferred; append-only in v1)
The precompact-flush-log is append-only. The plugin NEVER truncates or rewrites
the log. Log pruning (prune to most-recent-500 entries when count >1000) is
explicitly deferred to `precompact-flush-prune.sh` (S-18.04b, invoked by
check-state-health, NOT by this plugin per BC-7.07.001 INV7).

### AC-013 (traces to BC-7.07.001 postcondition 3 + invariant 4 — lock renewal failure is advisory; native path)
If `crates/factory-lock::renew_lock()` returns an error during lock renewal (AC-003)
— malformed frontmatter, parse failure, or write failure — the plugin writes an
advisory warning to stderr and proceeds with the flush commit anyway. Lock renewal
failure does NOT cause exit 2 and does NOT block compaction. This is the "advisory"
semantics per BC-7.07.001 PC3 and EC-004. No bash subprocess is involved in renewal.

### AC-014 (traces to BC-7.07.001 invariant 1 — hermetic; VP-085)
The plugin is hermetic: it interacts with the outside world ONLY via:
- Host `read_file` / `write_file` (path_allow = [".factory/"])
- `exec_subprocess("git", ...)` (binary_allow = ["git"] only)
No `curl`, no `jq`, no `python`, no `bash`, no `node`, no `npm`, no external network
calls except `git push` (via exec_subprocess). The plugin does NOT read from
`custom_instructions`, environment variables set by the LLM, or any in-context mechanism.

### AC-015 (traces to BC-7.07.001 invariant 3 step 7 — canonical log path)
The precompact-flush-log path is `.factory/hooks/precompact-flush-log` (no `.log`
extension, no `.txt` extension — exact path per BC-7.07.001 Architecture Anchors and
INV3 step 7).

### AC-016 (traces to BC-7.07.001 invariant 7 — log pruning deferred to S-18.04b)
Log pruning is NOT performed by this plugin. The plugin MUST NOT contain any pruning
logic. Pruning is exclusively handled by `precompact-flush-prune.sh` (S-18.04b),
invoked by `check-state-health` only (never by the precompact-flush plugin itself).

### AC-017 (traces to ADR-028 §Decision 1 + §Decision 13 — runtime worktree discovery via git worktree list; LOUD advisory on failure)
The native WASM plugin discovers the factory-artifacts worktree mount path at runtime
by executing `exec_subprocess("git", &["worktree", "list", "--porcelain"])` and parsing
the stanza whose `branch` line is `refs/heads/factory-artifacts`. The `worktree` line
of that stanza is the absolute path used for all subsequent factory-artifacts filesystem
operations (STATE.md read, log append, git commands).

**Failure mode — DURABILITY DEGRADED advisory (F-NW2-009 / ADR-028 §Decision 13):**

Two distinct failure cases require LOUD advisory output (uppercase `DURABILITY DEGRADED`
prefix) to stderr, followed by `exit 0` (fail-open per `on_error = continue`):

- If the `git worktree list --porcelain` subprocess exits non-zero (git binary not
  found, PATH issue, git process crash, or other environment error):
  Write to stderr exactly: `precompact-flush: DURABILITY DEGRADED — git worktree list
  command failed (exit <N>); factory-artifacts worktree cannot be discovered; flush
  SKIPPED this compaction event. Check PATH/git configuration.`
  Then exit 0.

- If the command succeeds but no stanza with `branch refs/heads/factory-artifacts` is
  found in the output (configuration error — worktree not mounted):
  Write to stderr exactly: `precompact-flush: DURABILITY DEGRADED — factory-artifacts
  branch not found in git worktree list output; flush SKIPPED this compaction event.
  Ensure the factory-artifacts worktree is mounted at .factory/ (run: git worktree add
  .factory factory-artifacts).`
  Then exit 0.

The `DURABILITY DEGRADED` prefix (uppercase) is mandatory — it distinguishes a
silently-disabled durability session from a normal no-op clean-state exit, making the
degraded state detectable in session logs. Do NOT use a quiet advisory message for these
failure cases. (traces to BC-7.07.001 PC4; ADR-028 §Decision 13 F-NW2-009)

No hard-coded `.factory` path assumption. The path is always derived from the
`git worktree list` output.

### AC-018 (traces to ADR-028 §Decision 2 + §Decision 9 + BC-7.07.001 PC3 — native lock renewal; pure content-in/content-out; no bash subprocess)
The plugin renews `factory_lock.expires_at = now + 2700s` natively in Rust via
`crates/factory-lock::renew_lock(state_md_content: &str) -> Result<RenewOutcome, LockError>`.
The `binary_allow` registry stanza contains only `["git"]` — no `exec_subprocess("bash", ...)`
call is ever made for lock renewal. The `crates/factory-lock` crate is a PURE LIBRARY
with no `std::fs` or host I/O calls — it is WASM-hermetic. The PLUGIN owns all
`host::read_file` and `host::write_file` calls; `crates/factory-lock` receives and
returns string content only.

**Binding signature and RenewOutcome enum (F-NW2-005 / ADR-028 §Decision 9):**

```rust
pub enum RenewOutcome {
    /// Lock was absent (or key absent regardless of fence shape) — STATE.md unchanged.
    NoOp,
    /// Lock was held and expires_at was updated. Contains the new full STATE.md content.
    Renewed(String),
}

pub fn renew_lock(state_md_content: &str) -> Result<RenewOutcome, LockError>
```

The path-based form `renew_lock(&Path)` or `renew_lock(&factory_artifacts_path)` is
STRUCK (F-NW2-005). Only the pure content-in/content-out form above is acceptable.

**Caller pattern in precompact-flush plugin:**

1. `let content = host::read_file(".factory/STATE.md")?;` — read STATE.md (errors handled per AC-002)
2. `match renew_lock(&content) { ... }`:
   - `Ok(RenewOutcome::NoOp)` → no `host::write_file` call; STATE.md unchanged; proceed to `git add`
   - `Ok(RenewOutcome::Renewed(new_content))` → call `host::write_file(".factory/STATE.md", new_content)`; proceed to `git add`
   - `Err(LockError::Malformed)` → advisory warn to stderr; no write_file call; proceed to `git add` (fail-open per EC-004)

**factory_lock: presence pre-check + bash parity (F-NW2-006 / ADR-028 §Decision 9):**
`renew_lock()` performs a `factory_lock:` key presence pre-check BEFORE calling the
full fence-aware parser. If `factory_lock:` is NOT present in the frontmatter (regardless
of fence shape — including malformed-fence without lock key), `renew_lock()` returns
`Ok(RenewOutcome::NoOp)` immediately without calling `parse_factory_lock()`. This
matches bash `factory-lock-write.sh renew` parity: bash checks `factory_lock:` presence
first and silently exits 0 if absent, never checking fence shape on the absent-key path.
`Err(LockError::Malformed)` is returned ONLY when `factory_lock:` IS present in the
frontmatter AND the block is malformed (missing field, empty field, missing closing fence
after the lock key is present).

**`expires_at` format contract (F-NW-008):** `expires_at` MUST be output in EXACTLY
`YYYY-MM-DDTHH:MM:SSZ` format: UTC, second precision, uppercase `Z` suffix. Use
`chrono::Utc::now().format("%Y-%m-%dT%H:%M:%SZ").to_string()` — NOT `to_rfc3339()`
(which emits `+00:00` and may include sub-seconds). This matches `factory-lock-write.sh`
(which uses `date -u +%Y-%m-%dT%H:%M:%SZ`) and BC-5.40.001 §Invariant 2. Any deviation
from the `Z`-suffix form or inclusion of sub-seconds is a spec violation and a failing
Red Gate test.

**CRLF normalization and file mode (F-NW-009):** `renew_lock()` outputs LF-only line
endings. CRLF normalization is performed internally by `parse_factory_lock()` (which
calls `content.replace("\r\n", "\n")`) before rewriting; any CRLF in the input STATE.md
is normalized to LF in the output. File mode is NOT preserved — host `write_file`
(which calls `std::fs::write`) creates/overwrites with platform umask defaults; there
is no `chmod` step. This is an accepted behavioral delta from the bash script for
STATE.md in git (git does not track mode for non-executable files).

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| precompact-flush native WASM plugin | `crates/hook-plugins/precompact-flush/src/main.rs` | Effectful (exec_subprocess git, host read_file/write_file) |
| precompact-flush pure logic | `crates/hook-plugins/precompact-flush/src/lib.rs` | Pure (state machine; no I/O) |
| factory-lock shared crate | `crates/factory-lock/src/lib.rs` | Pure (renew_lock, acquire_lock, clear_lock) |
| hooks-registry.toml native stanza | `plugins/vsdd-factory/hooks-registry.toml` | Pure (config) |
| precompact-flush-log | `.factory/hooks/precompact-flush-log` | Effectful (append-only log; written via host write_file) |
| compiled WASM artifact | `plugins/vsdd-factory/hook-plugins/precompact-flush.wasm` | Effectful (runtime binary) |

## Red Gate Test Table

| Test name | File | AC | BC clause | Red Gate condition |
|-----------|------|----|-----------|-------------------|
| `test_precompact_flush_creates_local_commit` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-005 | BC-7.07.001 PC4 (factory-artifacts commit) | Compile error — crate not yet implemented |
| `test_commit_message_has_exact_prefix` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-005 | BC-7.07.001 INV4 + BC-5.41.003 INV3 | Compile error — crate not yet implemented |
| `test_commit_message_derives_cycle_step_from_state_md` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-005 | BC-7.07.001 PC1 (STATE.md read for cycle/step) | Compile error — crate not yet implemented |
| `test_git_commit_failure_exits_2_no_push_no_log` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-005b | BC-7.07.001 PC6 | Compile error — crate not yet implemented |
| `test_sha_b_captured_after_commit_before_append` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-006 | BC-7.07.001 PC8 (SHA_B capture precedes append) | Compile error — crate not yet implemented |
| `test_log_append_newline_terminated` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-007 | BC-7.07.001 PC8 (newline-termination guarantee) | Compile error — crate not yet implemented |
| `test_log_entry_has_4_fields_field4_is_commit` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-007 | BC-7.07.001 PC8 + BC-5.41.003 PC1 | Compile error — crate not yet implemented |
| `test_append_failure_sha_match_resets_commit` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-008 | BC-7.07.001 PC8 (SHA-pinned concurrent-commit guard: HEAD==SHA_B) | Compile error — crate not yet implemented |
| `test_append_failure_sha_diverged_no_reset` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-008 | BC-7.07.001 PC8 (SHA-pinned guard: HEAD!=SHA_B — no reset) | Compile error — crate not yet implemented |
| `test_decide_append_failure_action_noreset_on_diverged` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-008 | BC-7.07.001 PC8 step 3b (HEAD!=SHA_B → NO reset; never discard concurrent commit) | Fails if guard resets unconditionally |
| `test_reset_failure_blocks_exit_2` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-008 | BC-7.07.001 PC8 step 4 (reset of orphan commit itself fails → exit 2 human-intervention) | Compile error — crate does not handle reset-failure |
| `test_push_failure_exits_2_with_retry_message` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-009 | BC-7.07.001 PC6b (push failure → exit 2; local commit + log retained) | Compile error — crate not yet implemented |
| `test_push_success_exits_0` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-009 | BC-7.07.001 PC5 (push success → exit 0) | Compile error — crate not yet implemented |
| `test_log_pruning_not_invoked_by_flush` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-016 | BC-7.07.001 INV7 | Compile error — crate not yet implemented |
| `test_no_state_md_is_noop` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-002 | BC-7.07.001 PC7 (STATE.md unreadable → exit 0 + warn) | Compile error — crate not yet implemented |
| `test_lock_held_renews_before_commit` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-003 | BC-7.07.001 PC3 (lock renewal conditional) | Compile error — crate not yet implemented |
| `test_lock_renewal_failure_is_advisory_not_exit_2` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-013 | BC-7.07.001 PC3 (lock renewal failure = advisory; flush proceeds; NOT exit 2) | Compile error — crate not yet implemented |
| `test_renew_lock_updates_expires_at_only` | `crates/factory-lock/tests/renew.rs` | AC-018 | ADR-028 §Decision 2 + BC-5.40.001 INV2 (TTL=2700s; holder+locked_at preserved) | Compile error — crates/factory-lock not yet implemented |
| `test_renew_lock_noop_when_absent` | `crates/factory-lock/tests/renew.rs` | AC-018 | BC-7.07.001 PC3 + ADR-028 §Decision 2 (no-op when factory_lock: absent) | Compile error — crates/factory-lock not yet implemented |
| `test_worktree_discovery_parses_factory_artifacts_branch` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-017 | ADR-028 §Decision 1 (git worktree list --porcelain parsing) | Compile error — crate not yet implemented |
| bats integration: `precompact-flush.wasm invoked via factory-dispatcher; no bash subprocess` | `plugins/vsdd-factory/tests/precompact-flush-native.bats` | AC-001, AC-014, AC-018 | BC-7.07.001 PC1 + INV1 + ADR-028 §Decision 2 | Hook not yet compiled; bats test fails to find precompact-flush.wasm |
| `test_git_push_succeeds_with_local_bare_remote` | `plugins/vsdd-factory/tests/precompact-flush-native.bats` | AC-001, AC-009 | ADR-028 §Decision 1 F-NW-001 (PATH+SSH_AUTH_SOCK in env_allow sufficient for git push to local bare remote) | Plugin cannot push to local bare remote without PATH in env_allow; test fails asserting (a) git push exits 0, (b) bare remote has received the commit |
| `test_renew_lock_malformed_returns_err_not_ok` | `crates/factory-lock/tests/renew.rs` | AC-018 | ADR-028 §Decision 2 F-NW-004 (library returns Err on malformed block — not Ok(None); library faithfulness preserved) | Compile error — crate not yet implemented; would fail if Err returned as Ok(None) |
| `test_renew_no_frontmatter_skips_noop` | `crates/factory-lock/tests/renew.rs` | AC-018 | ADR-028 §Decision 2 F-NW-005 (no-frontmatter STATE.md → Ok(None) skip, no advisory) | Compile error — crate not yet implemented; would fail if error or advisory emitted on no-frontmatter input |
| `test_expires_at_format_is_z_suffix_second_precision` | `crates/factory-lock/tests/renew.rs` | AC-018 | ADR-028 §Decision 2 F-NW-008 (expires_at MUST be YYYY-MM-DDTHH:MM:SSZ exact format; NOT chrono to_rfc3339 +00:00 or sub-second form) | Compile error — crate not yet implemented; would fail if chrono to_rfc3339() used instead of format("%Y-%m-%dT%H:%M:%SZ") |
| `test_caller_downgrades_renew_err_to_advisory` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-013 | ADR-028 §Decision 2 F-NW-004 (hook caller downgrades renew_lock() Err to advisory warning; proceeds to commit; does NOT exit 2) | Compile error — crate not yet implemented; would fail if hook propagates Err as exit 2 |
| `test_flush_completes_via_git_only_positive` | `plugins/vsdd-factory/tests/precompact-flush-native.bats` | AC-001, AC-005, AC-007 | BC-7.07.001 §INV3 (positive-flush-completion: assert BOTH (a) commit SHA exists on factory-artifacts AND (b) precompact-flush-log entry with that SHA is appended; NOT merely absence-of-bash-subprocess; ADR-028 §Decision 3 F-NW-001 env_allow + §Decision 8 bats non-tautological) | Plugin not yet compiled; currently asserts no bash only — tautological; strengthened to assert actual commit+log-append |
| `test_git_commit_succeeds_with_global_identity_via_home` | `plugins/vsdd-factory/tests/precompact-flush-native.bats` | AC-005, AC-017 | ADR-028 §Decision 10 F-NW2-004 (committer identity via HOME/.gitconfig; GIT_AUTHOR/COMMITTER vars absent from env_allow) | Plugin not yet compiled; test fixture creates temp $HOME with .gitconfig containing user.name + user.email; asserts git commit exits 0; author identity matches .gitconfig |
| `test_renew_lock_malformed_fence_no_lock_key_returns_noop` | `crates/factory-lock/tests/renew.rs` | AC-018 | ADR-028 §Decision 9 F-NW2-006 (malformed fence + no factory_lock: key → Ok(RenewOutcome::NoOp); bash parity; NO Err(Malformed)) | Compile error; would fail if Err(Malformed) returned for malformed-fence-without-lock-key input |
| `test_no_lock_clean_state_exits_0_no_commit` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-005, AC-011, AC-018 | ADR-028 §Decision 11 F-NW2-007 (RenewOutcome::NoOp + no staged changes → exit 0, no commit forced; INV5 clean-state) | Compile error; would fail if plugin forces an empty commit when renew returns NoOp and git diff --cached is empty |
| `test_first_flush_with_absent_log_appends_successfully` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-007 | ADR-028 §Decision 12 F-NW2-008 (read_file CAPABILITY_DENIED on absent log → empty baseline; log created with one LF-terminated entry; NOT an append failure) | Compile error; would fail if read_file CAPABILITY_DENIED on absent log propagated as append failure triggering SHA-pinned reset |
| `test_worktree_discovery_failure_emits_durability_degraded` | `crates/hook-plugins/precompact-flush/tests/integration.rs` | AC-017 | ADR-028 §Decision 13 F-NW2-009 (discovery failure → exit 0 + stderr DURABILITY DEGRADED prefix; NOT silent no-op; both failure cases covered) | Compile error; would fail if advisory omitted or if exit code is non-zero on discovery failure |

## Tasks

- T-1: Write failing cargo unit tests in `crates/factory-lock/tests/renew.rs` (Red Gate for `test_renew_lock_*` — all must fail before crate exists)
- T-2: Write failing cargo unit/integration tests in `crates/hook-plugins/precompact-flush/tests/integration.rs` (Red Gate for all plugin ACs)
- T-3: Write failing bats integration test `plugins/vsdd-factory/tests/precompact-flush-native.bats` asserting no bash subprocess in dispatcher log
- T-4: Create `crates/factory-lock/Cargo.toml` and `crates/factory-lock/src/lib.rs` implementing `renew_lock()`, `acquire_lock()`, `clear_lock()` with frontmatter-boundary-aware YAML surgery (depends on `crates/factory-lock-parse`)
- T-5: Create `crates/hook-plugins/precompact-flush/Cargo.toml` (cdylib + rlib + [[bin]]) and `src/lib.rs` (pure logic) + `src/main.rs` (vsdd-hook-sdk trampoline `vsdd_hook_sdk::__internal::run`)
- T-6: Implement step 1–3: runtime `git worktree list --porcelain` discovery; STATE.md read via host read_file; check factory_lock block; call `crates/factory-lock::renew_lock()` if held
- T-7: Implement step 4–7: `git add -u`, `git commit` (LOCAL), SHA_B capture via `git rev-parse HEAD`, append to precompact-flush-log via host write_file with `\n` termination
- T-8: Implement step 8: append-failure SHA-pinned concurrent-commit guard (SHA comparison; conditional `git reset --soft SHA_B^`; reset-failure exit 2)
- T-9: Implement step 9–12: `git push`, push-failure exit-2 (with retry message), push-success exit-0; empty-commit guard (exit 0 on clean state)
- T-10: Add native WASM stanza to `plugins/vsdd-factory/hooks-registry.toml` (`hook-plugins/precompact-flush.wasm`; `binary_allow = ["git"]`; no bash, no shell_bypass)
- T-11: Add `crates/factory-lock` and `crates/hook-plugins/precompact-flush` to `Cargo.toml` workspace members
- T-12: Compile WASM artifact: `cargo build --target wasm32-wasip1 -p precompact-flush --release` and check in `plugins/vsdd-factory/hook-plugins/precompact-flush.wasm`
- T-13: Verify cargo unit tests pass: `cargo test -p factory-lock -p precompact-flush`
- T-14: Verify bats integration suite passes: `cd plugins/vsdd-factory/tests && ./run-all.sh`
- T-15: Ensure no log pruning code appears anywhere in the plugin crate

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | STATE.md absent in factory-artifacts worktree | No-op, exit 0 (AC-002) |
| EC-002 | factory_lock.holder is null | Skip lock renewal; proceed directly to git add |
| EC-003 | factory_lock.holder is non-empty string | Call `crates/factory-lock::renew_lock()` (native Rust; no bash); if renewal returns error, write advisory to stderr and proceed with flush commit anyway; exit 0 if commit succeeds (AC-003, AC-013 — NOT exit 2 per BC-7.07.001 PC3/EC-004) |
| EC-004 | precompact-flush-log does not exist | Create it on first write via host write_file |
| EC-005 | Log write fails (host write_file returns error — e.g., permissions) | Append fails; HEAD==SHA_B → `git reset --soft SHA_B^`; exit 2 |
| EC-006 | Log append succeeds but git push fails | exit 2 with retry message (AC-009) |
| EC-007 | Log append succeeds, git push succeeds | exit 0 (AC-009) |
| EC-008 | Intervening commit between git commit and log append (race) | HEAD != SHA_B → no reset + human-intervention message + exit 2 (AC-008) |
| EC-009 | plugin timeout (>30000ms) | on_error=continue → advisory; dispatcher does NOT block compaction |
| EC-010 | harness version < v2.1.105 | Harness may not send PreCompact events at all; plugin not invoked (BC-1.15.001 INV3 at dispatcher level) |
| EC-011 | `git worktree list --porcelain` subprocess exits non-zero (PATH issue, git binary not found) | Exit 0 with LOUD advisory: `precompact-flush: DURABILITY DEGRADED — git worktree list command failed (exit <N>); factory-artifacts worktree cannot be discovered; flush SKIPPED this compaction event. Check PATH/git configuration.` (ADR-028 §Decision 13 F-NW2-009) |
| EC-011b | `git worktree list --porcelain` succeeds but no factory-artifacts stanza found | Exit 0 with LOUD advisory: `precompact-flush: DURABILITY DEGRADED — factory-artifacts branch not found in git worktree list output; flush SKIPPED this compaction event. Ensure the factory-artifacts worktree is mounted at .factory/ (run: git worktree add .factory factory-artifacts).` (ADR-028 §Decision 13 F-NW2-009) |
| EC-012 | `crates/factory-lock::renew_lock()` encounters malformed frontmatter (factory_lock: key present but block malformed) | `Err(LockError::Malformed)` returned; advisory warning to stderr; no write_file call; flush commit attempted anyway; exit 0 if commit succeeds (EC-004 / ADR-028 §Decision 9 error handling) |
| EC-013 | `crates/factory-lock::renew_lock()` called on STATE.md with malformed fence but NO factory_lock: key | `Ok(RenewOutcome::NoOp)` returned immediately (presence pre-check short-circuits); no advisory emitted; flush proceeds (ADR-028 §Decision 9 bash parity) |
| EC-014 | PreCompact fires; STATE.md has no factory_lock:; no pending factory-artifacts changes | `RenewOutcome::NoOp` + `git diff --cached` empty → INV5 clean-state → exit 0; no commit; no precompact-flush-log entry appended (ADR-028 §Decision 11 F-NW2-007) |
| EC-015 | PreCompact fires; precompact-flush-log does not yet exist (first flush) | `read_file` returns CAPABILITY_DENIED (file absent); plugin treats as empty baseline; concatenates entry to `""`; `write_file` creates file; exit 0 (ADR-028 §Decision 12 F-NW2-008) |

## Forbidden Dependencies

`crates/hook-plugins/precompact-flush` MUST NOT:
- Invoke `bash` (or any shell interpreter) via `exec_subprocess` — `binary_allow = ["git"]` ONLY
- Use `shell_bypass_acknowledged` in the registry stanza
- Call `precompact-flush-prune.sh` (log pruning is exclusively S-18.04b's responsibility per BC-7.07.001 INV7)
- Use `curl`, `jq`, `python`, `node`, `npm`, or any non-standard binary via exec_subprocess
- Use `unwrap()` or `expect()` in non-test code paths
- Hard-code `.factory` as the worktree path (must use runtime discovery per ADR-028 §Decision 1)
- Add AI attribution to commit messages
- Use `--no-verify` git flags

`crates/factory-lock` MUST NOT:
- Depend on `crates/hook-plugins/precompact-flush` (dependency is unidirectional: plugin → factory-lock)
- Import `crates/factory-lock-parse` in a way that creates a cycle (factory-lock → factory-lock-parse; never the reverse)
- Produce a `[[bin]]` target or a WASM output (it is a pure library crate — no `[[bin]]`)

## Token Budget

| Item | Estimated tokens |
|------|-----------------|
| This story file | ~6 000 |
| BC-7.07.001 (v1.17) | ~5 500 |
| ADR-028 (new input) | ~4 000 |
| VP-082 | ~2 000 |
| VP-085 | ~1 500 |
| cargo unit test files (new) | ~5 000 |
| bats integration test (new) | ~2 000 |
| crates/hook-plugins/precompact-flush/ (new Rust crate) | ~6 000 |
| crates/factory-lock/ (new Rust crate) | ~4 000 |
| hooks-registry.toml section | ~800 |
| Cargo.toml workspace edit | ~400 |
| Tool outputs (cargo test + bats) | ~3 000 |
| BC files (1 BC) | ~5 500 |
| **Total** | **~45 700** |

Budget is ~46k tokens, within 20-30% of a 200k-token context window. Safe for one dispatch.

## Dependencies

- **Depends on:** S-18.00 (PreCompact event type must be enumerated in dispatcher so the native WASM plugin can receive PreCompact dispatch packets)
- **Depends on:** S-17.04 (mid-burst heartbeat renewal wiring — PreCompact event routing plumbing must be live; `crates/factory-lock-parse` may also be available as a foundation for `crates/factory-lock`)
- **Blocks:** S-18.04b (precompact-flush-log format defined here; S-18.04b implements pruning based on this format)

## Traceability

| Source | ID | Coverage |
|--------|----|---------|
| Behavioral Contract | BC-7.07.001 precondition 1 | AC-001 (hooks-registry.toml native WASM stanza registration) |
| Behavioral Contract | BC-7.07.001 PC1 | AC-005 (STATE.md host read_file; current_cycle/current_step for commit message) |
| Behavioral Contract | BC-7.07.001 PC2 | (covered by hermetic INV1/INV2; custom_instructions not used is enforced by AC-014) |
| Behavioral Contract | BC-7.07.001 PC3 | AC-003, AC-013 (native lock renewal conditional; failure is advisory not exit 2) |
| Behavioral Contract | BC-7.07.001 Precondition 4 | AC-017, AC-018 (runtime worktree discovery via git worktree list --porcelain; discovered absolute path used ONLY as git -C argument; host read_file/write_file always use .factory/-relative paths; renew_lock() Err-on-malformed + caller-downgrade-to-advisory; expires_at YYYY-MM-DDTHH:MM:SSZ) |
| Behavioral Contract | BC-7.07.001 PC4 | AC-004, AC-005 (git add + LOCAL commit via exec_subprocess -C <wt>) |
| Behavioral Contract | BC-7.07.001 PC5 | AC-009 (push success → exit 0) |
| Behavioral Contract | BC-7.07.001 PC6 | AC-005b (git commit LOCAL failure → exit 2; no log entry; no push) |
| Behavioral Contract | BC-7.07.001 PC6b | AC-009 (push failure → exit 2; local commit + log retained; retry is push-only) |
| Behavioral Contract | BC-7.07.001 PC7 | AC-002 (STATE.md unreadable → exit 0 + warn to stderr) |
| Behavioral Contract | BC-7.07.001 PC8 | AC-006, AC-007, AC-008 (SHA_B capture; log append newline-terminated via host write_file; SHA-pinned concurrent-commit guard) |
| Behavioral Contract | BC-7.07.001 PC9 | AC-010 (crash → on_error=continue fail-open; Rust Result<T,E>/? error propagation) |
| Behavioral Contract | BC-7.07.001 INV1 | AC-014 (hermetic: STATE.md+git only via host read_file/write_file + exec_subprocess("git")) |
| Behavioral Contract | BC-7.07.001 INV2 | AC-014 (custom_instructions exclusion — same hermetic invariant) |
| Behavioral Contract | BC-7.07.001 INV3 | AC-011 (canonical 11-step execution order; step 1 now includes runtime worktree discovery) |
| Behavioral Contract | BC-7.07.001 INV4 | AC-005 (commit message format: `PreCompact flush <cycle>/<step> <ISO-timestamp>`) |
| Behavioral Contract | BC-7.07.001 INV5 | AC-005 (empty commit is forbidden; exit 0 on clean state) |
| Behavioral Contract | BC-7.07.001 INV6 | AC-010 (Rust Result<T,E>/? error propagation; no unwrap()/expect() in non-test code) |
| Behavioral Contract | BC-7.07.001 INV7 | AC-012, AC-016 (log pruning deferred to S-18.04b; append-only in v1) |
| ADR | ADR-028 §Decision 1 | AC-017 (runtime `git worktree list --porcelain` discovery; no hard-coded path) |
| ADR | ADR-028 §Decision 2 | AC-018 (native Rust lock renewal via crates/factory-lock::renew_lock(); binary_allow=["git"] only) |
| ADR | ADR-028 §Decision 3 (uniform git -C) | AC-004, AC-005, AC-006, AC-008, AC-009 (ALL git subprocesses except initial worktree list use exec_subprocess git -C <wt>; bare git commands without -C forbidden; wt is the absolute path discovered in AC-017; consistency-validator gap: this row was previously missing and is added to close the F-NW-002/003 traceability gap) |
| ADR | ADR-028 §Decision 4 (shared crate crates/factory-lock) | AC-018, Architecture Mapping (shared library crate crates/factory-lock; not inline in plugin; depends on crates/factory-lock-parse) |
| ADR | ADR-028 §Decision 5 (native renew precedes git add) | AC-011 step 4 (native renew precedes git add; ensures renewed expires_at is staged in the flush commit) |
| Verification Property | VP-082 | AC-005, AC-005b, AC-009 (flush commits STATE.md locally then pushes; blocks on commit/push failure) |
| Verification Property | VP-085 | AC-010, AC-014 (hermetic: Rust error propagation; STATE.md+git only; no in-context reasoning) |
| Domain Capability | CAP-032 | All ACs (context-durability feature) |

## Previous Story Intelligence

S-17.04 (mid-burst heartbeat renewal) shipped `verify-state-timestamp-refresh.wasm`
as a PostToolUse gate and established `crates/factory-lock-parse` as the authoritative
parse-only crate for STATE.md lock fields. Key lesson: the factory-artifacts worktree
is mounted at a path that may differ from the main repo CWD. Native WASM plugins must
discover this path at runtime rather than assuming a conventional location — ADR-028
§Decision 1 formalizes this as `git worktree list --porcelain` discovery.

`crates/factory-lock-parse` exists (S-17.04 dependency); this story's
`crates/factory-lock` SHOULD depend on it for the parse half of `renew_lock()`,
avoiding duplicate YAML frontmatter parsing logic. The new crate is write-capable
(CRUD); `factory-lock-parse` remains read-only.

## Architecture Compliance Rules

- Crate `crates/hook-plugins/precompact-flush` MUST have `crate-type = ["cdylib", "rlib"]` in its `[lib]` section AND a `[[bin]]` section pointing to `src/main.rs` (the vsdd-hook-sdk trampoline)
- Commit message MUST begin with exact string `PreCompact flush ` (case-sensitive, single space between words, trailing space) — this prefix is the exemption key used by validate-burst-log and validate-dispatch-advance (BC-5.41.003 INV3)
- All fallible function bodies in non-test code MUST use `Result<T, E>` and `?` — no `unwrap()` or `expect()` (BC-7.07.001 INV6; CLAUDE.md error taxonomy rule)
- `binary_allow = ["git"]` is the ONLY entry in the capabilities block — no `"bash"`, no `shell_bypass_acknowledged` (ADR-028 §Decision 2; binding constraint from human direction)
- `crates/factory-lock` MUST NOT have a `[[bin]]` section (pure library; does not inflate the CI WASM floor count per ADR-028 §Consequences)
- Dependency must be unidirectional: `crates/hook-plugins/precompact-flush` → `crates/factory-lock` → `crates/factory-lock-parse`; no cycles
- No `--no-verify` git flags anywhere in the plugin (CLAUDE.md absolute prohibition)
- Plugin MUST NOT add AI attribution to commit messages (CLAUDE.md explicit directive for vsdd-factory)
- host `write_file` (not exec_subprocess echo/printf) MUST be used for log append — the trailing `\n` must be embedded in the Rust string literal, not produced by a shell command

## Library & Framework Requirements

- `vsdd-hook-sdk` (workspace crate — pinned to workspace version; provides `__internal::run` trampoline for `src/main.rs`)
- `serde` + `serde_yaml` (YAML frontmatter parse; version pinned per workspace `Cargo.toml` dependency table)
- `chrono` (UTC timestamps for `expires_at` and log entry ISO-8601; version pinned per workspace)
- `crates/factory-lock-parse` (existing workspace crate; `crates/factory-lock` depends on it)
- `bats-core` (already in use by `plugins/vsdd-factory/tests/`; no version change)
- Rust toolchain: single workspace MSRV per `rust-toolchain.toml` (no per-crate MSRV divergence per CLAUDE.md)
- WASM compilation target: `wasm32-wasip1` (or `wasm32-wasi` per workspace convention — match existing hook-plugin crates)

## File Structure Requirements

| File | Action | Notes |
|------|--------|-------|
| `crates/hook-plugins/precompact-flush/Cargo.toml` | Create | `[lib] crate-type = ["cdylib", "rlib"]` + `[[bin]]`; depends on `crates/factory-lock`, `vsdd-hook-sdk`, `serde`, `serde_yaml`, `chrono` |
| `crates/hook-plugins/precompact-flush/src/lib.rs` | Create | Pure logic — worktree discovery parser, STATE.md frontmatter reader, log entry formatter, execution state machine |
| `crates/hook-plugins/precompact-flush/src/main.rs` | Create | vsdd-hook-sdk trampoline: `fn main() { vsdd_hook_sdk::__internal::run(lib::run_plugin) }` |
| `crates/hook-plugins/precompact-flush/tests/integration.rs` | Create | Cargo integration tests with injectable host mocks; all Red Gate tests from table above |
| `crates/factory-lock/Cargo.toml` | Create | Pure library crate (NO `[[bin]]`); depends on `crates/factory-lock-parse`, `serde`, `serde_yaml`, `chrono` |
| `crates/factory-lock/src/lib.rs` | Create | `pub enum RenewOutcome { NoOp, Renewed(String) }`; `pub fn renew_lock(state_md_content: &str) -> Result<RenewOutcome, LockError>` (pure content-in/content-out; no std::fs; WASM-hermetic); `pub fn acquire_lock(...) -> Result<String, LockError>`; `pub fn clear_lock(state_md_content: &str) -> Result<String, LockError>`; `pub struct FactoryLock { holder, locked_at, expires_at }`. Path-based `renew_lock(&Path)` form is STRUCK per ADR-028 §Decision 9 (F-NW2-005). |
| `crates/factory-lock/tests/renew.rs` | Create | Cargo unit tests: `test_renew_lock_updates_expires_at_only`, `test_renew_lock_noop_when_absent`, plus ≥18 frontmatter shape variants (held lock / absent lock / malformed block / body-content preservation) |
| `plugins/vsdd-factory/hooks-registry.toml` | Modify | Replace legacy-bash-adapter PreCompact entry with native WASM stanza (AC-001 exact TOML); remove `[hooks.config]` block; set `binary_allow = ["git"]` |
| `Cargo.toml` (workspace root) | Modify | Add `crates/factory-lock` and `crates/hook-plugins/precompact-flush` to `[workspace.members]` |
| `plugins/vsdd-factory/hook-plugins/precompact-flush.wasm` | Create (compiled artifact) | Output of `cargo build --target wasm32-wasip1 -p precompact-flush --release`; checked in for dispatcher consumption |
| `plugins/vsdd-factory/tests/precompact-flush-native.bats` | Create | Bats integration test: invokes factory-dispatcher with precompact-flush.wasm; asserts no bash subprocess in dispatcher log for this plugin; asserts commit and log entry produced |
| `.factory/hooks/precompact-flush-log` | Create (at runtime) | Append-only log — created by plugin on first flush; NOT pre-created by implementer |
| `plugins/vsdd-factory/hooks/precompact-flush.sh` | DELETE or RETAIN as NO-OP | The bash script is superseded by the native plugin. If the registry stanza is replaced, the .sh file is no longer invoked; it MAY be removed or retained as a historical artifact. No behavioral requirement to preserve it post-migration. |

## Changelog

| Version | Date | Change |
|---------|------|--------|
| v1.10 | 2026-06-20 | (ADR-028 v1.2; F-NW2-005/006/007/008/009 + consistency ISSUE-2/4) AC-011 canonical-order steps 5/6/7/10 corrected from bare git to exec_subprocess git -C <wt> form (ISSUE-2; B7); phantom Red Gate label F-NW-010 replaced with stable anchor BC-7.07.001 §INV3 positive-flush-completion (ISSUE-4; B8); AC-017 updated with two-form DURABILITY DEGRADED loud advisory (git-command-failed + factory-artifacts-not-found) + exit 0 fail-open (F-NW2-009; B9); AC-018 renew_lock() path-based form struck; RenewOutcome enum pinned (NoOp/Renewed(String)); caller pattern (NoOp→skip write_file / Renewed→write_file / Err→advisory proceed); factory_lock: presence pre-check bash-parity noted (F-NW2-005/006; B10); AC-007 absent-log-as-empty treatment added: read_file CAPABILITY_DENIED on absent log → empty baseline → write_file creates file; NOT an append failure (F-NW2-008; B11); AC-011 step 3 rewritten with RenewOutcome enum dispatch; step 5 INV5 interaction: NoOp + empty diff → exit 0 no commit (F-NW2-007; B12); +5 Red Gate rows: test_git_commit_succeeds_with_global_identity_via_home; test_renew_lock_malformed_fence_no_lock_key_returns_noop; test_no_lock_clean_state_exits_0_no_commit; test_first_flush_with_absent_log_appends_successfully; test_worktree_discovery_failure_emits_durability_degraded (B13); File Structure lib.rs signature updated (RenewOutcome + pure content form; path-based form struck); EC table: EC-011 split into EC-011 (subprocess-fail) + EC-011b (not-found); EC-012 clarified (Err only when key present); EC-013/014/015 added (malformed-fence-no-key; NoOp-clean-state; first-flush-absent-log); BC cite v1.16→v1.17. |
| v1.9 | 2026-06-20 | (ADR-028 v1.1; spec-re-convergence F-NW-001/003/008/009/010) AC-001 env_allow +PATH/+SSH_AUTH_SOCK (now [HOME,GIT_CONFIG_GLOBAL,XDG_CONFIG_HOME,PATH,SSH_AUTH_SOCK]; PATH for git push SSH binary discovery; SSH_AUTH_SOCK for SSH agent auth; dispatcher env_clear requires explicit injection per F-NW-001); AC-004/005/006/008/009 git subprocesses now uniformly use exec_subprocess git -C <wt> form (all git except initial worktree list must use -C per F-NW-003 ADR-028 §Decision 3); AC-007 implementation note replaced with read-modify-write log-append semantics + single-writer guarantee + bounded data-loss window note (F-NW-007); AC-018 updated with exact expires_at format pin YYYY-MM-DDTHH:MM:SSZ using chrono::format not to_rfc3339() + renew_lock() Err-on-malformed/caller-downgrade-to-advisory semantics + LF-only output + umask-default file-mode note (F-NW-008/009 F-NW-004/005); +6 Red Gate rows (test_git_push_succeeds_with_local_bare_remote bats; test_renew_lock_malformed_returns_err_not_ok factory-lock unit; test_renew_no_frontmatter_skips_noop factory-lock unit; test_expires_at_format_is_z_suffix_second_precision factory-lock unit; test_caller_downgrades_renew_err_to_advisory precompact-flush integration; test_flush_completes_via_git_only_positive bats strengthened positive-completion assert); +BC-7.07.001 Precondition 4 traceability row; +ADR-028 §Decision 3 (uniform git -C) traceability row; ADR-028 §Decision 3/4 renumbered per ADR v1.1; BC-7.07.001 cite v1.15→v1.16; behavioral ACs AC-002/004/005/005b/006/008/009/011/012/015/016 preserved unchanged in intent. |
| v1.8 | 2026-06-20 | (ADR-028) mechanism rewrite bash→native WASM plugin: target_module → crates/hook-plugins/precompact-flush/src/main.rs; target_module_secondary [crates/factory-lock/src/lib.rs]; subsystems: [SS-07, SS-04]; file list → Rust crate deliverables (Cargo.toml x2, lib.rs x2, main.rs, tests/ x2, hooks-registry.toml, Cargo.toml workspace, precompact-flush.wasm, bats integration); AC-001 registry stanza → native WASM (hook-plugins/precompact-flush.wasm; binary_allow=[git] only, no bash, no shell_bypass); AC-003/AC-013 lock renewal → native crates/factory-lock::renew_lock() (no bash exec); AC-007 log append → host write_file (read-modify-write; \n embedded in Rust string); AC-010 hardening → Rust Result<T,E>/? (no set -euo pipefail); AC-014 hermetic → only git via exec_subprocess + host read_file/write_file; AC-017 added (runtime worktree discovery via git worktree list --porcelain; fail-open if absent); AC-018 added (native lock renewal; binary_allow=[git] only); AC-011 step 3 updated (native crates/factory-lock::renew_lock(); no bash exec); Architecture Mapping updated (Rust crate layout); Red Gate → cargo unit tests (injectable mocks) + bats integration (21 tests total); Library Requirements → vsdd-hook-sdk + serde + serde_yaml + chrono + crates/factory-lock-parse; File Structure → Rust crate layout (7 create + 2 modify + 1 runtime-create); Forbidden Dependencies updated (bash, shell_bypass, legacy-bash-adapter, curl/jq/python/node/npm, unwrap/expect in non-test); Token Budget updated (~46k); BC-7.07.001 cite v1.14→v1.15; behavioral ACs AC-002/004/005/005b/006/008/009/012/015/016 preserved unchanged in intent. |
| v1.7 | 2026-06-20 | (adversary S-18.04a LOCAL F-ADV-003) added reset-failure-block + diverged-no-reset Red Gate Test Table rows for the append-failure branch matrix (BC-7.07.001 PC8 step 3b/step 4 canonical TVs); test-count note refreshed (~17 rows total, up from 15). |
| v1.6 | 2026-06-20 | (adversary S-18.04a LOCAL pass-1 F-PF1-001) corrected factory-lock-write.sh dependency anchor hooks/ → bin/ + BC-7.07.001 cite v1.13→v1.14; precompact-flush.sh location unchanged (hooks/). |
| v1.5 | 2026-06-17 | (C-P7-002) S-18.03 and S-18.07 added to blocks: both list S-18.04a in depends_on (direct successors); reverse edges were missing. Final blocks: [S-18.03, S-18.04b, S-18.07, S-18.08]. |
| v1.4 | 2026-06-17 | (C-P6-006 sibling-sweep) S-18.08 added to blocks; S-18.08 explicitly lists S-18.04a in depends_on; direct-successor-only blocks convention confirmed across E-18 corpus. |
| v1.3 | 2026-06-17 | (F-P5-001) BC-7.07.001 version cite in Behavioral Contracts table updated v1.12→v1.13 (current version per exhaustive version-cite freshness sweep). Descending-order convention: newest at top. |
| v1.2 | 2026-06-16 | (M-005) traces_to: added E-18-factory-context-durability.md epic path for bidirectional epic-story traceability. |
| v1.1 | 2026-06-16 | Fix 2 (F-MAJOR-001) — AC-PC realignment against BC-7.07.001 v1.12 actual postcondition numbering. Registration moved from "PC1" to "precondition 1". AC-002 corrected from "PC2" to "PC7". AC-003 corrected to include advisory semantics for lock renewal failure. Added AC-005b for PC6. AC-006/007/008/009/010/011/012/013/014/015 traces corrected. Traceability table rebuilt. Red Gate test table updated. Frontmatter normalization: epic_id "E-18", phase "F3", level implementation. |
| v1.0 | 2026-06-16 | Initial authoring by story-writer for E18 context-durability feature. |
