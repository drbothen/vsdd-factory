---
document_type: story
level: ops
story_id: "S-17.01"
epic_id: "E-17"
version: "1.0"
title: "S-17.01: factory_lock STATE.md frontmatter schema + state-burst fetch-then-CAS push (D3 + D6)"
status: draft
producer: story-writer
timestamp: 2026-06-10T00:00:00Z
last_amended: "2026-06-10 (v1.0) — Initial authoring (story-writer; brownfield-backfill issue #170; ADR-025 v1.2 D3/D6 deliverables; BC-5.40.001)."
phase: brownfield-backfill
cycle: v1.0-brownfield-backfill
inputs:
  - .factory/specs/architecture/decisions/ADR-025-single-writer-factory-locklease-prevent-concurrent-session-races-on-factory-artifacts-orphan-branch.md
  - .factory/specs/behavioral-contracts/ss-05/BC-5.40.001.md
  - .factory/specs/architecture/ARCH-INDEX.md
  - plugins/vsdd-factory/skills/state-burst/SKILL.md
input-hash: "[pending-recompute]"
traces_to:
  - .factory/specs/behavioral-contracts/ss-05/BC-5.40.001.md
  - .factory/specs/architecture/decisions/ADR-025-single-writer-factory-locklease-prevent-concurrent-session-races-on-factory-artifacts-orphan-branch.md
wave: 1
priority: P1
points: 5
track: factory-state-durability
effort_class: single-day
depends_on: []
blocks:
  - "S-17.02"
  - "S-17.03"
behavioral_contracts:
  - "BC-5.40.001"
verification_properties: []
# BC status: VPs to be allocated during verification scoping per TD-VSDD-063 lagging-VP precedent
domain_invariants: []
tdd_mode: strict
target_module: "plugins/vsdd-factory/skills/state-burst"
subsystems:
  - "SS-05"
estimated_days: 1.0
assumption_validations: []
risk_mitigations: []
anchored_adrs:
  - ADR-025
closes: []
modified: []
---

# S-17.01: factory_lock STATE.md Frontmatter Schema + state-burst Fetch-then-CAS Push

**Epic:** E-17 — Factory State Durability and Concurrency (brownfield-backfill #170)
**Wave:** 1 (no dependencies — delivers immediately)
**Blocks:** S-17.02 (guard reads schema), S-17.03 (skills write schema)
**Priority:** P1 (closes primary blind-push gap independently of the guard)
**BC gate:** BC-5.40.001 v1.0 (draft; POL-14 auto-promotion on PR merge)

## Narrative

- **As a** factory `state-manager` agent pushing to the `factory-artifacts` orphan branch
- **I want to** store the authoritative cross-session lock state in a canonical
  `factory_lock` frontmatter block in STATE.md and push that branch using a
  fetch-then-`--force-with-lease` CAS primitive
- **So that** (a) the lock record is human-readable, travels with the branch, and is
  available to all readers including the WASM guard, and (b) concurrent pushes from any
  source — another developer, a guard-crash fail-open scenario, or a self-vs-self
  two-session case — are detected as collisions rather than silently clobbering remote
  state

## Behavioral Contracts

| BC ID | Title | Version | PCs Exercised |
|-------|-------|---------|---------------|
| BC-5.40.001 | STATE.md factory_lock schema + TTL + mid-burst renewal + state-burst CAS push | 1.0 | PC1, PC2, PC3, PC4, PC5, PC6 |

## Acceptance Criteria

### AC-001 (traces to BC-5.40.001 postcondition PC1 — schema correctness)

When `state-manager` writes a lock record (via the `/factory-lock` acquire path —
implemented in S-17.03), the resulting STATE.md frontmatter MUST contain a well-formed
`factory_lock` block:
```yaml
factory_lock:
  holder: "<exact git config user.email output>"
  locked_at: "YYYY-MM-DDTHH:MM:SSZ"
  expires_at: "YYYY-MM-DDTHH:MM:SSZ"  # locked_at + exactly 2700 seconds
```
All three fields MUST be present; `holder` MUST be the trimmed (trailing-newline-only
removal) output of `git config user.email`; `locked_at` and `expires_at` MUST be
ISO-8601 UTC (format `YYYY-MM-DDTHH:MM:SSZ`); `expires_at` MUST equal `locked_at +
2700s` exactly.

**Error variant on violation:** `SchemaViolation`
**Test:** `test_BC_5_40_001_schema_correctness_on_acquire()`

### AC-002 (traces to BC-5.40.001 postcondition PC2 — unlock clears block)

When `state-manager` writes an unlock, the `factory_lock` key MUST be absent from
STATE.md frontmatter entirely after the write. A null placeholder
(`factory_lock: null`) is NOT an acceptable representation — the key must be removed.

**Error variant on violation:** `StaleNullBlock`
**Test:** `test_BC_5_40_001_unlock_removes_key_not_nulls()`

### AC-003 (traces to BC-5.40.001 postcondition PC3 — TTL auto-expiry guard pass)

When `factory_lock.expires_at` is in the past at the time the guard checks
(BC-4.13.001 PC2), the lock is treated as absent and the operation proceeds.
The expired block remains in STATE.md until the next `state-manager` write (which
either refreshes or removes it). The schema definition in STATE.md is the precondition
for this behavior — the `expires_at` field MUST be present and valid for expiry
comparison to work.

**Test:** `test_BC_5_40_001_ttl_expired_block_persists_until_next_write()`

### AC-004 (traces to BC-5.40.001 postcondition PC4 — mid-burst TTL renewal)

At EVERY intermediate `state-manager` commit within a burst while a lock is held,
`state-manager` MUST refresh `factory_lock.expires_at = now + 2700s` alongside the
commit payload. The renewal MUST be atomic with the same commit. The `locked_at` field
MUST remain unchanged (it records the original acquire instant, not the last renewal).

After a 3-commit burst (each committing intermediate STATE.md changes), the final
`expires_at` MUST be `≈ now + 2700s` measured from the last commit (not from the
original `locked_at`).

**Error variant on violation:** `RenewalMissed`
**Test:** `test_BC_5_40_001_mid_burst_renewal_updates_expires_at_preserves_locked_at()`

### AC-005 (traces to BC-5.40.001 postcondition PC5 — state-burst CAS push)

The `state-burst` skill's push command MUST be replaced from:
```bash
git push origin factory-artifacts
```
to:
```bash
git -C .factory fetch origin factory-artifacts
EXPECTED_SHA=$(git -C .factory rev-parse origin/factory-artifacts)
git -C .factory push --force-with-lease=factory-artifacts:"${EXPECTED_SHA}" origin factory-artifacts
```

When the CAS push is rejected (non-zero exit from `--force-with-lease`), `state-burst`
MUST exit non-zero and emit the human-readable error:
```
state-burst CAS push failed — concurrent write detected.
Fetch origin/factory-artifacts and retry.
```
The remote state MUST NOT be silently clobbered.

**Error variant on rejection:** `CASPushRejected`
**Test:** `test_BC_5_40_001_cas_push_rejected_on_concurrent_write()`

### AC-006 (traces to BC-5.40.001 postcondition PC6 — single-developer zero friction)

A developer who holds the lock and runs normal `state-manager` burst operations sees
no added friction. The guard (BC-4.13.001 PC3 — implemented in S-17.02) reads STATE.md
locally (sub-5ms), compares identities, and returns `Continue` on self-held lock
immediately. This story is responsible for the schema being present and well-formed so
that the guard can correctly identify the self-held case.

A developer who has NOT run `/factory-lock` operates with `factory_lock` absent —
the guard reads absent block and passes all checks (BC-4.13.001 PC4 fail-open path).

**Test:** `test_BC_5_40_001_absent_block_is_unlocked_state()`

### AC-007 (traces to BC-5.40.001 invariant 2 — default TTL is 45 minutes)

The TTL constant used by `state-manager` when writing `expires_at` MUST be exactly
2700 seconds (45 minutes). This constant MUST NOT be configurable by users. Any code
that computes `expires_at` MUST use this constant directly: `expires_at = now + 2700s`.

**Test:** `test_BC_5_40_001_ttl_constant_is_2700_seconds()`

### AC-008 (traces to BC-5.40.001 invariant 3 — expires_at always = now + 2700s at write time)

Whether the write is the initial acquire, a mid-burst renewal, or any state-manager
write that refreshes the lock, `expires_at` MUST be computed as the wall-clock instant
of the commit + 2700 seconds. The `locked_at` field is immutable after acquire.

**Test:** (covered by AC-004 and AC-007 tests)

### AC-009 (traces to BC-5.40.001 invariant 5 — force-with-lease already permitted)

The existing `plugins/vsdd-factory/hooks/verify-git-push.sh` hook MUST NOT require
modification for the CAS push. `--force-with-lease` is already in the push-hook
allow-list (ADR-025 §Decision 8 source verification). The story's implementation
MUST NOT modify `verify-git-push.sh`.

**Test:** `test_BC_5_40_001_verify_git_push_hook_unchanged()`
(bats: confirm `hooks/verify-git-push.sh` permits `--force-with-lease` before and
after this story's changes — contents diff MUST be empty for that file)

### AC-010 (traces to BC-5.40.001 edge case EC-003 — fetch failure before CAS push)

When `git fetch origin factory-artifacts` fails (non-zero exit) during the state-burst
CAS push sequence, `state-burst` MUST exit non-zero and emit:
```
state-burst CAS push failed — fetch error before push. Retry after resolving network.
```
The push MUST NOT proceed with a potentially stale `EXPECTED_SHA`.

**Test:** `test_BC_5_40_001_fetch_failure_aborts_push()`

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| `factory_lock` schema parser/writer | `agents/state-manager.md` (prose spec) | Effectful (writes STATE.md) |
| state-burst CAS push | `plugins/vsdd-factory/skills/state-burst/SKILL.md` | Effectful (git subprocess) |
| STATE.md frontmatter region | `.factory/STATE.md` | Effectful (file on factory-artifacts branch) |

**Subsystem anchor justification:** SS-05 owns this story's scope because SS-05 is
"Pipeline Orchestration" per ARCH-INDEX Subsystem Registry — it includes `state-manager`
as the sole STATE.md writer (TD-VSDD-053) and the `state-burst` skill that manages the
factory-artifacts push lifecycle. The `factory_lock` schema and CAS push are both
state-manager/state-burst responsibilities, squarely within SS-05.

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | `state-manager` crashes mid-burst before writing `expires_at` renewal | Existing `expires_at` remains; auto-expires at TTL boundary (max 45-min wedge); next developer can acquire |
| EC-002 | `state-burst` CAS push rejected (concurrent write) | Non-zero exit; `CASPushRejected` error emitted; local `.factory/` commit preserved; developer fetches and re-runs |
| EC-003 | `git fetch origin factory-artifacts` fails (network error) | Non-zero exit; error emitted; push MUST NOT proceed |
| EC-004 | `EXPECTED_SHA` fetch succeeds but remote advances before push | `--force-with-lease` rejects push; `CASPushRejected`; safe — no clobber |
| EC-005 | Factory unlocked (`factory_lock` absent); state-burst CAS proceeds | Fetch + CAS push proceeds; if remote advanced, `CASPushRejected`; developer fetches + retries |
| EC-006 | Lock held by self; `expires_at` within 5 minutes of current time | Mid-burst renewal still refreshes `expires_at = now + 2700s` on next commit; no special behavior for near-expiry |
| EC-007 | Long burst (3 intermediate commits spanning 70 minutes total) | Each commit resets `expires_at = now + 2700s`; lock remains valid throughout; final `expires_at` = 45 min from last commit |

## Tasks

- [ ] **T-1 — Read current STATE.md frontmatter to confirm no existing `factory_lock` key** (pre-implementation baseline — codebase grounding confirms absent field before adding)
- [ ] **T-2 — Add `factory_lock` schema definition to the state-manager prose spec** (`agents/state-manager.md`): document the three required fields (`holder`, `locked_at`, `expires_at`), the TTL constant (2700s), and the mid-burst renewal obligation. Include the YAML form verbatim from BC-5.40.001 PC1.
- [ ] **T-3 — Implement `factory_lock` write helper in state-manager workflow** (acquire write + renewal write + clear-on-unlock). The helper MUST:
  - Write the three-field YAML block on acquire (holder = `git config user.email`, locked_at = now, expires_at = now + 2700s)
  - Refresh `expires_at = now + 2700s` on every intermediate burst commit (renewal — same commit as any other STATE.md field advance)
  - Remove the key entirely (not null) on unlock
- [ ] **T-4 — Replace blind push in `plugins/vsdd-factory/skills/state-burst/SKILL.md`** with the fetch-then-CAS primitive: `git -C .factory fetch origin factory-artifacts` then capture `EXPECTED_SHA=$(git -C .factory rev-parse origin/factory-artifacts)` then `git -C .factory push --force-with-lease=factory-artifacts:"${EXPECTED_SHA}" origin factory-artifacts`. Add non-zero exit + `CASPushRejected` error message on push rejection. Add non-zero exit + `fetch error` message on fetch failure.
- [ ] **T-5 — Write unit tests** covering AC-001..AC-010 (Rust unit tests for schema writer helper; bats test for state-burst CAS push change). Red Gate: all tests FAIL before implementation. Test function names per AC bodies above.
- [ ] **T-6 — Verify `hooks/verify-git-push.sh` unchanged** (grep confirms `--force-with-lease` is already in the allow-list; file diff MUST be empty for this file after all story changes). Document the grep result in the Red Gate log.
- [ ] **T-7 — Run bats regression suite** against existing state-burst bats (if any) to confirm no regressions from the push command change.

## Previous Story Intelligence

N/A — first story in Epic E-17. No predecessor stories in this epic exist yet.

Adjacent context: The closest structural sibling is the `state-burst` skill itself
(`plugins/vsdd-factory/skills/state-burst/SKILL.md`). Read it before implementing T-4.
The `validate-artifact-path.wasm` crate (`crates/hook-plugins/validate-artifact-path/`)
is the nearest analogous WASM plugin — its Rust patterns and `Cargo.toml` structure
inform S-17.02 (the guard crate). Not needed for this story.

## Architecture Compliance Rules

From `architecture/module-decomposition.md` and ADR-025 v1.2:

1. **`state-manager` is the SOLE writer of `factory_lock`** (TD-VSDD-053 single-writer
   discipline). No other agent, skill, or tool writes the `factory_lock` block directly.
   Skills delegate to `state-manager`.

2. **`factory_lock` MUST be removed entirely (not nulled) on unlock.** A null value
   (`factory_lock: null`) violates BC-5.40.001 PC2. Implementation MUST use key
   deletion, not value assignment.

3. **`--force-with-lease` is already permitted by `hooks/verify-git-push.sh`** (ADR-025
   Decision 8). No changes to the push-hook allow-list are required or permitted by this
   story. `verify-git-push.sh` MUST be unchanged.

4. **The TTL constant is 2700 seconds (non-configurable).** Any code that computes
   `expires_at` MUST embed `2700u64` (or equivalent typed constant) directly. User-facing
   TTL configuration is out of scope per BC-5.40.001 Invariant 2.

5. **`expires_at` is always `now + 2700s` at write time.** `locked_at` is immutable
   after acquire. Renewal updates `expires_at` only; `locked_at` MUST NOT change.

## Library and Framework Requirements

| Dependency | Version | Usage |
|------------|---------|-------|
| bash (in SKILL.md) | system shell | `git -C .factory fetch/push --force-with-lease` shell commands |
| git | ≥2.6 (for `--force-with-lease=<refname>:<sha>` explicit form) | CAS push in state-burst |

No new Rust crate dependencies for this story (state-burst/SKILL.md is a bash skill).
The `factory_lock` schema write is a prose/skill-spec change, not a Rust crate change.

**Forbidden dependencies:** This story MUST NOT introduce a Rust crate dependency on
any WASM-compiled crate. The WASM guard crate is S-17.02's scope; S-17.01 touches only
the skill specification layer.

## File Structure Requirements

| File | Action | Purpose |
|------|--------|---------|
| `plugins/vsdd-factory/skills/state-burst/SKILL.md` | MODIFY | Replace blind push with fetch-then-CAS push sequence (D6) |
| `agents/state-manager.md` | MODIFY | Add `factory_lock` write/renewal/clear obligation to the burst commit protocol |
| `.factory/STATE.md` | MODIFY (frontmatter) | Add `factory_lock:` key (initially absent = unlocked; written by state-manager on acquire) |
| `plugins/vsdd-factory/hooks/verify-git-push.sh` | NO CHANGE | Already permits `--force-with-lease`; confirm by grep; MUST NOT be modified |

**Note:** No new files are created by this story. The schema is a frontmatter addition
to STATE.md (documented), the CAS push is an amendment to state-burst/SKILL.md, and
the write obligation is an addition to agents/state-manager.md.

## Token Budget Estimate

| Component | Estimated Tokens |
|-----------|-----------------|
| Story spec (this file) | ~6,000 |
| BC-5.40.001 (schema + CAS push BC) | ~5,500 |
| ADR-025 v1.2 (decisions 2, 5, 8, 10) | ~8,000 |
| state-burst/SKILL.md (target file) | ~3,000 |
| agents/state-manager.md (target file) | ~5,000 |
| .factory/STATE.md (frontmatter only) | ~2,000 |
| Test files (unit + bats) | ~4,000 |
| Tool outputs (git diffs, test output) | ~3,000 |
| **Total** | **~36,500** |

**Budget usage:** ~36,500 / ~200,000 ≈ 18% of agent context window. Within the 20–30%
single-story ceiling.

## Red Gate Test Table

| Test Name | BC Clause | Pass Condition | Initial State (Red Gate) |
|-----------|-----------|---------------|--------------------------|
| `test_BC_5_40_001_schema_correctness_on_acquire` | PC1 | STATE.md has holder + locked_at + expires_at = locked_at+2700s | FAIL: field absent |
| `test_BC_5_40_001_unlock_removes_key_not_nulls` | PC2 | `factory_lock` key absent after unlock | FAIL: key present or null |
| `test_BC_5_40_001_ttl_expired_block_persists_until_next_write` | PC3 | guard sees past `expires_at` → Continue; block remains in STATE.md | FAIL: no block written |
| `test_BC_5_40_001_mid_burst_renewal_updates_expires_at_preserves_locked_at` | PC4 | After renewal: `expires_at` advanced, `locked_at` unchanged | FAIL: no renewal logic |
| `test_BC_5_40_001_cas_push_rejected_on_concurrent_write` | PC5 | Non-zero exit + `CASPushRejected` error on mock push rejection | FAIL: blind push has no rejection handling |
| `test_BC_5_40_001_absent_block_is_unlocked_state` | PC6 | No `factory_lock` key = guard returns Continue | FAIL: no guard yet (S-17.02 installs it) |
| `test_BC_5_40_001_ttl_constant_is_2700_seconds` | Invariant 2 | expires_at - locked_at == 2700 | FAIL: no TTL constant |
| `test_BC_5_40_001_fetch_failure_aborts_push` | EC-003 | Fetch fails → non-zero exit, error message, no push | FAIL: current skill has no fetch step |
| `test_BC_5_40_001_verify_git_push_hook_unchanged` | Invariant 5 | `verify-git-push.sh` diff is empty | FAIL: not yet verified |

**D9 coverage note (BC-5.40.001):** The bats integration scenarios for the schema and
CAS push (BC-5.40.001 canonical test vectors rows 1–5) are the test surface for this
story. The guard-dependent bats scenarios (BC-4.13.001 T-1..T-10) belong to S-17.02.

## Demo Plan

Per-AC demo evidence required by demo-recorder:

| AC | Demo Command | Expected Output |
|----|-------------|-----------------|
| AC-001/AC-007 | `cat .factory/STATE.md` after mock acquire | `factory_lock:` block with all 3 fields; `expires_at - locked_at == 2700s` |
| AC-002 | `cat .factory/STATE.md` after mock unlock | No `factory_lock:` key in frontmatter |
| AC-004 | `git log --oneline -3` on factory-artifacts + `cat .factory/STATE.md` after 3-commit burst | `expires_at` updated on each commit; `locked_at` unchanged |
| AC-005 | Run state-burst with mock concurrent remote advance | Non-zero exit + `CASPushRejected` error message |
| AC-010 | Run state-burst with mock network failure on fetch | Non-zero exit + fetch error message |

## Risk

| Risk | Severity | Mitigation |
|------|----------|-----------|
| Mid-burst renewal missed on one of multiple intermediate commits | HIGH | AC-004 bats test covers 3-commit burst; test MUST verify each intermediate commit refreshes `expires_at` |
| CAS push using `--force-with-lease=factory-artifacts:<sha>` form (explicit sha) not supported by older git | MEDIUM | ADR-025 §Decision 8 cites git ≥2.6 for `--force-with-lease=<refname>:<expect>` form; document version requirement in skill |
| `state-burst` CAS push swallows non-fast-forward exit and returns 0 | HIGH | AC-005 Red Gate test explicitly mocks rejection and asserts non-zero exit |

## Changelog

| Version | Date | Description |
|---------|------|-------------|
| 1.0 | 2026-06-10 | Initial authoring (story-writer; brownfield-backfill issue #170; ADR-025 v1.2 D3/D6; BC-5.40.001 v1.0). 10 ACs tracing PC1–PC6 + Invariants 2/3/5 + EC-003. Wave 1; points 5; tdd_mode strict; depends_on []; blocks [S-17.02, S-17.03]. |
