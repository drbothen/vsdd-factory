---
document_type: story
level: implementation
story_id: "S-25.01"
epic_id: "E-25"
version: "1.9"
last_amended: "2026-08-31 (v1.9) — ADR-048 gate redesign sync: BC-1.18.002 v1.4→v1.5 (block_if_marker crash policy; PC5/PC6; INV6 ungated-escape; EC-031/EC-032 crash ECs; AC-011 updated; AC-020 added), BC-1.18.003 v1.1→v1.2 (TTL-expiry PC4; AC-021 added; story EC-035 added), BC-1.18.001 v1.0→v1.1 (expires_at 6th TOML field; AC-005 + T-3 updated). Story EC-033..EC-035 added. Token budget updated (ADR-048 row added). Architecture Compliance Rules gate rule updated. T-5 on_error updated to block_if_marker. Frontmatter bcs/verification_properties/input-hash unchanged. [Prior: 2026-08-31 (v1.8) — BC-1.18.003 v1.0→v1.1 sync (artifact-scoped marker clear): (1) BC table BC-1.18.003 version cell v1.0→v1.1; role description updated to note artifact-scoped clear (plugin_name AND artifact_path, with empty-artifact_path→name-only fallback). (2) AC-012 updated: clear now requires (a) plugin_name match AND (b) artifact_path match (exact normalized absolute path equality); empty-path fallback documented (non-artifact-scoped validators); same-plugin/different-non-empty-artifact → no-clear stated explicitly with story EC-031 reference. (3) Story EC-031 added: same plugin, different non-empty artifact, PostToolUse Pass → marker NOT cleared (BC-1.18.003 EC-008). (4) Story EC-032 added: same plugin, empty marker artifact_path, PostToolUse Pass on any artifact → marker cleared (BC-1.18.003 EC-009 empty-path fallback). (5) Token budget BC-1.18.003 row updated 1,800→2,200 (EC-008/EC-009 coverage); total ~56,600→~57,000; budget ~28%→~29%. ADR-047 §D5 (artifact-scoped clear + empty-path fallback). Frontmatter bcs/verification_properties/input-hash unchanged. [Prior: 2026-08-31 (v1.7) — MEDIUM-2 dep-table fix: `regex` row in Library & Framework Requirements (MANDATORY) replaced with `shell-words 1.1` (Phase 1b POSIX quote-aware tokenization; BC-1.18.002 v1.3+ forbids regex quote-stripping as parser-differential bypass; story body already mandated shell-words in AC-008/AC-009/Purity/T-4/T-5 — MANDATORY table row was the sole stale location); EC sync to BC-1.18.002 v1.4: EC-030 added (gate encounters unreadable marker EACCES/IO → Allow fail-open, INV2 self-lock avoidance); EC-008 cross-reference note updated (BC-1.18.002 EC-008 v1.4 rescoped: readable-but-malformed → Block gate path, distinct from story EC-008 write-fail BC-1.18.001 path); BC table BC-1.18.002 version cell v1.3→v1.4. Frontmatter bcs/verification_properties/input-hash unchanged. [Prior: 2026-08-31 (v1.6) — AC-008/AC-009/AC-011/AC-012/EC sync to BC-1.18.002 v1.3 (HIGH-1 fail-open-on-crash + MEDIUM-2 threat-model + quoting-in-scope): AC-011 corrected to require on_error=\"continue\" (was \"block\") for both gate arms + two-axis model note (ADR-039; ADR-047 §Decision 9); AC-012 adds PostToolUse qualifier (BC-1.18.003 PC1 — PreToolUse PASS does NOT clear marker); AC-008 adds Phase 1b POSIX quote-aware tokenization (shell-words crate: git \"commit\"/git 'push'/g'i't commit → BLOCKED) + out-of-scope boundary (eval/xargs/subshell/indirection — undecidable; caught by other layers); AC-009 adds Phase 1b note + EC-027..EC-029 NOT-blocked out-of-scope examples; story EC-021..EC-026 added for BC EC-024..EC-029; BC table BC-1.18.002 v1.2→v1.3; T-5 TOML on_error corrected to \"continue\"; token budget BC-1.18.002 3,500→4,000; Architecture Mapping/Purity Classification/T-4 v1.2→v1.3 refs updated. [Prior: 2026-08-31 (v1.5) — AC-008/AC-009/EC sync to BC-1.18.002 v1.2 (MEDIUM-1 adversary finding): three-phase algorithm (compound splitting, basename, complete arg-taking set, fail-safe on unrecognized options) is now the authoritative rule in AC-008; regex demoted to illustrative-only; AC-009 updated with all Phase 1–4 behaviors and new examples from BC EC-013..EC-023; added story EC-010..EC-020 for BC EC-013..EC-023; EC-006/EC-007 regex reference replaced with Phase 4 exact-subcommand citation; BC table v1.1→v1.2; Architecture Mapping/Purity Classification/T-4/T-5 is_git_commit_or_push description updated; token budget BC-1.18.002 2,600→3,500. [Prior: 2026-08-31 (v1.4) — AC-009 exact-subcommand sync to BC-1.18.002 v1.1 (pass-1 adversary BLOCKER-3 adjudication): regex replaced with authoritative is_git_commit_or_push exact-subcommand rule; EC-011/EC-012 examples added; BC table version updated v1.0→v1.1. [Prior: 2026-08-30 (v1.3) — input-hash re-sync 132d934→8fbeba1 (VP-105 v1.3→v1.4 LOW-001 proof-harness bats comment corrected; POLICY 18 three-way parity maintained). Story content UNCHANGED. [Prior: 2026-08-30 (v1.3) — re-audit M-1: BC-table Title cells → verbatim H1 (POLICY 7). [Prior: 2026-08-30 (v1.2) — consistency audit correction]]]]]"
status: draft
producer: story-writer
timestamp: 2026-08-30T00:00:00Z
phase: 3
inputs:
  - .factory/feature-delta/validation-integrity-layer1/F1-delta-analysis.md
  - .factory/specs/architecture/decisions/ADR-047-indeterminate-outcome-model-durable-mutation-marker-next-advance-gate.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.001.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.002.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.003.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.004.md
  - .factory/specs/behavioral-contracts/ss-03/BC-3.08.001.md
  - .factory/specs/verification-properties/VP-102.md
  - .factory/specs/verification-properties/VP-103.md
  - .factory/specs/verification-properties/VP-104.md
  - .factory/specs/verification-properties/VP-105.md
  - .factory/specs/verification-properties/VP-106.md
  - .factory/specs/verification-properties/VP-107.md
  - .factory/specs/architecture/decisions/ADR-048-fail-closed-but-recoverable-gate-block-if-marker-crash-policy-marker-ttl-deadman-and-ungated-escape-invariant.md
input-hash: "8bf7fa8"
traces_to: .factory/stories/epics/E-25-validation-integrity.md
points: "12"
depends_on: [S-21.10]
blocks: [S-25.02]
behavioral_contracts:
  - BC-1.18.001
  - BC-1.18.002
  - BC-1.18.003
  - BC-1.18.004
  - BC-3.08.001
verification_properties: [VP-102, VP-103, VP-104, VP-105, VP-106, VP-107]
priority: "P0"
cycle: v1.0-brownfield-backfill
wave: 1
target_module: crates/factory-dispatcher
subsystems: [SS-01, SS-03, SS-04, SS-07]
estimated_days: 5
assumption_validations: []
risk_mitigations: []
tdd_mode: strict
---

> **tdd_mode: strict** — Full TDD Iron Law enforced. All non-trivial function bodies MUST use
> `todo!()`. Red Gate density check ≥0.5 required before Step 4 (implementer) dispatch. Every
> test in the Red Gate list MUST be a failing stub before any implementation begins.

> **Execute:** `cargo test --workspace --all-targets && cd plugins/vsdd-factory/tests && ./run-all.sh`

# S-25.01: Dispatcher INDETERMINATE Outcome Layer 1 — Fail-Loud on Cannot-Complete: Durable Marker + Next-Advance Gate

## Narrative

- **As a** factory pipeline operator whose validators run in fuel-bounded and epoch-bounded WASM
  sandboxes
- **I want** the dispatcher to make "couldn't validate" a FIRST-CLASS, fail-LOUD outcome that
  writes a durable `.factory/unvalidated-mutation.marker` file and blocks the next state-advancing
  Agent dispatch and `git commit`/`git push` Bash dispatch until the affected artifact is
  re-validated or an operator explicitly clears the marker
- **So that** the silent CWE-754 hole (treating INDETERMINATE as PASS) is permanently closed for
  `validate-factory-path-staging` (Cohort A-immediate; EFFECTIVE-NOW at S-25.01 merge) and
  pre-configured for `validate-pr-merge-prerequisites` and `validate-wave-gate-prerequisite`
  (Cohort A-deferred; SET-BUT-LATENT — set in registry with comment, enforcement conditional on
  S-21.24); existing ~76 fail-open plugins are completely unchanged (advisory
  `plugin.indeterminate` event only)

## Context / Feature Summary

**Layer 1 of the three-layer validation-integrity architecture (ADR-047 v1.3, human-ratified
2026-08-30). Delivered independently of S-21.19–S-21.24 (enforcement seams).**

Operational forensics driving this story (F1-delta-analysis.md §3):
- ~11,262 `plugin.timeout { cause: Fuel }` events in the event log
- ~480 `plugin.timeout { cause: Epoch }` events
- 167 `host_fn_returned_output_too_large` events (read path OutputTooLarge returning −3 to WASM)
- ~455 events where the entire validator suite wiped out on a single artifact edit

Current behavior: all three root causes (fuel exhaustion, epoch timeout, OutputTooLarge) are
treated as PASS by the dispatcher because the plugin "returned" (via the trap mechanism) without
setting a non-zero exit code. This is a PASS misclassification — the correct outcome is
INDETERMINATE ("could not determine"). CWE-754: Improper Check for Exceptional Conditions.

Layer 1 fix:
1. New INDETERMINATE outcome class in the dispatcher (distinct from PASS and FAIL)
2. For fail-closed plugins: write durable `.factory/unvalidated-mutation.marker` + emit
   `plugin.indeterminate` event
3. New `validate-unvalidated-mutation-marker` WASM plugin: TWO hooks-registry entries — Arm 1
   blocks `^Agent$` PreToolUse dispatches while marker exists; Arm 2 blocks `git commit`/
   `git push` Bash PreToolUse dispatches while marker exists
4. Marker clear: PASS re-validation by the named plugin deletes the marker (unblocks both arms)
5. Cohort A fail-closed assignments: all three human-confirmed validators SET to
   `failure_policy = "fail-closed"` in hooks-registry.toml (ADR-047 §D8a v1.3):
   - **Cohort A-immediate — `validate-factory-path-staging`** (EFFECTIVE-NOW): set
     unconditionally; PreToolUse `^Bash$`; `on_error = "continue"`; bounded input (file path
     from tool params); no self-lock risk; no S-21.24 calibration dependency.
   - **Cohort A-deferred — `validate-pr-merge-prerequisites` and
     `validate-wave-gate-prerequisite`** (SET-BUT-LATENT): field SET in registry with comment
     `# ACTIVATION CONDITIONAL on S-21.24 calibration per ADR-047 §D8a Cohort A-deferred`.
     Field is inert (S-21.10 schema-only; ADR-039 Phase 4 enforcement not wired until S-21.24).
     Enforcement activates when S-21.24 merges (confirming S-21.21 calibration + S-21.23
     break-glass). PreToolUse `^Agent$`; `on_error = "block"` means self-lock risk if
     activated without calibration.
   - **Layer-1 effective fail-closed count at S-25.01 merge: 1**
     (`validate-factory-path-staging`). The two deferred validators are present in registry
     but their enforcement is latent until S-21.24.
6. For fail-open plugins (~76 existing): advisory `plugin.indeterminate` event only; no marker;
   no gate; backward-compat guard test MUST NOT be deleted

**Subsystem anchor justifications:**
- SS-01 owns this story's primary scope because SS-01 is the Hook Dispatcher Core (executor.rs,
  invoke.rs, indeterminate_marker.rs) per ARCH-INDEX Subsystem Registry; INDETERMINATE
  classification and marker-write logic are dispatcher core responsibilities
- SS-03 owns Event 8 plugin.indeterminate wire format per ARCH-INDEX Subsystem Registry (SS-03 =
  OTel-aligned event catalog and emission pipeline)
- SS-04 owns the new `validate-unvalidated-mutation-marker` WASM plugin crate per ARCH-INDEX
  Subsystem Registry (SS-04 = Hook Plugin SDK and WASM plugin development)
- SS-07 owns hooks-registry.toml configuration per ARCH-INDEX Subsystem Registry (SS-07 = Hook
  Registry Configuration; Cohort A fail-closed assignments and gate plugin registrations are
  registry-config changes)

**Dependency anchor justification:**
- S-25.01 depends_on [S-21.10] because S-21.10 (MERGED, PR #780) introduced the `FailurePolicy`
  enum and `RegistryEntry.failure_policy` field into the executor's hook invocation path. The
  `classify_outcome(PluginResult, FailurePolicy, bool) -> DispatchOutcome` pure function added
  in this story reads the `failure_policy` field to route INDETERMINATE outcomes to either the
  marker-write path (fail-closed) or the advisory-only path (fail-open). Without the
  `FailurePolicy` field, there is no mechanism to distinguish these two paths at runtime. S-25.01
  does NOT depend on S-21.19–S-21.24 (enforcement seams) — per the human decision recorded in
  the F3 dispatch; independent delivery confirmed.

## Acceptance Criteria

### BC-1.18.001 — INDETERMINATE Outcome Classification, plugin.indeterminate Event, Durable Marker

**AC-001** (traces to BC-1.18.001 postcondition 1 — fuel cause)

When a fail-closed plugin's WASM execution traps and `trap.downcast_ref::<Trap>()` yields
`Some(Trap::OutOfFuel)`, the dispatcher classifies the outcome as `DispatchOutcome::Indeterminate`
with `cause = "fuel"`. `get_fuel()` is NOT the authoritative check — only the Trap downcast is.

**AC-002** (traces to BC-1.18.001 postcondition 1 — epoch timeout cause)

When a fail-closed plugin's WASM execution traps and `trap.downcast_ref::<Trap>()` yields
`Some(Trap::Interrupt)`, the dispatcher classifies the outcome as `DispatchOutcome::Indeterminate`
with `cause = "epoch"`.

**AC-003** (traces to BC-1.18.001 postcondition 1 + invariant 1 — OutputTooLarge cause with
per-invocation Store flag reset)

When any host function returns `OutputTooLarge(-3)` to the plugin WASM during execution (setting
`StoreData::host_output_too_large_seen = true`), AND the plugin subsequently completes with
`exit_code = 0`, AND `failure_policy = fail-closed`, the outcome MUST be
`DispatchOutcome::Indeterminate` with `cause = "output-too-large"`. The `host_output_too_large_seen`
flag MUST be reset to `false` immediately before each WASM module invocation (not only at `Store`
creation) — this is a correctness invariant (per-invocation reset, not per-Store creation reset).

**AC-004** (traces to BC-1.18.001 postcondition 2 + postcondition 5 + invariant 2 — outcome trichotomy; non-exhaustive Trap wildcard)

`DispatchOutcome::Indeterminate` is a distinct variant from `DispatchOutcome::Pass` and
`DispatchOutcome::Fail`. PASS requires `exit_code = 0` AND `host_output_too_large_seen = false`.
FAIL requires non-zero `exit_code`. INDETERMINATE covers the remaining gap. Existing PASS/FAIL
semantics for `PluginResult::Ok`, `PluginResult::Crash`, and normal `PluginResult::Timeout`
variants are UNCHANGED (backward-compat anchor). The Trap match arm in `classify_outcome`
MUST include a wildcard arm `_ => { /* not INDETERMINATE; route to existing on_error handling */ }`
— `Trap` is `#[non_exhaustive]`; a future unrecognized Trap variant MUST NOT be silently bucketed
as INDETERMINATE (BC-1.18.001 invariant 2).

**AC-005** (traces to BC-1.18.001 postcondition 4 + invariant 3 + invariant 4 — atomic marker write; single-marker policy; PostToolUse-only)

When `failure_policy = fail-closed` AND hook type is `PostToolUse` AND outcome is `Indeterminate`,
the dispatcher atomically writes `.factory/unvalidated-mutation.marker` via write-to-temp + rename
(`O_CREAT | O_WRONLY | O_TRUNC` then `rename`). The marker file MUST contain all six required
TOML fields: `timestamp` (RFC 3339), `plugin_name` (string), `artifact_path` (string, empty
string when no artifact context — never omit the field), `cause` (one of `"fuel"`, `"epoch"`,
`"output-too-large"`), `trace_id` (string), `expires_at` (RFC 3339 UTC timestamp = `timestamp
+ UNVALIDATED_MUTATION_MARKER_TTL_SECONDS`; written by `write_indeterminate_marker` at creation;
`UNVALIDATED_MUTATION_MARKER_TTL_SECONDS = 86_400`; enables gate TTL auto-clear per
BC-1.18.003 PC4 and dispatcher native block_if_marker TTL check per BC-1.18.002 PC5/PC6;
BC-1.18.001 PC4 v1.1; ADR-048 §Decision 2). Single-marker policy: a second INDETERMINATE event
overwrites (last-writer-wins). The marker write is PostToolUse only.

**AC-006** (traces to BC-1.18.001 postcondition 3; BC-3.08.001 Event 8 — plugin.indeterminate
event)

The dispatcher emits a `plugin.indeterminate` event to the OTel-aligned FileSink (`events-*.jsonl`)
for EVERY INDETERMINATE outcome (both fail-closed and fail-open). The event wire format MUST
include all mandatory fields per BC-3.08.001 Event 8: `type` ("plugin.indeterminate"),
`trace_id`, `session_id`, `plugin_name`, `artifact_path` (empty string when no artifact context),
`cause` (`"fuel"` | `"epoch"` | `"output-too-large"`), `failure_policy` (`"fail-closed"` or
`"fail-open"`), `timestamp`. BC-3.08.001 is the wire-format authority; BC-1.18.001 is the
triggering-condition authority.

### BC-1.18.002 — Next-Advance Gate: Two-Arm Quarantine

**AC-007** (traces to BC-1.18.002 postcondition 1 + invariant 4 — Agent arm block; block message completeness)

When `.factory/unvalidated-mutation.marker` exists AND a `PreToolUse` hook fires for an `^Agent$`
dispatch, the `validate-unvalidated-mutation-marker` WASM plugin returns `exit_code = 2`
(block). The block message MUST contain: the `plugin_name` from the marker, the `artifact_path`
from the marker, the `cause` from the marker, a re-validation command (e.g., re-invoke the named
plugin by running the relevant dispatcher command), and the manual escape hatch instruction
(`rm .factory/unvalidated-mutation.marker`). The message MUST be machine-parseable (structured
fields, not freeform prose).

**AC-008** (traces to BC-1.18.002 postcondition 2 + invariant 4 — Bash arm block: git commit/push Phase 1/1b/2/3/4 security filter algorithm (v1.3); quoting-in-scope; block message completeness)

When `.factory/unvalidated-mutation.marker` exists AND a `PreToolUse` hook fires for a `^Bash$`
dispatch for which `is_git_commit_or_push(command)` returns `true`, the
`validate-unvalidated-mutation-marker-git` WASM plugin returns `exit_code = 2` (block). The
block message format is identical to AC-007.

`is_git_commit_or_push(command) → bool` is the authoritative production-grade security filter
(BC-1.18.002 PC2 v1.3; ADR-047 §Decision 9). Under-blocking is the dangerous failure mode;
the algorithm is fail-safe on ambiguity:

- **Phase 1 — Compound command splitting.** Split `command` on `&&`, `||`, `;`, `|`, `&`, and
  newline. Trim each segment. Apply Phases 1b–4 to each segment independently; return `true` if
  ANY segment returns `true`.
- **Phase 1b — POSIX quote-aware tokenization.** Before applying Phases 2–4, tokenize each
  segment from Phase 1 using POSIX word-splitting rules via the `shell-words` crate. Quote
  removal and token concatenation occur at this phase — Phases 2–4 operate on the resulting
  token list, not on the raw whitespace-split string. Examples: `git "commit"` → tokens
  `["git", "commit"]`; `git 'push' origin` → tokens `["git", "push", "origin"]`; `g'i't commit`
  → tokens `["git", "commit"]` (POSIX concatenates unquoted `g`, single-quoted `i`, and unquoted
  `t` into the token `git`). Do NOT implement ad-hoc quote-stripping (e.g., `s/["']//g` regex)
  — the `shell-words` crate provides POSIX parser-equivalence, eliminating the parser-differential
  bypass class where ad-hoc stripping could produce different tokenization than the actual shell.
- **Phase 2 — Executable identification by basename.** Strip leading env-var assignment tokens
  (matching `^[A-Za-z_][A-Za-z0-9_]*=`) and the literal `env` token if it is the first token.
  Compute `basename` of the first remaining token (strip all characters up to and including the
  last `/`). If `basename ≠ "git"` (case-sensitive), return `false` for that segment.
- **Phase 3 — Skip global options.** Scan tokens after `git`: `--` ends Phase 3 (next token is
  subcommand); `--flag=value` skips one token; tokens in the **complete recognized arg-taking set**
  (`-C`, `-c`, `--namespace`, `--git-dir`, `--work-tree`, `--super-prefix`, `--config-env`) skip
  that token AND the immediately following token; tokens in the recognized no-arg set are skipped;
  an unrecognized token starting with `-` triggers **fail-safe → return `true` (block)** — arity
  is unknown from the closed recognized set, and under-blocking is the dangerous failure mode.
- **Phase 4 — Subcommand match.** Return `true` iff the candidate subcommand is exactly `commit`
  or `push` (case-sensitive, full-token match). `commit-graph`, `commit-tree`, `push-pack`, and
  any other token that merely contains `commit` or `push` as a substring do NOT match.

The illustrative regex `\bgit\b.*\b(commit|push)\b` approximates this rule but is NOT
authoritative — it false-positives on `git commit-graph write` because the hyphen in `commit-graph`
is a word boundary making `\bcommit\b` match inside a different subcommand token (EC-011); it
cannot detect the gated subcommand in compound commands (EC-015/EC-016); and it does not enforce
basename matching (EC-017/EC-018). The Phase 1/1b/2/3/4 algorithm above is the sole authoritative
specification.

Commands that MUST be blocked (non-exhaustive): `git commit --amend` (EC-006), `git push
--force-with-lease` (EC-007), `git --git-dir .git commit -m "state"` (EC-013), `git --work-tree
/tmp push` (EC-014), `git status && git commit -m "x"` (EC-015), `git diff ; git push` (EC-016),
`/usr/bin/git commit -m "init"` (EC-017), `./git push origin main` (EC-018), `env GIT_DIR=.git
git commit -m "x"` (EC-021), `git --unknown-flag commit` (EC-022, fail-safe), `git --config-env
FOO=BAR commit` (EC-023), `git "commit"` (EC-024, Phase 1b quoting), `git 'push' origin`
(EC-025, Phase 1b quoting), `g'i't commit` (EC-026, Phase 1b POSIX token concatenation).

The following are OUT OF SCOPE by design — blocking these forms is UNDECIDABLE for static string
analysis (Rice's theorem). They are ALLOWED under the fail-open posture; the durable marker
written at PostToolUse time, the `^Agent$` next-advance arm, and GitHub server-side branch
protection provide defense-in-depth. See BC-1.18.002 Threat Model & Scope for the canonical
actor model (cooperating AI agent, NOT adversary-resistant boundary): `$(git commit -m "x")`
(EC-027, command substitution), `echo x | xargs git commit` (EC-028, xargs indirection),
`eval "git push origin main"` (EC-029, dynamic evaluation), and variable/alias/function
indirection forms.

**AC-009** (traces to BC-1.18.002 postcondition 3 — non-advancing not gated; out-of-scope forms documented)

Bash dispatches for which `is_git_commit_or_push(command)` returns `false` are NOT gated even
when the marker exists. Read, Edit, Write, MultiEdit tool dispatches are NOT gated.

The v1.3 algorithm (authoritative per BC-1.18.002 PC2 v1.3; ADR-047 §Decision 9; full
definition in AC-008) is the load-bearing pure filter. Key NOT-gated behaviors:

- **Phase 1 (compound splitting):** A compound command is only NOT gated if ALL segments return
  `false`; if any segment resolves to `commit` or `push`, the function returns `true` (blocked).
- **Phase 1b (POSIX quote-aware tokenization):** The `shell-words` crate tokenizes each segment.
  Quoted forms that resolve to `commit` or `push` after quote removal ARE blocked (e.g.,
  `git "commit"` → BLOCKED per EC-024; `git 'push' origin` → BLOCKED per EC-025; `g'i't commit`
  → BLOCKED per EC-026). This phase is what differentiates v1.3 from v1.2.
- **Phase 2 (basename):** A command is NOT gated if `basename(executable) ≠ "git"` (e.g.,
  `cat gitfile` — `basename("cat") = "cat" ≠ "git"`, returns `false`).
- **Phase 3–4 (option skip + subcommand match):** The recognized arg-taking global-option set
  (`-C`, `-c`, `--namespace`, `--git-dir`, `--work-tree`, `--super-prefix`, `--config-env`) each
  consume the following token as their argument before subcommand identification. Recognized no-arg
  flags are skipped without consuming a following token. An unrecognized `-`-prefixed token
  triggers fail-safe → `true` (blocked). Only exact tokens `commit` or `push` are gated.

The illustrative regex `\bgit\b.*\b(commit|push)\b` is NOT the authoritative rule — see AC-008.

BC-1.18.002 EC-001 through EC-005 MUST pass (non-advancing commands — `git status`, `git log`,
`git diff`, `git fetch`, non-git commands — `is_git_commit_or_push` returns `false`).
Additionally:
- `git commit-graph write` → NOT blocked: Phase 4 full-token match; subcommand is `commit-graph`,
  not `commit` (EC-011).
- `git -C .factory commit -m "state"` → BLOCKED: Phase 3 recognizes `-C` as arg-taking; `.factory`
  consumed as argument; Phase 4 subcommand is `commit` (EC-012).
- `git --git-dir .git commit -m "state"` → BLOCKED: Phase 3 `--git-dir` arg-taking; `.git`
  consumed; Phase 4 subcommand is `commit` (EC-013).
- `git --work-tree /tmp push` → BLOCKED: Phase 3 `--work-tree` arg-taking; `/tmp` consumed;
  Phase 4 subcommand is `push` (EC-014).
- `git status && git commit -m "x"` → BLOCKED: Phase 1 splits on `&&`; segment 2 subcommand is
  `commit`; any-segment `true` → function returns `true` (EC-015).
- `git diff ; git push` → BLOCKED: Phase 1 splits on `;`; segment 2 subcommand is `push` (EC-016).
- `/usr/bin/git commit -m "init"` → BLOCKED: Phase 2 `basename("/usr/bin/git") = "git"`;
  Phase 4 subcommand `commit` (EC-017).
- `./git push origin main` → BLOCKED: Phase 2 `basename("./git") = "git"`; Phase 4 subcommand
  `push` (EC-018).
- `git status && git log --oneline` → NOT blocked: Phase 1 splits; segment 1 subcommand `status`,
  segment 2 subcommand `log`; no segment matches `commit` or `push`; returns `false` (EC-019).
- `env GIT_DIR=.git git commit -m "x"` → BLOCKED: Phase 2 strips `env` + `GIT_DIR=.git`;
  executable is `git`; Phase 4 subcommand `commit` (EC-021).
- `git --unknown-flag commit` → BLOCKED (fail-safe): Phase 3 unrecognized option, no `=`,
  subcommand position uncertain; returns `true` (EC-022).
- `git --config-env FOO=BAR commit` → BLOCKED: Phase 3 `--config-env` arg-taking; `FOO=BAR`
  consumed; Phase 4 subcommand `commit` (EC-023).

**Phase 1b quoting (v1.3 additions — IN SCOPE):**
- `git "commit"` → BLOCKED: Phase 1b POSIX tokenization; `"commit"` → token `commit`; Phase 4
  match (EC-024).
- `git 'push' origin` → BLOCKED: Phase 1b POSIX tokenization; `'push'` → token `push`; Phase 4
  match (EC-025).
- `g'i't commit` → BLOCKED: Phase 1b POSIX token concatenation; `g'i't` → token `git`; Phase 2
  basename `git`; Phase 4 match (EC-026).

**Out-of-scope forms (NOT blocked by design — BC-1.18.002 Threat Model & Scope; defense-in-depth):**
- `$(git commit -m "x")` → NOT blocked: outer command is a command substitution, not a
  statically-literal `git` invocation; undecidable for static analysis; caught by other controls
  (EC-027).
- `echo x | xargs git commit` → NOT blocked: Phase 1 splits on `|`; segment 2 executable is
  `xargs` (basename `xargs` ≠ `git`); `git commit` is a string argument to `xargs`, not the
  top-level executable; returns `false` (EC-028).
- `eval "git push origin main"` → NOT blocked: Phase 2 executable is `eval` (basename `eval` ≠
  `git`); `git push` is a string argument subject to dynamic evaluation; undecidable for static
  analysis; caught by other controls (EC-029).

**AC-010** (traces to BC-1.18.002 postcondition 4 — absent marker: both arms pass)

When `.factory/unvalidated-mutation.marker` is absent, both Arm 1 (`^Agent$`) and Arm 2
(`^Bash$` git commit/push) return `exit_code = 0` (allow).

**AC-011** (traces to BC-1.18.002 precondition 1 + invariant 2 + postcondition 5 + postcondition 6 — gate registers on_error="block_if_marker" (fail-closed-but-recoverable on crash); two-axis model; ADR-048 §Decision 1; D-1135 reversed)

Both `validate-unvalidated-mutation-marker` hook entries in `hooks-registry.toml` MUST have
`on_error = "block_if_marker"` (NOT `"continue"` or `"block"`) AND `failure_policy = "fail-open"`.
`failure_policy = "fail-open"` is UNCHANGED from v1.3 (two-axis model — see below).

**Crash behavior (`block_if_marker`):** When the WASM gate plugin itself cannot complete at the
dispatcher layer (fuel exhaustion, epoch timeout, crash), the dispatcher executes a NATIVE
(non-WASM) marker-presence-and-TTL check — this supersedes the v1.3 `on_error = "continue"`
unconditional-allow-on-crash posture (ADR-048 §Decision 1; D-1135 reversed):
- **BLOCK (exit_code=2):** iff a non-expired `.factory/unvalidated-mutation.marker` exists
  (`expires_at > now UTC`; BC-1.18.002 PC5; story EC-033). Block message includes marker
  fields (`plugin_name`, `artifact_path`, `cause`, `expires_at`) plus three recovery options:
  (a) `rm .factory/unvalidated-mutation.marker`; (b) Edit/Write re-validate the failing
  artifact; (c) wait for TTL expiry (86400s).
- **ALLOW (exit_code=0):** if the marker is absent OR TTL-expired (`expires_at ≤ now UTC`) OR
  the crash-handler's own file-read fails (ENOENT → treat as absent; BC-1.18.002 PC6;
  story EC-034). No unconditional self-lock is possible: a crash with no valid quarantine
  signal cannot block indefinitely.

The self-lock hazard (INV2) is avoided by the no-valid-quarantine-signal → Allow branch, NOT
by unconditional allow. Three independent recoverability guarantees (INV6) ensure the system
is never in an irrecoverable quarantine state — see AC-020.

**Two-axis model (ADR-039; ADR-047 §Decision 9; ADR-048 §Decision 1):** Two orthogonal
failure-mode axes govern the gate and MUST NOT be conflated:
- **(i) `block_if_marker` crash policy** (`on_error = "block_if_marker"`): when the WASM plugin
  itself fails at the dispatcher layer — crash, fuel exhaustion, epoch timeout — the dispatcher
  executes the native marker-presence-and-TTL check (Block iff non-expired marker; Allow
  otherwise). This supersedes the v1.3 unconditional-allow-on-crash (`on_error = "continue"`)
  posture. `failure_policy = "fail-open"` governs a separate axis (the INDETERMINATE outcome
  of the gate plugin itself — see BC-1.18.004) and is orthogonal to `on_error`.
- **(ii) Filter-internal fail-SAFE posture** (Phase 3 of `is_git_commit_or_push`): when the
  filter encounters an UNRECOGNIZED git global option, it returns BLOCK (subcommand position is
  uncertain; under-blocking is the dangerous failure mode). This is a conservative within-filter
  design choice, orthogonal to axis (i): the filter's internal posture has no bearing on what the
  dispatcher does when the WASM plugin itself crashes.

### BC-1.18.002 Invariant 6 — Ungated-Escape Invariant

**AC-020** (traces to BC-1.18.002 invariant 6 — ungated-escape invariant; rm-marker, Edit/Write
re-validation, and TTL auto-expiry are never gated even while quarantined; ADR-048 §Decision 3;
VP-107)

The three recovery operations MUST NEVER be gated by this gate plugin's PreToolUse registrations
while the system is in a quarantined (marker-active) state:

- **(i) `rm .factory/unvalidated-mutation.marker` (Bash dispatch):** `is_git_commit_or_push`
  returns `false` for this command (`basename("rm") = "rm" ≠ "git"`); Arm 2 does NOT gate this
  dispatch. Arm 1 fires only on `^Agent$`; a Bash `rm` dispatch is not `^Agent$`. This escape is
  ungated by construction. The `block_if_marker` crash policy (AC-011) does not affect it because
  a Bash `rm` dispatch does not match Arm 1's `^Agent$` pattern.
- **(ii) Edit/Write to the artifact (re-validation dispatch):** Edit and Write tool dispatches do
  not match `^Agent$` (Arm 1) or the `is_git_commit_or_push` filter (Arm 2); they are ungated
  per BC-1.18.002 PC3/AC-009. Neither arm applies `block_if_marker` to these tools. Confirmed
  ungated by construction.
- **(iii) TTL auto-expiry (no dispatch required):** After `UNVALIDATED_MUTATION_MARKER_TTL_SECONDS`
  (86400s) have elapsed since the `expires_at` timestamp, any subsequent gate evaluation on the
  normal path finds `expires_at ≤ now (UTC)` → Allow + auto-delete (BC-1.18.003 PC4; AC-021).
  Any crash-path evaluation also finds marker expired → Allow (AC-011 PC6). No operator dispatch
  is required — passive self-healing.

VP-107 verifies all three escape paths remain ungated.

### BC-1.18.003 — Marker Clear Protocol

**AC-012** (traces to BC-1.18.003 postcondition 1 + invariant 2 — re-validation clear; scoped to named plugin AND artifact path; PostToolUse hook only)

When the plugin whose `plugin_name` matches the marker's `plugin_name` field subsequently
produces `DispatchOutcome::Pass` (i.e., exit_code = 0 AND `host_output_too_large_seen = false`)
**in a PostToolUse hook invocation**, the dispatcher calls `delete_marker_if_pass()` which deletes
`.factory/unvalidated-mutation.marker`. The clear is artifact-scoped: BOTH conditions must hold —
(a) the dispatched plugin's `plugin_name` equals the marker's `plugin_name` field, AND (b) the
current dispatch's artifact path matches the marker's `artifact_path` field (exact string equality
of the normalized absolute path — both resolved to absolute form before comparison). **Empty-path
fallback (BC-1.18.003 EC-009):** if the marker's `artifact_path` is empty (the failing plugin was
a non-artifact-scoped validator — e.g., a wave-gate or path-staging validator — with no single
associated file), the artifact condition is vacuously satisfied and plugin_name equality alone
suffices for clearance. If the same plugin produces PASS on a DIFFERENT non-empty artifact, the
marker is NOT cleared — artifact A's quarantine persists until A is successfully re-validated by
the named plugin (story EC-031; BC-1.18.003 EC-008). A PreToolUse PASS result from the named
plugin MUST NOT clear the marker. Other plugins' PASS results do not clear the marker regardless
of hook type.

**AC-013** (traces to BC-1.18.003 postcondition 2 — idempotent delete)

`delete_marker_if_pass()` is idempotent: if `.factory/unvalidated-mutation.marker` is absent at
delete time, the function returns `Ok(())` (no-op). `io::ErrorKind::NotFound` is silently
swallowed. All other `io::Error` variants are propagated as `Err`.

**AC-014** (traces to BC-1.18.002 invariant 3 + BC-1.18.003 postcondition 3 + BC-1.18.003 invariant 3 — operator rm escape hatch; both arms unblocked simultaneously)

`rm .factory/unvalidated-mutation.marker` is the fully supported operator escape hatch. No
special command is required. After the rm, both Arm 1 and Arm 2 unblock simultaneously (both
check for the marker's presence at runtime; absent marker → pass).

### BC-1.18.003 Postcondition 4 — TTL-Expiry Clear Path

**AC-021** (traces to BC-1.18.003 postcondition 4 + invariant 5 — TTL-expiry clear path;
marker-level deadman; ADR-048 §Decision 2)

When the `validate-unvalidated-mutation-marker` gate plugin reads `.factory/unvalidated-mutation.marker`
during normal (non-crash) gate evaluation and finds `expires_at ≤ now (UTC)` (TTL elapsed), the
gate MUST:
1. Return `exit_code = 0` (Allow the dispatch).
2. Auto-delete the marker file (idempotent; `io::ErrorKind::NotFound` swallowed; other IO errors
   logged but do NOT panic — auto-delete failure must not block the Allow return).

`UNVALIDATED_MUTATION_MARKER_TTL_SECONDS = 86_400` (24-hour deadman constant defined in
`crates/factory-dispatcher/src/indeterminate_marker.rs`). `expires_at` is the RFC 3339 UTC
timestamp written by `write_indeterminate_marker` at marker creation as `timestamp + 86400s`
(BC-1.18.001 PC4 v1.1; ADR-048 §Decision 2). Both Arm 1 (Agent) and Arm 2 (git commit/push)
are immediately unblocked by TTL expiry — same as marker absence.

**Backward compatibility — legacy markers (missing `expires_at`):** Markers written before
ADR-048 implementation lack the `expires_at` field. The gate plugin MUST treat a missing
`expires_at` field as NON-EXPIRED (conservative; no silent auto-clear of old markers). Such
markers remain in effect until explicitly cleared via `rm` (PC3/AC-014) or successful
re-validation (PC1/AC-012).

**TTL is marker-level (INV5):** TTL expiry operates solely on the `expires_at` timestamp; it
does NOT check `plugin_name` or `artifact_path`. This is orthogonal to the artifact-scoped
re-validation clear (PC1/AC-012), which requires (a) plugin_name match AND (b) artifact_path
match. Story EC-035 covers this path.

### BC-1.18.004 — Fail-Open Advisory-Only Backward Compatibility

**AC-015** (traces to BC-1.18.004 postcondition 1 + postcondition 2 + postcondition 3 — fail-open
advisory only; no marker; no gate)

For any plugin with `failure_policy` absent (default) or `failure_policy = "fail-open"`, an
INDETERMINATE outcome produces: (a) an advisory `plugin.indeterminate` event emitted to FileSink;
(b) NO `.factory/unvalidated-mutation.marker` written; (c) NO gate triggered. The predicate
`should_write_marker(Indeterminate, FailOpen) == false` is the load-bearing implementation gate.
`FailurePolicy::default() == FailOpen` (S-21.10 canonical; ADR-039 §Decision 1).

**AC-016** (traces to BC-1.18.004 postcondition 4 — Cohort A exactly three validators;
consistent with ADR-047 §D8a v1.3)

`plugins/vsdd-factory/hooks-registry.toml` MUST have `failure_policy = "fail-closed"` for
exactly these three Cohort A validators; no other validator receives `fail-closed` in this story.
The three validators are split into two operational sub-groups:

**Cohort A-immediate (EFFECTIVE-NOW — 1 validator):**
- `validate-factory-path-staging`: set unconditionally. PreToolUse `^Bash$`; `on_error =
  "continue"`; bounded input; calibration confirmed; no S-21.24 dependency. No registry comment
  required. This is the ONE validator that is fully, unconditionally fail-closed at S-25.01 merge.

**Cohort A-deferred (SET-BUT-LATENT — 2 validators):**
- `validate-pr-merge-prerequisites`: field SET with registry comment:
  `# ACTIVATION CONDITIONAL on S-21.24 calibration per ADR-047 §D8a Cohort A-deferred`
- `validate-wave-gate-prerequisite`: field SET with the same registry comment.

Both deferred validators are PreToolUse `^Agent$` with `on_error = "block"`. Setting the
`failure_policy = "fail-closed"` field in this story is safe because the field is INERT
(S-21.10 is schema-only; ADR-039 Phase 4 enforcement not wired until S-21.24). Their
enforcement activates when S-21.24 wires the executor — at which point S-21.24's own
dependency chain (S-21.19 → S-21.21 calibration values → S-21.23 break-glass code) has already
resolved the self-lock risk.

**Layer-1 effective fail-closed count at S-25.01 merge: 1** (`validate-factory-path-staging`).
The "3-validator Cohort A" reflects the complete membership; the immediate enforcement is 1.

**AC-017** (traces to BC-1.18.004 postcondition 5 — DO-NOT-DELETE backward-compat guard test)

The implementation MUST include the test
`test_BC_1_18_004_fail_open_default_preserves_advisory_behavior` in the executor.rs test module
(or equivalently `test_BC_1_18_004_fail_open_indeterminate_writes_no_marker` with a code comment
cross-referencing the canonical name per BC-1.18.004 PC5 allowance). This test MUST NOT be
deleted per ADR-047 §Decision 7. It is the canonical regression guard for ~76 existing fail-open
plugins.

### BC-1.18.001 Invariant 5 — OutputTooLarge Flag Is Dispatcher-Internal StoreData

**AC-018** (traces to BC-1.18.001 invariant 5 — host_output_too_large_seen is dispatcher-internal StoreData; no ABI exposure)

`StoreData::host_output_too_large_seen` MUST reside in `crates/factory-dispatcher/src/invoke.rs`
exclusively, as dispatcher-internal state. It MUST NOT be exposed in `crates/hook-sdk/` or any
public hook-sdk ABI type. Plugin WASM binaries do not read or write this flag directly — they
observe the `OutputTooLarge (-3)` return code from host functions and may return a non-zero
`exit_code` in response, but the flag is invisible to the plugin. No HOST_ABI_VERSION bump is
required for this story's changes. If any PR review reveals that `crates/hook-sdk` has gained a
dependency on this flag, the PR MUST be blocked and the dependency removed.

### BC-1.18.002 Invariant 1 — Single WASM Binary for Both Gate Arms

**AC-019** (traces to BC-1.18.002 invariant 1 — Arm 1 and Arm 2 use the same WASM binary)

The TWO `[[hook]]` entries for the gate plugin in `hooks-registry.toml` — `validate-unvalidated-mutation-marker`
(`tool = "^Agent$"`) and `validate-unvalidated-mutation-marker-git` (`tool = "^Bash$"`) — MUST
both reference the IDENTICAL compiled binary via `plugin = "hook-plugins/validate-unvalidated-mutation-marker.wasm"`.
There is exactly ONE `.wasm` file on disk; the dispatch routing between Arm 1 (Agent check) and
Arm 2 (Bash command-content filter) is implemented by plugin-internal logic reading the tool event
payload (`command` parameter for Bash dispatches). Confirming this invariant: the `crates/hook-plugins/validate-unvalidated-mutation-marker/`
crate produces a single `validate-unvalidated-mutation-marker.wasm` binary from `T-4`; `T-5`
registers it under two names pointing to the same `plugin` path.

## Behavioral Contracts

| BC-ID | Version | Title | Role in This Story |
|-------|---------|-------|-------------------|
| BC-1.18.001 | v1.1 | When a fail-closed Validator Cannot Complete, Dispatcher Classifies Outcome as INDETERMINATE, Emits `plugin.indeterminate` Event, and Writes Unvalidated-Mutation Marker | Primary contract: INDETERMINATE classification, cause routing, atomic marker write (6 required TOML fields including `expires_at = timestamp + 86400s`; ADR-048 §Decision 2); v1.1 adds `expires_at` as 6th required field |
| BC-1.18.002 | v1.5 | Next State-Advancing Dispatch Is Blocked While Unvalidated-Mutation Marker Exists (Agent Arm and git commit/push Arm) | Gate plugin behavioral contract: block rules, Phase 1/1b/2/3/4 command filter (compound splitting/POSIX quoting/basename/arg-taking set/fail-safe), `block_if_marker` crash policy (on_error="block_if_marker"; BLOCK iff non-expired marker on crash; ALLOW if absent/expired/crash-handler-IO-fail; ADR-048 §Decision 1; D-1135 reversed), two-axis model, INV6 ungated-escape (rm/Edit-Write/TTL), threat model & scope; v1.5 supersedes v1.3 on_error="continue"; adds PC5 (crash+non-expired→Block), PC6 (crash+absent/expired→Allow), INV6, BC EC-031/EC-032 crash cases |
| BC-1.18.003 | v1.2 | Successful Re-Validation Clears the Unvalidated-Mutation Marker (Idempotent; Operator Escape Hatch Supported) | Marker-clear protocol: delete_marker_if_pass (artifact-scoped: same plugin AND same non-empty artifact path, OR empty-artifact_path→name-only fallback; ADR-047 §D5), NotFound→no-op, both arms unblock; v1.2 adds TTL-expiry PC4 (gate reads expired `expires_at` on normal path → Allow + auto-delete; 86400s deadman; legacy markers without `expires_at` treated as non-expired; ADR-048 §Decision 2) and INV5 (TTL is marker-level, not artifact-scoped) |
| BC-1.18.004 | v1.1 | `failure_policy = fail-open` INDETERMINATE Is Advisory-Only — No Marker Written, No Gate Triggered (Backward-Compatibility Anchor) | Backward-compat anchor: ~76 existing fail-open plugins unchanged; DO-NOT-DELETE guard test |
| BC-3.08.001 | v1.29 | dispatcher async-semantics event types are catalogued and emitted via FileSink — `plugin.async_block_discarded`, `dispatcher.schema_mismatch`, `dispatcher.registry_invalid`, `plugin.timeout` (async path), `plugin.abandoned`, `plugin.completed` (async path), `plugin.fuel_headroom_warning`, `plugin.indeterminate` | Wire-format authority: mandatory field list, cause enum values, artifact_path semantics |

## Architecture Mapping

| Component | File/Module | Pure/Effectful |
|-----------|------------|---------------|
| `classify_outcome` pure function | `crates/factory-dispatcher/src/executor.rs` | Pure-core (inputs → DispatchOutcome; no I/O) |
| INDETERMINATE outcome integration in invocation loop | `crates/factory-dispatcher/src/executor.rs` | Effectful (calls write_indeterminate_marker, emits event) |
| `StoreData::host_output_too_large_seen` flag + per-invocation reset | `crates/factory-dispatcher/src/invoke.rs` | Effectful (mutable Store state; reset before WASM call) |
| `write_indeterminate_marker` (atomic temp+rename) | `crates/factory-dispatcher/src/indeterminate_marker.rs` | Effectful (filesystem write) |
| `delete_marker_if_pass` (idempotent rm) | `crates/factory-dispatcher/src/indeterminate_marker.rs` | Effectful (filesystem delete) |
| `should_write_marker` predicate | `crates/factory-dispatcher/src/indeterminate_marker.rs` | Pure-core (DispatchOutcome × FailurePolicy → bool) |
| `guard_logic::evaluate_gate` | `crates/hook-plugins/validate-unvalidated-mutation-marker/src/lib.rs` | Effectful (filesystem read: marker file presence check) |
| `guard_logic::is_git_commit_or_push` | `crates/hook-plugins/validate-unvalidated-mutation-marker/src/lib.rs` | Pure-core (command string → bool; Phase 1/1b/2/3/4 algorithm v1.3: compound splitting, POSIX quote-aware tokenization via `shell-words`, basename identification, arg-taking global-option skip with complete recognized set, fail-safe on unrecognized, exact subcommand match) |
| Arm 1 WASM entry point (`^Agent$`) | `crates/hook-plugins/validate-unvalidated-mutation-marker/src/lib.rs` | Effectful (reads tool params, reads marker file) |
| Arm 2 WASM entry point (`^Bash$` git filter) | `crates/hook-plugins/validate-unvalidated-mutation-marker/src/lib.rs` | Effectful (reads tool params, reads marker file) |
| hooks-registry.toml Cohort A + gate entries | `plugins/vsdd-factory/hooks-registry.toml` | Configuration |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| `classify_outcome(PluginResult, FailurePolicy, bool) -> DispatchOutcome` | Pure-core | Takes: the plugin result variant, the failure_policy from RegistryEntry, and the host_output_too_large_seen flag value (captured BEFORE reset). Returns an enum variant. No I/O. VP-102 proof harness covers this function. |
| `should_write_marker(DispatchOutcome, FailurePolicy) -> bool` | Pure-core | Returns true iff outcome=Indeterminate AND policy=FailClosed. VP-106 proof harness covers this predicate. |
| `is_git_commit_or_push(command: &str) -> bool` | Pure-core | Phase 1/1b/2/3/4 algorithm (BC-1.18.002 PC2 v1.3): Phase 1 compound splitting on `&&`/`||`/`;`/`|`/`&`/newline; Phase 1b POSIX quote-aware tokenization via `shell-words` crate (e.g., `git "commit"` → `["git", "commit"]`; `g'i't commit` → `["git", "commit"]`); Phase 2 basename identification with env-var-prefix and `env`-token stripping; Phase 3 global-option skip using complete seven-option arg-taking set (`-C`, `-c`, `--namespace`, `--git-dir`, `--work-tree`, `--super-prefix`, `--config-env`) + no-arg set + fail-safe on unrecognized; Phase 4 exact subcommand match (`commit`/`push` only). VP-105 unit-test cases cover this function. |
| `write_indeterminate_marker(fields: &MarkerFields, marker_path: &Path) -> io::Result<()>` | Effectful (write) | Atomic filesystem write (temp file in same directory + rename). No reads of external state. |
| `delete_marker_if_pass(marker_path: &Path) -> io::Result<()>` | Effectful (delete) | Filesystem delete; NotFound → Ok(()). |
| `evaluate_gate(marker_path: &Path) -> GateDecision` | Effectful (read) | Reads marker file from filesystem; parses TOML to extract fields for block message. |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | Second INDETERMINATE event for same plugin while marker already exists | Overwrite marker (last-writer-wins per BC-1.18.001 invariant 3 single-marker policy) |
| EC-002 | INDETERMINATE occurs on PreToolUse (not PostToolUse) for fail-closed plugin | No marker written (PostToolUse-only per BC-1.18.001 invariant 4); advisory event only |
| EC-003 | Gate plugin (validate-unvalidated-mutation-marker) itself fuel-exhausts | Gate dispatcher applies block_if_marker native check (AC-011; ADR-048 §Decision 1): if marker ABSENT or TTL-expired → dispatch proceeds (Allow, EC-034); if non-expired marker EXISTS → dispatch BLOCKED (EC-033). INDETERMINATE on the gate does NOT write a nested marker (gate is failure_policy="fail-open"; AC-015 / BC-1.18.004). The block_if_marker design prevents unconditional self-lock (INV2) via the no-valid-signal → Allow branch (EC-034). |
| EC-004 | Operator runs `rm .factory/unvalidated-mutation.marker`; next Agent dispatch fires | AC-010 path: marker absent → both arms pass; dispatch proceeds normally |
| EC-005 | A fail-open plugin fuel-exhausts while marker exists from a fail-closed plugin | Fail-open path: advisory event only; marker NOT updated (fail-open skips write_indeterminate_marker entirely) |
| EC-006 | `git commit --amend` command while marker exists | AC-008 path: Phase 4 exact-subcommand match on `commit`; BLOCKED (BC-1.18.002 EC-006). |
| EC-007 | `git push --force-with-lease` while marker exists | AC-008 path: Phase 4 exact-subcommand match on `push`; BLOCKED (BC-1.18.002 EC-007). |
| EC-008 | Marker file write fails (e.g., disk full, path not writable) — BC-1.18.001 dispatcher write-fail path. (Note: BC-1.18.002 EC-008 v1.4 rescoped to readable-but-malformed marker → Block at gate; that is a gate-read AC-007 scenario distinct from this write-fail path. See EC-030 for the gate-read unreadable → Allow path.) | Propagate io::Error; log error; treat as INDETERMINATE with marker unwritten (conservative path — still emit plugin.indeterminate event) |
| EC-009 | validate-factory-path-staging (PreToolUse `^Bash$`, Cohort A-immediate, fail-closed) fuel-exhausts | PreToolUse INDETERMINATE path: no marker written (PostToolUse-only per BC-1.18.001 invariant 4); advisory `plugin.indeterminate` event only. ADR-039 Phase 4 enforcement (current-dispatch blocking) activates at S-21.24; `on_error = "continue"` prevents self-lock. |
| EC-010 | `git --git-dir .git commit -m "state"` while marker exists | AC-008/AC-009 path: Phase 3 `--git-dir` is in recognized arg-taking set; `.git` consumed as its separate-token argument; Phase 4 subcommand is `commit`; BLOCKED (BC-1.18.002 EC-013). |
| EC-011 | `git --work-tree /tmp push` while marker exists | AC-008/AC-009 path: Phase 3 `--work-tree` is in recognized arg-taking set; `/tmp` consumed as its argument; Phase 4 subcommand is `push`; BLOCKED (BC-1.18.002 EC-014). |
| EC-012 | `git status && git commit -m "x"` while marker exists | AC-008/AC-009 path: Phase 1 splits on `&&`; segment 1 subcommand `status` (not gated); segment 2 subcommand `commit` (gated); any-segment `true` → function returns `true`; BLOCKED (BC-1.18.002 EC-015). |
| EC-013 | `git diff ; git push` while marker exists | AC-008/AC-009 path: Phase 1 splits on `;`; segment 1 subcommand `diff` (not gated); segment 2 subcommand `push` (gated); BLOCKED (BC-1.18.002 EC-016). |
| EC-014 | `/usr/bin/git commit -m "init"` while marker exists | AC-008/AC-009 path: Phase 2 `basename("/usr/bin/git") = "git"`; Phase 4 subcommand `commit`; BLOCKED (BC-1.18.002 EC-017). |
| EC-015 | `./git push origin main` while marker exists | AC-008/AC-009 path: Phase 2 `basename("./git") = "git"`; Phase 4 subcommand `push`; BLOCKED (BC-1.18.002 EC-018). |
| EC-016 | `git status && git log --oneline` while marker exists | AC-009 path: Phase 1 splits on `&&`; segment 1 subcommand `status`; segment 2 subcommand `log`; no segment resolves to `commit` or `push`; NOT blocked (BC-1.18.002 EC-019). |
| EC-017 | `cat gitfile` while marker exists | AC-009 path: Phase 2 `basename("cat") = "cat" ≠ "git"`; returns `false`; NOT blocked (BC-1.18.002 EC-020). |
| EC-018 | `env GIT_DIR=.git git commit -m "msg"` while marker exists | AC-008/AC-009 path: Phase 2 strips literal `env` token and `GIT_DIR=.git` env-var assignment; executable is `git`; Phase 4 subcommand `commit`; BLOCKED (BC-1.18.002 EC-021). |
| EC-019 | `git --unknown-flag commit` while marker exists | AC-008/AC-009 path: Phase 3 `--unknown-flag` is not in recognized arg-taking or no-arg sets and contains no `=`; fail-safe posture → return `true` (block); subcommand position cannot be determined from the closed recognized set (BC-1.18.002 EC-022). |
| EC-020 | `git --config-env FOO=BAR commit` while marker exists | AC-008/AC-009 path: Phase 3 `--config-env` is in recognized arg-taking set; `FOO=BAR` consumed as its argument; Phase 4 subcommand is `commit`; BLOCKED (BC-1.18.002 EC-023). |
| EC-021 | `git "commit"` while marker exists | AC-008/AC-009 path: Phase 1b POSIX tokenization; double-quoted `"commit"` → token `commit` after quote removal; Phase 4 exact match; BLOCKED (BC-1.18.002 EC-024). |
| EC-022 | `git 'push' origin` while marker exists | AC-008/AC-009 path: Phase 1b POSIX tokenization; single-quoted `'push'` → token `push` after quote removal; Phase 4 exact match; BLOCKED (BC-1.18.002 EC-025). |
| EC-023 | `g'i't commit` while marker exists | AC-008/AC-009 path: Phase 1b POSIX token concatenation; unquoted `g` + single-quoted `i` + unquoted `t` → token `git`; Phase 2 basename `git`; Phase 4 subcommand `commit`; BLOCKED (BC-1.18.002 EC-026). |
| EC-024 | `$(git commit -m "x")` while marker exists | AC-009 path: outer command is a command substitution; `git` is not the statically-literal top-level token; OUT OF SCOPE by design (see BC-1.18.002 Threat Model & Scope); NOT blocked; allowed under fail-open posture; durable marker and other controls provide defense-in-depth (BC-1.18.002 EC-027). |
| EC-025 | `echo x \| xargs git commit` while marker exists | AC-009 path: Phase 1 splits on `\|`; segment 1 `echo x` — executable `echo` ≠ `git`, false; segment 2 `xargs git commit` — executable `xargs` ≠ `git`, false; OUT OF SCOPE by design — `xargs` indirection; NOT blocked (BC-1.18.002 EC-028). |
| EC-026 | `eval "git push origin main"` while marker exists | AC-009 path: Phase 2 executable is `eval`; basename `eval` ≠ `git`; `git push` is a string argument subject to dynamic evaluation; OUT OF SCOPE by design; NOT blocked; allowed under fail-open posture (BC-1.18.002 EC-029). |
| EC-030 | Gate plugin tries to read `.factory/unvalidated-mutation.marker` but encounters EACCES or other IO error (unreadable marker; BC-1.18.002 EC-030 v1.4) | Gate allows the dispatch (fail-open, exit_code=0). An unreadable marker MUST NOT block — INV2 self-lock avoidance: a gate that cannot read the marker cannot determine quarantine state and must not prevent forward progress. Distinct from EC-008 (write-fail) and from BC-1.18.002 EC-008 (readable-but-malformed → Block). Covered by VP-105 unit-test property. |
| EC-031 | Same plugin as named in marker, DIFFERENT non-empty artifact path produces PostToolUse Pass while marker exists (BC-1.18.003 EC-008) | Marker NOT cleared. Artifact A's quarantine persists — the named plugin validated artifact B successfully but artifact A was never re-validated. Gate (both Arm 1 and Arm 2) remains active. Clear requires the named plugin to subsequently produce PASS on artifact A (exact normalized absolute path equality), or operator manual deletion (`rm .factory/unvalidated-mutation.marker`). `delete_marker_if_pass` returns Ok(()) with no deletion when artifact_path mismatch. |
| EC-032 | Same plugin as named in marker, empty marker `artifact_path` field, PostToolUse Pass on any artifact (BC-1.18.003 EC-009) | Marker cleared (empty-path fallback). When the marker's `artifact_path` is empty, the artifact condition is vacuously satisfied; plugin_name equality alone suffices for clearance. Both Arm 1 (Agent) and Arm 2 (git commit/push) immediately unblocked. Covers non-artifact-scoped validators (e.g., wave-gate, path-staging validators) whose marker carries no artifact context because they validate global pipeline state, not a specific file. |
| EC-033 | Gate plugin crashes/fuel-exhausts (Arm 1 or Arm 2) while a non-expired `.factory/unvalidated-mutation.marker` exists (`expires_at` in future) (BC-1.18.002 PC5, BC-1.18.002 EC-031) | Block (exit_code=2). Dispatcher native `block_if_marker` check: non-expired marker present → BLOCK even though WASM plugin crashed (ADR-048 §Decision 1). Block message includes marker fields (`plugin_name`, `artifact_path`, `cause`, `expires_at`) plus three recovery options: (a) `rm .factory/unvalidated-mutation.marker`; (b) Edit/Write re-validate the failing artifact (AC-020 INV6-ii); (c) wait for TTL expiry after 86400s (AC-020 INV6-iii). |
| EC-034 | Gate plugin crashes/fuel-exhausts (Arm 1 or Arm 2) while marker is absent OR while an existing marker's `expires_at` has elapsed (UTC) (BC-1.18.002 PC6, BC-1.18.002 EC-009/EC-032) | Allow (exit_code=0). Dispatcher native `block_if_marker` check: no valid quarantine signal → ALLOW. No unconditional self-lock is possible: a crash with no valid quarantine signal cannot block indefinitely. Crash-handler I/O failures on the marker file (ENOENT, EACCES) are also treated as absent → Allow (conservative). |
| EC-035 | Gate plugin reads `.factory/unvalidated-mutation.marker` on normal execution path and finds `expires_at ≤ now (UTC)` (TTL elapsed; BC-1.18.003 PC4) | Allow (exit_code=0) + auto-delete the marker (idempotent; NotFound swallowed; other IO errors logged, no panic). Both Arm 1 (Agent) and Arm 2 (git commit/push) immediately unblocked. No `plugin_name` or `artifact_path` check required (INV5 — TTL is marker-level, not artifact-scoped). Legacy markers lacking `expires_at` field → treated as non-expired (conservative; NOT auto-deleted by this path). |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| Story spec (S-25.01) | 5,500 |
| ADR-047 full text (9 decisions + rationale + impl notes, v1.2) | 9,000 |
| ADR-048 (fail-closed-but-recoverable gate; block_if_marker crash policy; marker TTL deadman 86400s; ungated-escape invariant; §D1/D2/D3) | 3,500 |
| ADR-039 relevant sections (§D1 failure_policy semantics, §D3 calibration prerequisites) | 3,500 |
| BC-1.18.001 v1.1 (INDETERMINATE outcome, marker write, Store flag, expires_at 6th TOML field; ADR-048 §Decision 2) | 3,400 |
| BC-1.18.002 v1.5 (next-advance gate, two arms, v1.3 Phase 1/1b/2/3/4 command filter, block_if_marker crash policy, PC5/PC6, INV6 ungated-escape, two-axis model, threat model & scope, 32+ ECs + extended canonical test vectors) | 4,800 |
| BC-1.18.003 v1.2 (marker-clear protocol, artifact-scoped clear, empty-path fallback, idempotent delete, TTL-expiry PC4, INV5 marker-level TTL, legacy-marker backward-compat) | 2,800 |
| BC-1.18.004 (fail-open backward-compat, DO-NOT-DELETE guard) | 1,600 |
| BC-3.08.001 §Event 8 (plugin.indeterminate wire format, mandatory fields) | 1,500 |
| VP-102 through VP-106 (proof harness skeletons, 5 VPs) | 6,000 |
| `crates/factory-dispatcher/src/executor.rs` (current) | 4,000 |
| `crates/factory-dispatcher/src/invoke.rs` (current, StoreData) | 3,500 |
| `crates/factory-dispatcher/src/registry.rs` (current, RegistryEntry) | 2,500 |
| `plugins/vsdd-factory/hooks-registry.toml` (Cohort A entries + surrounding context) | 3,000 |
| F1 delta analysis §9 (Red Gate tests), §10 (impact boundary), §11 (fail-closed assignments) | 3,500 |
| **Total** | **~60,000** |
| Agent context window | 200,000 (Sonnet 4.6) |
| **Budget usage** | **~30%** |

> Budget is at the 30% envelope boundary after ADR-048 addition. Implementer MUST load ONLY the
> sources listed above — no additional architecture files, no BC files beyond the 5 listed, no
> additional crate source files beyond executor.rs / invoke.rs / registry.rs. If context pressure
> is felt, drop ADR-039 background sections (keep §D1 + §D3 only) and drop ADR-047 rationale
> sections (keep §D1/D4→D5/D9 and the superseded-by-ADR-048 note).

## Tasks (MANDATORY)

**T-1: executor.rs — INDETERMINATE outcome classification**
- [ ] Add `DispatchOutcome::Indeterminate { cause: IndeterminateCause }` variant (or
  equivalent structured form) to the outcome enum in `executor.rs`
- [ ] Implement `classify_outcome(plugin_result: PluginResult, policy: FailurePolicy,
  output_too_large: bool) -> DispatchOutcome` pure function with:
  - Fuel trap: `trap.downcast_ref::<Trap>() == Some(&Trap::OutOfFuel)` → `Indeterminate { cause: Fuel }`
  - Epoch trap: `trap.downcast_ref::<Trap>() == Some(&Trap::Interrupt)` → `Indeterminate { cause: Epoch }`
  - OutputTooLarge: `output_too_large && exit_code == 0` → `Indeterminate { cause: OutputTooLarge }`
  - Wildcard Trap arm: `_ => { /* NOT Indeterminate — route to existing on_error handling */ }` (BC-1.18.001 invariant 2 — `Trap` is `#[non_exhaustive]`; future variants must not be silently bucketed as INDETERMINATE)
  - PASS: `exit_code == 0 && !output_too_large`
  - FAIL: `exit_code != 0`
- [ ] Wire `classify_outcome` into the executor invocation loop replacing the old outcome derivation
- [ ] Write RED Gate stub: `fn test_BC_1_18_001_fuel_exhaustion_yields_indeterminate_for_fail_closed_plugin()`
- [ ] Write RED Gate stub: `fn test_BC_1_18_001_epoch_timeout_yields_indeterminate_for_fail_closed_plugin()`
- [ ] Write RED Gate stub: `fn test_BC_1_18_001_output_too_large_then_ok_yields_indeterminate_for_fail_closed_plugin()`
- [ ] Write RED Gate stub: `fn test_BC_1_18_001_indeterminate_is_distinct_from_pass_and_fail()`
- [ ] Write RED Gate stub: `fn test_BC_1_18_001_indeterminate_writes_marker_to_factory_path()`
- [ ] Write RED Gate stub: `fn test_BC_1_18_001_marker_contains_required_fields()`
- [ ] Write RED Gate stub: `fn test_BC_1_18_004_fail_open_indeterminate_writes_no_marker()` (alias:
  `test_BC_1_18_004_fail_open_default_preserves_advisory_behavior` — cross-reference in comment)
- [ ] Write RED Gate stub: `fn test_backward_compat_pass_fail_on_error_semantics_unchanged()`
- [ ] Write RED Gate stub: `fn test_BC_1_18_001_unrecognized_trap_routes_to_on_error_not_indeterminate()` (AC-004 + BC-1.18.001 invariant 2 — wildcard arm routes to on_error, NOT INDETERMINATE)

**T-2: invoke.rs — OutputTooLarge Store flag + per-invocation reset + FileSink event emission**
- [ ] Add `host_output_too_large_seen: bool` field to `StoreData` struct
- [ ] Add `reset_output_too_large_flag(&mut store)` helper (or inline reset) that sets the flag
  to `false`; call it IMMEDIATELY BEFORE each `func.call(...)` invocation — NOT only at Store
  creation
- [ ] Set `store.data_mut().host_output_too_large_seen = true` in all host function wrappers that
  return `OutputTooLarge(-3)` to the WASM guest (read_file, read_prefix, and any future host
  function that can return −3)
- [ ] Wire `plugin.indeterminate` event emission to the OTel-aligned FileSink (`events-*.jsonl`)
  for EVERY INDETERMINATE outcome (both fail-closed and fail-open paths); event MUST include all
  BC-3.08.001 Event 8 mandatory fields: `type`, `trace_id`, `session_id`, `plugin_name`,
  `artifact_path` (empty string when no artifact context — never omit the field), `cause`,
  `failure_policy`, `timestamp` — this is the production implementation that makes
  `test_BC_1_18_001_emits_plugin_indeterminate_event_with_required_fields` pass (AC-006)
- [ ] Write RED Gate stub: `fn test_BC_1_18_001_emits_plugin_indeterminate_event_with_required_fields()`
  (traces to AC-006 + BC-3.08.001 Event 8; FileSink receives event with all 8 mandatory fields
  for both fail-closed and fail-open INDETERMINATE outcomes)
- [ ] Write RED Gate stub: `fn test_BC_1_18_001_output_too_large_then_ok_yields_indeterminate_for_fail_closed_plugin()`
  (NOTE: this stub is written in executor.rs test module per T-1 above; T-2 documents the
  production implementation in invoke.rs that makes it pass)
- [ ] Capture the `host_output_too_large_seen` value from StoreData AFTER `func.call()` completes
  (before the next per-invocation reset) and pass it to `classify_outcome`

**T-3: indeterminate_marker.rs (NEW FILE) — marker write/clear/predicate**
- [ ] Create `crates/factory-dispatcher/src/indeterminate_marker.rs`
- [ ] Implement `write_indeterminate_marker(fields: &MarkerFields, marker_path: &Path) -> io::Result<()>`:
  - Write to `<marker_path>.tmp` (same directory as target)
  - Serialize 6 TOML fields: timestamp (RFC 3339), plugin_name, artifact_path (empty string
    allowed, never omit), cause ("fuel" | "epoch" | "output-too-large"), trace_id,
    expires_at (RFC 3339 UTC = timestamp + UNVALIDATED_MUTATION_MARKER_TTL_SECONDS; define
    `UNVALIDATED_MUTATION_MARKER_TTL_SECONDS: u64 = 86_400` as a constant in this module;
    ADR-048 §Decision 2; enables gate TTL clear per BC-1.18.003 PC4 and dispatcher native
    block_if_marker TTL check per BC-1.18.002 PC5/PC6)
  - Atomic rename `.tmp` → final path
- [ ] Implement `delete_marker_if_pass(marker_path: &Path) -> io::Result<()>`:
  - `fs::remove_file(marker_path)` with `io::ErrorKind::NotFound` mapped to `Ok(())`
- [ ] Implement `should_write_marker(outcome: &DispatchOutcome, policy: FailurePolicy) -> bool`:
  - Returns `true` iff outcome is Indeterminate AND policy is FailClosed
- [ ] Write RED Gate stub: `fn test_BC_1_18_003_successful_revalidation_deletes_marker()`
- [ ] Write RED Gate stub: `fn test_BC_1_18_004_fail_open_indeterminate_writes_no_marker()` (if not
  already in executor.rs tests — consolidate per project test organization convention)
- [ ] Expose module: add `mod indeterminate_marker;` to `crates/factory-dispatcher/src/lib.rs`

**T-4: validate-unvalidated-mutation-marker (NEW WASM PLUGIN CRATE)**
- [ ] Create `crates/hook-plugins/validate-unvalidated-mutation-marker/Cargo.toml` as a WASM
  crate (target = `wasm32-unknown-unknown` or `wasm32-wasi`; match existing hook plugin crates)
- [ ] Create `crates/hook-plugins/validate-unvalidated-mutation-marker/src/lib.rs`:
  - `guard_logic::evaluate_gate(marker_path: &Path) -> GateDecision` — reads marker, parses TOML,
    extracts fields (plugin_name, artifact_path, cause)
  - `guard_logic::is_git_commit_or_push(command: &str) -> bool` — Phase 1/1b/2/3/4 algorithm (BC-1.18.002 PC2 v1.3): Phase 1 compound splitting (`&&`/`||`/`;`/`|`/`&`/newline), Phase 1b POSIX quote-aware tokenization via `shell-words` crate (`git "commit"` → `["git","commit"]`; `g'i't commit` → `["git","commit"]`; ad-hoc quote-stripping forbidden), Phase 2 basename identification (path-prefixed `/usr/bin/git`, `./git`; env-prefix stripped), Phase 3 global-option skip (seven arg-taking options: `-C`, `-c`, `--namespace`, `--git-dir`, `--work-tree`, `--super-prefix`, `--config-env`; recognized no-arg set; fail-safe on unrecognized options), Phase 4 exact subcommand match (`commit` or `push` only; `commit-graph` does NOT match)
  - Arm 1 entry point (fires on `^Agent$` PreToolUse): calls evaluate_gate; if marker present →
    return block message with 5 fields + recovery command + escape hatch
  - Arm 2 entry point (fires on `^Bash$` PreToolUse + git filter): calls `is_git_commit_or_push`;
    if match AND marker present → same block message format
  - Both entry points return `exit_code = 0` (allow) when marker absent
- [ ] Add crate to `crates/hook-plugins/Cargo.toml` workspace members (or top-level `Cargo.toml`)
- [ ] Write RED Gate stub: `fn test_BC_1_18_002_blocks_agent_dispatch_when_marker_exists()`
- [ ] Write RED Gate stub: `fn test_BC_1_18_002_passes_agent_dispatch_when_no_marker()`
- [ ] Write RED Gate stub: `fn test_BC_1_18_002_block_message_names_plugin_and_artifact()`
- [ ] Build WASM binary: `cargo build --target wasm32-unknown-unknown --release -p validate-unvalidated-mutation-marker`
- [ ] Copy WASM binary to `plugins/vsdd-factory/hook-plugins/validate-unvalidated-mutation-marker.wasm`
  (or wherever existing hook plugin WASMs live — confirm from hooks-registry.toml `plugin` field entries)

**T-5: hooks-registry.toml updates**
- [ ] Add Arm 1 entry (canonical key names per actual hooks-registry.toml schema: `event`, `tool`,
  `plugin`, `async`; NOT `trigger`/`tool_pattern`/`wasm_path`/`tier`):
  ```toml
  [[hooks]]
  name = "validate-unvalidated-mutation-marker"
  event = "PreToolUse"
  tool = "^Agent$"
  plugin = "hook-plugins/validate-unvalidated-mutation-marker.wasm"
  on_error = "block_if_marker"
  async = false
  failure_policy = "fail-open"
  ```
- [ ] Add Arm 2 entry (same WASM binary as Arm 1 per BC-1.18.002 invariant 1; `command_filter`
  is NOT a registry field — the Phase 1/1b/2/3/4 algorithm (BC-1.18.002 PC2 v1.3) is implemented
  INSIDE the WASM plugin by reading the `command` parameter from the Bash tool event payload):
  ```toml
  [[hooks]]
  name = "validate-unvalidated-mutation-marker-git"
  event = "PreToolUse"
  tool = "^Bash$"
  plugin = "hook-plugins/validate-unvalidated-mutation-marker.wasm"
  on_error = "block_if_marker"
  async = false
  failure_policy = "fail-open"
  ```
  (NOTE: Same WASM binary for both arms per BC-1.18.002 invariant 1 and AC-019; `on_error =
  "block_if_marker"` is load-bearing — NOT "continue" or "block" — per BC-1.18.002 PC1 v1.5
  and AC-011 (ADR-039; ADR-047 §Decision 9; ADR-048 §Decision 1): dispatcher-level
  block_if_marker crash policy: BLOCK iff non-expired marker exists on crash; ALLOW if
  absent/expired/crash-IO-fail; `failure_policy = "fail-open"` governs the orthogonal axis
  (gate's own INDETERMINATE class per BC-1.18.004); the Phase 1/1b/2/3/4 command filter
  (BC-1.18.002 PC2 v1.3) is implemented INSIDE the WASM plugin by reading the `command`
  parameter from the Bash tool event payload — it is NOT a registry field)
- [ ] Set `failure_policy = "fail-closed"` for `validate-factory-path-staging` (unconditional)
- [ ] Set `failure_policy = "fail-closed"` for `validate-pr-merge-prerequisites` and
  `validate-wave-gate-prerequisite` with registry comment (Cohort A-deferred per AC-016):
  `# ACTIVATION CONDITIONAL on S-21.24 calibration per ADR-047 §D8a Cohort A-deferred`

**T-6: Integration tests (bats) and Kani proof harnesses**
- [ ] Create `plugins/vsdd-factory/tests/validate-unvalidated-mutation-marker.bats` with helper
  functions `run_dispatcher_pretooluse_agent` and `run_dispatcher_pretooluse_bash`, and test cases
  VP-105-A through VP-105-G (7 bats tests as specified in VP-105 proof harness skeleton)
- [ ] Verify VP-102 proof harness skeleton in executor.rs test module covers the 5 unit test cases
  from VP-102 spec
- [ ] Run full suite: `cargo test --workspace --all-targets && cd plugins/vsdd-factory/tests && ./run-all.sh`
- [ ] Confirm full test suite stays green (backward-compat regression: all existing tests pass;
  new Red Gate tests FAIL before implementation; new tests PASS after implementation)

**T-7: CHANGELOG delivery (MANDATORY — before PR)**
- [ ] Add CHANGELOG entry under `[Unreleased]` > `Added`:
  `Dispatcher: INDETERMINATE outcome class for fail-closed plugins — fuel/epoch/OutputTooLarge cannot-complete events now write a durable \`.factory/unvalidated-mutation.marker\`; \`validate-unvalidated-mutation-marker\` gate blocks next Agent dispatch and \`git commit\`/\`git push\` while marker exists; successful re-validation clears marker; existing ~76 fail-open plugins unchanged (advisory event only); Cohort A validators assigned fail-closed (ADR-047 Layer 1; BC-1.18.001–004, BC-3.08.001 Event 8)`

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-21.10 (MERGED, PR #780, depends_on) | Introduced `FailurePolicy` enum (`FailOpen` / `FailClosed`) and `RegistryEntry.failure_policy` field into the registry parsing + executor dispatch path. ADR-039 §Decision 1 + §Decision 3. No behavioral change shipped — schema-only extension per ADR-039 Phase 1. | `FailurePolicy::default() == FailOpen` is canonical and load-bearing. Never change this default without a new ADR. The `failure_policy` field is read at dispatch time from the `RegistryEntry` — not from the WASM binary's own metadata. | S-21.10 is schema-only (Phase 1). The enforcement logic (reading the field and acting on it) is S-25.01's responsibility. Do NOT assume the executor already routes on failure_policy — confirm by reading executor.rs before writing. |
| N/A (first story in epic E-25 besides S-21.10 predecessor) | — | — | — |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| `classify_outcome` MUST use `Trap::OutOfFuel` downcast, NOT `get_fuel()` for fuel detection | BC-1.18.001 §Architecture Anchors; ADR-047 §D1 implementation note | AC-001; test_BC_1_18_001_fuel_exhaustion_yields_indeterminate_for_fail_closed_plugin covers this |
| `host_output_too_large_seen` is dispatcher-internal StoreData ONLY — NOT host-sdk ABI | BC-1.18.001 invariant 5; ADR-047 §D6; F1-delta-analysis §8 | AC-018; do NOT expose this field in hook-sdk; no HOST_ABI_VERSION bump required for this change. |
| Marker write is PostToolUse only | BC-1.18.001 invariant 4 | AC-005; architecture mapping table (PostToolUse constraint); EC-002 |
| Gate plugin (`validate-unvalidated-mutation-marker`) MUST be registered with `on_error = "block_if_marker"` AND `failure_policy = "fail-open"` — NOT `on_error = "continue"` or `on_error = "block"` (ADR-048 §Decision 1 supersedes v1.3 on_error="continue"; block_if_marker: BLOCK iff non-expired marker on crash; ALLOW if absent/expired/crash-IO-fail; two-axis model — failure_policy governs WASM INDETERMINATE class, on_error governs crash path; orthogonal per ADR-039; self-lock avoided by no-valid-signal→Allow branch; three ungated escapes per INV6/AC-020) | BC-1.18.002 precondition 1 + invariant 2 (v1.5); ADR-048 §Decision 1; ADR-047 §Decision 9; ADR-039 two-axis model | AC-011; T-5 hooks-registry entries; both gate arms |
| Same WASM binary for both gate arms — two hooks-registry entries, one binary | BC-1.18.002 invariant 1 | AC-019; T-4 and T-5; one compiled `.wasm` file serves both entries |
| The three Cohort A validators are the ONLY fail-closed assignments in S-25.01 | ADR-047 §D8a v1.3 — human-confirmed Cohort A (1 A-immediate + 2 A-deferred); BC-1.18.004 PC4 | AC-016; T-5 registry entries; no other plugin may receive fail-closed in this PR |
| `test_BC_1_18_004_fail_open_default_preserves_advisory_behavior` MUST NOT be deleted | ADR-047 §Decision 7; BC-1.18.004 PC5 | AC-017; PR review will verify test presence; name (or alias with cross-reference comment) required |
| Trap match wildcard arm MUST route to existing on_error handling — NOT produce INDETERMINATE | BC-1.18.001 invariant 2 (non-exhaustive Trap variants) | AC-004; implementer must use `_ => { /* route to on_error */ }` NOT `_ => Indeterminate` or `_ => unreachable!()` |
| No TD-VSDD-060 sibling-site violations: when changing executor outcome routing, grep ALL callsites that pattern-match on DispatchOutcome variants | TD-VSDD-060 sibling-site sweep; CLAUDE.md Self-Audit Checklist | Implementer must grep for all match arms on DispatchOutcome before committing |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| wasmtime | 46.0.2 | WASM runtime (fuel, epoch, Trap variants) — S-21.12 bump to 46.0.2 (PR #781). Do NOT downgrade. |
| serde | workspace version | Serialization for 5-field TOML marker write |
| toml (crate) | workspace version | TOML serialization of `.factory/unvalidated-mutation.marker` |
| tracing | workspace version | Structured logging for INDETERMINATE path (no `println!` in production code per CLAUDE.md) |
| shell-words | 1.1 | Phase 1b POSIX quote-aware tokenization in is_git_commit_or_push (WASM gate plugin); replaces split_whitespace to close the quoting class without a parser-differential |
| tempfile or std::fs + rename | std::fs (std only) | Atomic marker write: write to `.tmp` then `rename` — no extra crate needed if using `std::fs::rename` |

**Forbidden dependencies for `crates/factory-dispatcher`:**
- MUST NOT gain a dependency on `hook-sdk` for the `host_output_too_large_seen` flag —
  the flag is dispatcher-internal StoreData only (BC-1.18.001 invariant 5; AC-018). If this
  dependency appears, the build MUST fail (enforce via deny-check or review).
- MUST NOT depend on any crate that introduces a HOST_ABI_VERSION change for this story's
  changes (the StoreData extension is internal and does not change the ABI contract).

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| `crates/factory-dispatcher/src/executor.rs` | modify | Add `DispatchOutcome::Indeterminate`, `classify_outcome` pure fn, wire into invocation loop, 12 Red Gate test stubs (10 original + 2 added in audit: stubs 14 + 15) |
| `crates/factory-dispatcher/src/invoke.rs` | modify | Add `StoreData::host_output_too_large_seen: bool`, per-invocation reset before `func.call()`, set in OTL-returning host fn wrappers |
| `crates/factory-dispatcher/src/indeterminate_marker.rs` | create | `write_indeterminate_marker`, `delete_marker_if_pass`, `should_write_marker` |
| `crates/factory-dispatcher/src/lib.rs` | modify | Add `pub mod indeterminate_marker;` |
| `crates/hook-plugins/validate-unvalidated-mutation-marker/Cargo.toml` | create | New WASM hook plugin crate |
| `crates/hook-plugins/validate-unvalidated-mutation-marker/src/lib.rs` | create | `guard_logic::evaluate_gate`, `guard_logic::is_git_commit_or_push`, Arm 1 + Arm 2 entry points, 3 Red Gate test stubs |
| `crates/Cargo.toml` (or workspace root `Cargo.toml`) | modify | Add `validate-unvalidated-mutation-marker` to workspace members |
| `plugins/vsdd-factory/hook-plugins/validate-unvalidated-mutation-marker.wasm` | create (build artifact) | Compiled WASM binary from T-4; must be committed to the plugin hooks directory |
| `plugins/vsdd-factory/hooks-registry.toml` | modify | Add 2 gate entries (Arm 1 + Arm 2); set Cohort A fail-closed (3 entries); add activation comment for 2 conditional entries |
| `plugins/vsdd-factory/tests/validate-unvalidated-mutation-marker.bats` | create | 7 bats integration tests (VP-105-A through VP-105-G) |
| `CHANGELOG.md` | modify | T-7 mandatory CHANGELOG entry under [Unreleased] > Added |

## Red Gate Test Inventory (ALL MUST BE FAILING STUBS BEFORE IMPLEMENTATION)

### executor.rs test module (12 stubs)

```rust
#[test]
fn test_BC_1_18_001_fuel_exhaustion_yields_indeterminate_for_fail_closed_plugin() {
    todo!()
}

#[test]
fn test_BC_1_18_001_epoch_timeout_yields_indeterminate_for_fail_closed_plugin() {
    todo!()
}

#[test]
fn test_BC_1_18_001_output_too_large_then_ok_yields_indeterminate_for_fail_closed_plugin() {
    todo!()
}

#[test]
fn test_BC_1_18_001_indeterminate_is_distinct_from_pass_and_fail() {
    todo!()
}

#[test]
fn test_BC_1_18_001_indeterminate_writes_marker_to_factory_path() {
    todo!()
}

#[test]
fn test_BC_1_18_001_marker_contains_required_fields() {
    todo!()
}

#[test]
fn test_BC_1_18_003_successful_revalidation_deletes_marker() {
    todo!()
}

#[test]
fn test_BC_1_18_004_fail_open_indeterminate_writes_no_marker() {
    // Canonical backward-compat guard test. Cross-reference: BC-1.18.004 PC5 canonical name is
    // test_BC_1_18_004_fail_open_default_preserves_advisory_behavior. Both names acceptable per
    // BC-1.18.004 PC5 allowance — this is the canonical alias. DO NOT DELETE. ADR-047 §D7.
    todo!()
}

#[test]
fn test_BC_1_18_004_fail_open_default_preserves_advisory_behavior() {
    // Canonical backward-compat guard test per ADR-047 §Decision 7. DO NOT DELETE.
    // Cross-reference: test_BC_1_18_004_fail_open_indeterminate_writes_no_marker above.
    todo!()
}

#[test]
fn test_backward_compat_pass_fail_on_error_semantics_unchanged() {
    todo!()
}

#[test]
fn test_BC_1_18_001_unrecognized_trap_routes_to_on_error_not_indeterminate() {
    // Wildcard Trap arm: an unrecognized (future) Trap variant MUST NOT yield INDETERMINATE.
    // The wildcard `_ =>` arm must route to on_error handling, not classify as INDETERMINATE.
    // BC-1.18.001 invariant 2 (Trap is #[non_exhaustive]). Traces to AC-004 + BC-1.18.001 invariant 2.
    todo!()
}

#[test]
fn test_BC_1_18_001_emits_plugin_indeterminate_event_with_required_fields() {
    // FileSink receives a plugin.indeterminate event with all BC-3.08.001 Event 8 mandatory fields:
    // type, trace_id, session_id, plugin_name, artifact_path (empty string allowed, not omitted),
    // cause (fuel|epoch|output-too-large), failure_policy (fail-closed|fail-open), timestamp.
    // Applies to BOTH fail-closed AND fail-open INDETERMINATE outcomes (advisory path).
    // Traces to AC-006 + BC-3.08.001 Event 8.
    todo!()
}
```

### validate-unvalidated-mutation-marker/src/lib.rs test module (3 stubs)

```rust
#[test]
fn test_BC_1_18_002_blocks_agent_dispatch_when_marker_exists() {
    todo!()
}

#[test]
fn test_BC_1_18_002_passes_agent_dispatch_when_no_marker() {
    todo!()
}

#[test]
fn test_BC_1_18_002_block_message_names_plugin_and_artifact() {
    todo!()
}
```

**Red Gate density check (before Step 4 implementer dispatch):**
15 stubs total / estimated 13 non-trivial functions = density 1.15 ≥ 0.5 threshold. All 15 unique stubs MUST compile and MUST fail (panic with `todo!()`) before the implementer dispatch is authorized.

Stub summary:
- executor.rs section (12 stubs): stubs 1–10 original + stub 14 (unrecognized_trap_routes_to_on_error_not_indeterminate) + stub 15 (emits_plugin_indeterminate_event_with_required_fields) added in audit
- gate plugin section (3 stubs): test_BC_1_18_002_blocks_agent_dispatch_when_marker_exists, test_BC_1_18_002_passes_agent_dispatch_when_no_marker, test_BC_1_18_002_block_message_names_plugin_and_artifact
- Total unique stubs: 12 + 3 = 15

## Changelog

| Version | Date | Description |
|---------|------|-------------|
| 1.9 | 2026-08-31 | ADR-048 gate redesign sync (BC-1.18.002 v1.4→v1.5; BC-1.18.003 v1.1→v1.2; BC-1.18.001 v1.0→v1.1). (1) AC-011: on_error="continue" → on_error="block_if_marker" (ADR-048 §Decision 1; D-1135 reversed); crash behavior: BLOCK iff non-expired marker; ALLOW if absent/expired/crash-IO-fail; PC5/PC6 added; two-axis model rewritten. (2) AC-005: "five" → "six" TOML fields; expires_at added as 6th field (BC-1.18.001 v1.1; ADR-048 §Decision 2). (3) AC-020 added: Ungated-Escape invariant (BC-1.18.002 INV6; ADR-048 §Decision 3): rm/Edit-Write/TTL never gated; VP-107. (4) AC-021 added: TTL-expiry clear path (BC-1.18.003 PC4; ADR-048 §Decision 2): gate reads expired expires_at → Allow + auto-delete; 86400s; legacy markers without expires_at treated as non-expired. (5) Story EC-033..EC-035 added: crash+non-expired→Block, crash+absent/expired→Allow, TTL-expiry→Allow+auto-delete. (6) BC table: BC-1.18.001 v1.0→v1.1, BC-1.18.002 v1.4→v1.5, BC-1.18.003 v1.1→v1.2. (7) Token budget: ADR-048 row added (3,500); BC rows updated; total ~62,100; budget ~31%. (8) T-3: 5→6 TOML fields; expires_at added. (9) T-5: on_error="continue" → on_error="block_if_marker" in both gate TOML entries; NOTE updated. (10) Architecture Compliance Rules gate rule updated. Frontmatter bcs/verification_properties/input-hash unchanged. |
| 1.8 | 2026-08-31 | BC-1.18.003 v1.0→v1.1 sync (artifact-scoped marker clear; S-25.01 adversary M-1 resolution). (1) BC table BC-1.18.003 version v1.0→v1.1; role description updated to note artifact-scoped clear. (2) AC-012 updated to require (a) plugin_name match AND (b) artifact_path match (exact normalized absolute path equality); empty-path fallback added (non-artifact-scoped validators); same-plugin/different-non-empty-artifact → no-clear stated with story EC-031 reference. (3) Story EC-031 added: same plugin, different non-empty artifact, PostToolUse Pass → marker NOT cleared (BC-1.18.003 EC-008). (4) Story EC-032 added: same plugin, empty marker artifact_path, PostToolUse Pass on any artifact → marker cleared via empty-path fallback (BC-1.18.003 EC-009). (5) Token budget BC-1.18.003 row 1,800→2,200; total ~56,600→~57,000; budget ~28%→~29%. ADR-047 §D5 (artifact-scoped clear + empty-path fallback). Frontmatter bcs/verification_properties/input-hash unchanged. |
| 1.7 | 2026-08-31 | MEDIUM-2 dep-table fix: replaced stale `regex` row in Library & Framework Requirements (MANDATORY) table with `shell-words 1.1` (Phase 1b POSIX quote-aware tokenization in is_git_commit_or_push; BC-1.18.002 v1.3+ forbids regex quote-stripping as parser-differential bypass class; story body in AC-008/AC-009/Purity Classification/T-4/T-5 already correctly mandated shell-words — only the MANDATORY table row was stale). EC sync to BC-1.18.002 v1.4 (read-error adjudication): (a) EC-030 added — gate encounters unreadable marker (EACCES/IO) → Allow (fail-open), INV2 self-lock avoidance; (b) EC-008 cross-reference note updated — BC-1.18.002 EC-008 v1.4 rescoped to readable-but-malformed marker → Block (gate-read path; distinct from story EC-008 write-fail BC-1.18.001 path); (c) BC table BC-1.18.002 version cell v1.3→v1.4 with Role column updated to note v1.4 additions. Frontmatter bcs/verification_properties/input-hash unchanged. |
| 1.6 | 2026-08-31 | AC-008/AC-009/AC-011/AC-012/EC sync to BC-1.18.002 v1.3 (HIGH-1 fail-open-on-crash reconciliation + MEDIUM-2 threat-model + quoting-in-scope). (1) AC-011: `on_error = "block"` corrected to `on_error = "continue"` for both gate arms (BC-1.18.002 PC1 v1.3; HIGH-1 finding — "block" would produce unconditional self-lock on gate crash); two-axis model note added (ADR-039; ADR-047 §Decision 9) distinguishing dispatcher-level fail-open-on-crash (axis i) from filter-internal fail-SAFE posture (axis ii). (2) AC-012: PostToolUse hook qualifier added — only a PostToolUse PASS from the named plugin clears the marker; a PreToolUse PASS MUST NOT clear the marker (BC-1.18.003 PC1). (3) AC-008: Phase 1b POSIX quote-aware tokenization added (`shell-words` crate; `git "commit"` → BLOCKED EC-024; `git 'push' origin` → BLOCKED EC-025; `g'i't commit` → BLOCKED EC-026); out-of-scope boundary documented (eval/xargs/subshell/indirection — undecidable; Rice's theorem; caught by other layers); version reference v1.2→v1.3; "Phase 1/1b/2/3/4" nomenclature. (4) AC-009: v1.2→v1.3; Phase 1b note; EC-027..EC-029 NOT-blocked out-of-scope examples added. (5) Story EC-021..EC-026 added for BC EC-024..EC-029 (3 quoting-blocked + 3 out-of-scope-documented). (6) BC table BC-1.18.002 v1.2→v1.3. (7) T-5 TOML `on_error = "block"` → `"continue"` for both arms; note updated with v1.3 algorithm reference. (8) Architecture Mapping/Purity Classification/T-4 `is_git_commit_or_push` description updated v1.2→v1.3 with Phase 1b. (9) Architecture Compliance Rules: gate plugin rule updated to require both `on_error = "continue"` AND `failure_policy = "fail-open"`. (10) Token budget BC-1.18.002 row 3,500→4,000 (29 ECs + 30 test vectors). ADR-047 §Decision 9; ADR-039. |
| 1.5 | 2026-08-31 | AC-008/AC-009/EC sync to BC-1.18.002 v1.2 (MEDIUM-1 adversary finding adjudication). AC-008: regex `\bgit\b.*\b(commit|push)\b` demoted from authoritative rule to illustrative approximation only; replaced with authoritative three-phase algorithm (BC-1.18.002 PC2 v1.2): Phase 1 compound splitting (`&&`/`||`/`;`/`|`/`&`/newline; any-segment match returns `true`), Phase 2 basename identification (path-prefixed `/usr/bin/git`, `./git`; env-var-assignment tokens and `env` stripped), Phase 3 global-option skip with complete seven-option arg-taking set (`-C`, `-c`, `--namespace`, `--git-dir`, `--work-tree`, `--super-prefix`, `--config-env`) + recognized no-arg set + fail-safe on unrecognized options (return `true`), Phase 4 exact subcommand match (`commit`/`push` only — `commit-graph` excluded). AC-009: updated to reference all three phases, complete option sets, fail-safe behavior, and examples for BC ECs EC-013..EC-023. EC-006/EC-007: regex reference replaced with Phase 4 exact-subcommand citation. Added story EC-010..EC-020 covering BC-1.18.002 EC-013..EC-023 (compound splitting, basename, path-prefix, env-prefix, fail-safe, config-env). BC table: BC-1.18.002 v1.1→v1.2. Architecture Mapping + Purity Classification: `is_git_commit_or_push` description updated from regex to three-phase algorithm. T-4/T-5: command filter implementation description updated. Token budget BC-1.18.002: 2,600→3,500; total ~55,700→~56,600. ADR-047 §Decision 9 (POLICY 19). |
| 1.4 | 2026-08-31 | AC-009 exact-subcommand sync to BC-1.18.002 v1.1 (pass-1 adversary BLOCKER-3 adjudication). Regex `\bgit\b.*\b(commit\|push)\b` replaced by authoritative `is_git_commit_or_push` exact-subcommand rule (locate `git` token, skip git-global options incl. argument-taking `-C <path>`, `-c k=v`, `--namespace <ns>`, `--no-pager`, `--git-dir`; first remaining non-option token is the subcommand; block iff exactly `commit` or `push`). Added EC-011 example (`git commit-graph write` → NOT blocked; subcommand is `commit-graph`, not `commit`) and EC-012 example (`git -C .factory commit -m "state"` → BLOCKED; global option tolerated). Regex demoted to illustrative-only per BC-1.18.002 v1.1 clarification. BC table version BC-1.18.002 v1.0→v1.1. ADR-047 §Decision 9 stable anchor (POLICY 19). |
| 1.3 | 2026-08-30 | Re-audit M-1: BC-table Title cells updated to verbatim BC H1 text (POLICY 7 / criterion-75; story-writer). All 5 Title cells now match the authoritative BC H1 and BC-INDEX title cells exactly. Version bump only — no AC, task, or architecture changes. |
| 1.2 | 2026-08-30 | Consistency audit correction (story-writer). FINDING 1: AC-004 wildcard Trap arm corrected from `_other => Indeterminate` to `_ => { route to on_error }` per BC-1.18.001 invariant 2; Task T-1 pseudocode and Architecture Compliance Rules entry fixed to match; 14th Red Gate stub added (test_BC_1_18_001_unrecognized_trap_routes_to_on_error_not_indeterminate). FINDING 3: 15th Red Gate stub added for AC-006 FileSink emission (test_BC_1_18_001_emits_plugin_indeterminate_event_with_required_fields); Task T-2 scope updated. FINDING 4: Task T-5 TOML key names corrected to canonical hooks-registry.toml schema (wasm_path→plugin, trigger→event, tool_pattern→tool, tier→async=false; command_filter removed — filtering is WASM-internal). FINDING 5: Invariant trace annotations added to AC-004 (+inv2), AC-005 (+inv3+inv4), AC-007 (+inv4), AC-008 (+inv4), AC-012 (+BC-1.18.003 inv2), AC-014 (+BC-1.18.002 inv3 + BC-1.18.003 inv3); AC-018 added for BC-1.18.001 invariant 5 (dispatcher-internal StoreData); AC-019 added for BC-1.18.002 invariant 1 (same WASM binary). FINDING 10: Typo IndiceterminateCause → IndeterminateCause in T-1. Red Gate density: 15 stubs / 13 functions = 1.15. |
| 1.1 | 2026-08-30 | Consistency correction (architect; spec-burst). Cohort A split into A-immediate (validate-factory-path-staging: EFFECTIVE-NOW, no S-21.24 dependency) and A-deferred (validate-pr-merge-prerequisites, validate-wave-gate-prerequisite: SET-BUT-LATENT, set in registry with comment, enforcement conditional on S-21.24). AC-016 and Context §5 updated with explicit A-immediate/A-deferred labels and Layer-1 effective count. Narrative "So that" clause updated to state honest 1-effective/2-latent split. Consistent with ADR-047 D8a v1.3. |
| 1.0 | 2026-08-30 | Initial authoring (story-writer; F3 story-decomposition burst, validation-integrity-layer1). S-25.01 Layer-1 ACTIVE story. INDETERMINATE outcome + durable marker + next-advance gate. 17 ACs with BC-PC traces. 13 Red Gate test stubs. 7 tasks. 5 BCs: BC-1.18.001–004, BC-3.08.001. 5 VPs: VP-102–106. depends_on: [S-21.10]. |
