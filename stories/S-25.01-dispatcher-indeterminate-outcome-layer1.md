---
document_type: story
level: implementation
story_id: "S-25.01"
epic_id: "E-25"
version: "1.22"
last_amended: "2026-09-03 (v1.21 → v1.22 — state-manager, post-merge bookkeeping): PR #807 (feature/S-25.01) SQUASH-MERGED into develop as `f3f9b3a1` 2026-09-03 (base develop pre-merge HEAD `b4ff2383`); feature branch + `.worktrees/S-25.01` deleted. status draft→merged. Delivered on develop: dispatcher INDETERMINATE-outcome Layer-1 (fail-loud on cannot-complete) — durable unvalidated-mutation marker + next-advance gate (Agent arm + git commit/push arm), block_if_marker crash policy, 86400s TTL deadman, ungated three-path recovery (Edit/Write primary, TTL auto-expiry, human-OOB rm), audited marker.written/marker.cleared(REVALIDATED|TTL_EXPIRED|OPERATOR_OVERRIDE|SUPERSEDED) events. Full delivery arc: LOCAL BC-5.39.001 3-CLEAN convergence (passes 16/17/18, D-1153/D-1154/D-1155) atop certified base `3919ebcb`; finalization-doc-sweep COMPLETE (D-1156, 11 backlog items disposed, finalization commits `f1400e35`+`b46f48f6`+`3e463cdc`); validate-factory-path-staging zero-enforcement overclaim correction cascade (D-1157) — this story's own EFFECTIVE-NOW/count-ONE overclaim corrected to ASSIGNED-NOW/ZERO, sibling of ADR-047 v1.4/BC-1.18.004 v1.2/ADR-048 v1.6+VP-108 v1.9, new follow-up story S-25.04 opened to anchor the gap; ADR-050 ratification (D-1158) fixing the `bats-darwin-leg-macos` CI leg's stale-bundle registry-schema-forward-compatibility defect; a transient rustc-SIGSEGV CI flake cleared via rerun. POL-14 auto-promotion this burst: BC-1.18.001 v1.5→v1.6, BC-1.18.002 v1.7→v1.8, BC-1.18.003 v1.7→v1.8, BC-1.18.004 v1.2→v1.3 — all draft→active (BC-3.08.001 v1.34 already active, unchanged). merged_count 116→117. D-1159. Refs: PR #807, D-1159, D-1157, D-1158. [Prior: 2026-09-03 (v1.21) — validate-factory-path-staging zero-enforcement overclaim sweep (story-writer; single-commit TD-VSDD-053): corrects the S-25.01 narrative/AC overclaim that validate-factory-path-staging's Cohort A-immediate fail-closed assignment is 'EFFECTIVE-NOW' with a 'Layer-1 effective fail-closed count' of ONE, per the human-directed factual correction already made in ADR-047 v1.4 (20bf81dd), BC-1.18.004 v1.2 (df3219ea), and ADR-048 v1.6 + VP-108 v1.9 (3522c4bc) — routed from pr-reviewer's fresh-eyes MAJOR finding on PR #807. validate-factory-path-staging is registered PreToolUse `^Bash$`; the durable-marker write path (BC-1.18.001 invariant 4) is PostToolUse-only, so this registration structurally can never reach `write_indeterminate_marker`; combined with `on_error = \"continue\"` (never blocks the current dispatch) and this validator's absence from the ADR-039 §Decision 2 six-validator exhaustion-leg roadmap, the assignment produces ZERO current production enforcement effect — identical to fail-open. The mechanism is CODE-reachable and unit-tested but not production-live via this validator. Corrected FOUR narrative/AC loci: (1) Narrative 'So that' clause (EFFECTIVE-NOW → ASSIGNED-NOW; 'permanently closed' claim removed for validate-factory-path-staging, reframed around the ZERO-enforcement gap and its S-25.04 anchor). (2) Context §5 Cohort A-immediate bullet + 'Layer-1 effective fail-closed count' bullet (1 → ZERO; ADR-047 §D8a v1.3 → v1.4 citation bump). (3) AC-016 (traces to BC-1.18.004 postcondition 4) — headline Cohort A-immediate label and effective-count statement corrected to ZERO, reconciled with the story's OWN already-accurate EC-009 row (PreToolUse INDETERMINATE path: no marker written, advisory event only) — EC-009 itself UNCHANGED, narrative now consistent with it. (4) Behavioral Contracts table BC-1.18.004 row (v1.1 → v1.2, Role cell describes the PC4 factual correction) and Traceability table's Cohort A row (ADR-047 §D8a v1.3 → v1.4). The corrected characterization is anchored to new follow-up story **S-25.04** — \"Close validate-factory-path-staging zero-enforcement gap — real Layer-1 production trigger\" (Epic E-25, authored this same burst) — per CLAUDE.md Canonical Principle Rule 3 (a coverage gap requires an explicit future-story anchor, not a silent deferral). NOT a POLICY 22 design/security-model change — no decision content altered, only narrative/AC reconciliation to the already-corrected source-of-truth artifacts; the S-25.01 CODE and its own EC-009 row were already correct and remain unchanged; no stubs fabricated, no behavior changed. POLICY 18: `input-hash` — ADR-047/ADR-048/BC-1.18.004/VP-108 are declared inputs and changed on disk (this is the source correction this burst reconciles against); FLAG for state-manager to run `compute-input-hash S-25.01-dispatcher-indeterminate-outcome-layer1.md --update` and re-verify POLICY 18 three-way parity (frontmatter=catalog-row=blockquote). FLAG for state-manager: STORY-INDEX v-cell 1.20→1.21 + register new S-25.04 draft row under Epic E-25. [Prior: 2026-09-03 (v1.20) — Documentary reconciliation burst (story-writer; single-commit TD-VSDD-053; D-1141 companion + F-P13-001): TWO items anchored to the finalization-doc-sweep, resolved against the ALREADY-CONVERGED code/tests (feature/S-25.01 3-CLEAN at `3919ebcb`; finalization HEAD `3e463cdc`) — no behavior changed, no stubs fabricated. ITEM 1 (F-P13-001): AC-007's block-message parenthetical illustrative example (\"re-invoke the named plugin\"-style phrasing) was stale relative to AC-020's authoritative four-tier recovery model (T1 Edit/Write primary; T2 24h TTL deadman; T3 human OOB rm break-glass; T4 agent-tool rm de-sanctioned) — corrected the example to cite T1 Edit/Write re-validation as primary agent recovery and T3 human-operator-terminal rm as the escape hatch; AC-007's mandate (block message MUST contain plugin_name/artifact_path/cause/re-validation-instruction/escape-hatch, machine-parseable) is UNCHANGED. ITEM 2 (D-1141): the Red Gate Test Inventory + T-3 task checklist + Red Gate density count did not enumerate AC-021..AC-025 (marker.cleared TTL_EXPIRED/REVALIDATED/OPERATOR_OVERRIDE/SUPERSEDED + marker.written emission) — a documentary gap flagged repeatedly since v1.10 (v1.12/v1.13/v1.14/v1.15 \"Known gap\" notes). Grepped the feature/S-25.01 worktree and confirmed the implementing tests already exist and pass: AC-021 → `test_ADR_048_D4_check_and_clear_expired_marker_ttl_expired_deletes_and_emits`; AC-022 → `test_BC_1_18_003_named_plugin_pass_clears_marker_via_execute_tiers`; AC-023 → `test_ADR_048_D4_reconcile_raw_delete_unmatched_event_emits_operator_override`; AC-024 → `test_ADR_048_D4_v1_3_superseded_clear_emits_for_cross_pair_overwrite` + `test_ADR_048_D4_PC8_no_emit_on_cross_pair_write_failure`; AC-025 → `test_marker_written_emitted_on_successful_write_only` + `test_pretooluse_indeterminate_without_marker_written_does_not_fabricate_operator_override` (all in `crates/factory-dispatcher/src/indeterminate_marker.rs` except AC-022's in `crates/factory-dispatcher/tests/marker_integration.rs`). Added a new \"AC-021..AC-025 marker.cleared / marker.written emission coverage\" table to the Red Gate Test Inventory citing these by test-function name (TD-VSDD-091 anchor discipline), marked the corresponding T-3 checklist items `[x]` done, superseded (retained in a collapsed historical `<details>` block, not deleted) the recurring \"lack dedicated Red Gate test stubs — flagged as follow-up\" notes, and recomputed density: 15 original stubs + 7 newly-enumerated existing tests = 22 / 15 non-trivial functions = 1.47 (the original 15/15=1.00 as-of-implementer-dispatch figure is left unchanged per TD-VSDD-091 pass-report exception). ALSO IN SCOPE THIS BURST: while editing this file, discovered a pre-existing `validate-changelog-monotonicity` violation (frontmatter had advanced to v1.18/v1.19 via two POLICY-18 input-hash re-sync bursts that updated the frontmatter `last_amended` \"[Prior: ...]\" chain but never appended matching rows to the \"## Changelog\" table body) — fixed in-scope per the Production-Grade Default (CLAUDE.md Canonical Principle Rule 4: AI-built defects are the AI's responsibility to fix) by backfilling the missing v1.18 and v1.19 Changelog rows from the frontmatter's own preserved narrative, verbatim in substance. POLICY 18: `input-hash` intentionally left UNCHANGED — no BC/VP/ADR/architecture input file changed on disk this burst; this is a story-body-only documentary-reconciliation edit. FLAG for state-manager: re-sync/verify STORY-INDEX v-cell (1.19→1.20) + POLICY 18 three-way parity (frontmatter=catalog-row=blockquote, all currently `6ca47ed`, unchanged by this burst). [Prior: 2026-09-02 (v1.19) — POLICY 18 input-hash re-sync f3da248→6ca47ed (state-manager; single-commit TD-VSDD-053; D-1148): VP-108 v1.7→v1.8 (S-25.01 LOCAL adversary pass 15 F-P15-001 HIGH resolution — Postcondition 1 REVALIDATED clear's Property Statement paragraph contradicted BC-3.08.001 Event 9 trace_id semantics + sibling PC2/PC3/PC5 wording + code, a partial-fix regression where the F-P2-002/F-P3-001 trace_id-source + emission-locus corrections reached PC2/PC3/PC5 but never PC1; FIXED via architect commit `90675c7d` — trace_id source, emission locus, and trigger corrected in PC1; PC2-PC8 sibling-swept clean) changed on disk this burst; three-way parity restored (frontmatter=catalog-row=blockquote=6ca47ed). Story content UNCHANGED — input-hash re-sync class only, no body prose change. [Prior: 2026-09-02 (v1.18) — POLICY 18 input-hash re-sync 4727383→f3da248 (state-manager; single-commit TD-VSDD-053; D-1145): VP-108 v1.5→v1.7 (S-25.01 adversary pass 12 F-P12-001 MEDIUM resolution — PC8 postcondition had no implementing test + emission block duplicated at 2 callsites, FIXED via implementer helper extraction `adf3a1b1` + test-writer PC8 regression test `817c52ae`; architect corrected VP-108 v1.5→v1.6→v1.7 proof-harness anchors) changed on disk this burst; three-way parity restored (frontmatter=catalog-row=blockquote=f3da248). Story content UNCHANGED — input-hash re-sync class only, no body prose change. [Prior: 2026-09-01 (v1.17) — POLICY 19 (adr_version_cite_volatile_pin_prohibition, D-1079 story-bodies extension) normalization of ADR-048 sub-version citations in the story body (F-P11-002, S-25.01 adversary pass 11; story-writer, single-commit TD-VSDD-053): every load-bearing \"ADR-048 §D4 vX.Y\" / \"ADR-048 §Decision 4 vX.Y\" citation in AC headers, body prose, tables, and Traceability cells (AC-021/AC-023/AC-024/AC-025 headers and prose; BC table Role cells for BC-1.18.001/BC-1.18.002/BC-1.18.003/BC-3.08.001; Architecture Mapping and Purity Classification tables; Token Budget Estimate section; T-3/T-4 Tasks checklist; Architecture Compliance Rules table) is now cited by §Decision anchor ONLY, with the correction-event provenance (finding ID, human-ratification date, D-NNN where applicable) preserved as an explicitly non-load-bearing, historical parenthetical (e.g. \"ADR-048 §Decision 4 (F-P9-001 correction — historical provenance, non-load-bearing)\"). No AC semantics, IDs, BC/VP anchors, or behavioral content changed — citation FORM only; no new line-number pins introduced. The dedicated \"## Changelog\" table (a pass-report changelog, already excepted under TD-VSDD-091) and this frontmatter's own historical last_amended narrative are left unchanged, as are BC-1.18.001/BC-1.18.002/BC-1.18.003/BC-1.18.004/BC-3.08.001's own version self-citations (POLICY 8/POLICY 7 territory, not POLICY 19's scope). Post-edit tr-normalized grep confirms zero remaining load-bearing \"ADR-048 §D4/§Decision N vX.Y\" pins in the story body outside the Changelog. input-hash intentionally left UNCHANGED for state-manager to re-sync/verify — no BC/VP/ADR/architecture input file changed on disk this burst; this is a story-body-only citation-form edit. [Prior: 2026-09-01 (v1.16) — POLICY 18 input-hash re-sync 3b569a1→4727383 (state-manager; single-commit TD-VSDD-053; D-1143): VP-108 v1.4→v1.5 (S-25.01 adversary pass 10 F-P10-002 MEDIUM resolution — proof-harness timestamp-assertion gap closure; no BC/wire-format change) changed on disk this burst; three-way parity restored (frontmatter=catalog-row=blockquote=4727383). Story content UNCHANGED. [Prior: 2026-09-01 (v1.15) — ADR-048 v1.4→v1.5 Emission-Point Correction sync (S-25.01
adversary pass 9 F-P9-001 MEDIUM; architect adjudication; HUMAN-RATIFIED 2026-09-01, POLICY 22,
D-1142) + PO BC amendments BC-1.18.001 v1.4→v1.5, BC-1.18.003 v1.6→v1.7, BC-3.08.001 v1.33→v1.34
(BC-1.18.002 v1.7 unchanged, BC-1.18.004 v1.1 unchanged this burst). F-P9-001 found that AC-024's
SUPERSEDED `marker.cleared` emission (`emit_superseded_if_cross_pair`) was called BEFORE
`write_indeterminate_marker` attempted the overwrite it describes, and fired UNCONDITIONALLY on
that attempt's outcome — so a write failure (EC-007) left the OLD marker still durably present and
enforcing while a SUPERSEDED record had already been emitted falsely claiming it was overwritten
(NIST AU-3/AU-10 fabricated audit record), the un-swept sibling of AC-025's own v1.4
`marker.written`-after-`Ok(())` discipline. (1) AC-024 rewritten: the SUPERSEDED emission call is
relocated from the unconditional pre-overwrite read to INSIDE `write_indeterminate_marker`'s
`Ok(())` arm, at BOTH `executor.rs` `write_indeterminate_marker` call sites, immediately BEFORE
AC-025's `emit_marker_written` call in that same arm (SUPERSEDED first, then `marker.written`); a
new write-failure non-fabrication clause states that on `Err(_)` NEITHER `marker.cleared(SUPERSEDED)`
for the superseded pair NOR `marker.written` for the new pair is emitted — cites BC-1.18.001 v1.5
postcondition 4 + invariant 3, BC-1.18.003 v1.7 postcondition 5 + EC-018, BC-3.08.001 v1.34 Event 9,
VP-108 postcondition 8. (2) AC-025's opening paragraph corrected to drop the now-inaccurate "BEFORE
ANY other marker-lifecycle event for this write" framing and state the SUPERSEDED-then-marker.written
back-to-back Ok(()) ordering explicitly (sibling-sweep, TD-VSDD-060). (3) Architecture Mapping +
Purity Classification tables: `emit_superseded_if_cross_pair`, `emit_marker_written`, and
`emit_marker_cleared` rows corrected to name the Ok(()) arm co-location and BOTH callsites; no new
row added (no new function — pure call-site reordering per ADR-048 §D4 v1.5 Design (A)). (4) T-3
task checklist: the two executor.rs wiring items for `emit_superseded_if_cross_pair` and
`emit_marker_written` rewritten to reflect the corrected Ok(()) arm ordering at both call sites.
(5) BC table: BC-1.18.001 v1.4→v1.5, BC-1.18.003 v1.6→v1.7, BC-3.08.001 v1.33→v1.34; Role columns
updated. (6) Red Gate stub gap note extended: the pre-existing gap (AC-021..AC-025 lack dedicated
Red Gate test stubs) now also covers the new PC8 negative-control test
(`test_BC_1_18_001_superseded_not_fabricated_on_write_failure`); no stub fabricated in this burst
(pure re-ordering introduces no new non-trivial function). Red Gate density unchanged: 15 stubs /
15 non-trivial functions = 1.00 ≥ 0.5 threshold (pass). (7) Token budget: BC-1.18.001/BC-1.18.003/
BC-3.08.001/VP-102..108 rows bumped for the v1.5 correction content; total ~67,300→~67,900; budget
~34%→~34% (rounds unchanged). Frontmatter `bcs` list unchanged (same 5 BCs: BC-1.18.001–004,
BC-3.08.001 — only versions bumped). POLICY 18: input-hash NOT updated by story-writer —
BC-1.18.001/BC-1.18.003/BC-3.08.001/VP-108/ADR-048 all changed on disk this burst; drift flagged
for state-manager. [Prior: 2026-09-01 (v1.14) — ADR-048 v1.3→v1.4 Reconciliation-Premise Correction sync
(S-25.01 adversary pass 6 F-P6-001 MEDIUM; architect adjudication; HUMAN-RATIFIED 2026-09-01,
POLICY 22, D-1141) + PO BC amendments BC-1.18.001 v1.3→v1.4, BC-1.18.003
v1.5→v1.6, BC-3.08.001 v1.32→v1.33 (BC-1.18.002 v1.7 unchanged, BC-1.18.004 v1.1 unchanged this
burst). New Event 10 `marker.written` — a positive marker-creation audit record — closes a
false-`OPERATOR_OVERRIDE`-attribution gap in `reconcile_raw_delete`: the pre-v1.4 premise (an
unmatched fail-closed `plugin.indeterminate` proves a marker was durably written) is FALSE for a
PreToolUse fail-closed INDETERMINATE (marker write is PostToolUse-only, BC-1.18.001 INV4) or a
PostToolUse marker-write I/O failure (EC-007/story EC-008) — both leave an unmatched
`plugin.indeterminate` with no marker ever written, which the pre-v1.4 scan would misread as a raw
delete and fabricate `marker.cleared(OPERATOR_OVERRIDE)` for. (1) AC-023 retargeted: `reconcile_raw_delete`'s
bounded scan now matches an unmatched **`marker.written`** record (BC-3.08.001 Event 10), NOT an
unmatched `plugin.indeterminate` record — the `trace_id`/`plugin_name`/`artifact_path` sourced for
the retroactive `marker.cleared(OPERATOR_OVERRIDE)` now come from the matched `marker.written`
event; fail-closed-inference wording removed as structurally redundant (`marker.written` is never
emitted for a fail-open dispatch or a PreToolUse dispatch, BC-1.18.001 INV4). Non-fabrication
clause added (F-P6-001 fix): a fail-closed INDETERMINATE that never wrote a marker (PreToolUse
plugin, or PostToolUse write-failure) produces no `marker.written`, so `reconcile_raw_delete` MUST
NOT fabricate an OPERATOR_OVERRIDE for it — cites BC-1.18.003 v1.6 EC-017, BC-3.08.001 Event 10,
VP-108 postcondition 7. (2) New **AC-025** added under a new "BC-1.18.001 Postcondition 4 v1.4 —
`marker.written` Audited Creation Event" subsection: on successful `write_indeterminate_marker`,
the dispatcher's `executor.rs` callsite (the same site that performs the AC-024 SUPERSEDED check)
calls a new `emit_marker_written(ctx: &HostContext, fields: &MarkerFields)` function which emits
`marker.written` (BC-3.08.001 Event 10) via `ctx.emit_internal`; emitted ONLY on the `Ok(())` arm,
never before the write, never on `Err(_)` — cites BC-1.18.001 v1.4 postcondition 4, BC-3.08.001
Event 10, VP-108 postcondition 6. (3) Architecture Mapping + Purity Classification tables: new
`emit_marker_written` row added (Effectful; `indeterminate_marker.rs`); `reconcile_raw_delete` row
updated to note the scan retarget from unmatched `plugin.indeterminate` to unmatched
`marker.written`. (4) T-3 task checklist: new implement item for `emit_marker_written`;
`reconcile_raw_delete` implement item updated to name the `marker.written` scan target; new
executor.rs wiring item for the marker-write callsite's `emit_marker_written` invocation
(immediately after the AC-024 SUPERSEDED check, on the `Ok(())` arm only). File Structure
Requirements row for `indeterminate_marker.rs` updated to list `emit_marker_written` alongside the
seven existing functions. (5) BC table: BC-1.18.001 v1.3→v1.4, BC-1.18.003 v1.5→v1.6, BC-3.08.001
v1.32→v1.33; Role columns updated to describe the new `marker.written` creation event and the
`reconcile_raw_delete` scan retarget. (6) Red Gate stub gap note extended: the pre-existing gap
(AC-021/AC-022/AC-023/AC-024 lack dedicated Red Gate test stubs) now also covers AC-025 — no stub
fabricated in this burst; flagged as a follow-up story-writer/test-writer item. Red Gate density
recomputed: 15 stubs / 15 non-trivial functions (14 prior + new `emit_marker_written`) = 1.00 ≥ 0.5
threshold (unchanged pass). (7) New story EC-036 added: non-fabrication negative control
(PreToolUse fail-closed INDETERMINATE or PostToolUse write-failure → no `marker.written` → no
fabricated `marker.cleared(OPERATOR_OVERRIDE)`); story EC-008 (write-fail path) gains a
cross-reference clause noting no `marker.written` is emitted for that path either. (8) Token
budget: BC-1.18.001/BC-1.18.003/BC-3.08.001/VP-102..108 rows bumped for the new content; story spec
row bumped for AC-025 + architecture/task updates; total ~65,700→~67,300; budget ~33%→~34%.
Frontmatter `bcs` list unchanged (same 5 BCs: BC-1.18.001–004, BC-3.08.001 — only versions bumped).
POLICY 18: input-hash NOT updated by story-writer — BC-1.18.001/BC-1.18.003/BC-3.08.001/VP-108/
ADR-048 all changed on disk this burst; drift flagged for state-manager. [Prior: 2026-09-01 (v1.13) — ADR-048 v1.2→v1.3 Emission-Mechanism Precision Correction sync
(S-25.01 LOCAL adversary pass 3 F-P3-001 MEDIUM + F-P3-002 LOW; human-ratified) + PO BC amendments
BC-1.18.001 v1.2→v1.3, BC-1.18.003 v1.4→v1.5, BC-3.08.001 v1.31→v1.32 (BC-1.18.002 v1.7 unchanged
this burst). (1) New AC-024 added: the SUPERSEDED clear path (BC-1.18.001 INV3 corollary +
BC-1.18.003 PC5 + BC-3.08.001 Event 9 + VP-108 PC5) — a cross-pair marker overwrite (new
INDETERMINATE for a `(plugin_name, artifact_path)` pair DIFFERENT from the marker on disk) MUST
emit `marker.cleared(clear_mode=SUPERSEDED, actor_type=system, reason=non-null)` for the OLD pair
via `ctx.emit_internal` BEFORE the new marker write completes; same-pair overwrite emits nothing.
(2) AC-021/AC-023 signature citations updated to the `ctx: &HostContext` form:
`check_and_clear_expired_marker(factory_root, now, ctx)`, `reconcile_raw_delete(factory_root,
ctx)`, `emit_marker_cleared(ctx, fields, clear_mode, actor_type, reason)` — ALL Events 8/9
emissions now route through `HostContext::emit_internal` (dual-sink: durable InternalLog write +
`ctx.events` queue), never a raw `InternalLog::write`. AC-023's bounded-scan target corrected from
the current day's `events-*.jsonl` to `dispatcher-internal-{date}.jsonl` (via `ctx.internal_log`)
— the only log every default production dispatcher run writes unconditionally; the FileSink/Router
apparatus remains unwired pending S-4.07. (3) AC-006/AC-015 sink-mechanism phrasing corrected:
both previously claimed emission "to the OTel-aligned FileSink (`events-*.jsonl`)" — corrected to
name the ACTUAL mechanism, `HostContext::emit_internal` (dual-write: durable
`dispatcher-internal-{date}.jsonl` + the `ctx.events` pipeline that a future FileSink/Router will
drain, S-4.07); no claim of a literal `events-*.jsonl` file being written today. (4) Architecture
Mapping + Purity Classification tables updated: `emit_marker_cleared` row corrected to the
`ctx: &HostContext` signature and `HostContext::emit_internal` dual-sink description;
`check_and_clear_expired_marker` and `reconcile_raw_delete` rows gain the `ctx: &HostContext`
parameter and (for `reconcile_raw_delete`) the `dispatcher-internal-{date}.jsonl` scan-target
correction; new row added for `emit_superseded_if_cross_pair` (new helper, `indeterminate_marker.rs`
— reads the pre-overwrite marker's fields, compares `(plugin_name, artifact_path)` against the new
event's, and calls `emit_marker_cleared(ctx, ..., "SUPERSEDED", "system", Some(reason))` when they
differ; called from `executor.rs`'s marker-write callsite BEFORE `write_indeterminate_marker`
overwrites). (5) T-3 task checklist: `emit_marker_cleared`/`check_and_clear_expired_marker`/
`reconcile_raw_delete` implement items updated to the `ctx: &HostContext` signatures;
`ClearMode` enum gains `Superseded`; new implement item added for `emit_superseded_if_cross_pair`;
executor.rs wiring item updated to pass `ctx` to `check_and_clear_expired_marker`, plus a new
wiring item for the marker-write callsite's `emit_superseded_if_cross_pair` invocation. File
Structure Requirements row for `indeterminate_marker.rs` updated to list `emit_superseded_if_
cross_pair` alongside the six existing functions. (6) BC table: BC-1.18.001 v1.2→v1.3,
BC-1.18.003 v1.4→v1.5, BC-3.08.001 v1.31→v1.32; Role columns updated to note the SUPERSEDED
clear_mode addition and the emit_internal emission-mechanism correction. (7) Red Gate stub gap
note extended: the pre-existing gap (AC-021/AC-022/AC-023 lack dedicated Red Gate test stubs,
since v1.10) now also covers AC-024 — no stub fabricated in this burst; flagged as a follow-up
story-writer/test-writer item. Red Gate density recomputed: 15 stubs / 14 non-trivial functions
(13 prior + new `emit_superseded_if_cross_pair`) = 1.07 ≥ 0.5 threshold (unchanged pass). (8) Token
budget: BC-1.18.001/BC-1.18.003/BC-3.08.001/VP-102..108 rows bumped for the new content; story spec
row bumped for AC-024 + architecture/task updates; total ~64,100→~65,700; budget ~32%→~33%.
Frontmatter `bcs` list unchanged (same 5 BCs: BC-1.18.001–004, BC-3.08.001 — only versions bumped).
POLICY 18: input-hash NOT updated by story-writer — BC-1.18.001/BC-1.18.003/BC-3.08.001/VP-108/
ADR-048 all changed on disk this burst; drift flagged for state-manager. [Prior: 2026-08-31 (v1.12) — ADR-048 v1.1→v1.2 Emission-Point Correction sync (S-25.01 LOCAL adversary pass 2 F-P2-002 HIGH + F-P2-003 MED; human-ratified) + PO BC amendments BC-1.18.001 v1.1→v1.2, BC-1.18.002 v1.6→v1.7, BC-1.18.003 v1.3→v1.4, BC-3.08.001 v1.30→v1.31 (all narration-locus corrections; no wire-format or postcondition-semantic change). (1) AC-021 rewritten: TTL_EXPIRED detect+auto-delete+emit moved from the WASM gate plugin's `evaluate_gate` TTL-check branch to dispatcher-native `check_and_clear_expired_marker` (`indeterminate_marker.rs`), invoked from `executor.rs`'s tier-execution loop BEFORE every Arm 1/Arm 2 `on_error=block_if_marker` plugin invocation on the normal (non-crash) path; `evaluate_gate` no longer parses `expires_at`; clear-decision cites VP-106, emission cites VP-108; crash-path TTL-allow non-emission clause corrected to name `check_and_clear_expired_marker` as the sole normal-path emitter. (2) AC-023 rewritten: OPERATOR_OVERRIDE/RAW_DELETE_DETECTED reconciliation attributed to dispatcher-native `reconcile_raw_delete` (`indeterminate_marker.rs`), invoked from the same pre-check's marker-absent branch; production-grade bounded-scan requirement added per ADR-048 §D4 v1.2 (MUST cap scan to the current day's `events-*.jsonl` only — never scan prior days — and MUST cap total bytes/records read via a fixed-size tail read, not a full-file scan); VP citation corrected VP-106→VP-108. (3) Architecture Mapping + Purity Classification tables: `guard_logic::evaluate_gate` row corrected to 'pure marker-presence check; no TOML `expires_at` field access — TTL detection is entirely dispatcher-native, performed by `check_and_clear_expired_marker` before this function runs on the normal path'; two new rows added for `check_and_clear_expired_marker` and `reconcile_raw_delete`/`emit_marker_cleared` (both Effectful; `indeterminate_marker.rs`). (4) T-3 task checklist gains three new implement items (`emit_marker_cleared`, `check_and_clear_expired_marker`, `reconcile_raw_delete`) plus an executor.rs wiring item (tier-execution loop calls both pre-check functions before every `on_error=block_if_marker` plugin invocation on the normal path); File Structure Requirements row for `indeterminate_marker.rs` updated to list all six functions. T-4's `guard_logic::evaluate_gate` bullet corrected to an explicit pure-presence-check description — no TTL/RAW_DELETE_DETECTED logic ever belonged there per ADR-048 §D4 v1.2 architect adjudication (v1.1's 'or gate plugin' ADR framing was the drafting error being corrected, not an actual code placement needing removal). (5) BC table: BC-1.18.001 v1.1→v1.2, BC-1.18.002 v1.6→v1.7, BC-1.18.003 v1.3→v1.4, BC-3.08.001 v1.30→v1.31; Role columns updated to attribute TTL_EXPIRED/OPERATOR_OVERRIDE emission dispatcher-native. (6) Frontmatter: VP-108 added to `verification_properties` and `inputs` — VP-108 (anchor_story=S-25.01 since its v1.0 origination) was missing from this story's frontmatter despite being anchored here; pre-existing gap, closed in this burst. (7) Token budget: BC-1.18.001/1.18.002/1.18.003 version labels bumped; VP row 'VP-102 through VP-107' → 'VP-102 through VP-108' (+900 tokens for VP-108); total ~63,200→~64,100; budget still ~32%. (8) Inline body version pins updated only where they cited the now-corrected TTL-locus narration specifically (AC-005 'enables gate TTL auto-clear' → 'enables dispatcher-native TTL auto-clear'; two BC-1.18.001 PC4 v1.1→v1.2 citations); unrelated historical-anchor version pins (BC-1.18.002 v1.3/v1.5/v1.6 citing the Phase 1/1b/2/3/4 algorithm, PC1 block_if_marker introduction, and T3/PC5 recovery-model text untouched by v1.7) intentionally left unchanged per TD-VSDD-091 (justified historical anchors, not stale current-version pins). FLAG for orchestrator (not actioned in this burst — outside enumerated scope): AC-021/AC-022/AC-023 (`marker.cleared` emission postconditions) have NO dedicated Red Gate test stub coverage in this story's Red Gate Test Inventory or T-3 checklist — a pre-existing gap present since v1.10 introduced these ACs, not introduced by this amendment; recommend a follow-up story-writer or test-writer pass to add stubs for `emit_marker_cleared`/`check_and_clear_expired_marker`/`reconcile_raw_delete` and recompute Red Gate density. Frontmatter `bcs` list unchanged (same 5 BCs: BC-1.18.001–004, BC-3.08.001 — only versions bumped). POLICY 18: input-hash NOT updated by story-writer — inputs list changed (VP-108.md added); drift flagged for state-manager. [Prior: 2026-08-31 (v1.10) — ADR-048 v1.1 recovery reframe + audited clear event sync: (1) AC-020 rewritten to four-tier recovery model (T1=Edit/Write primary inherently ungated even through gate crash; T2=24h TTL deadman passive; T3=human OOB rm break-glass never intercepted; T4=agent-tool rm de-sanctioned may-be-blocked on crash path ACCEPTABLE NOT INV6 violation; guaranteed recovery is T1+T2+T3; ADR-048 §Decision 3 stable anchor; BC-1.18.002 v1.6 INV6). (2) AC-011 crash block message reframed: T1 Edit/Write re-validation listed first; rm moved to T3 human OOB; MUST NOT instruct agent to run rm per BC-1.18.002 v1.6 PC5. (3) AC-021 updated: TTL-expiry auto-delete now emits marker.cleared(TTL_EXPIRED) (BC-1.18.003 v1.3 PC4 TTL-loudness; crash-path native TTL-allow does NOT emit). (4) AC-022 added: REVALIDATED clear path emits marker.cleared(clear_mode=REVALIDATED, actor_type=validator, trace_id from originating plugin.indeterminate, reason=null; BC-1.18.003 v1.3 PC1 + BC-3.08.001 Event 9). (5) AC-023 added: OPERATOR_OVERRIDE retroactive marker.cleared emission via RAW_DELETE_DETECTED (best-effort; BC-1.18.003 v1.3 PC3 + BC-3.08.001 Event 9). (6) EC-033 block message updated: T1 primary, rm demoted to T3 human OOB. (7) BC table: BC-1.18.002 v1.5→v1.6 (four-tier recovery model reframe), BC-1.18.003 v1.2→v1.3 (audited clear model), BC-3.08.001 v1.29→v1.30 (Event 9 marker.cleared). (8) Token budget BC rows updated. Frontmatter bcs/verification_properties/input-hash unchanged. [Prior: 2026-08-31 (v1.9) — ADR-048 gate redesign sync: BC-1.18.002 v1.4→v1.5 (block_if_marker crash policy; PC5/PC6; INV6 ungated-escape; EC-031/EC-032 crash ECs; AC-011 updated; AC-020 added), BC-1.18.003 v1.1→v1.2 (TTL-expiry PC4; AC-021 added; story EC-035 added), BC-1.18.001 v1.0→v1.1 (expires_at 6th TOML field; AC-005 + T-3 updated). Story EC-033..EC-035 added. Token budget updated (ADR-048 row added). Architecture Compliance Rules gate rule updated. T-5 on_error updated to block_if_marker. Frontmatter bcs/verification_properties/input-hash unchanged. [Prior: 2026-08-31 (v1.8) — BC-1.18.003 v1.0→v1.1 sync (artifact-scoped marker clear): (1) BC table BC-1.18.003 version cell v1.0→v1.1; role description updated to note artifact-scoped clear (plugin_name AND artifact_path, with empty-artifact_path→name-only fallback). (2) AC-012 updated: clear now requires (a) plugin_name match AND (b) artifact_path match (exact normalized absolute path equality); empty-path fallback documented (non-artifact-scoped validators); same-plugin/different-non-empty-artifact → no-clear stated explicitly with story EC-031 reference. (3) Story EC-031 added: same plugin, different non-empty artifact, PostToolUse Pass → marker NOT cleared (BC-1.18.003 EC-008). (4) Story EC-032 added: same plugin, empty marker artifact_path, PostToolUse Pass on any artifact → marker cleared (BC-1.18.003 EC-009 empty-path fallback). (5) Token budget BC-1.18.003 row updated 1,800→2,200 (EC-008/EC-009 coverage); total ~56,600→~57,000; budget ~28%→~29%. ADR-047 §D5 (artifact-scoped clear + empty-path fallback). Frontmatter bcs/verification_properties/input-hash unchanged. [Prior: 2026-08-31 (v1.7) — MEDIUM-2 dep-table fix: `regex` row in Library & Framework Requirements (MANDATORY) replaced with `shell-words 1.1` (Phase 1b POSIX quote-aware tokenization; BC-1.18.002 v1.3+ forbids regex quote-stripping as parser-differential bypass; story body already mandated shell-words in AC-008/AC-009/Purity/T-4/T-5 — MANDATORY table row was the sole stale location); EC sync to BC-1.18.002 v1.4: EC-030 added (gate encounters unreadable marker EACCES/IO → Allow fail-open, INV2 self-lock avoidance); EC-008 cross-reference note updated (BC-1.18.002 EC-008 v1.4 rescoped: readable-but-malformed → Block gate path, distinct from story EC-008 write-fail BC-1.18.001 path); BC table BC-1.18.002 version cell v1.3→v1.4. Frontmatter bcs/verification_properties/input-hash unchanged. [Prior: 2026-08-31 (v1.6) — AC-008/AC-009/AC-011/AC-012/EC sync to BC-1.18.002 v1.3 (HIGH-1 fail-open-on-crash + MEDIUM-2 threat-model + quoting-in-scope): AC-011 corrected to require on_error=\"continue\" (was \"block\") for both gate arms + two-axis model note (ADR-039; ADR-047 §Decision 9); AC-012 adds PostToolUse qualifier (BC-1.18.003 PC1 — PreToolUse PASS does NOT clear marker); AC-008 adds Phase 1b POSIX quote-aware tokenization (shell-words crate: git \"commit\"/git 'push'/g'i't commit → BLOCKED) + out-of-scope boundary (eval/xargs/subshell/indirection — undecidable; caught by other layers); AC-009 adds Phase 1b note + EC-027..EC-029 NOT-blocked out-of-scope examples; story EC-021..EC-026 added for BC EC-024..EC-029; BC table BC-1.18.002 v1.2→v1.3; T-5 TOML on_error corrected to \"continue\"; token budget BC-1.18.002 3,500→4,000; Architecture Mapping/Purity Classification/T-4 v1.2→v1.3 refs updated. [Prior: 2026-08-31 (v1.5) — AC-008/AC-009/EC sync to BC-1.18.002 v1.2 (MEDIUM-1 adversary finding): three-phase algorithm (compound splitting, basename, complete arg-taking set, fail-safe on unrecognized options) is now the authoritative rule in AC-008; regex demoted to illustrative-only; AC-009 updated with all Phase 1–4 behaviors and new examples from BC EC-013..EC-023; added story EC-010..EC-020 for BC EC-013..EC-023; EC-006/EC-007 regex reference replaced with Phase 4 exact-subcommand citation; BC table v1.1→v1.2; Architecture Mapping/Purity Classification/T-4/T-5 is_git_commit_or_push description updated; token budget BC-1.18.002 2,600→3,500. [Prior: 2026-08-31 (v1.4) — AC-009 exact-subcommand sync to BC-1.18.002 v1.1 (pass-1 adversary BLOCKER-3 adjudication): regex replaced with authoritative is_git_commit_or_push exact-subcommand rule; EC-011/EC-012 examples added; BC table version updated v1.0→v1.1. [Prior: 2026-08-30 (v1.3) — input-hash re-sync 132d934→8fbeba1 (VP-105 v1.3→v1.4 LOW-001 proof-harness bats comment corrected; POLICY 18 three-way parity maintained). Story content UNCHANGED. [Prior: 2026-08-30 (v1.3) — re-audit M-1: BC-table Title cells → verbatim H1 (POLICY 7). [Prior: 2026-08-30 (v1.2) — consistency audit correction]]]]]]]]]]"
status: merged
producer: story-writer
timestamp: 2026-08-30T00:00:00Z
phase: 3
inputs:
  - .factory/feature-delta/validation-integrity-layer1/F1-delta-analysis.md
  - .factory/specs/architecture/decisions/ADR-047-indeterminate-outcome-model-durable-mutation-marker-next-advance-gate.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.001.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.002.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.003.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.004.md
  - .factory/specs/behavioral-contracts/ss-03/BC-3.08.001.md
  - .factory/specs/verification-properties/VP-102.md
  - .factory/specs/verification-properties/VP-103.md
  - .factory/specs/verification-properties/VP-104.md
  - .factory/specs/verification-properties/VP-105.md
  - .factory/specs/verification-properties/VP-106.md
  - .factory/specs/verification-properties/VP-107.md
  - .factory/specs/verification-properties/VP-108.md
  - .factory/specs/architecture/decisions/ADR-048-fail-closed-but-recoverable-gate-block-if-marker-crash-policy-marker-ttl-deadman-and-ungated-escape-invariant.md
input-hash: "ed6eb84"
traces_to: .factory/stories/epics/E-25-validation-integrity.md
points: "12"
depends_on: [S-21.10]
blocks: [S-25.02]
behavioral_contracts:
  - BC-1.18.001
  - BC-1.18.002
  - BC-1.18.003
  - BC-1.18.004
  - BC-3.08.001
verification_properties: [VP-102, VP-103, VP-104, VP-105, VP-106, VP-107, VP-108]
priority: "P0"
cycle: v1.0-brownfield-backfill
wave: 1
target_module: crates/factory-dispatcher
subsystems: [SS-01, SS-03, SS-04, SS-07]
estimated_days: 5
assumption_validations: []
risk_mitigations: []
tdd_mode: strict
---

> **tdd_mode: strict** — Full TDD Iron Law enforced. All non-trivial function bodies MUST use
> `todo!()`. Red Gate density check ≥0.5 required before Step 4 (implementer) dispatch. Every
> test in the Red Gate list MUST be a failing stub before any implementation begins.

> **Execute:** `cargo test --workspace --all-targets && cd plugins/vsdd-factory/tests && ./run-all.sh`

# S-25.01: Dispatcher INDETERMINATE Outcome Layer 1 — Fail-Loud on Cannot-Complete: Durable Marker + Next-Advance Gate

## Narrative

- **As a** factory pipeline operator whose validators run in fuel-bounded and epoch-bounded WASM
  sandboxes
- **I want** the dispatcher to make "couldn't validate" a FIRST-CLASS, fail-LOUD outcome that
  writes a durable `.factory/unvalidated-mutation.marker` file and blocks the next state-advancing
  Agent dispatch and `git commit`/`git push` Bash dispatch until the affected artifact is
  re-validated or an operator explicitly clears the marker
- **So that** the silent CWE-754 hole (treating INDETERMINATE as PASS) is closed for
  `validate-pr-merge-prerequisites` and `validate-wave-gate-prerequisite` once S-21.24 activates
  enforcement (Cohort A-deferred; SET-BUT-LATENT — set in registry with comment, enforcement
  conditional on S-21.24), and the fail-closed config bit is safely ASSIGNED-NOW for
  `validate-factory-path-staging` (Cohort A-immediate; corrected v1.21 — previously mislabeled
  EFFECTIVE-NOW, which overclaimed live enforcement) — though this validator's PreToolUse
  `^Bash$` registration currently produces ZERO observable enforcement effect (the durable-marker
  write path is PostToolUse-only per BC-1.18.001 invariant 4, so this registration structurally
  can never reach it; `on_error = "continue"` never blocks the current dispatch either), a
  genuine coverage gap tracked to follow-up story S-25.04 rather than silently accepted;
  existing ~76 fail-open plugins are completely unchanged (advisory `plugin.indeterminate` event
  only)

## Context / Feature Summary

**Layer 1 of the three-layer validation-integrity architecture (ADR-047 v1.3, human-ratified
2026-08-30). Delivered independently of S-21.19–S-21.24 (enforcement seams).**

Operational forensics driving this story (F1-delta-analysis.md §3):
- ~11,262 `plugin.timeout { cause: Fuel }` events in the event log
- ~480 `plugin.timeout { cause: Epoch }` events
- 167 `host_fn_returned_output_too_large` events (read path OutputTooLarge returning −3 to WASM)
- ~455 events where the entire validator suite wiped out on a single artifact edit

Current behavior: all three root causes (fuel exhaustion, epoch timeout, OutputTooLarge) are
treated as PASS by the dispatcher because the plugin "returned" (via the trap mechanism) without
setting a non-zero exit code. This is a PASS misclassification — the correct outcome is
INDETERMINATE ("could not determine"). CWE-754: Improper Check for Exceptional Conditions.

Layer 1 fix:
1. New INDETERMINATE outcome class in the dispatcher (distinct from PASS and FAIL)
2. For fail-closed plugins: write durable `.factory/unvalidated-mutation.marker` + emit
   `plugin.indeterminate` event
3. New `validate-unvalidated-mutation-marker` WASM plugin: TWO hooks-registry entries — Arm 1
   blocks `^Agent$` PreToolUse dispatches while marker exists; Arm 2 blocks `git commit`/
   `git push` Bash PreToolUse dispatches while marker exists
4. Marker clear: PASS re-validation by the named plugin deletes the marker (unblocks both arms)
5. Cohort A fail-closed assignments: all three human-confirmed validators SET to
   `failure_policy = "fail-closed"` in hooks-registry.toml (ADR-047 §D8a v1.4):
   - **Cohort A-immediate — `validate-factory-path-staging`** (ASSIGNED-NOW; corrected v1.21 —
     previously mislabeled EFFECTIVE-NOW, which overclaimed live enforcement): set
     unconditionally; PreToolUse `^Bash$`; `on_error = "continue"`; bounded input (file path
     from tool params); no self-lock risk; no S-21.24 calibration dependency for the assignment.
     Because the durable-marker write path (BC-1.18.001 invariant 4) fires ONLY on a PostToolUse
     dispatch, this PreToolUse-only registration structurally can never reach
     `write_indeterminate_marker` — the assignment currently produces ZERO observable enforcement
     effect, identical to fail-open. This is a genuine coverage gap, not silently accepted:
     closure is tracked to follow-up story S-25.04 ("Close validate-factory-path-staging
     zero-enforcement gap — real Layer-1 production trigger").
   - **Cohort A-deferred — `validate-pr-merge-prerequisites` and
     `validate-wave-gate-prerequisite`** (SET-BUT-LATENT): field SET in registry with comment
     `# ACTIVATION CONDITIONAL on S-21.24 calibration per ADR-047 §D8a Cohort A-deferred`.
     Field is inert (S-21.10 schema-only; ADR-039 Phase 4 enforcement not wired until S-21.24).
     Enforcement activates when S-21.24 merges (confirming S-21.21 calibration + S-21.23
     break-glass). PreToolUse `^Agent$`; `on_error = "block"` means self-lock risk if
     activated without calibration.
   - **Layer-1 effective fail-closed count at S-25.01 merge: ZERO** (corrected v1.21; misstated
     as ONE in v1.1–v1.20). `validate-factory-path-staging` sets `failure_policy = "fail-closed"`
     unconditionally and is safe to set, but the assignment is inert per the structural analysis
     above. The two deferred validators are present in registry but their enforcement is latent
     until S-21.24. All three Cohort A validators currently deliver ZERO effective enforcement at
     S-25.01 merge — see AC-016 and ADR-047 §D8a v1.4.
6. For fail-open plugins (~76 existing): advisory `plugin.indeterminate` event only; no marker;
   no gate; backward-compat guard test MUST NOT be deleted

**Subsystem anchor justifications:**
- SS-01 owns this story's primary scope because SS-01 is the Hook Dispatcher Core (executor.rs,
  invoke.rs, indeterminate_marker.rs) per ARCH-INDEX Subsystem Registry; INDETERMINATE
  classification and marker-write logic are dispatcher core responsibilities
- SS-03 owns Event 8 plugin.indeterminate wire format per ARCH-INDEX Subsystem Registry (SS-03 =
  OTel-aligned event catalog and emission pipeline)
- SS-04 owns the new `validate-unvalidated-mutation-marker` WASM plugin crate per ARCH-INDEX
  Subsystem Registry (SS-04 = Hook Plugin SDK and WASM plugin development)
- SS-07 owns hooks-registry.toml configuration per ARCH-INDEX Subsystem Registry (SS-07 = Hook
  Registry Configuration; Cohort A fail-closed assignments and gate plugin registrations are
  registry-config changes)

**Dependency anchor justification:**
- S-25.01 depends_on [S-21.10] because S-21.10 (MERGED, PR #780) introduced the `FailurePolicy`
  enum and `RegistryEntry.failure_policy` field into the executor's hook invocation path. The
  `classify_outcome(PluginResult, FailurePolicy, bool) -> DispatchOutcome` pure function added
  in this story reads the `failure_policy` field to route INDETERMINATE outcomes to either the
  marker-write path (fail-closed) or the advisory-only path (fail-open). Without the
  `FailurePolicy` field, there is no mechanism to distinguish these two paths at runtime. S-25.01
  does NOT depend on S-21.19–S-21.24 (enforcement seams) — per the human decision recorded in
  the F3 dispatch; independent delivery confirmed.

## Acceptance Criteria

### BC-1.18.001 — INDETERMINATE Outcome Classification, plugin.indeterminate Event, Durable Marker

**AC-001** (traces to BC-1.18.001 postcondition 1 — fuel cause)

When a fail-closed plugin's WASM execution traps and `trap.downcast_ref::<Trap>()` yields
`Some(Trap::OutOfFuel)`, the dispatcher classifies the outcome as `DispatchOutcome::Indeterminate`
with `cause = "fuel"`. `get_fuel()` is NOT the authoritative check — only the Trap downcast is.

**AC-002** (traces to BC-1.18.001 postcondition 1 — epoch timeout cause)

When a fail-closed plugin's WASM execution traps and `trap.downcast_ref::<Trap>()` yields
`Some(Trap::Interrupt)`, the dispatcher classifies the outcome as `DispatchOutcome::Indeterminate`
with `cause = "epoch"`.

**AC-003** (traces to BC-1.18.001 postcondition 1 + invariant 1 — OutputTooLarge cause with
per-invocation Store flag reset)

When any host function returns `OutputTooLarge(-3)` to the plugin WASM during execution (setting
`StoreData::host_output_too_large_seen = true`), AND the plugin subsequently completes with
`exit_code = 0`, AND `failure_policy = fail-closed`, the outcome MUST be
`DispatchOutcome::Indeterminate` with `cause = "output-too-large"`. The `host_output_too_large_seen`
flag MUST be reset to `false` immediately before each WASM module invocation (not only at `Store`
creation) — this is a correctness invariant (per-invocation reset, not per-Store creation reset).

**AC-004** (traces to BC-1.18.001 postcondition 2 + postcondition 5 + invariant 2 — outcome trichotomy; non-exhaustive Trap wildcard)

`DispatchOutcome::Indeterminate` is a distinct variant from `DispatchOutcome::Pass` and
`DispatchOutcome::Fail`. PASS requires `exit_code = 0` AND `host_output_too_large_seen = false`.
FAIL requires non-zero `exit_code`. INDETERMINATE covers the remaining gap. Existing PASS/FAIL
semantics for `PluginResult::Ok`, `PluginResult::Crash`, and normal `PluginResult::Timeout`
variants are UNCHANGED (backward-compat anchor). The Trap match arm in `classify_outcome`
MUST include a wildcard arm `_ => { /* not INDETERMINATE; route to existing on_error handling */ }`
— `Trap` is `#[non_exhaustive]`; a future unrecognized Trap variant MUST NOT be silently bucketed
as INDETERMINATE (BC-1.18.001 invariant 2).

**AC-005** (traces to BC-1.18.001 postcondition 4 + invariant 3 + invariant 4 — atomic marker write; single-marker policy; PostToolUse-only)

When `failure_policy = fail-closed` AND hook type is `PostToolUse` AND outcome is `Indeterminate`,
the dispatcher atomically writes `.factory/unvalidated-mutation.marker` via write-to-temp + rename
(`O_CREAT | O_WRONLY | O_TRUNC` then `rename`). The marker file MUST contain all six required
TOML fields: `timestamp` (RFC 3339), `plugin_name` (string), `artifact_path` (string, empty
string when no artifact context — never omit the field), `cause` (one of `"fuel"`, `"epoch"`,
`"output-too-large"`), `trace_id` (string), `expires_at` (RFC 3339 UTC timestamp = `timestamp
+ UNVALIDATED_MUTATION_MARKER_TTL_SECONDS`; written by `write_indeterminate_marker` at creation;
`UNVALIDATED_MUTATION_MARKER_TTL_SECONDS = 86_400`; enables dispatcher-native TTL auto-clear
(`check_and_clear_expired_marker`) per BC-1.18.003 PC4 and dispatcher native block_if_marker TTL
check per BC-1.18.002 PC5/PC6; BC-1.18.001 PC4 v1.2; ADR-048 §Decision 2). Single-marker policy: a second INDETERMINATE event
overwrites (last-writer-wins). The marker write is PostToolUse only.

**AC-006** (traces to BC-1.18.001 postcondition 3; BC-3.08.001 Event 8 — plugin.indeterminate
event)

**Sink-mechanism correction (v1.13 sync to BC-3.08.001 v1.32 / ADR-048 §Decision 4 — human-ratified,
S-25.01 LOCAL adversary pass 3 F-P3-001 MEDIUM resolution — historical provenance, non-load-bearing):**
the dispatcher emits a
`plugin.indeterminate` event via `HostContext::emit_internal` — the SAME dual-sink helper every
other dispatcher-native BC-3.08.001 event uses — for EVERY INDETERMINATE outcome (both fail-closed
and fail-open). `emit_internal` dual-writes: (a) durably, unconditionally, to
`dispatcher-internal-{date}.jsonl` (via `InternalLog`, when `ctx.internal_log` is `Some` — this is
the only sink every default production dispatcher run writes today, independent of
`observability-config.toml`), and (b) onto the `ctx.events` queue, which a future FileSink/Router
(pending S-4.07) or an operator's opt-in `VSDD_SINK_FILE` will drain into an `events-*.jsonl`-shaped
path. This story MUST NOT claim that a literal `events-*.jsonl` file is durably written today — the
`sinks::Router`/`FileSink` apparatus is not yet wired into `main.rs`. The event wire format MUST
include all mandatory fields per BC-3.08.001 Event 8: `type` ("plugin.indeterminate"),
`trace_id`, `session_id`, `plugin_name`, `artifact_path` (empty string when no artifact context),
`cause` (`"fuel"` | `"epoch"` | `"output-too-large"`), `failure_policy` (`"fail-closed"` or
`"fail-open"`), `timestamp`. BC-3.08.001 is the wire-format authority; BC-1.18.001 is the
triggering-condition authority.

### BC-1.18.002 — Next-Advance Gate: Two-Arm Quarantine

**AC-007** (traces to BC-1.18.002 postcondition 1 + invariant 4 — Agent arm block; block message completeness)

When `.factory/unvalidated-mutation.marker` exists AND a `PreToolUse` hook fires for an `^Agent$`
dispatch, the `validate-unvalidated-mutation-marker` WASM plugin returns `exit_code = 2`
(block). The block message MUST contain: the `plugin_name` from the marker, the `artifact_path`
from the marker, the `cause` from the marker, a re-validation instruction (T1 — Edit/Write
re-validate the artifact named in `artifact_path`; the primary, inherently-ungated agent recovery
path per AC-020 — NOT a "re-invoke the named plugin" dispatcher-command phrasing), and the manual
escape hatch instruction (`rm .factory/unvalidated-mutation.marker`, run by the human operator in
their own terminal — T3 per AC-020). The message MUST be machine-parseable (structured fields,
not freeform prose).

**AC-008** (traces to BC-1.18.002 postcondition 2 + invariant 4 — Bash arm block: git commit/push Phase 1/1b/2/3/4 security filter algorithm (v1.3); quoting-in-scope; block message completeness)

When `.factory/unvalidated-mutation.marker` exists AND a `PreToolUse` hook fires for a `^Bash$`
dispatch for which `is_git_commit_or_push(command)` returns `true`, the
`validate-unvalidated-mutation-marker-git` WASM plugin returns `exit_code = 2` (block). The
block message format is identical to AC-007.

`is_git_commit_or_push(command) → bool` is the authoritative production-grade security filter
(BC-1.18.002 PC2 v1.3; ADR-047 §Decision 9). Under-blocking is the dangerous failure mode;
the algorithm is fail-safe on ambiguity:

- **Phase 1 — Compound command splitting.** Split `command` on `&&`, `||`, `;`, `|`, `&`, and
  newline. Trim each segment. Apply Phases 1b–4 to each segment independently; return `true` if
  ANY segment returns `true`.
- **Phase 1b — POSIX quote-aware tokenization.** Before applying Phases 2–4, tokenize each
  segment from Phase 1 using POSIX word-splitting rules via the `shell-words` crate. Quote
  removal and token concatenation occur at this phase — Phases 2–4 operate on the resulting
  token list, not on the raw whitespace-split string. Examples: `git "commit"` → tokens
  `["git", "commit"]`; `git 'push' origin` → tokens `["git", "push", "origin"]`; `g'i't commit`
  → tokens `["git", "commit"]` (POSIX concatenates unquoted `g`, single-quoted `i`, and unquoted
  `t` into the token `git`). Do NOT implement ad-hoc quote-stripping (e.g., `s/["']//g` regex)
  — the `shell-words` crate provides POSIX parser-equivalence, eliminating the parser-differential
  bypass class where ad-hoc stripping could produce different tokenization than the actual shell.
- **Phase 2 — Executable identification by basename.** Strip leading env-var assignment tokens
  (matching `^[A-Za-z_][A-Za-z0-9_]*=`) and the literal `env` token if it is the first token.
  Compute `basename` of the first remaining token (strip all characters up to and including the
  last `/`). If `basename ≠ "git"` (case-sensitive), return `false` for that segment.
- **Phase 3 — Skip global options.** Scan tokens after `git`: `--` ends Phase 3 (next token is
  subcommand); `--flag=value` skips one token; tokens in the **complete recognized arg-taking set**
  (`-C`, `-c`, `--namespace`, `--git-dir`, `--work-tree`, `--super-prefix`, `--config-env`) skip
  that token AND the immediately following token; tokens in the recognized no-arg set are skipped;
  an unrecognized token starting with `-` triggers **fail-safe → return `true` (block)** — arity
  is unknown from the closed recognized set, and under-blocking is the dangerous failure mode.
- **Phase 4 — Subcommand match.** Return `true` iff the candidate subcommand is exactly `commit`
  or `push` (case-sensitive, full-token match). `commit-graph`, `commit-tree`, `push-pack`, and
  any other token that merely contains `commit` or `push` as a substring do NOT match.

The illustrative regex `\bgit\b.*\b(commit|push)\b` approximates this rule but is NOT
authoritative — it false-positives on `git commit-graph write` because the hyphen in `commit-graph`
is a word boundary making `\bcommit\b` match inside a different subcommand token (EC-011); it
cannot detect the gated subcommand in compound commands (EC-015/EC-016); and it does not enforce
basename matching (EC-017/EC-018). The Phase 1/1b/2/3/4 algorithm above is the sole authoritative
specification.

Commands that MUST be blocked (non-exhaustive): `git commit --amend` (EC-006), `git push
--force-with-lease` (EC-007), `git --git-dir .git commit -m "state"` (EC-013), `git --work-tree
/tmp push` (EC-014), `git status && git commit -m "x"` (EC-015), `git diff ; git push` (EC-016),
`/usr/bin/git commit -m "init"` (EC-017), `./git push origin main` (EC-018), `env GIT_DIR=.git
git commit -m "x"` (EC-021), `git --unknown-flag commit` (EC-022, fail-safe), `git --config-env
FOO=BAR commit` (EC-023), `git "commit"` (EC-024, Phase 1b quoting), `git 'push' origin`
(EC-025, Phase 1b quoting), `g'i't commit` (EC-026, Phase 1b POSIX token concatenation).

The following are OUT OF SCOPE by design — blocking these forms is UNDECIDABLE for static string
analysis (Rice's theorem). They are ALLOWED under the fail-open posture; the durable marker
written at PostToolUse time, the `^Agent$` next-advance arm, and GitHub server-side branch
protection provide defense-in-depth. See BC-1.18.002 Threat Model & Scope for the canonical
actor model (cooperating AI agent, NOT adversary-resistant boundary): `$(git commit -m "x")`
(EC-027, command substitution), `echo x | xargs git commit` (EC-028, xargs indirection),
`eval "git push origin main"` (EC-029, dynamic evaluation), and variable/alias/function
indirection forms.

**AC-009** (traces to BC-1.18.002 postcondition 3 — non-advancing not gated; out-of-scope forms documented)

Bash dispatches for which `is_git_commit_or_push(command)` returns `false` are NOT gated even
when the marker exists. Read, Edit, Write, MultiEdit tool dispatches are NOT gated.

The v1.3 algorithm (authoritative per BC-1.18.002 PC2 v1.3; ADR-047 §Decision 9; full
definition in AC-008) is the load-bearing pure filter. Key NOT-gated behaviors:

- **Phase 1 (compound splitting):** A compound command is only NOT gated if ALL segments return
  `false`; if any segment resolves to `commit` or `push`, the function returns `true` (blocked).
- **Phase 1b (POSIX quote-aware tokenization):** The `shell-words` crate tokenizes each segment.
  Quoted forms that resolve to `commit` or `push` after quote removal ARE blocked (e.g.,
  `git "commit"` → BLOCKED per EC-024; `git 'push' origin` → BLOCKED per EC-025; `g'i't commit`
  → BLOCKED per EC-026). This phase is what differentiates v1.3 from v1.2.
- **Phase 2 (basename):** A command is NOT gated if `basename(executable) ≠ "git"` (e.g.,
  `cat gitfile` — `basename("cat") = "cat" ≠ "git"`, returns `false`).
- **Phase 3–4 (option skip + subcommand match):** The recognized arg-taking global-option set
  (`-C`, `-c`, `--namespace`, `--git-dir`, `--work-tree`, `--super-prefix`, `--config-env`) each
  consume the following token as their argument before subcommand identification. Recognized no-arg
  flags are skipped without consuming a following token. An unrecognized `-`-prefixed token
  triggers fail-safe → `true` (blocked). Only exact tokens `commit` or `push` are gated.

The illustrative regex `\bgit\b.*\b(commit|push)\b` is NOT the authoritative rule — see AC-008.

BC-1.18.002 EC-001 through EC-005 MUST pass (non-advancing commands — `git status`, `git log`,
`git diff`, `git fetch`, non-git commands — `is_git_commit_or_push` returns `false`).
Additionally:
- `git commit-graph write` → NOT blocked: Phase 4 full-token match; subcommand is `commit-graph`,
  not `commit` (EC-011).
- `git -C .factory commit -m "state"` → BLOCKED: Phase 3 recognizes `-C` as arg-taking; `.factory`
  consumed as argument; Phase 4 subcommand is `commit` (EC-012).
- `git --git-dir .git commit -m "state"` → BLOCKED: Phase 3 `--git-dir` arg-taking; `.git`
  consumed; Phase 4 subcommand is `commit` (EC-013).
- `git --work-tree /tmp push` → BLOCKED: Phase 3 `--work-tree` arg-taking; `/tmp` consumed;
  Phase 4 subcommand is `push` (EC-014).
- `git status && git commit -m "x"` → BLOCKED: Phase 1 splits on `&&`; segment 2 subcommand is
  `commit`; any-segment `true` → function returns `true` (EC-015).
- `git diff ; git push` → BLOCKED: Phase 1 splits on `;`; segment 2 subcommand is `push` (EC-016).
- `/usr/bin/git commit -m "init"` → BLOCKED: Phase 2 `basename("/usr/bin/git") = "git"`;
  Phase 4 subcommand `commit` (EC-017).
- `./git push origin main` → BLOCKED: Phase 2 `basename("./git") = "git"`; Phase 4 subcommand
  `push` (EC-018).
- `git status && git log --oneline` → NOT blocked: Phase 1 splits; segment 1 subcommand `status`,
  segment 2 subcommand `log`; no segment matches `commit` or `push`; returns `false` (EC-019).
- `env GIT_DIR=.git git commit -m "x"` → BLOCKED: Phase 2 strips `env` + `GIT_DIR=.git`;
  executable is `git`; Phase 4 subcommand `commit` (EC-021).
- `git --unknown-flag commit` → BLOCKED (fail-safe): Phase 3 unrecognized option, no `=`,
  subcommand position uncertain; returns `true` (EC-022).
- `git --config-env FOO=BAR commit` → BLOCKED: Phase 3 `--config-env` arg-taking; `FOO=BAR`
  consumed; Phase 4 subcommand `commit` (EC-023).

**Phase 1b quoting (v1.3 additions — IN SCOPE):**
- `git "commit"` → BLOCKED: Phase 1b POSIX tokenization; `"commit"` → token `commit`; Phase 4
  match (EC-024).
- `git 'push' origin` → BLOCKED: Phase 1b POSIX tokenization; `'push'` → token `push`; Phase 4
  match (EC-025).
- `g'i't commit` → BLOCKED: Phase 1b POSIX token concatenation; `g'i't` → token `git`; Phase 2
  basename `git`; Phase 4 match (EC-026).

**Out-of-scope forms (NOT blocked by design — BC-1.18.002 Threat Model & Scope; defense-in-depth):**
- `$(git commit -m "x")` → NOT blocked: outer command is a command substitution, not a
  statically-literal `git` invocation; undecidable for static analysis; caught by other controls
  (EC-027).
- `echo x | xargs git commit` → NOT blocked: Phase 1 splits on `|`; segment 2 executable is
  `xargs` (basename `xargs` ≠ `git`); `git commit` is a string argument to `xargs`, not the
  top-level executable; returns `false` (EC-028).
- `eval "git push origin main"` → NOT blocked: Phase 2 executable is `eval` (basename `eval` ≠
  `git`); `git push` is a string argument subject to dynamic evaluation; undecidable for static
  analysis; caught by other controls (EC-029).

**AC-010** (traces to BC-1.18.002 postcondition 4 — absent marker: both arms pass)

When `.factory/unvalidated-mutation.marker` is absent, both Arm 1 (`^Agent$`) and Arm 2
(`^Bash$` git commit/push) return `exit_code = 0` (allow).

**AC-011** (traces to BC-1.18.002 precondition 1 + invariant 2 + postcondition 5 + postcondition 6 — gate registers on_error="block_if_marker" (fail-closed-but-recoverable on crash); two-axis model; ADR-048 §Decision 1; D-1135 reversed)

Both `validate-unvalidated-mutation-marker` hook entries in `hooks-registry.toml` MUST have
`on_error = "block_if_marker"` (NOT `"continue"` or `"block"`) AND `failure_policy = "fail-open"`.
`failure_policy = "fail-open"` is UNCHANGED from v1.3 (two-axis model — see below).

**Crash behavior (`block_if_marker`):** When the WASM gate plugin itself cannot complete at the
dispatcher layer (fuel exhaustion, epoch timeout, crash), the dispatcher executes a NATIVE
(non-WASM) marker-presence-and-TTL check — this supersedes the v1.3 `on_error = "continue"`
unconditional-allow-on-crash posture (ADR-048 §Decision 1; D-1135 reversed):
- **BLOCK (exit_code=2):** iff a non-expired `.factory/unvalidated-mutation.marker` exists
  (`expires_at > now UTC`; BC-1.18.002 PC5; story EC-033). Block message includes marker
  fields (`plugin_name`, `artifact_path`, `cause`, `expires_at`) plus three recovery options
  ordered by sanctioned preference (MUST NOT instruct agent to run `rm` — agent-tool rm is
  de-sanctioned per INV6/T4; AC-020): (a) Edit/Write re-validate the artifact named in
  `artifact_path` (T1 — primary agent recovery: inherently ungated, works even during gate
  crash; wait for PostToolUse PASS to clear the marker per BC-1.18.003 PC1); (b) wait for
  TTL expiry — 86400s from `expires_at` timestamp (T2 — passive self-healing); (c) human
  operator runs `rm .factory/unvalidated-mutation.marker` in their own terminal (T3 —
  break-glass; never intercepted by dispatcher; BC-1.18.002 v1.6 PC5).
- **ALLOW (exit_code=0):** if the marker is absent OR TTL-expired (`expires_at ≤ now UTC`) OR
  the crash-handler's own file-read fails (ENOENT → treat as absent; BC-1.18.002 PC6;
  story EC-034). No unconditional self-lock is possible: a crash with no valid quarantine
  signal cannot block indefinitely.

The self-lock hazard (INV2) is avoided by the no-valid-quarantine-signal → Allow branch, NOT
by unconditional allow. Three independent recoverability guarantees (INV6) ensure the system
is never in an irrecoverable quarantine state — see AC-020.

**Two-axis model (ADR-039; ADR-047 §Decision 9; ADR-048 §Decision 1):** Two orthogonal
failure-mode axes govern the gate and MUST NOT be conflated:
- **(i) `block_if_marker` crash policy** (`on_error = "block_if_marker"`): when the WASM plugin
  itself fails at the dispatcher layer — crash, fuel exhaustion, epoch timeout — the dispatcher
  executes the native marker-presence-and-TTL check (Block iff non-expired marker; Allow
  otherwise). This supersedes the v1.3 unconditional-allow-on-crash (`on_error = "continue"`)
  posture. `failure_policy = "fail-open"` governs a separate axis (the INDETERMINATE outcome
  of the gate plugin itself — see BC-1.18.004) and is orthogonal to `on_error`.
- **(ii) Filter-internal fail-SAFE posture** (Phase 3 of `is_git_commit_or_push`): when the
  filter encounters an UNRECOGNIZED git global option, it returns BLOCK (subcommand position is
  uncertain; under-blocking is the dangerous failure mode). This is a conservative within-filter
  design choice, orthogonal to axis (i): the filter's internal posture has no bearing on what the
  dispatcher does when the WASM plugin itself crashes.

### BC-1.18.002 Invariant 6 — Ungated-Escape Invariant

**AC-020** (traces to BC-1.18.002 invariant 6 — ungated-escape invariant; four-tier recovery
model; ADR-048 §Decision 3; VP-107)

The system is NEVER in an irrecoverable quarantine state. Three independent recovery tiers
(T1/T2/T3) guarantee the quarantine is always escapable. A fourth path (T4) is de-sanctioned.
The guaranteed recovery is T1+T2+T3, NOT agent-tool rm (ADR-048 §Decision 3 stable anchor):

- **(T1 — Primary agent recovery) Edit/Write to the artifact (re-validation dispatch):**
  Edit and Write tool dispatches do not match `^Agent$` (Arm 1) OR `^Bash$` (Arm 2). No arm
  fires; the crash-path native `block_if_marker_check` is not invoked for Edit/Write dispatches
  (no matched arm → no crash check). This path is INHERENTLY ungated: invariant to the gate
  plugin's crash behavior, to the marker's presence or expiry state, and to any `on_error` value
  on any arm. Confirmed ungated by construction (BC-1.18.002 PC3/AC-009). VP-107 verifies T1
  explicitly. This is the agent's genuine primary recovery path — re-validate the artifact named
  in `artifact_path` via Edit/Write, then wait for the PostToolUse gate plugin to produce PASS
  and clear the marker (BC-1.18.003 PC1; AC-012 + AC-022).

- **(T2 — Passive recovery) TTL auto-expiry (no dispatch required):**
  After `UNVALIDATED_MUTATION_MARKER_TTL_SECONDS` (86400s) have elapsed since the `expires_at`
  timestamp, any subsequent gate evaluation on the normal path finds `expires_at ≤ now (UTC)` →
  Allow + auto-delete (BC-1.18.003 PC4; AC-021). Any crash-path evaluation also finds marker
  expired → Allow (AC-011 PC6). No operator dispatch is required — passive self-healing.

- **(T3 — Human break-glass) Operator `rm` in own terminal:**
  The PreToolUse hook chain is invoked only when the AI agent calls a tool through the
  dispatcher. A terminal `rm .factory/unvalidated-mutation.marker` executed by the human
  operator in their own shell is NOT a dispatcher-mediated agent tool call and is NEVER
  intercepted by the hook chain. This is the break-glass pattern: the human principal, acting
  outside the mediation boundary, retains unconditional filesystem access.

- **(T4 — Agent-tool `rm`, DE-SANCTIONED) NOT a recovery path the invariant relies upon:**
  On the non-crash path, `is_git_commit_or_push("rm .factory/...")` returns `false`
  (`basename("rm") = "rm" ≠ "git"`) → Arm 2 does NOT gate the dispatch on the normal path.
  However, on the CRASH PATH (`on_error = "block_if_marker"`): when the `^Bash$` gate plugin
  crashes, the dispatcher's native `block_if_marker_check` runs for ALL `^Bash$` dispatches —
  including `rm .factory/unvalidated-mutation.marker` — with no `is_git_commit_or_push` filter.
  If a non-expired marker is present, the native check BLOCKS the `rm` dispatch. This is
  ACCEPTABLE — NOT an INV6 violation — because: (1) T1 (Edit/Write re-validation) is the agent's
  genuine ungated primary recovery; (2) a command-filter to whitelist `rm` in the crash handler
  is UNSOUND per Rice's theorem (same undecidable class as the `is_git_commit_or_push` scope,
  already accepted out-of-scope) and UNNECESSARY given T1; (3) allowing the agent to erase its
  own quarantine constraint in an anomalous crash state would be a CWE-636 regression.

**VP-107 scope:** VP-107 verifies T1 — that Edit/Write tool dispatch does NOT match either gate
arm's tool pattern (`^Agent$`, `^Bash$`); no arm fires; the `block_if_marker` crash-path native
check is never invoked for Edit/Write. VP-107 does NOT verify "agent-tool rm is never gated"
(that claim is de-sanctioned under T4). T3 is verified by architectural argument (human shell
outside dispatcher mediation). T2 is covered by the TTL invariant test.

### BC-1.18.003 — Marker Clear Protocol

**AC-012** (traces to BC-1.18.003 postcondition 1 + invariant 2 — re-validation clear; scoped to named plugin AND artifact path; PostToolUse hook only)

When the plugin whose `plugin_name` matches the marker's `plugin_name` field subsequently
produces `DispatchOutcome::Pass` (i.e., exit_code = 0 AND `host_output_too_large_seen = false`)
**in a PostToolUse hook invocation**, the dispatcher calls `delete_marker_if_pass()` which deletes
`.factory/unvalidated-mutation.marker`. The clear is artifact-scoped: BOTH conditions must hold —
(a) the dispatched plugin's `plugin_name` equals the marker's `plugin_name` field, AND (b) the
current dispatch's artifact path matches the marker's `artifact_path` field (exact string equality
of the normalized absolute path — both resolved to absolute form before comparison). **Empty-path
fallback (BC-1.18.003 EC-009):** if the marker's `artifact_path` is empty (the failing plugin was
a non-artifact-scoped validator — e.g., a wave-gate or path-staging validator — with no single
associated file), the artifact condition is vacuously satisfied and plugin_name equality alone
suffices for clearance. If the same plugin produces PASS on a DIFFERENT non-empty artifact, the
marker is NOT cleared — artifact A's quarantine persists until A is successfully re-validated by
the named plugin (story EC-031; BC-1.18.003 EC-008). A PreToolUse PASS result from the named
plugin MUST NOT clear the marker. Other plugins' PASS results do not clear the marker regardless
of hook type.

**AC-013** (traces to BC-1.18.003 postcondition 2 — idempotent delete)

`delete_marker_if_pass()` is idempotent: if `.factory/unvalidated-mutation.marker` is absent at
delete time, the function returns `Ok(())` (no-op). `io::ErrorKind::NotFound` is silently
swallowed. All other `io::Error` variants are propagated as `Err`.

**AC-014** (traces to BC-1.18.002 invariant 3 + BC-1.18.003 postcondition 3 + BC-1.18.003 invariant 3 — operator rm escape hatch; both arms unblocked simultaneously)

`rm .factory/unvalidated-mutation.marker` is the fully supported operator escape hatch. No
special command is required. After the rm, both Arm 1 and Arm 2 unblock simultaneously (both
check for the marker's presence at runtime; absent marker → pass).

### BC-1.18.003 Postcondition 4 — TTL-Expiry Clear Path

**AC-021** (traces to BC-1.18.003 postcondition 4 + invariant 5 — TTL-expiry clear path;
marker-level deadman; TTL-loudness audited event; ADR-048 §Decision 2 + §Decision 4 —
Emission-Point Correction, introduced by the F-P2-002 finding, human-ratified 2026-08-31 per
ADR-048's own revision history — historical provenance, non-load-bearing)

**Emission-point correction (v1.12 sync to BC-1.18.003 v1.4 / ADR-048 §Decision 4 — human-ratified,
S-25.01 LOCAL adversary pass 2 F-P2-002 HIGH resolution — historical provenance, non-load-bearing):**
TTL detection, auto-delete, and the
audited `marker.cleared` emission are performed ENTIRELY dispatcher-native by
`check_and_clear_expired_marker(factory_root, now, ctx: &HostContext) -> io::Result<Option<MarkerFields>>`
in `crates/factory-dispatcher/src/indeterminate_marker.rs`, called from `executor.rs`'s
tier-execution loop for every registry entry with `on_error == OnError::BlockIfMarker` BEFORE
that entry's Arm 1/Arm 2 WASM gate plugin (`evaluate_gate`) is invoked, on the normal (non-crash)
path. **Signature correction (v1.13 sync to BC-1.18.003 v1.5 / ADR-048 §Decision 4 — human-ratified,
S-25.01 LOCAL adversary pass 3 F-P3-001 MEDIUM resolution — historical provenance, non-load-bearing):**
the function takes `ctx: &HostContext`
(not a bare `log: &InternalLog, session_id: &str` pair) — `base_ctx: &HostContext` is already in
scope at the `executor.rs` call site, so this is a signature-widening refactor, not new plumbing.
`check_and_clear_expired_marker` MUST:
1. Read `.factory/unvalidated-mutation.marker` via `read_all_marker_fields`.
2. If present and `expires_at ≤ now (UTC)` (TTL elapsed): delete the marker file (idempotent;
   `io::ErrorKind::NotFound` swallowed; other IO errors logged but do NOT panic — auto-delete
   failure must not block the Allow return), then immediately call
   `emit_marker_cleared(ctx, &fields, "TTL_EXPIRED", "deadman", None)`, which internally routes the
   `marker.cleared` event through `ctx.emit_internal` (dual-sink: durable
   `dispatcher-internal-{date}.jsonl` write + `ctx.events` queue — NEVER a raw `InternalLog::write`
   call, per ADR-048 §Decision 4 (F-P3-001 correction — historical provenance, non-load-bearing)) with: `clear_mode = "TTL_EXPIRED"`, `actor_type =
   "deadman"`, `trace_id` from marker TOML `trace_id` field, `plugin_name` from marker TOML,
   `artifact_path` from marker TOML, `reason = null`, `timestamp` = time of the clear event, and
   return `Some(fields)` — signaling the caller (`executor.rs`) to treat the dispatch as Allow
   (`exit_code = 0`) without invoking the WASM gate plugin at all for this determination.
3. Otherwise (marker absent, or present and non-expired) return `None` without deleting.

By the time `evaluate_gate` subsequently runs (only reached when `check_and_clear_expired_marker`
returned `None`), the marker is guaranteed either absent or non-expired: `evaluate_gate` performs
NO `expires_at` parsing, deletion, or emission of its own — it is a PURE marker-presence check
(Block if present, Allow if absent). **TTL-loudness (ADR-048 §Decision 4):** the previously silent
auto-delete is now audited; emission is via `emit_marker_cleared(ctx, ...)`, called from
`check_and_clear_expired_marker` immediately after the native `std::fs::remove_file` succeeds,
routing through `ctx.emit_internal` (never a raw `InternalLog::write`; v1.13/ADR-048 §Decision 4,
F-P3-001 correction — historical provenance, non-load-bearing) —
and never from inside the WASM gate plugin via the `emit_event` host ABI, because the ABI's
RESERVED_FIELDS enrichment makes it structurally impossible for a WASM plugin to emit an event
carrying the marker's own (foreign) `trace_id`/`plugin_name` rather than its own dispatch identity
(BC-1.18.003 v1.5 PC4). The Allow+auto-delete decision is verified by VP-106; the
`marker.cleared(TTL_EXPIRED)` emission itself is verified by VP-108.

`UNVALIDATED_MUTATION_MARKER_TTL_SECONDS = 86_400` (24-hour deadman constant defined in
`crates/factory-dispatcher/src/indeterminate_marker.rs`). `expires_at` is the RFC 3339 UTC
timestamp written by `write_indeterminate_marker` at marker creation as `timestamp + 86400s`
(BC-1.18.001 PC4 v1.2; ADR-048 §Decision 2). Both Arm 1 (Agent) and Arm 2 (git commit/push)
are immediately unblocked by TTL expiry — same as marker absence.

**Crash-path TTL-allow does NOT emit `marker.cleared`:** the dispatcher's NATIVE crash-path TTL
check (AC-011 PC6 — crash + expired marker → Allow, `block_if_marker_check`) does NOT emit
`marker.cleared(TTL_EXPIRED)`. Only the dispatcher-native normal-path pre-check
(`check_and_clear_expired_marker`) emits it — `block_if_marker_check` (Decision 1) and
`check_and_clear_expired_marker` (this AC) are two distinct dispatcher-native functions; a crash
means the normal-path pre-check either did not run or was interrupted before completing. The
marker remains until the next normal-path pre-check clears it with the audited event
(BC-1.18.003 EC-014).

**Backward compatibility — legacy markers (missing `expires_at`):** Markers written before
ADR-048 implementation lack the `expires_at` field. `check_and_clear_expired_marker` MUST treat a
missing `expires_at` field as NON-EXPIRED (conservative; no silent auto-clear of old markers),
returning `None` and leaving the marker in place — the subsequent `evaluate_gate` then sees a
still-present marker and Blocks unconditionally, with no `expires_at` awareness required. Such
markers remain in effect until explicitly cleared via `rm` (PC3/AC-014) or successful
re-validation (PC1/AC-012).

**TTL is marker-level (INV5):** TTL expiry operates solely on the `expires_at` timestamp; it
does NOT check `plugin_name` or `artifact_path`. This is orthogonal to the artifact-scoped
re-validation clear (PC1/AC-012), which requires (a) plugin_name match AND (b) artifact_path
match. Story EC-035 covers this path.

### BC-1.18.003 Audited Clear Paths — marker.cleared Event Emissions (BC-3.08.001 Event 9)

**AC-022** (traces to BC-1.18.003 postcondition 1 + BC-3.08.001 Event 9 — REVALIDATED clear
path emits marker.cleared; trace_id links to originating plugin.indeterminate; ADR-048 §Decision 4)

Immediately after `delete_marker_if_pass` successfully removes `.factory/unvalidated-mutation.marker`
via the re-validation PASS clear path (AC-012), the dispatcher MUST emit `marker.cleared`
(BC-3.08.001 Event 9) with all mandatory fields:
- `type = "marker.cleared"`
- `clear_mode = "REVALIDATED"`
- `actor_type = "validator"`
- `trace_id` = trace_id from the originating `plugin.indeterminate` event (read from the marker
  TOML `trace_id` field before deletion; links this clear event back to the originating
  indeterminate event for operator correlation)
- `plugin_name` from marker TOML
- `artifact_path` from marker TOML
- `reason = null` (null for REVALIDATED clear)
- `timestamp` = time of the clear event

Emission is via `emit_marker_cleared(ClearMode::Revalidated, ...)` in
`crates/factory-dispatcher/src/indeterminate_marker.rs`, called from `delete_marker_if_pass`
after `std::fs::remove_file` succeeds. The `trace_id` link enables operator correlation across
the `plugin.indeterminate` → marker.write → `marker.cleared` lifecycle.

**AC-023** (traces to BC-1.18.003 postcondition 3 + BC-3.08.001 Event 9/Event 10 — OPERATOR_OVERRIDE
retroactive reconciliation emits marker.cleared; best-effort; bounded scan; RAW_DELETE_DETECTED;
non-fabrication; ADR-048 §Decision 4 — cumulative corrections: Emission-Point Correction
(F-P2-003, human-ratified 2026-08-31), Emission-Mechanism Precision Correction (F-P3-001,
human-ratified 2026-09-01), and Reconciliation-Premise Correction (F-P6-001, human-ratified
2026-09-01, POLICY 22, D-1141) — historical provenance, non-load-bearing)

**Emission-point correction (v1.12 sync to BC-1.18.003 v1.4 / ADR-048 §Decision 4 — human-ratified,
S-25.01 LOCAL adversary pass 2 F-P2-003 MED resolution — historical provenance, non-load-bearing):**
OPERATOR_OVERRIDE/RAW_DELETE_DETECTED
reconciliation is implemented ENTIRELY dispatcher-native by `reconcile_raw_delete(factory_root: &Path,
ctx: &HostContext) -> io::Result<()>` in `crates/factory-dispatcher/src/indeterminate_marker.rs`,
invoked from the SAME dispatcher-native pre-check as AC-021 (`check_and_clear_expired_marker`'s
marker-absent branch) — never by the WASM gate plugin, which is structurally unable to emit
`marker.cleared` carrying a foreign `trace_id`/`plugin_name` from inside the `emit_event` host
ABI's RESERVED_FIELDS enrichment wall (same root cause as AC-021's correction).

**Signature + scan-target correction (v1.13 sync to BC-1.18.003 v1.5 / ADR-048 §Decision 4 —
human-ratified, S-25.01 LOCAL adversary pass 3 F-P3-001 MEDIUM resolution — historical
provenance, non-load-bearing):** the function takes
`ctx: &HostContext` (not a bare `sink: &FileSink` parameter) — `ctx.internal_log` supplies the log
directory. When `check_and_clear_expired_marker` finds the marker absent, the caller invokes
`reconcile_raw_delete`, which performs a bounded, best-effort scan of
`<InternalLog log_dir>/dispatcher-internal-{date}.jsonl` (obtained via
`ctx.internal_log.as_ref().map(|l| l.log_dir())`, with `None` short-circuiting to a no-op exactly
like `emit_internal`'s own guard) — NOT a literal `events-{date}.jsonl` as prior versions of this
AC named.

**Scan match-type + reconciliation-premise correction (v1.14 sync to BC-1.18.003 v1.6 / ADR-048
§Decision 4 — S-25.01 adversary pass 6 F-P6-001 MEDIUM resolution; HUMAN-RATIFIED 2026-09-01,
POLICY 22, D-1141 — historical provenance, non-load-bearing):** the scan MUST match on an
unmatched **`marker.written`** record (BC-3.08.001
Event 10 — AC-025), NOT an unmatched `plugin.indeterminate` record, for the same
`(plugin_name, artifact_path)` pair with no corresponding `marker.cleared` (RAW_DELETE_DETECTED —
operator T3 out-of-band rm). `plugin.indeterminate` (Event 8) is emitted for EVERY INDETERMINATE
outcome regardless of hook phase (fail-closed and fail-open, PreToolUse and PostToolUse alike),
while `marker.written` (Event 10) is emitted ONLY after `write_indeterminate_marker` returns
`Ok(())` (AC-025; BC-1.18.001 postcondition 4 v1.4). Matching on `plugin.indeterminate` was
unsound: a PreToolUse fail-closed INDETERMINATE never attempts a marker write at all (marker write
is PostToolUse-only, BC-1.18.001 invariant 4), and a PostToolUse marker-write I/O failure (story
EC-008) leaves the identical no-marker-ever-existed footprint — both produce an unmatched
`plugin.indeterminate` with no marker ever written, which the pre-v1.14 scan would misread as a
raw delete and fabricate a `marker.cleared(OPERATOR_OVERRIDE)` for. `marker.written`'s existence is
proof-by-construction that a marker was actually, durably written, so an unmatched `marker.written`
is a sound RAW_DELETE_DETECTED signal — this makes the reconciliation premise TRUE BY CONSTRUCTION,
not merely narrowed. `dispatcher-internal-{date}.jsonl` remains the ONLY log every default
production dispatcher run writes unconditionally; the FileSink/Router apparatus that would produce
a literal `events-*.jsonl` file remains unwired pending S-4.07 — scanning a file that is not
durably written would make RAW_DELETE_DETECTED permanently inert in every real deployment. If
found, `reconcile_raw_delete` calls `emit_marker_cleared(ctx, &fields, "OPERATOR_OVERRIDE",
"operator", Some(reason))`, which routes through `ctx.emit_internal` (dual-sink: durable
`dispatcher-internal-{date}.jsonl` write + `ctx.events` queue; never a raw `InternalLog::write`)
with a retroactive `marker.cleared` (BC-3.08.001 Event 9):
- `clear_mode = "OPERATOR_OVERRIDE"`
- `actor_type = "operator"`
- `trace_id` = trace_id from the unmatched `marker.written` event (v1.14 — previously sourced from
  the unmatched `plugin.indeterminate` event; same field name, corrected source record)
- `reason = "RAW_DELETE_DETECTED: marker absent without prior marker.cleared event; inferred operator out-of-band clear"` (MUST be non-null for OPERATOR_OVERRIDE)
- `timestamp` = current evaluation time (deletion time is unobservable; operator shell is outside dispatcher mediation)
- `plugin_name` and `artifact_path` = likewise read from the unmatched `marker.written` event's own fields

**Non-fabrication (v1.14, ADR-048 §Decision 4 — F-P6-001 fix, human-ratified 2026-09-01 —
historical provenance, non-load-bearing):** a fail-closed INDETERMINATE that
NEVER wrote a marker — either a PreToolUse dispatch (no write is ever attempted, BC-1.18.001
invariant 4) or a PostToolUse dispatch whose `write_indeterminate_marker` call returned `Err(_)`
(story EC-008) — produces no `marker.written` event, so `reconcile_raw_delete` finds no unmatched
`marker.written` for that `(plugin_name, artifact_path)` pair and MUST emit ZERO
`marker.cleared(OPERATOR_OVERRIDE)` events for it — no fabricated NIST AU-3/AU-10 audit record.
This is the direct regression test for F-P6-001 (BC-1.18.003 v1.6 EC-017; BC-3.08.001 Event 10;
VP-108 postcondition 7). See story EC-036 for the canonical scenario.

**Bounded scan (production-grade requirement, ADR-048 §Decision 4 — MANDATORY, not
optional; requirement has held since its introduction and through subsequent §Decision 4
corrections, historical provenance non-load-bearing):** because `reconcile_raw_delete` runs on
the common marker-absent path before EVERY
Arm 1 (`^Agent$`) and Arm 2 (`^Bash$` git commit/push) dispatch, the scan MUST be bounded to avoid
unbounded I/O growth on the hot dispatch path: implementations MUST cap the scan to the CURRENT
DAY'S `dispatcher-internal-{date}.jsonl` file only (never scan prior days' files) AND MUST cap
total bytes/records read (a fixed-size tail read of the current day's file, not a full-file scan).
An unbounded scan on every Agent/git-commit dispatch would reintroduce the same class of
large-artifact resource cost that S-25.01 exists to eliminate (F1-delta-analysis §3). The exact
byte/record cap and any memoization strategy (e.g., a lightweight on-disk checkpoint to avoid
re-scanning already-reconciled ranges across dispatcher process invocations) are implementation
details, not fixed by the ADR or this AC — the implementer MUST choose and document a concrete
bound (e.g., last N KB or last N lines of the current day's file) satisfying the two MUST
constraints above.

This reconciliation step never gates the dispatch decision (BC-3.08.001 Invariant 3) — it MAY run
either synchronously before the Allow result is returned or as a best-effort step after, at
implementer's discretion.

**Best-effort:** if `ctx.internal_log` is `None` (the `dispatcher-internal-{date}.jsonl` log is
unavailable) or the unmatched record cannot be found, the annotation is omitted with no hard
failure. An unreconciled gap is observable by monitoring for `marker.written` events without
subsequent `marker.cleared` (v1.14 — previously `plugin.indeterminate`). VP-108 postcondition 3
covers this path's positive reconciliation (clear-path emission correctness); VP-108 postcondition
7 covers the non-fabrication negative control above; VP-106 does not cover either — VP-106 is
scoped to the clear-DECISION (Some/None + auto-delete file-state) property exercised by
`check_and_clear_expired_marker`, not to the emission-correctness of any `clear_mode`.

**AC-024** (traces to BC-1.18.001 v1.5 invariant 3 SUPERSEDED corollary + postcondition 4 v1.5
"write-success arm" clause + BC-1.18.003 v1.7 postcondition 5 + BC-3.08.001 v1.34 Event 9 —
SUPERSEDED clear path emits marker.cleared for cross-pair marker overwrite; NEW in v1.13;
ADR-048 §Decision 4 (F-P3-002 LOW correction, introduced v1.13 — historical provenance,
non-load-bearing); **emission-point corrected this burst per ADR-048 §Decision 4 (F-P9-001
MEDIUM correction; architect adjudication; human-ratified 2026-09-01, POLICY 22, D-1142 —
historical provenance, non-load-bearing)**)

Mirroring AC-022/AC-023's structure, this AC covers the FOURTH audited clear path: a cross-pair
marker overwrite under BC-1.18.001 invariant 3's single-marker, last-writer-wins policy. When a
`.factory/unvalidated-mutation.marker` currently belongs to pair A `(plugin_a, artifact_a)` (never
cleared) and a NEW INDETERMINATE fail-closed event fires for a DIFFERENT pair B `(plugin_b,
artifact_b)`, `write_indeterminate_marker`'s caller (`executor.rs`, at BOTH
`write_indeterminate_marker` call sites — the same sites that also call AC-025's
`emit_marker_written`) MUST:
1. Read the EXISTING marker's fields (`trace_id`, `plugin_name`, `artifact_path`) via
   `read_all_marker_fields` BEFORE the temp+rename overwrite — this read is unavoidable and
   UNCHANGED from v1.13, since the write itself overwrites the file the fields are read from; the
   read result is held, not emitted, until the write's outcome is known.
2. Call `write_indeterminate_marker(&new_fields, &marker_path)`.
3. **Emission-point correction (this burst — ADR-048 §Decision 4 (F-P9-001 correction — historical provenance, non-load-bearing)):** ONLY on the `Ok(())`
   arm (the write actually, durably succeeded), compare the pre-overwrite marker's
   `(plugin_name, artifact_path)` against pair B's:
   - If DIFFERENT (cross-pair), call `emit_marker_cleared(ctx, &existing_fields, "SUPERSEDED",
     "system", Some(reason))` — which routes through `ctx.emit_internal` (dual-sink: durable
     `dispatcher-internal-{date}.jsonl` write + `ctx.events` queue) — carrying pair A's OWN
     `trace_id`/`plugin_name`/`artifact_path` (never B's), with `reason = "SUPERSEDED: marker
     overwritten by a new plugin.indeterminate event for a different (plugin_name, artifact_path)
     pair before being cleared; last-writer-wins (BC-1.18.001 INV3)"` (non-null — `SUPERSEDED`
     joins `OPERATOR_OVERRIDE` as a `reason`-mandatory `clear_mode`), `timestamp` = time of the
     clear event. This call fires FIRST, immediately BEFORE AC-025's `emit_marker_written` call,
     in the SAME `Ok(())` arm (SUPERSEDED first, then `marker.written` — matching BC-1.18.001 v1.5
     postcondition 4's "write-success arm now emits BOTH write-tied events together" clause).
   - If the SAME pair (same-pair re-INDETERMINATE — continuous quarantine of the same target),
     emit NOTHING — unchanged from v1.13; already fully covered by the marker's own `trace_id`
     update at overwrite (BC-1.18.001 invariant 3 corollary).
4. **Write-failure non-fabrication (NEW this burst — ADR-048 §Decision 4 (F-P9-001 correction — historical provenance, non-load-bearing)):** on `Err(_)`
   (EC-007 — atomic rename fails, e.g. disk full or permissions), emit NEITHER
   `marker.cleared(SUPERSEDED)` for A NOR `marker.written` for B. The OLD marker for A remains
   durably present on disk and still enforcing its quarantine — emitting SUPERSEDED here would be
   a fabricated audit record (NIST AU-3/AU-10) falsely claiming A's marker was overwritten when it
   was not. **Previously (pre-this-burst),** step 3's emission was described as occurring
   unconditionally at the pre-overwrite READ, BEFORE `write_indeterminate_marker` was even called
   — a subsequent write failure could then leave a fabricated SUPERSEDED record on
   disk-truth-inconsistent grounds (A's marker still present and enforcing, yet a record already
   claimed it was overwritten). This is the direct fix for BC-1.18.003 v1.7 EC-018 (the F-P9-001
   regression test).

This closes a false-attribution audit-integrity gap (NIST AU-3/AU-10): without step 3,
`reconcile_raw_delete` (AC-023) would later find pair A's `marker.written` unmatched (if pair B's
marker is subsequently raw-deleted out-of-band) and mis-attribute A's silent supersession to a
human `OPERATOR_OVERRIDE` that never happened. No change to `reconcile_raw_delete`'s matching
logic is required — its scan already treats ANY `type == "marker.cleared"` record (regardless of
`clear_mode`) as closing the `(plugin_name, artifact_path)` key, so a `SUPERSEDED` record for A
closes A's key the moment B's write succeeds.

Wire format (BC-3.08.001 v1.34 Event 9): `clear_mode = "SUPERSEDED"`, `actor_type = "system"`,
`reason` mandatory non-null — UNCHANGED by this correction; only the emission POINT (WHEN) moved,
not the WHAT. VP-108 postcondition 5 verifies the (now write-then-emit-ordered) positive emission;
VP-108 postcondition 8 (NEW) is the direct negative-control regression test for F-P9-001 — a
cross-pair overwrite where `write_indeterminate_marker` returns `Err(_)` MUST yield ZERO
`marker.cleared(SUPERSEDED)` events AND ZERO `marker.written` events.

### BC-1.18.001 Postcondition 4 v1.4 — `marker.written` Audited Creation Event (BC-3.08.001 Event 10)

**AC-025** (traces to BC-1.18.001 postcondition 4 v1.4 — marker.written audited creation event;
BC-3.08.001 Event 10; VP-108 postcondition 6; NEW in v1.14; ADR-048 §Decision 4 (F-P6-001
correction, HUMAN-RATIFIED 2026-09-01, POLICY 22, D-1141 — historical provenance,
non-load-bearing))

Mirroring AC-021/AC-022/AC-023/AC-024's structure, this AC covers a FIFTH audited marker-lifecycle
event — but, unlike those four (all `marker.cleared` CLEAR paths), this one is a `marker.written`
CREATE event. Immediately after `write_indeterminate_marker` returns `Ok(())` — the atomic
temp-file-then-`rename` write of `.factory/unvalidated-mutation.marker` succeeded —
`write_indeterminate_marker`'s caller (`executor.rs`, the SAME INDETERMINATE-outcome callsite that
performs AC-024's SUPERSEDED check) MUST:

1. Call a new `emit_marker_written(ctx: &HostContext, fields: &MarkerFields)` function
   (`crates/factory-dispatcher/src/indeterminate_marker.rs`) immediately after the `Ok(())` return.
   **Ordering corrected this burst (ADR-048 §Decision 4 (F-P9-001 correction — historical provenance, non-load-bearing)):** `marker.written` no longer fires
   "BEFORE ANY other marker-lifecycle event for this write" — AC-024's SUPERSEDED check now ALSO
   emits (not merely reads) inside this same `Ok(())` arm, so the two write-tied events fire
   back-to-back in a fixed order: AC-024's `emit_marker_cleared(..., "SUPERSEDED", ...)` FIRST (if
   the pre-overwrite marker existed for a different pair), THEN `emit_marker_written` SECOND. Both
   calls happen only after `write_indeterminate_marker` returns `Ok(())`; neither fires on `Err(_)`.
2. `emit_marker_written` emits `marker.written` (BC-3.08.001 Event 10) via `ctx.emit_internal` —
   the SAME dual-sink helper (durable `dispatcher-internal-{date}.jsonl` write + `ctx.events`
   queue) every other dispatcher-native BC-3.08.001 event uses — carrying:
   - `type = "marker.written"`
   - `trace_id` = the marker's own `trace_id` (the current dispatch's trace_id)
   - `plugin_name` = the marker's own `plugin_name`
   - `artifact_path` = the marker's own `artifact_path`
   - `cause` = the marker's own `cause` (one of `"fuel"` | `"epoch"` | `"output-too-large"`)
   - `expires_at` = the marker's own `expires_at`
   - `ts` (common emission-timestamp field) = time of the write — `marker.written` does NOT carry
     a separate top-level `timestamp` field the way Events 8 and 9 do

`marker.written` is NEVER emitted when `write_indeterminate_marker` returns `Err(_)` (story EC-008
— atomic rename failure, e.g. disk full or permissions) and is NEVER emitted for a PreToolUse
INDETERMINATE (marker write is PostToolUse-only per BC-1.18.001 invariant 4 — `write_indeterminate_marker`
is never called on the PreToolUse path at all). `marker.written` is a positive creation record: it
exists if and only if a marker was actually, durably written. This is what makes AC-023's
`reconcile_raw_delete` OPERATOR_OVERRIDE inference sound BY CONSTRUCTION rather than inferred from
a proxy signal — see AC-023's non-fabrication clause and story EC-036 for the direct regression
case this closes.

`marker.written` never influences the marker-gate BLOCK/Allow decision (BC-1.18.002) — like Events
8 and 9, it is a pure observability/audit event.

VP-108 postcondition 6 verifies this write-path emission correctness; VP-108 postcondition 7
verifies the negative control (AC-023's non-fabrication clause).

### BC-1.18.004 — Fail-Open Advisory-Only Backward Compatibility

**AC-015** (traces to BC-1.18.004 postcondition 1 + postcondition 2 + postcondition 3 — fail-open
advisory only; no marker; no gate)

**Sink-mechanism correction (v1.13 sync to BC-3.08.001 v1.32 / ADR-048 §Decision 4,
F-P3-001 correction — historical provenance, non-load-bearing):** "emitted to
FileSink" below means via `HostContext::emit_internal` — see AC-006's sink-mechanism correction for
the full mechanism (durable `dispatcher-internal-{date}.jsonl` write + `ctx.events` queue; no
literal `events-*.jsonl` file is durably written today, pending S-4.07).

For any plugin with `failure_policy` absent (default) or `failure_policy = "fail-open"`, an
INDETERMINATE outcome produces: (a) an advisory `plugin.indeterminate` event emitted via
`HostContext::emit_internal`; (b) NO `.factory/unvalidated-mutation.marker` written; (c) NO gate
triggered. The predicate
`should_write_marker(Indeterminate, FailOpen) == false` is the load-bearing implementation gate.
`FailurePolicy::default() == FailOpen` (S-21.10 canonical; ADR-039 §Decision 1).

**AC-016** (traces to BC-1.18.004 postcondition 4 — Cohort A exactly three validators;
consistent with ADR-047 §D8a v1.4)

`plugins/vsdd-factory/hooks-registry.toml` MUST have `failure_policy = "fail-closed"` for
exactly these three Cohort A validators; no other validator receives `fail-closed` in this story.
The three validators are split into two operational sub-groups:

**Cohort A-immediate (ASSIGNED-NOW — 1 validator; corrected v1.21 — previously mislabeled
EFFECTIVE-NOW, which overclaimed live enforcement):**
- `validate-factory-path-staging`: set unconditionally. PreToolUse `^Bash$`; `on_error =
  "continue"`; bounded input; calibration confirmed; no S-21.24 dependency for the assignment. No
  registry comment required. This is the ONE validator whose fail-closed CONFIG BIT is fully,
  unconditionally set at S-25.01 merge — but the assignment currently produces ZERO observable
  enforcement effect: the durable-marker write path (BC-1.18.001 invariant 4) fires ONLY on a
  PostToolUse dispatch, and this validator's PreToolUse `^Bash$` registration structurally can
  never reach `write_indeterminate_marker`; combined with `on_error = "continue"` (which never
  blocks the current dispatch either) and this validator's absence from the ADR-039 §Decision 2
  six-validator exhaustion-leg roadmap, an INDETERMINATE outcome here degrades to the same
  advisory-only `plugin.indeterminate` event as fail-open (EC-009 documents this identical
  no-marker outcome already, correctly). This is a genuine coverage gap, not silently accepted:
  closure is anchored to follow-up story **S-25.04** — "Close validate-factory-path-staging
  zero-enforcement gap — real Layer-1 production trigger" (Epic E-25).

**Cohort A-deferred (SET-BUT-LATENT — 2 validators):**
- `validate-pr-merge-prerequisites`: field SET with registry comment:
  `# ACTIVATION CONDITIONAL on S-21.24 calibration per ADR-047 §D8a Cohort A-deferred`
- `validate-wave-gate-prerequisite`: field SET with the same registry comment.

Both deferred validators are PreToolUse `^Agent$` with `on_error = "block"`. Setting the
`failure_policy = "fail-closed"` field in this story is safe because the field is INERT
(S-21.10 is schema-only; ADR-039 Phase 4 enforcement not wired until S-21.24). Their
enforcement activates when S-21.24 wires the executor — at which point S-21.24's own
dependency chain (S-21.19 → S-21.21 calibration values → S-21.23 break-glass code) has already
resolved the self-lock risk.

**Layer-1 effective fail-closed count at S-25.01 merge: ZERO** (corrected v1.21 — misstated as
ONE in v1.1–v1.20 via the EFFECTIVE-NOW framing, which conflated "config bit SET" with
"enforcement EFFECTIVE"). The "3-validator Cohort A" reflects the complete fail-closed
CONFIG-BIT membership; none of the three currently deliver observable enforcement distinct from
fail-open — `validate-factory-path-staging` for the structural PreToolUse/PostToolUse reason
above, and the two deferred validators because their enforcement is latent until S-21.24.

**AC-017** (traces to BC-1.18.004 postcondition 5 — DO-NOT-DELETE backward-compat guard test)

The implementation MUST include the test
`test_BC_1_18_004_fail_open_default_preserves_advisory_behavior` in the executor.rs test module
(or equivalently `test_BC_1_18_004_fail_open_indeterminate_writes_no_marker` with a code comment
cross-referencing the canonical name per BC-1.18.004 PC5 allowance). This test MUST NOT be
deleted per ADR-047 §Decision 7. It is the canonical regression guard for ~76 existing fail-open
plugins.

### BC-1.18.001 Invariant 5 — OutputTooLarge Flag Is Dispatcher-Internal StoreData

**AC-018** (traces to BC-1.18.001 invariant 5 — host_output_too_large_seen is dispatcher-internal StoreData; no ABI exposure)

`StoreData::host_output_too_large_seen` MUST reside in `crates/factory-dispatcher/src/invoke.rs`
exclusively, as dispatcher-internal state. It MUST NOT be exposed in `crates/hook-sdk/` or any
public hook-sdk ABI type. Plugin WASM binaries do not read or write this flag directly — they
observe the `OutputTooLarge (-3)` return code from host functions and may return a non-zero
`exit_code` in response, but the flag is invisible to the plugin. No HOST_ABI_VERSION bump is
required for this story's changes. If any PR review reveals that `crates/hook-sdk` has gained a
dependency on this flag, the PR MUST be blocked and the dependency removed.

### BC-1.18.002 Invariant 1 — Single WASM Binary for Both Gate Arms

**AC-019** (traces to BC-1.18.002 invariant 1 — Arm 1 and Arm 2 use the same WASM binary)

The TWO `[[hook]]` entries for the gate plugin in `hooks-registry.toml` — `validate-unvalidated-mutation-marker`
(`tool = "^Agent$"`) and `validate-unvalidated-mutation-marker-git` (`tool = "^Bash$"`) — MUST
both reference the IDENTICAL compiled binary via `plugin = "hook-plugins/validate-unvalidated-mutation-marker.wasm"`.
There is exactly ONE `.wasm` file on disk; the dispatch routing between Arm 1 (Agent check) and
Arm 2 (Bash command-content filter) is implemented by plugin-internal logic reading the tool event
payload (`command` parameter for Bash dispatches). Confirming this invariant: the `crates/hook-plugins/validate-unvalidated-mutation-marker/`
crate produces a single `validate-unvalidated-mutation-marker.wasm` binary from `T-4`; `T-5`
registers it under two names pointing to the same `plugin` path.

## Behavioral Contracts

| BC-ID | Version | Title | Role in This Story |
|-------|---------|-------|-------------------|
| BC-1.18.001 | v1.5 | When a fail-closed Validator Cannot Complete, Dispatcher Classifies Outcome as INDETERMINATE, Emits `plugin.indeterminate` Event, and Writes Unvalidated-Mutation Marker | Primary contract: INDETERMINATE classification, cause routing, atomic marker write (6 required TOML fields including `expires_at = timestamp + 86400s`; ADR-048 §Decision 2); v1.1 adds `expires_at` as 6th required field; v1.2 corrects PC4's auto-delete-locus narration to dispatcher-native `check_and_clear_expired_marker` (no postcondition or wire-format change); v1.3 adds the invariant 3 SUPERSEDED-clear corollary — a cross-pair marker overwrite MUST emit `marker.cleared(clear_mode=SUPERSEDED, actor_type=system, reason=non-null)` for the superseded pair via `ctx.emit_internal` (AC-024; ADR-048 §Decision 4 (F-P3-002 correction — historical provenance, non-load-bearing)); v1.4 adds PC4's `marker.written` (BC-3.08.001 Event 10) audited creation event — emitted via `ctx.emit_internal` ONLY after `write_indeterminate_marker` returns `Ok(())`, never before, never on `Err(_)` (AC-025; ADR-048 §Decision 4 (F-P6-001 correction — historical provenance, non-load-bearing); HUMAN-RATIFIED 2026-09-01, POLICY 22, D-1141); v1.5 (ADR-048 §Decision 4 — Emission-Point Correction (F-P9-001 MEDIUM, S-25.01 adversary pass 9; architect adjudication; human-ratified 2026-09-01, POLICY 22, D-1142 — historical provenance, non-load-bearing)) relocates the v1.3 SUPERSEDED-clear corollary from an unconditional pre-overwrite read to INSIDE `write_indeterminate_marker`'s `Ok(())` arm, alongside the (unchanged) PC4 `marker.written` emission — SUPERSEDED fires first, then `marker.written`, both ONLY after `Ok(())`; on `Err(_)`, NEITHER event is emitted (AC-024; closes the fabricated-SUPERSEDED-record class NIST AU-3/AU-10, the un-swept sibling of v1.4's `marker.written` discipline) |
| BC-1.18.002 | v1.7 | Next State-Advancing Dispatch Is Blocked While Unvalidated-Mutation Marker Exists (Agent Arm and git commit/push Arm) | Gate plugin behavioral contract: block rules, Phase 1/1b/2/3/4 command filter (compound splitting/POSIX quoting/basename/arg-taking set/fail-safe), `block_if_marker` crash policy (on_error="block_if_marker"; BLOCK iff non-expired marker on crash; ALLOW if absent/expired/crash-handler-IO-fail; ADR-048 §Decision 1; D-1135 reversed), two-axis model, four-tier recovery model (T1=Edit/Write primary inherently ungated; T2=TTL deadman; T3=human OOB rm break-glass; T4=agent-tool rm de-sanctioned crash-path-block ACCEPTABLE NOT INV6 violation), threat model & scope; v1.6 ADR-048 §Decision 3 (amended — historical provenance, non-load-bearing) — recovery model reframe; VP-107 scope amended (verifies T1 only); v1.7 corrects the T2/TTL-deadman narration (§INV6, §Fail-Closed-But-Recoverable Design table, Traceability) to attribute normal-path TTL auto-delete to dispatcher-native `check_and_clear_expired_marker` (crash-path `block_if_marker_check` narration unchanged; no Block/Allow semantic change); unchanged this burst |
| BC-1.18.003 | v1.7 | Successful Re-Validation Clears the Unvalidated-Mutation Marker (Idempotent; Operator Escape Hatch Supported) | Marker-clear protocol: delete_marker_if_pass (artifact-scoped: same plugin AND same non-empty artifact path, OR empty-artifact_path→name-only fallback; ADR-047 §D5), NotFound→no-op, both arms unblock; v1.2 adds TTL-expiry PC4 (86400s deadman; legacy markers non-expired); v1.3 adds audited clear model (ADR-048 §Decision 4): all three clear paths emit `marker.cleared` (BC-3.08.001 Event 9) — REVALIDATED (from delete_marker_if_pass; trace_id from originating plugin.indeterminate; reason=null), TTL_EXPIRED, OPERATOR_OVERRIDE (retroactive RAW_DELETE_DETECTED reconciliation; best-effort); v1.4 (ADR-048 §Decision 4 — Emission-Point Correction, F-P2-002/F-P2-003, human-ratified 2026-08-31 — historical provenance, non-load-bearing) re-attributes TTL_EXPIRED and OPERATOR_OVERRIDE emission from the WASM gate plugin to the new dispatcher-native `check_and_clear_expired_marker` (TTL) and `reconcile_raw_delete` (OPERATOR_OVERRIDE) functions in `indeterminate_marker.rs`; `evaluate_gate` becomes a pure marker-presence check; VP attribution for the four audited-emission postconditions retargeted VP-106→VP-108; v1.5 (ADR-048 §Decision 4 — Emission-Mechanism Precision Correction, F-P3-001, human-ratified 2026-09-01 — historical provenance, non-load-bearing) routes all four `clear_mode` emissions through `HostContext::emit_internal` (not a raw `InternalLog::write`), corrects `reconcile_raw_delete`'s scan target to `dispatcher-internal-{date}.jsonl`, and adds PC5 the FOURTH clear path — SUPERSEDED (cross-pair marker overwrite; AC-024); v1.6 (ADR-048 §Decision 4 — Reconciliation-Premise Correction, F-P6-001, HUMAN-RATIFIED 2026-09-01, POLICY 22, D-1141 — historical provenance, non-load-bearing) retargets PC3's `reconcile_raw_delete` scan from an unmatched `plugin.indeterminate` (with its now-removed `failure_policy=fail-closed` filter) to an unmatched `marker.written` (BC-3.08.001 Event 10, new sibling BC-1.18.001 PC4 v1.4); new EC-017 is the direct non-fabrication regression test (AC-023); v1.7 (ADR-048 §Decision 4 — Emission-Point Correction (F-P9-001 MEDIUM, S-25.01 adversary pass 9; architect adjudication; human-ratified 2026-09-01, POLICY 22, D-1142 — historical provenance, non-load-bearing)) relocates PC5's SUPERSEDED emission from the unconditional pre-overwrite read to inside `write_indeterminate_marker`'s `Ok(())` arm, symmetric with BC-1.18.001 PC4's `marker.written` "emit only after `Ok(())`" rule (SUPERSEDED first, then `marker.written`); on `Err(_)`, NEITHER event is emitted; new EC-018 is the direct F-P9-001 regression test (AC-024) |
| BC-1.18.004 | v1.2 | `failure_policy = fail-open` INDETERMINATE Is Advisory-Only — No Marker Written, No Gate Triggered (Backward-Compatibility Anchor) | Backward-compat anchor: ~76 existing fail-open plugins unchanged; DO-NOT-DELETE guard test; v1.2 (factual correction, human-directed, NOT a POLICY 22 design change — routed from pr-reviewer's fresh-eyes finding on PR #807) corrects PC4's `validate-factory-path-staging` event/tool description to the registry ground truth (PreToolUse `^Bash$`-only, not PostToolUse Edit/Write/MultiEdit/Bash), softens EFFECTIVE-NOW to ASSIGNED-NOW, and states explicitly "Layer-1 effective fail-closed count at S-25.01 merge: ZERO" — closure anchored to follow-up story S-25.04 |
| BC-3.08.001 | v1.34 | dispatcher async-semantics event types are catalogued and emitted via FileSink — `plugin.async_block_discarded`, `dispatcher.schema_mismatch`, `dispatcher.registry_invalid`, `plugin.timeout` (async path), `plugin.abandoned`, `plugin.completed` (async path), `plugin.fuel_headroom_warning`, `plugin.indeterminate`, `marker.cleared`, `marker.written` | Wire-format authority for ten event types: Event 8 (`plugin.indeterminate`) mandatory fields (type, trace_id, session_id, plugin_name, artifact_path, cause, failure_policy, timestamp); Event 9 (`marker.cleared`) mandatory fields (type, trace_id, session_id, plugin_name, artifact_path, clear_mode, actor_type, reason, timestamp); `clear_mode`/`actor_type` correspondence table; `trace_id` semantics (links to originating plugin.indeterminate, or — v1.33 — the unmatched marker.written event for OPERATOR_OVERRIDE); `reason` nullability (null for REVALIDATED/TTL_EXPIRED; OPERATOR_OVERRIDE and — v1.32 — SUPERSEDED mandatory non-null text); Event 10 (`marker.written`, NEW in v1.33; AC-025) mandatory fields (type, trace_id, session_id, plugin_name, artifact_path, cause, expires_at, ts) — trigger: `write_indeterminate_marker` returns `Ok(())`; never before the write, never on `Err(_)`, never for PreToolUse; v1.31 re-attributes the TTL_EXPIRED/OPERATOR_OVERRIDE "Emission point" table cells to dispatcher-native `check_and_clear_expired_marker`/`reconcile_raw_delete` (no wire-format/field-shape change); v1.32 adds `clear_mode = SUPERSEDED` / `actor_type = system` to Event 9's enums (AC-024), corrects the §Sink destination prose to name `HostContext::emit_internal` explicitly (durable `dispatcher-internal-{date}.jsonl` write + `ctx.events` queue — not a literal `events-{date}.jsonl`), and applies the same correction to Events 8 and 9's emission-path paragraphs (AC-006/AC-015 sink-phrasing correction); v1.33 adds the new §Event 10 `marker.written` catalog entry and retargets Event 9's OPERATOR_OVERRIDE Trigger bullet + `clear_mode`/`actor_type` correspondence table + `trace_id` semantics from unmatched `plugin.indeterminate` to unmatched `marker.written` (AC-023/AC-025; ADR-048 §Decision 4, F-P6-001 correction, HUMAN-RATIFIED 2026-09-01, POLICY 22, D-1141 — historical provenance, non-load-bearing); v1.34 (ADR-048 §Decision 4 — Emission-Point Correction (F-P9-001 MEDIUM, S-25.01 adversary pass 9; architect adjudication; human-ratified 2026-09-01, POLICY 22, D-1142 — historical provenance, non-load-bearing)) relocates Event 9's SUPERSEDED trigger to inside `write_indeterminate_marker`'s `Ok(())` arm, symmetric with Event 10's "emit only after `Ok(())`" rule (SUPERSEDED first, then `marker.written`); on `Err(_)`, NEITHER event is emitted; wire-format/field-shape UNCHANGED — emission-point/ordering correction only (AC-024) |

## Architecture Mapping

| Component | File/Module | Pure/Effectful |
|-----------|------------|---------------|
| `classify_outcome` pure function | `crates/factory-dispatcher/src/executor.rs` | Pure-core (inputs → DispatchOutcome; no I/O) |
| INDETERMINATE outcome integration in invocation loop | `crates/factory-dispatcher/src/executor.rs` | Effectful (calls write_indeterminate_marker, emits event) |
| `StoreData::host_output_too_large_seen` flag + per-invocation reset | `crates/factory-dispatcher/src/invoke.rs` | Effectful (mutable Store state; reset before WASM call) |
| `write_indeterminate_marker` (atomic temp+rename) | `crates/factory-dispatcher/src/indeterminate_marker.rs` | Effectful (filesystem write) |
| `delete_marker_if_pass` (idempotent rm) | `crates/factory-dispatcher/src/indeterminate_marker.rs` | Effectful (filesystem delete) |
| `should_write_marker` predicate | `crates/factory-dispatcher/src/indeterminate_marker.rs` | Pure-core (DispatchOutcome × FailurePolicy → bool) |
| `check_and_clear_expired_marker` (TTL detect + auto-delete + Allow-signal; ADR-048 §Decision 4) | `crates/factory-dispatcher/src/indeterminate_marker.rs` | Effectful (filesystem read + conditional delete; calls `emit_marker_cleared`); called from `executor.rs`'s tier-execution loop BEFORE every `on_error=block_if_marker` plugin invocation, normal path only. **v1.13 signature:** `(factory_root: &Path, now: DateTime<Utc>, ctx: &HostContext) -> io::Result<Option<MarkerFields>>` — `ctx` widening per ADR-048 §Decision 4 (F-P3-001 correction — historical provenance, non-load-bearing) |
| `reconcile_raw_delete` (OPERATOR_OVERRIDE bounded scan; ADR-048 §Decision 4) | `crates/factory-dispatcher/src/indeterminate_marker.rs` | Effectful (bounded, best-effort read of the current day's `dispatcher-internal-{date}.jsonl`, obtained via `ctx.internal_log`; calls `emit_marker_cleared`); invoked from `check_and_clear_expired_marker`'s marker-absent branch. **v1.13 signature:** `(factory_root: &Path, ctx: &HostContext) -> io::Result<()>` — scan target corrected from a literal `events-*.jsonl` to `dispatcher-internal-{date}.jsonl` per ADR-048 §Decision 4 (F-P3-001 correction — historical provenance, non-load-bearing). **v1.14:** scan MATCH TYPE retargeted from unmatched `plugin.indeterminate` to unmatched `marker.written` (redundant `failure_policy` filter removed) per ADR-048 §Decision 4 (F-P6-001 correction — historical provenance, non-load-bearing) (AC-023) |
| `emit_marker_written(ctx: &HostContext, fields: &MarkerFields)` (NEW — marker.written creation event; ADR-048 §Decision 4 (F-P6-001 correction — historical provenance, non-load-bearing)) | `crates/factory-dispatcher/src/indeterminate_marker.rs` | Effectful (routes the `marker.written` event through `HostContext::emit_internal` — the same dual-sink helper every other dispatcher-native BC-3.08.001 event uses); called from `executor.rs`'s marker-write callsite immediately after `write_indeterminate_marker` returns `Ok(())` (and after the `emit_superseded_if_cross_pair` check, which — **corrected this burst, ADR-048 §Decision 4 (F-P9-001 correction — historical provenance, non-load-bearing)** — now ALSO fires inside this same `Ok(())` arm, immediately before this call), never on `Err(_)`, never for PreToolUse (AC-025) |
| `emit_marker_cleared` (REVALIDATED / TTL_EXPIRED / OPERATOR_OVERRIDE / SUPERSEDED emission) | `crates/factory-dispatcher/src/indeterminate_marker.rs` | Effectful (routes the `marker.cleared` event through `HostContext::emit_internal` — the dual-sink helper that writes durable `dispatcher-internal-{date}.jsonl` AND pushes onto `ctx.events`; bypasses the WASM `emit_event` host ABI entirely — never crosses the RESERVED_FIELDS enrichment wall). **v1.13 signature:** `(ctx: &HostContext, marker_fields: &MarkerFields, clear_mode: &str, actor_type: &str, reason: Option<&str>)` — replaces the pre-v1.13 `(clear_mode: ClearMode, marker: &MarkerContent, trace_id: &str)` form that called a raw `InternalLog::write` (ADR-048 §Decision 4 (F-P3-001 correction — historical provenance, non-load-bearing)) |
| `emit_superseded_if_cross_pair` (SUPERSEDED trigger; ADR-048 §Decision 4 (F-P3-002 correction — historical provenance, non-load-bearing); emission point corrected v1.5 F-P9-001) | `crates/factory-dispatcher/src/indeterminate_marker.rs` | Effectful (reads the pre-overwrite marker's fields via `read_all_marker_fields` BEFORE the overwrite, compares `(plugin_name, artifact_path)` against the new INDETERMINATE event's pair, and — when different — calls `emit_marker_cleared(ctx, &existing_fields, "SUPERSEDED", "system", Some(reason))`; no-op on same-pair overwrite); called from `executor.rs`'s marker-write callsite at BOTH `write_indeterminate_marker` call sites. **Corrected this burst (ADR-048 §Decision 4 (F-P9-001 correction — historical provenance, non-load-bearing)):** the emit call is now made INSIDE `write_indeterminate_marker`'s `Ok(())` arm — immediately BEFORE `emit_marker_written` in that same arm (SUPERSEDED first, then `marker.written`) — never before the write is attempted, never on `Err(_)`; the pre-overwrite field READ (unavoidable) remains BEFORE the write, but the EMIT no longer does (AC-024) |
| `guard_logic::evaluate_gate` | `crates/hook-plugins/validate-unvalidated-mutation-marker/src/lib.rs` | Effectful (filesystem read: PURE marker-presence check — no TOML `expires_at` field access, no deletion, no emission; TTL detection is entirely dispatcher-native, performed by `check_and_clear_expired_marker` before this function is ever invoked on the normal path; ADR-048 §Decision 4 (Emission-Point Correction, F-P2-002, human-ratified 2026-08-31 — historical provenance, non-load-bearing)) |
| `guard_logic::is_git_commit_or_push` | `crates/hook-plugins/validate-unvalidated-mutation-marker/src/lib.rs` | Pure-core (command string → bool; Phase 1/1b/2/3/4 algorithm v1.3: compound splitting, POSIX quote-aware tokenization via `shell-words`, basename identification, arg-taking global-option skip with complete recognized set, fail-safe on unrecognized, exact subcommand match) |
| Arm 1 WASM entry point (`^Agent$`) | `crates/hook-plugins/validate-unvalidated-mutation-marker/src/lib.rs` | Effectful (reads tool params, reads marker file) |
| Arm 2 WASM entry point (`^Bash$` git filter) | `crates/hook-plugins/validate-unvalidated-mutation-marker/src/lib.rs` | Effectful (reads tool params, reads marker file) |
| hooks-registry.toml Cohort A + gate entries | `plugins/vsdd-factory/hooks-registry.toml` | Configuration |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| `classify_outcome(PluginResult, FailurePolicy, bool) -> DispatchOutcome` | Pure-core | Takes: the plugin result variant, the failure_policy from RegistryEntry, and the host_output_too_large_seen flag value (captured BEFORE reset). Returns an enum variant. No I/O. VP-102 proof harness covers this function. |
| `should_write_marker(DispatchOutcome, FailurePolicy) -> bool` | Pure-core | Returns true iff outcome=Indeterminate AND policy=FailClosed. VP-106 proof harness covers this predicate. |
| `is_git_commit_or_push(command: &str) -> bool` | Pure-core | Phase 1/1b/2/3/4 algorithm (BC-1.18.002 PC2 v1.3): Phase 1 compound splitting on `&&`/`||`/`;`/`|`/`&`/newline; Phase 1b POSIX quote-aware tokenization via `shell-words` crate (e.g., `git "commit"` → `["git", "commit"]`; `g'i't commit` → `["git", "commit"]`); Phase 2 basename identification with env-var-prefix and `env`-token stripping; Phase 3 global-option skip using complete seven-option arg-taking set (`-C`, `-c`, `--namespace`, `--git-dir`, `--work-tree`, `--super-prefix`, `--config-env`) + no-arg set + fail-safe on unrecognized; Phase 4 exact subcommand match (`commit`/`push` only). VP-105 unit-test cases cover this function. |
| `write_indeterminate_marker(fields: &MarkerFields, marker_path: &Path) -> io::Result<()>` | Effectful (write) | Atomic filesystem write (temp file in same directory + rename). No reads of external state. |
| `delete_marker_if_pass(marker_path: &Path) -> io::Result<()>` | Effectful (delete) | Filesystem delete; NotFound → Ok(()). |
| `check_and_clear_expired_marker(factory_root: &Path, now: DateTime<Utc>, ctx: &HostContext) -> io::Result<Option<MarkerFields>>` | Effectful (read + conditional delete + emit) | Dispatcher-native TTL pre-check (ADR-048 §Decision 4): reads the marker via `read_all_marker_fields`; if present and `expires_at ≤ now`, deletes it (idempotent, swallow `NotFound`), calls `emit_marker_cleared(ctx, &fields, "TTL_EXPIRED", "deadman", None)`, and returns `Some(fields)`; missing/non-expired `expires_at` → returns `None` without deleting. Called from `executor.rs`'s tier-execution loop before every `on_error=block_if_marker` plugin invocation, normal path only. **v1.13:** takes `ctx: &HostContext` (widened from a bare `log: &InternalLog, session_id: &str` pair) so the emission routes through `ctx.emit_internal`. VP-106 (clear-decision) + VP-108 (emission correctness) proof harnesses cover this function. |
| `reconcile_raw_delete(factory_root: &Path, ctx: &HostContext) -> io::Result<()>` | Effectful (bounded read + emit) | Dispatcher-native OPERATOR_OVERRIDE reconciliation (ADR-048 §Decision 4): invoked from `check_and_clear_expired_marker`'s marker-absent branch; bounded, best-effort scan of the CURRENT DAY's `dispatcher-internal-{date}.jsonl` only (obtained via `ctx.internal_log`; capped bytes/records — fixed-size tail read, never a full-file or multi-day scan) for an unmatched `marker.written` (v1.14 — retargeted from unmatched `plugin.indeterminate`, redundant `failure_policy` filter removed); if found, calls `emit_marker_cleared(ctx, &fields, "OPERATOR_OVERRIDE", "operator", Some(reason))`. Never gates the dispatch decision (BC-3.08.001 Invariant 3). **v1.13:** takes `ctx: &HostContext` (replaces the pre-v1.13 `sink: &FileSink` parameter); scan target corrected from a literal `events-{date}.jsonl` to `dispatcher-internal-{date}.jsonl` per ADR-048 §Decision 4 (F-P3-001 correction — historical provenance, non-load-bearing). **v1.14:** scan match-type retargeted to `marker.written` per ADR-048 §Decision 4 (F-P6-001 correction — historical provenance, non-load-bearing) (AC-023). VP-108 proof harness (PC3 + PC7 negative control) covers this function. |
| `emit_marker_written(ctx: &HostContext, fields: &MarkerFields)` (NEW — ADR-048 §Decision 4 (F-P6-001 correction — historical provenance, non-load-bearing)) | Effectful (event emission) | Constructs a `marker.written` `InternalEvent` and routes it through `ctx.emit_internal` — the same dual-sink helper `emit_marker_cleared` uses. Called from `write_indeterminate_marker`'s caller (`executor.rs`), at BOTH call sites, ONLY on the `Ok(())` arm, immediately after the `emit_superseded_if_cross_pair` check (which — **corrected this burst, ADR-048 §Decision 4 (F-P9-001 correction — historical provenance, non-load-bearing)** — now ALSO fires only inside this same `Ok(())` arm, immediately before this call) and BEFORE control returns from the INDETERMINATE-outcome handling block; never called on `Err(_)`. AC-025; VP-108 postcondition 6 proof harness covers this function. |
| `emit_marker_cleared(ctx: &HostContext, marker_fields: &MarkerFields, clear_mode: &str, actor_type: &str, reason: Option<&str>) -> Result<()>` | Effectful (event emission) | Constructs a `marker.cleared` `InternalEvent` and routes it through `ctx.emit_internal` — the dual-sink helper (durable `dispatcher-internal-{date}.jsonl` write + `ctx.events` queue) that every OTHER dispatcher-native BC-3.08.001 event already uses. Bypasses the WASM `emit_event` host ABI entirely, so RESERVED_FIELDS enrichment never applies and the marker's own (foreign) `trace_id`/`plugin_name` can be carried on the wire. Called from `delete_marker_if_pass` (REVALIDATED), `check_and_clear_expired_marker` (TTL_EXPIRED), `reconcile_raw_delete` (OPERATOR_OVERRIDE), and `emit_superseded_if_cross_pair` (SUPERSEDED). **v1.13:** signature widened to `ctx: &HostContext`, replacing a raw `InternalLog::write` call (ADR-048 §Decision 4 (F-P3-001 correction — historical provenance, non-load-bearing)). VP-108 proof harness covers this function. |
| `emit_superseded_if_cross_pair(existing: Option<&MarkerFields>, new_pair: (&str, &str), ctx: &HostContext, reason: &str)` | Effectful (conditional emit) | Dispatcher-native SUPERSEDED trigger (ADR-048 §Decision 4 (F-P3-002 correction — historical provenance, non-load-bearing)): reads the pre-overwrite marker's fields (already resolved by the caller via `read_all_marker_fields`, BEFORE the overwrite — unavoidable and unchanged); if `existing`'s `(plugin_name, artifact_path)` differs from `new_pair`, calls `emit_marker_cleared(ctx, existing_fields, "SUPERSEDED", "system", Some(reason))`; no-op on same-pair overwrite or absent existing marker. Called from `executor.rs`'s marker-write callsite at BOTH `write_indeterminate_marker` call sites. **Emission point corrected this burst (ADR-048 §Decision 4 (F-P9-001 correction — historical provenance, non-load-bearing)):** the CALL to this function (and its resulting emit) now happens INSIDE `write_indeterminate_marker`'s `Ok(())` arm, immediately BEFORE `emit_marker_written` in that same arm — never before the write is attempted, never on `Err(_)` (AC-024). VP-108 postcondition 5 proof harness (positive emission, now write-then-emit-ordered) and postcondition 8 (NEW — negative control: forced `Err(_)` on a cross-pair overwrite yields ZERO SUPERSEDED and ZERO `marker.written` events) cover this function. |
| `evaluate_gate(marker_path: &Path) -> GateDecision` | Effectful (read) | PURE marker-presence check: reads marker file from filesystem; parses TOML to extract `plugin_name`/`artifact_path`/`cause` fields for the block message ONLY. Does NOT parse or evaluate `expires_at` — TTL detection, auto-delete, and RAW_DELETE_DETECTED reconciliation are entirely dispatcher-native (`check_and_clear_expired_marker` / `reconcile_raw_delete`, both invoked before this function ever runs on the normal path). No `fs::remove_file`, no `emit_marker_cleared` call, and no FileSink scan occur inside this crate (ADR-048 §Decision 4 (Emission-Point Correction, F-P2-002, human-ratified 2026-08-31 — historical provenance, non-load-bearing)). |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | Second INDETERMINATE event for same plugin while marker already exists | Overwrite marker (last-writer-wins per BC-1.18.001 invariant 3 single-marker policy) |
| EC-002 | INDETERMINATE occurs on PreToolUse (not PostToolUse) for fail-closed plugin | No marker written (PostToolUse-only per BC-1.18.001 invariant 4); advisory event only |
| EC-003 | Gate plugin (validate-unvalidated-mutation-marker) itself fuel-exhausts | Gate dispatcher applies block_if_marker native check (AC-011; ADR-048 §Decision 1): if marker ABSENT or TTL-expired → dispatch proceeds (Allow, EC-034); if non-expired marker EXISTS → dispatch BLOCKED (EC-033). INDETERMINATE on the gate does NOT write a nested marker (gate is failure_policy="fail-open"; AC-015 / BC-1.18.004). The block_if_marker design prevents unconditional self-lock (INV2) via the no-valid-signal → Allow branch (EC-034). |
| EC-004 | Operator runs `rm .factory/unvalidated-mutation.marker`; next Agent dispatch fires | AC-010 path: marker absent → both arms pass; dispatch proceeds normally |
| EC-005 | A fail-open plugin fuel-exhausts while marker exists from a fail-closed plugin | Fail-open path: advisory event only; marker NOT updated (fail-open skips write_indeterminate_marker entirely) |
| EC-006 | `git commit --amend` command while marker exists | AC-008 path: Phase 4 exact-subcommand match on `commit`; BLOCKED (BC-1.18.002 EC-006). |
| EC-007 | `git push --force-with-lease` while marker exists | AC-008 path: Phase 4 exact-subcommand match on `push`; BLOCKED (BC-1.18.002 EC-007). |
| EC-008 | Marker file write fails (e.g., disk full, path not writable) — BC-1.18.001 dispatcher write-fail path. (Note: BC-1.18.002 EC-008 v1.4 rescoped to readable-but-malformed marker → Block at gate; that is a gate-read AC-007 scenario distinct from this write-fail path. See EC-030 for the gate-read unreadable → Allow path.) | Propagate io::Error; log error; treat as INDETERMINATE with marker unwritten (conservative path — still emit plugin.indeterminate event). **v1.14:** NO `marker.written` event is emitted for this failed write — `marker.written` is emitted ONLY after a confirmed `Ok(())` return from `write_indeterminate_marker` (AC-025). See EC-036 for the non-fabrication consequence at reconciliation time. |
| EC-009 | validate-factory-path-staging (PreToolUse `^Bash$`, Cohort A-immediate, fail-closed) fuel-exhausts | PreToolUse INDETERMINATE path: no marker written (PostToolUse-only per BC-1.18.001 invariant 4); advisory `plugin.indeterminate` event only. ADR-039 Phase 4 enforcement (current-dispatch blocking) activates at S-21.24; `on_error = "continue"` prevents self-lock. |
| EC-010 | `git --git-dir .git commit -m "state"` while marker exists | AC-008/AC-009 path: Phase 3 `--git-dir` is in recognized arg-taking set; `.git` consumed as its separate-token argument; Phase 4 subcommand is `commit`; BLOCKED (BC-1.18.002 EC-013). |
| EC-011 | `git --work-tree /tmp push` while marker exists | AC-008/AC-009 path: Phase 3 `--work-tree` is in recognized arg-taking set; `/tmp` consumed as its argument; Phase 4 subcommand is `push`; BLOCKED (BC-1.18.002 EC-014). |
| EC-012 | `git status && git commit -m "x"` while marker exists | AC-008/AC-009 path: Phase 1 splits on `&&`; segment 1 subcommand `status` (not gated); segment 2 subcommand `commit` (gated); any-segment `true` → function returns `true`; BLOCKED (BC-1.18.002 EC-015). |
| EC-013 | `git diff ; git push` while marker exists | AC-008/AC-009 path: Phase 1 splits on `;`; segment 1 subcommand `diff` (not gated); segment 2 subcommand `push` (gated); BLOCKED (BC-1.18.002 EC-016). |
| EC-014 | `/usr/bin/git commit -m "init"` while marker exists | AC-008/AC-009 path: Phase 2 `basename("/usr/bin/git") = "git"`; Phase 4 subcommand `commit`; BLOCKED (BC-1.18.002 EC-017). |
| EC-015 | `./git push origin main` while marker exists | AC-008/AC-009 path: Phase 2 `basename("./git") = "git"`; Phase 4 subcommand `push`; BLOCKED (BC-1.18.002 EC-018). |
| EC-016 | `git status && git log --oneline` while marker exists | AC-009 path: Phase 1 splits on `&&`; segment 1 subcommand `status`; segment 2 subcommand `log`; no segment resolves to `commit` or `push`; NOT blocked (BC-1.18.002 EC-019). |
| EC-017 | `cat gitfile` while marker exists | AC-009 path: Phase 2 `basename("cat") = "cat" ≠ "git"`; returns `false`; NOT blocked (BC-1.18.002 EC-020). |
| EC-018 | `env GIT_DIR=.git git commit -m "msg"` while marker exists | AC-008/AC-009 path: Phase 2 strips literal `env` token and `GIT_DIR=.git` env-var assignment; executable is `git`; Phase 4 subcommand `commit`; BLOCKED (BC-1.18.002 EC-021). |
| EC-019 | `git --unknown-flag commit` while marker exists | AC-008/AC-009 path: Phase 3 `--unknown-flag` is not in recognized arg-taking or no-arg sets and contains no `=`; fail-safe posture → return `true` (block); subcommand position cannot be determined from the closed recognized set (BC-1.18.002 EC-022). |
| EC-020 | `git --config-env FOO=BAR commit` while marker exists | AC-008/AC-009 path: Phase 3 `--config-env` is in recognized arg-taking set; `FOO=BAR` consumed as its argument; Phase 4 subcommand is `commit`; BLOCKED (BC-1.18.002 EC-023). |
| EC-021 | `git "commit"` while marker exists | AC-008/AC-009 path: Phase 1b POSIX tokenization; double-quoted `"commit"` → token `commit` after quote removal; Phase 4 exact match; BLOCKED (BC-1.18.002 EC-024). |
| EC-022 | `git 'push' origin` while marker exists | AC-008/AC-009 path: Phase 1b POSIX tokenization; single-quoted `'push'` → token `push` after quote removal; Phase 4 exact match; BLOCKED (BC-1.18.002 EC-025). |
| EC-023 | `g'i't commit` while marker exists | AC-008/AC-009 path: Phase 1b POSIX token concatenation; unquoted `g` + single-quoted `i` + unquoted `t` → token `git`; Phase 2 basename `git`; Phase 4 subcommand `commit`; BLOCKED (BC-1.18.002 EC-026). |
| EC-024 | `$(git commit -m "x")` while marker exists | AC-009 path: outer command is a command substitution; `git` is not the statically-literal top-level token; OUT OF SCOPE by design (see BC-1.18.002 Threat Model & Scope); NOT blocked; allowed under fail-open posture; durable marker and other controls provide defense-in-depth (BC-1.18.002 EC-027). |
| EC-025 | `echo x \| xargs git commit` while marker exists | AC-009 path: Phase 1 splits on `\|`; segment 1 `echo x` — executable `echo` ≠ `git`, false; segment 2 `xargs git commit` — executable `xargs` ≠ `git`, false; OUT OF SCOPE by design — `xargs` indirection; NOT blocked (BC-1.18.002 EC-028). |
| EC-026 | `eval "git push origin main"` while marker exists | AC-009 path: Phase 2 executable is `eval`; basename `eval` ≠ `git`; `git push` is a string argument subject to dynamic evaluation; OUT OF SCOPE by design; NOT blocked; allowed under fail-open posture (BC-1.18.002 EC-029). |
| EC-030 | Gate plugin tries to read `.factory/unvalidated-mutation.marker` but encounters EACCES or other IO error (unreadable marker; BC-1.18.002 EC-030 v1.4) | Gate allows the dispatch (fail-open, exit_code=0). An unreadable marker MUST NOT block — INV2 self-lock avoidance: a gate that cannot read the marker cannot determine quarantine state and must not prevent forward progress. Distinct from EC-008 (write-fail) and from BC-1.18.002 EC-008 (readable-but-malformed → Block). Covered by VP-105 unit-test property. |
| EC-031 | Same plugin as named in marker, DIFFERENT non-empty artifact path produces PostToolUse Pass while marker exists (BC-1.18.003 EC-008) | Marker NOT cleared. Artifact A's quarantine persists — the named plugin validated artifact B successfully but artifact A was never re-validated. Gate (both Arm 1 and Arm 2) remains active. Clear requires the named plugin to subsequently produce PASS on artifact A (exact normalized absolute path equality), or operator manual deletion (`rm .factory/unvalidated-mutation.marker`). `delete_marker_if_pass` returns Ok(()) with no deletion when artifact_path mismatch. |
| EC-032 | Same plugin as named in marker, empty marker `artifact_path` field, PostToolUse Pass on any artifact (BC-1.18.003 EC-009) | Marker cleared (empty-path fallback). When the marker's `artifact_path` is empty, the artifact condition is vacuously satisfied; plugin_name equality alone suffices for clearance. Both Arm 1 (Agent) and Arm 2 (git commit/push) immediately unblocked. Covers non-artifact-scoped validators (e.g., wave-gate, path-staging validators) whose marker carries no artifact context because they validate global pipeline state, not a specific file. |
| EC-033 | Gate plugin crashes/fuel-exhausts (Arm 1 or Arm 2) while a non-expired `.factory/unvalidated-mutation.marker` exists (`expires_at` in future) (BC-1.18.002 PC5, BC-1.18.002 EC-031) | Block (exit_code=2). Dispatcher native `block_if_marker` check: non-expired marker present → BLOCK even though WASM plugin crashed (ADR-048 §Decision 1). Block message includes marker fields (`plugin_name`, `artifact_path`, `cause`, `expires_at`) plus three recovery options ordered by sanctioned preference (MUST NOT instruct agent to run `rm` — agent-tool rm is de-sanctioned per INV6/T4; AC-020): (a) Edit/Write re-validate the artifact named in `artifact_path` (T1 — primary agent recovery; inherently ungated even through gate crash; AC-020 T1); (b) wait for TTL expiry — 86400s from `expires_at` timestamp (T2 — passive self-healing; AC-020 T2); (c) human out-of-band `rm .factory/unvalidated-mutation.marker` in operator terminal (T3 — break-glass; never intercepted by dispatcher; AC-020 T3). |
| EC-034 | Gate plugin crashes/fuel-exhausts (Arm 1 or Arm 2) while marker is absent OR while an existing marker's `expires_at` has elapsed (UTC) (BC-1.18.002 PC6, BC-1.18.002 EC-009/EC-032) | Allow (exit_code=0). Dispatcher native `block_if_marker` check: no valid quarantine signal → ALLOW. No unconditional self-lock is possible: a crash with no valid quarantine signal cannot block indefinitely. Crash-handler I/O failures on the marker file (ENOENT, EACCES) are also treated as absent → Allow (conservative). |
| EC-035 | Gate plugin reads `.factory/unvalidated-mutation.marker` on normal execution path and finds `expires_at ≤ now (UTC)` (TTL elapsed; BC-1.18.003 PC4) | Allow (exit_code=0) + auto-delete the marker (idempotent; NotFound swallowed; other IO errors logged, no panic). Both Arm 1 (Agent) and Arm 2 (git commit/push) immediately unblocked. No `plugin_name` or `artifact_path` check required (INV5 — TTL is marker-level, not artifact-scoped). Legacy markers lacking `expires_at` field → treated as non-expired (conservative; NOT auto-deleted by this path). |
| EC-036 (NEW v1.14) | Non-fabrication negative control (ADR-048 §Decision 4 (F-P6-001 correction — historical provenance, non-load-bearing); BC-1.18.003 EC-017; direct regression test): a PreToolUse fail-closed INDETERMINATE (never attempts a marker write, BC-1.18.001 invariant 4) OR a PostToolUse marker-write I/O failure (EC-008) — neither of which ever emits `marker.written` — followed by `reconcile_raw_delete(factory_root, ctx)` while the marker is (correctly) absent | `reconcile_raw_delete` finds no unmatched `marker.written` for that `(plugin_name, artifact_path)` pair; ZERO `marker.cleared(OPERATOR_OVERRIDE)` events are emitted — no fabricated NIST AU-3/AU-10 audit record. This is the direct regression test that a scan matching the OLD `plugin.indeterminate` premise would have failed (AC-023; VP-108 postcondition 7). |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| Story spec (S-25.01) | 6,500 |
| ADR-047 full text (9 decisions + rationale + impl notes, v1.2) | 9,000 |
| ADR-048 (fail-closed-but-recoverable gate; block_if_marker crash policy; marker TTL deadman 86400s; ungated-escape invariant; §Decision 1/2/3/4 incl. cumulative §Decision 4 corrections — emission-mechanism precision, SUPERSEDED clear path, reconciliation-premise, and SUPERSEDED emission-point — historical provenance, non-load-bearing) | 4,100 |
| ADR-039 relevant sections (§D1 failure_policy semantics, §D3 calibration prerequisites) | 3,500 |
| BC-1.18.001 v1.5 (INDETERMINATE outcome, marker write, Store flag, expires_at 6th TOML field; ADR-048 §Decision 2; v1.2 PC4 auto-delete-locus narration correction; v1.3 invariant 3 SUPERSEDED-clear corollary; v1.4 PC4 marker.written audited creation event; v1.5 SUPERSEDED-clear corollary emission-point correction — SUPERSEDED + marker.written co-emit in the Ok(()) arm) | 4,000 |
| BC-1.18.002 v1.7 (next-advance gate, two arms, v1.3 Phase 1/1b/2/3/4 command filter, block_if_marker crash policy, PC5/PC6, four-tier recovery model T1/T2/T3/T4-de-sanctioned, two-axis model, threat model & scope, 32+ ECs + extended canonical test vectors; v1.7 T2/TTL narration-locus correction) | 5,000 |
| BC-1.18.003 v1.7 (marker-clear protocol, artifact-scoped clear, empty-path fallback, idempotent delete, TTL-expiry PC4 TTL-loudness, INV5 marker-level TTL, legacy-marker backward-compat, audited clear model: marker.cleared REVALIDATED/TTL_EXPIRED/OPERATOR_OVERRIDE/SUPERSEDED, RAW_DELETE_DETECTED reconciliation; v1.4 dispatcher-native emission-point correction; v1.5 emission-mechanism precision correction (`ctx.emit_internal`, `dispatcher-internal-{date}.jsonl` scan target) + new PC5 SUPERSEDED clear path; v1.6 reconciliation-premise correction — PC3 retargeted to unmatched marker.written + new EC-017 non-fabrication; v1.7 PC5 emission-point correction — SUPERSEDED emitted only after Ok(()) + new EC-018 non-fabrication) | 4,500 |
| BC-1.18.004 (fail-open backward-compat, DO-NOT-DELETE guard) | 1,600 |
| BC-3.08.001 §Events 8+9+10 (plugin.indeterminate Event 8 wire format; marker.cleared Event 9 wire format: clear_mode/actor_type table, trace_id semantics, reason nullability, mandatory fields; marker.written Event 10 wire format — NEW; v1.31 emission-point cell correction; v1.32 SUPERSEDED/system enum additions + emit_internal sink-destination prose correction; v1.33 new Event 10 catalog entry + Event 9 OPERATOR_OVERRIDE retarget to marker.written; v1.34 Event 9 SUPERSEDED trigger emission-point correction — no field-shape change to any Event 8/9/10 fields) | 3,100 |
| VP-102 through VP-108 (proof harness skeletons, 7 VPs incl. VP-107 ungated-escape invariant + VP-108 v1.4 marker.cleared/marker.written audited-emission correctness incl. PC5 SUPERSEDED (write-then-emit ordered) + PC6 marker.written write-path + PC7 non-fabrication negative control + PC8 SUPERSEDED write-failure negative control + `HostContext::emit_internal`/`drain_events` harness) | 8,600 |
| `crates/factory-dispatcher/src/executor.rs` (current) | 4,000 |
| `crates/factory-dispatcher/src/invoke.rs` (current, StoreData) | 3,500 |
| `crates/factory-dispatcher/src/registry.rs` (current, RegistryEntry) | 2,500 |
| `plugins/vsdd-factory/hooks-registry.toml` (Cohort A entries + surrounding context) | 3,000 |
| F1 delta analysis §9 (Red Gate tests), §10 (impact boundary), §11 (fail-closed assignments) | 3,500 |
| **Total** | **~67,900** |
| Agent context window | 200,000 (Sonnet 4.6) |
| **Budget usage** | **~34%** |

> Budget is just past the 30% envelope boundary after ADR-048's §Decision 4 corrections + BC amendments. Implementer MUST load ONLY the
> sources listed above — no additional architecture files, no BC files beyond the 5 listed, no
> additional crate source files beyond executor.rs / invoke.rs / registry.rs. If context pressure
> is felt, drop ADR-039 background sections (keep §D1 + §D3 only) and drop ADR-047 rationale
> sections (keep §D1/D4→D5/D9 and the superseded-by-ADR-048 note).

## Tasks (MANDATORY)

**T-1: executor.rs — INDETERMINATE outcome classification**
- [ ] Add `DispatchOutcome::Indeterminate { cause: IndeterminateCause }` variant (or
  equivalent structured form) to the outcome enum in `executor.rs`
- [ ] Implement `classify_outcome(plugin_result: PluginResult, policy: FailurePolicy,
  output_too_large: bool) -> DispatchOutcome` pure function with:
  - Fuel trap: `trap.downcast_ref::<Trap>() == Some(&Trap::OutOfFuel)` → `Indeterminate { cause: Fuel }`
  - Epoch trap: `trap.downcast_ref::<Trap>() == Some(&Trap::Interrupt)` → `Indeterminate { cause: Epoch }`
  - OutputTooLarge: `output_too_large && exit_code == 0` → `Indeterminate { cause: OutputTooLarge }`
  - Wildcard Trap arm: `_ => { /* NOT Indeterminate — route to existing on_error handling */ }` (BC-1.18.001 invariant 2 — `Trap` is `#[non_exhaustive]`; future variants must not be silently bucketed as INDETERMINATE)
  - PASS: `exit_code == 0 && !output_too_large`
  - FAIL: `exit_code != 0`
- [ ] Wire `classify_outcome` into the executor invocation loop replacing the old outcome derivation
- [ ] Write RED Gate stub: `fn test_BC_1_18_001_fuel_exhaustion_yields_indeterminate_for_fail_closed_plugin()`
- [ ] Write RED Gate stub: `fn test_BC_1_18_001_epoch_timeout_yields_indeterminate_for_fail_closed_plugin()`
- [ ] Write RED Gate stub: `fn test_BC_1_18_001_output_too_large_then_ok_yields_indeterminate_for_fail_closed_plugin()`
- [ ] Write RED Gate stub: `fn test_BC_1_18_001_indeterminate_is_distinct_from_pass_and_fail()`
- [ ] Write RED Gate stub: `fn test_BC_1_18_001_indeterminate_writes_marker_to_factory_path()`
- [ ] Write RED Gate stub: `fn test_BC_1_18_001_marker_contains_required_fields()`
- [ ] Write RED Gate stub: `fn test_BC_1_18_004_fail_open_indeterminate_writes_no_marker()` (alias:
  `test_BC_1_18_004_fail_open_default_preserves_advisory_behavior` — cross-reference in comment)
- [ ] Write RED Gate stub: `fn test_backward_compat_pass_fail_on_error_semantics_unchanged()`
- [ ] Write RED Gate stub: `fn test_BC_1_18_001_unrecognized_trap_routes_to_on_error_not_indeterminate()` (AC-004 + BC-1.18.001 invariant 2 — wildcard arm routes to on_error, NOT INDETERMINATE)

**T-2: invoke.rs — OutputTooLarge Store flag + per-invocation reset + FileSink event emission**
- [ ] Add `host_output_too_large_seen: bool` field to `StoreData` struct
- [ ] Add `reset_output_too_large_flag(&mut store)` helper (or inline reset) that sets the flag
  to `false`; call it IMMEDIATELY BEFORE each `func.call(...)` invocation — NOT only at Store
  creation
- [ ] Set `store.data_mut().host_output_too_large_seen = true` in all host function wrappers that
  return `OutputTooLarge(-3)` to the WASM guest (read_file, read_prefix, and any future host
  function that can return −3)
- [ ] Wire `plugin.indeterminate` event emission to the OTel-aligned FileSink (`events-*.jsonl`)
  for EVERY INDETERMINATE outcome (both fail-closed and fail-open paths); event MUST include all
  BC-3.08.001 Event 8 mandatory fields: `type`, `trace_id`, `session_id`, `plugin_name`,
  `artifact_path` (empty string when no artifact context — never omit the field), `cause`,
  `failure_policy`, `timestamp` — this is the production implementation that makes
  `test_BC_1_18_001_emits_plugin_indeterminate_event_with_required_fields` pass (AC-006)
- [ ] Write RED Gate stub: `fn test_BC_1_18_001_emits_plugin_indeterminate_event_with_required_fields()`
  (traces to AC-006 + BC-3.08.001 Event 8; FileSink receives event with all 8 mandatory fields
  for both fail-closed and fail-open INDETERMINATE outcomes)
- [ ] Write RED Gate stub: `fn test_BC_1_18_001_output_too_large_then_ok_yields_indeterminate_for_fail_closed_plugin()`
  (NOTE: this stub is written in executor.rs test module per T-1 above; T-2 documents the
  production implementation in invoke.rs that makes it pass)
- [ ] Capture the `host_output_too_large_seen` value from StoreData AFTER `func.call()` completes
  (before the next per-invocation reset) and pass it to `classify_outcome`

**T-3: indeterminate_marker.rs (NEW FILE) — marker write/clear/predicate**
- [ ] Create `crates/factory-dispatcher/src/indeterminate_marker.rs`
- [ ] Implement `write_indeterminate_marker(fields: &MarkerFields, marker_path: &Path) -> io::Result<()>`:
  - Write to `<marker_path>.tmp` (same directory as target)
  - Serialize 6 TOML fields: timestamp (RFC 3339), plugin_name, artifact_path (empty string
    allowed, never omit), cause ("fuel" | "epoch" | "output-too-large"), trace_id,
    expires_at (RFC 3339 UTC = timestamp + UNVALIDATED_MUTATION_MARKER_TTL_SECONDS; define
    `UNVALIDATED_MUTATION_MARKER_TTL_SECONDS: u64 = 86_400` as a constant in this module;
    ADR-048 §Decision 2; enables dispatcher-native TTL clear via `check_and_clear_expired_marker`
    per BC-1.18.003 PC4 and dispatcher native block_if_marker TTL check per BC-1.18.002 PC5/PC6)
  - Atomic rename `.tmp` → final path
- [ ] Implement `delete_marker_if_pass(marker_path: &Path) -> io::Result<()>`:
  - `fs::remove_file(marker_path)` with `io::ErrorKind::NotFound` mapped to `Ok(())`
  - On successful removal, call `emit_marker_cleared(ClearMode::Revalidated, ...)` (AC-022)
- [ ] Implement `should_write_marker(outcome: &DispatchOutcome, policy: FailurePolicy) -> bool`:
  - Returns `true` iff outcome is Indeterminate AND policy is FailClosed
- [ ] Implement `emit_marker_cleared(ctx: &HostContext, marker_fields: &MarkerFields, clear_mode: &str, actor_type: &str, reason: Option<&str>) -> Result<()>`
  (ADR-048 §Decision 4; AC-021/AC-022/AC-023/AC-024): constructs a
  `marker.cleared` `InternalEvent` and routes it through `ctx.emit_internal` (the SAME dual-sink
  helper every other dispatcher-native BC-3.08.001 event uses — durable
  `dispatcher-internal-{date}.jsonl` write + `ctx.events` queue) — MUST NOT call the WASM
  `emit_event` host ABI (never from this function or from the WASM gate plugin) and MUST NOT call
  a raw `InternalLog::write` directly (ADR-048 §Decision 4 (F-P3-001 correction — historical provenance, non-load-bearing)), so RESERVED_FIELDS enrichment
  never applies and the marker's own (foreign) `trace_id`/`plugin_name` can be carried on the wire;
  `clear_mode` values: `"REVALIDATED" | "TTL_EXPIRED" | "OPERATOR_OVERRIDE" | "SUPERSEDED"`
  (`Superseded` added to the `ClearMode` enum in v1.13; ADR-048 §Decision 4 (F-P3-002 correction — historical provenance, non-load-bearing))
- [ ] Implement `check_and_clear_expired_marker(factory_root: &Path, now: DateTime<Utc>, ctx: &HostContext) ->
  io::Result<Option<MarkerFields>>` (ADR-048 §Decision 4; AC-021): dispatcher-native TTL
  pre-check — reads the marker via `read_all_marker_fields`; if present and `expires_at ≤ now`,
  deletes it (idempotent, swallow `NotFound`), calls `emit_marker_cleared(ctx, &fields,
  "TTL_EXPIRED", "deadman", None)`, and returns `Some(fields)`; missing or non-expired `expires_at`
  → returns `None` without deleting (legacy markers without `expires_at` are conservatively
  non-expired); `ctx: &HostContext` parameter added in v1.13 (ADR-048 §Decision 4, F-P3-001
  correction — historical provenance, non-load-bearing; signature widening)
- [ ] Implement `reconcile_raw_delete(factory_root: &Path, ctx: &HostContext) -> io::Result<()>`
  (ADR-048 §Decision 4; AC-023): dispatcher-native OPERATOR_OVERRIDE reconciliation, invoked
  from `check_and_clear_expired_marker`'s marker-absent branch; bounded, best-effort scan of the
  CURRENT DAY's `dispatcher-internal-{date}.jsonl` only (obtained via `ctx.internal_log`; MUST cap
  total bytes/records read via a fixed-size tail read — never a full-file or multi-day scan) for an
  unmatched **`marker.written`** (v1.14 — retargeted from unmatched `plugin.indeterminate`; the
  redundant `failure_policy` filter is REMOVED, per ADR-048 §Decision 4 (F-P6-001 correction — historical provenance, non-load-bearing)); if found, calls
  `emit_marker_cleared(ctx, &fields, "OPERATOR_OVERRIDE", "operator", Some(reason))` with the
  mandatory RAW_DELETE_DETECTED `reason` string; best-effort — `ctx.internal_log` is `None` or no
  unmatched record found → no-op, no hard failure. Scan target corrected in v1.13 from a literal
  `events-*.jsonl` to `dispatcher-internal-{date}.jsonl` per ADR-048 §Decision 4 (F-P3-001 correction — historical provenance, non-load-bearing); scan MATCH
  TYPE corrected in v1.14 to `marker.written` per ADR-048 §Decision 4 (F-P6-001 correction — historical provenance, non-load-bearing) — see story EC-036 for
  the non-fabrication regression case this closes
- [ ] Implement `emit_marker_written(ctx: &HostContext, fields: &MarkerFields)` (NEW in v1.14;
  ADR-048 §Decision 4 (F-P6-001 correction — historical provenance, non-load-bearing); AC-025):
  constructs a `marker.written` `InternalEvent`
  (`type = "marker.written"`, `trace_id`/`plugin_name`/`artifact_path`/`cause`/`expires_at` from the
  marker's own fields) and routes it through `ctx.emit_internal` — the SAME dual-sink helper
  `emit_marker_cleared` uses; MUST NOT call the WASM `emit_event` host ABI and MUST NOT call a raw
  `InternalLog::write` directly
- [ ] Implement `emit_superseded_if_cross_pair(existing: Option<&MarkerFields>, new_pair: (&str, &str), ctx: &HostContext, reason: &str)`
  (NEW in v1.13; ADR-048 §Decision 4 (F-P3-002 correction — historical provenance,
  non-load-bearing); AC-024): if `existing`'s
  `(plugin_name, artifact_path)` differs from `new_pair`, calls `emit_marker_cleared(ctx,
  existing_fields, "SUPERSEDED", "system", Some(reason))` carrying the EXISTING (superseded)
  pair's own `trace_id`/`plugin_name`/`artifact_path` — never the new pair's; no-op when the pair
  is the same (same-pair re-INDETERMINATE — no event) or no existing marker is present. **Call-site
  emission point corrected this burst (ADR-048 §Decision 4 (F-P9-001 correction — historical provenance, non-load-bearing)):** called from the marker-write
  callsite (`executor.rs`, at BOTH `write_indeterminate_marker` call sites) ONLY inside
  `write_indeterminate_marker`'s `Ok(())` arm — never before the write is attempted, never on
  `Err(_)` — immediately BEFORE the `emit_marker_written` call in that same arm; the pre-overwrite
  field READ via `read_all_marker_fields` remains BEFORE the write (unavoidable, unchanged), only
  the EMIT moved
- [ ] Wire `executor.rs`'s tier-execution loop to call `check_and_clear_expired_marker(factory_root,
  now, ctx)` (passing `base_ctx: &HostContext`, already in scope at this call site; which
  internally invokes `reconcile_raw_delete(factory_root, ctx)` on its marker-absent branch) for
  every registry entry with `on_error == OnError::BlockIfMarker` BEFORE that entry's Arm 1/Arm 2
  WASM gate plugin is invoked, on the normal (non-crash) path only — this pre-check is a SEPARATE
  code path from the crash-path `block_if_marker_check` (Decision 1)
- [ ] Wire `executor.rs`'s marker-write callsite(s) for INDETERMINATE fail-closed outcomes to, at
  BOTH `write_indeterminate_marker` call sites: (a) read the pre-overwrite marker's fields via
  `read_all_marker_fields` BEFORE calling `write_indeterminate_marker` (unavoidable — unchanged
  from v1.13); (b) call `write_indeterminate_marker(&new_fields, &marker_path)`; (c) **ONLY on the
  `Ok(())` arm**, if a pre-overwrite marker existed for a DIFFERENT `(plugin_name, artifact_path)`
  pair, call `emit_superseded_if_cross_pair(existing, new_pair, ctx, reason)` FIRST — emitting
  `marker.cleared(SUPERSEDED)` for the superseded pair; (d) on `Err(_)`, call NEITHER
  `emit_superseded_if_cross_pair` NOR `emit_marker_written` (see next item) — log via
  `tracing::warn!` and stop (AC-024; ADR-048 §Decision 4 (F-P3-002 correction — historical provenance, non-load-bearing) origin + **§D4 v1.5 F-P9-001
  emission-point correction this burst** — relocates the call from before the write to inside the
  `Ok(())` arm; `HostContext` is already in scope at both call sites, so no new plumbing is
  introduced)
- [ ] Wire the SAME `executor.rs` marker-write callsite(s) so that, immediately after
  `write_indeterminate_marker` returns `Ok(())` — and immediately AFTER the (possibly no-op)
  `emit_superseded_if_cross_pair` call above, in the SAME `Ok(())` arm — it calls
  `emit_marker_written(ctx, &fields)` — never on the `Err(_)` arm (AC-025; ADR-048 §Decision 4,
  F-P6-001 correction — historical provenance, non-load-bearing; NEW in v1.14; `ctx` already in
  scope, no new plumbing). **This burst (ADR-048 §Decision 4, F-P9-001 correction — historical
  provenance, non-load-bearing):** both write-tied calls now live in the same `Ok(())` arm, in
  fixed order —
  SUPERSEDED (if applicable) first, then `marker.written` always second, at BOTH call sites.
- [ ] Write RED Gate stub: `fn test_BC_1_18_003_successful_revalidation_deletes_marker()`
- [ ] Write RED Gate stub: `fn test_BC_1_18_004_fail_open_indeterminate_writes_no_marker()` (if not
  already in executor.rs tests — consolidate per project test organization convention)
- [x] Red Gate test (AC-021, `TTL_EXPIRED` marker-level deadman):
  `test_ADR_048_D4_check_and_clear_expired_marker_ttl_expired_deletes_and_emits` in
  `crates/factory-dispatcher/src/indeterminate_marker.rs` — implemented and passing.
  Documentary reconciliation (D-1141): closes the AC-021..AC-025 dedicated-stub gap flagged
  since v1.10 (v1.12/v1.13/v1.14/v1.15 "Known gap" notes below) by enumerating the actual
  implementing test, not by fabricating a new stub.
- [x] Red Gate test (AC-022, `REVALIDATED` clear):
  `test_BC_1_18_003_named_plugin_pass_clears_marker_via_execute_tiers` in
  `crates/factory-dispatcher/tests/marker_integration.rs` — implemented and passing (D-1141).
- [x] Red Gate test (AC-023, `OPERATOR_OVERRIDE` reconcile):
  `test_ADR_048_D4_reconcile_raw_delete_unmatched_event_emits_operator_override` in
  `crates/factory-dispatcher/src/indeterminate_marker.rs` — implemented and passing (D-1141).
- [x] Red Gate test (AC-024, `SUPERSEDED` cross-pair overwrite):
  `test_ADR_048_D4_v1_3_superseded_clear_emits_for_cross_pair_overwrite` (positive path) and
  `test_ADR_048_D4_PC8_no_emit_on_cross_pair_write_failure` (EC-018/PC8 write-failure negative
  control) in `crates/factory-dispatcher/src/indeterminate_marker.rs` — implemented and
  passing (D-1141).
- [x] Red Gate test (AC-025, `marker.written` emission):
  `test_marker_written_emitted_on_successful_write_only` (positive path) and
  `test_pretooluse_indeterminate_without_marker_written_does_not_fabricate_operator_override`
  (PreToolUse no-fabricate negative control) in
  `crates/factory-dispatcher/src/indeterminate_marker.rs` — implemented and passing (D-1141).
- [ ] Expose module: add `mod indeterminate_marker;` to `crates/factory-dispatcher/src/lib.rs`

**T-4: validate-unvalidated-mutation-marker (NEW WASM PLUGIN CRATE)**
- [ ] Create `crates/hook-plugins/validate-unvalidated-mutation-marker/Cargo.toml` as a WASM
  crate (target = `wasm32-unknown-unknown` or `wasm32-wasi`; match existing hook plugin crates)
- [ ] Create `crates/hook-plugins/validate-unvalidated-mutation-marker/src/lib.rs`:
  - `guard_logic::evaluate_gate(marker_path: &Path) -> GateDecision` — PURE marker-presence check
    (ADR-048 §Decision 4 (Emission-Point Correction, F-P2-002, human-ratified 2026-08-31 — historical provenance, non-load-bearing)): reads marker, parses TOML, extracts fields (plugin_name, artifact_path,
    cause) for the block message ONLY; MUST NOT parse or evaluate `expires_at`, MUST NOT delete
    the marker file, and MUST NOT scan the FileSink log or call `emit_marker_cleared` — TTL
    detection/auto-delete (`check_and_clear_expired_marker`) and RAW_DELETE_DETECTED
    reconciliation (`reconcile_raw_delete`) are entirely dispatcher-native and run BEFORE this
    function is ever invoked on the normal path; this crate never crosses the `emit_event` host
    ABI's RESERVED_FIELDS enrichment wall for `marker.cleared`
  - `guard_logic::is_git_commit_or_push(command: &str) -> bool` — Phase 1/1b/2/3/4 algorithm (BC-1.18.002 PC2 v1.3): Phase 1 compound splitting (`&&`/`||`/`;`/`|`/`&`/newline), Phase 1b POSIX quote-aware tokenization via `shell-words` crate (`git "commit"` → `["git","commit"]`; `g'i't commit` → `["git","commit"]`; ad-hoc quote-stripping forbidden), Phase 2 basename identification (path-prefixed `/usr/bin/git`, `./git`; env-prefix stripped), Phase 3 global-option skip (seven arg-taking options: `-C`, `-c`, `--namespace`, `--git-dir`, `--work-tree`, `--super-prefix`, `--config-env`; recognized no-arg set; fail-safe on unrecognized options), Phase 4 exact subcommand match (`commit` or `push` only; `commit-graph` does NOT match)
  - Arm 1 entry point (fires on `^Agent$` PreToolUse): calls evaluate_gate; if marker present →
    return block message with 5 fields + recovery command + escape hatch
  - Arm 2 entry point (fires on `^Bash$` PreToolUse + git filter): calls `is_git_commit_or_push`;
    if match AND marker present → same block message format
  - Both entry points return `exit_code = 0` (allow) when marker absent
- [ ] Add crate to `crates/hook-plugins/Cargo.toml` workspace members (or top-level `Cargo.toml`)
- [ ] Write RED Gate stub: `fn test_BC_1_18_002_blocks_agent_dispatch_when_marker_exists()`
- [ ] Write RED Gate stub: `fn test_BC_1_18_002_passes_agent_dispatch_when_no_marker()`
- [ ] Write RED Gate stub: `fn test_BC_1_18_002_block_message_names_plugin_and_artifact()`
- [ ] Build WASM binary: `cargo build --target wasm32-unknown-unknown --release -p validate-unvalidated-mutation-marker`
- [ ] Copy WASM binary to `plugins/vsdd-factory/hook-plugins/validate-unvalidated-mutation-marker.wasm`
  (or wherever existing hook plugin WASMs live — confirm from hooks-registry.toml `plugin` field entries)

**T-5: hooks-registry.toml updates**
- [ ] Add Arm 1 entry (canonical key names per actual hooks-registry.toml schema: `event`, `tool`,
  `plugin`, `async`; NOT `trigger`/`tool_pattern`/`wasm_path`/`tier`):
  ```toml
  [[hooks]]
  name = "validate-unvalidated-mutation-marker"
  event = "PreToolUse"
  tool = "^Agent$"
  plugin = "hook-plugins/validate-unvalidated-mutation-marker.wasm"
  on_error = "block_if_marker"
  async = false
  failure_policy = "fail-open"
  ```
- [ ] Add Arm 2 entry (same WASM binary as Arm 1 per BC-1.18.002 invariant 1; `command_filter`
  is NOT a registry field — the Phase 1/1b/2/3/4 algorithm (BC-1.18.002 PC2 v1.3) is implemented
  INSIDE the WASM plugin by reading the `command` parameter from the Bash tool event payload):
  ```toml
  [[hooks]]
  name = "validate-unvalidated-mutation-marker-git"
  event = "PreToolUse"
  tool = "^Bash$"
  plugin = "hook-plugins/validate-unvalidated-mutation-marker.wasm"
  on_error = "block_if_marker"
  async = false
  failure_policy = "fail-open"
  ```
  (NOTE: Same WASM binary for both arms per BC-1.18.002 invariant 1 and AC-019; `on_error =
  "block_if_marker"` is load-bearing — NOT "continue" or "block" — per BC-1.18.002 PC1 v1.5
  and AC-011 (ADR-039; ADR-047 §Decision 9; ADR-048 §Decision 1): dispatcher-level
  block_if_marker crash policy: BLOCK iff non-expired marker exists on crash; ALLOW if
  absent/expired/crash-IO-fail; `failure_policy = "fail-open"` governs the orthogonal axis
  (gate's own INDETERMINATE class per BC-1.18.004); the Phase 1/1b/2/3/4 command filter
  (BC-1.18.002 PC2 v1.3) is implemented INSIDE the WASM plugin by reading the `command`
  parameter from the Bash tool event payload — it is NOT a registry field)
- [ ] Set `failure_policy = "fail-closed"` for `validate-factory-path-staging` (unconditional)
- [ ] Set `failure_policy = "fail-closed"` for `validate-pr-merge-prerequisites` and
  `validate-wave-gate-prerequisite` with registry comment (Cohort A-deferred per AC-016):
  `# ACTIVATION CONDITIONAL on S-21.24 calibration per ADR-047 §D8a Cohort A-deferred`

**T-6: Integration tests (bats) and Kani proof harnesses**
- [ ] Create `plugins/vsdd-factory/tests/validate-unvalidated-mutation-marker.bats` with helper
  functions `run_dispatcher_pretooluse_agent` and `run_dispatcher_pretooluse_bash`, and test cases
  VP-105-A through VP-105-G (7 bats tests as specified in VP-105 proof harness skeleton)
- [ ] Verify VP-102 proof harness skeleton in executor.rs test module covers the 5 unit test cases
  from VP-102 spec
- [ ] Run full suite: `cargo test --workspace --all-targets && cd plugins/vsdd-factory/tests && ./run-all.sh`
- [ ] Confirm full test suite stays green (backward-compat regression: all existing tests pass;
  new Red Gate tests FAIL before implementation; new tests PASS after implementation)

**T-7: CHANGELOG delivery (MANDATORY — before PR)**
- [ ] Add CHANGELOG entry under `[Unreleased]` > `Added`:
  `Dispatcher: INDETERMINATE outcome class for fail-closed plugins — fuel/epoch/OutputTooLarge cannot-complete events now write a durable \`.factory/unvalidated-mutation.marker\`; \`validate-unvalidated-mutation-marker\` gate blocks next Agent dispatch and \`git commit\`/\`git push\` while marker exists; successful re-validation clears marker; existing ~76 fail-open plugins unchanged (advisory event only); Cohort A validators assigned fail-closed (ADR-047 Layer 1; BC-1.18.001–004, BC-3.08.001 Event 8)`

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-21.10 (MERGED, PR #780, depends_on) | Introduced `FailurePolicy` enum (`FailOpen` / `FailClosed`) and `RegistryEntry.failure_policy` field into the registry parsing + executor dispatch path. ADR-039 §Decision 1 + §Decision 3. No behavioral change shipped — schema-only extension per ADR-039 Phase 1. | `FailurePolicy::default() == FailOpen` is canonical and load-bearing. Never change this default without a new ADR. The `failure_policy` field is read at dispatch time from the `RegistryEntry` — not from the WASM binary's own metadata. | S-21.10 is schema-only (Phase 1). The enforcement logic (reading the field and acting on it) is S-25.01's responsibility. Do NOT assume the executor already routes on failure_policy — confirm by reading executor.rs before writing. |
| N/A (first story in epic E-25 besides S-21.10 predecessor) | — | — | — |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| `classify_outcome` MUST use `Trap::OutOfFuel` downcast, NOT `get_fuel()` for fuel detection | BC-1.18.001 §Architecture Anchors; ADR-047 §D1 implementation note | AC-001; test_BC_1_18_001_fuel_exhaustion_yields_indeterminate_for_fail_closed_plugin covers this |
| `host_output_too_large_seen` is dispatcher-internal StoreData ONLY — NOT host-sdk ABI | BC-1.18.001 invariant 5; ADR-047 §D6; F1-delta-analysis §8 | AC-018; do NOT expose this field in hook-sdk; no HOST_ABI_VERSION bump required for this change. |
| Marker write is PostToolUse only | BC-1.18.001 invariant 4 | AC-005; architecture mapping table (PostToolUse constraint); EC-002 |
| Gate plugin (`validate-unvalidated-mutation-marker`) MUST be registered with `on_error = "block_if_marker"` AND `failure_policy = "fail-open"` — NOT `on_error = "continue"` or `on_error = "block"` (ADR-048 §Decision 1 supersedes v1.3 on_error="continue"; block_if_marker: BLOCK iff non-expired marker on crash; ALLOW if absent/expired/crash-IO-fail; two-axis model — failure_policy governs WASM INDETERMINATE class, on_error governs crash path; orthogonal per ADR-039; self-lock avoided by no-valid-signal→Allow branch; three ungated escapes per INV6/AC-020) | BC-1.18.002 precondition 1 + invariant 2 (v1.5); ADR-048 §Decision 1; ADR-047 §Decision 9; ADR-039 two-axis model | AC-011; T-5 hooks-registry entries; both gate arms |
| Same WASM binary for both gate arms — two hooks-registry entries, one binary | BC-1.18.002 invariant 1 | AC-019; T-4 and T-5; one compiled `.wasm` file serves both entries |
| The three Cohort A validators are the ONLY fail-closed assignments in S-25.01 | ADR-047 §D8a v1.4 — human-confirmed Cohort A (1 A-immediate + 2 A-deferred; Layer-1 effective fail-closed count corrected to ZERO in v1.4); BC-1.18.004 PC4 v1.2 | AC-016; T-5 registry entries; no other plugin may receive fail-closed in this PR |
| `test_BC_1_18_004_fail_open_default_preserves_advisory_behavior` MUST NOT be deleted | ADR-047 §Decision 7; BC-1.18.004 PC5 | AC-017; PR review will verify test presence; name (or alias with cross-reference comment) required |
| Trap match wildcard arm MUST route to existing on_error handling — NOT produce INDETERMINATE | BC-1.18.001 invariant 2 (non-exhaustive Trap variants) | AC-004; implementer must use `_ => { /* route to on_error */ }` NOT `_ => Indeterminate` or `_ => unreachable!()` |
| No TD-VSDD-060 sibling-site violations: when changing executor outcome routing, grep ALL callsites that pattern-match on DispatchOutcome variants | TD-VSDD-060 sibling-site sweep; CLAUDE.md Self-Audit Checklist | Implementer must grep for all match arms on DispatchOutcome before committing |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| wasmtime | 46.0.2 | WASM runtime (fuel, epoch, Trap variants) — S-21.12 bump to 46.0.2 (PR #781). Do NOT downgrade. |
| serde | workspace version | Serialization for 5-field TOML marker write |
| toml (crate) | workspace version | TOML serialization of `.factory/unvalidated-mutation.marker` |
| tracing | workspace version | Structured logging for INDETERMINATE path (no `println!` in production code per CLAUDE.md) |
| shell-words | 1.1 | Phase 1b POSIX quote-aware tokenization in is_git_commit_or_push (WASM gate plugin); replaces split_whitespace to close the quoting class without a parser-differential |
| tempfile or std::fs + rename | std::fs (std only) | Atomic marker write: write to `.tmp` then `rename` — no extra crate needed if using `std::fs::rename` |

**Forbidden dependencies for `crates/factory-dispatcher`:**
- MUST NOT gain a dependency on `hook-sdk` for the `host_output_too_large_seen` flag —
  the flag is dispatcher-internal StoreData only (BC-1.18.001 invariant 5; AC-018). If this
  dependency appears, the build MUST fail (enforce via deny-check or review).
- MUST NOT depend on any crate that introduces a HOST_ABI_VERSION change for this story's
  changes (the StoreData extension is internal and does not change the ABI contract).

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| `crates/factory-dispatcher/src/executor.rs` | modify | Add `DispatchOutcome::Indeterminate`, `classify_outcome` pure fn, wire into invocation loop, 12 Red Gate test stubs (10 original + 2 added in audit: stubs 14 + 15) |
| `crates/factory-dispatcher/src/invoke.rs` | modify | Add `StoreData::host_output_too_large_seen: bool`, per-invocation reset before `func.call()`, set in OTL-returning host fn wrappers |
| `crates/factory-dispatcher/src/indeterminate_marker.rs` | create | `write_indeterminate_marker`, `delete_marker_if_pass`, `should_write_marker`, `emit_marker_cleared`, `check_and_clear_expired_marker`, `reconcile_raw_delete`, `emit_superseded_if_cross_pair`, `emit_marker_written` (NEW in v1.14; ADR-048 §Decision 4 — all four clear-mode emissions (REVALIDATED/TTL_EXPIRED/OPERATOR_OVERRIDE/SUPERSEDED) plus the new `marker.written` creation emission, and the TTL/RAW_DELETE_DETECTED/SUPERSEDED dispatcher-native logic, live here, never in the WASM gate plugin; emissions route through `HostContext::emit_internal`; `reconcile_raw_delete`'s scan matches unmatched `marker.written`, not unmatched `plugin.indeterminate`) |
| `crates/factory-dispatcher/src/lib.rs` | modify | Add `pub mod indeterminate_marker;` |
| `crates/hook-plugins/validate-unvalidated-mutation-marker/Cargo.toml` | create | New WASM hook plugin crate |
| `crates/hook-plugins/validate-unvalidated-mutation-marker/src/lib.rs` | create | `guard_logic::evaluate_gate`, `guard_logic::is_git_commit_or_push`, Arm 1 + Arm 2 entry points, 3 Red Gate test stubs |
| `crates/Cargo.toml` (or workspace root `Cargo.toml`) | modify | Add `validate-unvalidated-mutation-marker` to workspace members |
| `plugins/vsdd-factory/hook-plugins/validate-unvalidated-mutation-marker.wasm` | create (build artifact) | Compiled WASM binary from T-4; must be committed to the plugin hooks directory |
| `plugins/vsdd-factory/hooks-registry.toml` | modify | Add 2 gate entries (Arm 1 + Arm 2); set Cohort A fail-closed (3 entries); add activation comment for 2 conditional entries |
| `plugins/vsdd-factory/tests/validate-unvalidated-mutation-marker.bats` | create | 7 bats integration tests (VP-105-A through VP-105-G) |
| `CHANGELOG.md` | modify | T-7 mandatory CHANGELOG entry under [Unreleased] > Added |

## Red Gate Test Inventory (ALL MUST BE FAILING STUBS BEFORE IMPLEMENTATION)

### executor.rs test module (12 stubs)

```rust
#[test]
fn test_BC_1_18_001_fuel_exhaustion_yields_indeterminate_for_fail_closed_plugin() {
    todo!()
}

#[test]
fn test_BC_1_18_001_epoch_timeout_yields_indeterminate_for_fail_closed_plugin() {
    todo!()
}

#[test]
fn test_BC_1_18_001_output_too_large_then_ok_yields_indeterminate_for_fail_closed_plugin() {
    todo!()
}

#[test]
fn test_BC_1_18_001_indeterminate_is_distinct_from_pass_and_fail() {
    todo!()
}

#[test]
fn test_BC_1_18_001_indeterminate_writes_marker_to_factory_path() {
    todo!()
}

#[test]
fn test_BC_1_18_001_marker_contains_required_fields() {
    todo!()
}

#[test]
fn test_BC_1_18_003_successful_revalidation_deletes_marker() {
    todo!()
}

#[test]
fn test_BC_1_18_004_fail_open_indeterminate_writes_no_marker() {
    // Canonical backward-compat guard test. Cross-reference: BC-1.18.004 PC5 canonical name is
    // test_BC_1_18_004_fail_open_default_preserves_advisory_behavior. Both names acceptable per
    // BC-1.18.004 PC5 allowance — this is the canonical alias. DO NOT DELETE. ADR-047 §D7.
    todo!()
}

#[test]
fn test_BC_1_18_004_fail_open_default_preserves_advisory_behavior() {
    // Canonical backward-compat guard test per ADR-047 §Decision 7. DO NOT DELETE.
    // Cross-reference: test_BC_1_18_004_fail_open_indeterminate_writes_no_marker above.
    todo!()
}

#[test]
fn test_backward_compat_pass_fail_on_error_semantics_unchanged() {
    todo!()
}

#[test]
fn test_BC_1_18_001_unrecognized_trap_routes_to_on_error_not_indeterminate() {
    // Wildcard Trap arm: an unrecognized (future) Trap variant MUST NOT yield INDETERMINATE.
    // The wildcard `_ =>` arm must route to on_error handling, not classify as INDETERMINATE.
    // BC-1.18.001 invariant 2 (Trap is #[non_exhaustive]). Traces to AC-004 + BC-1.18.001 invariant 2.
    todo!()
}

#[test]
fn test_BC_1_18_001_emits_plugin_indeterminate_event_with_required_fields() {
    // FileSink receives a plugin.indeterminate event with all BC-3.08.001 Event 8 mandatory fields:
    // type, trace_id, session_id, plugin_name, artifact_path (empty string allowed, not omitted),
    // cause (fuel|epoch|output-too-large), failure_policy (fail-closed|fail-open), timestamp.
    // Applies to BOTH fail-closed AND fail-open INDETERMINATE outcomes (advisory path).
    // Traces to AC-006 + BC-3.08.001 Event 8.
    todo!()
}
```

### validate-unvalidated-mutation-marker/src/lib.rs test module (3 stubs)

```rust
#[test]
fn test_BC_1_18_002_blocks_agent_dispatch_when_marker_exists() {
    todo!()
}

#[test]
fn test_BC_1_18_002_passes_agent_dispatch_when_no_marker() {
    todo!()
}

#[test]
fn test_BC_1_18_002_block_message_names_plugin_and_artifact() {
    todo!()
}
```

**Red Gate density check (before Step 4 implementer dispatch):**
15 stubs total / estimated 15 non-trivial functions (14 prior + new `emit_marker_written` added in
v1.14; ADR-048 §Decision 4 (F-P6-001 correction — historical provenance, non-load-bearing)) = density 1.00 ≥ 0.5 threshold (unchanged pass). This burst's
ADR-048 §Decision 4 (F-P9-001 correction — historical provenance, non-load-bearing; SUPERSEDED
emission-point relocation) is a pure call-site
reordering of two ALREADY-COUNTED functions (`emit_superseded_if_cross_pair`, `emit_marker_written`)
— it introduces NO new function, so the non-trivial-function denominator and the density figure are
UNCHANGED at 15/15 = 1.00. All 15 unique stubs MUST compile and MUST fail (panic with `todo!()`)
before the implementer dispatch is authorized.

Stub summary:
- executor.rs section (12 stubs): stubs 1–10 original + stub 14 (unrecognized_trap_routes_to_on_error_not_indeterminate) + stub 15 (emits_plugin_indeterminate_event_with_required_fields) added in audit
- gate plugin section (3 stubs): test_BC_1_18_002_blocks_agent_dispatch_when_marker_exists, test_BC_1_18_002_passes_agent_dispatch_when_no_marker, test_BC_1_18_002_block_message_names_plugin_and_artifact
- Total unique stubs: 12 + 3 = 15

### AC-021..AC-025 marker.cleared / marker.written emission coverage (documentary reconciliation — D-1141)

**Gap closed this burst.** The pre-existing gap below (flagged since v1.10, repeatedly re-flagged
in v1.12/v1.13/v1.14/v1.15) is a documentary gap, not an implementation gap: `feature/S-25.01`
(3-CLEAN converged at `3919ebcb`; finalization HEAD `3e463cdc`) already implements and tests all
five ACs. This story's inventory simply never enumerated the implementing tests. The table below
enumerates them by test-function name (TD-VSDD-091 behavioral-anchor discipline — no bare line
numbers), closing the gap by citation, not by fabricating new stubs:

| AC | `clear_mode` / event | Test function | File |
|----|----------------------|----------------|------|
| AC-021 | `TTL_EXPIRED` (marker-level deadman) | `test_ADR_048_D4_check_and_clear_expired_marker_ttl_expired_deletes_and_emits` | `crates/factory-dispatcher/src/indeterminate_marker.rs` |
| AC-022 | `REVALIDATED` | `test_BC_1_18_003_named_plugin_pass_clears_marker_via_execute_tiers` | `crates/factory-dispatcher/tests/marker_integration.rs` |
| AC-023 | `OPERATOR_OVERRIDE` | `test_ADR_048_D4_reconcile_raw_delete_unmatched_event_emits_operator_override` | `crates/factory-dispatcher/src/indeterminate_marker.rs` |
| AC-024 | `SUPERSEDED` (positive — cross-pair overwrite) | `test_ADR_048_D4_v1_3_superseded_clear_emits_for_cross_pair_overwrite` | `crates/factory-dispatcher/src/indeterminate_marker.rs` |
| AC-024 | `SUPERSEDED` (negative control — EC-018/PC8 write failure) | `test_ADR_048_D4_PC8_no_emit_on_cross_pair_write_failure` | `crates/factory-dispatcher/src/indeterminate_marker.rs` |
| AC-025 | `marker.written` (positive — successful write) | `test_marker_written_emitted_on_successful_write_only` | `crates/factory-dispatcher/src/indeterminate_marker.rs` |
| AC-025 | `marker.written` (negative control — PreToolUse no-fabricate) | `test_pretooluse_indeterminate_without_marker_written_does_not_fabricate_operator_override` | `crates/factory-dispatcher/src/indeterminate_marker.rs` |

**Recomputed Red Gate density (this burst, D-1141):** 15 original stubs + 7 newly-enumerated
existing tests (above) = 22 test entries / 15 non-trivial functions = 1.47 ≥ 0.5 threshold
(pass; density rises because several of the 15 already-counted functions — e.g.
`check_and_clear_expired_marker`, `reconcile_raw_delete`, `emit_superseded_if_cross_pair`,
`emit_marker_written` — carry both a positive-path test and a negative-control test). The
15/15 = 1.00 figure in the paragraph above remains the historically-accurate as-of-implementer-dispatch
figure for the original burst and is left unchanged per TD-VSDD-091 (a pass-report figure, not a
volatile pin); 22/15 = 1.47 is this burst's current-state figure.

**Historical "Known gap" note superseded this burst (D-1141):** the note below is retained
verbatim for historical provenance only — it is NO LONGER an open gap. AC-021 (TTL_EXPIRED),
AC-022 (REVALIDATED), AC-023 (OPERATOR_OVERRIDE), AC-024 (SUPERSEDED), and AC-025
(`marker.written` creation) now all have dedicated Red Gate test citations in the table above.

<details>
<summary>Historical "Known gap" note (v1.10–v1.15, superseded by the D-1141 table above)</summary>

**Known gap (NOT actioned in this burst — outside enumerated v1.15 scope; pre-existing since v1.10,
extended in v1.13, v1.14, and again in v1.15):** AC-021 (TTL_EXPIRED), AC-022 (REVALIDATED), AC-023
(OPERATOR_OVERRIDE), AC-024 (SUPERSEDED), and AC-025 (`marker.written` creation) — the four
`marker.cleared` emission postconditions plus the one `marker.written` emission postcondition —
have NO dedicated Red Gate test stubs in this inventory. No stub is fabricated here per this
burst's directive (the v1.15 SUPERSEDED emission-point correction is a pure call-site reordering,
not a new function, so it does not by itself add a stub obligation); this is tracked as a
follow-up story-writer/test-writer drift item to add
`test_BC_1_18_003_ttl_expired_emits_marker_cleared`, `test_BC_1_18_003_revalidated_emits_marker_cleared`,
`test_BC_1_18_003_operator_override_emits_marker_cleared` (fixture retargeted to seed an unmatched
`marker.written` record, not a raw `plugin.indeterminate` line, per v1.14),
`test_BC_1_18_001_superseded_emits_marker_cleared_for_cross_pair_overwrite` (fixture write-then-emit
ordered per v1.15), `test_BC_1_18_001_marker_written_emitted_on_successful_write`,
`test_BC_1_18_003_operator_override_not_fabricated_when_no_marker_written` (the direct EC-036/PC7
non-fabrication regression test), and — NEW this burst —
`test_BC_1_18_001_superseded_not_fabricated_on_write_failure` (the direct EC-018/PC8 non-fabrication
regression test — cross-pair overwrite where `write_indeterminate_marker` returns `Err(_)` must
yield ZERO `marker.cleared(SUPERSEDED)` AND ZERO `marker.written` events) stubs, and recompute Red
Gate density accordingly. VP-108's proof harness skeleton (all eight postconditions, including PC6
write-path emission, PC7 negative control, and PC8 write-failure negative control) already provides
a template for these seven stubs.

</details>

## Changelog

| Version | Date | Description |
|---------|------|-------------|
| 1.22 | 2026-09-03 | Post-merge bookkeeping (state-manager; single-commit TD-VSDD-053; D-1159): PR #807 (feature/S-25.01) SQUASH-MERGED into `develop` as `f3f9b3a1` 2026-09-03 (base `b4ff2383`); feature branch + `.worktrees/S-25.01` deleted. `status` draft→merged. POL-14 auto-promotion: BC-1.18.001 v1.5→v1.6, BC-1.18.002 v1.7→v1.8, BC-1.18.003 v1.7→v1.8, BC-1.18.004 v1.2→v1.3 — all draft→active (BC-3.08.001 v1.34 already active, unchanged). `merged_count` 116→117. Full delivery arc: LOCAL BC-5.39.001 3-CLEAN convergence (passes 16/17/18, D-1153/D-1154/D-1155); finalization-doc-sweep COMPLETE (D-1156); validate-factory-path-staging zero-enforcement overclaim correction cascade (D-1157, new follow-up story S-25.04 opened); ADR-050 ratification unblocking `bats-darwin-leg-macos` CI (D-1158); a transient rustc-SIGSEGV CI flake cleared via rerun. `input-hash` re-synced `d14039d`→`ed6eb84` via `compute-input-hash --update` (BC-1.18.001/002/003/004 declared inputs each changed on disk this burst — POL-14 version bump); POLICY 18 three-way parity to be re-verified (frontmatter=catalog-row=blockquote). |
| 1.21 | 2026-09-03 | validate-factory-path-staging zero-enforcement overclaim sweep (story-writer; single-commit TD-VSDD-053). Corrects the S-25.01 narrative/AC overclaim (Cohort A-immediate "EFFECTIVE-NOW"; "Layer-1 effective fail-closed count" of ONE) per the human-directed factual correction already made in ADR-047 v1.4, BC-1.18.004 v1.2, and ADR-048 v1.6 + VP-108 v1.9 (routed from pr-reviewer's fresh-eyes MAJOR finding on PR #807). `validate-factory-path-staging` is registered PreToolUse `^Bash$`; the durable-marker write path (BC-1.18.001 invariant 4) is PostToolUse-only, so this registration structurally can never reach `write_indeterminate_marker`; combined with `on_error = "continue"` and this validator's absence from the ADR-039 §Decision 2 six-validator roadmap, the assignment produces ZERO current production enforcement effect — the mechanism is CODE-reachable/unit-tested but not production-live via this validator. Corrected: (1) Narrative "So that" clause (EFFECTIVE-NOW → ASSIGNED-NOW; reframed around the ZERO-enforcement gap and its S-25.04 anchor). (2) Context §5 Cohort A-immediate bullet + effective-count bullet (1 → ZERO; ADR-047 §D8a v1.3→v1.4). (3) AC-016 headline label and effective-count statement (→ ZERO), reconciled with the story's OWN already-accurate EC-009 row — EC-009 itself UNCHANGED. (4) Behavioral Contracts table BC-1.18.004 row (v1.1→v1.2) and Traceability table's Cohort A row (§D8a v1.3→v1.4). Anchored to new follow-up story **S-25.04** — "Close validate-factory-path-staging zero-enforcement gap — real Layer-1 production trigger" (Epic E-25, authored this same burst) per CLAUDE.md Canonical Principle Rule 3. NOT a POLICY 22 change — pure narrative/AC reconciliation; S-25.01 CODE and its own EC-009 row were already correct and remain unchanged; no stubs fabricated, no behavior changed. POLICY 18: `input-hash` — ADR-047/ADR-048/BC-1.18.004/VP-108 changed on disk; FLAG for state-manager to run `compute-input-hash S-25.01-dispatcher-indeterminate-outcome-layer1.md --update` and re-verify three-way parity. FLAG for state-manager: STORY-INDEX v-cell 1.20→1.21 + register new S-25.04 draft row under Epic E-25. |
| 1.20 | 2026-09-03 | Documentary reconciliation (story-writer; single-commit TD-VSDD-053; D-1141 companion + F-P13-001) anchored to the finalization-doc-sweep — resolved against the ALREADY-CONVERGED code/tests (feature/S-25.01 3-CLEAN at `3919ebcb`; finalization HEAD `3e463cdc`); no behavior changed, no stubs fabricated. ITEM 1 (F-P13-001): AC-007's block-message parenthetical example ("re-invoke the named plugin"-style phrasing) corrected to align with AC-020's authoritative four-tier recovery model — now cites T1 Edit/Write re-validation as the primary agent recovery path and T3 human-operator-terminal `rm` as the escape hatch; AC-007's mandate is UNCHANGED. ITEM 2 (D-1141): added an "AC-021..AC-025 marker.cleared / marker.written emission coverage" table to the Red Gate Test Inventory citing the ALREADY-EXISTING implementing tests by function name (verified via worktree grep) — AC-021 → `test_ADR_048_D4_check_and_clear_expired_marker_ttl_expired_deletes_and_emits`; AC-022 → `test_BC_1_18_003_named_plugin_pass_clears_marker_via_execute_tiers`; AC-023 → `test_ADR_048_D4_reconcile_raw_delete_unmatched_event_emits_operator_override`; AC-024 → `test_ADR_048_D4_v1_3_superseded_clear_emits_for_cross_pair_overwrite` + `test_ADR_048_D4_PC8_no_emit_on_cross_pair_write_failure`; AC-025 → `test_marker_written_emitted_on_successful_write_only` + `test_pretooluse_indeterminate_without_marker_written_does_not_fabricate_operator_override`; marked the T-3 checklist items `[x]` done; superseded the recurring "lack dedicated Red Gate test stubs — flagged as follow-up" notes (retained collapsed for historical provenance, not deleted); recomputed density: 15 original stubs + 7 newly-enumerated existing tests = 22 / 15 non-trivial functions = 1.47 (original 15/15=1.00 as-of-implementer-dispatch figure left unchanged per TD-VSDD-091 pass-report exception). ALSO: backfilled the missing v1.18/v1.19 rows below (pre-existing `validate-changelog-monotonicity` gap — frontmatter had advanced via two POLICY-18 input-hash re-sync bursts that updated only the frontmatter `last_amended` chain, never this table; fixed in-scope per CLAUDE.md Canonical Principle Rule 4). POLICY 18: `input-hash` intentionally UNCHANGED (`6ca47ed`) — story-body-only edit; FLAG for state-manager to re-sync/verify STORY-INDEX v-cell (1.19→1.20) + POLICY 18 three-way parity. |
| 1.19 | 2026-09-02 | POLICY 18 input-hash re-sync f3da248→6ca47ed (state-manager; single-commit TD-VSDD-053; D-1148): VP-108 v1.7→v1.8 (S-25.01 LOCAL adversary pass 15 F-P15-001 HIGH resolution — Postcondition 1 REVALIDATED clear's Property Statement paragraph contradicted BC-3.08.001 Event 9 trace_id semantics + sibling PC2/PC3/PC5 wording + code, a partial-fix regression where the F-P2-002/F-P3-001 trace_id-source + emission-locus corrections reached PC2/PC3/PC5 but never PC1; FIXED via architect commit `90675c7d` — trace_id source, emission locus, and trigger corrected in PC1; PC2-PC8 sibling-swept clean) changed on disk this burst; three-way parity restored (frontmatter=catalog-row=blockquote=6ca47ed). Story content UNCHANGED — input-hash re-sync class only, no body prose change. Backfilled into this table at v1.20 (row was missing from the table when originally amended; reconstructed verbatim from the frontmatter `last_amended` narrative). |
| 1.18 | 2026-09-02 | POLICY 18 input-hash re-sync 4727383→f3da248 (state-manager; single-commit TD-VSDD-053; D-1145): VP-108 v1.5→v1.7 (S-25.01 adversary pass 12 F-P12-001 MEDIUM resolution — PC8 postcondition had no implementing test + emission block duplicated at 2 callsites, FIXED via implementer helper extraction `adf3a1b1` + test-writer PC8 regression test `817c52ae`; architect corrected VP-108 v1.5→v1.6→v1.7 proof-harness anchors) changed on disk this burst; three-way parity restored (frontmatter=catalog-row=blockquote=f3da248). Story content UNCHANGED — input-hash re-sync class only, no body prose change. Backfilled into this table at v1.20 (row was missing from the table when originally amended; reconstructed verbatim from the frontmatter `last_amended` narrative). |
| 1.17 | 2026-09-01 | POLICY 19 (adr_version_cite_volatile_pin_prohibition, D-1079 story-bodies extension) normalization of ADR-048 sub-version citations (F-P11-002, S-25.01 adversary pass 11; story-writer; single-commit TD-VSDD-053). Every load-bearing `ADR-048 §D4 vX.Y` / `ADR-048 §Decision 4 vX.Y` citation across AC headers (AC-021, AC-023, AC-024, AC-025), body prose, tables (Behavioral Contracts, Architecture Mapping, Purity Classification, Token Budget Estimate), Tasks (T-3/T-4 checklists), and Traceability cells (Architecture Compliance Rules) is now cited by `ADR-048 §Decision 4` anchor ONLY; the version-tied correction-event provenance (finding ID, human-ratification date, D-NNN where applicable) is preserved as an explicitly non-load-bearing, historical parenthetical (e.g. "ADR-048 §Decision 4 (F-P9-001 correction — historical provenance, non-load-bearing)"). No AC semantics, IDs, BC/VP anchors, or behavioral content changed — citation FORM only; no `file.rs:NNN` line-number pins introduced. This "## Changelog" table (a pass-report changelog, already excepted per TD-VSDD-091) and the frontmatter's own historical `last_amended` narrative are intentionally left unchanged, as are BC-1.18.001/BC-1.18.002/BC-1.18.003/BC-1.18.004/BC-3.08.001's own version self-citations (POLICY 7/POLICY 8 territory, not POLICY 19's scope). Post-edit tr-normalized sweep (`tr '\n' ' ' \| grep -oE "ADR-[0-9]+ *§?[A-Za-z0-9 ]*v[0-9]+\.[0-9]+"`) confirms zero remaining load-bearing ADR-048 sub-version pins in the story body outside the Changelog. Frontmatter `bcs`/`verification_properties` unchanged (same 5 BCs, same 7 VPs). POLICY 18: `input-hash` intentionally left UNCHANGED by story-writer for state-manager to re-sync/verify — no BC/VP/ADR/architecture input file changed on disk this burst; this is a story-body-only citation-form edit. |
| 1.16 | 2026-09-01 | POLICY 18 input-hash re-sync 3b569a1→4727383 (state-manager; single-commit TD-VSDD-053; D-1143): VP-108 v1.4→v1.5 (S-25.01 adversary pass 10 F-P10-002 MEDIUM resolution — proof-harness timestamp-assertion gap closure; no BC/wire-format change) changed on disk this burst; three-way parity restored (frontmatter=catalog-row=blockquote=4727383). Story content UNCHANGED. |
| 1.15 | 2026-09-01 | ADR-048 §D4 v1.4→v1.5 Emission-Point Correction sync (S-25.01 adversary pass 9 F-P9-001 MEDIUM; architect adjudication; HUMAN-RATIFIED 2026-09-01, POLICY 22, D-1142) + PO BC amendments BC-1.18.001 v1.4→v1.5, BC-1.18.003 v1.6→v1.7, BC-3.08.001 v1.33→v1.34 (BC-1.18.002 v1.7 unchanged, BC-1.18.004 v1.1 unchanged this burst). F-P9-001: AC-024's SUPERSEDED `marker.cleared` emission was called BEFORE `write_indeterminate_marker` attempted the overwrite it describes, unconditionally on outcome — a write failure (EC-007) could leave the OLD marker still durably present and enforcing while a SUPERSEDED record had already falsely claimed it was overwritten (NIST AU-3/AU-10), the un-swept sibling of AC-025's own v1.4 `marker.written`-after-`Ok(())` discipline. (1) AC-024 rewritten: the SUPERSEDED emission call is relocated to INSIDE `write_indeterminate_marker`'s `Ok(())` arm, at BOTH `executor.rs` call sites, immediately BEFORE AC-025's `emit_marker_written` call in that same arm (SUPERSEDED first, then `marker.written`); new write-failure non-fabrication clause — on `Err(_)`, NEITHER event is emitted — cites BC-1.18.001 v1.5 PC4/INV3, BC-1.18.003 v1.7 PC5/EC-018, BC-3.08.001 v1.34 Event 9, VP-108 PC8. (2) AC-025's opening paragraph corrected: drops the now-inaccurate "BEFORE ANY other marker-lifecycle event for this write" framing; states the SUPERSEDED-then-marker.written back-to-back Ok(()) ordering explicitly (sibling-sweep, TD-VSDD-060). (3) Architecture Mapping + Purity Classification tables: `emit_superseded_if_cross_pair`, `emit_marker_written`, and their T-3 implement/wiring items corrected to name the Ok(()) arm co-location and BOTH call sites — no new row added (pure call-site reordering, no new function, per ADR-048 §D4 v1.5 Design (A)). (4) T-3 task checklist: the two executor.rs wiring items rewritten to the corrected Ok(()) arm ordering. (5) BC table: BC-1.18.001 v1.4→v1.5, BC-1.18.003 v1.6→v1.7, BC-3.08.001 v1.33→v1.34; Role columns updated. (6) Red Gate stub gap note extended: pre-existing gap (AC-021..AC-025 lack dedicated stubs) now also covers the new PC8 negative-control test (`test_BC_1_18_001_superseded_not_fabricated_on_write_failure`); no stub fabricated this burst (pure reordering, no new non-trivial function). Density unchanged: 15 stubs / 15 non-trivial functions = 1.00 ≥ 0.5 threshold (pass). (7) Token budget: BC-1.18.001/BC-1.18.003/BC-3.08.001/ADR-048/VP-102..108/story-spec rows bumped; total ~67,300→~67,900; budget ~34%→~34% (rounds unchanged). Frontmatter `bcs` list unchanged (same 5 BCs). POLICY 18: input-hash NOT updated by story-writer — BC-1.18.001/BC-1.18.003/BC-3.08.001/VP-108/ADR-048 all changed on disk this burst; drift flagged for state-manager. |
| 1.14 | 2026-09-01 | ADR-048 §D4 v1.3→v1.4 Reconciliation-Premise Correction sync (S-25.01 adversary pass 6 F-P6-001 MEDIUM; architect adjudication; HUMAN-RATIFIED 2026-09-01, POLICY 22, D-1141) + PO BC amendments BC-1.18.001 v1.3→v1.4, BC-1.18.003 v1.5→v1.6, BC-3.08.001 v1.32→v1.33 (story-writer; BC-1.18.002 v1.7 unchanged, BC-1.18.004 v1.1 unchanged this burst). New Event 10 `marker.written` — a positive marker-creation audit record. (1) AC-023 retargeted: `reconcile_raw_delete`'s bounded scan now matches an unmatched `marker.written` (BC-3.08.001 Event 10), NOT an unmatched `plugin.indeterminate`; `trace_id`/`plugin_name`/`artifact_path` for the retroactive `marker.cleared(OPERATOR_OVERRIDE)` now sourced from the matched `marker.written` event; redundant `failure_policy` filter wording removed. Non-fabrication clause added (F-P6-001 fix): a fail-closed INDETERMINATE that never wrote a marker (PreToolUse plugin, or PostToolUse write-failure) produces no `marker.written`, so no OPERATOR_OVERRIDE is fabricated — cites BC-1.18.003 v1.6 EC-017, BC-3.08.001 Event 10, VP-108 PC7. (2) New AC-025 added under a new "BC-1.18.001 Postcondition 4 v1.4 — `marker.written` Audited Creation Event" subsection: on successful `write_indeterminate_marker`, a new `emit_marker_written(ctx, fields)` function (`indeterminate_marker.rs`) emits `marker.written` via `ctx.emit_internal`; emitted ONLY on the `Ok(())` arm, never before the write, never on `Err(_)` — cites BC-1.18.001 v1.4 PC4, BC-3.08.001 Event 10, VP-108 PC6. (3) Architecture Mapping + Purity Classification tables: new `emit_marker_written` row added (Effectful; `indeterminate_marker.rs`); `reconcile_raw_delete` row retargeted to the `marker.written` scan. (4) T-3 task checklist: new implement item for `emit_marker_written`; `reconcile_raw_delete` item updated to name the `marker.written` scan target; new executor.rs wiring item for the marker-write callsite's `emit_marker_written` invocation (immediately after the AC-024 SUPERSEDED check, `Ok(())` arm only). File Structure Requirements row for `indeterminate_marker.rs` updated to list `emit_marker_written` alongside the seven existing functions. (5) BC table: BC-1.18.001 v1.3→v1.4, BC-1.18.003 v1.5→v1.6, BC-3.08.001 v1.32→v1.33; Role columns updated. (6) Red Gate stub gap note extended: the pre-existing gap (AC-021/AC-022/AC-023/AC-024 lack dedicated Red Gate test stubs) now also covers AC-025 — no stub fabricated in this burst; flagged as a follow-up story-writer/test-writer item. Density recomputed: 15 stubs / 15 non-trivial functions (14 prior + new `emit_marker_written`) = 1.00 ≥ 0.5 threshold (unchanged pass). (7) New story EC-036 added: non-fabrication negative control; story EC-008 gains a cross-reference clause noting no `marker.written` is emitted for the write-fail path either. (8) Token budget: BC-1.18.001/BC-1.18.003/BC-3.08.001/VP-102..108 rows bumped; story spec row bumped for AC-025 + architecture/task updates; total ~65,700→~67,300; budget ~33%→~34%. Frontmatter `bcs` list unchanged (same 5 BCs). POLICY 18: input-hash NOT updated by story-writer — BC-1.18.001/BC-1.18.003/BC-3.08.001/VP-108/ADR-048 all changed on disk this burst; drift flagged for state-manager. |
| 1.13 | 2026-09-01 | ADR-048 §D4 v1.2→v1.3 Emission-Mechanism Precision Correction sync (S-25.01 LOCAL adversary pass 3 F-P3-001 MEDIUM + F-P3-002 LOW; human-ratified) + PO BC amendments BC-1.18.001 v1.2→v1.3, BC-1.18.003 v1.4→v1.5, BC-3.08.001 v1.31→v1.32 (story-writer; BC-1.18.002 v1.7 unchanged this burst). (1) New AC-024 added: SUPERSEDED clear path — a cross-pair marker overwrite (new INDETERMINATE for a `(plugin_name, artifact_path)` pair DIFFERENT from the marker on disk) MUST emit `marker.cleared(clear_mode=SUPERSEDED, actor_type=system, reason=non-null)` for the OLD pair via `ctx.emit_internal` BEFORE the new marker write completes; same-pair overwrite emits nothing; cites BC-1.18.001 INV3 corollary, BC-1.18.003 PC5, BC-3.08.001 Event 9, VP-108 PC5. (2) AC-021/AC-023 signature citations updated to the `ctx: &HostContext` form: `check_and_clear_expired_marker(factory_root, now, ctx)`, `reconcile_raw_delete(factory_root, ctx)`, `emit_marker_cleared(ctx, fields, clear_mode, actor_type, reason)` — all Events 8/9 emissions now route through `HostContext::emit_internal` (dual-sink: durable InternalLog + `ctx.events` queue), never a raw `InternalLog::write`; AC-023's reconcile scan target corrected from the current day's `events-*.jsonl` to `dispatcher-internal-{date}.jsonl` (via `ctx.internal_log`). (3) AC-006/AC-015 sink-mechanism phrasing corrected: both previously claimed emission "to the OTel-aligned FileSink (`events-*.jsonl`)" — corrected to name the ACTUAL mechanism, `HostContext::emit_internal` (dual-write: durable `dispatcher-internal-{date}.jsonl` + the `ctx.events` pipeline that a future FileSink/Router will drain, pending S-4.07); no claim of a literal `events-*.jsonl` file being durably written today. (4) Architecture Mapping + Purity Classification tables updated: `emit_marker_cleared`/`check_and_clear_expired_marker`/`reconcile_raw_delete` rows corrected to the `ctx: &HostContext` signatures and `HostContext::emit_internal` routing; new row added for `emit_superseded_if_cross_pair` (NEW helper, `indeterminate_marker.rs` — reads the pre-overwrite marker's fields, compares `(plugin_name, artifact_path)`, and calls `emit_marker_cleared(ctx, ..., "SUPERSEDED", "system", Some(reason))` when they differ; called from `executor.rs`'s marker-write callsite BEFORE `write_indeterminate_marker` overwrites). (5) T-3 task checklist: implement items for `emit_marker_cleared`/`check_and_clear_expired_marker`/`reconcile_raw_delete` updated to the `ctx: &HostContext` signatures; `ClearMode` enum gains `Superseded`; new implement item added for `emit_superseded_if_cross_pair`; executor.rs wiring item updated to pass `ctx`; new wiring item added for the marker-write callsite's `emit_superseded_if_cross_pair` invocation. File Structure Requirements row for `indeterminate_marker.rs` updated to list `emit_superseded_if_cross_pair` alongside the six existing functions. (6) BC table: BC-1.18.001 v1.2→v1.3, BC-1.18.003 v1.4→v1.5, BC-3.08.001 v1.31→v1.32; Role columns updated with SUPERSEDED clear_mode addition and emit_internal correction narration. (7) Red Gate stub gap note extended: the pre-existing gap (AC-021/AC-022/AC-023 lack dedicated Red Gate test stubs, since v1.10) now also covers AC-024 — no stub fabricated in this burst; flagged as a follow-up story-writer/test-writer item. Density recomputed: 15 stubs / 14 non-trivial functions (13 prior + new `emit_superseded_if_cross_pair`) = 1.07 ≥ 0.5 threshold (unchanged pass). (8) Token budget: BC-1.18.001/BC-1.18.003/BC-3.08.001/VP-102..108 rows bumped for new content; story spec row bumped for AC-024 + architecture/task updates; total ~64,100→~65,700; budget ~32%→~33%. Frontmatter `bcs` list unchanged (same 5 BCs). POLICY 18: input-hash NOT updated by story-writer — BC-1.18.001/BC-1.18.003/BC-3.08.001/VP-108/ADR-048 all changed on disk this burst; drift flagged for state-manager. |
| 1.12 | 2026-08-31 | ADR-048 §D4 v1.1→v1.2 Emission-Point Correction sync (S-25.01 LOCAL adversary pass 2 F-P2-002 HIGH + F-P2-003 MED; human-ratified) + PO BC amendments BC-1.18.001 v1.1→v1.2, BC-1.18.002 v1.6→v1.7, BC-1.18.003 v1.3→v1.4, BC-3.08.001 v1.30→v1.31 (story-writer; narration-locus corrections, no wire-format/postcondition-semantic change). AC-021 (TTL_EXPIRED) and AC-023 (OPERATOR_OVERRIDE) rewritten to attribute `marker.cleared` detection+auto-delete/reconciliation+emission to new dispatcher-native functions `check_and_clear_expired_marker` and `reconcile_raw_delete` (`indeterminate_marker.rs`), invoked from `executor.rs`'s tier-execution loop BEFORE every Arm 1/Arm 2 `on_error=block_if_marker` plugin invocation on the normal path — never from the WASM gate plugin, whose `emit_event` host ABI cannot honor the Event 9 wire contract's foreign `trace_id`/`plugin_name` requirement (RESERVED_FIELDS wall). AC-023 gains a production-grade bounded-scan requirement (current-day `events-*.jsonl` only; capped bytes/records). Architecture Mapping + Purity Classification: `guard_logic::evaluate_gate` corrected to pure marker-presence check (no `expires_at` access); three new rows added for `check_and_clear_expired_marker`/`reconcile_raw_delete`/`emit_marker_cleared`. T-3 task list gains implement items for the three new functions + an executor.rs wiring item; T-4's `evaluate_gate` bullet corrected to explicit pure-presence-check description; File Structure Requirements row for `indeterminate_marker.rs` lists all six functions. BC table: BC-1.18.001 v1.1→v1.2, BC-1.18.002 v1.6→v1.7, BC-1.18.003 v1.3→v1.4, BC-3.08.001 v1.30→v1.31 with Role-column narration corrected. Frontmatter: VP-108 added to `verification_properties` and `inputs` (pre-existing gap — VP-108 v1.0 already anchored `anchor_story: S-25.01` but was never added here). Token budget: BC version labels bumped; VP row "VP-102 through VP-107" → "VP-102 through VP-108" (+900); total ~63,200→~64,100; budget ~32% (unchanged). AC-005 and AC-021 inline citations of "the gate plugin" auto-clearing the marker corrected to dispatcher-native attribution; unrelated historical version-anchor citations (BC-1.18.002 v1.3/v1.5/v1.6 for the Phase 1/1b/2/3/4 filter, PC1 introduction, and T3/PC5 recovery text — none touched by v1.7's narration-only fix) intentionally left unchanged per TD-VSDD-091. FLAG (not actioned — outside enumerated scope): AC-021/022/023 have no dedicated Red Gate stub coverage in this story; pre-existing gap since v1.10, recommend follow-up story-writer/test-writer pass. Frontmatter `bcs` list unchanged (same 5 BCs). POLICY 18: input-hash NOT updated (inputs list changed — VP-108.md added); drift flagged for state-manager. |
| 1.11 | 2026-08-31 | POLICY 18 input-hash re-sync 8bf7fa8→e9a512d (state-manager; session-wrap D-1137): BC-1.18.002 v1.5→v1.6 + BC-1.18.003 v1.2→v1.3 + BC-3.08.001 v1.29→v1.30 + ADR-048 v1.0→v1.1 inputs changed this session; three-way parity restored (frontmatter=catalog-row=blockquote). Story content UNCHANGED. |
| 1.10 | 2026-08-31 | ADR-048 v1.1 recovery reframe + audited clear event sync (BC-1.18.002 v1.5→v1.6; BC-1.18.003 v1.2→v1.3; BC-3.08.001 v1.29→v1.30). (1) AC-020 rewritten to four-tier recovery model (T1=Edit/Write primary inherently ungated even through gate crash; T2=24h TTL deadman passive; T3=human OOB rm break-glass operator terminal never intercepted; T4=agent-tool rm de-sanctioned may-be-blocked on crash path ACCEPTABLE NOT INV6 violation; guaranteed recovery T1+T2+T3; ADR-048 §Decision 3 stable anchor POLICY 19; BC-1.18.002 v1.6 INV6). (2) AC-011 crash block message reframed: T1 Edit/Write re-validation listed first; rm moved to T3 human OOB; MUST NOT instruct agent to run rm per BC-1.18.002 v1.6 PC5. (3) AC-021 updated: TTL-expiry auto-delete now emits marker.cleared(TTL_EXPIRED) (BC-1.18.003 v1.3 PC4 TTL-loudness; ADR-048 §Decision 4); crash-path native TTL-allow does NOT emit. (4) AC-022 added: REVALIDATED clear path emits marker.cleared(clear_mode=REVALIDATED, actor_type=validator, trace_id from originating plugin.indeterminate, reason=null; BC-1.18.003 v1.3 PC1 + BC-3.08.001 Event 9). (5) AC-023 added: OPERATOR_OVERRIDE retroactive marker.cleared emission via RAW_DELETE_DETECTED (best-effort; no hard failure if FileSink unavailable; BC-1.18.003 v1.3 PC3 + BC-3.08.001 Event 9). (6) EC-033 block message updated: T1 primary, rm demoted to T3 human OOB. (7) BC table version cells: BC-1.18.002 v1.5→v1.6, BC-1.18.003 v1.2→v1.3, BC-3.08.001 v1.29→v1.30 (nine event types incl. marker.cleared). (8) Token budget BC rows updated (BC-1.18.002: 4,800→5,000; BC-1.18.003: 2,800→3,500; BC-3.08.001 §Event 8→§Events 8+9: 1,500→2,200; VP-102..106→VP-102..107: 6,000→6,500; total ~60,000→~63,200; budget ~30%→~32%). Frontmatter bcs/verification_properties/input-hash unchanged. |
| 1.9 | 2026-08-31 | ADR-048 gate redesign sync (BC-1.18.002 v1.4→v1.5; BC-1.18.003 v1.1→v1.2; BC-1.18.001 v1.0→v1.1). (1) AC-011: on_error="continue" → on_error="block_if_marker" (ADR-048 §Decision 1; D-1135 reversed); crash behavior: BLOCK iff non-expired marker; ALLOW if absent/expired/crash-IO-fail; PC5/PC6 added; two-axis model rewritten. (2) AC-005: "five" → "six" TOML fields; expires_at added as 6th field (BC-1.18.001 v1.1; ADR-048 §Decision 2). (3) AC-020 added: Ungated-Escape invariant (BC-1.18.002 INV6; ADR-048 §Decision 3): rm/Edit-Write/TTL never gated; VP-107. (4) AC-021 added: TTL-expiry clear path (BC-1.18.003 PC4; ADR-048 §Decision 2): gate reads expired expires_at → Allow + auto-delete; 86400s; legacy markers without expires_at treated as non-expired. (5) Story EC-033..EC-035 added: crash+non-expired→Block, crash+absent/expired→Allow, TTL-expiry→Allow+auto-delete. (6) BC table: BC-1.18.001 v1.0→v1.1, BC-1.18.002 v1.4→v1.5, BC-1.18.003 v1.1→v1.2. (7) Token budget: ADR-048 row added (3,500); BC rows updated; total ~62,100; budget ~31%. (8) T-3: 5→6 TOML fields; expires_at added. (9) T-5: on_error="continue" → on_error="block_if_marker" in both gate TOML entries; NOTE updated. (10) Architecture Compliance Rules gate rule updated. Frontmatter bcs/verification_properties/input-hash unchanged. |
| 1.8 | 2026-08-31 | BC-1.18.003 v1.0→v1.1 sync (artifact-scoped marker clear; S-25.01 adversary M-1 resolution). (1) BC table BC-1.18.003 version v1.0→v1.1; role description updated to note artifact-scoped clear. (2) AC-012 updated to require (a) plugin_name match AND (b) artifact_path match (exact normalized absolute path equality); empty-path fallback added (non-artifact-scoped validators); same-plugin/different-non-empty-artifact → no-clear stated with story EC-031 reference. (3) Story EC-031 added: same plugin, different non-empty artifact, PostToolUse Pass → marker NOT cleared (BC-1.18.003 EC-008). (4) Story EC-032 added: same plugin, empty marker artifact_path, PostToolUse Pass on any artifact → marker cleared via empty-path fallback (BC-1.18.003 EC-009). (5) Token budget BC-1.18.003 row 1,800→2,200; total ~56,600→~57,000; budget ~28%→~29%. ADR-047 §D5 (artifact-scoped clear + empty-path fallback). Frontmatter bcs/verification_properties/input-hash unchanged. |
| 1.7 | 2026-08-31 | MEDIUM-2 dep-table fix: replaced stale `regex` row in Library & Framework Requirements (MANDATORY) table with `shell-words 1.1` (Phase 1b POSIX quote-aware tokenization in is_git_commit_or_push; BC-1.18.002 v1.3+ forbids regex quote-stripping as parser-differential bypass class; story body in AC-008/AC-009/Purity Classification/T-4/T-5 already correctly mandated shell-words — only the MANDATORY table row was stale). EC sync to BC-1.18.002 v1.4 (read-error adjudication): (a) EC-030 added — gate encounters unreadable marker (EACCES/IO) → Allow (fail-open), INV2 self-lock avoidance; (b) EC-008 cross-reference note updated — BC-1.18.002 EC-008 v1.4 rescoped to readable-but-malformed marker → Block (gate-read path; distinct from story EC-008 write-fail BC-1.18.001 path); (c) BC table BC-1.18.002 version cell v1.3→v1.4 with Role column updated to note v1.4 additions. Frontmatter bcs/verification_properties/input-hash unchanged. |
| 1.6 | 2026-08-31 | AC-008/AC-009/AC-011/AC-012/EC sync to BC-1.18.002 v1.3 (HIGH-1 fail-open-on-crash reconciliation + MEDIUM-2 threat-model + quoting-in-scope). (1) AC-011: `on_error = "block"` corrected to `on_error = "continue"` for both gate arms (BC-1.18.002 PC1 v1.3; HIGH-1 finding — "block" would produce unconditional self-lock on gate crash); two-axis model note added (ADR-039; ADR-047 §Decision 9) distinguishing dispatcher-level fail-open-on-crash (axis i) from filter-internal fail-SAFE posture (axis ii). (2) AC-012: PostToolUse hook qualifier added — only a PostToolUse PASS from the named plugin clears the marker; a PreToolUse PASS MUST NOT clear the marker (BC-1.18.003 PC1). (3) AC-008: Phase 1b POSIX quote-aware tokenization added (`shell-words` crate; `git "commit"` → BLOCKED EC-024; `git 'push' origin` → BLOCKED EC-025; `g'i't commit` → BLOCKED EC-026); out-of-scope boundary documented (eval/xargs/subshell/indirection — undecidable; Rice's theorem; caught by other layers); version reference v1.2→v1.3; "Phase 1/1b/2/3/4" nomenclature. (4) AC-009: v1.2→v1.3; Phase 1b note; EC-027..EC-029 NOT-blocked out-of-scope examples added. (5) Story EC-021..EC-026 added for BC EC-024..EC-029 (3 quoting-blocked + 3 out-of-scope-documented). (6) BC table BC-1.18.002 v1.2→v1.3. (7) T-5 TOML `on_error = "block"` → `"continue"` for both arms; note updated with v1.3 algorithm reference. (8) Architecture Mapping/Purity Classification/T-4 `is_git_commit_or_push` description updated v1.2→v1.3 with Phase 1b. (9) Architecture Compliance Rules: gate plugin rule updated to require both `on_error = "continue"` AND `failure_policy = "fail-open"`. (10) Token budget BC-1.18.002 row 3,500→4,000 (29 ECs + 30 test vectors). ADR-047 §Decision 9; ADR-039. |
| 1.5 | 2026-08-31 | AC-008/AC-009/EC sync to BC-1.18.002 v1.2 (MEDIUM-1 adversary finding adjudication). AC-008: regex `\bgit\b.*\b(commit|push)\b` demoted from authoritative rule to illustrative approximation only; replaced with authoritative three-phase algorithm (BC-1.18.002 PC2 v1.2): Phase 1 compound splitting (`&&`/`||`/`;`/`|`/`&`/newline; any-segment match returns `true`), Phase 2 basename identification (path-prefixed `/usr/bin/git`, `./git`; env-var-assignment tokens and `env` stripped), Phase 3 global-option skip with complete seven-option arg-taking set (`-C`, `-c`, `--namespace`, `--git-dir`, `--work-tree`, `--super-prefix`, `--config-env`) + recognized no-arg set + fail-safe on unrecognized options (return `true`), Phase 4 exact subcommand match (`commit`/`push` only — `commit-graph` excluded). AC-009: updated to reference all three phases, complete option sets, fail-safe behavior, and examples for BC ECs EC-013..EC-023. EC-006/EC-007: regex reference replaced with Phase 4 exact-subcommand citation. Added story EC-010..EC-020 covering BC-1.18.002 EC-013..EC-023 (compound splitting, basename, path-prefix, env-prefix, fail-safe, config-env). BC table: BC-1.18.002 v1.1→v1.2. Architecture Mapping + Purity Classification: `is_git_commit_or_push` description updated from regex to three-phase algorithm. T-4/T-5: command filter implementation description updated. Token budget BC-1.18.002: 2,600→3,500; total ~55,700→~56,600. ADR-047 §Decision 9 (POLICY 19). |
| 1.4 | 2026-08-31 | AC-009 exact-subcommand sync to BC-1.18.002 v1.1 (pass-1 adversary BLOCKER-3 adjudication). Regex `\bgit\b.*\b(commit\|push)\b` replaced by authoritative `is_git_commit_or_push` exact-subcommand rule (locate `git` token, skip git-global options incl. argument-taking `-C <path>`, `-c k=v`, `--namespace <ns>`, `--no-pager`, `--git-dir`; first remaining non-option token is the subcommand; block iff exactly `commit` or `push`). Added EC-011 example (`git commit-graph write` → NOT blocked; subcommand is `commit-graph`, not `commit`) and EC-012 example (`git -C .factory commit -m "state"` → BLOCKED; global option tolerated). Regex demoted to illustrative-only per BC-1.18.002 v1.1 clarification. BC table version BC-1.18.002 v1.0→v1.1. ADR-047 §Decision 9 stable anchor (POLICY 19). |
| 1.3 | 2026-08-30 | Re-audit M-1: BC-table Title cells updated to verbatim BC H1 text (POLICY 7 / criterion-75; story-writer). All 5 Title cells now match the authoritative BC H1 and BC-INDEX title cells exactly. Version bump only — no AC, task, or architecture changes. |
| 1.2 | 2026-08-30 | Consistency audit correction (story-writer). FINDING 1: AC-004 wildcard Trap arm corrected from `_other => Indeterminate` to `_ => { route to on_error }` per BC-1.18.001 invariant 2; Task T-1 pseudocode and Architecture Compliance Rules entry fixed to match; 14th Red Gate stub added (test_BC_1_18_001_unrecognized_trap_routes_to_on_error_not_indeterminate). FINDING 3: 15th Red Gate stub added for AC-006 FileSink emission (test_BC_1_18_001_emits_plugin_indeterminate_event_with_required_fields); Task T-2 scope updated. FINDING 4: Task T-5 TOML key names corrected to canonical hooks-registry.toml schema (wasm_path→plugin, trigger→event, tool_pattern→tool, tier→async=false; command_filter removed — filtering is WASM-internal). FINDING 5: Invariant trace annotations added to AC-004 (+inv2), AC-005 (+inv3+inv4), AC-007 (+inv4), AC-008 (+inv4), AC-012 (+BC-1.18.003 inv2), AC-014 (+BC-1.18.002 inv3 + BC-1.18.003 inv3); AC-018 added for BC-1.18.001 invariant 5 (dispatcher-internal StoreData); AC-019 added for BC-1.18.002 invariant 1 (same WASM binary). FINDING 10: Typo IndiceterminateCause → IndeterminateCause in T-1. Red Gate density: 15 stubs / 13 functions = 1.15. |
| 1.1 | 2026-08-30 | Consistency correction (architect; spec-burst). Cohort A split into A-immediate (validate-factory-path-staging: EFFECTIVE-NOW, no S-21.24 dependency) and A-deferred (validate-pr-merge-prerequisites, validate-wave-gate-prerequisite: SET-BUT-LATENT, set in registry with comment, enforcement conditional on S-21.24). AC-016 and Context §5 updated with explicit A-immediate/A-deferred labels and Layer-1 effective count. Narrative "So that" clause updated to state honest 1-effective/2-latent split. Consistent with ADR-047 D8a v1.3. |
| 1.0 | 2026-08-30 | Initial authoring (story-writer; F3 story-decomposition burst, validation-integrity-layer1). S-25.01 Layer-1 ACTIVE story. INDETERMINATE outcome + durable marker + next-advance gate. 17 ACs with BC-PC traces. 13 Red Gate test stubs. 7 tasks. 5 BCs: BC-1.18.001–004, BC-3.08.001. 5 VPs: VP-102–106. depends_on: [S-21.10]. |
