---
document_type: story
level: implementation
story_id: S-19.02
epic_id: "E-19"
version: "1.2"
title: "verify-factory-lock FINDING-1: frontmatter-only STATE.md read + raised byte budget"
status: draft
producer: story-writer
timestamp: "2026-07-04T00:00:00Z"
last_amended: "2026-07-06 (v1.2) — E-19 pass-2 fix burst (story-writer): verification_properties: [VP-095, VP-096] wired; BC cite bump v1.4→v1.5 throughout; Invariant 10 soft-warning: AC-006 added (emit soft_warn_approaching_cap when STATE.md between 200000..262144 bytes, soft_warn_threshold from BC-4.13.001 v1.5 Invariant 10); AC-005 recast for byte-exact boundary verification (F-P2-011: parity-with-full-file-parse FORBIDDEN — must assert delimiter byte position determines inclusion/exclusion; parse-equal framing replaced with boundary-correctness framing); Behavioral Contracts section version v1.4→v1.5; F-P2-012 cargo test baseline task added. [Prior: 2026-07-06 (v1.1) — E-19 pass-1 fix burst (story-writer): F-P1-003 POLICY 8: body BC table (BC-4.13.001 v1.5) + per-AC BC/PC traceability column + Token Budget Estimate added. F-P1-004: implicit amendment language replaced with explicit 'BC-4.13.001 v1.5 amendment LANDED (product-owner, E-19 pass-1 fix burst); spec-first, code follows'. F-P1-008: narrative reworded — AC-001 (cap raise) alone closes FINDING-1 (read_bounded checks metadata.len() BEFORE reading; frontmatter-only parse cannot compensate for an under-sized cap); AC-003 is contract-mandated parser hygiene per BC-4.13.001 v1.5 Invariant 9, not a co-requisite; 'both arms are required' framing dropped; ADR-025 Decision 14 cited. Missing template sections added. [Prior: 2026-07-04 (v1.0) — initial draft authored by story-writer; post-rc.22 smoke gate authorization (2026-07-04); closes rc.22 smoke FINDING-1 (verify-factory-lock silently degraded on large STATE.md; dispatcher log traces a4b26f12, bcc3e6ef, cf4c2e4d, 2551d7db; StateReadError: OutputTooLarge).]]"
phase: F3
cycle: v1.0-feature-engine-discipline-pass-1
inputs:
  - crates/hook-plugins/verify-factory-lock/src/lib.rs
  - crates/factory-dispatcher/src/host/read_file.rs
  - plugins/vsdd-factory/hooks-registry.toml
  - .factory/specs/behavioral-contracts/ss-04/BC-4.13.001.md
input-hash: "166e309"
traces_to:
  - .factory/stories/epics/E-19-post-rc22-operator-hardening.md
wave: 1
priority: P0
points: 8
track: feature
effort_class: multi-day
depends_on: []
blocks: []
behavioral_contracts: [BC-4.13.001]
verification_properties: [VP-095, VP-096]
domain_invariants: []
tdd_mode: strict
target_module: "crates/hook-plugins/verify-factory-lock"
subsystems:
  - "SS-04"
estimated_days: 2.0
assumption_validations: []
risk_mitigations: []
anchored_adrs: [ADR-025]
closes:
  - "rc.22 smoke FINDING-1: verify-factory-lock silently degraded in operator installs when STATE.md exceeds 64 KiB (dispatcher traces a4b26f12, bcc3e6ef, cf4c2e4d, 2551d7db; reason=output_too_large)"
feature: "post-rc.22-smoke"
modified:
  - "2026-07-06 v1.2: verification_properties wired [VP-095, VP-096]; BC cite v1.4→v1.5; AC-006 added (Invariant 10 soft-warn); AC-005 recast byte-exact boundary (F-P2-011); F-P2-012 task"
  - "2026-07-06 v1.1: F-P1-003 POLICY 8 body BC table + AC traceability + Token Budget; F-P1-004 explicit BC LANDED language; F-P1-008 narrative reword + ADR-025 Decision 14; missing template sections"
  - "2026-07-04 v1.0: initial draft"
---

# S-19.02 — verify-factory-lock FINDING-1: frontmatter-only STATE.md read + raised byte budget

**Epic:** E-19 — Post-rc.22 Operator Hardening
**Wave:** 1 (P0 defect; begin immediately; no E-19 predecessor)
**Depends on:** none
**Priority:** P0 (functional defect — lock gate silently degraded in every operator install where STATE.md exceeds 64 KiB; this is production state for the vsdd-factory repo at rc.22)
**Subsystem anchor:** SS-04 (Plugin Ecosystem) owns the `verify-factory-lock` WASM hook plugin crate.

## Narrative

The `verify-factory-lock` PreToolUse WASM guard (S-17.02; BC-4.13.001) is the single-writer enforcement mechanism for the factory-artifacts orphan branch. It reads `.factory/STATE.md` via `host::read_file` with a 64 KiB cap (`STATE_MD_MAX_BYTES = 65536`), then parses the `factory_lock:` YAML frontmatter block.

**The defect:** In the vsdd-factory repo at rc.22, STATE.md is approximately 90 KB — larger than the 64 KiB cap. `read_file.rs::read_bounded()` returns `ReadErr::TooLarge` when the file's on-disk size exceeds `max_bytes`, emitting `internal.capability_denied reason=output_too_large` and returning `OUTPUT_TOO_LARGE (-3)` to the plugin. The plugin maps any non-zero `read_file` return as `StateReadError` and emits a WARN log, then falls back to Continue (fail-open per ADR-025 Decision 7). The lock gate is therefore **silently bypassed on every PreToolUse Edit/Write/MultiEdit/Agent dispatch** in any install where STATE.md has grown beyond 64 KiB.

Evidence from `.factory/logs/dispatcher-internal-2026-07-04.jsonl`: traces `a4b26f12`, `bcc3e6ef`, `cf4c2e4d`, `2551d7db` each show `internal.capability_denied reason=output_too_large` on `plugin_name=verify-factory-lock`, confirming STATE.md is consistently exceeding the cap in the production rc.22 install.

**Fix direction:**

**BC-4.13.001 v1.5 amendment LANDED (product-owner, E-19 pass-1 fix burst). Spec-first, code follows.** The amendment raises Precondition 3 `max_bytes` from 65536 to 262144 (256 KiB) and extends Invariant 9 with the frontmatter-only-parsing mandate. This story implements the code changes to bring the plugin into conformance with BC-4.13.001 v1.5 and ADR-025 Decision 14.

**AC-001 (cap raise) alone closes FINDING-1:** `read_bounded()` checks `metadata.len()` BEFORE reading any bytes — if the cap is below the file's on-disk size, the read fails immediately and returns `OUTPUT_TOO_LARGE`, before the frontmatter parser is ever invoked. A frontmatter-only parser cannot compensate for an under-sized cap, because the read never returns bytes. The fix is to raise `STATE_MD_MAX_BYTES` to 262144 so the cap is above any realistic STATE.md size (STATE.md observed ~90 KB / ~466 lines; the 500-line compaction hard cap per ADR-026 implies worst-case ≤200 KiB; 256 KiB gives ≥28% headroom). Cite: ADR-025 Decision 14.

**AC-003 (frontmatter-only extraction) is contract-mandated parser hygiene:** BC-4.13.001 v1.5 Invariant 9 mandates that the guard MUST abort scanning after the second `---\n` delimiter and MUST NOT parse file body content. The lock verdict is derived exclusively from the frontmatter region. AC-003 implements this invariant — not as a workaround for the byte-cap defect, but as the required parsing discipline. The `max_bytes = 262144` cap (Precondition 3) ensures the host read succeeds; Invariant 9 ensures the parser ignores the body.

## Acceptance Criteria

| ID | Criterion | Gate | BC Trace |
|----|-----------|------|----------|
| AC-001 | `STATE_MD_MAX_BYTES` constant in `crates/hook-plugins/verify-factory-lock/src/lib.rs` is raised to 262144 (256 KiB). | `grep -q "STATE_MD_MAX_BYTES.*262144" crates/hook-plugins/verify-factory-lock/src/lib.rs` exits 0. | BC-4.13.001 v1.5 Precondition 3 (max_bytes = 262144) |
| AC-002 | Plugin reads STATE.md successfully when the file is between 64 KiB and 256 KiB and the `factory_lock:` block is present in the YAML frontmatter. The plugin correctly detects a foreign unexpired lock and returns block intent. | Unit test with a fixture STATE.md of size > 65536 bytes (e.g., padded with comment lines after the closing frontmatter `---`): assert `run_check()` returns `Outcome::Block` when a foreign lock is held. Exit non-zero if this test fails. | BC-4.13.001 v1.5 Precondition 3 (operational at new cap) |
| AC-003 | Plugin extracts only the YAML frontmatter from the raw bytes before passing to the YAML parser: it truncates at the closing frontmatter delimiter (`\n---\n` or `\n---` at EOF) so the parser never processes post-frontmatter body content. The extraction must handle the case where the frontmatter end is not found (treat entire content as frontmatter). | Unit test: a fixture where `factory_lock:` appears only in frontmatter and large body content follows — assert parser succeeds and the factory_lock block is correctly parsed. Separate unit test: a fixture where frontmatter end marker is absent — assert extraction falls back to full content. Exit non-zero if either test fails. | BC-4.13.001 v1.5 Invariant 9 (frontmatter-only-parsing mandate) |
| AC-004 | No `internal.capability_denied reason=output_too_large` events are emitted in the dispatcher log for `verify-factory-lock` when STATE.md is ≤ 256 KiB. | Integration test: write a fixture STATE.md of 70000 bytes (> 64 KiB, < 256 KiB) with a factory_lock block in frontmatter; invoke the plugin via the test harness; assert zero `output_too_large` denial events in the captured event stream. Exit non-zero if any such events are found. | BC-4.13.001 v1.5 Precondition 3 (operational verification) |
| AC-005 | `extract_frontmatter(bytes)` truncates at the byte-exact position of the closing frontmatter delimiter: the extracted slice ends at the last byte before `\n---\n` (or `\n---` at EOF). No body byte from after the delimiter appears in the extracted slice. **Parity-with-full-file-parse is NOT the correctness criterion** (F-P2-011): the test must assert byte-exact boundary behavior — that the byte immediately preceding the delimiter's leading `\n` IS included and the delimiter itself IS excluded. | Unit test A (boundary): fixture = `---\nfactory_lock: null\n---\nbody content here`. Assert extracted slice ends at byte offset = index of `\n` beginning `\n---\n`; assert `body content here` does not appear in extracted slice; assert extracted_len == delimiter_start_offset. Exit non-zero if any body byte leaks. Unit test B (EOF delimiter): fixture = `---\nfactory_lock: null\n---` (no trailing body). Assert extracted slice contains only frontmatter content. Exit non-zero on failure. | BC-4.13.001 v1.5 Invariant 9 (byte-exact delimiter boundary; F-P2-011) |
| AC-006 | When `host::read_file` returns success for STATE.md with a byte count between 200000 (inclusive) and 262144 (exclusive), the plugin emits a `plugin.log` entry at warn level with message containing `state_md_approaching_cap` before proceeding with the lock check. Files below 200000 bytes do not emit the warning. | Unit test A: fixture STATE.md of 210000 bytes → `plugin.log` contains warn entry with `state_md_approaching_cap`. Unit test B: fixture STATE.md of 150000 bytes → zero `state_md_approaching_cap` log entries. Exit non-zero if warning absent on approaching-cap fixture or present on below-threshold fixture. | BC-4.13.001 v1.5 Invariant 10 (soft_warn_threshold=200000; VP-096) |

## Architecture Mapping

- `crates/hook-plugins/verify-factory-lock/src/lib.rs` — raise `STATE_MD_MAX_BYTES` to 262144; add `extract_frontmatter(bytes: &[u8]) -> &[u8]` function that truncates at the closing `---` boundary; call it before `parse_factory_lock`
- `crates/hook-plugins/verify-factory-lock/src/tests.rs` (or inline tests) — add unit tests for AC-001..AC-005
- No changes to `hooks-registry.toml` (the capability `path_allow` and `on_error` remain unchanged)
- No changes to `read_file.rs` (the fix is fully in the plugin; cap is a plugin-side constant, not a registry field — `ReadFileCaps` has no `max_bytes_per_call` field per ADR-025)

## Behavioral Contracts

| BC ID | Title | Version | Story ACs |
|-------|-------|---------|-----------|
| BC-4.13.001 | verify-factory-lock WASM PreToolUse guard: single-writer enforcement | v1.5 | AC-001 (Precondition 3), AC-002 (Precondition 3), AC-003 (Invariant 9), AC-004 (Precondition 3), AC-005 (Invariant 9), AC-006 (Invariant 10) |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | STATE.md is exactly 64 KiB (boundary case) | Plugin reads successfully at the new 262144 cap. No regression at the old boundary. |
| EC-002 | STATE.md exceeds 256 KiB | Plugin falls back to fail-open (Continue) per ADR-025 Decision 7 + emits `StateReadError: OutputTooLarge` warn. The 256 KiB cap is intended as a hard ceiling above D-442(e) soft/hard limits; any exceedance indicates a structural anomaly. |
| EC-003 | Frontmatter delimiter appears inside a YAML string value | Extraction algorithm searches for `\n---\n` or `\n---` at EOF as a line unto itself; embedded `---` within a scalar value will not match. |
| EC-004 | STATE.md has no closing frontmatter delimiter (malformed) | Extraction uses full content as frontmatter; YAML parser returns error; plugin falls back to fail-open with WARN. |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| `crates/hook-plugins/verify-factory-lock/src/lib.rs` | effectful-shell | Calls `host::read_file` (host ABI, file I/O side effect); calls `host::exec_subprocess` (git subprocess) |
| `extract_frontmatter(bytes: &[u8]) -> &[u8]` (new fn) | pure-core | Byte-slice scanning; no I/O; deterministic; side-effect free |
| `parse_factory_lock(bytes: &[u8])` (existing fn) | pure-core | YAML parsing; no I/O; deterministic |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~5,000 |
| `crates/hook-plugins/verify-factory-lock/src/lib.rs` | ~2,500 |
| BC-4.13.001 (v1.5 — Precondition 3 + Invariant 9) | ~3,000 |
| ADR-025 (Decision 14) | ~1,000 |
| `crates/factory-dispatcher/src/host/read_file.rs` (context only) | ~1,500 |
| Test files (new unit + integration) | ~2,500 |
| Tool outputs overhead | ~1,500 |
| **Total** | **~17,000** |
| Agent context window | 200K (Sonnet) |
| **Budget usage** | **~8.5%** |

## Tasks (MANDATORY)

1. [ ] Capture `cargo test --workspace --all-targets` baseline pass count pre-implementation; record in PR description (F-P2-012)
2. [ ] Write failing unit test: `STATE_MD_MAX_BYTES == 262144` assertion (AC-001) — test-writer
3. [ ] Write failing unit test: 70 KiB fixture + foreign lock → block (AC-002) — test-writer
4. [ ] Write failing unit tests: `extract_frontmatter` with/without delimiter (AC-003) — test-writer
5. [ ] Write failing integration test: 70 KiB STATE.md → zero output_too_large events (AC-004) — test-writer
6. [ ] Write failing unit tests: byte-exact boundary behavior for `extract_frontmatter` (AC-005 — unit test A: body byte does not leak; unit test B: EOF delimiter) — test-writer
7. [ ] Write failing unit tests: state_md_approaching_cap warn at 210000 bytes; no warn at 150000 bytes (AC-006) — test-writer
8. [ ] Verify Red Gate: all tests fail with stubs
9. [ ] Raise `STATE_MD_MAX_BYTES` to 262144 (implementer)
10. [ ] Implement `extract_frontmatter(bytes: &[u8]) -> &[u8]` function with byte-exact delimiter truncation (implementer)
11. [ ] Wire `extract_frontmatter` call before `parse_factory_lock` (implementer)
12. [ ] Implement soft-warning emission when byte count is in range [200000, 262144) per BC-4.13.001 v1.5 Invariant 10 (implementer)
13. [ ] Verify all tests pass; confirm zero `output_too_large` events on 70 KiB fixture; assert baseline preserved + new tests added (post-implementation F-P2-012)

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-17.02 (original verify-factory-lock impl) | Plugin source is in `crates/hook-plugins/verify-factory-lock/src/lib.rs`; cap is a compile-time constant in the plugin, not a registry field | WASM plugins use `host::read_file` with a plugin-side `max_bytes` constant; `ReadFileCaps` has only `path_allow`, no `max_bytes_per_call` | Deny-by-default silent-no-op class: `env_allow` must be present for exec_subprocess to pass HOME to git config |
| S-19.03 (same wave) | `read_file.rs::read_bounded()` checks `metadata.len()` BEFORE reading — cap must be above file size for any bytes to be returned | Host ABI fix vs plugin fix distinction: this story fixes the plugin constant; S-19.03 fixes `path_allowed()` in the dispatcher | None affecting S-19.02 |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| `STATE_MD_MAX_BYTES` must equal 262144 (256 KiB) | BC-4.13.001 v1.5 Precondition 3; ADR-025 Decision 14 | `grep -q "STATE_MD_MAX_BYTES.*262144"` gate in AC-001 |
| Guard MUST NOT parse file body content (frontmatter-only) | BC-4.13.001 v1.5 Invariant 9 | AC-003 gate; `extract_frontmatter` called before `parse_factory_lock` |
| Spec-first, code follows: BC-4.13.001 v1.5 ALREADY LANDED | CANONICAL PRINCIPLE + F-P1-004 | Implementer reads BC-4.13.001 v1.5 before writing code; no BC authorship needed in this story |
| No changes to `hooks-registry.toml` or `read_file.rs` | Architecture Mapping | The fix is entirely in the plugin; registry `ReadFileCaps` has no `max_bytes_per_call` field |
| No `println!` in production Rust | CLAUDE.md Conventions | CI clippy |
| Error taxonomy: named variants, no `unwrap()` | CLAUDE.md Conventions | Rust compiler + clippy |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| Rust toolchain | per `rust-toolchain.toml` at repo root | Single-workspace MSRV |
| `serde` / `serde_yaml` (or equivalent YAML parser) | workspace-pinned | YAML frontmatter parsing in plugin |
| `wasmtime` (via factory-dispatcher) | workspace-pinned | WASM plugin sandbox execution |
| Bats (Bash Automated Testing System) | per `plugins/vsdd-factory/tests/run-all.sh` | Integration test runner |

## Red Gate Test Plan

| Test ID | Description | Expected Outcome |
|---------|-------------|------------------|
| RG-001 | Fixture STATE.md of 70000 bytes with valid foreign lock in frontmatter → plugin blocks | Exit: block intent returned |
| RG-002 | Fixture STATE.md of 70000 bytes with no lock in frontmatter → plugin continues | Exit: continue returned |
| RG-003 | `extract_frontmatter` on bytes with `\n---\n` present → returns only bytes up to and including the delimiter | Assertion: extracted slice ends at delimiter |
| RG-004 | `extract_frontmatter` on bytes with no `\n---\n` → returns full slice | Assertion: extracted slice equals input |
| RG-005 | `extract_frontmatter`: fixture with `\n---\n` delimiter + body content → extracted slice ends at delimiter start; no body byte in slice | Assertion: extracted_len == delimiter_start_offset; body bytes absent |
| RG-006 | STATE.md of 210000 bytes → plugin.log warn entry contains `state_md_approaching_cap` | Assertion: warn log entry present |

## Test Plan

| Test ID | Story AC | Test File | Description |
|---------|----------|-----------|-------------|
| T-001 | AC-001 | crates/hook-plugins/verify-factory-lock/src/lib.rs (unit test) | STATE_MD_MAX_BYTES == 262144 assertion |
| T-002 | AC-002 | crates/hook-plugins/verify-factory-lock/src/lib.rs (unit test) | 70 KiB fixture + foreign lock → block |
| T-003 | AC-002 | crates/hook-plugins/verify-factory-lock/src/lib.rs (unit test) | 70 KiB fixture + no lock → continue |
| T-004 | AC-003 | crates/hook-plugins/verify-factory-lock/src/lib.rs (unit test) | extract_frontmatter: delimiter present |
| T-005 | AC-003 | crates/hook-plugins/verify-factory-lock/src/lib.rs (unit test) | extract_frontmatter: no delimiter → full content |
| T-006 | AC-004 | crates/hook-plugins/verify-factory-lock/tests/ (integration test) | 70 KiB STATE.md → zero output_too_large events |
| T-007 | AC-005 | crates/hook-plugins/verify-factory-lock/src/lib.rs (unit test) | extract_frontmatter byte-exact boundary: body bytes absent from extracted slice |
| T-008 | AC-005 | crates/hook-plugins/verify-factory-lock/src/lib.rs (unit test) | extract_frontmatter EOF delimiter: no body, full content returned |
| T-009 | AC-006 | crates/hook-plugins/verify-factory-lock/src/lib.rs (unit test) | state_md_approaching_cap warn at 210000 bytes; no warn at 150000 bytes |

## File Structure Requirements (MANDATORY)

| File | Action | Notes |
|------|--------|-------|
| `crates/hook-plugins/verify-factory-lock/src/lib.rs` | modify | Raise STATE_MD_MAX_BYTES to 262144; add extract_frontmatter() fn; call before parse_factory_lock; add unit tests T-001..T-007 |
| `crates/hook-plugins/verify-factory-lock/tests/` | modify or create | Integration test for AC-004 (zero output_too_large events on 70 KiB fixture) |
