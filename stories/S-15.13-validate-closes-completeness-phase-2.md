---
document_type: story
level: ops
story_id: "S-15.13"
epic_id: "E-12"
version: "1.3"
title: "S-15.13: validate-closes-completeness WASM hook Phase 2"
status: merged
producer: story-writer
timestamp: 2026-05-25T00:00:00Z
last_amended: "2026-05-27 (v1.3) — D-510 POL-14 auto-promotion frontmatter sync per HIGH-003 closure: status draft→merged. [Prior: 2026-05-25 (v1.2) — remove-uncertainty fix burst: U12/U13 host::read_file Vec<u8> conversion notes added to T-5 read_current_adversary_pass_number + run_hook_phase2_cross_site; U14 path_allow dependency note on S-15.12 configuration added to T-6. + residual sweep: File Structure lib.rs run_hook→on_post_tool_use. [Prior: 2026-05-25 (v1.1) — 3M3b-r pass-1 fix-burst: F-1 (HIGH) pointer file semantics corrected per ADR-022 (integer pass number, not path; renamed read_current_adversary_pass_number, added derive_adversary_review_path); F-3 (HIGH) Behavioral Contracts Table added; F-5 (MEDIUM) ADR-022 dependency note added to Dependencies section; F-12 (LOW) Demo Recording section added; AC-1b added for invalid-integer block path; 6th bats fixture added. [Prior: 2026-05-25 (v1.0) — Initial story spec authored by story-writer; status: draft. M3 wave. BC-5.39.007 Phase 2. Gated on ADR-022 + S-15.12 SHIPPED. STORY-INDEX v3.56→v3.57.]]"
phase: section-12-step-3M3b
cycle: brownfield-backfill
inputs:
  - .factory/cycles/v1.0-brownfield-backfill/s-15.03-wave-plan-2026-05-15.md
  - .factory/specs/behavioral-contracts/ss-05/BC-5.39.007.md
  - .factory/specs/architecture/decisions/ADR-022-hook-current-pass-context-discovery.md
  - .factory/stories/S-15.12-validate-closes-completeness.md
  - .factory/stories/STORY-INDEX.md
  - .factory/specs/architecture/ARCH-INDEX.md
  - plugins/vsdd-factory/hooks-registry.toml
  - crates/hook-sdk/src/host.rs
input-hash: "2b93de4"
traces_to:
  - .factory/cycles/v1.0-brownfield-backfill/s-15.03-wave-plan-2026-05-15.md
  - .factory/specs/behavioral-contracts/ss-05/BC-5.39.007.md
  - .factory/specs/architecture/decisions/ADR-022-hook-current-pass-context-discovery.md
wave: M3
milestone: M3
points: 8
priority: P1
track: engine-discipline-automation
effort_class: multi-day-with-research
depends_on: [S-15.12]
blocks: []
behavioral_contracts: [BC-5.39.007]
verification_properties: []
domain_invariants: []
tdd_mode: strict
target_module: "crates/hook-plugins/validate-closes-completeness"
subsystems: ["SS-05"]
estimated_days: 3.0
assumption_validations: []
risk_mitigations: []
anchored_adrs: [ADR-017, ADR-018, ADR-022]
closes:
  - "D-411(c)"
  - "D-413(b)"
  - "D-420(a)"
  - "D-445(a)"
  - "D-447(a)"
branch: "feature/S-15.13-validate-closes-completeness-p2"
dispatch_authority: ".factory/cycles/v1.0-brownfield-backfill/s-15.03-wave-plan-2026-05-15.md"
modified:
  - "2026-05-25 (v1.0) — initial authoring"
  - "2026-05-25 (v1.1) — 3M3b-r pass-1 fix-burst: F-1+F-3+F-5+F-12"
  - "2026-05-25 (v1.2) — remove-uncertainty fix burst: U12/U13 Vec<u8> conversion, U14 path_allow note"
---

# S-15.13: validate-closes-completeness WASM Hook Phase 2

**Epic:** E-12 — Engine Governance (brownfield-backfill wave, S-15.03 PRIORITY-A M3)
**Delivery order:** M3 (must ship AFTER S-15.12 merged; gated on ADR-022 Option c state-manager obligation)
**Depends on:** S-15.12 (Phase 1 crate must be merged; Phase 2 extends the same crate)
**Blocks:** none
**Priority:** P1 (closes D-411(c)+D-413(b)+D-420(a)+D-445(a)+D-447(a))
**ADR gate:** ADR-022 Option c ACCEPTED — state-manager must write `.factory/current-adversary-pass.txt`
  at every Commit A before Phase 2 is functional

## Narrative

- **As a** state-manager or agent writing a closing annotation in a fix burst
- **I want to** receive an immediate PostToolUse block when the Closes set in my burst-log,
  STATE.md, INDEX.md, or decision-log.md entry is missing a finding from the canonical finding
  set established in the current adversary review file
- **So that** the class of cross-cell Closes completeness failures — where a finding is cited
  in some sites but silently omitted from others — is caught mechanically at write time

## Why This Exists

Phase 1 (S-15.12) enforces Closes annotation FORMAT: correct bold-prefix form, no forbidden
annotations, umbrella cite flags. But Phase 1 cannot verify that the SET of findings cited
matches what the adversary actually reported — cross-site staleness (cite ID exists but references
a nonexistent finding) remains advisory-only in Phase 1.

Phase 2 adds cross-document Closes set agreement: the hook reads the current adversary review
file (located via `.factory/current-adversary-pass.txt` written by state-manager at Commit A
per ADR-022 Option c) to extract the canonical finding set, then validates that the 8 prescribed
citation sites each enumerate the complete finding set.

D-411(c)+D-413(b) codified the 8-site closure-set cross-validation. D-420(a) codified
multi-site cardinality agreement. D-445(a) established burst-log Dim-5 + Closes + STATE.md
Decisions Log row Closes must enumerate the complete finding set. D-447(a) established the
downstream-citation-scope coverage at Commit E.

## Precondition: ADR-022 State-Manager Obligation

**Phase 2 becomes operational when:**
1. S-15.12 is merged (Phase 1 shipped) — crate scaffolding exists.
2. state-manager's Commit A sequence writes `.factory/current-adversary-pass.txt` (ADR-022
   Option c obligation). This file contains **a single integer, the current pass number**
   (e.g., `15`). It does NOT contain a file path.

The hook must:
1. Read the integer from `.factory/current-adversary-pass.txt` via
   `host::read_file(".factory/current-adversary-pass.txt", 1024, 1000)`
2. Parse the trimmed content as a `u32` pass number
3. Construct the adversary review file path using the convention:
   `.factory/cycles/<cycle-id>/adv-cycle-pass-{N}.md`
   where N is the integer and cycle-id is the currently active cycle
   (hardcode `v1.0-brownfield-backfill` for M3 scope; cycle resolver is future scope)
4. Read the derived adversary review file via `host::read_file`

**Until state-manager's Commit A sequence is updated to write this file, Phase 2 checks
are fail-open** (hook reads the pointer file; if absent, logs advisory and returns Continue).
Phase 2 blocks only when the pointer file is present and the adversary review file is readable.

**Implementer MUST NOT remove the fail-open path** even after state-manager obligation is
activated — hooks that hardcode the pointer path and block when file is absent would break
all workflows that don't use the current-pass pointer (e.g., manual STATE.md edits).

## Scope

**In scope (Phase 2 addition to S-15.12's crate):**
- Extension to `crates/hook-plugins/validate-closes-completeness/src/lib.rs` with Phase 2
  cross-document finding-set validation
- New function: read `.factory/current-adversary-pass.txt` to locate the current adversary file
- New function: parse canonical finding set from the adversary review file (Part A findings)
- New function: cross-validate finding IDs across the 8 prescribed citation sites
- WASM recompilation to `plugins/vsdd-factory/hook-plugins/validate-closes-completeness.wasm`
  (replaces Phase 1 binary)
- New bats integration tests (appended to Phase 1 test suite; same directory)
- PASS + FAIL fixtures for cross-site agreement scenarios
- LOCAL adversary cascade per BC-5.39.001 (3-CLEAN convergence required)

**Not in scope:**
- Phase 1 behavior changes — Phase 1 checks are frozen; Phase 2 is additive
- Modification of state-manager's Commit A obligations (that is state-manager scope; ADR-022
  specifies the obligation but implementation is in state-manager's operating procedure)
- Enforcement of Closes completeness for non-adversary findings (e.g., TD-VSDD-NNN items
  not in the adversary review file are outside Phase 2 scope)

## D-NNN Sub-Clauses Closed (Phase 2)

| Sub-clause | What It Requires the Hook to Enforce |
|------------|---------------------------------------|
| D-411(c) | Closure-set completeness lint: Closes enumeration must cross-validate across 8 sites. Hook reads current adversary file (via `.factory/current-adversary-pass.txt` pointer), extracts canonical F-PNN-NNN finding IDs from Part A. Validates each of the 8 citation sites contains all finding IDs from the canonical set. Emits block naming any site missing a finding. |
| D-413(b) | Full 8-site Closes cross-validation (same as D-411(c)). The 8 sites are: (1) burst-log Dim-5, (2) burst-log Closes block, (3) STATE.md Decisions Log row Closes, (4) INDEX.md Convergence Status, (5) lessons.md entry Closes, (6) decision-log.md D-NNN row Closes, (7) ARCH-INDEX cite (if relevant), (8) BC-INDEX cite (if relevant). |
| D-420(a) | Multi-site closure-set cardinality agreement: all sites must cite the same count of findings. Hook validates cardinality parity across sites. |
| D-445(a) | Burst-log Dim-5 + Closes block + STATE.md Decisions Log row Closes must enumerate the complete finding set (the primary 3-site check that Phase 2 enables at write time for STATE.md and burst-log.md writes). |
| D-447(a) | Downstream-citation-scope coverage at Commit E: lesson body + burst-log Closes + Decisions Log row Closes must be updated in the same burst per D-447(a). Hook validates all three are consistent when any one of the target files is written. |

## Behavioral Contract

**BC ID:** BC-5.39.007 (Phase 2 extension)
**File:** `.factory/specs/behavioral-contracts/ss-05/BC-5.39.007.md` (v1.5 — Phase 2 reserved section)
**Phase 2 gate trigger:** S-15.12 MERGED AND state-manager writes `.factory/current-adversary-pass.txt` per ADR-022 Option c.

### Phase 2 Additional Postconditions (additive to Phase 1)

**P2-1 — Current-pass pointer resolution:**
If `.factory/current-adversary-pass.txt` is absent or unreadable via `host::read_file`,
the Phase 2 cross-site check is SKIPPED and the hook emits `HookResult::Continue` with
advisory log message: `"validate-closes-completeness Phase 2: current-adversary-pass.txt absent; skipping cross-site validation. Ensure state-manager's Commit A writes this file per ADR-022."` This is the fail-open path.

If the file is present but its content cannot be parsed as a valid positive integer, the
hook emits `HookResult::block_with_fix(...)` with a clear error message directing the
developer to run `state-manager update-pass-pointer` (per ADR-022 parse-error handling).

**P2-1b — Adversary file path construction:**
The hook constructs the adversary review file path as:
`.factory/cycles/v1.0-brownfield-backfill/adv-cycle-pass-{N}.md`
where N is the integer read from the pointer file. The hook reads the pointer file content,
trims whitespace, parses as `u32`, and derives this path. It does NOT use the pointer file
content as a literal path — the content is an integer pass number, not a path string.
Implemented by `read_current_adversary_pass_number() -> Option<u32>` which returns the
parsed integer (or None on absent/unreadable file, triggering fail-open per P2-1).
A separate function `derive_adversary_review_path(pass_n: u32) -> String` constructs the
`.factory/cycles/v1.0-brownfield-backfill/adv-cycle-pass-{N}.md` path from the integer.

**P2-2 — Adversary finding set extraction:**
If the adversary review file (path derived from the integer in the pointer file) is readable,
the hook extracts all finding IDs from Part A (pattern: `F-P\d+-\d+` or `F-BC\d+P\d+-\d+`)
as the canonical set. If Part A section is not found or the set is empty, the hook emits
advisory log and returns Continue (fail-open — cannot block on an unknown canonical set).

**P2-3 — Cross-site finding coverage:**
For each of the 8 citation sites that is accessible from the hook's `path_allow`, the hook
checks that all canonical finding IDs appear in that site's Closes citation. If any site is
missing a finding ID from the canonical set, the hook emits `HookResult::block_with_fix(...)`
naming the site and the missing finding IDs, citing D-411(c)+D-413(b).

**P2-4 — Cardinality parity:**
If the finding count cited at different sites diverges, the hook emits `HookResult::block_with_fix(...)`
naming the sites and their respective counts, citing D-420(a).

### Phase 2 Invariants

1. Phase 2 is fail-open at every step: pointer file absent → Continue + advisory; adversary file
   unreadable → Continue + advisory; Part A empty → Continue + advisory. Only when BOTH files
   are readable AND finding set is non-empty does Phase 2 produce blocking behavior.
2. Phase 1 behavior is unchanged. Phase 2 violations are additive to Phase 1 violations in the
   same BlockWithFix message.
3. The pointer file path is always `.factory/current-adversary-pass.txt` (canonical per ADR-022
   Option c). The file contains a single integer (the pass number), NOT a file path string. The
   hook MUST NOT treat the pointer file contents as a path, and MUST NOT derive the pass number
   from STATE.md parsing (that is ADR-022 Option a — the rejected alternative). The adversary
   review file path is always derived as `.factory/cycles/v1.0-brownfield-backfill/adv-cycle-pass-{N}.md`
   from the integer N.

## Behavioral Contracts Table

| BC ID | Title | Version | Postconditions Exercised |
|-------|-------|---------|--------------------------|
| BC-5.39.007 | validate-closes-completeness Phase 1 WASM hook MUST block on missing Closes blocks in burst-log entries and lessons.md entries, malformed cite IDs in Closes blocks, forbidden per-mechanism annotations, and umbrella citation sites missing the sample-vs-exhaustive flag in decision-log.md, STATE.md, INDEX.md, and lessons.md | v1.5 | Phase 2 extension: P2-1, P2-1b, P2-2, P2-3, P2-4; Phase 1 invariants 1-10 (non-regression) |

## Acceptance Criteria

| AC | Criterion | Verification | BC-5.39.007 Anchor |
|----|-----------|-------------|-------------------|
| AC-1 | When `.factory/current-adversary-pass.txt` is absent, hook emits Continue + log_warn advisory; Phase 2 cross-site check skipped (traces to Phase 2 P2-1 postcondition) | Bats: `pass-p2-pointer-absent.bats` — fixture with no pointer file → pass + advisory log | BC-5.39.007 Phase 2 reserved; ADR-022 Option c |
| AC-1b | When `.factory/current-adversary-pass.txt` is present but content is not a valid positive integer (e.g., `"abc"`), hook emits block with error message directing developer to run `state-manager update-pass-pointer` (traces to Phase 2 P2-1 parse-error path) | Bats: `fail-p2-pointer-invalid-integer.bats` — fixture with pointer file containing `"abc"` → block with parse-error message | ADR-022 parse-error handling |
| AC-2 | When pointer file present but adversary review file is unreadable, hook is fail-open (traces to Phase 2 P2-2) | Bats: `pass-p2-adversary-unreadable.bats` → pass + advisory log | BC-5.39.007 invariant 9 (fail-open) |
| AC-3 | When pointer file and adversary file present and complete, hook extracts finding IDs from Part A section correctly | Bats: `pass-p2-pointer-present-valid.bats` — fixture with pointer file pointing to adversary file with 3 findings in Part A → extraction succeeds, found 3 IDs | Phase 2 P2-2 |
| AC-4 | Hook blocks when a citation site (e.g., STATE.md Decisions Log row Closes) is missing a finding ID from the canonical set; block message names the site and missing ID, cites D-411(c) (traces to Phase 2 P2-3) | Bats: `fail-p2-site-missing-finding.bats` — STATE.md Decisions Log row Closes has `F-P74-001, F-P74-002` but canonical set is `F-P74-001, F-P74-002, F-P74-003` → block naming STATE.md Decisions Log row + missing F-P74-003 | BC-5.39.007 Phase 2; D-411(c) |
| AC-5 | Hook blocks on cardinality divergence: one site cites 3 findings, another cites 2 (traces to Phase 2 P2-4) | Bats: `fail-p2-cardinality-diverges.bats` → block citing D-420(a) | D-420(a) |
| AC-6 | All Phase 1 checks continue to fire correctly — Phase 2 additions do NOT regress Phase 1 behavior | Bats: Phase 1 PASS and FAIL fixtures from S-15.12 suite run against Phase 2 binary → all Phase 1 results unchanged | BC-5.39.007 Phase 1 invariants |
| AC-7 | Hook crate compiles to wasm32-wasip1 cleanly with zero warnings after Phase 2 additions | `cargo build --release --target wasm32-wasip1 -p validate-closes-completeness` passes | Architect Q4 convention |
| AC-8 | pre-flight 4-gate passes after Phase 2 additions | All four gate commands exit 0 | CLAUDE.md production-grade default |

## Tasks

### Phase: test-writer

- [ ] T-1: Read `plugins/vsdd-factory/tests/validate-closes-completeness/` (Phase 1 tests from S-15.12)
  to confirm naming conventions and layout. Phase 2 tests are added to the same directory.
- [ ] T-2: Create Phase 2 fixture directories under `plugins/vsdd-factory/tests/fixtures/validate-closes-completeness/`:
  - `pass-p2-pointer-absent/` — STATE.md fixture; no `.factory/current-adversary-pass.txt` file in fixture tree
  - `pass-p2-adversary-unreadable/` — pointer file present (contains integer `15`) pointing to a nonexistent adversary file
  - `pass-p2-pointer-present-valid/` — pointer file (contains integer `15`) + complete adversary review file at
    `.factory/cycles/v1.0-brownfield-backfill/adv-cycle-pass-15.md` with 3 Part A findings; all 8 sites correct
  - `fail-p2-site-missing-finding/` — pointer (integer `15`) + adversary file with 3 findings; STATE.md Decisions Log
    row Closes only cites 2
  - `fail-p2-cardinality-diverges/` — pointer (integer `15`) + adversary file; one site cites 3 findings, another cites 2
  - `fail-p2-pointer-invalid-integer/` — pointer file present but containing non-integer content `"abc"`
- [ ] T-3: Author bats files: `pass-p2-pointer-absent.bats`, `pass-p2-adversary-unreadable.bats`,
  `pass-p2-pointer-present-valid.bats`, `fail-p2-site-missing-finding.bats`,
  `fail-p2-cardinality-diverges.bats`, `fail-p2-pointer-invalid-integer.bats`.
  Mark `@pending`. Confirm red state.

### Phase: implementer

- [ ] T-4: Read `crates/hook-plugins/validate-closes-completeness/src/lib.rs` (Phase 1 implementation)
  in full before adding Phase 2 functions.
- [ ] T-5: Add Phase 2 functions to `src/lib.rs`:

  ```
  fn read_current_adversary_pass_number() -> Result<Option<u32>, String>
  ```
  Reads `.factory/current-adversary-pass.txt` via
  `host::read_file(".factory/current-adversary-pass.txt", 1024, 1000)`.
  `host::read_file` returns `Result<Vec<u8>, HostError>` — NOT String.
  Full pipeline:
  ```rust
  let bytes = match host::read_file(".factory/current-adversary-pass.txt", 1024, 1000) {
      Ok(b) => b,
      Err(_) => return Ok(None),  // fail-open: absent or unreadable
  };
  let content = match String::from_utf8(bytes) {
      Ok(s) => s,
      Err(_) => return Err("pointer file: UTF-8 decode error".to_string()),
  };
  let trimmed = content.trim();
  match trimmed.parse::<u32>() {
      Ok(n) => Ok(Some(n)),
      Err(_) => Err(format!(
          "validate-closes-completeness: .factory/current-adversary-pass.txt contains {:?} which is not a valid positive integer. Run state-manager update-pass-pointer to fix.",
          trimmed
      )),
  }
  ```
  On HostError (absent or unreadable): returns `Ok(None)` (fail-open advisory path per P2-1).
  On UTF-8 error: returns `Err(message)` (hard block — corrupt pointer file).
  On successful parse: returns `Ok(Some(n))`.
  On parse failure (non-integer content): returns `Err(message)` (hard block per ADR-022
  parse-error handling — this is NOT fail-open; a corrupt pointer file must be fixed).

  ```
  fn derive_adversary_review_path(pass_n: u32) -> String
  ```
  Derives the adversary review file path from the integer pass number.
  Returns `.factory/cycles/v1.0-brownfield-backfill/adv-cycle-pass-{pass_n}.md` (hardcoded
  cycle for M3 scope; cycle resolver is future scope per ADR-022 Implementation Notes).
  This function is pure (no I/O) and encapsulates the pointer-integer → path-string convention.

  ```
  fn extract_canonical_finding_set(adversary_content: &str) -> Vec<String>
  ```
  Find the `## Part A` section (or `# Part A` or `**Part A**` heading) in the adversary review
  file. Within that section, extract all finding IDs matching `F-P\d+-\d+` or `F-BC\d+P\d+-\d+`
  patterns. Return Vec<String> of unique IDs in order of appearance. Empty vec if Part A not found.

  ```
  fn collect_closes_cites(content: &str, site_name: &str) -> Vec<String>
  ```
  Given a document's content and the site name (e.g., "STATE.md Decisions Log row"), extract
  all finding IDs from Closes blocks in that document. The exact extraction strategy depends
  on the site: for STATE.md and decision-log.md, scan all `**Closes:**` lines in the Decisions
  Log section; for lessons.md, scan `**Closes:**` lines in lesson entries; for burst-log.md,
  scan the burst entry's Closes bold-block. Returns Vec<String> of unique cited finding IDs.

  ```
  fn check_cross_site_completeness(
      canonical_set: &[String],
      sites: &[(String, Vec<String>)],  // (site_name, cited_ids)
  ) -> Vec<Violation>
  ```
  For each site in `sites`: check that all IDs in `canonical_set` appear in the site's cited_ids.
  Missing IDs → add Violation naming site + missing IDs + D-411(c). If cardinality diverges
  across sites → add cardinality Violation citing D-420(a).

  ```
  fn run_hook_phase2_cross_site(file_path: &str, content: &str) -> Vec<Violation>
  ```
  Orchestrates Phase 2 cross-site validation:
  1. Call `read_current_adversary_pass_number()`.
     - If `Ok(None)`: log_warn (pointer file absent) + return empty vec (fail-open per P2-1).
     - If `Err(msg)`: return `vec![Violation { message: msg, ... }]` (parse-error is a hard block).
     - If `Ok(Some(n))`: proceed with pass number n.
  2. Call `derive_adversary_review_path(n)` to get the adversary review file path.
     Read adversary review file via `host::read_file(adversary_path, 524288, 2000)`.
     `host::read_file` returns `Result<Vec<u8>, HostError>` — convert to String:
     `String::from_utf8(bytes).unwrap_or_default()` (or map_err for explicit fail-open).
     If HostError or UTF-8 error → log_warn + return empty vec (fail-open).
  3. Extract canonical finding set. If empty → log_warn + return empty vec (fail-open).
  4. For the triggered file arm, determine which sites are accessible and collect their cites.
     On STATE.md write: collect cites from STATE.md Decisions Log Closes (already in `content`);
     also read burst-log.md and decision-log.md via `host::read_file` if path_allow permits.
     Each additional `host::read_file` call returns `Vec<u8>` — convert to String via
     `String::from_utf8(bytes).unwrap_or_default()` before passing to `collect_closes_cites`.
  5. Call `check_cross_site_completeness` with canonical set and collected sites.
  6. Return accumulated violations.

  **Important:** Not all 8 sites are readable from every write event. Reads that fail due to
  HostError are individually fail-open (skip that site; log_warn). Do not block because a
  secondary site is unreadable.

  **Integration into `run_hook`:** After Phase 1 check accumulation, call `run_hook_phase2_cross_site`
  and append its violations to the Phase 1 violations Vec. Single combined BlockWithFix emitted.

- [ ] T-6: Update `plugins/vsdd-factory/hooks-registry.toml` entry for `validate-closes-completeness`:
  add `.factory/current-adversary-pass.txt` and `.factory/cycles` subdirectory paths to `path_allow`
  if not already present. **Dependency on S-15.12 configuration:** Check what `path_allow` S-15.12
  configured for the registry entry. If S-15.12 used the broad `.factory/cycles` path_allow (as
  shown in its T-8 registry snippet), then `.factory/cycles` already covers the adversary review
  file path. You also need `.factory/current-adversary-pass.txt` explicitly if it is not under
  `.factory/cycles`. Check the current entry before editing — do NOT add duplicate paths.
- [ ] T-7: Recompile WASM:
  ```bash
  cargo build --release --target wasm32-wasip1 -p validate-closes-completeness
  cp target/wasm32-wasip1/release/validate-closes-completeness.wasm \
     plugins/vsdd-factory/hook-plugins/validate-closes-completeness.wasm
  ```
- [ ] T-8: Activate bats tests. Populate fixtures. Confirm Phase 1 tests still pass. Run 4-gate.

### Phase: LOCAL adversary cascade

- [ ] T-9: Dispatch fresh-context adversary. Track at
  `.factory/cycles/v1.0-brownfield-backfill/S-15.13/adversary-pass-N.md`. Budget: up to 8 passes.

### Phase: pr-manager

- [ ] T-10: Open PR. Include D-411(c)+D-413(b)+D-420(a)+D-445(a)+D-447(a) closure summary.
  ADR-022 activation status noted in PR description. Squash-merge.

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| Phase 2 orchestration (`run_hook_phase2_cross_site`) | `crates/hook-plugins/validate-closes-completeness/src/lib.rs` | Effectful (multiple `host::read_file` calls for pointer + adversary + secondary sites) |
| Pointer file reader (`read_current_adversary_pass_number`) | same | Effectful (`host::read_file`) |
| Adversary path deriver (`derive_adversary_review_path`) | same | Pure (integer → path string; no I/O) |
| Finding set extractor (`extract_canonical_finding_set`) | same | Pure |
| Closes site collector (`collect_closes_cites`) | same | Pure |
| Cross-site completeness check (`check_cross_site_completeness`) | same | Pure |
| Phase 1 behavior (unchanged) | same | Same as S-15.12 |

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-15.12 (Phase 1 — MUST BE MERGED FIRST) | All Phase 1 hook logic; is_char_boundary guards; path-component-strict guards; 4-arm routing | Phase 2 extends this crate — do NOT change any Phase 1 function signatures | Phase 2 is fail-open at every step; if pointer file absent, Cross-site check skips entirely. DO NOT block on absent pointer file. |
| S-15.14 (validate-dispatch-advance) | Multi-file read pattern: reads both STATE.md AND INDEX.md within a single invocation | Each additional `host::read_file` call is independently fail-open; don't let one failure cascade to block | Secondary site reads (burst-log.md, decision-log.md) are independently fail-open — each read error skips that site with log_warn |
| ADR-022 (context-discovery) | Option c: state-manager writes `.factory/current-adversary-pass.txt` at Commit A | Pointer file is a simple one-line text file containing a **single integer** — the current pass number (e.g., `15`). NOT a file path. The hook reads the integer, then derives the adversary review path as `.factory/cycles/v1.0-brownfield-backfill/adv-cycle-pass-{N}.md`. | ADR-022 Option a (parse STATE.md directly) REJECTED — hook must use Option c integer pointer file; never treat pointer file contents as a path string; never parse STATE.md to derive pass context |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| Phase 2 MUST be additive to Phase 1 crate (no Phase 1 function changes) | S-15.13 scope constraint | Code review: `git diff crates/hook-plugins/validate-closes-completeness/src/lib.rs` — Phase 1 function bodies unchanged |
| Pointer file path is always `.factory/current-adversary-pass.txt` | ADR-022 Option c | Code review: no `STATE.md` parse for pass discovery |
| Pointer file content is an integer pass number, NOT a file path — use `read_current_adversary_pass_number()` returning `Option<u32>` | ADR-022 pointer file specification | Code review: no code that calls `.trim().to_string()` on pointer content and passes it directly to `host::read_file`; must call `derive_adversary_review_path(n)` |
| Phase 2 is fail-open at every step | BC-5.39.007 Phase 2 invariant 1 | Bats: `pass-p2-pointer-absent.bats` MUST PASS (not block) |
| is_char_boundary() guards unchanged from Phase 1 | BC-5.39.007 invariant 10 | Code review |
| `host::read_file` returns `Vec<u8>` — must convert to String before parsing | `crates/hook-sdk/src/host.rs` API | Code review: every `host::read_file` call followed by `String::from_utf8(bytes)` |
| No `--no-verify` or AI attribution | CLAUDE.md §Git Workflow | Forbidden |

## Library & Framework Requirements (MANDATORY)

Same as S-15.12. No additional dependencies required. All libraries inherited from Phase 1
Cargo.toml workspace configuration.

## File Structure Requirements (MANDATORY)

### Modified files — Rust crate (1-2)

| File | Action | Purpose |
|------|--------|---------|
| `crates/hook-plugins/validate-closes-completeness/src/lib.rs` | modify | Add Phase 2 functions; update `on_post_tool_use` to call Phase 2 |
| `crates/hook-plugins/validate-closes-completeness/src/main.rs` | modify (if needed) | Update if new host imports required |

### Modified files — registry (1)

| File | Action | Purpose |
|------|--------|---------|
| `plugins/vsdd-factory/hooks-registry.toml` | modify (if needed) | Add `current-adversary-pass.txt` to `path_allow` if not already covered |

### Compiled artifact (1 — replaces Phase 1 binary)

| File | Action | Purpose |
|------|--------|---------|
| `plugins/vsdd-factory/hook-plugins/validate-closes-completeness.wasm` | replace | Updated binary with Phase 2 cross-site checks |

### New files — bats tests (6) and fixtures (6 directories)

| File | Purpose |
|------|---------|
| `plugins/vsdd-factory/tests/validate-closes-completeness/pass-p2-pointer-absent.bats` | AC-1 |
| `plugins/vsdd-factory/tests/validate-closes-completeness/fail-p2-pointer-invalid-integer.bats` | AC-1b |
| `plugins/vsdd-factory/tests/validate-closes-completeness/pass-p2-adversary-unreadable.bats` | AC-2 |
| `plugins/vsdd-factory/tests/validate-closes-completeness/pass-p2-pointer-present-valid.bats` | AC-3 |
| `plugins/vsdd-factory/tests/validate-closes-completeness/fail-p2-site-missing-finding.bats` | AC-4 |
| `plugins/vsdd-factory/tests/validate-closes-completeness/fail-p2-cardinality-diverges.bats` | AC-5 |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|----------------|-----------------|
| This story spec | ~7,500 |
| `crates/hook-sdk/src/host.rs` | ~2,500 |
| BC-5.39.007 (1 BC file) Phase 2 reserved section | ~3,000 |
| S-15.12 story (Phase 1 implementation reference) | ~9,000 |
| Phase 1 `src/lib.rs` (read before extending) | ~3,000 |
| ADR-022 (context discovery protocol) | ~3,000 |
| Wave plan S-15.13 scope | ~800 |
| Tool outputs overhead | ~1,000 |
| **Total** | **~29,800** |
| Agent context window | 200K (Sonnet) |
| **Budget usage** | **~15%** |

## Dependencies

**Blocking dependency: S-15.12 MERGED.** Phase 2 adds to Phase 1's crate scaffolding.

**Functional dependency: ADR-022 Option c ACTIVATED (state-manager writes pointer file).**
ADR-022 is a functional gate, not just a referenced ADR. Phase 2 is structurally fail-open
until state-manager's Commit A sequence writes `.factory/current-adversary-pass.txt` (a
single-integer file containing the current pass number). Until this obligation is activated,
all Phase 2 cross-site validation is skipped (hook returns Continue + advisory). Blocks are
only produced when the pointer file is present AND parseable as a valid integer AND the
derived adversary review file is accessible.

**Action required for Phase 2 to become fully operational:** After S-15.13 ships, the
state-manager Commit A operating procedure must be updated to write the pointer file as
step 0 of every Commit A (per ADR-022 Consequences/Mitigations). This is state-manager
scope, not implementer scope. Until that update is deployed, Phase 2 is inert but not
broken — the fail-open path guarantees no false blocks.

## Risk

| Risk | Severity | Mitigation |
|------|----------|-----------|
| ADR-022 state-manager obligation not yet activated when S-15.13 ships | HIGH | The fail-open path (pointer absent → Continue) guarantees Phase 2 is inert until activation. No false blocks. Document activation steps in PR description. |
| Part A finding extraction regex too narrow — misses some finding IDs in adversary review format | MEDIUM | Test with real adversary review file format (read actual `.factory/cycles/v1.0-brownfield-backfill/adv-cycle-pass-*.md` to verify Part A structure before writing regex) |
| Secondary site reads (burst-log, decision-log) fail due to path_allow restrictions | MEDIUM | Each secondary site read is independently fail-open; missing site is skipped with log_warn; only sites that are readable are validated |

## Demo Recording

Per per-story-delivery requirements, a demo recording is required before PR dispatch.

**Format:** VHS terminal cast or equivalent, following the validate-burst-log / validate-dispatch-advance demo pattern.

**Coverage required:**
1. Show the pointer-file-absent fail-open path: hook returns Continue + advisory log
2. Show the invalid-integer block path: hook returns block with parse-error message
3. Show a Phase 1 violation still fires (Phase 1 non-regression demonstration)
4. Show the cross-site site-missing-finding block: hook returns block naming site + finding

**Reference:** Follow the demo recording approach from S-15.12 (Phase 1 sibling story).

## LOCAL Adversary Cascade Plan

BC-5.39.001 3-CLEAN protocol. Budget: up to 8 passes.

**Adversary special notes:**
- Adversary MUST verify Phase 2 fail-open path produces Continue (not block) when pointer absent
- Adversary MUST verify invalid-integer pointer file produces a hard block (NOT fail-open)
- Adversary MUST verify pointer file is read as integer and path is derived via `derive_adversary_review_path(n)` — NOT treated as a literal path string
- Adversary MUST verify Phase 1 behavior is unchanged (all Phase 1 bats pass)
- Adversary MUST verify ADR-022 pointer file path is hardcoded, not derived from STATE.md parse

## References

| Source | Purpose |
|--------|---------|
| `.factory/cycles/v1.0-brownfield-backfill/s-15.03-wave-plan-2026-05-15.md` §S-15.13 | Binding scope |
| `.factory/specs/behavioral-contracts/ss-05/BC-5.39.007.md` Phase 2 reserved section | Phase 2 behavioral contract (to be authored per wave plan as part of S-15.13) |
| `.factory/specs/architecture/decisions/ADR-022-hook-current-pass-context-discovery.md` | Option c architecture: pointer file protocol |
| `.factory/stories/S-15.12-validate-closes-completeness.md` | Phase 1 reference implementation |

## Changelog

| Version | Date | Change |
|---------|------|--------|
| 1.2 | 2026-05-25 | remove-uncertainty fix burst: U12/U13 `host::read_file` returns `Vec<u8>` — full String::from_utf8 pipeline shown in `read_current_adversary_pass_number` T-5 implementation; Vec<u8>→String conversion notes added to `run_hook_phase2_cross_site` for adversary file read and secondary site reads; Architecture Compliance rule added. U14 T-6 path_allow dependency note on S-15.12 configuration added. |
| 1.1 | 2026-05-25 | 3M3b-r pass-1 fix-burst: F-1 (HIGH) pointer file semantics corrected per ADR-022 — content is integer pass number NOT path string; renamed `read_current_adversary_pass_path` → `read_current_adversary_pass_number` returning `Option<u32>`; added `derive_adversary_review_path(pass_n: u32) -> String`; ADR-022 invariant 3 updated; P2-1b added for parse-error hard block; AC-1b added; 6th bats fixture `fail-p2-pointer-invalid-integer/` added; Previous Story Intelligence ADR-022 row corrected. F-3 (HIGH) Behavioral Contracts Table added. F-5 (MEDIUM) ADR-022 activation dependency note added to Dependencies section. F-12 (LOW) Demo Recording section added. |
| 1.0 | 2026-05-25 | Initial story spec authored by story-writer; status: draft. M3. BC-5.39.007 Phase 2. ADR-022 gate. D-NNN: D-411(c)+D-413(b)+D-420(a)+D-445(a)+D-447(a). STORY-INDEX v3.56→v3.57. |
