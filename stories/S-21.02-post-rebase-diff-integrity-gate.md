---
document_type: story
level: implementation
story_id: S-21.02
epic_id: "E-21"
version: "1.0"
title: "post-rebase diff-integrity gate: detect and surface silent production-code drops before force-push-with-lease"
status: draft
producer: story-writer
timestamp: "2026-07-19T00:00:00Z"
last_amended: "2026-07-19 (v1.0) — Initial authoring (story-writer; E-21 factory-state data-loss hardening; issue #365; INV-E21-005 post-rebase diff-integrity gate; BC BC-5.44.001 CAP-035; ADR-031 §Decision 6)."
phase: brownfield-backfill
cycle: v1.0-brownfield-backfill
inputs:
  - .factory/cycles/v1.0-brownfield-backfill/e-21-arch-delta-analysis.md
  - .factory/specs/behavioral-contracts/ss-05/BC-5.44.001.md
  - .factory/specs/architecture/decisions/ADR-031-e21-factory-state-data-loss-hardening.md
  - plugins/vsdd-factory/agents/pr-manager.md
  - plugins/vsdd-factory/skills/deliver-story/steps/step-f-pr-lifecycle.md
input-hash: "f4c0f9d"
traces_to:
  - .factory/stories/epics/E-21-factory-state-data-loss-hardening.md
wave: 1
priority: P1
points: 3
track: feature
effort_class: single-day
depends_on: []
blocks: []
behavioral_contracts: [BC-5.44.001]
verification_properties: []
domain_invariants: []
tdd_mode: strict
target_module: "plugins/vsdd-factory/agents/pr-manager.md"
subsystems:
  - "SS-05"
estimated_days: 1.0
assumption_validations: []
risk_mitigations: []
anchored_adrs: [ADR-031]
closes:
  - "INV-E21-005 (issue #365): post-rebase diff-integrity gate between git rebase completion and git push --force-with-lease"
feature: "e21-factory-state-hardening"
modified:
  - "2026-07-19 v1.0: Initial authoring"
---

# S-21.02 — post-rebase diff-integrity gate: detect and surface silent production-code drops before force-push-with-lease

**Epic:** E-21 — Factory State Data-Loss Hardening
**Wave:** 1 (no inter-E-21 dependencies; can begin immediately in parallel with S-21.01 and S-21.03)
**Depends on:** none
**Priority:** P1 (process-gap; silent production-code drops are high-impact but empirically rare — real silent drops require larger-hunk or moved-code diffs per BC-5.44.001 description)
**Subsystem anchor:** SS-05 (Pipeline Orchestration) owns the pr-manager agent and the orchestrator per-story delivery playbook.

## Narrative

Issue #365 documents a confirmed process gap: git's ORT 3-way merge algorithm can silently drop production lines during a rebase when adjacent (non-overlapping) code regions on two branches collide in the merge base window. `git rebase --continue` reports success with no conflict markers, but lines added by the parent commit are absent from the rebased branch. The factory's per-story delivery protocol has no detection gate between "rebase clean" and `git push --force-with-lease`.

The canonical detection tool is `git range-diff` (git ≥ 2.19): it compares the pre-rebase and post-rebase commit sequences, flagging commits whose diff hunks changed during replay. BC-5.44.001 mandates a two-step gate:

1. **Primary detector — `git range-diff`:** Compare `<pre-rebase-tip>...<post-rebase-tip>` to detect commits that changed during replay. Any modified commit that touches a file also modified by a recently-merged sibling story requires explicit delta verification.

2. **Backup heuristic — `git diff origin/develop --stat`:** For each file with a net-negative line count, check whether a recently-merged sibling story also modified that file. If unverified net-negative delta is found, STOP with `UnverifiedNetNegativeDelta` before force-push.

This story is entirely skill-doc (no new WASM plugin or shell script). POLICY 21 satisfied.

## Acceptance Criteria

| ID | Criterion | Gate | BC Trace |
|----|-----------|------|----------|
| AC-001 | `plugins/vsdd-factory/agents/pr-manager.md` contains a mandatory post-rebase diff-integrity gate step between "rebase complete" and "force-push-with-lease". The gate step: (a) runs `git range-diff <pre-rebase-tip>...<post-rebase-tip>` as the primary detector; (b) falls back to `git diff origin/develop --stat` if range-diff unavailable; (c) for any file showing net-negative delta AND modified by a recently-merged sibling story that cannot be confirmed as intentional removal, emits STOP signal with `UnverifiedNetNegativeDelta` error message. | manual: confirm gate step present in pr-manager.md between the rebase step and the force-push step (grep for `range-diff` + `UnverifiedNetNegativeDelta` in pr-manager.md); bats fixture (below) exercises the halt path. | BC-5.44.001 PC2, Invariant 1 |
| AC-002 | The orchestrator per-story delivery playbook (`step-f-pr-lifecycle.md`) references the post-rebase diff-integrity gate as a required step when a rebase occurs before force-push-with-lease. | manual: confirm gate step present in step-f-pr-lifecycle.md; grep for `range-diff` or `diff-integrity gate`. | BC-5.44.001 Invariant 1 |
| AC-003 | A bats fixture simulates a rebase scenario where `git diff origin/develop --stat` returns a net-negative delta on a file also modified by a recently-merged sibling story. The fixture asserts: (a) the gate detects the delta; (b) `git push --force-with-lease` is NOT invoked; (c) `UnverifiedNetNegativeDelta` error message is present in output. | bats: mock `git diff origin/develop --stat` returning `-4` lines on `autoload.gd`; mock sibling-story commit on origin/develop modifying `autoload.gd`; assert force-push blocked + `UnverifiedNetNegativeDelta`. | BC-5.44.001 PC2, Invariant 1 |
| AC-004 | A bats fixture confirms the gate passes (force-push allowed) when no file in the diff is touched by a recently-merged sibling story. | bats: mock `git diff origin/develop --stat` returning only feature-branch files with no sibling overlap; assert force-push proceeds. | BC-5.44.001 PC3 |
| AC-005 | A bats fixture confirms the gate passes when the agent explicitly verifies a net-negative delta as intentional removal (i.e., the delta is present in the feature branch's own commit history). | bats: mock scenario with `-4` lines confirmed as intentional in feature branch commits; assert gate passes (PC1 path). | BC-5.44.001 PC1 |

## Architecture Mapping

This story amends two skill/agent files and adds one bats suite:

- `plugins/vsdd-factory/agents/pr-manager.md` — add post-rebase diff-integrity gate step after every `git rebase` / `git rebase --continue` / `git pull --rebase` and before `git push --force-with-lease` (BC-5.44.001 Invariant 1). Step must include `git range-diff` as primary detector + `git diff --stat` backup + explicit `UnverifiedNetNegativeDelta` halt signal
- `plugins/vsdd-factory/skills/deliver-story/steps/step-f-pr-lifecycle.md` — reference the gate as mandatory in the rebase-then-force-push sub-flow (BC-5.44.001 Invariant 1)
- `plugins/vsdd-factory/tests/post-rebase-diff-integrity-gate.bats` — new bats suite for AC-003..AC-005

**ADR-031** §Decision 6 governs: no new WASM plugin or shell script required. The gate is a skill-doc mandate.

## Behavioral Contracts

| BC ID | Title | Version | Story ACs |
|-------|-------|---------|-----------|
| BC-5.44.001 | pr-manager and orchestrator MUST run a post-rebase diff-integrity gate after any `git rebase` on a feature branch, asserting that no file touched by a recently-merged sibling story shows an unverified net-negative line-count delta before force-push-with-lease | v1.2 | AC-001 (PC2, Invariant 1), AC-002 (Invariant 1), AC-003 (PC2, Invariant 1), AC-004 (PC3), AC-005 (PC1) |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | Rebase clean; diff shows only feature-branch files; no sibling overlap | Gate passes (PC3); force-push proceeds (AC-004) |
| EC-002 | Rebase clean; diff shows `-7` lines in `autoload.gd`; sibling story also modified `autoload.gd`; agent confirms intentional | Gate passes (PC1); force-push proceeds (AC-005) |
| EC-003 | Rebase clean; diff shows `-4` lines in `autoload.gd`; sibling story also modified `autoload.gd`; agent cannot confirm | STOPPED: PC2; `UnverifiedNetNegativeDelta`; manual review required (AC-003) |
| EC-004 | `git diff origin/develop --stat` fails (network error) | Logged warning; gate escalates rather than force-pushing blind (BC-5.44.001 EC-005) |
| EC-005 | `git range-diff` unavailable (git < 2.19) | Fall back to `--stat` heuristic per BC-5.44.001 step 1b |
| EC-006 | No sibling-story commits since branch creation | Gate passes trivially (PC4); force-push proceeds |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| `plugins/vsdd-factory/agents/pr-manager.md` | N/A (agent definition) | Markdown specification; no code purity boundary |
| `plugins/vsdd-factory/skills/deliver-story/steps/step-f-pr-lifecycle.md` | N/A (skill-doc) | Skill step instruction document |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~3,000 |
| BC-5.44.001 v1.2 | ~3,500 |
| ADR-031 v1.0 (§Decision 6) | ~1,500 |
| e-21-arch-delta-analysis.md (issue #365 section) | ~2,000 |
| `plugins/vsdd-factory/agents/pr-manager.md` | ~3,000 |
| `plugins/vsdd-factory/skills/deliver-story/steps/step-f-pr-lifecycle.md` | ~1,500 |
| Test fixtures (bats suite) | ~2,000 |
| Tool outputs overhead | ~1,000 |
| **Total** | **~17,500** |
| Agent context window | 200K (Sonnet) |
| **Budget usage** | **~9%** |

## Tasks (MANDATORY)

1. [ ] Capture `cargo test --workspace --all-targets` baseline pass count pre-implementation; emit `pre-implementation cargo-test baseline: <N> pass` marker.
2. [ ] Write failing bats tests for AC-003 (detect unverified net-negative delta; assert force-push blocked + `UnverifiedNetNegativeDelta`) — test-writer
3. [ ] Write failing bats tests for AC-004 (no sibling overlap → gate passes) — test-writer
4. [ ] Write failing bats tests for AC-005 (intentional removal confirmed → gate passes) — test-writer
5. [ ] Verify Red Gate: all tests fail (gate not yet in agent protocols)
6. [ ] Amend `plugins/vsdd-factory/agents/pr-manager.md`: add post-rebase diff-integrity gate step with `git range-diff` primary + `git diff --stat` backup + `UnverifiedNetNegativeDelta` halt signal (BC-5.44.001 PC1/PC2/PC3/PC4 + Invariant 1) — implementer
7. [ ] Amend `plugins/vsdd-factory/skills/deliver-story/steps/step-f-pr-lifecycle.md`: add gate reference in rebase sub-flow — implementer
8. [ ] Verify all tests pass; confirm bats and cargo suites green

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-21.01 (same epic W1) | pr-manager agent file at `plugins/vsdd-factory/agents/pr-manager.md`; two concurrent W1 stories amend pr-manager.md — coordinate to avoid conflicts | Stage all pr-manager.md changes atomically per story; no rebase-conflict risk since S-21.01 and S-21.02 touch different sections | S-21.01 adds merge pre-check near top of merge flow; this story adds post-rebase gate near push step — different sections, no conflict |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| No new `.sh` files in this story | POLICY 21, ADR-031 §Decision 6 | Implementer adds skill-doc steps only; no shell scripts created |
| Gate runs before force-push, not after | BC-5.44.001 Invariant 1 | AC-001 gate asserts pr-manager.md step ordering |
| `git range-diff` is the PRIMARY detector; `--stat` is the backup | BC-5.44.001 v1.2 §Description step 1a/1b | pr-manager.md and step-f docs reflect range-diff as step 1a |
| No AI attribution in commits | CLAUDE.md Git Workflow | No `Co-Authored-By: Claude` in any commit |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| git | ≥ 2.19 | `git range-diff` primary detector; fall back to `--stat` on older versions |
| Bats | per `plugins/vsdd-factory/tests/run-all.sh` | Integration test runner for bats fixtures |

## Red Gate Test Plan

| Test ID | Description | Expected Outcome (pre-implementation = RED) |
|---------|-------------|---------------------------------------------|
| RG-001 | Mocked rebase with unverified net-negative delta in sibling-touched file → gate should STOP | FAIL: no gate in pr-manager.md; force-push proceeds |
| RG-002 | Clean rebase; no sibling overlap → gate should pass | FAIL: gate not present; trivially passes (but test checks gate is invoked) |
| RG-003 | Intentional removal confirmed → gate should pass | FAIL: gate not present |

## Test Plan

| Test ID | Story AC | Test File | Description |
|---------|----------|-----------|-------------|
| T-001 | AC-003 | plugins/vsdd-factory/tests/post-rebase-diff-integrity-gate.bats | Detect `-4` lines in sibling-touched file; assert force-push blocked + `UnverifiedNetNegativeDelta` |
| T-002 | AC-004 | plugins/vsdd-factory/tests/post-rebase-diff-integrity-gate.bats | No sibling overlap → gate passes; force-push proceeds |
| T-003 | AC-005 | plugins/vsdd-factory/tests/post-rebase-diff-integrity-gate.bats | Intentional removal confirmed → gate passes |

## File Structure Requirements (MANDATORY)

| File | Action | Notes |
|------|--------|-------|
| `plugins/vsdd-factory/agents/pr-manager.md` | modify | Add post-rebase diff-integrity gate step (BC-5.44.001); insert between rebase step and force-push step |
| `plugins/vsdd-factory/skills/deliver-story/steps/step-f-pr-lifecycle.md` | modify | Reference diff-integrity gate as mandatory in rebase sub-flow |
| `plugins/vsdd-factory/tests/post-rebase-diff-integrity-gate.bats` | create | New bats suite: T-001..T-003 covering AC-003..AC-005 |
