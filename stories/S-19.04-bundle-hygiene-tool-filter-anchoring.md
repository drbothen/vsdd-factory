---
document_type: story
level: implementation
story_id: S-19.04
epic_id: "E-19"
version: "1.14"
title: "Registry/bundle hygiene: orphan WASM removal + tool-filter regex anchoring convention + lint check"
status: draft
producer: story-writer
timestamp: "2026-07-04T00:00:00Z"
last_amended: "2026-07-13 (v1.14) — architect binding D-a table correction (story-writer): D-a rows 1+2+7 merged; ^(Edit|Write|MultiEdit)$ is the correct form for all former Edit|Write/Write|Edit/Edit|Write|MultiEdit hooks; architect-ruling note added; Finding (b) form updated; Task 13 count explicit 5; T-009+T-010 added; WASM deletion File Structure rows added. [Prior: 2026-07-13 (v1.13) — partial-sweep escape fix (story-writer): Purity Classification stale bats row removed (plugins/vsdd-factory/tests/bundle-orphan-detection.bats — v1.12 added Rust row but did not remove bats row); Task 12 rewritten to reference bundle_orphan_check.rs (was: 'Implement bundle-orphan-detection.bats dual-registry check'); Changelog section added; input-hash refreshed. [Prior: 2026-07-13 (v1.12) — no-new-.sh policy amendment (story-writer): AC-006 orphan-detection gate converted from bats test calling bundle-orphan-check.sh to Rust workspace test (crates/factory-dispatcher/tests/bundle_orphan_check.rs) per human directive 2026-07-13 no-new-.sh standing policy (full scope including CI/test helpers); File Structure updated (bundle-orphan-detection.bats row removed; Rust test + cargo fixtures rows added); Tasks items 4/5/6 (T-006..T-008) rework noted (previously committed as bats tests in .worktrees/S-19.04 must be rewritten as cargo tests after this amendment); Test Plan T-006..T-008 test file updated; Architecture Compliance Rules no-new-.sh row added; Purity Classification Bundle-script row replaced; Token Budget test-fixtures row updated. [Prior: 2026-07-07 (v1.11) — E-19 pass-12 fix burst (story-writer): F-P12-001 AC-001 + AC-007 keep-assertion redirected from artifact/ path to git-tracked source (two-check keep-leg: (i) test -f plugins/vsdd-factory/hook-plugins/vsdd-context-resolvers.wasm; (ii) grep -q hook-plugins/vsdd-context-resolvers.wasm resolvers-registry.toml); File Structure release.yml note updated; Task 15 updated; O-P12-01 canonical skip-path form added to Architecture Mapping (vsdd_context_resolvers.wasm|wasm_resolver_export.wasm) echo skip stale resolver artifact: $name; continue ;;). [Prior: 2026-07-07 (v1.10) — E-19 pass-10 fix burst (story-writer): F-P10-002 AC-004 gate paper-fix — corrects v1.9 false attestation: `grep -v 'tool = \"^'` → `grep -vE 'tool = [\"'"'"']\^'` (quote-style-tolerant; handles both double and single-quote TOML values; TD-VSDD-059); F-P10-006 Task 13 assertion clarified to distinct tool-value strings (`grep -E '^tool = ' … | sort -u | wc -l`) equals D-a table row count. [Prior: 2026-07-07 (v1.9) — E-19 pass-8 fix burst (story-writer): F-P8-009 AC-004 gate anchor-detection filter made quote-style-tolerant (grep -vE 'tool = [\"'"'"']\^' handles both double and single-quote TOML value styles). [Prior: 2026-07-07 (v1.8) — E-19 pass-7 fix burst (story-writer): F-P7-002 replace volatile ~line pins with stable pattern anchors (7 sites: Narrative ×4, AC-001, Architecture Mapping ×3, File Structure); add TD-VSDD-091 Architecture Compliance row; F-P7-010 AC-004 gate wrapped for intrinsic non-zero exit; O-P7-005 Task 1 PR-description checklist sentence. [Prior: 2026-07-07 (v1.7) — E-19 pass-6 fix burst (story-writer): F-P6-001 POLICY 17→POLICY 20 sweep (5 sites: AC-001, Architecture Mapping, Architecture Compliance Rules, File Structure, Tasks 11+14); F-P6-002 case-arm semantic corrected — vsdd_context_resolvers.wasm|wasm_resolver_export.wasm currently PASSED THROUGH via empty ;; arm (not excluded); fix MOVES pair to skip path at both sites (~lines 175/302); O-P6-002 adjudication cited (341,975 B vs 342,292 B distinct artifacts in rc.22 bundle). [Prior: 2026-07-07 (v1.6) — E-19 pass-5 fix burst (story-writer): F-P5-002 narrative Finding (a) rewritten — vsdd_context_resolvers.wasm|wasm_resolver_export.wasm are ALREADY in exclusion case-arm at release.yml lines 175+302; hello-hook.wasm is ACTIVELY built (line 151) and copied (line 183); fix is to REMOVE both steps. F-P5-005 Architecture Mapping release.yml bullet updated (remove hello-hook build+copy steps; preserve case-arm). F-P5-006 File Structure release.yml row updated (remove hello-hook build+copy steps; preserve existing case-arm; add keep-assertion). AC-001 criterion rewritten to adversary rephrase. [Prior: 2026-07-07 (v1.5) — E-19 pass-4 fix burst (story-writer): O-P4-002 AC-004 Gate intent-comment filter updated to case/whitespace-tolerant `grep -vi '# *intent:'`. [Prior: 2026-07-06 (v1.4) — E-19 pass-3 fix burst (story-writer): F-P3-008: AC-001 gate rewritten from grep-based to fixture-based; narrative corrected: hello-hook.wasm exclusion is EXPLICIT release.yml logic to remove (not a missing exclusion). F-P3-010: blocks: [] → [S-19.06]. F-P3-017: Task added — regenerate D-a anchoring table from current registry before commit; assert count parity; exit non-zero on drift. F-P3-018: AC-007 added — integration test for release.yml artifact-staging against workspace fixture; zero dual-registry-orphan WASM assertion; wired as EAC-005 load-bearing leg. O-P3-005: Task added — run policy-registry validation over policies.yaml. O-P3-006: F-P2-012 baseline task updated to emit machine-parseable marker. [Prior: 2026-07-06 (v1.3) — E-19 pass-2 fix burst (story-writer): verification_properties: [VP-099] wired; D-a per-entry anchoring table embedded in Architecture Mapping (all current tool= entries → correct anchored forms per architect decision); AC-004/AC-005 positive-scope verification added (verify-factory-lock fires on MultiEdit fixture post-anchoring — confirms ^(Edit\\|Write\\|MultiEdit\\|Agent)$ still triggers); D-f uniform singleton anchoring patterns ^Bash$, ^Read$, ^Agent$ encoded in Architecture Mapping; AC-006 negative-control clause strengthened (F-P2-010: bats test must exit non-zero on fixture where resolvers-registry.toml check is skipped — confirms fix is load-bearing, not advisory); bundle-policy cite updated to policies.yaml registration (POLICY 17) not ADR-030; F-P2-012 cargo test baseline task added. [Prior: 2026-07-06 (v1.2) — E-19 pass-1 fix burst (story-writer): F-P1-009: AC-004 gate rewritten — broken `grep -v '^\^'` (no-op: `grep -n` output starts with line numbers, not `^`; correctly-anchored entries get misclassified as unanchored) replaced with tool-value extraction (`grep -E '^tool = ' | grep -vE 'tool = [\"'"'"']\^' | grep -vi '# *intent:'`); positive fixture (anchored entry passes) and negative fixture (unanchored entry exits non-zero) added. Missing template sections added. [Prior: 2026-07-04 (v1.1) — correction (story-writer): vsdd-context-resolvers.wasm (hyphen) removed from orphan exclusion set — it is the live WaveContextResolver referenced by resolvers-registry.toml (plugin = hook-plugins/vsdd-context-resolvers.wasm); AC-002 keep-assertion added; EC-003 dual-registry orphan check codified; AC-006 dual-registry bats test added; narrative Finding (a) corrected to 3 true orphans; sibling-site sweep Architecture Mapping + File Structure Requirements. [Prior: 2026-07-04 (v1.0) — initial draft authored by story-writer; post-rc.22 smoke gate authorization (2026-07-04); closes rc.22 smoke findings (a) 3 orphan WASMs in bundle and (b) tool-filter strings match as regex SEARCH not fullmatch.]]]]"
phase: F3
cycle: v1.0-feature-engine-discipline-pass-1
inputs:
  - plugins/vsdd-factory/hooks-registry.toml
  - plugins/vsdd-factory/resolvers-registry.toml
  - .github/workflows/release.yml
  - crates/factory-dispatcher/src/registry.rs
input-hash: "67eee80"
traces_to:
  - .factory/stories/epics/E-19-post-rc22-operator-hardening.md
wave: 2
priority: P2
points: 5
track: feature
effort_class: single-day
depends_on: []
blocks: [S-19.06]
behavioral_contracts: []
verification_properties: [VP-099]
domain_invariants: []
tdd_mode: strict
target_module: "plugins/vsdd-factory/hooks-registry.toml"
subsystems:
  - "SS-07"
  - "SS-09"
estimated_days: 1.5
assumption_validations: []
risk_mitigations: []
anchored_adrs: []
closes:
  - "rc.22 smoke finding (a): 3 orphan WASMs ship unreferenced in bundle (hello-hook.wasm, vsdd_context_resolvers.wasm, wasm_resolver_export.wasm; ~0.5 MB); release whitelist sweep does not exclude them"
  - "rc.22 smoke finding (b): dispatcher tool-filter strings match as regex SEARCH not fullmatch (Edit|Write fires on MultiEdit); convention and lint check absent"
feature: "post-rc.22-smoke"
modified:
  - "2026-07-13 v1.14: architect binding D-a correction (F-1) — D-a rows 1+2+7 merged to ^(Edit|Write|MultiEdit)$; Finding (b) form updated; architect-ruling note added; Task 13 count explicit 5; T-009+T-010 added; WASM deletion File Structure rows added; POLICY 14 parity"
  - "2026-07-13 v1.13: partial-sweep escape fix — Purity Classification stale bats row removed; Task 12 rewritten to reference bundle_orphan_check.rs; Changelog section added; input-hash refreshed"
  - "2026-07-13 v1.12: no-new-.sh policy amendment — AC-006 gate converted from bats/bundle-orphan-check.sh to Rust cargo test (crates/factory-dispatcher/tests/bundle_orphan_check.rs); File Structure bats row removed + Rust test + fixtures rows added; Tasks 4/5/6 (T-006..T-008) rework noted; Architecture Compliance no-new-.sh row added; Purity Classification updated; Token Budget updated; Test Plan T-006..T-008 test file updated"
  - "2026-07-07 v1.11: F-P12-001 AC-001 + AC-007 keep-assertion redirected to git-tracked source (two-check keep-leg: test -f plugins/vsdd-factory/hook-plugins/vsdd-context-resolvers.wasm + grep -q registry ref); File Structure + Task 15 updated; O-P12-01 canonical skip-path syntax added to Architecture Mapping"
  - "2026-07-07 v1.10: F-P10-002 AC-004 paper-fix (corrects v1.9 false attestation) grep -vE quote-tolerant class actually applied; F-P10-006 Task 13 distinct tool-value strings gate (sort -u | wc -l)"
  - "2026-07-07 v1.9: F-P8-009 AC-004 gate anchor-detection filter quote-style-tolerant (grep -vE double/single-quote character class)"
  - "2026-07-07 v1.8: F-P7-002 volatile ~line pins replaced with stable anchors (Narrative ×4, AC-001, Architecture Mapping ×3, File Structure); TD-VSDD-091 row added to Architecture Compliance Rules; F-P7-010 AC-004 gate wrapped for intrinsic non-zero exit; O-P7-005 Task 1 PR-description checklist sentence"
  - "2026-07-07 v1.7: F-P6-001 POLICY 17→POLICY 20 sweep (5 sites: AC-001, Architecture Mapping, Architecture Compliance Rules, File Structure, Tasks 11+14); F-P6-002 case-arm inverted semantic corrected (pair PASSED THROUGH not excluded; fix MOVES to skip path); O-P6-002 two-file byte-verification cited"
  - "2026-07-07 v1.6: F-P5-002 Finding (a) narrative corrected (case-arm already covers underscore pair; fix = remove hello-hook build+copy); F-P5-005 Architecture Mapping release.yml bullet; F-P5-006 File Structure release.yml row; AC-001 criterion rewritten"
  - "2026-07-07 v1.5: O-P4-002 AC-004 Gate intent-comment filter → case/whitespace-tolerant `grep -vi '# *intent:'`"
  - "2026-07-06 v1.4: F-P3-008 AC-001 fixture-based gate + narrative corrected; F-P3-010 blocks: [S-19.06]; F-P3-017 D-a anchoring table drift-check task; F-P3-018 AC-007 integration test for release.yml artifact-staging; O-P3-005 policy-registry validation task; O-P3-006 F-P2-012 baseline marker"
  - "2026-07-06 v1.3: verification_properties wired [VP-099]; D-a anchoring table embedded; AC-004/005 positive-scope verification; D-f singleton patterns; AC-006 negative-control (F-P2-010); POLICY 17 cite; F-P2-012 task"
  - "2026-07-06 v1.2: F-P1-009 AC-004 gate rewrite (tool-value extraction); missing template sections added"
  - "2026-07-04 v1.1: corrected orphan WASM set from 4→3; vsdd-context-resolvers.wasm (hyphen) is live WaveContextResolver per resolvers-registry.toml; AC-002 keep-assertion added; AC-006 dual-registry check codified; EC-003 corrected; narrative Finding (a) corrected; sibling-site sweep Architecture Mapping + File Structure Requirements"
  - "2026-07-04 v1.0: initial draft"
---

# S-19.04 — Registry/bundle hygiene: orphan WASM removal + tool-filter regex anchoring convention + lint check

**Epic:** E-19 — Post-rc.22 Operator Hardening
**Wave:** 2 (no hard dependencies on S-19.01..S-19.03; can run in parallel with wave 1)
**Depends on:** none
**Priority:** P2 (hygiene defects; no blocking behavioral gap but increase bundle size and create latent mismatch bugs)
**Subsystem anchor:** SS-07 (Hook Bash Layer) owns `hooks-registry.toml`. SS-09 (Configuration and Activation) owns the bundle and release pipeline.

## Narrative

Post-rc.22 operator install inspection found two registry/bundle hygiene gaps:

**Finding (a) — Orphan WASMs in bundle:** The rc.22 release bundle at `~/.claude/plugins/cache/claude-mp/vsdd-factory/1.0.0-rc.22/hook-plugins/` contains three WASM files that are unreferenced by any entry in EITHER `hooks-registry.toml` OR `resolvers-registry.toml`:
- `hello-hook.wasm` — a development sample; never intended for production distribution
- `vsdd_context_resolvers.wasm` — context resolver library (underscore variant); the live resolver is the hyphen variant `vsdd-context-resolvers.wasm` (referenced by `resolvers-registry.toml`); this underscore variant is a stale build artifact
- `wasm_resolver_export.wasm` — resolver export artifact; not referenced by any registry entry

**Note — `vsdd-context-resolvers.wasm` (hyphen) is NOT an orphan:** It is the live WaveContextResolver registered in `resolvers-registry.toml` as `plugin = "hook-plugins/vsdd-context-resolvers.wasm"`. The dispatcher printed "Compiled 1 resolver modules from .../resolvers-registry.toml" at every smoke-gate dispatch, confirming it was loaded. Orphan-status determination requires checking BOTH `hooks-registry.toml` (hook plugins) AND `resolvers-registry.toml` (context resolvers); checking only `hooks-registry.toml` produces a false-positive orphan classification for this file (root cause of v1.0 story defect, corrected in v1.1).

These three unreferenced files add ~0.5 MB to every operator install bundle and create noise in operator directory listings.

**Ground truth for release.yml (verified at rc.22):** `vsdd_context_resolvers.wasm` and `wasm_resolver_export.wasm` are currently PASSED THROUGH into the bundle — the `vsdd_context_resolvers.wasm|wasm_resolver_export.wasm) ;;` arm exits the inner case WITHOUT `continue`, so control falls through to `cp "$wasm" artifact/`; the comment "pass through the two legitimate underscore binaries" confirms their inclusion. They are NOT currently excluded. `hello-hook.wasm` is ACTIVELY built via the `--example hello-hook` build step and explicitly copied via the `cp …examples/hello-hook.wasm artifact/` staging step. The fix requires TWO changes: (1) REMOVE the hello-hook build step and copy step; (2) MOVE the `vsdd_context_resolvers.wasm|wasm_resolver_export.wasm` pair from the pass-through arm into the skip path (joining `*) echo "skip..."; continue` behavior) at the inner case-arm `vsdd_context_resolvers.wasm|wasm_resolver_export.wasm) ;;` at both artifact-staging sites. Nothing is "preserved as exclusion" because no exclusion of the pair currently exists. **O-P6-002 (orchestrator adjudication):** both underscore (`vsdd_context_resolvers.wasm`) AND hyphen (`vsdd-context-resolvers.wasm`) resolver files exist as distinct artifacts in the rc.22 bundle — byte-verified in the post-install smoke: 341,975 B vs 342,292 B — confirming the two-file premise and the keep-assertion for the hyphen file stand.

**Decision for hello-hook.wasm:** This is a development sample created during initial plugin scaffolding. It has no production value and MUST NOT be included in release bundles. The corrective action requires two changes: (1) remove the explicit `cargo build --example hello-hook` step and the `cp ...hello-hook.wasm artifact/` staging step from `.github/workflows/release.yml`; (2) move `vsdd_context_resolvers.wasm|wasm_resolver_export.wasm` from the current pass-through arm into the skip path at the inner case-arm `vsdd_context_resolvers.wasm|wasm_resolver_export.wasm) ;;` at both artifact-staging sites — these two underscore artifacts are orphans that must not ship. Decision recorded here (D-NNN TBD at codification); policy change registered as POLICY 20 (release_bundle_no_dev_samples).

**Finding (b) — Tool-filter regex semantics undocumented:** The `tool` field in `[[hooks]]` entries (e.g., `tool = "Edit|Write"`) is matched as a regex SEARCH (substring match), not as a fullmatch. This means `tool = "Edit|Write"` also fires on `MultiEdit` (because "Edit" is a substring of "MultiEdit"). This is semantically incorrect for hooks that are meant only for `Edit` and `Write`.

The fix requires:
1. A documented anchoring convention: hooks targeting specific tools should use anchored patterns (`^(Edit|Write|MultiEdit)$` for multi-tool Edit/Write/MultiEdit guards; `^Bash$`/`^Read$`/`^Agent$` for single-tool singletons) to prevent substring matches and ensure all first-class mutating vectors are covered
2. A hooks-registry.toml preamble comment explaining the regex-search semantics
3. A lint check (bats test) that scans all `tool =` entries and warns when an unanchored pattern could match unintended tool names (specifically: `Edit` without `^` would match `MultiEdit`; `Write` without `^` would match `MultiWrite` if introduced in future; `Bash` without anchoring would match future `BashAsync`)
4. Existing unanchored entries that fire on wrong tools to be corrected with `^(...)$` anchors

## Acceptance Criteria

| ID | Criterion | Gate | VP/BC Trace |
|----|-----------|------|-------------|
| AC-001 | After this story, release.yml no longer copies `hello-hook.wasm` into the release artifact: the explicit `cargo build --example hello-hook` step AND the `cp ...examples/hello-hook.wasm artifact/` staging step are REMOVED from `.github/workflows/release.yml`. The `vsdd_context_resolvers.wasm\|wasm_resolver_export.wasm` pass-through arm (the inner case-arm `vsdd_context_resolvers.wasm\|wasm_resolver_export.wasm) ;;` at both artifact-staging sites) is MODIFIED to move the pair into the skip path. Bundle-policy changes are registered via policies.yaml (POLICY 20 — release_bundle_no_dev_samples). | Fixture-based gate: run the release.yml artifact-staging step against a workspace fixture that contains `hello-hook.wasm`, `vsdd_context_resolvers.wasm`, and `wasm_resolver_export.wasm`; assert `test ! -f artifact/hello-hook.wasm` exits 0; assert `test ! -f artifact/vsdd_context_resolvers.wasm` exits 0; assert `test ! -f artifact/wasm_resolver_export.wasm` exits 0; assert `test -f plugins/vsdd-factory/hook-plugins/vsdd-context-resolvers.wasm` exits 0 (keep-assertion (i): git-tracked file present and untouched by the exclusion change — this file is never produced by the artifact-staging step, so its absence would indicate an unintended filesystem change); assert `grep -q "hook-plugins/vsdd-context-resolvers.wasm" plugins/vsdd-factory/resolvers-registry.toml` exits 0 (keep-assertion (ii): registry reference intact). Exit non-zero if any orphan artifact is present in `artifact/` or either keep-assertion fails. | — |
| AC-002 | `vsdd_context_resolvers.wasm` (underscore, stale artifact) and `wasm_resolver_export.wasm` are excluded from the hook-plugins/ directory in the release bundle. `vsdd-context-resolvers.wasm` (hyphen) MUST remain — it is the live WaveContextResolver registered in `resolvers-registry.toml` as `plugin = "hook-plugins/vsdd-context-resolvers.wasm"`. A keep-assertion in the bundle verification step confirms `hook-plugins/vsdd-context-resolvers.wasm` is present after the bundle is assembled. | Exclusion gate: zero occurrences of `vsdd_context_resolvers.wasm` and `wasm_resolver_export.wasm` under `hook-plugins/` in the bundle (exit non-zero if present). Keep-assertion gate: `test -f hook-plugins/vsdd-context-resolvers.wasm` exits 0 (exit non-zero if absent — L-F2-silent-inert-validator-class: assertion must be load-bearing, not advisory). | — |
| AC-003 | `plugins/vsdd-factory/hooks-registry.toml` contains a preamble comment section (placed before the first `[[hooks]]` entry) that documents: (1) the `tool` field uses regex SEARCH semantics (not fullmatch); (2) the anchoring convention (`^(ToolA\|ToolB)$`) for hooks targeting specific tool sets; (3) why anchoring matters (e.g., `Edit` without anchoring matches `MultiEdit`). | `grep -q "regex SEARCH\|fullmatch\|anchoring" plugins/vsdd-factory/hooks-registry.toml` exits 0. | VP-099 |
| AC-004 | All existing `tool =` entries in `hooks-registry.toml` that target specific tools (not wildcard) use anchored patterns per the D-a per-entry anchoring table (see Architecture Mapping). Entries that intentionally match tool prefixes must carry an inline `# intent:` comment explaining the intent. D-f singleton patterns (`^Bash$`, `^Read$`, `^Agent$`) apply to all single-tool entries. Positive-scope verification: after all entries are anchored, the `verify-factory-lock` hook (which uses `^(Edit\|Write\|MultiEdit\|Agent)$`) still fires correctly on a MultiEdit fixture, confirming that anchored multi-tool patterns remain inclusive of their intended tool set. | Extract tool values and check anchoring: `[ -z "$(grep -E '^tool = ' plugins/vsdd-factory/hooks-registry.toml \| grep -vE 'tool = ["'"'"']\^' \| grep -vi '# *intent:')" ]` exits 0 (zero unanchored entries without intent comments → empty subshell output → condition true; any unanchored entry → non-empty output → exits 1). Positive fixture: entry `tool = "^(Edit\|Write)$"` → value starts with `^` → filtered out (passes, empty output). Negative fixture: entry `tool = "Edit\|Write"` (no leading `^`) → appears in subshell output → `[ -z ... ]` exits 1. Positive-scope verification: a MultiEdit fixture invocation confirms verify-factory-lock still fires (exit non-zero if verify-factory-lock does not trigger on MultiEdit after anchoring). | VP-099 |
| AC-005 | A bats lint test (`plugins/vsdd-factory/tests/registry-tool-filter-anchoring.bats`) verifies that every `tool =` line in `hooks-registry.toml` either (a) uses an anchored pattern beginning with `^` or (b) carries an `# intent:` inline comment. The test exits non-zero if any unanchored, uncommented `tool =` entry is found. Positive-scope verification: the test confirms that after anchoring, `verify-factory-lock`'s `^(Edit\|Write\|MultiEdit\|Agent)$` pattern is classified as anchored (starts with `^`) and passes the lint check. | `bats plugins/vsdd-factory/tests/registry-tool-filter-anchoring.bats` exits 0 after all registry entries are fixed. The test itself must exit non-zero on a fixture registry with an unanchored entry. The test must also confirm verify-factory-lock's pattern is correctly classified as anchored. | VP-099 |
| AC-006 | A Rust workspace test (`crates/factory-dispatcher/tests/bundle_orphan_check.rs`) determines WASM orphan status by querying BOTH `hooks-registry.toml` AND `resolvers-registry.toml`. The test: (a) enumerates `plugins/vsdd-factory/hook-plugins/*.wasm`; (b) parses both registry files for plugin path references; (c) asserts every bundled WASM is referenced by at least one registry (EAC-005: zero orphans); (d) on failure panics listing each orphan as `ORPHAN: <name>`. A WASM present under `hook-plugins/` but referenced only by `resolvers-registry.toml` is classified as non-orphan and must NOT be flagged. The test verifies against fixtures: (fixture-a) a fixture WASM referenced only by `resolvers-registry.toml` is classified non-orphan; (fixture-b) a fixture WASM referenced only by `hooks-registry.toml` is classified non-orphan; (fixture-c) a fixture WASM referenced by neither registry is classified as orphan and appears in the panic message as `ORPHAN: <name>`. **Negative-control clause (F-P2-010):** (fixture-d) when the detection logic is called with only hooks-registry data (resolvers-registry check omitted), a resolvers-only WASM IS classified as orphan — this confirms the dual-registry check is load-bearing, not advisory. No new `.sh` scripts: this Rust test replaces the previously planned `bundle-orphan-detection.bats` per human directive 2026-07-13 no-new-.sh standing policy. | `cargo test --test bundle_orphan_check` exits 0. The negative-control (fixture-d) sub-test must fail when the detection logic is called with only hooks-registry data — a resolvers-only WASM must be classified as orphan in that case (regression gate for v1.0 defect: false-positive orphan on `vsdd-context-resolvers.wasm`). Test must panic with `ORPHAN: <name>` for any unclassified WASM in fixture-c. | — |
| AC-007 | Integration test: the release.yml artifact-staging step, when run against a workspace fixture that includes `hello-hook.wasm`, `vsdd_context_resolvers.wasm`, and `wasm_resolver_export.wasm` in the hook-plugins directory, produces an artifact bundle containing zero WASMs unreferenced by BOTH registries (dual-registry-orphan count = 0). This wires EAC-005 as a load-bearing test: the assertion fails if any orphan WASM is present in the final bundle, NOT merely if the exclusion logic exists in release.yml. | Bats integration test (or equivalent CI-runnable test harness): stage a workspace fixture with the 3 known orphans + the live `vsdd-context-resolvers.wasm`; invoke the release.yml artifact-staging step; run orphan-detection script against the staged artifact directory; assert `ORPHAN_COUNT=0`; assert `test -f plugins/vsdd-factory/hook-plugins/vsdd-context-resolvers.wasm` exits 0 (keep-assertion (i): git-tracked file present and untouched); assert `grep -q "hook-plugins/vsdd-context-resolvers.wasm" plugins/vsdd-factory/resolvers-registry.toml` exits 0 (keep-assertion (ii): registry reference intact); exit non-zero if ORPHAN_COUNT > 0 or either keep-assertion fails. | EAC-005 (wires epic EAC-005 as load-bearing leg: zero dual-registry-orphan WASMs in rc.23 bundle) |

## Architecture Mapping

- `.github/workflows/release.yml` — REMOVE BOTH the `cargo build --example hello-hook` build step AND the `cp ...examples/hello-hook.wasm artifact/` copy step; MOVE `vsdd_context_resolvers.wasm|wasm_resolver_export.wasm` from the current pass-through arm (empty `;;` exits inner case without `continue`, causing fall-through to `cp "$wasm" artifact/`) into the skip path at both artifact-staging sites — canonical form: `vsdd_context_resolvers.wasm|wasm_resolver_export.wasm) echo "skip stale resolver artifact: $name"; continue ;;` (replacing the current pass-through arm `vsdd_context_resolvers.wasm|wasm_resolver_export.wasm) ;;`) — no exclusion of the pair currently exists; the fix creates one; add two-check keep-assertion: (i) `test -f plugins/vsdd-factory/hook-plugins/vsdd-context-resolvers.wasm` exits 0 (git-tracked; never produced by cargo; exit non-zero if absent); (ii) `grep -q "hook-plugins/vsdd-context-resolvers.wasm" plugins/vsdd-factory/resolvers-registry.toml` exits 0 (registry reference intact; exit non-zero if absent); register policy change via policies.yaml (POLICY 20 — release_bundle_no_dev_samples)
- `plugins/vsdd-factory/hooks-registry.toml` — add preamble comment block; update all `tool =` entries to use anchored patterns per the D-a table below; D-f singleton anchoring applies to all single-tool entries

**D-a per-entry anchoring table (architect decision — all current `tool =` values → correct anchored forms):**

| Current `tool` value | Plugins (representative) | Correct anchored form |
|---|---|---|
| `"Edit\|Write"` or `"Write\|Edit"` (order-normalized to Edit first); `"Edit\|Write\|MultiEdit"` form already correct — grouped here | convergence-tracker, purity-check, validate-anchor-capabilities-union, validate-bc-title, validate-changelog-monotonicity, validate-factory-path-root, validate-finding-format, validate-input-hash, validate-novelty-assessment, validate-story-bc-sync, validate-subsystem-names, validate-table-cell-count, validate-template-compliance, validate-vp-consistency, validate-wave-gate-completeness, validate-count-propagation, validate-red-ratio, brownfield-discipline, factory-branch-guard, protect-bc, protect-vp, red-gate, validate-artifact-path, validate-burst-log (entry 1), validate-index-cite-refresh, validate-state-structure, validate-dispatch-advance (entry 1), validate-closes-completeness, validate-policies-schema, validate-trajectory-tail-cell-completeness, validate-stable-anchors, lint-registry-async-invariant, validate-state-index-status-coherence, validate-state-pin-freshness, validate-state-size, validate-demo-evidence-story-scoped, validate-pr-description-completeness, validate-index-self-reference, validate-wave-handoff-completeness (was `"Write\|Edit"` — order-normalized), verify-state-timestamp-refresh (already `"Edit\|Write\|MultiEdit"` — grouped here) | `"^(Edit\|Write\|MultiEdit)$"` — **architect ruling F-1: `^(Edit\|Write)$` silently drops MultiEdit from block-mode guards (protect-bc, protect-vp, red-gate, factory-branch-guard, etc.); routing.rs `tool_matches` is regex SEARCH — unanchored `Edit\|Write` fires on MultiEdit via substring; MultiEdit is a first-class mutating vector per ADR-025 §Decision 12; MultiEdit MUST be included** |
| `"Bash"` | block-ai-attribution, check-factory-commit, destructive-command-guard, protect-secrets (bash entry), verify-git-push, validate-burst-log (entry 2), validate-dispatch-advance (entry 2), validate-heavy-op-delegation, verify-factory-lock-bash | `"^Bash$"` (D-f singleton) |
| `"Read"` | protect-secrets (read entry) | `"^Read$"` (D-f singleton) |
| `"Agent"` | track-agent-start, validate-pr-merge-prerequisites, validate-wave-gate-prerequisite | `"^Agent$"` (D-f singleton) |
| `"Edit\|Write\|MultiEdit\|Agent"` | verify-factory-lock | `"^(Edit\|Write\|MultiEdit\|Agent)$"` |

**Positive-scope verification note:** `verify-factory-lock` uses `^(Edit\|Write\|MultiEdit\|Agent)$` after anchoring. A MultiEdit fixture must confirm this hook still fires — the anchoring does NOT narrow the hook's tool scope, it only prevents false-fires on superstring tool names (AC-004 positive-scope gate). For former Edit\|Write guards (now corrected to `^(Edit\|Write\|MultiEdit)$`), MultiEdit is now IN SCOPE — protect-bc, protect-vp, red-gate, and factory-branch-guard all gain MultiEdit coverage that was silently missing under the `^(Edit\|Write)$` form.

**Architect ruling — NotebookEdit exclusion and PO routing flags:** NotebookEdit is excluded from the D-a table and from all block-mode guards by design — there are no `.ipynb` guarded artifacts in this factory; the exclusion is correct-by-design and does not represent a gap. Two PO routing flags are raised by this architect ruling: (a) BC-7.03.079 Invariant 1 tuple-scoping may need updating to reflect `^(Edit\|Write\|MultiEdit)$` as the canonical multi-tool form — PO amendment in flight, version TBD by PO; (b) BC-4.13.001 Invariant 5 snippet may reference the old `^(Edit\|Write)$` form — PO amendment in flight, version TBD by PO.

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | An existing hook intentionally uses an unanchored pattern (e.g., for future tool name extensions) | Must carry `# intent: unanchored — matches future Tool*` inline comment. Lint test accepts entries with `# intent:` comment. |
| EC-002 | A new hook is added without an anchored pattern | Lint test (AC-005) blocks the addition in CI. |
| EC-003 | A WASM file is present under `hook-plugins/` but referenced only by `resolvers-registry.toml`, not `hooks-registry.toml` | Orphan-status determination MUST check BOTH registries. A file referenced by either registry is non-orphan and must not be excluded from the bundle. `vsdd-context-resolvers.wasm` (hyphen) is the canonical example: it is the live WaveContextResolver registered in `resolvers-registry.toml` and must remain in `hook-plugins/`. Root cause of v1.0 story defect: checking only `hooks-registry.toml` produced a false-positive orphan classification for this file (corrected v1.1). |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| `.github/workflows/release.yml` | N/A (CI config) | YAML workflow definition; no code purity boundary |
| `plugins/vsdd-factory/hooks-registry.toml` | N/A (config) | TOML registry; no code purity boundary |
| `plugins/vsdd-factory/tests/registry-tool-filter-anchoring.bats` | effectful-shell | Runs `grep` (subprocess), reads registry file (file I/O) |
| `crates/factory-dispatcher/tests/bundle_orphan_check.rs` | effectful (test I/O) | Reads both registry files and fixture directories from disk; enumerates `hook-plugins/`; cargo integration test |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~5,500 |
| `plugins/vsdd-factory/hooks-registry.toml` | ~4,000 |
| `plugins/vsdd-factory/resolvers-registry.toml` | ~500 |
| `.github/workflows/release.yml` (bundle assembly section) | ~1,500 |
| Test fixtures (new Rust test + cargo test fixture files) | ~2,000 |
| Tool outputs overhead | ~1,500 |
| **Total** | **~15,000** |
| Agent context window | 200K (Sonnet) |
| **Budget usage** | **~7.5%** |

## Tasks (MANDATORY)

1. [ ] Capture `cargo test --workspace --all-targets` baseline pass count pre-implementation; emit machine-parseable marker line `pre-implementation cargo-test baseline: <N> pass` to stdout (replace `<N>` with actual count) and record in PR description (F-P2-012). This baseline-marker line is a required PR-description checklist item — the PR must not be merged until it appears in the description.
2. [ ] Write failing bats test: unanchored `tool =` entry → lint fails (AC-005 negative fixture) — test-writer
3. [ ] Write failing bats test: anchored entry → lint passes (AC-005 positive fixture) — test-writer
4. [ ] Write failing cargo test (T-006 rework): `resolvers-registry.toml`-only WASM fixture → non-orphan (AC-006 regression gate) in `crates/factory-dispatcher/tests/bundle_orphan_check.rs` — test-writer. **NOTE: T-006..T-008 previously committed as bats tests in .worktrees/S-19.04 calling bundle-orphan-check.sh must be REWORKED to cargo-test form per v1.12 no-new-.sh policy amendment. Existing bats tests are invalid; test-writer must rewrite them as Rust tests before Red Gate dispatch.**
5. [ ] Write failing cargo test (T-007 rework): neither-registry WASM fixture → orphan with `ORPHAN: <name>` panic (AC-006) in `crates/factory-dispatcher/tests/bundle_orphan_check.rs` — test-writer
6. [ ] Write failing cargo test (T-008 rework): negative-control (fixture-d) — detection logic called with only hooks-registry data classifies resolvers-only WASM as orphan (F-P2-010) — test-writer
7. [ ] Verify Red Gate: all tests fail with unmodified registry
8. [ ] Add preamble comment to `hooks-registry.toml`; anchor all unanchored `tool =` entries per D-a table (implementer)
9. [ ] Apply D-f singleton anchoring: `^Bash$`, `^Read$`, `^Agent$` for all single-tool entries (implementer)
10. [ ] Verify positive-scope: MultiEdit fixture confirms verify-factory-lock still fires after anchoring (AC-004/AC-005 positive-scope gate)
11. [ ] Add exclusion list + keep-assertion to `release.yml` bundle step; register policy change via policies.yaml POLICY 20 (release_bundle_no_dev_samples) (implementer)
12. [ ] Implement `bundle_orphan_check.rs` dual-registry orphan detection: all four fixture sub-tests pass (fixture-a: resolvers-only → non-orphan; fixture-b: hooks-only → non-orphan; fixture-c: neither-registry → orphan + panic `ORPHAN: <name>`; fixture-d: hooks-only detection data classifies resolvers-only as orphan — negative-control F-P2-010) (implementer)
13. [ ] Regenerate D-a anchoring table from current `hooks-registry.toml` before commit; assert distinct tool-value strings (`grep -E '^tool = ' plugins/vsdd-factory/hooks-registry.toml | sort -u | wc -l`) equals **5** (implementer-measured post-fix count: 5 distinct anchored forms — `^(Edit\|Write\|MultiEdit)$`, `^Bash$`, `^Read$`, `^Agent$`, `^(Edit\|Write\|MultiEdit\|Agent)$`); exit non-zero on drift (F-P3-017)
14. [ ] Run policy-registry validation over `policies.yaml`: assert POLICY 20 (release_bundle_no_dev_samples) is present in policies.yaml; exit non-zero if absent (O-P3-005)
15. [ ] Run AC-007 integration test: stage workspace fixture with 3 orphans + live vsdd-context-resolvers.wasm; invoke release.yml artifact-staging step; assert ORPHAN_COUNT=0; assert `test -f plugins/vsdd-factory/hook-plugins/vsdd-context-resolvers.wasm` exits 0 (keep (i): git-tracked source file untouched); assert `grep -q "hook-plugins/vsdd-context-resolvers.wasm" plugins/vsdd-factory/resolvers-registry.toml` exits 0 (keep (ii): registry reference intact) (F-P3-018)
16. [ ] Verify `bats registry-tool-filter-anchoring.bats` passes; verify AC-004 gate produces zero output; assert baseline preserved + new tests added (post-implementation F-P2-012)

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| v1.0 of this story (self) | Checked only `hooks-registry.toml`; missed `vsdd-context-resolvers.wasm` (hyphen) referenced in `resolvers-registry.toml` | Orphan detection MUST check BOTH registries | `vsdd_context_resolvers.wasm` (underscore) = stale; `vsdd-context-resolvers.wasm` (hyphen) = live WaveContextResolver — single character difference, different status |
| S-19.01 (same epic, wave 1) | CI job additions (`bats-darwin-leg-macos`) require modifying `.github/workflows/ci.yml` | New CI jobs are added to the existing `ci.yml` workflow | None affecting S-19.04 |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| Orphan detection checks BOTH `hooks-registry.toml` AND `resolvers-registry.toml` | EC-003 (this story v1.1 correction) | AC-006 regression gate (exits non-zero if `resolvers-registry.toml` check is skipped) |
| `vsdd-context-resolvers.wasm` (hyphen) MUST remain in bundle (live WaveContextResolver) | `resolvers-registry.toml` line; smoke-gate dispatcher log | AC-002 keep-assertion: `test -f hook-plugins/vsdd-context-resolvers.wasm` exits 0 |
| `tool =` entries use anchored `^(...)$` patterns or carry `# intent:` comment | D-a per-entry anchoring table (architect decision); this story AC-004 | AC-004 tool-value extraction gate; AC-005 bats lint test |
| D-f singleton anchoring: `^Bash$`, `^Read$`, `^Agent$` for single-tool entries | D-f (architect decision) | AC-004 gate; D-a table in Architecture Mapping |
| Bundle-policy changes registered via policies.yaml (POLICY 20 — release_bundle_no_dev_samples) | policies.yaml registration; POLICY 20 | Architecture Mapping cites POLICY 20 not ADR-030 |
| No `println!` in production Rust (applies to dispatcher source) | CLAUDE.md Conventions | N/A for this story (no Rust source changes) |
| No file:line volatile pins in spec artifacts | TD-VSDD-091 | Function/symbol anchors only; no line numbers in spec narrative, ACs, Architecture Mapping, or File Structure |
| No new `.sh` scripts (any scope, including CI/test helpers) | Human directive 2026-07-13 (no-new-.sh standing policy; platform-agnostic direction) | Orphan detection gate implemented as Rust cargo test (`crates/factory-dispatcher/tests/bundle_orphan_check.rs`) instead of any shell script; this supersedes the previously planned `bundle-orphan-check.sh` |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| Bats (Bash Automated Testing System) | per `plugins/vsdd-factory/tests/run-all.sh` | Integration + lint test runner |
| `grep` (POSIX) | system default | Tool-value extraction in AC-004 gate and bats tests |
| GitHub Actions | current | `release.yml` bundle assembly step |

## Red Gate Test Plan

| Test ID | Description | Expected Outcome |
|---------|-------------|------------------|
| RG-001 | Lint test against a fixture registry containing `tool = "Edit\|Write"` (unanchored, no intent comment) → exits non-zero | Exit 1 + diagnostic message |
| RG-002 | Lint test against a fixture with `tool = "^(Edit\|Write)$"` → exits 0 | Exit 0 |
| RG-003 | Lint test against a fixture with `tool = "Edit\|Write" # intent: unanchored by design` → exits 0 | Exit 0 |
| RG-004 | Lint test against the actual `hooks-registry.toml` after fixes → exits 0 | Exit 0 |
| RG-005 | Orphan check: resolvers-registry-only WASM with resolvers check omitted → orphan classified (negative-control) | Exit non-zero |

## Test Plan

| Test ID | Story AC | Test File | Description |
|---------|----------|-----------|-------------|
| T-001 | AC-004, AC-005 | plugins/vsdd-factory/tests/registry-tool-filter-anchoring.bats | Fixture unanchored entry → lint fails |
| T-002 | AC-004, AC-005 | plugins/vsdd-factory/tests/registry-tool-filter-anchoring.bats | Fixture anchored entry → lint passes |
| T-003 | AC-005 | plugins/vsdd-factory/tests/registry-tool-filter-anchoring.bats | Intent comment exemption → lint passes |
| T-004 | AC-004, AC-005 | plugins/vsdd-factory/tests/registry-tool-filter-anchoring.bats | Actual hooks-registry.toml post-fix → lint passes |
| T-005 | AC-004 | plugins/vsdd-factory/tests/registry-tool-filter-anchoring.bats | MultiEdit fixture: verify-factory-lock fires post-anchoring (positive-scope) |
| T-006 | AC-006 | crates/factory-dispatcher/tests/bundle_orphan_check.rs | resolvers-registry-only WASM fixture → non-orphan (regression gate); REWORK from bats to cargo test per v1.12 |
| T-007 | AC-006 | crates/factory-dispatcher/tests/bundle_orphan_check.rs | Neither-registry WASM fixture → orphan, panic includes `ORPHAN: <name>`; REWORK from bats to cargo test |
| T-008 | AC-006 | crates/factory-dispatcher/tests/bundle_orphan_check.rs | Negative-control (fixture-d): detection with only hooks-registry data classifies resolvers-only WASM as orphan (F-P2-010); REWORK from bats to cargo test |
| T-009 | AC-001, AC-002, AC-007 | crates/factory-dispatcher/tests/bundle_orphan_check.rs | Real-bundle gate: after WASM deletions and release.yml changes, `cargo test --test bundle_orphan_check` against the actual `plugins/vsdd-factory/hook-plugins/` directory confirms zero orphans in production tree (no fixture — exercises real source) |
| T-010 | AC-001, AC-007 | plugins/vsdd-factory/tests/registry-tool-filter-anchoring.bats (or equivalent CI harness) | Staging-simulation gate: run the release.yml artifact-staging step against a workspace fixture containing the 3 deleted WASM files as if they had not yet been removed; assert artifact/ directory contains zero files named `hello-hook.wasm`, `vsdd_context_resolvers.wasm`, or `wasm_resolver_export.wasm`; confirms exclusion logic is load-bearing, not advisory |

## File Structure Requirements (MANDATORY)

| File | Action | Notes |
|------|--------|-------|
| `.github/workflows/release.yml` | modify | Remove hello-hook build step (cargo build --example hello-hook) + copy step (cp ...examples/hello-hook.wasm artifact/); move vsdd_context_resolvers.wasm\|wasm_resolver_export.wasm from pass-through arm into skip path at the inner case-arm `vsdd_context_resolvers.wasm\|wasm_resolver_export.wasm) ;;` at both artifact-staging sites; add two-check keep-assertion: (i) `test -f plugins/vsdd-factory/hook-plugins/vsdd-context-resolvers.wasm` exits 0 (git-tracked; never cargo-produced; exit non-zero if absent); (ii) `grep -q "hook-plugins/vsdd-context-resolvers.wasm" plugins/vsdd-factory/resolvers-registry.toml` exits 0 (registry reference intact; exit non-zero if absent); register via policies.yaml POLICY 20 (release_bundle_no_dev_samples) |
| `plugins/vsdd-factory/hooks-registry.toml` | modify | Add preamble comment; anchor all unanchored tool= entries per D-a table; D-f singleton patterns for single-tool entries |
| `plugins/vsdd-factory/tests/registry-tool-filter-anchoring.bats` | create | New lint test: AC-005 (tool-filter anchoring) + T-005 positive-scope MultiEdit verification |
| `crates/factory-dispatcher/tests/bundle_orphan_check.rs` | create | Rust cargo test: AC-006 dual-registry orphan detection (fixture-a..fixture-d including negative-control clause (d) F-P2-010); EAC-005 load-bearing leg; replaces previously planned `bundle-orphan-detection.bats` per no-new-.sh policy |
| `crates/factory-dispatcher/tests/fixtures/bundle-orphan/` | create | Test fixture directory: resolver-only WASM fixture, hooks-only fixture, neither-registry fixture, negative-control mock registry files for T-006..T-008 |
| `plugins/vsdd-factory/tests/fixtures/registry-tool-filter/` | create | Fixture registries for RG-001..RG-003 + T-008 negative-control (registry-tool-filter lint tests; unchanged) |
| `plugins/vsdd-factory/hook-plugins/hello-hook.wasm` | delete | Orphan WASM — dev sample with no production value; never referenced by hooks-registry.toml or resolvers-registry.toml; actively built and copied by release.yml (both steps removed in release.yml modify above) |
| `plugins/vsdd-factory/hook-plugins/vsdd_context_resolvers.wasm` | delete | Orphan WASM (underscore variant) — stale resolver artifact; unreferenced by either registry; distinct from `vsdd-context-resolvers.wasm` (hyphen) which is the live WaveContextResolver and MUST NOT be deleted |
| `plugins/vsdd-factory/hook-plugins/wasm_resolver_export.wasm` | delete | Orphan WASM — stale export artifact; unreferenced by either registry; no production role |

## Changelog

| Version | Date | Author | Change |
|---------|------|--------|--------|
| 1.14 | 2026-07-13 | story-writer | Architect binding D-a table correction (F-1): D-a rows 1+2+7 merged — all Edit\|Write and Write\|Edit hooks now use `^(Edit\|Write\|MultiEdit)$` (was `^(Edit\|Write)$`; blanket form silently dropped MultiEdit from block-mode guards); verify-state-timestamp-refresh (already correct form) grouped into merged row; D-a row count 7→5 (5 distinct anchored forms); architect-ruling note added (NotebookEdit exclusion correct-by-design; PO routings flagged for BC-7.03.079 + BC-4.13.001); Task 13 count updated to explicit 5; Finding (b) example form updated; T-009 + T-010 added to Test Plan; WASM deletion rows added to File Structure; POLICY 14 parity. |
| 1.13 | 2026-07-13 | story-writer | Partial-sweep escape fix: Purity Classification stale `plugins/vsdd-factory/tests/bundle-orphan-detection.bats` row removed (v1.12 added `crates/factory-dispatcher/tests/bundle_orphan_check.rs` row but left bats row; now removed); Task 12 rewritten from `bundle-orphan-detection.bats` reference to `bundle_orphan_check.rs` with all four fixture sub-tests enumerated; Changelog section added; input-hash refreshed. |
| 1.12 | 2026-07-13 | story-writer | No-new-.sh policy amendment (human directive 2026-07-13): AC-006 orphan-detection gate converted from bats/bundle-orphan-check.sh to Rust cargo test (`crates/factory-dispatcher/tests/bundle_orphan_check.rs`); File Structure bats row removed + Rust test + fixtures rows added; Tasks 4/5/6 (T-006..T-008) rework noted; Architecture Compliance no-new-.sh row added; Purity Classification updated (partial — bats row not removed; corrected in v1.13); Token Budget test-fixtures row updated; Test Plan T-006..T-008 test file updated. |
