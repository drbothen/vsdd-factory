---
document_type: story
level: implementation
story_id: "S-21.22"
epic_id: "E-21"
version: "1.5"
status: draft
producer: story-writer
timestamp: 2026-08-20T00:00:00Z
phase: brownfield-backfill
inputs:
  - .factory/specs/architecture/decisions/ADR-039-validator-failure-policy-resource-exhaustion-fail-closed.md
  - .factory/specs/architecture/decisions/ADR-042-validate-cross-site-correspondence-fuel-budget-raise-and-loud-exhaustion-signaling.md
  - .factory/specs/architecture/decisions/ADR-044-split-topology-enforcement-flip-capstone-ownership.md
  - .factory/research/wasm-fuel-exhaustion-detection.md
  - plugins/vsdd-factory/hooks-registry.toml
input-hash: "3eba350"
traces_to: .factory/specs/architecture/decisions/ADR-039-validator-failure-policy-resource-exhaustion-fail-closed.md
points: "4"
depends_on: [S-21.10, S-21.19]
blocks: [S-21.24, S-21.13]
behavioral_contracts: ["BC-1.03.017 v1.23"]
verification_properties: []
priority: "P1"
cycle: v1.0-brownfield-backfill
wave: 7
target_module: crates/factory-dispatcher # F-S2122-P2-004: names the primary-concern module (the plugin whose fuel behavior this story calibrates); the files this story actually modifies are the SS-07 registry (plugins/vsdd-factory/hooks-registry.toml) plus the new SS-01 test/fixture files added by Task 5a — see File Structure Requirements
subsystems: [SS-01, SS-07]
estimated_days: null
assumption_validations: []
risk_mitigations: []
tdd_mode: strict
modified:
  - "2026-08-23 (v1.5) — Wave-7 pass-4/R3 story-layer remediation (story-writer). **F-S2122-P4-001 (LOW):** `ceil()` alignment confirmed — AC-007 already correct. **F-S2122-P4-002 (LOW):** ADR-044 v1.1 stale cites → v1.3 (Context line re: `plugin_fail_closed_on_exhaustion`, PSI S-21.19 row); `BC-1.03.017 v1.22 → v1.23` re-anchor (POLICY 8 full propagation — frontmatter, BC table version, AC-007/AC-008 headers and body cites, Token Budget, Architecture Compliance Rules). Mechanically inert for this story (v1.23 STOP-gate formula does not touch native-WASM calibration scope). Does NOT touch BC files, INDEX/STATE.md, or input-hash (state-manager step ③)."
  - "2026-08-23 (v1.4) — Wave-7 pass-3/R2 story-layer remediation (D-1074; story-writer). RE-ANCHOR
    ONLY. `BC-1.03.017 v1.21 -> v1.22` re-anchored with full POLICY 8 propagation: frontmatter
    `behavioral_contracts`, body BC table (version cite updated; title extended to include `,
    observed_max×1.5 fuel_cap calibration-sufficiency gate against a FROZEN corpus snapshot, and
    wave-7→8 migration coverage-continuity invariant`), AC-007/AC-008 headers and bodies, Token
    Budget, Architecture Compliance Rules. No AC text, BC trace, test predicate, or Task 5a
    mechanism changed — F-S2122-P3-001 (MEDIUM) was a BC-side divergence resolved in BC v1.22
    (Precondition 6 split: one-time live-corpus confirmation vs. durable frozen-snapshot standing
    gate); the story body was already correct. points/depends_on/blocks/wave UNCHANGED (4 pts,
    wave 7). Does NOT touch S-21.19/S-21.20/S-21.21/S-21.23/S-21.24, BC-1.03.017/ADR-044
    themselves (already landed), or any INDEX/STATE.md (state-manager's domain)."
  - "2026-08-22 (v1.3) — Wave-7 pass-2 remediation (D-1071; architect adjudication, RATIFIED
    2026-08-22). **F-S2122-P2-001 (HIGH) — durable CI-resident PC6 sufficiency gate:** the
    Precondition 6 sufficiency assertion previously existed only as a one-time devops-run
    check (Task 4) and a PR-description note — PC6 explicitly requires a machine-checkable
    REGRESSION assertion, not a one-time pass. Added new **Task 5a**: freeze the calibration
    corpus into a committed snapshot fixture
    (`plugins/vsdd-factory/tests/fixtures/validate-cross-site-correspondence/pc6-sufficiency-snapshot/`)
    — explicitly NOT the live-growing `lessons.md`/`STATE.md`/`decision-log.md` files Task 2's
    calibration corpus uses, because a standing CI assertion against those would be a
    false-fail time-bomb as they grow independently of this plugin's fuel behavior — and
    author a new STANDING Cargo integration test,
    `test_validate_cross_site_correspondence_fuel_cap_sufficiency()`, that re-runs the plugin
    against the FROZEN snapshot on every CI invocation and asserts `fuel_consumed × 1.5 <=
    registry fuel_cap`. AC-007's Test bullet, the Architecture Compliance Rules row, and File
    Structure Requirements updated to cite this durable gate as the standing evidence (Task 4's
    one-time re-run is now explicitly scoped as the INITIAL confirmation only, not the durable
    gate). **F-S2122-P2-002 (MEDIUM):** the `PRACTICAL_FUEL_CEILING` descope trigger corrected
    from `>`/\"exceed\" to `>=`/\"at or above\" at all 5 sites (Context/EC-004 escape valve,
    AC-007 body, EC-004 table, Task 4, Compliance Rules) to match BC Precondition 6's own
    `>=` wording and ADR-039 §Decision 4; pinned the rounding of `observed_max × 1.5` to
    `ceil()` (round UP to the nearest whole fuel unit) so the boundary case is deterministic —
    a plugin whose computed `fuel_cap` lands exactly at `PRACTICAL_FUEL_CEILING` after rounding
    now unambiguously triggers EC-004 (`>=`), not a coin-flip depending on truncation vs.
    rounding. **F-S2122-P2-003 (MEDIUM):** Task 6's stale-comment sweep instruction expanded —
    the same comment block ALSO carries a superseded `p99×1.5` formula reference (pre-dating
    the ADR-039 §Decision 4 v1.16/§Erratum E-007 target-statistic correction to
    `observed_max×1.5`); Task 6 now corrects BOTH the \"has not shipped\" clause AND the
    `p99×1.5`→`observed_max×1.5` formula reference in the same edit. **LOWs:** F-P2-004 —
    added a clarifying inline comment on `target_module:` naming it as the primary-concern
    module, since the files this story actually modifies are the SS-07 registry plus the new
    SS-01 test/fixture files. F-P2-005 — the S-21.13 `depends_on` redirect was described as
    merely \"recommended\" in three live-body sites and the Architecture Compliance Rules row;
    D-1070 (state-manager, prior burst) already executed this redirect — updated all four sites
    to state the redirect is DONE (S-21.13's `depends_on` already reads
    `[S-21.10, S-21.22]`), not pending. Added one sentence to Task 4: an unmeasurable
    calibration (e.g. `VSDD_SINK_FILE` capture fails to produce a `fuel_consumed` reading for
    any corpus input) MUST block the flip and surface to the orchestrator — it MUST NOT fall
    through to annotating the 50M floor as if calibration had succeeded. `BC-1.03.017 v1.20 ->
    v1.21` re-anchored with full POLICY 8 propagation: frontmatter, BC table, AC-007/AC-008
    headers and bodies, Token Budget. `points:`/`depends_on:`/`blocks:`/wave UNCHANGED (4 pts,
    wave 7). Does NOT touch S-21.19/S-21.20/S-21.21/S-21.23/S-21.24 (concurrent 3b sub-burst
    owns S-21.20/23/24), BC-1.03.017/ADR-044 themselves (already landed at commit 8ef46b8a), or
    any INDEX/STATE.md (state-manager's domain)."
  - "2026-08-22 (v1.2) — Wave-7 pass-1 remediation (D-1069; architect adjudication, RATIFIED
    2026-08-22). **F-S2122-P1-001 (HIGH):** added an explicit caveat to the Narrative and
    Context/Finding Summary sections stating that the `hooks-registry.toml` annotation this
    story lands (Task 6's flip) is BEHAVIORALLY INERT — produces no different observable
    dispatch outcome from the pre-annotation state — until S-21.24's Task 0 wires
    `plugin_fail_closed_on_exhaustion` (the exhaustion-leg function this plugin's `Timeout`
    outcomes are governed by; ADR-044 v1.1 Addendum's function split) into the real
    `execute_tiers`/`execute_tier` block-decision chain; CWE-636 closure for the native-WASM
    tier COMPLETES at S-21.24's merge, not at this story's merge (aligns to BC-1.03.017 v1.20
    Invariant 3 / Invariant 7). The pre-existing calibration-methodology-independence claim
    (native WASM needs no `timeout_ms` axis, no AMD-002-class wiring gap) is PRESERVED
    unchanged and correct — only the false ENFORCEMENT-independence claim (that this story's
    flip alone makes real dispatches block) is corrected. **F-S2122-P1-002 (HIGH):** AC-007
    strengthened to require the calibration log record `observed_max` PER CORPUS INPUT (not
    merely an aggregate figure), plus the new BC-1.03.017 v1.20 Precondition 6 machine-checkable
    sufficiency regression assertion (re-run against the mandated corpus, assert
    `fuel_consumed × 1.5 <= registry fuel_cap`) — not merely the 50M floor. **F-S2122-P1-003
    (MEDIUM):** the `PRACTICAL_FUEL_CEILING (500,000,000)` citation corrected to
    now-authoritative ADR-039 v1.16 §Decision 4 (where it is formally defined; the prior
    citation pointed to §Negative/Trade-offs, deprecated per ADR-039's own v1.16 erratum note).
    **F-S2122-P1-005 (MEDIUM):** Task 3's calibration harness specified explicitly — a
    temporary registry `fuel_cap` override to `u64::MAX/2` for the calibration run, plus
    `VSDD_SINK_FILE` capture of `plugin.completed.fuel_consumed` JSONL events as the
    machine-readable evidence source (per CLAUDE.md's VSDD_SINK_FILE diagnostic capture
    convention). `BC-1.03.017 v1.19 -> v1.20` re-anchored with full POLICY 8 propagation:
    frontmatter `behavioral_contracts`, body BC table, AC-007/AC-008 headers and bodies, Token
    Budget. `inputs:` adds ADR-044. `points:`/`depends_on:`/`blocks:`/wave UNCHANGED (4 pts,
    wave 7). Does NOT touch S-21.19/S-21.20/S-21.21/S-21.23/S-21.24, BC-1.03.017/ADR-044
    themselves (already landed at commit a3bfa1af), or any INDEX/STATE.md (state-manager's
    domain)."
  - "2026-08-21 (v1.1) — Wave-7 pre-cascade re-anchor burst (D-1060 deferral, EXTENDED D-1064; story-writer): BC-1.03.017 v1.18 -> v1.19 re-anchor (S-21.19's F-S2119-P2-001 pass-2 remediation moved Invariant 7's CWE-636 regression trigger from function-authoring to executor-wiring, keyed on the same PC11 static-scan signal; this story's AC-007/AC-008 narrative is unaffected in substance -- version string only). Frontmatter `behavioral_contracts` and all body cites (AC-007/AC-008 headers, BC table, Token Budget) swept to v1.19. No AC text, BC trace, or test predicate changed. Does NOT touch S-21.19/S-21.20/S-21.21/S-21.23/S-21.24, BC-1.03.017/BC-1.03.018 themselves, or any INDEX/STATE.md (state-manager's domain)."
  - "2026-08-20 (v1.0) — created by story-writer via the operator-directed split of CONVERGED S-21.11 (v2.11, 16-pass 3-CLEAN BC-5.39.001 convergence) per `.factory/planning/S-21.11-decomposition-plan.md` §1 (S-21.22 detail) and §3 (AC partition table). Owns AC-007 (native-WASM leg only) and AC-008 (owned in full), copied verbatim from S-21.11 v2.11 with zero weakening. Tasks renumbered #1–#6 (self-contained) from S-21.11 v2.11 Tasks #1, #2, #3, #4, #5, #26 — this seam owns and EXECUTES the native-WASM flip itself (Task #26), per the plan's §6 task-ownership resolution, not the capstone. `blocks:` includes S-21.13 per the plan's §4 recommended dependency redirect (S-21.13's own `depends_on` should be updated from `[S-21.10, S-21.11]` to `[S-21.10, S-21.22]` by state-manager as a sibling-sweep)."
---

> **tdd_mode: strict** — Full TDD Iron Law enforced: all non-trivial function bodies use `todo!()`;
> Red Gate density check ≥0.5 required before Step 4 dispatch.

> **Execute:** `/vsdd-factory:deliver-story S-21.22`

> **Split origin (2026-08-20).** One of six sub-stories partitioning CONVERGED S-21.11 (v2.11,
> 16-pass 3-CLEAN BC-5.39.001 convergence) per the operator-directed override of S-21.11's own
> prior "unified scope, no split" decision, documented in
> `.factory/planning/S-21.11-decomposition-plan.md`. S-21.22 is fully independent of the
> bash-adapter wiring/calibration seam (S-21.21) and the break-glass seam (S-21.23) — native
> WASM fuel accounting sees the whole execution, with no `legacy-bash-adapter` involvement.
> This seam OWNS and EXECUTES its own flip commit (unlike the other three bash-adapter/Agent-gate
> plugins, whose flips are deferred to the capstone S-21.24) because it has no cross-seam
> ordering dependency beyond S-21.19. S-21.11's v2.11 body is frozen as the historical
> source-of-truth this story's ACs are extracted from verbatim; no AC text, BC trace, or test
> predicate was weakened, dropped, or reinterpreted during partitioning.

# S-21.22: Native-WASM fuel-axis calibration and fail-closed flip — `validate-cross-site-correspondence` (ADR-039 Phase 3a+4d; split seam 4 of 6 from S-21.11)

## Narrative

- **As a** factory pipeline operator whose `validate-cross-site-correspondence` plugin
  currently fails open on fuel exhaustion, silently approving cross-site-correspondence
  violations on large inputs
- **I want** `validate-cross-site-correspondence`'s `fuel_cap` calibrated against a
  production-scale corpus and annotated `failure_policy = "fail-closed"` in the same commit
  as its calibrated `fuel_cap`
- **So that** this plugin closes CWE-636 for the native-WASM tier without waiting on the
  bash-adapter wiring fix, per-plugin calibration, or the break-glass mechanism — none of
  which this plugin's enforcement depends on

**Caveat (F-S2122-P1-001, HIGH; BC-1.03.017 v1.23 Invariant 3/Invariant 7):** the
`hooks-registry.toml` annotation this story lands (Task 6's flip) is BEHAVIORALLY INERT on
its own — it produces no different observable dispatch outcome from the pre-annotation state
until S-21.24's Task 0 wires `plugin_fail_closed_on_exhaustion` (the exhaustion-leg function
this plugin's `Timeout` outcomes are governed by, per ADR-044 v1.3)
into the real `execute_tiers`/`execute_tier` block-decision chain. **CWE-636 closure for the
native-WASM tier COMPLETES at S-21.24's merge, not at this story's merge** — Invariant 3
states closure holds "once Phase 4 is complete," and Phase 4 is not complete until enforcement
is genuinely active. This story's own calibration-methodology-independence claim below (no
`timeout_ms` axis, no AMD-002-class wiring gap) remains correct and unaffected — only the
ENFORCEMENT timing is clarified: landing early and safely is this seam's genuine advantage
(annotating a `Timeout`-governed plugin before wiring exists is inert, not unsafe), but it is
NOT equivalent to real, live enforcement until S-21.24 wires the exhaustion leg.

## Context / Finding Summary

**Native-WASM (fuel-axis calibration sufficient):** unlike the five
`legacy-bash-adapter.wasm`-hosted plugins, `validate-cross-site-correspondence` runs as a
native WASM guest — wasmtime's fuel counter sees the whole execution directly, with no host
subprocess in between. There is therefore no AMD-002-class wiring gap for this plugin: fuel
calibration alone (no `timeout_ms` axis, no wall-clock host-kill deadline) is a sufficient
CALIBRATION METHODOLOGY for this plugin. This is what makes this seam fully independent of S-21.21's
bash-adapter wiring/calibration work and S-21.23's break-glass mechanism — the only genuine
CALIBRATION dependency is S-21.19's decision-function authoring (Task 5's
`plugin_fail_closed_on_exhaustion`, unit-tested but unwired within S-21.19), which every one of
the six targeted plugins' enforcement rests on. **This methodology-independence claim is
distinct from ENFORCEMENT-timing (F-S2122-P1-001, HIGH):** this plugin's `Timeout` outcomes are
governed exclusively by `plugin_fail_closed_on_exhaustion`, whose LIVE WIRING remains
capstone-owned (S-21.24's Task 0, wave 8, per ADR-044 v1.3, unchanged from v1.0) —
this story's own flip (Task 6) lands the registry annotation early and safely (inert, not
unsafe, prior to wiring) but does not itself make any real dispatch block; see the Narrative
section's caveat above.

**Task-ownership resolution (per `.factory/planning/S-21.11-decomposition-plan.md` §6):** the
operator-approved baseline described this seam as owning "Tasks #1–5, #26" and the capstone as
owning "Tasks #26–32" — an apparent overlap. The plan resolves this: Task #26 (the flip itself)
depends only on this seam's own calibration (Node A) and S-21.19's decision-function extension
(Node D) landing — it has no dependency on S-21.21 (Node B/C) or S-21.23 (Node E), so there is
no structural reason to defer it to the capstone. Landing it here lets this plugin's flip
proceed as soon as S-21.19 merges, rather than waiting on the five-story capstone convergence
gate.

**EC-004 escape valve:** if calibration reveals this plugin's required `fuel_cap` would be AT
OR ABOVE (`>=`, per F-S2122-P2-002 — not merely "exceed"/`>`) `PRACTICAL_FUEL_CEILING`
(500,000,000; formally defined in ADR-039 §Decision 4 v1.16 per F-S2122-P1-003), this story's
flip is descoped and the plugin routes to S-21.13 instead (see AC-007 below and the
`depends_on` redirect in this story's `blocks:` frontmatter — ALREADY EXECUTED via D-1070,
state-manager, not merely recommended).

## Position in the Split DAG

```
S-21.10 (merged) ──┐
                    ├──▶ S-21.22 (this story: calibrate + flip) ──┬──▶ S-21.24 (capstone:
S-21.19 (merged) ──┘                                              │    AC-009 final-state
                                                                   │    assertion needs this
                                                                   │    seam's flip landed)
                                                                   └──▶ S-21.13 (depends_on
                                                                        redirect ALREADY
                                                                        EXECUTED via D-1070;
                                                                        activates if EC-004
                                                                        fires; parallel to
                                                                        S-21.24, not gated by it)
```

The full six-seam dependency graph and wave schedule live in
`.factory/planning/S-21.11-decomposition-plan.md` §2.

## Acceptance Criteria

### AC-007 (native-WASM leg): Per-plugin calibration against production-scale corpus — `fuel_cap` for native-WASM (traces to BC-1.03.017 v1.23 Preconditions 2–6, enabling PC9; ADR-039 §Decision 4 Option A + v1.16/§Erratum E-007 target-statistic correction)

**Leg-scope note (per `.factory/planning/S-21.11-decomposition-plan.md` §3):** AC-007 has two
legs. This story owns the native-WASM leg (`validate-cross-site-correspondence`) — the
`legacy-bash-adapter.wasm`-hosted leg (the five bash-adapter plugins) is owned by **S-21.21**.

For `validate-cross-site-correspondence` (native-WASM), calibration is bifurcated by adapter
class (ADR-039 §Decision 1/2/3/4 v1.8 amendment, §AMD-001): `fuel_cap` MUST be set to at least
`max(ceil(observed_max × 1.5), 50_000_000)` where `observed_max` is the maximum `fuel_consumed`
value observed over the calibration corpus below, and `ceil()` rounds UP to the nearest whole
fuel unit (F-S2122-P2-002: rounding is pinned explicitly — `fuel_consumed` is an integer count
of WASM instructions, so `observed_max × 1.5` may be fractional; truncating instead of
rounding up would under-calibrate by up to 1 fuel unit and make the `PRACTICAL_FUEL_CEILING`
boundary case non-deterministic across implementations). For the hard-block/fail-closed boundary
validators in this story, the appropriate percentile basis is the **observed maximum over
the mandated corpus** — with ~4 corpus inputs, "p99" and "observed max" are effectively
identical. `fuel_consumed` MUST be read from the `Ok`-path measurement ONLY: on a
`Trap::OutOfFuel` outcome, `fuel_consumed == fuel_cap` (conveys the cap, not actual
demand), so a `Timeout`-path reading is invalid for calibration purposes. This element has
no dependency on S-21.21's AMD-002 wiring fix.

The calibration corpus MUST include:

1. `lessons.md` at ≥4000 lines (at the D-442(e) hard limit; soft limit is 3500, hard is 4000)
2. `STATE.md` at current live size
3. `decision-log.md` at current live size
4. The ≥574 KB synthetic fixture committed at `plugins/vsdd-factory/tests/fixtures/validate-cross-site-correspondence/a1-production-scale/factory/specs/behavioral-contracts/BC-INDEX.md` (576,396 bytes; `BC-INDEX.md` is the calibration input file — the tree also holds `ss-05/BC-5.39.010.md`; added in S-21.07 merge e94767bc)

A calibration run MUST be executed by the devops-engineer role before the implementer
annotates hooks-registry.toml. **(F-S2122-P1-002, HIGH, strengthened this burst):** the
calibration log MUST record `observed_max` (the measured `fuel_consumed` value) PER CORPUS
INPUT individually (`lessons.md`, `STATE.md`, `decision-log.md`, and the ≥574 KB fixture each
get their own recorded `fuel_consumed` reading) — not merely an aggregate/summary figure — so
that a future re-calibration can identify which specific input drives the calibrated `fuel_cap`.
The chosen `fuel_cap` MUST be recorded in the PR description or a calibration log artifact, not
merely asserted in code. **Additionally, per BC-1.03.017 v1.23 Precondition 6 (Q2/F-S2122-P1-002
machine-checkable sufficiency gate; DURABLE CI-resident enforcement added F-S2122-P2-001):**
this story MUST deliver a STANDING Cargo regression test — not merely a one-time devops-run
check — that re-runs the plugin against a FROZEN corpus snapshot (Task 5a) and asserts
`fuel_consumed × 1.5 <= registry fuel_cap` on every CI invocation. A PASSING standing
regression assertion, not merely a documented `fuel_cap` value or a one-time PR-description
note, is the calibration-sufficiency evidence this AC requires; the 50,000,000 floor (PC8's
structural gate) is a necessary schema-level minimum but is NOT independently sufficient
evidence of calibration. Task 4's own one-time re-run (against the LIVE corpus, at calibration
time) remains the INITIAL confirmation that informs the chosen `fuel_cap`; it is not a
substitute for the durable, frozen-corpus standing gate.

**PRACTICAL_FUEL_CEILING escape valve (EC-004):** if calibration reveals a required `fuel_cap`
that would be AT OR ABOVE (`>=`, F-S2122-P2-002 — not merely "exceed"/`>`) `PRACTICAL_FUEL_CEILING`
(`PRACTICAL_FUEL_CEILING = 500_000_000`, formally defined in **ADR-039 §Decision 4 v1.16** — the
now-authoritative citation, per F-S2122-P1-003; downstream citations MUST cite §Decision 4, not
§Negative/Trade-offs, as its defining source — even at `ceil(observed_max × 1.5)` for the
largest corpus input), this MUST be surfaced to the orchestrator rather than silently annotated
with an insufficient cap. Because `validate-cross-site-correspondence` has `on_error="continue"`,
EC-004 Case A is a VALID descope path for this specific plugin (it is FORBIDDEN for the five
`on_error="block"` plugins owned by S-21.21): if EC-004 fires, this story's own flip
(AC-008/Task 6 below) is descoped, and this plugin routes to **S-21.13** (depends_on
`[S-21.10, S-21.22]` — this redirect is ALREADY EXECUTED via D-1070, state-manager, not merely
recommended) instead of being annotated here. S-21.13 (or a named successor) MUST then annotate
`validate-cross-site-correspondence` with `failure_policy = "fail-closed"` and a calibrated
`fuel_cap >= 50_000_000` once its O(n) fuel-ceiling algorithmic fix removes the excessive cap
requirement — a timing deferral only, not a permanent exemption. If EC-004 fires, S-21.19's
AC-009 asserted set correspondingly reduces to the five flippable plugins (per AC-009's own
EC-004 conditional, owned by S-21.19).

- **Test:** the new standing `test_validate_cross_site_correspondence_fuel_cap_sufficiency()`
  (Task 5a, F-S2122-P2-001) is the durable, CI-resident evidence for Precondition 6's
  sufficiency requirement — re-runs against a FROZEN corpus snapshot on every CI invocation and
  asserts `fuel_consumed × 1.5 <= registry fuel_cap`; S-21.19's AC-001 gate
  (`test_no_fail_closed_plugin_with_uncalibrated_cap`) separately enforces only the 50M floor
  structural invariant once this story's Task 6 flip lands — the two gates are complementary,
  neither substitutes for the other

### AC-008: Each targeted validator's `failure_policy` and calibrated field(s) annotated atomically (traces to BC-1.03.017 v1.23 Invariant 2, enforced via PC8 gate; ADR-039 §Decision 3 per-plugin atomic commit)

For each of the six targeted validator-class plugins, the commit that adds
`failure_policy = "fail-closed"` to its `plugins/vsdd-factory/hooks-registry.toml` entry
MUST be the same commit that sets the calibrated field(s) from AC-007 — `fuel_cap` for
`validate-cross-site-correspondence`; BOTH `fuel_cap` AND `timeout_ms` for the five
`legacy-bash-adapter.wasm`-hosted plugins — replacing the factory defaults. No plugin MAY
carry `failure_policy = "fail-closed"` with an uncalibrated `fuel_cap < 50_000_000` (or, for
`legacy-bash-adapter.wasm`-hosted entries, `timeout_ms < 30_000`) at any CI-passing commit.
The 50_000_000 / 30_000 floors are the calibration minima (inclusive) per ADR-039 §Decision 4;
exactly the floor values are valid calibrated values.

This constraint is enforced structurally by S-21.19's AC-001 gate test (the test would fail on
any half-state commit), and by the PR review gate — the reviewer MUST verify that no
intermediate commits in the PR carry the annotated-but-uncalibrated state on either axis.

**This story is where the atomicity-annotation discipline is FIRST demonstrated** among the
six seams (its own flip, Task 6 below, is the first flip commit to land chronologically since
it has no cross-seam ordering dependency beyond S-21.19). S-21.24's own flip commits (the
remaining five plugins) are equally bound by this AC's discipline but do not re-author or
re-own it — this AC is owned in full by this story.

- **Gate:** S-21.19's AC-001 test plus PR-level review of commit history

## Behavioral Contracts

| BC ID | Version | Title | Clause |
|-------|---------|-------|--------|
| BC-1.03.017 | v1.23 | factory-dispatcher::executor::failure_policy enforcement — exhaustion-outcome dispatch (fail-closed→block; fail-open→advisory), on_error axes independence, crash-versus-exhaustion distinct paths, Phase-3-before-Phase-4 structural half-state gate, migration-window on_error=block completeness gate, legacy-bash-adapter runtime-timeout-wiring verification with CI-jitter-robust kill-time margin, observed_max×1.5 fuel_cap calibration-sufficiency gate against a FROZEN corpus snapshot, and wave-7→8 migration coverage-continuity invariant | Precondition 2–6 (AC-007 native-WASM leg, including Precondition 6's durable sufficiency assertion, Task 5a), Invariant 2 / PC8 (AC-008, atomic-commit discipline) |
<!-- POLICY 7: BC-table Title cell MUST remain a verbatim SUBSET of the corresponding BC's H1. -->

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| Native-WASM calibration corpus measurement | devops-engineer tooling (external to crate) | Effectful-shell (runs `validate-cross-site-correspondence` against corpus inputs; reads `fuel_consumed`) |
| `hooks-registry.toml` annotation (`validate-cross-site-correspondence` entry) | `plugins/vsdd-factory/hooks-registry.toml` | Config (calibrated `fuel_cap` + `failure_policy = "fail-closed"`, one entry) |
| **NEW (Task 5a, F-S2122-P2-001):** `test_validate_cross_site_correspondence_fuel_cap_sufficiency` standing regression test | `crates/factory-dispatcher/tests/` (new or existing integration-test file) | Effectful-integration (drives the real dispatcher/plugin path against a frozen fixture; reads the live `hooks-registry.toml` `fuel_cap`) |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|----------------|
| Calibration corpus runner | Effectful-shell | Invokes the plugin against filesystem inputs; reads fuel metrics from dispatcher |
| `test_validate_cross_site_correspondence_fuel_cap_sufficiency` (Task 5a) | Effectful-integration | Drives the real dispatcher/plugin path against a frozen fixture; not a pure-core unit test — reads registry state and asserts an inequality over its output |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-004 | Calibration reveals `validate-cross-site-correspondence` needs `fuel_cap >= PRACTICAL_FUEL_CEILING` (`PRACTICAL_FUEL_CEILING = 500_000_000`, ADR-039 §Decision 4 v1.16; F-S2122-P2-002: `>=`, not `>` — matches BC Precondition 6's own wording) even at `ceil(observed_max × 1.5)` for the corpus | Deferral to S-21.13 is a VALID descope path for this plugin (behavior-neutral, `on_error="continue"`). MUST surface to orchestrator; this story's flip (Task 6) is descoped; `validate-cross-site-correspondence` routes to **S-21.13** (depends_on `[S-21.10, S-21.22]` — ALREADY EXECUTED via D-1070); S-21.19's AC-009 asserted set reduces to the five flippable plugins only (S-21.21/S-21.24 own that reduced set — unaffected by this story's own AC-012 non-involvement). **Annotation-landing obligation:** S-21.13 (or a named successor) MUST annotate `validate-cross-site-correspondence` with `failure_policy = "fail-closed"` and a calibrated `fuel_cap >= 50_000_000` once its O(n) fuel-ceiling algorithmic fix removes the excessive cap requirement — timing deferral only, not a permanent exemption. |

Edge cases EC-001–EC-003, EC-005–EC-014 are not cited by this story's owned ACs; they are
owned by S-21.19/S-21.21/S-21.23 respectively. The full 14-row table lives in S-21.11 v2.11
(frozen historical source-of-truth).

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | 6,300 |
| ADR-039 (§Decision 4 v1.16 calibration formula + PRACTICAL_FUEL_CEILING definition; EC-004 escape-valve constant) | 1,100 |
| ADR-042 (full — this is the plugin ADR-042 directly concerns) | 1,800 |
| ADR-044 (v1.1 Addendum — Narrative/Context caveat's citation basis) | 700 |
| Research file `wasm-fuel-exhaustion-detection.md` (calibration corpus methodology) | 2,000 |
| BC-1.03.017 v1.23 (Preconditions 2–6, Invariant 2/PC8) | 1,400 |
| `plugins/vsdd-factory/hooks-registry.toml` (`validate-cross-site-correspondence` entry + context) | 600 |
| Frozen corpus snapshot fixture (Task 5a; read-only reference for the standing test) | 600 |
| Calibration log artifact (devops-engineer output; per-input `observed_max` + Precondition 6 sufficiency assertion result) | 1,000 |
| **Total** | **~15,500** |
| Agent context window | 200K (Sonnet) |
| **Budget usage** | **~7.8%** |

**Budget note:** small and self-contained — the second-smallest of the six split seams, with
no cross-crate context (no `hook-sdk`, no `legacy-bash-adapter`) required.

## Tasks (MANDATORY)

This story's tasks are the Node (A) subset plus its own flip commit from S-21.11 v2.11's
original DAG, renumbered self-contained #1–#6 (source: S-21.11 v2.11 Tasks #1, #2, #3, #4, #5,
#26), plus **NEW Task 5a** (F-S2122-P2-001 — not part of the original S-21.11 v2.11 task set;
added this burst to deliver the durable, CI-resident Precondition 6 sufficiency gate).

1. [ ] Confirm the ≥574 KB production-scale fixture file exists at
       `plugins/vsdd-factory/tests/fixtures/validate-cross-site-correspondence/a1-production-scale/factory/specs/behavioral-contracts/BC-INDEX.md`
       (576,396 bytes; committed in S-21.07 merge e94767bc)
2. [ ] Build calibration corpus: (a) `lessons.md` at ≥4000 lines; (b) `STATE.md` at live
       size; (c) `decision-log.md` at live size; (d) ≥574 KB fixture
3. [ ] **(F-S2122-P1-005, MEDIUM — calibration harness specified explicitly this burst)** Run
       `validate-cross-site-correspondence` against the full corpus with an unlimited budget:
       apply a TEMPORARY registry `fuel_cap` override to `u64::MAX / 2` for this calibration
       run only (do not commit this override — it exists solely to prevent the calibration
       invocation itself from trapping on `OutOfFuel` before `fuel_consumed` can be measured);
       capture `fuel_consumed` per invocation via `VSDD_SINK_FILE` — set
       `VSDD_SINK_FILE=/absolute/path/to/calibration-events.jsonl` before invoking the
       dispatcher against each corpus input, then extract each input's `fuel_consumed` reading
       from the resulting `plugin.completed` JSONL events (per CLAUDE.md's VSDD_SINK_FILE
       diagnostic capture convention; SEC-003 path constraint applies — use an absolute path
       with no `..` traversal). Record `fuel_consumed` per invocation, keyed to its corpus
       input.
4. [ ] Compute observed-max `fuel_consumed` PER CORPUS INPUT (F-S2122-P1-002 — record each of
       the four inputs' own reading, not merely the aggregate); set the overall
       `fuel_cap = max(ceil(observed_max × 1.5), 50_000_000)` from the highest per-input
       reading, rounding UP to the nearest whole fuel unit (F-S2122-P2-002 — pinned rounding;
       `ceil()`, never truncation). If the result is AT OR ABOVE (`>=`, not merely "exceeds"/`>`)
       `PRACTICAL_FUEL_CEILING` (`500_000_000`, ADR-039 §Decision 4 v1.16 per F-S2122-P1-003),
       STOP — surface to the orchestrator per EC-004 rather than proceeding to Task 5/5a/6; do
       not annotate the registry. **(BC-1.03.017 v1.23 Precondition 6, Q2/F-S2122-P1-002):**
       additionally re-run the plugin against the full corpus with the FINAL chosen `fuel_cap`
       (not the unlimited calibration override) and assert the regression `fuel_consumed × 1.5
       <= fuel_cap` PASSES — this is the INITIAL confirmation only; Task 5a delivers the
       durable, CI-resident standing gate that re-asserts this on every future PR against a
       frozen snapshot, not merely at calibration time. **(BC-1.03.017 v1.23 Precondition 6):**
       see also BC v1.23's updated Precondition 6 wording (frozen-corpus-snapshot gate explicitly
       split from the one-time live-corpus confirmation, matching this story's converged Task 5a
       mechanism). **Unmeasurable-calibration gate (F-S2122-P2-005 fold-in):** if `VSDD_SINK_FILE`
       capture fails to produce a `fuel_consumed` reading for ANY corpus input (e.g. a malformed
       JSONL event, a dispatch that crashes before emitting `plugin.completed`, or a
       missing/unreadable sink file), this MUST be treated as an unmeasurable calibration and
       MUST block the flip — surface to the orchestrator immediately. It MUST NOT silently fall
       through to annotating the 50,000,000 floor as if calibration had succeeded; an unmeasured
       plugin is NOT calibration-complete under any reading of Precondition 6.
5. [ ] Document calibration result (per-input `observed_max`, p99, chosen `fuel_cap`, and the
       Precondition 6 sufficiency-assertion PASS result) in the PR description
5a. [ ] **(NEW — F-S2122-P2-001, HIGH) Deliver the durable, CI-resident Precondition 6
       sufficiency gate.** Freeze the four calibration corpus inputs AS THEY STOOD at
       calibration time into a committed snapshot fixture directory —
       `plugins/vsdd-factory/tests/fixtures/validate-cross-site-correspondence/pc6-sufficiency-snapshot/`
       — explicitly distinct from Task 2's LIVE corpus (`lessons.md`/`STATE.md`/`decision-log.md`
       at CURRENT live size, which grow over time independently of this plugin's fuel
       behavior). A standing CI assertion run against the live-growing files would be a
       false-fail time-bomb — as `lessons.md`/`decision-log.md` grow past their size at
       calibration time, `fuel_consumed` would rise and could eventually exceed the fixed
       `fuel_cap` for reasons unrelated to a genuine regression in this story's own change.
       The frozen snapshot is committed once, at this story's own merge, and stays fixed
       thereafter — a future re-calibration story is responsible for refreshing it if the
       corpus profile changes materially. Author
       `test_validate_cross_site_correspondence_fuel_cap_sufficiency()` — a Cargo integration
       test that drives the real dispatcher/plugin path against the FROZEN snapshot, reads
       `plugins/vsdd-factory/hooks-registry.toml`'s live `fuel_cap` for
       `validate-cross-site-correspondence`, and asserts `fuel_consumed × 1.5 <= fuel_cap`.
       This is a **standing regression/invariant gate** (like AC-001's own pattern in
       S-21.19) — GREEN once Task 6's flip lands with a sufficient cap, and RED only if a
       future edit lowers `fuel_cap` below the sufficiency bound the frozen snapshot requires.
       MUST land in the same PR as Task 6's flip (the test asserts against the flip's own
       annotated `fuel_cap`).
6. [ ] Flip `validate-cross-site-correspondence`: annotate `plugins/vsdd-factory/hooks-registry.toml`
       with Task 4's calibrated `fuel_cap` + `failure_policy = "fail-closed"` in the SAME
       commit (AC-008 atomicity discipline). Depends only on Tasks 1–5 (this story's own
       calibration) + S-21.19's decision-function extension (its Task 5) landing — no
       dependency on S-21.21 or S-21.23. **Caveat (F-S2122-P1-001):** this annotation is
       BEHAVIORALLY INERT until S-21.24's Task 0 wires `plugin_fail_closed_on_exhaustion` into
       the real block-decision chain — landing it here is safe (inert, not unsafe) and lets
       S-21.24's final flip proceed without waiting on this plugin's own calibration, but it
       does not itself make any real dispatch block. **TD-VSDD-060 stale comment sweep
       (F-S2122-P2-003, expanded this burst):** when annotating this entry, also correct
       BOTH stale clauses in the same comment block: (a) the "No fuel_cap field: the
       per-plugin fuel_cap registry field … has not shipped" clause — ADR-039 Phase 1 (S-21.10)
       HAS shipped on develop; update to reflect this story's Phase 3a+4d annotation; AND (b) a
       superseded `p99×1.5` formula reference in the SAME comment block — this predates the
       ADR-039 §Decision 4 v1.16/§Erratum E-007 target-statistic correction; update to
       `observed_max×1.5` (matching this story's own Task 4 formula). Both clauses MUST be
       corrected in the same edit — do not fix one and leave the other stale.
       Confirm S-21.19's AC-009 test (`test_all_six_validator_class_plugins_are_fail_closed`)
       still correctly reports this plugin as annotated (it remains RED overall until the
       remaining five plugins are flipped by S-21.24, or, if EC-004 fired, until the reduced
       five-plugin set is used per the AC-009 EC-004 conditional).

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-21.07 (W4; merged e94767bc) | ADR-035 §Decision 5 — `TimeoutCause::Fuel` is a resource-policy error; advisory MUST be emitted on exhaustion. Research file confirms detection is already correct; defect is enforcement. Committed the ≥574 KB production-scale fixture used as this story's calibration corpus item 4. | `validate-cross-site-correspondence` is the plugin whose `fuel_cap` calibration this story performs; large `STORY-INDEX`/`BC-INDEX`-scale inputs are the sizing concern (not just `lessons.md`). | None specific to the flip logic. |
| S-21.19 (W6; direct prerequisite) | Delivers `plugin_fail_closed_on_exhaustion(result, on_error, failure_policy)` (Task 5, ADR-044 v1.3) — the function this plugin's `Timeout` outcomes are governed by; unit-tested but unwired within S-21.19. | This story's flip is the FIRST of the six plugins to land chronologically (no cross-seam ordering beyond S-21.19) — do not assume it must wait for S-21.21/S-21.23's completion. Landing early is safe because the annotation is inert until S-21.24 wires the function (F-S2122-P1-001 caveat). | This plugin (`on_error="continue"`) is entirely outside PC13's `plugin_fail_closed_on_error_exit` coverage set — do not confuse this story's dependency with S-21.21's Task 5a (that wiring is irrelevant to this plugin). |
| S-21.11 pre-split history | ADR-042 (§Decision 2) raised this plugin's factory-default `fuel_cap` from an earlier value to 20,000,000 and added loud-exhaustion-signaling context, still below the 50,000,000 calibration floor this story's Task 4 must reach. | This story's own calibration is expected to land comfortably above the ADR-042 default, given the ×1.5 safety margin over observed_max. | If calibration reveals the required `fuel_cap` is implausibly close to `PRACTICAL_FUEL_CEILING`, treat that as a signal the O(n) algorithmic concern ADR-042/EC-004 anticipate may already be manifesting — do not simply raise the cap further without surfacing to the orchestrator. |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| `failure_policy = "fail-closed"` MUST NOT land without calibrated `fuel_cap >= 50_000_000` at the same commit, AND MUST additionally satisfy the Precondition 6 sufficiency regression assertion (not the 50M floor alone) | ADR-039 §Decision 3 per-plugin atomicity; ADR-039 §Decision 4 v1.16 calibration floor/target; BC-1.03.017 v1.23 Precondition 6 | S-21.19's AC-001 gate test; Task 4's one-time INITIAL confirmation; Task 5a's DURABLE standing regression test (the CI-resident gate — F-S2122-P2-001); CI failure on any half-state |
| A required `fuel_cap` AT OR ABOVE (`>=`) `PRACTICAL_FUEL_CEILING` (500,000,000) MUST be surfaced to the orchestrator, not silently annotated | EC-004; ADR-039 §Decision 4 v1.16 (F-S2122-P1-003 — the now-authoritative defining citation; F-S2122-P2-002 — `>=` boundary, not `>`) | Task 4 explicit STOP gate |
| EC-004 is a VALID descope path for this plugin only (`on_error="continue"`) — routes to S-21.13, not a permanent exemption | AC-007/AC-009 EC-004 conditional | S-21.13's `depends_on` redirect — ALREADY EXECUTED via D-1070 (F-S2122-P2-005: not merely recommended) |
| **(NEW — F-S2122-P1-001, HIGH)** This story's Task 6 annotation is BEHAVIORALLY INERT until S-21.24's Task 0 wires `plugin_fail_closed_on_exhaustion` — CWE-636 closure for this plugin completes at S-21.24's merge, not this story's | BC-1.03.017 v1.23 Invariant 3/Invariant 7; ADR-044 v1.3 | Narrative caveat; Task 6 caveat note; PR reviewer confirms the PR description does not claim enforcement is live at this story's own merge |
| **(NEW — F-S2122-P2-001, HIGH)** The Precondition 6 standing test (Task 5a) MUST run against a FROZEN corpus snapshot, NEVER against the live-growing `lessons.md`/`STATE.md`/`decision-log.md` files Task 2's calibration corpus uses — a live-corpus standing assertion is a false-fail time-bomb | BC-1.03.017 v1.23 Precondition 6 (machine-checkable, not one-time; frozen-corpus-snapshot gate explicitly split from the one-time live-corpus confirmation per v1.23 Precondition 6 rewrite); F-S2122-P2-001 | PR reviewer verifies Task 5a's test references the frozen fixture directory, not the live corpus paths |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| Rust toolchain (workspace pinned) | `rust-toolchain.toml` | N/A for calibration itself (devops-engineer tooling); relevant only if a defect surfaces requiring a code fix |
| wasmtime 46.0.2 (pinned) | Cargo.lock (S-21.12 #781 merged) | Fuel mechanics; `fuel_cap = u64::MAX / 2` unlimited-budget calibration run |

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| `plugins/vsdd-factory/hooks-registry.toml` | modify (one entry) | Add calibrated `fuel_cap` + `failure_policy = "fail-closed"` to `validate-cross-site-correspondence`'s entry (Task 6); correct BOTH the stale "no fuel_cap field" clause AND the superseded `p99×1.5` formula reference in the same comment block (F-S2122-P2-003) |
| Calibration log artifact (PR description or a dedicated file) | create | Record `fuel_consumed` p99/observed-max and the chosen `fuel_cap` (Task 5) |
| `plugins/vsdd-factory/tests/fixtures/validate-cross-site-correspondence/pc6-sufficiency-snapshot/` (new) | create | FROZEN copies of the four calibration corpus inputs as they stood at calibration time — the durable test's fixed input, distinct from Task 2's live-growing corpus (Task 5a, F-S2122-P2-001) |
| `crates/factory-dispatcher/tests/` (new or existing integration-test file) | modify/create | `test_validate_cross_site_correspondence_fuel_cap_sufficiency()` — the standing CI-resident Precondition 6 sufficiency gate (Task 5a) |

## Changelog

| Version | Date | Description |
|---------|------|--------------|
| 1.5 | 2026-08-23 | Wave-7 pass-4/R3 story-layer remediation. **F-S2122-P4-001 (LOW):** `ceil()` alignment confirmed — AC-007 already has `max(ceil(observed_max × 1.5), 50_000_000)` (correct). **F-S2122-P4-002 (LOW):** ADR-044 v1.1 stale cites updated to v1.3 (Context, PSI S-21.19 row); `BC-1.03.017 v1.22 → v1.23` re-anchor (POLICY 8 full propagation — frontmatter `behavioral_contracts`, BC table version, AC-007/AC-008 headers, body cites at Narrative/Context/Tasks, Token Budget, Architecture Compliance Rules). Mechanically inert (v1.23 changes do not touch native-WASM calibration scope). Does NOT touch BC files, INDEX/STATE.md, or input-hash (state-manager step ③). |
| 1.4 | 2026-08-23 | Wave-7 pass-3/R2 story-layer remediation (D-1074; story-writer). **RE-ANCHOR ONLY.** `BC-1.03.017 v1.21 → v1.22` re-anchored with full POLICY 8 propagation: frontmatter `behavioral_contracts`, body BC table (version cite + title extended with two new v1.22 clauses: `, observed_max×1.5 fuel_cap calibration-sufficiency gate against a FROZEN corpus snapshot, and wave-7→8 migration coverage-continuity invariant`), AC-007/AC-008 headers and bodies, Token Budget, Architecture Compliance Rules. No AC text, BC trace, test predicate, or Task 5a mechanism changed — F-S2122-P3-001 (MEDIUM) was a BC-side divergence resolved in BC v1.22; story body was already correct. points/depends_on/blocks/wave UNCHANGED (4 pts, wave 7). |
| 1.3 | 2026-08-22 | Wave-7 pass-2 remediation (D-1071; architect adjudication, RATIFIED 2026-08-22). **F-S2122-P2-001 (HIGH)** — new **Task 5a**: delivers a durable, CI-resident Precondition 6 sufficiency gate (`test_validate_cross_site_correspondence_fuel_cap_sufficiency()`) that re-runs against a FROZEN corpus snapshot on every CI invocation, replacing the prior one-time-devops-check-only evidence; frozen-vs-live corpus distinction resolved explicitly (frozen for the standing test; live corpus remains Task 2's calibration-time measurement basis). **F-S2122-P2-002 (MEDIUM)** — `PRACTICAL_FUEL_CEILING` descope trigger corrected `>`→`>=` at 5 sites; `observed_max × 1.5` rounding pinned to `ceil()`. **F-S2122-P2-003 (MEDIUM)** — Task 6's stale-comment sweep expanded to also correct a superseded `p99×1.5` formula reference in the same comment block. **LOWs** — F-P2-004: clarifying `target_module:` inline comment; F-P2-005: S-21.13 redirect wording corrected from "recommended" to "ALREADY EXECUTED via D-1070" at 4 sites; added an unmeasurable-calibration-MUST-block-the-flip sentence to Task 4. `BC-1.03.017 v1.20 -> v1.21` re-anchored with full POLICY 8 propagation: frontmatter, BC table, AC-007/AC-008 headers and bodies, Token Budget. `points:`/`depends_on:`/`blocks:`/wave UNCHANGED (4 pts, wave 7). Does NOT touch S-21.19/S-21.20/S-21.21/S-21.23/S-21.24, BC-1.03.017/ADR-044 themselves (already landed at commit 8ef46b8a), or any INDEX/STATE.md (state-manager's domain). |
| 1.2 | 2026-08-22 | Wave-7 pass-1 remediation (D-1069; architect adjudication, RATIFIED 2026-08-22). **F-S2122-P1-001 (HIGH)** — added explicit caveat to Narrative + Context (and Task 6): this story's `hooks-registry.toml` annotation is BEHAVIORALLY INERT until S-21.24's Task 0 wires `plugin_fail_closed_on_exhaustion`; CWE-636 closure for the native-WASM tier completes at S-21.24, not this story's merge (BC-1.03.017 v1.20 Invariant 3/Invariant 7). Preserved the correct calibration-methodology-independence claim unchanged. **F-S2122-P1-002 (HIGH)** — AC-007/Task 4 strengthened to require per-corpus-input `observed_max` recording plus BC-1.03.017 v1.20 Precondition 6's machine-checkable sufficiency regression assertion (`fuel_consumed × 1.5 <= registry fuel_cap`), not merely the 50M floor. **F-S2122-P1-003 (MEDIUM)** — `PRACTICAL_FUEL_CEILING` citation corrected to ADR-039 §Decision 4 v1.16 throughout. **F-S2122-P1-005 (MEDIUM)** — Task 3's calibration harness specified explicitly: temporary registry `fuel_cap` override to `u64::MAX/2` + `VSDD_SINK_FILE` capture of `plugin.completed.fuel_consumed`. `BC-1.03.017 v1.19 → v1.20` re-anchored with full POLICY 8 propagation. `inputs:` adds ADR-044. `points:`/`depends_on:`/`blocks:`/wave UNCHANGED (4 pts, wave 7). Does NOT touch S-21.19/S-21.20/S-21.21/S-21.23/S-21.24, BC-1.03.017/ADR-044 themselves (already landed at commit a3bfa1af), or any INDEX/STATE.md (state-manager's domain). |
| 1.1 | 2026-08-21 | Wave-7 pre-cascade re-anchor burst (D-1060 deferral, EXTENDED D-1064): BC-1.03.017 v1.18 → v1.19 re-anchor swept across frontmatter `behavioral_contracts` and all body cites (AC-007/AC-008 headers, BC table, Token Budget). No AC text, BC trace, or test predicate changed. |
| 1.0 | 2026-08-20 | Created by story-writer via the operator-directed 6-way split of CONVERGED S-21.11 (v2.11, 16-pass 3-CLEAN BC-5.39.001), per `.factory/planning/S-21.11-decomposition-plan.md` §1/§3/§6. Owns AC-007 (native-WASM leg only) and AC-008 (owned in full), copied verbatim from S-21.11 v2.11 with zero weakening. Tasks renumbered #1–#6 (self-contained) from S-21.11 v2.11 Tasks #1–#5, #26. This seam owns and EXECUTES the native-WASM flip itself, per the plan's §6 task-ownership resolution, not the capstone (S-21.24). |
