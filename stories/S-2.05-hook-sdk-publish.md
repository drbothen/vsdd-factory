---
document_type: story
level: ops
story_id: "S-2.05"
epic_id: "E-2"
version: "1.1"
status: partial
producer: story-writer
timestamp: 2026-04-25T00:00:00
phase: 1.8
inputs:
  - .factory/stories/v1.0/S-2.5-hook-sdk-publish.md
  - .factory/specs/architecture/ARCH-INDEX.md
input-hash: "28bc66c"
traces_to: .factory/specs/prd.md#FR-009
cycle: v1.0.0-greenfield
points: "2"
depends_on: ["S-1.03"]
blocks: ["S-2.08"]
behavioral_contracts: []
verification_properties: []
capabilities:
  - CAP-009
functional_requirements:
  - FR-009
priority: "P1"
wave: 7
target_module: crates/hook-sdk
subsystems: ["SS-02"]
estimated_days: 1
assumption_validations: []
risk_mitigations: []
---

# S-2.05: hook-sdk publish to crates.io (0.1.0)

**Epic:** E-2 — Legacy Adapter and Beta Release
**Phase:** Phase 2 — Legacy bash adapter
**Milestone:** `1.0.0-beta.1`
**Tier:** C
**Blocks:** S-2.08
**Blocked by:** S-1.03 (the SDK crate itself)
**Estimated effort:** S

> **Status note:** As of v1.0.0-beta.4, `cargo publish --dry-run` passes cleanly but the
> real `cargo publish` to crates.io has been deferred. The crate is publish-ready;
> the actual publishing step is outstanding.

## Narrative
- **As a** third-party plugin author
- **I want to** add `vsdd-hook-sdk` as a dependency from crates.io
- **So that** I can write WASM plugins without cloning the vsdd-factory repository

## Goal

Publish the `vsdd-hook-sdk` crate (and its proc-macro subcrate
`vsdd-hook-sdk-macros`) to crates.io at version 0.1.0. Establishes the
public Rust API surface third-party plugin authors depend on.

## Acceptance criteria

- [ ] `cargo publish --dry-run` passes for `vsdd-hook-sdk` and `vsdd-hook-sdk-macros`
- [ ] Crate metadata: `description`, `license`, `repository`, `keywords`, `categories` fields all set
- [ ] `README.md` at crate root with quickstart example
- [ ] `vsdd-hook-sdk` published at `0.1.0`
- [ ] `vsdd-hook-sdk-macros` published at `0.1.0`
- [ ] Release workflow has a `publish-sdk` job that publishes on tag push

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| crates/hook-sdk/Cargo.toml | crates/hook-sdk/ | pure (metadata only) |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | `cargo publish` token missing in CI | Workflow fails with clear error |
| EC-002 | Crate already published at 0.1.0 | `--dry-run` reports conflict; skip real publish |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| crates/hook-sdk/Cargo.toml | pure | Metadata configuration only |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~700 |
| hook-sdk/Cargo.toml | ~300 |
| Release.yml additions | ~400 |
| **Total** | **~1,400** |
| Agent context window | 200K for Sonnet |
| **Budget usage** | **~0.7%** |

## Tasks (MANDATORY)

1. [ ] Add `description`, `license`, `repository`, `keywords`, `categories` to both crate Cargo.toml files
2. [ ] Write `crates/hook-sdk/README.md` with quickstart example
3. [ ] Run `cargo publish --dry-run` for both crates
4. [ ] Add `publish-sdk` job to Release.yml (gated on `CRATES_IO_TOKEN` secret)
5. [ ] Execute real `cargo publish` on next tag (OUTSTANDING)

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-1.03 | hook-sdk + hook-sdk-macros are separate crates | Proc-macro crates must be published separately | Must publish macros crate before main crate |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| Macros crate published first (dependency order) | crates.io publishing protocol | Manual gate in workflow |

## Capability Anchor Justification

CAP-009 ("Author and publish WASM hook plugins using the Rust SDK") per capabilities.md §CAP-009.

Justification: S-2.05 publishes the `vsdd-hook-sdk` crate to crates.io, making it available for the third-party plugin author persona that CAP-009 targets. Publishing completes the distribution step of CAP-009's user outcome: "a third-party plugin author can add a dependency and ship a `.wasm` without cloning the vsdd-factory repository." The CAP-009 capability lifecycle spans both S-1.03 (build the SDK) and S-2.05 (distribute the SDK).

## Behavioral Contracts

S-2.05 is a packaging story — it introduces no new in-process behavior. Publishing to crates.io changes where the artifact lives, not what it does. The behavioral contracts for the SDK itself (all 22 BC-2.01–BC-2.05 contracts) are implemented and owned by S-1.03.

**No behavioral contracts apply to S-2.05.** The `behavioral_contracts: []` array is empty by design, not by omission.

### v1.1 BC Candidates

| Candidate ID | Title | Rationale |
|-------------|-------|-----------|
| BC-2.06.001-candidate | hook-sdk-published-on-crates-io — vsdd-hook-sdk@0.1.0 is resolvable at crates.io | Contracts the publish-pipeline outcome; verifiable via `cargo search vsdd-hook-sdk` returning 0.1.0; deferred to v1.1 when crates.io publish is executed |
| BC-2.06.002-candidate | hook-sdk-macros-published-before-main — vsdd-hook-sdk-macros published atomically before vsdd-hook-sdk | Contracts the ordering requirement for proc-macro crate; deferred to v1.1 |

## Verification Properties

S-2.05 has no verification properties. The VPs for SDK behavior (VP-038 through VP-042) are scoped to S-1.03 which implements the contracts those VPs verify. The publish action itself has no property-based or unit-test verification in the current cycle.

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| cargo | workspace-pinned | Publishing |

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| crates/hook-sdk/Cargo.toml | modify | Add publish metadata |
| crates/hook-sdk-macros/Cargo.toml | modify | Add publish metadata |
| crates/hook-sdk/README.md | create | Crate documentation |
| .github/workflows/Release.yml | modify | Add publish-sdk job |
