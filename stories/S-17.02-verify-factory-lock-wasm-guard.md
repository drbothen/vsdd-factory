---
document_type: story
level: ops
story_id: "S-17.02"
epic_id: "E-17"
version: "1.5"
title: "S-17.02: verify-factory-lock WASM guard crate + registry entries (D1 + D2 + D9 guard tests)"
status: merged
merged_commit: "df4f26b8"
merged_pr: 182
merged_date: "2026-06-11"
producer: story-writer
timestamp: 2026-06-10T00:00:00Z
last_amended: "2026-06-11 (v1.5) — POST-MERGE codification (state-manager; D-545; PR #182 squash-merged df4f26b8 to develop 2026-06-11; S-17.02 status draft→merged; merged_commit df4f26b8; BC-4.13.001 POL-14 draft→active; STORY-INDEX v3.89→v3.90; merged count 75→76; E-17 2/3 stories merged). [Prior: 2026-06-11 (v1.4) — AC-001 + AC-002 sibling-clause boundary-operator propagation (story-writer; F-ADV-S1702-001; issue #170). AC-001 blocking condition corrected `now ≤ expires_at` → `now < expires_at` (boundary is expired, not blocking; matches BC-4.13.001 PC1). AC-002 expired condition corrected `now > expires_at` → `now >= expires_at` (boundary treated as expired → Continue; matches BC-4.13.001 PC2). Full sibling sweep (TD-VSDD-060) confirmed zero remaining stale boundary operators. Outcomes unchanged. STORY-INDEX pending v1.4 bump. [Prior: 2026-06-11 (v1.3) — AC-015 + EC-002 boundary-semantics wording correction: `now >= expires_at` is the expired condition; at the exact boundary `now == expires_at` the lock is expired (Continue). Prior wording cited `now > expires_at` (strict greater-than) which was self-contradictory — `now == expires_at` is NOT greater-than, so the strict operator would have BLOCKED at the boundary rather than returning Continue as stated. Operator corrected to `>=` throughout AC-015, EC-002, and T-3. Outcome (boundary → Continue / LockExpired) unchanged. issue #170. [Prior: 2026-06-11 (v1.2) — EC-ID realignment to BC-4.13.001 canonical table + new Red Gate tests (story-writer; F-S1702-001/002/003 + LOW sweeps; issue #170). EC-014 concurrent-Edit/CAS row added (BC canonical EC-014, previously missing). EC-014→EC-015 (exec_subprocess timeout; matches BC). EC-015→EC-016 (env_allow absent; matches BC). 5 new Red Gate test rows registered: test_BC_4_13_001_registry_has_two_entries_with_both_capability_blocks, test_BC_4_13_001_registry_both_entries_async_false, test_BC_4_13_001_is_expired_now_equals_expires_at_is_expired, test_BC_4_13_001_crlf_state_md_foreign_lock_blocks, test_BC_4_13_001_missing_closing_delimiter_returns_continue. T-8 bats assertion strengthened to verify capability_denied log_warn. BC-4.13.001 version updated to v1.1 in Behavioral Contracts table. [Prior: 2026-06-11 (v1.1) — env_allow production-correctness spec gap (story-writer; issue #170; BC-4.13.001 amended; ADR-025 amended). AC-009 canonical TOML updated: env_allow = [\"HOME\", \"GIT_CONFIG_GLOBAL\", \"XDG_CONFIG_HOME\"] added to both exec_subprocess blocks. AC-016 added (registry env_allow assertion test). Architecture Compliance Rule 2 updated to require env_allow. Red Gate Test Table row added. Bats synthetic-registry note added. [Prior: 2026-06-10 (v1.0) — Initial authoring (story-writer; brownfield-backfill issue #170; ADR-025 v1.2 D1/D2/D9 deliverables; BC-4.13.001).]]]]]"
phase: brownfield-backfill
cycle: v1.0-brownfield-backfill
inputs:
  - .factory/specs/architecture/decisions/ADR-025-single-writer-factory-locklease-prevent-concurrent-session-races-on-factory-artifacts-orphan-branch.md
  - .factory/specs/behavioral-contracts/ss-04/BC-4.13.001.md
  - .factory/specs/behavioral-contracts/ss-05/BC-5.40.001.md
  - .factory/specs/architecture/ARCH-INDEX.md
  - plugins/vsdd-factory/hooks-registry.toml
  - crates/hook-plugins/validate-artifact-path/src/lib.rs
  - crates/hook-sdk/src/lib.rs
  - crates/factory-dispatcher/src/executor.rs
input-hash: "bb285ff"
traces_to:
  - .factory/specs/behavioral-contracts/ss-04/BC-4.13.001.md
  - .factory/specs/architecture/decisions/ADR-025-single-writer-factory-locklease-prevent-concurrent-session-races-on-factory-artifacts-orphan-branch.md
wave: 2
priority: P1
points: 8
track: factory-state-durability
effort_class: multi-day-with-research
depends_on:
  - "S-17.01"
blocks:
  - "S-17.03"
behavioral_contracts:
  - "BC-4.13.001"
verification_properties: []
# BC status: VPs to be allocated during verification scoping per TD-VSDD-063 lagging-VP precedent
domain_invariants: []
tdd_mode: strict
target_module: "crates/hook-plugins/verify-factory-lock"
subsystems:
  - "SS-04"
estimated_days: 2.5
assumption_validations: []
risk_mitigations: []
anchored_adrs:
  - ADR-016
  - ADR-019
  - ADR-020
  - ADR-025
closes:
  - "issue #170 (partial — S-17.02 MERGED; S-17.03 remains)"
modified:
  - "2026-06-11 (v1.5)"
---

# S-17.02: verify-factory-lock WASM Guard Crate + Registry Entries

**Epic:** E-17 — Factory State Durability and Concurrency (brownfield-backfill #170)
**Wave:** 2 (depends on S-17.01 schema; schema must exist before guard can be built)
**Depends on:** S-17.01 (factory_lock schema in STATE.md)
**Blocks:** S-17.03 (skills should be delivered with guard already active)
**Priority:** P1 (primary enforcement mechanism for CAP-031)
**BC gate:** BC-4.13.001 v1.2 (draft; POL-14 auto-promotion on PR merge)

## Narrative

- **As a** developer working on the `factory-artifacts` orphan branch
- **I want** a native-WASM PreToolUse guard that reads the `factory_lock` block from
  STATE.md and blocks mutating tools (Edit, Write, Agent dispatch, factory-artifacts
  push Bash commands) when a foreign unexpired lock is held
- **So that** accidental concurrent work from another developer is intercepted before
  any mutations are applied, with an actionable refusal message naming the holder, the
  expiry time, and the break-glass `/factory-unlock --force` command

## Behavioral Contracts

| BC ID | Title | Version | PCs Exercised |
|-------|-------|---------|---------------|
| BC-4.13.001 | verify-factory-lock WASM PreToolUse guard | 1.2 | PC1, PC2, PC3, PC4, PC5, PC6, PC7, PC8 |

## Acceptance Criteria

### AC-001 (traces to BC-4.13.001 postcondition PC1 — foreign unexpired lock: block with actionable message)

When ALL conditions are true — `factory_lock.holder` is present and non-null,
`now < factory_lock.expires_at`, and `factory_lock.holder != current_git_email` — the
guard MUST return `block_intent = true` (exit code 2) with a block message containing
ALL five required fields:
1. `holder` — exact git email of the current lock holder
2. `locked_at` — ISO-8601 timestamp when the lock was acquired
3. `expires_at` — ISO-8601 timestamp when the lock auto-expires
4. `time_remaining` — human-readable duration, e.g., "37 min remaining" (computed as
   `expires_at - now`, rounded down to the nearest minute)
5. `/factory-unlock --force` — the exact command string for break-glass force-release

The block path MUST run through `plugin_requests_block()` in
`crates/factory-dispatcher/src/executor.rs` (the `plugin_requests_block` function
invoked at `executor.rs:105–108` for sync-group plugins — `async = false` is required).

**Error variant:** `ForeignLockHeld`
**Test:** `test_BC_4_13_001_foreign_unexpired_lock_blocks_with_all_five_fields()`

### AC-002 (traces to BC-4.13.001 postcondition PC2 — expired lock: pass)

When `factory_lock.holder` is present but `now >= factory_lock.expires_at`, the guard
MUST return `HookResult::Continue` immediately. No warning emitted (expired-lock
pass-through is the normal TTL expiry path, not an error).

**Error variant (internal only):** `LockExpired`
**Test:** `test_BC_4_13_001_expired_lock_returns_continue()`

### AC-003 (traces to BC-4.13.001 postcondition PC3 — self-held lock: pass unconditionally)

When `factory_lock.holder == current_git_email` (regardless of `expires_at`), the
guard MUST return `HookResult::Continue`. The developer who acquired the lock is never
blocked by their own lock. Latency MUST be sub-5ms under normal conditions (one
`read_file` + one `exec_subprocess` + one timestamp comparison per ADR-025 Decision 10
and ADR-020 Class A p95 ≤ 1500ms budget).

**Test:** `test_BC_4_13_001_self_held_lock_returns_continue()`

### AC-004 (traces to BC-4.13.001 postcondition PC4 — absent/null/malformed lock: pass fail-open)

When the `factory_lock` block is absent, present but null, or malformed (missing
required fields `holder`/`locked_at`/`expires_at`, or fields not parseable as their
required types), the guard MUST return `HookResult::Continue`. A malformed block MUST
emit `host::log_warn` with `"MalformedLockBlock: <details>"` (advisory only; non-blocking).

**Error variant:** `MalformedLockBlock` (log_warn; no block)
**Test:** `test_BC_4_13_001_malformed_block_returns_continue_with_log_warn()`

### AC-005 (traces to BC-4.13.001 postcondition PC5 — read-only tools: not triggered)

Tools NOT in the registry entry `tool` regex (Read, non-push Bash, mcp__* tools) are
NOT intercepted by this plugin at all — the guard is never invoked. A blocked developer
CAN always read `.factory/STATE.md` to inspect the lock state.

**Test:** (bats T-5 and T-7 from BC-4.13.001 canonical test vectors; see AC-012/AC-013)

### AC-006 (traces to BC-4.13.001 postcondition PC6 — STATE.md read failure: pass fail-open)

When `host::read_file(".factory/STATE.md")` returns any `HostError` variant
(`OutputTooLarge`, `Timeout`, `CapabilityDenied`, `InvalidArgument`, `Other(i32)`), the
guard MUST return `HookResult::Continue` after emitting `host::log_warn` with the error
variant name. A guard that cannot read STATE.md MUST NOT block.

**Error variant:** `StateReadError`
**Test:** `test_BC_4_13_001_read_file_host_error_returns_continue()`

### AC-007 (traces to BC-4.13.001 postcondition PC7 — git subprocess failure: pass fail-open)

When `host::exec_subprocess(["git", "config", "user.email"])` fails (non-zero exit,
empty output, or HostError), the guard MUST return `HookResult::Continue` after
emitting `host::log_warn`. Identity resolution failure MUST NOT block.

**Error variant:** `IdentityResolutionFailed`
**Test:** `test_BC_4_13_001_git_subprocess_failure_returns_continue()`

### AC-008 (traces to BC-4.13.001 postcondition PC8 — on_error behavior: always Continue)

The plugin's `on_error` field in `hooks-registry.toml` MUST be `"continue"`. A
crashed or timed-out plugin returns `HookResult::Continue`. The factory MUST NOT be
wedged by a broken guard.

**Test:** (registry inspection; bats T-8 graceful-degrade coverage)

### AC-009 (traces to BC-4.13.001 invariant 5 — registry shape: two entries, both capability blocks, env_allow present)

The canonical registration in `hooks-registry.toml` MUST include BOTH entries with
BOTH capability blocks AND `env_allow` on every `exec_subprocess` block:

```toml
[[hooks]]
name = "verify-factory-lock"
plugin = "hook-plugins/verify-factory-lock.wasm"
event = "PreToolUse"
tool = "Edit|Write|Agent"
async = false
on_error = "continue"
timeout_ms = 5000

[hooks.capabilities.read_file]
path_allow = [".factory/STATE.md"]

[hooks.capabilities.exec_subprocess]
binary_allow = ["git"]
env_allow = ["HOME", "GIT_CONFIG_GLOBAL", "XDG_CONFIG_HOME"]

[[hooks]]
name = "verify-factory-lock-bash"
plugin = "hook-plugins/verify-factory-lock.wasm"
event = "PreToolUse"
tool = "Bash"
async = false
on_error = "continue"
timeout_ms = 5000

[hooks.capabilities.read_file]
path_allow = [".factory/STATE.md"]

[hooks.capabilities.exec_subprocess]
binary_allow = ["git"]
env_allow = ["HOME", "GIT_CONFIG_GLOBAL", "XDG_CONFIG_HOME"]
```

**Rationale for `env_allow`:** The dispatcher's exec_subprocess implementation calls
`env_clear()` before spawning the subprocess and only passes through variables listed in
`env_allow`. Without `HOME` (and the two XDG/Git config override vars), `git config
user.email` cannot locate `.gitconfig`, returns empty output, and the guard degrades to
`IdentityResolutionFailed → Continue`. The lock is then SILENTLY NEVER ENFORCED even
when STATE.md holds a valid foreign unexpired lock. This is the primary
production-correctness footgun for this story (verified empirically — issue #170).

The three required env vars:
- `HOME` — Git's primary config directory locator; without it `git config user.email` is empty
- `GIT_CONFIG_GLOBAL` — override path for global config (must pass through for non-default setups)
- `XDG_CONFIG_HOME` — XDG-compliant config location (used by some Linux environments)

Omitting EITHER capability block OR omitting `env_allow` causes a silent no-op guard
(capability-denied or IdentityResolutionFailed graceful-degrade to Continue — the
implementation-time footguns per ADR-025 D2 Rationale).
The bats test MUST cover the capability-omitted case (AC-011) and the env_allow-absent
case (AC-016).

**Test:** `test_BC_4_13_001_registry_has_two_entries_with_both_capability_blocks()`
(grep `hooks-registry.toml` for both capability block forms — literal shell with
captured stdout per POLICY 5 Part B)

### AC-010 (traces to BC-4.13.001 invariant 6 — capability-denied graceful degrade)

If either `[hooks.capabilities.read_file]` or `[hooks.capabilities.exec_subprocess]`
is omitted from the registry (footgun scenario), the plugin MUST catch the
`CapabilityDenied` `HostError` variant and graceful-degrade to `HookResult::Continue`
(NOT panic). The degrade MUST emit `host::log_warn("capability_denied: read_file")` or
`host::log_warn("capability_denied: exec_subprocess")` as appropriate. The guard is
now silently inert but does not crash.

**Test:** `test_BC_4_13_001_capability_denied_graceful_degrades_to_continue()`

### AC-011 (traces to BC-4.13.001 invariant 7 — latency budget)

The guard's hot path (one `read_file` call + one `exec_subprocess` call + timestamp
comparison) MUST complete within ADR-020 Class A p95 budget of 1500ms. Under normal
conditions the budget is well within 100ms. `timeout_ms = 5000` in the registry is
the backstop for pathological conditions.

**Test:** (addressed by ADR-020 Class A invariant; no separate unit test required;
noted as architecture compliance rule)

### AC-012 (traces to BC-4.13.001 canonical test vector T-6 — Bash push arm blocked)

When a `Bash` PreToolUse event arrives with a command matching `git.*push.*factory-artifacts`
AND a foreign unexpired lock is held, the guard MUST return `HookResult::Block`.
The push-regex check is internal to the WASM plugin for Bash payloads — the registry
`tool = "Bash"` is the trigger; the internal regex filters to factory-artifacts pushes
only.

**Test:** `test_BC_4_13_001_bash_factory_artifacts_push_blocked_when_foreign_lock()`
(bats T-6)

### AC-013 (traces to BC-4.13.001 canonical test vector T-7 — non-push Bash: Continue)

When a `Bash` PreToolUse event arrives with a command NOT matching
`git.*push.*factory-artifacts` (e.g., `cat .factory/STATE.md`, `ls`, any non-push
command), the guard MUST return `HookResult::Continue` immediately (no STATE.md read,
sub-millisecond).

**Test:** `test_BC_4_13_001_non_push_bash_returns_continue_immediately()`
(bats T-7)

### AC-014 (traces to BC-4.13.001 edge case EC-010 — async=true is a misconfiguration)

The guard MUST be registered with `async = false`. If the registry has `async = true`,
the dispatcher discards `block_intent` (advisory-only per ADR-019), silently making
the guard a no-op blocker. Implementation MUST enforce `async = false` in the registry
entries and include a comment: `# async = false REQUIRED — see ADR-019 + ADR-025`.

**Test:** (registry grep confirms `async = false` on both entries — captured stdout
per POLICY 5 Part B)

### AC-015 (traces to BC-4.13.001 edge case EC-002 — expires_at boundary: treat now==expires_at as expired)

When `now == factory_lock.expires_at` exactly (boundary case), the guard MUST treat
the lock as just-expired and return `HookResult::Continue` (LockExpired path). The
guard MUST treat the lock as expired and return Continue when `now >= expires_at`; at
the exact boundary `now == expires_at` the lock is expired (Continue, NOT Block).

**Test:** `test_BC_4_13_001_is_expired_now_equals_expires_at_is_expired()`

### AC-016 (traces to BC-4.13.001 invariant 5 — registry env_allow present on both exec_subprocess blocks)

The `hooks-registry.toml` MUST include `env_allow = ["HOME", "GIT_CONFIG_GLOBAL",
"XDG_CONFIG_HOME"]` on BOTH `[hooks.capabilities.exec_subprocess]` blocks (one per
entry). Omitting `env_allow` causes the dispatcher to `env_clear()` before spawning git,
making `git config user.email` return empty output → `IdentityResolutionFailed →
HookResult::Continue`. The foreign-lock Block path SILENTLY NEVER FIRES.

This is a correctness requirement, not a capability-completeness footnote. A registry
that passes all other AC-009 checks (two entries, both capability blocks present, correct
`binary_allow`) but omits `env_allow` will appear correctly configured yet produce a
guard that never enforces the lock.

**Test:** `test_BC_4_13_001_registry_exec_subprocess_has_env_allow_home()`
Assertion: grep `plugins/vsdd-factory/hooks-registry.toml` for `env_allow` containing
`HOME` in both `verify-factory-lock` and `verify-factory-lock-bash` exec_subprocess
blocks — literal shell with captured stdout per POLICY 5 Part B. The grep MUST find
exactly two occurrences, one per entry.

**Bats note (test-writer):** The synthetic registry fixture written by
`_write_full_registry` in the bats setup helpers MUST also include `env_allow =
["HOME", "GIT_CONFIG_GLOBAL", "XDG_CONFIG_HOME"]` on both exec_subprocess blocks.
A synthetic registry without env_allow will cause T-2 (foreign unexpired lock → Block)
to fail in bats integration tests for the same reason it fails in production: git identity
cannot be resolved, guard degrades to Continue, and the Block assertion is never reached.

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| `verify-factory-lock` WASM plugin | `crates/hook-plugins/verify-factory-lock/src/lib.rs` (new crate) | Pure-core (Rust logic); Effectful-shell (reads STATE.md via host::read_file; calls git via host::exec_subprocess) |
| Compiled WASM artifact | `plugins/vsdd-factory/hook-plugins/verify-factory-lock.wasm` | Effectful (WASM binary) |
| Registry entries (two entries) | `plugins/vsdd-factory/hooks-registry.toml` | Configuration |
| Dispatcher block path | `crates/factory-dispatcher/src/executor.rs` (`plugin_requests_block` function) | No changes — existing path |
| Hook SDK ABI | `crates/hook-sdk/src/lib.rs` (`HOST_ABI_VERSION = 1`) | No changes — ABI unchanged |

**Subsystem anchor justification:** SS-04 is "Plugin Ecosystem" per ARCH-INDEX
Subsystem Registry — it owns all native-WASM hook plugin crates under
`crates/hook-plugins/` and the compiled `.wasm` artifacts. The new
`verify-factory-lock` crate is a new member of this set, exactly as
`validate-artifact-path` is. The registry entries in `hooks-registry.toml` also
belong to SS-04's domain. No other subsystem better describes a new WASM guard crate.

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | `factory_lock` block absent from STATE.md | `HookResult::Continue` (PC4 fail-open: no lock = unlocked) |
| EC-002 | `factory_lock.expires_at` is exactly `now` (boundary) | `HookResult::Continue` — just-expired (LockExpired path; `now >= expires_at` is true at exact boundary → expired → Continue) |
| EC-003 | `factory_lock.holder` == `current_git_email` and lock unexpired | `HookResult::Continue` (PC3 self-held) |
| EC-004 | `factory_lock.holder` is empty string | `MalformedLockBlock` → `HookResult::Continue` (PC4; empty holder is malformed) |
| EC-005 | `factory_lock.expires_at` is not valid ISO-8601 | `MalformedLockBlock` → `HookResult::Continue` (PC4; unparseable expiry = treat as absent) |
| EC-006 | STATE.md missing (file not found) | `HostError` → `StateReadError` → `HookResult::Continue` (PC6 fail-open) |
| EC-007 | `[hooks.capabilities.read_file]` omitted from registry | `CapabilityDenied` → graceful-degrade to `Continue` with `log_warn`; lock NEVER enforces |
| EC-008 | `[hooks.capabilities.exec_subprocess]` omitted from registry | `CapabilityDenied` → graceful-degrade to `Continue` with `log_warn`; lock NEVER enforces |
| EC-009 | `git config user.email` returns empty output | `IdentityResolutionFailed` → `HookResult::Continue` (PC7 fail-open) |
| EC-010 | `async = true` in registry (misconfiguration) | Plugin fires but `block_intent` discarded by dispatcher; guard silently no-op |
| EC-011 | Non-push Bash command arrives at Bash entry | Internal push-regex: no match → `HookResult::Continue` immediately (no STATE.md read) |
| EC-012 | `factory_lock` has `holder` and `expires_at` but missing `locked_at` | `MalformedLockBlock` → `HookResult::Continue` (`locked_at` required for refusal message) |
| EC-013 | STATE.md frontmatter region missing closing `---\n` delimiter | `MalformedLockBlock` (parse fail-open) → `HookResult::Continue` |
| EC-014 | Two concurrent Edit calls from different sessions; both arrive before either is blocked | First Edit proceeds (no lock set yet); CAS push at `state-burst` is the safety net for the push layer (BC-5.40.001). The guard provides per-call blocking, not atomicity across concurrent arrivals. |
| EC-015 | `host::exec_subprocess` times out (git subprocess stalls) | `HostError::Timeout` → `IdentityResolutionFailed` → `HookResult::Continue` (PC7) |
| EC-016 | `env_allow` omitted from `exec_subprocess` capability block in registry | Dispatcher `env_clear()`s before spawning git → `git config user.email` returns empty → `IdentityResolutionFailed` → `HookResult::Continue`; foreign lock SILENTLY NEVER ENFORCED (issue #170 empirical) |

## Tasks

- [ ] **T-1 — Codebase grounding (SDK Grounding Evidence from BC-4.13.001):** Run the
  five canonical greps with captured stdout:
  1. `grep -rn "plugin_requests_block" crates/factory-dispatcher/src/` — confirm executor.rs block path
  2. `grep -c 'on_error.*continue' plugins/vsdd-factory/hooks-registry.toml` — confirm existing precedent
  3. `grep -n "HOST_ABI_VERSION" crates/hook-sdk/src/lib.rs` — confirm ABI=1 unchanged
  4. `grep -n 'binary_allow.*git' plugins/vsdd-factory/hooks-registry.toml` — confirm exec_subprocess precedent
  5. `grep -n "validate-artifact-path" plugins/vsdd-factory/hooks-registry.toml` — confirm PreToolUse read_file guard pattern

  Record all stdout in the Red Gate log. Do NOT implement until all greps return expected hits.

- [ ] **T-2 — Scaffold new crate** `crates/hook-plugins/verify-factory-lock/`:
  Copy `crates/hook-plugins/validate-artifact-path/Cargo.toml` as the template.
  `crate-type = ["cdylib"]`. Add `vsdd-hook-sdk` dependency at workspace version.
  Do NOT add `regex` crate — the frontmatter scanner MUST use a manual line-by-line
  scan (see Architecture Compliance Rule 4).

- [ ] **T-3 — Implement the WASM plugin entry point** (`src/lib.rs`):
  - Entry point: `pub fn on_pre_tool_use(payload: PluginPayload) -> HookResult`
  - Parse `tool` from payload. For `Bash` tool: apply internal push-regex
    `git.*push.*factory-artifacts` to `tool_input.command`. If no match: return
    `HookResult::Continue` immediately (sub-millisecond path, no STATE.md read).
  - For mutating tools (Edit, Write, Agent, matched Bash): proceed to lock check.
  - Lock check flow:
    1. `host::read_file(".factory/STATE.md", max_bytes = 65536)` — on HostError:
       `log_warn("StateReadError: <variant>")`, return Continue.
    2. Parse frontmatter region (between first and second `---\n`). Scan line-by-line
       for `factory_lock:` key and its three sub-fields. On parse fail: `log_warn`,
       return Continue (PC4 fail-open).
    3. If block absent or all fields absent: return Continue (unlocked).
    4. Parse `expires_at` as ISO-8601. On parse fail: `log_warn("MalformedLockBlock")`,
       return Continue.
    5. Compare `now >= expires_at`. If true: return Continue (LockExpired; boundary `now == expires_at` is expired).
    6. `host::exec_subprocess(["git", "config", "user.email"])` — on fail/empty/HostError:
       `log_warn("IdentityResolutionFailed")`, return Continue (PC7).
    7. Trim trailing newline from git email output.
    8. If `holder == current_git_email`: return Continue (PC3 self-held).
    9. Compute `time_remaining = expires_at - now`, round down to nearest minute.
    10. Return `HookResult::Block` with message containing all five required fields
        (PC1 ForeignLockHeld). Message MUST include `/factory-unlock --force`.

- [ ] **T-4 — Write unit tests** (ALL tests must FAIL before implementation — Red Gate):
  Unit test each AC via Rust `#[cfg(test)]` with mocked `host::read_file` and
  `host::exec_subprocess`. Use the validate-artifact-path test patterns as the sibling
  template. Test function names per AC bodies above.

- [ ] **T-5 — Add registry entries** to `plugins/vsdd-factory/hooks-registry.toml`:
  Both entries exactly as specified in AC-009. Both `async = false`. Both `on_error =
  "continue"`. Both `timeout_ms = 5000`. BOTH capability blocks on BOTH entries.
  Include comment `# async = false REQUIRED — see ADR-019 + ADR-025` on each entry.
  **CRITICAL:** Both `[hooks.capabilities.exec_subprocess]` blocks MUST include
  `env_allow = ["HOME", "GIT_CONFIG_GLOBAL", "XDG_CONFIG_HOME"]`. Omitting this causes
  `git config user.email` to return empty in the sandboxed environment → silent no-op
  guard (AC-016 / issue #170 empirical finding).

- [ ] **T-6 — Compile to WASM**: `cargo build --target wasm32-wasip1 -p verify-factory-lock`
  (or the workspace WASM target used by other hook-plugin crates — match the exact
  target used by `validate-artifact-path`). Output MUST be at
  `plugins/vsdd-factory/hook-plugins/verify-factory-lock.wasm`.

- [ ] **T-7 — Write bats tests** (D9 guard test vectors T-1..T-9 from BC-4.13.001
  Canonical Test Vectors table — T-10 belongs to S-17.03 skill tests):
  - T-1: absent lock → Edit → Continue
  - T-2: foreign unexpired lock → Edit → Block with 5-field message
  - T-3: foreign expired lock → Edit → Continue
  - T-4: self-held lock → Edit → Continue
  - T-5: foreign lock → Read → not triggered (Continue)
  - T-6: foreign lock → `Bash git push origin factory-artifacts` → Block
  - T-7: foreign lock → `Bash cat .factory/STATE.md` → Continue (non-push)
  - T-8: capability-omitted registry entry → Edit → Continue (graceful-degrade)
  - T-9: malformed `expires_at` → Edit → Continue (malformed fail-open)

- [ ] **T-8 — Run cargo test + bats suite**: confirm all unit tests and bats tests PASS.

## Previous Story Intelligence

**Predecessor:** S-17.01 (this story depends on it).

S-17.01 establishes the `factory_lock` STATE.md schema and the CAS push pattern. This
story READS that schema — the frontmatter line-by-line scanner in T-3 must align with
the YAML form defined in BC-5.40.001 PC1:
```yaml
factory_lock:
  holder: "..."
  locked_at: "..."
  expires_at: "..."
```
The scanner MUST handle exactly this indentation (2-space sub-fields under `factory_lock:`).

**Sibling pattern:** `crates/hook-plugins/validate-artifact-path/` is the closest
existing analogue — a native-WASM PreToolUse guard using `host::read_file` and
`on_error = "continue"`. Read its `src/lib.rs` and `Cargo.toml` before implementing T-2/T-3.

## Architecture Compliance Rules

From ADR-016, ADR-019, ADR-020, ADR-025:

1. **`async = false` is a correctness requirement.** ADR-019 CI lint invariant:
   `on_error=block ⇒ async=false`. For `verify-factory-lock`, the blocking is the
   entire purpose — `async = true` would silently reduce it to a no-op blocker.
   Both registry entries MUST have `async = false`.

2. **BOTH capability blocks AND `env_allow` are REQUIRED on BOTH entries.** Omitting
   `read_file` or `exec_subprocess` block causes `CapabilityDenied` graceful-degrade — a
   silent no-op guard (ADR-025 D2 Rationale §Capability blocks must be enumerated).
   Omitting `env_allow` from `exec_subprocess` causes `env_clear()` on the git subprocess,
   making `git config user.email` return empty output, triggering `IdentityResolutionFailed`
   graceful-degrade — also a silent no-op guard. Both failure modes are non-obvious because
   the guard still runs and still returns Continue; it just never blocks. The canonical
   `env_allow` value is `["HOME", "GIT_CONFIG_GLOBAL", "XDG_CONFIG_HOME"]`. The implementer
   MUST NOT scaffold the registry entry from a minimal template — the sibling crates do not
   use exec_subprocess so they provide no `env_allow` precedent to copy from. See AC-009 and
   AC-016 for the registry assertion tests that enforce this.

3. **`on_error = "continue"` (fail-open) is correct for an efficiency-class lock.**
   ADR-025 §Decision 7 + Kleppmann §8 distinction: a crashing guard that blocks all
   writes is worse than a missed block (which is bounded by S-17.01's CAS push safety net).

4. **No `regex` crate in the frontmatter scanner.** The frontmatter `factory_lock`
   scanner MUST use manual line-by-line scan within the `---\n` delimited region.
   Using `regex` adds ~200–600 KiB WASM binary bloat for a fixed-format parse that
   requires no regex. This is the same discipline as `validate-trajectory-tail` (ADR-025
   does not cite regex — the format is known and fixed).

5. **`HOST_ABI_VERSION = 1` is unchanged.** The dispatcher binary requires no changes
   for this story (ADR-025 Decision 1). `host::read_file` and `host::exec_subprocess`
   with `binary_allow = ["git"]` are already present in ABI version 1.

6. **Guard is read-only at runtime.** The guard MUST NEVER write STATE.md (BC-4.13.001
   Invariant 4). Writing the `factory_lock` block is exclusively state-manager's
   responsibility (TD-VSDD-053).

7. **Block path is synchronous.** `block_intent = true` is only effective when the
   plugin is in the sync-group (`async = false`). The block decision aggregates at
   `executor.rs:100–117`. Verified by T-1 canonical grep.

## Library and Framework Requirements

| Dependency | Version | Usage |
|------------|---------|-------|
| `vsdd-hook-sdk` | workspace version (per `validate-artifact-path/Cargo.toml`) | `PluginPayload`, `HookResult`, `host::read_file`, `host::exec_subprocess`, `host::log_warn` |
| `chrono` | workspace version (if present) OR `std::time::SystemTime` | ISO-8601 timestamp parsing and `now()` comparison |
| No other new dependencies | — | — |

**Version pin guidance:** Match the exact `vsdd-hook-sdk` version used by the sibling
crate `crates/hook-plugins/validate-artifact-path/Cargo.toml`. Do NOT introduce `regex`
(see Architecture Compliance Rule 4). For timestamp parsing: prefer `std::time::SystemTime`
if a WASM-compatible ISO-8601 parser is not already in the workspace; otherwise use
the workspace `chrono` version if present. Document the choice in the crate's `Cargo.toml`
comments.

**Forbidden dependencies:**
- `regex` crate in `verify-factory-lock` (WASM bloat; manual scan mandated)
- `serde_yaml` or `serde_norway` (full YAML parser overkill for fixed-format frontmatter)
- Any crate that does not compile to `wasm32-wasip1`
- Any dependency on `factory-dispatcher` internals (the guard is a plugin, not a dispatcher extension)

## File Structure Requirements

| File | Action | Purpose |
|------|--------|---------|
| `crates/hook-plugins/verify-factory-lock/Cargo.toml` | CREATE | New crate manifest (`crate-type = ["cdylib"]`; `vsdd-hook-sdk` + optionally `chrono`) |
| `crates/hook-plugins/verify-factory-lock/src/lib.rs` | CREATE | Plugin entry point: `on_pre_tool_use` + frontmatter scanner + block logic |
| `crates/hook-plugins/verify-factory-lock/src/lib.rs` (tests) | CREATE | `#[cfg(test)]` unit tests with mock host ABI (AC-001..AC-015) |
| `plugins/vsdd-factory/hook-plugins/verify-factory-lock.wasm` | CREATE | Compiled WASM artifact (T-6 output) |
| `plugins/vsdd-factory/hooks-registry.toml` | MODIFY | Add two `[[hooks]]` entries (Edit|Write|Agent arm + Bash arm) with both capability blocks |
| `Cargo.toml` (workspace root) | MODIFY | Add `crates/hook-plugins/verify-factory-lock` to workspace `members` array |
| `plugins/vsdd-factory/tests/verify-factory-lock/` | CREATE | Bats test directory with T-1..T-9 test files + fixture STATE.md variants |

## Token Budget Estimate

| Component | Estimated Tokens |
|-----------|-----------------|
| Story spec (this file) | ~8,000 |
| BC-4.13.001 (full guard spec) | ~9,000 |
| BC-5.40.001 (schema reference for scanner alignment) | ~5,500 |
| ADR-025 v1.2 (Decisions 1, 2, 3, 4, 7, 9, 10) | ~10,000 |
| `crates/hook-plugins/validate-artifact-path/src/lib.rs` (sibling pattern) | ~4,000 |
| `plugins/vsdd-factory/hooks-registry.toml` (existing + new entries) | ~6,000 |
| `crates/hook-sdk/src/lib.rs` (host ABI reference) | ~3,000 |
| `crates/factory-dispatcher/src/executor.rs` (block path) | ~4,000 |
| New crate source + tests (estimated) | ~6,000 |
| Bats test files (T-1..T-9) | ~5,000 |
| Tool outputs (cargo build, test, greps) | ~4,000 |
| **Total** | **~64,500** |

**Budget usage:** ~64,500 / ~200,000 ≈ 32%. Marginally above the 20–30% ceiling.
**Recommendation:** If the implementer's context is close to limit during T-3 (plugin
implementation), split the session: implement + unit-test in session A; bats tests +
registry in session B. The split point is after T-6 (WASM compiled successfully).

## Red Gate Test Table

| Test Name | BC Clause | Pass Condition | Initial State (Red Gate) |
|-----------|-----------|---------------|--------------------------|
| `test_BC_4_13_001_foreign_unexpired_lock_blocks_with_all_five_fields` | PC1 | Block + 5 fields in message | FAIL: crate doesn't exist |
| `test_BC_4_13_001_expired_lock_returns_continue` | PC2 | Continue on past `expires_at` | FAIL: no expiry check |
| `test_BC_4_13_001_self_held_lock_returns_continue` | PC3 | Continue when holder==self | FAIL: no identity check |
| `test_BC_4_13_001_malformed_block_returns_continue_with_log_warn` | PC4 | Continue + log_warn | FAIL: no parse logic |
| `test_BC_4_13_001_read_file_host_error_returns_continue` | PC6 | Continue on HostError | FAIL: no error handling |
| `test_BC_4_13_001_git_subprocess_failure_returns_continue` | PC7 | Continue on subprocess fail | FAIL: no subprocess call |
| `test_BC_4_13_001_capability_denied_graceful_degrades_to_continue` | Invariant 6 | Continue + log_warn on CapabilityDenied | FAIL: no capability handling |
| `test_BC_4_13_001_registry_has_two_entries_with_both_capability_blocks` | Invariant 5 / AC-009 | Production `hooks-registry.toml` grep: both `verify-factory-lock` entries present with both `read_file` + `exec_subprocess` capability blocks | FAIL: entries not added yet |
| `test_BC_4_13_001_bash_factory_artifacts_push_blocked_when_foreign_lock` | T-6 (D9) | Block on push-matching Bash | FAIL: no Bash arm |
| `test_BC_4_13_001_non_push_bash_returns_continue_immediately` | T-7 (D9) | Continue on non-push Bash | FAIL: no push-regex filter |
| `test_BC_4_13_001_registry_exec_subprocess_has_env_allow_home` | Invariant 5 (AC-016) | Both exec_subprocess blocks contain `env_allow` with `HOME` | FAIL: entries not added yet; env_allow not present |
| `test_BC_4_13_001_registry_both_entries_async_false` | EC-010 / AC-014 | Production registry grep: both `verify-factory-lock` and `verify-factory-lock-bash` entries have `async = false` | FAIL: entries not added yet |
| `test_BC_4_13_001_is_expired_now_equals_expires_at_is_expired` | EC-002 / AC-015 | Pure-helper boundary test: `now == expires_at` → `LockExpired` → Continue (not Block) | FAIL: boundary comparison not implemented |
| `test_BC_4_13_001_crlf_state_md_foreign_lock_blocks` | LOW sweep | A STATE.md with CRLF line endings holding a foreign unexpired lock MUST return Block (guard handles CRLF correctly, does not silently treat CRLF-terminated frontmatter as unlocked) | FAIL: CRLF handling not verified |
| `test_BC_4_13_001_missing_closing_delimiter_returns_continue` | EC-013 | STATE.md with no closing `---` delimiter and a body-resident `factory_lock`-shaped block → `MalformedLockBlock` → Continue (no over-block; guard does not scan past the frontmatter region) | FAIL: parse boundary not implemented |

**T-8 bats assertion note (test-writer):** The T-8 (capability-omitted registry entry graceful-degrades) bats assertion MUST be strengthened to verify the `capability_denied` `log_warn` message in addition to the exit-code/Continue behavior. Asserting exit-code-only is insufficient — a guard that silently continues without emitting the warning is also non-compliant (BC-4.13.001 Invariant 6 requires `host::log_warn("capability_denied: ...")`). The bats T-8 assertion MUST grep the dispatcher advisory log for the `capability_denied` warn string, not rely on exit code alone.

## Demo Plan

| AC | Demo Command | Expected Output |
|----|-------------|-----------------|
| AC-001 (T-2) | bats `verify-factory-lock/T-2-foreign-unexpired.bats` | Block output with holder email, locked_at, expires_at, time_remaining, `/factory-unlock --force` |
| AC-002 (T-3) | bats `verify-factory-lock/T-3-expired.bats` | Continue (no block) |
| AC-003 (T-4) | bats `verify-factory-lock/T-4-self-held.bats` | Continue (no block) |
| AC-008/AC-009 | `grep -A8 'verify-factory-lock' plugins/vsdd-factory/hooks-registry.toml` | Two entries; both `async = false`; both `on_error = "continue"`; both capability blocks |
| AC-010 (T-8) | bats `verify-factory-lock/T-8-capability-omitted.bats` | Continue (graceful-degrade) with log_warn |

## Risk

| Risk | Severity | Mitigation |
|------|----------|-----------|
| Capability block omitted from one of the two registry entries | HIGH (SILENT) | T-7 bats T-8 explicitly tests the capability-omitted case; AC-009 tests both entries have both blocks |
| `env_allow` omitted from `exec_subprocess` block | CRITICAL (SILENT) | Guard runs, no crash, no capability error — git returns empty, guard degrades to Continue; foreign lock never enforces. AC-016 `test_BC_4_13_001_registry_exec_subprocess_has_env_allow_home` is the literal-shell assertion that catches this at test time. Verified empirically (issue #170). |
| `async = true` misconfiguration causes silent no-op | HIGH (SILENT) | AC-014 registry grep asserts `async = false` on both entries; T-1 documents the sync-group executor path |
| Frontmatter scanner fails on edge-case YAML (quoted colons, multiline) | MEDIUM | EC-013 + EC-004/EC-005 test vectors cover malformed cases; scanner MUST fail-open to Continue, never Block on ambiguity |
| WASM binary size exceeds fuel budget | MEDIUM | No `regex` crate (Architecture Compliance Rule 4); crate should be comparable in size to `validate-artifact-path.wasm` |

## Changelog

| Version | Date | Description |
|---------|------|-------------|
| 1.4 | 2026-06-11 | AC-001/AC-002 sibling-clause boundary-operator propagation (story-writer; F-ADV-S1702-001; issue #170). AC-001 blocking condition corrected `now ≤ expires_at` → `now < expires_at` (boundary is expired, not blocking; matches BC-4.13.001 PC1). AC-002 expired condition corrected `now > expires_at` → `now >= expires_at` (boundary treated as expired → Continue; matches BC-4.13.001 PC2). Full TD-VSDD-060 sibling sweep confirmed zero remaining stale boundary operators in any story clause. Outcomes unchanged. STORY-INDEX pending v1.4 bump. [Label fix: BC-4.13.001 version label corrected to v1.2 in Behavioral Contracts table (was v1.1) and in BC gate line (was v1.0); AC content was already aligned to v1.2 semantics; no behavioral change.] |
| 1.3 | 2026-06-11 | Boundary-semantics wording correction in AC-015, EC-002, and T-3 (story-writer; issue #170). Prior wording cited `now > expires_at` (strict greater-than) as the expired condition — self-contradictory because `now == expires_at` is NOT greater-than, so the guard would have BLOCKED at the exact boundary rather than returning Continue. Corrected to `now >= expires_at`: the lock is expired when `now >= expires_at`, including the exact boundary `now == expires_at` (Continue / LockExpired). Outcome unchanged — boundary → Continue. STORY-INDEX pending v1.3 bump. Amended in place: removed trace to deleted redundant boundary test `test_BC_4_13_001_expires_at_exact_boundary_treated_as_expired` from AC-015 Test line and Red Gate Test Table; `test_BC_4_13_001_is_expired_now_equals_expires_at_is_expired` is the sole EC-002/AC-015 boundary trace. |
| 1.2 | 2026-06-11 | EC-ID realignment to BC-4.13.001 canonical table + new Red Gate tests (story-writer; F-S1702-001/002/003 + LOW sweeps; issue #170). EC-014 concurrent-Edit/CAS row added (BC canonical EC-014, previously missing in story). EC-014 (exec_subprocess timeout) renamed EC-015 to match BC. EC-015 (env_allow absent) renamed EC-016 to match BC. All story EC-NNN IDs now match BC EC-NNN for the same concept. Behavioral Contracts table BC version updated v1.0→v1.1. 5 new Red Gate test rows registered: `test_BC_4_13_001_registry_has_two_entries_with_both_capability_blocks` (AC-009/Inv5), `test_BC_4_13_001_registry_both_entries_async_false` (AC-014/EC-010), `test_BC_4_13_001_is_expired_now_equals_expires_at_is_expired` (AC-015/EC-002), `test_BC_4_13_001_crlf_state_md_foreign_lock_blocks` (LOW sweep), `test_BC_4_13_001_missing_closing_delimiter_returns_continue` (EC-013). T-8 bats assertion strengthened: must verify `capability_denied` log_warn, not exit-code-only. STORY-INDEX pending v3.88→v3.89. |
| 1.1 | 2026-06-11 | env_allow production-correctness spec gap (story-writer; issue #170; BC-4.13.001 amended; ADR-025 amended). AC-009 canonical TOML updated: `env_allow = ["HOME", "GIT_CONFIG_GLOBAL", "XDG_CONFIG_HOME"]` added to BOTH `[hooks.capabilities.exec_subprocess]` blocks. AC-016 added: registry env_allow assertion (`test_BC_4_13_001_registry_exec_subprocess_has_env_allow_home`). Architecture Compliance Rule 2 updated to require env_allow alongside capability blocks. EC-015 added (env_allow-absent silent no-op). Risk table: CRITICAL row added for env_allow omission. Bats synthetic-registry note added (test-writer must carry env_allow in `_write_full_registry`). T-5 CRITICAL annotation added. STORY-INDEX pending v3.88→v3.89. |
| 1.0 | 2026-06-10 | Initial authoring (story-writer; brownfield-backfill issue #170; ADR-025 v1.2 D1/D2/D9; BC-4.13.001 v1.0). 15 ACs tracing PC1–PC8 + Invariants 5/6/7/8 + EC-002/EC-010/EC-011 + D9 bats T-1..T-9. Wave 2; points 8; tdd_mode strict; depends_on [S-17.01]; blocks [S-17.03]. |
