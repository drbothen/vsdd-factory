---
document_type: story
level: implementation
story_id: "S-25.03"
epic_id: "E-25"
version: "1.0"
last_amended: "2026-08-30"
status: draft
# BC status: pending PO authorship
producer: story-writer
timestamp: 2026-08-30T00:00:00Z
phase: 3
inputs:
  - .factory/feature-delta/validation-integrity-layer1/F1-delta-analysis.md
  - .factory/specs/architecture/decisions/ADR-047-indeterminate-outcome-model-durable-mutation-marker-next-advance-gate.md
  - .factory/stories/S-25.01-dispatcher-indeterminate-outcome-layer1.md
  - .factory/stories/S-25.02-artifact-sharding-layer2.md
input-hash: "2e14ed1"
traces_to: .factory/stories/epics/E-25-validation-integrity.md
points: "12"
depends_on: [S-25.02]
blocks: []
behavioral_contracts: []
# BC status: pending PO authorship — BCs to be authored (product-owner) when S-25.02 merges
# and this story is activated. Layer-3 bounded-window behavioral contracts depend on the
# sharding architecture from Layer 2 being finalized and in production.
verification_properties: []
priority: "P2"
cycle: v1.0-brownfield-backfill
wave: null
target_module: crates/factory-dispatcher
subsystems: [SS-01, SS-04, SS-07]
estimated_days: null
assumption_validations: []
risk_mitigations: []
tdd_mode: strict
---

> **REGISTERED BACKLOG — Deliberate Feature-Ordering**
>
> S-25.03 is REGISTERED BACKLOG under E-25 per CLAUDE.md Canonical Principle §2 (feature
> ordering is the only acceptable speed lever). It is NOT a tech-debt-register entry and MUST
> NOT be silently deferred. Activation occurs when S-25.02 merges to develop and the sharding
> architecture is in production. At activation, product-owner authors BCs, architect elaborates
> the bounded-window query API, and story-writer promotes this story to `status: ready`.

> **tdd_mode: strict** — Full TDD Iron Law enforced: all non-trivial function bodies use `todo!()`;
> Red Gate density check ≥0.5 required before Step 4 dispatch.

# S-25.03: Bounded Validator Windows Layer 3 — Validators Read from Shards via Bounded Lookups

## Narrative

- **As a** factory pipeline operator who has Layer 2 (artifact sharding) deployed
- **I want** all validators to read BOUNDED WINDOWS from individual shards rather than scanning
  whole files, with trusted-boundary-checkpoints carrying cross-shard invariants (changelog
  monotonicity, trajectory-tail) across shard boundaries, and the regression-gate's own
  state file bounded and rotated under the same shard rules
- **So that** fuel-exhaustion INDETERMINATE events from the read path are eliminated by
  construction — each validator invocation reads at most one bounded shard, completing within
  the fuel budget even as the corpus grows indefinitely

## Context / Feature Summary

**Registered at:** F3 story-decomposition burst, validation-integrity-layer1 cycle,
2026-08-30 (story-writer). Source: ADR-047 §Decision 8b (Ratified Future Phase Layer 3).

**Why Layer 3 follows Layer 2 (not concurrent):**
Layer 3 requires the sharding infrastructure from Layer 2 to exist (shard files, shard index,
bounded shard sizes). Validators cannot read from shards until shards exist and their format is
finalized. The bounded-window query API (how validators navigate from a shard index to a specific
shard, and how checkpoint records are structured) is a Layer-2 output that Layer-3 consumes.

**Two categories of bounded-window validators (from ADR-047 §D8b):**

*Window-local validators (safe — no cross-shard state needed):*
- `validate-closes-completeness` → current burst shard only (Closes blocks are local to a burst)
- `validate-factory-path-staging` → tool params (already bounded at Layer 1 Cohort A)

*Whole-corpus validators requiring trusted-boundary-checkpoint carry-forward:*
Cross-shard invariants (e.g., changelog monotonicity — sequence numbers must increase across
shard boundaries; trajectory tail — the STATE.md banner must match the prior shard's last entry)
require a trusted-boundary-checkpoint carried forward from the prior SEALED shard. Each sealed
shard's checkpoint record contains: `(last_validated_seq, last_shard_id, last_shard_digest, summary)`.
The validator for shard N reads the checkpoint from shard N-1 (sealed) and validates the seam.
This follows RFC 9162 (Certificate Transparency checkpoint-carry-forward) and the RocksDB
two-layer model: per-file local check + separate global manifest check.

**Retroactive edits (our cycle-specific requirement):**
Our cycle performs retroactive remediations. If a remediation edits a SEALED shard, that shard
must be re-validated and its checkpoint recomputed; downstream shard seam validations also re-run.
This MUST be explicit in the Layer 3 implementation — a "sealed shard" is NOT immutable in our
cycle, and the validator design must handle re-sealing.

**regression-gate state-file reform:**
The regression-gate plugin failed to persist its own state file 22 times due to OutputTooLarge
(from the F1 forensics). Layer 3 reforms the regression-gate to write its state file to a
bounded-rotated file under the same shard rules. After this reform, regression-gate can be flipped
to fail-closed (if not already done in Layer 2) with its STATE-FILE WRITE also guaranteed to
succeed within the OutputTooLarge envelope.

## Acceptance Criteria

> **PRELIMINARY — Requires product-owner BC authorship at activation.**
> ACs below are PROVISIONAL based on ADR-047 §Decision 8b ratified content. Product-owner
> MUST author formal BCs with canonical PC numbering before this story advances to ready.

### AC-001: Window-local validators read only the current shard (traces to ADR-047 §D8b — BC-TBD)

`validate-closes-completeness` and `validate-factory-path-staging` MUST read only the current
shard file (or tool params respectively). Neither validator MUST scan the full shard history.
Each invocation MUST complete within the single-shard fuel budget established by Layer 2.

### AC-002: Whole-corpus validators use checkpoint carry-forward (traces to ADR-047 §D8b — BC-TBD)

Validators for cross-shard invariants (changelog monotonicity, trajectory-tail) MUST read:
(a) the current shard's data, AND (b) the boundary checkpoint from the prior sealed shard.
The checkpoint record MUST contain: `last_validated_seq`, `last_shard_id`, `last_shard_digest`,
`summary`. The seam between shard N-1 and shard N MUST be validated using this checkpoint.

### AC-003: Re-sealing of retroactively edited shards (traces to ADR-047 §D8b retroactive-edits — BC-TBD)

When a remediation edits a sealed shard, the shard MUST be re-validated and its checkpoint
recomputed. Downstream shard seam validations (shard N+1, N+2, ...) MUST also re-run to
propagate the new checkpoint values. The shard manager MUST NOT treat sealed shards as immutable.

### AC-004: regression-gate state-file bounded and rotated (traces to ADR-047 §D8b, F1 §10 — BC-TBD)

The regression-gate plugin MUST write its state file to a bounded-rotated file (under the same
shard rules as the cycle artifacts). The state file MUST NOT exceed the single-shard fuel budget.
After this reform, regression-gate MUST successfully persist its state file (the 22× OutputTooLarge
failure mode is eliminated by construction).

### AC-005: OutputTooLarge read-path elimination (traces to ADR-047 §D8b, ADR-047 §D6 — BC-TBD)

After Layer 3, `host_fn_returned_output_too_large` events from the read path (read_file,
read_prefix host functions) MUST drop to zero on normal pipeline writes to sharded artifacts.
The INDETERMINATE outcome from OutputTooLarge (introduced in Layer 1 for fail-closed plugins)
SHOULD become a dead code path for the sharded read-path. (The Store-flag mechanism remains for
defense-in-depth; the root-cause elimination is confirmed by monitoring plugin.indeterminate
events with cause=output-too-large in production after Layer 3.)

### AC-006: Full regression suite stays green (traces to ADR-047 §D7 — BC-TBD)

`cargo test --workspace --all-targets` and `plugins/vsdd-factory/tests/run-all.sh` MUST stay
green after Layer 3 lands.

## Behavioral Contracts

> TBD — pending product-owner authorship at story activation (when S-25.02 merges).
> Expected BC section: BC-1.18.006+ or new section covering bounded-window read semantics,
> checkpoint carry-forward invariants, re-sealing protocol, regression-gate state-file reform.

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| Bounded-window query API | `crates/factory-dispatcher/src/shard_reader.rs` (or within shard manager — TBD) | Effectful (filesystem: reads specific shard + checkpoint) |
| Checkpoint record format | `crates/factory-dispatcher/src/shard_checkpoint.rs` (TBD) | Pure (data structure + serialization) |
| regression-gate state-file rotation | `crates/hook-plugins/regression-gate/src/state.rs` (TBD) | Effectful (filesystem: writes bounded state file) |
| Re-sealing coordinator | `crates/factory-dispatcher/src/` (TBD) | Effectful (validates + re-computes checkpoints on edit) |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| Checkpoint record computation | Pure-core | Hashing + struct construction; no I/O |
| Shard seam validation | Pure-core (given loaded checkpoint + shard data) | Pure once data is loaded |
| Bounded-window query | Effectful (read) | Reads shard file from filesystem |
| regression-gate state-file write | Effectful (write) | Writes to bounded rotated state file |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | First shard in a series (no prior sealed shard; no checkpoint) | Validator validates the first shard as a complete corpus; writes initial checkpoint for shard 0 |
| EC-002 | Retroactive edit of a sealed shard N | Re-validate shard N; recompute checkpoint N; re-validate seam N→N+1; cascade to all downstream seams |
| EC-003 | Checkpoint file for shard N-1 is absent or corrupt | Fail-loud; block dispatch until operator restores checkpoint (analogous to missing marker in Layer 1) |
| EC-004 | regression-gate state-file write exceeds shard cap | Roll to new state-file rotation BEFORE writing (same roll-before-write rule as artifact shards; AC-004 reform) |
| EC-005 | Validator reads two shards (current + prior checkpoint) but both are at cap | Each shard read is independently bounded; combined fuel = 2 × single-shard fuel; must remain within fuel budget. Architecture must account for this in cap derivation |

## Token Budget Estimate (MANDATORY)

> **PRELIMINARY — to be finalized at story activation.**

| Context Source | Estimated Tokens |
|---------------|-----------------|
| Story spec (S-25.03) | 3,500 |
| ADR-047 §Decision 8b (Layer 3 ratified future phase) | 3,000 |
| S-25.02 (completed Layer-2 story — sharding architecture context) | 4,000 |
| S-25.01 (Layer-1 precedent — INDETERMINATE outcome + Store flag) | 2,500 |
| Architecture elaboration (bounded-window API + checkpoint format — to be authored at activation) | 5,000 |
| BCs (to be authored at activation — estimated 3-4 BCs) | 4,000 |
| Affected code: shard_reader.rs, regression-gate state reform, validator amendments | 6,000 |
| RFC 9162 precedent reference (checkpoint-carry-forward pattern) | 500 |
| **Total (estimated)** | **~28,500** |
| Agent context window | 200K (Sonnet) |
| **Budget usage (est.)** | **~14.3%** |

## Tasks (MANDATORY)

> **PRELIMINARY — to be refined at activation. Tasks below are scope anchors derived from
> ADR-047 §Decision 8b ratified content.**

1. [ ] (Activation prerequisite) Product-owner: author Layer 3 BCs (BC-1.18.006+ or adjacent)
2. [ ] (Activation prerequisite) Architect: elaborate bounded-window query API, checkpoint
       record schema, re-sealing coordinator design, regression-gate state-file rotation scheme
3. [ ] Implement bounded-window query API (reads single shard + checkpoint from shard index)
4. [ ] Implement checkpoint record format + seam validation for whole-corpus validators
5. [ ] Implement re-sealing coordinator (retroactive shard edit → checkpoint cascade)
6. [ ] Reform regression-gate state-file to bounded-rotated under shard rules (AC-004)
7. [ ] Migrate window-local validators (validate-closes-completeness) to read only current shard
8. [ ] Migrate whole-corpus validators (validate-changelog-monotonicity, validate-trajectory-tail)
       to use checkpoint carry-forward
9. [ ] Write RED Gate test stubs (failing todo!() bodies) for all Layer-3 BCs
10. [ ] Verify OutputTooLarge read-path events drop to zero in integration tests (AC-005)
11. [ ] Run cargo test --workspace --all-targets and plugins/vsdd-factory/tests/run-all.sh
12. [ ] Add CHANGELOG entry under [Unreleased] > Changed: "Validators: bounded-window reads from shards via checkpoint carry-forward; regression-gate state-file bounded+rotated (ADR-047 Layer 3)"

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-25.01 (Layer 1) | INDETERMINATE outcome + Store flag + marker + next-advance gate. Layer-1 cause=output-too-large path for fail-closed. | indeterminate_marker.rs atomic write. validate-unvalidated-mutation-marker WASM plugin. | The Store-flag OutputTooLarge detection (invoke.rs) remains active even after Layer 3 — it is defense-in-depth. Do NOT remove it when bounded reads eliminate the read-path root cause. |
| S-25.02 (Layer 2 — predecessor) | Shard rotation + roll-before-write + shard index + Cohort B flip. Shard cap formula empirically calibrated. | Shard append helper. Atomic shard index publication. Retention/compaction policy. | Layer 3 validators read at most TWO shards per invocation (current + prior checkpoint). The shard cap formula must account for this doubled per-invocation read budget when calculating the safe per-shard size. See EC-005. |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| Sealed shards are NOT immutable — re-sealing protocol required for retroactive edits | ADR-047 §D8b retroactive-edits clause; CLAUDE.md Canonical Principle §1 | AC-003; architect review at elaboration |
| Checkpoint carry-forward is REQUIRED for all whole-corpus invariants | ADR-047 §D8b; RFC 9162 precedent | AC-002; BC authorship must enumerate covered invariants |
| Store-flag OutputTooLarge mechanism (Layer 1) MUST NOT be removed | ADR-047 §D6 defense-in-depth; Layer 3 eliminates root cause, not the detector | Architecture Compliance Rules column in BC; Task 9 includes a guard test |
| regression-gate state-file reform is a MANDATORY subtask of Layer 3 | ADR-047 §D8b explicit mention; F1 §10 (22× OutputTooLarge failure confirmed) | AC-004; must ship in same PR |
| Bounded-window fuel budget: two-shard invocation (current + checkpoint) must fit within fuel cap | ADR-047 §D8b; EC-005 above | Cap derivation must use 2× single-shard read cost, not 1×. Architect must confirm at elaboration. |

## Library & Framework Requirements (MANDATORY)

> **PRELIMINARY — to be finalized at architecture elaboration.**

| Tool | Version | Purpose |
|------|---------|---------|
| wasmtime (workspace) | 46.0.2 (per S-21.12) | WASM runtime for updated validator plugins |
| serde / toml (workspace) | workspace versions | Checkpoint record TOML serialization |
| sha2 or blake3 (TBD) | TBD | Shard digest computation for checkpoint records |

## File Structure Requirements (MANDATORY)

> **PRELIMINARY — to be finalized at architecture elaboration.**

| File | Action | Purpose |
|------|--------|---------|
| `crates/factory-dispatcher/src/shard_reader.rs` | create | Bounded-window query API; reads (shard, checkpoint) pair |
| `crates/factory-dispatcher/src/shard_checkpoint.rs` | create | Checkpoint record format + seam validation |
| `crates/factory-dispatcher/src/shard_reseal.rs` | create | Re-sealing coordinator for retroactive shard edits |
| `crates/hook-plugins/regression-gate/src/state.rs` | modify | Reform state-file to bounded-rotated under shard rules |
| Relevant validator plugin crates | modify | Migrate to bounded-window read API |
| `CHANGELOG.md` | modify | Mandatory CHANGELOG delivery |

## Changelog

| Version | Date | Description |
|---------|------|-------------|
| 1.0 | 2026-08-30 | Initial registration as REGISTERED BACKLOG story (story-writer; F3 story-decomposition burst, validation-integrity-layer1). Source: ADR-047 §D8b. Preliminary ACs, tasks, architecture anchors. Status: draft. BCs pending PO authorship at activation. |
