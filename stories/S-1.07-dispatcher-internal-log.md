---
document_type: story
level: ops
story_id: "S-1.07"
epic_id: "E-1"
version: "1.1"
status: merged
producer: story-writer
timestamp: 2026-04-25T00:00:00
phase: 1.8
inputs:
  - .factory/stories/v1.0/S-1.7-dispatcher-internal-log.md
  - .factory/specs/architecture/ARCH-INDEX.md
input-hash: "48a1fc5"
traces_to: .factory/specs/domain-spec/capabilities.md
cycle: v1.0.0-greenfield
points: "3"
depends_on: ["S-1.01", "S-1.02"]
blocks: ["S-1.08", "S-2.07", "S-4.07"]
behavioral_contracts: []
verification_properties: []
priority: "P0"
wave: 3
target_module: crates/factory-dispatcher/src/internal_log.rs
subsystems: ["SS-01", "SS-03"]
estimated_days: 2
assumption_validations: []
risk_mitigations: []
---

# S-1.07: dispatcher-internal.jsonl writer

**Epic:** E-1 — Dispatcher Foundation
**Phase:** Phase 1 — Dispatcher foundation
**Milestone:** `1.0.0-beta.1`
**Tier:** B.x
**Blocks:** S-1.08, S-2.07, S-4.07
**Blocked by:** S-1.01, S-1.02
**Estimated effort:** S

## Narrative
- **As a** factory operator debugging dispatcher behavior
- **I want to** inspect `.factory/logs/dispatcher-internal.jsonl`
- **So that** every lifecycle event and internal telemetry is always persisted, regardless of sink configuration

## Goal

Implement the always-on internal self-telemetry log at
`.factory/logs/dispatcher-internal.jsonl`. Every lifecycle event and
`internal.*` event written by the dispatcher goes here in addition to
(or instead of) user-configured sinks. This log is the operator's
reliable fallback for debugging.

## Acceptance criteria

- [ ] `internal_log.rs` implements `InternalLogger` with JSONL append to `.factory/logs/dispatcher-internal.jsonl`
- [ ] Log file path derived from `CLAUDE_PLUGIN_ROOT` env var
- [ ] All 17+ internal event constants defined (`dispatcher.started`, `dispatcher.completed`, `internal.dispatcher_error`, `plugin.invoked`, etc.)
- [ ] Each JSONL entry: `{"timestamp":"...", "type":"...", "trace_id":"...", "attrs":{...}}`
- [ ] `InternalLogger::emit(event)` is synchronous (no async buffering for reliability)
- [ ] Log file created on first write if missing; parent dirs created
- [ ] Daily log rotation: new file per calendar day (UTC) e.g. `dispatcher-internal.2026-04-25.jsonl`
- [ ] Integration test: invoke dispatcher, verify expected events in log

## Tasks

1. Create `crates/factory-dispatcher/src/internal_log.rs`.
2. Define all `INTERNAL_*` event-type constants.
3. Implement `InternalLogger::new(plugin_root: &Path) -> Self` and `emit()`.
4. Add log rotation by UTC date.
5. Create parent dirs on first write with `fs::create_dir_all`.
6. Write integration test.

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| internal_log.rs | crates/factory-dispatcher/src/internal_log.rs | effectful-shell (filesystem append) |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | `CLAUDE_PLUGIN_ROOT` not set | Use fallback path or emit error to stderr |
| EC-002 | Log directory does not exist | Create with `fs::create_dir_all` |
| EC-003 | Disk full on log write | Log error to stderr; do not crash dispatcher |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| internal_log.rs | effectful-shell | Writes to filesystem |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~1,000 |
| internal_log.rs | ~700 |
| Integration test | ~800 |
| Tool outputs overhead | ~300 |
| **Total** | **~2,800** |
| Agent context window | 200K for Sonnet |
| **Budget usage** | **~1.4%** |

## Tasks (MANDATORY)

1. [ ] Create `internal_log.rs` with `InternalLogger` struct
2. [ ] Define all 17+ internal event type constants
3. [ ] Implement daily log rotation by UTC date
4. [ ] Implement parent directory auto-creation
5. [ ] Write integration test verifying event emission

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-1.02 | `dispatcher_trace_id` UUID per invocation | Propagated to all events | Assign early in main() |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| Internal log is always-on (not config-gated) | BC-1.06 internal log contracts | Integration test without any sink config |
| Daily rotation by UTC date | BC-1.06 log rotation | Integration test at midnight boundary |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| chrono or std::time | workspace-pinned | UTC timestamp for log rotation |
| serde_json | workspace-pinned | JSONL serialization |

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| crates/factory-dispatcher/src/internal_log.rs | create | InternalLogger implementation |
| crates/factory-dispatcher/tests/internal_log.rs | create | Integration tests |
