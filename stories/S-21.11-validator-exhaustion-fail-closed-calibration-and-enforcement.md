---
document_type: story
level: implementation
story_id: "S-21.11"
epic_id: "E-21"
version: "1.9"
status: draft
producer: story-writer
timestamp: 2026-08-06T00:00:00Z
phase: brownfield-backfill
inputs:
  - .factory/specs/architecture/decisions/ADR-039-validator-failure-policy-resource-exhaustion-fail-closed.md
  - .factory/research/wasm-fuel-exhaustion-detection.md
  - plugins/vsdd-factory/hooks-registry.toml
input-hash: "dda0a2f"
traces_to: .factory/specs/architecture/decisions/ADR-039-validator-failure-policy-resource-exhaustion-fail-closed.md
points: "16"
depends_on: [S-21.10]
blocks: []
behavioral_contracts: ["BC-1.01.016 v1.3", "BC-1.03.017 v1.7"]
verification_properties: []
priority: "P1"
cycle: v1.0-brownfield-backfill
wave: 6
target_module: crates/factory-dispatcher
subsystems: [SS-01, SS-07]
estimated_days: null
assumption_validations: []
risk_mitigations: []
tdd_mode: strict
---

> **tdd_mode: strict** — Full TDD Iron Law enforced: all non-trivial function bodies use `todo!()`;
> Red Gate density check ≥0.5 required before Step 4 dispatch.

> **Execute:** `/vsdd-factory:deliver-story S-21.11`

# S-21.11: Per-plugin fuel-cap calibration + fail-closed enforcement flip (ADR-039 Phase 3+4 — CWE-636 closure for validator-class WASM plugins)

## Narrative

- **As a** factory pipeline operator whose validator writes are currently approved silently
  when a WASM plugin exhausts its fuel budget
- **I want** each of the six validator-class plugins to have a measured, corpus-calibrated
  `fuel_cap` and an enforced `failure_policy = "fail-closed"` annotation, so that exhaustion
  blocks the write (exit 2) rather than silently approving it
- **So that** CWE-636 is closed for the authorization-class WASM validator tier, the
  pipeline's write-gating guarantee is semantically meaningful, and D-442(e)'s
  size-workaround is superseded by a calibrated budget that accommodates production-scale
  `lessons.md` without exhausting

## Context / Finding Summary

ADR-039 §Context establishes:

1. **The detection mechanism is already correct.** `Trap::OutOfFuel` is distinguished from
   clean completion in `invoke.rs` → `PluginResult::Timeout { cause: TimeoutCause::Fuel }`.
   The defect is enforcement, not detection.

2. **The enforcement defect is CWE-636.** `plugin_fail_closed` returns `false` when
   `on_error == OnError::Continue`, regardless of `TimeoutCause`. The existing deliberate
   test `fail_closed_timeout_with_on_error_continue_is_open` encodes this policy. Any change
   requires revising that test **deliberately** (TD-VSDD-059: not deleting it, not renaming
   it; revising it to cover the new semantics for the `fail-open` configuration case).

3. **Self-lock hazard is live today.** `lessons.md` already exhausts the 10M fuel budget
   (D-442(e); ≤3500 soft / ≤4000 hard line-count workaround). The ADR-039 authoring session
   itself triggered `fail-closed: plugin timed out` blocks from `validate-factory-path-root`,
   `validate-input-hash`, and `validate-template-compliance`. A naive global flip would
   immediately hard-block all `.factory/` writes.

4. **Safe migration ordering (ADR-039 §Decision 3):** S-21.10 (Phase 1, now merged as
   prerequisite) added the `failure_policy` TOML field to the registry schema with a
   `fail-open` default. This story delivers Phase 3 (calibration) + Phase 4 (enforcement
   flip) as a single delivery unit. Phase 3 and Phase 4 MUST be atomic per-plugin (see
   AC-001 gate).

**Targeted validator-class plugins (ADR-039 §Decision 2):**
- `validate-factory-path-root`
- `validate-input-hash`
- `validate-template-compliance`
- `validate-wave-gate-prerequisite`
- `validate-pr-merge-prerequisites`
- `validate-cross-site-correspondence`

## Acceptance Criteria

### AC-001: No `failure_policy = "fail-closed"` entry may carry `fuel_cap < 50_000_000` — standing calibration regression/invariant gate (traces to BC-1.03.017 v1.7 PC8; ADR-039 §Decision 3 atomicity constraint; POLICY 15 gate-outcome discipline; Invariant 7 ordering constraint is mechanically enforced by AC-012/PC11)

A Cargo integration test MUST assert that no `[[hook]]` entry in
`plugins/vsdd-factory/hooks-registry.toml` (the production registry; this is the ONLY scan
target for this gate — test-fixture registries are exempt) carries both
`failure_policy = "fail-closed"` AND `fuel_cap < 50_000_000`. The calibration floor is
50_000_000 (inclusive) per ADR-039 §Decision 4 (`max(observed_max × 1.5, 50M)`); exactly
50_000_000 PASSES the gate (inclusive floor). The factory default of 20_000_000 per
ADR-042 §Decision 2 is below this floor and insufficient for fail-closed annotation.

This gate is a **standing regression/invariant gate** — NOT a red-first TDD gate:
- **GREEN when the live registry contains zero fail-closed entries** (vacuously satisfied
  today; no such entries exist yet in `plugins/vsdd-factory/hooks-registry.toml`)
- **GREEN at final state** after all targeted plugins carry `failure_policy = "fail-closed"`
  with calibrated `fuel_cap >= 50_000_000` (exactly 50_000_000 is the inclusive floor and
  a VALID calibrated value per ADR-039 §Decision 4)
- **RED only if a bad half-state edit** introduces a fail-closed entry with an
  uncalibrated `fuel_cap < 50_000_000`

The genuine red-first TDD gates for this story are AC-009
(`test_all_six_validator_class_plugins_are_fail_closed`, which is RED before Phase 4
annotations land) and AC-002/003/004/005/010 (all RED because the extended 3-arg
`plugin_fail_closed` signature does not compile yet — their RED state is natural, not forced).

The gate test MUST include an in-test **POSITIVE-CONTROL fixture** to prevent false-green
vacuity: inject a hard-coded fail-closed entry with `fuel_cap = 20_000_000` (the factory
default per ADR-042 §Decision 2, strictly below the 50_000_000 inclusive floor → RED)
directly in the test body (NOT in the live registry) and assert the gate logic returns an
error for that fixture. This proves the gate is non-vacuous and cannot silently pass when
the live registry contains zero fail-closed entries.

The gate test MUST ALSO include a **NEGATIVE-CONTROL fixture** (per BC-1.03.017 v1.7 PC8
symmetric extension + POLICY 15 gate-outcome discipline): inject a hard-coded synthetic
fail-closed entry with `fuel_cap = 75_000_000` (above the 50M floor) directly in the test
body and assert the gate logic ACCEPTS / PASSES that fixture (returns no error). This
proves the gate does not over-fire on legitimately calibrated entries and is symmetric:
positive-control confirms rejection of the bad half-state; negative-control confirms
acceptance of the good state.

The gate test MUST ALSO assert the **boundary-pass case**: a synthetic fail-closed entry
with `fuel_cap = 50_000_000` (exactly at the inclusive floor) MUST PASS the gate (no
error), confirming that exactly 50_000_000 is a valid calibrated value per ADR-039
§Decision 4. This prevents the gate from incorrectly rejecting entries at the minimum
valid cap.

**Non-normative note on Invariant 7 (informational only — AC-012/PC11 is the authoritative
mechanical gate for the migration-window ordering constraint):** The Invariant 7
fail-open-window invariant (BC-1.03.017 v1.7 Invariant 7) states that once the extended
decision function is in effect, no `on_error="block"` targeted plugin may remain at
`failure_policy = fail-open`. This gate (`test_no_fail_closed_plugin_with_uncalibrated_cap`)
asserts ONLY the calibration constraint (no `failure_policy = "fail-closed"` entry with
`fuel_cap < 50_000_000`). The migration-window ordering constraint is mechanically enforced
by AC-012 (`test_no_on_error_block_without_fail_closed_when_3arg_executor` / PC11), which
is the MUST gate. This gate SHOULD warn (not pass silently) if it detects a live registry
entry that has `on_error = "block"` (for the five targeted plugins) but lacks
`failure_policy = "fail-closed"` after Phase 4 completes — but this SHOULD-warn is
non-normative; defer to AC-012 for the MUST assertion. Reserve "MUST assert" for AC-012.

- **Test:** `test_no_fail_closed_plugin_with_uncalibrated_cap()` — parses
  `plugins/vsdd-factory/hooks-registry.toml`; for each entry where
  `failure_policy = "fail-closed"`, asserts `fuel_cap >= 50_000_000`; includes a
  POSITIVE-CONTROL fixture (hard-coded synthetic fail-closed entry with
  `fuel_cap = 20_000_000` — the factory default per ADR-042 §Decision 2, strictly below
  the 50_000_000 inclusive floor — in the test body) that asserts the gate fires an error
  on that fixture; includes a NEGATIVE-CONTROL fixture (synthetic fail-closed entry with
  `fuel_cap = 75_000_000` in the test body) that asserts the gate ACCEPTS the fixture
  (no error); includes a boundary-pass fixture (synthetic fail-closed entry with
  `fuel_cap = 50_000_000`) that asserts the gate PASSES (exactly at inclusive floor);
  MUST FAIL only if any live production-registry entry has
  `failure_policy = "fail-closed"` with `fuel_cap < 50_000_000`

### AC-002: Exhaustion + fail-closed → block (exit 2) (traces to BC-1.03.017 PC1; BC-1.01.016 PC1/PC5; ADR-039 §Decision 6 test case 1)

A unit test MUST invoke the dispatcher's `plugin_fail_closed` (or its replacement function)
with `PluginResult::Timeout { cause: TimeoutCause::Fuel, ... }` and
`failure_policy = FailurePolicy::FailClosed`. The function MUST return `true` (block
intent). The unit test reads `RegistryEntry.failure_policy` (traces to BC-1.01.016 PC1:
`failure_policy = "fail-closed"` parses to `FailurePolicy::FailClosed` — the field is
parseable from TOML and accessible on `RegistryEntry`; and BC-1.01.016 PC5:
`failure_policy` is an independent axis in `RegistryEntry` alongside `on_error` —
axes-independence: both fields coexist without structural conflict, enabling the
executor to read `failure_policy` independently of `on_error`).
The test exercises the `Timeout + FailClosed` branch, not configuration intent.

Separately, a bats test MUST verify that a real dispatch with a plugin configured
`failure_policy = "fail-closed"` and a deliberately tiny `fuel_cap = 100` produces
dispatcher exit code 2 (BLOCK). **The `fuel_cap = 100` + `failure_policy = "fail-closed"`
entry MUST live in a dedicated TEST-FIXTURE registry** (NOT
`plugins/vsdd-factory/hooks-registry.toml`), so that AC-001's gate scan does not trip on
this fixture. The `.bats` test MUST be registered in the Bats harness at
`plugins/vsdd-factory/tests/`; no new `.sh` file may be introduced per POLICY 21 (`.bats`
files and `tests/fixtures/` entries are exempt from POLICY 21).

- **Test:** `test_exhaustion_fail_closed_blocks()` — unit: `Timeout { cause: Fuel }` +
  `FailClosed` → `plugin_fail_closed` returns `true` (traces to BC-1.01.016 PC1/PC5)
- **Test:** bats integration test (TEST-FIXTURE registry only, NOT production registry) —
  dispatch with `fuel_cap = 100` + `failure_policy = "fail-closed"` on a
  budget-exhausting payload → observed dispatcher exit code 2 asserted

### AC-003: Exhaustion + fail-open → advisory, not block (exit 0) (traces to BC-1.03.017 PC2; ADR-039 §Decision 6 test case 2)

The same exhaustion scenario (`Timeout { cause: TimeoutCause::Fuel }`) with
`failure_policy = FailurePolicy::FailOpen` MUST produce exit 0 (advisory, not block).
This verifies the `fail-open` path is preserved for plugins that legitimately need it.

- **Test:** `test_exhaustion_fail_open_is_advisory()` — unit: `Timeout { cause: Fuel }` +
  `FailOpen` → `plugin_fail_closed` returns `false`; exit 0 asserted

### AC-004: `on_error = "continue"` + `failure_policy = "fail-closed"` + exhaustion → BLOCK (axes are independent) (traces to BC-1.03.017 PC3; ADR-039 §Decision 6 test case 3)

A plugin with BOTH `on_error = "continue"` AND `failure_policy = "fail-closed"` that
exhausts its fuel budget MUST produce a block (exit 2). The `on_error` field governs crash
outcomes only; it MUST NOT override `failure_policy` for exhaustion outcomes.

This is the primary correctness test of the axes-independence design. It directly validates
the behavior that the prior `fail_closed_timeout_with_on_error_continue_is_open` test
denied.

- **Test:** `test_on_error_continue_with_fail_closed_exhaustion_still_blocks()` — unit:
  `Timeout { cause: Fuel }` + `on_error = Continue` + `failure_policy = FailClosed` →
  `plugin_fail_closed` returns `true`; axes do not interfere

### AC-005: Crash vs exhaustion as distinct paths (traces to BC-1.03.017 PC4+PC5; ADR-039 §Decision 6 test case 4)

Two scenarios MUST be verified as distinct paths:

(a) A crashed plugin (`on_error = "block"`, `failure_policy = "fail-open"`) MUST block
    via the `on_error` path. `PluginResult::Crashed` + `on_error = Block` → block (exit 2).

(b) A fuel-exhausted plugin (`on_error = "block"`, `failure_policy = "fail-open"`) MUST
    NOT block — exhaustion is not a crash. `Timeout { cause: Fuel }` + `on_error = Block`
    + `failure_policy = FailOpen` → exit 0.

This verifies that `on_error = "block"` does not accidentally gate exhaustion outcomes,
preserving the original semantics of `on_error` for crash paths only.

- **Test:** `test_crash_blocks_via_on_error_not_failure_policy()` — `PluginResult::Crashed` +
  `Block` + `FailOpen` → exit 2
- **Test:** `test_exhaustion_with_on_error_block_but_fail_open_is_advisory()` — `Timeout { cause: Fuel }` +
  `Block` + `FailOpen` → exit 0

### AC-006: `fail_closed_timeout_with_on_error_continue_is_open` revised, not deleted (traces to BC-1.03.017 PC7; TD-VSDD-059)

The existing test `fail_closed_timeout_with_on_error_continue_is_open` in `executor.rs`
(or its module) encodes the current deliberate fail-open policy. When Phase 4 enforcement
ships, this test MUST be revised to assert the new invariant for the `fail-open`
configuration case — it is not the unconditional policy it once was, but it still describes
valid behavior when `failure_policy = FailOpen`.

Specifically, the revised test MUST:
- Retain the function name (or a derivative that clearly conveys the same intent)
- Assert: `Timeout { cause: Fuel } + on_error=Continue + failure_policy=FailOpen → NOT block`
- NOT be deleted, as deletion would allow silent regression of the `fail-open` path

Deletion of this test without an equivalent replacement is a TD-VSDD-059 (paper-fix) violation.

- **Test:** revised `fail_closed_timeout_with_on_error_continue_is_open` (or
  `fail_closed_timeout_with_continue_and_fail_open_is_open`) — asserts the fail-open
  config case remains advisory; MUST exist in the PR diff

### AC-007: Per-plugin fuel-cap calibration against production-scale corpus (traces to BC-1.03.017 Preconditions 2–4, enabling PC9; ADR-039 §Decision 4 Option A)

For each of the six targeted validator-class plugins, `fuel_cap` MUST be set to at least
`max(observed_max × 1.5, 50_000_000)` where `observed_max` is the maximum `fuel_consumed`
value observed over the calibration corpus below. For the hard-block/fail-closed boundary
validators in this story, the appropriate percentile basis is the **observed maximum over
the mandated corpus** — with ~4 corpus inputs, "p99" and "observed max" are effectively
identical (p99 ≈ the single largest value; using the observed max eliminates statistical
ambiguity). `fuel_consumed` MUST be read from the `Ok`-path measurement ONLY: on a
`Trap::OutOfFuel` outcome, `fuel_consumed == fuel_cap` (conveys the cap, not actual
demand), so a `Timeout`-path reading is invalid for calibration purposes. The calibration
corpus MUST include:

1. `lessons.md` at ≥4000 lines (at the D-442(e) hard limit; soft limit is 3500, hard is 4000)
2. `STATE.md` at current live size
3. `decision-log.md` at current live size
4. The ≥574 KB synthetic fixture committed at `plugins/vsdd-factory/tests/fixtures/validate-cross-site-correspondence/a1-production-scale/factory/specs/behavioral-contracts/BC-INDEX.md` (576,396 bytes; `BC-INDEX.md` is the calibration input file — the tree also holds `ss-05/BC-5.39.010.md`; added in S-21.07 merge e94767bc)

A calibration run MUST be executed for each plugin by the devops-engineer role before the
implementer annotates hooks-registry.toml. The measured `fuel_consumed` p99 and the chosen
`fuel_cap` MUST be recorded in the PR description or a calibration log artifact, not merely
asserted in code.

Any plugin whose calibration reveals a required `fuel_cap` that would exceed
`PRACTICAL_FUEL_CEILING` (`PRACTICAL_FUEL_CEILING = 500_000_000`, even at observed_max×1.5
for the largest corpus input) MUST be surfaced to the orchestrator rather than silently
annotated with an insufficient cap. EC-004 governs this case; see also the EC-004 resolution
path under Edge Cases.

- **Test:** AC-001 gate (`test_no_fail_closed_plugin_with_uncalibrated_cap`) enforces the
  structural invariant; the calibration log provides the human-auditable evidence

### AC-008: Each targeted validator's `failure_policy` and `fuel_cap` annotated atomically (traces to BC-1.03.017 Invariant 2, enforced via PC8 gate; ADR-039 §Decision 3 per-plugin atomic commit)

For each of the six targeted validator-class plugins, the commit that adds
`failure_policy = "fail-closed"` to its `plugins/vsdd-factory/hooks-registry.toml` entry
MUST be the same commit that sets the calibrated `fuel_cap` from AC-007 (replacing the
factory default of 20_000_000). No plugin MAY carry `failure_policy = "fail-closed"` with
an uncalibrated `fuel_cap < 50_000_000` at any CI-passing commit. The 50_000_000 floor is
the calibration minimum (inclusive) per ADR-039 §Decision 4; exactly 50_000_000 is a
valid calibrated value; the 20_000_000 factory default per ADR-042 §Decision 2 is below
this floor.

This constraint is enforced structurally by AC-001's gate test (the test would fail on
any half-state commit), and by the PR review gate — the reviewer MUST verify that no
intermediate commits in the PR carry the annotated-but-uncalibrated state.

- **Gate:** AC-001 test plus PR-level review of commit history

### AC-009: All targeted validator-class plugins carry `failure_policy = "fail-closed"` in final state (traces to BC-1.03.017 PC9; ADR-039 §Decision 2)

After all Phase 4 calibration-and-annotation commits land, `plugins/vsdd-factory/hooks-registry.toml`
MUST contain `failure_policy = "fail-closed"` AND `fuel_cap >= 50_000_000` for all plugins
in the **post-amendment targeted set**. The default targeted set is all six of:
- `validate-factory-path-root`
- `validate-input-hash`
- `validate-template-compliance`
- `validate-wave-gate-prerequisite`
- `validate-pr-merge-prerequisites`
- `validate-cross-site-correspondence`

Advisory-only and observability plugins MUST NOT receive `failure_policy = "fail-closed"`.
Classification of any ambiguous plugin MUST be surfaced to the orchestrator rather than
decided silently by the implementer.

**EC-004 conditional (Case A — `validate-cross-site-correspondence`, on_error="continue" only):**
If EC-004 fires for `validate-cross-site-correspondence` (calibration reveals
`PRACTICAL_FUEL_CEILING` would be exceeded for this on_error="continue" plugin), S-21.11 is
descoped to the five on_error="block" plugins only via orchestrator-approved spec amendment;
this AC's asserted set is reduced to the five flippable plugins; `validate-cross-site-correspondence`
routes to S-21.13 (depends_on [S-21.10, S-21.11]). AC-009 asserts the post-amendment set —
a partial-set completion is valid if EC-004 Case A applied. **EC-004 is FORBIDDEN for the five
on_error="block" plugins** (validate-factory-path-root, validate-input-hash,
validate-template-compliance, validate-pr-merge-prerequisites, validate-wave-gate-prerequisite)
— there is no valid descope path for these plugins from S-21.11; they MUST be annotated
fail-closed within this story OR block the entire Phase-4 flip (see EC-004 Case B).

This is the **genuine red-first TDD gate**: RED before Phase 4 annotations land (none of
the six plugins have `failure_policy = "fail-closed"` today), GREEN only after all targeted
plugins in the post-amendment set carry calibrated fail-closed annotations.

- **Test:** `test_all_six_validator_class_plugins_are_fail_closed()` — parses
  `plugins/vsdd-factory/hooks-registry.toml`; asserts each plugin in the post-amendment
  targeted set has `failure_policy = "fail-closed"` and `fuel_cap >= 50_000_000`.
  **EC-004 reduced-set mechanism (F-S2111-P2-008, Case A only):** if EC-004 fires for
  `validate-cross-site-correspondence` and S-21.11 is descoped to the five on_error="block"
  plugins via orchestrator-approved spec amendment, this test MUST be renamed or parameterized
  to reflect the actual post-amendment set (e.g., `test_flippable_validator_class_plugins_are_fail_closed`)
  and MUST enumerate only the plugins annotated in this story — it MUST NOT hardcode "six"
  when fewer than six plugins were annotated. `validate-cross-site-correspondence`'s assertion
  belongs in S-21.13, not here. Both the test name and the asserted set must be updated
  atomically with the spec amendment. **This reduced-set mechanism does NOT apply to the five
  on_error="block" plugins** — EC-004 is forbidden for them; their five-plugin assertion
  stands unconditionally.

### AC-010: Epoch exhaustion treated identically to fuel exhaustion under fail-closed enforcement (traces to BC-1.03.017 PC6; ADR-039 §Decision 1)

`PluginResult::Timeout { cause: TimeoutCause::Epoch }` with
`failure_policy = FailurePolicy::FailClosed` MUST cause the executor decision function to
return `true` (block intent); the dispatcher exit code MUST be 2. `TimeoutCause::Epoch`
(epoch deadline exceeded) is a resource-exhaustion outcome identical to
`TimeoutCause::Fuel` for enforcement purposes per ADR-039 §Decision 1. Both exhaustion
causes trigger the `failure_policy` enforcement path; `on_error` is not consulted for
either.

- **Test:** `test_epoch_exhaustion_fail_closed_blocks()` — unit:
  `Timeout { cause: TimeoutCause::Epoch }` + `FailurePolicy::FailClosed` →
  `plugin_fail_closed` returns `true`; exit 2 asserted

### AC-011: `fail_closed_timeout_with_on_error_block` deliberately revised — both FailOpen and FailClosed branches asserted (traces to BC-1.03.017 v1.7 PC10; TD-VSDD-059)

The existing test `fail_closed_timeout_with_on_error_block` in `executor.rs` (or its module)
encodes behavior for the `Timeout + on_error=Block` combination. When Phase 4 enforcement
ships, this test MUST be deliberately revised (TD-VSDD-059: NOT deleted — deletion is a
paper-fix violation) to assert BOTH branches of the new dual semantics:

(a) `Timeout { cause: Fuel | Epoch } + on_error=Block + failure_policy=FailOpen → NOT block`
    (exhaustion governed by `failure_policy`, not `on_error`; with `FailOpen`, result is
    advisory, not block — even though `on_error=Block`)

(b) `Timeout { cause: Fuel | Epoch } + on_error=Block + failure_policy=FailClosed → block`
    (exhaustion governed by `failure_policy = FailClosed`; result is block, consistent with
    both `on_error=Block` and `failure_policy=FailClosed` independently requiring block)

Deletion of this test without an equivalent replacement is a TD-VSDD-059 (paper-fix)
violation. This is the sibling test to AC-006's revision of
`fail_closed_timeout_with_on_error_continue_is_open`; both tests MUST exist in the PR diff.

- **Test:** revised `fail_closed_timeout_with_on_error_block` — asserts (a) `FailOpen` arm
  (exhaustion does not block when `failure_policy = FailOpen`, even with `on_error = Block`)
  AND (b) `FailClosed` arm (exhaustion blocks when `failure_policy = FailClosed`); MUST
  exist in the PR diff as a deliberate revision, not a deletion

### AC-012: Hard migration-window completeness gate — if failure_policy enforcement is in effect (any block-decision site in the executor block-decision chain referencing `.failure_policy` for a Timeout block decision; design-flow-independent detection), every `on_error="block"` targeted plugin MUST carry `failure_policy="fail-closed"` (CWE-636 static gate; traces to BC-1.03.017 v1.7 PC11)

A Cargo integration test MUST assert: if `crates/factory-dispatcher/src/executor.rs`
contains enforcement-active state — detected by the **design-flow-independent signal**
per BC-1.03.017 v1.7 PC11: any block-decision site in the executor block-decision
chain — `execute_tier`, `execute_tiers`, or helpers — that references a `.failure_policy`
value when deciding to block on a `Timeout` outcome, however the data reaches it — then
every `[[hook]]` entry in
`plugins/vsdd-factory/hooks-registry.toml` whose `name` is one of the five targeted
`on_error = "block"` plugins (`validate-factory-path-root`, `validate-input-hash`,
`validate-template-compliance`, `validate-pr-merge-prerequisites`,
`validate-wave-gate-prerequisite`) MUST carry an explicit
`failure_policy = "fail-closed"`. The absence of this annotation while enforcement is
active MUST cause the test to FAIL (CI blocks merge).

**Detection specifics (design-flow-independent):** scan `executor.rs` for any block-decision
site in the executor block-decision chain (`execute_tier`, `execute_tiers`, or helpers) that
references a `.failure_policy` value when deciding to block on a `Timeout` outcome, however
the data reaches it. This signal holds whether the implementer extends `plugin_fail_closed`
in-place (Design A), introduces a replacement function such as `plugin_exhaustion_fail_closed`
(Design B), or routes `failure_policy` through an intermediate struct — the test does NOT
scan for any specific function name or data-flow path.

This gate is:
- **GREEN when enforcement is absent** (Phase 1–2 state today; no block-decision site in
  the executor block-decision chain references `.failure_policy` for a `Timeout` block
  decision → test passes; GREEN is real because VACUITY-CONTROL verifies the
  enforcement-detection logic ran and returned `EnforcementAbsent` via a tri-state
  diagnostic, and that RED-emission was skipped as a consequence — distinguishing genuine
  Phase-1/2 GREEN from vacuous GREEN)
- **GREEN at Phase 4 complete state** (enforcement-active signal fires AND all five
  `on_error="block"` plugins carry `failure_policy = "fail-closed"`)
- **RED on the bad intermediate state** (enforcement-active signal fires AND any
  `on_error="block"` targeted plugin lacks `failure_policy = "fail-closed"`) — precisely
  the CWE-636 regression window Invariant 7 prohibits by ordering rule

This gate makes the CWE-636 migration-window regression mechanically detectable at any
single commit; design-flow-independent: it evaluates both conditions (executor
enforcement-active signal AND registry annotation) at the same commit tree, with no
ordering dependency, and with no dependence on any specific function name or data-flow
path. It is the machine-checkable complement to Invariant 7's human-readable ordering
policy.

**Relationship to AC-001:** AC-001 (PC8) gates uncalibrated caps; AC-012 (PC11) gates
missing fail-closed annotations on on_error=block plugins while enforcement is active.
These address different failure modes and both are required. **No EC-004 reduced-set
escape for this AC:** AC-012's five-plugin assertion stands unconditionally — EC-004 is
forbidden for on_error="block" plugins and cannot reduce the set of plugins this gate checks.

- **Test:** `test_no_on_error_block_without_fail_closed_when_3arg_executor()` — detects
  enforcement-active state in `crates/factory-dispatcher/src/executor.rs` using the
  design-flow-independent signal per BC-1.03.017 v1.7 PC11 (any block-decision site in
  the executor block-decision chain referencing `.failure_policy` for a Timeout block
  decision, however the data reaches it; holds for extend-in-place OR introduce-a-replacement
  OR intermediate-struct routing); if enforcement is active, parses
  `plugins/vsdd-factory/hooks-registry.toml` and asserts each of the five `on_error="block"`
  targeted plugins carries `failure_policy = "fail-closed"`; MUST FAIL if enforcement active
  and any of the five lacks the annotation; GREEN when enforcement absent (pre-Phase-4 state)
  AND GREEN when enforcement active + all five annotated; MUST include all four controls
  per BC-1.03.017 v1.7 PC11 (three injectable pure-function controls + one live-tree control).

  The test MUST be structured with **four controls** — three as **PURE FUNCTIONS over
  INJECTABLE inputs** (synthetic executor-source snippet string + synthetic registry entries,
  NOT a live-tree scan for control purposes) and one **live-tree assertion** at Phase-4-complete:
  - **POSITIVE-CONTROL:** inject enforcement-active source snippet + synthetic registry
    MISSING one of the five `on_error="block"` fail-closed annotations → assert the detector
    returns RED (enforcement active + missing annotation detected)
  - **NEGATIVE-CONTROL:** inject enforcement-active source snippet + synthetic registry with
    ALL five `on_error="block"` plugins annotated `failure_policy = "fail-closed"` → assert
    the detector returns PASS (GREEN; no missing annotations)
  - **VACUITY-CONTROL:** inject enforcement-ABSENT source snippet (no `.failure_policy`
    reference in the block-decision chain for `Timeout` outcomes) + any registry → assert
    GREEN, AND assert the detector's enforcement-detection logic ran and returned
    `EnforcementAbsent` (via a `detection_ran` / tri-state diagnostic), and that RED-emission
    was skipped as a consequence — distinguishing genuine Phase-1/2 GREEN from vacuous GREEN
    (i.e., a detector that never ran the enforcement-detection logic at all)
  - **LIVE-TREE-CONTROL:** at Phase-4-complete, run the detector against the ACTUAL
    `crates/factory-dispatcher/src/executor.rs` (not a synthetic snippet) and assert
    `enforcement_active = true`. Acceptable form: the POSITIVE-CONTROL synthetic snippet
    MUST be a verbatim excerpt of the real `execute_tiers` block-decision site, AND the same
    detector run on the live tree MUST return `enforcement_active = true`. This proves the
    detector's enforcement-detection logic recognizes the real enforcement code as shipped —
    a syntactically-wrong detector that passes synthetic controls (a), (b), (c) yet produces
    zero matches against the actual `execute_tiers` form would produce a silent CWE-636
    false-green; the live-tree assertion closes this gap.

  These four controls prove the test is non-vacuous and that the vacuous-GREEN case
  (enforcement absent → enforcement-detection logic returns EnforcementAbsent, RED-emission
  skipped) is explicitly differentiated from the real-GREEN case (enforcement active + all
  five annotated), and that the detector fires correctly against the actual live codebase.

## Behavioral Contracts

| BC ID | Version | Title | Clause |
|-------|---------|-------|--------|
| BC-1.03.017 | v1.7 | factory-dispatcher::executor::failure_policy enforcement — exhaustion-outcome dispatch (fail-closed→block; fail-open→advisory), on_error axes independence, crash-versus-exhaustion distinct paths, Phase-3-before-Phase-4 structural half-state gate, migration-window on_error=block completeness gate (PC11 CWE-636 static gate), corpus calibration floor ≥4000 lines (Precondition 2; v1.7), PC10 sub-cases widened to Fuel\|Epoch (v1.7) | PC1 (AC-002), PC2 (AC-003), PC3 (AC-004), PC4+PC5 (AC-005), PC6 (AC-010), PC7 (AC-006), PC8 (AC-001), PC9 (AC-009), PC10 (AC-011), PC11 (AC-012) |
| BC-1.01.016 | v1.3 | factory-dispatcher::registry::failure_policy field — parse semantics, backward-compatibility default (fail-open), unknown-value rejection, and on_error axes independence | PC1 (`failure_policy = "fail-closed"` parses to `FailClosed`; field parseable from TOML and accessible on `RegistryEntry`; AC-002 unit test reads this field); PC5 (axes-independence: `failure_policy` is an independent field in `RegistryEntry` alongside `on_error`; prerequisite schema contract; AC-002/AC-003 depend on this structural guarantee) |

> **Detection context (non-governed):** BC-1.03.002 v1.0 ("Plugin in tight arithmetic loop
> runs out of fuel") establishes `PluginResult::Timeout { cause: TimeoutCause::Fuel }` as
> the detection output — it is a sibling detection precondition enabling this story's
> enforcement postconditions. BC-1.03.002 is NOT a governed behavioral contract for S-21.11
> (no story ACs trace postconditions to it); enforcement postconditions are fully covered by
> BC-1.03.017. BC-1.03.002 context: `invoke_plugin` returns
> `PluginResult::Timeout { cause: TimeoutCause::Fuel }` when fuel is exhausted;
> BC-1.03.017 governs the enforcement decision the executor makes with that result based on
> `failure_policy`.

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| `plugin_fail_closed` (extended) | `crates/factory-dispatcher/src/executor.rs` | Pure (consults `failure_policy` in addition to `on_error` for `Timeout` outcomes) |
| `hooks-registry.toml` annotations | `plugins/vsdd-factory/hooks-registry.toml` | Config (six entries receive calibrated `fuel_cap` + `failure_policy = "fail-closed"`) |
| Calibration corpus measurement | devops-engineer tooling (external to crate) | Effectful-shell (runs plugins against corpus inputs; reads `fuel_consumed`) |
| `test_no_fail_closed_plugin_with_uncalibrated_cap` | `crates/factory-dispatcher` or integration test crate | Pure (reads hooks-registry.toml; asserts invariant) |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| `plugin_fail_closed` extension | Pure-core | Decision function; takes `PluginResult` + `FailurePolicy` + `OnError`; returns `bool`; no I/O |
| Calibration corpus runner | Effectful-shell | Invokes plugins against filesystem inputs; reads fuel metrics from dispatcher |
| `test_no_fail_closed_plugin_with_uncalibrated_cap` | Pure-core | Reads config file; asserts structural invariant |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | Plugin with `failure_policy = "fail-closed"` completes successfully (no exhaustion) | Exit 0; `on_error` and `failure_policy` do not affect clean-pass outcomes |
| EC-002 | Plugin with `failure_policy = "fail-closed"` + `PluginResult::Crashed` (`on_error = "continue"`) | Crash governed via `on_error`; exit 0 (crash is advisory per `continue`); `failure_policy` is NOT consulted for crash outcomes |
| EC-003 | Plugin with `failure_policy = "fail-closed"` + `PluginResult::Crashed` (`on_error = "block"`) | Crash governed via `on_error`; exit 2 — crash blocks regardless of `failure_policy` |
| EC-004 | Calibration reveals plugin needs `fuel_cap > PRACTICAL_FUEL_CEILING` (`PRACTICAL_FUEL_CEILING = 500_000_000`) even at observed_max×1.5 for the corpus | **Bifurcated on `on_error` value (BC-1.03.017 v1.7 PC11 + EC-004 clause):** **Case A — `validate-cross-site-correspondence` (on_error="continue"):** Deferral to S-21.13 is a VALID descope path — behavior-neutral (exhaustion was already advisory for this on_error="continue" plugin; deferring it does NOT reintroduce CWE-636). MUST surface to orchestrator; S-21.11 is descoped to the five on_error="block" plugins only via orchestrator-approved spec amendment; `validate-cross-site-correspondence` routes to **S-21.13** (depends_on [S-21.10, S-21.11]); AC-009's asserted set reduces to the five flippable plugins only; AC-012's five-plugin assertion is unaffected (validate-cross-site-correspondence is not in AC-012's five-plugin set). **Annotation-landing obligation (BC-1.03.017 v1.7 PC11 EC-004 Case A):** S-21.13 (or a named successor) MUST annotate `validate-cross-site-correspondence` with `failure_policy = "fail-closed"` and a calibrated `fuel_cap >= 50_000_000` once its O(n) fuel-ceiling algorithmic fix removes the excessive cap requirement. The EC-004 Case A descope is a **timing deferral only, NOT a permanent exemption** — the fail-closed annotation MUST NOT fall through subsequent stories without explicit orchestrator reaffirmation. S-21.13 must carry this obligation in its AC set. **Case B — five on_error="block" plugins (`validate-factory-path-root`, `validate-input-hash`, `validate-template-compliance`, `validate-pr-merge-prerequisites`, `validate-wave-gate-prerequisite`):** EC-004 is **FORBIDDEN** as a descope path for these plugins. Deferring an on_error="block" plugin is NOT behavior-neutral — post-merge, the deferred plugin would carry on_error="block" (incidentally blocking crashes) but failure_policy=fail-open (failing open on exhaustion), directly reintroducing CWE-636. Options: (a) annotate fail-closed within S-21.11 with whatever calibrated cap is feasible (PREFERRED — if the fuel demand is high but not infinite, calibrate conservatively); (b) block the entire Phase-4 flip until a per-plugin algorithm optimization makes the cap feasible (no Phase 4 merge until all five are annotatable). There is NO valid "record transient fail-open window" path for on_error="block" plugins — this path is deleted. S-21.13 is scoped to on_error="continue" plugin only (Case A) and is NOT a valid routing target for any on_error="block" plugin. AC-012's five-plugin assertion stands unconditionally; there is no reduced-set escape for on_error="block" plugins. |
| EC-005 | `lessons.md` validator post-flip exhausts on a >4000-line `lessons.md` (above D-442(e) hard limit) | This MUST NOT happen if calibration was run against ≥4000-line corpus; if it does, the calibration was insufficient and AC-007 was not met — surfaced to orchestrator |
| EC-006 | A new validator-class plugin added after S-21.11 merges (without `failure_policy = "fail-closed"`) | It defaults to `fail-open` per S-21.10 backward-compat; AC-001 gate only fires for annotated-but-uncalibrated entries; classification of new plugins is a future-story concern |
| EC-007 | `TimeoutCause::Epoch` (epoch deadline) with `failure_policy = "fail-closed"` | MUST block (exit 2); both `TimeoutCause::Fuel` and `TimeoutCause::Epoch` are resource-exhaustion outcomes per ADR-039 §Decision 1 |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | 4,400 |
| ADR-039 (full text, 6 decisions) | 4,500 |
| Research file `wasm-fuel-exhaustion-detection.md` | 6,000 |
| BC files (2 BCs): BC-1.03.017 v1.7 (governing — 11 postconditions, 7 invariants, 9 edge cases; PC10 + Invariant 7 added at v1.2; PC11 migration-window gate added at v1.3; data-anchored name-independent detection + EC-004 bifurcation + H1 enriched at v1.4; design-flow-independent broadened signal + three non-vacuity controls + annotation-landing obligation at v1.5; LIVE-TREE-CONTROL (fourth control) + VACUITY-CONTROL tri-state diagnostic fix + PC8/PC11 ownership clarification at v1.6; corpus floor ≥4000 lines in Precondition 2 + PC10 sub-cases widened to Fuel\|Epoch at v1.7) + BC-1.01.016 v1.3 (prerequisite schema) | 2,000 |
| `crates/factory-dispatcher/src/executor.rs` (current, including `fail_closed_timeout_with_on_error_continue_is_open`) | 4,500 |
| `plugins/vsdd-factory/hooks-registry.toml` (all six targeted plugin entries + context) | 2,500 |
| Calibration log artifact (devops-engineer output) | 1,500 |
| **Total** | **~25,200** |
| Agent context window | 200K (Sonnet) |
| **Budget usage** | **~12.5%** |

## Tasks (MANDATORY)

### Phase 3 — Per-plugin calibration (devops-engineer role)

1. [ ] Confirm the ≥574 KB production-scale fixture file exists at
       `plugins/vsdd-factory/tests/fixtures/validate-cross-site-correspondence/a1-production-scale/factory/specs/behavioral-contracts/BC-INDEX.md`
       (576,396 bytes; `BC-INDEX.md` is the calibration input — the tree also holds
       `ss-05/BC-5.39.010.md`; committed in S-21.07 merge e94767bc; S-21.07 has no task #33 —
       the fixture file path above is the confirmed artifact)
2. [ ] Build calibration corpus: (a) `lessons.md` at ≥4000 lines; (b) `STATE.md` at live
       size; (c) `decision-log.md` at live size; (d) ≥574 KB fixture
3. [ ] For each of the six targeted plugins: run the plugin against the full calibration
       corpus with an unlimited budget (e.g., `fuel_cap = u64::MAX / 2`); record
       `fuel_consumed` per invocation
4. [ ] Compute p99 per plugin from corpus measurements; set `fuel_cap = max(p99 × 1.5, 50_000_000)`
5. [ ] Document calibration results (plugin name, p99 measured, chosen `fuel_cap`) in the
       PR description

### Phase 4 — Enforcement code (implementer role)

6. [ ] Read ADR-039 §Decision 3+6 + `executor.rs::fail_closed_timeout_with_on_error_continue_is_open` fully before writing any code
7. [ ] Write `test_no_fail_closed_plugin_with_uncalibrated_cap` (AC-001 **regression/invariant gate**, NOT a red-first gate): parse `plugins/vsdd-factory/hooks-registry.toml`; iterate entries; assert each fail-closed entry has `fuel_cap >= 50_000_000` (inclusive floor; `fuel_cap = 50_000_000` is VALID). Include a **POSITIVE-CONTROL fixture** in the test body — a hard-coded synthetic fail-closed entry with `fuel_cap = 20_000_000` (the factory default per ADR-042 §Decision 2, strictly below the 50_000_000 inclusive floor) — and assert the gate logic returns an error for that fixture, proving the gate is non-vacuous. Include a **boundary-pass fixture** (synthetic fail-closed entry with `fuel_cap = 50_000_000`) and assert the gate PASSES (exactly at inclusive floor). This test is GREEN today (zero fail-closed entries in the live registry). The genuine RED-first TDD gate is AC-009 (Task #14). Genuine RED tests for AC-002/003/004/005/010 are naturally RED because the 3-arg `plugin_fail_closed` signature does not compile yet.
8. [ ] Write failing RED Gate tests for AC-002 (`test_exhaustion_fail_closed_blocks`) and AC-003 (`test_exhaustion_fail_open_is_advisory`). For AC-002's bats/integration leg: create a fuel-exhausting fixture in `plugins/vsdd-factory/tests/fixtures/` and a TEST-FIXTURE registry (NOT the production `hooks-registry.toml`); add a `.bats` test registered in `plugins/vsdd-factory/tests/run-all.sh`; confirm NO new `.sh` file is introduced (POLICY 21; `.bats` files and `tests/fixtures/` entries are exempt).
9. [ ] Write failing RED Gate tests for AC-004 (`test_on_error_continue_with_fail_closed_exhaustion_still_blocks`) and AC-005 (both sub-tests)
10. [ ] Extend `plugin_fail_closed` (or introduce `plugin_exhaustion_fail_closed`) in `executor.rs`
        to accept `failure_policy: FailurePolicy`; for `Timeout { cause: TimeoutCause::Fuel | TimeoutCause::Epoch }`,
        return `true` when `failure_policy == FailurePolicy::FailClosed` regardless of `on_error`.
        Also add `failure_policy: FailurePolicy` to `PluginOutcome` and populate it at ALL
        construction sites in `executor.rs` (grep-confirmed: `struct PluginOutcome` is defined
        and ALL construction sites are in `executor.rs`; `invoke.rs` has ZERO `PluginOutcome`
        constructions; do NOT confuse with the decoy `pub enum PluginOutcome` in `routing.rs`
        which is a different type — do not edit `routing.rs` or `invoke.rs` for this task)
        so that AC-002/003/004/010 can exercise the real dispatch path end-to-end; see SR-005
        plumbing sweep note in File Structure Requirements.
        **ATOMICITY GATE (BC-1.03.017 v1.7 Invariant 7 + AC-012 PC11 — fail-open-window hazard):**
        Once the extended decision function is in effect (signaled by the name-independent,
        data-anchored detection: `PluginOutcome` carrying `failure_policy: FailurePolicy`
        + `execute_tiers` consulting it), any targeted on_error="block" plugin that still
        has `failure_policy = fail-open` (the default) will FAIL OPEN on exhaustion — a
        regression from today's behavior where `on_error = "block"` incidentally caused a
        block. The five on_error="block" targeted plugins (validate-factory-path-root,
        validate-input-hash, validate-template-compliance, validate-pr-merge-prerequisites,
        validate-wave-gate-prerequisite) MUST have their fail-closed annotations committed
        in the SAME atomic commit as this decision-function extension, OR the annotations
        for those five MUST land in a PRIOR commit (annotate-before-flip ordering).
        **⚠ IMPLEMENTER WARNING: Task #10 (this task) and Task #13 (the five on_error="block"
        annotations) are NOT independent per-task commits. Committing Task #10 alone WILL
        open the CWE-636 regression window AND WILL cause AC-012's
        `test_no_on_error_block_without_fail_closed_when_3arg_executor` to FAIL (its
        name-independent detection will fire as soon as `PluginOutcome` carries
        `failure_policy: FailurePolicy` + `execute_tiers` consults it), blocking CI merge.
        Deliver them as a SINGLE atomic commit or annotate-before-flip.**
        The on_error="continue" plugin (validate-cross-site-correspondence) carries no
        current-blocking behavior on exhaustion and may be committed in a separate commit
        (see EC-004 Case A for its non-neutral deferral note).
11. [ ] Verify RED Gates for AC-002 through AC-005 pass
12. [ ] Revise `fail_closed_timeout_with_on_error_continue_is_open` per **AC-006** — MUST assert
        `Timeout + Continue + FailOpen → NOT block`; retain the function name or a derivative;
        document the deliberate revision in the commit message.
        Also revise `fail_closed_timeout_with_on_error_block` per **AC-011** — MUST assert BOTH:
        (a) `Timeout + on_error=Block + failure_policy=FailOpen → NOT block` (exhaustion governed
        by `failure_policy`, not `on_error`; `on_error` governs crash paths only), AND
        (b) `Timeout + on_error=Block + failure_policy=FailClosed → block` (both axes independently
        require block; result is block). TD-VSDD-059: this test MUST be revised, NOT deleted — it
        encodes deliberate semantics for the `on_error=Block` combination. Both
        `fail_closed_timeout_with_on_error_continue_is_open` and `fail_closed_timeout_with_on_error_block`
        MUST appear in the PR diff as deliberate revisions.
        Also perform the **TD-VSDD-060 sibling-site sweep**: update ALL call-sites of
        `plugin_fail_closed` to the new 3-arg signature (5 test sites + 1 production call-site —
        enumerate by `grep -rn plugin_fail_closed crates/` before committing); confirm
        `cargo build --workspace` passes before staging.
13. [ ] For each of the six targeted plugins: update `hooks-registry.toml` entry atomically —
        set calibrated `fuel_cap` from Phase 3 AND `failure_policy = "fail-closed"` in the
        SAME commit per AC-008; verify AC-001 gate passes after each commit.
        **(a) Fail-open-window atomicity (Invariant 7 + AC-012 PC11):** The five
        on_error="block" plugins (validate-factory-path-root, validate-input-hash,
        validate-template-compliance, validate-pr-merge-prerequisites,
        validate-wave-gate-prerequisite) MUST be annotated in the SAME commit as the Task #10
        decision-function extension, OR in a PRIOR commit (annotate-before-flip). **⚠
        IMPLEMENTER WARNING: This task and Task #10 are NOT independent per-task commits.
        Committing Task #10 alone WILL open the CWE-636 regression window AND WILL cause
        AC-012's `test_no_on_error_block_without_fail_closed_when_3arg_executor` to FAIL,
        blocking CI merge. The five annotations in this task and the executor flip in Task
        #10 MUST arrive in a single atomic commit or via annotate-before-flip ordering.**
        Committing Task #10 code without these five annotations in place opens a fail-open
        regression window per BC-1.03.017 v1.7 Invariant 7.
        **(b) TD-VSDD-060 stale comment sweep (F-S2111-P2-007):** When annotating the
        `validate-cross-site-correspondence` entry in `plugins/vsdd-factory/hooks-registry.toml`,
        correct the stale comment block (beginning "No fuel_cap field: the per-plugin fuel_cap
        registry field … has not shipped") — ADR-039 Phase 1 (S-21.10: FailurePolicy +
        failure_policy field; RegistryEntry.fuel_cap: Option<u64>) HAS shipped on develop
        (merged before this story). Update the comment to reflect that Phase 1 is complete and
        this entry is being annotated as part of Phase 3+4 (this story). This is an in-scope
        TD-VSDD-060 sibling-sweep obligation; do not defer to a follow-up story.
14. [ ] Write `test_all_six_validator_class_plugins_are_fail_closed` (AC-009)
14a. [ ] Write `test_no_on_error_block_without_fail_closed_when_3arg_executor` (AC-012 —
         BC-1.03.017 v1.7 PC11): detect enforcement-active state in `executor.rs` using
         the **design-flow-independent signal** (any block-decision site in the executor
         block-decision chain — `execute_tier`, `execute_tiers`, or helpers — that
         references a `.failure_policy` value when deciding to block on a `Timeout`
         outcome, however the data reaches it); this detection holds whether
         `plugin_fail_closed` is extended in-place OR a replacement function is introduced
         OR `failure_policy` is routed through an intermediate struct
         (do NOT scan for `fn plugin_fail_closed(` by name — name-coupled false-green
         risk per F-S2111-P4-001; do NOT bare-grep `failure_policy:` across all of
         `executor.rs` — see DECOY NOTE below); if enforcement is active, parse
         `hooks-registry.toml` and assert each of the five `on_error="block"` targeted
         plugins carries `failure_policy = "fail-closed"`; MUST FAIL in the
         bad-intermediate state (enforcement active + any of the five lacking annotation);
         GREEN when enforcement absent AND GREEN at Phase 4 complete state; no EC-004
         reduced-set escape for this test.

         **Four controls (three PURE FUNCTIONS over INJECTABLE inputs + one live-tree assertion
         per BC-1.03.017 v1.7 PC11):**
         Structure the detection logic and annotation-check logic to accept injectable
         parameters (synthetic source snippet string + synthetic registry entries), NOT
         live-tree-only paths. Provide four control sub-tests or sub-cases:
         - POSITIVE-CONTROL: enforcement-active source snippet + synthetic registry
           MISSING one of the five on_error="block" fail-closed annotations → assert
           RED (gate fires; enforcement active + missing annotation)
         - NEGATIVE-CONTROL: enforcement-active source snippet + synthetic registry with
           ALL five on_error="block" plugins annotated `failure_policy = "fail-closed"`
           → assert PASS (GREEN; gate does not fire; no missing annotations)
         - VACUITY-CONTROL: enforcement-ABSENT source snippet (no `.failure_policy`
           reference in the block-decision chain for `Timeout` outcomes) + any registry
           → assert GREEN AND assert the detector's enforcement-detection logic ran and
           returned `EnforcementAbsent` (via a `detection_ran` / tri-state diagnostic),
           and that RED-emission was skipped as a consequence — distinguishing genuine
           Phase-1/2 GREEN from vacuous GREEN (a detector that never ran the
           enforcement-detection logic at all)
         - LIVE-TREE-CONTROL (at Phase-4-complete): run the detector against the ACTUAL
           `crates/factory-dispatcher/src/executor.rs` and assert `enforcement_active = true`.
           Acceptable form: the POSITIVE-CONTROL synthetic snippet MUST be a verbatim excerpt
           of the real `execute_tiers` block-decision site, AND the same detector run on the
           live tree MUST return `enforcement_active = true`. This proves the detector
           recognizes the real enforcement code — a syntactically-wrong detector that passes
           synthetic controls yet is inert against actual code produces a silent CWE-636
           false-green; the live-tree assertion closes this gap.

         **⚠ DECOY NOTE (F-S2111-P5-004):** `executor.rs` contains a pre-existing
         `failure_policy: crate::registry::FailurePolicy::FailOpen` literal inside an
         existing test function (`emit_lifecycle_timeout_carries_fuel_cap_and_consumed`)
         present TODAY before any enforcement lands. A naive bare-grep for
         `failure_policy:` anywhere in `executor.rs` would false-match this literal
         and incorrectly conclude enforcement is active. Scope detection to `.failure_policy`
         DOT-ACCESS within the block-decision chain (`execute_tier`/`execute_tiers`/helpers);
         the `PluginOutcome` struct-definition field declaration is NOT itself an
         enforcement-active signal (a colon-init `failure_policy:` declaration is not a
         `.failure_policy` block-decision reference — this is also why the pre-existing
         `executor.rs` `emit_lifecycle_timeout_...` test literal must not false-match).
         The VACUITY-CONTROL fixture (enforcement-ABSENT snippet) should be calibrated to
         confirm the decoy literal alone does NOT trigger the enforcement-active signal.
15. [ ] Run `cargo test --workspace --all-targets`; confirm all new and existing tests pass
16. [ ] Run `cargo fmt --check --all && cargo clippy --workspace --all-targets -- -D warnings`
17. [ ] Add CHANGELOG entry under [Unreleased] > Security:
        "Validator-class WASM plugins (validate-factory-path-root, validate-input-hash,
        validate-template-compliance, validate-wave-gate-prerequisite,
        validate-pr-merge-prerequisites, validate-cross-site-correspondence) now fail closed
        on fuel exhaustion — CWE-636 closed (ADR-039 Decision 2+3+4; per-plugin calibrated
        fuel_cap ≥ 50M; failure_policy = fail-closed)"

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-21.10 (W5; prerequisite) | Delivered `FailurePolicy` enum + `RegistryEntry.failure_policy` field; absent field defaults to `fail-open`; `plugin_fail_closed` NOT modified. | `failure_policy` field is now parseable in all `[[hook]]` entries; implementer reads the `failure_policy` from the dispatched `RegistryEntry` in the executor. | S-21.10 AC-006 explicitly blocked any change to `plugin_fail_closed` — S-21.11 is where that gate lifts. Do not re-implement AC-006 as a constraint here. |
| S-21.07 (W4; draft) | ADR-035 §Decision 5 — `TimeoutCause::Fuel` is a resource-policy error; advisory MUST be emitted on exhaustion. Research file confirms detection is already correct; defect is enforcement. | S-21.07 (merged e94767bc) committed the ≥574 KB production-scale fixture at `plugins/vsdd-factory/tests/fixtures/validate-cross-site-correspondence/a1-production-scale/factory/specs/behavioral-contracts/BC-INDEX.md` (576,396 bytes; 2,578 lines; synthetic BC-INDEX); it drove `validate-cross-site-correspondence` to 99.21% fuel consumption (9,920,913 / 10,000,000) — documented in S-21.07 spec and BC-5.39.010 v1.15. Note: S-21.07 has only 22 tasks; any prior "task #33" reference is inaccurate. | `validate-cross-site-correspondence` is one of the six targeted plugins; its `fuel_cap` calibration may reveal that large STORY-INDEX inputs are the sizing concern (not just `lessons.md`). |
| S-21.09 (W4; draft) | POLICY 21 — no new `.sh` files. | The calibration gate test follows the same pattern as S-21.09's `test_registry_parity_declared_set_equals_tracked_set` — a Cargo integration test reading config files. | None relevant to enforcement logic. |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| `failure_policy = "fail-closed"` MUST NOT land without calibrated `fuel_cap >= 50_000_000` at the same commit (exactly 50M is the inclusive floor and a VALID calibrated value) | ADR-039 §Decision 3 per-plugin atomicity; ADR-039 §Decision 4 calibration floor 50M inclusive (factory default 20M per ADR-042 §Decision 2 is below floor) | AC-001 gate test; CI failure on any half-state |
| `TimeoutCause::Fuel` AND `TimeoutCause::Epoch` are both resource exhaustion — both trigger fail-closed when `failure_policy = "fail-closed"` | ADR-039 §Decision 1 | EC-007 test; both cause variants must be covered in AC-002 |
| `fail_closed_timeout_with_on_error_continue_is_open` MUST be revised, NOT deleted | TD-VSDD-059 paper-fix prevention | AC-006; PR reviewer checks the function exists in the diff with updated semantics |
| Advisory-only plugins (observability, telemetry, convergence-tracking) MUST remain `fail-open` | ADR-039 §Decision 2 | AC-009 test only asserts the six targeted plugins; any additional annotation of advisory-only plugins is a scope violation |
| D-442(e) line-count workaround remains in force until S-21.11 calibration confirms a `fuel_cap` that handles ≥4000-line `lessons.md` | ADR-039 §Decision 3 §Rationale | If calibration shows `lessons.md` validators exhaust at ≥4000 lines even with 50M cap, surface to orchestrator rather than quietly adjusting D-442(e) |
| POLICY 21 — no new `.sh` files | POLICY 21 (D-836) | `git diff --name-only HEAD` output must contain no `.sh` filenames |
| **Fail-open-window hazard:** The decision-function extension (Task #10) and fail-closed annotations for the five on_error="block" targeted plugins (validate-factory-path-root, validate-input-hash, validate-template-compliance, validate-pr-merge-prerequisites, validate-wave-gate-prerequisite) MUST be in the SAME atomic commit OR follow annotate-before-flip ordering (plugin annotations land BEFORE or CONCURRENT WITH the decision-function extension). Violating this ordering places the five plugins in a window where exhaustion fails open — regressing from current behavior where on_error="block" incidentally blocked exhaustion. AC-012 (`test_no_on_error_block_without_fail_closed_when_3arg_executor`) makes this mechanically detectable at any single commit; design-flow-independent: the bad intermediate state causes AC-012 to FAIL | BC-1.03.017 v1.7 Invariant 7 + PC11; ADR-039 §Decision 3 | Task #10 ATOMICITY GATE; Task #13(a); AC-012 CI gate (design-flow-independent detection — fires on any block-decision site in executor block-decision chain referencing .failure_policy for a Timeout block decision, however the data reaches it; not tied to function name or data-flow path); PR reviewer verifies commit ordering in PR history: no commit containing only the Task #10 code change may land without the five annotations |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| Rust toolchain (workspace pinned) | `rust-toolchain.toml` | Implementing `plugin_fail_closed` extension |
| serde / toml (workspace) | workspace version | Parsing `hooks-registry.toml` in AC-001 and AC-009 gate tests |
| wasmtime 46.0.2 (pinned) | Cargo.lock (S-21.12 #781 merged) | Fuel mechanics; `TimeoutCause::Fuel` variant; `Store::set_fuel`/`get_fuel` API (stable across 44→46; implementer MUST confirm parity before relying on fuel API behavior); `fuel_cap = 100` for AC-002 bats integration test |

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| `crates/factory-dispatcher/src/executor.rs` | modify | Extend `plugin_fail_closed` to accept `FailurePolicy`; revise `fail_closed_timeout_with_on_error_continue_is_open` (AC-006) and `fail_closed_timeout_with_on_error_block` (AC-011) — both must be deliberately revised, not deleted (TD-VSDD-059) |
| `plugins/vsdd-factory/hooks-registry.toml` | modify (six entries) | Add calibrated `fuel_cap` + `failure_policy = "fail-closed"` to each of the six targeted validator plugins; atomic per-plugin commits |
| `CHANGELOG.md` | modify (Security section) | Mandatory CHANGELOG delivery |
| `crates/factory-dispatcher/src/registry.rs` | read-only | `FailurePolicy` enum and `RegistryEntry` struct delivered by S-21.10; no modifications unless a defect is found |
| `crates/factory-dispatcher/src/executor.rs` (`struct PluginOutcome` field addition) | modify (`failure_policy: FailurePolicy` field + all construction sites) | `struct PluginOutcome` is defined in `executor.rs`; all construction sites in `executor.rs` (grep-confirmed: zero constructions in `invoke.rs`). Add `failure_policy: FailurePolicy` to `struct PluginOutcome` and populate at ALL construction sites in `executor.rs`, including the hardcoded spawn-failure/`on_error: Continue` path — required so AC-002/003/004/010 exercise the real dispatch path end-to-end. **DISAMBIGUATION:** `routing.rs` contains `pub enum PluginOutcome` (a different type — do NOT modify `routing.rs` or `invoke.rs` for this task); see Task #10 (SR-005 plumbing sweep) |

## Routing Proposals (surfaced, not authored per instructions)

### BC authoring routing — RESOLVED

BC-1.03.017 (`factory-dispatcher::executor::failure_policy enforcement`) has been authored
at v1.7 (status: draft) and anchors AC-001 through AC-012 in this story via PC1–PC11
(PC10 added at v1.2 — deliberate revision of `fail_closed_timeout_with_on_error_block`;
Invariant 7 added at v1.2 — fail-open-window atomicity constraint; PC11 added at v1.3 —
hard migration-window completeness gate `test_no_on_error_block_without_fail_closed_when_3arg_executor`;
data-anchored name-independent PC11 detection + EC-004 bifurcation + H1 enriched at v1.4;
design-flow-independent broadened signal + three non-vacuity controls + annotation-landing
obligation at v1.5; LIVE-TREE-CONTROL (fourth control) + VACUITY-CONTROL tri-state
diagnostic fix + PC8/PC11 ownership clarification at v1.6; corpus calibration floor raised
to ≥4000 lines in Precondition 2 + PC10 sub-cases widened to Fuel|Epoch at v1.7).
The routing proposal is closed. BC-1.03.002 (`Plugin in tight arithmetic loop runs out of fuel`)
remains a brownfield-extracted stub; it is referenced as a detection-context sibling (demoted
from the governed BC table in v1.3) — enforcement postconditions are fully covered by
BC-1.03.017.

### D-442(e) disposition routing

If calibration shows `validate-input-hash` or `validate-template-compliance` exhausts fuel
on ≥4000-line `lessons.md` even at 50M cap (meaning the required cap would be impractically
large), proposed routing: **orchestrator** with calibration evidence. D-442(e) may need to
remain longer than anticipated, or Option B size-proportional budgeting (ADR-039 §Decision 4
Option B) may need expedited delivery.

## Changelog

| Version | Date | Description |
|---------|------|-------------|
| 1.10 | 2026-08-18 | Adversary pass-8 remediation (story-writer): [F-S2111-P8-002 MEDIUM — corpus floor undershoot] AC-007 corpus `lessons.md` floor and Phase-3 Task #2 item (a) raised from ≥3000 to ≥4000 lines (matching BC-1.03.017 v1.7 Precondition 2 + D-442(e) hard limit); parenthetical "(above the D-442(e) soft limit)" corrected to "(at the D-442(e) hard limit; soft limit is 3500, hard is 4000)"; EC-005 corpus reference raised to ≥4000 for consistency; D-442(e) disposition routing reference raised to ≥4000; BC-1.03.017 v1.6 → v1.7 propagated throughout (frontmatter behavioral_contracts, BC table title + version, AC-001/AC-011/AC-012 heading traces, AC-001 NEGATIVE-CONTROL body cite, AC-012 body PC11 cites, EC-004 Case A annotation-landing obligation cite, Token Budget BC description, Task #10 ATOMICITY GATE, Task #13 Invariant 7 cite, Task #14a header + four-controls cite, Architecture Compliance Rules fail-open-window row, Routing Proposals BC authoring history); BC-1.01.016 stays v1.3; POLICY 8 parity unaffected (PC1..PC11 ↔ AC-001..AC-012 unchanged; no AC renumbering) |
| 1.9 | 2026-08-18 | Adversary pass-7 residuals (story-writer): [F-S2111-P7-001 LOW] Task #14a DECOY NOTE reworded — removed `PluginOutcome` struct definition from detection scope (colon-init field declaration is not a `.failure_policy` DOT-ACCESS block-decision reference); narrowed detection scope to `.failure_policy` DOT-ACCESS within the block-decision chain (`execute_tier`/`execute_tiers`/helpers), consistent with AC-012 body and BC-1.03.017 PC11 design-flow-independent signal; [F-S2111-P7-002 ADVISORY] AC-007 corpus item 4 and Phase-3 Task #1 sibling path corrected from "the directory also holds `BC-5.39.010.md`" to "the tree also holds `ss-05/BC-5.39.010.md`" — BC-5.39.010.md is in an `ss-05/` subdirectory, not the same directory as BC-INDEX.md; load-bearing BC-INDEX.md fixture path (576,396 bytes) unchanged; POLICY 8 parity unaffected (BC cites BC-1.03.017 v1.6 + BC-1.01.016 v1.3 unchanged; PC1..PC11 ↔ AC-001..AC-012 mapping intact; no AC renumbering) |
| 1.8 | 2026-08-17 | Adversary pass-6 remediation (story-writer): [F-S2111-P6-001 HIGH POLICY 7] De-enriched BC-1.03.017 BC-table Title cell — removed downstream-only enrichment "design-flow-independent signal + three non-vacuity controls" (not present in BC H1); story Title cell is now a faithful abbreviation/subset of the H1; [F-S2111-P6-002 HIGH live-tree detector-fired] Added LIVE-TREE-CONTROL (fourth control) to AC-012 and Task #14a per BC-1.03.017 v1.6 PC11(d): at Phase-4-complete detector MUST run against ACTUAL executor.rs and return enforcement_active=true; POSITIVE-CONTROL synthetic snippet MUST be verbatim excerpt of real execute_tiers block-decision site; AC-012 test bullet and four-controls block updated; [F-S2111-P6-003 MEDIUM VACUITY wording] Fixed AC-012 and Task #14a VACUITY-CONTROL wording — replaced contradictory "evaluated the annotation-check branch…correctly skipped" with BC v1.6 form: detector's enforcement-detection logic ran and returned EnforcementAbsent (tri-state diagnostic), RED-emission skipped as consequence — distinguishes genuine Phase-1/2 GREEN from vacuous GREEN; AC-012 GREEN description updated to match; [F-S2111-P6-004 MEDIUM AC-001 MUST-vs-SHOULD] Fixed AC-001 Invariant-7 language — downgraded to explicit NON-NORMATIVE SHOULD-warn; AC-001 title simplified to calibration constraint only; added cross-reference to AC-012/PC11 as authoritative mechanical Invariant-7 gate; AC-001 test asserts ONLY calibration constraint; "MUST assert" reserved for AC-012; BC-1.03.017 v1.5→v1.6 propagated throughout (frontmatter, BC table, AC-001/AC-012 traces, Task #14a, Token Budget, Routing Proposals); POLICY 8 parity confirmed — 2 BCs, PC1..PC11 all traced, PC11↔AC-012; input-hash refresh owed (state-manager next burst) |
| 1.7 | 2026-08-17 | Adversary pass-5 remediation (story-writer): [F-S2111-P5-001 HIGH] AC-012 restructured as PURE FUNCTION over INJECTABLE inputs (synthetic executor-source snippet + synthetic registry, NOT live-tree scan) with three non-vacuity controls — POSITIVE-CONTROL (enforcement-active source + registry MISSING one annotation → RED), NEGATIVE-CONTROL (enforcement-active source + all five annotated → PASS), VACUITY-CONTROL (enforcement-ABSENT source → GREEN + assert annotation-check branch was evaluated; distinguishes real-GREEN from vacuous-GREEN); Task #14a updated with three control fixtures, decoy note, and PURE/INJECTABLE requirement; [F-S2111-P5-002 MEDIUM] AC-012 detection signal broadened to design-flow-independent (any block-decision site in executor block-decision chain — execute_tier/execute_tiers/helpers — referencing .failure_policy for a Timeout block decision, however the data reaches it); "structurally impossible to merge" softened to "mechanically detectable at any single commit; design-flow-independent" throughout (AC-012 body, Architecture Compliance Rules); [F-S2111-P5-004 LOW] Task #14a decoy warning added: executor.rs contains pre-existing `failure_policy: crate::registry::FailurePolicy::FailOpen` in test `emit_lifecycle_timeout_carries_fuel_cap_and_consumed` present TODAY before enforcement lands — bare grep false-matches; scope detection to executor block-decision chain / PluginOutcome struct definition; [F-S2111-P5-005 LOW] EC-004 Case A annotation-landing obligation added (BC-1.03.017 v1.6 PC11): S-21.13 MUST annotate validate-cross-site-correspondence fail-closed + calibrated fuel_cap >= 50M once O(n) algorithmic fix removes excessive cap; descope is timing deferral only, NOT permanent exemption; BC-1.03.017 v1.4→v1.5 propagated throughout (frontmatter, BC table, AC-001/AC-012 traces, Task #10/#13/#14a, Architecture Compliance Rules, Token Budget, Routing Proposals); POLICY 8 parity confirmed — 2 BCs, PC1..PC11 all traced, PC11↔AC-012; input-hash refresh owed (state-manager next burst) |
| 1.6 | 2026-08-17 | Adversary pass-4 remediation (story-writer): [F-S2111-P4-001] AC-012 detection made name-independent (data-anchored: `PluginOutcome` carrying `failure_policy: FailurePolicy` field + `execute_tiers` consulting it for block decisions; holds for extend-in-place OR introduce-a-replacement per Task #10); [F-S2111-P4-002] EC-004 bifurcated (Case A = `validate-cross-site-correspondence` on_error="continue" → VALID descope to S-21.13; Case B = five on_error="block" plugins → EC-004 FORBIDDEN, annotate-within-S-21.11 or block-the-flip, old path (b) record-transient-fail-open deleted, S-21.13 mis-route removed); AC-009 EC-004 conditional scoped to Case A only; AC-012 five-plugin assertion stands unconditionally (no reduced-set escape); AC-009/AC-012 mutual consistency confirmed; [F-S2111-P4-003] BC table title updated to faithful derivation of enriched BC-1.03.017 v1.4 H1 (adds "half-state" to structural gate, "on_error=block" + "(PC11 CWE-636 static gate)" to migration-window gate); [F-S2111-P4-004] "(line 42)" volatile pin removed from File Structure Requirements `struct PluginOutcome` row (struct-name + executor.rs is the stable anchor per TD-VSDD-091); [Observation] AC-007 corpus item 4, Task #1, Previous Story Intelligence S-21.07 row corrected to cite file path `…/a1-production-scale/factory/specs/behavioral-contracts/BC-INDEX.md` (576,396 bytes) instead of directory; BC-1.03.017 v1.3 → v1.4 propagated throughout (frontmatter, BC table, AC-001/AC-012 traces, Token Budget, Task #10/#13/#14a cites, Architecture Compliance Rules, Routing Proposals); POLICY 8 parity confirmed — 2 BCs, PC1..PC11 all traced, PC11↔AC-012; input-hash refresh owed (state-manager next burst) |
| 1.5 | 2026-08-17 | Adversary pass-3 remediation (story-writer): [F-S2111-P3-001] 50M boundary sibling sweep — `fuel_cap >= 50_000_000` ACCEPT / `fuel_cap < 50_000_000` REJECT throughout (AC-001 title, body prose, GREEN/RED descriptions, positive-control fixture value, test bullet, AC-008, AC-009, Task #7, Architecture Compliance Rules); POSITIVE-CONTROL fixture changed from `fuel_cap ≤ 50_000_000` to `fuel_cap = 20_000_000` (factory default per ADR-042 §Decision 2, clearly below floor and realistic); boundary-pass assertion added (exactly `fuel_cap = 50_000_000` PASSES gate — inclusive floor); `fuel_cap >= 50M` note added to GREEN final-state description; [F-S2111-P3-002] PluginOutcome anchor corrected — File Structure Requirements and Task #10 re-anchored from `invoke.rs` to `executor.rs` (struct PluginOutcome defined at executor.rs:42 and ALL constructions in executor.rs; grep-confirmed zero constructions in invoke.rs); disambiguation note added for decoy `pub enum PluginOutcome` in routing.rs (different type — do not modify); [F-S2111-P3-003] AC-002 BC-1.01.016 PC trace corrected — title changed PC5/PC7 to PC1/PC5; inline trace body updated: PC5 false description ("field parseable/accessible") replaced with correct "axes-independence — independent field in RegistryEntry alongside on_error"; PC7 false description ("absent defaults to fail-open, exercised by AC-003") removed; PC1 added ("failure_policy = fail-closed parses to FailClosed — field parseable from TOML and accessible on RegistryEntry"); false "exercised by AC-003" absent-default claim removed; BC table clause column for BC-1.01.016 updated to PC1/PC5 with accurate descriptions; [F-S2111-P3-004] EC-004 path (b) circular-dependency wording fixed — deleted unsatisfiable "must not be merged without S-21.13 completing first" (circular: S-21.13 depends_on [S-21.10, S-21.11]); replaced with "no merge of any commit that relies on the deferred plugin's fail-closed enforcement until that plugin's annotation lands in S-21.13" (BC-1.03.017 coherent wording); path (a) marked PREFERRED; [F-S2111-P3-005] AC-012 added (append-only, not renumbered, BC-1.03.017 v1.3 PC11 — hard migration-window completeness CI gate `test_no_on_error_block_without_fail_closed_when_3arg_executor`: if 3-arg plugin_fail_closed present then every on_error=block targeted plugin MUST carry failure_policy=fail-closed; GREEN pre-flip and at Phase 4 complete, RED on bad intermediate CWE-636 state); Task #14a added for AC-012 test; Task #10 and Task #13(a) atomicity warnings strengthened — explicit implementer advisory that per-task commits WILL trigger AC-012 RED; Architecture Compliance Rule for fail-open-window hazard updated with AC-012 CI gate reference; BC table updated to v1.3 with PC11↔AC-012; BC-1.03.017 v1.2→v1.3 propagated throughout (frontmatter, BC table, AC-001/011 traces, Task #10/#13 Invariant 7 citations, Token Budget, Routing Proposals); POLICY 8 parity confirmed — PC1..PC11 all traced; PC11↔AC-012 confirmed; input-hash refresh owed (state-manager next burst) |
| 1.4 | 2026-08-17 | Adversary pass-2 remediation (story-writer): [F-S2111-P2-001] AC-001 extended with BC-1.03.017 v1.2 Invariant 7 fail-open-window invariant (no on_error="block" targeted plugin may remain at failure_policy=fail-open once decision function is extended); NEGATIVE-control fixture added to AC-001 (fuel_cap=75_000_000 MUST ACCEPT/PASS, POLICY 15 gate-outcome discipline); Tasks #10/#13 restructured for annotate-before-flip atomicity; Architecture Compliance Rule added for fail-open-window hazard; [F-S2111-P2-002] AC-011 added (append-only, BC-1.03.017 v1.2 PC10 — deliberate revision of fail_closed_timeout_with_on_error_block, both FailOpen and FailClosed branches); Task #12 updated to name fail_closed_timeout_with_on_error_block explicitly; [F-S2111-P2-003] EC-004 updated — on_error="block" plugin deferral is NOT behavior-neutral (CWE-636 reintroduction hazard), validate-cross-site-correspondence (on_error=continue) carve-out explicit, must retain fallback or gate S-21.13 as HARD BLOCKER; [F-S2111-P2-004] AC-001 NEGATIVE-control fixture added; [F-S2111-P2-006] EC-004 follow-up story named S-21.13 (depends_on [S-21.10, S-21.11]); [F-S2111-P2-007] Task #13 TD-VSDD-060 sweep note added for stale validate-cross-site-correspondence comment correction; [F-S2111-P2-008] AC-009 reduced-set mechanism specified (rename/parameterize test under EC-004 descope); BC-1.03.017 v1.1→v1.2 propagated throughout (frontmatter, body BC table, AC traces, Routing Proposals); POLICY 8 parity — PC1–PC10 all traced, PC10↔AC-011 confirmed; input-hash refresh owed (state-manager next burst) |
| 1.3 | 2026-08-17 | Spec-review remediation (story-writer): [F-S2111-P1-002] subsystems SS-05→SS-07 (SS-07 owns hooks-registry.toml per ARCH-INDEX); [F-S2111-P1-003/SR-001] AC-001 reclassified as standing regression/invariant gate — GREEN when zero fail-closed entries, GREEN at final state, RED on bad half-state; POSITIVE-CONTROL fixture required; Task #7 reframed; [F-S2111-P1-004/SR-008] threshold 20M→50M throughout (AC-001, AC-008, Architecture Compliance Rules); [F-S2111-P1-005] POLICY 8 parity — BC-1.03.017 v1.0→v1.1 in frontmatter+body+AC traces; AC-002 trace added to BC-1.01.016 PC5/PC7; BC-1.03.002 demoted from governed BC table to detection-context prose note; Token Budget BC label updated; [SR-002] EC-004 descoping resolution path added; AC-009 conditional on post-amendment targeted set; [SR-003] AC-002 bats leg scoped to TEST-FIXTURE registry; AC-001 scan target scoped to production registry only; [SR-004] PluginResult::Error→Crashed in AC-005 sub-tests + EC-002/EC-003; [SR-005] PluginOutcome.failure_policy plumbing enumerated in Task #10 + File Structure; [SR-006] AC-007 calibration methodology tightened — observed max over corpus; fuel_consumed read from Ok path only; [SR-007] wasmtime 44.0.1→46.0.2; [SR-009] EC-004 ceiling constant PRACTICAL_FUEL_CEILING=500_000_000; [SR-010] Task #12 call-site sweep added; [SR-011] AC-002 bats fixture note added to Task #8; input-hash refresh owed (state-manager next burst) |
| 1.2 | 2026-08-17 | Spec refresh (story-writer; pre-Phase-3 readiness): BC-1.01.016 cite v1.0→v1.3 (POL-14 auto-promoted at S-21.10 merge; v1.1–v1.3 non-semantic corrections — struct name HookEntry→RegistryEntry, count de-hardcode, modified[] parity); BC table struct name HookEntry→RegistryEntry (BC-1.01.016 v1.1 correction); ADR-039 v1.3 reconciliation — AC-001 fuel-default parenthetical updated (10M→20M factory default per ADR-042 §Decision 2; no numeric threshold change), Architecture Compliance Rules epoch clause source corrected (Decision 1 only, Decision 4 reference removed); AC-007 + Phase-3 Task #1 + Previous Story Intelligence: corrected stale "S-21.07 task #33" citation — fixture confirmed committed at plugins/vsdd-factory/tests/fixtures/validate-cross-site-correspondence/a1-production-scale/ (576,396 bytes); Routing Proposals BC authoring routing closed — BC-1.03.017 v1.0 authored (D-442(e) disposition routing remains open); input-hash refresh owed (state-manager next burst) |
| 1.1 | 2026-08-06 | BC wiring (story-writer): behavioral_contracts → ["BC-1.01.016 v1.0", "BC-1.03.017 v1.0"]; POLICY 8 cascade — body BC table updated (BC-1.03.017 primary, BC-1.01.016 prerequisite, BC-1.03.002 detection precondition), AC-001 through AC-009 traces updated to specific BC PCs/Preconditions/Invariants, AC-010 added (BC-1.03.017 PC6 epoch-exhaustion gap closure), Token Budget updated; input-hash refreshed |
| 1.0 | 2026-08-06 | Initial authoring (story-writer; E-21 W6; ADR-039 Decision 2+3+4+6; depends_on S-21.10; CWE-636 closure for six validator-class WASM plugins; structural Phase-3-before-Phase-4 gate via AC-001) |
