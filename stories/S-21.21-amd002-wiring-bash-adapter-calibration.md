---
document_type: story
level: implementation
story_id: "S-21.21"
epic_id: "E-21"
version: "1.4"
status: draft
producer: story-writer
timestamp: 2026-08-20T00:00:00Z
phase: brownfield-backfill
inputs:
  - .factory/specs/architecture/decisions/ADR-039-validator-failure-policy-resource-exhaustion-fail-closed.md
  - .factory/specs/architecture/decisions/ADR-044-split-topology-enforcement-flip-capstone-ownership.md
  - .factory/research/wasm-fuel-exhaustion-detection.md
  - plugins/vsdd-factory/hooks-registry.toml
input-hash: "1e3efaa"
traces_to: .factory/specs/architecture/decisions/ADR-039-validator-failure-policy-resource-exhaustion-fail-closed.md
points: "9"
depends_on: [S-21.10, S-21.19]
blocks: [S-21.24]
behavioral_contracts: ["BC-1.03.017 v1.22"]
verification_properties: []
priority: "P1"
cycle: v1.0-brownfield-backfill
wave: 7
target_module: crates/factory-dispatcher
subsystems: [SS-01, SS-02, SS-04, SS-07]
estimated_days: null
assumption_validations: []
risk_mitigations: []
tdd_mode: strict
modified:
  - "2026-08-23 (v1.4) — Wave-7 pass-3/R2 story-layer remediation (D-1074; story-writer). **F-S2121-P3-001 (HIGH) — Task 5a ADDITIVE wiring rewrite:** the wave-7 error-exit wiring step is ADDITIVE, NOT a literal replacement — RETAIN the existing 2-arg `plugin_fail_closed(result, on_error)` call AND ADD `plugin_fail_closed_on_error_exit(result, on_error)` alongside it (OR-combined), NEVER removing the 2-arg call (it is the sole live disjunct for `Timeout + on_error=Block` during the wave-7→8 window; a literal replacement would open a Timeout fail-open window for one full wave per BC-1.03.017 v1.22 Invariant 12). Task 5a body rewritten with ADDITIVE language; added explicit regression test: `Timeout + on_error=Block` still blocks post-Task-5a additive wiring (proving retained 2-arg call still handles it). Architecture Mapping executor.rs row, Architecture Compliance Rules row, and File Structure Requirements row all updated from `replacing` to ADDITIVE language. ADR-044 v1.1/v1.2 → v1.3 updated at Task 5a header, Architecture Mapping, Architecture Compliance Rules, File Structure Requirements, Tasks section intro, AC-013b/AC-013c headers, Token Budget, PSI S-21.19 row. **F-S2121-P3-002 (MEDIUM) — EC-011 baseline correction:** EC-011's claim that 'in BOTH sub-cases the calibrated budget is silently violated with no fail-closed enforcement' was WRONG for sub-case (b) — `Timeout{cause: Epoch}` IS blocked today by the existing 2-arg `plugin_fail_closed(result, on_error)` call for `on_error=Block` plugins; only sub-case (a) `Ok{exit_code:1}` lacks enforcement absent PC13. EC-011 corrected in both the Edge Cases table and the AC-013 body. `BC-1.03.017 v1.21 → v1.22` re-anchored with full POLICY 8 propagation: frontmatter `behavioral_contracts`, body BC table (version v1.21→v1.22 + title extended with ', observed_max×1.5 fuel_cap calibration-sufficiency gate against a FROZEN corpus snapshot, and wave-7→8 migration coverage-continuity invariant'), every citing AC header (AC-007/AC-013/AC-013b/AC-013c), Token Budget, all Task cites, Architecture Compliance Rules. `points:`/`depends_on:`/`blocks:`/wave/status UNCHANGED (9 pts, wave 7, `draft`). Does NOT touch STATE.md, STORY-INDEX, BC-INDEX, ARCH-INDEX, VP-INDEX, or the BC/ADR spec files (state-manager's domain)."
  - "2026-08-22 (v1.3) — Wave-7 pass-2 remediation (D-1071; architect adjudication, RATIFIED
    2026-08-22). **F-S2121-P2-001 (HIGH) — error-exit function-scope correction:** Task 5a's
    wired `plugin_fail_closed_on_error_exit` was previously described in the Architecture
    Compliance Rules row as governing `on_error=Block` + `Ok{exit_code!=0}` \"ONLY\" — WRONG,
    this silently dropped the pre-existing `Crashed` fail-closed guarantee for all 18
    `on_error=Block` registry entries. Corrected (per ADR-044 v1.2's own item-2 correction, and
    matching S-21.19's own already-clean body): the function governs `Crashed | Ok{exit_code!=0}`
    — `Timeout` remains unconditionally excluded. Task 5a's own body strengthened to explicitly
    require the `Crashed` arm be RETAINED (not dropped) by the call-site replacement, plus a
    real-dispatch crash-still-blocks regression check. **F-S2121-P2-002 (MEDIUM):** fixed a
    dangling `Task-6a` (hyphenated) reference in AC-013b's test bullet — my round-1 file-wide
    rename covered space-separated `Task 6a` but missed this hyphenated form — corrected to
    `Task-5a`. **F-S2121-P2-003 (MEDIUM):** `BC PC3` corrected to `BC Precondition 3` at Task
    11 (the `timeout_ms` formula lives in a Precondition, not Postcondition PC3, which governs
    `on_error` independence for exhaustion) and, per explicit coordinator direction, at the
    v1.2 `modified:`/Changelog historical notes' own mis-citation (annotated in-place with a
    correction bracket rather than silently rewritten, preserving the historical record).
    **F-S2121-P2-004 (MEDIUM):** AC-013's \"reconciled with PC12\" claim was premature when
    written at v1.2 (BC-1.03.017 v1.20's PC12 was not yet one-sided at that time); now that
    BC-1.03.017 v1.21 has genuinely rewritten PC12 to the one-sided, hundreds-of-ms-observed-
    margin model this story's own AC-013/Test bullet already used, the citation is corrected to
    reference the CORRECTED v1.21 PC12 and made truthful (AT-OR-AFTER-`X` floor + CI-jitter-
    robust observed window, internal ~5ms poll granularity explicitly distinguished from the
    e2e-observable margin). `BC-1.03.017 v1.20 -> v1.21` re-anchored with full POLICY 8
    propagation: frontmatter, BC table, every citing AC, Token Budget, all Task cites.
    `points:`/`depends_on:`/`blocks:`/wave/status UNCHANGED (9 pts, wave 7, `draft`). Does NOT
    touch S-21.19/S-21.20/S-21.22/S-21.23/S-21.24 (concurrent 3b sub-burst owns S-21.20/23/24;
    S-21.19 is this sub-burst's own separate file, edited independently), BC-1.03.017/ADR-044
    themselves (already landed at commit 8ef46b8a), or any INDEX/STATE.md (state-manager's
    domain)."
  - "2026-08-22 (v1.2) — Wave-7 pass-1 remediation (D-1069; architect Q1/Q2 adjudication,
    RATIFIED 2026-08-22): **Q1/F-S2121-P1-002 (HIGH) — new live-wiring task + AC prerequisite
    correction.** Per ADR-044 v1.1 Addendum (Option A), this story now owns the LIVE WIRING of
    `plugin_fail_closed_on_error_exit(result, on_error)` into the `execute_tiers`/`execute_tier`
    real block-decision loop at wave 7 (new Task 5a, inserted after S-21.19's unit predicate
    check and before the existing Task 6 end-to-end fixture test) — so a real dispatch actually
    blocks on `Ok{exit_code:1}` for `on_error=Block` at wave 7, not merely a merged-but-unwired
    unit predicate. AC-013b/AC-013c's stated prerequisites corrected: previously cited
    \"S-21.19's unit predicate has landed\" as sufficient for the end-to-end leg — WRONG, a
    merged-but-unwired predicate cannot make a real dispatch block. Both ACs now correctly cite
    this story's own new Task 5a wiring task as the prerequisite. **F-S2121-P1-001 (calibration)
    — Task 11 statistic-name fix:** `observed_max_ms` corrected to `measured_p99_ms` in the
    `timeout_ms` computation task, matching its own AC-007 body and BC Precondition 3's
    unchanged `timeout_ms` axis formula (`max(measured_p99_ms × 2.0, 30_000)` — the
    `observed_max × 1.5` correction applies ONLY to the `fuel_cap` axis, per ADR-039 §Decision
    4 v1.16/§Erratum E-007; the `timeout_ms` axis was never affected). [F-S2121-P2-003,
    2026-08-22: this note's own original wording, written at v1.2, mis-cited the formula's
    home as "BC PC3" — corrected to "BC Precondition 3" the formula's actual location; PC3 is
    a Postcondition (`on_error` independence for exhaustion), a different clause entirely.]
    AC-007/Task 10 `fuel_cap` wording updated to cite the corrected BC Precondition 3 formula
    (`observed_max × 1.5`) and to additionally require
    coverage of new BC Precondition 6's machine-checkable sufficiency regression assertion
    (`fuel_consumed × 1.5 <= registry fuel_cap` against the mandated corpus), not merely the 50M
    floor. **F-S2121-P1-003 (MEDIUM) — AC-013 kill-time tolerance restated as a ONE-SIDED
    bound:** the observed-kill-time assertion (`≈X` plus poll tolerance) is now stated as
    `X <= observed_kill_time_ms <= X + margin_ms`, with `margin_ms` a CI-jitter-robust value in
    the hundreds of milliseconds (still ≪ 60s) rather than the ambiguous `±5ms` framing,
    reconciled with BC PC12's own one-sided wording (kill fires AT OR AFTER `X`, never before).
    `BC-1.03.017 v1.19 -> v1.20` re-anchored with full POLICY 8 propagation: frontmatter
    `behavioral_contracts`, body BC table, every citing AC (AC-007/AC-013/AC-013b/AC-013c
    headers and bodies), Token Budget, and all Task cites. Does NOT touch S-21.19 (concurrent
    story-writer sub-burst owns S-21.19's own Task 6 narrowing — the two halves of the same
    ADR-044 v1.1 split), S-21.20/S-21.22/S-21.23/S-21.24, BC-1.03.017/ADR-044 themselves
    (already landed at commit a3bfa1af), or any INDEX/STATE.md (state-manager's domain)."
  - "2026-08-21 (v1.1) — Wave-7 pre-cascade re-anchor burst (D-1060 deferral, EXTENDED D-1064; story-writer): BC-1.03.017 v1.18 -> v1.19 re-anchor (S-21.19's F-S2119-P2-001 pass-2 remediation moved Invariant 7's CWE-636 regression trigger from function-authoring to executor-wiring, keyed on the same PC11 static-scan signal; this story's AC-007/AC-013/AC-013b/AC-013c narrative is unaffected in substance -- version string only). Frontmatter `behavioral_contracts` and all body cites (AC-007/AC-013/AC-013b/AC-013c headers, body prose at Precondition 2/5, PC12 correction note, BC table, Token Budget, Tasks 1/5/6, Architecture Compliance Rules) swept to v1.19. No AC text, BC trace, or test predicate changed. Does NOT touch S-21.19/S-21.20/S-21.22/S-21.23/S-21.24, BC-1.03.017/BC-1.03.018 themselves, or any INDEX/STATE.md (state-manager's domain)."
  - "2026-08-20 (v1.0) — created by story-writer via the operator-directed split of CONVERGED S-21.11 (v2.11, 16-pass 3-CLEAN BC-5.39.001 convergence) per `.factory/planning/S-21.11-decomposition-plan.md` §1 (S-21.21 detail) and §3 (AC partition table). Owns AC-007 (bash-adapter leg only), AC-013, AC-013b (end-to-end leg only), AC-013c, copied verbatim from S-21.11 v2.11 with zero weakening. Tasks renumbered #1–#12 (self-contained) from S-21.11 v2.11 Tasks #6, #7, #8, #9, #10, #10b, #10c, #11, #12, #13, #14, #15."
---

> **tdd_mode: strict** — Full TDD Iron Law enforced: all non-trivial function bodies use `todo!()`;
> Red Gate density check ≥0.5 required before Step 4 dispatch.

> **Execute:** `/vsdd-factory:deliver-story S-21.21`

> **Split origin (2026-08-20).** One of six sub-stories partitioning CONVERGED S-21.11 (v2.11,
> 16-pass 3-CLEAN BC-5.39.001 convergence) per the operator-directed override of S-21.11's own
> prior "unified scope, no split" decision, documented in
> `.factory/planning/S-21.11-decomposition-plan.md`. S-21.21 carries the widest blast radius of
> the six seams — the AMD-002 wiring fix affects all 37 `legacy-bash-adapter.wasm`-routed
> registry entries, not only the five plugins this story calibrates — and is the basis for the
> ADR-039 `subsystems_affected` sweep finding closed directly by the architect in the
> decomposition plan (§5). S-21.11's v2.11 body is frozen as the historical source-of-truth
> this story's ACs are extracted from verbatim; no AC text, BC trace, or test predicate was
> weakened, dropped, or reinterpreted during partitioning.

# S-21.21: AMD-002 `legacy-bash-adapter` runtime-timeout wiring fix + bifurcated bash-adapter calibration (ADR-039 Phase 3b+3c; split seam 3 of 6 from S-21.11)

## Narrative

- **As a** factory pipeline operator whose registry-config `timeout_ms` calibration for the
  five `legacy-bash-adapter.wasm`-hosted validator plugins currently has **zero effect** on
  the bash subprocess's actual kill deadline
- **I want** `legacy-bash-adapter/src/lib.rs` to honor the registry's calibrated `timeout_ms`
  instead of the hardcoded `BASH_TIMEOUT_MS = 60_000` constant, and per-plugin
  `fuel_cap`/`timeout_ms` calibration for the five bash-adapter plugins measured against that
  wiring-fixed code path
- **So that** calibrating and raising the registry's `timeout_ms` is not a no-op — the
  necessary-but-not-sufficient gap ADR-039 §AMD-002 identifies is closed at the actual
  enforcement point, and S-21.24's flip of these five plugins rests on real, wiring-verified
  calibration evidence rather than a number that reaches nothing

## Context / Finding Summary

**The AMD-002 runtime-wiring gap (RATIFIED v1.10).** Calibrating and raising the registry's
`timeout_ms` for the five `legacy-bash-adapter.wasm`-hosted plugins has, as currently coded,
**zero effect** on the bash subprocess's actual kill deadline: `legacy-bash-adapter/src/lib.rs`
hardcodes `BASH_TIMEOUT_MS = 60_000` and passes that constant — not the registry's calibrated
`timeout_ms` — into `exec_subprocess.rs::run()`. Registry-config calibration (`timeout_ms`
set in `hooks-registry.toml`) is therefore **necessary but not sufficient**: the value must
also reach the real enforcement point. This is a distinct engineering task from calibration
itself (measuring the right number vs. wiring the right number through) — this story delivers
both, in the correct order (wiring first, calibration second), because pre-wiring calibration
measures the wrong mechanism.

**Safe ordering within this story (from S-21.11 v2.11's original DAG, nodes (B) and (C)):**

```
(B) AMD-002 RUNTIME WIRING                        — MUST precede (C)'s meaningful calibration
    wire registry timeout_ms through executor.rs → hook-sdk payload →
    legacy-bash-adapter (AC-013 / PC12)
        │  "meaningful calibration" edge: (C)'s time_consumed_ms measurements are only
        │  evidence of the REAL enforcement point once (B) has landed — pre-wiring,
        │  measuring against the hardcoded 60s backstop calibrates the wrong mechanism.
        ▼
(C) PER-PLUGIN timeout_ms CALIBRATION (5 bash-adapter plugins) — depends on (B)
    measure time_consumed_ms + fuel_consumed over corpus for the five
    bash-adapter plugins; set fuel_cap + timeout_ms (AC-007 / PC2,3,4 / Inv 8)
```

**Targeted `legacy-bash-adapter.wasm`-hosted plugins (fuel-axis necessary but NOT sufficient —
`timeout_ms` additionally required, AND the AMD-002 wiring fix must land for that `timeout_ms`
to mean anything at the real enforcement point):**
- `validate-factory-path-root` (PostToolUse, `on_error="block"`)
- `validate-input-hash` (PostToolUse, `on_error="block"`)
- `validate-template-compliance` (PostToolUse, `on_error="block"`)
- `validate-wave-gate-prerequisite` (**PreToolUse `^Agent$`**, `on_error="block"` — self-lock
  hazard; requires S-21.23's break-glass before/atomic-with its S-21.24-delivered fail-closed
  flip)
- `validate-pr-merge-prerequisites` (**PreToolUse `^Agent$`**, `on_error="block"` — self-lock
  hazard; same as above)

**This story does NOT flip any of these five plugins to `failure_policy = "fail-closed"`** —
it wires the enforcement point and produces the calibration evidence (`fuel_cap`/`timeout_ms`
values, recorded in the PR description) that S-21.24 uses to perform the actual flip
(three non-Agent plugins) once S-21.23's break-glass code is also merged (two Agent-gate
plugins).

## Position in the Split DAG

```
S-21.10 (merged) ──┐
                    ├──▶ S-21.21 (this story) ──▶ S-21.24 (capstone: flip using this
S-21.19 (merged) ──┘                              story's calibration + AC-013b e2e leg)
```

The full six-seam dependency graph and wave schedule live in
`.factory/planning/S-21.11-decomposition-plan.md` §2. This story depends on S-21.19 because
AC-013b's end-to-end fixture leg (owned here) asserts against S-21.19's Task-6 decision-function
predicate — the wiring fix itself (AC-013) has no dependency on S-21.19.

## Acceptance Criteria

### AC-007 (bash-adapter leg): Per-plugin calibration against production-scale corpus, measured against the AMD-002-wired code path — `fuel_cap` AND `timeout_ms` for `legacy-bash-adapter.wasm`-hosted plugins (traces to BC-1.03.017 v1.22 Preconditions 2–6, enabling PC9; ADR-039 §Decision 4 Option A + host-wall-clock-timeout-axis formula + §Decision 4 v1.16/§Erratum E-007 target-statistic correction)

**Leg-scope note (per `.factory/planning/S-21.11-decomposition-plan.md` §3):** AC-007 has two
legs. This story owns the `legacy-bash-adapter.wasm`-hosted leg (the five bash-adapter
plugins) — the native-WASM leg (`validate-cross-site-correspondence`) is owned by **S-21.22**.

For each of the five `legacy-bash-adapter.wasm`-hosted plugins
(`validate-factory-path-root`, `validate-input-hash`, `validate-template-compliance`,
`validate-wave-gate-prerequisite`, `validate-pr-merge-prerequisites`), calibration is
bifurcated by adapter class (ADR-039 §Decision 1/2/3/4 v1.8 amendment, §AMD-001). BOTH of the
following MUST be measured and set — neither substitutes for the other:
- `fuel_cap` set to `max(observed_max × 1.5, 50_000_000)` for the adapter's own marshaling
  step (this axis bounds only the adapter shim, not the bash script it invokes).
- `timeout_ms` set to `max(measured_p99_ms × 2.0, 30_000)`, where `measured_p99_ms` is the
  bash subprocess's wall-clock `time_consumed_ms` over the calibration corpus.

**DAG dependency (this AC's `timeout_ms` measurement depends on this story's own AC-013 wiring
fix landing first — see "Context / Finding Summary" above):** The `timeout_ms` measurement
above MUST be taken against the code path AFTER AC-013's AMD-002 wiring fix has landed.
Measuring `time_consumed_ms` before the wiring fix still produces a real wall-clock number
(the bash script runs for however long it runs regardless of wiring), but the CALIBRATED VALUE
WRITTEN TO THE REGISTRY has no effect on the actual kill deadline until AC-013 lands
(BC-1.03.017 v1.22 Precondition 5) — so calibration performed pre-wiring is not evidence that
the resulting `hooks-registry.toml` state is protective. Devops-engineer MUST run (or re-run)
the bash-adapter calibration pass AFTER AC-013 is merged within THIS story, not before. If
calibration data exists from before AC-013 landed, it MUST be re-validated (a re-run against
the wired code path, not merely re-asserted) before being used as evidence.

The calibration corpus MUST include:

1. `lessons.md` at ≥4000 lines (at the D-442(e) hard limit; soft limit is 3500, hard is 4000)
2. `STATE.md` at current live size
3. `decision-log.md` at current live size
4. The ≥574 KB synthetic fixture committed at `plugins/vsdd-factory/tests/fixtures/validate-cross-site-correspondence/a1-production-scale/factory/specs/behavioral-contracts/BC-INDEX.md` (576,396 bytes; `BC-INDEX.md` is the calibration input file — the tree also holds `ss-05/BC-5.39.010.md`; added in S-21.07 merge e94767bc)

The same corpus backs both `fuel_consumed` and `time_consumed_ms` measurements — only the
metric collected differs by adapter class (BC-1.03.017 v1.22 Precondition 2).

**Calibration-sufficiency regression assertion (BC-1.03.017 v1.22 Precondition 6, Q2/F-S2122-P1-002):**
`fuel_cap` calibration is complete for each of the five plugins ONLY IF it additionally
satisfies a MACHINE-CHECKABLE regression assertion, not merely a documented value: re-run each
plugin against the mandated calibration corpus and assert `fuel_consumed × 1.5 <= registry
fuel_cap` for the corpus's observed `fuel_consumed` reading. The 50,000,000 floor (Invariant 2,
PC8) is a necessary schema-level minimum but is NOT independently sufficient evidence of
calibration for any plugin whose `observed_max × 1.5` exceeds it — this story's calibration log
MUST record the passing regression assertion result per plugin, not merely the chosen `fuel_cap`
value.

A calibration run MUST be executed for each of the five plugins by the devops-engineer role
before the implementer annotates hooks-registry.toml (in S-21.24, not this story — this story
records the calibration evidence for S-21.24 to apply). The measured `fuel_consumed`/`time_consumed_ms`
values and the chosen `fuel_cap`/`timeout_ms` MUST be recorded in the PR description or a
calibration log artifact, not merely asserted in code.

Any plugin whose calibration reveals a required `fuel_cap` that would exceed
`PRACTICAL_FUEL_CEILING` (`PRACTICAL_FUEL_CEILING = 500_000_000`, even at observed_max×1.5
for the largest corpus input) MUST be surfaced to the orchestrator rather than silently
annotated with an insufficient cap. Per S-21.19's AC-009, EC-004 is **FORBIDDEN** for these
five `on_error="block"` plugins — there is no valid descope path; the required action is (a)
annotate fail-closed with whatever calibrated cap is feasible (PREFERRED), or (b) block the
entire Phase-4 flip pending per-plugin algorithm optimization. The same surface-to-orchestrator
obligation applies if a required `timeout_ms` (per the host-wall-clock-timeout-axis formula)
would be impractically large: treat any `timeout_ms` calibration result exceeding 120_000 ms
(2 minutes) as a signal requiring orchestrator review before annotation, since it would
materially change the two Agent gates' worst-case dispatch latency.

- **Test:** S-21.19's AC-001 gate (`test_no_fail_closed_plugin_with_uncalibrated_cap`) enforces
  the structural invariant on both axes once S-21.24 applies this story's calibration; the
  calibration log this story produces provides the human-auditable evidence

### AC-013: AMD-002 runtime-wiring fix — `legacy-bash-adapter` honors the registry's calibrated `timeout_ms` instead of the hardcoded `BASH_TIMEOUT_MS` constant (traces to BC-1.03.017 v1.22 PC12; ADR-039 §AMD-002, RATIFIED v1.10)

For any `[[hook]]` entry with `plugin = "hook-plugins/legacy-bash-adapter.wasm"` and
registry field `timeout_ms = X` (resolved via `RegistryEntry::timeout_ms(&defaults)`, which
already exists in `crates/factory-dispatcher/src/registry.rs`), a real dispatch that invokes
that plugin against a bash subprocess whose runtime exceeds `X` MUST have its subprocess
killed within the ONE-SIDED bound `X <= observed_kill_time_ms <= X + margin_ms` (F-S2121-P1-003,
now genuinely CORRECT per the corrected BC-1.03.017 v1.22 PC12 — F-S2121-P2-004: PC12 itself
was rewritten this pass to the same one-sided, hundreds-of-ms-margin model this AC already used,
closing a story-vs-BC divergence that existed against the pre-correction v1.20 PC12, which still
described the ~5 ms internal poll interval as if it were the e2e-observable bound): kill fires
AT OR AFTER `X`, `X` is a FLOOR never a ceiling, the kill MUST NOT fire before `X` — matching
BC-1.03.017 v1.22 PC12's corrected AT-OR-AFTER-`X` floor plus its explicit distinction between
(i) the internal ~5 ms poll granularity (`exec_subprocess.rs::run()`'s own sampling interval —
an implementation-mechanism detail, not an e2e-observable bound) and (ii) the CI-jitter-robust,
hundreds-of-ms OBSERVED end-to-end kill-time window this AC's own assertion is stated against —
and completes within a CI-jitter-robust `margin_ms` measured in the hundreds of milliseconds. The
`~5 ms` figure is `exec_subprocess.rs::run()`'s steady-state poll interval under ideal
scheduling; `margin_ms` is deliberately wider than that steady-state figure to absorb CI-runner
scheduling jitter, while remaining ≪ the current hardcoded 60,000 ms
(`legacy_bash_adapter::BASH_TIMEOUT_MS`) this fix replaces). This is a distinct enforcement point from
wasmtime's `EPOCH_TICK_MS` (10 ms, `crates/factory-dispatcher/src/engine.rs`) — that ticker
governs guest-WASM epoch interruption and is unrelated to this host-level subprocess-kill
deadline; do not conflate the two.

**This is a GENERIC, adapter-level fix, not a per-plugin one.** The defect affects all 37
`legacy-bash-adapter.wasm`-routed `hooks-registry.toml` entries (ADR-039 §AMD-002 blast-radius
finding, v1.10), not only the five §Decision 2 plugins. `test_legacy_bash_adapter_honors_registry_timeout_ms`
MUST therefore be written generically against the adapter's wiring behavior (any
`legacy-bash-adapter.wasm`-hosted entry, exercised via a synthetic test registry entry), not
hardcoded to only the five targeted plugins — the fix is adapter-level. This story's
calibration scope (AC-007) remains the five named plugins only; AC-013 does not expand that set.

**Host-to-guest plumbing gap (this is what makes the defect possible).** The dispatcher-side
`RegistryEntry.timeout_ms(&defaults)` value is resolved and used to set the WASM store's
epoch deadline (`crates/factory-dispatcher/src/invoke.rs`), but it is NOT currently included
in the per-plugin JSON envelope (`payload_value` / `per_plugin_value`) that
`crates/factory-dispatcher/src/executor.rs`'s `execute_tier` and `spawn_async_plugin`
functions splice `plugin_config` onto before serializing to bytes and piping to the guest via
WASI stdin (confirmed: both functions currently insert only `"plugin_config"` into that JSON
map). The guest-facing `HookPayload` in `crates/hook-sdk/src/payload.rs` has no `timeout_ms`
field to receive it even if the host did send it. `legacy-bash-adapter/src/lib.rs::run_bash_via_host`
therefore has no host-known value to read and falls back to its own hardcoded constant.

**Implementer design choice (not prescribed by this story):** the exact host-to-guest channel
is an implementer decision — a new typed field on `HookPayload` (host-populated from the
dispatched `RegistryEntry`'s resolved `timeout_ms` at the same point `execute_tier`/
`spawn_async_plugin` already splice in `plugin_config`) is the natural mechanism, consistent
with the existing `plugin_config` precedent; an equivalent channel (e.g. injected into
`plugin_config` itself) is also acceptable. AC-013's assertion is BEHAVIORAL (observed kill
time), not mechanism-specific — do not write the test to assert a specific field name or code
path.

**POSITIVE control (the wiring fix's target behavior):** a `hooks-registry.toml`-equivalent
test entry hosted by `legacy-bash-adapter.wasm` with `timeout_ms = 2_000` (deliberately
short), invoked against a bash script that sleeps 10 s. The subprocess MUST be observed killed
within the ONE-SIDED bound `2_000 <= observed_kill_time_ms <= 2_000 + margin_ms`
(F-S2121-P1-003; a CI-jitter-robust `margin_ms` in the hundreds of milliseconds) — well under
the hardcoded 60 s — proving the wiring fix reads and applies the registry's calibrated value
rather than the hardcoded constant. The dispatcher MUST report `PluginResult::Ok { exit_code: 1, .. }`
(via `adapter_logic`'s `HookResult::Error` mapping) for that invocation, **NEVER**
`PluginResult::Timeout { cause: TimeoutCause::Epoch }` — that variant is constructed only by
`classify_trap` on a genuine guest `Trap::Interrupt` and cannot fire while the guest is blocked
inside the synchronous `exec_subprocess` host call (BC-1.03.017 v1.22 PC12 correction, F-002).
PC12 owns the timing/wiring guarantee asserted here — that the kill fires at the registry's
calibrated `timeout_ms`, not the hardcoded constant; PC13 (this story's AC-013b below) owns the
complementary, non-redundant guarantee that for `on_error = Block` entries this resulting
`Ok { exit_code != 0 }` outcome is treated as a fail-closed block regardless of
`failure_policy`.

**NEGATIVE reference (the AMD-002 defect state — documented as the current/pre-fix baseline,
not a standing test that must continue passing):** under the CURRENT (pre-wiring-fix)
implementation, the same `timeout_ms = 2_000` entry invoked against the same 10 s-sleeping
script is NOT killed until ≈60 s, because `run_bash_via_host` passes its own hardcoded
`BASH_TIMEOUT_MS` constant to the host call, never the registry's `timeout_ms`. The wiring
fix's test MUST assert the POSITIVE behavior, and MUST fail against the pre-fix code path
(red-first against the current implementation, green only after this task's wiring fix
lands) — this is the Envoy #38801 discipline (Invariant 6) applied to the wiring fix itself:
the test drives the actual dispatch/subprocess path with a real short `timeout_ms` and a real
long-running script, not merely a unit-level assertion that adapter code reads a config
field.

**EC-011 (highest-risk defect window, CORRECTED per F-002):** a script running LONGER than the
calibrated `timeout_ms` but SHORTER than the hardcoded 60,000 ms constant (e.g.
`timeout_ms=30_000`, script runs 45 s) is, pre-fix, NOT killed at 30 s — but the outcome is
NONDETERMINISTIC, not a deterministic clean pass. Because `run_bash_via_host` passes the
hardcoded `BASH_TIMEOUT_MS=60_000` constant regardless of the registry's calibrated
`timeout_ms=30_000`, the dispatch surfaces as EITHER (a) `PluginResult::Ok { exit_code: 1, .. }`
via `adapter_logic`'s `HookResult::Error` mapping (if the bash script, `exec_subprocess`, or
the adapter's own marshaling encounters any error condition), OR (b) a guest-epoch
`PluginResult::Timeout { cause: TimeoutCause::Epoch }` race on control-return (if wasmtime's
independent `EPOCH_TICK_MS` guest-interruption ticker fires against the adapter's own WASM
execution once control returns from the blocking host call). Sub-case (a) `Ok{exit_code:1}` has no fail-closed enforcement absent PC13 under
PC1-PC12's pre-§AMD-003 rules. Sub-case (b) `Timeout{cause: Epoch}` IS blocked today by the
existing 2-arg `plugin_fail_closed(result, on_error)` call (which governs `Crashed | Timeout`
for `on_error=Block`); Task 5a's ADDITIVE wiring RETAINS this 2-arg call, so `Timeout +
on_error=Block` blocking is PRESERVED post-Task-5a (BC-1.03.017 v1.22 Invariant 12 guarantee;
F-S2121-P3-002 correction). AC-013's test suite MUST include a case covering this
window (asserting the pre-fix nondeterministic-defect behavior as documentation, and the
post-fix kill-at-≈timeout_ms behavior — which blocks every sub-case, but by DIFFERENT
governing axes per outcome: the `Ok{exit_code != 0}` sub-case via this story's own AC-013b/PC13
under `on_error=Block` (`failure_policy`-independent); the `Timeout` sub-case via S-21.19's
AC-011(b)/PC1/PC6/PC10(b) under `failure_policy=FailClosed` (`on_error`-independent for the
exhaustion decision, assuming the plugin's steady-state `failure_policy=FailClosed`
annotation per PC9/AC-008 has landed via S-21.24) — NOT by `on_error=Block` alone in either
case — as the required green state) — see Edge Cases below.

- **Test:** `test_legacy_bash_adapter_honors_registry_timeout_ms()` — Cargo integration test
  driving the real dispatcher/`invoke_plugin` path against the actual
  `hook-plugins/legacy-bash-adapter.wasm` binary with a synthetic test registry entry
  (`timeout_ms = 2_000`) and a bash fixture script that sleeps 10 s; asserts observed kill
  time satisfies the ONE-SIDED bound `2_000 <= observed_kill_time_ms <= 2_000 + margin_ms`
  (F-S2121-P1-003; a CI-jitter-robust `margin_ms` in the hundreds of milliseconds — NOT a
  symmetric `±5 ms` tolerance, which is too tight for CI-runner scheduling jitter and wrongly
  implies the kill could fire before `timeout_ms`, contradicted by BC-1.03.017 v1.22 PC12's own
  corrected one-sided, hundreds-of-ms-observed-margin wording, F-S2121-P2-004) and
  `PluginResult::Ok { exit_code: 1, .. }`
  reported (via `HookResult::Error`) — NEVER `Timeout{cause: Epoch}` (guest-trap-only variant,
  cannot fire inside the blocking `exec_subprocess` host call); MUST be written generically
  (parameterizable `timeout_ms`, not hardcoded to the five targeted plugins); documents the
  pre-fix NEGATIVE reference (≈60_000 ms kill regardless of `timeout_ms`) as a code comment or
  a `#[ignore]`d historical-baseline test, not a standing assertion; includes an EC-011 case
  (`timeout_ms=30_000`, script runs 45 s → pre-fix: NONDETERMINISTIC — `Ok{exit_code:1}` OR a
  guest-epoch `Timeout{Epoch}` race, never a deterministic clean pass / post-fix: killed ≈30 s)

### AC-013b (end-to-end leg): PC13 enforcement — `on_error = Block` fails closed on ANY plugin-reported error exit, not only a crash/timeout outcome (traces to BC-1.03.017 v1.22 PC13; ADR-039 §AMD-003, RATIFIED v1.11; ADR-044 v1.3; closes S-21.11 v2.0 adversarial pass-1 BLOCKER F-S2111V2-P1-001)

**Leg-scope note (per `.factory/planning/S-21.11-decomposition-plan.md` §3, corrected per
ADR-044 v1.3 / F-S2121-P1-002, F-S2121-P3-001):** AC-013b has two legs. The UNIT-level leg (authoring
the standalone `plugin_fail_closed_on_error_exit(result, on_error)` function) is owned by
**S-21.19**, calibration-axis-agnostic, and MAY land before or concurrently with this story's
AC-013 wiring fix. This story owns the END-TO-END fixture leg, which MUST land AFTER ALL of the
following are GREEN: (1) this story's own AC-013 wiring fix (a real `timeout_ms` overrun must
actually be enforced before its downstream block decision can be exercised realistically); (2)
S-21.19's unit predicate (`plugin_fail_closed_on_error_exit`) has merged; AND (3) **this story's
own NEW Task 5a**, which ADDITIVELY wires `plugin_fail_closed_on_error_exit` (alongside the
retained 2-arg `plugin_fail_closed` call) into the real `execute_tiers`/`execute_tier`
block-decision loop at wave 7. **Corrected prerequisite (F-S2121-P1-002, HIGH):** the prior
framing cited "S-21.19's unit predicate has landed" as the wiring-side prerequisite — WRONG, a
merged-but-unwired predicate cannot make a real dispatch block. Per ADR-044 v1.3 ADDITIVE-then-MIGRATE
wiring sequence, the wave-7 ADDITIVE wiring of this leg is owned by THIS story (S-21.21), NOT
deferred to S-21.24 as the pre-v1.3 single-function framing implied — Task 5a below is the
actual prerequisite this end-to-end test depends on.

For any plugin dispatched with `on_error = OnError::Block`, if the plugin's outcome is
`PluginResult::Ok { exit_code, .. }` where `exit_code != 0`, the executor's block-decision
function `plugin_fail_closed_on_error_exit` (S-21.19's standalone predicate, wired live by this
story's Task 5a) MUST return `true` (`block_intent = true`, dispatcher exit code 2), REGARDLESS
of `failure_policy`. This rule closes F-001 — a `legacy-bash-adapter.wasm`-hosted plugin's
bash-subprocess host-wall-clock timeout (this story's AC-013 `exec_subprocess.rs::run()`
poll-loop kill) does NOT produce `PluginResult::Timeout { .. }` — it propagates as
`PluginResult::Ok { exit_code: 1, .. }` (see AC-013's own body for the full propagation chain).

- **Test (end-to-end fixture, owned here, gated on this story's own AC-013 AND Task 5a):** a
  real dispatch driving `legacy-bash-adapter.wasm` against a fixture bash script that exceeds
  the registry's `timeout_ms` (post-AC-013 wiring AND post-Task-5a wiring) MUST produce
  `block_intent = true` / dispatcher exit code 2 for a gate registered `on_error = "block"` —
  this is the F-001 leg closing end-to-end, not just at the unit level (S-21.19 owns the
  unit-level leg); MUST land strictly after this story's own AC-013 tasks are GREEN, S-21.19's
  unit predicate has merged, AND this story's Task 5a wiring is GREEN.

### AC-013c: F-005 sibling coverage — a generic (non-timeout) `HookResult::Error` exit on an `on_error = Block` gate ALSO fails closed under the identical PC13 mechanism (traces to BC-1.03.017 v1.22 PC13(b); ADR-039 §AMD-003 F-005 in-scope ruling; ADR-044 v1.3)

ANY error path inside `adapter_logic` that returns `HookResult::Error` for a reason OTHER than
a host-wall-clock timeout (e.g. a missing `script_path`, or a bash exit code other than 0/2, or
`exec_subprocess` itself returning a non-timeout error) produces the identical
`PluginResult::Ok { exit_code: 1, .. }` shape as F-001's timeout leg, and the identical
fail-open gap applies absent this story's Task 5a wiring. F-S2111V2-P1-001-mechanism-adjudication
ruled F-005 IN SCOPE (not deferred to a follow-up story): both instances are closed by the
identical predicate S-21.19 authors and this story wires live at Task 5a, so this AC adds only
the sibling test coverage proving the general (not merely timeout-specific) case is closed — it
does not require any additional production code beyond S-21.19's decision-function extension and
this story's Task 5a wiring.

**Corrected prerequisite (F-S2121-P1-002, HIGH, same correction as AC-013b):** this test's
"MAY land independently" status refers only to independence from AC-013's `timeout_ms` wiring
path — it still requires S-21.19's unit predicate AND this story's Task 5a live wiring to be
GREEN for the assertion to be meaningful against a real dispatch.

- **Test:** a real dispatch driving `legacy-bash-adapter.wasm` with a fixture that produces a
  NON-timeout `HookResult::Error` (e.g. a plugin config with a missing/invalid `script_path`,
  or a bash fixture script that exits with a code other than 0 or 2) for a gate registered
  `on_error = "block"` MUST produce `block_intent = true` / dispatcher exit code 2 — proving
  S-21.19's decision-function extension, wired live by this story's Task 5a, closes the general
  `HookResult::Error` case, not only the timeout-specific F-001 instance; this test does NOT
  depend on this story's own AC-013 wiring fix (it does not exercise the `timeout_ms` path at
  all) but DOES depend on Task 5a (the error-exit leg's live wiring) and MAY land independently
  of AC-013's own tasks once Task 5a is GREEN.

## Behavioral Contracts

| BC ID | Version | Title | Clause |
|-------|---------|-------|--------|
| BC-1.03.017 | v1.22 | factory-dispatcher::executor::failure_policy enforcement — exhaustion-outcome dispatch (fail-closed→block; fail-open→advisory), on_error axes independence, crash-versus-exhaustion distinct paths, Phase-3-before-Phase-4 structural half-state gate, migration-window on_error=block completeness gate, legacy-bash-adapter runtime-timeout-wiring verification with CI-jitter-robust kill-time margin, observed_max×1.5 fuel_cap calibration-sufficiency gate against a FROZEN corpus snapshot, and wave-7→8 migration coverage-continuity invariant | Precondition 2–6 (AC-007 bash-adapter leg, including Precondition 6's sufficiency assertion), PC12 (AC-013), PC13(a) end-to-end leg + ADDITIVE live wiring (AC-013b, Task 5a) and PC13(b) (AC-013c), Invariant 12 (wave-7→8 Timeout+on_error=Block coverage continuity; Task 5a regression test) |
<!-- POLICY 7: BC-table Title cell MUST remain a verbatim SUBSET of the corresponding BC's H1. -->

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| AMD-002 host-to-guest `timeout_ms` channel | `crates/factory-dispatcher/src/executor.rs` (`execute_tier`, `spawn_async_plugin`) + `crates/hook-sdk/src/payload.rs` (`HookPayload`) | Effectful (host constructs per-plugin JSON envelope and writes to WASI stdin) |
| `legacy-bash-adapter::run_bash_via_host` (wiring fix) | `crates/hook-plugins/legacy-bash-adapter/src/lib.rs` | Effectful-shell (invokes `host::exec_subprocess` with the registry-derived `timeout_ms` instead of the hardcoded constant) |
| **NEW (ADR-044 v1.3, F-S2121-P1-002, F-S2121-P3-001):** ADDITIVE live wiring of `plugin_fail_closed_on_error_exit(result, on_error)` into the real block-decision loop (Task 5a) | `crates/factory-dispatcher/src/executor.rs` (`execute_tier`/`execute_tiers` block-decision call site) | Effectful (RETAINS the existing 2-arg `plugin_fail_closed(result, on_error)` call AND ADDS `plugin_fail_closed_on_error_exit` alongside it, OR-combined; the retained 2-arg call remains the sole live coverage for `Timeout + on_error=Block` during the wave-7→8 window; this is the enforcement-active ADDITIVE flip for the error-exit leg ONLY — the exhaustion leg's flip and the 2-arg removal both remain S-21.24's Task 0 per ADR-044 v1.3 ADDITIVE-then-MIGRATE sequence) |
| Bash-adapter calibration corpus measurement | devops-engineer tooling (external to crate) | Effectful-shell (runs the five plugins against corpus inputs; reads `fuel_consumed`/`time_consumed_ms`, post-wiring-fix) |
| `test_legacy_bash_adapter_honors_registry_timeout_ms` | `crates/factory-dispatcher/tests/legacy_bash_adapter_timeout_wiring.rs` (new) | Effectful-integration (drives real dispatch/subprocess path) |
| End-to-end AC-013b/AC-013c tests | `crates/factory-dispatcher/tests/legacy_bash_adapter_timeout_wiring.rs` (new) | Effectful-integration (real dispatch against `legacy-bash-adapter.wasm`) |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|----------------|
| Calibration corpus runner | Effectful-shell | Invokes plugins against filesystem inputs; reads fuel/wall-clock metrics from dispatcher |
| `legacy-bash-adapter::run_bash_via_host` | Effectful-shell | Already effectful (shells out); the fix changes WHICH `timeout_ms` value it passes, not its purity class |
| Task 5a wiring of `plugin_fail_closed_on_error_exit` into `execute_tiers`/`execute_tier` | Effectful | The call-site replacement is an executor-flow change (invokes a pure decision function from an effectful dispatch loop); the function itself remains pure-core (authored/classified in S-21.19) |
| `test_legacy_bash_adapter_honors_registry_timeout_ms` | Effectful-integration | Drives the real dispatcher/subprocess path; not a pure-core unit test |
| End-to-end AC-013b/AC-013c tests | Effectful-integration | Real dispatch against the actual `legacy-bash-adapter.wasm` binary |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-005 | `lessons.md` validator post-flip exhausts on a >4000-line `lessons.md` (above D-442(e) hard limit) | This MUST NOT happen if calibration was run against ≥4000-line corpus; if it does, the calibration was insufficient and AC-007 was not met — surfaced to orchestrator |
| EC-010 | `legacy-bash-adapter.wasm`-hosted plugin's script completes within the calibrated `timeout_ms` (no exhaustion) | Clean pass; AC-013/PC12 not exercised (no kill event) — same as EC-001 baseline (owned by S-21.19) |
| EC-011 | `legacy-bash-adapter.wasm`-hosted plugin's script runs LONGER than the calibrated `timeout_ms` but SHORTER than the hardcoded 60,000 ms constant (e.g. `timeout_ms=30_000`, script runs 45 s) — the highest-risk AMD-002/AMD-003 defect window | **Pre-wiring-fix (current implementation):** NOT killed at 30 s, but the outcome is NONDETERMINISTIC, not a deterministic clean pass — surfaces as EITHER (a) `PluginResult::Ok { exit_code: 1, .. }` via `HookResult::Error`, OR (b) a guest-epoch `Timeout{cause: Epoch}` race on control-return. **Baseline enforcement today (F-S2121-P3-002 correction):** sub-case (a) `Ok{exit_code:1}` has no fail-closed enforcement absent PC13; sub-case (b) `Timeout{Epoch}` IS blocked today by the existing 2-arg `plugin_fail_closed(result, on_error)` call (which governs `Crashed | Timeout` for `on_error=Block`) — Task 5a's ADDITIVE wiring RETAINS this 2-arg call, so `Timeout + on_error=Block` blocking is PRESERVED post-Task-5a (BC-1.03.017 v1.22 Invariant 12). **Post-wiring-fix + PC12 + PC13 (this story's AC-013 + AC-013b, gated on this story's own Task 5a ADDITIVE live-wiring of `plugin_fail_closed_on_error_exit`; assumes the plugin's steady-state annotation `failure_policy=FailClosed` per PC9/AC-008, delivered by S-21.24, in addition to its pre-existing `on_error=Block`):** PC12/AC-013 fixes the kill timing (killed at ≈30 s); whichever outcome results is blocked, but by DIFFERENT governing axes, NOT by `on_error=Block` alone: `PluginResult::Ok { exit_code: 1, .. }` (via `HookResult::Error`) is blocked by PC13/AC-013b under `on_error=Block` (`failure_policy`-independent); `PluginResult::Timeout { .. }` is blocked by PC1/PC6/PC10(b)/AC-011(b) (S-21.19) under `failure_policy=FailClosed` (`on_error`-independent for the exhaustion decision) — a `Timeout` does NOT block merely because `on_error=Block`; that requires `failure_policy=FailClosed`. The nondeterminism in WHICH outcome surfaces no longer has a safety impact under this `FailClosed` steady state (reached at S-21.24) — every sub-case now blocks, each via its own axis |

Edge cases EC-001–EC-004, EC-006–EC-009, EC-012–EC-014 are not cited by this story's owned
ACs; they are owned by S-21.19/S-21.22/S-21.23 respectively. The full 14-row table lives in
S-21.11 v2.11 (frozen historical source-of-truth).

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | 11,400 |
| ADR-039 (§AMD-001, §AMD-002 amendments; §Decision 4 calibration-formula subset) | 2,800 |
| ADR-044 (v1.3 — Task 5a's ADDITIVE live-wiring ownership rationale; ADDITIVE-then-MIGRATE wiring sequence) | 900 |
| Research file `wasm-fuel-exhaustion-detection.md` (calibration corpus methodology) | 2,500 |
| BC-1.03.017 v1.22 (Preconditions 2–6, PC12, PC13, Invariant 12, Architecture Anchors' two-function split) | 2,800 |
| `crates/factory-dispatcher/src/executor.rs` (`execute_tier`, `spawn_async_plugin` splice sites; Task 5a's block-decision call site is in the same file, already counted here) | 3,200 |
| `crates/hook-sdk/src/payload.rs` (guest-facing `HookPayload`, for the `timeout_ms` plumbing addition) | 1,200 |
| `crates/hook-plugins/legacy-bash-adapter/src/lib.rs` (`adapter_logic`, `run_bash_via_host`, `BASH_TIMEOUT_MS`) | 1,200 |
| `crates/factory-dispatcher/src/host/exec_subprocess.rs` (read-only reference — the actual kill-deadline enforcement point) | 900 |
| Calibration log artifact (devops-engineer output; both axes, five plugins, plus Precondition 6 sufficiency assertion results) | 1,900 |
| **Total** | **~28,800** |
| Agent context window | 200K (Sonnet) |
| **Budget usage** | **~14.4%** |

**Budget note:** well under the 20–30% ceiling. The wiring-fix phase (AC-013) and the
calibration phase (AC-007) can be delivered as sequential commits within this one story
(wiring first, calibration second, per the hard ordering constraint), each carrying a smaller
working set than the story-total figure above.

## Tasks (MANDATORY)

This story's tasks are the Node (B)+(C) subset of S-21.11 v2.11's original DAG, renumbered
self-contained #1–#12 (source: S-21.11 v2.11 Tasks #6, #7, #8, #9, #10, #10b, #10c, #11, #12,
#13, #14, #15), plus **NEW Task 5a** (ADR-044 v1.3 ADDITIVE-then-MIGRATE, F-S2121-P1-002, F-S2121-P3-001 — not part of the
original S-21.11 v2.11 task set; added this burst to wire the error-exit leg ADDITIVELY at wave 7).

### AMD-002 runtime wiring (implementer role)

1. [ ] Read ADR-039 §AMD-002 and BC-1.03.017 v1.22 PC12/Precondition 5 fully before writing
       any code. Read `crates/hook-plugins/legacy-bash-adapter/src/lib.rs::run_bash_via_host`
       (grep-confirmed: `BASH_TIMEOUT_MS` is defined and consumed there — no other call site).
2. [ ] Choose and implement a host-to-guest channel for the registry's resolved `timeout_ms`:
       add a field (or equivalent channel per the AC-013 implementer-design-choice note) to
       `crates/hook-sdk/src/payload.rs`'s `HookPayload`, and populate it at BOTH per-plugin
       payload construction sites in `crates/factory-dispatcher/src/executor.rs` — grep-confirmed
       exactly two `map.insert("plugin_config".to_string(), plugin_config)` call sites
       (`execute_tier` and `spawn_async_plugin`); the new value MUST be spliced in alongside
       `plugin_config` at BOTH sites (TD-VSDD-060 sibling-site sweep — missing one site leaves
       the async-group dispatch path unfixed).
3. [ ] Update `crates/hook-plugins/legacy-bash-adapter/src/lib.rs::run_bash_via_host` (and/or
       `adapter_logic`) to read the new host-supplied value and pass it to
       `host::exec_subprocess` instead of the hardcoded `BASH_TIMEOUT_MS` constant. Retain
       `BASH_TIMEOUT_MS` as a documented fallback default only for the case where the new
       field/channel is absent (e.g. a hand-constructed test payload) — do not silently drop
       the constant; re-purpose its doc comment to explain the fallback role and correct the
       stale claim that "the wasmtime epoch interrupt is the source of truth" (AMD-002 disproved
       this — the epoch deadline cannot preempt a blocking host call).
4. [ ] Write `test_legacy_bash_adapter_honors_registry_timeout_ms` (AC-013) as a RED-first
       Cargo integration test against the CURRENT pre-fix code — confirm it fails (subprocess
       killed at ≈60s, not ≈2s) before making any of Tasks 2–3's changes; only then implement
       the fix and confirm the test goes GREEN.
5. [ ] Confirm the fix is adapter-level, not per-plugin: the test MUST exercise a
       parameterizable `timeout_ms`, not a hardcoded value tied to one of the five named
       plugins (BC-1.03.017 v1.22 PC12 blast-radius note — the fix affects all 37
       `legacy-bash-adapter.wasm`-routed entries).
5a. [ ] **(NEW — ADR-044 v1.3, F-S2121-P2-001, F-S2121-P3-001, HIGH) Wire `plugin_fail_closed_on_error_exit(result, on_error)` ADDITIVELY into the real `execute_tiers`/`execute_tier` block-decision loop.** Once S-21.19's Task 6
       (authoring the standalone `plugin_fail_closed_on_error_exit` function) has landed,
       RETAIN the existing 2-arg `plugin_fail_closed(result, on_error)` call AND ADD a call to
       `plugin_fail_closed_on_error_exit(result, on_error)` alongside it (OR-combined) in
       `crates/factory-dispatcher/src/executor.rs`'s block-decision chain. **CRITICAL
       (F-S2121-P3-001, HIGH — wave-7→8 Timeout coverage continuity; BC-1.03.017 v1.22
       Invariant 12):** NEVER remove the retained 2-arg `plugin_fail_closed(result, on_error)`
       call in this commit. That 2-arg call currently governs `Crashed | Timeout` for
       `on_error=Block` and is the ONLY live disjunct that handles `Timeout + on_error=Block`
       during the wave-7→8 window. Removing it here would open a `Timeout + on_error=Block`
       fail-open window for the entire wave-7→8 period — a CWE-636 regression. The wave-7 step
       is ADDITIVE; S-21.24's Task 0 (the MIGRATE step) removes the 2-arg call AND wires
       `plugin_fail_closed_on_exhaustion` IN THE SAME COMMIT per ADR-044 v1.3's
       ADDITIVE-then-MIGRATE wiring sequence. **Function scope (F-S2121-P2-001 correction —
       `Crashed | Ok{exit_code!=0}`, NOT `Ok{exit_code!=0}` alone):** the newly wired
       `plugin_fail_closed_on_error_exit` function governs `on_error=Block && (Crashed |
       Ok{exit_code!=0})` — `Timeout` is UNCONDITIONALLY excluded from this function's scope
       (it remains governed by the retained 2-arg call during the wave-7→8 window, and by
       `plugin_fail_closed_on_exhaustion` via `.failure_policy` after S-21.24's MIGRATE step).
       This is the live-wiring commit for the error-exit leg ONLY — it is functionally and
       structurally independent of Task 5's AMD-002 `timeout_ms` wiring (this task does not
       touch `run_bash_via_host` or `exec_subprocess.rs`) and independent of the exhaustion
       leg's own wiring (`plugin_fail_closed_on_exhaustion`, which remains capstone-owned,
       deferred to S-21.24's Task 0 per ADR-044 v1.3's ADDITIVE-then-MIGRATE wiring sequence).
       This task does NOT trip PC11's detector (PC11 fires only on `.failure_policy`-referencing
       block-decision sites for `Timeout` outcomes; this call site references neither
       `.failure_policy` nor `Timeout`). Write/confirm
       `test_on_error_block_fails_closed_on_plugin_error_exit_code`'s assertions now also hold
       via a real dispatch (not only the synthetic unit-level `PluginOutcome` S-21.19
       constructs), AND add/confirm a real-dispatch crash-still-blocks regression check (a
       `PluginResult::Crashed` + `on_error=Block` fixture MUST still produce `block_intent =
       true` after this ADDITIVE wiring — proving the retained 2-arg call's Crashed arm
       remains live), AND add a regression test confirming `Timeout + on_error=Block` STILL
       blocks post-Task-5a (a `PluginResult::Timeout` + `on_error=Block` fixture MUST produce
       `block_intent = true` after the ADDITIVE wiring — proving the retained 2-arg
       `plugin_fail_closed` call continues to cover `Timeout` for `on_error=Block` plugins
       during the wave-7→8 window; this is the BC-1.03.017 v1.22 Invariant 12 sentinel test
       for the wave-7 window). MAY land in parallel with Tasks 1–5 (independent code paths)
       but MUST land before Task 6/Task 7 below, which depend on this wiring to exercise a
       real dispatch.
6. [ ] (AC-013b end-to-end leg; ADR-039 §AMD-003, BC-1.03.017 v1.22 PC13; ADR-044 v1.3) Once Tasks 4/5 are GREEN, S-21.19's unit predicate (its Task 6) has landed,
       AND this story's own Task 5a wiring is GREEN, write the AC-013b end-to-end fixture
       test: a real dispatch driving `legacy-bash-adapter.wasm` against a fixture bash script
       that exceeds the registry's `timeout_ms` MUST produce `block_intent = true` /
       dispatcher exit code 2 for a gate registered `on_error = "block"` — this closes F-001
       end-to-end, not just at the unit level. MUST land strictly after Tasks 4/5, S-21.19's
       Task 6, AND this story's own Task 5a.
7. [ ] (AC-013c F-005 sibling test; ADR-039 §AMD-003 F-005 in-scope ruling) Write a real
       dispatch test driving `legacy-bash-adapter.wasm` with a fixture producing a NON-timeout
       `HookResult::Error` (e.g. missing/invalid `script_path`, or a bash exit code other than
       0/2) on an `on_error = "block"` gate → asserts `block_intent = true` via the identical
       AC-013b/PC13 predicate extension (S-21.19's Task 6, wired live by this story's Task 5a)
       — no additional production code required; does not exercise the `timeout_ms` path
       (independent of Tasks 1–5) but DOES require Task 5a to be GREEN, and MAY land
       independently of Tasks 1–5 once Task 5a is GREEN.

### Per-plugin bash-adapter calibration (devops-engineer role; depends on wiring — do not run before Task 4 is GREEN)

8. [ ] Confirm Task 4 (`test_legacy_bash_adapter_honors_registry_timeout_ms`) is GREEN before
       starting this section. If bash-adapter calibration data was captured before Task 1–5's
       wiring fix landed, discard it and re-run — pre-wiring-fix measurements are not valid
       calibration evidence per BC-1.03.017 v1.22 Precondition 5.
9. [ ] For each of the five `legacy-bash-adapter.wasm`-hosted plugins
       (`validate-factory-path-root`, `validate-input-hash`, `validate-template-compliance`,
       `validate-wave-gate-prerequisite`, `validate-pr-merge-prerequisites`): run the plugin
       against the calibration corpus (§AC-007's corpus above) with a generously raised
       `timeout_ms` (so the run is not itself artificially truncated); record BOTH
       `fuel_consumed` (adapter marshaling step) AND `time_consumed_ms` (bash subprocess
       wall-clock, now genuinely enforced end-to-end per Tasks 1–5)
10. [ ] Compute observed-max `fuel_consumed` per plugin; set `fuel_cap = max(observed_max × 1.5, 50_000_000)`.
        **(F-S2121-P1-001 fold-in / BC-1.03.017 v1.22 Precondition 6, Q2/F-S2122-P1-002):**
        additionally run the machine-checkable sufficiency regression assertion per plugin — a
        re-run against the mandated calibration corpus asserting `fuel_consumed × 1.5 <=
        registry fuel_cap` — and record the PASS result in the calibration log; the 50M floor
        alone (PC8's structural gate) is NOT independently sufficient calibration evidence.
11. [ ] Compute observed-max `time_consumed_ms` per plugin; set
        `timeout_ms = max(measured_p99_ms × 2.0, 30_000)`. **(F-S2121-P1-001, calibration
        statistic-name fix)** — this task previously read `observed_max_ms`, which contradicted
        this story's own AC-007 body (`timeout_ms` set to `max(measured_p99_ms × 2.0, 30_000)`)
        and BC-1.03.017 v1.22 Precondition 3's unchanged `timeout_ms`-axis formula
        (F-S2121-P2-003: corrected from "PC3" — a Postcondition, not the Precondition where
        this formula actually lives); the `observed_max ×
        1.5` correction (ADR-039 §Decision 4 v1.16/§Erratum E-007) applies ONLY to the
        `fuel_cap` axis (Task 10 above), never to `timeout_ms`, because wall-clock duration is
        non-deterministic across CI runners/developer machines in a way fuel consumption is
        not — `measured_p99_ms`, not `observed_max_ms`, remains the correct, deliberately more
        conservative `timeout_ms` target. If the result exceeds 120_000 ms, surface to the
        orchestrator before recording (AC-007 note).
12. [ ] Document calibration results (plugin name, `fuel_consumed` p99/observed-max,
        `time_consumed_ms` p99, chosen `fuel_cap`, chosen `timeout_ms`, and the Precondition 6
        sufficiency-assertion PASS result per plugin) for all five plugins in the PR
        description. **This story does NOT annotate `hooks-registry.toml`** — S-21.24 applies
        these calibrated values to the registry as part of its gated flip (three non-Agent
        plugins directly; the two Agent-gate plugins additionally gated on S-21.23's
        break-glass code).

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-21.11 pre-split history (v1.0–v2.11) | The AMD-002 runtime-wiring gap was discovered by architect self-verification AFTER the original story's pass-13 fuel-vs-timeout bifurcation finding, via direct source-code verification of the actual call chain (`invoke.rs` → `exec_subprocess.rs` → `legacy-bash-adapter/src/lib.rs`), not generic wasmtime documentation review. | When a spec amendment cites a specific enforcement mechanism, verify it against the actual call chain in the repo, not just the referenced library's documentation — Task 1 repeats this discipline explicitly. | The `legacy-bash-adapter`'s own doc comment ("the wasmtime epoch interrupt is the source of truth") was a plausible-sounding but incorrect design assumption that survived in the codebase until AMD-002's direct verification — Task 3 requires correcting it, not just the code. |
| S-21.19 (W6; direct prerequisite for AC-013b's end-to-end leg only) | Delivers the STANDALONE `plugin_fail_closed_on_error_exit(result, on_error)` function's narrow PC13 predicate (`on_error == OnError::Block && matches!(result, PluginResult::Ok { exit_code } if exit_code != 0)`), authored as a unit-level test against synthetic outcomes, per ADR-044 v1.3 ADDITIVE-then-MIGRATE wiring sequence — a function structurally distinct from S-21.19's own Task 5 exhaustion function. | This story's own Task 5a WIRES that function live into the real block-decision chain at wave 7 (NOT deferred to S-21.24); AC-013b's end-to-end leg (Task 6) then proves the predicate closes the gap against a REAL dispatch, not just a synthetic one — do not re-derive the predicate here, and do not conflate this wiring with S-21.24's separate Task 0 (which wires the UNRELATED exhaustion function). | Task 4's wiring fix, Task 5a's error-exit wiring, and Task 6's end-to-end predicate test are three independent code changes gated in sequence only by test/dependency ordering (Task 4 GREEN for AC-013's own leg; S-21.19's predicate must exist AND this story's own Task 5a GREEN for AC-013b/AC-013c) — they do not share a commit. F-S2121-P1-002 (HIGH, this burst) corrected the prior framing that omitted Task 5a as a prerequisite. |
| S-21.07 (merged e94767bc) | Committed the ≥574 KB production-scale fixture used as this story's calibration corpus item 4. | The fixture path is stable: `plugins/vsdd-factory/tests/fixtures/validate-cross-site-correspondence/a1-production-scale/...` (originally built for native-WASM calibration, reused here for the bash-adapter corpus). | None specific to bash-adapter wiring. |
| S-21.09 (W4) | POLICY 21 — no new `.sh` files. | Calibration/test scripts must stay within `.rs`/`.bats` conventions. | None relevant to this seam's production code. |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| **AMD-002 wiring-precedes-calibration hazard (this story's own Task 8 gate):** `timeout_ms` calibration for the five bash-adapter plugins performed before the wiring fix lands is not valid calibration evidence and MUST be re-run | BC-1.03.017 v1.22 Precondition 5 | Task 8 explicit re-run gate; PR reviewer checks the wiring-fix commit predates or is atomic with the calibration-log timestamps |
| The wiring fix MUST be adapter-level (all 37 `legacy-bash-adapter.wasm`-routed entries), not hardcoded to the five named plugins | ADR-039 §AMD-002 blast-radius finding, v1.10 | Task 5; `test_legacy_bash_adapter_honors_registry_timeout_ms` parameterizable `timeout_ms` |
| `BASH_TIMEOUT_MS` retained as a documented fallback, not silently dropped | ADR-039 §AMD-002; TD-VSDD-059-adjacent (do not delete without an equivalent replacement path) | Task 3; PR reviewer checks the constant remains present with a corrected doc comment |
| This story does NOT annotate `hooks-registry.toml` with `failure_policy = "fail-closed"` — annotation is S-21.24's responsibility | Split scope boundary per `.factory/planning/S-21.11-decomposition-plan.md` §1 | Task 12 explicit note; PR reviewer verifies no `failure_policy` edits appear in this story's diff |
| **(NEW — ADR-044 v1.3, F-S2121-P2-001, F-S2121-P3-001, HIGH)** Task 5a's ADDITIVE live wiring of `plugin_fail_closed_on_error_exit` MUST NOT reference `.failure_policy` or consult `Timeout` outcomes AT ALL — `Timeout` during the wave-7→8 window is handled by the RETAINED 2-arg `plugin_fail_closed(result, on_error)` call (which Task 5a MUST NOT remove); `Timeout` in steady state (post-S-21.24) is governed exclusively by `plugin_fail_closed_on_exhaustion` via `.failure_policy`. The ADDITIVE wiring RETAINS the 2-arg call AND ADDS `plugin_fail_closed_on_error_exit` (OR-combined). Task 5a's diff MUST show BOTH functions present post-commit — NOT a replacement. The `plugin_fail_closed_on_error_exit` function governs `on_error=Block && (Crashed | Ok{exit_code!=0})` — `Crashed \| Ok{exit_code!=0}`, NOT `Ok{exit_code!=0}` in isolation | ADR-044 v1.3 ADDITIVE-then-MIGRATE wiring sequence (item 2, `Crashed \| Ok{exit_code!=0}`, `Timeout` unconditionally excluded from this function); BC-1.03.017 v1.22 Invariant 12 (migration coverage-continuity — at every commit from S-21.19 through S-21.24, some live disjunct covers `Timeout + on_error=Block`) | PR reviewer verifies Task 5a's call-site diff (a) shows BOTH `plugin_fail_closed` AND `plugin_fail_closed_on_error_exit` in the post-commit block-decision chain (ADDITIVE, not replacement), (b) references only `plugin_fail_closed_on_error_exit` for the error-exit-only path, never `.failure_policy`, and (c) includes the regression test confirming `Timeout + on_error=Block` still blocks post-Task-5a; AC-013b NEGATIVE control 3 (S-21.19) remains the mechanical guard against `Timeout` leaking into the error-exit function |
| **(NEW — F-S2121-P1-001)** `timeout_ms` calibration MUST use `measured_p99_ms`, NEVER `observed_max_ms` — the `observed_max × 1.5` correction is `fuel_cap`-axis-only | ADR-039 §Decision 4 v1.16/§Erratum E-007; BC-1.03.017 v1.22 Precondition 3 (unchanged `timeout_ms` axis) | Task 11 explicit statistic-name fix; PR reviewer checks the calibration log's `timeout_ms` derivation cites `measured_p99_ms`, not `observed_max_ms` |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| Rust toolchain (workspace pinned) | `rust-toolchain.toml` | Implementing the AMD-002 wiring fix and end-to-end tests |
| serde / toml (workspace) | workspace version | The new `HookPayload` field; parsing test registries |
| wasmtime 46.0.2 (pinned) | Cargo.lock (S-21.12 #781 merged) | `Store::set_epoch_deadline` and related WASM store APIs referenced by `exec_subprocess.rs`; implementer MUST confirm parity before relying on epoch/fuel API behavior across the 44→46 bump |

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| `crates/factory-dispatcher/src/executor.rs` | modify | Add the registry-`timeout_ms`-to-payload splice at BOTH `execute_tier` and `spawn_async_plugin` (Task 2); **NEW (Task 5a, ADR-044 v1.3 ADDITIVE-then-MIGRATE):** ADDITIVE wiring: RETAIN the existing 2-arg `plugin_fail_closed(result, on_error)` call AND ADD `plugin_fail_closed_on_error_exit(result, on_error)` alongside it (OR-combined). NEVER remove the 2-arg call in this story's commits — the wave-8 MIGRATE step (S-21.24 Task 0) owns its removal alongside wiring `plugin_fail_closed_on_exhaustion` in the same atomic commit (BC-1.03.017 v1.22 Invariant 12) |
| `crates/hook-sdk/src/payload.rs` | modify | Add a new field (or equivalent channel) to guest-facing `HookPayload` so `legacy-bash-adapter` can read the host's resolved `timeout_ms` (Task 2) |
| `crates/hook-plugins/legacy-bash-adapter/src/lib.rs` | modify | `run_bash_via_host` reads the registry-derived `timeout_ms` from the new channel instead of the hardcoded `BASH_TIMEOUT_MS` constant; retain `BASH_TIMEOUT_MS` as a documented fallback only; correct the stale "epoch interrupt is the source of truth" doc comment (Task 3) |
| `crates/factory-dispatcher/tests/legacy_bash_adapter_timeout_wiring.rs` (new) | create | `test_legacy_bash_adapter_honors_registry_timeout_ms` (AC-013), the AC-013b end-to-end fixture test, and the AC-013c F-005 sibling test |
| `crates/factory-dispatcher/src/registry.rs` | read-only | `RegistryEntry::timeout_ms(&defaults)` accessor delivered by S-21.10; no modifications unless a defect is found |
| `crates/factory-dispatcher/src/host/exec_subprocess.rs` | read-only | `run()`'s `Instant`-based poll+kill loop is the actual enforcement point AC-013 asserts against; no change required — the fix is in what value reaches this function's `timeout_ms` parameter, not in this function itself |
| `plugins/vsdd-factory/hooks-registry.toml` | read-only | This story records calibration evidence in the PR description; it does NOT annotate the registry — S-21.24 applies the values |

## Changelog

| Version | Date | Description |
|---------|------|--------------|
| 1.4 | 2026-08-23 | Wave-7 pass-3/R2 story-layer remediation (D-1074; story-writer). **F-S2121-P3-001 (HIGH) — Task 5a ADDITIVE wiring:** the wave-7 wiring is ADDITIVE, not a replacement — RETAIN the existing 2-arg `plugin_fail_closed(result, on_error)` call AND ADD `plugin_fail_closed_on_error_exit` alongside it (OR-combined); NEVER remove the 2-arg call (it is the sole live coverage for `Timeout + on_error=Block` during the wave-7→8 window until S-21.24's MIGRATE step). Task 5a body, Architecture Mapping, Architecture Compliance Rules, File Structure Requirements updated to reflect ADDITIVE language. Added regression test for `Timeout + on_error=Block` still blocks post-Task-5a. **F-S2121-P3-002 (MEDIUM) — EC-011 baseline correction:** sub-case (b) `Timeout{Epoch}` IS blocked today by the existing 2-arg `plugin_fail_closed` call for `on_error=Block` — the "no fail-closed enforcement in both sub-cases" framing was wrong; only sub-case (a) `Ok{exit_code:1}` lacks PC13 enforcement. EC-011 in Edge Cases table and AC-013 body both corrected. `BC-1.03.017 v1.21 → v1.22` re-anchored with full POLICY 8 propagation: frontmatter, BC table (version + title extended with two new v1.22 clauses), all AC headers (AC-007/AC-013/AC-013b/AC-013c), Token Budget, all Task cites, Architecture Compliance Rules. ADR-044 v1.1/v1.2→v1.3 cites updated at Task 5a header, Architecture Mapping, Architecture Compliance Rules, File Structure Requirements, Tasks section, AC-013b/AC-013c headers, Token Budget, PSI. points/depends_on/blocks/wave UNCHANGED (9 pts, wave 7). |
| 1.3 | 2026-08-22 | Wave-7 pass-2 remediation (D-1071; architect adjudication, RATIFIED 2026-08-22). **F-S2121-P2-001 (HIGH)** — corrected the Architecture Compliance Rules row's `Ok{exit_code!=0}` "ONLY" scope claim for Task 5a's wired `plugin_fail_closed_on_error_exit`, which silently dropped the pre-existing `Crashed` fail-closed guarantee; corrected to `Crashed \| Ok{exit_code!=0}` per ADR-044 v1.2's own correction, matching S-21.19's already-clean body; strengthened Task 5a's own text to require the `Crashed` arm be retained (not dropped) plus a real-dispatch crash-still-blocks regression check. **F-S2121-P2-002 (MEDIUM)** — fixed a dangling hyphenated `Task-6a` reference in AC-013b's test bullet (my round-1 rename missed this non-space-separated form) to `Task-5a`. **F-S2121-P2-003 (MEDIUM)** — `BC PC3` corrected to `BC Precondition 3` at Task 11's live body and (per explicit coordinator direction) annotated in-place at the v1.2 historical notes' own mis-citation. **F-S2121-P2-004 (MEDIUM)** — AC-013's "reconciled with PC12" claim, premature at v1.2 (BC v1.20's PC12 was not yet one-sided), corrected to cite the now-genuinely-matching BC-1.03.017 v1.22 PC12 (AT-OR-AFTER-`X` floor + CI-jitter-robust observed window, distinct from the internal ~5ms poll granularity). `BC-1.03.017 v1.20 -> v1.21` re-anchored with full POLICY 8 propagation: frontmatter, BC table, every citing AC, Token Budget, all Task cites. `points:`/`depends_on:`/`blocks:`/wave UNCHANGED (9 pts, wave 7). Does NOT touch S-21.19/S-21.20/S-21.22/S-21.23/S-21.24, BC-1.03.017/ADR-044 themselves (already landed at commit 8ef46b8a), or any INDEX/STATE.md (state-manager's domain). |
| 1.2 | 2026-08-22 | Wave-7 pass-1 remediation (D-1069; architect Q1/Q2 adjudication, RATIFIED 2026-08-22). **Q1/F-S2121-P1-002 (HIGH)** — new **Task 5a** (live-wiring `plugin_fail_closed_on_error_exit` into `execute_tiers`/`execute_tier` at wave 7, per ADR-044 v1.1 Addendum, Option A); AC-013b/AC-013c prerequisites corrected to cite Task 5a (not merely "S-21.19's unit predicate has landed"). **F-S2121-P1-001** — Task 11 `observed_max_ms` → `measured_p99_ms` fix (matches AC-007 body and unchanged BC Precondition 3 `timeout_ms` formula — [F-S2121-P2-003 correction: originally mis-cited as "PC3" here, a Postcondition, not the Precondition where the formula actually lives]); AC-007/Task 10 updated to cite BC-1.03.017 v1.20 Precondition 6's sufficiency regression assertion. **F-S2121-P1-003 (MEDIUM)** — AC-013/Task 6/Test-bullet kill-time tolerance restated as a ONE-SIDED bound (`X <= observed <= X + margin_ms`, CI-jitter-robust margin). `BC-1.03.017 v1.19 → v1.20` re-anchored with full POLICY 8 propagation. `inputs:` adds ADR-044. `points:`/`depends_on:`/`blocks:`/wave UNCHANGED (9 pts, wave 7) — ADR-044 v1.1 Addendum confirms zero DAG changes. Does NOT touch S-21.19 (concurrent story-writer sub-burst owns its own Task 6 narrowing), S-21.20/S-21.22/S-21.23/S-21.24, BC-1.03.017/ADR-044 themselves (already landed at commit a3bfa1af), or any INDEX/STATE.md (state-manager's domain). |
| 1.1 | 2026-08-21 | Wave-7 pre-cascade re-anchor burst (D-1060 deferral, EXTENDED D-1064): BC-1.03.017 v1.18 → v1.19 re-anchor swept across frontmatter `behavioral_contracts` and all body cites (AC-007/AC-013/AC-013b/AC-013c headers, body prose, BC table, Token Budget, Tasks, Architecture Compliance Rules). No AC text, BC trace, or test predicate changed. |
| 1.0 | 2026-08-20 | Created by story-writer via the operator-directed 6-way split of CONVERGED S-21.11 (v2.11, 16-pass 3-CLEAN BC-5.39.001), per `.factory/planning/S-21.11-decomposition-plan.md` §1/§3. Owns AC-007 (bash-adapter leg only), AC-013, AC-013b (end-to-end leg only), AC-013c, copied verbatim from S-21.11 v2.11 with zero weakening. Tasks renumbered #1–#12 (self-contained) from S-21.11 v2.11 Tasks #6, #7, #8, #9, #10, #10b, #10c, #11, #12, #13, #14, #15. Subsystems `[SS-01, SS-02, SS-04, SS-07]` per the decomposition plan's ADR-039 `subsystems_affected` sweep (plan §5) — this is the widest-blast-radius seam of the six. |
