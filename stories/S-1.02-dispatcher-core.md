---
document_type: story
level: ops
story_id: "S-1.02"
epic_id: "E-1"
version: "1.1"
status: merged
producer: story-writer
timestamp: 2026-04-25T00:00:00
phase: 1.8
inputs:
  - .factory/stories/v1.0/S-1.2-dispatcher-core.md
  - .factory/specs/architecture/ARCH-INDEX.md
input-hash: "cd689e2"
traces_to: .factory/specs/domain-spec/capabilities.md
cycle: v1.0.0-greenfield
points: "8"
depends_on: ["S-1.01"]
blocks: ["S-1.04", "S-1.05", "S-1.06", "S-1.07", "S-2.01", "S-2.03", "S-2.07"]
behavioral_contracts: []
verification_properties: []
priority: "P0"
wave: 2
target_module: crates/factory-dispatcher/src
subsystems: ["SS-01"]
estimated_days: 4
assumption_validations: []
risk_mitigations: []
---

# S-1.02: factory-dispatcher core (stdin, TOML load, routing)

**Epic:** E-1 — Dispatcher Foundation
**Phase:** Phase 1 — Dispatcher foundation
**Milestone:** `1.0.0-beta.1`
**Tier:** B.x
**Blocks:** S-1.04, S-1.05, S-1.06, S-1.07, S-2.01, S-2.03, S-2.07
**Blocked by:** S-1.01
**Estimated effort:** L

## Narrative
- **As a** Claude Code hook system
- **I want to** have a dispatcher that reads stdin JSON, loads the registry, and routes to the right plugins
- **So that** hook events are delivered to matching plugins in priority order

## Goal

Implement the core skeleton of `factory-dispatcher`: read stdin JSON from
Claude Code, parse it, load `hooks-registry.toml` (cached + mtime-invalidated),
filter registered plugins by event + tool match, group them by priority tier,
and hand off to the execution machinery. This story owns the dispatcher's
I/O boundary, config loading, and routing decisions.

## Acceptance criteria

- [ ] `factory-dispatcher` reads stdin JSON and parses Claude Code hook payload
- [ ] `dispatcher_trace_id` generated per invocation (UUID v4)
- [ ] `hooks-registry.toml` loaded via `toml` crate into strongly typed structs; schema_version field validated (must be 1)
- [ ] Plugin filter: selects entries where `event == payload.event_name` AND (no `tool` field OR tool regex matches `payload.tool_name`)
- [ ] Matched plugins grouped by `priority` field; tiers emit in ascending priority order
- [ ] Dispatcher exits 0 by default; exit 2 only when an `on_error = "block"` plugin returned a block result
- [ ] Structured error envelope on malformed stdin, missing registry, etc. — emit `internal.dispatcher_error` event and exit 0
- [ ] Covered by unit tests for every parsing/routing branch

## Tasks

1. Define Rust types: `HookPayload`, `RegistryEntry`, `Registry`.
2. Implement `load_registry(path: &Path) -> Result<Registry>` with TOML parsing + schema_version validation.
3. Implement `match_plugins(registry: &Registry, payload: &HookPayload) -> Vec<&RegistryEntry>`: filter by event and tool regex, drop `enabled == false`.
4. Implement `group_by_priority(matched: Vec<&RegistryEntry>) -> Vec<Vec<&RegistryEntry>>`: sorted tiers.
5. Main entry point: parse stdin, assign `dispatcher_trace_id`, load registry, match + group, call execution layer stub.
6. Error handling: any recoverable error emits `internal.dispatcher_error` via S-1.07 writer stub.
7. Tests: unit coverage for tool-regex matching, event filtering, priority grouping, malformed registry rejection.

## Artifacts

- `crates/factory-dispatcher/src/main.rs` — entry point + I/O
- `crates/factory-dispatcher/src/payload.rs` — HookPayload types
- `crates/factory-dispatcher/src/registry.rs` — TOML types + loader
- `crates/factory-dispatcher/src/routing.rs` — match + group logic
- `crates/factory-dispatcher/Cargo.toml` — dependencies added (serde, toml, uuid, regex, anyhow)
- `crates/factory-dispatcher/tests/` — integration tests with fixture registries

## Contracts

**`Registry` Rust type (consumed by S-1.04, S-1.05, S-1.06):**

```rust
pub struct RegistryEntry {
    pub name: String,
    pub event: String,
    pub tool: Option<String>,        // regex; None = all tools
    pub plugin: PathBuf,
    pub priority: u32,               // default 500
    pub enabled: bool,               // default true
    pub timeout_ms: Option<u32>,
    pub fuel_cap: Option<u64>,       // default 10_000_000
    pub on_error: Option<OnError>,
    pub capabilities: Option<Capabilities>,
}
pub enum OnError { Continue, Block }
```

**Routing decision contract:** Execution layer receives `Vec<Vec<&RegistryEntry>>` (tiers ordered ascending by priority).

## Test plan

- Unit tests for registry parse/validate
- Unit tests for each filter/group permutation
- Integration test: feed synthetic stdin + registry, verify dispatcher emits right events
- Malformed-input tests: broken JSON, missing registry, schema_version=2

## Notes

Use `serde` with `deny_unknown_fields` on registry types so typos in TOML fail loudly.

`dispatcher_trace_id` is a UUID v4 string; propagated to every emitted event.

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| payload.rs | crates/factory-dispatcher/src/payload.rs | pure-core (deserialization) |
| registry.rs | crates/factory-dispatcher/src/registry.rs | effectful-shell (file I/O) |
| routing.rs | crates/factory-dispatcher/src/routing.rs | pure-core (filtering/grouping) |
| main.rs | crates/factory-dispatcher/src/main.rs | effectful-shell (stdin/stdout/exit) |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | Malformed stdin JSON | Emit `internal.dispatcher_error`, exit 0 |
| EC-002 | Missing `hooks-registry.toml` | Emit `internal.dispatcher_error`, exit 0 |
| EC-003 | Registry schema_version=2 | Hard fail with error message |
| EC-004 | Zero matching plugins | Exit 0, no-op |
| EC-005 | Plugin with `enabled = false` | Excluded from match results |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| routing.rs | pure-core | Pure function: in=registry+payload, out=grouped entries |
| payload.rs | pure-core | Pure deserialization |
| registry.rs | effectful-shell | Reads filesystem for TOML file |
| main.rs | effectful-shell | Reads stdin, writes stdout, calls exit() |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~1,500 |
| Registry TOML schema reference | ~500 |
| hooks-registry.toml fixture | ~800 |
| Unit test files | ~1,500 |
| Tool outputs overhead | ~500 |
| **Total** | **~4,800** |
| Agent context window | 200K for Sonnet |
| **Budget usage** | **~2.4%** |

## Tasks (MANDATORY)

1. [ ] Define `HookPayload`, `Registry`, `RegistryEntry`, `OnError` types
2. [ ] Implement `load_registry()` with schema_version validation
3. [ ] Implement `match_plugins()` with event + tool-regex filtering
4. [ ] Implement `group_by_priority()` returning sorted tiers
5. [ ] Write main entry point wiring stdin → registry → routing → execution stub
6. [ ] Write unit tests for all routing branches
7. [ ] Write integration test with fixture registry

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-1.01 | `resolver = "2"`, workspace.dependencies | Use `workspace = true` for all shared deps | `wasm32-wasip1` target must be added in CI |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| Use `deny_unknown_fields` on registry serde | BC-1.01 registry schema strictness | serde attribute |
| Exit 0 on recoverable errors (non-blocking events) | BC-1.08 architectural contract | Unit test for error paths |
| Pure routing logic in pure-core module | ARCH purity boundary map | Module-level isolation |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| serde + serde_json | workspace-pinned | Deserialization |
| toml | workspace-pinned | Registry TOML parsing |
| uuid | workspace-pinned | `dispatcher_trace_id` generation |
| regex | workspace-pinned | Tool name matching |
| anyhow | workspace-pinned | Error propagation |

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| crates/factory-dispatcher/src/payload.rs | create | HookPayload type |
| crates/factory-dispatcher/src/registry.rs | create | TOML types + loader |
| crates/factory-dispatcher/src/routing.rs | create | Filter and group logic |
| crates/factory-dispatcher/src/main.rs | modify | Wire stdin → routing → execution stub |
| crates/factory-dispatcher/Cargo.toml | modify | Add serde, toml, uuid, regex deps |
| crates/factory-dispatcher/tests/routing.rs | create | Unit tests |
