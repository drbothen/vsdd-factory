---
document_type: story
level: ops
story_id: "S-1.09"
epic_id: "E-1"
version: "1.1"
status: merged
producer: story-writer
timestamp: 2026-04-25T00:00:00
phase: 1.8
inputs:
  - .factory/stories/v1.0/S-1.9-sink-otel-grpc-driver.md
  - .factory/specs/architecture/ARCH-INDEX.md
input-hash: "9bb1ea0"
traces_to: .factory/specs/domain-spec/capabilities.md
cycle: v1.0.0-greenfield
points: "5"
depends_on: ["S-1.01", "S-1.08"]
blocks: ["S-2.07", "S-4.07"]
behavioral_contracts: []
verification_properties: []
priority: "P0"
wave: 5
target_module: crates/sink-otel-grpc
subsystems: ["SS-03"]
estimated_days: 3
assumption_validations: []
risk_mitigations: []
---

# S-1.09: sink-otel-grpc driver

**Epic:** E-1 — Dispatcher Foundation
**Phase:** Phase 1 — Dispatcher foundation (feeds into Phase 4 sink system)
**Milestone:** `1.0.0-beta.1`
**Tier:** B.x
**Blocks:** S-2.07, S-4.07
**Blocked by:** S-1.01, S-1.08 (needs the `Sink` trait)
**Estimated effort:** M

## Narrative
- **As a** factory operator running Grafana/Loki/Prometheus
- **I want to** have events forwarded to an OTel collector over OTLP/gRPC
- **So that** the existing local observability stack receives vsdd-factory telemetry without any code changes

## Goal

Implement the `otel-grpc` sink type — forwards events to an OTel collector
over OTLP/gRPC. This is the second sink type in beta.1 scope, feeding the
existing local Grafana/Loki/Prometheus stack directly.

## Acceptance criteria

- [ ] `crates/sink-otel-grpc` implements the `Sink` trait
- [ ] Events batched and exported via OTLP/gRPC to the configured endpoint
- [ ] Configurable endpoint (`OTLP_ENDPOINT` or registry config field)
- [ ] Configurable batch size and flush interval
- [ ] Handles gRPC errors: retries on transient errors (5xx, deadline exceeded)
- [ ] `flush()` exports the current batch and waits for ack
- [ ] Integration test with a mock OTLP receiver verifies batch delivery
- [ ] `submit()` non-blocking; batching happens on dedicated background thread

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| sink-otel-grpc/src/lib.rs | crates/sink-otel-grpc/ | effectful-shell (gRPC I/O) |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | Collector unreachable at startup | Warn and continue; retry on each batch |
| EC-002 | gRPC deadline exceeded | Retry up to N times (from S-4.04 resilience layer) |
| EC-003 | Batch size exceeds gRPC limit | Split into sub-batches |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| sink-otel-grpc/src/lib.rs | effectful-shell | Network I/O over gRPC |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~1,000 |
| sink-otel-grpc/src/lib.rs | ~1,000 |
| mock OTLP receiver test | ~800 |
| Tool outputs overhead | ~400 |
| **Total** | **~3,200** |
| Agent context window | 200K for Sonnet |
| **Budget usage** | **~1.6%** |

## Tasks (MANDATORY)

1. [ ] Create `crates/sink-otel-grpc` crate implementing `Sink` trait
2. [ ] Implement OTLP/gRPC export with tonic/prost
3. [ ] Implement configurable batch size and flush interval
4. [ ] Implement transient error retry logic
5. [ ] Write integration test with mock OTLP receiver

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-1.08 | `Sink` trait interface, dedicated writer thread pattern | Re-use same non-blocking submit() pattern | OTel gRPC needs tonic + prost generated code |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| `submit()` must be non-blocking | BC-3.03 OTLP batching | Test: submit does not block |
| Batch exported at configured interval or when full | BC-3.03 batching lifecycle | Integration test with flush |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| tonic | workspace-pinned | gRPC client |
| prost | workspace-pinned | Protobuf code generation |
| opentelemetry-proto | workspace-pinned | OTLP protobuf definitions |

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| crates/sink-otel-grpc/Cargo.toml | create | OTel gRPC sink crate |
| crates/sink-otel-grpc/src/lib.rs | create | Sink implementation |
| crates/sink-otel-grpc/src/exporter.rs | create | OTLP export logic |
| crates/sink-otel-grpc/tests/integration.rs | create | Mock receiver test |
