---
document_type: story
level: implementation
story_id: S-18.13
epic_id: "E-18"
version: "1.6"
title: "wave-handoff skill writes HANDOFF.md via the Write tool so the PostToolUse completeness gate fires (gate-trigger fix)"
status: ready
producer: story-writer
timestamp: "2026-06-19T00:00:00Z"
last_amended: "2026-06-19 (v1.6) — EPIC-COMPLETE --commit carve-out (story-writer): (F-SP13-P5-001) propagate architect ADR-026 §Decision 8 (v1.24) + PO BC-5.41.001 (v1.25 PC10 step 4 + EC-017) + BC-5.41.002 (v1.18 PC6 note) EPIC-COMPLETE carve-out into S-18.13. §Scope --commit bullet rewritten to two-arm conditional: HAS-NEXT-WAVE verifies BOTH HANDOFF.md + wave-state.yaml, hard-abort if EITHER absent; EPIC-COMPLETE (epic_status:complete / next_wave_stories:[]) verifies HANDOFF.md alone, stages HANDOFF.md alone (removes stale wave-state.yaml), hard-abort ONLY on missing HANDOFF.md. T-2a note updated to reflect two-arm presence guard. AC-005 clarified: asserts ONE atomic commit on BOTH arms — two files on HAS-NEXT-WAVE, HANDOFF.md alone on EPIC-COMPLETE — so the test does NOT falsely flag a legitimate EPIC-COMPLETE single-file commit. AC-006 added: EPIC-COMPLETE --commit arm succeeds with HANDOFF.md alone, no wave-state.yaml, single commit, no abort; traces BC-5.41.001 PC10 (EPIC-COMPLETE arm) + BC-5.41.002 PC6. EC-017 updated to path-conditional form: HandoffFileAbsent fires on missing HANDOFF.md on BOTH paths; wave-state.yaml absence is NOT an error on EPIC-COMPLETE. BC cites bumped: BC-5.41.001 v1.24→v1.25; BC-5.41.002 v1.17→v1.18; ADR-026 informational cite v1.23→v1.24 (body BC table + Token Budget + §Spec Amendments + §File Structure Requirements). §Forbidden Dependencies --commit language updated to path-conditional form. POLICY 14 five-leg parity: version 1.5→1.6; last_amended prefix updated; modified[] entry prepended; body Changelog row added. [Prior: 2026-06-19 (v1.5) — architect redesign: restructure wave-handoff from monolithic bash run to agent-orchestrated subcommand model (story-writer): §Scope rewritten — the skill is NOT agent-orchestrated in v1.4's sense (SKILL.md numbered-step body with Write in allowed-tools); it is a MONOLITHIC bash run ending in `main \"$@\"` with no agent seam for a Write tool call between Step 4 (write_handoff) and Step 6 (commit_to_artifacts). The proper fix restructures the skill across FOUR files + 1 new fixture dir: (1) wave-handoff.sh split into subcommands --emit-handoff (payload to stdout, no disk write, no commit), --emit-wave-state (bash writes wave-state.yaml, skipped on EPIC-COMPLETE), --commit (stages HANDOFF.md + wave-state.yaml, ONE atomic git commit, verifies BOTH files present before staging); (2) lib/write-handoff.sh: write_handoff() changed to emit to stdout (remove `} > \"$output_path\"`); (3) SKILL.md rewritten from descriptive overview to genuine numbered agent steps S1–S5; (4) tests/fixtures/wave-handoff-golden/ created (T-4a golden capture BEFORE T-2). ACs updated: AC-001 expanded (two-file oracle: absence of `} > \"$output_path\"` + presence of --emit-handoff arm); AC-002 requires production-grade PostToolUse gate-firing test (dispatcher-integration or mock PostToolUse injection); AC-005 added (single atomic --commit contains BOTH files; traces BC-5.41.002 PC6); EC-017 added (--commit but HANDOFF.md absent → HandoffFileAbsent hard error). behavioral_contracts: BC-5.41.002 added (BC array POLICY 8 propagation: body BC table + ACs + Token Budget count updated). points 5→10; effort_class single-day→multi-day; estimated_days 1→2. ADR-026 cite bumped: §Decision 8 now v1.23. F-SP13-P3-001 + F-SP13-P3-002 findings resolved. POLICY 14 five-leg parity: version 1.4→1.5; last_amended prefix updated; modified[] entry prepended; body Changelog row added. [Prior: 2026-06-19 (v1.4) — F-SP13-P2-001 + F-SP13-P2-002 precision fixes (story-writer): (1) §Scope + T-2 corrected from 'one-line change in write-handoff.sh' to accurate two-file mechanism: write-handoff.sh write_handoff() changed to emit assembled HANDOFF.md payload to stdout (no bash redirect); SKILL.md gains a numbered agent step instructing Write-tool invocation by the agent — PC10 is implementable as confirmed by architect (AGENT-ORCHESTRATED skill, allowed-tools includes Write); (2) AC-003 oracle re-anchored from 'previous bash-redirect implementation' (which is deleted by this story and unavailable at test time) to a frozen golden fixture committed under plugins/vsdd-factory/tests/fixtures/wave-handoff-golden/; T-4 gains a sub-task capturing the golden fixture BEFORE T-2 implementation; File Structure Requirements updated: SKILL.md added as MODIFY target. Version 1.3→1.4. [Prior: 2026-06-19 (v1.3) — spec-first propagation (story-writer): BC-5.41.001 v1.22→v1.23; PC10 (write-path enablement) added to clauses-exercised column; AC-001 re-anchored to BC-5.41.001 PC10; AC-002 re-anchored to BC-5.41.001 PC10; AC-003 re-anchored to BC-5.41.001 PC10 byte-identity verification; AC-004 traces to ADR-026 §Decision 8 (unchanged anchor); EC-016 (Write tool unavailable → HandoffWriteToolUnavailable) added to Edge Cases table; 'SPEC AMENDMENT NEEDED' section rewritten — amendments are now DONE (BC v1.23 PC10 + ADR §Decision 8 v1.22 both in place); status draft→ready (spec gate satisfied); version 1.2→1.3. [Prior: 2026-06-19 (v1.2) — C-P5-001 fix (LOCAL pass-5 consistency review): added missing version: field (frontmatter POLICY 14 leg-1 was broken — field was present but the five-leg bump to v1.2 is applied here). C-P5-002 fix: corrected non-canonical subsystem labels in Architecture Mapping prose — SS-05 parenthetical corrected from 'Wave Boundary Orchestration' to 'Pipeline Orchestration'; SS-06 parenthetical corrected from 'Skill Implementations'→'Skill Catalog' (ARCH-INDEX Subsystem Registry canonical names per POLICY 6). Version 1.1→1.2. [Prior: 2026-06-19 (v1.1) — C-P4-001 fix (LOCAL pass-4 consistency review): BC table Title column corrected — replaced enriched title text with a strict subset of BC-5.41.001 H1; italic annotation updated to accurately reflect the subset relationship. Version 1.0→1.1. [Prior: 2026-06-19 (v1.0) — initial draft authored by story-writer; anchors finding F-S1802-02 from S-18.02 LOCAL pass-3 adversary (validate-wave-handoff-completeness gate is functionally inert in production because the wave-handoff skill writes HANDOFF.md via bash redirection, emitting no PostToolUse event). Architect determination: Option A — replace bash-redirect write with Write-tool-mediated write + amend ADR-026 §Decision 8.]]]]"
phase: "F3"
cycle: v1.0-feature-context-durability-E18
inputs:
  - .factory/specs/behavioral-contracts/ss-05/BC-5.41.001.md
  - .factory/specs/behavioral-contracts/ss-05/BC-5.41.002.md
  - .factory/specs/architecture/decisions/ADR-026-wave-boundary-checkpoint-reset-and-lossless-intra-wave-compaction.md
  - plugins/vsdd-factory/skills/wave-handoff/lib/write-handoff.sh
  - .factory/stories/S-18.02-validate-wave-handoff-completeness-wasm.md
input-hash: "7d6acdc"
traces_to:
  - CAP-032
  - .factory/stories/epics/E-18-factory-context-durability.md
wave: 4
priority: P0
points: 10
track: feature
effort_class: multi-day
depends_on:
  - S-18.01
  - S-18.02
blocks: []
behavioral_contracts:
  - BC-5.41.001
  - BC-5.41.002
verification_properties: []
domain_invariants: []
tdd_mode: strict
target_module: plugins/vsdd-factory/skills/wave-handoff
subsystems:
  - SS-05
  - SS-06
estimated_days: 2
assumption_validations: []
risk_mitigations: []
anchored_adrs:
  - ADR-026
closes:
  - "F-S1802-02 (S-18.02 LOCAL pass-3 adversary finding: validate-wave-handoff-completeness gate is functionally inert because wave-handoff writes HANDOFF.md via bash redirection, not via the Write tool, emitting no PostToolUse event)"
feature: "issue #173"
modified:
  - "2026-06-19 (v1.6) — EPIC-COMPLETE --commit carve-out (story-writer): (F-SP13-P5-001) §Scope --commit bullet rewritten to two-arm conditional (HAS-NEXT-WAVE: verify BOTH + stage both; EPIC-COMPLETE: verify HANDOFF.md only + stage HANDOFF.md alone + remove stale wave-state.yaml). T-2a note updated. AC-005 clarified: asserts ONE atomic commit on BOTH arms (two-file HAS-NEXT-WAVE; single-file EPIC-COMPLETE). AC-006 added: EPIC-COMPLETE --commit succeeds with HANDOFF.md alone, no abort, single commit; traces BC-5.41.001 PC10 (EPIC-COMPLETE arm) + BC-5.41.002 PC6. EC-017 updated to path-conditional form (HandoffFileAbsent fires on missing HANDOFF.md on both paths; wave-state.yaml absence NOT an error on EPIC-COMPLETE). BC cites updated: BC-5.41.001 v1.24→v1.25; BC-5.41.002 v1.17→v1.18; ADR-026 informational cite v1.23→v1.24. §Forbidden Dependencies --commit language updated to path-conditional form. POLICY 14 five-leg parity advanced."
  - "2026-06-19 (v1.5) — architect redesign: restructure wave-handoff from monolithic bash run to agent-orchestrated subcommand model (story-writer). §Scope rewritten to reflect the proper fix: four-file restructure + 1 new fixture dir — wave-handoff.sh (--emit-handoff / --emit-wave-state / --commit subcommands), lib/write-handoff.sh (stdout-emit), SKILL.md (genuine S1–S5 agent steps), tests/fixtures/wave-handoff-golden/ (golden capture T-4a). Tasks replaced per architect: T-1 → T-4a → T-2a → T-2b → T-2c → T-3 → T-4 → T-5 → T-6. AC-001 expanded (two-file oracle); AC-002 requires production-grade PostToolUse test; AC-004 ADR cite updated §Decision 8 v1.23; AC-005 added (atomicity, BC-5.41.002 PC6); EC-017 added (HandoffFileAbsent). BC-5.41.002 added to behavioral_contracts (POLICY 8: body BC table row + AC-005 trace + Token Budget count). points 5→10; effort_class single-day→multi-day; estimated_days 1→2. POLICY 14 five-leg parity advanced."
  - "2026-06-19 (v1.4) — F-SP13-P2-001 + F-SP13-P2-002 precision fixes (story-writer): §Scope corrected from 'ONE file ... one-line change' to accurate two-file mechanism (write-handoff.sh stdout-emit + SKILL.md Write-tool agent step); T-2 rewritten to match confirmed AGENT-ORCHESTRATED mechanism; AC-003 oracle re-anchored from deleted bash-redirect path to frozen golden fixture committed to plugins/vsdd-factory/tests/fixtures/wave-handoff-golden/; T-4 gains golden-fixture capture sub-task (must precede T-2); File Structure Requirements: SKILL.md added as MODIFY target. POLICY 14 five-leg parity advanced."
  - "2026-06-19 (v1.3) — spec-first propagation (story-writer): BC-5.41.001 cite bumped v1.22→v1.23; PC10 added to clauses-exercised; AC-001/AC-002/AC-003 re-anchored to BC-5.41.001 PC10; EC-016 added; SPEC AMENDMENT NEEDED section rewritten (amendments complete); status draft→ready; POLICY 14 five-leg parity advanced."
  - "2026-06-19 (v1.2) — C-P5-001 + C-P5-002 fix (LOCAL pass-5 consistency review): version: field bump to 1.2 (five-leg POLICY 14 parity); subsystem prose labels corrected to ARCH-INDEX canonical names: SS-05 'Wave Boundary Orchestration'→'Pipeline Orchestration'; SS-06 'Skill Implementations'→'Skill Catalog'. POLICY 6 + POLICY 14 compliance restored."
  - "2026-06-19 (v1.1) — C-P4-001 fix (LOCAL pass-4 consistency review): BC table Title column corrected — replaced enriched title with strict BC-5.41.001 H1 subset; italic annotation updated. POLICY 7 compliance restored."
---

# S-18.13 — wave-handoff skill writes HANDOFF.md via the Write tool so the PostToolUse completeness gate fires (gate-trigger fix)

**Epic:** E-18 — Factory Context Durability (feature #173)
**Wave:** 4 (depends on S-18.01 merged + S-18.02 WASM gate built; placed immediately after the gate is in place)
**Depends on:** S-18.01 (the skill being restructured), S-18.02 (the gate this fix makes effective)
**Blocks:** none
**Priority:** P0 — the validate-wave-handoff-completeness gate (S-18.02) is functionally inert in production until this story ships; without it, wave-handoff completeness is never validated at write-time

## Originating Finding

**Finding ID:** F-S1802-02
**Source:** S-18.02 LOCAL pass-3 adversary review
**Classification:** [process-gap] — ADR-026 §Decision 8 constrained the WASM gate's hook registration (PostToolUse on Write|Edit) but did NOT constrain the HANDOFF.md *producer's* write path. The wave-handoff skill (`plugins/vsdd-factory/skills/wave-handoff/lib/write-handoff.sh`) writes HANDOFF.md using bash redirection (`} > "$output_path"`), which emits NO PostToolUse event. The validate-wave-handoff-completeness gate therefore never fires on the production wave-close path — it is dead code in the current integration.

**Architect determination (v1.23 redesign):** The v1.22 fix was insufficient. The v1.22 normative rule stated `write-handoff.sh` MUST "route its HANDOFF.md output through the Write tool", which is IMPOSSIBLE for a monolithic bash run: `wave-handoff.sh` ends in `main "$@"` and has no agent seam between Step 4 (write_handoff call) and Step 6 (commit_to_artifacts). The correct fix is an **agent-orchestrated subcommand restructure** per ADR-026 §Decision 8 (v1.23).

**ADR gap:** ADR-026 §Decision 8 (v1.24) specifies the four-step agent-orchestrated flow (emit→Write→emit-wave-state→commit) with EPIC-COMPLETE carve-out on the `--commit` step. This story's scope implements that restructure.

## Spec Amendments: COMPLETE

Both spec amendments required for this story are in place. This story may proceed to TDD (status: ready).

1. **BC-5.41.001 v1.25** (product-owner, 2026-06-19): PC10 step 4 (`--commit` presence guard) and EC-017 amended to make the presence guard CONDITIONAL on wave path. HAS-NEXT-WAVE path: `--commit` verifies BOTH `HANDOFF.md` AND `wave-state.yaml` present; hard-abort if EITHER absent. EPIC-COMPLETE path (`epic_status: complete` / `next_wave_stories: []`): `--commit` verifies `HANDOFF.md` present and stages `HANDOFF.md` ALONE; removes any stale `wave-state.yaml`; hard-abort ONLY on missing `HANDOFF.md`. A single-file EPIC-COMPLETE commit is NOT a BC-5.41.002 PC6 violation. [Earlier at v1.24: PC10 rewritten to reflect architect-designed four-step agent-orchestrated flow; EC-017 added (`HandoffFileAbsent`); bash redirection forbidden.]

2. **BC-5.41.002 v1.18** (product-owner, 2026-06-19): PC6 atomicity note extended to clarify the EPIC-COMPLETE path: on EPIC-COMPLETE, `wave-state.yaml` is not produced; `--commit` creates a single atomic commit containing `HANDOFF.md` alone — this is NOT a PC6 violation. PC6 forbids splitting a wave-close into two separate commits; it does not require `wave-state.yaml` to exist when the epic is complete. Two-file atomicity applies only on the HAS-NEXT-WAVE path. [Earlier at v1.17: atomicity defined at git-commit boundary, not disk-write boundary.]

3. **ADR-026 §Decision 8 v1.24** (architect, 2026-06-19): EPIC-COMPLETE carve-out added to Step 5 (`--commit`) of the canonical SKILL.md step structure. HAS-NEXT-WAVE arm stages both files and hard-aborts if either is absent; EPIC-COMPLETE arm stages `HANDOFF.md` alone (removes stale `wave-state.yaml`) and hard-aborts only on missing `HANDOFF.md`. A single-file EPIC-COMPLETE commit remains ONE atomic commit and is NOT a BC-5.41.002 PC6 violation. [Earlier at v1.23: four-step agent-orchestrated emit→Write→commit control flow specified.]

The ACs below trace to BC-5.41.001 PC10 (four-step flow postcondition, EPIC-COMPLETE carve-out), BC-5.41.002 PC6 (atomicity postcondition, single-file EPIC-COMPLETE commit permitted), and ADR-026 §Decision 8 as established.

## Narrative

As a factory-dispatcher hook runtime,
I want the wave-handoff skill to be restructured into an agent-orchestrated subcommand model where HANDOFF.md is written by the agent via the Claude Code Write tool (not by bash redirection),
so that the PostToolUse `validate-wave-handoff-completeness` gate (BC-4.14.001, S-18.02) actually fires on every production wave-close path and malformed or incomplete HANDOFF.md files are caught at write-time.

## Scope

This story restructures the wave-handoff skill across **four files** and creates **one new fixture directory**. The current skill is a MONOLITHIC bash run (`wave-handoff.sh` ends in `main "$@"`) with no agent seam that permits a Write tool call between Step 4 (write_handoff) and Step 6 (commit_to_artifacts). The proper fix is to split the monolith into agent-orchestrated subcommands.

### Files and changes

1. **`plugins/vsdd-factory/skills/wave-handoff/wave-handoff.sh`** — Split `main()` into three bash subcommands dispatched via `$1`:
   - `--emit-handoff`: assembles the complete HANDOFF.md payload with full anti-fabrication cross-checks (all PC3 checks), emits the assembled YAML to stdout. NO file written to disk. NO git commit. Returns the payload for the agent to consume.
   - `--emit-wave-state`: writes wave-state.yaml to `${ARTIFACTS_WT}/wave-state.yaml` via bash; skipped on EPIC-COMPLETE (consistent with BC-5.41.002 PC3 EPIC-COMPLETE exception).
   - `--commit`: creates ONE atomic git commit via `commit_to_artifacts`. The presence guard is CONDITIONAL on wave path:
     - **HAS-NEXT-WAVE path** (non-final wave; `next_wave_stories` is non-empty): MUST verify BOTH `HANDOFF.md` AND `wave-state.yaml` are present on disk at `${ARTIFACTS_WT}` before staging. If EITHER is absent, MUST abort with a hard error (`HandoffFileAbsent` on missing `HANDOFF.md`; equivalent hard error on missing `wave-state.yaml`). Stages both files in ONE git commit.
     - **EPIC-COMPLETE path** (`epic_status: complete` / `next_wave_stories: []`): `wave-state.yaml` is intentionally not produced on this path. MUST verify `HANDOFF.md` is present on disk at `${ARTIFACTS_WT}/HANDOFF.md`; if absent, MUST abort with `HandoffFileAbsent`. MUST NOT check for `wave-state.yaml` presence. If a stale `wave-state.yaml` exists on disk (e.g., from a prior non-final wave invocation), MUST remove it before staging. Stages `HANDOFF.md` ALONE in ONE atomic git commit. A single-file commit on the EPIC-COMPLETE path is NOT a BC-5.41.002 PC6 violation.

2. **`plugins/vsdd-factory/skills/wave-handoff/lib/write-handoff.sh`** — Change `write_handoff()` to emit the assembled HANDOFF.md payload to stdout (remove `} > "$output_path"` bash-redirect construct). Update all callers of `write_handoff()` to capture stdout rather than relying on file side-effects.

3. **`plugins/vsdd-factory/skills/wave-handoff/SKILL.md`** — Rewrite from a descriptive "Behavior Overview" to GENUINE numbered agent steps that reflect the new subcommand model:
   - **S1 (Bash):** invoke `wave-handoff.sh --emit-handoff` to assemble and emit HANDOFF.md payload to stdout; capture the output.
   - **S2 (Agent Write tool):** write the captured payload to `${ARTIFACTS_WT}/HANDOFF.md` via the Claude Code Write tool. The PostToolUse `validate-wave-handoff-completeness` gate fires at this step.
   - **S3 (Agent verifies):** confirm the written content is byte-identical to the `--emit-handoff` payload before proceeding.
   - **S4 (Bash):** invoke `wave-handoff.sh --emit-wave-state` to write wave-state.yaml (skipped on EPIC-COMPLETE).
   - **S5 (Bash):** invoke `wave-handoff.sh --commit` to create the single atomic git commit. On HAS-NEXT-WAVE, stages both `HANDOFF.md` and `wave-state.yaml`. On EPIC-COMPLETE, stages `HANDOFF.md` alone (removes stale `wave-state.yaml` if present). Both arms produce ONE atomic git commit.
   This is a genuine agent-orchestration rewrite per ADR-026 §Decision 8 (v1.24) — the SKILL.md body MUST be numbered steps that drive the agent, not a description of what the bash script does internally.

4. **`plugins/vsdd-factory/tests/fixtures/wave-handoff-golden/`** (CREATE) — Frozen golden HANDOFF.md fixture(s) captured from the CURRENT bash-redirect path BEFORE the T-2 refactor (T-4a). Serves as the regression oracle for AC-003 byte-identity verification.

5. **`plugins/vsdd-factory/tests/wave-handoff.bats`** — Add:
   - AC-002 gate-fires test (PostToolUse event for the HANDOFF.md Write tool call)
   - AC-003 golden-byte-identity test
   - EC-017 HandoffFileAbsent test (`--commit` invoked but HANDOFF.md absent → hard error on BOTH paths)
   - AC-006 EPIC-COMPLETE `--commit` arm test: given `HANDOFF.md` present and `wave-state.yaml` absent, `--commit` exits 0 with a single-file commit — MUST NOT raise `HandoffFileAbsent` (verifies the EPIC-COMPLETE carve-out that closes F-SP13-P5-001)

Atomicity is preserved on both arms: on the HAS-NEXT-WAVE path, `--emit-wave-state` writes `wave-state.yaml` to disk via bash; `--commit` co-stages `HANDOFF.md` (already on disk from the Write tool step) and `wave-state.yaml` in ONE git commit. On the EPIC-COMPLETE path, `wave-state.yaml` is intentionally absent; `--commit` stages `HANDOFF.md` ALONE in ONE atomic git commit. The two files being written at different times (Write tool vs bash) does NOT violate BC-5.41.002 PC6 — atomicity is defined at the git-commit boundary per BC-5.41.002 PC6 v1.18. A single-file EPIC-COMPLETE commit is explicitly NOT a PC6 violation per BC-5.41.002 PC6 v1.18 (PC6 forbids two separate commits; it does not require `wave-state.yaml` to exist when the epic is complete).

This story does NOT change: the HANDOFF.md schema (S-18.01), the WASM gate logic (S-18.02), or any other E-18 skill.

## Behavioral Contracts

| BC ID | Title | Version | Clauses Exercised |
|-------|-------|---------|------------------|
| BC-5.41.001 | wave-gate writes verified HANDOFF.md with all 9 base required fields (+epic_status on EPIC-COMPLETE wave) | v1.25 | PC1, PC2, PC3, PC10 (four-step agent-orchestrated flow: --emit-handoff stdout-only, agent Write tool, PostToolUse gate fires, --commit two-arm conditional: HAS-NEXT-WAVE stages both files / EPIC-COMPLETE stages HANDOFF.md alone + removes stale wave-state.yaml; HandoffFileAbsent hard-abort on missing HANDOFF.md on both arms; fail-loud-no-fallback; EC-017 path-conditional) |
| BC-5.41.002 | wave-gate produces curated wave-state.yaml manifest listing next-wave stories and spec deps — no RAG | v1.18 | PC6 (atomicity at git-commit boundary: HAS-NEXT-WAVE — HANDOFF.md + wave-state.yaml in ONE --commit; EPIC-COMPLETE — HANDOFF.md alone in ONE --commit; two separate git commits forbidden; single-file EPIC-COMPLETE commit is NOT a PC6 violation) |

*Title columns are strict subsets of the respective BC H1s (all text drawn verbatim from the H1; anti-fabrication cross-checks and wave-closed tail omitted as editorial abbreviation; load-bearing traceability keys are the BC IDs + version cites + clause lists).*

## Acceptance Criteria

### AC-001 (traces to BC-5.41.001 PC10 — four-step agent-orchestrated flow; Write tool mandate)
The restructure is applied in BOTH `plugins/vsdd-factory/skills/wave-handoff/wave-handoff.sh` (subcommand dispatch: `--emit-handoff` arm present; no HANDOFF.md written in the `--emit-handoff` code path) AND `plugins/vsdd-factory/skills/wave-handoff/lib/write-handoff.sh` (`write_handoff()` emits to stdout; the `} > "$output_path"` bash-redirect construct is ABSENT from the file). Oracle: (a) `grep -n '} > "$output_path"' lib/write-handoff.sh` returns no matches; (b) `grep -n '\-\-emit-handoff' wave-handoff.sh` returns the dispatch arm. No bash-redirection path may remain as primary or fallback HANDOFF.md write mechanism (BC-5.41.001 PC10 fail-loud-no-silent-fallback).

### AC-002 (traces to BC-5.41.001 PC10 — PostToolUse gate fires on the agent Write-tool HANDOFF.md call)
An integration test confirms that running the wave-handoff skill end-to-end causes the factory-dispatcher to emit a `PostToolUse` event for the HANDOFF.md Write tool call, and the `validate-wave-handoff-completeness` gate (BC-4.14.001) is invoked. The gate must fire — not be skipped — on the production wave-close path.

**Test strategy — production-grade requirement:** The existing bats suite runs `wave-handoff.sh` directly as a bash script; this produces no PostToolUse event. The production-grade AC-002 test requires one of the following approaches (in preference order):
1. **Dispatcher-integration test:** A bats test that drives the full `claude` harness in a sandbox session, invoking the wave-handoff SKILL.md steps via the agent, and verifies the dispatcher emits a `PostToolUse` event logged in `dispatcher-internal-<date>.jsonl` for the HANDOFF.md Write call.
2. **Mock PostToolUse event injection:** A bats harness that directly invokes the `validate-wave-handoff-completeness` WASM gate with a synthesized PostToolUse payload matching a HANDOFF.md Write tool call, and asserts the gate fires (not a no-op). This genuinely exercises the gate-firing path.

If neither approach is feasible with the current bats infrastructure, the implementer MUST document the structural constraint explicitly in the bats test file with a `skip` or `@test ... # INFRASTRUCTURE-GAP: requires dispatcher-integration test harness` annotation — do NOT silently substitute a `grep` check on the SKILL.md for a genuine gate-firing assertion. The fallback of asserting that the gate would fire because SKILL.md contains a Write-tool step is a structural assertion, not a gate-firing verification, and must be labeled as such. Per the production-grade principle, the implementer must pursue the real test first and document explicitly if a genuine limitation prevents it.

Additionally: if the Write tool is unavailable at invocation time, the skill MUST surface `HandoffWriteToolUnavailable` and NOT fall back to bash redirection (BC-5.41.001 PC10 / EC-016).

### AC-003 (traces to BC-5.41.001 PC10 — byte-identity write-path verification)
After the Write tool call completes (SKILL.md step S2/S3), the skill MUST verify that the written content is byte-identical to the `--emit-handoff` payload before the `--commit` subcommand is invoked. An integration test confirms this verification fires and that the HANDOFF.md content produced by the amended skill is byte-identical to the frozen golden fixture committed under `plugins/vsdd-factory/tests/fixtures/wave-handoff-golden/`. The golden fixture is captured during T-4a (BEFORE any T-2 changes are made) and serves as the cross-version regression oracle. If content verification fails, the skill surfaces a hard error identifying the byte-count discrepancy before attempting to commit (BC-5.41.001 PC10 write-path verification clause).

### AC-004 (traces to ADR-026 §Decision 8 — spec constraint codified in restructured form)
ADR-026 §Decision 8 (v1.23) carries the explicit agent-orchestrated emit→Write→commit control flow constraint. The implementer must verify this amendment is in place before beginning T-1. The stable anchor is `ADR-026 §Decision 8` per POLICY 19 (informational, non-load-bearing; the behavioral anchor is the section heading, not the version number).

### AC-005 (traces to BC-5.41.002 PC6 — ONE atomic git commit on both wave paths; two separate commits forbidden)
Under the restructured flow, the `--commit` bash subcommand creates ONE atomic git commit via `commit_to_artifacts` on BOTH arms:

- **HAS-NEXT-WAVE arm:** `--commit` stages BOTH `HANDOFF.md` (on disk from the agent Write tool step) AND `wave-state.yaml` (on disk from `--emit-wave-state`) in ONE git commit. A bats test verifies that the git log on `factory-artifacts` after a HAS-NEXT-WAVE run contains a single new commit that includes both files.
- **EPIC-COMPLETE arm:** `--commit` stages `HANDOFF.md` ALONE in ONE git commit (see AC-006 for the dedicated EPIC-COMPLETE test). A test MUST NOT falsely flag the legitimate single-file EPIC-COMPLETE commit as a PC6 violation — PC6 forbids splitting the wave-close into two separate commits; it does not require `wave-state.yaml` to exist when the epic is complete.

Two separate git commits — one for `HANDOFF.md` and a second for `wave-state.yaml` — remain a BC-5.41.002 PC6 violation on both arms. This atomicity guarantee is preserved across the agent-Write/bash-commit boundary per BC-5.41.002 PC6 v1.18 clarification and the EPIC-COMPLETE single-file permission per v1.18.

### AC-006 (traces to BC-5.41.001 PC10 EPIC-COMPLETE arm + BC-5.41.002 PC6 — EPIC-COMPLETE `--commit` succeeds with HANDOFF.md alone)
When `wave-handoff` is invoked on the EPIC-COMPLETE path (`epic_status: complete` / `next_wave_stories: []`), the `--commit` bash subcommand MUST:

1. Succeed (exit 0) with `HANDOFF.md` present at `${ARTIFACTS_WT}/HANDOFF.md` and `wave-state.yaml` ABSENT from disk (as it is intentionally not produced on this path by `--emit-wave-state`).
2. Remove any stale `wave-state.yaml` that may exist on disk before staging (so the commit contains only `HANDOFF.md`).
3. Create exactly ONE atomic git commit containing only `HANDOFF.md` — not two commits, and not a commit that also includes a stale `wave-state.yaml`.
4. NOT abort with `HandoffFileAbsent` or any other error solely because `wave-state.yaml` is absent (its absence is expected and correct on the EPIC-COMPLETE path per BC-5.41.002 PC3 EPIC-COMPLETE exception).

A bats test verifies the EPIC-COMPLETE `--commit` arm: given a test fixture with `HANDOFF.md` present and `wave-state.yaml` absent (simulating the state after `--emit-wave-state` is skipped on EPIC-COMPLETE), `--commit` exits 0, produces a single commit on `factory-artifacts` containing only `HANDOFF.md`, and does not raise `HandoffFileAbsent`. This test confirms that the pre-v1.6 regression (where the unconditional two-file presence guard would have aborted on the EPIC-COMPLETE path) is closed.

(traces to BC-5.41.001 PC10 step 4 EPIC-COMPLETE arm — `--commit` stages `HANDOFF.md` ALONE, hard-abort ONLY on missing `HANDOFF.md`; traces to BC-5.41.002 PC6 — single-file EPIC-COMPLETE commit is NOT a PC6 violation; references ADR-026 §Decision 8 EPIC-COMPLETE carve-out)

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| `wave-handoff.sh` (subcommand dispatch) | `plugins/vsdd-factory/skills/wave-handoff/wave-handoff.sh` | Effectful (bash, git, stdout) |
| `write_handoff()` (stdout-emit refactor) | `plugins/vsdd-factory/skills/wave-handoff/lib/write-handoff.sh` | Effectful (stdout only, after refactor) |
| SKILL.md (genuine agent steps S1–S5) | `plugins/vsdd-factory/skills/wave-handoff/SKILL.md` | Architecture spec (agent orchestration instructions) |
| PostToolUse gate (existing) | `crates/hook-plugins/validate-wave-handoff-completeness/` | Pure (YAML parse; built by S-18.02) |
| ADR-026 §Decision 8 amendment | `.factory/specs/architecture/decisions/ADR-026-*.md` | Architecture spec (non-code) |

**Subsystem anchors:**
- SS-05 owns this story's orchestration scope because SS-05 (Pipeline Orchestration) governs the wave-handoff skill's behavioral contract (BC-5.41.001, BC-5.41.002) per ARCH-INDEX Subsystem Registry.
- SS-06 owns the skill file artifacts (`plugins/vsdd-factory/skills/wave-handoff/`) per ARCH-INDEX (SS-06: Skill Catalog).

**Dependency anchors:**
- S-18.13 depends on S-18.01 because S-18.01 defines the HANDOFF.md schema that the amended `--emit-handoff` subcommand must continue to produce correctly; verifying AC-003 (byte-identical content) requires S-18.01's schema to be settled.
- S-18.13 depends on S-18.02 because S-18.02 builds the WASM gate that AC-002 verifies is triggered by the Write tool call; the integration test cannot pass until the gate crate exists and is registered.
- S-18.13 blocks nothing because no downstream story depends on the write-path being Write-tool-mediated via the subcommand model; the gate (S-18.02) is the dependency for S-18.08, not this story.

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | Write tool call fails (e.g., permission error on factory-artifacts worktree path) | Skill propagates the error; HANDOFF.md is NOT written; wave-close aborts with a clear error message |
| EC-002 | HANDOFF.md content is malformed (gate fires and blocks) | Gate returns block_intent=true; wave-close is prevented; operator sees HandoffIncomplete error from gate |
| EC-003 | Wave-handoff skill invoked outside Claude Code session (no Write tool available) | Hard error: `HandoffWriteToolUnavailable`; skill MUST NOT fall back to bash redirection; maps to BC-5.41.001 EC-016 (see below) |
| EC-004 | HANDOFF.md already exists at path (idempotent re-run) | Write tool overwrites; gate fires and validates the new content; existing EC-015 in BC-5.41.001 covers byte-identical re-run |
| EC-016 (mirrors BC-5.41.001 EC-016) | The Claude Code `Write` tool is unavailable in the current harness context at the time `wave-handoff` attempts to write HANDOFF.md (e.g., tool not registered, harness version too old, or tool disabled by session config) | HARD ERROR: `HandoffWriteToolUnavailable`; skill surfaces human-readable message: `HandoffWriteToolUnavailable: HANDOFF.md must be written via the Write tool (Claude Code native tool call); bash redirection is forbidden. Ensure the Write tool is available in the current harness context.` Skill MUST NOT fall back to bash redirection or any other non-tool write mechanism. Wave-close does NOT proceed. |
| EC-017 (mirrors BC-5.41.001 EC-017) | The `--commit` bash subcommand is invoked but `HANDOFF.md` is absent on disk at `${ARTIFACTS_WT}/HANDOFF.md` — the Write tool step (S2 in SKILL.md) either was not run, failed silently, or wrote to a different path | HARD ERROR: `HandoffFileAbsent` on BOTH the HAS-NEXT-WAVE and EPIC-COMPLETE paths. **HAS-NEXT-WAVE path:** `--commit` MUST verify BOTH `HANDOFF.md` AND `wave-state.yaml` are present before staging; abort if EITHER is absent (`HandoffFileAbsent` message for whichever file is absent). **EPIC-COMPLETE path:** `--commit` MUST verify `HANDOFF.md` is present before staging; `wave-state.yaml` is INTENTIONALLY ABSENT on this path and MUST NOT be checked for presence (its absence is correct, not an error). On both paths, if `HANDOFF.md` is absent, `--commit` MUST abort with message: `HandoffFileAbsent: HANDOFF.md not found at ${ARTIFACTS_WT}/HANDOFF.md before commit; aborting atomic commit`. The `--commit` subcommand MUST NOT proceed to `git add` or `git commit`. Wave-close does NOT proceed. |

## Tasks

- **T-1:** Read `plugins/vsdd-factory/skills/wave-handoff/wave-handoff.sh` (identify `main()` structure, write_handoff call site, commit_to_artifacts call site), `plugins/vsdd-factory/skills/wave-handoff/lib/write-handoff.sh` (identify `} > "$output_path"` bash-redirect), and `plugins/vsdd-factory/skills/wave-handoff/SKILL.md` (confirm current "Behavior Overview" descriptive structure). Read ADR-026 §Decision 8 (v1.23) and BC-5.41.001 PC10 before beginning implementation.
- **T-4a (MUST precede T-2):** BEFORE any code change, run the wave-handoff skill with standard input fixtures using the CURRENT bash-redirect path and capture the produced HANDOFF.md output as frozen golden fixtures. Commit them to `plugins/vsdd-factory/tests/fixtures/wave-handoff-golden/`. These fixtures become the regression oracle for AC-003 byte-identity verification. If T-2 runs before T-4a, the regression oracle cannot be captured from the baseline path.
- **T-2a:** In `wave-handoff.sh`: replace the monolithic `main()` function with a subcommand dispatcher. Implement the three subcommands: `--emit-handoff` (assemble HANDOFF.md payload, emit to stdout, no disk write, no commit), `--emit-wave-state` (write wave-state.yaml via bash, skip on EPIC-COMPLETE), `--commit` (implements the two-arm presence guard per BC-5.41.001 PC10 step 4: HAS-NEXT-WAVE verifies BOTH `HANDOFF.md` + `wave-state.yaml` present before staging both in ONE atomic git commit; EPIC-COMPLETE verifies `HANDOFF.md` alone, removes stale `wave-state.yaml`, stages `HANDOFF.md` alone in ONE atomic git commit; `HandoffFileAbsent` hard-abort on missing `HANDOFF.md` on both arms).
- **T-2b:** In `plugins/vsdd-factory/skills/wave-handoff/lib/write-handoff.sh`: change `write_handoff()` to emit the assembled HANDOFF.md payload to stdout — remove the `} > "$output_path"` bash-redirect. Update all callers of `write_handoff()` to capture stdout rather than relying on file side-effects.
- **T-2c:** Rewrite `plugins/vsdd-factory/skills/wave-handoff/SKILL.md` from its current descriptive "Behavior Overview" to GENUINE numbered agent steps per ADR-026 §Decision 8 (v1.23): S1 parse sprint-state via Bash (`--emit-handoff`) → S2 agent writes HANDOFF.md via Write tool (PostToolUse gate fires here) → S3 agent verifies byte-identity → S4 bash writes wave-state.yaml (`--emit-wave-state`) → S5 bash atomic commit (`--commit`). The SKILL.md MUST drive the agent's actual execution sequence, not describe bash internals.
- **T-3:** Write a failing bats integration test confirming that a wave-handoff skill run causes a PostToolUse event for HANDOFF.md (gate fires). Pursue the production-grade approach (dispatcher-integration test or mock PostToolUse event injection) per AC-002. Document explicitly if the bats infrastructure cannot support real gate-firing verification.
- **T-4:** Write a bats test confirming HANDOFF.md content is byte-identical to the frozen golden fixture (AC-003).
- **T-5:** Run existing wave-handoff bats tests to confirm no behavioral regression under the subcommand model.
- **T-6:** Confirm in `hooks-registry.toml` that the gate entry's `tool = "Write|Edit"` covers the Write tool call emitted by the restructured SKILL.md S2 step (should require no change — verify only).

**Implementation sequence:** T-1 → T-4a (capture golden fixture from current bash-redirect path BEFORE any code change) → T-2a (wave-handoff.sh subcommand dispatch) → T-2b (write-handoff.sh stdout-emit) → T-2c (SKILL.md genuine agent-step rewrite per ADR-026 §Decision 8 v1.23) → T-3 → T-4 → T-5 → T-6.

**Critical ordering:** T-4a MUST precede T-2a. T-2c (SKILL.md) must be a genuine rewrite — it is not acceptable to prepend a Write-tool step to an otherwise unchanged "Behavior Overview" body.

## Forbidden Dependencies

`plugins/vsdd-factory/skills/wave-handoff/wave-handoff.sh` and `lib/write-handoff.sh` after this change MUST NOT:
- Write HANDOFF.md to disk via bash redirection (`>` or `>>` operators, `} >` form, `tee`) as the primary write path
- Shell out in a way that bypasses the Claude Code tool layer for the HANDOFF.md write
- Silently fall back to bash redirection if the Write tool is unavailable (must fail loudly per EC-003 / EC-016)

`wave-handoff.sh --commit` MUST NOT:
- Run `git add` or `git commit` if `HANDOFF.md` is absent from `${ARTIFACTS_WT}` — this is a hard error on BOTH the HAS-NEXT-WAVE and EPIC-COMPLETE paths (EC-017)
- On the HAS-NEXT-WAVE path: run `git add` or `git commit` if `wave-state.yaml` is also absent from `${ARTIFACTS_WT}` (EC-017 equivalent guard)
- On the EPIC-COMPLETE path: check for `wave-state.yaml` presence or abort solely because `wave-state.yaml` is absent — its absence is correct and expected on EPIC-COMPLETE
- Create two separate git commits — one for `HANDOFF.md` and a second for `wave-state.yaml` — on either path (BC-5.41.002 PC6 violation; a single-file EPIC-COMPLETE commit is still ONE commit and is permitted)

## Token Budget

| Item | Estimated tokens |
|------|-----------------|
| This story file | ~4 000 |
| BC-5.41.001 (v1.25) | ~9 000 |
| BC-5.41.002 (v1.18, PC6 atomicity + EPIC-COMPLETE single-file) | ~5 000 |
| ADR-026 §Decision 8 excerpt | ~1 500 |
| `wave-handoff.sh` (existing, read) | ~2 000 |
| `lib/write-handoff.sh` (existing, read) | ~1 500 |
| `SKILL.md` (existing, read) | ~1 000 |
| Bats integration tests (new) | ~3 000 |
| Golden fixture capture output | ~1 000 |
| Tool outputs (bats run, grep oracle) | ~1 500 |
| **Total** | **~29 500** |

Budget is ~29.5k tokens — within 20–30% of a 200k-token context window. The four-file restructure justifies the higher budget vs v1.4's ~15.5k estimate; still safe for one dispatch.

## Dependencies

- **Depends on:** S-18.01 (HANDOFF.md schema; byte-identical content AC-003 requires schema to be settled; `write-handoff.sh` is the S-18.01 deliverable being refactored)
- **Depends on:** S-18.02 (WASM gate must be built and registered before AC-002 integration test can pass)
- **Blocks:** none

## Previous Story Intelligence

S-18.01 (wave-handoff skill) established the HANDOFF.md schema and the skill's overall structure, including `wave-handoff.sh`, `lib/write-handoff.sh`, and `SKILL.md`. S-18.02 (WASM gate) built the PostToolUse completeness validator. This story closes the integration gap between them.

**Key lesson from v1.4:** The v1.4 scope assumed the skill was already agent-orchestrated (SKILL.md with numbered steps and Write in `allowed-tools`). The actual implementation (confirmed by architect reading `wave-handoff.sh`) is a monolithic bash run ending in `main "$@"`. There is no agent seam in the current code. The implementer MUST read T-1's targets before assuming anything about the skill's structure — the baseline reality, not the v1.4 assumption, drives the T-2a/T-2b/T-2c implementation.

The bats test pattern for dispatcher hook invocation exists in `plugins/vsdd-factory/tests/` (see `wave-handoff.bats`). The implementer should read those tests before writing T-3 to follow the established pattern.

## Architecture Compliance Rules

- The `--commit` subcommand must target the same file paths that the prior bash-redirect path wrote to (no path change)
- The factory-artifacts worktree path conventions from ADR-026 §Decision 1 must be respected
- `on_error = "continue"` on the WASM gate (S-18.02) means a gate crash is fail-open; this story's fix ensures the gate fires, but a gate crash still results in Continue per BC-4.14.001 PC6 / AC-013 in S-18.02
- `set -euo pipefail` must be enforced in all modified bash scripts (equivalent of `#[deny(warnings)]` for shell)
- SKILL.md MUST use genuine numbered execution steps, not a "Behavior Overview" section that describes what the bash internals do — the agent reads SKILL.md to know what steps to execute, not what the bash script does
- The `--commit` subcommand MUST implement the two-arm conditional per BC-5.41.001 PC10 step 4 (v1.25): HAS-NEXT-WAVE stages both files; EPIC-COMPLETE stages `HANDOFF.md` alone and removes stale `wave-state.yaml`. A single-arm unconditional implementation that always demands both files is a PC10 violation on the EPIC-COMPLETE path.

## Library & Framework Requirements

- No new library dependencies; this is a structural refactor of an existing shell skill
- The Write tool is a Claude Code native tool — no external packages required
- Bats version: pinned per existing `plugins/vsdd-factory/tests/` test infrastructure

## File Structure Requirements

| File | Action | Notes |
|------|--------|-------|
| `plugins/vsdd-factory/skills/wave-handoff/wave-handoff.sh` | Modify | Replace monolithic `main()` with subcommand dispatcher; implement `--emit-handoff`, `--emit-wave-state`, `--commit` subcommands; `--commit` must verify both files present before staging |
| `plugins/vsdd-factory/skills/wave-handoff/lib/write-handoff.sh` | Modify | Change `write_handoff()` to emit assembled HANDOFF.md payload to stdout (remove `} > "$output_path"` bash-redirect); update callers |
| `plugins/vsdd-factory/skills/wave-handoff/SKILL.md` | Modify | Rewrite from descriptive "Behavior Overview" to genuine numbered agent steps S1–S5 per ADR-026 §Decision 8 (v1.23) |
| `plugins/vsdd-factory/tests/fixtures/wave-handoff-golden/` | Create | Frozen golden HANDOFF.md fixture(s) captured from the current bash-redirect path (T-4a, BEFORE T-2a) — regression oracle for AC-003 |
| `plugins/vsdd-factory/tests/wave-handoff.bats` | Modify | Add AC-002 PostToolUse gate-fires test (production-grade approach per AC-002 test strategy) + AC-003 golden-fixture byte-identical content test + EC-017 HandoffFileAbsent test |
| `.factory/specs/architecture/decisions/ADR-026-*.md` | (Done) | §Decision 8 amended at v1.24 by architect — EPIC-COMPLETE carve-out on `--commit` step; implementer confirms before T-1 |
| `.factory/specs/behavioral-contracts/ss-05/BC-5.41.001.md` | (Done) | PC10 step 4 + EC-017 amended to path-conditional form at v1.25 by product-owner; implementer reads before T-1 |
| `.factory/specs/behavioral-contracts/ss-05/BC-5.41.002.md` | (Done) | PC6 EPIC-COMPLETE single-file commit explicitly permitted at v1.18 by product-owner; implementer reads before T-1 |

## Changelog

- v1.6 (2026-06-19): EPIC-COMPLETE `--commit` carve-out (story-writer). (F-SP13-P5-001) Propagates architect ADR-026 §Decision 8 (v1.24) + PO BC-5.41.001 (v1.25) + BC-5.41.002 (v1.18) EPIC-COMPLETE carve-out into S-18.13. §Scope `--commit` bullet rewritten from unconditional two-file guard to two-arm conditional: HAS-NEXT-WAVE verifies BOTH `HANDOFF.md` + `wave-state.yaml` and stages both; EPIC-COMPLETE (`epic_status: complete` / `next_wave_stories: []`) verifies `HANDOFF.md` only, removes stale `wave-state.yaml`, stages `HANDOFF.md` alone — hard-abort ONLY on missing `HANDOFF.md` (both arms). T-2a note updated to reflect two-arm implementation obligation. AC-005 clarified: asserts ONE atomic commit on BOTH arms (HAS-NEXT-WAVE two-file, EPIC-COMPLETE single-file); test MUST NOT falsely flag single-file EPIC-COMPLETE commit as PC6 violation. AC-006 added: dedicated EPIC-COMPLETE `--commit` arm test — given `HANDOFF.md` present + `wave-state.yaml` absent, `--commit` exits 0, creates ONE commit on `factory-artifacts` containing only `HANDOFF.md`, does not raise `HandoffFileAbsent` (traces BC-5.41.001 PC10 EPIC-COMPLETE arm + BC-5.41.002 PC6). EC-017 updated to path-conditional form per BC-5.41.001 EC-017 v1.25: `HandoffFileAbsent` fires on missing `HANDOFF.md` on both paths; `wave-state.yaml` absence is NOT an error on EPIC-COMPLETE. BC table version cites: BC-5.41.001 v1.24→v1.25 (clauses column updated with two-arm PC10 step 4 language); BC-5.41.002 v1.17→v1.18 (PC6 clause updated with EPIC-COMPLETE single-file note). ADR-026 informational cite v1.23→v1.24 in §Spec Amendments, SKILL.md S5 note, and §File Structure Requirements Done row. §Forbidden Dependencies `--commit` block rewritten to path-conditional form. §Architecture Compliance Rules: two-arm mandate added as explicit rule. Token Budget BC version cites updated (informational). POLICY 14 five-leg parity: version 1.5→1.6; `last_amended` prefix updated; `modified[]` entry prepended; this Changelog row added.
- v1.5 (2026-06-19): Architect redesign — restructure wave-handoff from monolithic bash run to agent-orchestrated subcommand model (story-writer). Root cause: the v1.4 §Scope assumed the skill was already agent-orchestrated (SKILL.md numbered-step body with Write in `allowed-tools`). The actual implementation is a monolithic bash run (`wave-handoff.sh` ends in `main "$@"`); there is no agent seam between Step 4 (write_handoff call) and Step 6 (commit_to_artifacts). §Scope rewritten to reflect the proper four-file restructure: (1) wave-handoff.sh — monolithic `main()` replaced with three bash subcommands (`--emit-handoff`: payload→stdout, no disk write; `--emit-wave-state`: bash writes wave-state.yaml, skipped on EPIC-COMPLETE; `--commit`: verify BOTH files present, ONE atomic git commit); (2) lib/write-handoff.sh — `write_handoff()` emits to stdout (remove `} > "$output_path"`); (3) SKILL.md — genuine numbered agent steps S1–S5 per ADR-026 §Decision 8 v1.23 (not a descriptive overview); (4) tests/fixtures/wave-handoff-golden/ — golden fixture directory (T-4a capture). Tasks replaced: T-1 → T-4a → T-2a → T-2b → T-2c → T-3 → T-4 → T-5 → T-6. AC-001 expanded: two-file oracle (absence of `} > "$output_path"` + presence of `--emit-handoff` dispatch arm); AC-002 requires production-grade PostToolUse gate-firing test (dispatcher-integration or mock injection; fallback-as-structural-assertion must be labeled); AC-004 ADR cite updated to §Decision 8 (v1.23); AC-005 added (single atomic `--commit` contains BOTH files; traces BC-5.41.002 PC6); EC-017 added (HandoffFileAbsent: `--commit` invoked but HANDOFF.md absent → hard error). behavioral_contracts: BC-5.41.002 added (POLICY 8 propagation: body BC table row for BC-5.41.002 v1.17 PC6 added; AC-005 trace added; Token Budget BC count updated to 2 BCs). inputs: BC-5.41.002 added. points 5→10; effort_class single-day→multi-day; estimated_days 1→2. POLICY 14 five-leg parity: version 1.4→1.5; last_amended prefix updated; modified[] entry prepended; body Changelog row added.
- v1.4 (2026-06-19): F-SP13-P2-001 + F-SP13-P2-002 precision fixes (story-writer). F-SP13-P2-001: §Scope rewritten from 'ONE file ... one-line change' to accurate two-file mechanism — `write-handoff.sh` `write_handoff()` changed to emit assembled HANDOFF.md payload to stdout (remove bash-redirect); `SKILL.md` gains a new numbered agent step instructing Write-tool invocation to `${ARTIFACTS_WT}/HANDOFF.md`. Architect confirmed skill is AGENT-ORCHESTRATED (SKILL.md `allowed-tools` includes Write; body is numbered agent steps, not a monolithic bash run); PC10 is implementable with no BC/ADR amendment. T-1 and T-2 expanded to reflect the two-file change; implementation sequence updated with T-4a ordering constraint (golden fixture capture before T-2a). F-SP13-P2-002: AC-003 oracle re-anchored from 'previous bash-redirect implementation' (deleted by this story; unavailable at test time) to a frozen golden fixture committed to `plugins/vsdd-factory/tests/fixtures/wave-handoff-golden/`; T-4 gains T-4a sub-task (capture golden fixture BEFORE implementing T-2). File Structure Requirements: SKILL.md added as MODIFY; golden fixtures directory added as CREATE; ADR-026 row marked Done. POLICY 14 five-leg parity: version 1.3→1.4; last_amended updated; modified[] entry prepended; this Changelog row added.
- v1.3 (2026-06-19): Spec-first propagation (story-writer). BC-5.41.001 cite bumped v1.22→v1.23; PC10 (Write tool mandate, fail-loud-no-fallback, byte-identity verification) added to clauses-exercised column. AC-001 re-anchored: `postcondition 1` placeholder → `BC-5.41.001 PC10 — Write tool mandate` with expanded no-fallback requirement. AC-002 re-anchored: `postcondition 1` placeholder → `BC-5.41.001 PC10 — PostToolUse gate fires in production` with `HandoffWriteToolUnavailable` hard-error requirement per EC-016. AC-003 re-anchored: `postcondition 2` placeholder → `BC-5.41.001 PC10 — byte-identity write-path verification` with pre-commit content verification requirement. AC-004 updated: confirmed ADR-026 §Decision 8 v1.22 amendment already in place (T-6 done). EC-016 row added to Edge Cases table (mirrors BC-5.41.001 EC-016). EC-003 updated to reference EC-016. T-6 marked DONE in Tasks. SPEC AMENDMENT NEEDED section rewritten to COMPLETE. Frontmatter pending-PO-comment block removed. `status: draft` → `status: ready`. POLICY 14 five-leg parity: frontmatter version 1.2→1.3; last_amended prefix updated; modified[] entry prepended; body Changelog row added.
- v1.2 (2026-06-19): C-P5-001 fix (LOCAL pass-5 consistency review, POLICY 14). Frontmatter `version:` field bumped 1.1→1.2; all five POLICY 14 parity legs advanced. C-P5-002 fix (POLICY 6): Architecture Mapping subsystem prose labels corrected to ARCH-INDEX Subsystem Registry canonical names — SS-05 parenthetical changed from 'Wave Boundary Orchestration' to 'Pipeline Orchestration'; SS-06 parenthetical changed from 'Skill Implementations' to 'Skill Catalog'. Frontmatter `subsystems: [SS-05, SS-06]` was already correct and left unchanged.
- v1.1 (2026-06-19): C-P4-001 fix (LOCAL pass-4 consistency review, POLICY 7). BC table Title column: replaced enriched text (`wave-handoff skill — HANDOFF.md production, anti-fabrication, and wave-state.yaml production`) — which contained text not present in BC-5.41.001 H1 — with a strict verbatim subset of the H1 (`wave-gate writes verified HANDOFF.md with all 9 base required fields (+epic_status on EPIC-COMPLETE wave)`). Italic annotation updated to accurately describe the subset relationship. BC anchor BC-5.41.001 confirmed correct for this story's scope (PC1 = HANDOFF.md written; PC7 = WASM gate passes after write). No BC-5.41.002 reroute needed (wave-state.yaml is out of scope for this story). Five-leg parity: frontmatter version 1.0→1.1; last_amended prefix updated; modified[] entry added; body Changelog row added.
- v1.0 (2026-06-19): Initial draft authored by story-writer. Anchors finding F-S1802-02 (S-18.02 LOCAL pass-3 adversary). Documents architect determination (Option A: Write-tool write path), originating [process-gap] in ADR-026 §Decision 8, and the spec amendment obligation (PO to amend BC-5.41.001; architect to amend ADR-026 §Decision 8). High-level ACs scoped for anchor/draft status; detailed ACs to be refined during delivery.
