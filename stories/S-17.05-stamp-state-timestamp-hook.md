---
document_type: story
level: ops
story_id: "S-17.05"
epic_id: "E-17"
version: "1.8"
title: "S-17.05: stamp-state-timestamp PostToolUse hook — hook-authored STATE.md timestamp re-stamp + identity-gated factory_lock keep-alive (retires verify-state-timestamp-refresh) (ADR-046)"
status: draft
producer: story-writer
timestamp: 2026-08-25T00:00:00Z
last_amended: "2026-08-28 (v1.8) — D-1127 finalization doc-sweep: exhaustive Red Gate count correction sweep (all doc-only; post-3-CLEAN per D-1128): F-P12-001 tally-sentence 28→32/31→35/35→39; T-1 18→32 + 22→39; T-7 27→39; T-3 + Purity table + File Structure table 18→32; Out-of-Scope + T-7 ~28 verify-state-timestamp-refresh count de-numbered (drift-resistant). | 2026-08-28 (v1.7) — F-P11-001 + O-P11-1: BC gate header updated to current versions (BC-4.17.001 v1.28, BC-5.40.001 v1.21); [pending] traceability claim corrected (BC-5.40.001 v1.21 Traceability lists S-17.05 as confirmed implementing/anchor story since v1.10 F-P25-002); Token Budget self-version label updated v1.1→v1.7. Doc-only; no AC/PC/semantic change. | 2026-08-28 (v1.6) — BC-4.17.001 body-table version cite sync 1.27→1.28 (F-P10-001 leg-5 propagation from pass-9 burst BC-4.17.001 v1.27→v1.28 seal; doc-only; no AC/PC/semantic change). | 2026-08-28 (v1.5) — Consolidated AC↔BC-4.17.001 reconciliation (D-1124 wave, post-local-pass-4): fix AC-008/EC-013 contradiction + add ACs for Precondition-1 tool-write-success, Invariant-8 soft-warn, EC-010/EC-014/EC-015; Red Gate table + EC table completed. BC governs; story conformance only. | 2026-08-28 (v1.4) — Add AC-015 tracing BC-4.17.001 PC3b renewal-indeterminate diagnostic (emit_event + log_warn) — closes O-1 traceability gap behind local-pass-3 finding F-S1705-P3-001. | 2026-08-28 (v1.3) — ADR-046 Wave-5 decomposition boundary correction (D-1126 follow-up): S-17.05 now owns the renew_lock_with_now→TTL_SECONDS literal migration (moved from S-17.06 which correctly deferred it; S-17.05 creates TTL_SECONDS). Sub-task added to T-2; MODIFY row for crates/factory-lock/src/lib.rs (literal migration only, not doc-comments) added to File Structure; Red Gate test test_renew_lock_with_now_uses_ttl_seconds_not_bare_literal added (20 Rust total); NOTE-T-8 clarified; Explicitly NOT modified list updated. | 2026-08-27 (v1.2) — ADR-046 Wave-5 decomposition re-scope (D-1124): T-8 (factory-lock doc-comment corrections) relocated to S-17.06 which owns all crates/factory-lock/src/lib.rs edits; depends_on updated [] → [S-17.06] (stamper main.rs calls factory_lock::renew_lock_if_holder created by S-17.06); T-2 extended to add STATE_MD_MAX_BYTES: u32 = 262144 canonical constant to factory-lock-parse + Red Gate test test_state_md_max_bytes_constant_equals_262144 (19 Rust total); T-3 updated — main.rs wires factory_lock::renew_lock_if_holder(content_after_pc1, resolve_identity, now_fn) where resolve_identity = || factory_lock::classify_identity_resolution(host::exec_subprocess([git,config,user.email])); lib.rs injectable-callback signature unchanged, 18 existing Rust unit test mocks unchanged; VP-TBD comment corrected to VP-TBD-1..4, VP-TBD-7..9 (7 total); trim_git_email concern removed (S-17.06 owns canonical home; S-17.05 uses it transparently via renew_lock_if_holder). | 2026-08-27 (v1.1) — Added T-8: correct stale factory-lock crate doc-comments (renew_lock_with_now algorithm doc, Ok(None)-arm inline comment, parse_lock doc) to reflect post-F-P56-001 semantics; bundled per O-P61-001/O-P62-001 human directive. Updated BC version citations (BC-4.17.001 1.0→1.26; BC-5.40.001 1.4→1.21). Updated input-hash (f2c092e→4702970). | 2026-08-25 (v1.0) — Initial authoring (story-writer; ADR-046 ratified 2026-08-25; BC-4.17.001 v1.0 + BC-5.40.001 v1.4 PC4 actor-reassignment). New E-17 wave-5 story: crates/hook-plugins/stamp-state-timestamp/ PostToolUse plugin (unconditional timestamp: re-stamp; identity-gated factory_lock.expires_at renewal); TTL_SECONDS canonical constant added to crates/factory-lock-parse/; hooks-registry.toml stamper entry added + verify-state-timestamp-refresh entry deregistered in the same commit (ADR-046 Decision 3 atomicity); new bats suite; verify-state-timestamp-refresh.bats retired (crate source + Rust unit tests explicitly LEFT IN PLACE per ADR-046 Decision 2, pending a separate future cleanup story). 14 ACs; exhaustive 5-row identity-gate truth table (AC-003..AC-007)."
phase: brownfield-backfill
cycle: v1.0-feature-engine-discipline-pass-1
inputs:
  - .factory/specs/architecture/decisions/ADR-046-posttooluse-hook-authored-statemd-wall-clock-stamping-timestamp-lock-keep-alive.md
  - .factory/specs/behavioral-contracts/ss-04/BC-4.17.001.md
  - .factory/specs/behavioral-contracts/ss-05/BC-5.40.001.md
  - .factory/specs/architecture/decisions/ADR-025-single-writer-factory-locklease-prevent-concurrent-session-races-on-factory-artifacts-orphan-branch.md
  - crates/hook-plugins/verify-factory-lock/src/lib.rs
  - crates/factory-lock-parse/src/lib.rs
  - plugins/vsdd-factory/bin/factory-lock-write.sh
  - plugins/vsdd-factory/hooks-registry.toml
input-hash: "6067e5f"
traces_to:
  - .factory/specs/behavioral-contracts/ss-04/BC-4.17.001.md
  - .factory/specs/behavioral-contracts/ss-05/BC-5.40.001.md
  - .factory/specs/architecture/decisions/ADR-046-posttooluse-hook-authored-statemd-wall-clock-stamping-timestamp-lock-keep-alive.md
wave: 5
priority: P1
points: 8
track: factory-state-durability
effort_class: multi-day
depends_on: ["S-17.06"]
blocks: []
behavioral_contracts:
  - "BC-4.17.001"
  - "BC-5.40.001"
verification_properties: []
# BC status: VP-TBD-1..4, VP-TBD-7..9 (BC-4.17.001 Verification Properties table; 7 total) pending formal-verifier
# VP-INDEX registration per vp_index_is_vp_catalog_source_of_truth policy — not authored by
# story-writer. State-manager/architect to allocate VP-NNN IDs once this story is scheduled.
domain_invariants: []
tdd_mode: strict
target_module: "crates/hook-plugins/stamp-state-timestamp, crates/factory-lock-parse"
subsystems:
  - "SS-04"
  - "SS-07"
estimated_days: 2.0
assumption_validations: []
risk_mitigations: []
anchored_adrs:
  - ADR-046
closes: []
modified:
  - "2026-08-27 (v1.1): added T-8 factory-lock doc-comment fix task (O-P61-001/O-P62-001, human-directed 2026-08-27); updated BC version citations (BC-4.17.001→1.26, BC-5.40.001→1.21); refreshed input-hash"
  - "2026-08-27 (v1.2): ADR-046 Wave-5 re-scope (D-1124): T-8 relocated to S-17.06; depends_on [] → [S-17.06]; T-2 STATE_MD_MAX_BYTES added; T-3 renew_lock_if_holder wiring note; VP-TBD comment corrected; trim_git_email concern removed"
  - "2026-08-28 (v1.3): ADR-046 Wave-5 decomposition boundary correction (D-1126 follow-up): renew_lock_with_now→TTL_SECONDS literal migration added to T-2 sub-task; MODIFY crates/factory-lock/src/lib.rs row added to File Structure (literal migration only; doc-comments remain S-17.06); Red Gate test test_renew_lock_with_now_uses_ttl_seconds_not_bare_literal added; Red Gate count 19+4=23 → 20+4=24; NOTE-T-8 text clarified (doc-comments only prohibition); Explicitly NOT modified updated; BC-4.17.001 citation updated 1.26→1.27"
  - "2026-08-28 (v1.5): consolidated AC↔BC-4.17.001 reconciliation (7 gaps, post-local-pass-4): AC-008 narrowed to structural failures only (missing-timestamp-anchor clause removed; OutputTooLarge + advisory log_warn added); AC-006 extended (EC-010 AlreadyExpired expired-self-held case); AC-016 added (EC-013 valid-frontmatter-no-timestamp-anchor scoped exception); AC-017 added (Precondition-1 F-013 tool-write-success gate); AC-018 added (Invariant-8 state_md_approaching_cap soft-warn); AC-019 added (EC-014 duplicate-timestamp-key advisory); EC table updated (EC-010/EC-013/EC-014/EC-015); Red Gate table: test_missing_timestamp_anchor_writes_nothing retired (asserted wrong behavior per BC PC3a scoped exception); 9 new rows added; count 27→35 (31 Rust + 4 bats); Changelog row v1.5 added"
  - "2026-08-28 (v1.4): add AC-015 tracing BC-4.17.001 PC3b renewal-indeterminate diagnostic (emit_event + log_warn); 3 new Rust unit Red Gate tests (stamp-state-timestamp/src/lib.rs); Red Gate count 24→27 (23 Rust + 4 bats); EC-004 updated to reference AC-015; BC table updated to cite PC3b; T-7 count updated 23→27; Changelog row v1.4 added — closes O-1 traceability gap behind local-pass-3 finding F-S1705-P3-001"
  - "2026-08-28 (v1.6): BC-4.17.001 body-table version cite sync 1.27→1.28 (F-P10-001 leg-5 propagation from pass-9 burst BC-4.17.001 v1.27→v1.28 seal; doc-only; no AC/PC/semantic change)"
  - "2026-08-28 (v1.7): F-P11-001 + O-P11-1 — BC gate header version cites updated (BC-4.17.001 v1.0→v1.28, BC-5.40.001 v1.4→v1.21); [pending] traceability claim corrected (BC-5.40.001 v1.21 Traceability confirms S-17.05 as implementing/anchor story); Token Budget self-version label updated v1.1→v1.7; Changelog rows v1.6 and v1.7 added"
  - "2026-08-28 (v1.8): D-1127 finalization doc-sweep — exhaustive Red Gate count correction (all doc-only; post-3-CLEAN per D-1128): F-P12-001 tally 28→32/31→35/35→39; T-1 18→32 + 22→39; T-7 27→39; T-3 18→32 (x2); Purity table 18→32; File Structure table 18→32; Out-of-Scope ~28 de-numbered; T-7 ~28 verify-state-timestamp-refresh de-numbered; Token Budget self-version v1.7→v1.8; Changelog row v1.8 added"
---

# S-17.05: stamp-state-timestamp PostToolUse Hook — Hook-Authored Timestamp + Identity-Gated Lock Keep-Alive

**Epic:** E-17 — Factory State Durability and Concurrency (brownfield-backfill #170 lineage)
**Wave:** 5
**Depends on:** S-17.06 (`factory_lock::renew_lock_if_holder`, `factory_lock::classify_identity_resolution`,
`factory_lock::trim_git_email` — all created by S-17.06 and called from `stamp-state-timestamp/src/main.rs`;
`crates/factory-lock-parse/` exists and is merged — S-17.04 PR #184 3b2a378c)
**Blocks:** none
**Priority:** P1 (engine-hardening; targets rc.25 per operator directive; ships as one atomic
release per ADR-046 Decision 3 — see Delivery Constraint below)
**BC gate:** BC-4.17.001 v1.28 (draft; POL-14 auto-promotes to active on this story's PR merge)
+ BC-5.40.001 v1.21 (active; PC4 actor already reassigned to this hook by product-owner
amendment — BC-5.40.001 v1.21 Traceability lists S-17.05 as a confirmed implementing/anchor
story)

## Narrative

- **As a** `state-manager` agent (or any agent) writing `.factory/STATE.md` via `Edit`,
  `Write`, or `MultiEdit`
- **I want** a `PostToolUse` WASM hook that unconditionally re-stamps `timestamp:` to the
  current wall-clock UTC instant after every qualifying write, and — only when I am the
  recorded `factory_lock.holder` — automatically renews `factory_lock.expires_at` to
  `now + 2700s`
- **So that** I (and every other agent) can never author a stale `timestamp:` or a stale
  `expires_at` again, the mid-burst TTL keep-alive obligation (BC-5.40.001 PC4) becomes a
  mechanically-guaranteed property instead of a documentary "state-manager MUST remember to
  run `factory-lock-write.sh renew`" instruction, and a non-holder writer legitimately admitted
  through an expired-lock window (BC-4.13.001 PC2 `LockExpired`) can never silently resurrect
  a foreign holder's dead lock

## Behavioral Contracts

| BC ID | Title (H1, abbreviated) | Version | PCs / Invariants Exercised |
|-------|--------------------------|---------|----------------------------|
| BC-4.17.001 | `stamp-state-timestamp` PostToolUse hook — unconditional `timestamp:` re-stamp, identity-gated `expires_at` renewal, fail-open, frontmatter-only, no lock-lifecycle involvement | 1.28 | PC1 (unconditional re-stamp), PC2 (identity-gated renewal), PC3 (fail-open), PC3b (renewal-indeterminate diagnostic), PC4 (idempotent frontmatter-only rewrite), PC5 (no acquire/release/CAS involvement); Invariants 1–6; Preconditions 1–3 |
| BC-5.40.001 | STATE.md `factory_lock` frontmatter block, sole-writer discipline, TTL auto-expiry, hook-authored mid-burst renewal, state-burst CAS push | 1.21 | PC4 (mid-burst TTL renewal — actor reassigned to this hook); Invariant 1 (mechanized sole-writer exception) |

## Acceptance Criteria

### AC-001 (traces to BC-4.17.001 postcondition PC1)

On any qualifying `PostToolUse` invocation (an `Edit`, `Write`, or `MultiEdit` that landed a
write to `.factory/STATE.md`) where **no** `factory_lock` block is present in the post-write
frontmatter, the hook MUST re-stamp the top-level `timestamp:` field to `chrono::Utc::now()`
formatted as `YYYY-MM-DDTHH:MM:SSZ`, regardless of what value (if any) the agent's own write
proposed. No `factory_lock` block is created as a side effect of this stamp.

**Test (Rust unit):** `test_timestamp_always_restamped_no_lock_present()`

### AC-002 (traces to BC-4.17.001 postcondition PC1 + Invariant 1)

On any qualifying invocation where a `factory_lock` block **is** present, `timestamp:` MUST
still be unconditionally re-stamped — including in the identity-mismatch case where PC2's
gate will separately decline to renew `expires_at` (AC-006). `timestamp:` re-stamping has no
identity gate of any kind; the only paths that skip it are the PC3 fail-open error paths
(AC-008/AC-009).

**Test (Rust unit):** `test_timestamp_restamped_when_lock_held_regardless_of_identity_match()`

### AC-003 (traces to BC-4.17.001 postcondition PC2 — identity-gate truth-table row 1: full match → renew)

When the post-write frontmatter has a `factory_lock` block with a non-empty `holder`, AND
the hook's own resolved caller identity (`git config user.email` via `host::exec_subprocess`)
is byte-equal to that `holder`, the hook MUST renew `factory_lock.expires_at = now +
TTL_SECONDS` (2700s). `holder` and `locked_at` MUST remain byte-identical to their pre-write
values — only `expires_at` changes.

**Test (Rust unit):** `test_identity_match_renews_expires_at()`

### AC-004 (traces to BC-4.17.001 postcondition PC2 — identity-gate truth-table row 2: lock absent → no renewal attempted)

When no `factory_lock` block is present in the post-write frontmatter, the hook MUST NOT
attempt any renewal and MUST NOT create a `factory_lock` block. This is not a "gate failure"
in the safety sense — there is simply nothing to keep alive.

**Test (Rust unit):** `test_no_lock_block_skips_renewal_entirely()`

### AC-005 (traces to BC-4.17.001 postcondition PC2 — identity-gate truth-table row 3: holder present but empty → no renewal)

When a `factory_lock` block is present but its `holder` field is present and extracted as the
empty string, the hook MUST treat this identically to "no lock held" for renewal purposes:
`expires_at` MUST be left byte-identical to its pre-write value. (`timestamp:` is still
re-stamped per AC-002.)

**Test (Rust unit):** `test_empty_holder_skips_renewal()`

### AC-006 (traces to BC-4.17.001 postcondition PC2 + Invariant 2 — identity-gate truth-table row 4: identity MISMATCH → no renewal — SAFETY-CRITICAL; EC-010: AlreadyExpired self-held lock → no renewal)

When a `factory_lock` block is present with a non-empty `holder`, AND the hook's resolved
caller identity is **NOT** byte-equal to `holder` — including the specific hazard case where
the writer was legitimately admitted through BC-4.13.001 PC2 (`LockExpired`, i.e. a foreign,
expired lock let a new caller's tool call through) — the hook MUST leave `expires_at`
byte-identical to its pre-write value. A foreign or expired holder's lock is NEVER silently
resurrected by this hook. This is the load-bearing safety property distinguishing this
design from a naive "renew whenever a `holder` is present" hook (ADR-046 Rationale §"Why the
identity gate on `expires_at`").

This AC also covers EC-010: when the hook's resolved identity IS byte-equal to `holder` but
`now >= expires_at` (the writer holds the lock but the TTL has elapsed — `SkipReason::AlreadyExpired`,
PC2 case 2, resolved BEFORE identity resolution is even attempted), the hook MUST leave
`expires_at` byte-identical to its pre-write value. No `exec_subprocess` call is made in
this case; no renewal occurs; the expired lock is left inert exactly as `verify-factory-lock`
PC2 (`LockExpired`) treats it, until an explicit `/factory-lock` re-acquire (BC-4.17.001
Invariant 2 / EC-010 / Canonical Test Vector row 4).

**Test (Rust unit):** `test_identity_mismatch_never_renews_expires_at()` and
`test_lock_expired_admitted_non_holder_writer_never_renews()` (the specific
BC-4.13.001-PC2-admission scenario) and `test_expired_self_held_lock_never_renewed()`
(EC-010: identity matches but lock expired — AlreadyExpired, no exec_subprocess invoked)
**Test (bats):** `test_stamp_state_timestamp_foreign_holder_write_never_renews_e2e()`

### AC-007 (traces to BC-4.17.001 postcondition PC2 + PC3 — identity-gate truth-table row 5: identity-resolution failure → fail-closed on renewal, unaffected on timestamp)

When `host::exec_subprocess(["git", "config", "user.email"])` fails, times out, or returns
empty/unparseable output, the hook MUST treat the PC2 gate as failed (no renewal — same
outcome as AC-006) while `timestamp:` re-stamping (PC1) proceeds unaffected, since it never
depends on identity resolution.

**Test (Rust unit):** `test_identity_resolution_failure_skips_renewal_but_timestamp_still_restamped()`

### AC-008 (traces to BC-4.17.001 postcondition PC3 / PC3a — fail-open on structural read/parse errors: zero bytes written for BOTH arms)

When `host::read_file` fails (including `OutputTooLarge` — file exceeds `STATE_MD_MAX_BYTES`
262144 bytes), or the on-disk content has malformed/missing frontmatter delimiters (opening or
closing `---` absent or malformed), or the content is non-UTF-8, the hook MUST write
**nothing** for that invocation (neither `timestamp:` nor `expires_at` changes) and MUST NOT
surface this as a block or otherwise alter the `PostToolUse` result. The agent's own STATE.md
write is never reverted, corrupted, or partially rewritten. These are PC3a fully-structural
fail-open causes — both PC1 and PC2 are suppressed.

When `host::read_file` returns `OutputTooLarge` specifically, the hook MUST additionally call
`host::log_warn` with an advisory message naming the condition (operator visibility; EC-015).
The write-nothing behavior is unchanged for this sub-case.

**Scope note:** a missing `timestamp:` anchor line within **structurally valid** frontmatter
(well-formed delimiters, readable, valid UTF-8) is a PC3a **scoped exception** — NOT a
structural failure covered by this AC. In that case PC1 is suppressed but PC2 is still
evaluated independently. See AC-016 for the correct behavior for that case (EC-013).

**Test (Rust unit):** `test_malformed_frontmatter_writes_nothing()`,
`test_read_file_error_writes_nothing()`, `test_non_utf8_content_writes_nothing()`,
`test_output_too_large_writes_nothing_with_advisory()`

### AC-009 (traces to BC-4.17.001 postcondition PC3 — no accumulated failure state across invocations)

Following a PC3 fail-open invocation (AC-008), the next qualifying write to STATE.md MUST be
treated as an independent attempt — the hook MUST NOT remember or be influenced by the prior
failure. A subsequent well-formed write MUST stamp `timestamp:` normally.

**Test (Rust unit):** `test_failure_then_success_is_independent_per_invocation()`

### AC-010 (traces to BC-4.17.001 postcondition PC4 — idempotent, frontmatter-only rewrite)

The hook's rewrite MUST touch at most two single lines within the YAML frontmatter region
(bounded by the opening and closing `---` delimiters): the `timestamp:` line and, only when
PC2's gate is satisfied, the `factory_lock.expires_at` line. The hook MUST NOT modify, read
for decision purposes, or depend on any content after the closing `---` delimiter (mirrors
BC-4.13.001 Invariant 9 / BC-5.40.001 Invariant 7 frontmatter-only mandate; uses
`factory_lock_parse::extract_frontmatter` to scope parsing). Re-running the hook against its
own just-written output is idempotent in the sense defined by BC-4.17.001 PC4: `timestamp:`
always advances to a new "now" (never a no-op — it is a live wall clock), while PC2's
renewal outcome is deterministic given the same frontmatter + identity inputs.

**Test (Rust unit):** `test_rewrite_touches_only_two_frontmatter_lines()` and
`test_body_content_after_closing_delimiter_never_modified()`

### AC-011 (traces to BC-4.17.001 postcondition PC5 — no acquire/release/CAS-push involvement)

The hook's registered `tool` matcher (`^(Edit|Write|MultiEdit)$`) MUST NOT include `Bash` or
`Agent`. This is a structural, registry-level guarantee that the hook never intercepts
`factory-lock-write.sh acquire`/`clear` (both invoked via the Bash tool, mutating STATE.md
via direct file I/O — `mv "$tmpfile" "$file"` — never via `Edit`/`Write`/`MultiEdit`) or
`state-burst`'s fetch-then-`--force-with-lease` CAS push (a `Bash` git-push invocation).

**Test (bats):** `test_stamp_state_timestamp_registry_tool_matcher_excludes_bash_and_agent()`

### AC-012 (traces to BC-4.17.001 precondition 3 + Invariant 3 — TTL_SECONDS single canonical constant)

The plugin MUST import `TTL_SECONDS` from a single canonical source shared with
`plugins/vsdd-factory/bin/factory-lock-write.sh`'s own `TTL_SECONDS=2700` — a
`pub const TTL_SECONDS: u32 = 2700` added to `crates/factory-lock-parse/src/lib.rs` (already
a dependency of every guard in this family) and re-exported for `stamp-state-timestamp` to
import. Independently declaring a second `2700`-valued literal inside
`stamp-state-timestamp`'s own source is a production blocker. A cross-reference comment MUST
be added on both the new Rust constant and the existing bash literal.

**Test (Rust unit):** `test_ttl_seconds_constant_equals_2700()` and
`test_ttl_seconds_is_imported_not_redeclared()` (source-scan assertion: no second
`2700`-valued `const`/literal declaration for TTL purposes exists in
`stamp-state-timestamp/src/lib.rs`)

### AC-013 (traces to BC-4.17.001 preconditions 1–2 + BC-5.40.001 PC4 — registry: stamper added AND verify-state-timestamp-refresh deregistered in the same commit — DELIVERY-ATOMICITY)

`plugins/vsdd-factory/hooks-registry.toml` MUST, in the same commit:
1. Contain a new `[[hooks]]` entry `name = "stamp-state-timestamp"`, `event =
   "PostToolUse"`, `tool = "^(Edit|Write|MultiEdit)$"`, capability-scoped
   `path_allow = [".factory/STATE.md"]` for both `[hooks.capabilities.read_file]` and
   `[hooks.capabilities.write_file]`, and `[hooks.capabilities.exec_subprocess]` with
   `binary_allow = ["git"]` / `env_allow = ["HOME", "GIT_CONFIG_GLOBAL",
   "XDG_CONFIG_HOME"]` (identical to `verify-factory-lock`'s block — Precondition 2).
2. NOT contain a `verify-state-timestamp-refresh` `[[hooks]]` entry (deregistered).

Both conditions MUST hold in the same commit — never a commit with (1) but not (2), or (2)
but not (1). See **Delivery Constraint** below for the rationale (ADR-046 Decision 3).

**Test (bats):** `test_hooks_registry_stamper_present_and_old_guard_absent_atomically()`

### AC-014 (traces to BC-5.40.001 postcondition PC4 — end-to-end: hook mechanically performs mid-burst renewal)

Through the real WASM dispatcher: an `Edit` to `.factory/STATE.md` while a `factory_lock`
block is held by the same git identity as the caller MUST result, after the write, in
`expires_at` advanced to `write_instant + 2700s` with `locked_at` unchanged — demonstrating
end-to-end that this hook mechanically discharges BC-5.40.001 PC4 on `state-manager`'s
behalf without `state-manager` invoking `factory-lock-write.sh renew` itself.

**Test (bats):** `test_stamp_state_timestamp_mid_burst_renewal_e2e()`

### AC-015 (traces to BC-4.17.001 postcondition PC3b — renewal-indeterminate diagnostic + case-1 malformed advisory)

When `host::exec_subprocess(["git", "config", "user.email"])` fails or returns an unusable
result during PC2's identity-gate evaluation (Case 4 —
`SkipReason::IdentityResolutionFailed { reason, holder, locked_at, expires_at }`), the hook
MUST:

1. Call `host::emit_event("factory.lock.renewal_indeterminate", &[("plugin",
   "stamp-state-timestamp"), ("holder", holder), ("locked_at", locked_at),
   ("expires_at", expires_at), ("resolution_error", reason)])` with the five fields
   destructured directly from the returned `SkipReason` variant — NOT re-parsed from the
   STATE.md `factory_lock:` block (ADR-046 §Decision 1(b)/§Decision 4, F-P21-001), AND
2. Call `host::log_warn` with a human-readable message (e.g., "RenewalIndeterminate:
   factory_lock held by {holder}, but this session's identity could not be confirmed
   ({reason}); expires_at was NOT renewed — if this session is the holder, the lock may
   expire mid-burst").

When the PC2 gate reaches a case-1 `Err(LockError::Malformed(msg))` outcome (a `factory_lock`
block with a malformed or empty/absent `holder` while sibling sub-fields are present), the hook
MUST call `host::log_warn` with an advisory message for operator visibility. The
`factory.lock.renewal_indeterminate` event MUST NOT be emitted for this case.

The `factory.lock.renewal_indeterminate` event MUST NOT be emitted for any of the following
working-as-designed non-renewal paths: `NotHolder` (case 3, Resolved+Mismatch),
`AlreadyExpired` (case 2), a fully-absent or fully-null `factory_lock` block (the 0th case),
or the case-1 malformed path above (ADR-046 Decision 4 non-goal). In all PC3b paths,
`timestamp:` re-stamping (PC1) proceeds unaffected — AC-015 governs only the diagnostic
side-effects on top of the no-renewal outcome already established by AC-007.

**Tests (Rust unit):**
- `test_renewal_indeterminate_emits_event_and_log_warn_on_identity_resolution_failure()` —
  positive: asserts `emit_event("factory.lock.renewal_indeterminate", ...)` is called with
  all five fields and `log_warn` is called, on the identity-resolution-failure fixture
- `test_no_renewal_indeterminate_event_for_not_holder_or_already_expired_or_absent()` —
  negative: asserts no diagnostic event is emitted for `NotHolder`, `AlreadyExpired`, or
  absent-block skip paths
- `test_malformed_lock_block_case1_emits_advisory_log_warn_not_event()` — asserts
  `log_warn` is called (not `emit_event`) for the case-1 malformed-block path

### AC-016 (traces to BC-4.17.001 PC3a scoped exception / EC-013 / Invariant-9 row 3)

When the post-write frontmatter is structurally valid (UTF-8, frontmatter delimiters present,
`host::read_file` succeeded) but contains no `timestamp:` line, this absence MUST NOT cause
the hook to suppress PC2 for that invocation. Specifically:

1. PC1 is suppressed — the hook MUST NOT insert, synthesize, or otherwise write a new
   `timestamp:` line. The `TimestampAnchorMissing` advisory path fires (PC3a scoped exception).
2. PC2 is evaluated independently and in full. If a `factory_lock` block is present with a
   non-empty `holder` that matches the hook's resolved identity and whose `expires_at` has not
   yet elapsed, the hook MUST renew `factory_lock.expires_at = now + TTL_SECONDS` and write
   the modified frontmatter back (Invariant-9 row 3: `new_content` from PC2 is the written
   content since PC1 contributed no change). No `timestamp:` line is added as a side-effect.
3. If PC2 is not satisfied (no lock block, empty holder, mismatched identity, expired lock, or
   identity-resolution failure), no write occurs — neither arm produced a change.

This is a PC1-only suppression path, not a PC3a fully-structural fail-open (which suppresses
BOTH arms). These two cases MUST NOT be conflated (BC-4.17.001 Invariant 4 / Invariant 7
fence-not-located correction).

**Test (Rust unit):** `test_no_timestamp_anchor_with_matching_lock_renews_expires_at()` —
valid frontmatter with no `timestamp:` line; identity matches; `expires_at` unexpired; asserts
`expires_at` is written to the renewed value; asserts no `timestamp:` line is inserted.

### AC-017 (traces to BC-4.17.001 Precondition 1 / F-013 — tool-write-success gate)

When the `PostToolUse` payload's `tool_response` field is `null`/absent **OR** is a JSON
object carrying a top-level `"error"` key with a non-null value, the hook MUST return
`HookResult::Continue` without reading or writing `.factory/STATE.md` — both PC1 and PC2
arms are skipped entirely. This is a distinct, earlier-evaluated gate from PC3a/PC3b; it is
not itself an error variant of either arm. **Accepted residual (explicit, not deferred per
BC-4.17.001 Precondition 1):** a tool failure whose `tool_response` carries no `"error"` key
is treated as success (spurious-but-harmless re-stamp bounded by the next real edit).

**Test (Rust unit):** `test_failed_tool_write_skips_both_arms()` — asserts that when
`tool_response` carries a top-level `"error"` key, neither `host::read_file` nor
`host::write_file` is called and `HookResult::Continue` is returned.

### AC-018 (traces to BC-4.17.001 Invariant 8 — `state_md_approaching_cap` soft-warn)

When `host::read_file` succeeds and the returned content byte-length is in the range
`(200000, 262144]` (strictly greater than 200000 and at most 262144), the hook MUST call
`host::emit_event("state_md_approaching_cap", &[("bytes_read", <actual>), ("cap_bytes",
"262144")])` as a diagnostic side-effect. This event emission MUST NOT suppress PC1 or PC2 —
stamping and renewal proceed normally regardless of file size within the cap. The threshold is
inclusive at the cap boundary (262144 bytes → warn fires; at-or-below 200000 → no warn). The
`emit_event` callback MUST support being invoked in the same hook call as the
`factory.lock.renewal_indeterminate` event from AC-015 (no single-invocation constraint).

**Tests (Rust unit):**
- `test_approaching_cap_emits_soft_warn_event()` — `bytes_read = 250000`; asserts
  `state_md_approaching_cap` event emitted with `bytes_read = "250000"` and
  `cap_bytes = "262144"`; asserts PC1 and PC2 proceed normally
- `test_at_cap_boundary_emits_soft_warn()` — `bytes_read = 262144`; asserts event emitted
  (warn fires AT the cap boundary per BC-4.17.001 Invariant 8 table)
- `test_below_threshold_no_soft_warn()` — `bytes_read = 200000`; asserts no
  `state_md_approaching_cap` event is emitted (threshold is strictly-greater-than 200000)

### AC-019 (traces to BC-4.17.001 postcondition PC1 / EC-014 — duplicate `timestamp:` key advisory)

When the frontmatter region contains two or more lines beginning with `timestamp:` (a
pre-existing malformed-YAML/duplicate-key condition this hook does not create), the hook MUST
rewrite ONLY the first-matched `timestamp:` line (unconditional re-stamp per PC1 first-match
semantics), leave subsequent duplicate occurrences byte-identical to their pre-write values,
and call `host::log_warn` with an advisory message referencing `DuplicateTimestampKey` (e.g.,
`"DuplicateTimestampKey: STATE.md frontmatter contains more than one timestamp: line; only the
first was rewritten; the stale second line is a pre-existing corruption this hook does not
repair"`). This is an advisory-only path — no block, no `PostToolUse` result alteration; PC2
evaluation is unaffected (PC2 does not read `timestamp:` at all).

**Test (Rust unit):** `test_duplicate_timestamp_rewrites_first_emits_advisory()` — frontmatter
with two `timestamp:` lines; asserts only the first is rewritten to `now`; asserts the second
is unchanged; asserts `host::log_warn` is called with a message containing
`"DuplicateTimestampKey"`.

## Delivery Constraint (ADR-046 Decision 3 — single-release atomicity)

The new `stamp-state-timestamp` registry entry going live and the
`verify-state-timestamp-refresh` registry entry being deregistered MUST ship in the same
release (never split across two rc cuts). ADR-046's Rollout/Migration Note is explicit: a
window where the old guard is removed but the new stamper is not yet deployed would silently
reintroduce agent-authored, error-prone wall-clock fields with **no detector at all**; a
window where the stamper is live but the old guard still blocks on staleness it no longer
needs to block on is merely redundant (not unsafe) but wastes a registry priority slot and a
dispatcher PreToolUse cycle on every STATE.md write for no purpose. AC-013 is the mechanical
enforcement point for this constraint (both registry conditions verified in one bats
assertion against one `hooks-registry.toml` state). This is a same-PR requirement, not
merely a same-epic one — the implementer MUST NOT split T-4/T-5 below (registry add + old
entry removal) into separate PRs.

## Out of Scope (explicit — do not implement in this story)

- **Deleting the `verify-state-timestamp-refresh` crate source or its Rust unit test module**
  (`crates/hook-plugins/verify-state-timestamp-refresh/src/lib.rs`). Per ADR-046 Decision
  point 2, the crate source and its pre-existing Rust unit test suite are LEFT IN PLACE as
  historical/reference material at this release — they are pure function tests of
  `guard_logic()` via injectable callbacks and do not depend on registry state, so they
  continue to compile and pass unmodified after deregistration. Actual crate deletion is an
  explicit, human-approved future deferral (ADR-046: "pending a follow-up cleanup story that
  deletes the crate outright once the human has confirmed the new mechanism is stable in
  production") — this satisfies the CANONICAL PRINCIPLE Rule 3 deferral bar (human-directed
  in the ratified ADR; concrete future dependency = production-stability observation window;
  attach to a specific future story once state-manager allocates its ID — flagged in the
  Report below, not invented here).
- **`plugins/vsdd-factory/tests/verify-state-timestamp-refresh.bats` is NOT left in place.**
  Unlike the crate's unit tests, this bats suite drives the real dispatcher through the
  now-deregistered registry entry — once deregistered, its "expect exit 2 Block" assertions
  would start failing as false regressions (the guard structurally cannot fire; the
  dispatcher has no entry to invoke). This file MUST be retired (deleted) in this story's
  scope (T-6 below) — retiring it is required for CI to stay green, not merely tidy.
- **`verify-state-timestamp-advisory`** (`crates/hook-plugins/verify-state-timestamp-advisory/`,
  registry priority 159, `event = "PostToolUse"`, `tool = "^Bash$"`, BC-5.41.003) is a
  wholly separate, already-shipped advisory mechanism (git-commit-diff-scoped, exec-free,
  never writes) unrelated to ADR-046. It is NOT touched by this story.
- **ADR-045 / E-23 / S-23.\* / ARCH-INDEX / STATE.md / `.factory/policies.yaml`** — strictly
  out of scope per this story's isolation mandate.

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|-----------------|
| `stamp-state-timestamp` guard logic (PC1–PC5) | `crates/hook-plugins/stamp-state-timestamp/src/lib.rs` | Effectful (reads + writes `.factory/STATE.md` via `host::read_file`/`host::write_file`; resolves identity via `host::exec_subprocess`) — pure `guard_logic(payload, callbacks) -> RewriteOutcome`-style core with injectable I/O, mirroring `verify-factory-lock`'s testable-without-WASM-runtime pattern |
| WASI entry point | `crates/hook-plugins/stamp-state-timestamp/src/main.rs` | Effectful (wires `on_post_tool_use` to the WASM ABI, mirroring `regression-gate/src/main.rs`) |
| `TTL_SECONDS` canonical constant | `crates/factory-lock-parse/src/lib.rs` | Pure (compile-time constant; no I/O) |
| `factory-lock-write.sh` cross-reference comment | `plugins/vsdd-factory/bin/factory-lock-write.sh` | Documentation (one-line comment addition near its own `TTL_SECONDS=2700`; no behavior change) |
| Registry entry (new) | `plugins/vsdd-factory/hooks-registry.toml` | Config (declarative `[[hooks]]` addition) |
| Registry entry (removed) | `plugins/vsdd-factory/hooks-registry.toml` | Config (`verify-state-timestamp-refresh` entry deleted) |
| Rust unit tests (new) | `crates/hook-plugins/stamp-state-timestamp/src/lib.rs` (`#[cfg(test)] mod tests`) | Pure (injectable-callback tests; no I/O) |
| Bats integration tests (new) | `plugins/vsdd-factory/tests/stamp-state-timestamp.bats` | Effectful (real WASM dispatcher; file fixtures) |
| Bats integration tests (retired) | `plugins/vsdd-factory/tests/verify-state-timestamp-refresh.bats` | Deleted (see Out of Scope) |

**Subsystem anchor justifications (Anchor Justification Requirement):**

- **SS-04 (Plugin Ecosystem) owns this story's primary scope** because ARCH-INDEX Subsystem
  Registry anchors all `crates/hook-plugins/*` WASM crates to SS-04, and this story's core
  deliverable is a new `[[bin]]`-bearing crate at
  `crates/hook-plugins/stamp-state-timestamp/`. The `crates/factory-lock-parse/`
  constant addition is also SS-04-owned by the same reasoning as its own creation in S-17.04
  (a lib-only crate serving the plugin ecosystem's shared parsing needs, deliberately placed
  outside `hook-plugins/` to avoid inflating the CI WASM floor-count gate — directory
  placement does not change subsystem ownership).
- **SS-07 (Hook Bash Layer) owns the `hooks-registry.toml` add/remove pair** because
  ARCH-INDEX explicitly anchors `plugins/vsdd-factory/hooks-registry.toml` to SS-07 (registry
  bindings, exit code semantics). This story's registry mutation (new entry + deregistration)
  is SS-07 scope regardless of which subsystem owns the plugin binary the entry points at —
  the same split ownership pattern (SS-04 crate + SS-07 registry entry) is already
  established by `verify-factory-lock`/`verify-state-timestamp-refresh`'s own registry rows.
- **SS-05 (Pipeline Orchestration) is deliberately NOT anchored.** Unlike S-17.04 (which
  amended `state-burst/SKILL.md` and `state-manager.md`, both SS-05-owned files), this story
  creates no new obligation text for `state-manager` to follow — the entire point of ADR-046
  is that `state-manager` no longer needs a documentary renewal instruction at all (the hook
  replaces the SKILL-step obligation mechanically). No SS-05-owned file is created or
  modified by this story; BC-5.40.001 (an SS-05-numbered BC) is cited for traceability only,
  which does not itself require an SS-05 file-ownership anchor.

## Red Gate Test Table

The following tests MUST be written in the stub phase and MUST fail before any
implementation exists (Red Gate, BC-8.30.001 strict `tdd_mode`):

| Test name | File | AC | BC clause | Red Gate condition |
|-----------|------|----|-----------|--------------------|
| `test_timestamp_always_restamped_no_lock_present` | `stamp-state-timestamp/src/lib.rs` | AC-001 | BC-4.17.001 PC1 | stub returns input unchanged → timestamp-advanced assertion fails |
| `test_timestamp_restamped_when_lock_held_regardless_of_identity_match` | `stamp-state-timestamp/src/lib.rs` | AC-002 | BC-4.17.001 PC1 / Invariant 1 | stub gates timestamp on identity → fails for mismatch case |
| `test_identity_match_renews_expires_at` | `stamp-state-timestamp/src/lib.rs` | AC-003 | BC-4.17.001 PC2 | stub never renews → `expires_at` unchanged assertion fails |
| `test_no_lock_block_skips_renewal_entirely` | `stamp-state-timestamp/src/lib.rs` | AC-004 | BC-4.17.001 PC2 | stub creates a `factory_lock` block unconditionally → no-block-created assertion fails |
| `test_empty_holder_skips_renewal` | `stamp-state-timestamp/src/lib.rs` | AC-005 | BC-4.17.001 PC2 | stub treats empty `holder` as present-and-non-empty → renews incorrectly → assertion fails |
| `test_identity_mismatch_never_renews_expires_at` | `stamp-state-timestamp/src/lib.rs` | AC-006 | BC-4.17.001 PC2 / Invariant 2 | stub renews unconditionally when `holder` present → foreign-lock-resurrection assertion fails |
| `test_lock_expired_admitted_non_holder_writer_never_renews` | `stamp-state-timestamp/src/lib.rs` | AC-006 | BC-4.17.001 PC2 / Invariant 2 | same stub → fails on the specific BC-4.13.001-PC2-admission scenario |
| `test_identity_resolution_failure_skips_renewal_but_timestamp_still_restamped` | `stamp-state-timestamp/src/lib.rs` | AC-007 | BC-4.17.001 PC2 / PC3 | stub either blocks timestamp or renews on identity error → both assertions fail |
| `test_malformed_frontmatter_writes_nothing` | `stamp-state-timestamp/src/lib.rs` | AC-008 | BC-4.17.001 PC3 | stub panics or writes partial content on malformed input → zero-bytes-written assertion fails |
| `test_read_file_error_writes_nothing` | `stamp-state-timestamp/src/lib.rs` | AC-008 | BC-4.17.001 PC3 | stub does not handle `HostError` → fails |
| `test_non_utf8_content_writes_nothing` | `stamp-state-timestamp/src/lib.rs` | AC-008 | BC-4.17.001 PC3 | stub assumes UTF-8 → panics or corrupts → fails |
| ~~`test_missing_timestamp_anchor_writes_nothing`~~ | ~~`stamp-state-timestamp/src/lib.rs`~~ | ~~AC-008~~ | ~~BC-4.17.001 PC3~~ | **RETIRED v1.5** — this test asserted "write nothing on missing anchor" which contradicts BC-4.17.001 PC3a scoped exception / EC-013: missing anchor suppresses PC1 only; PC2 still evaluates. Replaced by AC-016 + `test_no_timestamp_anchor_with_matching_lock_renews_expires_at`. Not a mandated Red Gate test. |
| `test_failure_then_success_is_independent_per_invocation` | `stamp-state-timestamp/src/lib.rs` | AC-009 | BC-4.17.001 PC3 | stub carries state across calls (e.g., a "poisoned" flag) → second invocation fails to stamp → assertion fails |
| `test_rewrite_touches_only_two_frontmatter_lines` | `stamp-state-timestamp/src/lib.rs` | AC-010 | BC-4.17.001 PC4 | stub performs a full-file rewrite → diff assertion (only 2 lines changed) fails |
| `test_body_content_after_closing_delimiter_never_modified` | `stamp-state-timestamp/src/lib.rs` | AC-010 | BC-4.17.001 PC4 / Invariant 5 | stub scans/touches body content → assertion fails |
| `test_ttl_seconds_constant_equals_2700` | `stamp-state-timestamp/src/lib.rs` | AC-012 | BC-4.17.001 Precondition 3 / Invariant 3 | stub hardcodes a different or duplicated literal → `assert_eq!(TTL_SECONDS, 2700)` fails |
| `test_ttl_seconds_is_imported_not_redeclared` | `stamp-state-timestamp/src/lib.rs` | AC-012 | BC-4.17.001 Precondition 3 / Invariant 3 | source-scan test: grep `stamp-state-timestamp/src/lib.rs` for a second `2700`-valued const/literal — fails if one exists |
| `test_ttl_seconds_constant_equals_2700` (factory-lock-parse) | `factory-lock-parse/src/lib.rs` | AC-012 | BC-4.17.001 Precondition 3 | stub omits the constant entirely → compile failure counts as Red |
| `test_state_md_max_bytes_constant_equals_262144` | `factory-lock-parse/src/lib.rs` | AC-012 | BC-4.17.001 Precondition 3 / BC-5.40.001 cap parity | stub omits the constant → compile failure counts as Red; or stub with wrong value → `assert_eq!(STATE_MD_MAX_BYTES, 262144)` fails |
| `test_renew_lock_with_now_uses_ttl_seconds_not_bare_literal` | `factory-lock/src/lib.rs` | T-2 literal migration | BC-4.17.001 Invariant 3 / ADR-046 F-006 | source-scan test: grep `crates/factory-lock/src/lib.rs` for the literal `2700` inside the `renew_lock_with_now` function body and assert no match — fails before migration (literal still present) and passes after (replaced by `TTL_SECONDS` reference). Mirrors the `test_ttl_seconds_is_imported_not_redeclared` source-scan pattern. |
| `test_stamp_state_timestamp_foreign_holder_write_never_renews_e2e` | `stamp-state-timestamp.bats` | AC-006 | BC-4.17.001 PC2 / Invariant 2 | WASM absent → non-zero exit; stub renewing unconditionally → `expires_at` changed assertion fails |
| `test_stamp_state_timestamp_registry_tool_matcher_excludes_bash_and_agent` | `stamp-state-timestamp.bats` | AC-011 | BC-4.17.001 PC5 | registry entry absent → grep assertion fails; entry present with `Bash`/`Agent` in matcher → assertion fails |
| `test_hooks_registry_stamper_present_and_old_guard_absent_atomically` | `stamp-state-timestamp.bats` | AC-013 | BC-4.17.001 Preconditions 1–2 / ADR-046 Decision 3 | either condition unmet (stamper entry missing OR old entry still present) → assertion fails |
| `test_stamp_state_timestamp_mid_burst_renewal_e2e` | `stamp-state-timestamp.bats` | AC-014 | BC-5.40.001 PC4 | WASM absent → non-zero exit; stub not renewing → `expires_at` unchanged assertion fails. **MANDATORY real-WASM-runtime test** — mirrors the S-17.04 AC-018 lesson: native-env unit tests do not validate the deployed WASM trigger/registry-wiring path. |
| `test_renewal_indeterminate_emits_event_and_log_warn_on_identity_resolution_failure` | `stamp-state-timestamp/src/lib.rs` | AC-015 | BC-4.17.001 PC3b | stub neither calls `emit_event` nor `log_warn` on identity-resolution failure → both assertions fail |
| `test_no_renewal_indeterminate_event_for_not_holder_or_already_expired_or_absent` | `stamp-state-timestamp/src/lib.rs` | AC-015 | BC-4.17.001 PC3b (non-goal) | stub emits event for every non-renewal path → no-event assertion fails |
| `test_malformed_lock_block_case1_emits_advisory_log_warn_not_event` | `stamp-state-timestamp/src/lib.rs` | AC-015 | BC-4.17.001 PC2 case-1 / PC3b | stub does not call `log_warn` for case-1 malformed block → advisory-warn assertion fails |
| `test_expired_self_held_lock_never_renewed` | `stamp-state-timestamp/src/lib.rs` | AC-006 | BC-4.17.001 PC2 / Invariant 2 / EC-010 (AlreadyExpired) | stub renews when identity matches regardless of expiry gate → `expires_at`-unchanged assertion fails; also asserts no `exec_subprocess` call made (PC2 case 2 resolves before identity resolution) |
| `test_no_timestamp_anchor_with_matching_lock_renews_expires_at` | `stamp-state-timestamp/src/lib.rs` | AC-016 | BC-4.17.001 PC3a scoped exception / EC-013 / Invariant-9 row 3 | stub writes nothing on missing `timestamp:` anchor → `expires_at`-renewed assertion fails; also asserts no `timestamp:` line is inserted |
| `test_failed_tool_write_skips_both_arms` | `stamp-state-timestamp/src/lib.rs` | AC-017 | BC-4.17.001 Precondition 1 / F-013 | stub does not check `tool_response` for error key → reads/writes STATE.md even on failed tool call → no-read-no-write assertion fails |
| `test_approaching_cap_emits_soft_warn_event` | `stamp-state-timestamp/src/lib.rs` | AC-018 | BC-4.17.001 Invariant 8 | stub does not emit `state_md_approaching_cap` event → event-emitted assertion fails (`bytes_read = 250000`) |
| `test_at_cap_boundary_emits_soft_warn` | `stamp-state-timestamp/src/lib.rs` | AC-018 | BC-4.17.001 Invariant 8 | stub does not emit event at cap boundary → assertion fails (`bytes_read = 262144`, inclusive boundary per Invariant 8 table) |
| `test_below_threshold_no_soft_warn` | `stamp-state-timestamp/src/lib.rs` | AC-018 | BC-4.17.001 Invariant 8 (negative) | stub emits event for all file sizes → no-event assertion fails (`bytes_read = 200000`, at threshold, not above it) |
| `test_duplicate_timestamp_rewrites_first_emits_advisory` | `stamp-state-timestamp/src/lib.rs` | AC-019 | BC-4.17.001 PC1 / EC-014 | stub either rewrites both `timestamp:` lines or does not emit `DuplicateTimestampKey` advisory → first-only-rewritten + warn-emitted assertion fails |
| `test_output_too_large_writes_nothing_with_advisory` | `stamp-state-timestamp/src/lib.rs` | AC-008 | BC-4.17.001 PC3 / EC-015 | stub treats `OutputTooLarge` as a normal success read → writes content when it should write nothing + fails to emit `log_warn` → zero-bytes-written assertion fails |
| `test_crlf_frontmatter_delimiters_handled_correctly` | `stamp-state-timestamp/src/lib.rs` | AC-010 | BC-4.17.001 PC4 / Invariant 5 | stub uses `\n`-only scanning and fails to locate `---` delimiters in CRLF (`\r\n`) content → `extract_frontmatter` boundary detection fails → timestamp not rewritten → assertion fails (GAP 2 coverage) |

**Red Gate minimum:** 35 Rust unit + 4 bats = 39 mandated failing tests before any
implementation exists. All 39 MUST be submitted via PR in the stub phase with CI red.
(Tally: 32 Rust unit in `stamp-state-timestamp/src/lib.rs` [30 guard_logic-calling + 2
constant (`test_ttl_seconds_constant_equals_2700` +
`test_ttl_seconds_is_imported_not_redeclared`); composition verified by `grep -c 'fn test_'`
= 32 ✓ as of D-1127 sweep] + 2 in `factory-lock-parse/src/lib.rs`
[`test_ttl_seconds_constant_equals_2700` + `test_state_md_max_bytes_constant_equals_262144`]
+ 1 in `factory-lock/src/lib.rs` [`test_renew_lock_with_now_uses_ttl_seconds_not_bare_literal`]
= 35 Rust unit total across three crates. `test_missing_timestamp_anchor_writes_nothing`
retired in v1.5 — asserted wrong behavior; see AC-016 +
`test_no_timestamp_anchor_with_matching_lock_renews_expires_at`.)

**Payload/field discipline (carried forward from S-17.04 lesson L-1704):** this hook is
`PostToolUse`, so its payload additionally carries the tool's already-completed write — the
hook reads the **on-disk, post-write** file via `host::read_file`, not a proposed/pre-write
payload fragment. There is no `old_string`/`new_string`/`edits[]` reconstruction step (unlike
the retired PreToolUse `verify-state-timestamp-refresh`) — the write has already landed by
the time this hook fires. Do not port the Edit/MultiEdit reconstruction logic from the
retired guard; it does not apply here.

**Block-message assertions:** N/A for this hook — per BC-4.17.001 Invariant 4, this hook has
no `block_intent` capability in its intended design; `HookResult::Continue` is the only
outcome it produces (PostToolUse, best-effort write). Tests assert *file-content* outcomes
(timestamp/expires_at values pre- vs post-invocation), not `HookResult` block variants.

## Tasks

- [ ] **T-1 (test-writer):** Write `crates/hook-plugins/stamp-state-timestamp/src/lib.rs`
  with all 32 Rust unit Red Gate tests (30 guard_logic-calling spanning AC-001 through
  AC-019, plus 2 constant: `test_ttl_seconds_constant_equals_2700` +
  `test_ttl_seconds_is_imported_not_redeclared`; using the injectable-callback pattern
  from `verify-factory-lock/src/lib.rs`: `guard_logic(payload, callbacks) ->
  RewriteOutcome` where `callbacks` provides `read_file`, `write_file`, and
  `exec_subprocess`, and tests inject mock implementations to assert what the hook *would*
  write without touching a real filesystem). Also add the `TTL_SECONDS` constant-equality
  test and `STATE_MD_MAX_BYTES` constant-equality test to
  `crates/factory-lock-parse/src/lib.rs` (2 tests total in that crate), and the
  source-scan test `test_renew_lock_with_now_uses_ttl_seconds_not_bare_literal` to
  `crates/factory-lock/src/lib.rs` (1 test). All tests MUST fail initially (stub
  returns the input frontmatter unchanged / never renews / never resolves identity).
  Then write `plugins/vsdd-factory/tests/stamp-state-timestamp.bats` with 4 bats
  integration tests (AC-006 foreign-holder e2e, AC-011 registry tool-matcher shape, AC-013
  registry atomicity, AC-014 mid-burst renewal e2e). Submit Red Gate PR; CI MUST show
  failures for all 39 (32 stamp-state-timestamp + 2 factory-lock-parse + 1 factory-lock
  + 4 bats).

- [ ] **T-2 (implementer):** Add `pub const TTL_SECONDS: u32 = 2700;` and
  `pub const STATE_MD_MAX_BYTES: u32 = 262144;` to `crates/factory-lock-parse/src/lib.rs`
  with doc comments. `TTL_SECONDS` cross-references `plugins/vsdd-factory/bin/factory-lock-write.sh`'s
  own `TTL_SECONDS=2700` literal (Precondition 3 / Invariant 3 / AC-012). `STATE_MD_MAX_BYTES`
  is the canonical read-cap shared with `stamp-state-timestamp`'s `host::read_file` call
  (F-P5-001/Decision 5 — single canonical home in `factory-lock-parse`). Add a matching
  one-line comment above `factory-lock-write.sh`'s `TTL_SECONDS=2700` (locate by function
  context, not line number per TD-VSDD-091) cross-referencing the new Rust constant's exact
  path. Do not change `factory-lock-write.sh`'s runtime behavior — comment-only edit on the
  bash side. Add Red Gate test `test_state_md_max_bytes_constant_equals_262144()` in
  `crates/factory-lock-parse/src/lib.rs` asserting `STATE_MD_MAX_BYTES == 262144`.

  **Post-S-17.06-merge literal migration (boundary correction, D-1126):** After the
  `TTL_SECONDS` constant is created in `factory-lock-parse` (above), migrate the bare
  `Duration::seconds(2700)` literal in `renew_lock_with_now` within
  `crates/factory-lock/src/lib.rs` to
  `Duration::seconds(i64::from(factory_lock_parse::TTL_SECONDS))`. This is a
  behavior-neutral refactor (`2700 == TTL_SECONDS`) fulfilling ADR-046 F-006's
  single-canonical-TTL-source intent. S-17.06 is already merged (`3200149d`) before
  this story's worktree is cut from develop, so `renew_lock_with_now` and the shared
  fns (`renew_lock_if_holder`, `classify_identity_resolution`, `trim_git_email`) are
  all present; no merge conflict is expected. Locate `renew_lock_with_now` by function
  name (not line number, per TD-VSDD-091). Do NOT touch any other line in
  `crates/factory-lock/src/lib.rs` — doc-comment corrections on that file (T-8) remain
  S-17.06 scope and are already shipped. `factory-lock` must already depend on
  `factory-lock-parse` (as a workspace dep) for this import to compile; verify
  `factory-lock/Cargo.toml` has `factory-lock-parse = { workspace = true }` before
  landing the migration.

- [ ] **T-3 (implementer):** Create `crates/hook-plugins/stamp-state-timestamp/` with the
  same crate structure as `regression-gate` (the closest existing PostToolUse
  read+write-file precedent): `Cargo.toml`, `src/lib.rs` (pure `guard_logic` core +
  `#[cfg(test)] mod tests`), `src/main.rs` (`on_post_tool_use` WASI entry point). Import
  `factory_lock_parse::{extract_frontmatter, parse_factory_lock, TTL_SECONDS, STATE_MD_MAX_BYTES}`
  (Precondition 3 / Invariant 3 / AC-012 — both constants from `factory-lock-parse`). In
  `src/lib.rs` the `guard_logic` injectable-callback signature remains unchanged (accepts
  `read_file`, `write_file`, and `exec_subprocess` callbacks — mirrors `verify-factory-lock`'s
  pure-core/effectful-shell split, enabling all 32 Rust unit tests (30 guard_logic-calling
  + 2 constant, per the Red Gate table) to run without a WASM runtime; all test mocks are
  unchanged because mock `exec_subprocess` result flows through
  `classify_identity_resolution` → `renew_lock_if_holder` transparently). In
  `src/main.rs` wire the lazy `resolve_identity` closure as
  `|| factory_lock::classify_identity_resolution(host::exec_subprocess(["git", "config", "user.email"]))`
  and pass it to `factory_lock::renew_lock_if_holder(content_after_pc1, resolve_identity, now_fn)`
  (function signatures created by S-17.06; this story depends on S-17.06). Read on-disk
  STATE.md (full content, `max_bytes = factory_lock_parse::STATE_MD_MAX_BYTES` per
  BC-5.40.001/BC-4.13.001 cap parity), locate the `timestamp:` and `factory_lock.expires_at`
  lines within `extract_frontmatter`'s returned slice only, perform the two targeted
  single-line replacements (unconditional for `timestamp:`; gated per PC2 for `expires_at`
  via `renew_lock_if_holder`) against the **full** file content (frontmatter substitution +
  unchanged body), and write the reconstructed full content back via `host::write_file`. Never
  touch content after the closing `---` delimiter. Fail-open (no write) on any
  read/parse/UTF-8 error per PC3/AC-008/AC-009.

- [ ] **T-4 (implementer):** Add the `stamp-state-timestamp` `[[hooks]]` entry to
  `plugins/vsdd-factory/hooks-registry.toml`. See "Registry Entry Spec" below for the
  canonical TOML. Verify the current highest `PostToolUse`-tier `priority` value in the live
  registry at implementation time (470 was the next free slot as of this story's authoring —
  reverify before hard-coding, per the registry's own append-only priority-slot convention).

- [ ] **T-5 (implementer — MUST land in the same commit as T-4, per Delivery Constraint /
  AC-013):** Remove the `verify-state-timestamp-refresh` `[[hooks]]` entry from
  `hooks-registry.toml` (currently `priority = 143`). Do NOT delete the crate source at
  `crates/hook-plugins/verify-state-timestamp-refresh/` (see Out of Scope).

- [ ] **T-6 (implementer):** Delete
  `plugins/vsdd-factory/tests/verify-state-timestamp-refresh.bats`. Its assertions (expect
  `Block`/exit 2 on stale-timestamp writes) can never pass again once T-5 deregisters the
  guard — retiring it is required for CI correctness, not cosmetic (see Out of Scope for why
  the Rust unit test module in the same crate is NOT deleted).

- [ ] **T-7 (test-writer + implementer):** Verify all 39 Red Gate tests pass (35 Rust
  unit across three crates + 4 bats) after T-2/T-3/T-4/T-5/T-6 are complete. Run `cd plugins/vsdd-factory/tests && ./run-all.sh` — all
  remaining bats tests MUST continue to pass (regression gate; confirms
  `verify-state-timestamp-refresh.bats` is gone and no other suite referenced it). Run
  `cargo test --workspace` — all Rust tests MUST pass, including the pre-existing
  `verify-state-timestamp-refresh` unit tests (left in place, unmodified, still green because
  they call `guard_logic()` directly and never touch the registry). Verify
  `stamp-state-timestamp.wasm` is produced by `cargo build --target wasm32-wasip1` and that
  `hooks-registry.toml` parses without error. Confirm via `grep -c
  'name = "verify-state-timestamp-refresh"' plugins/vsdd-factory/hooks-registry.toml` outputs
  `0` and `grep -c 'name = "stamp-state-timestamp"'` outputs `1`, in the same commit (T-4/T-5
  atomicity check, AC-013).

**NOTE — T-8 relocated (ADR-046 Wave-5 decomposition, D-1124); TTL literal migration
NOT part of T-8 (D-1126 boundary correction):** T-8 (correct stale `factory-lock` crate
doc-comments: `renew_lock_with_now` algorithm doc, `Ok(None)`-arm inline comment,
`parse_lock` doc) has been relocated to **S-17.06**, which merged at `3200149d` and
shipped those doc-comment corrections. **This story DOES modify `crates/factory-lock/src/lib.rs`
in one narrow way** — the TTL literal migration sub-task in T-2 above migrates
`renew_lock_with_now`'s `Duration::seconds(2700)` to
`Duration::seconds(i64::from(factory_lock_parse::TTL_SECONDS))`. That single-line change
is distinct from T-8's doc-comment edits and was correctly deferred by S-17.06 (ADR-046
F-006; S-17.06 could not reference `TTL_SECONDS` that only S-17.05 creates). All other
`crates/factory-lock/src/lib.rs` edits remain out of scope for this story.

## Previous Story Intelligence

- **S-17.01 (merged c64b46d2):** Delivered `factory-lock-write.sh` (`acquire`/`renew`/`clear`).
  Its `TTL_SECONDS=2700` literal (line 66) is the bash-side half of the canonical-constant
  pair this story establishes (T-2/AC-012). `_epoch_to_iso` is the RFC-3339 format helper
  this hook's `timestamp:` stamping must match byte-for-byte in shape (`YYYY-MM-DDTHH:MM:SSZ`).
- **S-17.02 (merged df4f26b8):** Delivered `verify-factory-lock`, the proven source of the
  `git config user.email` / `host::exec_subprocess` identity-resolution pattern
  (Precondition 2 of BC-4.17.001 cites this exact precedent). Its `read_prefix` capability
  (distinct from `read_file`) is a related but different capability name in this registry's
  vocabulary — this story uses `read_file` + `write_file` (full-content read/write), not
  `read_prefix` (verify-factory-lock's specific prefix-only read).
- **S-17.04 (merged 3b2a378c):** Delivered `verify-state-timestamp-refresh` (the guard this
  story retires) and extracted `crates/factory-lock-parse/` as the shared parsing crate
  (`extract_frontmatter`, `parse_factory_lock`, `LockState`, `extract_yaml_string_value`,
  `parse_iso8601` — all already available for this story to import unmodified). Its
  hard-won lesson (adversary deep-pass 7, v1.6): Claude Code emits **absolute** `file_path`
  values in production, not relative ones, and the WASI sandbox has **no environment
  variables** (`$CLAUDE_PROJECT_DIR`-strip approaches are dead code in the WASM runtime).
  This story's hook is capability-scoped by `path_allow` (dispatcher-side matching, not
  plugin-side string comparison), so the absolute-vs-relative path-matching bug class that
  affected S-17.04's own trigger logic does **not** apply here in the same way — but the
  T-1/T-7 mandatory-bats-e2e discipline (never trust a native-env unit test to validate the
  deployed WASM trigger/registry-wiring path) is carried forward directly into AC-014's bats
  test. Its local `STATE_MD_MAX_BYTES = 262144` established the read-cap pattern. Per
  ADR-046 Decision 5 / F-P5-001, `STATE_MD_MAX_BYTES` is now also promoted to a canonical
  shared constant in `crates/factory-lock-parse/src/lib.rs` (alongside `TTL_SECONDS`) — same
  single-canonical-home principle. S-17.05 T-2 adds both constants to `factory-lock-parse`;
  `stamp-state-timestamp` imports `factory_lock_parse::STATE_MD_MAX_BYTES` for its
  `max_bytes` argument (no per-crate re-declaration of 262144).
- **ADR-046 (ratified 2026-08-25):** Establishes that only a `PostToolUse` plugin can fix
  (not merely detect) agent-authored staleness, because `HookResult` has no
  "continue-with-modified-payload" variant. This story is that plugin. ADR-046's own
  Rationale section ("Why takeover safety is preserved") already proves no new CAS/race
  handling is needed — `verify-factory-lock` blocks all other sessions' mutating calls while
  a foreign unexpired lock is held, so this hook's local re-read-then-rewrite has no
  cross-process contender in its window. Do not add new locking/CAS logic to this crate.

## Architecture Compliance Rules

Extracted from ADR-046 and BC-4.17.001; enforced by both implementer and adversarial review:

1. **`event = "PostToolUse"`, `tool = "^(Edit|Write|MultiEdit)$"` — no `Bash`, no `Agent`
   (BC-4.17.001 PC5 / AC-011):** This is the structural guarantee that the hook never
   observes `factory-lock-write.sh acquire`/`clear` (Bash-invoked) or the CAS push
   (Bash-invoked).
2. **`async = false` is a correctness requirement (ADR-019 / ADR-025 precedent):** Only
   sync-group plugins participate in the dispatcher's result aggregation at the correct
   phase; this hook has no `block_intent` need (PC3/Invariant 4: it never blocks), but
   `async = false` MUST still be explicit for consistent ordering semantics with sibling
   entries.
3. **`on_error = "continue"` (BC-4.17.001 PC3 / Invariant 4):** Fail-open on crash — this
   hook has no `block_intent` capability by design; a crash MUST NOT be surfaced as a block
   of the agent's own already-completed write.
4. **`[hooks.capabilities.read_file]` AND `[hooks.capabilities.write_file]` are both
   mandatory, both `path_allow = [".factory/STATE.md"]` and nothing else** (BC-4.17.001
   Precondition 1). Omitting either capability block causes `CapabilityDenied` on the
   corresponding host call → silent inertness on that half of the hook (timestamp stamps
   but lock renewal silently never fires, or vice versa, depending on which capability is
   missing) — this is the sixth instance of the deny-by-default silent-no-op footgun class
   documented across this hook family (ADR-025/BC-4.13.001/BC-5.40.001 precedent).
5. **`[hooks.capabilities.exec_subprocess]` with `binary_allow = ["git"]`,
   `env_allow = ["HOME", "GIT_CONFIG_GLOBAL", "XDG_CONFIG_HOME"]` — required ONLY for PC2's
   gate check** (BC-4.17.001 Precondition 2). Omitting `env_allow` produces the same
   `IdentityResolutionFailed`-class silent inertness documented in BC-4.13.001 EC-016 — the
   gate would always fail closed (never renew), which is the *safe* direction but still a
   functional defect (PC4 mid-burst keep-alive would never actually happen for any writer).
6. **No `serde_yaml`/`serde_norway`, no `regex` crate (established Architecture Compliance
   Rule across this crate family):** Manual line-by-line scan only, via
   `factory_lock_parse::extract_frontmatter` + the crate's existing scanning helpers.
7. **No independent TTL literal (BC-4.17.001 Precondition 3 / Invariant 3):** `TTL_SECONDS`
   MUST be imported from `crates/factory-lock-parse`, never redeclared.
8. **Frontmatter-only; body never read, parsed, or depended upon (BC-4.17.001 Invariant 5):**
   Mirrors BC-4.13.001 Invariant 9 / BC-5.40.001 Invariant 7. Full-file read is necessary
   (to reconstruct and write back the complete file), but all *decision* logic and all
   *substitution targets* are confined to the region bounded by the frontmatter delimiters.
9. **String/byte-equality identity comparison via `factory_lock::trim_git_email` (canonical
   home established by S-17.06; BC-4.17.001 Precondition 2, mirrors BC-4.13.001's
   `trim_git_email` precedent):** `stamp-state-timestamp` MUST NOT reimplement email-string
   normalization. `trim_git_email` is promoted to `factory_lock::trim_git_email` by S-17.06;
   it is consumed transitively through `factory_lock::classify_identity_resolution` →
   `factory_lock::renew_lock_if_holder` — no direct call to `trim_git_email` is required in
   this crate.

### Registry Entry Spec (canonical TOML)

The new registry entry MUST match this shape (priority value to be reverified against the
live registry's current `PostToolUse`-tier maximum at implementation time — `470` was the
next free slot as of this story's authoring, one above the then-current maximum of `460`):

```toml
# ---------- stamp-state-timestamp (PostToolUse, Edit|Write|MultiEdit) ----------
# S-17.05 / ADR-046 / BC-4.17.001: hook-authored STATE.md wall-clock stamping.
# Unconditionally re-stamps timestamp: (PC1). Renews factory_lock.expires_at
# (PC2) ONLY when a lock is held AND this hook's resolved writer identity
# (git config user.email) byte-equals the recorded holder — never resurrects
# a foreign or expired holder's lock. Fail-open on any error (PC3). Never
# touches acquire/release/CAS-push (PC5). Retires verify-state-timestamp-refresh
# in the same commit (ADR-046 Decision 3 atomicity — see AC-013).
# async = false REQUIRED — see ADR-019 + ADR-046

[[hooks]]
name = "stamp-state-timestamp"
event = "PostToolUse"
tool = "^(Edit|Write|MultiEdit)$"
plugin = "hook-plugins/stamp-state-timestamp.wasm"
priority = 470
timeout_ms = 5000
on_error = "continue"
async = false

[hooks.capabilities.read_file]
path_allow = [".factory/STATE.md"]

[hooks.capabilities.write_file]
path_allow = [".factory/STATE.md"]

[hooks.capabilities.exec_subprocess]
binary_allow = ["git"]
env_allow = ["HOME", "GIT_CONFIG_GLOBAL", "XDG_CONFIG_HOME"]
```

**Critical:** `read_file` and `write_file` capability blocks are both `path_allow`-only
(`ReadFileCaps`/`WriteFileCaps` structs reject unknown fields — do not add `max_bytes` or
`timeout_ms` as TOML fields; those are Rust-source-level arguments to the `host::` functions).

## Purity Classification

| Module | Classification | Justification |
|--------|-----------------|----------------|
| `stamp-state-timestamp/src/lib.rs` — `guard_logic` core | pure-core (callback-injectable) | Decision logic (PC1–PC5 gating, line-substitution computation) takes `read_file`/`write_file`/`exec_subprocess` as injectable closures — mirrors `verify-factory-lock`'s pure-core/effectful-shell split, enabling all 32 Rust unit tests (per Red Gate table) to run without a WASM runtime or real filesystem. |
| `stamp-state-timestamp/src/main.rs` — WASI entry point | effectful-shell | Wires the pure `guard_logic` core to the real `host::read_file`/`host::write_file`/`host::exec_subprocess` WASM host calls; owns all actual I/O. |
| `factory-lock-parse::TTL_SECONDS` | pure-core | Compile-time constant; no I/O, no side effects. |

## Library & Framework Requirements

| Dependency | Version | Purpose |
|------------|---------|---------|
| Rust toolchain | pinned via `rust-toolchain.toml` | All crates; WASM target `wasm32-wasip1` |
| `hook-sdk` | workspace dep (`crates/hook-sdk/`) | `HookResult`, `HookPayload`, `host::read_file`/`write_file`/`exec_subprocess` |
| `factory-lock-parse` | workspace dep (`crates/factory-lock-parse/`) — existing crate, amended | `extract_frontmatter`, `parse_factory_lock`, `LockState`, `extract_yaml_string_value`, `parse_iso8601`, new `TTL_SECONDS` constant |
| `chrono` | workspace dep | `chrono::Utc::now()` wall-clock read; RFC-3339 formatting |
| bats-core | `>=1.5.0` | Integration test framework for the new bats suite |

**Forbidden dependencies:**
- `stamp-state-timestamp` MUST NOT depend on `serde_yaml`, `serde_norway`, or any
  general-purpose YAML parser.
- `stamp-state-timestamp` MUST NOT depend on `verify-factory-lock` or
  `verify-state-timestamp-refresh` directly — all three depend on `factory-lock-parse` as the
  shared library, never on each other.
- `factory-lock-parse` MUST NOT gain a dependency on any `hook-plugins/*` crate (it is the
  shared base; a reverse dependency would be circular).
- No Python, Ruby, or Node.js tooling in any test or script in this story's scope.

## File Structure Requirements

| Action | Path | Notes |
|--------|------|-------|
| CREATE | `crates/hook-plugins/stamp-state-timestamp/Cargo.toml` | New `[[bin]]`-bearing WASM plugin crate manifest |
| CREATE | `crates/hook-plugins/stamp-state-timestamp/src/lib.rs` | Guard logic (PC1–PC5) + 32 Rust unit Red Gate tests (per Red Gate table) |
| CREATE | `crates/hook-plugins/stamp-state-timestamp/src/main.rs` | WASI entry point (`on_post_tool_use` wiring, mirrors `regression-gate/src/main.rs`) |
| MODIFY | `crates/factory-lock-parse/src/lib.rs` | Add `pub const TTL_SECONDS: u32 = 2700;` + cross-reference doc comment + 1 constant-equality test |
| MODIFY | `crates/factory-lock/src/lib.rs` | **Literal migration only (T-2 sub-task, D-1126):** migrate `renew_lock_with_now`'s `Duration::seconds(2700)` → `Duration::seconds(i64::from(factory_lock_parse::TTL_SECONDS))`. Single-line change. Doc-comment corrections (T-8) are already shipped by S-17.06 (`3200149d`); do NOT re-touch them. |
| ~~MODIFY (T-8)~~ | ~~`crates/factory-lock/src/lib.rs`~~ | ~~T-8 doc-comment corrections relocated to S-17.06~~ — S-17.06 owned and shipped those edits (`3200149d`). The literal migration above is a separate, distinct change scoped to this story per D-1126. |
| MODIFY | `plugins/vsdd-factory/bin/factory-lock-write.sh` | One-line cross-reference comment above line 66 `TTL_SECONDS=2700` (no behavior change) |
| MODIFY | `plugins/vsdd-factory/hooks-registry.toml` | ADD `stamp-state-timestamp` `[[hooks]]` entry; REMOVE `verify-state-timestamp-refresh` `[[hooks]]` entry — same commit (AC-013) |
| CREATE | `plugins/vsdd-factory/tests/stamp-state-timestamp.bats` | 4 bats integration tests (AC-006, AC-011, AC-013, AC-014) |
| DELETE | `plugins/vsdd-factory/tests/verify-state-timestamp-refresh.bats` | Retired — see Out of Scope for rationale |

**Explicitly NOT modified (see Out of Scope):**
- `crates/factory-lock/src/lib.rs` — **doc-comment edits (T-8)** are NOT modified here;
  they were shipped by S-17.06 (`3200149d`). Only the single-line TTL literal migration
  (T-2 sub-task) touches this file; all other content is left exactly as S-17.06 merged it.
- `crates/hook-plugins/verify-state-timestamp-refresh/src/lib.rs` (crate source + ~28 Rust
  unit tests left in place per ADR-046 Decision 2)
- `crates/hook-plugins/verify-state-timestamp-refresh/Cargo.toml`
- `.factory/specs/architecture/ARCH-INDEX.md`, `.factory/STATE.md`, `.factory/policies.yaml`

## Token Budget Estimate

| Context component | Estimated tokens |
|--------------------|-------------------|
| This story spec (S-17.05 v1.8) | ~7,800 |
| ADR-046 (full — Decision, Rationale, File-Change Plan, Rollout) | ~9,500 |
| BC-4.17.001 (full — PC1–PC5, Invariants, Edge Cases, Test Vectors) | ~6,500 |
| BC-5.40.001 (PC4, Invariant 1, relevant edge cases only) | ~3,000 |
| `verify-factory-lock/src/lib.rs` (identity-resolution + `guard_logic` pattern source) | ~4,000 |
| `regression-gate/src/lib.rs` (`on_post_tool_use` + read+write PostToolUse pattern) | ~3,000 |
| `factory-lock-parse/src/lib.rs` (existing crate to amend) | ~3,000 |
| `hooks-registry.toml` (verify-factory-lock + verify-state-timestamp-refresh region for insertion/removal point) | ~2,000 |
| `stamp-state-timestamp/src/lib.rs` (new file being written) | ~3,500 |
| `stamp-state-timestamp.bats` (new file being written) | ~1,500 |
| Agent context overhead + Cargo workspace config | ~2,000 |
| BC files total (2 BCs — BC-4.17.001 + BC-5.40.001) | (counted above) |
| **Total estimated** | **~45,800 tokens (~22.9% of 200k context window)** |

Within the 20–30% ceiling. No split required. Implementer should load
`verify-factory-lock/src/lib.rs` and `regression-gate/src/lib.rs` as the two primary
structural precedents (identity resolution + PostToolUse read/write respectively) rather than
the retired `verify-state-timestamp-refresh` crate, whose reconstruction logic does not apply
to a PostToolUse hook (see Red Gate Test Table payload-discipline note).

## Edge Cases

Reused directly from BC-4.17.001 (no new edge cases invented — per story-writer discipline,
ACs and edge cases trace to the BC, not the reverse):

| ID | Description | Expected Behavior | AC Reference |
|----|-------------|--------------------|--------------|
| EC-001 | Write while unlocked (no `factory_lock` block) | `timestamp:` re-stamped; no block created | AC-001, AC-004 |
| EC-002 | Write while lock held by the writer's own identity | `timestamp:` + `expires_at` both re-stamped; `locked_at` unchanged | AC-002, AC-003 |
| EC-003 | Write by a caller admitted via BC-4.13.001 PC2 (`LockExpired`) — non-holder | `timestamp:` re-stamped; `expires_at` NOT renewed | AC-002, AC-006 |
| EC-004 | `git config user.email` unset / `exec_subprocess` fails during PC2 gate check | `expires_at` left untouched (fail-closed on renewal only); `timestamp:` unaffected; `factory.lock.renewal_indeterminate` event + `log_warn` emitted (PC3b diagnostic) | AC-007, AC-015 |
| EC-005 | Malformed frontmatter (missing closing `---`) at read time | No write performed at all; agent's content untouched | AC-008 |
| EC-006 | Two back-to-back qualifying writes within the same burst | Each invocation independently re-stamps; renewal re-evaluated fresh each time (stateless per-invocation) | AC-009 |
| EC-007 | `factory_lock` present, `holder` non-empty, but `expires_at` malformed/unparseable | Hook still computes and writes a fresh `expires_at` if PC2's identity gate is satisfied — repairs the malformed value | AC-003, AC-010 |
| EC-008 | Hook's write races a concurrent `state-manager` git add/commit | Not a hazard — `verify-factory-lock` blocks all other sessions' mutating calls while a foreign unexpired lock is held (ADR-046 Rationale) | N/A (no new logic required) |
| EC-009 | Agent proposes its own (now-superfluous) `timestamp:`/`expires_at` value in its own Edit/Write payload | Hook's rewrite runs after the tool's own write lands and unconditionally/conditionally overwrites — no conflict-resolution logic needed | AC-001, AC-003 |
| EC-010 | `factory_lock` block present, `holder` non-empty, `expires_at` parseable but already elapsed (`now >= expires_at`), and hook's own git email matches the stale `holder` (expired self-held lock) | `timestamp:` re-stamped; `expires_at` NOT renewed — PC2 case 2 (`SkipReason::AlreadyExpired`) resolves before identity resolution is even attempted; no `exec_subprocess` call; no `renewal_indeterminate` event; expired lock left inert until explicit re-acquire | AC-006 |
| EC-013 | STATE.md frontmatter well-formed (valid delimiters, valid UTF-8, `host::read_file` succeeded) but contains no `timestamp:` key/line; `factory_lock` block IS present with non-empty `holder`, `expires_at` unexpired, identity resolves+matches | PC1 suppressed (`TimestampAnchorMissing` advisory); PC2 unaffected — evaluates in full and renews `expires_at` to `now + TTL_SECONDS`; no `timestamp:` line is inserted as a side-effect | AC-016 |
| EC-014 | STATE.md frontmatter contains two lines beginning with `timestamp:` (duplicate-key pre-existing corruption) | First `timestamp:` line rewritten to `now`; second left unchanged; advisory `DuplicateTimestampKey` `host::log_warn` emitted; no block; PC2 unaffected | AC-019 |
| EC-015 | `host::read_file(".factory/STATE.md")` returns `OutputTooLarge` (file exceeds `STATE_MD_MAX_BYTES` 262144 bytes) | PC3a structural fail-open: both PC1 and PC2 suppressed; no write; advisory `host::log_warn` emitted; agent's original write left untouched | AC-008 |

## Demo Plan

1. **AC-001/AC-004 (unlocked write):** Fixture STATE.md with no `factory_lock` block. Run an
   `Edit` through the real dispatcher. Assert post-write `timestamp:` differs from pre-write
   value; assert no `factory_lock` block was created.
2. **AC-003 (identity match — renews):** Fixture with `factory_lock.holder` equal to the
   test harness's configured `git config user.email`. Run a `Write`. Assert
   `expires_at` = write-instant + 2700s; assert `locked_at` unchanged.
3. **AC-006 (identity mismatch — SAFETY-CRITICAL, never renews):** Fixture with
   `factory_lock.holder` set to a foreign email, `expires_at` in the past (simulating a
   `LockExpired`-admitted writer). Run a `MultiEdit`. Assert `expires_at` is byte-identical
   pre- and post-write; assert `timestamp:` still advanced.
4. **AC-013 (registry atomicity):** `grep -c 'name = "stamp-state-timestamp"'
   hooks-registry.toml` → 1; `grep -c 'name = "verify-state-timestamp-refresh"'
   hooks-registry.toml` → 0. Both assertions in the same test run against the same commit.
5. **AC-014 (mid-burst renewal e2e):** Simulate a `state-manager` burst: acquire a lock via
   `factory-lock-write.sh acquire`, then perform an `Edit` to STATE.md as the same identity
   without calling `renew`. Assert the hook alone advanced `expires_at` by 2700s from the
   write instant — no manual `renew` invocation required.
6. **AC-012 (TTL sourcing):** `cargo test -p factory-lock-parse
   test_ttl_seconds_constant_equals_2700` and the `stamp-state-timestamp` crate's mirror test
   both pass; grep confirms no second `2700` literal exists in
   `stamp-state-timestamp/src/lib.rs`.

## Dependency Anchor Justifications

- **`depends_on: ["S-17.06"]`** — S-17.06 creates `factory_lock::renew_lock_if_holder`,
  `factory_lock::classify_identity_resolution`, and `factory_lock::trim_git_email` (canonical
  home). This story's `stamp-state-timestamp/src/main.rs` calls `factory_lock::renew_lock_if_holder`
  directly, which requires S-17.06 to be implemented first. `crates/factory-lock-parse/`
  (S-17.04, merged 3b2a378c) is already on develop (T-2 adds constants to it). S-17.05 MUST
  be scheduled after S-17.06 in the topological order.
- **`blocks: []`** — no downstream story currently depends on `stamp-state-timestamp`
  existing; it is a self-contained hardening addition to the hook family.
- **Wave 5 placement:** S-17.01–04 (waves 1–4) are all MERGED; wave 5 is the natural next
  slot for a story with zero product dependencies within E-17.

## Changelog

| Version | Date | Author | Change |
|---------|------|--------|--------|
| 1.8 | 2026-08-28 | story-writer | D-1127 finalization doc-sweep — exhaustive Red Gate count correction (all doc-only; post-3-CLEAN per D-1128). F-P12-001 tally: 28→32 in `stamp-state-timestamp/src/lib.rs` (30 guard_logic-calling + 2 constant; `grep -c 'fn test_'` = 32 ✓); Rust total 31→35; mandated tests 35→39. T-1: stub count 18→32, CI-fail total 22→39. T-7: 27→39. T-3 (x2), Purity Classification table, File Structure table: 18→32. Out-of-Scope `~28` and T-7 `~28` verify-state-timestamp-refresh counts de-numbered (actual 50; drift-resistant). Token Budget self-version label updated v1.7→v1.8. No AC/PC/semantic change. |
| 1.7 | 2026-08-28 | story-writer | F-P11-001 + O-P11-1: BC gate header updated to current versions (BC-4.17.001 v1.28, BC-5.40.001 v1.21); stale "[pending]" traceability claim corrected (BC-5.40.001 v1.21 Traceability confirms S-17.05 as implementing/anchor story); Token Budget self-version label updated v1.1→v1.7. Doc-only; no AC/PC/semantic change. |
| 1.6 | 2026-08-28 | story-writer | BC-4.17.001 body-table version cite sync 1.27→1.28 (F-P10-001 leg-5 propagation from pass-9 burst BC-4.17.001 v1.27→v1.28 seal; doc-only; no AC/PC/semantic change). |
| 1.5 | 2026-08-28 | story-writer | Consolidated AC↔BC-4.17.001 reconciliation (7 gaps, post-local-pass-4): AC-008 narrowed to structural-failures-only (missing-`timestamp:`-anchor clause removed; `OutputTooLarge` + advisory `log_warn` added); AC-006 extended (EC-010 `AlreadyExpired` expired-self-held case made explicit); AC-016 added (EC-013 valid-frontmatter-no-`timestamp:`-anchor PC3a scoped exception); AC-017 added (Precondition-1 F-013 tool-write-success gate); AC-018 added (Invariant-8 `state_md_approaching_cap` soft-warn); AC-019 added (EC-014 duplicate-`timestamp:`-key advisory); EC table updated (EC-010/EC-013/EC-014/EC-015 rows added); Red Gate table updated (`test_missing_timestamp_anchor_writes_nothing` retired — asserted wrong behavior per BC PC3a scoped exception; 9 new rows added); Red Gate count 27→35 (31 Rust + 4 bats). `last_amended` and `modified[]` updated per POLICY 14/17. |
| 1.4 | 2026-08-28 | story-writer | Add AC-015 tracing BC-4.17.001 PC3b renewal-indeterminate diagnostic (`emit_event` + `log_warn`) — closes O-1 traceability gap behind local-pass-3 finding F-S1705-P3-001. BC table updated to cite PC3b. EC-004 updated to reference AC-015 diagnostic side-effect. Three new Rust unit Red Gate tests added to `stamp-state-timestamp/src/lib.rs` (Red Gate count 24→27: 23 Rust + 4 bats). `last_amended` and `modified[]` updated per POLICY 14/17. |
| 1.3 | 2026-08-28 | story-writer | ADR-046 Wave-5 decomposition boundary correction (D-1126 follow-up): S-17.05 now owns the `renew_lock_with_now`→`TTL_SECONDS` literal migration (moved from S-17.06 which correctly deferred it; S-17.05 creates `TTL_SECONDS` in T-2). Sub-task added to T-2 to migrate `crates/factory-lock/src/lib.rs` `renew_lock_with_now`'s `Duration::seconds(2700)` → `Duration::seconds(i64::from(factory_lock_parse::TTL_SECONDS))` after S-17.06 is merged (`3200149d`). MODIFY row for `crates/factory-lock/src/lib.rs` added to File Structure Requirements (literal migration only; doc-comment corrections remain S-17.06 scope). Red Gate test `test_renew_lock_with_now_uses_ttl_seconds_not_bare_literal` added to `factory-lock/src/lib.rs` (20 Rust unit total, 24 total including 4 bats). NOTE-T-8 text updated to clarify that its prohibition covers doc-comments only, not the TTL literal migration. `crates/factory-lock/src/lib.rs` removed from Explicitly NOT modified list with explanation. |
| 1.2 | 2026-08-27 | story-writer | ADR-046 Wave-5 decomposition re-scope (D-1124): T-8 (factory-lock doc-comment corrections) relocated to S-17.06 which owns all `crates/factory-lock/src/lib.rs` edits. `depends_on` updated `[]` → `["S-17.06"]` (stamper `main.rs` calls `factory_lock::renew_lock_if_holder` created by S-17.06). T-2 extended: add `pub const STATE_MD_MAX_BYTES: u32 = 262144;` to `crates/factory-lock-parse/src/lib.rs` + Red Gate test `test_state_md_max_bytes_constant_equals_262144` (19 Rust unit total). T-3 wiring note updated: `main.rs` passes lazy `resolve_identity` closure `\|\| factory_lock::classify_identity_resolution(host::exec_subprocess(["git","config","user.email"]))` to `factory_lock::renew_lock_if_holder`; `lib.rs` injectable-callback signature unchanged; 18 Rust unit test mocks unchanged. VP-TBD comment corrected to `VP-TBD-1..4, VP-TBD-7..9` (7 total). `trim_git_email` concern removed from Architecture Compliance Rule 9 (S-17.06 owns canonical home; S-17.05 uses it transparently via `renew_lock_if_holder`). File Structure Requirements: `crates/factory-lock/src/lib.rs` MODIFY row removed (moved to S-17.06). Red Gate count: 22 → 23. |
| 1.1 | 2026-08-27 | story-writer | Added T-8: correct stale `factory-lock` crate doc-comments (`renew_lock_with_now` algorithm doc, `Ok(None)`-arm inline comment, `parse_lock` doc) to reflect post-F-P56-001 semantics (`Ok(None)` fires only for absent-or-fully-null block; empty/absent `holder` with siblings present → `Err(Malformed)`). Bundled per O-P61-001/O-P62-001 human directive 2026-08-27. Added MODIFY row for `crates/factory-lock/src/lib.rs` to File Structure Requirements. Updated Token Budget Estimate to v1.1. Updated `last_amended` and `modified[]` frontmatter fields. |
| 1.0 | 2026-08-25 | story-writer | Initial authoring. ADR-046 ratified 2026-08-25; BC-4.17.001 v1.0 + BC-5.40.001 v1.4 (PC4 actor reassignment). New E-17 wave-5 story: `stamp-state-timestamp` PostToolUse WASM crate (PC1 unconditional timestamp re-stamp, PC2 identity-gated `expires_at` renewal with exhaustive 5-row truth table, PC3 fail-open, PC4 idempotent frontmatter-only rewrite, PC5 no lock-lifecycle involvement); `TTL_SECONDS` canonical constant added to `factory-lock-parse`; `hooks-registry.toml` stamper entry + `verify-state-timestamp-refresh` deregistration in the same commit (AC-013/Delivery Constraint); new 4-bats + 18-Rust-unit Red Gate suite; `verify-state-timestamp-refresh.bats` retired (crate source + ~28 Rust unit tests explicitly left in place per ADR-046 Decision 2, pending a separate future cleanup story). 14 ACs, 9 edge cases (reused from BC-4.17.001), 8 story points, wave 5, `depends_on: []`. |
