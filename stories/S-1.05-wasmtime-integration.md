---
document_type: story
level: ops
story_id: "S-1.05"
epic_id: "E-1"
version: "1.1"
status: merged
producer: story-writer
timestamp: 2026-04-25T00:00:00
phase: 1.8
inputs:
  - .factory/stories/v1.0/S-1.5-wasmtime-integration.md
  - .factory/specs/architecture/ARCH-INDEX.md
input-hash: "0342b0e"
traces_to: .factory/specs/domain-spec/capabilities.md
cycle: v1.0.0-greenfield
points: "8"
depends_on: ["S-1.01", "S-1.02", "S-1.04"]
blocks: ["S-1.06", "S-2.01", "S-3.01", "S-3.02", "S-3.03"]
behavioral_contracts: []
verification_properties: []
priority: "P0"
wave: 4
target_module: crates/factory-dispatcher/src/engine.rs
subsystems: ["SS-01"]
estimated_days: 4
assumption_validations: []
risk_mitigations: []
---

# S-1.05: wasmtime integration + epoch/fuel enforcement

**Epic:** E-1 — Dispatcher Foundation
**Phase:** Phase 1 — Dispatcher foundation
**Milestone:** `1.0.0-beta.1`
**Tier:** B.x
**Blocks:** S-1.06, S-2.01, S-3.01, S-3.02, S-3.03
**Blocked by:** S-1.01, S-1.02, S-1.04
**Estimated effort:** L

## Narrative
- **As a** dispatcher operator
- **I want to** have plugins run in isolated wasmtime stores with epoch/fuel limits
- **So that** a hanging or resource-exhausting plugin cannot block or crash the dispatcher

## Goal

Wire wasmtime into the dispatcher so it can instantiate a `.wasm` plugin,
run its `_start` entry point with a stdin payload, and enforce time + fuel
limits. Each invocation gets a fresh wasmtime `Store`, its own epoch
deadline, and its own fuel cap.

## Acceptance criteria

- [ ] Shared `wasmtime::Engine` with epoch interruption + fuel consumption enabled
- [ ] Dispatcher spawns an epoch-increment timer (background tokio task bumping engine epoch every 10ms)
- [ ] `load_plugin(path) -> Result<Module>` — compiles and caches modules by path + mtime
- [ ] `invoke_plugin(module, payload, limits) -> PluginResult` — creates Store, sets epoch deadline, sets fuel, pipes stdin, runs `_start`, catches traps
- [ ] Epoch-timeout produces `PluginResult::Timeout { elapsed_ms, fuel_consumed }`
- [ ] Fuel-exhaustion produces `PluginResult::Timeout`
- [ ] Panic/trap produces `PluginResult::Crashed { trap: String }`
- [ ] Normal completion produces `PluginResult::Ok { exit_code: i32, result: HookResult, elapsed_ms, fuel_consumed }`
- [ ] Dispatcher emits `plugin.invoked`, `plugin.completed`, `plugin.timeout`, `plugin.crashed` events
- [ ] Integration test: plugin that runs normally, hangs, panics, exhausts fuel — all handled
- [ ] No memory leak across 10,000 invocations

## Tasks

1. Create `crates/factory-dispatcher/src/engine.rs` — global lazily-initialized `wasmtime::Engine`.
2. Create `crates/factory-dispatcher/src/plugin_loader.rs` — module cache keyed by `(plugin_path, mtime)`.
3. Create `crates/factory-dispatcher/src/invoke.rs` — `invoke_plugin()` with full lifecycle.
4. Wire epoch timer as background tokio task in `main.rs`.
5. Write integration tests for all PluginResult variants.

## Contracts

**`PluginResult` type (consumed by S-1.06):**

```rust
pub enum PluginResult {
    Ok { exit_code: i32, result: HookResult, elapsed_ms: u64, fuel_consumed: u64 },
    Timeout { elapsed_ms: u64, fuel_consumed: u64 },
    Crashed { trap: String },
}
```

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| engine.rs | crates/factory-dispatcher/src/engine.rs | effectful-shell (global engine init) |
| plugin_loader.rs | crates/factory-dispatcher/src/plugin_loader.rs | effectful-shell (filesystem + cache) |
| invoke.rs | crates/factory-dispatcher/src/invoke.rs | effectful-shell (WASM execution) |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | Plugin exceeds epoch deadline | `PluginResult::Timeout` |
| EC-002 | Plugin exhausts fuel cap | `PluginResult::Timeout` |
| EC-003 | Plugin calls `panic!()` | `PluginResult::Crashed` with trap string |
| EC-004 | `.wasm` file does not exist | `Err(...)` propagated from `load_plugin()` |
| EC-005 | Module mtime changes | `plugin_loader` recompiles on next invocation |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| engine.rs | effectful-shell | Global engine initialization with side effects |
| plugin_loader.rs | effectful-shell | Reads filesystem, maintains mutable cache |
| invoke.rs | effectful-shell | WASM execution with I/O |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~1,400 |
| engine.rs | ~600 |
| invoke.rs | ~1,200 |
| plugin_loader.rs | ~600 |
| Integration tests | ~1,500 |
| Tool outputs overhead | ~500 |
| **Total** | **~5,800** |
| Agent context window | 200K for Sonnet |
| **Budget usage** | **~2.9%** |

## Tasks (MANDATORY)

1. [ ] Create `engine.rs` with lazy global Engine + epoch config
2. [ ] Create `plugin_loader.rs` with mtime-keyed module cache
3. [ ] Create `invoke.rs` with full `invoke_plugin()` lifecycle
4. [ ] Wire epoch timer background task in main.rs
5. [ ] Write integration tests for Timeout, Crashed, Ok, fuel-exhausted paths
6. [ ] Memory leak test: 10k invocations

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-1.03 | WASI preview 1 `_start` entry point | Plugin ABI convention | proc-macro generates `_start` |
| S-1.04 | Linker with host functions | HostContext per invocation | OOB must not crash dispatcher |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| Epoch interruption enabled (not just fuel) | BC-1.04 engine and ticker | Engine Config check |
| Module cache keyed by path + mtime | BC-1.09 plugin cache semantics | Integration test with mtime change |
| No memory leak across invocations | BC-1.07 regression contracts | 10k invocation test |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| wasmtime | workspace-pinned | WASM engine, Store, Linker, Module |
| tokio | workspace-pinned | Epoch timer background task |

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| crates/factory-dispatcher/src/engine.rs | create | Global wasmtime Engine |
| crates/factory-dispatcher/src/plugin_loader.rs | create | Module cache |
| crates/factory-dispatcher/src/invoke.rs | create | Plugin invocation lifecycle |
| crates/factory-dispatcher/tests/invoke_integration.rs | create | Integration tests |
