---
document_type: story
level: ops
story_id: "S-8.08"
epic_id: "E-8"
version: "1.0"
status: draft
producer: story-writer
timestamp: 2026-04-30T00:00:00Z
phase: 2
inputs:
  - .factory/stories/epics/E-8-native-wasm-migration.md
  - .factory/stories/S-8.00-perf-baseline-bc-anchor-verification.md
  - plugins/vsdd-factory/hooks/track-agent-start.sh
  - plugins/vsdd-factory/hooks-registry.toml
input-hash: "5015917"
traces_to: .factory/stories/epics/E-8-native-wasm-migration.md
functional_requirements: []
cycle: v1.0-brownfield-backfill
points: "3"
depends_on: ["S-8.00"]
blocks: ["S-8.09"]
behavioral_contracts: ["BC-7.03.079", "BC-7.03.080"]
# BC status: anchored — track-agent-start has two existing BCs:
# BC-7.03.079: identity & registry binding (PreToolUse:Agent, priority=110, on_error=continue)
# BC-7.03.080: emits agent.start with subagent_type + best-effort story_id extraction
#   (regex: S-[0-9]+\.[0-9]+ first, then STORY-[0-9]+)
# Both confirmed by BC-INDEX grep (lines 1606-1607) on track-agent-start.sh path.
# Per D-2 Option C: existing BCs reused; no new BC-7.xx migration family.
# Cross-story note: track-agent-start (S-8.08, PreToolUse:Agent) pairs with
# track-agent-stop (S-8.03, SubagentStop) for agent lifecycle telemetry.
# The two can be implemented in parallel within W-15; no dependency between them.
# Status: draft (behavioral_contracts non-empty; may advance to ready after PO review).
verification_properties: []
priority: "P2"
wave: 15
target_module: "crates/hook-plugins/track-agent-start/"
subsystems: ["SS-01", "SS-07"]
capabilities: ["CAP-022"]
estimated_days: 1
assumption_validations: []
risk_mitigations: ["R-8.01", "R-8.05"]
---

<!-- input-hash: 5015917 = commit "chore(stories): S-8.00 v1.5 status=ready CONVERGENCE_REACHED".
Verify via: git log --all --oneline -- .factory/stories/epics/E-8-native-wasm-migration.md -->

# S-8.08: Native port: track-agent-start (PreToolUse:Agent)

**Epic:** E-8 — Native WASM Migration Completion
**Phase:** Phase 2 — Story Decomposition (W-15 Tier 1 port)
**Milestone:** `v1.1`
**Tier:** Tier 1 — PreToolUse:Agent lifecycle hooks
**Blocks:** S-8.09
**Blocked by:** S-8.00
**Estimated effort:** S (1 day, 3 pts)

## Capability Anchor Justification

**Primary anchor:** CAP-022 ("Port hook plugins from bash to native WASM") per
`domain-spec/capabilities.md` §CAP-022. S-8.08 ports `track-agent-start.sh` to
native WASM, eliminating legacy-bash-adapter indirection for this telemetry hook.

**Stretch-Anchor Disclosure:** S-8.08 anchors to CAP-022 but spans two subsystems:
- **SS-07 Hook Bash Layer** owns `track-agent-start.sh` and its BC-7.03.079/080
  behavioral contracts. SS-07 is the source-of-truth for the hook's behavior spec.
- **SS-01 Hook Dispatcher Core** owns the hooks-registry.toml routing and the host fn
  surface (`vsdd_hook_sdk::host::*`) used by the native crate. Registry migration
  is a dispatcher-scope change.

Both subsystem anchors verified against ARCH-INDEX Subsystem Registry:
- SS-01 Hook Dispatcher Core: canonical name confirmed.
- SS-07 Hook Bash Layer: canonical name confirmed.
(Per POLICY 5 anchor justification requirement + POLICY 6 architecture-is-source-of-truth.)

**CAP-022 cross-CAP stretch disclosure:** Same rationale as S-8.01..S-8.07 — sanctioned
cross-CAP stretch per Wave 7 F-204 methodology. CAP-022 declared Subsystems (SS-04,
SS-06) are the source of truth for hook plugin framework concerns.

**Complexity note (Low — 3 pts):** track-agent-start is a pure telemetry hook. It:
(a) checks `jq` availability (bash version), exits 0 if absent; (b) reads stdin JSON;
(c) extracts `tool_name`, exits 0 if not "Agent"; (d) extracts `tool_input.subagent_type`
and `tool_input.prompt`; (e) applies two regex patterns to extract story_id from prompt;
(f) calls `_emit()` (bin/emit-event) with `type=agent.start`. Never blocks. Always exits 0.

The WASM port replaces: `jq` calls → `serde_json` JSON parsing; `grep -oE` regex
extraction → Rust regex crate; `_emit()` → `host::emit_event`. All three replacements
are straightforward; no subprocess calls needed in the native crate.

**Cross-story pairing:** track-agent-start (S-8.08) pairs with track-agent-stop (S-8.03,
already authored in burst 1) for agent lifecycle telemetry. S-8.03 established the
event emission pattern. S-8.08 follows the same pattern. The two stories are
informational complements — no implementation dependency between them.

**Dependency anchor:** S-8.08 depends on S-8.00, not S-8.03 — the stories are
parallel within W-15. Ordering within the tier is informational only.

## Behavioral Contracts

| BC ID | Title | Trace |
|-------|-------|-------|
| BC-7.03.079 | track-agent-start: identity & registry binding | AC-001: WASM crate implements PreToolUse:Agent binding, priority=110, on_error=continue; always exits 0 |
| BC-7.03.080 | track-agent-start: emits agent.start with subagent + best-effort story_id | AC-003: WASM port emits agent.start with subagent field; AC-004: story_id extracted via S-N.NN regex then STORY-NNN fallback |

## Verification Properties

| VP ID | Purpose |
|-------|---------|
| (none assigned) | No VP currently contracts track-agent-start behavior. See v1.1 BC/VP Candidates below. |

## v1.1 BC/VP Candidates

> Registered per F-206 5-col format.

| Candidate ID | Type | Title | Rationale | Blocking? |
|---|---|---|---|---|
| [process-gap] VP-track-agent-start-parity | VP | WASM track-agent-start must produce identical agent.start event emission as bash version for all fixture PreToolUse:Agent inputs | No VP currently verifies parity between bash and WASM versions; AC-005 bats parity test is the enforcement mechanism | No |

## Narrative

- **As a** vsdd-factory operator running agent pipelines
- **I want** track-agent-start to execute as a native WASM plugin instead of through the legacy-bash-adapter
- **So that** agent.start telemetry events fire on all platforms (including Windows
  without jq), with lower latency, no adapter indirection, and no jq dependency

## Goal

Port `plugins/vsdd-factory/hooks/track-agent-start.sh` to a native Rust WASM crate at
`crates/hook-plugins/track-agent-start/`. The hook fires on PreToolUse filtered to the
Agent tool. It reads stdin JSON, extracts `tool_input.subagent_type` and
`tool_input.prompt`, applies two regex patterns to extract a story_id from the prompt
(pattern 1: `S-[0-9]+\.[0-9]+`; pattern 2: `STORY-[0-9]+` as fallback), then calls
`host::emit_event` with `type=agent.start hook=track-agent-start matcher=Agent
subagent=<subagent_type>` (plus optional `story_id=<id>` if extracted). Always exits 0.

Behavior parity is the sole requirement (E-8 D-2). The hook is best-effort: missing jq
(bash), missing JSON fields, and emit_event failures all result in silent exit 0 (no
error output). The WASM port must preserve this best-effort semantics.

## Acceptance Criteria

### AC-001 (traces to BC-7.03.079 postcondition 1)
WASM crate `crates/hook-plugins/track-agent-start/` exists with `Cargo.toml` targeting
`wasm32-wasi`, implements the `vsdd_hook_sdk` hook interface, and builds successfully
via `cargo build --target wasm32-wasi`. Registry entry in `hooks-registry.toml` is
updated: `plugin = "hook-plugins/track-agent-start.wasm"`, `script_path` removed,
`shell_bypass_acknowledged` removed. Event binding preserved: `event = "PreToolUse"`,
`tool = "Agent"`, `priority = 110`, `on_error = "continue"`.

### AC-002 (traces to BC-7.03.079 invariant 1)
`hooks.json` command entry for `track-agent-start` is deleted from all
platform-specific `hooks.json.*` files and the template.
`plugins/vsdd-factory/hooks/track-agent-start.sh` is deleted from the repository.

### AC-003 (traces to BC-7.03.080 postcondition 1)
When stdin contains a valid PreToolUse JSON envelope with `tool_name = "Agent"`:
WASM plugin extracts `tool_input.subagent_type` (falls back to `"unknown"` if absent)
and calls `host::emit_event` with at minimum:
`type=agent.start hook=track-agent-start matcher=Agent subagent=<subagent_type>`.
Exits 0.

### AC-004 (traces to BC-7.03.080 postcondition 1)
Story ID extraction follows the two-pattern cascade from the bash source:
1. Search prompt for pattern `S-[0-9]+\.[0-9]+` (e.g., `S-8.03`); use first match.
2. If no match, search for `STORY-[0-9]+` (e.g., `STORY-042`); use first match.
3. If neither matches, omit `story_id` field from the event entirely.
When a story_id is found, the emitted event includes `story_id=<extracted_id>`.
Bats test verifies: prompt containing "S-6.07" produces `story_id=S-6.07`;
prompt containing "STORY-042" (no S-N.NN) produces `story_id=STORY-042`;
prompt with no story pattern produces event without story_id field.

### AC-005 (traces to BC-7.03.079 postcondition 1 + BC-7.03.080 postcondition 1)
Bats parity test at `tests/integration/hooks/track-agent-start.bats` covers:
(a) Agent dispatch with subagent=pr-manager, prompt containing "S-6.07" →
    exit 0 + agent.start event with subagent=pr-manager story_id=S-6.07;
(b) Agent dispatch with subagent=implementer, prompt containing "STORY-042" →
    exit 0 + agent.start event with subagent=implementer story_id=STORY-042;
(c) Agent dispatch with subagent=reviewer, prompt with no story pattern →
    exit 0 + agent.start event with subagent=reviewer, no story_id field;
(d) Non-Agent tool_name in envelope → exit 0, no event emitted;
(e) Malformed JSON stdin → exit 0, no event, no panic;
(f) WASM warm invocation time compared to S-8.00 baseline; within 20% regression ceiling.
All bats tests pass.

### AC-006 (traces to BC-7.03.079 invariant 2)
Best-effort semantics preserved: if JSON parse fails, if `tool_name != "Agent"`,
or if `host::emit_event` returns an error, the hook exits 0 with no stderr output.
No error is surfaced to the dispatcher. Verify in bats test (case d + e above).
`bin/emit-event` binary is NOT removed (E-8 D-10 — deferred to S-8.28).

## Acceptance Criteria with BC Traces

| AC | Statement | BC Trace |
|----|-----------|----------|
| AC-001 | WASM crate exists, builds for wasm32-wasi, registry updated with PreToolUse:Agent binding; priority=110, on_error=continue preserved | traces to BC-7.03.079 postcondition 1 (identity & registry binding) |
| AC-002 | hooks.json entry deleted; .sh file deleted | traces to BC-7.03.079 invariant 1 (hook path lifecycle) |
| AC-003 | Valid Agent dispatch: emit agent.start with subagent field; exit 0 | traces to BC-7.03.080 postcondition 1 |
| AC-004 | story_id extracted: S-N.NN first, STORY-NNN fallback, omitted if absent | traces to BC-7.03.080 postcondition 1 (extraction cascade) |
| AC-005 | Bats parity tests pass (6 scenarios); WASM perf within 20% ceiling | traces to BC-7.03.079 postcondition 1 + BC-7.03.080 postcondition 1 |
| AC-006 | Best-effort semantics: all error paths exit 0 silently; bin/emit-event not removed | traces to BC-7.03.079 invariant 2 (exit-code semantics) |

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| track-agent-start WASM crate | `crates/hook-plugins/track-agent-start/` | Effectful (reads stdin JSON, calls host::emit_event) |
| hooks-registry.toml (modified) | `plugins/vsdd-factory/hooks-registry.toml` | Pure (declarative config; updated once) |
| hooks.json (deleted entry) | `plugins/vsdd-factory/hooks/hooks.json.*` | Pure (deleted entry) |
| track-agent-start.sh (deleted) | `plugins/vsdd-factory/hooks/track-agent-start.sh` | N/A (deleted) |
| bats parity tests | `tests/integration/hooks/track-agent-start.bats` | Effectful (provides fixture JSON envelopes via stdin; captures event emission) |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | `tool_input.subagent_type` missing from JSON | Use `"unknown"` as subagent value (per bash: `jq -r '.tool_input.subagent_type // "unknown"'`) |
| EC-002 | `tool_input.prompt` missing or null | Treat as empty string; no story_id extracted; event emitted without story_id field |
| EC-003 | Prompt contains both S-N.NN and STORY-NNN patterns | S-N.NN wins (pattern 1 tried first); only first match of pattern 1 used |
| EC-004 | tool_name present but not "Agent" (e.g., "Bash") | Hook exits 0 immediately after tool_name check; no event emitted |
| EC-005 | stdin is empty (zero bytes) | JSON parse returns error; hook exits 0 silently |
| EC-006 | host::emit_event returns Err | Silently swallowed; exit 0 (best-effort pattern — same as bash `\|\| true`) |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| track-agent-start WASM crate | Effectful | Reads stdin; calls host::emit_event side-effecting host function |
| bats parity tests | Effectful | Provides JSON stdin via process substitution; verifies event emission side effects |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~2,600 |
| E-8 epic spec (relevant sections: D-2, D-6, D-10, Goals) | ~2,000 |
| track-agent-start.sh bash source | ~350 |
| BC files (2 BCs: BC-7.03.079, .080 — ~400 tokens each) | ~800 |
| hooks-registry.toml (PreToolUse:Agent section, lines 763-781) | ~200 |
| vsdd-hook-sdk Rust API reference | ~800 |
| serde_json + regex crate API reference | ~600 |
| Bats test scaffolding + fixture JSON | ~600 |
| Tool outputs overhead | ~400 |
| **Total** | **~8,350** |
| Agent context window | 200K for Sonnet |
| **Budget usage** | **~4.2%** |

> Token budget well under 20-30% limit. S-8.03 (track-agent-stop, burst 1) is the
> closest structural sibling — the implementer should read S-8.03 alongside this story
> to share patterns (both use stdin JSON parse + emit_event; S-8.03 established the
> ok/empty/blocked classification pattern).

## Tasks

- [ ] T-1: Read `plugins/vsdd-factory/hooks/track-agent-start.sh` and BC-7.03.079/080
        in full. Also read S-8.03 (track-agent-stop) for the established JSON-parse +
        emit_event pattern. Verify BC-anchor table from S-8.00 confirms Spec-Current = Y
        for both BCs before beginning port.
- [ ] T-2: Create `crates/hook-plugins/track-agent-start/` crate skeleton:
        `Cargo.toml` (target wasm32-wasi, `vsdd-hook-sdk`, `serde_json`, `regex`
        dependencies), `src/lib.rs` with hook entry point.
- [ ] T-3: Implement track-agent-start logic in Rust:
        - Read full stdin to String; parse as JSON with serde_json. On parse error, exit 0.
        - Extract `tool_name`; if not "Agent", exit 0 immediately.
        - Extract `tool_input.subagent_type` (default: `"unknown"`).
        - Extract `tool_input.prompt` (default: `""`).
        - Apply regex `S-[0-9]+\.[0-9]+` to prompt; take first match as story_id.
        - If no match, apply regex `STORY-[0-9]+`; take first match.
        - Call `host::emit_event` with type=agent.start, hook=track-agent-start,
          matcher=Agent, subagent=<value>, and optionally story_id=<value>.
        - On emit_event error, exit 0 silently.
        - Exit 0.
- [ ] T-4: Build and test: `cargo build --target wasm32-wasi -p track-agent-start`.
        Verify `.wasm` artifact produced.
- [ ] T-5: Write bats parity tests at `tests/integration/hooks/track-agent-start.bats`
        covering all 6 scenarios per AC-005. Run all — all pass.
- [ ] T-6: Update `plugins/vsdd-factory/hooks-registry.toml`:
        Replace track-agent-start entry with native WASM plugin path. Remove
        script_path, shell_bypass_acknowledged. Update binary_allow: remove "bash" and
        "jq" (no longer needed — JSON parsing is native). Keep or remove exec_subprocess
        section as appropriate (likely remove entirely).
- [ ] T-7: Delete `plugins/vsdd-factory/hooks/track-agent-start.sh` and the
        corresponding command entry from all `hooks.json.*` files and template.
- [ ] T-8: Run full bats test suite; measure WASM warm invocation time; compare to
        S-8.00 baseline. If regression >20%, file perf finding before merging.

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-8.00 | BC-anchor table gates port stories | S-8.08 MUST verify BC-7.03.079/080 Spec-Current = Y before beginning | If BCs are stale, file a BC update before porting |
| S-8.03 (track-agent-stop) | Sibling hook in the agent lifecycle pair; also uses stdin JSON parse + emit_event; established ok/empty/blocked classification | JSON-parse-then-emit pattern: parse stdin with serde_json, extract fields with .get() chaining, call host::emit_event, exit 0 on any error path | track-agent-stop uses SubagentStop (different dispatcher path than PreToolUse); the stdin JSON envelope shape differs. Read both bash sources carefully — do not assume identical field names |
| S-8.01..S-8.07 | First 7 Tier 1 ports completed; registry migration + delete cycle confirmed | All hooks: (1) serde_json replaces jq, (2) host::emit_event replaces bin/emit-event, (3) no subprocess calls, (4) exec_subprocess block removed | jq availability check (`if ! command -v jq`) in bash translates to: no equivalent check needed in WASM — JSON parsing is always available via serde_json |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| HOST_ABI_VERSION = 1 unchanged throughout E-8 | E-8 Goal #5; BC-2.01.003; ADR-006 | track-agent-start uses only stdin read + emit_event — both existing host fns. No new host fn needed |
| BC-anchor strategy is Option C — reuse existing BCs; no new BC-7.02.x migration family | E-8 D-2 | BC-7.03.079/080 are the anchors. No new BC unless S-8.00 audit table shows Spec-Current = N |
| bin/emit-event must NOT be removed until S-8.28 | E-8 D-10; R-8.07 | T-3 replaces bin/emit-event logic with host::emit_event. Binary at `bin/emit-event` is NOT touched |
| Dispatcher routing: native WASM plugins do NOT register in hooks.json | E-8 D-7; DRIFT-004 | AC-002: hooks.json entry deleted |
| exec_subprocess block REMOVED for track-agent-start | bash source analysis (jq + bash only; no other subprocess) | After port: JSON parsing is native serde_json; no subprocess needed. Remove binary_allow entirely |
| All commits must follow Conventional Commits | BC-8.22.001 | Commit type for Rust crate: `feat(hook-plugins)`; registry: `chore(registry)`; .sh deletion: `chore(cleanup)` |
| Forbidden dependency: track-agent-start WASM crate must NOT depend on legacy-bash-adapter | E-8 D-10 | Build-time enforcement rule |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| Rust toolchain | stable (existing CI dependency) | Compile WASM crate |
| wasm32-wasi target | (via rustup, existing) | WASM compilation target |
| vsdd-hook-sdk | (workspace version, existing) | Hook entry point macro, host::emit_event |
| serde_json | >=1.0 (workspace pin, existing) | Parse PreToolUse JSON from stdin; replaces jq |
| regex | >=1.0 (workspace pin) | Extract story_id via S-N.NN and STORY-NNN patterns; replaces grep -oE |
| bats-core | >=1.10 (existing CI dependency) | Parity test harness |
| hyperfine | >=1.18 (added in S-8.00 pre-work) | Warm-invocation perf measurement vs S-8.00 baseline |

> Note: `regex` crate must be pinned in workspace Cargo.toml. Check existing workspace
> dependencies before adding. The `regex` crate compiles to WASM without issue but adds
> ~200KB to the `.wasm` artifact — acceptable for this hook profile.

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| `crates/hook-plugins/track-agent-start/Cargo.toml` | create | Crate manifest; wasm32-wasi target; vsdd-hook-sdk, serde_json, regex deps |
| `crates/hook-plugins/track-agent-start/src/lib.rs` | create | Hook implementation: stdin parse, tool_name guard, subagent extract, story_id regex, emit_event |
| `tests/integration/hooks/track-agent-start.bats` | create | Bats parity tests (6 scenarios per AC-005) |
| `plugins/vsdd-factory/hooks-registry.toml` | modify | Replace legacy-bash-adapter entry with native track-agent-start.wasm; remove exec_subprocess block |
| `plugins/vsdd-factory/hooks/hooks.json.template` | modify | Delete track-agent-start command entry |
| `plugins/vsdd-factory/hooks/hooks.json.darwin-arm64` | modify | Delete track-agent-start command entry |
| `plugins/vsdd-factory/hooks/hooks.json.darwin-x64` | modify | Delete track-agent-start command entry |
| `plugins/vsdd-factory/hooks/hooks.json.linux-arm64` | modify | Delete track-agent-start command entry |
| `plugins/vsdd-factory/hooks/hooks.json.linux-x64` | modify | Delete track-agent-start command entry |
| `plugins/vsdd-factory/hooks/hooks.json.windows-x64` | modify | Delete track-agent-start command entry |
| `plugins/vsdd-factory/hooks/track-agent-start.sh` | delete | Replaced by native WASM crate |

## Changelog

| Version | Date | Author | Summary |
|---------|------|--------|---------|
| 1.0 | 2026-04-30 | story-writer | Initial draft. Single-hook port story: track-agent-start (PreToolUse:Agent, on_error=continue, 3 pts). BC anchors: BC-7.03.079/080 confirmed via BC-INDEX grep (lines 1606-1607). 6 ACs covering WASM crate creation, Agent tool_name guard, agent.start emission with subagent field, story_id two-pattern extraction cascade, bats parity tests, best-effort error handling. Pairs with S-8.03 (track-agent-stop). exec_subprocess block removed entirely post-migration. bin/emit-event → host::emit_event. Status: draft. |
