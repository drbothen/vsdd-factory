---
document_type: story
level: ops
story_id: "S-9.00"
epic_id: "E-9"
version: "1.1"
status: draft
producer: story-writer
timestamp: 2026-05-03T00:00:00Z
phase: 2
inputs:
  - .factory/stories/epics/E-9-tier-2-native-wasm-migration.md
  - .factory/specs/architecture/decisions/ADR-014-tier-2-native-wasm-migration.md
  - .factory/measurements/E-8-bash-baseline.json
  - plugins/vsdd-factory/hooks-registry.toml
input-hash: "[pending-recompute]"
traces_to: .factory/stories/epics/E-9-tier-2-native-wasm-migration.md
functional_requirements: []
cycle: v1.0-brownfield-backfill
points: "TBD"
depends_on: ["E-8"]
# depends_on E-8 (W-15 / rc.4 closure) — baseline measurement requires post-rc.4
# release artifacts. S-9.30 withdrawn (ADR-014 D-9.2 amendment 2026-05-03).
blocks: ["S-9.01", "S-9.02", "S-9.03", "S-9.04", "S-9.05", "S-9.06", "S-9.07"]
behavioral_contracts: []
# BC status: pending — S-9.00 is a pre-work + baseline story, analogous to S-8.00.
# Per E-9 D-9.4 (Option C reuse-existing-BC strategy), S-9.00 does not PORT a hook —
# it MEASURES bundle size and ESTABLISHES a W-16 growth ceiling. Outputs (baseline
# data file, architecture doc) feed S-9.01..S-9.07 but are not themselves contracted
# by a BC. [process-gap] anchor disclosure: CAP-022 + SS-02 + SS-04 subsystem anchors
# are the available semantic grounding for this pre-work story. behavioral_contracts
# remains [] at v1.0 draft; same rationale as S-8.00.
verification_properties: []
priority: "P2"
wave: 16
# [process-gap] Wave 16 is provisional (E-9 D-9.3). Final wave assignment is
# state-manager's responsibility at wave-scheduling time. `wave: 16` is the
# planning value.
target_module: ".factory/measurements/ + .factory/architecture/perf-baseline-w16.md"
subsystems: ["SS-02", "SS-04"]
capabilities: ["CAP-022"]
estimated_days: 1
assumption_validations: ["OQ-1"]
risk_mitigations: ["R-W16-003"]
---

<!-- input-hash convention: short-SHA of the most recent commit on factory-artifacts
that touched .factory/stories/epics/E-9-tier-2-native-wasm-migration.md as of S-9.00
authoring. Verify via:
  git log -1 --format=%h .factory/stories/epics/E-9-tier-2-native-wasm-migration.md
Re-run on each story version bump and update this field accordingly. -->

<!-- [process-gap] behavioral_contracts: [] is intentional for this pre-work + baseline
story. S-9.00 MEASURES bundle sizes and ESTABLISHES a ceiling — it does not PORT a
hook. Per E-9 D-9.4 (Option C, analogous to E-8 D-2 Option C), behavioral_contracts
remains [] at v1.0 draft. Wave 7 F-204 cross-wave complementary anchor pattern
sanctioned. POLICY 8 (BC array propagates to body+ACs) is satisfied via [process-gap]
entries in the AC table below. -->

# S-9.00: Perf baseline + W-16 bundle growth ceiling

**Epic:** E-9 — Tier 2 Native WASM Migration (W-16)
**Phase:** Phase 2 — Story Decomposition (W-16 pre-work entry point)
**Milestone:** `v1.2`
**Tier:** Pre-work (W-16 entry point)
**Blocks:** S-9.01, S-9.02, S-9.03, S-9.04, S-9.05, S-9.06, S-9.07
**Blocked by:** E-8 (W-15 / rc.4 closure required for post-rc.4 release artifacts; develop@d49f33b)
**Estimated effort:** S-M (1 day, TBD pts)

## W-16 Lessons Applied Up-Front

> Applied before adversarial pass-1 to compress convergence cost (W-15 / S-8.00 pattern).

- **No BC anchor is correct for this story:** S-9.00 is pure pre-work (measurement +
  ceiling establishment). Forcing a BC anchor would be semantic drift (POLICY 4).
  [process-gap] disclosure follows the S-8.00 precedent exactly.
- **Wave-ID provisional disclosure required:** E-9 D-9.3 notes W-16 wave IDs are
  provisional. Added `wave: 16` with [process-gap] comment explaining provisional status.
- **Measurement path may need creation:** `.factory/architecture/perf-baseline-w16.md`
  does not exist yet. File Structure Requirements enumerates the creation step.
- **S-8.00 baseline is the reference:** The post-rc.4 bundle baseline (after 12 Tier 1
  ports shipped) is the starting point for the W-16 ceiling. Read
  `.factory/measurements/E-8-bash-baseline.json` for the existing per-plugin Tier 2
  median (19ms, measured 2026-05-02).
- **Cold-start latency measurement IS needed (v1.1 update):** ADR-014 R-8.09 revised
  (Amendment 2026-05-03) makes cold-start p95 ≤ 500ms the PRIMARY gate. S-9.00 must
  measure cold-start latency once (using `hyperfine --warmup 0`) to establish the W-16
  Wave 0 baseline. Warm-invocation data inherited from S-8.00 (19ms/plugin, 437ms
  aggregate) remains valid for the advisory ceiling model.
- **Sibling consistency:** All 7 batch stories (S-9.01..S-9.07) list S-9.00 in their
  `depends_on:` field. S-9.00 lists each in `blocks:`. S-9.30 withdrawn 2026-05-03;
  S-9.00 is no longer the "only E-9 story with depends_on: []" — it now has
  `depends_on: ["E-8"]` for baseline measurement on rc.4 artifacts.

## Capability Anchor Justification

**Primary anchor:** CAP-022 ("Port hook plugins from bash to native WASM") per
`domain-spec/capabilities.md` §CAP-022. S-9.00 is the entry-point pre-work story for
E-9's W-16 porting wave. The bundle-size baseline (Responsibility A) and ceiling
establishment (Responsibility B) are both enabling work for the CAP-022 migration.
Without S-9.00's outputs, S-9.01..S-9.07 cannot confirm their per-plugin bundle-size
compliance or budget against R-W16-003.

**Stretch-Anchor Disclosure:** S-9.00 anchors to CAP-022 but spans two subsystems:
- **SS-04 Plugin Ecosystem** owns the 23 Tier 2 hooks being ported. The bundle-size
  measurement directly touches SS-04's `crates/hook-plugins/` artifact surface. The
  per-plugin size baseline is an SS-04 concern.
- **SS-02 Hook SDK and Plugin ABI** is affected if any Tier 2 batch story's bundle
  size growth requires SDK-level optimization. S-9.30 (host::run_subprocess) adds to
  the SDK surface; its bundle contribution is measured by S-9.00.

Both subsystem anchors are verified against ARCH-INDEX Subsystem Registry:
- SS-02 Hook SDK and Plugin ABI: canonical name confirmed.
- SS-04 Plugin Ecosystem: canonical name confirmed.
(Per POLICY 5 anchor justification requirement + POLICY 6 architecture-is-source-of-truth.)

**CAP-022 cross-CAP stretch disclosure:** Same rationale as S-8.00: S-9.00 is enabling
work for CAP-022 (benchmarking + ceiling establishment for the hooks that will be
ported), not an instance of CAP-022 itself. CAP-022's declared subsystems remain the
source of truth for actual port stories (S-9.01..S-9.07).

## Behavioral Contracts

<!-- [process-gap] S-9.00 is a pre-work + baseline story; no BC currently contracts
its deliverables. Same justification as S-8.00. Per E-9 D-9.4, behavioral_contracts:
[] is correct for this story at v1.0. -->

| BC ID | Title | Trace |
|-------|-------|-------|
| (none) | S-9.00 has no formal BC anchor. See [process-gap] disclosure above. | — |

## Verification Properties

| VP ID | Purpose |
|-------|---------|
| (none assigned) | S-9.00 produces measurement data and a ceiling doc, not a testable behavioral invariant. |

## Narrative

- **As a** W-16 wave gatekeeper preparing to dispatch 7 batch stories
- **I want to** know (a) the current WASM bundle sizes for all hook plugins post-rc.4,
  (b) a per-plugin bundle-size delta budget for W-16 that keeps total growth within
  R-W16-003, and (c) a reference measurement script committed for reproducibility
- **So that** S-9.01..S-9.07 each have an unambiguous per-plugin bundle size budget
  and a concrete measurement baseline to validate against at delivery time

## Goal

Establish the two enabling artifacts that all 7 batch stories (S-9.01..S-9.07)
depend on:

1. **Responsibility A (Bundle-size baseline):** Measure the current WASM plugin bundle
   sizes for the post-rc.4 release artifacts (after all 12 Tier 1 WASM ports from W-15
   are merged). Capture total bundle size (dispatcher binary + all hook-plugins/*.wasm)
   and per-plugin sizes. Commit results to `.factory/architecture/perf-baseline-w16.md`
   and update `.factory/measurements/E-8-bash-baseline.json` (or create a parallel
   `W-16-bundle-baseline.json`) with the W-16 starting point.

2. **Responsibility B (W-16 ceiling establishment):** Based on the bundle-size and
   cold-start latency measurements, establish the W-16 gate model per ADR-014 R-8.09
   revised (Amendment 2026-05-03):
   - **Primary gate (HARD):** cold-start p95 ≤ 500ms (inherited from S-8.00 / E-8 AC-7b).
   - **Advisory soft cap:** cumulative bundle growth ≤ 100% vs pre-W-15 baseline at
     end of W-17 (~14MB target). Document the post-rc.4 total as the W-16 starting point.
   - **Hard kill-switch:** 30MB cumulative bundle; crossing requires architecture review.
   - **Required per-wave telemetry:** `(bundle_size_delta_bytes, cold_start_p95_delta_ms)`.
   - **Pause criterion:** wave paused if cold-start regresses >10% vs previous wave.
   The revised model REPLACES E-8 R-8.09's simple 25% bundle-only ceiling. Original
   25% ceiling would have blocked W-16 even when cold-start budget is satisfied.

Both responsibilities are in this single story (not splittable; mirrors S-8.00 D-8).

## Acceptance Criteria with BC Traces

| AC | Statement | BC Trace |
|----|-----------|----------|
| AC-1 | Current total WASM bundle size measured for post-rc.4 release artifacts: use `wc -c < <file>` (portable) or `stat -c %s <file>` (Linux) for per-file byte counts on the release bundle directory at `develop` HEAD (post-W-15 merge, commit d49f33b or later). Record: (a) `dispatcher_binary_bytes` (factory-dispatcher binary for each CI platform), (b) `all_hook_plugins_wasm_bytes` (aggregate of all .wasm files in the bundle), (c) `per_plugin_bytes` map (one entry per plugin file, format: `{"plugin_name": <bytes>}`), (d) `total_bytes` aggregate. Measurement runs on all 5 CI platforms: darwin-arm64, darwin-x64, linux-x64, linux-arm64, windows-x64. Record both `release_tag_sha` (v1.0.0-rc.4 tag SHA) and `develop_head_sha` (HEAD at measurement time). | [process-gap] (bundle baseline measurement; R-W16-003 mitigation; analogous to S-8.00 AC-7) |
| AC-2 | Per-plugin bundle-size data captured in JSON format: for each of the 12 Tier 1 WASM plugins shipped in W-15 (legacy-bash-adapter, capture-commit-activity, capture-pr-activity, block-ai-attribution, handoff-validator, pr-manager-completion-guard, track-agent-stop, update-wave-state-on-merge, session-learning, warn-pending-wave-gate, track-agent-start, regression-gate) — plus any additional plugins present — record `wasm_bytes` in the baseline data file as a JSON array: `[{"plugin_name": "...", "wasm_bytes": <N>}, ...]` with a `total_bytes` aggregate field. | [process-gap] (per-plugin baseline; R-W16-003 mitigation) |
| AC-3 | W-16 latency-primary and advisory bundle ceiling established per ADR-014 R-8.09 revised model (Amendment 2026-05-03): (a) Primary gate (HARD): cold-start p95 ≤ 500ms — inherited from S-8.00 / E-8 AC-7b; measure using `hyperfine` with `--warmup 0` to capture cold-start latency. (b) Advisory soft cap: cumulative bundle growth ≤ 100% vs pre-W-15 baseline at end of W-17 (~14MB target); document current post-rc.4 total as the W-16 starting point. (c) Hard kill-switch: 30MB cumulative bundle — if reached, fresh architecture review required. (d) Required telemetry per wave: `(bundle_size_delta_bytes, cold_start_p95_delta_ms)`. (e) Pause criterion: wave paused if cold-start regresses >10% vs previous wave. Commit all values + formulas to `perf-baseline-w16.md` with source citations. (Replaces original 25% bundle-only ceiling per ADR-014 R-8.09 amendment; resolves E-9 OQ-1.) | [process-gap] (ceiling model; ADR-014 R-8.09 revised; E-9 AC-3) |
| AC-4 | Measurement script committed: a shell script that reproduces the AC-1 measurement from the release bundle artifacts is committed to `.factory/measurements/measure-bundle-sizes.sh`. The script must: (a) accept a bundle directory as `$1`, (b) emit output as JSON with `{"plugin_name": <str>, "wasm_bytes": <N>}` array and `{"total_bytes": <N>}` aggregate, (c) use `wc -c < <file>` for byte counts (portable across macOS BSD and Linux GNU), NOT `du -sb` (GNU-only `-b` flag). Script is `#!/usr/bin/env bash` with `set -euo pipefail`. Example expected output format: `# [{"plugin_name": "legacy-bash-adapter.wasm", "wasm_bytes": 123456}, ..., {"total_bytes": 987654}]` | [process-gap] (measurement reproducibility; tooling for future waves) |
| AC-5 | Running `.factory/measurements/measure-bundle-sizes.sh <release-bundle-dir>` (where `<release-bundle-dir>` contains the .wasm files measured in AC-1) reproduces the per-plugin sizes and total recorded in `perf-baseline-w16.md` within ±0 bytes for the same fixed bundle dir. If there is a documented divergence (e.g., platform-specific build variation), document the reason explicitly in `perf-baseline-w16.md` notes. This AC prevents hand-written numbers (POLICY 11 — no test tautologies). | [process-gap] (measurement reproducibility; POLICY 11 anti-tautology) |
| AC-6 | Baseline data file committed to canonical path: the measurement results from AC-1 + AC-2 are committed to `.factory/architecture/perf-baseline-w16.md` with the schema defined in Tasks §A.4. The file includes: `measured_at`, `release_tag_sha`, `develop_head_sha`, `platform`, `total_wasm_bytes`, `per_plugin_bytes` JSON array, `w16_cold_start_p95_ms`, `w16_advisory_bundle_soft_cap_bytes`, `w16_bundle_hard_killswitch_bytes`, `per_wave_telemetry_fields`, and a `notes` field. | [process-gap] (artifact persistence; analogous to S-8.00 AC-8) |
| AC-7 | S-8.00 latency baseline re-confirmed (no re-measurement required for warm-invocation): S-8.00 measured 19ms/plugin warm-invocation for Tier 2 representative (validate-bc-title.sh); aggregate 437ms. S-9.00 documents that W-16 cold-start p95 ≤ 500ms gate is INHERITED from S-8.00 / E-8 AC-7b. Cold-start latency is measured once at W-16 Wave 0 using `hyperfine --warmup 0` to capture the first-invocation cost. Source file path: `perf-baseline-w16.md` §"Latency Baseline" section cites S-8.00 PR #47 (develop@9e649ed) as the warm-invocation source. | [process-gap] (latency baseline continuity; E-9 AC-7b equivalence) |
| AC-8 | New plugin ceiling policy for W-16 additions: each of the 23 new `validate-*.wasm` plugins added in W-16 MUST be ≤ the median existing plugin size from AC-2 × 3 (upper-bound heuristic) OR justified in `perf-baseline-w16.md` notes. This prevents trivially satisfying the ceiling with a single oversized plugin. | [process-gap] (new-plugin ceiling; F-6 resolution) |
| AC-9 | All output artifacts committed to their canonical paths: `.factory/architecture/perf-baseline-w16.md` (baseline + ceiling) and `.factory/measurements/measure-bundle-sizes.sh` (measurement script). State-manager commits as a single burst after AC-6 is satisfied. Per-platform measurement data (all 5 platforms) included or documented as CI-only with re-run instructions. | [process-gap] (artifact persistence; POLICY 3 state-manager-runs-last) |
| AC-10 | ADR-013 convergence requirement noted: adversarial convergence (3 consecutive NITPICK_ONLY passes per ADR-013) required before S-9.00 is implementation-ready and before S-9.01..S-9.07 may be dispatched for implementation. | [process-gap] (process gate) |

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| `.factory/architecture/perf-baseline-w16.md` | `.factory/architecture/` (new file) | Pure (data + ceiling doc artifact; written once) |
| Measurement script | `.factory/measurements/measure-bundle-sizes.sh` (new file) | Effectful (shell invocation; `du -sb` per plugin) |
| `plugins/vsdd-factory/hook-plugins/` (read for measurement) | `plugins/vsdd-factory/hook-plugins/` | Pure (read-only; not modified by S-9.00) |
| `factory-dispatcher` binary (read for size) | CI release artifacts | Pure (read-only measurement) |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | Plugin bundle directory has only legacy-bash-adapter.wasm + Tier 1 plugins but not all 12 (partial W-15 merge state) | Record whichever plugins are present; note which are missing; S-9.00 does not gate on exactly 12 plugins — it documents the state at measure time |
| EC-002 | Release artifacts not available locally (CI-only artifacts) | Document that measurement was performed on CI runner; record the SHA of the develop HEAD measured; provide script that can re-run against a locally built release bundle |
| EC-003 | Per-plugin sizes vary significantly across platforms | Record per-platform values for all 5 platforms (darwin-arm64, darwin-x64, linux-x64, linux-arm64, windows-x64); propose ceiling using the platform with the LARGEST aggregate (conservative ceiling); note the platform variance in `perf-baseline-w16.md` |
| EC-004 | W-16 ceiling computation produces a per-plugin budget that is 0 or negative (bundle already over ceiling) | Flag as R-W16-003 triggered; note that the ceiling must be re-negotiated before S-9.01..S-9.07 dispatch; escalate to orchestrator |
| EC-005 | `.factory/architecture/` directory exists but perf-baseline-w16.md does not | Create the file; do NOT overwrite any existing architecture doc |
| EC-006 | S-8.00 baseline data at `.factory/measurements/E-8-bash-baseline.json` is absent or corrupted | Re-read S-8.00 story spec for the latency figures; document the inherited values verbatim from S-8.00 AC-2 (19ms/plugin, 437ms aggregate, 500ms p95 ceiling) |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| Measurement script | Effectful | Invokes `du -sb` with filesystem state; result depends on release build artifacts |
| perf-baseline-w16.md | Pure (data) | Written once per measurement run; content is deterministic for a given release artifact set |

## Token Budget Estimate

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec (v1.1) | ~3,200 |
| E-9 epic spec v1.1 (referenced sections) | ~2,500 |
| S-8.00 (reference for methodology) | ~3,000 |
| `.factory/measurements/E-8-bash-baseline.json` (existing data) | ~300 |
| ADR-014 (relevant sections + R-8.09 amendment) | ~1,500 |
| Tool outputs (`wc -c`, `hyperfine`, measurement script output) | ~600 |
| BC files (0 BCs in behavioral_contracts) | 0 |
| **Total** | **~11,100** |
| Agent context window | 200K for Sonnet |
| **Budget usage** | **~5.6%** |

Well within the 20-30% agent context window limit. No split needed.

## Tasks

### Responsibility A — Bundle-Size Baseline Measurement

- [ ] A.1: Read `.factory/measurements/E-8-bash-baseline.json` to extract the existing
       `bundle_size` section (post-W-15 baseline established by S-8.00). Note the
       `all_hook_plugins_wasm_bytes` and `legacy_bash_adapter_wasm_bytes` fields.
- [ ] A.2: Locate the release bundle directory containing post-rc.4 `.wasm` artifacts
       (expected: `plugins/vsdd-factory/hook-plugins/*.wasm` in the repo; or CI
       release artifacts from `v1.0.0-rc.4` tag). Document whether measurement uses
       dispatcher binary path (`plugins/vsdd-factory/hook-plugins/`) or raw `.wasm`
       files from build output; record this in `perf-baseline-w16.md` §"Measurement
       Path" to avoid F-5 ambiguity in future waves.
- [ ] A.3: Measure each .wasm file's byte count using `wc -c < <file>` (portable;
       works on macOS BSD and Linux GNU). Do NOT use `du -sb` (GNU-only `-b` flag;
       macOS `du` uses `-k` for kibibytes — cross-platform inconsistency). Record
       each plugin's byte count across all 5 CI platforms: darwin-arm64, darwin-x64,
       linux-x64, linux-arm64, windows-x64. Use `stat -c %s <file>` as alternative
       on Linux. Also measure `factory-dispatcher` binary for each platform.
       # Expected output format per plugin:
       # {"plugin_name": "legacy-bash-adapter.wasm", "wasm_bytes": 123456}
- [ ] A.4: Write the per-plugin size map to `.factory/architecture/perf-baseline-w16.md`
       with schema:
       ```markdown
       ## W-16 Bundle Baseline (post-rc.4, pre-Tier 2)

       | Metric | Value |
       |--------|-------|
       | measured_at | <ISO-8601> |
       | release_tag_sha | <7-char SHA of v1.0.0-rc.4 tag> |
       | develop_head_sha | <7-char SHA of develop HEAD at measurement> |
       | platform | <e.g., darwin-arm64> (one row per platform) |
       | all_hook_plugins_wasm_bytes | <N> |
       | dispatcher_binary_bytes | <N> |

       ### Per-Plugin Sizes

       ```json
       [
         {"plugin_name": "legacy-bash-adapter.wasm", "wasm_bytes": <N>},
         {"plugin_name": "capture-commit-activity.wasm", "wasm_bytes": <N>},
         ...
         {"total_bytes": <N>}
       ]
       ```

       ## W-16 Gate Model (ADR-014 R-8.09 revised)

       | Field | Value |
       |-------|-------|
       | cold_start_p95_gate_ms | 500 (HARD gate; inherited from E-8 AC-7b) |
       | advisory_soft_cap_bytes | <post-rc.4 total × 2 (~14MB at end of W-17)> |
       | hard_killswitch_bytes | 30_000_000 (30MB; requires architecture review if hit) |
       | per_wave_telemetry_fields | bundle_size_delta_bytes, cold_start_p95_delta_ms |
       | pause_criterion | cold-start regresses >10% vs previous wave |
       | notes | <rationale; replaces E-8 R-8.09 25% bundle-only ceiling> |

       ## Latency Baseline

       | Metric | Value | Source |
       |--------|-------|--------|
       | warm_invocation_p50_ms | 19 | S-8.00 PR #47 develop@9e649ed |
       | aggregate_437ms_projection | 19ms × 23 plugins | S-8.00 AC-7b |
       | cold_start_p95_gate_ms | 500 | E-8 AC-7b (raised from 200ms) |
       | cold_start_p95_measured_ms | <N> | measured by S-9.00 hyperfine --warmup 0 |
       ```
- [ ] A.5: Write the measurement script to `.factory/measurements/measure-bundle-sizes.sh`.
       Script accepts bundle directory as `$1`; emits JSON per-plugin byte counts plus
       aggregate. Uses `wc -c < <file>` for portable byte counts (NOT `du -sb` —
       GNU-only; NOT `du -k` — kibibytes not bytes). Script must be `#!/usr/bin/env bash`
       with `set -euo pipefail`. Must run on macOS (BSD tools) and Linux (GNU tools)
       without modification. Windows: document that `wc -c` is available in Git Bash
       and WSL; native PowerShell alternative: `(Get-Item <file>).Length`.

### Responsibility B — W-16 Gate Model Establishment (ADR-014 R-8.09 revised)

- [ ] B.1: Measure cold-start p95 latency using `hyperfine --warmup 0 --runs 10` on
       a representative existing .wasm plugin (e.g., `legacy-bash-adapter.wasm`) to
       capture the first-invocation cost. Record as `cold_start_p95_measured_ms` in
       `perf-baseline-w16.md`. Confirm ≤ 500ms gate is satisfied at W-16 baseline.
       If cold-start exceeds 500ms at baseline, flag as R-W16-003 triggered (EC-004
       variant) and escalate to orchestrator before dispatching S-9.01..S-9.07.
- [ ] B.2: Establish the advisory soft cap and hard kill-switch per ADR-014 R-8.09
       revised model: (a) advisory soft cap = post-rc.4 total × 2 (aiming for ≤14MB
       cumulative at end of W-17); (b) hard kill-switch = 30MB (requires architecture
       review if crossed); (c) per-wave telemetry fields = `bundle_size_delta_bytes`
       and `cold_start_p95_delta_ms`; (d) pause criterion = cold-start regresses >10%
       vs previous wave. Document all values + formulas in `perf-baseline-w16.md`.
       Note: the old E-8 R-8.09 25% bundle-only ceiling is SUPERSEDED by this model.
- [ ] B.3: Confirm that E-8 epic AC-7b 500ms p95 cold-start ceiling is inherited
       unchanged for W-16. Document in `perf-baseline-w16.md` under "Latency Baseline"
       section with explicit citation: "Source: S-8.00 PR #47 develop@9e649ed; warm
       p50 = 19ms/plugin; aggregate 437ms; cold-start p95 gate 500ms per E-8 AC-7b."

### Artifact Commit

- [ ] C.1: All output artifacts committed to canonical paths:
       `.factory/architecture/perf-baseline-w16.md`,
       `.factory/measurements/measure-bundle-sizes.sh`.
       State-manager commits per POLICY 3 (state_manager_runs_last). No artifacts
       left uncommitted or under `.scratch/`.

## Previous Story Intelligence

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-8.00 (W-15 pre-work) | behavioral_contracts: [] valid for pre-work story; [process-gap] anchor via F-204 cross-wave-complementary methodology; E-8-bash-baseline.json schema with bundle_size section | Wave ID provisional disclosure; measurement path creation; bats + hyperfine harness for latency | EC-008: bundle dir may be empty at S-8.00 time; same risk applies here — document that legacy-bash-adapter may be the only .wasm at some dev states |
| E-8 D-2 + D-8 | BC anchor is Option C; single story for pre-work is not splittable | Option C reuse strategy reduces BC authoring overhead | Do NOT force BC anchor for a measurement story |
| S-9.30 (withdrawn) | D-9.2 amendment 2026-05-03: exec_subprocess sufficient; S-9.30 withdrawn | No SDK bundle size contribution from run_subprocess; per-plugin budget unaffected by SDK overhead | S-9.07 uses existing exec_subprocess — no new SDK symbols to measure |

## Architecture Compliance Rules

| Rule | Source | Enforcement |
|------|--------|-------------|
| HOST_ABI_VERSION = 1 unchanged throughout E-9 | E-9 Goal #4; BC-2.01.003; ADR-014 | S-9.00 does not touch ABI; measurement-only story |
| BC-anchor strategy is Option C — reuse existing BCs; no new BC family | E-9 D-9.4 | behavioral_contracts: [] is intentional; [process-gap] disclosure is the correct implementation |
| POLICY 3 state-manager-runs-last | POLICY 3 | All .factory/ writes committed by state-manager in a single burst |
| No .sh files deleted | E-9 D-9.5; R-W16-001 | S-9.00 reads hook registry + measures bundle artifacts; does not modify any .sh or hooks-registry.toml entry |
| Measurement must use release artifacts, not debug artifacts | R-W16-003 | `wc -c` on release-profile `.wasm` files; document the build profile used |
| S-9.30 withdrawn; no run_subprocess scope | ADR-014 D-9.2 amendment 2026-05-03 | S-9.00 does not measure SDK bundle contribution from run_subprocess (story withdrawn) |
| Ceiling model: latency-primary (ADR-014 R-8.09 revised) | ADR-014 Amendment 2026-05-03 | Primary gate is cold-start p95 ≤ 500ms; bundle-size is advisory only |

## Library & Framework Requirements

| Tool | Version | Purpose |
|------|---------|---------|
| bash | >=5.0 (existing) | Measurement script runtime |
| wc | system (POSIX) | Byte-count measurement: `wc -c < <file>` portable across macOS BSD and Linux GNU. Do NOT use `du -sb` (GNU-only `-b` flag, macOS uses `-k` for kibibytes). Alternative: `stat -c %s` on Linux. |
| hyperfine | >=1.18 | Cold-start p95 latency measurement (`hyperfine --warmup 0 --runs 10`) for Responsibility B |
| jq | >=1.6 (existing) | Optional: pretty-printing existing `E-8-bash-baseline.json` for comparison |

> No new Rust, WASM, or test framework dependencies introduced in S-9.00.
> This is a pure measurement story using existing shell tooling.

## File Structure Requirements

| File | Action | Purpose |
|------|--------|---------|
| `.factory/architecture/perf-baseline-w16.md` | create | W-16 bundle baseline doc + ceiling table; canonical reference for S-9.01..S-9.07 per-plugin budgets |
| `.factory/measurements/measure-bundle-sizes.sh` | create | Reproducible measurement script; accepts bundle dir as $1 |
| `.factory/measurements/E-8-bash-baseline.json` | read-only | Source for existing Tier 1 bundle size data (reference, not modified) |

## Changelog

| Version | Date | Author | Summary |
|---------|------|--------|---------|
| 1.0 | 2026-05-03 | story-writer | Initial draft. W-16 entry-point pre-work story. 7 ACs (AC-1..AC-7). Two responsibilities: Responsibility A (bundle-size baseline post-rc.4) + Responsibility B (W-16 growth ceiling establishment, resolves E-9 OQ-1). Latency re-measurement NOT required; inherits S-8.00 baseline (19ms/plugin, 500ms p95 ceiling). Status draft; behavioral_contracts: [] per [process-gap] disclosure. |
| 1.1 | 2026-05-03 | story-writer | Pass-1 fix burst (12 findings). F-1 (HIGH): 5-platform coverage (darwin-arm64, darwin-x64, linux-x64, linux-arm64, windows-x64) added to AC-1, EC-003, A.3. F-2 (HIGH): ceiling model replaced with ADR-014 R-8.09 revised latency-primary gate (cold-start p95 ≤ 500ms HARD, advisory ≤100% growth, hard kill-switch 30MB); AC-3 + Responsibility B + Task B.1..B.3 updated. F-3 (HIGH/POLICY 11): AC-5 added requiring measure-bundle-sizes.sh execution reproduces perf-baseline-w16.md values (anti-tautology); AC-4 tightened with script-must-run assertion. F-4 (F-5 from S-9.00 pass-1): output format specified as JSON `{"plugin_name": ..., "wasm_bytes": ...}` array. F-5 (rc.4 binaries path): A.2 clarified with measurement path documentation. F-6 (conflicting deliverable paths): resolved — perf-baseline-w16.md is canonical; E-8-bash-baseline.json is read-only reference. F-7 (du portability): `wc -c < <file>` replaces `du -sb`; macOS/Linux divergence documented. F-8 (10_000 threshold arbitrary): threshold removed from B.2; cold-start gate replaces bundle-floor as primary escalation trigger. F-9 (CAP-022 SS-06 disclosure): process-gap noted (S-9.00 is pre-work; SS-06 not primary anchor). F-10 (develop_sha ambiguity): both release_tag_sha + develop_head_sha recorded in AC-1 and schema. F-11 (S-9.07 depends_on): note updated — S-9.30 withdrawn; S-9.07 depends only on S-9.00. F-12 (latency baseline cite): AC-7 cites source file path (PR #47 develop@9e649ed). depends_on updated to [E-8]. AC count 7 → 10. |
