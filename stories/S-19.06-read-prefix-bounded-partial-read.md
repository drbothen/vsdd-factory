---
document_type: story
level: implementation
story_id: S-19.06
epic_id: "E-19"
version: "1.22"
title: "host::read_prefix bounded partial read: head-c semantics, NEVER OUTPUT_TOO_LARGE, additive FFI entry point"
status: ready
producer: story-writer
timestamp: "2026-07-06T00:00:00Z"
last_amended: "2026-07-14 (v1.22) — CONVERGED pass-11 (3-CLEAN passes 9+10+11); status draft→ready; HEAD 7156a4c3. [Prior: 2026-07-14 (v1.21) — F-P2-001 (LOW) §Architecture Mapping reordered: max_bytes=0 short-circuit promoted to step 3 (before existence check), per BC-1.17.001 EC-001 ('The file is not read'; existence not consulted); EC-001 Expected Behavior extended to pin composite case (absent allowlisted file, max_bytes=0 → empty payload, exit 0, NO NOT_FOUND, NO file_not_found); T-012 (EC-001 composite) added to Test Plan + Task list. [Prior: 2026-07-14 (v1.20) — F-P1-001 (MEDIUM) AC-003 gate rewrite: scoped to production code via awk test-module strip, POSIX-portable block-comment strip (awk replaces BSD-incompatible sed label loop), execution site stated (T-009g bats test in read-prefix-wasm.bats). [Prior: 2026-07-10 (v1.19) — E-19 pass-46 F-P46-001 propagation: BC-1.17.001 v1.5→v1.6 cite sweep (frontmatter-ordering-only amendment). [Prior: 2026-07-09 (v1.18) — E-19 pass-37 F-P37-001: Token Budget Total aggregation-cell parity fix (POLICY 5 v1.3.7 category (i)) — Total ~22,500 → ~22,000; adjudication (a): stale inception error present since v1.0 (10-row sum = 22,000 at initial draft bfab9ad7; no dropped row in 17-version history; default-(a) per finding rubric). [Prior: 2026-07-09 (v1.17) — E-19 pass-30 fix burst (story-writer): BC-1.17.001 v1.4→v1.5 cite sweep (metadata-only — L2 Domain Invariants TBD→none; behavioral content unchanged; 10 body sites: Narrative ×1, AC-007 ×4, BC table Version cell ×1, Token Budget ×1, Architecture Compliance Rules ×3) + F-P30-004 (LOW): EC-004 failing-test Task 8 insertion (old Tasks 8–13 renumbered → 9–14). [Prior: 2026-07-08 (v1.16) — E-19 pass-28 fix burst (story-writer): BC-1.17.001-v1.4-propagation (proof-method-cite-only) — v1.3→v1.4 body-scope cite sweep: Narrative ×1, AC-007 ×4, BC table version ×1, Token Budget ×1, Architecture Compliance Rules ×3 = 10 sites total; input-hash refreshed 5af0d9f→a8bb758 (BC-1.17.001.md updated upstream). [Prior: 2026-07-08 (v1.15) — E-19 pass-27 fix burst (story-writer): F-P27-001 story leg — §Depends on S-19.04 prose fixed to DISTINCT-block form (was: 'creates preamble comment block in `hooks-registry.toml`; `capabilities.read_prefix` schema doc extends that section'; now: DISTINCT \"Capability Schemas\" preamble block for `capabilities.read_prefix` schema — separate from, not embedded in, S-19.04's tool-filter-anchoring block; ordering-only dependency so two preamble blocks land without merge conflict). [Prior: 2026-07-08 (v1.14) — E-19 pass-22 fix burst BC-1.17.001-v1.3-propagation (story-writer): BC-1.17.001 v1.2→v1.3 cite propagation (anchoring-only change — ffi.rs bullet added to §Architecture Anchors in BC v1.3); Narrative 'The fix' paragraph, §Behavioral Contracts table Version cell, Token Budget, AC-007 Gate + BC Trace, §Architecture Compliance Rules updated. [Prior: 2026-07-08 (v1.13) — E-19 pass-16 fix burst (story-writer): F-P16-002 inputs: list extended with crates/hook-sdk/src/ffi.rs (5 entries total; pass-15 sibling-propagation miss); input-hash recomputed 03c6f12→617adeb. [Prior: 2026-07-07 (v1.12) — E-19 pass-15 sweep (story-writer): F-P15-001 AC-007 Gate 2 clause (iii) rewritten to awk block-containment form (both wasm32+host_stubs blocks; vacuously-true grep-B1 eliminated; gate execution evidence: current ffi.rs exits 1 = read_prefix absent, /tmp/ffi_fixed.rs exits 0 = read_prefix present in both blocks); F-P15-002 File Structure ffi.rs row added + host.rs row corrected to safe-wrapper description + Task 11 corrected; F-P15-003 Architecture Mapping hook-sdk section corrected (host.rs gets safe wrapper Result<Vec<u8>, HostError>; ffi.rs gets 6-param extern + host_stubs stub); O-P15-05 markdown-escape convention note added near AC-007. [Prior: 2026-07-07 (v1.11) — E-19 pass-14 fix burst (story-writer): F-P14-003 AC-003 gate wrapped for intrinsic exit semantics ([ -z \"$(...)\" ] exits 0 form; mirrors S-19.04 F-P7-010; grep -oE); F-P14-006 Gate 2 clause (i) BSD-portable alternation (grep -cE 'path_len\|out_ptr_out'; -E flag makes \| ERE alternation); O-P14-02 Gate 2 clause (iii) tightened to grep -B1 -E 'fn read_prefix\(' | grep -q 'cfg(not(target_arch = \"wasm32\"))' (asserts cfg gates read_prefix itself). [Prior: 2026-07-07 (v1.10) — E-19 pass-13 fix burst (story-writer): F-P13-002 AC-007 Gate 1 tightened to full function signature (pub fn read_prefix(path: &str, max_bytes: u32, timeout_ms: u32) -> Result<Vec<u8>, HostError>; rejects bare -> i32 wrapper); F-P13-003 AC-007 Gate 2 expanded to three parallel clauses (i: pub safe fn + awk 6-param shape; ii: #[link(wasm_import_module = \"vsdd\")] attribute; iii: cfg(not(wasm32)) stub). [Prior: 2026-07-07 (v1.9) — E-19 pass-12 fix burst (story-writer): BC-cite drift: Narrative line 68 BC-1.17.001 v1.1→v1.2 (stale body-scope cite in 'The fix' paragraph; caught by standing preflight matrix). [Prior: 2026-07-07 (v1.8) — E-19 pass-12 fix burst (story-writer): F-P12-002 AC-007 replaced with three-gate formulation (Gate 1 grep -qE 'pub fn read_prefix\(path: &str' hook-sdk/src/host.rs; Gate 2 grep -qE 'fn read_prefix\(path_ptr' hook-sdk/src/ffi.rs; Gate 3 dispatcher dispatch-table cite); signature-disambiguation note added to AC-007 + Architecture Compliance Rules (BC-1.17.001 v1.2 layering parenthetical: Result<Vec<u8>, HostError> safe wrapper vs -> i32 FFI wire ABI); BC table v1.1→v1.2; O-P12-04 Task 12 + File Structure + Architecture Mapping + Previous Story Intel S-19.04 row updated (DISTINCT Capability Schemas preamble block, not inside S-19.04's tool-filter-anchoring block). [Prior: 2026-07-07 (v1.7) — E-19 pass-11 fix burst (story-writer): F-P11-006 Previous Story Intel S-19.04 Gotchas Discovered — bolded DEFERRAL GATE added (MUST NOT begin until S-19.04 PR MERGED; verify via git log Merge-PR pattern; mirrors S-19.03 row structure). [Prior: 2026-07-07 (v1.6) — E-19 pass-10 fix burst (story-writer): F-P10-005 deferral gate tightened — `git log develop --oneline \| grep S-19.03` → `git log develop --oneline \| grep -E "Merge pull request .* (from )?(feature/)?S-19\.03"` (Merge-PR pattern; Previous Story Intel S-19.03 Gotchas row); F-P10-007 Architecture Compliance Rules Signature row — `(not ADR-025 D-15)` → `(normative); ADR-025 D-15 concurring`. [Prior: 2026-07-07 (v1.5) — E-19 pass-9 fix burst (story-writer): F-P9-003 AC-003 gate extended with block-comment stripper — sed -E ':a; s:/\*[^*]*\*+([^/*][^*]*\*+)*/::; ta' chains before the existing sed 's://.*::' to handle /* ... */ block comments that evade the line-comment-only filter; T-004 noted as runtime load-bearing check. [Prior: 2026-07-07 (v1.4) — E-19 pass-8 fix burst (story-writer): F-P8-005 AC-003 gate updated to strip trailing comments via sed before grep (both leading-comment lines and trailing inline comments stripped; OUTPUT_TOO_LARGE must not appear in non-comment code). [Prior: 2026-07-07 (v1.3) — E-19 pass-7 fix burst (story-writer): F-P7-003 Narrative line 72 stale ADR narrative corrected — ADR-025 Decision 15 u64/i64 claim replaced with reconciled fact (ADR and BC agree on u32/i32 after pass-3 amendment; BC-1.17.001 remains normative); F-P7-011 AC-003 gate scoped to non-comment code (grep -vE to strip comments before searching OUTPUT_TOO_LARGE); O-P7-005 Task 1 PR-description checklist sentence. [Prior: 2026-07-07 (v1.2) — E-19 pass-4 fix burst (story-writer): F-P4-002 BC-1.17.001 v1.0→v1.1 (narrative, BC table, Token Budget = 3 sites); blocks [] → [S-19.07]; T-010 added (EC-004 path_allow mismatch → CAPABILITY_DENIED). [Prior: 2026-07-06 (v1.1) — E-19 pass-3 fix burst (story-writer): F-P3-005: subsystems: SS-02 added → [SS-01, SS-02] (hook-sdk wrapper is SS-02 boundary). F-P3-010: depends_on: [S-19.03] → [S-19.03, S-19.04] (S-19.04 stabilizes hooks-registry.toml preamble that S-19.06 extends with capabilities.read_prefix schema). F-P3-020: Purity Classification codes.rs row corrected to `pub mod codes` block in host/mod.rs (not standalone codes.rs). O-P3-001: note added — this story DEFERS until S-19.03 PR MERGES to develop (not just story completion). O-P3-007: AC-007 grep tightened to grep -qE 'fn read_prefix\\(' crates/hook-sdk/src/host.rs. O-P3-006: F-P2-012 baseline task updated to emit machine-parseable marker. Body Depends-on line updated. [Prior: (v1.0) — initial draft (story-writer): E-19 pass-2 fix burst — new story: host::read_prefix bounded partial read. Contract: BC-1.17.001 v1.0. Scope per ADR-025 Decision 15: new FFI entry point, host impl, capabilities.read_prefix registry capability, hook-sdk wrapper; read_file semantics UNCHANGED; absent file returns NOT_FOUND (-5) consistent with BC-2.07.001. Wave W2, depends_on: [S-19.03] (path_util.rs shared module). POLICY 8 full treatment: BC table + per-AC BC-trace column + Token Budget.]]"
phase: F3
cycle: v1.0-feature-engine-discipline-pass-1
inputs:
  - crates/factory-dispatcher/src/host/read_file.rs
  - crates/hook-sdk/src/host.rs
  - crates/hook-sdk/src/ffi.rs
  - plugins/vsdd-factory/hooks-registry.toml
  - .factory/specs/behavioral-contracts/ss-01/BC-1.17.001.md
input-hash: "e6c23de"
traces_to:
  - .factory/stories/epics/E-19-post-rc22-operator-hardening.md
wave: 2
priority: P2
points: 8
track: feature
effort_class: multi-day
depends_on: [S-19.03, S-19.04]
blocks: [S-19.07]
behavioral_contracts: [BC-1.17.001]
verification_properties: [VP-101]
domain_invariants: []
tdd_mode: strict
target_module: "crates/factory-dispatcher/src/host"
subsystems:
  - "SS-01"
  - "SS-02"
estimated_days: 2.0
assumption_validations: []
risk_mitigations: []
anchored_adrs: [ADR-025]
closes: []
feature: "post-rc.22-smoke"
modified:
  - "2026-07-14 v1.22: CONVERGED pass-11, 3-CLEAN passes 9+10+11, status draft→ready; s-19.06-local-adversary-pass-11.md created"
  - "2026-07-14 v1.21: F-P2-001 (LOW) §Architecture Mapping reordered — max_bytes=0 short-circuit promoted to step 3 (before existence check) per BC-1.17.001 EC-001 ('file is not read'; existence not consulted); EC-001 Expected Behavior extended: composite case (absent allowlisted file, max_bytes=0 → empty, exit 0, NO NOT_FOUND, NO file_not_found); T-012 (EC-001 composite) added to Test Plan + Task list"
  - "2026-07-14 v1.20: F-P1-001 (MEDIUM) AC-003 gate rewrite — awk test-module strip (production-only scope), POSIX-portable block-comment strip (awk replaces BSD-incompatible sed label loop), execution site T-009g in read-prefix-wasm.bats; T-009g row added to Test Plan"
  - "2026-07-10 v1.19: E-19 pass-46 F-P46-001 propagation: BC-1.17.001 v1.5→v1.6 cite sweep (frontmatter-ordering-only amendment)"
  - "2026-07-09 v1.18: F-P37-001 (MEDIUM) Token Budget Total aggregation-cell parity fix — Total ~22,500 → ~22,000; adjudication (a): 10-row sum = 22,000 at initial draft (bfab9ad7); no dropped row in 17-version history; stale inception error"
  - "2026-07-09 v1.17: E-19 pass-30 BC-1.17.001 v1.4→v1.5 cite sweep (metadata-only — L2 Domain Invariants TBD→none; behavioral content unchanged) + F-P30-004 (LOW): EC-004 failing-test Task 8 insertion (old Tasks 8–13 renumbered → 9–14)"
  - "2026-07-08 v1.16: BC-1.17.001-v1.4-propagation — BC-1.17.001 v1.3→v1.4 body-scope cite sweep (proof-method-cite-only): Narrative ×1, AC-007 ×4, BC table version cell ×1, Token Budget ×1, Architecture Compliance Rules ×3 = 10 sites total; input-hash 5af0d9f→a8bb758 (BC-1.17.001.md updated upstream)"
  - "2026-07-08 v1.15: F-P27-001 story leg — §Depends on S-19.04 prose fixed to DISTINCT-block form (was: 'extends that section'; now: DISTINCT \"Capability Schemas\" preamble block separate from S-19.04's tool-filter-anchoring block; ordering-only dependency)"
  - "2026-07-08 v1.14: BC-1.17.001-v1.3-propagation — BC-1.17.001 v1.2→v1.3 cite propagation (anchoring-only change: ffi.rs bullet added to §Architecture Anchors in BC v1.3); Narrative, BC table, Token Budget, AC-007 Gate + BC Trace, Architecture Compliance Rules updated"
  - "2026-07-08 v1.13: F-P16-002 inputs: crates/hook-sdk/src/ffi.rs added (5 entries); input-hash 03c6f12→617adeb (pass-15 sibling-propagation miss closure)"
  - "2026-07-07 v1.12: F-P15-001 AC-007 Gate 2 clause (iii) awk block-containment (wasm32+host_stubs; grep-B1 replaced; evidence: current ffi.rs exits 1, /tmp/ffi_fixed.rs exits 0 for both blocks); F-P15-002 ffi.rs File Structure row added + host.rs row corrected to safe-wrapper; Task 11 corrected; F-P15-003 Architecture Mapping hook-sdk split corrected; O-P15-05 markdown-escape note"
  - "2026-07-07 v1.11: F-P14-003 AC-003 gate [ -z \"$(...)\" ] intrinsic-exit form (grep -oE; mirrors S-19.04); F-P14-006 Gate 2 clause (i) grep -cE BSD-portable; O-P14-02 Gate 2 clause (iii) grep -B1 fn read_prefix | grep cfg(not(wasm32)) form"
  - "2026-07-07 v1.10: F-P13-002 AC-007 Gate 1 full-signature tighten (pub fn read_prefix(path: &str, max_bytes: u32, timeout_ms: u32) -> Result<Vec<u8>, HostError>; rejects -> i32); F-P13-003 AC-007 Gate 2 three-clause expansion (pub safe fn + awk 6-param; vsdd link attr; wasm32 cfg stub)"
  - "2026-07-07 v1.9: BC-cite drift: Narrative BC-1.17.001 v1.1→v1.2 (line 68 'The fix' paragraph; stale body-scope cite caught by standing preflight)"
  - "2026-07-07 v1.8: F-P12-002 AC-007 three-gate formulation (hook-sdk/src/host.rs safe wrapper + hook-sdk/src/ffi.rs raw extern 6-param); signature-disambiguation note (BC-1.17.001 v1.2 layering); BC table v1.1→v1.2; O-P12-04 DISTINCT Capability Schemas preamble block (Task 12 + File Structure + Architecture Mapping + Previous Story Intel S-19.04)"
  - "2026-07-07 v1.7: F-P11-006 Previous Story Intel S-19.04 row gains bolded DEFERRAL GATE (Merge-PR pattern; mirrors S-19.03 row)"
  - "2026-07-07 v1.6: F-P10-005 deferral gate → Merge-PR grep pattern (Previous Story Intel S-19.03 row); F-P10-007 Architecture Compliance Rules Signature row (not ADR-025 D-15) → (normative); ADR-025 D-15 concurring"
  - "2026-07-07 v1.5: F-P9-003 AC-003 gate block-comment stripper chained (sed -E ':a; s:/\*...\*+.../::; ta' before sed 's://.*::'; /* ... */ block comments now stripped before OUTPUT_TOO_LARGE check; T-004 noted as runtime load-bearing)"
  - "2026-07-07 v1.4: F-P8-005 AC-003 gate sed trailing-comment strip added (sed 's://.*::' replaces grep -vE leading-only comment filter)"
  - "2026-07-07 v1.3: F-P7-003 Narrative ADR-025 D-15 stale claim corrected; F-P7-011 AC-003 gate non-comment scope grep; O-P7-005 Task 1 PR-description checklist sentence"
  - "2026-07-07 v1.2: F-P4-002 BC-1.17.001 v1.0→v1.1 (3 sites); blocks → [S-19.07]; T-010 EC-004 path_allow mismatch test"
  - "2026-07-06 v1.1: F-P3-005 SS-02 added; F-P3-010 depends_on [S-19.03, S-19.04]; F-P3-020 Purity Classification codes.rs → pub mod codes in host/mod.rs; O-P3-001 deferred-until-merge note; O-P3-007 AC-007 grep tightened; O-P3-006 F-P2-012 baseline marker"
---

# S-19.06 — host::read_prefix bounded partial read: head-c semantics, NEVER OUTPUT_TOO_LARGE, additive FFI entry point

**Epic:** E-19 — Post-rc.22 Operator Hardening
**Wave:** 2 (depends on S-19.03 — `path_util::resolve_path_for_allowlist` and `codes::NOT_FOUND` must be available; depends on S-19.04 — `hooks-registry.toml` preamble section must be in place before adding `capabilities.read_prefix` schema documentation)
**Depends on:** S-19.03 (creates `crates/factory-dispatcher/src/host/path_util.rs` and `codes::NOT_FOUND = -5`); S-19.04 (creates the tool-filter-anchoring preamble comment block in `hooks-registry.toml`; S-19.06 adds a DISTINCT "Capability Schemas" preamble block for the `capabilities.read_prefix` schema — separate from, not embedded in, S-19.04's block; the dependency is ordering-only so the two preamble blocks land without merge conflict)
**Priority:** P2 (new capability; no blocking behavioral defect; unlocks plugins that need bounded prefix reads without OUTPUT_TOO_LARGE risk)
**Subsystem anchor:** SS-01 (Hook Dispatcher Core) owns the host function implementation. The hook-sdk wrapper (SS-02 boundary) enables WASM plugins to call the new entry point.

## Narrative

`host::read_file` is an all-or-nothing read: it returns OUTPUT_TOO_LARGE (-3) when the file exceeds `max_bytes`. This is correct for plugins that parse TOML or YAML and require a complete, untruncated document. However, plugins that only need the first N bytes of a file — for example, reading YAML frontmatter from a large markdown artifact, or scanning a log file for a pattern in its header — cannot use `read_file` without risking OUTPUT_TOO_LARGE on any file that grows beyond the cap.

**The gap:** No `head -c`-style partial-read host function exists. Plugins that want bounded prefix reads must either set an aggressively large `max_bytes` cap (risking OUTPUT_TOO_LARGE on even larger files) or implement workarounds.

**The fix (BC-1.17.001 v1.6, ADR-025 Decision 15):** Add `read_prefix(path: &str, max_bytes: u32, timeout_ms: u32) -> i32` as an additive FFI entry point in the `vsdd` WASM import namespace. The function returns at most `max_bytes` bytes from the start of the file (equivalent to `head -c max_bytes`). It is **guaranteed never to return OUTPUT_TOO_LARGE (-3)** — by construction, `max_bytes` IS the output cap, so the response always fits within the requested bound.

**Critical invariants (BC-1.17.001):**
- `read_file` all-or-nothing semantics are **IMMUTABLE** (Invariant 2). Plugins relying on `read_file` for complete TOML/YAML parsing are unaffected. Silent truncation-as-success would corrupt those plugins' parse (D-d rationale). `read_prefix` is the alternative for plugins that explicitly want a bounded prefix.
- **Separate `capabilities.read_prefix` block** is required in the registry entry (Invariant 3). A plugin granted `capabilities.read_file` does NOT automatically receive `capabilities.read_prefix`. The two capabilities are independently declared, enabling defense-in-depth.
- **Traversal defense is identical to `read_file`** (Invariant 4). `read_prefix` imports `resolve_path_for_allowlist` from `crates/factory-dispatcher/src/host/path_util.rs` (created by S-19.03), using the same rejoin algorithm and `starts_with` allowlist check.
- **NOT_FOUND semantics are consistent with BC-2.07.001** (Invariant 5). Absent-file paths within the allowlist return NOT_FOUND (-5) and emit `internal.file_not_found` — same as `read_file` after S-19.03.

**ABI treatment:** `read_prefix` is additive. HOST_ABI_VERSION remains 1. Plugins that do not import `read_prefix` are unaffected; the new import is visible only to plugins that declare it in their FFI binding.

**Signature authority:** BC-1.17.001 is authoritative. The signature is `read_prefix(path: &str, max_bytes: u32, timeout_ms: u32) -> i32`. ADR-025 Decision 15 and BC-1.17.001 agree on the signature (reconciled at the ADR's pass-3 amendment); BC-1.17.001 remains the normative contract.

## Acceptance Criteria

| ID | Criterion | Gate | BC Trace |
|----|-----------|------|----------|
| AC-001 | `read_prefix` is implemented in `crates/factory-dispatcher/src/host/read_prefix.rs`. When `file_size > max_bytes`, the function returns exactly `max_bytes` bytes (head-c semantics), exit code 0. The return payload is the raw first `max_bytes` bytes with no UTF-8 boundary trimming (byte-exact). | Unit test: file with 100 bytes content, `max_bytes=50` → response byte count = 50, exit code 0. `grep -q "fn read_prefix" crates/factory-dispatcher/src/host/read_prefix.rs` exits 0. Exit non-zero if byte count ≠ max_bytes or exit code ≠ 0. | BC-1.17.001 PC-1 + PC-6 |
| AC-002 | When `file_size < max_bytes`, `read_prefix` returns the complete file content with no padding or truncation markers, exit code 0. | Unit test: file with 30 bytes, `max_bytes=100` → response byte count = 30, exit code 0. Exit non-zero if response length ≠ file_size or if response is padded to max_bytes. | BC-1.17.001 PC-2 |
| AC-003 | `read_prefix` NEVER returns OUTPUT_TOO_LARGE (-3) for any valid input combination (any max_bytes, any file size). | Gate: `[ -z "$(awk '/^#\[cfg\(test\)\]/{exit} {print}' crates/factory-dispatcher/src/host/read_prefix.rs \| awk 'BEGIN{b=0}{if(b){if(sub(/.*\*\//,""))b=0;else next};while(match($0,/\/\*/)){s=substr($0,1,RSTART-1);r=substr($0,RSTART+2);if(match(r,/\*\//)){$0=s substr(r,RSTART+RLENGTH)}else{$0=s;b=1;break}};print}' \| sed 's://.*::' \| grep -oE 'OUTPUT_TOO_LARGE')" ]` exits 0 (violation → non-empty output → non-zero exit). Stage 1: `awk '/^#\[cfg\(test\)\]/{exit} {print}'` scopes output to production code only — stops at the test-module boundary, excluding T-004's `assert_ne!(codes::OUTPUT_TOO_LARGE, ...)` and its assertion-message string (POSIX awk, replaces BSD-incompatible `sed -E ':a; ...; ta'` label loop). Stage 2: second awk strips `/* ... */` block comments (stateful POSIX portable). Stage 3: `sed 's://.*::'` strips `//` line and doc comments. Stage 4: `grep -oE 'OUTPUT_TOO_LARGE'` emits the match only, not full lines. Execution site: bats suite `plugins/vsdd-factory/tests/read-prefix-wasm.bats` T-009g (static gate test — closes described-but-unexecuted gap). Note: T-004 is the runtime load-bearing check that OUTPUT_TOO_LARGE is never returned at runtime; this gate is a static defense-in-depth check that the constant does not appear in non-comment production code of `read_prefix.rs`. Unit test: file of size 10000 bytes with `max_bytes=50` → return code ≠ -3 confirmed. Exit non-zero if OUTPUT_TOO_LARGE appears in non-comment production code. | BC-1.17.001 PC-3 |
| AC-004 | A plugin without a `[hooks.capabilities.read_prefix]` block in its registry entry receives CAPABILITY_DENIED (-1) on `read_prefix` invocation, before any filesystem access. A plugin that has only a `[hooks.capabilities.read_file]` block also receives CAPABILITY_DENIED (-1) — the two capabilities are independent. | Unit test A (negative): plugin with only `capabilities.read_file` calls `read_prefix` → CAPABILITY_DENIED (-1). Unit test B (positive): plugin with `capabilities.read_prefix` → capability check passes (proceeds to path check). Exit non-zero if CAPABILITY_DENIED absent on missing block, or if `capabilities.read_file` is erroneously accepted. | BC-1.17.001 PC-4, Invariant 3 |
| AC-005 | An absent file on an allowlisted path returns NOT_FOUND (-5) and emits `internal.file_not_found` (same semantics as BC-2.07.001 part c, now extended to `read_prefix`). Zero `capability_denied` events are emitted for this path. | Unit test: allowlisted path, file does not exist, `capabilities.read_prefix` present → return code -5; event stream contains `type=internal.file_not_found`; zero `capability_denied` events in captured stream. Exit non-zero if return code ≠ -5, or if `file_not_found` event absent, or if any `capability_denied` event appears. | BC-1.17.001 PC-5, Invariant 5 |
| AC-006 | `max_bytes = 0` is a valid degenerate input: `read_prefix` returns an empty byte sequence (0 bytes) with exit code 0. The file is not read. | Unit test: file present, `max_bytes=0` → payload is empty (0 bytes), exit code 0. Exit non-zero if exit code ≠ 0 or if payload is non-empty. | BC-1.17.001 EC-001 |
| AC-007 | `crates/hook-sdk` exposes `read_prefix` at two distinct layers required by BC-1.17.001 v1.6 §(a) layering parenthetical: (1) a safe Rust wrapper (`pub fn read_prefix`) in `crates/hook-sdk/src/host.rs` that returns `Result<Vec<u8>, HostError>`, mirroring the `read_file` wrapper structurally; (2) a raw wire-ABI extern (`fn read_prefix(path_ptr: *const u8, ...)`) in `crates/hook-sdk/src/ffi.rs` with a 6-parameter pointer/length shape mirroring `ffi::read_file` exactly, plus a `#[cfg(not(target_arch = "wasm32"))]` non-wasm stub; (3) dispatcher registration in the host dispatch table (cross-ref AC-001). **Signature-disambiguation note (BC-1.17.001 v1.6):** the `-> i32` return belongs exclusively to the raw wire-ABI extern; the safe wrapper returns `Result<Vec<u8>, HostError>`. Both layers are required — the safe wrapper is the hook author interface; the raw extern is the WASM import boundary. | Gate 1 (safe wrapper — full signature): `grep -qE 'pub fn read_prefix\(path: &str, max_bytes: u32, timeout_ms: u32\) -> Result<Vec<u8>, HostError>' crates/hook-sdk/src/host.rs` exits 0 (asserts full signature including return type; a bare `-> i32` wrapper fails this gate, enforcing BC-1.17.001 v1.6 layering). Gate 2 (raw extern — three parallel clauses): (i) `grep -qE 'pub safe fn read_prefix\(' crates/hook-sdk/src/ffi.rs` exits 0 AND `awk '/pub safe fn read_prefix\(/,/-> i32;/' crates/hook-sdk/src/ffi.rs \| grep -cE 'path_len\|out_ptr_out'` outputs 2 (asserts multi-line 6-param ptr/len shape mirroring ffi::read_file; `-E` flag makes `\|` ERE alternation, portable on BSD grep); (ii) `grep -qF '#[link(wasm_import_module = "vsdd")]' crates/hook-sdk/src/ffi.rs` exits 0 (asserts extern lives under vsdd import module attribute); (iii) `awk '/^#\[cfg\(target_arch = "wasm32"\)\]/,/^}/' crates/hook-sdk/src/ffi.rs \| grep -q 'fn read_prefix'` exits 0 AND `awk '/^#\[cfg\(not\(target_arch = "wasm32"\)\)\]/,/^}/' crates/hook-sdk/src/ffi.rs \| grep -q 'fn read_prefix'` exits 0 (asserts `read_prefix` is present in BOTH the `#[cfg(target_arch = "wasm32")]` extern block AND the `pub mod host_stubs` non-wasm block; block-range form replaces grep-B1 which could spuriously match cfg gates on adjacent definitions; gate execution evidence: current ffi.rs — both ranges exit 1 [read_prefix not yet present]; /tmp/ffi_fixed.rs — both ranges exit 0 [read_prefix added to both blocks]). Note: `\|` inside table-cell shell commands is markdown-escaping for `\|`; execute with unescaped `\|`. Each clause exits non-zero on failure. Gate 3 (dispatcher registration): `read_prefix` registered in host dispatch table per AC-001. Integration test: a fixture WASM plugin that imports and calls `read_prefix` compiles and links successfully. Exit non-zero if any gate fails or the fixture plugin fails to compile. | BC-1.17.001 v1.6 §(a) layering parenthetical, §Architecture Anchors, Invariant 2 |

## Architecture Mapping

**SS-01 changes (`crates/factory-dispatcher/src/host/read_prefix.rs` — new file):**
- Implement `read_prefix(path: &str, max_bytes: u32, timeout_ms: u32) -> i32`:
  1. Capability check: require `capabilities.read_prefix` block in registry; return CAPABILITY_DENIED (-1) if absent (does NOT fall back to `capabilities.read_file`)
  2. Path resolution: call `resolve_path_for_allowlist` from `path_util.rs` (created by S-19.03); check `starts_with` against the `capabilities.read_prefix` `path_allow` list; return CAPABILITY_DENIED (-1) for out-of-allowlist paths
  3. `max_bytes = 0` short-circuit: if `max_bytes = 0`, return empty payload immediately — the file is NOT opened and existence is NOT checked (per BC-1.17.001 EC-001: "The file is not read"); exit code 0
  4. Existence check: if path is allowlisted but file absent, return NOT_FOUND (-5) and emit `internal.file_not_found` (same semantics as `read_file` post S-19.03)
  5. Bounded read: open file, read at most `max_bytes` bytes from start (head-c semantics); return exit code 0 on success
  6. Timeout: respect `timeout_ms`; return TIMEOUT (-2) on expiry
  7. Directory/OS error: return INTERNAL_ERROR (-99)
  8. **NEVER emit or return OUTPUT_TOO_LARGE (-3)** — `max_bytes` IS the cap; data beyond the cap is not read
- Unit tests for AC-001..AC-006 (T-001..T-007) inline in `read_prefix.rs`

**SS-01 changes (`crates/factory-dispatcher/src/host/mod.rs`):**
- Register `read_prefix` in the host dispatch table (additive, parallel to `read_file`)

**`read_file.rs` is NOT modified** — `read_file` semantics are immutable per BC-1.17.001 Invariant 2. No changes to `read_file.rs` in this story.

**`path_util.rs` is NOT modified** — created by S-19.03; `resolve_path_for_allowlist` is imported as-is. If S-19.03 is not yet merged, this story cannot proceed (enforced by `depends_on: [S-19.03]`).

**`codes::NOT_FOUND = -5` is NOT re-declared** — defined by S-19.03 in the codes module; `read_prefix.rs` imports from there.

**hook-sdk changes (`crates/hook-sdk/src/host.rs` + `crates/hook-sdk/src/ffi.rs`):**
- `crates/hook-sdk/src/host.rs`: Add `pub fn read_prefix(path: &str, max_bytes: u32, timeout_ms: u32) -> Result<Vec<u8>, HostError>` safe wrapper (mirrors `read_file` wrapper structurally; this is the hook-author interface — returns `Result<Vec<u8>, HostError>`, NOT `-> i32`)
- `crates/hook-sdk/src/ffi.rs`: Add `pub safe fn read_prefix` 6-param extern inside the `#[cfg(target_arch = "wasm32")]` unsafe extern block (raw wire-ABI; returns `-> i32`; 6-param ptr/len shape mirrors `ffi::read_file`); add matching stub function in `pub mod host_stubs` for non-wasm targets
- Return value of the raw extern uses same `codes::*` vocabulary; OUTPUT_TOO_LARGE is mechanically excluded from this fn's return paths (by construction in the dispatcher implementation)

**Registry changes (`plugins/vsdd-factory/hooks-registry.toml`):**
- Document `capabilities.read_prefix` capability block schema in a DISTINCT "Capability Schemas" preamble comment block in `hooks-registry.toml` — this block is separate from S-19.04's tool-filter-anchoring preamble block and must not be embedded inside it. No live `[[hooks]]` entries in this story declare `capabilities.read_prefix` — the capability is available for operators to use in their own plugins. The distinct preamble schema comment makes the capability discoverable without polluting S-19.04's anchoring section.

## Behavioral Contracts

| BC ID | Title | Version | Story ACs |
|-------|-------|---------|-----------|
| BC-1.17.001 | host::read_prefix — bounded partial read (head-c semantics), NEVER OUTPUT\_TOO\_LARGE, additive FFI entry point, path\_allow + rejoin capability model | v1.6 | AC-001 (PC-1, PC-6), AC-002 (PC-2), AC-003 (PC-3), AC-004 (PC-4, Invariant 3), AC-005 (PC-5, Invariant 5), AC-006 (EC-001), AC-007 (§(a) layering parenthetical, §Architecture Anchors, Invariant 2) |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | `max_bytes = 0` (file present or absent) | Returns empty byte sequence (0 bytes), exit code 0. The file is not opened and existence is NOT consulted (per BC-1.17.001 EC-001: "The file is not read"). Composite case: (absent allowlisted file, max_bytes=0) → empty payload, exit 0, NO NOT_FOUND (-5), NO `file_not_found` event — existence check is skipped entirely because the short-circuit fires at step 3, before the existence check at step 4. T-012 pins this composite case. |
| EC-002 | `max_bytes` greater than file size | Returns full file content (no padding, no OUTPUT_TOO_LARGE). Response length = file_size. |
| EC-003 | File is absent, path is within `capabilities.read_prefix` allowlist | Returns NOT_FOUND (-5); emits `internal.file_not_found`. Same semantics as BC-2.07.001 part c. |
| EC-004 | Path is outside all allowed prefixes in `capabilities.read_prefix` | Returns CAPABILITY_DENIED (-1); emits `internal.capability_denied reason=path_not_allowed`. Check is against `capabilities.read_prefix` path_allow, not `capabilities.read_file`. |
| EC-005 | Plugin has `capabilities.read_file` but no `capabilities.read_prefix` block | Returns CAPABILITY_DENIED (-1). `read_file` capability does not extend to `read_prefix` (Invariant 3 defense-in-depth). |
| EC-006 | Read times out before `max_bytes` bytes are returned | Returns TIMEOUT (-2); emits `internal.timeout`. Same timeout semantics as `read_file`. |
| EC-007 | Target path is a directory, not a regular file | Returns INTERNAL_ERROR (-99); does not return partial content. |
| EC-008 | Plugin compiled without importing `read_prefix` from hook-sdk | Plugin cannot call `read_prefix` (absent from its import table). No runtime error; the capability is opt-in by declaration. |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| `crates/factory-dispatcher/src/host/read_prefix.rs` | effectful-shell | File I/O (open, bounded read), telemetry emission via event sink |
| `crates/factory-dispatcher/src/host/path_util.rs` | pure-core | Path manipulation (ancestor walk, rejoin, starts_with); no I/O. Created by S-19.03; referenced here. |
| `pub mod codes` block in `crates/factory-dispatcher/src/host/mod.rs` | pure-core | Constant definitions only. `NOT_FOUND = -5` defined by S-19.03 as an inline `pub mod codes` block in `host/mod.rs` (not a standalone file). |
| `crates/hook-sdk/src/host.rs` (read_prefix fn) | pure-core | FFI extern declaration and type boundary; no I/O side effects |
| `plugins/vsdd-factory/hooks-registry.toml` (capability schema) | N/A (config) | TOML registry config; no code purity boundary |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~5,500 |
| `crates/factory-dispatcher/src/host/read_file.rs` (reference implementation) | ~2,000 |
| `crates/factory-dispatcher/src/host/path_util.rs` (S-19.03 output; prerequisite) | ~1,000 |
| `crates/hook-sdk/src/host.rs` (parallel read_file wrapper pattern) | ~1,500 |
| `plugins/vsdd-factory/hooks-registry.toml` (capability schema preamble) | ~2,000 |
| BC-1.17.001 v1.6 | ~3,000 |
| ADR-025 (Decisions 13 + 15) | ~1,500 |
| BC-2.07.001 (absent-file semantics reference) | ~1,500 |
| Test files (new unit + integration) | ~2,500 |
| Tool outputs overhead | ~1,500 |
| **Total** | **~22,000** |
| Agent context window | 200K (Sonnet) |
| **Budget usage** | **~11%** |

## Tasks (MANDATORY)

1. [ ] Capture `cargo test --workspace --all-targets` baseline pass count pre-implementation; emit machine-parseable marker line `pre-implementation cargo-test baseline: <N> pass` to stdout (replace `<N>` with actual count) and record in PR description (F-P2-012). This baseline-marker line is a required PR-description checklist item — the PR must not be merged until it appears in the description.
2. [ ] Write failing unit tests AC-001/AC-002 (bounded prefix + full content for short files) — test-writer
3. [ ] Write failing unit test AC-003 (NEVER OUTPUT_TOO_LARGE) — test-writer
4. [ ] Write failing unit tests AC-004 negative + positive (capabilities.read_prefix gate) — test-writer
5. [ ] Write failing unit test AC-005 (NOT_FOUND for absent allowlisted; zero capability_denied) — test-writer
6. [ ] Write failing unit test AC-006 (max_bytes=0 → empty payload, exit 0) — test-writer
7. [ ] Write failing integration test AC-007 (hook-sdk wrapper callable from fixture WASM) — test-writer
8. [ ] Write failing unit test for EC-004: capabilities.read_prefix present, path_allow mismatch → CAPABILITY_DENIED (-1) (T-010) — test-writer
9. [ ] Write failing unit test for EC-001 composite case: absent allowlisted file, max_bytes=0 → empty payload, exit 0, NO NOT_FOUND (-5), NO file_not_found event (T-012) — test-writer
10. [ ] Verify Red Gate: all tests fail with stubs
11. [ ] Implement `read_prefix.rs`: capability check → path resolution → max_bytes=0 short-circuit (step 3: file not opened, existence not checked, per BC-1.17.001 EC-001) → existence check → bounded read → return codes (implementer); import `resolve_path_for_allowlist` from `path_util.rs`; NEVER emit OUTPUT_TOO_LARGE
12. [ ] Register `read_prefix` in `host/mod.rs` dispatch table (implementer)
13. [ ] Add `read_prefix` safe wrapper (`pub fn read_prefix(...) -> Result<Vec<u8>, HostError>`) to `crates/hook-sdk/src/host.rs` AND raw FFI extern (`pub safe fn read_prefix` 6-param) to `crates/hook-sdk/src/ffi.rs` with matching `host_stubs` stub (implementer)
14. [ ] Document `capabilities.read_prefix` capability block schema in a DISTINCT "Capability Schemas" preamble comment block in `hooks-registry.toml` (implementer) — place this block separately from S-19.04's tool-filter-anchoring preamble block; do NOT add schema documentation inside S-19.04's anchoring block
15. [ ] Verify all tests pass; assert baseline preserved + new tests added (post-implementation F-P2-012 assertion)

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-19.03 (prerequisite) | Created `path_util::resolve_path_for_allowlist` in `crates/factory-dispatcher/src/host/path_util.rs`; also added `codes::NOT_FOUND = -5` and `HostError::NotFound` named variant (no `#[non_exhaustive]`) | `read_prefix` MUST import `resolve_path_for_allowlist` from `path_util` (same rejoin semantics); `codes::NOT_FOUND = -5` is already defined — do NOT re-declare | **O-P3-001 DEFERRAL GATE: This story MUST NOT begin implementation until S-19.03 PR has MERGED to develop** (not just "story completed" in tracking). `path_util.rs` physically does not exist until the merge lands; `depends_on: [S-19.03]` enforces ordering but story-completion status alone is insufficient — verify merge with `git log develop --oneline \| grep -E "Merge pull request .* (from )?(feature/)?S-19\.03"` before starting. |
| S-19.02 (same epic) | `STATE_MD_MAX_BYTES` is a plugin-side compile-time constant pattern; `read_bounded()` checks `metadata.len()` before reading | `read_prefix` uses per-call `max_bytes: u32` parameter (not a compile-time constant) — different pattern; the `read_bounded()` OUTPUT_TOO_LARGE path is irrelevant since `read_prefix` reads at most max_bytes | Ensure no accidental import of OUTPUT_TOO_LARGE logic from read_file.rs |
| S-19.04 (same epic, wave 2) | All `tool =` entries anchored to `^(...)$`; preamble comment added to hooks-registry.toml | `capabilities.read_prefix` schema documentation goes in a DISTINCT "Capability Schemas" preamble comment block within `hooks-registry.toml`, separate from S-19.04's tool-filter-anchoring preamble block — do NOT add schema doc inside S-19.04's anchoring block | S-19.04 and S-19.06 both touch hooks-registry.toml; ordering within wave 2 should place S-19.04 first or merge atomically. **DEFERRAL GATE: implementation MUST NOT begin until S-19.04 PR has MERGED to develop** — verify: `git log develop --oneline \| grep -E "Merge pull request .* (from )?(feature/)?S-19\.04"` |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| `read_prefix` imports `resolve_path_for_allowlist` from `path_util.rs`; traversal defense identical to `read_file` | BC-1.17.001 Invariant 4 | `grep -q "path_util::resolve_path_for_allowlist\|use.*path_util" crates/factory-dispatcher/src/host/read_prefix.rs` exits 0 |
| Separate `capabilities.read_prefix` block required; `capabilities.read_file` does NOT grant read_prefix access | BC-1.17.001 PC-4, Invariant 3 | AC-004 negative-control gate: CAPABILITY_DENIED on read_file-only plugin |
| `read_file.rs` is NOT modified in this story | BC-1.17.001 Invariant 2 | No edits to `crates/factory-dispatcher/src/host/read_file.rs` |
| `codes::NOT_FOUND = -5` is reused from S-19.03; not re-declared | ADR-025 Decision 13; BC-2.07.001 | `grep -rq "NOT_FOUND.*=.*-5" crates/` exits 0; `read_prefix.rs` imports from codes module |
| Signature per BC-1.17.001 v1.6 (normative); ADR-025 D-15 concurring. **Layering parenthetical (BC-1.17.001 v1.6 §(a)):** safe wrapper in `hook-sdk/src/host.rs` returns `Result<Vec<u8>, HostError>` (mirroring `read_file`); `-> i32` is the raw wire-ABI extern in `hook-sdk/src/ffi.rs` (6-param ptr/len shape). Both layers required. | CLAUDE.md §Architectural Authority + BC-1.17.001 v1.6 §(a) layering parenthetical | AC-007 Gate 1 + Gate 2; Rust type signatures in `read_prefix.rs`, `hook-sdk/src/host.rs`, and `hook-sdk/src/ffi.rs` match BC-1.17.001 v1.6 exactly |
| No `println!` in production Rust | CLAUDE.md Conventions | CI clippy |
| Error taxonomy: named error variants, no `unwrap()` in critical paths | CLAUDE.md Conventions | Rust compiler + clippy |
| No file:line volatile pins in spec artifacts | TD-VSDD-091 | Function/symbol anchors only; no line numbers |
| HOST_ABI_VERSION = 1 unchanged | BC-1.17.001 §Description (d); ADR-025 Decision 15 | `read_prefix` is additive; no breaking change to existing host ABI |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| Rust toolchain | per `rust-toolchain.toml` at repo root | Single-workspace MSRV enforcement |
| `wasmtime` (via factory-dispatcher) | workspace-pinned | WASM plugin sandbox execution; `read_prefix` exposed via `vsdd` import namespace |
| `serde` / `serde_json` | workspace-pinned | JSON event serialization for `internal.file_not_found`, `internal.capability_denied` events |
| `tokio` | workspace-pinned | Async I/O for timeout enforcement in `read_prefix` |
| Bats (Bash Automated Testing System) | per `plugins/vsdd-factory/tests/run-all.sh` | Integration test runner for AC-007 WASM fixture test |

## Red Gate Test Plan

| Test ID | Description | Expected Outcome |
|---------|-------------|------------------|
| RG-001 | file > max_bytes → exactly max_bytes bytes returned, exit 0 | 50 bytes exactly, exit 0 |
| RG-002 | file < max_bytes → full file content returned, exit 0 | 30 bytes (file_size), exit 0 |
| RG-003 | capabilities.read_prefix absent → CAPABILITY_DENIED | -1 |
| RG-004 | capabilities.read_file only (no read_prefix) → CAPABILITY_DENIED | -1 |
| RG-005 | absent allowlisted file → NOT_FOUND (-5), file_not_found event, zero capability_denied | -5; event present; zero denied |
| RG-006 | max_bytes=0, file present → empty payload, exit 0 | 0 bytes, exit 0 |
| RG-007 | hook-sdk read_prefix wrapper exports in vsdd namespace; fixture plugin compiles | Compiles and links |

## Test Plan

| Test ID | Story AC | Test File | Description |
|---------|----------|-----------|-------------|
| T-001 | AC-001 | crates/factory-dispatcher/src/host/read_prefix.rs (unit) | Bounded prefix: 100-byte file, max_bytes=50 → 50 bytes, exit 0 |
| T-002 | AC-001 | crates/factory-dispatcher/src/host/read_prefix.rs (unit) | Byte-exact: partial multi-byte UTF-8 sequence at boundary returned without trimming |
| T-003 | AC-002 | crates/factory-dispatcher/src/host/read_prefix.rs (unit) | Full content: 30-byte file, max_bytes=100 → 30 bytes, exit 0 |
| T-004 | AC-003 | crates/factory-dispatcher/src/host/read_prefix.rs (unit) | NEVER OUTPUT_TOO_LARGE: 10000-byte file, max_bytes=50 → return code ≠ -3 |
| T-005 | AC-004 | crates/factory-dispatcher/src/host/read_prefix.rs (unit) | No capabilities.read_prefix → CAPABILITY_DENIED (-1) |
| T-006 | AC-004 | crates/factory-dispatcher/src/host/read_prefix.rs (unit) | capabilities.read_file only → CAPABILITY_DENIED (-1); capability independence |
| T-007 | AC-005 | crates/factory-dispatcher/src/host/read_prefix.rs (unit) | Absent allowlisted → NOT_FOUND (-5); file_not_found event; zero capability_denied |
| T-008 | AC-006 | crates/factory-dispatcher/src/host/read_prefix.rs (unit) | max_bytes=0 → empty payload, exit 0 |
| T-009 | AC-007 | plugins/vsdd-factory/tests/read-prefix-wasm.bats | hook-sdk read_prefix wrapper callable from fixture WASM plugin |
| T-009g | AC-003 (static gate) | plugins/vsdd-factory/tests/read-prefix-wasm.bats | Static gate: production-code-only awk strip + block/line comment strip confirms OUTPUT_TOO_LARGE absent from non-comment production code of `read_prefix.rs` |
| T-010 | EC-004 | crates/factory-dispatcher/src/host/read_prefix.rs (unit) | `capabilities.read_prefix` present but `path_allow` lists a different path than the requested path → CAPABILITY_DENIED (-1) |
| T-012 | EC-001 (composite: absent allowlisted file, max_bytes=0) | crates/factory-dispatcher/src/host/read_prefix.rs (unit) | absent allowlisted file, max_bytes=0 → empty payload, exit 0, NO NOT_FOUND (-5), NO `file_not_found` event (existence not consulted per BC-1.17.001 EC-001) |

## File Structure Requirements (MANDATORY)

| File | Action | Notes |
|------|--------|-------|
| `crates/factory-dispatcher/src/host/read_prefix.rs` | create | New host function: capability check → path resolution (path_util) → existence check → bounded read; NEVER OUTPUT_TOO_LARGE; unit tests T-001..T-008 inline |
| `crates/factory-dispatcher/src/host/mod.rs` | modify | Register `read_prefix` in host dispatch table (additive; no changes to existing entries) |
| `crates/hook-sdk/src/host.rs` | modify | Add `pub fn read_prefix(path: &str, max_bytes: u32, timeout_ms: u32) -> Result<Vec<u8>, HostError>` safe wrapper; mirrors `read_file` wrapper structurally; hook-author interface (NOT the raw `-> i32` extern) |
| `crates/hook-sdk/src/ffi.rs` | modify | Add `pub safe fn read_prefix` 6-param extern inside `#[cfg(target_arch = "wasm32")]` unsafe extern block; add matching stub in `pub mod host_stubs` (non-wasm); raw wire-ABI (`-> i32`) |
| `plugins/vsdd-factory/hooks-registry.toml` | modify | Document `capabilities.read_prefix` schema in a DISTINCT "Capability Schemas" preamble comment block (separate from S-19.04's tool-filter-anchoring preamble block; do NOT embed inside S-19.04's anchoring section) |
| `plugins/vsdd-factory/tests/read-prefix-wasm.bats` | create | Integration test T-009: fixture plugin uses read_prefix; assert compiles and returns correct result on a test file |
