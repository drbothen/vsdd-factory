---
document_type: story
level: implementation
story_id: "S-21.19"
epic_id: "E-21"
version: "1.4"
status: draft
producer: story-writer
timestamp: 2026-08-20T00:00:00Z
phase: brownfield-backfill
inputs:
  - .factory/specs/architecture/decisions/ADR-039-validator-failure-policy-resource-exhaustion-fail-closed.md
  - .factory/specs/architecture/decisions/ADR-042-validate-cross-site-correspondence-fuel-budget-raise-and-loud-exhaustion-signaling.md
  - .factory/specs/architecture/decisions/ADR-044-split-topology-enforcement-flip-capstone-ownership.md
  - .factory/research/wasm-fuel-exhaustion-detection.md
  - plugins/vsdd-factory/hooks-registry.toml
input-hash: "8291b42"
traces_to: .factory/specs/architecture/decisions/ADR-039-validator-failure-policy-resource-exhaustion-fail-closed.md
points: "7"
depends_on: [S-21.10]
blocks: [S-21.20, S-21.21, S-21.22, S-21.23, S-21.24]
behavioral_contracts: ["BC-1.01.016 v1.3", "BC-1.03.017 v1.20"]
verification_properties: []
priority: "P1"
cycle: v1.0-brownfield-backfill
wave: 6
target_module: crates/factory-dispatcher
subsystems: [SS-01]
estimated_days: null
assumption_validations: []
risk_mitigations: []
tdd_mode: strict
modified:
  - "2026-08-22 (v1.4) — Wave-7 pass-1 remediation REOPEN of S-21.19 (D-1069; ADR-044 v1.1
    Addendum, architect Q1 adjudication Option A, RATIFIED 2026-08-22 via orchestrator relay of
    human approval; source finding F-S2121-P1-002, HIGH, raised against sibling story S-21.21's
    pre-TDD adversarial pass-1): Task 6 NARROWED (not expanded in scope, only in function
    boundary) to author the PC13 predicate as the STANDALONE `plugin_fail_closed_on_error_exit(result,
    on_error)` function, no longer bundled with Task 5's Timeout/failure_policy predicate. Task 5
    renamed to introduce the canonical `plugin_fail_closed_on_exhaustion(result, on_error,
    failure_policy)` name per ADR-044 v1.1's Architecture Anchors (BC-1.03.017 v1.20); its own
    live wiring remains capstone-owned, deferred to S-21.24 (wave 8) — UNCHANGED by this burst.
    Task 6's `plugin_fail_closed_on_error_exit` function is now wireable immediately and its live
    wiring is owned by **S-21.21 at wave 7** (NOT S-21.24, correcting the pre-Addendum
    single-function framing) — swept throughout this story's AC-013b unit-leg body, Architecture
    Mapping (split into two rows), Purity Classification (split into two rows), File Structure
    Requirements, Library & Framework Requirements, and Architecture Compliance Rules (new row
    added for Task 6's wiring-ownership correction) to reflect the two-function, two-owner model.
    All other `plugin_fail_closed` references naming Task 5's function (AC-002/003/004/010's test
    bodies, Task 2's detection-guidance illustrative name, Tasks 3/4/5/7) swept to
    `plugin_fail_closed_on_exhaustion`; Task 6/AC-013b's references swept to
    `plugin_fail_closed_on_error_exit`. `BC-1.03.017 v1.19 -> v1.20` re-anchored with full
    POLICY 8 propagation: frontmatter `behavioral_contracts`, body BC table (Title cell corrected
    to remain a verbatim subset of the new H1 per POLICY 7 — the H1's punctuation before
    \"legacy-bash-adapter runtime-timeout-wiring verification\" changed from \", and\" to a bare
    comma), every citing AC (AC-001/AC-009/AC-011/AC-012/AC-013b headers and bodies), Token
    Budget, and Task 2's cite. **This edit REOPENS S-21.19: its BC-5.39.001 3-CLEAN adversarial
    convergence streak resets to 0/3** — state-manager records Wave 6 as no longer COMPLETE.
    `points:`/`depends_on:`/`blocks:`/wave assignment UNCHANGED (7 pts, wave 6) — this is a
    function-boundary correction within already-owned scope, not a scope or DAG change. Does NOT
    touch S-21.20/S-21.22/S-21.23/S-21.24 (unaffected — see
    `.factory/planning/S-21.11-decomposition-plan.md` §8.4's \"no change required\" list, itself
    unrevised by this burst except for the new S-21.21 wiring-task entry authored in that story's
    own file), BC-1.03.017/ADR-044 themselves (already landed at commit a3bfa1af, this burst is
    EDIT-ONLY against the already-ratified spec text), or any INDEX/STATE.md (state-manager's
    domain)."
  - "2026-08-20 (v1.3) — S-21.19 adversarial pass-3 remediation: F-S2119-P3-001 (MEDIUM, reset-driving) — Task 2's `test_no_on_error_block_without_fail_closed_when_3arg_executor` (AC-012) cite was still `BC-1.03.017 v1.18 PC11`, a stale live cite the pass-2 v1.18->v1.19 sweep MISSED despite that pass's changelog note claiming \"all 18 body cites re-anchored.\" Swept to `BC-1.03.017 v1.19 PC11`. Verified via whitespace-normalized/backtick-tolerant detector (`tr '\\n' ' ' | grep -oE 'BC-1\\.03\\.017[^v]{0,80}v1\\.[0-9]+' | sort | uniq -c`), run before and after: BEFORE showed 4 live-pattern `v1.18` occurrences (1 the Task-2 line-wrapped cite at old lines 755-756, 3 embedded in the v1.2 changelog's own \"v1.18 -> v1.19\" narrative text) plus 18 already-correct `v1.19` cites; AFTER shows 0 live `v1.18` cites remaining — the only surviving `v1.18` substrings are the two historical changelog rows (v1.2 and this v1.3 entry) narrating the sweep itself, which are exempt (Changelog/`modified:` rows are historical, not live cites). BC-1.01.016 was independently checked and confirmed already correctly anchored at `v1.3` throughout (no drift found, no change needed). Correcting the record: the v1.2 changelog's \"all 18 body cites re-anchored\" claim was false by one — 18 of 19 total live `BC-1.03.017` cites were swept at v1.2, with Task 2's cite (the 19th) missed; this v1.3 burst closes that gap, bringing the true total to 19/19 swept. F-S2119-P3-002 (LOW, fold in) — `blocks:` frontmatter was missing `S-21.24` despite S-21.24's `depends_on:` directly listing S-21.19 (S-21.24 Task 0 consumes this story's dormant 3-arg `plugin_fail_closed` extension per ADR-044). Added `S-21.24` to `blocks:` (now `[S-21.20, S-21.21, S-21.22, S-21.23, S-21.24]`) for bidirectional depends_on/blocks parity — no cycle introduced (19->24 is the same direction as S-21.24.depends_on=[S-21.19]). Note for state-manager: STORY-INDEX D-1057 DAG needs the direct 19->24 edge added to match. `input-hash` not touched this burst (BC-1.03.017 itself unchanged; the `blocks:` array is not an input-hash input). Does NOT touch S-21.20/S-21.21/S-21.22/S-21.23, S-21.24, BC-1.03.017/BC-1.01.016 themselves, or any INDEX/STATE.md (state-manager's domain)."
  - "2026-08-20 (v1.2) — S-21.19 adversarial pass-2 remediation: F-S2119-P2-002 (MEDIUM/high-impact — AC-009 red-first gate breaks green-trunk CI) + BC-1.03.017 v1.18 -> v1.19 re-anchor. AC-009's `test_all_six_validator_class_plugins_are_fail_closed` is now authored with `#[ignore = \"enforcement gate; enabled at S-21.24 Task 0 flip\"]` (Task 8), so it is authored+compilable and RED-verifiable via `cargo test --workspace --all-targets -- --ignored`, but does NOT run under the default `cargo test --workspace --all-targets` invocation that gates S-21.19's own PR CI and develop-trunk CI across waves 6-8; the \"remains RED through completion/downstream\" narrative is reconciled to \"authored + `#[ignore]`d; un-ignored + turned GREEN at S-21.24 Task 5.\" The cross-test assertion against S-21.21's not-yet-merged `test_legacy_bash_adapter_honors_registry_timeout_ms` is now COMPILE-SAFE: a `std::fs::read_to_string`-based source-scan of `crates/factory-dispatcher/tests/legacy_bash_adapter_timeout_wiring.rs` (tolerating file-not-found pre-S-21.21) rather than a compiled Rust symbol reference, so it does not break `cargo build --tests` at S-21.19's merge. New Architecture Compliance Rules row added mechanically enforcing the `#[ignore]` presence + no-symbol-reference discipline. Frontmatter `behavioral_contracts` and all 18 body cites of `BC-1.03.017 v1.18` (AC-001/AC-009/AC-011/AC-012/AC-013b headers and bodies, BC table row, Token Budget, Task 6) re-anchored to `BC-1.03.017 v1.19` (Invariant 7 now keys the CWE-636 regression trigger on WIRING — S-21.24's Task 0 — not on authoring the inert 3-arg function, per ADR-044; PC9/PC11 and this story's own existing narrative were already wiring-keyed and required no semantic rewrite, only the version-string sweep). ADR-044 already present in `inputs:` (confirmed, no change needed). AC partition unchanged (43/43; no AC moved this burst — only AC-009's commit-mechanism and the BC-1.03.017 version cite changed). `input-hash` not touched (state-manager's domain). Does NOT touch S-21.20/S-21.21/S-21.22/S-21.23, BC-1.03.017 itself, ADR-044/ADR-039, S-21.25, or any INDEX/STATE.md."
  - "2026-08-20 (v1.1) — F-S2119-P1-001 remediation (BLOCKER, this story's own pre-TDD adversarial convergence pass): the executor enforcement-active wiring (formerly Task 5's trailing clause + \"ATOMICITY GATE\" note) is REMOVED from this story and moves to S-21.24's new Task 0, per ADR-044 (.factory/specs/architecture/decisions/ADR-044-split-topology-enforcement-flip-capstone-ownership.md) and .factory/planning/S-21.11-decomposition-plan.md §8. Task 5 now delivers the 3-arg `plugin_fail_closed` extension and `PluginOutcome.failure_policy` field ONLY, as a standalone unit-tested pure function never wired into the real execute_tiers/execute_tier block-decision chain within this story. AC-002 and AC-011 are each SPLIT: this story retains the unit-test leg only; the bats fixture-registry dispatch leg (AC-002) and the TC-12 integration-mirror leg (AC-011) move to S-21.24, since both require the wiring this story no longer performs. Tasks 3 and 7 revised accordingly (bats/TC-12 authoring instructions removed). AC-012's LIVE-TREE-CONTROL cross-reference and Task 2's closing sentence corrected to cite S-21.24's Task 0 (the wiring genuinely never exists within S-21.19). Architecture Compliance Rules' fail-open-window-hazard row rewritten to reflect the new no-wiring-in-this-story discipline. Token Budget drops the `full_stack_plugin_invocation.rs` context line (TC-12 no longer touched here). `points:` 9 -> 7 (sheds the bats-fixture authoring and TC-12 integration-mirror revision, moved to S-21.24; combined S-21.19+S-21.24 point total unchanged at 12 — a redistribution, not a scope increase). `inputs:` adds ADR-044. This resolution makes \"annotate before flip\" hold by wave-schedule construction alone: at no commit does PC11 ever observe \"enforcement-active + any of the five targeted plugins unannotated,\" because the wiring commit that makes enforcement active (S-21.24's Task 0) does not exist until the same story that also confirms all five annotations are already present. Zero depends_on/blocks edges or wave assignment changed. Does NOT touch S-21.20/S-21.21/S-21.22/S-21.23 (verified unaffected per decomposition plan §8.4) or any INDEX/STATE.md (state-manager's domain)."
  - "2026-08-20 (v1.0) — created by story-writer via the operator-directed split of CONVERGED S-21.11 (v2.11, 16-pass 3-CLEAN BC-5.39.001 convergence) per `.factory/planning/S-21.11-decomposition-plan.md` §1 (S-21.19 detail) and §3 (AC partition table). Owns AC-001, AC-002–AC-006, AC-009–AC-012, and AC-013b's UNIT LEG ONLY, copied verbatim from S-21.11 v2.11 with zero weakening, zero AC-text alteration, and zero BC-trace change. Tasks renumbered #1–#8 (self-contained) from S-21.11 v2.11 Tasks #16, #16a, #17, #18, #19, #19b, #20, #20a. `inputs:` adds ADR-042 (cited in AC-001's `fuel_cap = 20_000_000` factory-default reference but never declared as an S-21.11 input — closing that drift-detection gap for this seam, per dispatch audit finding 1.4)."
---

> **tdd_mode: strict** — Full TDD Iron Law enforced: all non-trivial function bodies use `todo!()`;
> Red Gate density check ≥0.5 required before Step 4 dispatch.

> **Execute:** `/vsdd-factory:deliver-story S-21.19`

> **Split origin (2026-08-20).** One of six sub-stories partitioning CONVERGED S-21.11 (v2.11,
> 16-pass 3-CLEAN BC-5.39.001 convergence) per the operator-directed override of S-21.11's own
> prior "unified scope, no split" decision, documented in
> `.factory/planning/S-21.11-decomposition-plan.md`. S-21.19 is the conceptual heart of the
> split — Node (D) of S-21.11's original DAG, the Executor decision-function core — and it
> gates all four parallel downstream seams (S-21.20, S-21.21, S-21.22, S-21.23) and,
> transitively, the capstone (S-21.24). S-21.11's v2.11 body is frozen as the historical
> source-of-truth this story's ACs are extracted from verbatim; no AC text, BC trace, or test
> predicate was weakened, dropped, or reinterpreted during partitioning. S-21.11 itself is now
> `status: superseded` — see its own frontmatter for the disposition record.

# S-21.19: Executor decision-function core — 3-arg `failure_policy` extension + axes-independence (ADR-039 Phase 4a; split seam 1 of 6 from S-21.11)

## Narrative

- **As a** factory pipeline operator whose validator writes are currently approved silently
  when a WASM plugin exhausts its fuel budget
- **I want** the dispatcher's block-decision function extended to consult a
  `failure_policy: FailurePolicy` axis (independent of `on_error`) for `Timeout{Fuel|Epoch}`
  outcomes, plus the narrow PC13 `on_error=Block` + `Ok{exit_code!=0}` predicate extension
  that closes the plugin-error-exit gap — with the standing AC-001 calibration gate and the
  AC-012 migration-window gate already authored and passing their pure-function controls
  before this extension lands
- **So that** the exhaustion-outcome enforcement decision is axes-independent and
  calibration-gated from the moment it ships, and every downstream seam (bash-adapter
  wiring/calibration, native-WASM flip, break-glass ordering, PC13 full-registry coverage)
  has a stable, already-tested decision-function core to build against

## Context / Finding Summary

ADR-039 §Context establishes the enforcement defect this story closes:

1. **The detection mechanism is already correct.** `Trap::OutOfFuel` is distinguished from
   clean completion in `invoke.rs` → `PluginResult::Timeout { cause: TimeoutCause::Fuel }`.
   The defect is enforcement, not detection.

2. **The enforcement defect is CWE-636.** `plugin_fail_closed` returns `false` when
   `on_error == OnError::Continue`, regardless of `TimeoutCause`. The existing deliberate
   test `fail_closed_timeout_with_on_error_continue_is_open` encodes this policy. Any change
   requires revising that test **deliberately** (TD-VSDD-059: not deleting it, not renaming
   it; revising it to cover the new semantics for the `fail-open` configuration case).

3. **Safe migration ordering (ADR-039 §Decision 3).** S-21.10 (Phase 1, merged prerequisite)
   added the `failure_policy` TOML field to the registry schema with a `fail-open` default.
   This story delivers the Phase 4a decision-function extension — the conceptual heart of
   ADR-039 Phase 3+4 enforcement, split from the originally-unified S-21.11 per the operator's
   2026-08-20 split directive. This story has **no dependency** on the bash-adapter wiring fix
   (S-21.21), per-plugin calibration (S-21.21/S-21.22), or the break-glass mechanism (S-21.23)
   — it is calibration-axis-agnostic, which is why it lands first (wave 6) and the four
   parallel seams land afterward (wave 7).

4. **PC13 (`on_error=Block` + `Ok{exit_code!=0}` → block, ADR-039 §AMD-003).** This story also
   authors a SECOND, STANDALONE decision-function predicate — `plugin_fail_closed_on_error_exit`
   (ADR-044 v1.1 Addendum) — distinct from point 3's `plugin_fail_closed_on_exhaustion`, to
   close a distinct gap: a plugin that ran to completion without crashing or timing out at the
   WASM-trap/epoch layer, but returned a nonzero exit via its own reported `HookResult::Error`.
   This closes F-001 (a `legacy-bash-adapter.wasm`-hosted plugin's host-wall-clock timeout does
   NOT produce `PluginResult::Timeout{..}` — it propagates as `Ok{exit_code:1}`) at the unit
   level. Unlike point 3's exhaustion leg, this leg's LIVE WIRING is not deferred to S-21.24 —
   per ADR-044 v1.1 Addendum (F-S2121-P1-002), it is wireable immediately and owned by
   **S-21.21 at wave 7**, because it references neither `.failure_policy` nor `Timeout` and
   trips nothing in PC11's detector. The end-to-end fixture proof that this predicate closes
   F-001 against a real dispatch belongs to S-21.21 (gated on its own AMD-002 wiring fix AND
   its own new live-wiring task for this predicate); the registry-wide demonstration that this
   predicate protects all 18 `on_error="block"` entries, not just the six exhaustion-targeted
   plugins, belongs to S-21.20.

## Position in the Split DAG

```
S-21.10 (merged) ──▶ S-21.19 (this story) ──┬──▶ S-21.20 (PC13 full-registry coverage)
                                              ├──▶ S-21.21 (AMD-002 wiring + bash-adapter calibration)
                                              ├──▶ S-21.22 (native-WASM calibration + flip)
                                              └──▶ S-21.23 (break-glass mechanism)
                                                         │  (all four converge)
                                                         ▼
                                                    S-21.24 (capstone: gated flip + regression)
```

The full six-seam dependency graph, wave schedule, and 43-identifier AC partition table live
in `.factory/planning/S-21.11-decomposition-plan.md` §2–§3. This section shows only S-21.19's
own upstream/downstream edges.

## Acceptance Criteria

### AC-001: No `failure_policy = "fail-closed"` entry may carry `fuel_cap < 50_000_000`, and no `legacy-bash-adapter.wasm`-hosted `failure_policy = "fail-closed"` entry may carry `timeout_ms < 30_000` — standing calibration regression/invariant gate (traces to BC-1.03.017 v1.20 PC8; ADR-039 §Decision 3 atomicity constraint; POLICY 15 gate-outcome discipline; Invariant 7 ordering constraint is mechanically enforced by AC-012/PC11)

A Cargo integration test MUST assert that no `[[hook]]` entry in
`plugins/vsdd-factory/hooks-registry.toml` (the production registry; this is the ONLY scan
target for this gate — test-fixture registries are exempt) carries both
`failure_policy = "fail-closed"` AND `fuel_cap < 50_000_000`. The calibration floor is
50_000_000 (inclusive) per ADR-039 §Decision 4 (`max(observed_max × 1.5, 50M)`); exactly
50_000_000 PASSES the gate (inclusive floor). The factory default of 20_000_000 per
ADR-042 §Decision 2 is below this floor and insufficient for fail-closed annotation.

This gate is a **standing regression/invariant gate** — NOT a red-first TDD gate:
- **GREEN when the live registry contains zero fail-closed entries** (vacuously satisfied
  today; no such entries exist yet in `plugins/vsdd-factory/hooks-registry.toml`)
- **GREEN at final state** after all targeted plugins carry `failure_policy = "fail-closed"`
  with calibrated `fuel_cap >= 50_000_000` (exactly 50_000_000 is the inclusive floor and
  a VALID calibrated value per ADR-039 §Decision 4) — that final state is reached only after
  S-21.20/S-21.21/S-21.22/S-21.23's calibration and S-21.24's flip commits land; this story
  authors and observes the gate GREEN in its own vacuous (pre-flip) state only
- **RED only if a bad half-state edit** introduces a fail-closed entry with an
  uncalibrated `fuel_cap < 50_000_000`, or (see below) an uncalibrated `timeout_ms < 30_000`
  on a `legacy-bash-adapter.wasm`-hosted entry

The genuine red-first TDD gate for this story is AC-009
(`test_all_six_validator_class_plugins_are_fail_closed`, which is RED before any Phase-4c
annotations land) and AC-002/003/004/005/010 (all RED because the new
`plugin_fail_closed_on_exhaustion` function does not exist/compile yet — their RED state is
natural, not forced).

The gate test MUST include an in-test **POSITIVE-CONTROL fixture** to prevent false-green
vacuity: inject a hard-coded fail-closed entry with `fuel_cap = 20_000_000` (the factory
default per ADR-042 §Decision 2, strictly below the 50_000_000 inclusive floor → RED)
directly in the test body (NOT in the live registry) and assert the gate logic returns an
error for that fixture. This proves the gate is non-vacuous and cannot silently pass when
the live registry contains zero fail-closed entries.

The gate test MUST ALSO include a **NEGATIVE-CONTROL fixture** (per BC-1.03.017 v1.20 PC8
symmetric extension + POLICY 15 gate-outcome discipline): inject a hard-coded synthetic
fail-closed entry with `fuel_cap = 75_000_000` (above the 50M floor) directly in the test
body and assert the gate logic ACCEPTS / PASSES that fixture (returns no error). This
proves the gate does not over-fire on legitimately calibrated entries and is symmetric:
positive-control confirms rejection of the bad half-state; negative-control confirms
acceptance of the good state.

The gate test MUST ALSO assert the **boundary-pass case**: a synthetic fail-closed entry
with `fuel_cap = 50_000_000` (exactly at the inclusive floor) MUST PASS the gate (no
error), confirming that exactly 50_000_000 is a valid calibrated value per ADR-039
§Decision 4. This prevents the gate from incorrectly rejecting entries at the minimum
valid cap.

**Parallel host-wall-clock-timeout-axis assertion for `legacy-bash-adapter.wasm`-hosted entries
(ADR-039 §Decision 1/4 v1.8 amendment — fuel-axis calibration is necessary but NOT sufficient
for these entries; BC-1.03.017 v1.20 PC8):** The same test MUST ALSO assert that no `[[hook]]`
entry whose `plugin = "hook-plugins/legacy-bash-adapter.wasm"` carries both
`failure_policy = "fail-closed"` AND `timeout_ms < 30_000` (the host-wall-clock-timeout-axis
calibration floor per ADR-039 §Decision 4 formula: `max(measured_p99_ms × 2.0, 30_000)`;
exactly `30_000` is the inclusive minimum). This assertion is IN ADDITION to the `fuel_cap`
assertion above, not a replacement — a `legacy-bash-adapter.wasm`-hosted entry satisfying
`fuel_cap >= 50_000_000` alone remains half-state and MUST still fail this gate if
`timeout_ms < 30_000`, because `fuel_cap` gives no protection against that adapter class's
actual exhaustion axis. This axis-parallel assertion requires two more controls:
- **TIMEOUT-POSITIVE-CONTROL fixture:** a hard-coded `legacy-bash-adapter.wasm` entry with
  `failure_policy = "fail-closed"` and `timeout_ms = 10_000` (the current live default for
  four of the five targeted bash-adapter plugins, strictly below the 30_000 floor) injected
  directly in the test body → assert the gate fires RED on that fixture.
- **TIMEOUT-NEGATIVE-CONTROL fixture:** a hard-coded `legacy-bash-adapter.wasm` entry with
  `failure_policy = "fail-closed"` and `timeout_ms = 45_000` (above the 30_000 floor) injected
  directly in the test body → assert the gate does NOT fire (PASS / no error).

**Non-normative note on Invariant 7 (informational only — AC-012/PC11 is the authoritative
mechanical gate for the migration-window ordering constraint):** The Invariant 7
fail-open-window invariant (BC-1.03.017 v1.20 Invariant 7) states that once the extended
decision function is in effect, no `on_error="block"` targeted plugin may remain at
`failure_policy = fail-open`. This gate (`test_no_fail_closed_plugin_with_uncalibrated_cap`)
asserts ONLY the calibration constraint (both axes). The migration-window ordering constraint
for the five `on_error="block"` plugins is mechanically enforced by AC-012
(`test_no_on_error_block_without_fail_closed_when_3arg_executor` / PC11); the ADDITIONAL
break-glass-ordering constraint for the two `PreToolUse` `^Agent$` plugins among those five is
mechanically enforced by AC-022 (`test_no_agent_gate_fail_closed_flip_without_break_glass` /
BC-1.03.018 PC9, owned by S-21.23). This gate SHOULD warn (not pass silently) if it detects a
live registry entry that has `on_error = "block"` (for the five targeted plugins) but lacks
`failure_policy = "fail-closed"` after Phase 4 completes — but this SHOULD-warn is
non-normative; defer to AC-012/AC-022 for the MUST assertions.

- **Test:** `test_no_fail_closed_plugin_with_uncalibrated_cap()` — parses
  `plugins/vsdd-factory/hooks-registry.toml`; for each entry where
  `failure_policy = "fail-closed"`, asserts `fuel_cap >= 50_000_000`; for each such entry
  additionally hosted by `legacy-bash-adapter.wasm`, asserts `timeout_ms >= 30_000`; includes
  a POSITIVE-CONTROL fixture (`fuel_cap = 20_000_000`), a NEGATIVE-CONTROL fixture
  (`fuel_cap = 75_000_000`), a boundary-pass fixture (`fuel_cap = 50_000_000`), a
  TIMEOUT-POSITIVE-CONTROL fixture (`timeout_ms = 10_000` on a `legacy-bash-adapter.wasm`
  entry), and a TIMEOUT-NEGATIVE-CONTROL fixture (`timeout_ms = 45_000` on a
  `legacy-bash-adapter.wasm` entry); MUST FAIL only if any live production-registry entry has
  `failure_policy = "fail-closed"` with `fuel_cap < 50_000_000`, or is `legacy-bash-adapter.wasm`-hosted
  with `failure_policy = "fail-closed"` and `timeout_ms < 30_000`

### AC-002 (SPLIT — unit leg only; bats/integration leg owned by S-21.24): Exhaustion + fail-closed → block (exit 2) (traces to BC-1.03.017 PC1; BC-1.01.016 PC1/PC5; ADR-039 §Decision 6 test case 1)

**Leg-scope note (revised 2026-08-20, F-S2119-P1-001 remediation, ADR-044 +
`.factory/planning/S-21.11-decomposition-plan.md` §8):** AC-002 has two legs with different
landing dependencies. This story owns the UNIT-level leg only — a direct function-level test
against the extended `plugin_fail_closed_on_exhaustion` (the canonical name per ADR-044 v1.1
Addendum), calibration-axis-agnostic and independent of the real dispatch path. The
bats/integration leg (a real dispatch against a TEST-FIXTURE registry,
which requires `execute_tiers`/`execute_tier`'s real block-decision call site to be wired to
the extended function) is owned by **S-21.24**, not this story, because that wiring does not
exist until S-21.24's Task 0 lands.

A unit test MUST invoke the dispatcher's `plugin_fail_closed_on_exhaustion` (or its replacement
function) with `PluginResult::Timeout { cause: TimeoutCause::Fuel, ... }` and
`failure_policy = FailurePolicy::FailClosed`. The function MUST return `true` (block
intent). The unit test reads `RegistryEntry.failure_policy` (traces to BC-1.01.016 PC1:
`failure_policy = "fail-closed"` parses to `FailurePolicy::FailClosed` — the field is
parseable from TOML and accessible on `RegistryEntry`; and BC-1.01.016 PC5:
`failure_policy` is an independent axis in `RegistryEntry` alongside `on_error` —
axes-independence: both fields coexist without structural conflict, enabling the
executor to read `failure_policy` independently of `on_error`).
The test exercises the `Timeout + FailClosed` branch, not configuration intent.

**Bats/integration leg (owned by S-21.24, listed here for traceability only, NOT delivered by
this story):** a bats test MUST verify that a real dispatch with a plugin configured
`failure_policy = "fail-closed"` and a deliberately tiny `fuel_cap = 100` produces
dispatcher exit code 2 (BLOCK). **The `fuel_cap = 100` + `failure_policy = "fail-closed"`
entry MUST live in a dedicated TEST-FIXTURE registry** (NOT
`plugins/vsdd-factory/hooks-registry.toml`), so that AC-001's gate scan does not trip on
this fixture. The `.bats` test MUST be registered in the Bats harness at
`plugins/vsdd-factory/tests/`; no new `.sh` file may be introduced per POLICY 21 (`.bats`
files and `tests/fixtures/` entries are exempt from POLICY 21). This leg requires
`execute_tiers`/`execute_tier`'s real block-decision call site to be wired to the extended
function (S-21.24's Task 0) — it cannot be authored before that wiring exists.

- **Test (unit-level, owned here):** `test_exhaustion_fail_closed_blocks()` — unit:
  `Timeout { cause: Fuel }` + `FailClosed` → `plugin_fail_closed_on_exhaustion` returns `true`
  (traces to BC-1.01.016 PC1/PC5)
- **Test (bats/integration, owned by S-21.24):** bats integration test (TEST-FIXTURE registry
  only, NOT production registry) — dispatch with `fuel_cap = 100` +
  `failure_policy = "fail-closed"` on a budget-exhausting payload → observed dispatcher exit
  code 2 asserted. **This test is authored and delivered in S-21.24, not S-21.19.**

### AC-003: Exhaustion + fail-open → advisory, not block (exit 0) (traces to BC-1.03.017 PC2; ADR-039 §Decision 6 test case 2)

The same exhaustion scenario (`Timeout { cause: TimeoutCause::Fuel }`) with
`failure_policy = FailurePolicy::FailOpen` MUST produce exit 0 (advisory, not block).
This verifies the `fail-open` path is preserved for plugins that legitimately need it.

- **Test:** `test_exhaustion_fail_open_is_advisory()` — unit: `Timeout { cause: Fuel }` +
  `FailOpen` → `plugin_fail_closed_on_exhaustion` returns `false`; exit 0 asserted

### AC-004: `on_error = "continue"` + `failure_policy = "fail-closed"` + exhaustion → BLOCK (axes are independent) (traces to BC-1.03.017 PC3; ADR-039 §Decision 6 test case 3)

A plugin with BOTH `on_error = "continue"` AND `failure_policy = "fail-closed"` that
exhausts its fuel budget MUST produce a block (exit 2). The `on_error` field governs crash
outcomes only; it MUST NOT override `failure_policy` for exhaustion outcomes.

This is the primary correctness test of the axes-independence design. It directly validates
the behavior that the prior `fail_closed_timeout_with_on_error_continue_is_open` test
denied.

- **Test:** `test_on_error_continue_with_fail_closed_exhaustion_still_blocks()` — unit:
  `Timeout { cause: Fuel }` + `on_error = Continue` + `failure_policy = FailClosed` →
  `plugin_fail_closed_on_exhaustion` returns `true`; axes do not interfere

### AC-005: Crash vs exhaustion as distinct paths (traces to BC-1.03.017 PC4+PC5; ADR-039 §Decision 6 test case 4)

Two scenarios MUST be verified as distinct paths:

(a) A crashed plugin (`on_error = "block"`, `failure_policy = "fail-open"`) MUST block
    via the `on_error` path. `PluginResult::Crashed` + `on_error = Block` → block (exit 2).

(b) A fuel-exhausted plugin (`on_error = "block"`, `failure_policy = "fail-open"`) MUST
    NOT block — exhaustion is not a crash. `Timeout { cause: Fuel }` + `on_error = Block`
    + `failure_policy = FailOpen` → exit 0.

This verifies that `on_error = "block"` does not accidentally gate exhaustion outcomes,
preserving the original semantics of `on_error` for crash paths only.

- **Test:** `test_crash_blocks_via_on_error_not_failure_policy()` — `PluginResult::Crashed` +
  `Block` + `FailOpen` → exit 2
- **Test:** `test_exhaustion_with_on_error_block_but_fail_open_is_advisory()` — `Timeout { cause: Fuel }` +
  `Block` + `FailOpen` → exit 0

### AC-006: `fail_closed_timeout_with_on_error_continue_is_open` revised, not deleted (traces to BC-1.03.017 PC7; TD-VSDD-059)

The existing test `fail_closed_timeout_with_on_error_continue_is_open` in `executor.rs`
(or its module) encodes the current deliberate fail-open policy. When this story's
enforcement ships, this test MUST be revised to assert the new invariant for the
`fail-open` configuration case — it is not the unconditional policy it once was, but it
still describes valid behavior when `failure_policy = FailOpen`.

Specifically, the revised test MUST:
- Retain the function name (or a derivative that clearly conveys the same intent)
- Assert: `Timeout { cause: Fuel } + on_error=Continue + failure_policy=FailOpen → NOT block`
- NOT be deleted, as deletion would allow silent regression of the `fail-open` path

Deletion of this test without an equivalent replacement is a TD-VSDD-059 (paper-fix) violation.

- **Test:** revised `fail_closed_timeout_with_on_error_continue_is_open` (or
  `fail_closed_timeout_with_continue_and_fail_open_is_open`) — asserts the fail-open
  config case remains advisory; MUST exist in the PR diff

### AC-009: All targeted validator-class plugins carry `failure_policy = "fail-closed"` in final state, with calibration sufficient for their adapter class, AND (for the five bash-adapter plugins) the AMD-002 wiring fix landed — registry-complete vs runtime-complete distinction (traces to BC-1.03.017 v1.20 PC9; ADR-039 §Decision 2)

After all Phase 4 calibration-and-annotation commits land, `plugins/vsdd-factory/hooks-registry.toml`
MUST contain `failure_policy = "fail-closed"` AND calibration sufficient for its adapter class
for all plugins in the **post-amendment targeted set**:
- `validate-cross-site-correspondence` (native-WASM): `fuel_cap >= 50_000_000`.
- `validate-factory-path-root`, `validate-input-hash`, `validate-template-compliance`,
  `validate-wave-gate-prerequisite`, `validate-pr-merge-prerequisites`
  (`legacy-bash-adapter.wasm`-hosted): BOTH `fuel_cap >= 50_000_000` AND
  `timeout_ms >= 30_000`.

**PC12 dependency for full protection (ADR-039 §AMD-002, RATIFIED v1.10; BC-1.03.017 v1.20
PC9 PC12-dependency clause):** satisfying the registry-config criteria above is necessary but
NOT alone sufficient for the five `legacy-bash-adapter.wasm`-hosted plugins to be treated as
fully protective — S-21.21's AC-013's runtime-wiring assertion (the calibrated `timeout_ms`
value must actually reach the bash-subprocess kill deadline, not the hardcoded
`BASH_TIMEOUT_MS` constant) MUST additionally hold before the Phase 4 fail-closed flip for
these five plugins is considered complete. Per the split DAG's ordering (S-21.21 lands before
S-21.24's three-plugin flip, and gates S-21.24's two-Agent-gate flip together with S-21.23),
this dependency is structural (satisfied by construction) rather than a residual risk to
track — but this AC's gate test MUST still assert S-21.21's `test_legacy_bash_adapter_honors_registry_timeout_ms`
passes as a PREREQUISITE check before asserting the five bash-adapter plugins' registry
annotations, so that a future regression in the wiring fix is caught by the same gate that
guards the registry state.

Advisory-only and observability plugins MUST NOT receive `failure_policy = "fail-closed"`.
Classification of any ambiguous plugin MUST be surfaced to the orchestrator rather than
decided silently by the implementer.

**EC-004 conditional (Case A — `validate-cross-site-correspondence`, on_error="continue" only):**
If EC-004 fires for `validate-cross-site-correspondence` (calibration reveals
`PRACTICAL_FUEL_CEILING` would be exceeded for this on_error="continue" plugin, evaluated in
S-21.22), S-21.11's split scope is descoped to the five flippable on_error="block" plugins
only via orchestrator-approved spec amendment; this AC's asserted set is reduced to the five
flippable plugins; `validate-cross-site-correspondence` routes to S-21.13
(depends_on [S-21.10, S-21.22] per the split's recommended redirect). AC-009 asserts the
post-amendment set — a partial-set completion is valid if EC-004 Case A applied. **EC-004 is
FORBIDDEN for the five on_error="block" plugins** (validate-factory-path-root,
validate-input-hash, validate-template-compliance, validate-pr-merge-prerequisites,
validate-wave-gate-prerequisite) — there is no valid descope path for these plugins; they MUST
be annotated fail-closed within the split scope OR block the entire Phase-4 flip (see EC-004
Case B).

**Red-first authoring, `#[ignore]`d for green-trunk CI, and cross-story confirmation
(revised 2026-08-20, F-S2119-P2-002 remediation):** this is the **genuine red-first TDD
gate** for the split — RED before any Phase 4 annotations land (none of the six plugins have
`failure_policy = "fail-closed"` today). A COMMITTED, genuinely-failing `#[test]` under
`cargo test --workspace --all-targets` would make S-21.19's own PR CI RED (blocking its
merge under the green-to-merge gate) and, if merged, leave develop trunk RED across waves
6-8 (breaking CI for S-21.20/S-21.21/S-21.22/S-21.23) — this is incompatible with
CLAUDE.md's green-trunk CI discipline (`cargo test --workspace --all-targets` on every PR
to develop). This story therefore authors the test with
`#[ignore = "enforcement gate; enabled at S-21.24 Task 0 flip"]`: the test is authored,
compiles, and is RED-**capable** — verifiable on demand via
`cargo test --workspace --all-targets -- --ignored test_all_six_validator_class_plugins_are_fail_closed`
(Task 8 below confirms this RED-via-`--ignored` observation) — but it does NOT run, and
therefore CANNOT fail, under the default `cargo test --workspace --all-targets` invocation
that gates S-21.19's own PR CI and develop-trunk CI through waves 6-8. It remains
`#[ignore]`d through this story's own completion and through S-21.20/S-21.21/S-21.22/S-21.23
— S-21.24's Task 5 removes the `#[ignore]` attribute and confirms the test turns GREEN under
the default (un-ignored) `cargo test` run following S-21.24's own Task 0 wiring and Tasks
1-2's flip commits; S-21.19 does not re-author, un-ignore, or re-claim ownership of that
confirmation.

- **Test:** `test_all_six_validator_class_plugins_are_fail_closed()` — annotated
  `#[ignore = "enforcement gate; enabled at S-21.24 Task 0 flip"]`; parses
  `plugins/vsdd-factory/hooks-registry.toml`; asserts each plugin in the post-amendment
  targeted set has `failure_policy = "fail-closed"` and calibration sufficient for its adapter
  class (`fuel_cap >= 50_000_000` for native-WASM; BOTH `fuel_cap >= 50_000_000` AND
  `timeout_ms >= 30_000` for `legacy-bash-adapter.wasm`-hosted).
  **Compile-safe S-21.21 prerequisite check:** the test additionally asserts S-21.21's
  `test_legacy_bash_adapter_honors_registry_timeout_ms` is present and not itself
  `#[ignore]`d, as a prerequisite check before evaluating the five bash-adapter plugins'
  registry state — but it does so via a **source-scan, not a Rust symbol reference**, because
  at S-21.19's merge S-21.21 is not yet merged and `crates/factory-dispatcher/tests/legacy_bash_adapter_timeout_wiring.rs`
  (S-21.21's own new test file, per S-21.21's File Structure Requirements) does not yet exist
  — a direct call to or `#[test_case]`-style reference of that symbol would fail
  `cargo build --tests` regardless of the outer test's own `#[ignore]` status (ignored tests
  still compile). The prerequisite check instead reads
  `crates/factory-dispatcher/tests/legacy_bash_adapter_timeout_wiring.rs` as plain text via
  `std::fs::read_to_string` (tolerating `Err` — file-not-found is the expected pre-S-21.21
  state and MUST NOT panic or fail compilation) and, when the file is present, asserts
  textually that it contains a `fn test_legacy_bash_adapter_honors_registry_timeout_ms`
  definition whose preceding lines do not contain `#[ignore`. This is pure file-text I/O with
  no compile-time dependency on S-21.21's symbol, so it compiles unconditionally whether or
  not S-21.21 has merged, and it degrades to a documented "prerequisite file not yet present"
  skip-with-message (not a panic) before S-21.21 lands. Authored here `#[ignore]`d (Task 8);
  un-ignored and confirmed GREEN by S-21.24 (Task 5).
  **EC-004 reduced-set mechanism (Case A only):** if EC-004 fires for
  `validate-cross-site-correspondence` and the split scope is descoped to the five
  on_error="block" plugins via orchestrator-approved spec amendment, this test MUST be renamed
  or parameterized to reflect the actual post-amendment set (e.g.,
  `test_flippable_validator_class_plugins_are_fail_closed`) and MUST enumerate only the
  plugins actually annotated — it MUST NOT hardcode "six" when fewer than six plugins were
  annotated. `validate-cross-site-correspondence`'s assertion belongs in S-21.13, not here.
  Both the test name and the asserted set must be updated atomically with the spec amendment.
  **This reduced-set mechanism does NOT apply to the five on_error="block" plugins** — EC-004
  is forbidden for them; their five-plugin assertion stands unconditionally.

### AC-010: Epoch exhaustion treated identically to fuel exhaustion under fail-closed enforcement (traces to BC-1.03.017 PC6; ADR-039 §Decision 1)

`PluginResult::Timeout { cause: TimeoutCause::Epoch }` with
`failure_policy = FailurePolicy::FailClosed` MUST cause the executor decision function to
return `true` (block intent); the dispatcher exit code MUST be 2. `TimeoutCause::Epoch`
(epoch deadline exceeded) is a resource-exhaustion outcome identical to
`TimeoutCause::Fuel` for enforcement purposes per ADR-039 §Decision 1. Both exhaustion
causes trigger the `failure_policy` enforcement path; `on_error` is not consulted for
either.

- **Test:** `test_epoch_exhaustion_fail_closed_blocks()` — unit:
  `Timeout { cause: TimeoutCause::Epoch }` + `FailurePolicy::FailClosed` →
  `plugin_fail_closed_on_exhaustion` returns `true`; exit 2 asserted

### AC-011 (SPLIT — unit leg only; TC-12 integration-mirror leg owned by S-21.24): `fail_closed_timeout_with_on_error_block` and its integration mirror TC-12 deliberately revised — both FailOpen and FailClosed branches asserted in both unit and integration tests (traces to BC-1.03.017 v1.20 PC10; TD-VSDD-059)

**Leg-scope note (revised 2026-08-20, F-S2119-P1-001 remediation, ADR-044 +
`.factory/planning/S-21.11-decomposition-plan.md` §8):** AC-011 has two legs with different
landing dependencies. This story owns the UNIT-level leg only — the revised
`fail_closed_timeout_with_on_error_block`, a direct function-level test against the extended
decision function. The `TC-12` integration-mirror leg (a real end-to-end dispatch through
`execute_tiers`/`execute_tier`'s real block-decision call site) is owned by **S-21.24**, not
this story, because that wiring does not exist until S-21.24's Task 0 lands.

The existing test `fail_closed_timeout_with_on_error_block` in `executor.rs` (or its module)
encodes behavior for the `Timeout + on_error=Block` combination. When this story's
enforcement ships, this test MUST be deliberately revised (TD-VSDD-059: NOT deleted —
deletion is a paper-fix violation) to assert BOTH branches of the new dual semantics:

(a) `Timeout { cause: Fuel | Epoch } + on_error=Block + failure_policy=FailOpen → NOT block`
    (exhaustion governed by `failure_policy`, not `on_error`; with `FailOpen`, result is
    advisory, not block — even though `on_error=Block`)

(b) `Timeout { cause: Fuel | Epoch } + on_error=Block + failure_policy=FailClosed → block`
    (exhaustion governed by `failure_policy = FailClosed`; result is block, consistent with
    both `on_error=Block` and `failure_policy=FailClosed` independently requiring block)

Deletion of `fail_closed_timeout_with_on_error_block` without an equivalent replacement is a
TD-VSDD-059 (paper-fix) violation. The revision MUST appear in the PR diff. This is the
sibling test to AC-006's revision of `fail_closed_timeout_with_on_error_continue_is_open`;
both tests MUST exist in the PR diff.

**Integration mirror TC-12 (owned by S-21.24, listed here for traceability only, NOT
delivered by this story; BC-1.03.017 v1.20 PC10 extension):**
The existing E2E integration test `test_e2e_BC_7_06_001_sync_hook_timeout_fail_closed_on_error_block`
(TC-12) in `crates/factory-dispatcher/tests/full_stack_plugin_invocation.rs` currently constructs
`on_error=Block + failure_policy=FailOpen (registry default) + Timeout{Epoch}` and asserts
`exit_code == 2`. Under the axes-independent semantics this story mandates (AC-005(b) /
EC-009: `Timeout{Fuel|Epoch} + on_error=Block + failure_policy=FailOpen → NOT block → exit 0`),
that assertion becomes FALSE — TC-12 MUST be deliberately revised (TD-VSDD-059, non-deletion)
to assert the new semantics, once S-21.24's Task 0 wiring exists for the assertion to be
meaningful against a real dispatch:

- **TC-12 arm (a) [MUST]:** `on_error=Block + failure_policy=FailOpen + Timeout{Fuel|Epoch} → exit 0`
  (exhaustion governed by `failure_policy`, not `on_error`; fail-open means advisory)
- **TC-12 arm (b) [SHOULD]:** `on_error=Block + failure_policy=FailClosed + Timeout{Fuel|Epoch} → exit 2`
  (symmetric behavioral coverage; Invariant 6)

**IMPORTANT:** TC-12 revision is a PREREQUISITE for S-21.24's own green gate, not this
story's — `cargo test --workspace --all-targets` will be RED post-flip if TC-12 is not
revised before S-21.24's Task 0 wiring lands (TC-12's stale `exit_code == 2` assertion would
fail under the axes-independent semantics).

Deletion of TC-12 without an equivalent replacement is a TD-VSDD-059 (paper-fix) violation.
The revision MUST appear in the PR diff.

- **Test (unit-level, owned here):** revised `fail_closed_timeout_with_on_error_block` —
  asserts (a) `FailOpen` arm (exhaustion does not block when `failure_policy = FailOpen`,
  even with `on_error = Block`) AND (b) `FailClosed` arm (exhaustion blocks when
  `failure_policy = FailClosed`); MUST exist in the PR diff as a deliberate revision, not a
  deletion
- **Test (integration mirror, owned by S-21.24):** revised TC-12
  `test_e2e_BC_7_06_001_sync_hook_timeout_fail_closed_on_error_block` in
  `crates/factory-dispatcher/tests/full_stack_plugin_invocation.rs` — arm (a) [MUST] asserts
  `on_error=Block + failure_policy=FailOpen + Timeout{Epoch} → exit 0`; arm (b) [SHOULD]
  asserts `on_error=Block + failure_policy=FailClosed + Timeout{Epoch} → exit 2`; TC-12
  revision MUST exist in the PR diff as a deliberate revision, not a deletion (arm (a) is
  required); correct stale BC-7.06.001 Invariant 1 trace to BC-1.03.017 PC5. **This test is
  authored and delivered in S-21.24, not S-21.19.**

### AC-012: Hard migration-window completeness gate — if `failure_policy` enforcement is in effect (any block-decision site in the executor block-decision chain referencing `.failure_policy` for a Timeout block decision; design-flow-independent detection), every `on_error="block"` targeted plugin MUST carry `failure_policy="fail-closed"` (CWE-636 static gate; traces to BC-1.03.017 v1.20 PC11)

A Cargo integration test MUST assert: if `crates/factory-dispatcher/src/executor.rs`
contains enforcement-active state — detected by the **design-flow-independent signal**
per BC-1.03.017 v1.20 PC11: any block-decision site in the executor block-decision
chain — `execute_tier`, `execute_tiers`, or helpers — that references a `.failure_policy`
value when deciding to block on a `Timeout` outcome, however the data reaches it — then
every `[[hook]]` entry in
`plugins/vsdd-factory/hooks-registry.toml` whose `name` is one of the five targeted
`on_error = "block"` plugins (`validate-factory-path-root`, `validate-input-hash`,
`validate-template-compliance`, `validate-pr-merge-prerequisites`,
`validate-wave-gate-prerequisite`) MUST carry an explicit
`failure_policy = "fail-closed"`. The absence of this annotation while enforcement is
active MUST cause the test to FAIL (CI blocks merge).

**Relationship to AC-022 (BC-1.03.018 PC9, owned by S-21.23):** this gate covers all five
`on_error="block"` plugins uniformly — it does not distinguish the two `PreToolUse` `^Agent$`
gates from the other three. AC-022 is an ADDITIONAL gate, scoped specifically to
`validate-wave-gate-prerequisite` and `validate-pr-merge-prerequisites`, that further
requires the break-glass mechanism to be present before either of those two specifically may
carry `failure_policy="fail-closed"`. Both gates MUST pass; neither substitutes for the
other.

This gate is:
- **GREEN when enforcement is absent** (true throughout this story's entire duration —
  S-21.19 never wires the extended decision function into the real block-decision chain, per
  ADR-044/§8; authored here at Task 2; no block-decision site in the executor block-decision
  chain references `.failure_policy` for a `Timeout` block decision → test passes; GREEN is
  real because VACUITY-CONTROL verifies the enforcement-detection logic ran and returned
  `EnforcementAbsent` via a tri-state diagnostic, and that RED-emission was skipped as a
  consequence — distinguishing genuine pre-enforcement GREEN from vacuous GREEN)
- **GREEN at Phase 4 complete state** (enforcement-active signal fires AND all five
  `on_error="block"` plugins carry `failure_policy = "fail-closed"` — confirmed by S-21.24)
- **RED on the bad intermediate state** (enforcement-active signal fires AND any
  `on_error="block"` targeted plugin lacks `failure_policy = "fail-closed"`) — precisely
  the CWE-636 regression window Invariant 7 prohibits by ordering rule

This gate makes the CWE-636 migration-window regression mechanically detectable at any
single commit; design-flow-independent: it evaluates both conditions (executor
enforcement-active signal AND registry annotation) at the same commit tree, with no
ordering dependency, and with no dependence on any specific function name or data-flow
path. It is the machine-checkable complement to Invariant 7's human-readable ordering
policy.

**Relationship to AC-001:** AC-001 (PC8) gates uncalibrated caps on both axes; AC-012 (PC11)
gates missing fail-closed annotations on on_error=block plugins while enforcement is
active. These address different failure modes and both are required. **No EC-004 reduced-set
escape for this AC:** AC-012's five-plugin assertion stands unconditionally — EC-004 is
forbidden for on_error="block" plugins and cannot reduce the set of plugins this gate checks.

**Authoring split (this story authors three of four controls; S-21.24 confirms the fourth):**
this AC is authored here (Task 2 below) with its three pure-function controls
(POSITIVE/NEGATIVE/VACUITY, injectable synthetic snippet+registry, no live-tree dependency).
The fourth control, LIVE-TREE-CONTROL, requires enforcement genuinely active in the real tree
— impossible within this story at all, since S-21.19 never wires the extended decision
function into `execute_tiers`/`execute_tier`'s real block-decision chain (per ADR-044 and
`.factory/planning/S-21.11-decomposition-plan.md` §8, that wiring is S-21.24's own new Task 0,
landed only after all five targeted plugins' annotations are already known-present) — so it
is confirmed by S-21.24, not authored there.

- **Test:** `test_no_on_error_block_without_fail_closed_when_3arg_executor()` — detects
  enforcement-active state in `crates/factory-dispatcher/src/executor.rs` using the
  design-flow-independent signal per BC-1.03.017 v1.20 PC11 (any block-decision site in
  the executor block-decision chain referencing `.failure_policy` for a Timeout block
  decision, however the data reaches it; holds for extend-in-place OR introduce-a-replacement
  OR intermediate-struct routing); if enforcement is active, parses
  `plugins/vsdd-factory/hooks-registry.toml` and asserts each of the five `on_error="block"`
  targeted plugins carries `failure_policy = "fail-closed"`; MUST FAIL if enforcement active
  and any of the five lacks the annotation; GREEN when enforcement absent (this story's
  pre-Task-5 state) AND GREEN when enforcement active + all five annotated (S-21.24-confirmed
  final state); MUST include all four controls per BC-1.03.017 v1.20 PC11 (three injectable
  pure-function controls, authored here, + one live-tree control, confirmed by S-21.24).

  The test MUST be structured with **four controls** — three as **PURE FUNCTIONS over
  INJECTABLE inputs** (synthetic executor-source snippet string + synthetic registry entries,
  NOT a live-tree scan for control purposes), authored in this story, and one **live-tree
  assertion** at Phase-4-complete, confirmed by S-21.24:
  - **POSITIVE-CONTROL:** inject enforcement-active source snippet + synthetic registry
    MISSING one of the five `on_error="block"` fail-closed annotations → assert the detector
    returns RED (enforcement active + missing annotation detected)
  - **NEGATIVE-CONTROL:** inject enforcement-active source snippet + synthetic registry with
    ALL five `on_error="block"` plugins annotated `failure_policy = "fail-closed"` → assert
    the detector returns PASS (GREEN; no missing annotations)
  - **VACUITY-CONTROL:** inject enforcement-ABSENT source snippet (no `.failure_policy`
    reference in the block-decision chain for `Timeout` outcomes) + any registry → assert
    GREEN, AND assert the detector's enforcement-detection logic ran and returned
    `EnforcementAbsent` (via a `detection_ran` / tri-state diagnostic), and that RED-emission
    was skipped as a consequence — distinguishing genuine pre-enforcement GREEN from vacuous
    GREEN (i.e., a detector that never ran the enforcement-detection logic at all)
  - **LIVE-TREE-CONTROL (S-21.24-confirmed, not authored here):** at Phase-4-complete, run the
    detector against the ACTUAL `crates/factory-dispatcher/src/executor.rs` (not a synthetic
    snippet) and assert `enforcement_active = true`. Acceptable form: the POSITIVE-CONTROL
    synthetic snippet MUST be a verbatim excerpt of the real `execute_tiers` block-decision
    site, AND the same detector run on the live tree MUST return `enforcement_active = true`.
    This proves the detector's enforcement-detection logic recognizes the real enforcement
    code as shipped — a syntactically-wrong detector that passes synthetic controls (a), (b),
    (c) yet produces zero matches against the actual `execute_tiers` form would produce a
    silent CWE-636 false-green; the live-tree assertion closes this gap.

  These four controls prove the test is non-vacuous and that the vacuous-GREEN case
  (enforcement absent → enforcement-detection logic returns EnforcementAbsent, RED-emission
  skipped) is explicitly differentiated from the real-GREEN case (enforcement active + all
  five annotated), and that the detector fires correctly against the actual live codebase.

### AC-013b (unit leg): PC13 enforcement — `on_error = Block` fails closed on ANY plugin-reported error exit, not only a crash/timeout outcome (traces to BC-1.03.017 v1.20 PC13; ADR-039 §AMD-003, RATIFIED v1.11; ADR-044 v1.1 Addendum; closes S-21.11 v2.0 adversarial pass-1 BLOCKER F-S2111V2-P1-001)

**Leg-scope note (per `.factory/planning/S-21.11-decomposition-plan.md` §3, as reshaped by
ADR-044 v1.1 Addendum / F-S2121-P1-002):** AC-013b has two legs with different landing
dependencies. This story owns the UNIT-level leg only — authoring the STANDALONE
`plugin_fail_closed_on_error_exit(result, on_error)` function (Task 6), which is
calibration-axis-agnostic and MAY land before or concurrently with Task 5's
`plugin_fail_closed_on_exhaustion`. The END-TO-END fixture leg (a real dispatch driving
`legacy-bash-adapter.wasm` against a fixture that overruns `timeout_ms`, gated on S-21.21's
AMD-002 wiring fix being merged AND S-21.21's own NEW task that wires
`plugin_fail_closed_on_error_exit` into the real block-decision chain at wave 7 — per ADR-044
v1.1 Addendum, this leg's live wiring is owned by S-21.21, NOT deferred to S-21.24) is owned by
**S-21.21**, not this story. AC-013c (the F-005 non-timeout sibling coverage test) is likewise
owned by S-21.21.

For any plugin dispatched with `on_error = OnError::Block`, if the plugin's outcome is
`PluginResult::Ok { exit_code, .. }` where `exit_code != 0`, the executor's block-decision
function `plugin_fail_closed_on_error_exit` (this story's Task 6, standalone from Task 5's
`plugin_fail_closed_on_exhaustion` per ADR-044 v1.1 Addendum) MUST return `true`
(`block_intent = true`, dispatcher exit code 2), REGARDLESS of `failure_policy`.
This is a THIRD axis alongside AC-002/003/004/010's `failure_policy` (resource-exhaustion)
coverage, AC-005(a)'s `on_error`-governs-crash coverage, and AC-011's `failure_policy`-governs-
`Timeout` axes-independence coverage — it is the missing `on_error`-vs-clean-nonzero-exit
case: a plugin that ran to completion without crashing or timing out at the WASM-trap/epoch
layer, but returned a nonzero exit via its own reported `HookResult::Error` (exit code 1 per
`crates/hook-sdk/src/result.rs::HookResult::exit_code`).

This rule closes F-001 — a `legacy-bash-adapter.wasm`-hosted plugin's bash-subprocess
host-wall-clock timeout (S-21.21's `exec_subprocess.rs::run()` poll-loop kill) does NOT
produce `PluginResult::Timeout { .. }` — that variant is constructed ONLY by `classify_trap`
on a genuine `Trap::Interrupt`, which cannot fire while the guest is blocked inside the
synchronous `exec_subprocess` host call. Instead it propagates as `host::exec_subprocess`'s
`Err(codes::TIMEOUT)` → `run_bash_via_host`'s string-erasing `Err` map → `adapter_logic`'s
`HookResult::error(...)` → `exit_code = 1` → `classify_trap`'s `Err(I32Exit(1))` arm →
`PluginResult::Ok { exit_code: 1, .. }`.

**POSITIVE control:** a synthetic `PluginOutcome` with `on_error = OnError::Block` and
`result = PluginResult::Ok { exit_code: 1, .. }` MUST cause the decision function to return
`true` (`block_intent = true`; dispatcher exit code 2).

**NEGATIVE control 1 (clean exit is not blocked):** a synthetic `PluginOutcome` with
`on_error = OnError::Block` and `result = PluginResult::Ok { exit_code: 0, .. }` MUST NOT
cause a block via this rule (exit 0) — a genuinely clean pass remains unaffected.

**NEGATIVE control 2 (`on_error = Continue` is unaffected by this rule):** a synthetic
`PluginOutcome` with `on_error = OnError::Continue` and
`result = PluginResult::Ok { exit_code: 1, .. }` MUST NOT cause a block via this rule — the
pre-existing fail-open-on-crash-when-`on_error=Continue` semantics are preserved unchanged.

**NEGATIVE control 3 (F-001 regression guard — `Timeout` is NOT matched by this rule):** a
synthetic `PluginOutcome` with `on_error = OnError::Block`,
`failure_policy = FailurePolicy::FailOpen`, and `result = PluginResult::Timeout { cause: Fuel |
Epoch, .. }` MUST NOT cause a block via THIS rule — PC13's predicate is an exact match on
`Ok { exit_code != 0, .. }` and MUST NOT be implemented as a negation of `Ok { exit_code: 0 }`
(which would also catch `Timeout` and `Crashed`). The outcome remains governed exclusively by
Task 5's `plugin_fail_closed_on_exhaustion` function, which independently resolves `Timeout +
FailOpen` to `NOT block` per PC5 / AC-005(b) / AC-011's revised tests / TC-12 arm (a). This
control is the mechanical guard confirming Task 6's standalone `plugin_fail_closed_on_error_exit`
function never intercepts `Timeout` outcomes — per ADR-044 v1.1 Addendum's non-bundling ruling,
the two functions are structurally separate, not one function extending the other.

- **Test (unit-level, owned here):** `test_on_error_block_fails_closed_on_plugin_error_exit_code()`
  — asserts the POSITIVE control and all three NEGATIVE controls above against the standalone
  `plugin_fail_closed_on_error_exit` function directly; MAY land before or concurrently with
  this story's Task 5 (the two functions are independent; neither depends on the other landing
  first).
- **Test (end-to-end fixture — owned by S-21.21, listed here for traceability only, NOT
  delivered by this story):** a real dispatch driving `legacy-bash-adapter.wasm` against a
  fixture bash script that exceeds the registry's `timeout_ms` (post-S-21.21-wiring) MUST
  produce `block_intent = true` / dispatcher exit code 2 for a gate registered
  `on_error = "block"` — this closes the F-001 leg end-to-end, not just at the unit level;
  MUST land strictly after this story's Task 6 (unit predicate) AND S-21.21's own AMD-002
  wiring-fix tasks AND S-21.21's new `plugin_fail_closed_on_error_exit` live-wiring task
  (ADR-044 v1.1 Addendum) are GREEN. **This test is authored and delivered in S-21.21, not
  S-21.19.**

## Behavioral Contracts

| BC ID | Version | Title | Clause |
|-------|---------|-------|--------|
| BC-1.03.017 | v1.20 | factory-dispatcher::executor::failure_policy enforcement — exhaustion-outcome dispatch (fail-closed→block; fail-open→advisory), on_error axes independence, crash-versus-exhaustion distinct paths, Phase-3-before-Phase-4 structural half-state gate, migration-window on_error=block completeness gate, legacy-bash-adapter runtime-timeout-wiring verification | PC1 (AC-002 — unit leg only; bats/integration leg owned by S-21.24), PC2 (AC-003), PC3 (AC-004), PC4+PC5 (AC-005), PC6 (AC-010), PC7 (AC-006), PC8 (AC-001), PC9 (AC-009 — authored+red-first here, `#[ignore]`d for green-trunk CI per F-S2119-P2-002; un-ignored + final GREEN confirmed by S-21.24), PC10 (AC-011 — unit leg only; TC-12 integration mirror owned by S-21.24), PC11 (AC-012 — three pure-function controls authored here; LIVE-TREE-CONTROL confirmed by S-21.24), PC13 (AC-013b — UNIT LEG ONLY, standalone `plugin_fail_closed_on_error_exit`; end-to-end leg + AC-013c + live wiring owned by S-21.21 per ADR-044 v1.1 Addendum) |
| BC-1.01.016 | v1.3 | factory-dispatcher::registry::failure_policy field — parse semantics, backward-compatibility default (fail-open), unknown-value rejection, and on_error axes independence | PC1 (`failure_policy = "fail-closed"` parses to `FailClosed`; field parseable from TOML and accessible on `RegistryEntry`; AC-002 unit test reads this field); PC5 (axes-independence: `failure_policy` is an independent field in `RegistryEntry` alongside `on_error`; prerequisite schema contract; AC-002/AC-003 depend on this structural guarantee) |
<!-- POLICY 7: BC-table Title cell MUST remain a verbatim SUBSET of the corresponding BC's H1. -->

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| `plugin_fail_closed_on_exhaustion(result, on_error, failure_policy)` (Task 5; canonical name per ADR-044 v1.1 Addendum) | `crates/factory-dispatcher/src/executor.rs` | Pure (consults `failure_policy` for `Timeout` outcomes, independent of `on_error`; governs PC1/PC2/PC3/PC5/PC6/PC10; wiring deferred to S-21.24) |
| `plugin_fail_closed_on_error_exit(result, on_error)` (Task 6; standalone, NOT bundled with the exhaustion function per ADR-044 v1.1 Addendum, F-S2121-P1-002) | `crates/factory-dispatcher/src/executor.rs` | Pure (governs PC4's `Crashed` case, already live, EXTENDED with the narrow `on_error==Block && Ok{exit_code!=0}` PC13 predicate; wiring wireable immediately, owned by S-21.21 at wave 7) |
| `struct PluginOutcome` (`failure_policy: FailurePolicy` field addition) | `crates/factory-dispatcher/src/executor.rs` | Pure (data; all construction sites are in `executor.rs`) |
| `test_no_fail_closed_plugin_with_uncalibrated_cap` (AC-001) | `crates/factory-dispatcher` or integration test crate | Pure (reads hooks-registry.toml; asserts invariant on both axes) |
| `test_no_on_error_block_without_fail_closed_when_3arg_executor` (AC-012, three pure-function controls) | `crates/factory-dispatcher` or integration test crate | Pure (reads hooks-registry.toml + executor-source snippet; asserts ordering invariant) |
| `test_all_six_validator_class_plugins_are_fail_closed` (AC-009, red-first authoring) | `crates/factory-dispatcher` or integration test crate | Pure (reads hooks-registry.toml; asserts final-state invariant) |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|----------------|
| `plugin_fail_closed_on_exhaustion` (Task 5) | Pure-core | Decision function; takes `PluginResult` + `OnError` + `FailurePolicy`; returns `bool`; no I/O |
| `plugin_fail_closed_on_error_exit` (Task 6, standalone per ADR-044 v1.1 Addendum) | Pure-core | Decision function; takes `PluginResult` + `OnError` only (no `FailurePolicy` — never consults it); returns `bool`; no I/O |
| `test_no_fail_closed_plugin_with_uncalibrated_cap` | Pure-core | Reads config file; asserts structural invariant on both axes |
| `test_no_on_error_block_without_fail_closed_when_3arg_executor` | Pure-core | Reads config file + source snippet; asserts ordering invariant |
| `test_all_six_validator_class_plugins_are_fail_closed` | Pure-core | Reads config file; asserts final-state invariant |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | Plugin with `failure_policy = "fail-closed"` completes successfully (no exhaustion) | Exit 0; `on_error` and `failure_policy` do not affect clean-pass outcomes |
| EC-002 | Plugin with `failure_policy = "fail-closed"` + `PluginResult::Crashed` (`on_error = "continue"`) | Crash governed via `on_error`; exit 0 (crash is advisory per `continue`); `failure_policy` is NOT consulted for crash outcomes |
| EC-003 | Plugin with `failure_policy = "fail-closed"` + `PluginResult::Crashed` (`on_error = "block"`) | Crash governed via `on_error`; exit 2 — crash blocks regardless of `failure_policy` |
| EC-004 | Calibration (performed in S-21.22) reveals `validate-cross-site-correspondence` needs `fuel_cap > PRACTICAL_FUEL_CEILING` (`PRACTICAL_FUEL_CEILING = 500_000_000`) even at observed_max×1.5 for the corpus | Governs AC-009's descope mechanism (Case A only — `on_error="continue"` plugin; see AC-009 above). **FORBIDDEN for the five `on_error="block"` plugins** — no valid descope path for those; see AC-009/AC-012's unconditional five-plugin assertion. Full resolution path (annotation-landing obligation, S-21.13 routing) is evaluated in S-21.22, which owns the calibration for the affected plugin. |
| EC-007 | `TimeoutCause::Epoch` (epoch deadline) with `failure_policy = "fail-closed"` | MUST block (exit 2); both `TimeoutCause::Fuel` and `TimeoutCause::Epoch` are resource-exhaustion outcomes per ADR-039 §Decision 1 |
| EC-008 | `on_error = "block"` + `failure_policy = "fail-closed"` exhausts fuel | BLOCK via `failure_policy` path; `on_error = block` is redundant for exhaustion when `failure_policy = "fail-closed"` but both agree on the block outcome |
| EC-009 | `on_error = "block"` + `failure_policy = "fail-open"` exhausts fuel | Exit 0; exhaustion governed by `failure_policy = fail-open`; `on_error = block` does not apply to exhaustion outcomes (PC5) |

Edge cases EC-005, EC-006, EC-010 through EC-014 (bash-adapter timing windows, break-glass
malformed-value handling, etc.) are not cited by this story's owned ACs; they are owned by
S-21.21/S-21.22/S-21.23 respectively. The full 14-row table lives in S-21.11 v2.11 (frozen
historical source-of-truth).

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | 10,500 |
| ADR-039 (§Decision 1, 2, 3, 4, 6 — the decision-function-relevant subset; §AMD-002/§AMD-003 amendments are context, not load-bearing for this seam) | 3,500 |
| ADR-042 (§Decision 2, factory-default fuel_cap context for AC-001) | 700 |
| Research file `wasm-fuel-exhaustion-detection.md` (fuel/epoch detection mechanics) | 2,500 |
| BC files (2 BCs): BC-1.03.017 v1.20 (PC1–PC12 + PC13's narrow-predicate clause + Precondition 6) + BC-1.01.016 v1.3 (prerequisite schema) | 5,500 |
| `crates/factory-dispatcher/src/executor.rs` (current, `plugin_fail_closed`, `PluginOutcome`, `fail_closed_timeout_with_on_error_continue_is_open`, `fail_closed_timeout_with_on_error_block`) | 6,000 |
| `plugins/vsdd-factory/hooks-registry.toml` (read-only; no annotation in this story) | 1,000 |
| **Total** | **~29,700** |
| Agent context window | 200K (Sonnet) |
| **Budget usage** | **~14.9%** |

**Budget note:** well under the 20–30% ceiling — the split's intended effect. Per the
2026-08-20 F-S2119-P1-001 remediation (ADR-044), this story no longer touches
`crates/factory-dispatcher/tests/full_stack_plugin_invocation.rs` (TC-12's revision moved to
S-21.24), dropping that context source entirely. All remaining context sources are needed
simultaneously (the decision-function extension and its two revised sibling tests live in the
same file and are logically inseparable), so no further phase-by-phase sub-splitting is
proposed for this seam.

## Tasks (MANDATORY)

This story's tasks are the Node (D) subset of S-21.11 v2.11's original DAG, renumbered
self-contained #1–#8 (source: S-21.11 v2.11 Tasks #16, #16a, #17, #18, #19, #19b, #20, #20a).

1. [ ] Write `test_no_fail_closed_plugin_with_uncalibrated_cap` (AC-001 **regression/invariant
       gate**, NOT a red-first gate): parse `plugins/vsdd-factory/hooks-registry.toml`;
       iterate entries; assert each fail-closed entry has `fuel_cap >= 50_000_000` (inclusive
       floor), and each `legacy-bash-adapter.wasm`-hosted fail-closed entry ALSO has
       `timeout_ms >= 30_000`. Include POSITIVE-CONTROL (`fuel_cap = 20_000_000`),
       NEGATIVE-CONTROL (`fuel_cap = 75_000_000`), boundary-pass (`fuel_cap = 50_000_000`),
       TIMEOUT-POSITIVE-CONTROL (`timeout_ms = 10_000` on a `legacy-bash-adapter.wasm`
       entry), and TIMEOUT-NEGATIVE-CONTROL (`timeout_ms = 45_000` on a
       `legacy-bash-adapter.wasm` entry) fixtures. This test is GREEN today (zero fail-closed
       entries in the live registry). The genuine RED-first TDD gate is AC-009 (Task 8 below).
       AC-012 (also a registry-state/structural regression gate, not red-first) is authored
       immediately below at Task 2, mirroring this task's early placement, so it exists
       before Task 5's extension lands.
2. [ ] (AC-012 early authoring) Write
       `test_no_on_error_block_without_fail_closed_when_3arg_executor` (AC-012, BC-1.03.017
       v1.20 PC11) with its POSITIVE-CONTROL, NEGATIVE-CONTROL, and VACUITY-CONTROL — the
       three controls that are pure functions over INJECTABLE inputs (synthetic
       executor-source snippet + synthetic registry entries, NOT a live-tree scan), per the
       detailed control specification in AC-012 above. Confirm all three pass now:
       VACUITY-CONTROL asserts GREEN today because enforcement is genuinely absent in the live
       tree at this point (Task 5 has not yet landed) — the natural, real-world confirmation
       that this task lands before Node (D)'s decision-function extension, exactly mirroring
       AC-001's Task 1 placement. Do NOT scan for `fn plugin_fail_closed_on_exhaustion(` by
       name for AC-012's detection (name-coupled false-green risk); do NOT bare-grep `failure_policy:`
       across all of `executor.rs` — a pre-existing
       `failure_policy: crate::registry::FailurePolicy::FailOpen` literal exists TODAY inside
       the test function `emit_lifecycle_timeout_carries_fuel_cap_and_consumed`, which would
       false-match a bare grep; scope detection to `.failure_policy` DOT-ACCESS within the
       block-decision chain. The fourth control (LIVE-TREE-CONTROL) CANNOT be completed here
       — it requires enforcement to be genuinely active in the real tree, which never occurs
       within this story at all (per ADR-044 and `.factory/planning/S-21.11-decomposition-plan.md`
       §8, the wiring that makes enforcement active is S-21.24's own Task 0, landed only after
       all five targeted plugins' annotations are already known-present) — and is deferred to
       **S-21.24**, which confirms it once its Task 0 wiring and Phase-4c flip commits are in
       place. Authoring this test here, before Task 5's pure-function extension, means three
       of its four controls (POSITIVE, NEGATIVE, VACUITY) are already GREEN well before
       enforcement is ever active in the live tree — the VACUITY-CONTROL in particular remains
       GREEN throughout this story's own completion and every downstream seam's landing, since
       S-21.19 never wires the extended function into the real block-decision chain.
3. [ ] Write failing RED Gate tests for AC-002 (`test_exhaustion_fail_closed_blocks`, UNIT LEG
       ONLY — a direct function-level test against `plugin_fail_closed_on_exhaustion`) and
       AC-003 (`test_exhaustion_fail_open_is_advisory`). **AC-002's bats/integration leg is
       REMOVED from this story per ADR-044 / `.factory/planning/S-21.11-decomposition-plan.md`
       §8 (F-S2119-P1-001 remediation)** — the fuel-exhausting fixture, TEST-FIXTURE registry,
       and `.bats` test registration are authored in **S-21.24**, once that story's Task 0
       wiring exists for a real dispatch to be meaningful. Do NOT author the bats fixture or
       TEST-FIXTURE registry in this story.
4. [ ] Write failing RED Gate tests for AC-004
       (`test_on_error_continue_with_fail_closed_exhaustion_still_blocks`), AC-005 (both
       sub-tests), and AC-010 (`test_epoch_exhaustion_fail_closed_blocks` —
       `Timeout { cause: TimeoutCause::Epoch }` + `FailurePolicy::FailClosed` →
       `plugin_fail_closed_on_exhaustion` returns `true`; exit 2 asserted), with the same
       RED-first discipline as its fuel-axis sibling (AC-002)
5. [ ] Introduce `plugin_fail_closed_on_exhaustion(result, on_error, failure_policy)` in
       `executor.rs` — the canonical exhaustion-leg function name per ADR-044 v1.1 Addendum
       (Architecture Anchors, BC-1.03.017 v1.20). For
       `Timeout { cause: TimeoutCause::Fuel | TimeoutCause::Epoch }`, return `true` when
       `failure_policy == FailurePolicy::FailClosed` regardless of `on_error`. Also add
       `failure_policy: FailurePolicy` to `PluginOutcome` and populate it at ALL construction
       sites in `executor.rs` (grep-confirmed: `struct PluginOutcome` is defined and ALL
       construction sites are in `executor.rs`; `invoke.rs` has ZERO `PluginOutcome`
       constructions; do NOT confuse with the decoy `pub enum PluginOutcome` in `routing.rs`
       which is a different type — do not edit `routing.rs` or `invoke.rs` for this task).
       This task delivers the exhaustion-leg function and the
       `PluginOutcome.failure_policy` field ONLY, proven correct via direct function-level
       tests (AC-002/003/004/010's unit legs) that call the function directly,
       bypassing `execute_tiers`. **This is a functionally and structurally SEPARATE function
       from Task 6's `plugin_fail_closed_on_error_exit` below — per ADR-044 v1.1 Addendum's
       non-bundling ruling (F-S2121-P1-002), do NOT fold `Timeout`/`failure_policy` handling
       into the error-exit function, and do NOT fold the error-exit predicate into this
       function.**
       **This function is NOT called from `execute_tier`/`execute_tiers`'s real
       block-decision chain within this story — that wiring is S-21.24's Task 0 (per ADR-044,
       `.factory/specs/architecture/decisions/ADR-044-split-topology-enforcement-flip-capstone-ownership.md`,
       and `.factory/planning/S-21.11-decomposition-plan.md` §8, resolving BLOCKER
       F-S2119-P1-001).** Do not add a call site inside `execute_tiers`/`execute_tier` that
       references `.failure_policy` for a `Timeout` block decision in this story; doing so
       would trip AC-012/PC11's live-tree detection prematurely and reopen F-S2119-P1-001 (a
       multi-wave CWE-636 fail-open window between this story's merge and the five targeted
       plugins' annotations landing in S-21.21/S-21.23/S-21.24).
6. [ ] (AC-013b unit leg; ADR-039 §AMD-003, BC-1.03.017 v1.20 PC13; ADR-044 v1.1 Addendum,
       F-S2121-P1-002) Author the STANDALONE `plugin_fail_closed_on_error_exit(result,
       on_error)` function in `executor.rs` — a function distinct from Task 5's
       `plugin_fail_closed_on_exhaustion`, per ADR-044 v1.1 Addendum's non-bundling ruling
       (the pre-Addendum framing that treated this as "the SAME predicate... extended from
       Task 5" is superseded). This function encapsulates the pre-existing
       `on_error == OnError::Block` + `PluginResult::Crashed` → block logic (PC4, already live
       and unchanged) and EXTENDS it with the NARROW predicate
       `on_error == OnError::Block && matches!(result, PluginResult::Ok { exit_code } if exit_code != 0)`
       — an EXACT match on the `Ok` variant carrying a nonzero `exit_code`. **Do NOT implement
       this as a negation of `Ok { exit_code: 0, .. }`** (i.e. do NOT write
       `!matches!(result, PluginResult::Ok { exit_code: 0, .. })`): that broader formulation
       would ALSO match `PluginResult::Timeout { .. }` and `PluginResult::Crashed`, which are
       NOT in scope for this rule — `Timeout` remains governed exclusively by Task 5's
       `plugin_fail_closed_on_exhaustion` (PC1–PC6, PC10); `Crashed` remains governed by this
       same function's pre-existing `on_error`-crash path (PC4/PC5). This function MUST NOT
       consult `failure_policy` at all — it takes only `result` and `on_error`. In particular,
       `Timeout { cause: Fuel | Epoch } + on_error=Block + failure_policy=FailOpen` MUST
       continue to resolve to `NOT block` (exit 0) per PC5 / AC-005(b) / AC-011's revised
       `fail_closed_timeout_with_on_error_block` and its TC-12 integration mirror (arm (a)) —
       a broad "NOT Ok{0}" formulation would silently override that fail-open outcome and
       regress F-001. This closes the `PluginResult::Ok { exit_code: 1 }` gap PC1-PC12's
       `Crashed | Timeout` matching does not catch, and ONLY that gap. Write
       `test_on_error_block_fails_closed_on_plugin_error_exit_code`
       asserting the POSITIVE control (`on_error=Block` + `Ok{exit:1}` → block) and THREE
       NEGATIVE controls: (1) `on_error=Block` + `Ok{exit:0}` → no block; (2)
       `on_error=Continue` + `Ok{exit:1}` → unaffected; (3) [F-001 regression guard]
       `on_error=Block` + `failure_policy=FailOpen` + `Timeout{cause: Fuel | Epoch}` →
       NOT blocked by this rule (the narrow predicate does not match `Timeout`; the outcome
       remains governed by Task 5's `plugin_fail_closed_on_exhaustion`, which independently
       resolves this combination to `NOT block` per PC5). This function is authored and
       unit-tested here (S-21.19) but, per ADR-044 v1.1 Addendum, is NOT wired into the real
       `execute_tiers`/`execute_tier` block-decision chain within this story either — its live
       wiring is owned by **S-21.21 at wave 7** (a new task in S-21.21's own file), NOT S-21.24,
       because this leg references neither `.failure_policy` nor `Timeout`-as-a-calibration-gate
       and trips nothing in PC11's detector, unlike Task 5's exhaustion leg. This extension is
       calibration-axis-agnostic and MAY land in the same commit as Task 5, or independently —
       the two functions have no landing-order dependency on each other. **This is the predicate
       S-21.20's 18 registry-wide coverage tests exercise, and the function S-21.21 wires live
       at wave 7 for its end-to-end/F-005 tests — do not rename the function signature without
       a sibling-site sweep across S-21.20 and S-21.21.**
7. [ ] Verify RED Gates for AC-002 through AC-005 and AC-010 pass (unit legs only). Revise
       `fail_closed_timeout_with_on_error_continue_is_open` per AC-006 and
       `fail_closed_timeout_with_on_error_block` (UNIT LEG ONLY — **TC-12's integration mirror
       is REMOVED from this story per ADR-044 / `.factory/planning/S-21.11-decomposition-plan.md`
       §8 and is authored in S-21.24**) per AC-011 — both MUST be deliberately revised, not
       deleted (TD-VSDD-059); both revisions MUST appear in the PR diff. Correct the stale
       `BC-7.06.001 Invariant 1` doc-comment mis-citation immediately above whichever of
       `fn plugin_fail_closed_on_exhaustion` / `fn plugin_fail_closed_on_error_exit` carries it
       in `executor.rs` (these two functions succeed the pre-split `plugin_fail_closed`; NOT the
       adjacent `BC-1.14.001` trace line, which is correct) to `BC-1.03.017 PC5`. **Do NOT
       revise TC-12 in `full_stack_plugin_invocation.rs` in this story** — that revision,
       including its own stale `BC-7.06.001 Invariant 1` correction, is S-21.24's Task 0
       follow-on work. Perform the TD-VSDD-060 sibling-site sweep for ALL callsites of
       `plugin_fail_closed_on_exhaustion`, `plugin_fail_closed_on_error_exit`, and the retired
       `plugin_fail_closed` name (`grep -rn 'plugin_fail_closed_on_exhaustion\|plugin_fail_closed_on_error_exit\|plugin_fail_closed' crates/`)
       restricted to this story's own unit-test sites — do NOT modify
       `full_stack_plugin_invocation.rs`.
8. [ ] **(AC-009 red-first authoring, `#[ignore]`d for green-trunk CI — F-S2119-P2-002
       remediation)** Write `test_all_six_validator_class_plugins_are_fail_closed` as a Cargo
       integration test annotated `#[ignore = "enforcement gate; enabled at S-21.24 Task 0
       flip"]`: parses `plugins/vsdd-factory/hooks-registry.toml`; asserts each plugin in the
       post-amendment targeted set has `failure_policy = "fail-closed"` and calibration
       sufficient for its adapter class (`fuel_cap >= 50_000_000` for native-WASM; BOTH
       `fuel_cap >= 50_000_000` AND `timeout_ms >= 30_000` for `legacy-bash-adapter.wasm`-hosted).
       Implement the S-21.21 prerequisite check as a **compile-safe source-scan**, NOT a Rust
       symbol reference: read `crates/factory-dispatcher/tests/legacy_bash_adapter_timeout_wiring.rs`
       via `std::fs::read_to_string`, tolerating `Err` (file-not-found is the expected state
       until S-21.21 merges — return a clear diagnostic string, do NOT `.unwrap()` or panic,
       and do NOT reference `test_legacy_bash_adapter_honors_registry_timeout_ms` as a callable
       symbol anywhere in this file, since that symbol does not exist at S-21.19's own merge
       and a direct reference would fail `cargo build --tests` independent of `#[ignore]`
       status); when the file IS present, assert textually that it contains
       `fn test_legacy_bash_adapter_honors_registry_timeout_ms` not preceded by `#[ignore`.
       Confirm the test COMPILES cleanly under `cargo build --tests` (it must, since S-21.21
       has not merged yet and the source-scan degrades gracefully rather than referencing an
       absent symbol) and CONFIRM RED-CAPABILITY via
       `cargo test --workspace --all-targets -- --ignored test_all_six_validator_class_plugins_are_fail_closed`
       — none of the six plugins carry `failure_policy = "fail-closed"` in the live registry
       yet, so the `--ignored` run MUST fail — this is the genuine red-first observation
       AC-009's postcondition requires, made CI-safe by the `#[ignore]` attribute: the default
       `cargo test --workspace --all-targets` (S-21.19's own PR CI and develop-trunk CI gate)
       MUST pass because this test does not execute under it. The test remains authored and
       `#[ignore]`d through this story's own completion and through S-21.20/S-21.21/S-21.22/
       S-21.23 — none of those stories' default `cargo test` runs are affected by it. **Do not
       remove the `#[ignore]` attribute or attempt to turn this test GREEN under the default
       run within this story — S-21.24's Task 5 un-ignores it and confirms the un-ignored,
       default-run GREEN state as part of its own flip confirmation.**

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-21.11 pre-split history (v1.0–v2.11; 13 pre-ratification + 3 post-ratification pre-TDD adversarial passes, CONVERGED at BC-5.39.001 3-CLEAN) | The original single-scope-element version of this story (native fuel-axis + bash-adapter timeout_ms calibration + enforcement flip) survived 13 adversarial passes before pass-13 (F-S2111-P13-001) surfaced the fuel-vs-timeout axis bifurcation and, in follow-on architect self-verification, the AMD-002 runtime-wiring gap. Product-owner then unified five scope elements into one story (v2.0), which converged at v2.11 before the operator directed this 6-way split. | The AC-001/AC-012 four-control pattern (POSITIVE/NEGATIVE/VACUITY/LIVE-TREE, pure-function-over-injectable-inputs, design-flow-independent detection) proved durable across 16 passes — this pattern is load-bearing for this story's own AC-001/AC-012 tests and was reused for AC-022 (now in S-21.23). The Envoy #38801 discipline (assert observed outcomes, not configuration intent) is load-bearing throughout. | The `PluginOutcome` vs. `routing.rs`'s decoy `pub enum PluginOutcome` confusion, and the bare-`failure_policy:`-grep false-match hazard (the `emit_lifecycle_timeout_carries_fuel_cap_and_consumed` test literal) both recur as live traps for Tasks 5/6/2 — repeated explicitly in this story's own task text (Task 2, Task 5). The AC-009/AC-012 red-first-vs-registry-state-gate ordering distinction (AC-009 genuinely red-first; AC-001/AC-012 vacuously-GREEN-then-real-GREEN regression gates) was itself the subject of two adversarial-pass fixes (v2.10/v2.11) in the source story — preserve the Task 1/Task 2/Task 8 ordering exactly; do not re-collapse AC-012's authoring back onto its confirmation task. |
| S-21.10 (W5; prerequisite, merged) | Delivered `FailurePolicy` enum + `RegistryEntry.failure_policy` field; absent field defaults to `fail-open`; `plugin_fail_closed` NOT modified. | `failure_policy` field is now parseable in all `[[hook]]` entries; this story reads the `failure_policy` from the dispatched `RegistryEntry` in the executor. | S-21.10 AC-006 explicitly blocked any change to `plugin_fail_closed` — this story is where that gate lifts. Do not re-implement AC-006 as a constraint here. |
| ADR-039 §AMD-001/§AMD-002 authoring session (2026-08-18/19) | Direct source-code verification (not generic library documentation) was required to find the AMD-002 gap. | When a spec amendment cites a specific enforcement mechanism, verify it against the actual call chain in the repo, not just the referenced library's documentation. | Not directly load-bearing for this seam's own tasks (AMD-002 wiring is S-21.21's scope), but the discipline applies equally to this story's decision-function work: verify `plugin_fail_closed`'s actual callers via grep before assuming the extension's blast radius. |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| `failure_policy = "fail-closed"` MUST NOT land without calibrated `fuel_cap >= 50_000_000` (and, for `legacy-bash-adapter.wasm`-hosted entries, `timeout_ms >= 30_000`) at the same commit | ADR-039 §Decision 3 per-plugin atomicity; ADR-039 §Decision 4 calibration floors (50M / 30_000ms inclusive) | AC-001 gate test (Task 1); CI failure on any half-state on either axis |
| `TimeoutCause::Fuel` AND `TimeoutCause::Epoch` are both resource exhaustion — both trigger fail-closed when `failure_policy = "fail-closed"` | ADR-039 §Decision 1 | EC-007 test; both cause variants must be covered in AC-002 (Fuel) and AC-010 (Epoch) |
| `fail_closed_timeout_with_on_error_continue_is_open` and `fail_closed_timeout_with_on_error_block` MUST be revised, NOT deleted (TC-12's revision is REMOVED from this story per ADR-044/§8 — owned by S-21.24) | TD-VSDD-059 paper-fix prevention | AC-006/AC-011 unit legs (Task 7); PR reviewer checks the functions exist in the diff with updated semantics |
| Advisory-only plugins (observability, telemetry, convergence-tracking) MUST remain `fail-open` | ADR-039 §Decision 2 | AC-009 test only asserts the six targeted plugins; any additional annotation of advisory-only plugins is a scope violation |
| POLICY 21 — no new `.sh` files | POLICY 21 (D-836) | `git diff --name-only HEAD` output must contain no `.sh` filenames |
| **Fail-open-window hazard (Node D atomicity, revised 2026-08-20 per ADR-044/F-S2119-P1-001):** this story's exhaustion-leg function `plugin_fail_closed_on_exhaustion` (Task 5) MUST NOT be wired into `execute_tiers`/`execute_tier`'s real block-decision call site — that wiring is S-21.24's Task 0, landed only after all five on_error="block" targeted plugins' annotations are already known-present | BC-1.03.017 v1.20 Invariant 7 + PC11; ADR-044 | AC-012 CI gate (design-flow-independent, authored here at Task 2; S-21.24 Task 5 confirms LIVE-TREE-CONTROL); PR reviewer verifies no call site inside `execute_tiers`/`execute_tier` in this story's diff references `.failure_policy` for a Timeout block decision |
| **(ADR-044 v1.1 Addendum, F-S2121-P1-002)** Task 6's `plugin_fail_closed_on_error_exit` MUST NOT be wired into the real block-decision chain within this story either — unlike Task 5's exhaustion leg, its live wiring is owned by **S-21.21 at wave 7** (a new task in S-21.21's own file), NOT S-21.24 | BC-1.03.017 v1.20 Architecture Anchors (two-function split); ADR-044 v1.1 Addendum | PR reviewer verifies no call site inside `execute_tiers`/`execute_tier` in this story's diff references the error-exit predicate; S-21.21's own PR is where its wiring lands |
| PC13's narrow predicate (Task 6, `plugin_fail_closed_on_error_exit`) MUST NOT be implemented as a negation of `Ok{exit_code:0}` | ADR-039 §AMD-003; F-001 regression guard | AC-013b NEGATIVE control 3 |
| **(F-S2119-P2-002 remediation)** `test_all_six_validator_class_plugins_are_fail_closed` (AC-009) MUST carry `#[ignore = "enforcement gate; enabled at S-21.24 Task 0 flip"]` in this story's PR diff, and MUST NOT reference S-21.21's `test_legacy_bash_adapter_honors_registry_timeout_ms` as a compiled Rust symbol (source-scan only) | CLAUDE.md green-trunk CI discipline (`cargo test --workspace --all-targets` on every PR to develop); TD-VSDD-059 (this is authoring, not deletion, of red-first evidence) | Task 8; PR reviewer confirms `#[ignore = "enforcement gate; enabled at S-21.24 Task 0 flip"]` is present verbatim on the test and `cargo build --tests` succeeds pre-S-21.21-merge |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| Rust toolchain (workspace pinned) | `rust-toolchain.toml` | Implementing the split `plugin_fail_closed_on_exhaustion` (Task 5) and `plugin_fail_closed_on_error_exit` (Task 6) functions |
| serde / toml (workspace) | workspace version | Parsing `hooks-registry.toml` in AC-001/AC-009/AC-012 gate tests |
| wasmtime 46.0.2 (pinned) | Cargo.lock (S-21.12 #781 merged) | Fuel mechanics; `TimeoutCause::Fuel`/`TimeoutCause::Epoch` variants; `fuel_cap = 100` for AC-002 bats integration test |

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| `crates/factory-dispatcher/src/executor.rs` | modify | Introduce `plugin_fail_closed_on_exhaustion(result, on_error, failure_policy)` (Task 5) AND the standalone `plugin_fail_closed_on_error_exit(result, on_error)` (Task 6, PC13 narrow predicate — NOT bundled with Task 5's function per ADR-044 v1.1 Addendum); revise `fail_closed_timeout_with_on_error_continue_is_open` (AC-006) and `fail_closed_timeout_with_on_error_block` UNIT LEG ONLY (AC-011) — both deliberately revised, not deleted (TD-VSDD-059); correct stale BC-7.06.001 Invariant 1 trace to BC-1.03.017 PC5; add `failure_policy: FailurePolicy` to `struct PluginOutcome` and populate at all construction sites. **This story does NOT add a call site inside `execute_tiers`/`execute_tier` referencing `.failure_policy` for a Timeout block decision, nor a call site invoking `plugin_fail_closed_on_error_exit` — Task 5's wiring is S-21.24's Task 0 (wave 8); Task 6's wiring is S-21.21's new task (wave 7), per ADR-044/ADR-044 v1.1 Addendum.** |
| `crates/factory-dispatcher/tests/full_stack_plugin_invocation.rs` | **NOT touched by this story (revised 2026-08-20, ADR-044/F-S2119-P1-001)** | TC-12's revision moved to S-21.24 — that story's Task 0 wiring must land first for the revision to be meaningful |
| `crates/factory-dispatcher/src/registry.rs` | read-only | `FailurePolicy` enum, `RegistryEntry` struct delivered by S-21.10; no modifications unless a defect is found |
| `plugins/vsdd-factory/hooks-registry.toml` | read-only | No annotation performed by this story — this story extends the decision function only; per-plugin annotations happen in S-21.21/S-21.22/S-21.24 |

## Changelog

| Version | Date | Description |
|---------|------|--------------|
| 1.4 | 2026-08-22 | Wave-7 pass-1 remediation REOPEN of S-21.19 (D-1069; ADR-044 v1.1 Addendum, architect Q1 adjudication Option A, RATIFIED 2026-08-22; source finding F-S2121-P1-002, HIGH, raised against sibling story S-21.21). Task 6 NARROWED to author the PC13 predicate as the STANDALONE `plugin_fail_closed_on_error_exit(result, on_error)` function, no longer bundled with Task 5's Timeout/failure_policy predicate. Task 5 renamed to introduce the canonical `plugin_fail_closed_on_exhaustion(result, on_error, failure_policy)` name per ADR-044 v1.1's Architecture Anchors (BC-1.03.017 v1.20); its wiring remains capstone-owned, deferred to S-21.24 (wave 8) — UNCHANGED. Task 6's function is now wireable immediately and its live wiring is owned by **S-21.21 at wave 7** (NOT S-21.24) — swept throughout AC-013b's unit-leg body, Architecture Mapping (split into two rows), Purity Classification (split into two rows), File Structure Requirements, Library & Framework Requirements, and Architecture Compliance Rules (new row added). All other `plugin_fail_closed` references naming Task 5's function swept to `plugin_fail_closed_on_exhaustion`; Task 6/AC-013b's references swept to `plugin_fail_closed_on_error_exit`. `BC-1.03.017 v1.19 -> v1.20` re-anchored with full POLICY 8 propagation: frontmatter `behavioral_contracts`, body BC table (Title cell corrected to remain a verbatim subset of the new H1 per POLICY 7), every citing AC, Token Budget, Task 2's cite. **This edit REOPENS S-21.19: its BC-5.39.001 3-CLEAN adversarial convergence streak resets to 0/3** — state-manager records Wave 6 as no longer COMPLETE. `points:`/`depends_on:`/`blocks:`/wave assignment UNCHANGED (7 pts, wave 6). Does NOT touch S-21.20/S-21.22/S-21.23/S-21.24, BC-1.03.017/ADR-044 themselves (already landed at commit a3bfa1af), or any INDEX/STATE.md (state-manager's domain). |
| 1.3 | 2026-08-20 | S-21.19 adversarial pass-3 remediation: **F-S2119-P3-001** (MEDIUM, reset-driving) — Task 2's `test_no_on_error_block_without_fail_closed_when_3arg_executor` (AC-012) cite was still `BC-1.03.017 v1.18 PC11`, a stale live cite the pass-2 sweep MISSED (its own changelog falsely claimed "all 18 body cites re-anchored"). Swept to `v1.19 PC11`. Verified via whitespace-normalized/backtick-tolerant detector (`tr '\n' ' ' \| grep -oE 'BC-1\.03\.017[^v]{0,80}v1\.[0-9]+' \| sort \| uniq -c`) run before and after: BEFORE showed 4 live-pattern `v1.18` occurrences (the Task-2 cite + 3 embedded in the v1.2 changelog's own narrative text) alongside 18 already-correct `v1.19` cites; AFTER shows 0 live `v1.18` residuals — only the two historical changelog rows (v1.2, v1.3) narrating the sweep retain the substring, which is exempt. True count: 19/19 live `BC-1.03.017` cites now swept (v1.2's claim of 18/18 was off by one). `BC-1.01.016` independently checked and confirmed already correctly anchored at v1.3 throughout — no drift, no change. **F-S2119-P3-002** (LOW, fold in) — `blocks:` frontmatter was missing `S-21.24` despite S-21.24's `depends_on:` directly listing S-21.19 (S-21.24 Task 0 consumes this story's dormant 3-arg `plugin_fail_closed` extension per ADR-044). Added `S-21.24` to `blocks:` for bidirectional depends_on/blocks parity — no cycle (19→24 matches S-21.24.depends_on=[S-21.19]'s direction). STORY-INDEX D-1057 DAG needs the direct 19→24 edge added (state-manager). `input-hash` not touched. |
| 1.2 | 2026-08-20 | S-21.19 adversarial pass-2 remediation: **F-S2119-P2-002** (MEDIUM/high-impact) — AC-009's `test_all_six_validator_class_plugins_are_fail_closed` now carries `#[ignore = "enforcement gate; enabled at S-21.24 Task 0 flip"]` (Task 8), so it is authored, compiles, and is RED-verifiable via `cargo test --workspace --all-targets -- --ignored` without breaking the default `cargo test --workspace --all-targets` green-trunk gate (CLAUDE.md); S-21.24's Task 5 un-ignores it and confirms the un-ignored GREEN state. The S-21.21 cross-test prerequisite check is now a compile-safe `std::fs::read_to_string` source-scan of `crates/factory-dispatcher/tests/legacy_bash_adapter_timeout_wiring.rs` (tolerating file-not-found pre-S-21.21) instead of a compiled Rust symbol reference, so it does not break `cargo build --tests` before S-21.21 merges. New Architecture Compliance Rules row added. **BC-1.03.017 v1.18 → v1.19 re-anchor:** frontmatter `behavioral_contracts` and all body cites swept to v1.19 (Invariant 7 now keys the CWE-636 regression trigger on WIRING — S-21.24's Task 0 — not authoring; this story's own narrative was already wiring-keyed, so only the version string changed). Cycle: `v1.0-brownfield-backfill`. AC partition unchanged (43/43). |
| 1.1 | 2026-08-20 | F-S2119-P1-001 remediation (BLOCKER) per ADR-044 (`.factory/specs/architecture/decisions/ADR-044-split-topology-enforcement-flip-capstone-ownership.md`) and `.factory/planning/S-21.11-decomposition-plan.md` §8: the executor enforcement-active wiring is REMOVED from this story and moves to S-21.24's new Task 0. Task 5 now delivers the 3-arg `plugin_fail_closed` extension and `PluginOutcome.failure_policy` field ONLY, as a standalone unit-tested pure function never wired into the real `execute_tiers`/`execute_tier` block-decision chain within this story. AC-002 and AC-011 are each SPLIT: this story retains the unit-test leg only; the bats fixture-registry dispatch leg (AC-002) and the TC-12 integration-mirror leg (AC-011) move to S-21.24. Tasks 3 and 7 revised accordingly. AC-012's LIVE-TREE-CONTROL cross-reference corrected to cite S-21.24's Task 0. `points:` 9 -> 7 (combined S-21.19+S-21.24 point total unchanged at 12 — a redistribution, not a scope increase). `inputs:` adds ADR-044. |
| 1.0 | 2026-08-20 | Created by story-writer via the operator-directed 6-way split of CONVERGED S-21.11 (v2.11, 16-pass 3-CLEAN BC-5.39.001), per `.factory/planning/S-21.11-decomposition-plan.md` §1/§3. Owns AC-001, AC-002–AC-006, AC-009–AC-012, AC-013b (unit leg only) — 10 full ACs + 1 split leg, copied verbatim from S-21.11 v2.11 with zero weakening or drop. Tasks renumbered #1–#8 (self-contained) from S-21.11 v2.11 Tasks #16, #16a, #17, #18, #19, #19b, #20, #20a. `inputs:` adds ADR-042 (closing an input-declaration drift-detection gap present in S-21.11: AC-001 cites ADR-042 §Decision 2 but S-21.11 never declared ADR-042 as an input). |
