---
document_type: story
level: implementation
story_id: "S-25.02"
epic_id: "E-25"
version: "2.3"
last_amended: "2026-09-06 (v2.3 — story-writer, F4 cluster-1 LOCAL-adversary pass-3 F-C1-P3-001 EC-014 propagation fix-burst, single-commit TD-VSDD-053, POLICY 8 EC-mirror): BC-1.18.005 was amended v1.7→v1.9 (product-owner adjudication, GRACEFUL ruling mirroring EC-004's flat-shape missing-file-is-zero precedent) — intervening v1.8 was a pure wording-timing clarification for `low_water_mark`'s default (Invariant 4/EC-010 reworded; explicitly \"no code or test change required,\" so it produced no story-side propagation) — to add EC-014 (item-count shape's `read_changelog_item_count` maps `io::ErrorKind::NotFound` to `Ok(0)` on a not-yet-existing target artifact; any OTHER `io::Error` kind on an EXISTING file is unchanged/fail-loud) plus a new Postcondition 8 missing-file/first-write sub-bullet and a matching Canonical Test Vector. Propagated per POLICY 8 (`bc_array_changes_propagate_to_body_and_acs`): EXTENDED AC-005's trace header to add edge case EC-014 and appended a paragraph describing the missing-file → `Ok(0)` → `Continue` behavior and its VP-deferral; ADDED new §Edge Cases table row EC-026 (source BC-1.18.005) mirroring EC-014; §Behavioral Contracts table BC-1.18.005 version cell v1.7→v1.9; §Token Budget BC-1.18.005 and Story-spec context-source lines nudged upward (+800/+300 tokens) to reflect the BC's v1.9 growth, Total ~73,600→~74,700, Budget usage stays ~37%. Per product-owner's own routing note on BC-1.18.005 v1.9 (no VP allocated for EC-014 this burst — routed to architect/formal-verifier as a SIXTH facet of VP-140, mirroring the BC's own Postcondition-8→VP-140 and this story's own Postcondition-9/EC-013/AC-023 deferral precedents), this story's `verification_properties:` frontmatter is UNCHANGED this burst (still VP-116..VP-141, 26 VPs) — AC-005's new paragraph carries an explicit inline VP-deferral note (EC-014's formal VP owed to Phase F6; unit-tested at F4) instead of a frontmatter VP entry. No other AC, Task body, or Architecture Compliance Rule touched. FLAG for state-manager: story `version:` frontmatter 2.2→2.3; STORY-INDEX S-25.02 row → v2.3 + BC-1.18.005 cell → v1.9 (note: STORY-INDEX may still show an earlier BC-1.18.005 cell value if the v1.7 bump from the prior burst was not itself propagated there — reconcile to v1.9 directly); BC-INDEX BC-1.18.005 cell → v1.9; story `input-hash:` recompute via `compute-input-hash --update` (BC-1.18.005 input content changed across v1.8/v1.9; current value `36fea89` is stale as of this burst)."
status: ready
producer: story-writer
timestamp: 2026-09-06T09:30:00Z
phase: 3
inputs:
  - .factory/feature-delta/validation-integrity-layer1/F1-delta-analysis.md
  - .factory/specs/architecture/decisions/ADR-051-layer-2-two-mechanism-size-triggered-shard-rotation-append-logs-and-bc-index-sharding.md
  - .factory/specs/architecture/decisions/ADR-047-indeterminate-outcome-model-durable-mutation-marker-next-advance-gate.md
  - .factory/specs/architecture/decisions/ADR-042-validate-cross-site-correspondence-fuel-budget-raise-and-loud-exhaustion-signaling.md
  - .factory/specs/architecture/decisions/ADR-049-last-amended-write-path-durable-fix-current-entry-plus-changelog-sequence.md
  - .factory/specs/architecture/decisions/ADR-039-validator-failure-policy-resource-exhaustion-fail-closed.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.005.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.006.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.007.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.008.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.009.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.010.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.011.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.012.md
  - .factory/specs/behavioral-contracts/ss-07/BC-7.08.001.md
  - .factory/specs/verification-properties/VP-INDEX.md
  - .factory/specs/prd-supplements/error-taxonomy.md
  - .factory/specs/domain-spec/capabilities.md
  - .factory/specs/architecture/ARCH-INDEX.md
  - .factory/cycles/v1.0-brownfield-backfill/S-25.02-f1-delta-analysis.md
  - .factory/cycles/v1.0-brownfield-backfill/S-25.02-f2-architecture-delta.md
  - .factory/cycles/v1.0-brownfield-backfill/S-25.02-f2-prd-delta.md
  - .factory/cycles/v1.0-brownfield-backfill/S-25.02-f2-verification-delta.md
  - .factory/stories/S-25.01-dispatcher-indeterminate-outcome-layer1.md
input-hash: "0b88830"
traces_to: .factory/stories/epics/E-25-validation-integrity.md
points: "45"
depends_on: [S-25.01]
blocks: [S-25.03]
behavioral_contracts:
  - BC-1.18.005
  - BC-1.18.006
  - BC-1.18.007
  - BC-1.18.008
  - BC-1.18.009
  - BC-1.18.010
  - BC-1.18.011
  - BC-1.18.012
  - BC-7.08.001
verification_properties: [VP-116, VP-117, VP-118, VP-119, VP-120, VP-121, VP-122, VP-123, VP-124, VP-125, VP-126, VP-127, VP-128, VP-129, VP-130, VP-131, VP-132, VP-133, VP-134, VP-135, VP-136, VP-137, VP-138, VP-139, VP-140, VP-141]
priority: "P1"
cycle: v1.0-brownfield-backfill
wave: "W2"
target_module: crates/factory-dispatcher
subsystems: [SS-01, SS-04, SS-07]
estimated_days: 15
assumption_validations: []
risk_mitigations: []
tdd_mode: strict
---

> **F2 CONVERGED, F3 POPULATED — READY FOR DISPATCH.**
>
> S-25.02 was REGISTERED BACKLOG under E-25 per CLAUDE.md Canonical Principle §2 (feature
> ordering is the only acceptable speed lever). Activation occurred when S-25.01 merged to develop
> (2026-09-03, D-1159). F2 spec-evolution (product-owner BC authorship + architect ADR-051 design)
> ran a 12-pass fresh-context adversarial cascade to a 3-consecutive-clean streak (passes 10/11/12)
> plus a consistency-validator gate audit plus a CLEAN input-hash drift check; **the human
> RATIFIED the current two-mechanism shard-rotation design as-is 2026-09-06** (D-1167, ADR-051
> v1.8 status: accepted). This F3 burst (story-writer) populates the story's frontmatter and body
> from that ratified spec and integrates it into E-25's dependency graph/wave schedule. `status:
> ready` — this story is now dispatch-eligible for test-writer/implementer under the standard
> per-story Phase 3 sub-workflow (stubs → failing tests → TDD green → LOCAL adversary 3-CLEAN →
> demo-recorder per-AC → push → pr-manager 9-step PR cycle).

> **tdd_mode: strict** — Full TDD Iron Law enforced: all non-trivial function bodies use `todo!()`;
> Red Gate density check ≥0.5 required before Step 4 dispatch. This applies uniformly to the
> ONGOING per-write mechanisms (BC-1.18.005/006/007/009/010) AND the THREE one-time governed
> migrations (BC-1.18.008/011/012) — a one-time migration is still real, non-trivial business logic
> (content-preservation verification, independent census, crash-atomicity, rollback), not a
> facade/scaffold candidate.

# S-25.02: Artifact Sharding Layer 2 — Size-Triggered Shard Rotation for Cycle Artifacts

## Narrative

- **As a** factory pipeline operator who has Layer 1 (INDETERMINATE markers, BC-1.18.001-004) deployed
- **I want** the append-only cycle artifacts (decision-log.md, burst-log.md, lessons.md,
  session-checkpoints.md) AND `BC-INDEX.md`'s two independent size drivers (its frontmatter
  `changelog:` array and its per-subsystem body tables) to be automatically split into capped
  shards when they approach the validator-completion fuel envelope, with a deterministic,
  native-dispatcher size/item-count rule enforced by a PreToolUse gate rather than LLM-side
  awareness
- **So that** no shard ever exceeds the size at which fuel-exhaustion INDETERMINATE events fire,
  eliminating the root cause of Layer-1 marker writes BY CONSTRUCTION (for both mechanism-A
  append-logs and BC-INDEX's own size drivers — BC-INDEX alone produced 45.2% of the first 708
  observed `plugin.indeterminate` events) and making the Cohort B validators
  (`validate-burst-log`'s Edit/Write/MultiEdit arm, `regression-gate`, `convergence-tracker`) safe
  to flip to `failure_policy = "fail-closed"`

## Context / Feature Summary

**Registered at:** F3 story-decomposition burst, validation-integrity-layer1 cycle, 2026-08-30
(story-writer). **Populated at:** F3 incremental-stories burst, 2026-09-06 (story-writer), from
the F2-converged, human-ratified spec. Source: ADR-047 §Decision 8b (originally: Ratified Future
Phase Layer 2) elaborated into ADR-051 (accepted v1.8, D-1167) — Two-Mechanism Size-Triggered
Shard Rotation for Cycle Append-Logs and BC-INDEX Sharding.

**Why Layer 2 follows Layer 1 (not concurrent):** Layer 1 instruments the INDETERMINATE failure
mode (detects and surfaces it). Layer 2 attacks the root cause (large artifacts). Layer-1
`plugin.indeterminate` production forensics (available after S-25.01 merged) confirmed
`BC-INDEX.md` alone accounts for 45.2% of the first 708 observed events, directly shaping ADR-051's
decision to widen scope beyond the original mechanism-A-only draft to also cover BC-INDEX's two
independent size drivers (D-1166, human widest-scope decision).

**Two mechanisms, three artifact shapes, one native gate (ADR-051 §Decision 1):**

1. **Mechanism A — byte-size-denominated shard rotation for the four append-only cycle logs**
   (`decision-log.md`, `burst-log.md`, `lessons.md`, `session-checkpoints.md`). A `stat()`-only
   size check (BC-1.18.005) triggers a roll-before-write (BC-1.18.006: publish the current
   content as a NEW sealed file via copy, then atomically replace the canonical file's content
   with empty in place — never a rename-away) plus retention/compaction (BC-1.18.007) plus a
   mandatory one-time backfill-split of the four pre-existing oversized files (BC-1.18.008).
2. **Mechanism B1 — item-count-denominated rotation of `BC-INDEX.md`'s frontmatter `changelog:`
   array** (BC-INDEX's dominant size driver, 177,305 of 539,713 bytes measured 2026-09-05), reusing
   the already-shipped `rotate_changelog` primitive (ADR-049 §Decision 6, CAP-042) via a
   single-actor block-and-retry contract (BC-1.18.009) plus a governed one-time cold-start backfill
   migration (BC-1.18.012, ~1,997 unrotated items).
3. **Mechanism B2 — per-subsystem body-table sharding of `BC-INDEX.md`'s ten `### SS-NN` sections**
   into individually-addressable shard files, with zero-lookup first-level addressing (via the
   already-authoritative ARCH-INDEX BC-S-prefix→SS-NN mapping, POLICY 6) and manifest-based
   second-level sub-sharding for the two subsystems (SS-05, SS-06) already over cap on section size
   alone (BC-1.18.010), plus a governed one-time migration (BC-1.18.011).

**Shard cap formula (production-grade; ADR-051 §Decision 2, BC-1.18.005 Postcondition 4/6):**
```
shard_cap_bytes <= (PRACTICAL_FUEL_CEILING / WORST_CASE_FUEL_PER_BYTE) - MAX_SINGLE_RECORD_BYTES - SAFETY_MARGIN
```
Constants are PROVISIONAL until the F4 synthetic calibration harness runs (owned by
performance-engineer, co-run with implementer per BC-1.18.005 Postcondition 7); the formula SHAPE
is locked now (illustrative worked value: 49,152 bytes / 48 KiB), the numbers are not. Effective
per-artifact cap is the MINIMUM across every Cohort B validator that reads that artifact
(Cross-Validator Minimum Rule, BC-1.18.005 Postcondition 5).

**Three governed one-time migrations are MANDATORY companions, not follow-ups** (BC-1.18.008 for
mechanism A, BC-1.18.011 for B2, BC-1.18.012 for B1) — per CLAUDE.md's production-grade default, a
gate that only prevents FUTURE overflow without retroactively shrinking the artifacts already
producing the majority of observed INDETERMINATE events is an incomplete delivery of this story's
own stated purpose.

**Cohort B flip is the capstone (BC-7.08.001):** once BC-1.18.005/006's gate is F4-locked AND
BC-1.18.008's backfill-split has COMPLETED (hard sequencing gate — flipping fail-closed before the
pre-existing oversized files are split would immediately re-trigger the exact INDETERMINATE loop
this story exists to eliminate), `hooks-registry.toml` assigns `failure_policy = "fail-closed"` to
exactly three registry entries: `validate-burst-log`'s `^(Edit|Write|MultiEdit)$` arm ONLY (its
sibling `^Bash$` git-commit chain-detection arm is exec-free, unrelated, and explicitly out of
scope), `regression-gate`, and `convergence-tracker`.

**SS-04 touchpoint (ADR-051 §Decision 6, adjudicated 2026-09-06):** the retention/compaction
policy's validator enumeration directly audits four SS-04-owned WASM crates
(`validate-dispatch-advance`, `validate-state-structure`, `validate-closes-completeness`,
`validate-cross-site-correspondence`) for archival-scheme correctness, and imposes a new
archive-inclusive-glob obligation on POLICY-1's (`append_only_numbering`) SS-04-adjacent
enforcement path (`consistency-validator`/adversary-prompt agent level, `lint_hook: null`) — the
one genuine whole-corpus-scanning gap the code-grounded validator audit identified. This is the
substantive basis for adding SS-04 to this story's `subsystems:` frontmatter array.

## Acceptance Criteria

### AC-001: Native `stat()`-only deterministic size-trigger, zero-cost bypass (traces to BC-1.18.005 postcondition 1, postcondition 2)

A native (non-WASM) dispatcher function, invoked from the PreToolUse handling path before the
registry-driven plugin loop, checks the tool call's target path against a `[[shard]]` config
table. An unmatched path returns `Continue` immediately with zero `stat()` calls (zero-cost bypass
for the ~99% of `Edit`/`Write`/`MultiEdit` calls Layer 2 does not touch). A matched path reads
ONLY the current shard's byte size via filesystem metadata — never file content, never a WASM
sandbox, never a fuel budget — so the check itself is structurally incapable of producing a
`plugin.timeout`/`plugin.indeterminate` event.

### AC-002: Per-tool-semantics projected-size formula (traces to BC-1.18.005 postcondition 3, edge case EC-005)

`projected_size` is computed per-tool: for `Write`, `projected_size = len(content)` alone
(`current_shard_bytes` is NOT added — `content` is the complete post-apply file). For
`Edit`/`MultiEdit`, `projected_size = current_shard_bytes + net_delta_bytes` (the net length delta,
summed across all edit blocks for `MultiEdit`, which may be negative). `projected_size <=
shard_cap_bytes` (inclusive boundary) returns `Continue`; `> shard_cap_bytes` triggers the roll
sequence.

### AC-003: Cross-Validator Minimum Rule (traces to BC-1.18.005 postcondition 5)

An artifact's effective `shard_cap_bytes` is the MINIMUM across every Cohort B validator that reads
it (e.g., `burst-log.md`'s cap is `MIN(cap_for(validate-burst-log), cap_for(regression-gate),
cap_for(convergence-tracker))`; `decision-log.md`/`lessons.md`/`session-checkpoints.md`'s cap
excludes `validate-burst-log`'s per-plugin cap, since that validator does not read them). A single
global cap across all four mechanism-A artifacts MUST NOT be used.

### AC-004: Cap-formula constants are config, not hardcoded — F4 calibration harness (traces to BC-1.18.005 postcondition 6, postcondition 7)

`PRACTICAL_FUEL_CEILING`, `WORST_CASE_FUEL_PER_BYTE`, `MAX_SINGLE_RECORD_BYTES`, and
`SAFETY_MARGIN` are read from the `[[shard]]` config, never embedded as Rust constants in
`shard_manager.rs`. A synthetic calibration harness (performance-engineer, co-run with
implementer) locks the final numeric values before this BC's postconditions are treated as final,
re-run whenever `DEFAULT_FUEL_CAP` changes at the operator level; if a Cohort B plugin's fuel/byte
relationship is superlinear, `WORST_CASE_FUEL_PER_BYTE` uses the LOCAL marginal rate at the largest
tested size, never the global average slope.

### AC-005: Item-count trigger for the `"frontmatter-changelog-array"` shape, with `low_water_mark` config (traces to BC-1.18.005 postcondition 8, edge cases EC-008, EC-010, EC-011, EC-012, EC-014)

The SAME native gate dispatches to an item-count trigger (`current_item_count + 1 > N`) when the
`[[shard]]` config entry declares `shape = "frontmatter-changelog-array"`, distinguished from the
`"flat"` byte-size shape by that field alone (never content-sniffed). A sibling `low_water_mark`
config value (default `floor(N/2)` when omitted) declares the rotation TARGET — never `N` itself
and never a hardcoded `N-1`. Fail-loud (`HookResult::Error`) on `low_water_mark >= N` (the `== N`
boundary included) or negative; any legal value in `(floor(N/2), N)` up to and including `N-1`
loads normally but emits a non-fatal `tracing::warn!` amortization advisory.

**Missing-file / first-write create path (added S-25.02 Phase F4 LOCAL adversary cluster-1 pass-3
finding F-C1-P3-001, product-owner adjudication, BC-1.18.005 v1.9).** When the
`"frontmatter-changelog-array"`-shaped target artifact does not yet exist on disk — a legitimate
first-ever `Write` that CREATES the artifact — `read_changelog_item_count` MUST map
`io::ErrorKind::NotFound` to `Ok(0)`, treating the not-yet-existing artifact as holding 0 existing
changelog items, mirroring EC-004's flat-shape missing-file-is-zero precedent (BC-1.18.005
postcondition 8's missing-file/first-write sub-bullet). The item-count trigger then evaluates
`current_item_count + 1 = 0 + 1 = 1 > N`, which is `false` for any config `N >= 1`, so the create
always returns `Continue` — it is NEVER hard-blocked as `HookResult::Error`. Any OTHER `io::Error`
kind encountered reading an EXISTING file (a malformed frontmatter fence, a permissions failure,
etc.) is UNCHANGED and still propagates as `HookResult::Error` (fail-loud) — ONLY `NotFound` is
special-cased.

**VP deferral note:** this facet's formal verification property is NOT allocated this burst. Per
BC-1.18.005's own §Verification Properties adjudication note (v1.9, EC-014), product-owner routed
VP allocation to architect/formal-verifier as a SIXTH facet of VP-140 (or a new VP) — mirroring the
BC's own Postcondition-8→VP-140 precedent and this story's own AC-023 deferral for
Postcondition-9/EC-013. This story's `verification_properties:` frontmatter is therefore NOT
extended this burst; this facet's formal VP is owed to Phase F6 (targeted hardening). At F4, this
behavior is verified by unit test only (the missing-file → `Ok(0)` → `Continue` case, table-driven
alongside AC-005's existing item-count-trigger tests).

### AC-006: Staged, crash-recoverable roll-before-write sequence — copy-then-atomic-truncate, never rename-away (traces to BC-1.18.006 postcondition 1, invariant 2, invariant 3)

When a roll is triggered, the gate performs, in order, before returning any `HookResult`: (a) read
the canonical file's current content (once, roll-only); (b) publish the sealed shard as a brand-NEW
file at `<stem>.<seq:04>.md` via `write_atomic`; (c) atomically REPLACE the canonical file's content
with empty via the SAME `write_atomic` temp-file-then-rename primitive (never a delete-then-create,
never a rename of the canonical path away — the canonical path resolves to SOME valid file at every
observable instant); (d) atomically publish the updated shard-index TOML. Steps (a)-(b) failure
returns `HookResult::Error` (`E-SHD-001`) with the canonical file untouched; step (c) failure after
(b) succeeded self-heals via resume-from-truncate on the next dispatch (`E-SHD-006`); step (d)
failure after (c) succeeded self-heals via index reconciliation on the next dispatch (`E-SHD-007`).

### AC-007: Unified, single-template retry-instruction `Block` message (traces to BC-1.18.006 postcondition 2, invariant 4)

The observable outcome of an over-cap write is `HookResult::Block` with ONE fixed, unified
retry-instruction template (never a per-tool-name divergent wording) naming the artifact, the cap
reached, the fact the current shard is now empty, and explicit per-tool retry guidance (recompute
`content` for `Write`; re-read/reissue for `Edit`/`MultiEdit`, since `old_string` will no longer
match). Transparent write-redirection is never attempted (`HookResult`'s three-variant
`Continue`/`Block`/`Error` contract makes it structurally impossible).

### AC-008: No shard ever observed over cap; same-invocation shard+index atomicity (traces to BC-1.18.006 postcondition 3, postcondition 4)

The sealed shard's final size is always `<= shard_cap_bytes` (evaluated at seal time) and the new
current shard starts at exactly 0 bytes — a structural guarantee holding even if the agent never
retries. The shard-index TOML write and the sealed-shard publish are both filesystem writes issued
by the SAME PreToolUse invocation, before any `git add`/`git commit`, making TD-VSDD-053's
single-commit-per-burst hold structurally for shard+index atomicity.

### AC-009: Shard-index schema and stable-current-filename addressing (traces to BC-1.18.006 postcondition 5, postcondition 6)

Each sharded mechanism-A artifact has its own `<artifact-stem>.shard-index.toml`
(`schema_version`, `current_shard`, cap-formula constants, `retention_count`, and one `[[shard]]`
entry per seal with `seq`/`path`/`sealed_at`/`bytes_at_seal`). Because the canonical filename is
never renamed away, any shard-unaware reader that opens the canonical filename always sees the
current/latest shard, and the whole-corpus glob `<stem>*.md` sorts sealed shards before the current
file with zero special-casing.

### AC-010: Configurable retention count with same-invocation archival (traces to BC-1.18.007 postcondition 1, postcondition 2)

`retention_count` (default 10, config-value not hardcoded) lives in the shard-index. When a seal
crosses the retention threshold, the oldest sealed shard(s) beyond the window are moved (never
deleted) to `.factory/cycles/<cycle>/archive/<artifact-stem>/<sealed-filename>` in the SAME
native-gate invocation that performed the triggering seal.

### AC-011: Archive stays path_allow-visible but is excluded from default whole-corpus glob scope — honest O(active) accounting (traces to BC-1.18.007 postcondition 3, postcondition 4, invariant 3)

Existing directory-scoped `path_allow` globs continue matching archived shards transparently (no
validator capability-declaration amendment needed). The DEFAULT whole-corpus glob (`<stem>*.md` at
the cycle root) does NOT match `archive/<artifact-stem>/`; a reader wanting archived history must
explicitly opt in with a second glob pass. Default (non-opted-in) whole-corpus validators are
honestly `O(active shards)`, bounded by `retention_count`, never `O(all shards ever)`. The
shard-index remains the single source of truth for an artifact's true lifetime shard count
regardless of archival state.

### AC-012: SS-04 archive-inclusive-glob obligation for POLICY-1 enforcement (traces to BC-1.18.007 Postcondition 6, BC-1.18.007 EC-006; ADR-051 §Decision 6)

The four SS-04-owned WASM crates (`validate-dispatch-advance`, `validate-state-structure`,
`validate-closes-completeness`, `validate-cross-site-correspondence`) are audited for
archival-scheme correctness and confirmed NOT to require amendment (each is either STATE.md-scoped
or already correctly current-shard-scoped). POLICY-1's (`append_only_numbering`)
`consistency-validator`/adversary-prompt-level enforcement is the one genuine whole-corpus gap
identified and MUST default to an archive-inclusive scan mode — an explicit, documented carve-out
from AC-011's opt-in-required default, not a silent inconsistency.

### AC-013: Mandatory one-time backfill-split of the four pre-existing oversized append-logs (traces to BC-1.18.008 postcondition 1, postcondition 2, postcondition 3)

Executed exactly once, at F4 activation (never as an ongoing mechanism), the backfill-split
partitions each of the four pre-existing monolithic files into `ceil(current_bytes /
shard_cap_bytes)` sealed shards plus a fresh current file, splitting ONLY at structural record
boundaries native to the artifact's own format (never mid-record). The full shard index is
published for the complete pre-existing history in the same operation, composing immediately with
AC-010's retention policy if the resulting shard count already exceeds `retention_count`.

### AC-014: Backfill-split content-preservation verification, crash-atomicity, idempotency (traces to BC-1.18.008 postcondition 5, postcondition 6, invariant 3)

Before the original monolithic file's role is retired, the backfill-split verifies: (a) the
concatenation of all resulting shards in order reproduces the original file byte-for-byte, and (b)
every record appears in exactly one resulting shard (never zero, never two). This is a hard gate —
failure aborts the operation with the original file left untouched (`E-SHD-003`, fail-loud, never
partial-and-silent). The operation is safely re-runnable from scratch after an interruption, and
idempotent against an already-migrated artifact.

### AC-015: B1 single-actor block-and-retry via `rotate_changelog` only — never a gate-side prepend (traces to BC-1.18.009 postcondition 2, postcondition 3, invariant 1, invariant 4)

On the `BC-INDEX.md` frontmatter `changelog:` item-count trigger firing, the gate invokes
`rotate_changelog` ONLY — trimming the live sequence to `low_water_mark` items (never a hardcoded
`N-1`) and appending the overflow tail to a SINGLE, evergreen archive file at
`.factory/specs/behavioral-contracts/BC-INDEX-changelog-archive.md` (reached via a small, named,
additive `archive_path`-parameterized extension to `rotate_changelog`'s existing path-resolution
surface — never a per-`seq` sealed-shard-directory layout) — THEN returns `HookResult::Block` with
a shape-appropriate retry instruction. The gate NEVER calls `prepend_changelog_item`; the new
item's prepend is exclusively the responsibility of whichever `Edit`/`Write`/`MultiEdit` call
ultimately lands (original or retried) — a strict structural mirror of BC-1.18.006's actor-ownership
contract, never a "rotate-and-Continue" divergence.

### AC-016: B1 fail-loud rotation failure and no-history-loss archive (traces to BC-1.18.009 postcondition 5, postcondition 6, edge case EC-003)

If `rotate_changelog` itself fails, the gate returns `HookResult::Error` (`E-SHD-004`,
`rotate_changelog` invocation failure — never the mechanism-A `E-SHD-001` code), leaving the
frontmatter in its exact pre-rotation state. Every `rotate_changelog` invocation appends to the
SAME single evergreen archive file — no prior content is ever overwritten, truncated, or deleted by
a subsequent rotation; full changelog history remains reconstructable by reading the archive file
followed by the current frontmatter sequence.

### AC-017: B2 zero-lookup first-level addressing and manifest-keyed second-level sub-sharding (traces to BC-1.18.010 postcondition 1, postcondition 2, postcondition 4)

Each of `BC-INDEX.md`'s ten `### SS-NN` sections is extracted verbatim to its own
`shards/BC-INDEX-SS-NN.md`. First-level addressing (which shard holds a given `BC-X.YY.NNN`) is a
PURE FUNCTION of the BC ID's numeric prefix via the already-authoritative ARCH-INDEX
`BC-S Prefix`→`SS-NN` mapping (POLICY 6) — zero shard-manifest reads for the common single-BC
lookup. SS-05 and SS-06 (both already over the provisional cap on section size alone) receive
manifest-keyed second-level sub-sharding, since sub-shard boundaries are growth-based, not
ID-prefix-deterministic; the same size-check gate triggers second-level sub-sharding for any future
subsystem that grows over cap.

### AC-018: B2 governed one-time migration — content-preservation, independent census, crash-atomicity, rollback (traces to BC-1.18.011 postcondition 1, postcondition 2, postcondition 3, postcondition 4)

The one-time transition from `BC-INDEX.md`'s monolithic body to AC-017's end-state verifies, before
promoting any split output as authoritative: (a) byte-for-byte content-preservation across the
concatenation of all resulting shard files plus the retained lean top-level body, and (b) an
independent census confirming every `BC-X.YY.NNN` ID (cross-checked against the `total_bcs`
frontmatter field as a sanity bound) appears in EXACTLY ONE resulting shard — covering SS-05/SS-06's
second-level sub-split within the SAME operation. Either check failing ABORTS the migration with
the original body left completely untouched (`E-SHD-005`, fail-loud rollback); the migration has
NO ordering dependency on BC-1.18.008's mechanism-A backfill or on BC-7.08.001's Cohort B flip.

### AC-019: B1 governed one-time cold-start changelog backfill migration (traces to BC-1.18.012 postcondition 1, postcondition 2, postcondition 3)

Executed exactly once at F4 activation, BEFORE BC-1.18.005's steady-state read-cost
characterization is treated as true, this migration invokes `rotate_changelog` with `keep_recent =
low_water_mark` (never `keep_recent = N`) against `BC-INDEX.md`'s cold-start `changelog:` sequence
(~1,997 unrotated items measured 2026-09-05), appending the overflow tail to the SAME single
evergreen archive file AC-016 establishes. An independent pre-migration census verifies `(items
retained) + (items appended to archive) == (pre-migration count)` exactly; a mismatch aborts the
migration with the frontmatter left in its exact pre-migration state (`E-SHD-003`, reused). This
migration has NO ordering dependency on BC-1.18.008 or BC-1.18.011.

### AC-020: Cohort B fail-closed flip — corrected plugin identity, hard-gated sequencing (traces to BC-7.08.001 postcondition 1, postcondition 2, precondition 3)

`hooks-registry.toml` assigns `failure_policy = "fail-closed"` to exactly three registry entries:
`validate-burst-log`'s `event = "PostToolUse" tool = "^(Edit|Write|MultiEdit)$"` arm ONLY (its
sibling `tool = "^Bash$"` git-commit chain-detection arm is explicitly NEVER touched by this
story), `regression-gate`, and `convergence-tracker`. This registry change MUST NOT be deployed in
the same release as (or before) AC-013/AC-014's backfill-split completing — it is a SUBSEQUENT
deployment, after production observation confirms the shard-capped corpus no longer produces
`cause=fuel` INDETERMINATE events for these three plugins.

### AC-021: Per-validator calibration evidence and closed three-entry cohort (traces to BC-7.08.001 postcondition 3, postcondition 5, invariant 3)

For each of the three validators, the PR performing the registry flip cites measured fuel
consumption against the largest post-split shard size for every artifact it reads, confirming this
falls comfortably under the F4-locked `PRACTICAL_FUEL_CEILING` (ADR-039 §Decision 3 calibration
discipline). Calibration may pass per-validator independently — the three do not need to flip
atomically in lockstep. Cohort B remains exactly three entries; no other currently-fail-open
validator is flipped as a side effect, and any future expansion requires its own BC and its own
ADR-039 §Decision 3 gate.

### AC-022: Full regression suite stays green; Cohort A contract unchanged (traces to BC-7.08.001 postcondition 4)

`cargo test --workspace --all-targets`, `cargo clippy --workspace --all-targets -- -D warnings`,
and `plugins/vsdd-factory/tests/run-all.sh` MUST stay green after this story lands. Once flipped,
the three Cohort B validators follow BC-1.18.001/002/003's existing fail-closed INDETERMINATE
contract exactly like the three Cohort A validators — this story introduces no new
INDETERMINATE-handling semantics, only the flip and its sequencing gate.

### AC-023: Load-time fail-loud enforcement of cap-vs-formula inequality (traces to BC-1.18.005 postcondition 9, edge case EC-013)

For EVERY `[[shard]]` config entry — regardless of `shape` (Postcondition 8 already establishes that
`shard_cap_bytes` bounds an artifact's TOTAL byte footprint as a whole-artifact concern even for the
`"frontmatter-changelog-array"` shape, so this check is NOT `"flat"`-shape-only) — `ShardRegistry::load()`
independently recomputes `compute_shard_cap_bytes(entry.cap_formula_inputs())` from that SAME entry's
own four parsed formula inputs (`practical_fuel_ceiling`, `worst_case_fuel_per_byte`,
`max_single_record_bytes`, `safety_margin`) and compares it against that entry's own declared
`shard_cap_bytes`. If the declared `shard_cap_bytes` is GREATER than the computed ceiling, `load()`
returns `HookResult::Error` — the misconfigured entry is REJECTED; it is NEVER silently accepted and
NEVER silently clamped down to the computed ceiling. A declared `shard_cap_bytes` exactly EQUAL to the
computed ceiling (boundary case) loads normally (`Ok`) — the comparison is inclusive of the boundary,
mirroring EC-002's inclusive-boundary precedent for the per-write trigger. This is a load-time
misconfiguration guard over a `[[shard]]` entry's own internal consistency between its declared cap and
its own declared four inputs; it does not require or perform any re-derivation of the F4 harness's
calibration itself (BC-1.18.005 Postcondition 6/7 continue to own HOW the constants are measured).

**VP deferral note:** this postcondition's formal verification property is NOT allocated this burst.
Per BC-1.18.005's own §Verification Properties adjudication note (Postcondition 9/EC-013),
product-owner routed VP allocation to architect/formal-verifier — mirroring the BC's own
Postcondition-8→VP-140 precedent (added at v1.1 without a VP; VP-140 allocated by formal-verifier in a
later burst) and consistent with VP-116's kani-proof deferral pattern. This story's
`verification_properties:` frontmatter is therefore NOT extended this burst; PC9's formal VP is owed to
Phase F6 (targeted hardening). At F4, this AC's behavior is verified by unit test only (table-driven
over the `[[shard]]` config surface: declared-cap-above-ceiling → `HookResult::Error`;
declared-cap-equal-to-ceiling → `Ok`).

## Behavioral Contracts

| BC ID | Title | Version | Role |
|-------|-------|---------|------|
| BC-1.18.005 | Byte-Size-Denominated Shard-Cap Formula and Native Deterministic Size-Trigger (Provisional Constants Pending F4 Harness Calibration) | v1.9 | Owns the cap formula and BOTH trigger shapes (mechanism-A byte-size `stat()`-only; mechanism-B1 item-count) the native gate dispatches on; also owns load-time fail-loud enforcement of the cap-vs-formula inequality (Postcondition 9) and the item-count shape's missing-file/first-write `NotFound→Ok(0)` graceful-degradation behavior (EC-014) |
| BC-1.18.006 | Roll-Before-Write via Block-and-Retry (Not Transparent Redirection) Plus Same-Invocation Atomic Shard-Index Publication | v1.3 | Owns mechanism A's observable roll/block outcome once BC-1.18.005's byte-size trigger fires |
| BC-1.18.007 | Shard Retention/Compaction — Configurable Retention Count, Archive Relocation, and Honest O(Active-Shards) Whole-Corpus Accounting | v1.2 | Mandatory companion policy bounding shard count growth; SS-04 archive-glob obligation source |
| BC-1.18.008 | Mandatory One-Time Backfill-Split of the Four Pre-Existing Oversized Cycle Append-Logs | v1.1 | Governed one-time migration retroactively sharding the four pre-existing oversized files; hard Cohort-B-flip precondition |
| BC-1.18.009 | BC-INDEX Frontmatter `changelog:` Array Auto-Rotation (Mechanism B1, Reusing `rotate_changelog`) — Single-Actor Block-and-Retry | v1.5 | Owns mechanism B1's observable rotate/block-and-retry outcome once BC-1.18.005's item-count trigger fires |
| BC-1.18.010 | BC-INDEX Per-Subsystem Body-Table Sharding (Mechanism B2) with Zero-Lookup First-Level Addressing and Manifest-Based Second-Level Sub-Sharding | v1.2 | Specifies mechanism B2's end-state addressing scheme (not the transition) |
| BC-1.18.011 | Governed One-Time Migration for the B2 BC-INDEX Body Split (Content-Preservation, Independent Census, Crash-Atomicity, Rollback) | v1.0 | Governed one-time migration producing BC-1.18.010's end-state; NO Cohort-B dependency |
| BC-1.18.012 | Governed One-Time B1 Changelog Backfill Migration (Cold-Start `changelog:` Array Trim via `rotate_changelog`) | v1.1 | Governed one-time migration establishing the precondition for BC-1.18.009's steady-state read-cost characterization; NO Cohort-B dependency |
| BC-7.08.001 | Cohort B `failure_policy = fail-closed` Flip for `validate-burst-log` (Edit/Write/MultiEdit Arm Only), `regression-gate`, and `convergence-tracker` — Gated on Shard-Cap Enforcement AND Backfill-Split Completion | v1.1 | The capstone flip, hard-gated on BC-1.18.005/006/008 holding in production |

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| Cap-formula evaluation, `[[shard]]` config resolution, shape dispatch | `crates/factory-dispatcher/src/shard_manager.rs` (new module) | Pure-core (arithmetic on config values, no I/O) |
| Native `stat()`-only size check (mechanism A) / bounded frontmatter parse (mechanism B1 item-count read) | `crates/factory-dispatcher/src/shard_manager.rs` | Effectful (filesystem read) |
| Roll-before-write: publish sealed shard, atomic-truncate canonical, publish shard-index | `crates/factory-dispatcher/src/shard_manager.rs` | Effectful (filesystem write) |
| Retention/compaction archival move | `crates/factory-dispatcher/src/shard_manager.rs` | Effectful (filesystem write) |
| Mechanism-A one-time backfill-split; B1 one-time cold-start migration; B2 one-time body-split migration | `crates/factory-dispatcher/src/shard_manager.rs` (migration entry points) | Effectful (filesystem write, staging + atomic replace) |
| PreToolUse dispatch invocation point, before the registry-driven plugin loop | `crates/factory-dispatcher/src/executor.rs` | Effectful (dispatcher control flow) |
| `block_if_marker_check` native-check precedent this story's placement pattern follows | `crates/factory-dispatcher/src/indeterminate_marker.rs` | Effectful (precedent, read-only reference) |
| `rotate_changelog`/`rewrite_source_after_rotation`/`resolve_archive_path` (reused; gains an additive `archive_path`-parameterized call surface for non-cycle-scoped callers) | `crates/last-amended-migrate/src/rotate.rs` | Effectful (filesystem read/write) |
| `write_atomic` (reused, temp-file-then-rename atomic-write primitive) | `crates/last-amended-migrate/src/atomic_write.rs` | Effectful (filesystem write) |
| `prepend_changelog_item` (agent-side tooling ONLY — never called by the gate) | `crates/last-amended-migrate/src/changelog.rs` | Effectful (reference only; not a gate call site) |
| `[[shard]]` config table, `shape`/`N`/`low_water_mark` fields | `plugins/vsdd-factory/hooks-registry.toml` (or sibling `shard-config.toml`, TBD at F4 implementation) | Configuration |
| Cohort B fail-closed registry updates | `plugins/vsdd-factory/hooks-registry.toml` | Configuration |
| SS-04 archival-scheme audit touchpoints (verified NOT requiring code amendment) | `crates/hook-plugins/validate-dispatch-advance/src/lib.rs`, `validate-state-structure/src/lib.rs`, `validate-closes-completeness/src/lib.rs`, `validate-cross-site-correspondence/src/arm_b.rs` | Effectful (reference-only; audit confirms no change needed) |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|----------------|
| Shard-cap formula computation; shape dispatch (`"flat"` vs `"frontmatter-changelog-array"` vs `"per-subsystem-body-table"`) | Pure-core | Arithmetic/config-lookup on already-loaded config values; no I/O |
| Mechanism-A `stat()`-only size check | Effectful (read) | Reads current shard byte size via filesystem metadata only |
| Mechanism-B1 item-count read (steady-state, bounded; cold-state, one-time-oversized) | Effectful (read) | Reads and lightly parses frontmatter content — more than `stat()`, still fuel-budget-free native code |
| Roll-before-write: publish sealed shard + atomic-truncate canonical + publish index | Effectful (write) | Three independent `write_atomic` temp-file-then-rename calls, each its own crash point |
| Retention/compaction archival move | Effectful (write) | Filesystem move + shard-index update |
| Three one-time governed migrations (BC-1.18.008/011/012) | Effectful (write, staged) | Staging + verify + atomic-replace against pre-existing content; content-preservation/census verification is itself pure computation over read content |
| Cohort B `hooks-registry.toml` flip | Configuration (declarative) | TOML edit; no runtime code path |

## Edge Cases

| ID | Source BC | Description | Expected Behavior |
|----|-----------|-------------|-------------------|
| EC-001 | BC-1.18.005 | `projected_size` exactly equals `shard_cap_bytes` | `Continue` — inclusive boundary, no roll |
| EC-002 | BC-1.18.005 | `projected_size` exceeds `shard_cap_bytes` by exactly 1 byte | Roll triggers |
| EC-003 | BC-1.18.005 | The matched artifact's current shard does not yet exist (first write ever) | `current_shard_bytes` treated as 0 |
| EC-004 | BC-1.18.005 | `MultiEdit` with some net-negative, some net-positive edit blocks | `payload_bytes` is the SUM of each edit's net delta (may be negative overall) |
| EC-005 | BC-1.18.005 | A `[[shard]]` config entry omits the `shape` field entirely | Fail-loud `HookResult::Error` — never a silent default to either shape |
| EC-006 | BC-1.18.005 | A `"frontmatter-changelog-array"` entry declares `low_water_mark >= N` (incl. `== N`) or negative | Fail-loud `HookResult::Error`; `low_water_mark = N-1` is VALID, not in this scope |
| EC-007 | BC-1.18.005 | A `"frontmatter-changelog-array"` entry declares a legal-but-poorly-amortizing `low_water_mark` in `(floor(N/2), N)` incl. `N-1` | Loads normally + non-fatal `tracing::warn!` amortization advisory |
| EC-008 | BC-1.18.006 | Single appended block exceeds shard cap | Roll to new shard BEFORE the write is applied; the write itself is blocked, never partially applied |
| EC-009 | BC-1.18.006 | Roll-sequence step (a)-(b) fails (disk full/permission error) before the sealed shard is durably published | `HookResult::Error` (`E-SHD-001`); canonical file left in exact pre-roll state |
| EC-010 | BC-1.18.006 | Step (c) (atomic-truncate) fails after step (b) succeeded | `E-SHD-006`: self-healing resume-from-truncate on next dispatch |
| EC-011 | BC-1.18.006 | Step (d) (index publish) fails after step (c) succeeded | `E-SHD-007`: self-healing index reconciliation on next dispatch |
| EC-012 | BC-1.18.006 | An `Edit`/`MultiEdit` targets content already relocated to a sealed shard by an earlier roll | Retry text is inapplicable; the sealed shard is an ordinary, ungated file the caller addresses directly by filename |
| EC-013 | BC-1.18.007 | Shard count reaches `retention_count` exactly | Oldest shard archived in the SAME invocation as the triggering seal |
| EC-014 | BC-1.18.007 | `retention_count` lowered mid-flight while more shards are active than the new limit | Next seal archives enough of the oldest shards to bring the active count within the new limit, potentially archiving more than one in a single invocation |
| EC-015 | BC-1.18.007 | Shard-index missing or corrupt at retention-check time | Fail-loud `HookResult::Error` (`E-SHD-002`); write blocked; no archival attempted |
| EC-016 | BC-1.18.008 | An artifact's byte count at F4 execution time is smaller than `shard_cap_bytes` | `ceil(bytes/cap) = 1` — no sealing; a shard-index IS still created with zero `[[shard]]` entries |
| EC-017 | BC-1.18.008 | A single record is itself larger than `shard_cap_bytes` | NOT truncated/split; that shard is allowed to exceed cap for that ONE record, flagged `oversized_record: true` |
| EC-018 | BC-1.18.009 | `current_item_count` reaches `N` again after a prior rotation (amortized re-trigger) | NOT a defect — the SAME single-actor block-and-retry contract applies identically to the re-trigger |
| EC-019 | BC-1.18.009 | An implementer mistakenly has the gate call `prepend_changelog_item` after a successful `rotate_changelog`, then returns `Continue` | A static-analysis check (VP-126) MUST detect any `prepend_changelog_item` call site inside `shard_manager.rs`'s B1 handler as a defect |
| EC-020 | BC-1.18.010 | A reader looks up `BC-5.39.006` (SS-05, sub-sharded) vs. `BC-1.18.005` (SS-01, not sub-sharded) | SS-05 lookup requires TWO manifest reads (top-level + sub-manifest); SS-01 lookup requires ZERO |
| EC-021 | BC-1.18.011 | Content-preservation passes but the independent census finds a duplicated row across two shard files | Migration ABORTS — passing one check is not sufficient; both are independently mandatory |
| EC-022 | BC-1.18.011 | SS-05/SS-06's second-level sub-split census fails | Migration ABORTS for the ENTIRE operation, not just the failing subsystem — no partial-success outcome |
| EC-023 | BC-1.18.012 | Migration re-run against a `changelog:` sequence already `<= N` items | Idempotent no-op — zero writes, zero `rotate_changelog` invocations |
| EC-024 | BC-7.08.001 | The flip is attempted before BC-1.18.008's backfill-split has completed | MUST NOT be deployed — would reproduce the exact self-inflicted-DoS scenario Cohort A/B partitioning was designed to prevent |
| EC-025 | BC-1.18.005 | A `[[shard]]` config entry declares `shard_cap_bytes` GREATER than `compute_shard_cap_bytes(entry.cap_formula_inputs())` (applies to ANY `shape`, not `"flat"`-only) | Fail-loud: `ShardRegistry::load()` returns `HookResult::Error` — the misconfigured entry is REJECTED; NEVER silently accepted, NEVER silently clamped down to the computed ceiling (BC-1.18.005 postcondition 9, EC-013) |
| EC-026 | BC-1.18.005 | A `"frontmatter-changelog-array"`-shaped target artifact does not yet exist on disk (a legitimate first-ever `Write` CREATES it) | `read_changelog_item_count` maps `io::ErrorKind::NotFound` to `Ok(0)` — treated as 0 existing items, mirroring EC-003's flat-shape missing-file precedent; the item-count trigger (`current_item_count + 1 > N`) evaluates `0 + 1 > N`, `false` for any `N >= 1` → `Continue` — the legitimate create is NEVER hard-blocked as `HookResult::Error`. Any OTHER `io::Error` kind reading an EXISTING file is UNCHANGED and still propagates as `HookResult::Error` (BC-1.18.005 postcondition 8, EC-014) |

## Token Budget Estimate (MANDATORY)

> **Exceeds the normal 20-30% single-story guideline — an explicit, documented exception.** See
> mitigation note below and the `last_amended` re-estimation rationale.

| Context Source | Estimated Tokens |
|---------------|-----------------|
| Story spec (S-25.02, this file) | 8,600 |
| ADR-051 full text (14 decisions + rationale + alternatives + status/changelog, v1.8) | 18,000 |
| BC-1.18.005 (cap formula + both trigger shapes + Postcondition 9 load-time cap-vs-formula guard + Postcondition 8 missing-file/first-write `NotFound→Ok(0)` sub-bullet, v1.9) | 6,000 |
| BC-1.18.006 (roll-before-write + 3-crash-point partial-failure) | 4,500 |
| BC-1.18.007 (retention/compaction) | 2,000 |
| BC-1.18.008 (mechanism-A one-time backfill-split) | 2,500 |
| BC-1.18.009 (B1 single-actor rotation) | 4,500 |
| BC-1.18.010 (B2 end-state addressing) | 3,000 |
| BC-1.18.011 (B2 one-time migration) | 3,000 |
| BC-1.18.012 (B1 one-time cold-start migration) | 3,000 |
| BC-7.08.001 (Cohort B flip) | 2,500 |
| VP-116..VP-141 (26 VPs incl. proof-method assignments) (9 BC files above already counted) | 6,000 |
| `prd-supplements/error-taxonomy.md` (`E-SHD-001..007`) | 800 |
| `plugins/vsdd-factory/hooks-registry.toml` (Cohort B entries + surrounding context) | 1,000 |
| `crates/factory-dispatcher/src/indeterminate_marker.rs` (native-check precedent) | 2,000 |
| `crates/last-amended-migrate/src/rotate.rs` + `atomic_write.rs` (existing primitives) | 2,000 |
| S-25.01 (predecessor story — Layer-1 forensics context) | 3,000 |
| Cap-constant numeric bounds table (BC-1.18.005 Postcondition 6) | 1,300 |
| **Total** | **~74,700** |
| Agent context window | 200,000 (Sonnet) |
| **Budget usage** | **~37%** |

**Mitigation (required, per this story's own token-budget overage):** implementer/test-writer MUST
load only the BC(s) relevant to the specific T-N task in flight (e.g., T-1/T-2 load ONLY
BC-1.18.005; T-9 loads ONLY BC-7.08.001 + `hooks-registry.toml`) rather than all 9 BCs + the full
ADR-051 simultaneously — mirroring this project's own DF-021 architecture-context-discipline
convention. The 36% figure above is the worst-case ceiling if every input were loaded at once,
which no single T-N task requires.

### Routing Note — Points/Budget Ceiling Exception (surfaced, not silently absorbed)

Both `points: "45"` and the ~36% token-budget figure above EXCEED this project's own
story-writer-ceiling conventions (`points` 1-13; token budget 20-30% of the implementing agent's
context window), per the STORY-INDEX.md "Rules" section. This is an intentional, human-directed
deviation, not a story-writer oversight — mirroring the established precedent at S-21.11 (32
points, exceeds-13 explicitly surfaced in that story's own Routing Note rather than silently
absorbed or split). The deviation is grounded in D-1166 (human widest-scope decision: both
mechanisms — and, after F2's forensic-driven scope widening, all three governed migrations plus the
Cohort B flip — stay in ONE story rather than splitting into S-25.02a/b/c). Per CLAUDE.md's
Companion Principle (correct-agent-routing, not silent absorption), story-writer surfaces this
ceiling exception for the record rather than re-litigating D-1166 or unilaterally splitting the
story. If a future cycle boundary makes a split preferable (e.g., to bring individual PRs under
the per-story token budget for wave-scheduling purposes), that is an architect/orchestrator
routing decision requiring its own human check-in — not assumed here.

## Tasks (MANDATORY)

1. [ ] **T-1** — Implement `crates/factory-dispatcher/src/shard_manager.rs`: `[[shard]]` config
       parsing, shape dispatch (`"flat"` / `"frontmatter-changelog-array"` /
       `"per-subsystem-body-table"`), cap-formula arithmetic (BC-1.18.005 AC-001..AC-004).
2. [ ] **T-2** — Wire `shard_manager.rs`'s PreToolUse check into `executor.rs`, before the
       registry-driven plugin loop, following the `block_if_marker_check` native-check precedent
       (BC-1.18.005 AC-001).
3. [ ] **T-3** — Implement the item-count trigger + `low_water_mark` config validation for the
       `"frontmatter-changelog-array"` shape (BC-1.18.005 AC-005).
4. [ ] **T-4** — Implement the staged 4-step roll-before-write sequence (read/publish-sealed/
       atomic-truncate/publish-index) with the 3 named crash-point error codes `E-SHD-001`/
       `E-SHD-006`/`E-SHD-007` and their self-healing recovery paths (BC-1.18.006 AC-006..AC-009).
5. [ ] **T-5** — Implement retention/compaction: `retention_count` config, same-invocation
       archival move to `archive/<artifact-stem>/`, default-glob exclusion (BC-1.18.007
       AC-010..AC-011). Confirm the SS-04 four-crate archival-scheme audit finding (AC-012) — no
       code change expected in those crates, only the POLICY-1 archive-inclusive-glob default.
6. [ ] **T-6** — Implement the mechanism-A one-time backfill-split migration entry point:
       record-boundary-safe partitioning, content-preservation verification gate, crash-atomicity,
       idempotency (BC-1.18.008 AC-013..AC-014).
7. [ ] **T-7** — Extend `crates/last-amended-migrate/src/rotate.rs`'s `resolve_archive_path` with
       the additive, explicit `archive_path: &Path` parameter (or equivalent
       `resolve_archive_path_at`) for non-cycle-scoped callers; verify every existing cycle-scoped
       caller is unaffected (BC-1.18.009 AC-015, SDK Grounding Evidence in BC-1.18.009).
8. [ ] **T-8** — Implement the B1 `"frontmatter-changelog-array"` handler: calls `rotate_changelog`
       ONLY (zero `prepend_changelog_item` call sites — static-analysis-verifiable per VP-126),
       single evergreen archive file, block-and-retry (BC-1.18.009 AC-015..AC-016).
9. [ ] **T-9** — Implement the B1 one-time cold-start changelog backfill migration
       (`keep_recent = low_water_mark`, independent census reconciliation) (BC-1.18.012 AC-019).
10. [ ] **T-10** — Implement the B2 end-state addressing: first-level extraction to
        `shards/BC-INDEX-SS-NN.md`, zero-lookup pure-function addressing via the ARCH-INDEX
        `BC-S Prefix` mapping, second-level manifest-based sub-sharding for SS-05/SS-06
        (BC-1.18.010 AC-017).
11. [ ] **T-11** — Implement the B2 one-time governed migration: content-preservation,
        independent census (cross-checked against `total_bcs`), crash-atomicity, rollback,
        covering SS-05/SS-06's sub-split in the same operation (BC-1.18.011 AC-018).
12. [ ] **T-12** — Run the F4 synthetic calibration harness (performance-engineer, co-run with
        implementer) to lock the cap-formula constants; THEN perform the Cohort B
        `hooks-registry.toml` flip for `validate-burst-log`'s Edit/Write/MultiEdit arm,
        `regression-gate`, and `convergence-tracker` ONLY — never the `^Bash$` arm — with
        per-validator ADR-039 §Decision 3 calibration evidence cited in the PR (BC-7.08.001
        AC-020..AC-021).
13. [ ] **T-13** — Write RED Gate test stubs (failing `todo!()` bodies) for all 23 ACs above; run
        `cargo test --workspace --all-targets`, `cargo clippy --workspace --all-targets -- -D
        warnings`, and `plugins/vsdd-factory/tests/run-all.sh` (AC-022); add a CHANGELOG.md entry
        under `[Unreleased] > Added` describing the shipped shard-rotation capability, BEFORE
        creating the PR.

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-25.01 (Layer 1 — predecessor, MERGED) | INDETERMINATE outcome classification + durable marker + next-advance gate shipped. Cohort A (3 validators) assigned fail-closed. `plugin.indeterminate` event forensics now available in production — BC-INDEX.md alone is 45.2% of the first 708 events, directly motivating this story's D-1166 widest-scope decision to cover BC-INDEX's own two size drivers, not just the four append-logs. | `indeterminate_marker.rs`'s atomic-write (temp+rename) pattern, reused throughout this story as the `write_atomic`/shard-index-publish precedent. `block_if_marker_check`'s native-(non-WASM)-check placement in `executor.rs`, reused as this story's own PreToolUse placement pattern for `shard_manager.rs`. Many-small-implementer-commits-under-one-PR delivery pattern for a large, multi-BC story — this story's own 45-point size makes that pattern load-bearing, not optional. | S-25.01's own LOCAL adversary cascade needed 18 passes to reach 3-CLEAN, with several BLOCKER/HIGH findings surfacing only after multiple "clean-looking" passes (e.g., pass-15's PC1 trace_id regression that survived 14 prior passes) — this story's spec-side adversarial cascade already absorbed an analogous lesson (12 F2 passes to 3-CLEAN, several BLOCKER-class corrections: B1's double-actor prepend hazard in pass-1, the impossible per-`seq` archive-path citation in pass-2, the `N-1` fail-loud/VALID contradiction in pass-4) — implementer should expect the SAME pattern at F4/TDD time and not treat an early clean-looking pass as convergence. |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| The size/item-count check is native code, never a WASM plugin — no `[[hooks]]` registry entry, no `.wasm` binary, no `HOST_ABI_VERSION` bump | BC-1.18.005 invariant 1 | Code review; the check must be structurally incapable of producing a `plugin.timeout`/`plugin.indeterminate` event |
| Cap-formula constants (`PRACTICAL_FUEL_CEILING`, `WORST_CASE_FUEL_PER_BYTE`, `MAX_SINGLE_RECORD_BYTES`, `SAFETY_MARGIN`, `N`, `low_water_mark`) are config, never embedded Rust constants | BC-1.18.005 invariant 4 | AC-004/AC-005; F4 harness recalibration must be a config change, not a code change |
| Roll-before-write is copy-then-atomic-truncate-in-place — the canonical filename is NEVER renamed away, only its content is replaced | BC-1.18.006 invariant 2, invariant 3 | AC-006; the canonical path must resolve to SOME valid file at every observable instant (unit test asserts `stat(canonical_path)` never fails across a roll sequence) |
| `HookResult::Block` is the ONLY variant returned on an over-cap/over-N-item write — never `Continue` after determining the trigger fired, never `Error` for a normal (non-crash) rotation | BC-1.18.006 invariant 1; BC-1.18.009 invariant 4 | AC-007, AC-015; static-analysis + unit test |
| The B1 gate calls `rotate_changelog` ONLY — zero `prepend_changelog_item` call sites inside `shard_manager.rs`'s B1 handler | BC-1.18.009 invariant 1 | AC-015; VP-126 static-analysis grep, CI-enforceable |
| Retention/compaction is move-only — archival NEVER deletes, truncates, or lossily compresses a sealed shard's content | BC-1.18.007 invariant 1 | AC-010/AC-011 |
| All three one-time migrations (BC-1.18.008/011/012) are idempotent and safely re-runnable from an interrupted state — never double-split, never double-archive | BC-1.18.008 invariant 3; BC-1.18.011 postcondition 5; BC-1.18.012 postcondition 7 | AC-014, AC-018, AC-019; fault-injection integration tests |
| Cohort B fail-closed flip ONLY after BC-1.18.008's backfill-split has COMPLETED and per-validator ADR-039 §Decision 3 calibration evidence is recorded — never deployed in the same release as, or before, the backfill-split | BC-7.08.001 precondition 3, invariant 2 | AC-020/AC-021; PR-template gate requiring the backfill-split PR's merge SHA to be an ancestor of the Cohort B flip PR (VP-130) |
| The `validate-burst-log` `^Bash$` chain-detection arm is NEVER touched by the Cohort B flip | BC-7.08.001 invariant 1 | AC-020; static-check config-diff (VP-129) asserting the `^Bash$` entry's `failure_policy` is absent both before and after |
| **Forbidden dependency:** `crates/last-amended-migrate` MUST NOT gain a dependency on `crates/factory-dispatcher` | BC-1.18.009 precondition 3 (ADR-051 §8 dependency-acyclicity check) | Build-time enforcement — `factory-dispatcher` → `last-amended-migrate` is the ONLY allowed direction of this new workspace-internal edge; if `last-amended-migrate` ever gains a `factory-dispatcher` dependency, the build MUST fail (cargo dependency-cycle detection) |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| wasmtime (workspace) | pinned per workspace `Cargo.toml` (no new WASM plugin introduced by this story — the shard-cap check is native, per BC-1.18.005 invariant 1; wasmtime is unaffected) | N/A — cited only to confirm no version bump is required |
| serde / toml (workspace) | workspace-pinned versions | `[[shard]]` config parsing, shard-index TOML serialization, shard-manifest TOML serialization |
| tempfile (workspace, if not already a dependency of `factory-dispatcher`) | workspace-pinned version if added | Staging location for the 3 one-time migrations' write-then-verify-then-atomic-replace sequence |
| No new external crate dependencies | N/A | This story reuses `write_atomic` (`last-amended-migrate::atomic_write`) and `rotate_changelog` (`last-amended-migrate::rotate`) verbatim; the only new workspace-internal edge is `factory-dispatcher` → `last-amended-migrate` (confirmed acyclic, ADR-051 §8) |

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| `crates/factory-dispatcher/src/shard_manager.rs` | create | New module: cap-formula, shape dispatch, roll-before-write, retention/compaction, all 3 one-time migration entry points |
| `crates/factory-dispatcher/src/executor.rs` | modify | Wire `shard_manager`'s PreToolUse check before the registry-driven plugin loop |
| `crates/last-amended-migrate/src/rotate.rs` | modify | Add the additive `archive_path`-parameterized extension to `resolve_archive_path` (BC-1.18.009 T-7); existing cycle-scoped callers unaffected |
| `.factory/cycles/<cycle>/<artifact-stem>.shard-index.toml` (mechanism A, per artifact) | create (runtime) | Shard index — `schema_version`, cap constants, `retention_count`, `[[shard]]` entries |
| `.factory/cycles/<cycle>/archive/<artifact-stem>/` (mechanism A) | create (runtime) | Archived-shard directory beyond `retention_count` |
| `.factory/specs/behavioral-contracts/BC-INDEX-changelog-archive.md` (mechanism B1) | create (runtime) | Single evergreen archive file for `changelog:` array rotation overflow |
| `.factory/specs/behavioral-contracts/shards/BC-INDEX-SS-NN.md` (mechanism B2, 10 files) | create (runtime, one-time migration) | Per-subsystem BC-INDEX shard files |
| `.factory/specs/behavioral-contracts/shards/BC-INDEX.shard-manifest.toml` (mechanism B2) | create (runtime, one-time migration) | Top-level manifest listing all 10 subsystem shards + `sub_sharded` state |
| `.factory/specs/behavioral-contracts/shards/BC-INDEX-SS-05.manifest.toml`, `BC-INDEX-SS-06.manifest.toml` (mechanism B2, second level) | create (runtime, one-time migration) | Second-level sub-shard boundary manifests for SS-05/SS-06 |
| `plugins/vsdd-factory/hooks-registry.toml` | modify | `[[shard]]` config table entries (shape/`N`/`low_water_mark`/cap constants); Cohort B `failure_policy = "fail-closed"` assignments (T-12) |
| `.factory/specs/prd-supplements/error-taxonomy.md` | modify | Already materialized upstream (F2, product-owner) with `E-SHD-001..007`; no new codes expected from this story's implementation, verify wiring matches |
| `CHANGELOG.md` | modify | Mandatory CHANGELOG delivery task (T-13) |

## Changelog

| Version | Date | Description |
|---------|------|--------------|
| 2.3 | 2026-09-06 | F4 cluster-1 LOCAL-adversary pass-3 F-C1-P3-001 EC-014 propagation fix-burst (story-writer, POLICY 8 EC-mirror): BC-1.18.005 amended v1.7→v1.9 (intervening v1.8 was a pure wording-timing clarification for `low_water_mark`'s default, explicitly no-code/no-test-change, so it produced no story-side propagation) to add EC-014 (item-count shape's `read_changelog_item_count` maps `io::ErrorKind::NotFound` to `Ok(0)` on a not-yet-existing target artifact, mirroring EC-004's flat-shape missing-file-is-zero precedent) plus a new Postcondition 8 missing-file/first-write sub-bullet and Canonical Test Vector. EXTENDED AC-005's trace header (added edge case EC-014) and appended a paragraph describing the missing-file → `Ok(0)` → `Continue` behavior plus an inline VP-deferral note; ADDED §Edge Cases row EC-026 (source BC-1.18.005) mirroring EC-014; §Behavioral Contracts table BC-1.18.005 version cell v1.7→v1.9; §Token Budget BC-1.18.005/Story-spec lines nudged upward (Total ~73,600→~74,700, Budget usage stays ~37%). No VP allocated this burst — routed to architect/formal-verifier as a proposed sixth VP-140 facet, per BC-1.18.005's own Postcondition-8→VP-140 and Postcondition-9/EC-013 deferral precedents; `verification_properties:` frontmatter stays VP-116..VP-141 (26 VPs). See `last_amended` above for full detail. |
| 2.2 | 2026-09-06 | F4 cluster-1 LOCAL-adversary PC4-load-enforcement-intent fix-burst (story-writer): propagated BC-1.18.005 v1.6→v1.7's new Postcondition 9 / EC-013 (load-time fail-loud enforcement of the cap-vs-formula inequality at `ShardRegistry::load()` time) into this story per POLICY 8. Added AC-023 (traces to BC-1.18.005 postcondition 9, edge case EC-013), covering the load-time misconfiguration guard including its inclusive-boundary (`==` ceiling loads `Ok`) behavior; added §Edge Cases row EC-025 (source BC-1.18.005) mirroring EC-013; §Behavioral Contracts table BC-1.18.005 version cell v1.6→v1.7; T-13's RED-Gate stub-coverage count corrected 22 ACs→23 ACs; §Token Budget BC-1.18.005/Story-spec context-source lines nudged upward to reflect the BC's v1.7 growth (Total ~72,600→~73,600, Budget usage ~36%→~37%). No VP allocated this burst — Postcondition 9/EC-013's VP is routed to architect/formal-verifier per BC-1.18.005's own adjudication note (mirroring its Postcondition-8→VP-140 precedent); `verification_properties:` frontmatter stays VP-116..VP-141 (26 VPs) and AC-023 carries an inline VP-deferral note (owed to Phase F6; unit-tested at F4) instead. See `last_amended` above for full detail. |
| 2.1 | 2026-09-06 | F3→F4 consistency-gate finding F-1 closure burst (story-writer): re-pointed AC-012's BC citation from `BC-1.18.007 postcondition 3` (wrong — governs the general opt-in-required default) to `BC-1.18.007 Postcondition 6, BC-1.18.007 EC-006` (correct — the archive-inclusive POLICY-1 whole-corpus-scan obligation, added to BC-1.18.007 v1.2 by product-owner specifically to give this AC its BC home); AC-012's asserted behavior confirmed already matching PC6 exactly, citation-only fix. `verification_properties:` VP-116..VP-140 (25 VPs)→VP-116..VP-141 (26 VPs), adding VP-141 (BC-1.18.007 §Postcondition 6/§EC-006). §Behavioral Contracts table BC-1.18.007 version cell v1.1→v1.2; §Token Budget VP line count updated to match. All other 8 BC-table version cells re-verified against BC-INDEX v5.58, confirmed unchanged. See `last_amended` above for full detail. |
| 2.0 | 2026-09-06 | F3 incremental-stories population burst (story-writer): populated frontmatter (`behavioral_contracts:` 9 BCs, `verification_properties:` VP-116..VP-140, `subsystems:` +SS-04, `status:` draft→ready) and body (Acceptance Criteria 7→22, Behavioral Contracts table TBD→9 rows, Edge Cases 5→24 rows, Token Budget re-estimated ~72,600/36%, Tasks 10→13) from the F2-converged, human-ratified spec (D-1167, ADR-051 v1.8 accepted). Point estimate 15→45 (decomposition-by-BC-cluster re-estimation, D-1166 widest-scope-in-one-story justification for exceeding the normal 13-point cap). Confirmed E-25 dependency graph (S-25.01→S-25.02→S-25.03) unchanged and acyclic; wave assignment W2 confirmed unchanged. See `last_amended` above for full detail. |
| 1.0 | 2026-08-30 | Initial registration as REGISTERED BACKLOG story (story-writer; F3 story-decomposition burst, validation-integrity-layer1). Source: ADR-047 §D8b. Preliminary ACs, tasks, architecture anchors. Status: draft. BCs pending PO authorship at activation. |
