---
document_type: story
level: implementation
story_id: "S-21.23"
epic_id: "E-21"
version: "1.6"
status: draft
producer: story-writer
timestamp: 2026-08-20T00:00:00Z
phase: brownfield-backfill
inputs:
  - .factory/specs/architecture/decisions/ADR-039-validator-failure-policy-resource-exhaustion-fail-closed.md
  - plugins/vsdd-factory/hooks-registry.toml
input-hash: "33ca0c4"
traces_to: .factory/specs/architecture/decisions/ADR-039-validator-failure-policy-resource-exhaustion-fail-closed.md
points: "7"
depends_on: [S-21.10, S-21.19]
blocks: [S-21.24]
behavioral_contracts: ["BC-1.03.018 v1.6"]
verification_properties: []
priority: "P1"
cycle: v1.0-brownfield-backfill
wave: 7
target_module: crates/factory-dispatcher
subsystems: [SS-01]
estimated_days: null
assumption_validations: []
risk_mitigations: []
tdd_mode: strict
modified:
  - "2026-08-24 (v1.6) — Wave-7 floor-breaking pass-6/R5 story-layer remediation (story-writer). **BC-1.03.018 v1.5 → v1.6** re-anchor (POLICY 8 full propagation — frontmatter `behavioral_contracts`, BC table version column, Token Budget, Task 6 version cite). Does NOT touch BC files, INDEX/STATE.md, or input-hash (state-manager step ④)."
  - "2026-08-23 (v1.5) — Wave-7 pass-5/R4 story-layer remediation (story-writer). **F-S2123-P5-001 (HIGH):** AC-022 PC9 control re-lettering to match BC-1.03.018 v1.5 canonical partition — (e)=VACUITY and (f)=COMMENT-ONLY [(f.i)/(f.ii)]. Story had (e)=COMMENT-ONLY and (f)=VACUITY, inverted vs. BC. Fixed: swapped control (e) and (f) in the AC-022 enumeration; renamed sub-cases (e.i)→(f.i) and (e.ii)→(f.ii) throughout; corrected the false cross-reference at L354 ('present as (e)' → 'present as (f)'); propagated re-lettering to Task 5 control list, Architecture Compliance Rules detector row, and File Structure Requirements. BC-1.03.017 not cited by version in this story — no re-anchor needed. Does NOT touch BC-1.03.017/BC-1.03.018 bodies, INDEX/STATE.md, decomposition plan, or input-hash (state-manager step ④)."
  - "2026-08-23 (v1.4) — Wave-7 pass-4/R3 remediation sub-burst 3b (cycle v1.0-brownfield-backfill, D-1075): BC-1.03.018 v1.4 → v1.5 re-anchor (POLICY 8). F-S2123-P4-001 confirm-only (no edit). F-S2123-P4-002 (MED): AC-022 COMMENT-ONLY-CONTROL (e) extended with mandatory comment-stripping before executable-code check, split into (e.i) token-in-comment and (e.ii) full-call-in-comment MUST-NOT-satisfy sub-cases; Architecture Compliance Rules detector row updated; Task 5 updated; File Structure Requirements updated. F-S2123-P4-003 (MED): ADR-044 v1.2 → v1.3 in Position-in-DAG. F-S2123-P4-004 (LOW): AC-045 test description extended with all+non-named-alone sub-case. BC table Title cell updated per POLICY 7 (verbatim subset of v1.5 H1). Does NOT touch S-21.20/S-21.21/S-21.22/S-21.24/S-21.11-decomp or BC bodies (disjoint scope); INDEX/STATE.md owned by state-manager step ③."
  - "2026-08-23 (v1.3) — Wave-7 pass-3 remediation sub-burst 3b (cycle v1.0-brownfield-backfill, D-1074): BC-1.03.018 v1.3 → v1.4 re-anchor (POLICY 8). New AC-045 closes F-S2123-P3-001 (HIGH) `all`×non-named-plugin negative-control. AC-022 and Task 5 control counts reconciled six→seven for F-S2123-P3-002 (MED). BC table Title+Clause updated. Architecture Compliance Rules updated."
  - "2026-08-22 (v1.2) — Wave-7 pass-2 remediation sub-burst 3b (cycle v1.0-brownfield-backfill, D-1071; security-sensitive burst): BC-1.03.018 v1.2 -> v1.3 re-anchor (POLICY 8 full propagation -- frontmatter `behavioral_contracts`, body BC table Title cell + Clause cell, Token Budget). **F-S2123-P2-001 (HIGH):** EC-001/EC-002/EC-003 previously appeared 'covered' in the Edge Case Cross-Map via implementation-equivalence reasoning, not an actual falsifiable test -- gave each a dedicated test: `test_break_glass_case_variant_no_bypass` (EC-003, AC-017), `test_break_glass_trailing_empty_token_not_wildcard` + `test_break_glass_whitespace_only_no_bypass_no_panic` (EC-001, AC-017), `test_break_glass_explicit_two_name_list_bypasses_both` (EC-002, AC-016). Cross-Map updated to cite the real tests, not equivalence reasoning. **F-S2123-P2-002 (MED):** AC-022 rewritten for BC-1.03.018 v1.3 PC9's DUAL-TRIGGER correction -- the ordering gate now detects EITHER `on_error='block'` OR `failure_policy='fail-closed'` on either Agent gate (both gates already carry `on_error='block'` live today); controls extended from four to six (new `on_error`-only-trigger POSITIVE, new combined-both-triggers POSITIVE, corrected VACUITY requiring BOTH triggers absent); Task 5 rewritten to author five of six controls plus an immediately-assertable `on_error`-trigger LIVE-TREE-CONTROL sub-check (meaningful from THIS story's own merge, not deferred to S-21.24). **F-S2123-P2-003 (MED):** new Architecture Compliance Rules row + Library row + Tasks-section precondition note mandating serialization (`serial_test::serial` or a shared `Mutex` guard, implementer's choice) plus save/restore of the prior env value for ALL ~15+ env-var-mutating tests in `break_glass_gate_bypass.rs` -- `std::env::set_var` mutating shared process state is unsound under concurrent test execution. **F-S2123-P2-004 (MED):** new 'Operator Runbook Note: Mount-Gate Wedge Escape' subsection documenting the `VSDD_LOG_DIR=<writable path>` recovery path when the audit sink is mount-gated (`SkippedMountGate`), cross-referencing EC-007/AC-042. **F-S2123-P2-005 (MED):** AC-022's source-detector pinned to an EXECUTABLE read-pattern (e.g. `env::var_os(...)`), not a bare identifier grep; new COMMENT-ONLY-CONTROL (e) proves a comment-only mention does NOT satisfy the detector. **F-S2123-P2-006 (MED):** new AC-044 + Task 9 for BC-1.03.018 v1.3 PC12 audit-loss observability/EC-008 -- a clean-pass matched invocation with a failed/skipped audit write still fires an unconditional stderr diagnostic (gate name, trace UUID, failure variant), independent of `block_intent`. **LOWs:** F-P2-007 clarifies `Written` = write-syscall success, not `fsync`, in Task 3 and the Architecture Compliance Rules row (do not add/remove durability semantics); F-P2-008 drops the stale `v1.1` version pin from AC-023's header (now version-agnostic like the other ACs); F-P2-009 adds a note under AC-016 documenting the legitimate split-outcome case (`all` with per-gate-independent `write_checked` results); F-P2-010 corrects 'S-21.19 (merged)' to 'S-21.19 (REOPENED -- ADR-044 v1.2 function-split reconvergence in flight)' in the Position-in-DAG diagram. Token Budget bumped (~19.6K -> ~23.1K tokens, still ~11.6% of context, well under ceiling). Does NOT touch S-21.20/S-21.24/S-21.13/S-21.16 (disjoint sub-burst scope; concurrent 3a agent owns S-21.19/S-21.21/S-21.22) or any INDEX/STATE.md (state-manager's domain)."
  - "2026-08-22 (v1.1) — Wave-7 pass-1 remediation sub-burst 3b (cycle v1.0-brownfield-backfill, D-1069; Q3/F-S2123-P1-001..006): BC-1.03.018 v1.1 -> v1.2 re-anchor (POLICY 8 full propagation — frontmatter `behavioral_contracts`, body BC table Title cell + Clause cell, Token Budget). **F-S2123-P1-001 (HIGH):** new AC-042 (sibling to AC-018) + `test_break_glass_denied_on_unwritable_audit_sink` covering BC-1.03.018 PC11/Invariant 7/EC-007 — suppression DENIED (block_intent unchanged) when the `break_glass.activated` audit write returns `Failed`/`SkippedMountGate` via the new `InternalLog::write_checked` tri-state method; Task 3 revised to implement `write_checked` in `internal_log.rs` (new, additive) and to gate the break-glass call site's suppression decision on a `Written` result; new Task 7 authors AC-042's test. **F-S2123-P1-002 (MED):** non-UTF-8 override-value edge case (EC-015) — Task 3 revised to read via `std::env::var_os` (not `var`) and treat a non-Unicode value as equivalent to unset, never panic; new test under AC-014. **F-S2123-P1-003 (MED):** new AC-043 + `test_break_glass_not_derivable_from_non_env_sources` (new Task 8) — negative control proving the override is sourced ONLY from the OS process environment, never from a JSON tool-call payload field, `plugin_config`, a `hooks-registry.toml` field, or an on-disk file (BC-1.03.018 PC10/Invariant 1 full claim). **F-S2123-P1-004 (MED):** new \"BC-1.03.018 Edge Case Cross-Map\" subsection enumerating all seven BC-1.03.018 edge cases (EC-001–EC-007) against this story's covering AC/test. **F-S2123-P1-005 (LOW):** AC-015's crash-suppression test note corrected from \"this story's own unrelated EC-006\" to \"this story's origin's (S-21.11 v2.11) unrelated EC-006 row\", matching AC-019's existing convention. **F-S2123-P1-006 (LOW):** AC-016 clarified + new test proving `all` matches as a trimmed comma-separated TOKEN (e.g. `<junk>,all`), not only as the whole env-var value. File Structure Requirements' `internal_log.rs` row corrected from read-only to modify (this story is `InternalLog::write_checked`'s own implementation site per BC-1.03.018 Architecture Anchors). Does NOT touch S-21.20/S-21.24/S-21.13/S-21.16 (disjoint sub-burst scope) or any INDEX/STATE.md (state-manager's domain)."
  - "2026-08-20 (v1.0) — created by story-writer via the operator-directed split of CONVERGED S-21.11 (v2.11, 16-pass 3-CLEAN BC-5.39.001 convergence) per `.factory/planning/S-21.11-decomposition-plan.md` §1 (S-21.23 detail) and §3 (AC partition table). Owns AC-014 through AC-023 (10 ACs, all of BC-1.03.018 PC1–PC10 — entirely BC-1.03.018's own sibling BC), copied verbatim from S-21.11 v2.11 with zero weakening. Tasks renumbered #1–#6 (self-contained) from S-21.11 v2.11 Tasks #21, #22, #23, #24, #25, #25b."
---

> **tdd_mode: strict** — Full TDD Iron Law enforced: all non-trivial function bodies use `todo!()`;
> Red Gate density check ≥0.5 required before Step 4 dispatch.

> **Execute:** `/vsdd-factory:deliver-story S-21.23`

> **Split origin (2026-08-20).** One of six sub-stories partitioning CONVERGED S-21.11 (v2.11,
> 16-pass 3-CLEAN BC-5.39.001 convergence) per the operator-directed override of S-21.11's own
> prior "unified scope, no split" decision, documented in
> `.factory/planning/S-21.11-decomposition-plan.md`. S-21.23 has the cleanest BC boundary of
> the six seams — entirely BC-1.03.018 (its own sibling BC), logically independent of the
> exhaustion-calibration seams' internals. It depends on S-21.19 only for source-level
> integration ordering (both land edits inside the same `execute_tiers` block-decision loop
> body), not for any semantic reason. S-21.11's v2.11 body is frozen as the historical
> source-of-truth this story's ACs are extracted from verbatim; no AC text, BC trace, or test
> predicate was weakened, dropped, or reinterpreted during partitioning.

# S-21.23: Break-glass override mechanism — `VSDD_BREAK_GLASS_GATE_BYPASS` for self-locking `PreToolUse` `^Agent$` gates (ADR-039 Phase 4b; split seam 5 of 6 from S-21.11)

## Narrative

- **As a** factory pipeline operator whose two `PreToolUse` `^Agent$` gates
  (`validate-wave-gate-prerequisite`, `validate-pr-merge-prerequisites`) risk a self-lock if
  flipped to fail-closed without a way to escape a miscalibration — a hard block on either
  gate prevents ANY future `Agent` dispatch, including the dispatch needed to fix the
  miscalibration
- **I want** an authenticated-by-possession, human-operator-only break-glass override for
  these two gates specifically, audited on every activation
- **So that** the two self-locking gates cannot wedge all future `Agent` dispatch without a
  documented recovery path, before S-21.24's flip of these two plugins to
  `failure_policy = "fail-closed"` can safely proceed

## Context / Finding Summary

**Self-lock hazard is live today, on two axes (ADR-039 §Context).** `lessons.md` already
exhausts the 10M fuel budget (D-442(e); ≤3500 soft / ≤4000 hard line-count workaround). The
ADR-039 authoring session itself triggered `fail-closed: plugin timed out` blocks from
`validate-factory-path-root`, `validate-input-hash`, and `validate-template-compliance`. A
naive global flip would immediately hard-block all `.factory/` writes. **A second, distinct
self-lock axis exists for the two `PreToolUse` `^Agent$` gates specifically:** a hard block on
either `validate-wave-gate-prerequisite` or `validate-pr-merge-prerequisites` prevents ANY
future `Agent` dispatch — including the dispatch needed to fix the miscalibration. This is why
this story's break-glass mechanism is mandatory, not optional, for these two gates
specifically (ADR-039 §Decision 3 v1.9/v1.10 amendment).

**Why this seam depends on S-21.19 (integration ordering, not semantics).** BC-1.03.018's own
preconditions do not cite S-21.19's decision-function internals — this seam is logically
independent of the exhaustion-calibration Nodes' internals. But the operator's baseline
explicitly sequences this seam AFTER S-21.19 because both land edits inside the SAME
`execute_tiers` block-decision loop body — landing after S-21.19 avoids a signature-mismatch
merge conflict against the 2-arg→3-arg `plugin_fail_closed` call site. This is a source-level
integration ordering choice, preserved as directed from the original DAG's own "(D) and (E)
are independent of each other" statement.

**This story does NOT flip the two Agent gates to `failure_policy = "fail-closed"`** — it
delivers the break-glass mechanism and its own AC-022 mechanical ordering gate that S-21.24's
flip of those two plugins is gated on.

## Position in the Split DAG

```
S-21.10 (merged) ──────────────────────────┐
                                            ├──▶ S-21.23 (this story) ──▶ S-21.24 (capstone:
S-21.19 (REOPENED — ADR-044 v1.3 function-  ┘     flips the two Agent gates, gated on this
split reconvergence in flight, F-P2-010)          seam's AC-022)
```

The full six-seam dependency graph and wave schedule live in
`.factory/planning/S-21.11-decomposition-plan.md` §2.

## Acceptance Criteria

### AC-014: Override unset/absent → no behavioral change (baseline regression control) (traces to BC-1.03.018 PC1)

When `VSDD_BREAK_GLASS_GATE_BYPASS` is unset (or set to the empty string), both named gates'
block decisions are computed exactly as if the break-glass mechanism did not exist —
BC-1.03.017's `failure_policy`/`on_error` semantics apply unmodified. Introducing the
break-glass check MUST NOT alter any existing BC-1.03.017 postcondition's observed outcome
when the override is inactive.

- **Test:** `test_break_glass_unset_no_behavior_change()` — real dispatch:
  `VSDD_BREAK_GLASS_GATE_BYPASS` unset; `validate-wave-gate-prerequisite` returns
  `PluginResult::Timeout{Epoch}` + `failure_policy=FailClosed` → `block_intent=true`; exit 2
  (BC-1.03.017 PC6 semantics unmodified)
- **Test:** `test_break_glass_non_utf8_value_treated_as_unset()` (F-S2123-P1-002, EC-015) —
  set `VSDD_BREAK_GLASS_GATE_BYPASS` to a non-UTF-8 byte sequence (e.g. via `std::env::set_var`
  with an `OsString` built from invalid-UTF-8 bytes on a Unix target); the implementation reads
  the variable via `std::env::var_os` (not `std::env::var`) and treats a value that fails
  UTF-8 conversion (`VarError::NotUnicode`'s underlying condition) as equivalent to unset (this
  AC's baseline) — `block_intent=true`; exit 2 (unmodified); the dispatcher MUST NOT panic

### AC-015: Override set to an exact gate name → that gate's block suppressed for that invocation, regardless of block cause (traces to BC-1.03.018 PC2)

When `VSDD_BREAK_GLASS_GATE_BYPASS` contains the plugin `name` of
`validate-wave-gate-prerequisite` or `validate-pr-merge-prerequisites` (exact match;
comma-separated list; each token trimmed of leading/trailing whitespace before comparison),
that specific gate's contribution to `block_intent` for that invocation is forced to `false`
— REGARDLESS of which decision path would otherwise have produced the block
(`plugin_requests_block`'s `HookResult::Block` path, OR `plugin_fail_closed`'s exhaustion path
under `failure_policy = FailClosed`), and regardless of the plugin's own `PluginResult`
variant (`Crashed`, `Timeout{Fuel}`, `Timeout{Epoch}`).

- **Test:** `test_break_glass_exact_name_suppresses_block()` — real dispatch:
  `VSDD_BREAK_GLASS_GATE_BYPASS=validate-wave-gate-prerequisite`; same plugin returns
  `PluginResult::Timeout{Epoch}` + `failure_policy=FailClosed` → this gate's contribution to
  `block_intent` suppressed → exit 0 for this gate's contribution; `break_glass.activated`
  event emitted with gate name, timestamp, trace UUID (see AC-018)
- **Test:** `test_break_glass_suppresses_crash_block_too()` — real dispatch: override names
  the gate; the plugin CRASHES (`PluginResult::Crashed`, `on_error=Block`) rather than
  exhausting a resource budget → the crash-caused block is ALSO suppressed (PC2's "regardless
  of which decision path... and regardless of the plugin's own `PluginResult` variant" — the
  override is outcome-level, not exhaustion-specific; covers BC-1.03.018 EC-006 — BC-1.03.018's
  own internal edge-case numbering, distinct from this story's origin's (S-21.11 v2.11)
  unrelated EC-006 row in its own Edge Cases table — F-S2123-P1-005 wording correction)

### AC-016: Override set to the literal `all` → both named gates bypassed (traces to BC-1.03.018 PC3)

`VSDD_BREAK_GLASS_GATE_BYPASS=all` suppresses both `validate-wave-gate-prerequisite`'s and
`validate-pr-merge-prerequisites`' block contributions for that invocation, equivalent to
naming both explicitly. Both gates are dispatched in separate `execute_tiers` tiers (priority
130 vs 120 — `routing.rs::group_by_priority` starts a new tier on every distinct priority
value), evaluated unconditionally within the same dispatch via `execute_tiers`'s no-early-return
tier loop. If both would have blocked, TWO separate `break_glass.activated` events MUST be
emitted (one per gate name, per BC-1.03.018 EC-005 — BC-1.03.018's own internal edge-case
numbering), not a single combined event — this conclusion does not depend on, and is not
evidenced by, the two gates sharing a tier; it follows from `execute_tiers` looping over every
tier unconditionally (no early return on a block-producing tier), so both gates are always
invoked and both outcomes always folded into `block_intent` within one dispatch, regardless of
tier membership.

- **Test:** `test_break_glass_all_bypasses_both_gates()` — real dispatch:
  `VSDD_BREAK_GLASS_GATE_BYPASS=all`; both gates return blocking outcomes → both suppressed;
  two `break_glass.activated` events emitted, one per gate name, sharing the dispatch trace
  UUID
- **Test:** `test_break_glass_all_as_trimmed_token_in_list()` (F-S2123-P1-006) — `all` matches
  as a trimmed COMMA-SEPARATED TOKEN, not only when it is the entire env-var value verbatim:
  `VSDD_BREAK_GLASS_GATE_BYPASS=<junk>,all` (where `<junk>` is a non-matching token, e.g. a
  typo or an unrelated plugin name) → both named gates are STILL bypassed, because `all` is
  present as one of the comma-separated, whitespace-trimmed tokens (Task 3's parsing already
  treats the value as a token list per AC-015; this test proves `all`-as-wildcard participates
  in that same tokenization rather than requiring an exact whole-value match)
- **Test:** `test_break_glass_explicit_two_name_list_bypasses_both()` (F-S2123-P2-001, EC-002
  dedicated coverage — Cross-Map previously mapped EC-002 to this AC's `all` coverage only via
  behavioral-equivalence REASONING, not an actual test; this test makes that claim
  falsifiable): `VSDD_BREAK_GLASS_GATE_BYPASS=validate-wave-gate-prerequisite,validate-pr-merge-prerequisites`
  (both named gates listed explicitly, comma-separated, WITHOUT using `all`); both gates return
  blocking outcomes → both suppressed, behaviorally identical to the `all`-form test above; two
  `break_glass.activated` events emitted, one per gate name

**Note (F-P2-009, LOW — split-outcome case under `all`, audit-write divergence per gate):** a
single invocation where `VSDD_BREAK_GLASS_GATE_BYPASS=all`, BOTH named gates would have
blocked, and `InternalLog::write_checked` returns `Written` for `validate-wave-gate-prerequisite`'s
audit attempt but `Failed`/`SkippedMountGate` for `validate-pr-merge-prerequisites`'s — is a
legitimate, per-gate-independent outcome under AC-042/PC11: gate A (Written) is suppressed
(exit-0 contribution), gate B (Failed/SkippedMountGate) is NOT suppressed (its block stands),
within the SAME dispatch. This is not a new behavior — PC11's gating is per-gate, per-invocation
by construction (each gate's own `write_checked` call is independent) — but is called out
explicitly here because it is easy to mis-implement as an all-or-nothing suppression decision
across both gates under `all`. No dedicated test is mandated beyond AC-042's own per-gate
`Failed`/`SkippedMountGate`/`Written` coverage, provided the implementer's test harness exercises
at least one dispatch where the two gates' `write_checked` results genuinely differ (a
recommended, not separately numbered, test variation on AC-042).

### AC-017: Override set to a non-matching value → no suppression (negative control) (traces to BC-1.03.018 PC4)

When `VSDD_BREAK_GLASS_GATE_BYPASS` is set to a value that does not exact-match either named
gate's `name` and is not `all` (e.g., a typo, or the name of an unrelated plugin such as
`validate-factory-path-root`), neither named gate's normal block behavior is suppressed. This
proves the matcher is name-scoped and not a blanket kill-switch that fires on any non-empty
value.

- **Test:** `test_break_glass_non_matching_value_no_suppression()` — real dispatch:
  `VSDD_BREAK_GLASS_GATE_BYPASS=validate-factory-path-root` (non-matching name);
  `validate-wave-gate-prerequisite` returns a blocking outcome → block NOT suppressed;
  `block_intent=true`; exit 2; NO `break_glass.activated` event for
  `validate-wave-gate-prerequisite`
- **Test:** `test_break_glass_case_variant_no_bypass()` (F-S2123-P2-001, EC-003 dedicated
  coverage — Cross-Map previously mapped EC-003 to this AC via equivalence reasoning without
  a case-variant-specific test): `VSDD_BREAK_GLASS_GATE_BYPASS=All` and, in a second sub-case,
  `VSDD_BREAK_GLASS_GATE_BYPASS=VALIDATE-WAVE-GATE-PREREQUISITE` (all-caps variant of the exact
  gate name); the named gate returns a blocking outcome → block NOT suppressed in EITHER
  sub-case; `block_intent=true`; exit 2; NO `break_glass.activated` event — proves matching is
  exact-case (case-sensitive), not case-folded
- **Test:** `test_break_glass_trailing_empty_token_not_wildcard()` (F-S2123-P2-001, EC-001
  dedicated coverage — trailing-empty-token sub-case): `VSDD_BREAK_GLASS_GATE_BYPASS=validate-wave-gate-prerequisite,`
  (trailing comma producing an empty second token); `validate-wave-gate-prerequisite` returns a
  blocking outcome → suppressed (the named token still matches); `validate-pr-merge-prerequisites`
  (the OTHER gate, not named) ALSO returns a blocking outcome in the same dispatch → NOT
  suppressed — proves the empty trailing token is discarded as non-matching, not silently
  treated as `all`-equivalent or as a wildcard that bypasses the other gate
- **Test:** `test_break_glass_whitespace_only_no_bypass_no_panic()` (F-S2123-P2-001, EC-001
  dedicated coverage — whitespace-only sub-case): `VSDD_BREAK_GLASS_GATE_BYPASS="   "`
  (whitespace-only value); either named gate returns a blocking outcome → block NOT
  suppressed; `block_intent=true`; exit 2; dispatcher MUST NOT crash or panic — proves a
  fully-whitespace value is equivalent to unset (AC-014 baseline), not a match against
  anything

### AC-018: Mandatory audit on every activation — no silent bypass (traces to BC-1.03.018 PC5)

Every time AC-015 or AC-016 suppresses a gate's block contribution for a given invocation,
the dispatcher MUST emit a structured `break_glass.activated` event to
`.factory/logs/dispatcher-internal-YYYY-MM-DD.jsonl` (the same event-sink family documented
for `VSDD_SINK_FILE` diagnostics, via the existing `InternalLog`/`InternalEvent` mechanism in
`crates/factory-dispatcher/src/internal_log.rs`) carrying at minimum: the bypassed gate
name(s), an ISO-8601 timestamp, and the dispatch's trace UUID (`dispatcher_trace_id`). A test
asserting AC-015/AC-016's suppression behavior fires with NO corresponding
`break_glass.activated` record in the captured log output MUST fail — silent bypass is a
defect, not an acceptable implementation.

- **Test:** `test_break_glass_activation_emits_audit_event()` — integration test asserting
  captured dispatcher-internal JSONL output contains a `break_glass.activated` record with
  the bypassed gate name, an ISO-8601 timestamp field, and the invocation's
  `dispatcher_trace_id`, for a real dispatch that triggers AC-015's suppression path

**Note (PC11/AC-042 cross-reference):** as of PC11, the break-glass call site emits this
record via the new `InternalLog::write_checked` tri-state method (Task 3), not the pre-existing
best-effort `InternalLog::write`. This AC tests the EMISSION obligation (a record is present in
the captured JSONL output) for the healthy-sink case, independent of whether suppression is
ultimately granted — see AC-042 for the audit-write-DURABILITY-gates-suppression behavior when
the write is unhealthy (`Failed`/`SkippedMountGate`).

### AC-019: Audit fires on every matched invocation, independent of whether the plugin's own outcome would have blocked (traces to BC-1.03.018 PC6; BC-1.03.018 EC-004 — BC-1.03.018's own internal edge-case numbering, distinct from this story's origin's unrelated EC-004 row [`PRACTICAL_FUEL_CEILING` calibration overflow])

The `break_glass.activated` event MUST be emitted whenever the override names a gate that is
dispatched in that invocation's tier set, REGARDLESS of whether that gate's own
`PluginResult` would have produced a block absent the override — "active" is defined as "the
override names this gate AND this gate is invoked," not "the override suppressed an outcome
that would otherwise have blocked." An operator who leaves `VSDD_BREAK_GLASS_GATE_BYPASS` set
after the emergency has passed is an auditable fact regardless of whether any individual
invocation would have blocked.

- **Test:** `test_break_glass_audit_fires_even_on_clean_pass()` — real dispatch:
  `VSDD_BREAK_GLASS_GATE_BYPASS=validate-wave-gate-prerequisite`; plugin returns a CLEAN PASS
  (no block) → `break_glass.activated` event STILL emitted (audit fires on matched
  invocation, independent of counterfactual block); observable exit code unchanged (exit 0
  either way)

### AC-020: Non-persistent, validation-preserving override — no registry mutation, plugin still runs (traces to BC-1.03.018 PC7)

The override does NOT write to, or otherwise mutate, `hooks-registry.toml` — it is a
per-invocation runtime decision only, never a persistent configuration change. The named
plugin still executes and its `PluginResult` is still recorded in `per_plugin_results` /
logged via the normal `emit_invoked`/lifecycle event path; only that outcome's contribution
to the aggregate `block_intent` is suppressed.
`TierExecutionSummary.per_plugin_results` for a break-glass-active invocation MUST still
contain an entry for the bypassed plugin showing its actual (would-be-blocking) result, not a
synthesized "skipped" or "not-run" result.

- **Test:** `test_break_glass_does_not_mutate_registry()` — assert
  `hooks-registry.toml`'s on-disk content is byte-identical before and after a
  break-glass-active dispatch
- **Test:** `test_break_glass_plugin_still_executes_and_is_logged()` — assert
  `TierExecutionSummary.per_plugin_results` contains the bypassed plugin's actual
  `PluginResult` (not a synthesized skip marker) after a break-glass-active dispatch

### AC-021: Scope restriction — override has no effect on any plugin other than the two named gates (traces to BC-1.03.018 PC8)

Setting `VSDD_BREAK_GLASS_GATE_BYPASS` to the exact `name` of any registered plugin OTHER
than `validate-wave-gate-prerequisite` or `validate-pr-merge-prerequisites` (e.g.,
`validate-factory-path-root`, or any `on_error="continue"` advisory plugin) has NO effect on
that other plugin's block decision — the mechanism is hard-scoped to the two named gates at
the implementation level, not a generic "bypass any plugin by name" facility.

- **Test:** `test_break_glass_scoped_to_two_named_gates_only()` — real dispatch:
  `VSDD_BREAK_GLASS_GATE_BYPASS=validate-factory-path-root` (a real, valid plugin name, but
  not one of the two named gates); that plugin returns a blocking outcome → block is NOT
  suppressed (scope restriction proven against a real plugin name, not only a typo per
  AC-017)
- **Test:** `test_break_glass_named_gate_does_not_bypass_the_other_gate()` — real dispatch:
  `VSDD_BREAK_GLASS_GATE_BYPASS=validate-wave-gate-prerequisite`; `validate-pr-merge-prerequisites`
  (a different named gate, not the one set) returns a blocking outcome → NOT suppressed
  (per-name scope, not global)

### AC-022: Mechanical intra-story ordering gate, DUAL-TRIGGER — no `failure_policy="fail-closed"` OR `on_error="block"` on either `PreToolUse` `^Agent$` gate without the break-glass consultation code already present in the same commit tree (traces to BC-1.03.018 PC9; ADR-039 §Decision 1 axes-independence + §Decision 3 v1.10 sequencing rule; mirrors S-21.19's AC-012 pattern for BC-1.03.017 PC11; **rewritten Q-A/F-S2123-P2-002, Wave-7 pass-2 remediation**)

A Cargo integration test MUST assert: if `hooks-registry.toml` carries, for
`validate-wave-gate-prerequisite` OR `validate-pr-merge-prerequisites`, EITHER (i)
`failure_policy = "fail-closed"` OR (ii) `on_error = "block"` — these are TWO
INDEPENDENTLY-SUFFICIENT triggers, not a conjunction; EITHER ONE ALONE is sufficient — then
that SAME commit tree's `crates/factory-dispatcher/src/executor.rs` MUST already contain the
break-glass consultation code (a `VSDD_BREAK_GLASS_GATE_BYPASS`-reading check in the
`execute_tiers` block-decision path, name-independent detection analogous to S-21.19's AC-012
`.failure_policy`-reference detection). Absence of the break-glass code while EITHER trigger
is present on EITHER gate MUST cause the test to FAIL (CI blocks merge). This is the
mechanical enforcement of the hard edge: this story's break-glass mechanism MUST precede, or
be atomic with, S-21.24's Phase 4c flip commit for these two specific plugins.

**Why the dual trigger, not `failure_policy` alone (Q-A/F-S2123-P2-002):** the prior
single-trigger wording missed that BOTH named gates ALREADY carry `on_error = "block"` in the
LIVE `hooks-registry.toml` TODAY, independent of any `failure_policy` annotation. Per ADR-039
§Decision 1's axes-independence, `on_error = "block"` alone already blocks on
`PluginResult::Crashed` and, under the pre-migration 2-arg decision path, on `Timeout` as
well — meaning these two Agent gates ALREADY self-lock on a crash or timeout TODAY, with or
without any `failure_policy` flip. A `failure_policy`-only trigger left this CURRENT,
ALREADY-LIVE self-lock exposure entirely outside this gate: the break-glass consultation code
could be completely absent from `executor.rs` today, with both Agent gates already capable of
a crash/timeout self-lock, and this gate would have reported GREEN because no `failure_policy`
flip had yet occurred. The dual trigger closes this gap.

**This gate is a strict superset, for these two plugins only, of S-21.19's AC-012.** AC-012
alone would allow `validate-wave-gate-prerequisite`/`validate-pr-merge-prerequisites` to flip
to fail-closed as soon as the executor's `failure_policy` consultation exists and the plugin
is annotated — it does not know about break-glass. This AC closes that gap specifically for
these two self-locking gates, now on BOTH axes.

**Detector precision (F-S2123-P2-005, MED):** the break-glass-consultation-active detection
MUST be pinned to an EXECUTABLE source-pattern — a real read form such as
`env::var_os("VSDD_BREAK_GLASS_GATE_BYPASS")` (or an equivalent executable statement reading
that exact env-var name), NOT a bare identifier/string-literal grep for
`VSDD_BREAK_GLASS_GATE_BYPASS` anywhere in the file. A bare-identifier grep is satisfied by a
STALE COMMENT mentioning the variable name (e.g., `// TODO: consult VSDD_BREAK_GLASS_GATE_BYPASS
here`) with no actual consultation code present — a false GREEN that would let a commit merge
with the break-glass mechanism documented-but-not-implemented. This mirrors S-21.24's own
existing discipline for AC-012/AC-022 LIVE-TREE detection (scope to `.failure_policy`
DOT-ACCESS, not a bare `failure_policy:` grep) — the same false-match hazard class, applied to
this AC's own detector.

The gate MUST include SEVEN controls (extended from four to six in Q-A/F-S2123-P2-002 + F-S2123-P2-005; confirmed seven at F-S2123-P3-002 reconciling BC-1.03.018 v1.5 PC9 — the COMMENT-ONLY control already present as (f) in this story, now mirrored back into the BC):
- **(a) POSITIVE-CONTROL (`failure_policy` trigger):** synthetic registry with
  `validate-wave-gate-prerequisite` at `failure_policy="fail-closed"` (`on_error`
  irrelevant/absent for this control) + synthetic executor-source snippet WITHOUT the
  break-glass check → assert the gate fires RED.
- **(b) POSITIVE-CONTROL (`on_error` trigger, NEW — Q-A/F-S2123-P2-002):** synthetic registry
  with `validate-pr-merge-prerequisites` at `on_error="block"` and `failure_policy`
  absent/fail-open + synthetic executor-source snippet WITHOUT the break-glass check → assert
  the gate fires RED — proving the `on_error`-only trigger is independently sufficient,
  matching the LIVE registry's current state for both named gates.
- **(c) POSITIVE-CONTROL (combined-both-triggers, NEW — Q-A/F-S2123-P2-002):** synthetic
  registry with a named gate carrying BOTH `failure_policy="fail-closed"` AND `on_error="block"`
  simultaneously + synthetic executor-source snippet WITHOUT the break-glass check → assert
  the gate fires RED — proving the disjunction correctly fires when both triggers co-occur.
- **(d) NEGATIVE-CONTROL:** the same three synthetic registry states from (a)/(b)/(c), each
  paired with a synthetic executor-source snippet WITH the break-glass check present (using
  the EXECUTABLE pattern, e.g. an actual `env::var_os("VSDD_BREAK_GLASS_GATE_BYPASS")` call)
  → assert the gate PASSES for each.
- **(e) VACUITY-CONTROL (corrected — Q-A/F-S2123-P2-002):** synthetic registry with BOTH named
  gates at `failure_policy` absent/fail-open AND `on_error != "block"` (NEITHER trigger
  present — necessarily a SYNTHETIC-ONLY state, since the live registry already carries
  `on_error="block"` for both named gates) + any executor-source state → assert the gate
  returns GREEN vacuously (neither trigger is present). A vacuity fixture keying only on
  `failure_policy` absent (the pre-dual-trigger wording) is FALSE-VACUOUS against the live
  `on_error="block"` state and MUST NOT be used to represent "no trigger present."
- **(f) COMMENT-ONLY-CONTROL (NEW — F-S2123-P2-005; extended F-S2123-P4-002):** The detector
  MUST strip `//` and `/* */` comment blocks BEFORE searching for the executable pattern —
  comment-stripping is MANDATORY (closes CWE-636: a commented-out call that would textually
  match the executable pattern MUST NOT satisfy a comment-stripping-aware detector). This
  control has two sub-cases:
  - **(f.i) token-in-comment (pre-existing — F-S2123-P2-005):** synthetic registry from (a)
    (or (b)/(c)) + synthetic executor-source snippet containing ONLY a comment that mentions
    the variable name (e.g. `// consult VSDD_BREAK_GLASS_GATE_BYPASS here`), with NO executable
    read of the variable → assert the gate STILL fires RED.
  - **(f.ii) full-call-in-comment MUST-NOT-satisfy (NEW — F-S2123-P4-002):** synthetic
    executor-source snippet containing a commented-out FULL call pattern (e.g.
    `// env::var_os("VSDD_BREAK_GLASS_GATE_BYPASS")` or
    `/* env::var_os("VSDD_BREAK_GLASS_GATE_BYPASS") */`) — a snippet that WOULD satisfy a
    substring detector that searches for `env::var_os("VSDD_BREAK_GLASS_GATE_BYPASS")` without
    comment-stripping, but MUST NOT satisfy a comment-stripping-aware detector → assert the
    gate STILL fires RED, proving the strip-then-search order is in effect (not
    search-then-discard).
- **(g) LIVE-TREE-CONTROL (outcome CORRECTED — Q-A/F-S2123-P2-002):** at S-21.24 completion,
  the detector MUST be run against the actual `crates/factory-dispatcher/src/executor.rs` and
  the actual `hooks-registry.toml`, and MUST report the correct ordering state given BOTH
  triggers' live values. Because both named gates ALREADY carry `on_error="block"` live, this
  control's correct outcome TODAY — independent of any `failure_policy` flip, and in fact
  correct even BEFORE S-21.24's flip lands — is that the gate reports the ordering rule AS
  ALREADY TRIGGERED, requiring the break-glass consultation code to already be present in the
  live tree. This differs from the pre-dual-trigger reading, under which the live-tree gate
  could report GREEN-vacuously until a `failure_policy` flip occurred; this final live-tree
  confirmation is performed by S-21.24, which owns the flip that makes the `failure_policy`
  half of the disjunction meaningful, but the `on_error` half is ALREADY meaningful today —
  this story authors the test and controls (a)-(f); S-21.24 confirms (g) post-flip.

- **Test:** `test_no_agent_gate_fail_closed_flip_without_break_glass()` — parses
  `hooks-registry.toml` for `failure_policy="fail-closed"` OR `on_error="block"` on either
  named `^Agent$` gate; if EITHER is present, detects break-glass-consultation-active state in
  `executor.rs` using a COMMENT-STRIPPING-AWARE, EXECUTABLE-pattern signal (the detector MUST
  strip `//` and `/* */` blocks before matching — an actual
  `env::var_os("VSDD_BREAK_GLASS_GATE_BYPASS")`-style read in the `execute_tiers`
  block-decision path, not a bare identifier grep, and not a commented-out call such as
  `// env::var_os("VSDD_BREAK_GLASS_GATE_BYPASS")` — F-S2123-P4-002); MUST FAIL if either
  trigger is present on either gate and the break-glass code is absent; includes all seven
  controls above (six authored here — (a) through (f) with (f.i) and (f.ii) sub-cases; the
  seventh, LIVE-TREE-CONTROL (g), confirmed by S-21.24 post-flip, though its `on_error`-trigger
  half is ALREADY meaningful pre-flip); GREEN vacuously ONLY in the synthetic
  neither-trigger-present state (this story's own completion state, since the LIVE registry
  already trips the `on_error` trigger); GREEN at S-21.24-complete state (both gates flipped,
  break-glass code present)

### AC-023: Break-glass negative control — an `Agent`-tool-invoked path CANNOT arm `VSDD_BREAK_GLASS_GATE_BYPASS` for the dispatcher's own process environment (traces to BC-1.03.018 PC10; F-006, S-21.11 v2.0 adversarial pass-1 threat-boundary directive; F-P2-008 — version pin dropped, version-agnostic like the other ACs)

`VSDD_BREAK_GLASS_GATE_BYPASS` is armed if and only if it is present in the OS process
environment of the process that ultimately launches the `factory-dispatcher` binary for a
given dispatch — i.e., the human operator's shell session (or an ancestor process the operator
directly controls). This is a structural property of the OS process model, not an
application-level access check: a child process's environment is a COPY taken at
`fork`/`exec` time, and a child mutating its own environment can NEVER write back into its
parent's environment. Consequently, none of the following can arm the override for the
dispatcher's own process: (a) a Bash-tool subprocess spawned by an `Agent`-tool-invoked Claude
session (a DESCENDANT of the dispatcher's own process tree, never an ancestor); (b) an MCP
tool invocation (runs entirely outside the harness's own environment-inheritance chain); (c) a
hook-wrapper subprocess (e.g. `legacy-bash-adapter`'s own `exec_subprocess` calls) — itself a
CHILD of the dispatcher process for that single invocation.

**Explicit threat-boundary note (out of this AC's enforcement scope, per F-006):** this AC does
NOT claim the dispatcher validates WHO set the variable in its inherited environment. If the
human operator's own shell session is itself compromised, that compromised process COULD set
the variable before launching Claude Code, and the dispatcher would inherit it exactly as it
would inherit a legitimate operator's setting (Invariant 1's "authentication-by-possession, not
identity" scope). What this AC closes is narrower: the `Agent` tool dispatch path ITSELF
provides no route to arm the override, by construction of the OS process model, regardless of
what any `Agent`-invoked tool call attempts.

- **Test:** `test_break_glass_env_not_settable_by_child_process()` — an integration test MUST
  spawn a child process that attempts to set `VSDD_BREAK_GLASS_GATE_BYPASS` in its own
  environment (via `std::env::set_var`, or an equivalent shell `export`, executed inside the
  spawned child) and then assert that the PARENT test process's own
  `std::env::var("VSDD_BREAK_GLASS_GATE_BYPASS")` remains unset/unchanged after the child
  process exits — proving the OS-level parent-environment isolation this AC relies on, in the
  actual runtime the dispatcher executes in, not merely asserted as documentation

### AC-042: Audit fail-closed-on-failure — suppression is granted ONLY IF the `break_glass.activated` audit write DURABLY completes (sibling to AC-018; traces to BC-1.03.018 PC11/Invariant 7; F-S2123-P1-001, HIGH, Q3, ADR-039 §AMD-004)

When AC-015 or AC-016 would otherwise suppress a gate's block contribution for a given
invocation, that suppression is granted ONLY IF the corresponding `break_glass.activated`
audit write (AC-018/AC-019) is CONFIRMED DURABLY WRITTEN — i.e., `InternalLog::write_checked`
(the new, additive, tri-state method Task 3 implements ALONGSIDE the existing best-effort
`InternalLog::write`, which remains UNCHANGED for its other hot-path telemetry callers)
returns `Written`. If the audit write FAILS (`write_checked` returns `Failed(io::Error)`) OR
is SKIPPED (`write_checked` returns `SkippedMountGate` — the `.factory/` mount-gate skip path,
which the pre-existing best-effort `write()` conflates with a successful write, since both
return `Ok(())`), the override MUST NOT be applied for that gate on that invocation — the
underlying gate's block decision stands, computed EXACTLY as if the override were unset
(AC-014 baseline semantics), even though the override was set and named this gate. This is the
fail-closed-on-audit-failure posture: an audit write that cannot be confirmed durable MUST NOT
silently grant a bypass, because a break-glass activation whose audit trail is dropped —
precisely during a live self-lock, the scenario this mechanism exists to recover from —
reproduces exactly the CWE-636 silent-approval failure mode AC-018/AC-019's own mandatory-audit
requirement exists to prevent, one layer down (BC-1.03.018 EC-007).

- **Test:** `test_break_glass_denied_on_unwritable_audit_sink()` — real dispatch:
  `VSDD_BREAK_GLASS_GATE_BYPASS=validate-wave-gate-prerequisite`; that gate returns a blocking
  outcome; `InternalLog::write_checked` simulated to return `Failed(io::Error)` (unwritable
  audit sink) → suppression DENIED — `block_intent=true`; exit 2 (gate's block decision stands,
  unchanged from the no-override baseline); NO durable `break_glass.activated` record confirmed
- **Test:** same test, second sub-case — `InternalLog::write_checked` simulated to return
  `SkippedMountGate` (`.factory/` mount not yet ready) → suppression DENIED; `block_intent=true`;
  exit 2 (identical outcome to the `Failed` sub-case — a skip is NOT treated as equivalent to a
  durable write)
- **Test:** `test_break_glass_granted_on_written_audit_sink()` (NEGATIVE control) — real
  dispatch: `VSDD_BREAK_GLASS_GATE_BYPASS=validate-wave-gate-prerequisite`; that gate returns a
  blocking outcome; `InternalLog::write_checked` returns `Written` (audit sink healthy) →
  suppression GRANTED — `block_intent` contribution suppressed → exit 0 for this gate's
  contribution (restores AC-015's baseline behavior once the audit write is confirmed durable;
  proves PC11 does not over-block a healthy audit path)

### AC-043: Break-glass negative control — the override is NOT derivable from any Agent-writable source; env-only sourcing (traces to BC-1.03.018 PC10/Invariant 1's full claim; F-S2123-P1-003, MED)

Invariant 1's "authentication-by-possession, human-operator-only" claim requires that
`VSDD_BREAK_GLASS_GATE_BYPASS` be read EXCLUSIVELY from the OS process environment — AC-023
proves no `Agent`-tool-invoked path can arm the environment variable itself; this AC closes the
complementary gap: no OTHER channel available to an `Agent`-tool-invoked call (a JSON tool-call
payload field, a `plugin_config` value, a `hooks-registry.toml` field, or an on-disk file) may
be consulted as an alternate source for the same suppression decision, even if such a channel
happens to carry a value that matches a gate name or `all`.

- **Test:** `test_break_glass_not_derivable_from_non_env_sources()` — real dispatch: construct
  a scenario where a synthetic JSON tool-call payload field, a `plugin_config` entry, an
  alternate `hooks-registry.toml` field (e.g. a hypothetical `break_glass_bypass` key on the
  plugin entry itself), and an on-disk file all carry the literal value
  `validate-wave-gate-prerequisite` (or `all`) under some non-`VSDD_BREAK_GLASS_GATE_BYPASS`
  key, while the actual OS process environment variable `VSDD_BREAK_GLASS_GATE_BYPASS` remains
  UNSET — assert suppression does NOT occur for any of the four alternate-channel sub-cases
  (`block_intent=true`; exit 2 unchanged from the no-override baseline) and NO
  `break_glass.activated` event is emitted, proving the ONLY channel ever consulted for this
  decision is `std::env::var_os("VSDD_BREAK_GLASS_GATE_BYPASS")`

### AC-044: Audit-loss observability — clean-pass matched invocation with a failed/skipped audit write still fires the PC12 stderr diagnostic (traces to BC-1.03.018 PC12; EC-008; Q-B, F-S2123-P2-006, MED)

Whenever `InternalLog::write_checked` is invoked for a `break_glass.activated` audit attempt
and returns other than `Written` (`Failed(io::Error)` or `SkippedMountGate`), the dispatcher
MUST emit a distinct immediate diagnostic to the same fallback channel `InternalLog::write`
already uses for its own internal errors (`eprintln!`/stderr), identifying the matched gate
name, the dispatch's trace UUID (`dispatcher_trace_id`), and the failure variant. This
emission is UNCONDITIONAL on whether the invocation was blocking: it fires identically in
AC-042's suppression-denial case (the gate WOULD have blocked) AND in a
counterfactual-clean-pass case (the gate would NOT have blocked, EC-004-class) — in the
clean-pass case, AC-042's suppression-denial logic is a no-op (there was no block to deny), so
`block_intent` is unaffected either way (exit 0 with or without a successful write); WITHOUT
this diagnostic, a failed/skipped audit write on a clean-pass matched invocation is SILENT — it
drops the AC-018/AC-019-mandated audit record with no other observable signal distinguishing it
from a normal clean pass with a successful write. The diagnostic itself is best-effort — it is
NOT itself gated on a further confirmed-write check (that would recurse indefinitely).

- **Test:** `test_break_glass_pc12_diagnostic_fires_on_clean_pass_audit_loss()` — real dispatch:
  `VSDD_BREAK_GLASS_GATE_BYPASS=validate-wave-gate-prerequisite`; that gate returns a CLEAN
  PASS (would NOT have blocked); `InternalLog::write_checked` simulated to return
  `Failed(io::Error)` → `block_intent` UNCHANGED (exit 0 — identical to a normal clean pass at
  the block_intent/exit-code level); captured stderr output MUST contain the diagnostic (gate
  name, trace UUID, `Failed` variant) — the only observable trace of the dropped audit record
- **Test:** same test, second sub-case — `InternalLog::write_checked` simulated to return
  `SkippedMountGate` → identical assertions (diagnostic fires, `block_intent` unchanged)
- **Test:** `test_break_glass_pc12_diagnostic_fires_on_suppression_denial_too()` — real
  dispatch reusing AC-042's `test_break_glass_denied_on_unwritable_audit_sink` scenario (the
  gate WOULD have blocked, audit write fails) → assert the SAME diagnostic ALSO fires in this
  case, proving PC12's emission is unconditional on whether the invocation was blocking, not
  scoped to the clean-pass case alone

### AC-045: `all`-wildcard negative control — non-named blocking plugin's block stands and receives no audit record under `VSDD_BREAK_GLASS_GATE_BYPASS=all` (traces to BC-1.03.018 PC8; F-S2123-P3-001, HIGH, Wave-7 pass-3 remediation)

`all` (PC3) is name-scoped shorthand for "both of the two named gates," NOT a blanket
"suppress every blocking plugin" switch. Under `VSDD_BREAK_GLASS_GATE_BYPASS=all`, a
NON-named blocking plugin (e.g., `validate-factory-path-root`, `validate-input-hash`,
`validate-template-compliance`) MUST have its block STAND — `block_intent=true` from that
plugin's contribution, UNAFFECTED by the `all` override — AND MUST NOT have a
`break_glass.activated` event emitted attributing bypass or audit to it; only the two named
gates' contributions are suppressed and audited under `all`.

This closes the compound CWE-636+CWE-863 hazard a naive `all` branch implemented as "suppress
every outcome in the `tier_outcomes` loop" (rather than "suppress only entries whose
`RegistryEntry.name` is one of the two named gates, even when the override value is `all`")
would introduce: such an implementation would silently defeat fail-closed enforcement for
`validate-factory-path-root`/`validate-input-hash`/`validate-template-compliance` with NO
audit trail — materially worse than the exact-name PC4/AC-017 and PC8/AC-021 negative controls
already cover, because `all` is the operationally MORE LIKELY value an operator reaches for
under a live self-lock.

This AC is DISTINCT from:
- AC-017's negative control (the override value IS the name of a non-named plugin, e.g.
  `validate-factory-path-root` — a non-matching name, so neither named gate is suppressed):
  here the override is `all`, which DOES suppress the two named gates but must NOT suppress
  any other plugin.
- AC-021's scope test (`VSDD_BREAK_GLASS_GATE_BYPASS=validate-wave-gate-prerequisite`;
  `validate-pr-merge-prerequisites` is not suppressed — per-name scope, not global): here we
  test the `all` literal specifically, which is the operationally common break-glass value.

- **Test:** `test_break_glass_all_does_not_suppress_non_named_plugin()` — integration test with
  two sub-cases (F-S2123-P4-004):
  - **(primary sub-case)** `VSDD_BREAK_GLASS_GATE_BYPASS=all`; a NON-named blocking plugin
    (e.g. `validate-factory-path-root`) returns a blocking outcome in the SAME dispatch that
    also has one or both named gates returning blocking outcomes; assert ALL of:
    (a) `block_intent=true` — the non-named plugin's block stands, unaffected by `all`; the
        two named gates' contributions ARE suppressed per PC3/AC-016 within the same dispatch
    (b) NO `break_glass.activated` record naming `validate-factory-path-root` (or whichever
        non-named plugin was used) appears in captured dispatcher-internal JSONL output — `all`
        does NOT emit an audit record for non-named plugins; only records for the two named
        gates (suppressed and audited per PC5/AC-018) appear
  - **(all+non-named-alone sub-case, NEW — F-S2123-P4-004)** `VSDD_BREAK_GLASS_GATE_BYPASS=all`;
    a NON-named blocking plugin returns a blocking outcome, but the two named gates produce NO
    blocking outcome in this dispatch (i.e., the non-named plugin is the SOLE blocker); assert:
    (a) `block_intent=true` — the non-named plugin's sole block stands; `all` does not
        suppress it even though no named gate is blocking in this dispatch
    (b) NO `break_glass.activated` record for the non-named plugin appears — `all` does not
        grant audit-free bypass to non-named plugins regardless of the named gates' own
        outcomes; proves the name-scoping is per-plugin, not contingent on named gates firing

### Operator Runbook Note: Mount-Gate Wedge Escape (F-S2123-P2-004, MED)

**Symptom:** if the `break_glass.activated` audit sink is mount-gated (`.factory/` not yet
mounted — see the known #206 bootstrap-window class of issue), `InternalLog::write_checked`
returns `SkippedMountGate` for every audit attempt at that call site. Per AC-042/PC11, this
DENIES every suppression — the break-glass override becomes structurally inert (every matched
gate's block stands, exactly as if the override were unset) for as long as the mount-gate
condition persists. Because this can occur precisely during the live self-lock scenario the
break-glass mechanism exists to recover from, an operator relying on
`VSDD_BREAK_GLASS_GATE_BYPASS` during a mount-gate window would find it silently ineffective
without PC12's diagnostic (AC-044) surfacing the `SkippedMountGate` variant to stderr.

**Recovery:** set `VSDD_LOG_DIR=<writable path>` (see CLAUDE.md's "VSDD_SINK_FILE diagnostic
capture" section for the sibling `VSDD_SINK_FILE` mechanism this env var parallels) to point
the dispatcher-internal log sink at an already-writable directory outside the mount-gated
`.factory/` path. Once the sink is writable, `write_checked` returns `Written` on the next
matched invocation, and break-glass suppression is restored per AC-042's NEGATIVE control. This
runbook note MUST be added to the implementer's PR description or an operator-facing doc
comment near the `write_checked` call site, cross-referencing BC-1.03.018 EC-007 (this story's
own `test_break_glass_denied_on_unwritable_audit_sink`) as the behavioral proof that
`SkippedMountGate` denies suppression, and EC-008/AC-044 as the proof that the operator is
told about it.

## Behavioral Contracts

| BC ID | Version | Title | Clause |
|-------|---------|-------|--------|
| BC-1.03.018 | v1.6 | factory-dispatcher `execute_tiers` break-glass override — `VSDD_BREAK_GLASS_GATE_BYPASS` environment-variable escape hatch for the two self-locking PreToolUse `^Agent$` gates (`validate-wave-gate-prerequisite`, `validate-pr-merge-prerequisites`), with `all`-as-trimmed-comma-token (matched by same comma-split-and-trim rule as named gates, not a separate parsing mode; F-S2123-P4-005) `all`-wildcard scope preserving non-named-plugin fail-closed enforcement, mandatory `break_glass.activated` JSONL audit, intra-S-21.11 dual-trigger (failure_policy OR on_error) pre-flip-ordering gate with executable-code comment-stripping-mandatory (strip `//` and `/* */` before pattern check; closes CWE-636 full-call-in-comment false-green; F-S2123-P4-002) detector precision and seven-control partition (a)-(e) synthetic (f) COMMENT-ONLY [(f.i) token-in-comment (f.ii) full-call-in-comment] (g) LIVE-TREE aligned to story AC-022 (F-S2123-P4-001), human-operator-only Agent-tool-cannot-arm threat-boundary control, audit-write-durability-gates-suppression fail-closed-on-audit-failure control, and audit-loss stderr observability independent of suppression outcome | PC1 (AC-014), PC2 (AC-015), PC3 (AC-016), PC4 (AC-017), PC5 (AC-018), PC6 (AC-019), PC7 (AC-020), PC8 (AC-021, AC-045 — `all`×non-named-plugin negative control, F-S2123-P3-001), PC9 (AC-022 — DUAL-TRIGGER, SEVEN controls: six authored here, LIVE-TREE-CONTROL confirmed by S-21.24), PC10 (AC-023, AC-043), PC11 (AC-042), PC12 (AC-044) |
<!-- POLICY 7: BC-table Title cell MUST remain a verbatim SUBSET of the corresponding BC's H1. -->

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| `execute_tiers` block-decision loop (break-glass consultation) | `crates/factory-dispatcher/src/executor.rs` | Pure decision + effectful audit-log write (env-var read is effectful I/O; the suppression decision itself is a pure function of the env value + gate name + would-be block outcome) |
| `test_no_agent_gate_fail_closed_flip_without_break_glass` | `crates/factory-dispatcher` or integration test crate | Pure (reads hooks-registry.toml + executor-source snippet; asserts ordering invariant) |
| `break_glass.activated` audit emission (`InternalLog::write_checked`, NEW — F-S2123-P1-001) | `crates/factory-dispatcher/src/internal_log.rs` (`InternalEvent`/`InternalLog`; new additive tri-state method alongside the existing best-effort `write`) | Effectful (JSONL append to `.factory/logs/dispatcher-internal-YYYY-MM-DD.jsonl`); returns `Written`/`SkippedMountGate`/`Failed(io::Error)` |
| PC12 audit-loss stderr diagnostic (NEW — Q-B/F-S2123-P2-006) | `crates/factory-dispatcher/src/executor.rs` break-glass call site (same site as `write_checked`) | Effectful (`eprintln!`/stderr, best-effort, matching `InternalLog::write`'s own internal-error posture); fires unconditional on `block_intent` outcome whenever `write_checked` returns other than `Written` |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|----------------|
| Break-glass suppression decision (name match + `all` handling + `write_checked` result gating) | Pure-core | Given the env-var's already-read string value, gate name, would-be block outcome, and `write_checked`'s already-returned tri-state result, the suppression decision itself is a pure function; only the env-var read and the audit-log write are effectful |
| `test_no_agent_gate_fail_closed_flip_without_break_glass` | Pure-core | Reads config file + source snippet; asserts ordering invariant |
| `break_glass.activated` audit emission (`InternalLog::write_checked`) | Effectful | JSONL file append; new tri-state method, additive alongside the existing best-effort `InternalLog::write` (unchanged for its other callers) |
| PC12 audit-loss stderr diagnostic | Effectful | `eprintln!`/stderr append; best-effort, non-gating (does not alter `block_intent`) |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-012 | Break-glass override set but malformed (whitespace-only, or a stray trailing comma producing an empty token) | Malformed/empty tokens treated as non-matching (no bypass for that token); a fully-empty-after-trim value is equivalent to unset (AC-014 baseline); dispatcher MUST NOT crash or panic |
| EC-013 | Break-glass override contains both named gates explicitly, comma-separated, without using `all` | Both gates bypassed — behaviorally equivalent to `VSDD_BREAK_GLASS_GATE_BYPASS=all` |
| EC-014 | Break-glass override value has case variation (e.g. `All`, `VALIDATE-WAVE-GATE-PREREQUISITE`) | Matching is exact-case (case-sensitive); a case-mismatched value does NOT bypass (falls under AC-017's negative-control behavior) |
| EC-015 | Break-glass override value is non-UTF-8 (an `OsString` that fails UTF-8 conversion) (F-S2123-P1-002) | Read via `std::env::var_os` (not `std::env::var`); treated as equivalent to unset (AC-014 baseline); dispatcher MUST NOT panic |

Edge cases EC-001–EC-011 (this story's own local numbering — note: these are S-21.11 v2.11
origin edge cases, i.e. BC-1.03.017's, not BC-1.03.018's) are not cited by this story's owned
ACs; they are owned by S-21.19/S-21.21/S-21.22. The full 14-row table lives in S-21.11 v2.11
(frozen historical source-of-truth).

### BC-1.03.018 Edge Case Cross-Map (F-S2123-P1-004; extended F-S2123-P2-001/P2-006)

BC-1.03.018 (this story's own sibling BC) carries its own internal EC-001–EC-008 numbering,
distinct from the S-21.11-origin EC-001–EC-011/EC-012–EC-015 numbering in the table above. This
cross-map enumerates all eight BC-1.03.018 edge cases against this story's covering AC/test so
a Phase-3 test-writer working from this story's own Edge Cases table alone does not
under-cover:

| BC-1.03.018 EC | Description | Covering AC | Covering Test |
|----------------|-------------|-------------|----------------|
| EC-001 | Override set but malformed (whitespace-only, stray trailing comma) | AC-017 (F-S2123-P2-001) | `test_break_glass_whitespace_only_no_bypass_no_panic()` (whitespace-only sub-case) + `test_break_glass_trailing_empty_token_not_wildcard()` (trailing-empty-token sub-case) — dedicated falsifiable tests, not implementation-equivalence reasoning |
| EC-002 | Override contains both named gates explicitly, comma-separated, without `all` | AC-016 (F-S2123-P2-001) | `test_break_glass_explicit_two_name_list_bypasses_both()` — dedicated test, not merely asserted equivalent to AC-016's `all` coverage |
| EC-003 | Override value has case variation | AC-017 (F-S2123-P2-001) | `test_break_glass_case_variant_no_bypass()` — dedicated test (both `All` and all-caps sub-cases), not the pre-existing `test_break_glass_non_matching_value_no_suppression()`'s unrelated typo/non-matching-name assertion |
| EC-004 | Override active; the named gate would have produced a CLEAN PASS even without the override | AC-019 | `test_break_glass_audit_fires_even_on_clean_pass()` |
| EC-005 | Both named gates dispatched (separate tiers), override set to `all`, both would have blocked | AC-016 | `test_break_glass_all_bypasses_both_gates()` |
| EC-006 | Override active; the underlying plugin CRASHES rather than exhausts a resource budget | AC-015 | `test_break_glass_suppresses_crash_block_too()` |
| EC-007 | Override matches a blocking gate; the `break_glass.activated` audit write FAILS or is SKIPPED | AC-042 (F-S2123-P1-001) | `test_break_glass_denied_on_unwritable_audit_sink()` — see also the Operator Runbook Note above (F-S2123-P2-004) |
| EC-008 | Override matches a gate that would NOT have blocked (clean pass) AND the audit write FAILS or is SKIPPED | AC-044 (Q-B, F-S2123-P2-006) | `test_break_glass_pc12_diagnostic_fires_on_clean_pass_audit_loss()` |

**F-S2123-P2-001 remediation note (HIGH):** EC-001/EC-002/EC-003 previously appeared "covered"
in this table via implementation-equivalence reasoning (e.g., "behaviorally equivalent to
AC-016's `all` coverage") rather than an actual falsifiable RED-gate test targeting that exact
edge case. Each now has a dedicated test (see Covering Test column above and the corresponding
AC bodies) — no row in this table is covered by reasoning alone.

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | 12,400 |
| ADR-039 (§Decision 3 v1.10 minimum-viable break-glass definition + §Decision 1 axes-independence + §AMD-004 audit-durability amendment) | 2,200 |
| BC-1.03.018 v1.6 (full — 12 postconditions, 8 invariants, 8 edge cases) | 4,400 |
| `crates/factory-dispatcher/src/executor.rs` (`execute_tiers` block-decision loop, the sole `block_intent` aggregation site) | 3,000 |
| `crates/factory-dispatcher/src/internal_log.rs` (existing `InternalEvent`/`InternalLog` best-effort mechanism, plus this story's own new `write_checked` tri-state method) | 1,100 |
| **Total** | **~23,100** |
| Agent context window | 200K (Sonnet) |
| **Budget usage** | **~11.6%** |

**Budget note:** well under the 20–30% ceiling — the cleanest BC boundary of the six seams
means no calibration or bash-adapter context is required at all.

## Tasks (MANDATORY)

This story's tasks are the Node (E) subset of S-21.11 v2.11's original DAG, renumbered
self-contained #1–#6 (source: S-21.11 v2.11 Tasks #21, #22, #23, #24, #25, #25b), extended
with new Tasks 7–9 (Task 7/8: Wave-7 pass-1 remediation; Task 9: Wave-7 pass-2 remediation).

**Test-infrastructure precondition (F-S2123-P2-003, MED, applies to ALL tasks below that
write or run env-var-mutating tests):** `crates/factory-dispatcher/tests/break_glass_gate_bypass.rs`
sets/reads the SHARED process-global `VSDD_BREAK_GLASS_GATE_BYPASS` env var in ~15+ test
functions (AC-014 through AC-023, AC-042, AC-043); `std::env::set_var` mutating shared
process state is unsound under Rust's memory model when other threads may be reading the
environment concurrently, and Cargo's default test harness runs test functions in parallel
threads within one process by default. ALL env-var-mutating tests in this file MUST be
serialized — the implementer chooses EITHER (a) the `serial_test` crate's `#[serial]`
attribute on every test function in this file, OR (b) a shared
`static ENV_LOCK: std::sync::Mutex<()> = std::sync::Mutex::new(())` guard acquired at the top
of every such test — and MUST save the prior value of `VSDD_BREAK_GLASS_GATE_BYPASS` (if any)
before mutating it and restore it (or remove the var if it was previously unset) after the
test body completes, including on panic (a `Drop`-guard/RAII pattern, or an explicit
`#[serial]`-plus-teardown-helper, is acceptable). The chosen approach MUST be documented in a
doc-comment at the top of the file. See also the Architecture Compliance Rules entry below.

1. [ ] Read ADR-039 §Decision 3 v1.10 minimum-viable definition + §AMD-004 audit-durability
       amendment + §Decision 1 axes-independence and BC-1.03.018 PC1–PC12 fully before writing
       any code.
2. [ ] Write failing RED Gate tests for AC-014 through AC-021 (all eight BC-1.03.018 PC1–PC8
       behaviors, including AC-014's non-UTF-8 sub-case (F-S2123-P1-002, EC-015), AC-016's
       `all`-as-trimmed-token sub-case (F-S2123-P1-006) and explicit-two-name-list sub-case
       (F-S2123-P2-001, EC-002), and AC-017's case-variant, trailing-empty-token, and
       whitespace-only sub-cases (F-S2123-P2-001, EC-001/EC-003)) — except the PC9 ordering
       gate (Task 5), the PC10 threat-boundary/env-only-sourcing gates (Tasks 6, 8), the PC11
       audit-durability gate (Task 7), and the PC12 audit-loss observability gate (Task 9),
       each authored in its own dedicated task below.
3. [ ] **[Revised, F-S2123-P1-001, PC11]** Implement the `VSDD_BREAK_GLASS_GATE_BYPASS`
       consultation in `execute_tiers`'s block-decision loop
       (`crates/factory-dispatcher/src/executor.rs`, the
       `for outcome in &tier_outcomes { if plugin_requests_block(...) || plugin_fail_closed(...) { block_intent = true; } }`
       loop — grep-confirmed as the sole block_intent aggregation site for the tiered/sync
       dispatch path both named gates use). Read the env var once per `execute_tiers` call via
       `std::env::var_os` (never `std::env::var` — a non-Unicode value MUST be treated as
       equivalent to unset, never panic, per F-S2123-P1-002/EC-015); parse the comma-separated
       list (trim whitespace per token; treat `all` as a wildcard token for the two named gates
       only, matching as a trimmed TOKEN within the list, not only as the whole value —
       F-S2123-P1-006). First, add a new `InternalLog::write_checked` tri-state method to
       `crates/factory-dispatcher/src/internal_log.rs` (additive, ALONGSIDE the existing
       best-effort `InternalLog::write`/`write_inner`, which remains UNCHANGED for its other
       ~6 hot-path telemetry callers) returning `Written` / `SkippedMountGate` /
       `Failed(io::Error)` — `Written` only on a confirmed durable append; `SkippedMountGate`
       on the pre-existing `.factory/` mount-gate skip path (which the best-effort `write()`
       silently conflates with success); `Failed` on any I/O error. **F-P2-007 (LOW,
       clarification, no behavior change):** "confirmed durable append" for `Written` means
       the underlying `write`/`write_all` (or equivalent buffered-writer flush) syscall
       returned `Ok(())` — an ordinary successful file-append, NOT an `fsync`/`fdatasync`
       durability guarantee. Do NOT add an `fsync` call to reach `Written` (this would be a
       meaningful, unrequested behavior/performance change to every JSONL append this method
       makes) and do NOT weaken `Written` to a lesser signal than "the write syscall
       succeeded" either — `Written` is exactly "the OS accepted the write," which is
       sufficient to distinguish it from `Failed`/`SkippedMountGate`, the actual gap PC11
       closes. Then, at the break-glass
       call site ONLY: for each `outcome`, if `outcome.plugin_name` matches a bypassed gate
       name, ALWAYS attempt the `break_glass.activated` audit write via `write_checked` first
       (regardless of whether that outcome would actually have blocked — AC-019/PC6 emission-
       independent-of-counterfactual semantics are unchanged), carrying the gate name, an
       ISO-8601 timestamp, and the invocation's `dispatcher_trace_id`; suppress that outcome's
       contribution to `block_intent` ONLY IF `write_checked` returned `Written` (AC-042/PC11)
       — if it returned `Failed` or `SkippedMountGate`, do NOT suppress; the underlying gate's
       block decision stands exactly as if the override were unset. The check MUST be scoped
       by exact `plugin_name` match to `validate-wave-gate-prerequisite` and
       `validate-pr-merge-prerequisites` ONLY (AC-021/PC8) — do not implement a generic
       "any name matches → bypass" facility, and consult NO source other than the OS process
       environment variable itself (AC-043/PC10 — no JSON payload field, `plugin_config`,
       registry field, or on-disk file may be treated as an alternate source). Note: land this
       after S-21.19's decision-function extension merges, to avoid a signature-mismatch merge
       conflict against the 2-arg→3-arg `plugin_fail_closed` call site in this same loop.
4. [ ] Verify RED Gates for AC-014 through AC-021 pass.
5. [ ] **[Rewritten, Q-A/F-S2123-P2-002, DUAL-TRIGGER]** Write
       `test_no_agent_gate_fail_closed_flip_without_break_glass` (AC-022, BC-1.03.018 PC9)
       with SIX of its seven controls — (a) `failure_policy`-trigger POSITIVE, (b) `on_error`-
       trigger POSITIVE (NEW), (c) combined-both-triggers POSITIVE (NEW), (d) NEGATIVE
       (all three synthetic registry states paired with the break-glass check present), (e)
       VACUITY (corrected: BOTH triggers absent, a synthetic-only state since the live registry
       already trips `on_error`), (f) COMMENT-ONLY-CONTROL (extended F-S2123-P2-005/F-S2123-P4-002
       — two sub-cases: (f.i) a comment mentioning only the variable name MUST still fire RED;
       (f.ii) a commented-out FULL call pattern such as `// env::var_os("VSDD_BREAK_GLASS_GATE_BYPASS")`
       MUST also still fire RED — proving comment-stripping-before-match is in effect) —
       mirroring S-21.19's AC-012 pattern for the pure-function-over-injectable-inputs
       structure.
       **Additionally, run control (g) LIVE-TREE-CONTROL's `on_error`-trigger half NOW, as
       part of this task, not deferred to S-21.24:** because both named Agent gates ALREADY
       carry `on_error="block"` in the LIVE `hooks-registry.toml` TODAY (independent of any
       `failure_policy` flip), the moment this task's own Task 3 break-glass code lands in the
       same tree, running the detector against the ACTUAL `executor.rs` and the ACTUAL
       `hooks-registry.toml` MUST already report the ordering rule AS TRIGGERED (via the
       `on_error` half) AND the break-glass code present → PASS. Assert this explicitly as
       part of Task 5 (a `#[test]` run against the live tree, not only the synthetic fixtures
       above) — this is meaningfully assertable AS SOON AS Task 3 merges, unlike the
       pre-dual-trigger reading, which could only assert the live-tree state meaningfully
       after S-21.24's `failure_policy` flip. **S-21.24 STILL separately confirms the FULL
       live-tree state (both triggers, post its own `failure_policy` flip) as its own
       existing final-state gate condition — this task's live-tree assertion does not replace
       S-21.24's, it adds an earlier, `on_error`-trigger-scoped confirmation that is true from
       this story's own merge onward.**
6. [ ] (AC-023; BC-1.03.018 v1.6 PC10, F-006 threat-boundary directive) Write
       `test_break_glass_env_not_settable_by_child_process` — an integration test that spawns
       a child process attempting to set `VSDD_BREAK_GLASS_GATE_BYPASS` in its own environment
       (via `std::env::set_var` or an equivalent shell `export` executed inside the child),
       then asserts the PARENT test process's own
       `std::env::var("VSDD_BREAK_GLASS_GATE_BYPASS")` remains unset/unchanged after the child
       exits — proves the OS-level parent-environment isolation no `Agent`-tool-invoked path
       can defeat. Independent of Task 3's implementation (this is a threat-boundary proof
       about the OS process model, not a new code path).
7. [ ] **[NEW — F-S2123-P1-001, PC11, HIGH]** Write `test_break_glass_denied_on_unwritable_audit_sink`
       (AC-042) — construct a scenario where `InternalLog::write_checked` is simulated (via a
       test-only injectable sink, or a mount-gated/unwritable `.factory/` fixture directory) to
       return `Failed(io::Error)`, and a second sub-case returning `SkippedMountGate`; for both,
       assert `block_intent` for the matched gate is UNCHANGED from the no-override baseline
       (suppression denied). Add the NEGATIVE control: `write_checked` returns `Written` →
       suppression GRANTED (restores AC-015's baseline). This task depends on Task 3's
       `write_checked` implementation existing.
8. [ ] **[NEW — F-S2123-P1-003, PC10/Invariant 1, MED]** Write
       `test_break_glass_not_derivable_from_non_env_sources` (AC-043) — construct a dispatch
       where a synthetic JSON tool-call payload field, a `plugin_config` entry, an alternate
       `hooks-registry.toml` field, and an on-disk file all carry a value that would match a
       gate name or `all` under a non-`VSDD_BREAK_GLASS_GATE_BYPASS` key, while the actual OS
       env var remains unset; assert suppression does NOT occur for any of the four sub-cases
       and NO `break_glass.activated` event is emitted.
9. [ ] **[NEW — Q-B/F-S2123-P2-006, PC12, MED]** Extend Task 3's break-glass call site so
       that, whenever `InternalLog::write_checked` returns other than `Written` for a
       `break_glass.activated` audit attempt, the dispatcher emits a distinct immediate
       diagnostic to the same fallback channel `InternalLog::write` already uses for its own
       internal errors (`eprintln!`/stderr), identifying the matched gate name, the
       dispatch's trace UUID (`dispatcher_trace_id`), and the `write_checked` failure variant
       (`Failed(io::Error)` vs. `SkippedMountGate`). This emission is UNCONDITIONAL on whether
       the invocation was blocking — it MUST fire identically in AC-042's suppression-denial
       case AND in a counterfactual-clean-pass case where there was no block to deny. The
       diagnostic itself is best-effort (a further confirmed-write check on the diagnostic
       would recurse indefinitely) — matching `InternalLog::write`'s own best-effort posture
       for its internal I/O errors. Write `test_break_glass_pc12_diagnostic_fires_on_clean_pass_audit_loss`
       (AC-044) — construct a scenario where the matched gate would NOT have blocked (a clean
       pass, EC-004-class) AND `InternalLog::write_checked` is simulated to return
       `Failed`/`SkippedMountGate`; assert `block_intent` is UNCHANGED (exit 0, identical to a
       normal clean pass) BUT captured stderr output contains the diagnostic (gate name, trace
       UUID, failure variant) — proving the diagnostic fires independent of the
       suppression-decision outcome, closing BC-1.03.018 EC-008.

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-21.19 (W6; integration-ordering prerequisite) | Extends `plugin_fail_closed` to 3-arg inside the same `execute_tiers` block-decision loop this story's Task 3 also edits. | Land this story's Task 3 after S-21.19 merges to avoid a signature-mismatch merge conflict — the two changes touch the same loop body but are semantically independent. | This is a source-level ordering choice, not a BC-level dependency — do not read S-21.19's `depends_on` edge as implying BC-1.03.018 has any precondition on BC-1.03.017's internals. |
| S-21.11 pre-split history (v2.0, adversarial pass-1) | AC-023's threat-boundary control (F-006) and AC-013c-class "closes the class, not the instance" reasoning were both added in the same v2.0/v2.1 remediation burst that unified this story's original scope elements. | The AC-001/AC-012 four-control pattern (POSITIVE/NEGATIVE/VACUITY/LIVE-TREE) was reused verbatim for AC-022 — this story's Task 5 explicitly follows that precedent. | Do not re-litigate the break-glass mechanism's minimum-viable scope (environment-variable, not a file-based or API-based mechanism) — ADR-039 §Decision 3 v1.10 ratified this choice explicitly. |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| **Break-glass-before-flip hazard, DUAL-TRIGGER (this story gates S-21.24's flip for the two Agent gates only):** the break-glass mechanism (Task 3) MUST land before, or atomically with, S-21.24's fail-closed-flip commit for `validate-wave-gate-prerequisite` and `validate-pr-merge-prerequisites` — and, per PC9's dual-trigger correction, the break-glass code MUST already be present in the live tree TODAY, because both gates already carry `on_error="block"` independent of any `failure_policy` flip | BC-1.03.018 PC9 + Invariant 6, corrected Q-A/F-S2123-P2-002; ADR-039 §Decision 1 axes-independence + §Decision 3 v1.10 amendment | AC-022 CI gate (seven controls); PR reviewer verifies commit ordering for these two plugins specifically at S-21.24, AND verifies this story's own Task 5 on_error-trigger live-tree confirmation is present |
| The AC-022 detector MUST strip `//` and `/* */` comment blocks BEFORE searching for the executable pattern (comment-stripping MANDATORY, F-S2123-P4-002); a comment-only mention MUST NOT satisfy the detector whether it contains merely the variable name token (f.i — pre-existing F-S2123-P2-005) or a full commented-out call pattern such as `// env::var_os("VSDD_BREAK_GLASS_GATE_BYPASS")` (f.ii NEW — which WOULD satisfy a bare substring search but MUST NOT satisfy the comment-stripping-aware detector); closes CWE-636 full-call-in-comment false-green | F-S2123-P2-005 + F-S2123-P4-002 (MED) | AC-022's COMMENT-ONLY-CONTROL (f.i) and (f.ii) |
| Break-glass mechanism MUST NOT mutate `hooks-registry.toml` and MUST be hard-scoped to the two named gates only — this scope restriction applies identically under the `all` wildcard: non-named blocking plugins MUST NOT be suppressed and MUST NOT receive `break_glass.activated` records (F-S2123-P3-001) | BC-1.03.018 PC7/PC8 + Invariants 4/5; PC8 `all`-wildcard extension (v1.5) | AC-020/AC-021/AC-045 tests |
| Every break-glass activation MUST emit an audit event, independent of whether a counterfactual block would have occurred | BC-1.03.018 PC5/PC6 + Invariant 3 | AC-018/AC-019 tests |
| Suppression MUST be gated on a CONFIRMED DURABLE `InternalLog::write_checked` result (`Written`); a `Failed`/`SkippedMountGate` result MUST deny suppression, not merely log the failure. `Written` means the write syscall succeeded — NOT an `fsync` guarantee (F-P2-007); do not add or remove durability semantics beyond what PC11 specifies | BC-1.03.018 PC11 + Invariant 7 (Q3, F-S2123-P1-001, ADR-039 §AMD-004) | AC-042 tests; `InternalLog::write` (best-effort) remains unchanged/unused at this specific call site |
| The override MUST be sourced ONLY from the OS process environment — no JSON payload field, `plugin_config`, registry field, or on-disk file may be consulted as an alternate source | BC-1.03.018 PC10 + Invariant 1 (F-S2123-P1-003) | AC-043 test |
| Whenever `InternalLog::write_checked` returns other than `Written`, the dispatcher MUST emit an unconditional stderr diagnostic (gate name, trace UUID, failure variant) independent of whether the invocation was blocking | BC-1.03.018 PC12 + Invariant 8 (Q-B, F-S2123-P2-006) | AC-044 tests (clean-pass case AND suppression-denial case) |
| **ALL env-var-mutating tests in `break_glass_gate_bypass.rs` MUST be serialized (`serial_test::serial` or a shared `Mutex` guard) and MUST save/restore the prior `VSDD_BREAK_GLASS_GATE_BYPASS` value** — `std::env::set_var` mutating shared process state is unsound under concurrent test execution | F-S2123-P2-003 (MED) | Code review of `break_glass_gate_bypass.rs`; an unserialized env-var-mutating test is a review-blocking defect |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| Rust toolchain (workspace pinned) | `rust-toolchain.toml` | Implementing the break-glass check |
| serde / toml (workspace) | workspace version | Parsing `hooks-registry.toml` in AC-022's gate test |
| chrono (or workspace-equivalent) | workspace version | ISO-8601 timestamp field for `break_glass.activated` audit events (AC-018) — reuse `InternalEvent::now`/`with_ts` (already present in `internal_log.rs`), do not introduce a new timestamp dependency |
| `serial_test` (OPTIONAL — F-S2123-P2-003) | workspace-compatible version, if chosen | Serializes the ~15+ env-var-mutating tests in `break_glass_gate_bypass.rs`; the implementer MAY instead use a hand-rolled `static Mutex<()>` guard requiring no new dependency — see this file's Architecture Compliance Rules entry for the mandatory serialization requirement itself, which is independent of which mechanism is chosen |

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| `crates/factory-dispatcher/src/executor.rs` | modify | Add the `VSDD_BREAK_GLASS_GATE_BYPASS` consultation to `execute_tiers`'s block-decision loop (Task 3) |
| `crates/factory-dispatcher/tests/break_glass_gate_bypass.rs` (new) | create | AC-014 through AC-021 real-dispatch tests (incl. AC-014's non-UTF-8, AC-016's `all`-as-token and explicit-two-name-list sub-cases, AC-017's case-variant/trailing-empty-token/whitespace-only sub-cases), AC-022's `test_no_agent_gate_fail_closed_flip_without_break_glass` (six of seven controls, incl. the COMMENT-ONLY-CONTROL (f.i) token-in-comment and (f.ii) full-call-in-comment sub-cases (F-S2123-P4-002) and the immediately-assertable `on_error`-trigger LIVE-TREE-CONTROL sub-check; seventh LIVE-TREE confirmed by S-21.24 post-flip), AC-023's `test_break_glass_env_not_settable_by_child_process`, AC-042's `test_break_glass_denied_on_unwritable_audit_sink` (+ NEGATIVE control), AC-043's `test_break_glass_not_derivable_from_non_env_sources`, AC-044's `test_break_glass_pc12_diagnostic_fires_on_clean_pass_audit_loss` (+ suppression-denial-case variant), and AC-045's `test_break_glass_all_does_not_suppress_non_named_plugin()` (F-S2123-P3-001 — `all`×non-named-plugin negative-control); ALL env-var-mutating tests in this file MUST be serialized per the Architecture Compliance Rules entry (F-S2123-P2-003) |
| `crates/factory-dispatcher/src/internal_log.rs` | **modify** (F-S2123-P1-001 correction — was incorrectly marked read-only) | This story is `InternalLog::write_checked`'s own implementation site (BC-1.03.018 Architecture Anchors, PC11): add the new, additive, tri-state method (`Written`/`SkippedMountGate`/`Failed(io::Error)`) ALONGSIDE the existing best-effort `InternalLog::write`/`write_inner`, which remains UNCHANGED for its other ~6 hot-path telemetry callers; the break-glass call site (Task 3) is the ONLY caller of `write_checked` |
| `plugins/vsdd-factory/hooks-registry.toml` | read-only | This story does not annotate either Agent gate — S-21.24 applies the flip once this story's break-glass code is merged |

## Changelog

| Version | Date | Description |
|---------|------|--------------|
| 1.6 | 2026-08-24 | Wave-7 floor-breaking pass-6/R5 story-layer remediation (story-writer). **BC-1.03.018 v1.5 → v1.6** re-anchor (POLICY 8 full propagation — frontmatter `behavioral_contracts`, BC table version column, Token Budget, Task 6 version cite). Does NOT touch BC files, INDEX/STATE.md, or input-hash (state-manager step ④). |
| 1.5 | 2026-08-23 | Wave-7 pass-5/R4 story-layer remediation (story-writer). **F-S2123-P5-001 (HIGH):** AC-022 PC9 control re-lettering to align with BC-1.03.018 v1.5 canonical partition (e)=VACUITY/(f)=COMMENT-ONLY — story had (e)=COMMENT-ONLY/(f)=VACUITY, inverted vs. BC. Swapped control positions and sub-case labels: (e.i)→(f.i), (e.ii)→(f.ii) throughout. Fixed false cross-reference at L354 ('present as (e)' → 'present as (f)'). Propagated to Task 5 control list, Architecture Compliance Rules detector row (two `(e.i)`/`(e.ii)` cites → `(f.i)`/`(f.ii)`), File Structure Requirements, and test description sub-case parenthetical. BC-1.03.017 confirmed not cited by version in this story body — no BC-1.03.017 re-anchor needed. Does NOT touch BC-1.03.017/BC-1.03.018 bodies, INDEX/STATE.md, or input-hash (state-manager step ④). |
| 1.4 | 2026-08-23 | Wave-7 pass-4/R3 remediation sub-burst 3b (cycle v1.0-brownfield-backfill, D-1075): BC-1.03.018 v1.4 → v1.5 re-anchor (POLICY 8 full propagation — frontmatter `behavioral_contracts`, body BC table version + Title cell, Token Budget, Task 6, Architecture Compliance Rules). **F-S2123-P4-001 (MED):** Confirmed story (g)=LIVE-TREE already matches BC-1.03.018 v1.5 — no story edit required. **F-S2123-P4-002 (MED):** AC-022 COMMENT-ONLY-CONTROL (e) extended: comment-stripping MANDATORY before executable-code check; split into (e.i) token-in-comment (pre-existing) and (e.ii) full-call-in-comment MUST-NOT-satisfy sub-case (e.g. `// env::var_os("VSDD_BREAK_GLASS_GATE_BYPASS")` commented-out full call MUST still fire RED); Architecture Compliance Rules detector row updated; Task 5 (e) extended; test description notes comment-stripping-aware detector; File Structure Requirements updated; closes CWE-636 full-call-in-comment false-green. **F-S2123-P4-003 (MED):** ADR-044 v1.2 → v1.3 in Position-in-DAG. **F-S2123-P4-004 (LOW):** AC-045 test extended with `all`+non-named-alone sub-case (non-named plugin is SOLE blocker; named gates produce no blocking outcome; `all` override still does not suppress it). BC table Title cell updated per POLICY 7 to verbatim subset of BC-1.03.018 v1.5 H1 (added `all`-as-trimmed-comma-token clause + comment-stripping-mandatory + seven-control partition (a)-(e) synthetic (f) COMMENT-ONLY (g) LIVE-TREE labels). |
| 1.3 | 2026-08-23 | Wave-7 pass-3 remediation sub-burst 3b (cycle v1.0-brownfield-backfill, D-1074): BC-1.03.018 v1.3 → v1.4 re-anchor (POLICY 8 full propagation — frontmatter `behavioral_contracts`, body BC table Title cell + Clause cell, Token Budget). **F-S2123-P3-001 (HIGH):** new AC-045 — `all`×non-named-plugin negative-control: under `VSDD_BREAK_GLASS_GATE_BYPASS=all`, a NON-named blocking plugin's block MUST STILL STAND and MUST NOT receive a `break_glass.activated` record; closes CWE-636+CWE-863 compound hazard a naive `all`-suppress-every-outcome implementation would introduce with no audit trail; `test_break_glass_all_does_not_suppress_non_named_plugin()`. **F-S2123-P3-002 (MED):** AC-022 control-count reconciled six→seven to match BC-1.03.018 v1.4 PC9 (COMMENT-ONLY control already present as (e) in story; BC now mirrors at seven); test description reconciled to "seven controls above (six authored here — (a) through (f); the seventh, LIVE-TREE-CONTROL (g) confirmed by S-21.24)"; Task 5 reconciled "FIVE of its six" → "SIX of its seven". BC table Title cell updated per POLICY 7 to subset of v1.4 H1 (added `all`-wildcard scope-restriction and detector-precision clauses); Clause cell: PC8 extended with AC-045, PC9 updated to "SEVEN controls: six authored here". Architecture Compliance Rules: PC8 scope row extended to cite AC-045; AC-022 CI gate count updated to seven controls. Task 6 version cite v1.3→v1.4. Zero stale v1.3 live cites remain. BC-1.03.017 v1.21 sweep: no explicit v1.21 cites found in story body — confirmed zero stale cites. |
| 1.2 | 2026-08-22 | Wave-7 pass-2 remediation sub-burst 3b (D-1071, security-sensitive): BC-1.03.018 v1.2 → v1.3 re-anchor (POLICY 8). F-S2123-P2-001 (HIGH): EC-001/002/003 given dedicated falsifiable tests, replacing equivalence-reasoning cross-map claims. F-S2123-P2-002 (MED): AC-022 rewritten dual-trigger (`on_error` OR `failure_policy`), six controls, immediately-assertable `on_error`-trigger live-tree confirmation in Task 5. F-S2123-P2-003 (MED): mandatory test serialization for env-var-mutating tests. F-S2123-P2-004 (MED): mount-gate wedge operator runbook note (`VSDD_LOG_DIR`). F-S2123-P2-005 (MED): AC-022 detector pinned to executable pattern + comment-only negative control. F-S2123-P2-006 (MED): new AC-044/Task 9 for PC12 audit-loss observability/EC-008. LOWs F-P2-007..010: fsync clarification, stale version pin dropped, split-outcome note, "S-21.19 (merged)" → "REOPENED". |
| 1.1 | 2026-08-22 | Wave-7 pass-1 remediation sub-burst 3b (D-1069; Q3/F-S2123-P1-001..006): BC-1.03.018 v1.1 → v1.2 re-anchor (POLICY 8 full propagation). New AC-042 (audit fail-closed-on-audit-failure, PC11/EC-007, HIGH) + new Task 7. Non-UTF-8 override-value handling (EC-015, MED) added to Task 3/AC-014. New AC-043 (env-only sourcing negative control, PC10/Invariant 1, MED) + new Task 8. New "BC-1.03.018 Edge Case Cross-Map" subsection (all 7 BC ECs, MED). AC-015 wording corrected ("origin's" EC-006, LOW). AC-016 clarified + new test for `all` as a trimmed comma-separated token (LOW). File Structure Requirements' `internal_log.rs` row corrected to `modify` (this story implements `InternalLog::write_checked`). |
| 1.0 | 2026-08-20 | Created by story-writer via the operator-directed 6-way split of CONVERGED S-21.11 (v2.11, 16-pass 3-CLEAN BC-5.39.001), per `.factory/planning/S-21.11-decomposition-plan.md` §1/§3. Owns AC-014 through AC-023 (10 ACs, all of BC-1.03.018 PC1–PC10 — entirely BC-1.03.018's own sibling BC), copied verbatim from S-21.11 v2.11 with zero weakening. Tasks renumbered #1–#6 (self-contained) from S-21.11 v2.11 Tasks #21, #22, #23, #24, #25, #25b. |
