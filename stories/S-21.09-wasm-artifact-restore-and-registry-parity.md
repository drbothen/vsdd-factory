---
document_type: story
level: ops
story_id: "S-21.09"
epic_id: "E-21"
version: "1.32"
status: merged
producer: story-writer
timestamp: 2026-08-04T00:00:00Z
phase: brownfield-backfill
inputs:
  - .factory/specs/behavioral-contracts/ss-04/BC-4.16.001.md
  - .factory/specs/architecture/decisions/ADR-031-e21-factory-state-data-loss-hardening.md
  - plugins/vsdd-factory/hooks-registry.toml
  - plugins/vsdd-factory/resolvers-registry.toml
input-hash: "8aba09e"
# BC status: input-hash computed after artifact write; POLICY 18 three-way equality owed at first state-manager burst
traces_to: .factory/specs/behavioral-contracts/ss-04/BC-4.16.001.md
points: "16"
depends_on: []
blocks: []
behavioral_contracts: [BC-4.16.001]
verification_properties: []
# VP gap: verification_properties: [] — all 4 VP rows in BC-4.16.001 §Verification Properties
# read "(TBD — to be assigned by state-manager after VP authoring pass)". VP allocation owed;
# see §Routing Proposals.
priority: "P1"
cycle: v1.0-brownfield-backfill
wave: 4
target_module: crates/hook-plugins/validate-factory-path-staging
subsystems: [SS-04]
estimated_days: null
assumption_validations: []
risk_mitigations: []
tdd_mode: strict
---

> **tdd_mode: strict** — Full TDD Iron Law enforced: all non-trivial function bodies use `todo!()`;
> Red Gate density check ≥0.5 required before Step 4 dispatch.

> **Execute:** `/vsdd-factory:deliver-story S-21.09`

# S-21.09: validate-factory-path-staging.wasm artifact restore + per-name registry parity check

## Narrative

- **As a** factory pipeline operator running any Bash tool call
- **I want** the `validate-factory-path-staging` WASM guard to actually execute (its `.wasm`
  artifact is committed and tracked in git) AND a per-name registry parity CI check that
  detects any future declared-but-missing artifact before it reaches production
- **So that** INV-E21-001 Layer-1 enforcement (BC-4.16.001) is not silently inert, and the
  structural class of failure — declared name absent from git — is permanently closed

## Context / Finding Summary

S-21.01 (merged PR #759, `7bb0e797`, 2026-07-23) created the crate source at
`crates/hook-plugins/validate-factory-path-staging/` and registered the plugin in
`hooks-registry.toml` with `event = "PreToolUse"`, `tool = "^Bash$"`, `priority = 140`,
`on_error = "continue"`. The WASM artifact was never built and committed.

**Root cause: release lag, not a parity-check gap.** `v1.0.0-rc.23` was tagged
2026-07-18 — five days BEFORE S-21.01 merged on 2026-07-23. The operator runtime at
rc.23 carries a `hooks-registry.toml` with ZERO occurrences of
`validate-factory-path-staging`, so the guard has been inert for 100% of its existence at
the operator level. The develop branch is 44 commits unreleased
(`v1.0.0-rc.23-44-g62fbcf1a`).

**The missing WASM on develop is a hygiene/self-hosting defect.** The release pipeline
splits across two jobs in `release.yml`, each containing an identically-named "Verify
registry-declared WASM plugins are staged" step — describing "the Verify step" in the
singular is ambiguous and was the source of earlier miscommunication. The `build-binaries`
job builds every declared WASM and runs the FIRST Verify step, which parses
`hooks-registry.toml` ONLY (inline comment on that step: "Only hooks-registry entries are
checked here; resolvers-registry is checked in the commit-binaries job"); a separate
"Stage artifact directory" step in `build-binaries` also carries two hardcoded
keep-assertions naming `vsdd-context-resolvers.wasm` specifically
(`test -f plugins/vsdd-factory/hook-plugins/vsdd-context-resolvers.wasm` and
`grep -q "hook-plugins/vsdd-context-resolvers.wasm"` against `resolvers-registry.toml`).
The `commit-binaries` job downloads the built artifacts, stages binaries and WASMs with
`git add -f`, and runs the SECOND Verify step — a general dual-registry parity check that
parses BOTH `hooks-registry.toml` and `resolvers-registry.toml` via the same
`grep -E '^\s*plugin\s*=\s*"hook-plugins/'` predicate applied twice. The resolver registry
read in this step is suppressed with `2>/dev/null`; if `resolvers-registry.toml` is
absent, renamed, or its `plugin =` lines change shape, `resolver_plugins` becomes empty
and the resolver half passes vacuously (no non-vacuity floor on the resolver arm; see
§Routing Proposals below). `origin/main` has 34-tracked/34-declared parity. The
develop-branch gap means develop's own tree cannot run its own guard — state-manager and
other agents invoking `git add` on develop are unprotected. This defect self-heals at
rc.24 release time (`commit-binaries` stages the WASM with `git add -f` and its
dual-registry Verify step covers both registries) but is a live self-hosting gap on
develop.

**Evidence of non-invocation in session logs.** Dispatcher log grep of
`plugin_name == "validate-factory-path-staging"` across all
`.factory/logs/dispatcher-internal-*.jsonl` files (2026-07-23 to 2026-08-10) returns
**0 records** for all event types. Same-matcher `PreToolUse`/`^Bash$` siblings show
substantially more invocations over the same window — for example, `verify-git-push`
registered ≥14,000 `plugin.invoked` events as of 2026-08-11 (this is a live counter;
exact value grows daily; siblings differ from each other). The 0-vs-non-zero contrast is
the load-bearing signal; the exact per-sibling figure is not fixed in this spec.
**Post-fix evidence (preliminary):** a test of the AC-001 fix on 2026-08-11 (WASM built
and force-tracked, not yet merged) produced 4 records for
`plugin_name == "validate-factory-path-staging"` (2 × `plugin.invoked`, 2 ×
`plugin.completed`, timestamped 2026-08-11T08:55) in a live dispatcher session —
confirming the guard is reachable once the artifact is tracked. These 4 records fall
outside the 2026-07-23 to 2026-08-10 gap window; the "0 records" claim for that range
still reproduces exactly.

**Why CI does not catch this.** `.github/workflows/ci.yml` has a step "Stage WASM plugins
for run-all.sh" that runs a full `--workspace` WASM build and copies every artifact into
the gitignored `plugins/vsdd-factory/hook-plugins/` directory, immediately BEFORE "Run
full bats suite (run-all.sh)". That step does NOT set `CI_REQUIRE_ARTIFACTS`, and
`_require_artifacts` skip-passes on absence of `CI_REQUIRE_ARTIFACTS`. CI validates the
SOURCE (the Rust crate compiles and its unit tests pass) but never the DELIVERY (the
artifact must be tracked in git for the guard to load in production sessions).

## BC Status

`behavioral_contracts: [BC-4.16.001]` — BC-4.16.001 v1.8 is the governing BC.
`lifecycle_status: active` — auto-promoted by POL-14 at S-21.01 PR #759 merge
(`7bb0e797`, 2026-07-23). Document-level `status: draft` in the BC frontmatter is stale
metadata and does not govern enforcement; treat as active.

ACs 001–007 are all active. **VP gap:** `verification_properties: []` because all 4 VP
rows in BC-4.16.001 §Verification Properties read
"(TBD — to be assigned by state-manager after VP authoring pass)". No VP-NNN IDs are
invented here; VP allocation is owed — routing proposal in §Routing Proposals below.

## Acceptance Criteria

### AC-001: Artifact is tracked in git (traces to BC-4.16.001 Precondition 3)

The file `plugins/vsdd-factory/hook-plugins/validate-factory-path-staging.wasm` MUST
be present in the git index — i.e., `git ls-files plugins/vsdd-factory/hook-plugins/validate-factory-path-staging.wasm`
returns a non-empty result. File existence on disk alone is insufficient; the artifact must
be committed.

**Note:** `plugins/vsdd-factory/hook-plugins/` is excluded by `.gitignore` (gitignored
directory for WASM binaries not shipped via source control in normal commits). The
established precedent for committing WASM binaries to this path is `git add -f`, which
overrides the gitignore. This same pattern was used to track all 35 existing `.wasm` files
in that directory.

**Byte provenance** (independently reproduced at commit `12f280d1` by two adversarial
passes; values are host-toolchain-specific — the release pipeline rebuilds all WASMs at
tag-push and is functionally equivalent but not necessarily byte-identical):

- Build command: `cargo build -p validate-factory-path-staging --target wasm32-wasip1 --release`
- Toolchain: `rustc 1.95.0 (59807616e 2026-04-14)`
- Target: `wasm32-wasip1`
- Byte count: **193,427**
- SHA-256: `6f6570f9a776f741f6d610b841cedff8a17766136db605284f377e5305f6ce17`
- Git blob (HEAD at `12f280d1`): `611303b3b8edcebc70cf014919e2f73809e7ef52`
- Verification: `shasum -a 256 plugins/vsdd-factory/hook-plugins/validate-factory-path-staging.wasm`

- **Test:** Property is covered class-wise by T-012 (bundle_orphan_check.rs) in both
  directions: before AC-001 fix, `validate-factory-path-staging.wasm` appears in
  `declared_set − tracked_set` (assertion fires, confirming non-tracked state); after
  fix, T-009 confirms zero tracked-but-undeclared artifacts (no orphan gap introduced).
  No separate `git ls-files` test function exists for this AC.

### AC-002: WASM-absent skip count drops to zero after artifact is committed (traces to BC-4.16.001 Precondition 3)

`_require_artifacts` gates on **two** artifacts: (1) the `factory-dispatcher` binary at
`target/release/factory-dispatcher`, and (2) `validate-factory-path-staging.wasm` copied
into `$WORK/hook-plugins/` during `setup()`. Committing the WASM (AC-001) eliminates the
WASM-absent skip condition but NOT the dispatcher-absent skip condition.

**Important:** `_require_artifacts` tests `$DISPATCHER` **first**. Without a dispatcher
binary, every test skips at the dispatcher check before the WASM branch is reached.
The string "validate-factory-path-staging.wasm not present" can therefore never appear
in bats output unless the dispatcher is already present. Asserting its absence when the
dispatcher is missing is vacuous — zero matches are guaranteed regardless of whether the
WASM is committed.

AC-002's meaningful assertion is the **positive claim**: after AC-001, with both the
dispatcher built and `CI_REQUIRE_ARTIFACTS=1` set, the 25 tests that previously skipped
due to the absent WASM now **execute** (produce block/pass decisions, not skips).

**Note on live-session invocation:** A live dispatcher session log confirming
`plugin.completed` events requires a fresh Claude Code session, which is outside the
mechanical bats test scope. The bats harness is the mechanically verifiable proof of
invocation — when both artifacts are present, the dispatcher loads and invokes the WASM;
the block/pass decisions produced by AC-003/AC-004/AC-005 ARE the invocation evidence
within the test context.

- **Test:** run `validate-factory-path-staging.bats` with `CI_REQUIRE_ARTIFACTS=1` set
  and `factory-dispatcher` binary built (`cargo build --release -p factory-dispatcher`);
  assert all 25 previously WASM-absent tests execute — zero skip lines for either
  "validate-factory-path-staging.wasm not present" or "factory-dispatcher binary not
  built" (WASM-absent skip count = 0; full end-to-end execution confirmed)

### AC-003: Guard BLOCKS git add of .factory/ path on product branch (traces to BC-4.16.001 Postcondition 1)

When a Bash payload `git add .factory/STATE.md` is dispatched on a product branch (e.g.,
`develop`), the dispatcher MUST return `block_intent = true` (exit code 2) and the log
MUST contain a `FactoryPathOnProductBranch` error emitted by the `validate-factory-path-staging`
plugin.

**Existing coverage:** T-001 (`@test "T-001 S-21.01 AC-001: guard blocks git add .factory/STATE.md on develop branch"`)
already exists in `validate-factory-path-staging.bats` and runs green in CI (after the CI
build step produces the WASM). After AC-001 (WASM committed), T-001 MUST run without the
`_require_artifacts` skip in all environments, including production sessions.

**What is genuinely missing:** delivery/reachability coverage for the operator runtime —
the rc.23 bundle lacks the registry entry and WASM entirely. That gap resolves at rc.24
release; it is outside narrow scope.

- **Test:** T-001 in `validate-factory-path-staging.bats` — bats mock with
  `branch = develop`, payload = `git add .factory/STATE.md`; assert exit 2 and
  `FactoryPathOnProductBranch` in output. Must not skip after AC-001 fix.

### AC-004: Guard PASSES git add of non-.factory/ path (traces to BC-4.16.001 Postcondition 2)

When a Bash payload `git add src/main.rs` is dispatched on any branch, the dispatcher
MUST return `block_intent = false` (exit code 0) and the log MUST contain a
`plugin.completed` event for `validate-factory-path-staging` with no block.

**Existing coverage:** T-004 exists in `validate-factory-path-staging.bats` and runs
green in CI. After AC-001 (WASM committed), T-004 MUST not skip in all environments.

- **Test:** T-004 in `validate-factory-path-staging.bats` — bats mock with any branch,
  payload = `git add src/main.rs`; assert exit 0. Must not skip after AC-001 fix.

### AC-005: Guard PASSES git add of .factory/ path on factory-artifacts branch (traces to BC-4.16.001 Postcondition 3)

When a Bash payload `git add .factory/STATE.md` is dispatched on the `factory-artifacts`
branch, the dispatcher MUST return `block_intent = false` (exit code 0). This is the
state-manager's canonical mounted-worktree workflow and MUST NOT be blocked.

**Existing coverage:** T-002 exists in `validate-factory-path-staging.bats` and runs
green in CI. After AC-001 (WASM committed), T-002 MUST not skip in all environments.

- **Test:** T-002 in `validate-factory-path-staging.bats` — bats mock with
  `branch = factory-artifacts`, payload = `git add .factory/STATE.md`; assert exit 0.
  Must not skip after AC-001 fix.

### AC-006: Exhaustive per-name declared-vs-tracked set-difference (declared→tracked direction) (traces to BC-4.16.001 Precondition 3)

A new Cargo integration test in `crates/factory-dispatcher/tests/bundle_orphan_check.rs`
MUST extend the existing dual-registry orphan machinery (established by S-19.04/S-19.06)
with the INVERSE direction: **declared→tracked**.

The existing T-009 asserts `tracked_set ⊆ declared_set` (zero tracked WASMs absent from
both registries). AC-006's new test (T-012) asserts `declared_set ⊆ tracked_set` (every
declared WASM must be tracked in git — no declared-but-missing artifacts).

**Implementation:** extend `bundle_orphan_check.rs` with a new test function
`test_S_21_09_ac006_T012_declared_set_subset_of_tracked_set()` that:

1. **Registry-inventory assertion (T-017, T-018):** enumerate `plugins/vsdd-factory/*.toml` and
   assert the result equals exactly `{hooks-registry.toml, resolvers-registry.toml}`.
   **The narrower `*-registry.toml` glob is explicitly rejected:** `metrics_registry.toml`
   (underscore separator) escapes the hyphenated filter; T-024 in `bundle_orphan_check.rs`
   mutation-proves this failure case. `*.toml` catches all TOML files in the directory
   regardless of separator style. Hard-fail if either expected file is absent or if the
   enumeration returns any file outside the expected set.
2. Parse both `hooks-registry.toml` and `resolvers-registry.toml` using the hardened
   `parse_plugin_refs()` function — handles canonical form (`plugin = "hook-plugins/…"`),
   nospace form (`plugin="hook-plugins/…"`, T-013), and dotslash form
   (`plugin = "./hook-plugins/…"`, T-014). Path-value normalisation is delegated to
   `extract_hook_plugin_name(registry_path: &Path, plugin_path: &str) -> Option<String>`
   applying four steps: (1) resolve `plugin_path` against the registry file's parent via
   `registry_parent.join(plugin_path)`; (2) lex_norm (extracted to module level; shared by
   `extract_hook_plugin_name` and `detect_ungated_declarations`) — push `Normal` components,
   pop on `ParentDir`, skip `CurDir`; the `RootDir | Prefix(_)` arm clears the accumulated
   parts — this is the fourth provably-unreachable arm (prior guards prevent reaching it
   in practice); (3) three reachable sequential exclusion gates — if any gate fires, returns
   `None` and the chain short-circuits (the function also contains four defensive unreachable
   exits that serve as structural safety nets but are unreachable given the prior guards):
   (a) minimum-length check: normalised component count must meet `expected_depth + 2`;
   short absolute fixtures fail here before the prefix loop is consulted (T-026 short
   fixture, T-033);
   (b) prefix assertion: normalised path must carry the
   registry-parent prefix; depth-matched absolute fixtures fail here (T-026 depth-matched
   fixture: counts tempdir Normal components at runtime to derive `expected_depth`, builds
   path with exactly `expected_depth` Normal segments before `hook-plugins` so the length
   check passes, then prefix loop fires);
   (c) `hook-plugins` component match: a normalised component must match
   `eq_ignore_ascii_case("hook-plugins")` — paths with a different directory name at the
   correct depth fail here (T-035); `eq_ignore_ascii_case` admits case-variant forms (e.g.,
   `Hook-Plugins/x.wasm`) into the declared set **verbatim** — no lowercasing is applied to
   the return value; on case-sensitive CI Linux, a verbatim case-variant declared path will
   not match the lowercase git-tracked path and will appear in `MISSING:`; **a case-variant
   declaration is always a gate failure by design** (T-031, T-042);
   (4) return the registry-relative path (`joined_parts[expected_depth..].join("/")`) — e.g.,
   `hook-plugins/foo.wasm` for a flat entry or `hook-plugins/sub/nested.wasm` for a nested
   entry; case-variant forms are returned verbatim (see gate (c) note above). This replaces
   the old `trim_start_matches("./").strip_prefix("hook-plugins/")` chain and closes
   traversal-into (T-025), nested-subdirectory (T-032) fail-opens; traversal-that-cancels
   and absolute forms are correctly EXCLUDED (T-023(b), T-026). Covers dual-registry
   aggregation; handles `vsdd-context-resolvers.wasm` without special-casing.
2a. **Hooks non-vacuity floor:** assert `hooks_plugins.len() >= 30`. An empty or
   under-populated parse would vacuously pass every downstream check; this guard
   hard-fails before the set operations run. Boundary controls: T-016 (30 entries: PASS;
   also the PASS-arm positive control), T-027 (29 entries: fires with
   `"...has only 29 entries"`), T-019 (1-entry set: fires).
2b. **Resolvers non-vacuity floor:** assert `resolver_plugins.len() >= 1`. The
   `2>/dev/null` suppression on the shell-side resolver registry read (§Routing Proposals)
   motivated this gate — an empty result must not silently pass. Boundary control: T-022
   (`resolver_plugins.is_empty()` arm: fires when resolvers list is empty).
2c. **Ungated-declaration gate (`detect_ungated_declarations`):** before the git calls,
   scan all raw `plugin =` declarations. First apply the worktree-root containment predicate
   (`joined_parts.len() > root_parts.len() && joined[0..root_parts.len()] == root_parts`) to
   determine in-repo vs. outside-repo; for in-repo declarations, the gated-vs-ungated
   determination delegates to `extract_hook_plugin_name(registry_path,
   plugin_path).is_none()` — the SAME three-gate check `parse_plugin_refs()` uses to build
   the declared set, not a second hand-written copy. This classifies each declaration into
   exactly one of three mutually exclusive classes.

   **Single-copy detect gate refactor (pass-11, closes pass-10 BLOCKER-1):** before pass-11,
   `detect_ungated_declarations` re-derived gates 1–3 inline as its own four-conjunct
   containment predicate, duplicating the logic in `extract_hook_plugin_name`. A composed
   mutation (M1 length-conjunct weakening combined with M4 gate-3 case-check weakening)
   survived the full suite because the two independent copies could drift out of sync while
   every single-gate mutation was still individually caught. `detect_ungated_declarations`
   now delegates entirely to `extract_hook_plugin_name` for the correctness gates — there is
   exactly one copy of the three-gate logic in the codebase, so a mutation to it is caught by
   both `parse_plugin_refs()`'s tests and `detect_ungated_declarations`'s tests
   simultaneously.

   **Total predicate invariant:** every declaration falls into exactly one of — (1) resolves
   under `registry_parent/hook-plugins/` → gated, not reported; (2) resolves inside the
   worktree root but outside `hook-plugins/` → emit `UNGATED-DECLARATION: <path>` and return
   `Err`; (3) resolves outside the worktree root → emit `OUTSIDE-REPO-DECLARATION: <path>`
   and return `Err`. No declaration is silently dropped.

   **Now asserted directly (pass-11, closes pass-10 BLOCKER-2):** the totality invariant
   above was previously narrative-only — no test asserted it. T-048 is an 18-candidate
   property test that partitions declaration values across all three classes and asserts,
   for every candidate, `extract_hook_plugin_name(...).is_some()` ⟺
   `detect_ungated_declarations(...).is_empty()`, plus a negative-identifier check that the
   *other* class's identifier is absent. This closes the class of mutants that could flip
   which identifier fires (`UNGATED-DECLARATION` vs. `OUTSIDE-REPO-DECLARATION`) at a
   boundary without any single-case test catching it — with one documented exception: T-048's
   own `OUTSIDE-REPO-DECLARATION` candidates are all over-determined with respect to the
   containment predicate's length conjunct specifically (mutant M2, `len >` → `len >=`) —
   each candidate independently fails the prefix conjunct too, so relaxing `>` to `>=` alone
   does not flip any of T-048's 18 outcomes. **T-050** (added in the pass-11 fix-burst,
   closing F-1) is the dedicated length-conjunct isolation control: a root-resolving,
   filename-less declaration (`plugin = "../.."`) whose joined path equals `root_parts`
   exactly (not merely equal length), making the prefix conjunct a trivial self-match and the
   length conjunct the sole determinant. Empirically verified: under mutant M2, only T-050
   goes RED; T-047 and T-048 stay GREEN.

   **Both conjuncts of the two-conjunct `in_repo` predicate are now independently isolated
   (pass-12 fix-burst, closing F-1 against the pass-12 dispatch).** The containment
   predicate is `let in_repo = joined_parts.len() > root_parts.len() &&
   root_parts.iter().enumerate().all(|(i, p)| joined_parts.get(i) == Some(p));` — a
   two-conjunct AND. T-050 isolates the LENGTH conjunct (`>` → `>=`, mutant M2); it does
   NOT isolate the PREFIX conjunct (`.all(...)` → `.any(...)` or `.all(...)` → `true`) —
   every OUTSIDE-REPO-DECLARATION candidate available prior to pass-12 (T-047, T-048's
   OUTSIDE rows, T-050 itself) is over-determined against the prefix mutant: each either
   fails the length conjunct too (T-047, T-048) or trivially satisfies the prefix conjunct
   via exact self-match (T-050), so mutating `.all(...)` survived the full suite
   undetected — this was the claimed-total predicate's un-isolated-conjunct gap. **T-051**
   (`test_S_21_09_ac006_T051_prefix_conjunct_isolation_kills_all_mutants`, added in the
   pass-12 fix-burst, closing adversary finding F-1, HIGH, POLICY 13) is the dedicated
   prefix-conjunct isolation control — the orthogonal sibling of T-050. Fixture
   `plugin = "../../../sib/ghost.wasm"` resolves (three `ParentDir` hops from
   `plugins/vsdd-factory` clear both registry-path segments and pop one component off
   `root_parts`, then `sib`/`ghost.wasm` are pushed back on) to
   `root_parts[0..N-1] + ["sib", "ghost.wasm"]`: the length conjunct is TRUE (`N+1 >
   N`), but the prefix conjunct diverges from `root_parts` at the last index (`"sib"` ≠
   `root_parts[N-1]`) and is therefore FALSE — the prefix conjunct is the sole
   determinant of `in_repo` for this candidate. Empirically verified: under mutant
   `.all(...)` → `.any(...)` AND under mutant `.all(...)` → `true`, ONLY T-051 goes RED
   (observes `UNGATED-DECLARATION` instead of the asserted `OUTSIDE-REPO-DECLARATION`);
   T-047, T-048, and T-050 all stay GREEN under both mutants. Orthogonality vs T-050
   confirmed: under mutant M2 (`len >` → `len >=`) applied to T-051's candidate, the
   length conjunct (already true under live code) stays true and the prefix conjunct
   (already false under live code) stays false — no observable behaviour change — so
   T-051 stays GREEN under M2 while T-050 goes RED, and conversely T-050 stays GREEN
   under the `.all`-mutants while T-051 goes RED, confirming the two isolation controls
   are independent. With T-050 (length) and T-051 (prefix) both landed, the previously
   claimed-total `in_repo` predicate now has every conjunct independently isolated —
   closing the "claimed-total predicate with an un-isolated conjunct" gap identified by
   adversary pass-12 F-1.

   Both identifiers constitute a **hard failure** — `run_t012_gate` propagates the error and
   does not continue to the git steps. Fires before step 3 (before any git operations). Prior
   length-based threshold approach was replaced entirely (the threshold could be walked
   downward: `../ghost.wasm`, `../../ghost.wasm` both confirmed escaping). Verified: all
   production registry entries (75 `[[hooks]]` + 1 `[[resolvers]]` = 76 entries, 36 unique
   names) use `hook-plugins/<name>.wasm` — neither identifier fires.
   Boundary controls: T-038 (no-git fixture; `other-dir/evil-probe.wasm` →
   `UNGATED-DECLARATION`); T-040 (resolvers side; `UNGATED-DECLARATION`); T-043 (bare name
   → `UNGATED-DECLARATION`); T-044 (traversal-that-cancels → `UNGATED-DECLARATION`); T-045
   (`../ghost.wasm` → `plugins/ghost.wasm`, `UNGATED-DECLARATION`); T-046 (`../../ghost.wasm`
   → `<root>/ghost.wasm`, `UNGATED-DECLARATION` at tightest INSIDE margin, root+1); T-047
   (`../../../ghost.wasm` → above root, `OUTSIDE-REPO-DECLARATION` at tightest OUTSIDE
   margin, `len == root_parts.len()` exactly; both length and prefix conjuncts fail
   simultaneously — T-047 proves the outcome class at this boundary, not which conjunct
   is individually operative; it does not kill mutant M2, see T-050); T-048 (18-candidate
   totality-property partition covering all three classes in one parametrized test — see
   above; also does not kill M2, for the same over-determination reason); T-050 (dedicated
   length-conjunct isolation control — `plugin = "../.."` resolves to exactly `root_parts`,
   making the length conjunct alone determinative; kills mutant M2 `len >` → `len >=`, which
   T-047 and T-048 cannot isolate); T-051 (pass-12 fix-burst dedicated prefix-conjunct
   isolation control — `plugin = "../../../sib/ghost.wasm"` resolves to
   `root_parts[0..N-1] + ["sib", "ghost.wasm"]`: length conjunct TRUE, prefix conjunct
   FALSE at the last index, making the prefix conjunct alone determinative; kills mutants
   `.all(...)` → `.any(...)` and `.all(...)` → `true`, which none of T-047/T-048/T-050 can
   isolate; orthogonal to T-050 — T-051 does not kill M2 and T-050 does not kill the
   `.all`-mutants).
2d. **Production-validation gate (`Registry::parse_str`; pass-11, HIGH-3):** before parsing
   `hooks-registry.toml` for the declared set, `run_t012_gate` validates it against
   `factory_dispatcher::Registry::parse_str` — the SAME production parser the dispatcher
   uses to load the registry at runtime (`schema_version == 2` enforcement,
   `deny_unknown_fields`, tool-pattern regex validity). A registry that fails this
   validation is rejected by production in its entirety (all hooks inert); without this
   gate, T-012's declared-set check could pass against a registry production will never
   load, making the gate meaningless. `resolvers-registry.toml` is separately checked for
   `schema_version == 1` (production's requirement for that registry) via a direct TOML
   read — no `Registry::parse_str`-equivalent public function exists for the resolvers
   schema. Both validations panic with a `T-012:`-prefixed message on failure — hard
   failures, consistent with the other gate arms. **Both determinants are now each
   isolated by a dedicated firing control (pass-14 fix-burst, closes adversary pass-14
   MEDIUM):** prior to pass-14, every T-012..T-051 fixture wrote `schema_version = 2` in
   `hooks-registry.toml` and `schema_version = 1` in `resolvers-registry.toml` — both
   production-valid — so deleting either validation block changed no observable outcome
   for the existing suite; the gate was exercised only incidentally, never isolated.
   **T-052** (`test_S_21_09_ac006_T052_hooks_production_validation_isolation_kills_parse_str_deletion`)
   isolates the hooks-side determinant: a full, otherwise-production-valid git fixture (30
   hooks + 1 resolver, all 31 WASMs committed — matching the T-030 phase-B/T-050/T-051
   pattern) whose `hooks-registry.toml` declares `schema_version = 3` against
   `REGISTRY_SCHEMA_VERSION = 2`, with `resolvers-registry.toml` fully production-valid
   (`schema_version = 1`); `#[should_panic(expected = "T-012: hooks-registry.toml fails
   production validation")]`. Empirically verified: deleting the
   `Registry::parse_str(&hooks_content)` block causes T-052 to go RED (does not panic as
   expected) while all other 47 tests stay GREEN — every other gate (inventory,
   `detect_ungated_declarations`, both floors, declared ⊆ tracked) would otherwise pass
   against this fixture, isolating the hooks production-validation block as the sole
   determinant. **T-053**
   (`test_S_21_09_ac006_T053_resolvers_schema_version_isolation_kills_assert_deletion`)
   isolates the resolvers-side determinant: the same full production-valid fixture shape
   with `hooks-registry.toml` fully production-valid (`schema_version = 2`, so
   `Registry::parse_str` passes) and `resolvers-registry.toml` declaring `schema_version =
   2` against the production-required 1; `#[should_panic(expected = "T-012:
   resolvers-registry.toml schema_version=2 but production requires 1")]`. Empirically
   verified: deleting the resolvers `assert_eq!(resolvers_schema_version, 1, …)` block
   causes T-053 to go RED while all other 47 tests stay GREEN. The two fixtures put the
   defect on opposite registries so each test isolates its own determinant from the
   other's. T-049's fixture is the first to combine a real git repository with a
   syntactically valid, schema-conformant 30-entry registry that has no WASM content — it
   confirms the validation gate does not itself block a registry-valid-but-artifact-empty
   fixture before EC-005a's own guard is reached.
3. **`declared − tracked` via `git ls-files`:** compute `declared_set − tracked_set`
   where `tracked_set` is returned by `git_tracked_wasm_names()` (uses `git ls-files`;
   returns raw paths; `run_t012_gate` strips the `plugins/vsdd-factory/` prefix before
   diffing). Each path in the difference is emitted as `MISSING: <path>` (registry-relative,
   e.g., `MISSING: hook-plugins/foo.wasm`). T-015 tests this arm (one declared-but-untracked
   path); T-020 covers EC-005 (empty-tracked-set guard: `tracked.is_empty()` fires before the
   set-difference loop; panics with `"T-012 EC-005"`; `#[should_panic]` — no `MISSING:` emitted).
4. **`tracked − committed` via `git ls-tree --name-only HEAD`** (authorized operation,
   S-21.09): compute `tracked_set − committed_set` where `committed_set` is queried via
   `git ls-tree --name-only HEAD`. Each path in the difference is emitted as
   `STAGED-NOT-COMMITTED: <path>` (registry-relative). This query is intentionally stricter
   than relying solely on `git ls-files` (staging area includes uncommitted artifacts);
   `git_committed_wasm_names()` passes `-r` to `git ls-tree` for recursive traversal,
   matching `git ls-files` semantics. T-021 tests this arm; T-023 documents the
   boundary-polarity record — T-025 proves traversal-into forms ARE included; T-026 proves
   absolute forms are EXCLUDED (step (3) gates fire: depth-coincidental fixture rejected
   by minimum-length check; depth-matched fixture rejected by prefix assertion; both
   independently return `None`); T-023(b) adds the traversal-that-cancels exclusion proof.
5. Asserts both `declared_set − tracked_set` (step 3) and `tracked_set − committed_set`
   (step 4) are empty — no declared-but-missing artifacts, no staged-but-uncommitted
   artifacts
6. **Red Gate:** MUST FAIL before AC-001 fix because `validate-factory-path-staging.wasm`
   is declared in `hooks-registry.toml` but not committed in git
7. **Green Gate:** MUST PASS after AC-001 fix

Do NOT create a new crate `crates/plugin-parity-check/` — that would duplicate ~200 lines
of machinery already in `bundle_orphan_check.rs`. Extend the existing file.

**Note on `vsdd-context-resolvers.wasm`:** declared in `resolvers-registry.toml`; also
committed in git. Because `parse_plugin_refs()` is called on both registries and the
declared set is their UNION, `vsdd-context-resolvers.wasm` is in the declared set and
committed — no false-positive.

**Note on absolute-path declarations:** `registry_parent.join(absolute_path)` returns the
absolute path unchanged under Rust `Path::join`/POSIX semantics; `lex_norm`'s
`RootDir | Prefix(_)` arm is unreachable in practice (prior guards prevent reaching it), so
`lex_norm` produces Normal components with no absoluteness marker. An absolute path whose
leading Normal components equal the registry parent **is admitted** by the gate — this is
correct behaviour because production loads that same in-repo file. Only absolute paths with
a **divergent prefix** (i.e., components that do not match the registry parent) are excluded:
(a) minimum-length check — absolute paths that normalise to fewer components than
`expected_depth + 2` fail here before the prefix assertion is reached (T-026 short fixture);
(b) prefix assertion — absolute paths with sufficient component count but a divergent prefix
fail here (T-026 depth-matched fixture — counts tempdir Normal components at runtime to
derive `expected_depth`, builds path with exactly that many Normal segments before
`hook-plugins`, clearing the length check; captured proof: deleting the prefix loop yields
`got refs_depth: {"hook-plugins/evil.wasm"}` and T-026 fails).
In-repo absolute paths are not excluded and gating a divergent-prefix absolute against the
git-tracked set would produce a false positive, consistent with production behavior
(`resolve_plugin_paths_is_idempotent_for_absolute_paths` in `registry.rs`).

- **Tests:** `test_S_21_09_ac006_T012_declared_set_subset_of_tracked_set()` (now calls
  extracted `run_t012_gate(&workspace_root())`) plus T-013..T-056 in
  `crates/factory-dispatcher/tests/bundle_orphan_check.rs`, plus 1 registry.rs unit test
  (`on_error_falls_back_to_registry_defaults_when_entry_omits_it`, out-of-gate). T-013/T-014: BLOCKER-1 parse
  controls (nospace and dotslash forms; T-014 dotslash handled by `CurDir` no-op in
  lex_norm, not `trim_start_matches`). Both T-013 and T-014 additionally assert that their
  fixture's TOML **body** — not merely a comment — carries the distinguishing syntax: each
  test filters out lines starting with `#` (`.lines().filter(|l|
  !l.trim_start().starts_with('#'))`) before asserting the live `[[hooks]]` entry contains
  the nospace/dotslash form; this closes a control-integrity gap where a stale live entry
  using standard form could still pass if the fixture's header *comment* merely documented
  the nospace/dotslash syntax. T-015: declared-but-untracked arm (`MISSING:
  hook-plugins/<name>` emitted, path-valued). T-016: PASS arm and hooks floor boundary
  HIGH end (30 entries passes). T-017: inventory — hyphen `metrics-registry.toml` →
  `UNEXPECTED:` (pass-20 format-lock burst `fc0e613b`: assertion tightened to the
  two-space-indented `"  UNEXPECTED: metrics-registry.toml"` form, matching
  `check_registry_inventory`'s real `"  UNEXPECTED: {}"` format string — an unindented
  needle would also match inside the indented production line via `.contains()` and
  could never catch an indent-dropping mutation). T-018: inventory — both registries
  absent → two `MISSING:` identifiers (pass-20 `fc0e613b`: both the hooks-registry.toml
  and resolvers-registry.toml arms tightened to the two-space-indented
  `"  MISSING: {}"` form, matching `check_registry_inventory`'s real format string).
  T-019: hooks floor fires on 1-entry set. T-020: EC-005 tracked-set-empty arm.
  T-021: staged-not-committed arm (`STAGED-NOT-COMMITTED: hook-plugins/<name>` emitted,
  path-valued). T-022: resolvers floor (`is_empty()` arm). T-023: boundary-polarity
  record — traversal-into (T-025) and case-variant (T-031) are in the INCLUDED region;
  bare names, traversal-that-cancels (T-023(b)), and `../`-prefix form are in the EXCLUDED
  region (all three excluded by minimum-length check — normalise to `expected_depth + 1`);
  absolute paths with divergent prefix (T-026) excluded by step-(3) gates; LOW-1
  false-negative class CLOSED — gate-1-failure forms (bare names → T-043,
  traversal-that-cancels → T-023(b)/T-044, `../`-prefix → T-023(c)/T-045) are excluded from
  `declared_set` by minimum-length check AND now caught by `detect_ungated_declarations` as
  `UNGATED-DECLARATION` (containment predicate confirms inside worktree root); `../../ghost.wasm`
  form (T-046) similarly UNGATED at tightest INSIDE margin; gate-3-failure paths (correct
  prefix and depth, non-`hook-plugins` component) cause hard-failure via
  `detect_ungated_declarations` (T-038, T-040). T-023(c)
  `../hook-plugins/evil.wasm` now executed: gate 1 fires (`expected_depth + 1 <
  expected_depth + 2`); EXCLUDED. T-024: underscore-registry escape proof
  (`metrics_registry.toml` with `*.toml` glob fires inventory assertion; pass-20
  format-lock burst `fc0e613b`: assertion tightened to the two-space-indented
  `"  UNEXPECTED: metrics_registry.toml"` form, matching `check_registry_inventory`'s
  real `"  UNEXPECTED: {}"` format string). T-025: traversal
  form (`hooks/../hook-plugins/ghost-traversal.wasm`) normalises to declared set as
  registry-relative path `hook-plugins/ghost-traversal.wasm`. T-026: two absolute-form
  sub-fixtures proving two distinct gates — (a) short fixture rejected by minimum-length
  check; (b) depth-matched fixture (runtime `expected_depth` derivation; captured proof:
  deleting prefix loop yields `got refs_depth: {"hook-plugins/evil.wasm"}`) rejected by
  prefix assertion; both independently return `None`. **Windows CI portability fix
  (`c20cf2fe`):** sub-fixture (b)'s path was rebuilt from `PathBuf::push` + `.to_str()`
  (OS-native separator, invalid TOML escape on Windows) to explicit forward-slash string
  concatenation — test-fixture construction only; runtime classification (rejection via
  the prefix assertion) unchanged on every platform. T-027: hooks floor boundary LOW end
  (29 entries fires `"...has only 29 entries"`). T-028: inventory narrowing — non-recursive
  (subdirectory TOMLs e.g.
  `tests/fixtures/validate-policies-schema/pass-valid-lint-hook/hooks-registry.toml` are
  invisible; nine subdirectory .toml files exist but none are loaded —
  `resolve_registry_path()` in `main.rs` resolves registries at `${CLAUDE_PLUGIN_ROOT}`
  top level only). T-029: inventory narrowing — case-sensitive (`metrics-registry.TOML`
  invisible on CI Linux; safe per exact-name loading in `resolve_registry_path()`).
  T-030: T-012 wiring control, two phases — (1) inventory: tempdir `{hooks-registry.toml,
  resolvers-registry.toml, taplo.toml}` asserts `UNEXPECTED: taplo.toml` (pass-20
  format-lock burst `fc0e613b`: phase A assertion tightened to the two-space-indented
  `"  UNEXPECTED: taplo.toml"` form, matching `check_registry_inventory`'s real
  `"  UNEXPECTED: {}"` format string — distinguishing the inventory outcome from the
  floor-error outcome by exact production format, not merely by identifier substring);
  (2) declared-
  subset: tempdir with registries and tracked WASM confirms full gate wiring; phase B
  invokes git via `.output()` (stdout captured for assertion) (pass-21 completion sweep
  `1c93f499`: phase B's positive assertion tightened from the unindented substring
  `"MISSING: hook-plugins/ctx.wasm"` to the two-space-indented `"  MISSING:
  hook-plugins/ctx.wasm"` form, matching `check_declared_subset_tracked`'s real
  `"  MISSING: {}"` format string — closes the sixth remaining sibling gap under
  F-S2109-LOCAL-P?-001, LOW). T-031: case-variant
  `Hook-Plugins/ghost-case.wasm` → INCLUDED via `eq_ignore_ascii_case` match; step (4)
  returns verbatim path `Hook-Plugins/ghost-case.wasm` (no lowercasing); `refs.contains
  ("Hook-Plugins/ghost-case.wasm")` asserts verbatim form present; mutation direction 1:
  reverting gate (c) to strict byte equality returns `None` → refs empty; mutation
  direction 2: re-introducing lowercasing returns lowercase form → verbatim assertion fails
  (T-042 proves downstream: verbatim path appears in `MISSING:` on case-sensitive Linux).
  T-032: nested subdirectory `hook-plugins/sub/nested.wasm` → returns registry-relative
  path `hook-plugins/sub/nested.wasm` (not last component `nested.wasm`); mutation:
  reverting to last-component extraction returns `nested.wasm` → declared set contains
  `nested.wasm` while git returns `hook-plugins/sub/nested.wasm` → `MISSING: nested.wasm`
  (wrong path). T-033: minimum-length lower boundary — `plugin = "hook-plugins"` (directory,
  no filename) gives `len == expected_depth + 1`, caught by `< expected_depth + 2`; captured
  proof: weakening constant to `< expected_depth + 1` yields `got refs: {"hook-plugins"}`.
  T-034: `git ls-tree -r` recursive flag — nested committed `hook-plugins/sub/nested.wasm`
  returned as `["hook-plugins/sub/nested.wasm"]` (path-valued); captured proof: dropping
  `-r` yields `got committed: []`. T-035: gate-3 isolated control — `plugin =
  "other-dir/evil-probe.wasm"` in synthetic tempdir registry; gates 1+2 pass
  (`expected_depth + 2` components, prefix intact), gate 3 fires (`"other-dir"` ≠
  `"hook-plugins"`); captured proof: removing gate 3 alone admits `other-dir/evil-probe.wasm`
  → `got refs: {"other-dir/evil-probe.wasm"}`; completes three-gate enumeration (gate 1 ↔
  T-033, gate 2 ↔ T-026(b), gate 3 ↔ T-035). T-036: gitignored-but-present — exercises
  the S-21.09 motivating condition; git fixture with `.gitignore` covering `hook-plugins/`
  (load-bearing: `git check-ignore -q` assertion present); 30 hooks + resolver
  force-committed via `git add -f`; `config.yaml` filter control confirms `*.wasm` files
  are excluded from normal staging; `gitignored-probe.wasm` never staged; gate emits
  `MISSING: hook-plugins/gitignored-probe.wasm` (path-valued); M17 mutation proof:
  replacing `git_tracked_wasm_names()` with `fs::read_dir` makes probe appear in tracked
  set → gate returns Ok (false negative) (pass-21 completion sweep `1c93f499`: positive
  assertion tightened from the unindented substring to the two-space-indented `"  MISSING:
  hook-plugins/gitignored-probe.wasm"` form — closes a remaining sibling gap under
  F-S2109-LOCAL-P?-001, LOW). T-037: staged-but-not-committed — commits 30
  hooks + resolver; `git add -f staged-probe.wasm` without committing; gate emits
  `STAGED-NOT-COMMITTED: hook-plugins/staged-probe.wasm` (path-valued); M18 mutation:
  swapping `git_committed_wasm_names()` for `git ls-files` makes committed==tracked → gate
  returns Ok (false negative); M16 mutation: swapping tracked/committed args emits
  `MISSING:` instead of `STAGED-NOT-COMMITTED:`; both function identities are load-bearing
  (pass-21 completion sweep `1c93f499`: positive assertion tightened from the unindented
  substring to the two-space-indented `"  STAGED-NOT-COMMITTED:
  hook-plugins/staged-probe.wasm"` form — closes a remaining sibling gap under
  F-S2109-LOCAL-P?-001, LOW).
  T-038: `detect_ungated_declarations` pure-core control — no-git fixture;
  `other-dir/evil-probe.wasm` passes gates 1+2 but fails gate 3 → helper emits
  `UNGATED-DECLARATION: other-dir/evil-probe.wasm`; fires before git calls in
  `run_t012_gate` (pass-20 format-lock burst `fc0e613b`: assertion tightened to the
  two-space-indented `"  UNGATED-DECLARATION: other-dir/evil-probe.wasm"` form,
  matching `run_t012_gate`'s real `format!("  {}", p)` wrapping around each ungated
  finding — an unindented needle would also match inside the indented production line
  via `.contains()` and could never catch an indent-dropping mutation; the
  negative-identifier assertion pattern in T-043/T-047/T-050/T-051 was left unchanged
  by this burst). T-039: EC-008 closed defect control (Effectful-shell) — 30 hooks declared at
  `hook-plugins/sub/hNN.wasm` committed flat at `hook-plugins/hNN.wasm`; path-based diff
  catches mismatch; asserts `Err` containing `MISSING: hook-plugins/sub/h00.wasm`
  (pass-21 completion sweep `1c93f499`: positive assertion tightened from the unindented
  substring to the two-space-indented `"  MISSING: hook-plugins/sub/h00.wasm"` form —
  closes a remaining sibling gap under F-S2109-LOCAL-P?-001, LOW).
  T-040: resolvers-side UNGATED-DECLARATION control (Pure-core) — synthetic resolver entry
  passes gates 1+2, fails gate 3; `detect_ungated_declarations` emits
  `UNGATED-DECLARATION: <path>` (pass-21 completion sweep `1c93f499`: positive assertion
  tightened from the unindented substring to the two-space-indented `"  UNGATED-DECLARATION:
  other-dir/evil-resolver.wasm"` form — closes a remaining sibling gap under
  F-S2109-LOCAL-P?-001, LOW). T-041: `#[should_panic(expected = "git ls-files exited
  with status")]` (Effectful-shell) — invokes `run_t012_gate` on a non-git tempdir; panics
  at git ls-files step; confirms error propagation. T-042: case-variant end-to-end
  (Pure-core) — declares `Hook-Plugins/ghost-missing.wasm`; git tree has lowercase
  `hook-plugins/ghost-missing.wasm`; path-based diff: verbatim declared path ≠ lowercase
  tracked path → `MISSING: Hook-Plugins/ghost-missing.wasm`; mutation: re-introducing
  lowercasing makes declared path match tracked path → `is_err()` assertion fails; proves
  case-variant gate failure by design (pass-21 completion sweep `1c93f499`: positive
  assertion tightened from the unindented substring to the two-space-indented `"  MISSING:
  Hook-Plugins/ghost-missing.wasm"` form — closes the final remaining sibling gap under
  F-S2109-LOCAL-P?-001, LOW, completing the format-lock sweep).
  T-043: bare-name UNGATED control (Pure-core) — `ghost-bare.wasm` joined against registry
  parent resolves inside worktree root but outside `hook-plugins/`; containment predicate
  confirms; `detect_ungated_declarations` emits `UNGATED-DECLARATION`. T-044:
  traversal-that-cancels UNGATED control (Pure-core) — cancels to inside-repo
  non-`hook-plugins` path; `UNGATED-DECLARATION` emitted. T-045: `../ghost.wasm` →
  `plugins/ghost.wasm`, UNGATED (Pure-core) — inside worktree root, outside `hook-plugins/`;
  `UNGATED-DECLARATION`. T-046: `../../ghost.wasm` → `<root>/ghost.wasm`, UNGATED at tightest
  INSIDE margin (Pure-core) — `joined_parts.len() == root_parts.len() + 1` → inside repo;
  `UNGATED-DECLARATION`. T-047: `../../../ghost.wasm` → above root, OUTSIDE-REPO-DECLARATION
  at tightest OUTSIDE margin (Pure-core) — `len == root_parts.len()` → containment predicate
  fails → `OUTSIDE-REPO-DECLARATION` (T-047 proves the outcome class at root depth; both
  length and prefix conjuncts fail simultaneously — no conjunct isolation).
  **Pass-20 format-lock burst (`fc0e613b`), sibling sweep across T-043/T-044/T-045/T-046/
  T-047:** each of these five positive-identifier assertions was tightened to the
  two-space-indented form (`"  UNGATED-DECLARATION: ghost-bare.wasm"`,
  `"  UNGATED-DECLARATION: ../vsdd-factory/ghost-updir.wasm"`,
  `"  UNGATED-DECLARATION: ../ghost.wasm"`, `"  UNGATED-DECLARATION: ../../ghost.wasm"`,
  `"  OUTSIDE-REPO-DECLARATION: ../../../ghost.wasm"` respectively), matching
  `run_t012_gate`'s real `format!("  {}", p)` wrapping around each ungated/outside
  finding — an unindented needle would also match inside the indented production line
  via `.contains()` and could never catch an indent-dropping mutation; each was
  empirically verified RED under an indent-drop mutant. The pre-existing
  negative-identifier assertions on these five tests (e.g. T-043/T-047's
  `!err.contains("OUTSIDE-REPO-DECLARATION: ")` / `!err.contains("UNGATED-DECLARATION: ")`
  sibling checks) were preserved intact — this burst tightened only the positive match.
  T-048: pass-11 totality-property partition (Pure-core) — an 18-candidate table (5 `gated`,
  9 `UNGATED-DECLARATION`, 4 `OUTSIDE-REPO-DECLARATION`) drives `detect_ungated_declarations`
  — the function `run_t012_gate` actually calls — per candidate and asserts a single
  exact-vec-equality check: `gated` candidates assert `detected.is_empty()`; non-`gated`
  candidates assert `detected == vec![format!("{}: {}", ident, path)]` (PR-review fix,
  mutation-hardening burst `c9cccea9`: a prior version additionally called
  `extract_hook_plugin_name` directly and paired `extract.is_some()`/`is_none()` with the
  `detect_ungated_declarations`-based check — tautological, since `detect_ungated_declarations`
  decides its own emission by calling `extract_hook_plugin_name(...).is_none()` internally;
  the single exact-vec-equality assertion now subsumes the former separate
  "other identifier absent" check via exact-set equality); closes pass-10 BLOCKER-2 (totality
  invariant previously asserted by no test) and independently re-confirms pass-10 BLOCKER-1's
  closure (a mutation to the single shared gate logic now fails the exact-match assertion on
  the affected row).
  T-048 kills mutant M4 (`>=+2→+1` — caught on the `"hook-plugins"` UNGATED-DECLARATION row's
  exact-match assertion; **corrected attribution, PR-review fix:** a prior version of this
  narrative mistakenly credited a GATED-branch assertion with catching M4 via the
  `"hook-plugins"` case, but `"hook-plugins"` is classified UNGATED-DECLARATION in the table,
  never exercised by the GATED branch) and the M1+M4 composite; it does NOT kill mutant M2
  (`len >` → `len >=`) — its OUTSIDE-REPO-DECLARATION candidates are all over-determined
  (they fail the length conjunct AND the prefix conjunct simultaneously), so relaxing `>`
  to `>=` alone does not flip any of the 18 outcomes. See T-050 for the dedicated M2
  isolation control.
  T-049: pass-11 EC-005a control (Effectful-shell) — git fixture with a syntactically valid,
  `Registry::parse_str`-conformant 30-entry `hooks-registry.toml` whose `hook-plugins/`
  directory contains only a non-WASM `config.yaml` (no WASM files committed); `git ls-files`
  filtered for `.wasm` returns an empty set; `run_t012_gate` panics with
  `#[should_panic(expected = "T-012 EC-005a")]` at the `tracked_raw.is_empty()` guard — pins
  EC-005a (the raw-git-output guard in `run_t012_gate`, fired before the
  `REGISTRY_PARENT_PREFIX` strip) as distinct from EC-005b (T-020's fixture-empty guard
  inside `check_declared_subset_tracked`).
  T-050: pass-11 fix-burst length-conjunct isolation control (Pure-core) — closes F-1
  (adversary pass-11 dispatch, HIGH). `plugin = "../.."` resolves to exactly `root_parts`
  (an exact prefix self-match, not merely equal length), isolating the containment
  predicate's length conjunct as the sole determinant. Empirically verified: under mutant
  M2 applied locally, only T-050 goes RED while T-047 and T-048 stay GREEN — confirming
  T-050, not T-047/T-048, is the true M2 kill. No git operations. See the T-050 Red Gate
  Test Plan row for the full mechanism. **Pass-20 format-lock burst (`fc0e613b`):**
  T-050's positive assertion tightened to the two-space-indented
  `"  OUTSIDE-REPO-DECLARATION: ../.."` form, matching `run_t012_gate`'s real
  `format!("  {}", p)` wrapping — empirically verified RED under an indent-drop mutant.
  T-051: pass-12 fix-burst prefix-conjunct isolation control (Pure-core) — closes F-1
  (adversary pass-12 dispatch, HIGH, POLICY 13 mutation-completeness), the orthogonal
  sibling of T-050. `plugin = "../../../sib/ghost.wasm"` resolves to
  `root_parts[0..N-1] + ["sib", "ghost.wasm"]`: length conjunct TRUE (`N+1 > N`), prefix
  conjunct FALSE (diverges from `root_parts` at the last index) — the sole determinant
  of `in_repo` for this candidate. Empirically verified: under mutant `.all(...)` →
  `.any(...)` AND under mutant `.all(...)` → `true`, only T-051 goes RED (observes
  `UNGATED-DECLARATION` instead of the asserted `OUTSIDE-REPO-DECLARATION`); T-047,
  T-048, and T-050 stay GREEN under both mutants. Orthogonality vs T-050 confirmed:
  T-051 stays GREEN under mutant M2 (which T-050 kills); T-050 stays GREEN under the
  `.all`-mutants (which T-051 kills). No git operations. See the T-051 Red Gate Test
  Plan row for the full mechanism. **Pass-20 format-lock burst (`fc0e613b`):** T-051's
  positive assertion tightened to the two-space-indented
  `"  OUTSIDE-REPO-DECLARATION: ../../../sib/ghost.wasm"` form, matching
  `run_t012_gate`'s real `format!("  {}", p)` wrapping — empirically verified RED under
  an indent-drop mutant.
  T-052 (pass-14 fix-burst, Effectful-shell): hooks production-validation isolation
  control — full production-valid git fixture (30 hooks + 1 resolver, all WASMs
  committed) with `hooks-registry.toml` declaring `schema_version = 3` against
  `REGISTRY_SCHEMA_VERSION = 2`; kills deletion of the
  `Registry::parse_str(&hooks_content)` block in `run_t012_gate`;
  `#[should_panic(expected = "T-012: hooks-registry.toml fails production validation")]`.
  Empirically verified: deleting the block sends T-052 RED while all other 47 tests stay
  GREEN. See the T-052 Red Gate Test Plan row for the full mechanism.
  T-053 (pass-14 fix-burst, Effectful-shell): resolvers schema-version isolation
  control — same full production-valid fixture shape with `resolvers-registry.toml`
  declaring `schema_version = 2` against the production-required 1 (hooks side kept
  fully valid to isolate this determinant from T-052's); kills deletion of the
  `assert_eq!(resolvers_schema_version, 1, …)` block in `run_t012_gate`;
  `#[should_panic(expected = "T-012: resolvers-registry.toml schema_version=2 but
  production requires 1")]`. Empirically verified: deleting the block sends T-053 RED
  while all other 47 tests stay GREEN. See the T-053 Red Gate Test Plan row for the full
  mechanism.
  T-054 (mutation-audit hardening, Effectful-shell): isolates
  `resolvers_schema_version`'s `.unwrap_or(-1)` fail-closed sentinel — same full
  production-valid git fixture shape as T-052/T-053 but `resolvers-registry.toml` OMITS
  `schema_version` entirely (absent-key path, distinct from T-053's explicit-wrong-integer
  fixture); `#[should_panic(expected = "schema_version=-1 but production requires 1")]`
  (**PR-review fix, `c9cccea9`:** the expected-string was tightened from the looser
  `"but production requires 1"` to pin the `-1` fail-closed sentinel value itself, so the
  test cannot pass against a different, non-`1` fail-closed value). Under live code
  `.unwrap_or(-1)` fires (fail-closed) → `assert_eq!(-1, 1, …)` panics; under an
  `.unwrap_or(-1)` → `.unwrap_or(1)` mutant the absent key silently coerces to the
  production-required value and every subsequent gate passes — `run_t012_gate` returns
  `Ok(())` and T-054 goes RED, proving the `-1` sentinel itself (not just the `assert_eq!`
  block T-053 isolates) is load-bearing.
  T-055 (mutation-audit hardening, Pure-core): direct unit test of
  `detect_ungated_declarations`'s fail-open `content.parse::<toml::Value>()`
  `Err(_) => return Vec::new()` arm — unreachable via `run_t012_gate` because upstream
  `Registry::parse_str` (hooks) and the generic `toml::Value` parse (resolvers) both panic
  first on malformed TOML. Calls `detect_ungated_declarations` directly with an
  unterminated `[[hooks` array-of-tables header; asserts `result.is_empty()`. A fail-open
  mutant returning a spurious non-empty finding from the `Err(_)` arm fails this assertion.
  **PR-review fix, `c9cccea9`:** the malformed-TOML assertion alone is vacuous against a
  coarser mutant — a whole-function `detect_ungated_declarations` body replaced with
  `Vec::new()` (globally inert) would ALSO pass it, since malformed input is empty BY
  CONSTRUCTION under both the live fail-open arm and an inert function. T-055 now adds a
  positive control in the same test: calls `detect_ungated_declarations` with VALID TOML
  containing a genuine ungated declaration (`ghost-bare.wasm`) and asserts the result is
  non-empty; a whole-function `Vec::new()` mutant fails this second assertion, closing the
  gap the malformed-TOML case alone could not.
  T-056 (mutation-audit hardening, Pure-core): direct unit test pinning `lex_norm`'s
  `Component::CurDir => {}` arm contract — dead in every gate call path (all call sites
  join absolute worktree roots; `Path::components()` does not synthesize `CurDir` for
  registry-relative fixture paths built by this suite). Asserts
  `lex_norm(Path::new("./a/b")) == ["a", "b"]`; a mutant pushing `"."` onto `parts` for the
  CurDir arm yields `[".", "a", "b"]` instead.
  51-test suite T-006..T-056; 45 S-21.09-owned tests T-012..T-056; plus 1 registry.rs unit
  test (`on_error_falls_back_to_registry_defaults_when_entry_omits_it`, out-of-gate
  production accessor, not part of the T-NNN gate sequence).
  SHA `c20cf2fe`.
  The registry-inventory assertion (step 1) ensures the test self-invalidates if a new
  registry file is added without updating the declared-set parsing.

#### Mutation-Completeness Audit (mutation-hardening burst, commit `c20cf2fe`)

An exhaustive mutation audit of the S-21.09 declared-set gate (`run_t012_gate`,
`detect_ungated_declarations`, `lex_norm`) plus `cargo-mutants` against the production
`crates/factory-dispatcher/src/registry.rs` module was conducted per POLICY 13
(mutation-completeness). The audit enumerated **68 determinant mutations** across every
mutation class applicable to the gate's logic — comparison/boundary operators, boolean
structure (`&&`/`||`/`.all`/`.any`), boundary constants (`+1`/`+2`, floor thresholds),
early-return/panic arms, classification-literal swaps (`MISSING:` /
`STAGED-NOT-COMMITTED:` / `UNGATED-DECLARATION:` / `OUTSIDE-REPO-DECLARATION:`), and
collection/string operations (`.clear()`, `.push()`, `eq_ignore_ascii_case`,
`unwrap_or`). **64 of 68 were KILLED** by the pre-existing T-006..T-053 suite. **5
survived**, of which **4 were killable** and are now closed by this burst, and **1 is
provably un-isolatable** and is recorded as an accepted-residual. After this burst, the
suite has **zero killable surviving mutants** across the audited scope.

| Survivor | Class | Locus | Disposition | Control |
|----------|-------|-------|-------------|---------|
| SURV-01 | Structural (dead-arm no-op) | `lex_norm`'s `RootDir \| Prefix(_) => parts.clear()` arm | **Accepted-residual** — provably un-isolatable | None (strengthened doc comment only) |
| SURV-02 | Structural (dead-arm contract) | `lex_norm`'s `Component::CurDir => {}` arm | Closed | T-056 |
| SURV-03 | Structural (fail-open dead arm) | `detect_ungated_declarations`'s `Err(_) => Vec::new()` on malformed TOML | Closed | T-055 |
| SURV-04 | **Correctness (fail-closed sentinel)** | `run_t012_gate`'s `resolvers_schema_version` `.unwrap_or(-1)` | Closed | T-054 |
| SURV-05 | Structural (out-of-gate production accessor) | `RegistryEntry::on_error` defaults-fallback (`registry.rs`) | Closed | `registry::tests::on_error_falls_back_to_registry_defaults_when_entry_omits_it` |

**SURV-04 is the one genuine correctness gap the audit found.** `resolvers_schema_version`
reads `resolvers_doc.get("schema_version").and_then(toml::Value::as_integer).unwrap_or(-1)`
before the `assert_eq!(resolvers_schema_version, 1, …)` check T-053 already isolates. T-053's
fixture always wrote an explicit (wrong) integer `schema_version`, so it isolates the
`assert_eq!` block's deletion but never exercised the `.unwrap_or(-1)` sentinel itself: a
resolvers-registry.toml with an **absent** `schema_version` key would, under an
`.unwrap_or(-1)` → `.unwrap_or(1)` mutant, silently coerce to the production-required value
and pass every gate undetected — a real resolvers registry missing its `schema_version` key
would otherwise be admitted rather than rejected. T-054 closes this with a dedicated
absent-key fixture (distinct from T-053's explicit-wrong-integer fixture), asserting
`#[should_panic(expected = "schema_version=-1 but production requires 1")]` (PR-review fix,
`c9cccea9`: tightened from the looser `"but production requires 1"` to pin the `-1`
fail-closed sentinel value itself).

**SURV-02 and SURV-03 are structural (dead-code-path) controls**, not correctness gaps —
both arms are unreachable through any `run_t012_gate` fixture (upstream panics fire first),
so T-055 and T-056 call the target functions directly to pin their contracts. T-055's
malformed-TOML assertion alone was vacuous against a whole-function `Vec::new()`
(globally-inert) mutant, since malformed input is empty by construction under both the
live fail-open arm and an inert function; the PR-review fix burst (`c9cccea9`) added a
positive control in the same test — valid TOML with a genuine ungated declaration, asserted
non-empty — closing that gap.

**SURV-05 lives outside the T-NNN gate sequence entirely** — `RegistryEntry::on_error` is a
production accessor used by the dispatcher at hook-invocation time, not by the S-21.09
declared-set gate. `cargo-mutants` against `registry.rs` found the `Default::default()`
body-replacement mutant survived because every prior fixture's expected `on_error` value
happened to coincide with `OnError`'s own `#[default]` variant (`Continue`). The new
`on_error_falls_back_to_registry_defaults_when_entry_omits_it` unit test (in
`crates/factory-dispatcher/src/registry.rs`'s `#[cfg(test)]` module) pairs an omitted
entry-level `on_error` with a `RegistryDefaults` explicitly set to the NON-default variant
(`OnError::Block`), so the live accessor and the mutant diverge observably.

**SURV-01 is a provably un-isolatable accepted-residual, not a deferral.** `lex_norm`'s
`RootDir | Component::Prefix(_) => parts.clear()` arm is the fourth defensive-unreachable
arm: on any path, a root/prefix component is always the FIRST component `Path::components()`
yields, before any `Normal` component has pushed onto `parts` — so `parts` is provably empty
at every call site that reaches this arm, for every possible input. No fixture, synthetic or
gate-integration, can construct a path where a `Normal` component precedes a `RootDir` or
`Prefix` component (this is a `std::path` invariant, not a gate-specific one), so mutating
`parts.clear()` to a no-op or to a spurious push is behaviorally unobservable under every
possible input, not merely every input tried. This is recorded via a strengthened doc
comment directly above the `lex_norm` function definition in
`crates/factory-dispatcher/tests/bundle_orphan_check.rs` (the `"**Mutation-audit
accepted-residual (SURV-01):**"` paragraph) rather than via a test — a test asserting
non-behavior would itself be vacuous.

Each of the four killable controls (T-054, T-055, T-056, and the registry.rs `on_error`
test) was empirically verified by applying its corresponding mutant to a scratch copy,
confirming the new test alone went RED with every other test GREEN, then reverting the
mutant. Full catalog persisted separately as `mutation-audit-s21.09`
(`.factory/cycles/v1.0-brownfield-backfill/`).

**Pass-20 sibling-sweep gap (F-S2109-LOCAL-FMTLOCK-01) — EXTENDS the audited scope.**
The mutation-completeness audit above, and the PR-review fix burst (`c9cccea9`) that
preceded it, both scoped their assertion-format review to `run_t012_gate`,
`detect_ungated_declarations`, and `lex_norm` — the audit's own opening sentence names
exactly these three functions plus `registry.rs`. `check_registry_inventory`'s output
format was never in scope for either pass, and its leading-indent behavior was
consequently never audited — this is stated honestly rather than retroactively folded
into the 68-mutation count above, which remains an accurate record of what was actually
enumerated at the time. Separately, T-021's `c9cccea9`-era `check_declared_subset_tracked`
indent-lock fix (the `"  MISSING: {}"` / `"  STAGED-NOT-COMMITTED: {}"` arms) was never
generalized to its siblings even though all five format strings share the same
two-space-indent shape and the same `.contains()`-on-an-unindented-needle vulnerability.
Adversary pass-20 (LOCAL) identified this as a live gap: an indent-dropping mutation on
`check_registry_inventory`'s `"  UNEXPECTED: {}"` / `"  MISSING: {}"` format strings, and
on `run_t012_gate`'s ungated-findings `format!("  {}", p)` wrapper, would have survived
the full T-006..T-056 suite undetected, because T-017/T-018/T-024/T-030(phase A) and
T-038/T-043/T-044/T-045/T-046/T-047/T-050/T-051 all asserted only the unindented
substring form. This burst (`fc0e613b`) closes the gap by locking the two-space indent
across all five indented production format strings — the two `check_registry_inventory`
emitters (`"  UNEXPECTED: {}"`, `"  MISSING: {}"`), the two pre-existing
`check_declared_subset_tracked` emitters (unchanged, already locked at `c9cccea9`), and
the `run_t012_gate` ungated/outside wrapper (`format!("  {}", p)`) — each verified RED
under an indent-drop mutant applied locally, then reverted. No production code changed;
all 12 affected tests are positive-assertion-only tightenings; every pre-existing
negative-identifier assertion (T-043/T-047/T-050/T-051's `!err.contains(...)` sibling
checks) was left intact. Suite remains 51 tests T-006..T-056 (45 S-21.09-owned); no test
IDs added, removed, or renamed.

**Pass-21 completion sweep (`1c93f499`, F-S2109-LOCAL-P?-001, LOW) — CLOSES the
format-lock sweep.** The pass-20 burst above tightened 12 positive assertions but, despite
its own §Scope-honesty and gap-closure framing, did not sweep all sibling call sites of the
two `check_declared_subset_tracked` emitters it had already locked at `c9cccea9` (T-015,
T-021): six further tests still asserted the unindented substring form against those same
`"  MISSING: {}"` / `"  STAGED-NOT-COMMITTED: {}"` / `run_t012_gate`'s ungated wrapper
production strings — T-030 phase B (the declared-subset wiring-control half; phase A was
locked at pass-20), T-036, T-037, T-039, T-040, and T-042 — leaving an indent-dropping
mutation on any of those six call sites able to survive the full T-006..T-056 suite
undetected. This burst tightens all six from unindented substrings to the exact
two-space-indented production form, each empirically verified RED under an indent-drop
mutant applied locally to its respective production format string, then reverted; the
pre-existing negative-identifier assertions on T-043/T-047/T-050/T-051 remain untouched
(these six tests carry no negative-identifier siblings). A mandatory completeness grep —
`.contains("(MISSING|STAGED-NOT-COMMITTED|UNGATED-DECLARATION|OUTSIDE-REPO-DECLARATION|
UNEXPECTED)` needles not prefixed by two spaces, excluding `!err.contains(...)`
negative-identifier assertions and comments — run against the post-fix test file confirms
**zero remaining unindented positive needles** against any indented production emitter:
the format-lock sweep across all five indented production format strings is now
COMPLETE. No production code changed; the suite remains 51 tests T-006..T-056 (45
S-21.09-owned); no test IDs added, removed, or renamed; net file size unchanged at 5,554
lines (12 insertions / 12 deletions — two lines per tightened test: the `.contains(...)`
needle and its accompanying panic-message literal).

### AC-007: Pre-fix audit confirms validate-factory-path-staging.wasm is the only gap (traces to BC-4.16.001 Invariant 1)

An exhaustive set-difference audit across both registries (conducted as part of this story's
implementation) MUST confirm that `validate-factory-path-staging.wasm` is the only
declared-but-missing artifact.

**Pre-story audit result (verified 2026-08-10):**
- Declared in `hooks-registry.toml` (unique): **35 plugins**
  (Note: a `hook-plugins/` path-token grep yields 36 tokens because the prose comment
  `# Most entries route through hook-plugins/legacy-bash-adapter.wasm,` terminates with
  a comma, making `legacy-bash-adapter.wasm,` a distinct comma-suffixed token from
  `legacy-bash-adapter.wasm`; a `plugin = ` key-value grep yields 75 hits — 35 unique
  names each appearing in multiple TOML sections; exclude the comma-terminated spurious
  token to get 35 unique declared names)
- Declared in `resolvers-registry.toml`: 1 plugin (`vsdd-context-resolvers.wasm`)
- Total declared across both registries: **36 unique plugin artifacts** (no overlap)
- Tracked in git (`git ls-files plugins/vsdd-factory/hook-plugins/ | grep \.wasm$`): **35 files**
- Declared-but-missing: `{validate-factory-path-staging.wasm}` (1 artifact)
- Tracked-but-undeclared-in-hooks-registry: `{vsdd-context-resolvers.wasm}` (correctly
  declared in resolvers-registry; 0 true undeclared in the dual-registry sense)
- True gap: exactly 1 artifact missing; no others

This AC is satisfied when AC-006's T-012 (bundle_orphan_check.rs) passes. The `declared − tracked = ∅` subset check is stronger than an explicit audit-count assertion: it would catch any undeclared missing artifact, not just the pre-audited one. No explicit "zero additional gaps" count assertion exists or is needed — the subset check covers it.

- **Test:** AC-006's T-012 (bundle_orphan_check.rs) `declared − tracked = ∅` subset assertion — passes when `validate-factory-path-staging.wasm` is both declared in hooks-registry.toml and present in the git-tracked set; any remaining declared-but-missing artifact would also fire this gate

## Behavioral Contracts

| BC ID | Version | Title | Clause |
|-------|---------|-------|--------|
| BC-4.16.001 | v1.8 | validate-factory-path-staging WASM PreToolUse guard MUST block any `git add` or `git stage` command that stages a path under `.factory/` on a product branch, and MUST pass all non-`.factory/` staging commands unconditionally | PC1, PC2, PC3, PC4, Precondition 3, Invariant 1 |

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| validate-factory-path-staging plugin | crates/hook-plugins/validate-factory-path-staging/ | Effectful (WASM; exec_subprocess for branch detection via host ABI) |
| registry-parity Cargo tests (T-012..T-056, bundle_orphan_check.rs) | crates/factory-dispatcher/tests/bundle_orphan_check.rs (extend existing) | Mixed: most tests Pure-core (reads config files + tracked index + committed HEAD; no mutations); T-030 phase B, T-034, T-036, T-037, T-039, T-041, T-049, T-052, T-053, T-054 Effectful-shell (git init/add/commit in tempdir or non-git tempdir); T-026(b) Effectful-shell (runtime tempdir depth derivation); T-038/T-040/T-042..T-048/T-050/T-051/T-055/T-056 Pure-core (no-git-op fixtures, T-055/T-056 call the target function directly with no fixture at all); also imports `factory_dispatcher::Registry` (workspace-crate pub API) for `Registry::parse_str` production-validation in `run_t012_gate`; see Purity Classification table |
| hooks-registry.toml | plugins/vsdd-factory/hooks-registry.toml | Config (read-only for this story; entry already present — do NOT modify) |
| resolvers-registry.toml | plugins/vsdd-factory/resolvers-registry.toml | Config (read-only for this story) |
| hook-plugins artifact directory | plugins/vsdd-factory/hook-plugins/ | Effectful-shell (force-tracked binary; gitignored path requires `git add -f`) |
| registry.rs `on_error` mutation-audit unit test | crates/factory-dispatcher/src/registry.rs (extend existing `#[cfg(test)]` module) | Pure-core (parses a TOML string via `Registry::parse_str`; no filesystem/git operations); out-of-gate production accessor (`RegistryEntry::on_error`), not part of the T-NNN `run_t012_gate` sequence |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| crates/hook-plugins/validate-factory-path-staging/ | Effectful-shell | Uses `host::exec_subprocess` to invoke `git branch --show-current` for branch detection (BC-4.16.001 Invariant 3); output of subprocess read to determine block/pass |
| T-013..T-025, T-027..T-029, T-031..T-033, T-035, T-038, T-040, T-042..T-048, T-050, T-051, T-055, T-056 (bundle_orphan_check.rs) fixture-based parity tests | Pure-core | Fixture-based path computations and set-difference checks; no git mutations; deterministic given fixed inputs. T-040/T-042 use no-git-op fixtures; T-043..T-047 test containment predicate boundary conditions (no git operations); T-048 (pass-11) drives the same fixture-only path through both `extract_hook_plugin_name` and `detect_ungated_declarations` across 18 candidates — writes a tempdir registry file per candidate but performs no git operations; T-050 (pass-11 fix-burst) is a single-candidate fixture-only length-conjunct isolation control (`plugin = "../.."`) — no git operations; T-051 (pass-12 fix-burst) is a single-candidate fixture-only prefix-conjunct isolation control (`plugin = "../../../sib/ghost.wasm"`), orthogonal to T-050 — no git operations; T-055 (mutation-audit hardening) calls `detect_ungated_declarations` directly with malformed TOML — no fixture registry file on disk beyond the malformed TOML string itself, no git operations; T-056 (mutation-audit hardening) calls `lex_norm` directly with a literal `Path::new("./a/b")` — no fixture, no filesystem, no git operations |
| T-026(b), T-030, T-034, T-036, T-037, T-039, T-041, T-049, T-052, T-053, T-054 (bundle_orphan_check.rs) tempdir-dependent parity tests | Effectful-shell | T-026(b) derives `expected_depth` from runtime OS tempdir path (non-deterministic given fixed inputs); T-030 phase B runs `git init`/`add`/`commit` in tempdir; T-034 commits `hook-plugins/sub/nested.wasm` into a tempdir git repo; T-036 exercises git fixture with force-committed WASMs and gitignored-probe on disk; T-037 exercises staged-but-not-committed condition via `git add -f` without commit; T-039 runs `git init`/`config`/`add`/`commit` in tempdir to set up flat-committed hooks vs nested-declared paths; T-041 invokes `run_t012_gate` on a non-git tempdir (`#[should_panic]`); T-049 (pass-11) runs `git init`/`config`/`add`/`commit` in tempdir to commit a valid 30-entry registry with a WASM-free `hook-plugins/` directory (`config.yaml` only), triggering the EC-005a guard; T-052 (pass-14) runs `git init`/`config`/`add`/`commit` in tempdir to commit a full production-valid fixture (30 hooks + 1 resolver, all WASMs) with `hooks-registry.toml` `schema_version = 3`, isolating the hooks `Registry::parse_str` production-validation block; T-053 (pass-14) runs the same tempdir git sequence with `resolvers-registry.toml` `schema_version = 2`, isolating the resolvers `schema_version == 1` assert block; T-054 (mutation-audit hardening) runs the same tempdir git sequence as T-052/T-053 with `resolvers-registry.toml` OMITTING `schema_version` entirely, isolating the `.unwrap_or(-1)` fail-closed sentinel from the `assert_eq!` block T-053 isolates |
| `crates/factory-dispatcher/src/registry.rs` `#[cfg(test)]` module (`on_error_falls_back_to_registry_defaults_when_entry_omits_it`) | Pure-core | Parses an in-memory TOML string via `Registry::parse_str`; no filesystem, git, or subprocess operations; deterministic given fixed input |

## Red Gate Test Plan

| Test ID | Description | Expected Outcome (pre-implementation = RED) |
|---------|-------------|---------------------------------------------|
| RG-001 | `git ls-files plugins/vsdd-factory/hook-plugins/validate-factory-path-staging.wasm` before AC-001 fix | FAIL: empty output — artifact not tracked in git |
| RG-002 | Run T-012 (bundle_orphan_check.rs: `test_S_21_09_ac006_T012_declared_set_subset_of_tracked_set`) before AC-001 fix | FAIL: `validate-factory-path-staging.wasm` in declared set but absent from tracked set; assertion fires |
| RG-003 | Run `validate-factory-path-staging.bats` with `CI_REQUIRE_ARTIFACTS=1`, dispatcher binary built, WASM **not** committed (pre-fix state) | FAIL: 25 tests skip with "validate-factory-path-staging.wasm not present" (dispatcher present → WASM branch is reached; without dispatcher binary, skips fire earlier at missing `$DISPATCHER` and this string never appears) |
| RG-004 | After AC-001 fix: run T-012 (bundle_orphan_check.rs) | GREEN (Red-Gate-by-design): declared set equals tracked set; `validate-factory-path-staging.wasm` now in both |
| RG-005 | After AC-001 fix: run bats suite with `CI_REQUIRE_ARTIFACTS=1` and dispatcher binary built | PASS: all 25 previously WASM-skipping tests execute; zero skip lines for "validate-factory-path-staging.wasm not present" |
| T-013 | BLOCKER-1 nospace parse control: `parse_plugin_refs()` correctly handles `plugin="hook-plugins/foo.wasm"` (no spaces around `=`) | GREEN-only control: PASS after parse hardening; FAIL if using un-hardened original matcher |
| T-014 | BLOCKER-1 dotslash parse control: `parse_plugin_refs()` correctly handles `plugin = "./hook-plugins/foo.wasm"` (dotslash prefix); in the new algorithm, `./hook-plugins/foo.wasm` is resolved relative to the registry parent with `CurDir` acting as a no-op in lex_norm — path extracts to registry-relative `hook-plugins/foo.wasm`; old `trim_start_matches("./")` chain no longer used | GREEN-only control: PASS after parse hardening; FAIL if using un-hardened original matcher |
| T-015 | Declared-but-untracked arm: one name declared in the registry but absent from `git ls-files` tracked set; gate emits the two-space-indented `"  MISSING: hook-plugins/<name>"` line (registry-relative path) — the exact indented format production's `check_declared_subset_tracked` emits. **PR-review fix:** a prior version of this test asserted the unindented substring `"MISSING: hook-plugins/..."`; `.contains()` on an unindented needle also matches inside the real indented `"  MISSING: ..."` line, so that assertion could never catch an indent-dropping mutation. Asserting the indented form closes the gap. | FAIL: `"  MISSING: hook-plugins/<name>"` emitted (two-space indent, path-valued); PASS after name is tracked in git |
| T-016 | PASS arm and hooks floor boundary HIGH end: fixture with exactly 30 entries passes both the `>= 30` floor guard and the set-difference gate; no `MISSING:` or `STAGED-NOT-COMMITTED:` emitted | GREEN-only control: PASS at 30 entries |
| T-017 | Inventory — hyphen-named `metrics-registry.toml` appears in `plugins/vsdd-factory/*.toml` glob; gate emits the two-space-indented `"  UNEXPECTED: metrics-registry.toml"` line (the exact indented format `check_registry_inventory` emits); inventory assertion distinguishes ungated plugin registry from unintended tooling config. **Pass-20 format-lock fix (`fc0e613b`):** a prior version asserted the unindented substring `"UNEXPECTED: metrics-registry.toml"`; `.contains()` on an unindented needle also matches inside the real indented `"  UNEXPECTED: ..."` line, so that assertion could never catch an indent-dropping mutation — closes sibling gap F-S2109-LOCAL-FMTLOCK-01. | FAIL: `"  UNEXPECTED: metrics-registry.toml"` emitted (two-space indent); inventory assertion fires |
| T-018 | Inventory — both registries absent from fixture directory; gate emits two two-space-indented `"  MISSING: {}"` identifiers (one per expected registry, `"  MISSING: hooks-registry.toml"` and `"  MISSING: resolvers-registry.toml"`); hard-fail before set operations run. **Pass-20 format-lock fix (`fc0e613b`):** both arms tightened from unindented substrings to the exact indented production form — closes sibling gap F-S2109-LOCAL-FMTLOCK-01. | FAIL: two `"  MISSING: ..."` entries (two-space indent) |
| T-019 | Hooks floor fires on 1-entry set (`#[should_panic]`): fixture with only 1 hooks entry triggers the `>= 30` floor guard before set operations run | Expected panic: floor guard fires |
| T-020 | EC-005b — tracked-set-empty guard (fixture-empty path inside `check_declared_subset_tracked`; EC-005a fires separately in `run_t012_gate` on raw git output before the prefix strip): `check_declared_subset_tracked` is called with an empty tracked `HashSet` (passed directly — `git_tracked_wasm_names()` is not invoked); the `tracked.is_empty()` check fires immediately, panicking with `"T-012 EC-005b"` before the `declared − tracked` loop runs; no `MISSING:` lines are emitted. T-020's `#[should_panic(expected = "T-012 EC-005b")]` specifically pins **EC-005b** | Expected panic: `"T-012 EC-005b"` guard fires; `declared − tracked` computation never executes; pins EC-005b (not EC-005a) |
| T-021 | Staged-not-committed arm: a WASM is `git add -f`-staged but not yet committed; `git ls-files` includes it but `git ls-tree HEAD` does not; gate emits `STAGED-NOT-COMMITTED: hook-plugins/<name>` (registry-relative path) | FAIL: `STAGED-NOT-COMMITTED: hook-plugins/<name>` emitted (path-valued) |
| T-022 | Resolvers floor — `resolver_plugins.is_empty()` arm (`#[should_panic]`): empty resolver parse triggers the `>= 1` floor guard | Expected panic: resolvers floor fires |
| T-023 | Boundary-polarity record. (a) INCLUDED region — standard `hook-plugins/foo.wasm`, dotslash `./hook-plugins/foo.wasm`, traversal-into `hooks/../hook-plugins/ghost-traversal.wasm` (T-025), case-variant `Hook-Plugins/ghost-case.wasm` (T-031), nested `hook-plugins/sub/nested.wasm` → `hook-plugins/sub/nested.wasm` (T-032); case-variant `Hook-Plugins/ghost-case.wasm` is admitted by gate (c) and returned verbatim — on CI Linux the verbatim path appears in `MISSING:` vs the lowercase git-tracked form (T-042). EXCLUDED region — bare names (e.g., `ghost-bare.wasm`: lex_norm yields `expected_depth + 1` components → **minimum-length check** fires), traversal-that-cancels (T-023(b): pop restores length to `expected_depth + 1` → **minimum-length check** fires), `../`-prefix form (`../hook-plugins/foo.wasm`: pop of registry-parent's last segment yields `expected_depth + 1` components → **minimum-length check** fires), absolute paths (T-026; step-(3) gates fire: depth-coincidental via minimum-length check, depth-matched via prefix assertion). All three first-named EXCLUDED forms are excluded from `declared_set` by the minimum-length check and are now caught by `detect_ungated_declarations` as `UNGATED-DECLARATION` (T-043/T-044/T-045 respectively; containment predicate confirms inside worktree root). `../../ghost.wasm` form (T-046) similarly UNGATED at tightest INSIDE margin. `../../../ghost.wasm` (T-047) → `OUTSIDE-REPO-DECLARATION` at tightest OUTSIDE margin (`len == root_parts.len()` exactly). LOW-1 false-negative class CLOSED. Gate-3-failure paths (correct prefix and depth, non-`hook-plugins` component at position `expected_depth`) cause hard-failure via `detect_ungated_declarations` emitting `UNGATED-DECLARATION: <path>` and returning `Err` (T-038, T-040). (b) Traversal-that-cancels: `hook-plugins/../ghost-cancels.wasm` — lex_norm pushes `hook-plugins`, pops on `ParentDir` (stack clears `hook-plugins`), pushes `ghost-cancels.wasm`; component count falls to `expected_depth + 1` → minimum-length check fires before prefix loop → returns `None` → EXCLUDED. Demonstrated (ParentDir pop is load-bearing): with the pop active, `hook-plugins` is removed and path has `expected_depth + 1` components — `refs_cancels` is empty; removing the pop retains `hook-plugins` at depth position (`expected_depth + 2` components, gate 1 passes, gate 3 admits `ghost-cancels.wasm`). (c) `../hook-plugins/evil.wasm` form (now executed): `registry_parent.join("../hook-plugins/evil.wasm")` — lex_norm pops registry_parent's last Normal segment, pushes `hook-plugins`, pushes `evil.wasm` → `expected_depth + 1` components → gate 1 fires (needs `expected_depth + 2`); EXCLUDED. Closes the previously-enumerated-but-unexecuted case | T-023(a) bare-name control: `ghost-bare.wasm` → EXCLUDED from `declared_set`; GREEN control (T-043 controls UNGATED-DECLARATION for this form). T-023(b) traversal-that-cancels: `ghost-cancels.wasm` → EXCLUDED from `declared_set`; GREEN control (T-044). T-023(c) `../hook-plugins/evil.wasm` → gate 1 fires; EXCLUDED from `declared_set`; GREEN control (T-045) |
| T-024 | Inventory glob escape proof: `metrics_registry.toml` (underscore) escapes `*-registry.toml` but is caught by `*.toml`; T-024 mutation-proves why `*-registry.toml` was rejected as the inventory predicate; gate emits the two-space-indented `"  UNEXPECTED: metrics_registry.toml"` line. **Pass-20 format-lock fix (`fc0e613b`):** assertion tightened from the unindented substring to the exact indented `check_registry_inventory` production form — closes sibling gap F-S2109-LOCAL-FMTLOCK-01. | GREEN: with `*.toml` glob, adding a third underscore-named registry file causes the inventory assertion to FAIL as required (`"  UNEXPECTED: metrics_registry.toml"`, two-space indent) |
| T-025 | Traversal form `hooks/../hook-plugins/ghost-traversal.wasm`: `extract_hook_plugin_name(registry_path, "hooks/../hook-plugins/ghost-traversal.wasm")` — step (1): join against registry parent; step (2): lex_norm pushes `hooks`, pops on `ParentDir`, pushes `hook-plugins`, pushes `ghost-traversal.wasm`; step (3) all three gates pass — (a) length check: `expected_depth + 2` components; (b) prefix intact; (c) `eq_ignore_ascii_case("hook-plugins")` matches the `hook-plugins` component; step (4): returns registry-relative path `hook-plugins/ghost-traversal.wasm`; path is present in declared set | GREEN: traversal-into form correctly included after normalisation; registry-relative path returned |
| T-026 | Two absolute-form sub-fixtures prove two independent step-(3) gates. (a) Short fixture: `registry_parent.join("/abs/hook-plugins/ghost-absolute.wasm")` yields an absolute path; after lex_norm component count is below `expected_depth + 2` → minimum-length check fires before the prefix loop is consulted → returns `None`. Mutation note: removing the prefix assertion alone leaves sub-fixture (a) green — the depth check fires first. **PR-review fix:** a prior version of this test additionally asserted `!refs.contains("ghost-absolute.wasm")` (the BARE basename) — vacuous, since `extract_hook_plugin_name` never returns a bare basename for a gated entry; a gated absolute path would instead return the FULL registry-parent-relative form `hook-plugins/ghost-absolute.wasm` (see (b) below). `refs.is_empty()` is the sole, load-bearing check — it fails regardless of which form a defect admits. (b) Depth-matched fixture: counts tempdir Normal components at runtime to derive `expected_depth`, then builds `/seg0/…/seg_{N-1}/hook-plugins/evil.wasm` with exactly `expected_depth` Normal components before `hook-plugins`, so `len == expected_depth + 2` clears the minimum-length check and the prefix-verification loop becomes the operative gate → returns `None`. Captured proof (empirically re-confirmed in the PR-review fix burst): deleting the prefix loop yields `got refs_depth: {"hook-plugins/evil.wasm"}` and T-026 fails — the FULL registry-relative form, not the bare basename `{"evil.wasm"}`. **PR-review fix:** a prior version of this test additionally asserted `!refs_depth.contains("evil.wasm")` (the bare basename) and claimed that string was the mutant's actual output; that assertion was vacuous (it never fails, under live code OR the mutant) and the claimed mutant output was wrong — the mutant's real output is `{"hook-plugins/evil.wasm"}`. `refs_depth.is_empty()` is the sole, load-bearing check. Production confirms: `resolve_plugin_paths_is_idempotent_for_absolute_paths` in `registry.rs` shows absolute paths pass through unchanged and are loaded from outside the repository. **Windows CI portability fix (`c20cf2fe`):** sub-fixture (b)'s absolute-path string was previously built via `PathBuf::push` + `.to_str()`, which renders the OS-native path separator — valid on macOS/Linux but on Windows produces `\`, an invalid TOML escape sequence that panics `Registry::parse_str` when interpolated into the `registry-depth.toml` fixture; a windows-x64-only failure the all-macOS LOCAL cascade and mutation audit could not surface. Fixed by building the string via explicit forward-slash concatenation (registry paths are forward-slash by convention regardless of host OS); sub-fixture (a) was already string-built and unaffected. Test-fixture construction only — the gate's runtime classification (returns `None` via the prefix assertion) is semantically identical on all platforms, before and after | GREEN (exclusion correct): sub-fixture (a) via minimum-length check; sub-fixture (b) via prefix assertion; both independently proven; post-fix, sub-fixture (b) is also platform-portable (windows-x64 CI green) |
| T-027 | Hooks floor boundary LOW end: fixture with exactly 29 hooks entries fires the `>= 30` guard with `"...has only 29 entries"` message | Expected failure: floor fires with undercount message |
| T-028 | Inventory narrowing (a) — non-recursive: a subdirectory TOML (e.g., `tests/fixtures/validate-policies-schema/pass-valid-lint-hook/hooks-registry.toml`) is invisible to `plugins/vsdd-factory/*.toml` glob (non-recursive). `find plugins/vsdd-factory -mindepth 2 -name '*.toml'` returns nine files: `fixtures/smoke-project/Cargo.toml`; `tests/fixtures/registry-tool-filter/{anchored,comment-inject,intent-comment,prefix-only-anchor,unanchored}.toml` (5); `tests/fixtures/validate-policies-schema/{fail-nonexistent-plugin,pass-namespaced-lint-hook,pass-valid-lint-hook}/hooks-registry.toml` (3). A recursive walk would surface all nine as `UNEXPECTED:`. Narrowing is safe: `resolve_registry_path()` in `crates/factory-dispatcher/src/main.rs` loads `hooks-registry.toml` and `resolvers-registry.toml` via `plugin_root.join(...)` at `${CLAUDE_PLUGIN_ROOT}` top level only — a subdirectory file is architecturally inert and declares nothing that must exist in the tracked set | GREEN: subdirectory TOMLs are not enumerated; narrowing is safe and intentionally retained |
| T-029 | Inventory narrowing (b) — case-sensitive: `metrics-registry.TOML` (uppercase extension) is invisible to `plugins/vsdd-factory/*.toml` glob on case-sensitive filesystems (CI runs on Linux). Narrowing is safe: `resolve_registry_path()` in `main.rs` loads registries by exact lowercase name (`hooks-registry.toml`, `resolvers-registry.toml`) via `plugin_root.join(...)` — a case-variant file is architecturally inert and declares nothing that must exist | GREEN: uppercase extension not enumerated on CI Linux; narrowing is safe and intentionally retained |
| T-030 | T-012 wiring control — two phases: (1) inventory call path: `run_t012_gate` called against a tempdir seeded with `{hooks-registry.toml, resolvers-registry.toml, taplo.toml}`; asserts the two-space-indented `"  UNEXPECTED: taplo.toml"` line — confirms `check_registry_inventory` is wired through `run_t012_gate`, distinguishing the inventory outcome from the floor-error outcome ("hooks registry declared set has only 0 entries") by exact production format, not merely by identifier substring. **Pass-20 format-lock fix (`fc0e613b`):** phase A assertion tightened from the unindented substring to the exact indented form — closes sibling gap F-S2109-LOCAL-FMTLOCK-01. (2) declared-subset set-difference path: `run_t012_gate` called against a tempdir with the two registries and a corresponding git-tracked WASM; confirms `check_declared_subset_tracked` is wired into `test_S_21_09_ac006_T012_declared_set_subset_of_tracked_set()`. Phase B git operations are invoked via `.output()` (stdout captured for assertion checks) with flags `-c core.excludesFile=/dev/null -c commit.gpgsign=false -c core.hooksPath=/dev/null -c init.templateDir=` to isolate from ambient git config. **Pass-21 completion sweep (`1c93f499`):** phase B's positive assertion (previously left unindented by the pass-20 sweep, which tightened phase A only) tightened from the unindented substring `"MISSING: hook-plugins/ctx.wasm"` to the exact two-space-indented `"  MISSING: hook-plugins/ctx.wasm"` form, matching `check_declared_subset_tracked`'s real `"  MISSING: {}"` format string — closes the sixth remaining sibling gap under F-S2109-LOCAL-P?-001 (LOW), completing the format-lock sweep | GREEN: both phases pass after AC-001 fix; phase A error contains `"  UNEXPECTED: taplo.toml"` (two-space indent); phase B error contains `"  MISSING: hook-plugins/ctx.wasm"` (two-space indent) |
| T-031 | Case-variant `Hook-Plugins/ghost-case.wasm` (mixed capitalisation): `extract_hook_plugin_name` step (3)(c) uses `eq_ignore_ascii_case("hook-plugins")` — `Hook-Plugins` matches; step (4) returns verbatim registry-relative path `Hook-Plugins/ghost-case.wasm` (no lowercasing); `refs.contains("Hook-Plugins/ghost-case.wasm")` asserts verbatim form present. Two-direction mutation proof: (1) reverting step (3)(c) to strict byte equality `!= "hook-plugins"` causes `Hook-Plugins` to fail the component match → `extract_hook_plugin_name` returns `None` → refs empty → assertion fails; (2) re-introducing lowercasing of the returned path yields `hook-plugins/ghost-case.wasm` → `refs.contains("Hook-Plugins/ghost-case.wasm")` fails. **Case-variant design note:** admitted verbatim; on case-sensitive CI Linux, `Hook-Plugins/ghost-case.wasm` ≠ git-tracked `hook-plugins/ghost-case.wasm` → appears in `MISSING:`; this is intentional — a mis-cased declaration cannot load its plugin on CI (T-042 proves the end-to-end consequence) | GREEN: case-variant form admitted by gate (c); returned verbatim; `eq_ignore_ascii_case` closes fail-open; case-variant downstream behavior is by design |
| T-032 | Nested subdirectory `hook-plugins/sub/nested.wasm`: `extract_hook_plugin_name` step (3)(c) matches `hook-plugins` component; step (4) returns the full registry-relative path `hook-plugins/sub/nested.wasm` (not the last component `nested.wasm` alone, nor the intermediate `sub`). Mutation proof: reverting to last-component extraction (`last()`) returns `nested.wasm` → declared set contains `nested.wasm` while git ls-files returns path `hook-plugins/sub/nested.wasm` → `declared − tracked = {"nested.wasm"}` → `MISSING: nested.wasm` (wrong path, false alarm); `is_ok()` assertion on the gate result fails | GREEN: full registry-relative path extraction correct; nested subdirectory handled without truncating to basename |
| T-033 | Minimum-length lower boundary — `plugin = "hook-plugins"` (directory path with no filename appended) after lex_norm yields a component vector of length `expected_depth + 1`; the minimum-length check `len < expected_depth + 2` fires and returns `None` before the prefix-assertion loop is entered. Captured proof: weakening the constant to `< expected_depth + 1` allows the path through; `last()` then returns `"hook-plugins"` itself as the name → `got refs: {"hook-plugins"}` | GREEN: bare `hook-plugins` component (depth `expected_depth + 1`) excluded by minimum-length check before prefix loop. **Scope note:** the threshold enforces component count (`≥ expected_depth + 2`), not file extension; `hook-plugins/sub/` (depth `expected_depth + 2`) passes the minimum-length check, enters `declared_set`, and fires `MISSING: hook-plugins/sub` if untracked — loud, not a silent pass |
| T-034 | `git ls-tree -r` recursive flag — fixture commits `hook-plugins/sub/nested.wasm` into a tempdir git repo; `git_committed_wasm_names()` passes `-r` to `git ls-tree` and returns `["hook-plugins/sub/nested.wasm"]` (full path within the repo). Captured proof: dropping `-r` from the `git ls-tree` invocation yields an empty result set → `got committed: []` — demonstrating that non-recursive tree listing misses files in subdirectories | GREEN: `-r` flag required and tested; nested committed WASMs enumerated with full path |
| T-035 | **Gate-3 isolated control — completes the exclusion-gate enumeration.** Fixture: `plugin = "other-dir/evil-probe.wasm"` in a synthetic tempdir registry. Constructed to clear the first two gates so gate 3 alone is operative: (1) gate 1 (minimum-length): lex_norm yields `expected_depth + 2` components → length check passes; (2) gate 2 (registry-parent prefix): parent prefix intact → prefix loop passes; (3) gate 3 (`eq_ignore_ascii_case("hook-plugins")` component match): `"other-dir"` ≠ `"hook-plugins"` → returns `None` → EXCLUDED. Captured mutation proof (embedded in test): deleting the `eq_ignore_ascii_case` check while leaving gates 1 and 2 intact admits the full registry-relative path `other-dir/evil-probe.wasm` → `got refs: {"other-dir/evil-probe.wasm"}` — demonstrating that gates 1+2 alone cannot reject it. **Enumeration closure:** gate 1 is individually controlled by T-033, gate 2 by T-026(b), gate 3 by T-035; each gate's necessity is independently proven by mutation with the other two intact | GREEN: gate-3 exclusion correct; `eq_ignore_ascii_case` match closes the fail-open; all three sequential exclusion gates are now individually mutation-proven |
| T-036 | **Gitignored-but-present artifact — exercises the S-21.09 motivating condition.** Git fixture with `.gitignore` covering `hook-plugins/`; `git check-ignore -q` assertion present in test makes `.gitignore` mechanism load-bearing; 30 hooks plugins + resolver force-committed via `git add -f`; `config.yaml` filter control confirms `*.wasm` files are excluded from normal staging by the `.gitignore` rule; `gitignored-probe.wasm` written to disk after commit, never staged (never run through `git add`). `git_tracked_wasm_names()` (git ls-files) returns only the 31 force-committed names; gate emits `MISSING: hook-plugins/gitignored-probe.wasm` (path-valued). M15 mutation proof: replacing `git_tracked_wasm_names()` with `fs::read_dir` makes `gitignored-probe.wasm` appear in the tracked set → gate returns Ok (false negative proving git-index-based detection load-bearing over filesystem presence). Proved condition: a gitignored-but-present artifact (present on disk, absent from git index due to `.gitignore`) produces `MISSING:`. **Pass-21 completion sweep (`1c93f499`):** positive assertion tightened from the unindented substring to the exact two-space-indented `"  MISSING: hook-plugins/gitignored-probe.wasm"` form, matching `check_declared_subset_tracked`'s real `"  MISSING: {}"` format string — closes a remaining sibling gap under F-S2109-LOCAL-P?-001 (LOW). | FAIL: `"  MISSING: hook-plugins/gitignored-probe.wasm"` emitted (two-space indent, path-valued); gitignored artifact correctly detected; `.gitignore` mechanism and git-index-based detection both proven load-bearing |
| T-037 | **Staged-but-not-committed control — two-function identity proof.** Commits 30 hooks plugins + resolver; then `git add -f staged-probe.wasm` without committing. `git ls-files` (tracked set) includes `staged-probe.wasm`; `git ls-tree -r --name-only HEAD` (committed set) does not. Gate emits `STAGED-NOT-COMMITTED: hook-plugins/staged-probe.wasm` (path-valued). M18 mutation: swapping `git_committed_wasm_names()` for `git ls-files` makes committed==tracked → gate returns Ok silently (false negative — staged but uncommitted artifact passes undetected; proves `git ls-tree` is load-bearing for the committed-set query). M16 mutation: swapping the tracked and committed arguments to the set-difference step emits `MISSING: hook-plugins/staged-probe.wasm` instead of `STAGED-NOT-COMMITTED: hook-plugins/staged-probe.wasm` — proving both function identities and argument order are load-bearing. **Pass-21 completion sweep (`1c93f499`):** positive assertion tightened from the unindented substring to the exact two-space-indented `"  STAGED-NOT-COMMITTED: hook-plugins/staged-probe.wasm"` form, matching `check_declared_subset_tracked`'s real `"  STAGED-NOT-COMMITTED: {}"` format string — closes a remaining sibling gap under F-S2109-LOCAL-P?-001 (LOW). | FAIL: `"  STAGED-NOT-COMMITTED: hook-plugins/staged-probe.wasm"` emitted (two-space indent, path-valued); `git ls-tree -r HEAD` correctly distinguishes staged-only from committed artifacts |
| T-038 | **`detect_ungated_declarations` hard-failure control.** No-git fixture; synthetic registry declares `plugin = "other-dir/evil-probe.wasm"`. This path passes gate 1 (lex_norm yields `expected_depth + 2` components) and gate 2 (registry-parent prefix intact) in `extract_hook_plugin_name`, but fails gate 3 (`"other-dir"` ≠ `"hook-plugins"`). `detect_ungated_declarations` fires before the git calls in `run_t012_gate`; emits the two-space-indented `"  UNGATED-DECLARATION: other-dir/evil-probe.wasm"` line (matching `run_t012_gate`'s `format!("  {}", p)` wrapping) and causes `run_t012_gate` to return `Err`. T-038 asserts `result.is_err()` and that the error contains the indented form. An ungated declaration is a hard failure — `run_t012_gate` does not continue to the git steps. Pure-core: no git operations; deterministic given fixed inputs. **Pass-20 format-lock fix (`fc0e613b`):** assertion tightened from the unindented substring to the exact indented `run_t012_gate` production form — closes sibling gap F-S2109-LOCAL-FMTLOCK-01. | FAIL (pre-implementation); GREEN (post-implementation): gate returns `Err("  UNGATED-DECLARATION: other-dir/evil-probe.wasm")` (two-space indent); hard failure confirmed; gate-3-failure class loudly surfaced |
| T-039 | **EC-008 closed defect control (Effectful-shell).** Runs `git init`/`config`/`add`/`commit` in a tempdir to create a git repo with 30 hooks committed flat at `hook-plugins/h00.wasm`..`hook-plugins/h29.wasm`; registry declares them at `hook-plugins/sub/h00.wasm`..`hook-plugins/sub/h29.wasm`; path-based declared set = `{hook-plugins/sub/hNN.wasm}`; git ls-files tracked set = `{hook-plugins/hNN.wasm}`; `declared − tracked` non-empty → gate returns `Err`; asserts `Err` string contains `MISSING: hook-plugins/sub/h00.wasm`. Under the old basename design, both sets collapsed to `{hNN.wasm}` and the gate returned `Ok(())` silently. Effectful-shell: requires a real git repo in tempdir. **Pass-21 completion sweep (`1c93f499`):** positive assertion tightened from the unindented substring to the exact two-space-indented `"  MISSING: hook-plugins/sub/h00.wasm"` form, matching `check_declared_subset_tracked`'s real `"  MISSING: {}"` format string — closes a remaining sibling gap under F-S2109-LOCAL-P?-001 (LOW). | FAIL: `Err` containing `"  MISSING: hook-plugins/sub/h00.wasm"` (two-space indent); proves path-based diff closes EC-008 |
| T-040 | **Resolvers-side UNGATED-DECLARATION hard-failure control (Pure-core).** Synthetic resolver registry entry with a non-`hook-plugins` component at the `expected_depth` position; passes gates 1+2, fails gate 3 in `extract_hook_plugin_name`; `detect_ungated_declarations` emits `UNGATED-DECLARATION: <path>` and `run_t012_gate` returns `Err`. T-040 asserts `result.is_err()`. Confirms the helper covers both hooks and resolvers registries, and that the failure is hard (not advisory). Pure-core: no git operations. **Pass-21 completion sweep (`1c93f499`):** positive assertion tightened from the unindented substring to the exact two-space-indented `"  UNGATED-DECLARATION: other-dir/evil-resolver.wasm"` form, matching `run_t012_gate`'s real `format!("  {}", p)` wrapping — closes a remaining sibling gap under F-S2109-LOCAL-P?-001 (LOW). | FAIL (pre-implementation); GREEN (post-implementation): gate returns `Err("  UNGATED-DECLARATION: other-dir/evil-resolver.wasm")` (two-space indent); hard failure confirmed on resolvers side |
| T-041 | **`#[should_panic(expected = "git ls-files exited with status")]` on non-git directory (Effectful-shell).** Invokes `run_t012_gate` on a tempdir that was NOT initialised as a git repository; `git_tracked_wasm_names()` calls `git ls-files` which exits with a non-zero status; `run_t012_gate` propagates the error as a panic. Confirms that `git ls-files` failure is loudly surfaced rather than silently returning an empty set. | Expected panic: `"git ls-files exited with status"` string present in panic message |
| T-042 | **Case-variant end-to-end control (Pure-core).** Declares `Hook-Plugins/ghost-missing.wasm` (capitalised); git tree has lowercase-tracked `hook-plugins/ghost-missing.wasm`; path-based diff: verbatim declared path `Hook-Plugins/ghost-missing.wasm` ≠ lowercase tracked path `hook-plugins/ghost-missing.wasm` → gate returns `Err` containing `MISSING: Hook-Plugins/ghost-missing.wasm`; asserts `is_err()`. Mutation: re-introducing lowercasing of the extracted path makes declared path equal tracked path → `is_err()` assertion fails → proves verbatim-return is load-bearing for gate-failure-by-design behavior. Pure-core: uses pre-built HashSet fixture, no git operations. **Pass-21 completion sweep (`1c93f499`):** positive assertion tightened from the unindented substring to the exact two-space-indented `"  MISSING: Hook-Plugins/ghost-missing.wasm"` form, matching `check_declared_subset_tracked`'s real `"  MISSING: {}"` format string — closes the final remaining sibling gap under F-S2109-LOCAL-P?-001 (LOW), completing the format-lock sweep. | FAIL: `Err` containing `"  MISSING: Hook-Plugins/ghost-missing.wasm"` (two-space indent); case-variant declaration correctly surfaces as gate failure; verbatim-return proven load-bearing |
| T-043 | **Bare-name UNGATED control (Pure-core).** `ghost-bare.wasm` joined against registry parent (`registry_parent.join("ghost-bare.wasm")`) resolves to `registry_parent/ghost-bare.wasm`; lex_norm yields one Normal component past the registry parent; containment predicate confirms inside worktree root (`joined_parts.len() > root_parts.len()`); the pass-11 single-copy refactor delegates the gated/ungated determination to `extract_hook_plugin_name(registry_path, "ghost-bare.wasm").is_none()`, which fails the minimum-length gate; `detect_ungated_declarations` emits the two-space-indented `"  UNGATED-DECLARATION: ghost-bare.wasm"` line (matching `run_t012_gate`'s `format!("  {}", p)` wrapping) and returns `Err`. Prior analysis: bare names were believed to be excluded by minimum-length check; they are excluded from `declared_set` by that check but their declared presence still causes `run_t012_gate` to return `Err` via the containment predicate. The pre-existing negative-identifier assertion `!err.contains("OUTSIDE-REPO-DECLARATION: ")` is unchanged. **Pass-20 format-lock fix (`fc0e613b`):** positive assertion tightened from the unindented substring to the exact indented form — closes sibling gap F-S2109-LOCAL-FMTLOCK-01. | FAIL: `Err` containing `"  UNGATED-DECLARATION: ghost-bare.wasm"` (two-space indent); bare-name escape class closed by containment predicate |
| T-044 | **Traversal-that-cancels UNGATED control (Pure-core).** Traversal form that cancels the `hook-plugins` component (e.g., `hook-plugins/../ghost-cancels.wasm`); lex_norm pushes then pops; resolves inside worktree root but outside `hook-plugins/`; containment predicate fires; `detect_ungated_declarations` emits the two-space-indented `"  UNGATED-DECLARATION: ../vsdd-factory/ghost-updir.wasm"` line and returns `Err`. Prior analysis: this form was excluded from `declared_set` by minimum-length check; it still triggers the containment predicate. **Pass-20 format-lock fix (`fc0e613b`):** positive assertion tightened from the unindented substring to the exact indented `run_t012_gate` production form — closes sibling gap F-S2109-LOCAL-FMTLOCK-01. | FAIL: `Err` containing `"  UNGATED-DECLARATION: ../vsdd-factory/ghost-updir.wasm"` (two-space indent); traversal-that-cancels escape class closed |
| T-045 | **`../ghost.wasm` → `plugins/ghost.wasm`, UNGATED (Pure-core).** `../ghost.wasm` relative to `plugins/vsdd-factory/` resolves to `plugins/ghost.wasm`; inside worktree root but outside `hook-plugins/`; containment predicate confirms; `detect_ungated_declarations` emits the two-space-indented `"  UNGATED-DECLARATION: ../ghost.wasm"` line and returns `Err`. **Pass-20 format-lock fix (`fc0e613b`):** positive assertion tightened from the unindented substring to the exact indented `run_t012_gate` production form — closes sibling gap F-S2109-LOCAL-FMTLOCK-01. | FAIL: `Err` containing `"  UNGATED-DECLARATION: ../ghost.wasm"` (two-space indent); one-level `../` escape closed |
| T-046 | **`../../ghost.wasm` → `<root>/ghost.wasm`, UNGATED at tightest INSIDE margin (Pure-core).** `../../ghost.wasm` relative to `plugins/vsdd-factory/` resolves to `<worktree-root>/ghost.wasm`; `joined_parts.len() == root_parts.len() + 1` — one component past root, satisfying `len > root_parts.len()`; containment predicate prefix check passes; the pass-11 single-copy refactor delegates to `extract_hook_plugin_name(...).is_none()`, which fails gate 3 (the `hook-plugins`-component match); emits the two-space-indented `"  UNGATED-DECLARATION: ../../ghost.wasm"` line. Proves the tightest possible INSIDE boundary — one component below root is inside. **Pass-20 format-lock fix (`fc0e613b`):** positive assertion tightened from the unindented substring to the exact indented `run_t012_gate` production form — closes sibling gap F-S2109-LOCAL-FMTLOCK-01. | FAIL: `Err` containing `"  UNGATED-DECLARATION: ../../ghost.wasm"` (two-space indent); tightest INSIDE margin proven |
| T-047 | **`../../../ghost.wasm` → above root, OUTSIDE-REPO-DECLARATION at tightest OUTSIDE margin (Pure-core).** `../../../ghost.wasm` relative to `plugins/vsdd-factory/` resolves to one level above the worktree root; `joined_parts.len() == root_parts.len()`. At this depth both the length conjunct (`len > root_parts.len()`) and the prefix conjunct (`joined[0..root_parts.len()] == root_parts`) fail simultaneously — the path neither exceeds root depth nor shares the root prefix. `detect_ungated_declarations` emits the two-space-indented `"  OUTSIDE-REPO-DECLARATION: ../../../ghost.wasm"` line and returns `Err`. T-047 proves that a path resolving at exactly root depth triggers the identifier; it does not isolate which conjunct is individually operative — both the length conjunct and the prefix conjunct fail simultaneously at this candidate, so T-047 does NOT kill mutant M2 (`len >` → `len >=`); T-050 (pass-11 fix-burst) is the dedicated length-conjunct isolation control. The pre-existing negative-identifier assertion `!err.contains("UNGATED-DECLARATION: ")` is unchanged. **Pass-20 format-lock fix (`fc0e613b`):** positive assertion tightened from the unindented substring to the exact indented form — closes sibling gap F-S2109-LOCAL-FMTLOCK-01. | FAIL: `Err` containing `"  OUTSIDE-REPO-DECLARATION: ../../../ghost.wasm"` (two-space indent); tightest OUTSIDE margin for the identifier class proven; both conjuncts fail at root depth; first occurrence of new identifier class; does not isolate M2 (see T-050) |
| T-048 | **pass-11 totality-property partition (Pure-core) — closes pass-10 BLOCKER-2.** 18-candidate parametrized test: for each `(plugin_path, expected_class)` pair (5 `gated`, 9 `UNGATED-DECLARATION`, 4 `OUTSIDE-REPO-DECLARATION`), writes a minimal single-entry registry, then calls `detect_ungated_declarations` — the function `run_t012_gate` actually calls — on the candidate. `gated` candidates assert `detected.is_empty()`; non-`gated` candidates assert `assert_eq!(detected, vec![format!("{}: {}", ident, path)])` (exact-vec equality against the classification identifier + path), which in one assertion pins the correct identifier fires (no UNGATED/OUTSIDE swap), the emitted path matches verbatim, and nothing extraneous is also emitted. **PR-review fix:** a prior version of this test additionally called `extract_hook_plugin_name` directly and asserted `extract.is_some()`/`is_none()` paired with the `detect_ungated_declarations`-based check — tautological, since `detect_ungated_declarations` decides its own emission by calling `extract_hook_plugin_name(...).is_none()` internally (the pass-11 single-copy design); pairing the two exercised the identical computation twice over identical inputs. The single exact-vec-equality assertion is now the sole information-bearing check per case; it subsumes the former separate "other identifier absent" check via exact-set equality. Kills mutant M4 (`>=+2→+1`): under M4, `extract_hook_plugin_name` wrongly returns `Some("hook-plugins")` for the bare-directory `"hook-plugins"` case (classified `UNGATED-DECLARATION` in this table, never `gated`), so `detect_ungated_declarations` no longer emits its finding — the exact-match assertion on that row (expects `["UNGATED-DECLARATION: hook-plugins"]`, gets `[]`) catches it. **Corrected attribution (PR-review fix):** a prior version of this row's mutation-kill narrative mistakenly credited a GATED-branch assertion with catching M4 via the `"hook-plugins"` case — but `"hook-plugins"` is classified `UNGATED-DECLARATION` in the table, never exercised by the GATED branch; the catch happens on the UNGATED-DECLARATION row's exact-match assertion, as corrected above. Also kills the M1+M4 composite and any identifier-swap mutation among its 18 candidates via exact-vec equality; does NOT isolate M2 `len >` → `len >=` — every OUTSIDE-REPO-DECLARATION candidate in this table fails both the length conjunct and the prefix conjunct simultaneously, so relaxing `>` to `>=` alone does not flip any of T-048's outcomes; T-050 is the dedicated M2 length-conjunct isolation control. | FAIL (pre-pass-11): no test asserted the `extract.is_some() ⟺ detect.is_empty()` totality invariant at all — pass-10 BLOCKER-2. GREEN (post-pass-11, re-verified post-PR-review-fix): all 18 candidates partition correctly via the single exact-vec-equality assertion; mutation-resistant via the negative-identifier subsumption; does not isolate M2 (see T-050) |
| T-049 | **pass-11 EC-005a control (Effectful-shell).** Git fixture: `git init` in a tempdir; commits a `Registry::parse_str`-conformant 30-entry `hooks-registry.toml` (`hook-plugins/h00.wasm`..`h29.wasm`) plus a `resolvers-registry.toml` with `schema_version = 1`, but the committed `hook-plugins/` directory contains only `config.yaml` — zero `.wasm` files are ever staged or committed. `run_t012_gate` passes registry inventory, `Registry::parse_str` validation, and `detect_ungated_declarations` (all 30 declarations are well-formed), then calls `git_tracked_wasm_names()`; the `.wasm`-filtered `git ls-files` result is empty; the `tracked_raw.is_empty()` guard fires. | Expected panic: `"T-012 EC-005a"` string present in panic message; pins EC-005a as distinct from EC-005b (T-020, which exercises the guard *inside* `check_declared_subset_tracked` via a directly-constructed empty `HashSet`, bypassing git entirely) |
| T-050 | **pass-11 fix-burst length-conjunct isolation control (Pure-core) — kills M2, closes F-1.** Dedicated root-resolving, filename-less OUTSIDE candidate: `plugin = "../.."` from `plugins/vsdd-factory/hooks-registry.toml` resolves (after two `ParentDir` pops in lex_norm) to exactly `root_parts` — `joined_parts == root_parts`, not merely equal length — so the prefix conjunct (`joined[0..root_parts.len()] == root_parts`) is a trivial self-match and the length conjunct (`joined_parts.len() > root_parts.len()`) alone determines the outcome. Under live `>` code the length conjunct fails (equal, not greater) → `OUTSIDE-REPO-DECLARATION: ../..`. Under mutant M2 (`>=`) the length conjunct passes and, since the prefix already matches, `in_repo` flips true → delegates to `extract_hook_plugin_name`, which independently fails its own minimum-length gate → `None` → `UNGATED-DECLARATION: ../..` instead — a genuine classification flip. Empirically verified (fix-burst pass-11, F-1 closure): with M2 applied locally, only T-050 goes RED (44/45 pass); T-047 and T-048 stay GREEN, confirming neither isolates the length conjunct — T-050 is the sole isolating control. No git init required; `detect_ungated_declarations` fires before git calls. Positive assertion is the two-space-indented `"  OUTSIDE-REPO-DECLARATION: ../.."` line (matching `run_t012_gate`'s `format!("  {}", p)` wrapping); the negative-identifier assertion `!err.contains("UNGATED-DECLARATION: ")` is unchanged. **Pass-20 format-lock fix (`fc0e613b`):** positive assertion tightened from the unindented substring to the exact indented form — closes sibling gap F-S2109-LOCAL-FMTLOCK-01. | GREEN at HEAD: error contains `"  OUTSIDE-REPO-DECLARATION: ../.."` (two-space indent) and does NOT contain `UNGATED-DECLARATION: `; RED only under mutant M2 applied locally (empirical mutation-kill proof, not a standing pre-implementation Red Gate) |
| T-051 | **pass-12 fix-burst prefix-conjunct isolation control (Pure-core) — kills `.all(...)`→`.any(...)` and `.all(...)`→`true`, closes F-1 (pass-12).** The orthogonal sibling of T-050. Dedicated OUTSIDE candidate whose length conjunct is unambiguously TRUE: `plugin = "../../../sib/ghost.wasm"` from `plugins/vsdd-factory/hooks-registry.toml` resolves (three `ParentDir` hops clear both registry-path segments and pop one component off `root_parts`, then `sib`/`ghost.wasm` are pushed back) to `root_parts[0..N-1] + ["sib", "ghost.wasm"]` — length `N+1 > N` (length conjunct TRUE), but at index `N-1` the joined path carries `"sib"` while `root_parts[N-1]` is the worktree root's actual last path component, so the prefix conjunct (`root_parts.iter().enumerate().all(|(i, p)| joined_parts.get(i) == Some(p))`) is FALSE — the sole determinant of `in_repo` for this candidate. Under live `.all(...)` code: length TRUE && prefix FALSE → `in_repo = false` → `OUTSIDE-REPO-DECLARATION: ../../../sib/ghost.wasm`. Under mutant `.all(...)` → `.any(...)`: at least one index (0..N-2) matches, so `.any()` returns TRUE → `in_repo` flips true → delegates to `extract_hook_plugin_name`, which independently fails its own registry-parent-prefix gate → `None` → `UNGATED-DECLARATION: ../../../sib/ghost.wasm` instead — a genuine classification flip. Under mutant `.all(...)` → `true`: the prefix conjunct is unconditionally TRUE → identical flip to `UNGATED-DECLARATION`. Empirically verified (fix-burst pass-12, F-1 closure): under mutant `.all→.any` AND under mutant `.all→true` applied locally, ONLY T-051 goes RED (observes `UNGATED-DECLARATION` instead of the asserted `OUTSIDE-REPO-DECLARATION`); T-047, T-048, and T-050 all stay GREEN under both mutants, confirming none of them isolates the prefix conjunct — T-051 is the sole isolating control. Orthogonality vs T-050 confirmed: under mutant M2 (`len >` → `len >=`) applied to T-051's candidate, the length conjunct (already true under live code) stays true and the prefix conjunct (already false under live code) stays false — no observable behaviour change — so T-051 stays GREEN under M2 while T-050 goes RED. No git init required; `detect_ungated_declarations` fires before git calls. No production code changed. Positive assertion is the two-space-indented `"  OUTSIDE-REPO-DECLARATION: ../../../sib/ghost.wasm"` line (matching `run_t012_gate`'s `format!("  {}", p)` wrapping); the negative-identifier assertion `!err.contains("UNGATED-DECLARATION: ")` is unchanged. **Pass-20 format-lock fix (`fc0e613b`):** positive assertion tightened from the unindented substring to the exact indented form — closes sibling gap F-S2109-LOCAL-FMTLOCK-01. | GREEN at HEAD: error contains `"  OUTSIDE-REPO-DECLARATION: ../../../sib/ghost.wasm"` (two-space indent) and does NOT contain `UNGATED-DECLARATION: `; RED only under mutant `.all(...)→.any(...)` or `.all(...)→true` applied locally (empirical mutation-kill proof, not a standing pre-implementation Red Gate) |
| T-052 | **pass-14 fix-burst hooks production-validation isolation control (Effectful-shell) — kills deletion of the `Registry::parse_str(&hooks_content)` block, closes adversary pass-14 MEDIUM.** Full, otherwise-production-valid git fixture (30 hooks + 1 resolver, all 31 WASMs committed — matching the T-030 phase-B/T-050/T-051 pattern) whose `hooks-registry.toml` declares `schema_version = 3` against `REGISTRY_SCHEMA_VERSION = 2`, with `resolvers-registry.toml` fully production-valid (`schema_version = 1`, isolating this determinant from T-053's). Every other gate in `run_t012_gate` (registry inventory, `detect_ungated_declarations`, both non-vacuity floors, declared ⊆ tracked) would PASS against this fixture. Under live code `Registry::parse_str` returns `Err(RegistryError::SchemaVersion { got: 3, expected: 2 })` (E-REG-001), causing `run_t012_gate` to panic with `"T-012: hooks-registry.toml fails production validation"` before any other gate is reached. Under the deletion mutant no production check runs; `hooks_refs`/`resolvers_refs` parse the same 30+1 declarations via the schema-version-agnostic `parse_plugin_refs`, and every subsequent gate passes against the fully-committed fixture — `run_t012_gate` returns `Ok(())` and the `#[should_panic]` assertion does not fire (test goes RED). Empirically verified: deleting the block sends T-052 RED while all other 47 tests stay GREEN. | GREEN at HEAD: panics with `"T-012: hooks-registry.toml fails production validation"`; RED only under deletion of the `Registry::parse_str(&hooks_content)` block applied locally (empirical mutation-kill proof, not a standing pre-implementation Red Gate) |
| T-053 | **pass-14 fix-burst resolvers schema-version isolation control (Effectful-shell) — kills deletion of the `assert_eq!(resolvers_schema_version, 1, …)` block, closes adversary pass-14 MEDIUM.** Same full production-valid fixture shape as T-052 (30 hooks + 1 resolver, all 31 WASMs committed) but with the defect on the OTHER registry: `hooks-registry.toml` is fully production-valid (`schema_version = 2`, so `Registry::parse_str` passes, isolating this determinant from T-052's) and `resolvers-registry.toml` declares `schema_version = 2` against the production-required 1. `resolvers-registry.toml` is never passed through `Registry::parse_str` — it is parsed generically as `toml::Value` and its `schema_version` integer is checked directly by the `assert_eq!` block. Under live code the `assert_eq!` panics with `"T-012: resolvers-registry.toml schema_version=2 but production requires 1"` before any declared-set gate is reached. Under the deletion mutant no resolvers-schema check runs and every subsequent gate passes against the fully-committed fixture — `run_t012_gate` returns `Ok(())` and the `#[should_panic]` assertion does not fire (test goes RED). Empirically verified: deleting the block sends T-053 RED while all other 47 tests stay GREEN. Orthogonal to T-052 — the two fixtures put the defect on opposite registries. | GREEN at HEAD: panics with `"T-012: resolvers-registry.toml schema_version=2 but production requires 1"`; RED only under deletion of the resolvers `assert_eq!` block applied locally (empirical mutation-kill proof, not a standing pre-implementation Red Gate) |
| T-054 | **Mutation-audit hardening SURV-04 isolation control (Effectful-shell) — kills `.unwrap_or(-1)` → `.unwrap_or(1)`, closes mutation-audit CORRECTNESS survivor.** Same full production-valid fixture shape as T-052/T-053 (30 hooks + 1 resolver, all 31 WASMs committed; `hooks-registry.toml` fully production-valid) but `resolvers-registry.toml` OMITS the `schema_version` key entirely — the absent-key path, distinct from T-053's explicit-wrong-integer (`schema_version = 2`) fixture. Under live code `resolvers_doc.get("schema_version").and_then(toml::Value::as_integer).unwrap_or(-1)` yields `-1` (fail-closed sentinel) → `assert_eq!(-1, 1, …)` panics with `"T-012: resolvers-registry.toml schema_version=-1 but production requires 1; …"` before any declared-set gate is reached. Under the `.unwrap_or(-1)` → `.unwrap_or(1)` mutant, the absent key silently coerces to the production-required value `1` → `assert_eq!` passes → every subsequent gate (ungated-declaration, both floors, declared ⊆ tracked) also passes against this fully-committed fixture → `run_t012_gate` returns `Ok(())` and the `#[should_panic]` assertion does not fire (test goes RED). Empirically verified: applying the mutant sends T-054 RED while all other tests stay GREEN; reverting restores GREEN. Proves the `-1` sentinel itself — not just the `assert_eq!` block T-053 isolates — is load-bearing: a resolvers registry with no `schema_version` key at all would otherwise be silently admitted. | GREEN at HEAD: panics with `"T-012: resolvers-registry.toml schema_version=-1 but production requires 1"` (matched via `#[should_panic(expected = "schema_version=-1 but production requires 1")]` — **PR-review fix:** the `#[should_panic]` expected-string was tightened from the looser `"but production requires 1"` to pin the `-1` fail-closed sentinel value itself, not merely the generic tail; the looser string would also match a hypothetical wrong-non-1 sentinel, so it under-specified which value fires); RED only under the `.unwrap_or(-1)` → `.unwrap_or(1)` mutant applied locally (empirical mutation-kill proof, not a standing pre-implementation Red Gate) |
| T-055 | **Mutation-audit hardening SURV-03 isolation control (Pure-core) — kills the `Err(_) => Vec::new()` fail-open arm in `detect_ungated_declarations`, closes mutation-audit structural survivor.** This arm is unreachable through any `run_t012_gate` fixture — `Registry::parse_str(&hooks_content)` (hooks side) and the generic `resolvers_content.parse::<toml::Value>()` call (resolvers side) both panic on malformed TOML before `detect_ungated_declarations` is ever invoked, so no gate-integration control can isolate this arm. T-055 calls `detect_ungated_declarations` DIRECTLY (bypassing `run_t012_gate`) with a registry file containing an unterminated `[[hooks` array-of-tables header (missing the closing `]]`), which fails `content.parse::<toml::Value>()`. Under live code `Err(_) => return Vec::new()` → the function returns an empty vec; T-055 asserts `result.is_empty()`. Under a fail-open mutant that returns a spurious non-empty finding from the `Err(_)` arm (e.g. `vec!["UNGATED-DECLARATION: mutant-injected".to_string()]`), the `.is_empty()` assertion fails (test goes RED). Empirically verified: applying the mutant sends T-055 RED while all other tests stay GREEN; reverting restores GREEN. No git operations. **PR-review fix — positive control added:** the malformed-TOML assertion alone is vacuous against a coarser mutant — a whole-function `detect_ungated_declarations` body replaced with `Vec::new()` (dead/inert function) would ALSO pass the malformed-TOML assertion, since malformed input is empty BY CONSTRUCTION under both the live fail-open arm and a globally-inert function; the malformed-TOML case in isolation cannot distinguish "the `Err(_)` arm specifically returns empty" from "this function always returns empty." T-055 now also calls `detect_ungated_declarations` with VALID TOML containing a genuine ungated declaration (`ghost-bare.wasm`) and asserts the result is non-empty — a whole-function `Vec::new()` mutant fails this second assertion (empirically verified), closing the blind spot. | GREEN at HEAD: `detect_ungated_declarations(...).is_empty()` on malformed TOML AND `!detect_ungated_declarations(...).is_empty()` on the valid-TOML positive control; RED only under a fail-open `Err(_)` arm mutant (first assertion) or a whole-function `Vec::new()` mutant (second assertion) applied locally (empirical mutation-kill proof, not a standing pre-implementation Red Gate) |
| T-056 | **Mutation-audit hardening SURV-02 isolation control (Pure-core) — kills a `Component::CurDir => {}` push mutant in `lex_norm`, closes mutation-audit structural survivor.** `lex_norm`'s `CurDir` arm (skip `.` components) is dead in every `run_t012_gate` call path — every registry-relative plugin-path join site uses absolute worktree roots, and `Path::components()` does not synthesize `CurDir` components for leading `.` segments unless a fixture literally spells `./` at the start of the path, which no existing `run_t012_gate` fixture does. T-056 calls `lex_norm` DIRECTLY (no fixture, no filesystem, no git) with `Path::new("./a/b")` — the `.` is a LEADING component (Path::components() preserves leading `./`, which is why the CurDir arm is reachable at all) — pinning the function's own contract rather than an integration side-effect. Under live code `Component::CurDir => {}` skips the `.` component, contributing nothing to `parts` → `lex_norm(Path::new("./a/b")) == ["a", "b"]`. Under a mutant `Component::CurDir => parts.push(".".to_owned())`, the `.` component is pushed → result is `[".", "a", "b"]` instead → `assert_eq!` fails (test goes RED), with the assert message reading "T-056: lex_norm must skip a literal leading `.` (CurDir) component...". Empirically verified: applying the mutant sends T-056 RED while all other tests stay GREEN; reverting restores GREEN. No git operations; no production code changed by T-056 — it is a test-only mutation-completeness control. | GREEN at HEAD: `lex_norm(Path::new("./a/b")) == ["a", "b"]`; RED only under the CurDir-push mutant applied locally (empirical mutation-kill proof, not a standing pre-implementation Red Gate) |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | `.wasm` is built but artifact has wrong content (truncated, corrupt, or zero-byte) | Dispatcher fails with plugin.crashed; `on_error = "continue"` → fail-open per BC-4.16.001 Invariant 2. **The parity gate does not catch this**: T-012's `declared − tracked = ∅` check compares names only — a zero-byte or corrupt file with the correct name satisfies the gate. **CI does not catch this**: the `bats-full-suite` job's "Stage WASM plugins for run-all.sh" step runs a full workspace wasm32 rebuild and overwrites `hook-plugins/` immediately before bats — CI exercises freshly-built bytes, not committed bytes; `release.yml` also rebuilds. The committed bytes matter exclusively in developer checkouts (and production sessions). Byte-reproducibility was verified at commit time (36/36 bats green); this is regression exposure, consistent with all 35 sibling WASMs. A content-validation gate (minimum-size check or hash comparison) belongs to **S-21.14** (gate sweep). |
| EC-002 | Registry parity test run in CI before WASM is committed | Test fails with non-empty declared-but-missing set; CI gate fires as expected |
| EC-003 | A future story adds a new hooks-registry.toml entry without committing the artifact | Cargo parity test T-012 (bundle_orphan_check.rs) catches the gap in the same `cargo test` run; blocks the PR |
| EC-004 | Registry parity test cannot find `hooks-registry.toml` at expected path | EC-004 fulfilled by step 1 MISSING arm — `check_registry_inventory()` returns `Err(...MISSING: hooks-registry.toml...)` propagated via `?` before `parse_plugin_refs` is reached; separate hard-fail blocks were deleted as dead code (Option A) |
| EC-005a | `run_t012_gate` calls `git_tracked_wasm_names(root)` and asserts the raw result is non-empty (`tracked_raw.is_empty()` guard) BEFORE stripping the `plugins/vsdd-factory/` prefix. An empty raw result means `git ls-files` matched nothing under the `plugins/vsdd-factory/hook-plugins/` pathspec — either because no `.wasm` was ever committed under that path (T-049's fixture: a valid 30-entry registry, `config.yaml` committed, zero WASMs), or because a case-variant `git add -f Hook-Plugins/…` re-spelled the index directory on case-sensitive Linux CI. Once the raw set is confirmed non-empty, a SEPARATE panic guards a wrong-prefix path: each raw entry is `.strip_prefix(REGISTRY_PARENT_PREFIX).expect(...)`'d — a path that does not start with `REGISTRY_PARENT_PREFIX` (a coding defect, not expected in practice since `git_tracked_wasm_names()` already scopes the pathspec) panics with a distinct `"T-012: every git-tracked WASM path must start with REGISTRY_PARENT_PREFIX"` message | Hard failure (two independent guards): `tracked_raw.is_empty()` panics with `"T-012 EC-005a"` (pinned by T-049); a non-empty-but-wrong-prefix result panics on the `strip_prefix` `.expect(…)` with a distinct message. Neither path silently produces an empty or wrong tracked set |
| EC-005b | `check_declared_subset_tracked` called with an empty tracked `HashSet` (fixture-empty path, bypassing `git_tracked_wasm_names()`); `tracked.is_empty()` guard fires before the set-difference loop | Hard failure: panics with `"T-012 EC-005b"`; `declared − tracked` computation never executes; T-020 pins this path with `#[should_panic(expected = "T-012 EC-005b")]` |
| EC-006 | A future registry split adds a third `.toml` file (e.g., `metrics_registry.toml` underscore, or `metrics-registry.toml` hyphen) without updating T-012 (bundle_orphan_check.rs) | T-012's `declared_set − tracked_set` assertion stays GREEN (artifacts from unparsed registry never enter `declared`). Defense is T-012's registry-inventory assertion using `plugins/vsdd-factory/*.toml` — the narrower `*-registry.toml` was explicitly rejected because `metrics_registry.toml` (underscore) escapes it (T-024 mutation-proves this). Adding any third `.toml` file causes the inventory assertion to FAIL, forcing the developer to update T-012 before the test can pass. |
| EC-007 | A registry entry with `enabled = false` is declared but its artifact was never committed | `enabled = false` entries still contribute their `plugin` value to `declared_set` (POLICY 13 latent-class record); a disabled-but-untracked plugin produces a `MISSING:` false positive. Currently zero such instances in either registry. If a future story adds `enabled = false` support, the gate may need to filter disabled entries from `declared_set`. No code change needed now. |
| EC-008 | A registry entry's `plugin =` path differs from the git-tracked path yet resolves to the same basename — e.g., declared at `hook-plugins/sub/x.wasm` but committed flat at `hook-plugins/x.wasm` | **Closed defect with control (T-039).** Basename-comparison design produced a real false negative requiring only ONE mis-matched entry: `declared_set` contained `x.wasm` (extracted basename), `tracked_set` also contained `x.wasm` (git ls-files basename) → `declared − tracked = ∅` → gate returned `Ok(())` even though the committed artifact was at a different path. Fix: `extract_hook_plugin_name` now returns the full registry-relative path (`hook-plugins/sub/x.wasm`); git ls-files/ls-tree paths are stripped to registry-relative before diffing; mismatched paths now appear in `MISSING:`. T-039 is the control (Effectful-shell — uses a real git repo in tempdir): 30 hooks declared at `hook-plugins/sub/hNN.wasm` committed flat at `hook-plugins/hNN.wasm`; path-based diff catches all 30; asserts `Err` containing `MISSING: hook-plugins/sub/h00.wasm`. Prior deferral to **S-21.14** rescinded. |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | 2,400 |
| BC-4.16.001 v1.8 | 5,500 |
| ADR-031 §Decision 2+3 (§Rationale) | 2,200 |
| hooks-registry.toml (validate-factory-path-staging entry + siblings context) | 1,800 |
| resolvers-registry.toml | 300 |
| Crate source: lib.rs + main.rs + tests.rs (validate-factory-path-staging/) | 3,000 |
| bundle_orphan_check.rs (existing S-19.04/S-19.06 test file; T-012 extension, now 51 tests / ~5,555 lines at `c20cf2fe` — +1 net line from `1c93f499` (9 insertion / 8 deletion Windows TOML path portability fix, test-fixture construction only, T-026(b) — see Changelog v1.32); the pass-21 completion sweep that preceded it was a 12-insertion/12-deletion assertion-tightening pass with no net line delta) | 3,000 |
| registry.rs `#[cfg(test)]` module (mutation-audit `on_error` unit test; byte-unchanged, cite is a current-state-HEAD pointer, `c20cf2fe`) | 200 |
| Tool outputs (git ls-files, dispatcher log snippets) | 800 |
| **Total** | **~19,200** |
| Agent context window | 200K (Sonnet) |
| **Budget usage** | **~9.6%** |

## Tasks (MANDATORY)

### Half 1 — Restore the WASM artifact

1. [ ] Confirm crate builds: `cargo build -p validate-factory-path-staging --target wasm32-wasip1 --release`
2. [ ] Copy artifact: `cp target/wasm32-wasip1/release/validate-factory-path-staging.wasm plugins/vsdd-factory/hook-plugins/validate-factory-path-staging.wasm`
3. [ ] Force-track the binary (gitignored path): `git add -f plugins/vsdd-factory/hook-plugins/validate-factory-path-staging.wasm`
4. [ ] Verify AC-001: `git ls-files plugins/vsdd-factory/hook-plugins/validate-factory-path-staging.wasm` → non-empty output
5. [ ] Verify AC-002: `cargo build --release -p factory-dispatcher` (if not already built); run `CI_REQUIRE_ARTIFACTS=1 ./run-all.sh` or `CI_REQUIRE_ARTIFACTS=1 bats validate-factory-path-staging.bats`; assert all 25 previously WASM-skipping tests execute — zero skip lines for either artifact skip reason
6. [ ] Confirm AC-003 (T-001), AC-004 (T-004), AC-005 (T-002) execute without `_require_artifacts` skip after AC-001 fix
7. [ ] Run full bats suite; confirm all existing test vectors pass

### Half 2 — Per-name registry parity check (declared→tracked direction)

8. [ ] Introduce `extract_hook_plugin_name(registry_path: &Path, plugin_path: &str) -> Option<String>`
       and update `parse_plugin_refs()` to handle all TOML-legal variant forms before
       relying on it in T-012 (bundle_orphan_check.rs): `plugin="…"` (no spaces),
       `./hook-plugins/…` dotslash prefix (CurDir no-op in lex_norm), traversal-into
       (`hooks/../hook-plugins/…`), case-variant (`Hook-Plugins/…`), nested subdirectory
       (`hook-plugins/sub/…`); absolute paths (`/abs/hook-plugins/…`) correctly excluded
       by prefix assertion; 4-step algorithm via `Path::components()` + registry-parent join;
       returns registry-relative path `joined_parts[expected_depth..].join("/")`
9. [ ] Write failing Cargo test `test_S_21_09_ac006_T012_declared_set_subset_of_tracked_set()`
       in `crates/factory-dispatcher/tests/bundle_orphan_check.rs`:
       (a) registry-inventory assertion: enumerate `plugins/vsdd-factory/*.toml` (NOT
           `*-registry.toml` — underscore-named files e.g. `metrics_registry.toml` escape
           the hyphenated glob; T-024 proves this); assert result equals exactly
           `{hooks-registry.toml, resolvers-registry.toml}`; hard-fail if either expected
           file is missing or if the enumeration returns any unexpected file;
       (b) declared set from both registries via `parse_plugin_refs()` (hardened);
       (c) tracked set via `git_tracked_wasm_names()`;
       (d) assert `declared − tracked` is empty
10. [ ] Verify Red Gate: test fails before AC-001 fix (validate-factory-path-staging.wasm
        absent from tracked set, present in declared set)
11. [ ] After AC-001 fix, confirm Green Gate: T-012 (bundle_orphan_check.rs) passes
12. [ ] Verify AC-007: assert no additional declared-but-missing artifacts in audit
13. [ ] Run `cargo test --workspace --all-targets`; confirm all tests pass including T-009 and T-012 (bundle_orphan_check.rs)
14. [ ] Add a CHANGELOG entry under [Unreleased] > Fixed describing:
       "validate-factory-path-staging WASM artifact committed and tracked in git; per-name
       declared→tracked registry parity Cargo test (T-012 bundle_orphan_check.rs) added
       to prevent silent declared-but-missing artifact gaps; registry-inventory assertion
       added to self-invalidate on new registry additions"

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-21.01 (merged PR #759, `7bb0e797`, 2026-07-23) | Created `crates/hook-plugins/validate-factory-path-staging/` and registered the hooks-registry.toml entry. The WASM artifact build-and-commit step was not completed. | All E-21 WASM guards use `on_error = "continue"` (fail-open) per BC-4.16.001 Invariant 2 — never `on_error = "block"` for `^Bash$` guards. | The `plugins/vsdd-factory/hook-plugins/` directory is gitignored. `git add -f` is required to force-track WASM binaries in that path. |
| S-21.06 (draft; W3) | Establishes pattern for a second PreToolUse `^Bash$` guard. S-21.09 does NOT depend on S-21.06 — its scope is entirely orthogonal (artifact restore, not guard logic). | The `validate-factory-path-staging` registry entry already exists at `hooks-registry.toml`: do NOT add a second entry. | None relevant to S-21.09 |
| S-21.07 (draft; W4) | Seven-arm PostToolUse hook; new WASM crate. Pattern: new crate + `git add -f` for artifact. | New WASM crates targeting `wasm32-wasip1` use the workspace Rust toolchain; no per-crate rust-version override needed. | None relevant to S-21.09 |
| S-19.04 (merged) | Established `bundle_orphan_check.rs` in `crates/factory-dispatcher/tests/`: `parse_plugin_refs()`, `git_tracked_wasm_names()`, `collect_orphans_dual()`, `workspace_root()`, and tests T-006..T-010. T-009 is the standing tracked→declared orphan gate (zero tracked WASMs absent from both registries). T-008 (`collect_orphans_hooks_only` negative control) was strengthened during S-21.09 implementation with an explicit **NOT-orphan** assertion — `!orphans.contains(&"hooks-only.wasm".to_string())` — proving `hooks-only.wasm` (referenced by `HOOKS_REGISTRY_FIXTURE`) is correctly excluded from the orphan set; this kills mutant M22 (`"hook-plugins/{}"` → `"bogus/{}"` in `collect_orphans_hooks_only`, which would otherwise falsely orphan every hooks-declared WASM while T-008's original positive-only assertion stayed green). T-008 is not S-21.09-owned (it is an S-19.04 test in the S-21.09-extended file) but its strengthening is part of the `1c59a669` diff. **T-007** (`collect_orphans_dual` fixture-c control; also not S-21.09-owned) was tightened during the PR-review test-quality fix burst (`c9cccea9`): a prior version additionally re-derived `format!("ORPHAN: {}", name)` from `orphans` and asserted the derived string round-trips — tautological, since it can only restate whatever the preceding line already established. Removed; the sole, load-bearing check is now `assert_eq!(orphans, vec!["neither-registry.wasm".to_string()])`, an exact-set assertion against `collect_orphans_dual`'s real return value (AC-006 clause (d)'s `ORPHAN: <name>` panic-message format is built by T-007's *callers*, e.g. T-009/T-010, from this same return value — not by `collect_orphans_dual` itself). | Dual-registry orphan detection: `vsdd-context-resolvers.wasm` is in resolvers-registry, not hooks-registry; using both registries avoids false-positive. `git ls-files` (not `fs::read_dir`) ensures only committed artifacts are examined. | The declared→tracked direction (AC-006 T-012 bundle_orphan_check.rs) was NOT implemented in S-19.04 — T-009 covers only tracked→declared. T-012 adds the inverse. A single-direction assertion on a shared fixture (T-008 pre-S-21.09) can stay green under a mutation that inverts the classification predicate — pair every "X is classified correctly" assertion with a sibling "Y is NOT misclassified" assertion on a fixture entry that should land in the opposite class. |
| S-19.06 (merged) | Established T-011 in `bundle_orphan_check.rs`: POLICY 20 defense proof for `read-prefix-fixture.wasm`. Confirmed build-exclusion (`--exclude read-prefix-fixture` in release.yml) is the governing mechanism for hyphen-named WASMs that pass the staging glob. | Test naming convention: `test_S_NN_NN_acNNN_TYYY_<description>`. POLICY 20 covers tracked→declared orphan gaps in release bundles. AC-006 covers the inverse declared→tracked direction. | POLICY 20 and AC-006 are complementary, not overlapping. Do not conflate them. |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| validate-factory-path-staging guard scope is narrow: `git add`/`git stage` only; MUST NOT be extended to `git pull`/`git merge` | ADR-031 §Decision 2 Layer-1 scope boundary | Read the guard's `on_invocation` logic; confirm no `git pull` or `git merge` interception exists |
| New WASM crate is `crates/hook-plugins/validate-factory-path-staging/`; MUST NOT modify `crates/hook-plugins/validate-artifact-path/` (serves BC-4.11.001/S-13.01) | ADR-031 §Decision 3 naming authority | `git diff --name-only HEAD` must not include any path under `crates/hook-plugins/validate-artifact-path/` |
| `on_error = "continue"` is MANDATORY for this plugin (fail-open; a crashed guard never wedges the session) | BC-4.16.001 Invariant 2 + ADR-031 §Decision 2 §Rationale | `grep 'on_error' hooks-registry.toml` — verify the `validate-factory-path-staging` entry shows `on_error = "continue"`, not `"block"` |
| The hooks-registry.toml entry for `validate-factory-path-staging` already exists — DO NOT add a duplicate entry | hooks-registry.toml (entry verified present) | `grep -c 'validate-factory-path-staging' plugins/vsdd-factory/hooks-registry.toml` — expect 4 (comment × 2 + name entry + plugin entry) |
| `plugins/vsdd-factory/hook-plugins/` is gitignored — use `git add -f` to force-track the WASM binary | `.gitignore` | After `cp` step, confirm `git ls-files` shows the file after `git add -f` |
| AC-006 extends `bundle_orphan_check.rs` — MUST NOT create a new crate `crates/plugin-parity-check/` | S-19.04/S-19.06 existing machinery | `git diff --name-only HEAD` must not include any path under `crates/plugin-parity-check/` |
| POLICY 21 — no new `.sh` files | POLICY 21 (D-836 codified) | `git diff --name-only HEAD` output must contain no `.sh` filenames for this PR |
| T-012..T-039, T-041, T-049, T-052, T-053, and T-054 involve git operations; T-040/T-042..T-048/T-050/T-051/T-055/T-056 are Pure-core (no-git-op fixtures); T-038 is Pure-core (no-git fixture). Tests T-012..T-037 exercise both `git ls-files` (tracked-set query, step 3) and `git ls-tree -r --name-only HEAD` via `git_committed_wasm_names()` (committed-set query, step 4); T-038 tests `detect_ungated_declarations` before git calls (no git); T-041 tests the `git ls-files` failure arm (`#[should_panic]`); T-043..T-047 test containment predicate boundary conditions (no git operations); T-048 (pass-11) drives 18 fixture-only candidates through both functions without any git operation; T-049 (pass-11) is a git fixture that commits a WASM-free `hook-plugins/` directory to trigger the EC-005a `tracked_raw.is_empty()` guard; T-050 (pass-11 fix-burst) is a single fixture-only candidate isolating the containment predicate's length conjunct (no git operation); T-051 (pass-12 fix-burst) is a single fixture-only candidate isolating the containment predicate's prefix conjunct, orthogonal to T-050 (no git operation); T-052 (pass-14 fix-burst) is a full production-valid git fixture isolating the hooks `Registry::parse_str` production-validation block; T-053 (pass-14 fix-burst) is the same full production-valid git fixture shape isolating the resolvers `schema_version == 1` assert block, orthogonal to T-052; T-054 (mutation-audit hardening burst) is the same full production-valid git fixture shape as T-052/T-053 with `resolvers-registry.toml` omitting `schema_version` entirely, isolating the `.unwrap_or(-1)` fail-closed sentinel; T-055/T-056 (mutation-audit hardening burst) call `detect_ungated_declarations`/`lex_norm` directly with no fixture and no git operations, pinning otherwise-unreachable dead-arm contracts; the two-query design is authorised by S-21.09 | AC-006 steps 3–4 (S-21.09) | Confirm `declared − tracked` uses `git ls-files` and `tracked − committed` uses `git ls-tree -r --name-only HEAD`; the two-step distinction is intentional and tested by T-021, T-036, and T-037; T-041 confirms git failure is loudly surfaced; T-049 confirms the same failure-surfacing discipline applies to the zero-WASM-tracked edge case (EC-005a); T-052/T-053/T-054 confirm the production-validation gate's three determinants (hooks parse, resolvers assert, resolvers sentinel) are each isolated; T-055/T-056 confirm the two dead-code defensive arms have pinned contracts even though unreachable via `run_t012_gate` |
| The mutation-audit accepted-residual (SURV-01, `lex_norm`'s `RootDir \| Prefix(_) => parts.clear()` arm) MUST remain undocumented by a test — a test asserting non-behavior on a provably-unreachable-with-non-empty-`parts` arm would itself be vacuous; the arm's un-isolatability is recorded via a strengthened doc comment instead | mutation-audit hardening burst (`c20cf2fe`), §AC-006 Mutation-Completeness Audit | `git diff --name-only HEAD` for the mutation-hardening burst must show a doc-comment-only change at the `lex_norm` `RootDir \| Prefix(_)` arm, no new `#[test]` function targeting it |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| Rust toolchain (wasm32-wasip1 target) | Pinned via workspace `rust-toolchain.toml` | Building `validate-factory-path-staging.wasm` for wasm32-wasip1 target |
| hook-sdk crate | workspace member (crates/hook-sdk/) | WASM plugin host ABI; `exec_subprocess` for branch detection |
| toml crate (Rust) | workspace version | IS used: `parse_plugin_refs()` calls `content.parse::<toml::Value>()` and iterates `doc.get("hooks")`/`doc.get("resolvers")` — the same parser as `registry.rs`. `crates/factory-dispatcher/Cargo.toml` already carried `toml.workspace = true` so no manifest change was required; "line-matching" is no longer the mechanism anywhere in the file. |
| std::path::Path | Rust stdlib | `extract_hook_plugin_name(registry_path: &Path, plugin_path: &str) -> Option<String>`: (1) `registry_parent.join(plugin_path)`; (2) lex_norm (extracted to module level; shared by `extract_hook_plugin_name` and `detect_ungated_declarations`) via `Path::components()` — push `Normal`, pop on `ParentDir`, skip `CurDir`; `RootDir \| Prefix(_)` clears accumulated parts (fourth provably-unreachable arm — prior guards prevent reaching it); (3) three reachable sequential exclusion gates (function also contains four defensive unreachable exits that serve as structural safety nets but are unreachable given the prior guards) — (a) minimum-length: `len < expected_depth + 2` → `None`; (b) prefix assertion: normalised path must carry registry-parent prefix → `None`; (c) `eq_ignore_ascii_case("hook-plugins")` component match → `None` if absent; case-variant forms admitted verbatim — a mis-cased `hook-plugins` component causes MISSING: on case-sensitive CI Linux (by design); (4) return registry-relative path (`joined_parts[expected_depth..].join("/")`) — e.g., `hook-plugins/foo.wasm` or `hook-plugins/sub/nested.wasm`; case-variant forms returned verbatim |
| std::process::Command (stdlib) | Rust stdlib | Already used in `bundle_orphan_check.rs` via `git_tracked_wasm_names()` (git ls-files); also used by the new `git ls-tree --name-only HEAD` query in step 4 |
| `factory_dispatcher::Registry` (workspace crate, pub API) | workspace version | pass-11 (HIGH-3): `Registry::parse_str(&hooks_content)` is invoked by `run_t012_gate` before the declared set is parsed — the SAME production parser/validator the dispatcher uses at runtime (`schema_version == 2`, `deny_unknown_fields`, tool-pattern regex validity). Guards against T-012's gate becoming meaningless by validating against a registry production would reject outright. Imported via `use factory_dispatcher::Registry;`. |

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| `plugins/vsdd-factory/hook-plugins/validate-factory-path-staging.wasm` | create (force-tracked via `git add -f`) | The missing WASM artifact; built from `crates/hook-plugins/validate-factory-path-staging/` targeting `wasm32-wasip1` |
| `crates/factory-dispatcher/tests/bundle_orphan_check.rs` | modify (extend) | Adds T-012..T-056 (T-050 added in the pass-11 fix-burst as a dedicated length-conjunct isolation control for mutant M2, closing adversary finding F-1; T-051 added in the pass-12 fix-burst as the orthogonal dedicated prefix-conjunct isolation control for mutants `.all(...)` → `.any(...)` and `.all(...)` → `true`, closing adversary finding F-1 against the pass-12 dispatch — POLICY 13 mutation-completeness; T-032's underlying test function renamed `..._yields_filename` → `..._yields_full_path`, numeric ID preserved; T-023/T-047/T-048 module-header rows and assertion messages corrected to remove false M2-kill claims, closing pass-11 F-2; T-052/T-053 added in the pass-14 fix-burst as dedicated isolation controls for the hooks `Registry::parse_str` production-validation block and the resolvers `schema_version == 1` assert block in `run_t012_gate` respectively, closing adversary pass-14 MEDIUM; T-054/T-055/T-056 added in the mutation-audit hardening burst as dedicated isolation controls closing the exhaustive mutation audit's three killable survivors SURV-04/SURV-03/SURV-02 respectively — no production code changed by any of the three, all test-only mutation-completeness controls; the fourth killable survivor SURV-05 is closed in `registry.rs`, not this file; the fifth survivor SURV-01 is an accepted-residual, closed via a strengthened doc comment on `lex_norm`, not a new test); new helpers: `extract_hook_plugin_name(registry_path: &Path, plugin_path: &str) -> Option<String>` (four-step normalisation: join against registry parent, lex_norm [module-level, shared with `detect_ungated_declarations`; `RootDir \| Prefix(_)` is fourth unreachable arm], three reachable exclusion gates + four defensive unreachable exits, return registry-relative path `joined_parts[expected_depth..].join("/")`; case-variant forms returned verbatim; replaces `trim_start_matches("./").strip_prefix("hook-plugins/")`), `run_t012_gate(root: &Path) -> Result<(), String>` (extracted gate body for T-030 wiring control; strips `plugins/vsdd-factory/` prefix from git paths before diffing; pass-11: also invokes `Registry::parse_str` production-validation on hooks-registry.toml and a `schema_version == 1` check on resolvers-registry.toml before parsing the declared set), `detect_ungated_declarations(...)` (fires before git calls in `run_t012_gate`; worktree-root containment predicate for in-repo/outside-repo classification; pass-11: delegates the gated-vs-ungated determination for in-repo declarations to `extract_hook_plugin_name` — single-copy refactor, no duplicated three-gate logic; emits `UNGATED-DECLARATION: <path>` for in-repo non-`hook-plugins` declarations, `OUTSIDE-REPO-DECLARATION: <path>` for above-root declarations; T-038/T-040/T-043..T-048/T-050/T-051/T-055; no production code changed by T-051 or T-055 — both are test-only mutation-completeness controls); inherited helpers `collect_orphans_dual` / `collect_orphans_hooks_only` have updated internal comparison format to registry-relative paths (T-006..T-011 assertions unchanged; T-008 gained a pass-11 NOT-orphan negative assertion, see Previous Story Intelligence); pass-11 adds `use factory_dispatcher::Registry;` import; **PR-review test-quality fix burst (`c9cccea9`, REOPENS the LOCAL 3-CLEAN cascade — see Changelog v1.29):** tightened 6 vacuous/tautological assertions (T-007, T-015, T-026(a), T-026(b), T-048, T-054, T-055 — see their individual Red Gate Test Plan rows and AC-006 Tests-bullet descriptions for the corrected narratives) with no test-ID/count/range change (still 51 tests, T-006..T-056, 45 S-21.09-owned); also extracts four new git-fixture bootstrap helpers — `git_init_fixture(root: &Path, label: &str)`, `git_add_all_fixture(root: &Path, label: &str)`, `git_commit_fixture(root: &Path, label: &str, message: &str)` (all three: `git -c core.excludesFile=/dev/null -c commit.gpgsign=false -c core.hooksPath=/dev/null -c init.templateDir= <init|add .|commit -m <message>>`, panicking with a `label`-prefixed message on non-zero exit), and `fixture_body_without_comments(fixture: &str) -> String` (strips `#`-prefixed lines, shared by T-013/T-014's HIGH-1 fixture-content assertions) — removing ~600 lines of byte-for-byte duplicated git-init/add/commit boilerplate across the 9 git-backed T-0NN fixtures (T-030 phase B, T-034, T-036, T-037, T-039, T-049, T-052, T-053, T-054) and T-013/T-014; net file size drops from 5,803 to 5,485 lines (~318-line reduction) with the 304 insertion / 622 deletion diff at `c9cccea9`; no test semantics changed by the dedup itself; **pass-20 format-lock burst (`fc0e613b`), sibling sweep closing adversary finding F-S2109-LOCAL-FMTLOCK-01 — see Changelog v1.30:** locks the two-space indent across all five indented production format strings (`check_registry_inventory`'s `"  UNEXPECTED: {}"` and `"  MISSING: {}"`; `check_declared_subset_tracked`'s `"  MISSING: {}"` and `"  STAGED-NOT-COMMITTED: {}"`, already locked at `c9cccea9`, unchanged this burst; `run_t012_gate`'s ungated-findings `format!("  {}", p)` wrapper) by tightening the positive assertions in T-017/T-018/T-024/T-030(phase A)/T-038/T-043/T-044/T-045/T-046/T-047/T-050/T-051 from unindented substrings to the exact indented production form; net file size rises from 5,485 to 5,554 lines (~69-line increase, primarily added mutation-rationale doc comments) with a 97 insertion / 28 deletion diff at `fc0e613b`; no test-ID/count/range change (still 51 tests, T-006..T-056, 45 S-21.09-owned); no production code changed; all pre-existing negative-identifier assertions (T-043/T-047/T-050/T-051) preserved intact; MUST FAIL pre-fix; MUST PASS after AC-001; **pass-21 completion sweep (`1c93f499`), F-S2109-LOCAL-P?-001 (LOW) — COMPLETES the format-lock sweep, see Changelog v1.31:** tightens the six remaining sibling call sites the pass-20 burst left unindented against `check_declared_subset_tracked`'s already-locked `"  MISSING: {}"` / `"  STAGED-NOT-COMMITTED: {}"` emitters and `run_t012_gate`'s ungated wrapper — T-030 phase B, T-036, T-037, T-039, T-040, T-042 — from unindented substrings to the exact indented production form; a mandatory completeness grep post-fix confirms zero remaining unindented positive needles against any indented production emitter; net file size unchanged at 5,554 lines (12 insertion / 12 deletion diff at `1c93f499`, two lines per tightened test); no test-ID/count/range change (still 51 tests, T-006..T-056, 45 S-21.09-owned); no production code changed; pre-existing negative-identifier assertions untouched (these six tests carry none); **Windows CI portability fix (`c20cf2fe`), caught by windows-x64 CI after the D-988 re-convergence — see Changelog v1.32:** `build-dispatcher (windows-x64)` CI failed on the D-988-converged head (`1c93f499`) — `test_S_21_09_ac006_T026_absolute_form_excluded_from_declared` sub-test (b), the depth-matched absolute-path control building `registry-depth.toml`, constructed its `plugin = "..."` value via `PathBuf::push` + `.to_str()`, which renders the OS-native separator (`\` on Windows), an invalid TOML escape that panicked `Registry::parse_str` on the fixture. A full audit of all ~90 TOML-path-building sites in the file confirmed T-026(b) was the SOLE non-portable site; fixed by building the path via explicit forward-slash string concatenation (registry paths are forward-slash by convention on all platforms). Semantically identical on macOS/Linux (still classifies OUTSIDE / `refs_depth.is_empty()`); now valid on Windows too. Net file size rises from 5,554 to 5,555 lines (9 insertion / 8 deletion diff at `c20cf2fe`); no test-ID/count/range change (still 51 tests, T-006..T-056, 45 S-21.09-owned); test-fixture-construction only — the D-988 re-convergence (assertion quality / mutation completeness confirmed at `1c93f499`) is PRESERVED, not reopened, by this fix |
| `crates/factory-dispatcher/src/registry.rs` | modify (extend `#[cfg(test)]` module only; no production code changed) | Adds `on_error_falls_back_to_registry_defaults_when_entry_omits_it`, the mutation-audit hardening burst's SURV-05 control: isolates `RegistryEntry::on_error`'s defaults-fallback body from a `Default::default()` body-replacement mutant that survived because every prior fixture's expected `on_error` value happened to coincide with `OnError`'s own `#[default]` variant (`Continue`); this test pairs an omitted entry-level `on_error` with a `RegistryDefaults` explicitly set to `OnError::Block` (the non-default variant) so the live accessor and the mutant diverge observably; out-of-gate production accessor, not part of the T-NNN `run_t012_gate` sequence |
| `CHANGELOG.md` | modify (add entry under [Unreleased]) | Mandatory CHANGELOG delivery per story template BC requirement |
| `plugins/vsdd-factory/hooks-registry.toml` | read-only (NO modifications) | The plugin entry already exists; this story only adds the binary artifact |
| `plugins/vsdd-factory/resolvers-registry.toml` | read-only | Input to the parity check |
| `crates/hook-plugins/validate-factory-path-staging/` | read-only (source exists from S-21.01) | Build source for the WASM artifact |
| `crates/factory-dispatcher/tests/fixtures/bundle-orphan/hooks-registry-nospace-fixture.toml` | create | Fixture for T-013 nospace parse-hardening control; contains `plugin="hook-plugins/…"` form entries (no spaces on either side of `=`) |
| `crates/factory-dispatcher/tests/fixtures/bundle-orphan/hooks-registry-dotslash-fixture.toml` | create | Fixture for T-014 dotslash parse-hardening control; contains `plugin = "./hook-plugins/…"` form entries |

## Routing Proposals (surfaced, not authored per instructions)

### POLICY 20 clarification

POLICY 20 (`release_bundle_no_dev_samples`, registered at D-753, HIGH severity) IS
present in `.factory/policies.yaml` at `id: 20`. Its scope is `[release, bundle]`;
it covers dual-registry orphan detection for release bundles — the
**tracked→declared** direction (a tracked WASM absent from both registries = orphan).

The prior story draft v1.0 falsely claimed POLICY 20 was absent from policies.yaml. That
claim is incorrect and has been removed. The "count-based parity check masked by a 1-for-1
swap (POLICY 20)" narrative has also been removed — the actual masking mechanism is the
ci.yml "Stage WASM plugins for run-all.sh" build step combined with `_require_artifacts`
skip-passing on absent `CI_REQUIRE_ARTIFACTS` (see §Context).

AC-006's T-012 (bundle_orphan_check.rs) covers the **declared→tracked** direction (complementary to POLICY 20's
tracked→declared scope). No POLICY 20 amendment is needed. This conclusion holds under the corrected understanding
of `release.yml`'s structure: `build-binaries`'s "Verify registry-declared WASM plugins" step checks
`hooks-registry.toml` only; `commit-binaries`'s step checks both registries via the same weak grep predicate.
These implementation details of the release pipeline's verification layer are independent of POLICY 20's governance
scope, which is enforced by the `bundle_orphan_check.rs` T-009 Rust gate. The weak predicate in the release pipeline
shell steps is a separate concern documented in §Sibling weak-predicate sites below.

### VP allocation (owed — route to state-manager)

`verification_properties: []` in story frontmatter. All 4 VP rows in
`BC-4.16.001 §Verification Properties` read
"(TBD — to be assigned by state-manager after VP authoring pass)". No VP-NNN IDs are
invented in this elaboration pass. This gap requires a VP authoring pass — proposed
routing: state-manager / VP-authoring dispatch after this story is accepted and merged.

### New BC routing (optional)

BC-4.16.001 Precondition 3 covers the restore of the artifact (its existence is a
precondition for the dispatcher to invoke the plugin). The parity check ACs are traced
to BC-4.16.001 Precondition 3 as the indirect anchor (the check exists to guarantee
Precondition 3 is satisfiable at build time).

If a dedicated BC for "registry-declared WASM artifacts must all be tracked in git" is
desired (e.g., a new clause in BC-4.16.001), proposed routing: product-owner via standard
BC authoring dispatch. This is optional — the AC traces to BC-4.16.001 Precondition 3
suffice for Spec-First Gate purposes.

### Sibling weak-predicate sites in release.yml and pr-manager-hardening.bats (defer to follow-up story)

The same weak parse predicate (`grep -E '^\s*plugin\s*=\s*"hook-plugins/'`) that
S-21.09's BLOCKER-1 hardens in `bundle_orphan_check.rs` (`parse_plugin_refs` hardening)
survives at five sibling sites outside S-21.09's scope:

1. `.github/workflows/release.yml` — `build-binaries` FIRST "Verify registry-declared WASM
   plugins are staged" step (hooks-registry.toml parse; has `-lt 30` non-vacuity floor)
2. `.github/workflows/release.yml` — `commit-binaries` SECOND "Verify registry-declared WASM
   plugins are staged" step (hooks-registry.toml parse; has `-lt 30` non-vacuity floor)
3. `.github/workflows/release.yml` — `commit-binaries` SECOND "Verify registry-declared WASM
   plugins are staged" step (resolvers-registry.toml parse; `2>/dev/null`; **no** non-vacuity floor)
4. `plugins/vsdd-factory/tests/pr-manager-hardening.bats` — fixture-builder occurrence
5. `plugins/vsdd-factory/tests/pr-manager-hardening.bats` — T-017 mapfile negative control

All five use the same predicate that misses: (a) `./hook-plugins/…` alternate path prefix,
and (b) `plugin="…"` no-space TOML-legal form — the same false-negative polarity as
BLOCKER-1 in `bundle_orphan_check.rs`.

**Two additional findings beyond the weak predicate:**

- **Vacuous-pass on resolver arm.** In `commit-binaries`' Verify step (site 3 above),
  the `-lt 30` non-vacuity floor guards `hooks_plugins` ONLY. The `resolvers-registry.toml`
  read is suppressed with `2>/dev/null`; if the registry file is absent, renamed, or its
  `plugin =` lines change shape, `resolver_plugins` becomes empty and the resolver half
  passes vacuously — a zero result is indistinguishable from a correct empty parse and no
  error is emitted.

- **T-017 first-match-only extractor.** In `pr-manager-hardening.bats`, T-017 extracts
  the "Verify registry-declared WASM plugins are staged" step using
  `awk '... && !found { found=1 }'`. The `!found` flag pins extraction to the FIRST
  occurrence — i.e., `build-binaries`' Verify step (hooks-registry.toml only). The
  `commit-binaries` Verify step (the dual-registry one with the resolver arm) is not
  extracted or exercised by T-017. A predicate regression reintroduced in that step would
  pass the bats suite.

Additionally, the `>= 30` floor constant (guard against vacuous pass on empty parse) is
duplicated at three sites with no shared source: `release.yml` ×2 (one per Verify step)
and `bundle_orphan_check.rs` ×1 — a future declared-plugin-count change requires
synchronized updates in all three.

**POLICY 5 sibling-sweep disposition:** Per the human's standing ruling to keep S-21.09
narrow (WASM artifact restore + Rust parity test only), these five sites are not remediated
within S-21.09. Deferred to **S-21.14** (release-pipeline predicate + gate sweep) to address
all three concerns: (1) harden the five weak-predicate sites (matching the Rust implementation),
(2) add a non-vacuity floor for the resolver arm in `commit-binaries`' Verify step,
and (3) fix T-017's first-match-only extractor to also cover `commit-binaries`' dual-registry
Verify step.

## Changelog

| Version | Date | Description |
|---------|------|-------------|
| 1.32 | 2026-08-13 | **Windows TOML path portability fix — cross-platform CI catch post-D-988-re-convergence.** Implementation advanced from `1c93f499` to `c20cf2fe`. `build-dispatcher (windows-x64)` CI failed on the D-988-converged head: `test_S_21_09_ac006_T026_absolute_form_excluded_from_declared` sub-test (b), the depth-matched absolute-path control building `registry-depth.toml`, constructed its `plugin = "..."` value via `PathBuf::push` + `.to_str()`, which renders the OS-native path separator — on Windows that's `\`, an invalid TOML escape sequence — panicking `Registry::parse_str` on the fixture. A full audit of all ~90 TOML-path-building sites in the file confirmed T-026(b) was the SOLE non-portable site. Fixed by building the path with explicit forward-slash string concatenation (registry paths are forward-slash by convention on all platforms). Semantically identical on macOS/Linux (still classifies OUTSIDE / `refs_depth.is_empty()`); now valid on Windows too. 51 green on macOS; fmt/clippy/workspace clean. **Cross-platform note:** cross-platform CI (windows-x64) caught a non-portable TOML fixture path that the all-macOS LOCAL cascade and mutation audit could not; fixed in `c20cf2fe`. The D-988 re-convergence (assertion quality / mutation completeness confirmed at `1c93f499`) is PRESERVED — this is a fixture-construction portability change with identical runtime semantics, not an assertion/logic/coverage change. **Story-side corrections (this burst):** the T-026 Red Gate Test Plan row and its AC-006 Tests-bullet prose counterpart updated to document the forward-slash portability fix (test-fixture construction only; runtime classification unchanged); the `bundle_orphan_check.rs` File Structure Requirements row extended to document the fix. **Sweep (POLICY 5 / TD-VSDD-060):** all 5 current-state commit cites updated from `1c93f499` to `c20cf2fe` — the AC-006 Tests bullet SHA line, the Mutation-Completeness Audit subsection heading, the Architecture Compliance Rules SURV-01 accepted-residual row, and the two Token Budget Estimate rows (`bundle_orphan_check.rs` line count 5,554→5,555, a +1 net line from the 9 insertion / 8 deletion Windows-portability diff; token estimate unchanged at 3,000; `registry.rs` byte-unchanged, cite is a current-state-HEAD pointer per the pattern established at v1.27–v1.31; Token Budget Total and usage percentage unchanged, ~19,200/~9.6%, for the same reason). Historical SHA/count citations in Changelog rows below (including the v1.31 row's own `1c93f499` cites and all "pass-21 completion sweep (`1c93f499`)" narrative prose describing that specific burst) and the legitimately-historical S-19.04 Previous Story Intelligence `1c59a669` citation are preserved as historical and not rewritten. Counts unchanged: 51 tests T-006..T-056, 45 S-21.09-owned, plus 1 registry.rs unit test. Points unchanged at 16 — cross-platform portability fix within existing AC-006 scope, not new AC scope; no test IDs, count, or logic changed. Status remains ready (BC-anchored, AC↔BC traces intact). |
| 1.31 | 2026-08-12 | **Pass-21 (LOCAL) completion sweep — format-lock sweep now COMPLETE.** Implementation advanced from `fc0e613b` to `1c93f499`. **Finding F-S2109-LOCAL-P?-001 (LOW):** despite the pass-20 burst's (v1.30) own §Scope-honesty framing claiming the two-space-indent lock had been applied "across all five indented production format strings," it left SIX sibling positive assertions unindented against those same already-locked `check_declared_subset_tracked` emitters (`"  MISSING: {}"` / `"  STAGED-NOT-COMMITTED: {}"`) and the `run_t012_gate` ungated wrapper (`format!("  {}", p)`) — T-030 phase B (only phase A was locked at pass-20), T-036, T-037, T-039, T-040, and T-042 — leaving an indent-dropping mutation on any of those six call sites able to survive the full T-006..T-056 suite undetected. This burst (`1c93f499`) tightens all six from unindented substrings to the exact two-space-indented production form, each empirically verified RED under an indent-drop mutant applied locally to its respective production format string, then reverted. A mandatory completeness grep (`.contains("(MISSING\|STAGED-NOT-COMMITTED\|UNGATED-DECLARATION\|OUTSIDE-REPO-DECLARATION\|UNEXPECTED)` needles not prefixed by two spaces, excluding `!err.contains(...)` negative-identifier assertions and comments) run against the post-fix test file confirms **zero remaining unindented positive needles** against any indented production emitter — the format-lock sweep is now COMPLETE. No production code changed; the suite remains 51 tests T-006..T-056 (no IDs added/removed; 45 S-21.09-owned); net file size unchanged at 5,554 lines (12 insertion / 12 deletion diff — two lines per tightened test: the `.contains(...)` needle and its accompanying panic-message literal). Pre-existing negative-identifier assertions (T-043/T-047/T-050/T-051) untouched — none of the six tightened tests carries a negative-identifier sibling. **Story-side corrections (this burst):** the T-030, T-036, T-037, T-039, T-040, T-042 Red Gate Test Plan rows and their AC-006 Tests-bullet prose counterparts updated to document the completion and cite the pass-21 fix; a new pass-21 paragraph added to the Mutation-Completeness Audit subsection recording the completion and the completeness-grep evidence; the `bundle_orphan_check.rs` File Structure Requirements row extended to document the completion sweep. **Sweep (POLICY 5 / TD-VSDD-060):** all 5 current-state commit cites updated from `fc0e613b` to `1c93f499` — the AC-006 Tests bullet SHA line, the Mutation-Completeness Audit subsection heading, the Architecture Compliance Rules SURV-01 accepted-residual row, and the two Token Budget Estimate rows (`bundle_orphan_check.rs` line count unchanged at 5,554 — this burst is a pure assertion-content tightening pass with no net line delta, unlike pass-20's added rationale comments; token estimate unchanged at 3,000; `registry.rs` byte-unchanged, cite is a current-state-HEAD pointer per the pattern established at v1.27–v1.30; Token Budget Total and usage percentage unchanged, ~19,200/~9.6%, for the same reason). Historical SHA/count citations in Changelog rows below (including the v1.30 row's own `fc0e613b` cites) and the legitimately-historical S-19.04 Previous Story Intelligence `1c59a669` citation are preserved as historical and not rewritten. Counts unchanged: 51 tests T-006..T-056, 45 S-21.09-owned, plus 1 registry.rs unit test. Points unchanged at 16 — assertion-tightening completion pass within existing AC-006 scope, not new AC scope; no test IDs, count, or logic changed. Status remains ready (BC-anchored, AC↔BC traces intact); LOCAL 3-CLEAN streak remains 0/3 per the v1.29 reopening — this burst is a fix landing within that still-open cascade, not a clean adversary pass. |
| 1.30 | 2026-08-12 | **Pass-20 (LOCAL) mutation-completeness fix burst — format-lock sibling sweep.** Implementation advanced from `c9cccea9` to `fc0e613b`. **Finding F-S2109-LOCAL-FMTLOCK-01:** the v1.29/`c9cccea9` PR-review fix burst locked the two-space indent on `check_declared_subset_tracked`'s `"  MISSING: {}"` and `"  STAGED-NOT-COMMITTED: {}"` format strings (T-015, T-021) but did NOT sweep the identical fix to the sibling indented emitters — `check_registry_inventory`'s `"  UNEXPECTED: {}"` and `"  MISSING: {}"` format strings, and `run_t012_gate`'s ungated-findings `format!("  {}", p)` wrapper — leaving an indent-dropping mutation on any of those three sites able to survive the full T-006..T-056 suite undetected, since the corresponding tests (T-017, T-018, T-024, T-030 phase A, T-038, T-043, T-044, T-045, T-046, T-047, T-050, T-051) asserted only unindented substrings. This burst (`fc0e613b`) locks the two-space indent across all five indented production format strings — the two `check_registry_inventory` emitters (T-017/T-024/T-030 phase A → `"  UNEXPECTED: {}"`; T-018 → both `"  MISSING: {}"` arms), the two pre-existing `check_declared_subset_tracked` emitters (T-015/T-021, already locked at `c9cccea9`, unchanged this burst), and the `run_t012_gate` ungated/outside wrapper (T-038/T-043/T-044/T-045/T-046/T-047/T-050/T-051 → `format!("  {}", p)`). Each of the twelve tightened positive assertions was empirically verified RED under an indent-drop mutant applied locally to its respective production format string, then reverted. The pre-existing negative-identifier assertions on T-043/T-047/T-050/T-051 (`!err.contains("OUTSIDE-REPO-DECLARATION: ")` / `!err.contains("UNGATED-DECLARATION: ")` sibling checks) were preserved intact — this burst tightened only the positive match on each test. No production code changed; the suite remains 51 tests T-006..T-056 (no IDs added/removed; 45 S-21.09-owned). **Scope-honesty note:** the Mutation-Completeness Audit subsection (§AC-006) previously covered only `run_t012_gate`/`detect_ungated_declarations`/`lex_norm` plus `registry.rs` — `check_registry_inventory`'s leading-indent behavior was never in the audited 68-mutation enumeration for either the mutation-hardening burst or the PR-review fix burst; a new paragraph added to that subsection states this honestly rather than retroactively inflating the historical mutation count. **Story-side corrections (this burst):** the T-017, T-018, T-024, T-030, T-038, T-043, T-044, T-045, T-046, T-047, T-050, T-051 Red Gate Test Plan rows and their AC-006 Tests-bullet prose counterparts updated to document the indent-lock and cite the pass-20 fix; a new pass-20 paragraph added to the Mutation-Completeness Audit subsection; the `bundle_orphan_check.rs` File Structure Requirements row extended to document the sibling sweep. **Sweep (POLICY 5 / TD-VSDD-060):** all 5 current-state commit cites updated from `c9cccea9` to `fc0e613b` — the AC-006 Tests bullet SHA line, the Mutation-Completeness Audit subsection heading, the Architecture Compliance Rules SURV-01 accepted-residual row, and the two Token Budget Estimate rows (`bundle_orphan_check.rs` line count 5,485→5,554, a ~69-line increase from added mutation-rationale doc comments across the twelve tightened assertions, token estimate unchanged at 3,000 — the ~1.3% line-count delta does not move the rounded estimate; `registry.rs` byte-unchanged, cite is a current-state-HEAD pointer per the pattern established at v1.27/v1.28/v1.29; Token Budget Total and usage percentage unchanged, ~19,200/~9.6%, for the same reason). Historical SHA/count citations in Changelog rows below (including the v1.29 row's own `c9cccea9` cites) and the legitimately-historical S-19.04 Previous Story Intelligence `1c59a669` citation are preserved as historical and not rewritten. Counts unchanged: 51 tests T-006..T-056, 45 S-21.09-owned, plus 1 registry.rs unit test. Points unchanged at 16 — mutation-completeness sibling-sweep pass within existing AC-006 scope, not new AC scope; no production code changed. Status remains ready (BC-anchored, AC↔BC traces intact); LOCAL 3-CLEAN streak remains 0/3 per the v1.29 reopening — this burst is a fix landing within that still-open cascade, not a clean adversary pass. |
| 1.29 | 2026-08-12 | **PR-review test-quality fix burst REOPENS the LOCAL adversary cascade.** Implementation advanced from `12d0fe98` to `c9cccea9` (via the intervening non-test-affecting demo-evidence commit `6ae075a6`). **Convergence status:** the LOCAL 3-CLEAN convergence recorded by the prior D-982 codification was scoped to the adversary's own review rubric and did not include a vacuity/tautology lens over test assertions; a subsequent PR-review pass applied that lens and found an 8-weak-assertion + 1-MAJOR-duplication finding set (F1-F8: 2 tautological, 3 vacuous, 3 fixture/assertion-mismatch) that the prior mutation-completeness audit and LOCAL adversary cascade both missed. All 8 findings plus the MAJOR duplication are fixed and empirically re-verified in `c9cccea9` (each claimed mutation was applied locally, the specific test confirmed RED, then reverted); the suite remains 51 tests T-006..T-056 (no IDs added/removed; 45 S-21.09-owned), all green. **Per D-970/POLICY-13 discipline, this reopens the LOCAL 3-CLEAN streak to 0/3** — the cascade must re-accumulate three consecutive clean passes from this state; the story's `status: ready` is retained (no AC scope change, no production code changed) but downstream dispatch must treat convergence as NOT settled. **F1 (tautological) — T-007:** removed a self-built `ORPHAN: <name>` re-derivation that could only restate the line above it; tightened to `assert_eq!(orphans, vec!["neither-registry.wasm"])` against `collect_orphans_dual`'s real return value. **F2 (tautological) — T-048:** collapsed the paired `extract_hook_plugin_name()`/`detect_ungated_declarations()` assertions (tautological — `detect` calls `extract(...).is_none()` internally) into a single exact-vec-equality check against `detect_ungated_declarations`'s own output; corrected a mis-attributed M4 mutation-kill narrative that had credited a GATED-branch assertion for the `"hook-plugins"` case's catch — the catch is on the UNGATED-DECLARATION row's exact-match assertion (empirically re-confirmed: `+2→+1` → RED on the `hook-plugins` row). **F3/F4 (vacuous) — T-026(a)/(b):** removed vacuous bare-basename `!refs.contains(...)` / `!refs_depth.contains(...)` checks — a gated absolute path returns the FULL `hook-plugins/<name>` form, never a bare basename; `refs.is_empty()`/`refs_depth.is_empty()` are the sole, load-bearing checks. The prior T-026(b) narrative claiming the gate-2-prefix-loop-deletion mutant admits bare `evil.wasm` was WRONG — it admits `hook-plugins/evil.wasm`; corrected (empirically confirmed). **F5 (vacuous) — T-055:** added a positive control (valid TOML with a genuine ungated declaration → non-empty result) closing the whole-function-`Vec::new()` blind spot the malformed-TOML-only assertion could not distinguish. **F6 (fixture/assertion mismatch) — T-054:** tightened `#[should_panic(expected = ...)]` from `"but production requires 1"` to `"schema_version=-1 but production requires 1"`, pinning the `-1` fail-closed sentinel value itself. **F7 (fixture/assertion mismatch) — T-015:** assertion tightened to the two-space-indented `"  MISSING: hook-plugins/hooks-only.wasm"` form, locking the exact production format (the prior unindented-substring check could never catch an indent-dropping mutation). **F8 (fixture/assertion mismatch, MAJOR duplication) — git-fixture dedup:** extracted `git_init_fixture`/`git_add_all_fixture`/`git_commit_fixture` (shared `git init`/`add .`/`commit -m` bootstrap with global-config-isolating `-c` overrides) and `fixture_body_without_comments` (shared comment-stripping helper for T-013/T-014) across the 9 git-backed T-0NN fixtures (T-030 phase B, T-034, T-036, T-037, T-039, T-049, T-052, T-053, T-054) and T-013/T-014, removing ~600 lines of byte-for-byte duplicated boilerplate; no test semantics changed. **Story-side corrections (this burst):** the T-015, T-026, T-048, T-054, T-055 Red Gate Test Plan rows and their AC-006 Tests-bullet counterparts corrected to match the fixed code (see individual row diffs); the S-19.04 Previous Story Intelligence row extended with the T-007 fix (T-007 is not S-21.09-owned; correction included for completeness since the row already documents T-008's sibling strengthening); the `bundle_orphan_check.rs` File Structure Requirements row extended to document the four new fixture helpers and the PR-review fix burst. **Sweep (POLICY 5 / TD-VSDD-060):** all 5 current-state commit cites updated from `12d0fe98` to `c9cccea9` — the AC-006 Tests bullet SHA line, the Mutation-Completeness Audit subsection heading, the Architecture Compliance Rules SURV-01 accepted-residual row, and the two Token Budget Estimate rows (`bundle_orphan_check.rs` line count 5,803→5,485, a ~318-line reduction from the git-fixture dedup, token estimate 3,100→3,000; `registry.rs` byte-unchanged, cite is a current-state-HEAD pointer per the pattern established at v1.27/v1.28; Token Budget Total ~19,300→~19,200, usage ~9.7%→~9.6%). No occurrences of `6ae075a6` required correction (that commit touched only demo evidence, not the test file, and was never cited in this story). Historical SHA/count citations in Changelog rows below (including the v1.28 row's own `12d0fe98` cites) and the legitimately-historical S-19.04 Previous Story Intelligence `1c59a669` citation are preserved as historical and not rewritten. Counts unchanged: 51 tests T-006..T-056, 45 S-21.09-owned, plus 1 registry.rs unit test. Points unchanged at 16 — test-quality hardening pass within existing AC-006 scope, not new AC scope; no production code changed. Status remains ready (BC-anchored, AC↔BC traces intact); convergence status explicitly reopened per above. |
| 1.28 | 2026-08-12 | Pass-16 F-L-01 (T-011 provenance) + F-N-01 (T-056 wording) fix closure at `12d0fe98` (still 51 tests T-006..T-056, 45 S-21.09-owned, plus 1 registry.rs unit test, all green — no count/range change), plus a comprehensive comment-vs-code consistency sweep of the test file (landed across two commits: `23af4ab7` fixed F-L-01/F-N-01 and a recurring pre-pass-9 drift class where several comments still cited the old basename-only return form of `extract_hook_plugin_name()`/`parse_plugin_refs()` — e.g. `MISSING: ctx.wasm`, `a set containing ghost-traversal.wasm` — where live code has returned the full registry-parent-relative path since the pass-9 HIGH-1 fix; corrected in the module docstring Test-Plan rows T-015/T-021 and the T-012/T-013/T-014/T-015/T-025/T-030/T-035/T-036/T-037 preamble and inline comments; `12d0fe98` then corrected the T-056 assert-message string itself, which still read "interior" after the first commit fixed only the surrounding prose). **Story-side sweep (this burst):** the story's own T-056 Red Gate Test Plan row repeated the same "interior `.` segments" mis-description as the test file's pre-fix comment; corrected to "leading `.` segments" (`Path::components()` preserves leading `./`, which is why the CurDir arm is reachable at all) and the row now quotes the corrected assert-message wording. **Sweep (POLICY 5 / TD-VSDD-060):** all current-state commit cites updated from `05480619` to `12d0fe98` — the AC-006 Tests bullet SHA line, the Mutation-Completeness Audit subsection heading, the Architecture Compliance Rules SURV-01 accepted-residual row, and the two Token Budget Estimate rows (`bundle_orphan_check.rs` line count 5,797→5,803 reflecting the two comment-only commits, `registry.rs` — registry.rs itself is byte-unchanged between `05480619` and `12d0fe98`; its cite is a current-state-HEAD pointer, not a last-touched pointer, consistent with the pattern already used at v1.27). A full comprehensive re-sweep of every Red-Gate Test Plan row (T-012..T-056), every AC/EC, and the Mutation-Completeness Audit subsection against the current test file at `12d0fe98` found no further drift beyond the T-056 "interior→leading" site: all T-NNN descriptions, function names, `MISSING:`/`STAGED-NOT-COMMITTED:`/`UNGATED-DECLARATION:`/`OUTSIDE-REPO-DECLARATION:` identifier strings, and provenance attributions (S-19.04 T-006..T-010, S-19.06 T-011, S-21.09 T-012..T-056) already match the code verbatim. Historical SHA/count citations in Changelog rows below (including the v1.27 row's own `05480619` cites) and the legitimately-historical S-19.04 Previous Story Intelligence `1c59a669` citation are preserved as historical and not rewritten. No production behavior changed; no AC scope change. Points unchanged at 16 — comment-only drift fix closing an adversary pass-16 finding plus a story-side consistency sweep, not new AC scope. Status remains ready. |
| 1.27 | 2026-08-12 | Pass-15 F-1 comment-drift fix closure at `05480619` (still 51 tests T-006..T-056, 45 S-21.09-owned, plus 1 registry.rs unit test, all green — no count/range change). Comments-only correction in `bundle_orphan_check.rs`: three stale comments describing the closed HIGH-1/pass-9 basename-collapse form as current were corrected to state the full registry-relative path — the T-023 preamble "Included region" bullet, the T-034 preamble declared-side sentence, and the T-034 "Mutation-proof" committed-side pseudocode. No change to any test body, assertion, fixture, count, or range. **Sweep (POLICY 5 / TD-VSDD-060):** all current-state commit cites updated from `b761477f` to `05480619` — the AC-006 Tests bullet SHA line, the Mutation-Completeness Audit subsection heading, the Architecture Compliance Rules SURV-01 accepted-residual row, and the two Token Budget Estimate rows (`bundle_orphan_check.rs`, `registry.rs`). Historical SHA/count citations in Changelog rows below (including the v1.26 row's own `b761477f` cites) and the legitimately-historical S-19.04 Previous Story Intelligence `1c59a669` citation are preserved as historical and not rewritten. No production behavior changed; no AC scope change. Points unchanged at 16 — comment-only drift fix closing an adversary pass-15 finding, not new AC scope. Status remains ready. |
| 1.26 | 2026-08-12 | Mutation-hardening burst closure at `b761477f` (51 tests T-006..T-056, 45 S-21.09-owned, plus 1 registry.rs unit test, all green) — documents the exhaustive mutation-completeness audit (POLICY 13) of the S-21.09 declared-set gate (`run_t012_gate`, `detect_ungated_declarations`, `lex_norm`) plus `cargo-mutants` against `crates/factory-dispatcher/src/registry.rs`, and the hardening burst that closed all killable survivors. **Audit scope: 68 determinant mutations** across every applicable mutation class (comparison/boundary operators, boolean structure, boundary constants, early-return/panic arms, classification-literal swaps, collection/string operations) enumerated against the T-006..T-053 suite plus `registry.rs`. **64 of 68 KILLED** by the pre-existing suite; **5 survived**; **4 closed in this burst, 1 accepted-residual.** New "Mutation-Completeness Audit" subsection added under AC-006 recording the full survivor disposition table. **T-054** (`test_S_21_09_ac006_T054_resolvers_schema_version_absent_key_fail_closed_sentinel`, Effectful-shell) closes **SURV-04 (the one genuine CORRECTNESS survivor)** — isolates `resolvers_schema_version`'s `.unwrap_or(-1)` fail-closed sentinel from the `assert_eq!` block T-053 already isolates: a resolvers-registry.toml with an ABSENT `schema_version` key (distinct from T-053's explicit-wrong-integer fixture) would, under an `.unwrap_or(-1)` → `.unwrap_or(1)` mutant, silently coerce to the production-required value and pass every gate undetected; `#[should_panic(expected = "but production requires 1")]`. **T-055** (`test_S_21_09_ac006_T055_detect_ungated_declarations_malformed_toml_fail_open_arm`, Pure-core) closes **SURV-03** — direct unit test of `detect_ungated_declarations`'s fail-open `Err(_) => Vec::new()` arm on malformed TOML, unreachable via `run_t012_gate` because upstream `Registry::parse_str`/resolvers TOML parsing panic first; asserts `.is_empty()`. **T-056** (`test_S_21_09_ac006_T056_lex_norm_curdir_arm_direct_contract_pin`, Pure-core) closes **SURV-02** — direct unit test pinning `lex_norm`'s `Component::CurDir => {}` arm contract (dead in every gate call path): `lex_norm(Path::new("./a/b")) == ["a", "b"]`. **`registry::tests::on_error_falls_back_to_registry_defaults_when_entry_omits_it`** (new unit test in `crates/factory-dispatcher/src/registry.rs`, Pure-core) closes **SURV-05** — an out-of-gate production accessor (`RegistryEntry::on_error`'s defaults-fallback), not part of the T-NNN gate sequence; a `Default::default()` body mutant survived because every prior fixture's expected `on_error` value coincided with `OnError`'s own `#[default]` variant; the new test pairs an omitted entry-level `on_error` with a `RegistryDefaults` explicitly set to the non-default `OnError::Block` so the accessor and the mutant diverge observably. **SURV-01** (`lex_norm`'s `RootDir | Prefix(_) => parts.clear()` arm) is recorded as an **audited accepted-residual, not a deferral**: this is the fourth defensive-unreachable arm — a root/prefix path component always fires FIRST in `Path::components()`, before any `Normal` component pushes onto `parts`, so `parts` is provably empty at every call site that reaches this arm for every possible input; no fixture can construct a `Normal`-before-`RootDir` path (a `std::path` invariant, not gate-specific), so mutating `parts.clear()` is behaviorally unobservable; closed via a strengthened doc comment on `lex_norm`, deliberately without a test (a non-behavior assertion would itself be vacuous). Each of the four killable controls was empirically verified: mutant applied to a scratch copy → new test alone goes RED, all others GREEN → mutant reverted. Full audit catalog persisted separately as `mutation-audit-s21.09` (`.factory/cycles/v1.0-brownfield-backfill/`). Three new Red Gate Test Plan rows added (T-054, T-055, T-056) mirroring the T-052/T-053 row structure. **Sweep (POLICY 5 / TD-VSDD-060):** all current-state test-count/range references updated from 48 tests/T-006..T-053/42-owned/`7f540ddc` to 51 tests/T-006..T-056/45-owned (T-012..T-056) plus 1 registry.rs unit test/`b761477f` across the AC-006 Tests bullet, Architecture Mapping, Purity Classification, Architecture Compliance Rules, File Structure Requirements, and Token Budget Estimate (`bundle_orphan_check.rs` line count 5,511→5,797; new registry.rs Token Budget row added; total ~19,000→~19,300; budget usage ~9.5%→~9.7%). Two new File Structure Requirements rows/updates: `bundle_orphan_check.rs` row extended to cite T-054/T-055/T-056 and SURV-04/03/02 closure; new `crates/factory-dispatcher/src/registry.rs` row added for the SURV-05 control. New Architecture Compliance Rules row added recording the SURV-01 no-test-required disposition. Historical SHA/count citations in Changelog rows below and the legitimately-historical S-19.04 Previous Story Intelligence `1c59a669` citation are preserved as historical and not rewritten. No production behavior changed — the gate logic was already correct; all four new controls are append-only test-side isolation controls. Points unchanged at 16 — mutation-completeness hardening pass closing an already-implicit POLICY 13 obligation within existing AC-006 scope, not new AC scope. Status remains ready. |
| 1.25 | 2026-08-12 | Pass-14 fix-burst closure at `7f540ddc` (48 tests T-006..T-053, 42 S-21.09-owned, all green) — closes adversary pass-14 MEDIUM and LOW findings. **MEDIUM — AC-006 step 2d production-validation determinants isolated.** Pass-14 observed that the `Registry::parse_str(&hooks_content)` hooks production-validation block and the resolvers `assert_eq!(resolvers_schema_version, 1, …)` block inside `run_t012_gate` (both pass-11 HIGH-3) were sole-determinant deletion mutants with no isolating control — every fixture through T-051 wrote production-valid `schema_version` values on both registries, so deleting either block changed no observable outcome for the existing suite. **T-052** (`test_S_21_09_ac006_T052_hooks_production_validation_isolation_kills_parse_str_deletion`, Effectful-shell) isolates the hooks-side determinant: a full production-valid git fixture (30 hooks + 1 resolver, all 31 WASMs committed) with `hooks-registry.toml` declaring `schema_version = 3` against `REGISTRY_SCHEMA_VERSION = 2`; `#[should_panic(expected = "T-012: hooks-registry.toml fails production validation")]`. **T-053** (`test_S_21_09_ac006_T053_resolvers_schema_version_isolation_kills_assert_deletion`, Effectful-shell) isolates the resolvers-side determinant: the same fixture shape with `resolvers-registry.toml` declaring `schema_version = 2` against the production-required 1 (hooks side kept fully valid); `#[should_panic(expected = "T-012: resolvers-registry.toml schema_version=2 but production requires 1")]`. Empirically verified: deleting each block in turn sends its corresponding test RED while all other 47 tests stay GREEN; both blocks were reverted after the proof. AC-006 step 2d narrative rewritten to replace the prior self-disclosure ("No single test ID owns this gate in isolation…") with this corrected statement; two new Red Gate Test Plan rows added (T-052, T-053) mirroring the T-050/T-051 row structure. **LOW — spec↔impl drift on T-020/EC-005b corrected (2 sites).** The Red Gate Test Plan T-020 row and the EC-005b Edge Cases row both cited `#[should_panic(expected = "T-012 EC-005")]`; the actual code (confirmed at `7f540ddc`) uses `#[should_panic(expected = "T-012 EC-005b")]` (the guard panics `"T-012 EC-005b: tracked set passed to check_declared_subset_tracked is empty…"`). The stale truncated literal `"T-012 EC-005"` is a substring of both `EC-005a` and `EC-005b`, contradicting the story's own guarantee that T-020 pins EC-005b specifically. Both sites corrected to `#[should_panic(expected = "T-012 EC-005b")]` and the accompanying plain-text panic-message citations in the same two rows corrected to `"T-012 EC-005b"` for internal consistency. **Sweep (POLICY 5 / TD-VSDD-060):** all current-state test-count/range references updated from 46 tests/T-006..T-051/40-owned/`a922ad82` to 48 tests/T-006..T-053/42-owned (T-012..T-053)/`7f540ddc` across the AC-006 Tests bullet, Architecture Mapping, Purity Classification, Architecture Compliance Rules, File Structure Requirements, and Token Budget Estimate (`bundle_orphan_check.rs` line count 5,230→5,511; total ~19,000 unchanged; budget usage ~9.5% unchanged). Historical SHA/count citations in Changelog rows below and the legitimately-historical S-19.04 Previous Story Intelligence `1c59a669` citation are preserved as historical and not rewritten. No production code changed; no AC scope change. Points unchanged at 16 — test-hardening pass closing two already-scoped adversary findings via two new isolation controls plus a doc-fidelity correction, not new AC scope. Status remains ready. |
| 1.24 | 2026-08-12 | Pass-13 fix-burst count-parity correction (adversary pass-13 finding F-1). The AC-006 "Tests:" bullet (immediately preceding the T-013/T-014 BLOCKER-1 parse-controls description) still read `test_S_21_09_ac006_T012_declared_set_subset_of_tracked_set()` (now calls extracted `run_t012_gate(&workspace_root())`) plus **T-013..T-050** — the pass-12 sweep (v1.23) updated Architecture Mapping, Purity Classification, Architecture Compliance Rules, File Structure Requirements, and Token Budget Estimate to the T-051/46-test/40-owned/`a922ad82` state but missed this one AC-006 Tests-bullet site. Corrected to **T-013..T-051**, consistent with every other range citation in the story (T-051 is AC-006-owned; the pass-12 fix-burst's dedicated prefix-conjunct isolation control). **Sibling sweep (POLICY 5 / TD-VSDD-060):** literal-shell grep of the full story body (excluding Changelog rows 943+ and the legitimately-historical S-19.04 Previous Story Intelligence `1c59a669` citation) for `T-012..T-050`, `T-013..T-050`, `..T-050` as a range terminator, `45 tests`, `45-test`, `39 owned`, `39 S-21.09-owned`, and SHA `69663255` used as a current-state cite found exactly one genuine stale site (the line-475 AC-006 Tests bullet fixed above); all other `T-050`/`45`/`39`/`69663255` occurrences in the body are either legitimate historical references to the specific T-050 test/M2 mutant-kill narrative or already-corrected pass-12 output (lines 623-624, 751) — no further sibling sites required correction. No production code changed; no AC scope change. Points unchanged at 16. Status remains ready. |
| 1.23 | 2026-08-12 | Pass-12 fix-burst story-side reconciliation at `a922ad82` (46 tests T-006..T-051, 40 S-21.09-owned, all green) — documents T-051, the pass-12 fix-burst closure of adversary finding F-1 (HIGH, POLICY 13 mutation-completeness) against the pass-12 dispatch. **F-1 — un-isolated prefix conjunct closed.** Adversary pass-12 observed that `detect_ungated_declarations`'s containment predicate is a two-conjunct AND (`joined_parts.len() > root_parts.len() && root_parts.iter().enumerate().all(|(i, p)| joined_parts.get(i) == Some(p))`); T-050 (pass-11) isolates the LENGTH conjunct via a self-match-prefix candidate, but no existing candidate isolated the PREFIX conjunct — every OUTSIDE-REPO-DECLARATION candidate available (T-047, T-048's OUTSIDE rows, T-050 itself) was over-determined against a prefix mutant (`.all(...)` → `.any(...)` or `.all(...)` → `true`), so that mutant survived the full suite undetected. **T-051 is the dedicated prefix-conjunct isolation control** (new test, `test_S_21_09_ac006_T051_prefix_conjunct_isolation_kills_all_mutants`), the orthogonal sibling of T-050. Fixture `plugin = "../../../sib/ghost.wasm"` resolves to `root_parts[0..N-1] + ["sib", "ghost.wasm"]`: the length conjunct is TRUE (`N+1 > N`), but the prefix conjunct diverges from `root_parts` at the last index — the sole determinant of `in_repo` for this candidate. Empirically verified: under mutant `.all(...)` → `.any(...)` AND under mutant `.all(...)` → `true`, ONLY T-051 goes RED (observes `UNGATED-DECLARATION` instead of the asserted `OUTSIDE-REPO-DECLARATION`); T-047, T-048, and T-050 all stay GREEN under both mutants. Orthogonality vs T-050 confirmed empirically: under mutant M2 (`len >` → `len >=`), T-051 stays GREEN while T-050 goes RED — the two isolation controls are independent. No production code changed. AC-006 step 2c totality-invariant narrative now states directly that BOTH conjuncts of the two-conjunct `in_repo` predicate are independently isolated (T-050 length, T-051 prefix), closing the "claimed-total predicate with an un-isolated conjunct" gap; the T-047/T-048/T-050 boundary-controls prose is extended (not replaced) with the T-051 cross-reference; a new T-051 Red Gate Test Plan row is added mirroring T-050's structure (GREEN at HEAD: error contains `OUTSIDE-REPO-DECLARATION: ../../../sib/ghost.wasm`, not `UNGATED-DECLARATION:`; RED only under `.all→.any` / `.all→true` mutants applied locally). **Sweep:** all test-count/range references updated from 45 tests/T-006..T-050/39-owned/`69663255` to 46 tests/T-006..T-051/40-owned (T-012..T-051)/`a922ad82` across the AC-006 Tests bullet, Architecture Mapping, Purity Classification, Architecture Compliance Rules, File Structure Requirements, and Token Budget Estimate (`bundle_orphan_check.rs` line count 5,082→5,230; total ~19,000 unchanged; budget usage ~9.5% unchanged). Historical SHA citations predating this pass (e.g., the S-19.04 Previous Story Intelligence row's `1c59a669`) are preserved as legitimately historical and not rewritten. Points unchanged at 16 — test-hardening pass closing an already-scoped adversary finding via a new orthogonal isolation control, not new AC scope. Status remains ready. |
| 1.22 | 2026-08-12 | Pass-11 fix-burst story-side reconciliation at `69663255` (45 tests T-006..T-050, 39 S-21.09-owned, all green) — corrects adversary findings F-1 (HIGH) and F-2 (MEDIUM) against the pass-11 dispatch (`1c59a669`). **F-1 — self-contradiction and false M2-kill claims resolved.** T-047 and T-048 were both documented as killing mutant M2 (`detect_ungated_declarations` containment predicate's length conjunct `>` relaxed to `>=`) while a separate location honestly admitted "no control exists that satisfies the prefix conjunct while failing only the length conjunct" — a direct contradiction. Ground truth: T-047's (`../../../ghost.wasm`) and T-048's OUTSIDE-REPO-DECLARATION candidates are all over-determined — they fail the length conjunct AND the prefix conjunct simultaneously, so M2 survives both undetected. **T-050 is now the isolating control** (new test, `test_S_21_09_ac006_T050_length_conjunct_isolation_kills_m2`): a root-resolving, filename-less candidate (`plugin = "../.."`, resolving to exactly `root_parts` — an exact prefix self-match) that makes the length conjunct the sole determinant. Empirically verified: under mutant M2, only T-050 goes RED; T-047 and T-048 stay GREEN. AC-006 step 2c narrative, the boundary-controls list, the T-047 and T-048 Red Gate Test Plan rows (false kill-claims removed, cross-references to T-050 added), and a new T-050 Red Gate row are all updated to state this accurately. **F-2 — stale references corrected.** (1) The `is_hook_plugins` symbol (T-043 and T-046 Red Gate rows) was removed in the pass-11 single-copy refactor — both rows rewritten to describe delegation to `extract_hook_plugin_name(...).is_none()`. (2) T-032's underlying test function was renamed `..._yields_filename` → `..._yields_full_path` (numeric ID preserved, POLICY 1 append-only applies to IDs not function-name text); the story already described full-path (not basename) semantics correctly at every T-032 reference, so no content correction was needed there beyond confirming parity. (3) T-023 was checked for a claim that absolute/traversal forms are INCLUDED; the story already correctly places absolute paths in the EXCLUDED region (fixed in v1.9/v1.10) — no change needed. **Sweep:** all test-count/range references updated from 44 tests/T-006..T-049/38-owned/`1c59a669` to 45 tests/T-006..T-050/39-owned (T-012..T-050)/`69663255` across the AC-006 Tests bullet, Architecture Mapping, Purity Classification, Architecture Compliance Rules, File Structure Requirements, and Token Budget Estimate (`bundle_orphan_check.rs` line count 4,936→5,082; total ~18,900→~19,000; budget usage ~9.5% unchanged). Points unchanged at 16 — test-hardening + doc-fidelity pass correcting two already-scoped adversary findings, not new AC scope. Status remains ready. |
| 1.21 | 2026-08-12 | Implementation closure round at `1c59a669` (44 tests T-006..T-049, 38 S-21.09-owned, all green) — mandatory gating fix for LOCAL adversary pass-11 dispatch; corrects v1.20's under-description of the test suite (which described 42 tests T-006..T-047 at `b951461a`). **Pass-10 BLOCKER-1 closed — single-copy detect gate refactor:** `detect_ungated_declarations` previously re-derived the three-gate correctness check inline as its own hand-written four-conjunct containment predicate, duplicating `extract_hook_plugin_name`'s logic; a composed mutation (M1 length-conjunct weakening + M4 gate-3 case-check weakening) survived the full suite because the two independently-maintained copies could drift out of sync while every single-gate mutation was individually caught. `detect_ungated_declarations` now delegates the gated-vs-ungated determination for in-repo declarations directly to `extract_hook_plugin_name(...).is_none()` — exactly one copy of the three-gate logic exists in the codebase. Documented in AC-006 step 2c and the `bundle_orphan_check.rs` File Structure Requirements row. **Pass-10 BLOCKER-2 closed — T-048 totality property test added:** the invariant `extract_hook_plugin_name(x).is_some() ⟺ detect_ungated_declarations(x).is_empty()` was previously asserted by no test (narrative-only in AC-006 step 2c). T-048 is an 18-candidate parametrized property test (5 `gated`, 9 `UNGATED-DECLARATION`, 4 `OUTSIDE-REPO-DECLARATION`) that drives both functions per candidate and asserts the totality partition directly, plus a negative-identifier check that kills identifier-swap boundary mutants (e.g. M2 `len >` → `len >=`). Documented in AC-006 step 2c, the AC-006 Tests bullet, and a new Red Gate Test Plan row. **T-049 added — EC-005a control:** a `#[should_panic(expected = "T-012 EC-005a")]` git-fixture test (a valid, `Registry::parse_str`-conformant 30-entry registry whose committed `hook-plugins/` directory contains no WASM files) pins the `tracked_raw.is_empty()` guard in `run_t012_gate` as EC-005a, distinct from T-020's EC-005b (fixture-empty `HashSet` passed directly to `check_declared_subset_tracked`, bypassing git). The EC-005a Edge Cases table row was corrected to match: it previously described only the `strip_prefix(...).expect(...)` wrong-prefix panic path, which is a real but SEPARATE guard from the `tracked_raw.is_empty()` guard T-049 actually pins — both are now documented as independent hard failures. New Red Gate Test Plan row added for T-049. **Production-validation gate documented (AC-006 new step 2d, pass-11 HIGH-3):** `run_t012_gate` now validates `hooks-registry.toml` via `factory_dispatcher::Registry::parse_str` (the same production parser; `schema_version == 2`, `deny_unknown_fields`, tool-pattern regex validity) and `resolvers-registry.toml` for `schema_version == 1`, both as hard-panic pre-checks before the declared set is parsed — prevents T-012 from gating against a registry production would reject outright. `factory_dispatcher::Registry` added to Library & Framework Requirements. **T-013/T-014 comment-filtering documented:** both tests filter out `#`-prefixed lines from their fixture TOML before asserting the distinguishing nospace/dotslash syntax is present in the live `[[hooks]]` entry (not merely in a header comment) — added to the AC-006 Tests bullet. **T-008 NOT-orphan assertion documented:** the pre-existing S-19.04 test `test_S_19_04_ac006_T008_...` (not S-21.09-owned, but touched by the `1c59a669` diff since it lives in the S-21.09-extended file) gained an explicit `!orphans.contains(&"hooks-only.wasm".to_string())` assertion killing mutant M22 (`"hook-plugins/{}"` → `"bogus/{}"` in `collect_orphans_hooks_only`); documented in the S-19.04 row of Previous Story Intelligence. **Sweep:** Architecture Mapping, Purity Classification, Architecture Compliance Rules, and File Structure Requirements tables updated from T-012..T-047/36-owned to T-012..T-049/38-owned throughout (the File Structure Requirements row was already stale at T-012..T-042 prior to this pass — corrected in the same sweep). Token Budget Estimate bumped (story spec 2,200→2,400; `bundle_orphan_check.rs` 2,500→2,900 reflecting file growth to ~4,936 lines/44 tests; total ~18,300→~18,900; budget usage ~9.2%→~9.5%). Points unchanged at 16 — this is a documentation-fidelity pass closing two already-scoped BLOCKERs plus two supporting controls within the existing AC-006 scope, not new AC scope. Status remains ready. |
| 1.20 | 2026-08-12 | Pass-10 NOT-CLEAN fix burst (story-writer). **MEDIUM-4 — T-047 boundary claim restated and symmetry dropped (5 sites).** T-047's fixture (`../../../ghost.wasm` → root depth) fails both the length conjunct (`len > root_parts.len()`) and the prefix conjunct simultaneously — no control exists that passes the prefix conjunct while failing only the length conjunct, so T-047 cannot isolate which conjunct is operative. Restated in T-047 row, T-046 row, AC-006 step 2c boundary controls, AC-006 Tests bullet, and v1.19 changelog: T-047 proves the outcome class fires at root depth; symmetry claim removed everywhere. **MEDIUM-1 — T-033 Expected Outcome narrowed.** "directory-only declaration is correctly excluded before any name is extracted" was a false generalization: `hook-plugins/sub/` (depth `expected_depth + 2`) passes the minimum-length check, enters `declared_set`, and fires `MISSING: hook-plugins/sub` — loud, not silent. The threshold enforces component count (at least `expected_depth + 2`), not file extension. T-033 proves only that a bare `hook-plugins` component (depth `expected_depth + 1`) is caught; Expected Outcome updated to state that scope explicitly. **NIT — "52 production registry entries" count corrected (2 sites).** Figure was an inherited rc.16 value; reviewer verified 75 `[[hooks]]` entries + 1 `[[resolvers]]` entry = 76 entries, 36 unique names. Updated in AC-006 step 2c and v1.19 changelog to "all production registry entries (75 `[[hooks]]` + 1 `[[resolvers]]` = 76 entries, 36 unique names)". Points unchanged at 16; status remains ready. |
| 1.19 | 2026-08-12 | Implementation closure round at `b951461a` (42 tests T-006..T-047, 36 S-21.09-owned, all green). **BLOCKER closed — `detect_ungated_declarations` rewritten:** length-based threshold approach replaced entirely with worktree-root containment predicate (`joined_parts.len() > root_parts.len() && joined[0..root_parts.len()] == root_parts`); any in-repo declaration not satisfying `is_hook_plugins` emits `UNGATED-DECLARATION: <path>` — closes gate-1/gate-2 escapes (bare names, traversal-that-cancels, `../`-prefix forms no longer silently passed); any declaration resolving outside the worktree root emits new identifier `OUTSIDE-REPO-DECLARATION: <path>` (previously silently dropped). **Total predicate invariant added to AC-006 step 2c:** every declaration falls into exactly one of three classes — (1) resolves under `registry_parent/hook-plugins/` → gated, not reported; (2) resolves inside worktree root but outside `hook-plugins/` → `UNGATED-DECLARATION`; (3) resolves outside worktree root → `OUTSIDE-REPO-DECLARATION`. No declaration silently dropped. Verified: all production registry entries (75 `[[hooks]]` + 1 `[[resolvers]]` = 76 entries, 36 unique names) use `hook-plugins/<name>.wasm` form — neither new identifier fires. **Five new controls T-043..T-047:** T-043 bare name (`ghost-bare.wasm`) → UNGATED; T-044 traversal-that-cancels → UNGATED; T-045 `../ghost.wasm` → `plugins/ghost.wasm`, UNGATED; T-046 `../../ghost.wasm` → `<root>/ghost.wasm`, UNGATED at tightest INSIDE margin (root+1); T-047 `../../../ghost.wasm` → above root, OUTSIDE-REPO-DECLARATION at tightest OUTSIDE margin (`len == root_parts.len()`); T-047 proves the outcome class at root depth — both length and prefix conjuncts fail simultaneously; no conjunct isolation. LOW-1 false-negative class **CLOSED**: containment predicate covers all gate-1-failure forms — no silent escapes remain. **T-009 filter_map fix:** silently-dropping `filter_map` strip call (drops entries silently on prefix mismatch, producing an empty set without panic) replaced by `map` + `.strip_prefix(…).expect(…)` in T-009 and in `run_t012_gate` for both tracked and committed sets; wrong prefix now panics; both local `const REGISTRY_PARENT_PREFIX` declarations removed; single module-level const used at all three call sites. **EC-005 split:** EC-005 becomes EC-005a (fires in `run_t012_gate` on raw git output before strip; pathspec is case-sensitive on Linux CI — operative failure) and EC-005b (fixture-empty path inside `check_declared_subset_tracked`; T-020's `#[should_panic(expected = "T-012 EC-005")]` pins EC-005b specifically). Architecture tables T-012..T-042 → T-012..T-047; Purity Classification and Compliance Rules updated. Points re-estimated 14 → 16: containment predicate is an architectural replacement (not a gate tweak), `OUTSIDE-REPO-DECLARATION` is a new output class, 36 S-21.09-owned tests (up from 31). Status remains ready. |
| 1.18 | 2026-08-12 | Pass-9 NOT-CLEAN fix burst (story-writer). F1 BLOCKER — T-023 safety argument deleted: "visible to `git status` so cannot be silently exploited" addressed present-but-untracked files, not declared-but-absent artifacts; gate-1-failure declarations (bare names, traversal-that-cancels, `../`-prefix) escape BOTH `declared_set` AND `detect_ungated_declarations` — a declared-but-absent artifact of this form passes the gate silently; T-023 "Boundary record" replaced with extension note; Tests bullet same language removed; `detect_ungated_declarations` is being extended to cover gate-1 and gate-2 escapes (pending test-writer inventory relay). F3 MEDIUM — AC-006 step 2c corrected: "gate result is unaffected" was wrong; `run_t012_gate` returns `Err(...)` for an UNGATED-DECLARATION — it is a hard failure, not a warning; T-038/T-040 descriptions updated to note Err return. F4 MEDIUM — T-039 reclassified Effectful-shell (5 sites): Architecture Mapping, Purity Classification table, AC-006 Tests bullet, T-039 Red Gate row, EC-008 row; T-039 runs `git init`/`config`/`add`/`commit`; was incorrectly copied from T-042 Pure-core rationale. F6 LOW — M17 → M15 in T-036 Red Gate row and AC-006 Tests bullet (code calls it M15, story was the outlier). F7 LOW — §Note on absolute-path declarations narrowed: "out of this gate's scope" generalisation removed; only absolute paths with a divergent (non-registry-parent) prefix are excluded; an absolute path whose leading components equal the real registry parent is admitted via `lex_norm` root-arm no-op. Points unchanged at 14 pending test-writer relay (detect_ungated_declarations gate-1/gate-2 extension, T-009 filter_map fix, EC-005 split). Status remains ready. |
| 1.17 | 2026-08-12 | Implementation closure at `c05a926b` (37 tests T-006..T-042, 31 S-21.09-owned, all green). **Path-based diff (10 output-string sites swept):** `extract_hook_plugin_name` now returns registry-relative path via `joined_parts[expected_depth..].join("/")` (e.g., `hook-plugins/foo.wasm`, `hook-plugins/sub/nested.wasm`); `MISSING:` and `STAGED-NOT-COMMITTED:` gate outputs are now path-valued; git queries return raw paths; `run_t012_gate` strips `plugins/vsdd-factory/` prefix before diffing. `UNGATED-DECLARATION` was already path-valued — unchanged. **`lex_norm` reclassification:** `RootDir \| Prefix(_)` arm reclassified from operative to fourth provably-unreachable arm (joins the three existing `?` exits); language updated to "four defensive unreachable exits" throughout. **Case-variant behavior (deliberate platform-correctness decision):** lowercasing of the `hook-plugins` component removed from extraction return; `eq_ignore_ascii_case` stays in gate 3 so `Hook-Plugins/x.wasm` is admitted to `declared_set` verbatim; verbatim path then fails case-sensitive comparison against lowercase git-tracked names on CI Linux — a case-variant declaration is always a gate failure by design; T-031 updated to assert `refs.contains("Hook-Plugins/ghost-case.wasm")` (verbatim); T-042 new (end-to-end: declared `Hook-Plugins/ghost-missing.wasm`, tracked lowercase → `MISSING: Hook-Plugins/ghost-missing.wasm`; Pure-core). **T-036 framing restored:** `git check-ignore -q` assertion is present and makes `.gitignore` load-bearing; reinstate "exercises S-21.09 motivating condition"; config.yaml filter control added; output updated to path form (`MISSING: hook-plugins/gitignored-probe.wasm`). **T-037 output:** path form `STAGED-NOT-COMMITTED: hook-plugins/staged-probe.wasm`. **T-039 (EC-008 closed defect control; Pure-core):** 30 hooks declared at `hook-plugins/sub/hNN.wasm` committed flat; asserts `Err` containing `MISSING: hook-plugins/sub/h00.wasm`; closes EC-008. **T-040 (resolvers-side UNGATED-DECLARATION; Pure-core):** synthetic resolver entry that passes gates 1+2, fails gate 3 → `detect_ungated_declarations` emits `UNGATED-DECLARATION:`. **T-041 (`#[should_panic(expected = "git ls-files exited with status")]`; Effectful-shell):** invokes `run_t012_gate` on a non-git tempdir; panics at git ls-files step. **T-025/T-032/T-034** updated: `T-025` step (4) returns registry-relative path `hook-plugins/ghost-traversal.wasm`; `T-032` returns `hook-plugins/sub/nested.wasm` (not last component `nested.wasm`); `T-034` `git_committed_wasm_names()` returns `["hook-plugins/sub/nested.wasm"]`. **T-026/T-035 mutation proofs** updated: `got refs_depth: {"hook-plugins/evil.wasm"}`; `got refs: {"other-dir/evil-probe.wasm"}`. **EC-008** promoted from "fix in progress" to "closed defect with control (T-039)". **Purity sweep:** T-039/T-040/T-042 → Pure-core; T-041 → Effectful-shell. Architecture Mapping T-012..T-042; Compliance Rules updated; Library/File-Structure Requirements updated. Points re-estimated 12 → 14: path-based return type is a structural change to the extraction contract, case-variant behavior is a deliberate design decision, inherited helpers (`collect_orphans_dual`/`collect_orphans_hooks_only`) have updated internal comparison format, and 31 S-21.09-owned tests represent scope materially exceeding the original five-arm estimate. Status remains ready. |
| 1.16 | 2026-08-12 | Pass-8 NOT-CLEAN fix burst (story-writer). HIGH-1 — EC-008 rewritten as closed defect: false negative requires only ONE mis-matched entry (declared at `hook-plugins/sub/hN.wasm`, committed flat at `hook-plugins/hN.wasm` → `run_t012_gate` returns `Ok(())` with all 30 effectively undetected); remedy is local (return registry-relative path from `extract_hook_plugin_name`, diff full paths — not a scope-expanding redesign); prior deferral to S-21.14 rescinded; test-writer implementing path-based diff within S-21.09. MEDIUM-2 — T-036 framing corrected: v1.15 overstated the proved condition as 'gitignored-but-present' (`.gitignore` is not load-bearing — removing it leaves test passing); reframed to 'never-indexed artifact on disk' (proved condition: present on disk, absent from git index, never run through `git add`); pending `git check-ignore -q` assertion from test-writer will make `.gitignore` mechanism load-bearing; Red Gate row, AC-006 Tests bullet corrected. NIT-1 — AC-006 step 1 heading `(T-024)` → `(T-017, T-018)`: T-024 is the underscore-escape glob mutant correctly cited in step 1 prose; T-017 (UNEXPECTED arm) and T-018 (MISSING arm) are the operative inventory controls (POLICY 4 anchor mismatch). Points unchanged at 12; status remains ready. |
| 1.15 | 2026-08-11 | Implementation closure at `e0cc5480` (33 tests T-006..T-038, 27 S-21.09-owned, all green). T-036 (gitignored-but-present): git fixture, `.gitignore` covers `hook-plugins/`; 30 hooks + resolver force-committed via `git add -f`; `gitignored-probe.wasm` present on disk after commit but never staged; gate emits `MISSING: gitignored-probe.wasm`; mutation proof: replacing `git_tracked_wasm_names()` with `fs::read_dir` makes probe appear in tracked set → gate returns Ok (false negative, silently dropping the missing artifact); first test exercising the S-21.09 motivating condition; specific outcome string (`MISSING:`) is load-bearing. T-037 (staged-not-committed): commits 30 hooks + resolver; `git add -f staged-probe.wasm` without committing; gate emits `STAGED-NOT-COMMITTED: staged-probe.wasm`; M18 mutation: swapping `git_committed_wasm_names()` for `git ls-files` makes committed==tracked → gate returns Ok (false negative); M16 mutation: swapping tracked/committed arguments emits `MISSING:` instead of `STAGED-NOT-COMMITTED:`; both function identities load-bearing. T-038 (`detect_ungated_declarations`): no-git fixture; `other-dir/evil-probe.wasm` passes gates 1+2 (sufficient depth + registry-parent prefix), fails gate 3 → helper emits `UNGATED-DECLARATION: other-dir/evil-probe.wasm`; fires before git calls in `run_t012_gate`; Pure-core. `lex_norm` extracted to module level (shared by `extract_hook_plugin_name` and `detect_ungated_declarations`); noted in step 2 algorithm description and Library Requirements. `extract_hook_plugin_name` description updated to "three reachable sequential exclusion gates plus three defensive unreachable `?` exits." AC-006 step 2c added for `detect_ungated_declarations`. 'Permanent accepted narrowing' framing replaced throughout: gate-1-failure paths (bare names, traversal-that-cancels, `../`-prefix) remain outside the gate's structural scope (excluded by gate 1 before `hook-plugins`-component check applies); gate-3-failure paths (correct prefix and depth, non-`hook-plugins` component) are now loudly reported via `detect_ungated_declarations` rather than silently dropped. T-023(c) `../hook-plugins/evil.wasm` now executed: gate 1 fires (`expected_depth + 1 < expected_depth + 2`); EXCLUDED; closes previously-enumerated-but-unexecuted case. T-023 Expected Outcome updated to include T-023(c). Purity Classification: T-036/T-037 added to Effectful-shell row; T-038 added to Pure-core row. Architecture Mapping: range T-012..T-035 → T-012..T-038. Architecture Compliance Rules: T-012..T-035 range updated to T-012..T-037 (T-038 is Pure-core, no git). Library Requirements and File Structure Requirements `bundle_orphan_check.rs` row updated for detect_ungated_declarations + range T-012..T-038. Points re-estimated 10 → 12: new production helper (`detect_ungated_declarations`), module-level `lex_norm` extraction, and 27 S-21.09-owned tests represent scope beyond the original five-arm refinement estimate. Status remains ready. |
| 1.14 | 2026-08-11 | Pass-7 NOT-CLEAN fix burst (story-writer). MEDIUM-1 — T-020 row corrected: T-020 passes an empty HashSet directly (no git invocation); `check_declared_subset_tracked` hits `tracked.is_empty()` guard before the set-difference loop; panics with "T-012 EC-005"; T-020 asserts `#[should_panic(expected = "T-012 EC-005")]`; no `MISSING:` lines emitted. AC-006 step 3 reference corrected to match. MEDIUM-2 — Gate enumeration unified at three gates throughout: AC-006 step 2 algorithm description changed "five steps / two exclusion gates" to "four steps / three sequential exclusion gates"; gate (c) `eq_ignore_ascii_case("hook-plugins")` match moved from old clause (4) into step (3)(c); old step (5) renumbered to step (4). §Note changed "Two step-(3) gates" → "Three step-(3) gates". T-025/T-031/T-032 step references updated (step 4 → step (3)(c); step 5 → step (4)). Library Requirements algorithm description updated to four-step / three-gate form. MEDIUM-3 — T-033 reclassified to Pure-core: T-033 writes `plugin = "hook-plugins"` and asserts `refs.is_empty()`; no runtime depth derivation; moved from Effectful-shell to Pure-core row (T-013..T-025, T-027..T-029, T-031..T-033, T-035). Effectful-shell row now T-026(b), T-030, T-034 only; "and T-033" removed from justification. v1.13 changelog `(?)` artifact corrected. NIT-1 — Architecture Mapping "Pure/no mutations" label corrected to reflect mixed classification (most tests pure-core; T-030/T-034 effectful-shell; see Purity Classification table). Points unchanged at 10. Status remains ready. |
| 1.13 | 2026-08-11 | Test-writer closure round at `b5ec1710` (30 tests T-006..T-035, all green). T-035 (gate-3 isolated control): fixture `plugin = "other-dir/evil-probe.wasm"` in synthetic tempdir registry; gate 1 passes (`expected_depth + 2` components), gate 2 passes (registry-parent prefix intact), gate 3 fires (`"other-dir"` ≠ `"hook-plugins"` → excluded); captured mutation proof: removing gate 3 admits `evil-probe.wasm` → `got refs: {"evil-probe.wasm"}`; completes three-gate enumeration (gate 1 → T-033, gate 2 → T-026(b), gate 3 → T-035). T-035 pure-core (synthetic tempdir registry; no git mutation; no runtime depth derivation). T-023(b) wording updated from hypothetical to declarative (matches code's embedded comment: ParentDir pop demonstrated load-bearing). Counts updated: 30-test suite T-006..T-035; 24 S-21.09-owned tests T-012..T-035; SHA `b5ec1710`. Architecture tables T-012..T-034 → T-012..T-035; Purity Classification pure-core row extended to include T-035 (row: T-013..T-025, T-027..T-029, T-031..T-032, T-035); T-033 remained in Effectful-shell at this version (corrected in v1.14). Points unchanged at 10. Status remains ready. |
| 1.12 | 2026-08-11 | Pass-6 NOT-CLEAN fix burst (story-writer). MEDIUM-2 — AC-002 rewritten: "measurable without a dispatcher build" claim removed (proven vacuous — `_require_artifacts` skips on missing `$DISPATCHER` before WASM branch is reached); Test bullet now asserts positive (tests execute) with `CI_REQUIRE_ARTIFACTS=1` + dispatcher built; RG-003 clarified to require dispatcher present pre-fix. MEDIUM-1 — T-023 gate attribution corrected: bare names, traversal-that-cancels, and `../`-prefix forms all normalise to `expected_depth + 1` via lex_norm → **minimum-length check** fires before prefix loop is consulted (prefix assertion is NOT the operative gate for these three); false-negative class description corrected to match. LOW-1 — Four deferrals anchored: sibling-predicate sites → S-21.14; EC-001 byte-freshness (+ zero-byte name collision) → S-21.14; EC-008 basename collision (full-path tracking) → S-21.14; domain-widening false-negative class → permanent accepted narrowing. LOW-4 — Purity Classification split: T-013..T-025/T-027..T-029/T-031..T-032 Pure-core; T-026(b)/T-030/T-033/T-034 Effectful-shell (runtime tempdir depth or git mutations). NIT-2 — AC-007 Test bullet corrected to describe `declared − tracked = ∅` subset check (no explicit audit-count assertion exists; subset check is stronger); "devops build confirms no other artifacts absent" claim removed (subset check covers this automatically). Points unchanged at 10; status remains ready. |
| 1.11 | 2026-08-11 | Test-writer closure round at `54ab6802` (29 tests T-006..T-034, all green). T-026(b) depth-matched fixture EXISTS: forward reference removed; describes runtime `expected_depth` derivation and `/seg0/…/seg_{N-1}/hook-plugins/evil.wasm` construction; captured proof: deleting prefix loop yields `got refs_depth: {"evil.wasm"}`. T-033 (minimum-length lower boundary: `plugin = "hook-plugins"` gives `len == expected_depth + 1`, caught before reach; proof: weakening constant yields `got refs: {"hook-plugins"}`). T-034 (`git ls-tree -r` nested committed WASM: `hook-plugins/sub/nested.wasm` → `["nested.wasm"]`; proof: dropping `-r` yields `got committed: []`). EC-004 rewritten (Option A — unreachable panic blocks deleted; obligation via step-1 MISSING arm). T-023 boundary-polarity row updated: `../`-prefix form added to EXCLUDED; false-negative class (bare names, traversal-that-cancels, `../`-prefix resolve within repo but outside `hook-plugins/`) acknowledged as known recorded class. Two POLICY 13 latent-class notes added: EC-007 (enabled=false entries still contribute to declared_set — zero current instances); EC-008 (basename-collision — `hook-plugins/a/x.wasm` and `hook-plugins/b/x.wasm` both collapse to `x.wasm`; deliberate parity-design). T-030 NIT: `.status()` → `.output()`. Architecture tables T-012..T-032 → T-012..T-034. Points unchanged at 10 (23 S-21.09-owned tests; refinements within existing five arms). Status remains ready. |
| 1.10 | 2026-08-11 | Pass-5 NOT-CLEAN fix burst (story-writer). HIGH-1 — T-026 mutation-proof claim corrected: the depth-coincidental fixture (`/abs/hook-plugins/ghost-absolute.wasm` normalises to fewer components than `expected_depth + 2`) is rejected by the minimum-length check before the prefix assertion is consulted; removing the prefix assertion leaves T-026 green. Two-mechanism description added: (a) minimum-length check fires for depth-coincidental fixtures; (b) prefix assertion fires for depth-matched absolute fixtures (test-writer adding depth-matched fixture). AC-006 step 2 clause (3) updated to name both gates; §Note on absolute-path declarations updated; T-023 EXCLUDED region corrected; AC-006 step 4 (was 6) corrected. MEDIUM-2 — T-030 phase A was vacuous: fixed to describe actual tempdir fixture `{hooks-registry.toml, resolvers-registry.toml, taplo.toml}` asserting `UNEXPECTED: taplo.toml`. AC-006 Tests bullet T-030 description updated to match. LOW-2 — AC-006 step numbers renumbered to match code identifiers: hooks floor → Step 2a, resolvers floor → Step 2b, declared−tracked → Step 3, tracked−committed → Step 4, assert-both-empty → Step 5, Red Gate → Step 6, Green Gate → Step 7; Architecture Compliance Rules "steps 5–6" → "steps 3–4"; Step 5 (new) body references updated. Points unchanged at 10; status remains ready. |
| 1.9 | 2026-08-11 | Test-writer closure round at `a054513b` (27 tests T-006..T-032 all green). T-026 **inverted**: absolute form `/abs/hook-plugins/ghost-absolute.wasm` is now EXCLUDED from `declared_set` — `registry_parent.join(absolute_path)` yields an absolute path outside the registry-parent prefix; `extract_hook_plugin_name` returns `None`. Algorithm updated to new signature `extract_hook_plugin_name(registry_path: &Path, plugin_path: &str) -> Option<String>` with five steps: (1) `registry_parent.join(plugin_path)`; (2) lex_norm (push Normal, pop ParentDir, skip CurDir, clear on RootDir/Prefix); (3) prefix-assert result carries registry-parent prefix; (4) `eq_ignore_ascii_case("hook-plugins")` component match; (5) return last normalised component. New rows: T-031 (case-variant `Hook-Plugins/ghost-case.wasm` → INCLUDED via `eq_ignore_ascii_case`; mutation: reverting to `!= "hook-plugins"` strict returns `None`), T-032 (nested `hook-plugins/sub/nested.wasm` → `nested.wasm` last component; mutation: `get(expected_depth + 1)` reversion returns `sub`). T-023(b) sub-test added: traversal-that-cancels `hook-plugins/../ghost-cancels.wasm` — lex_norm pops `hook-plugins` on `ParentDir`; no `hook-plugins` component remains → returns `None` (EXCLUDED); mutation: no-op ParentDir falsely admits it. AC-006 step 2 updated: new signature, 5-step description, absolute-path out-of-scope note added. AC-006 step 6 corrected: T-026 now proves EXCLUSION (was "ARE included"). T-028/T-029 rationale refined with `resolve_registry_path()` function name and exact `find` output paths. T-014 dotslash description updated to reflect CurDir no-op via lex_norm (not `trim_start_matches("./")` chain). T-030 phase B git flags updated with `-c core.excludesFile=/dev/null -c commit.gpgsign=false -c core.hooksPath=/dev/null -c init.templateDir=`. RG-004/T-012 Green label updated to `GREEN (Red-Gate-by-design)`. Architecture tables T-012..T-030 → T-012..T-032. Library Requirements `extract_hook_plugin_name` description updated for new signature. Points unchanged at 10 (only 21 of 27 tests are S-21.09-owned; algorithm refinement is same complexity tier). Status remains ready. |
| 1.8 | 2026-08-11 | Fix AC-001 byte-provenance block: replaced instruction to "check the AC-001 commit body" with inline values. Root cause: story PRs squash-merge into develop, so the feature branch commit body does not survive; pointing provenance at `27123d27` made the values unverifiable from a fresh clone. Inline values independently reproduced at `12f280d1` via two adversarial passes (host-toolchain-specific; release pipeline rebuilds are functionally equivalent but not necessarily byte-identical): 193,427 bytes; SHA-256 `6f6570f9a776f741f6d610b841cedff8a17766136db605284f377e5305f6ce17`; git blob `611303b3b8edcebc70cf014919e2f73809e7ef52`; toolchain `rustc 1.95.0 (59807616e 2026-04-14)`. Points unchanged at 10; status remains ready. |
| 1.7 | 2026-08-11 | Pass-4 NOT-CLEAN fix burst (story-writer). HIGH-1 — fabricated directory state corrected at three sites: `plugins/vsdd-factory/config/` holds ONE file (`artifact-path-registry.yaml`) and zero .toml files; the three `hooks-registry.toml` files are bats fixtures under `tests/fixtures/validate-policies-schema/`; a recursive walk surfaces nine subdirectory .toml files total (not three). T-028 and T-029 narrowing rationales rewritten from convention/code-review to the load-path argument: `crates/factory-dispatcher/src/main.rs` resolves `hooks-registry.toml` and `resolvers-registry.toml` at `${CLAUDE_PLUGIN_ROOT}` top level only — subdirectory and case-variant files are never loaded and declare nothing that must exist. AC-006 Tests bullet T-028 description corrected to cite `tests/fixtures/validate-policies-schema/` example. HIGH-2 — Library Requirements toml crate row corrected: toml IS used — `parse_plugin_refs()` calls `content.parse::<toml::Value>()` and iterates `doc.get("hooks")`/`doc.get("resolvers")`; manifest already carried `toml.workspace = true`; no change needed; "line-matching" is no longer the mechanism; v1.6 row was derived from the absent manifest change rather than the code. MEDIUM-5 — EC-001 corrected: CI never exercises committed bytes (full wasm32 rebuild from source immediately before bats run; no `CI_REQUIRE_ARTIFACTS`; release.yml also rebuilds); committed bytes matter only in developer checkouts. Byte provenance added to AC-001: build command, toolchain (`rustc 1.95.0`), target triple, sha256 instruction. LOW-5 — AC-001 Test bullet: phantom function `test_validate_factory_path_staging_wasm_is_git_tracked()` removed; replaced with T-012 class-wise coverage note. AC-003 bats test name corrected from Rust-style to actual: `@test "T-001 S-21.01 AC-001: guard blocks git add .factory/STATE.md on develop branch"`. Opportunistic: EC-006 stale "declared_set − committed_set" → "declared_set − tracked_set" (v1.6 restructure missed). v1.6 changelog corrected: fabricated `config/` claim replaced. Points unchanged at 10; status remains ready. |
| 1.6 | 2026-08-11 | Final test-writer inventory relay (commit `12f280d1`; 25-test suite T-006..T-030 all green). S-21.09 owns T-012..T-030. New functions: `extract_hook_plugin_name(plugin_path: &str) -> Option<String>` (walks `Path::components()`, handles traversal-into/absolute/dotslash/standard/nospace; replaces old `trim_start_matches("./").strip_prefix("hook-plugins/")` chain; closes pass-3 F-1 traversal/absolute fail-opens); `run_t012_gate(root: &Path) -> Result<(), String>` (extracted from T-012 body; wired by T-030). AC-006 steps 3–7 restructured to reflect five gate arms — not three: (1) registry-inventory; (2a) hooks floor >= 30; (2b) resolvers floor >= 1; (3) `declared − tracked` via `git ls-files` emitting `MISSING:`; (4) `tracked − committed` via `git ls-tree HEAD` emitting `STAGED-NOT-COMMITTED:` — old step 5 conflated these two distinct operations. Steps 3/4 filled with test IDs: hooks floor → T-016 HIGH / T-027 LOW / T-019 1-entry; resolvers floor → T-022. Red Gate Test Plan: T-015..T-018 and T-019..T-023 placeholder rows replaced with individual rows (all six filled); T-025..T-030 rows added. T-023 boundary-polarity record corrected: v1.5 record incorrectly claimed traversal forms add no stealth path; T-025/T-026 prove traversal-into and absolute forms ARE included after `extract_hook_plugin_name` normalization; traversal-that-cancels (`hook-plugins/../ghost.wasm` → `[ghost.wasm]`) is excluded. Inventory narrowings documented in AC-006 Tests and Red Gate Plan: T-028 (non-recursive) and T-029 (case-sensitive); both deliberately retained (narrowing rationale cited incorrectly — corrected in v1.7). Library Requirements: toml crate incorrectly stated as not used — corrected in v1.7. Architecture Mapping, Purity, and Compliance tables updated T-012..T-024 → T-012..T-030. Points re-estimated 8 → 10. |
| 1.5 | 2026-08-11 | LOCAL pass-3 fix burst (story-writer): F-4 HIGH — three sites prescribed `*-registry.toml` glob (AC-006 step 1, Task 9(a), EC-006); corrected to `*.toml` at all three; rejection rationale added at each site (underscore-named `metrics_registry.toml` escapes hyphenated glob; T-024 mutation-proves this). F-5 HIGH — documented three gate arms missing from AC-006: hooks non-vacuity floor (assert len >= 30), resolvers non-vacuity floor (assert len >= 1), staged-vs-committed arm via `git ls-tree --name-only HEAD` (authorized operation, intentionally stricter than `git ls-files` used by S-19.04 T-009); AC-006 steps 1-7 expanded to 1-9; Test: bullet updated to reference T-013..T-024; T-013..T-024 added to Red Gate Test Plan (T-013/T-014 BLOCKER-1 parse controls confirmed; T-015..T-023 pending final inventory relay; T-024 underscore-registry escape confirmed); Architecture Mapping and Purity Classification updated to T-012..T-024 and git ls-tree; authorized-operation compliance rule added. F-7 MEDIUM — two fixture files added to File Structure Requirements: hooks-registry-nospace-fixture.toml and hooks-registry-dotslash-fixture.toml under crates/factory-dispatcher/tests/fixtures/bundle-orphan/. Points re-estimated 5→8 (scope is 13 tests across 3 gate arms plus fixture files vs original T-012-only estimate). Story stays ready after these corrections; would have warranted draft between pass-3 and this fix (F-4 would cause implementer to introduce defective predicate). |
| 1.4 | 2026-08-11 | LOCAL pass-3 fix burst (story-writer): corrected via independent verification that two identically-named "Verify registry-declared WASM plugins are staged" steps exist in different jobs (source of pass-2 ambiguity). §Context updated: FIRST Verify in build-binaries (hooks only); SECOND Verify in commit-binaries (dual-registry general parity check via same weak predicate); commit-binaries resolver parse is `2>/dev/null`; self-heals reasoning corrected to credit commit-binaries staging+dual-verify. §Routing Proposals sibling-sites entry expanded: site inventory updated with FIRST/SECOND labels and bats specifics (fixture-builder + T-017 mapfile negative control); two additional findings added — vacuous-pass on resolver arm (no non-vacuity floor for resolver_plugins; 2>/dev/null suppresses registry read errors) and T-017 first-match-only extractor (!found flag pins to build-binaries Verify; commit-binaries dual-registry Verify uncovered); floor-constant duplication corrected to three sites (release.yml x2 + bundle_orphan_check.rs x1); deferred story scope expanded to three concerns. Story stays ready. |
| 1.3 | 2026-08-11 | LOCAL pass-2 fix burst (story-writer): MEDIUM-2 — §Context corrected: release pipeline splits across build-binaries (builds WASMs; checks hooks-registry.toml only via weak grep predicate; has two hardcoded keep-assertions for vsdd-context-resolvers.wasm in Stage-artifact-directory step) and commit-binaries (downloads artifacts; stages with git add -f; checks both registries via same weak predicate); routing conclusion (no POLICY 20 amendment) restated to hold under this corrected understanding. MEDIUM-3 — Task 2 cp source path corrected from validate_factory_path_staging.wasm (underscored, does not exist) to validate-factory-path-staging.wasm (hyphenated; crate declares [[bin]] name = "validate-factory-path-staging"; [lib] target produces .rlib, not a .wasm). LOW-1 — invocation-count figure restated as ≥14,000 as of 2026-08-11 (live counter; siblings differ; exact-figure rot prevented); post-fix evidence sentence added (4 records timestamped 2026-08-11T08:55 from preliminary AC-001 test; 0-records claim for gap window still reproduces); swept 2 story sites carrying stale exact figure. Addition — §Routing Proposals entry added for 5 sibling weak-predicate sites (3 in release.yml + 2 in pr-manager-hardening.bats) and >=30 floor-constant duplication; POLICY 5 sibling-sweep disposition recorded; remediation deferred per human standing ruling. Story status stays ready; all ACs implementable as written. |
| 1.2 | 2026-08-11 | LOCAL pass-1 fix burst (story-writer): HIGH-3 — AC-002 rewritten; `_require_artifacts` gates on two artifacts (dispatcher + WASM); committing the WASM reduces WASM-absent skips from 25 to 0 (not total skips); precondition and assertion restated correctly. HIGH-2 — EC-006 rewritten; T-012 (bundle_orphan_check.rs) declared→tracked assertion stays GREEN if a third registry is added (names from unparsed registry never enter `declared`); defense is registry-inventory assertion (AC-006 impl step 1) that enumerates plugins/vsdd-factory registry toml files and asserts exactly the two known registries; forces test update before new registry can be ignored. MEDIUM-3 — Library & Framework Requirements toml crate row corrected: `parse_plugin_refs()` is a hand-rolled line matcher (not toml-crate-based); hardening to toml crate now in scope for T-012. MEDIUM-1 — 27→25 sweep at 4 sites (AC-002 x2, RG-003, RG-005): verified 25 call sites by grep excluding definition and comment lines. MEDIUM-2 — AC-007 mechanism corrected: `plugin =` key-value grep yields 75 hits; 35 unique names; 36th token is comma-terminated `legacy-bash-adapter.wasm,` from prose comment (path-token grep artefact). LOW-2 — T-012 qualified as `T-012 (bundle_orphan_check.rs)` at all reference sites (T-012 already used in validate-factory-path-staging.bats for S-21.01 AC-009). AC-006 impl steps renumbered with registry-inventory assertion as step 1. Tasks 8/9 revised; tasks renumbered through 14. Points remain 5. |
| 1.1 | 2026-08-10 | Elaboration pass (story-writer; human-directed): corrected root cause to release-lag (rc.23 tagged 5 days before S-21.01 merge; operator runtime has zero occurrences of validate-factory-path-staging in registry); removed POLICY 20 misattribution (POLICY 20 IS registered at id:20 in policies.yaml); removed pending-human block; corrected non-invocation magnitude to 0 events vs ≥14,000 for sibling guards (figure was 14,151 on 2026-08-10; live counter, grows daily — see v1.3 for restatement); corrected masking mechanism to ci.yml Stage-WASM-plugins build step + _require_artifacts skip-pass on absent CI_REQUIRE_ARTIFACTS; re-anchored AC-006 to extend crates/factory-dispatcher/tests/bundle_orphan_check.rs (S-19.04/S-19.06 existing machinery) — removed crates/plugin-parity-check/ new-crate proposal; restated AC-002 against bats harness no-skip gate; restated AC-003/004/005 to acknowledge T-001/T-002/T-004 exist and are green in CI; corrected declared-WASM count to 35 hooks-registry (not 36 — spurious prose token excluded); added BC Status section (BC-4.16.001 v1.8 active per POL-14/S-21.01); added Red Gate Test Plan section; added S-19.04/S-19.06 to Previous Story Intelligence; updated File Structure Requirements; added VP gap routing proposal; re-estimated points 8→5; status draft→ready. |
| 1.0 | 2026-08-04 | Initial authoring (story-writer; human-directed; E-21 W4; BC-4.16.001 v1.8; ADR-031 §Decision 2+3; validate-factory-path-staging.wasm declared-but-missing root cause; per-name registry parity CI check; both halves in scope) |
