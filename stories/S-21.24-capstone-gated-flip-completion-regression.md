---
document_type: story
level: implementation
story_id: "S-21.24"
epic_id: "E-21"
version: "1.1"
status: draft
producer: story-writer
timestamp: 2026-08-20T00:00:00Z
phase: brownfield-backfill
inputs:
  - .factory/specs/architecture/decisions/ADR-039-validator-failure-policy-resource-exhaustion-fail-closed.md
  - .factory/specs/architecture/decisions/ADR-044-split-topology-enforcement-flip-capstone-ownership.md
  - plugins/vsdd-factory/hooks-registry.toml
input-hash: "e3c75a4"
traces_to: .factory/specs/architecture/decisions/ADR-039-validator-failure-policy-resource-exhaustion-fail-closed.md
points: "5"
depends_on: [S-21.19, S-21.20, S-21.21, S-21.22, S-21.23]
blocks: [S-21.16]
behavioral_contracts: ["BC-1.03.017 v1.18", "BC-1.03.018 v1.1"]
verification_properties: []
priority: "P1"
cycle: v1.0-brownfield-backfill
wave: 8
target_module: crates/factory-dispatcher
subsystems: [SS-01, SS-07]
estimated_days: null
assumption_validations: []
risk_mitigations: []
tdd_mode: strict
modified:
  - "2026-08-20 (v1.1) — F-S2119-P1-001 remediation (BLOCKER, S-21.19's own pre-TDD adversarial convergence pass): per ADR-044 (.factory/specs/architecture/decisions/ADR-044-split-topology-enforcement-flip-capstone-ownership.md) and .factory/planning/S-21.11-decomposition-plan.md §8, this story now owns the enforcement-active executor wiring commit itself — NEW Task 0, wiring execute_tiers/execute_tier's real block-decision call site to the 3-arg plugin_fail_closed S-21.19 authored (standalone, unit-tested, previously left unwired). Task 0 lands strictly after Tasks 1-2's five-plugin annotation commits (annotate-before-flip by wave-schedule construction). Receives AC-002 (bats/integration leg) and AC-011 (TC-12 integration-mirror leg) as new owned ACs, moved here from S-21.19 (whose unit legs are unaffected). New Tasks 3-4 author and confirm GREEN these two legs. Existing Tasks 3/4 (confirmation, cargo test) renumbered 5/6; existing Tasks 5/6 (fmt/clippy, CHANGELOG) renumbered 7/8. `points:` 3 -> 5 (combined S-21.19+S-21.24 point total unchanged at 12 — a redistribution, not a scope increase). `inputs:` adds ADR-044. This closes BLOCKER F-S2119-P1-001: at no commit in develop's history does PC11 ever observe enforcement-active with any of the five targeted plugins unannotated, because the wiring commit (this story's Task 0) does not exist until the same story that also confirms all five annotations are already present. Zero depends_on/blocks edges or wave assignment changed. Does NOT touch S-21.19/S-21.20/S-21.21/S-21.22/S-21.23 (verified unaffected per decomposition plan §8.4) or any INDEX/STATE.md (state-manager's domain)."
  - "2026-08-20 (v1.0) — created by story-writer via the operator-directed split of CONVERGED S-21.11 (v2.11, 16-pass 3-CLEAN BC-5.39.001 convergence) per `.factory/planning/S-21.11-decomposition-plan.md` §1 (S-21.24 detail) and §3 (AC partition table). Owns zero ACs exclusively — confirms AC-009/AC-012 (owned by S-21.19) and AC-022 (owned by S-21.23) reach their final GREEN/LIVE-TREE state, and delivers the flip execution + full regression + CHANGELOG Definition-of-Done tasks. Tasks renumbered #1–#6 (self-contained) from S-21.11 v2.11 Tasks #27, #28, #29, #30, #31, #32. STRICTLY LAST of the six split seams — depends on all five others."
---

> **tdd_mode: strict** — Full TDD Iron Law enforced: all non-trivial function bodies use `todo!()`;
> Red Gate density check ≥0.5 required before Step 4 dispatch.

> **Execute:** `/vsdd-factory:deliver-story S-21.24`

> **Split origin (2026-08-20).** One of six sub-stories partitioning CONVERGED S-21.11 (v2.11,
> 16-pass 3-CLEAN BC-5.39.001 convergence) per the operator-directed override of S-21.11's own
> prior "unified scope, no split" decision, documented in
> `.factory/planning/S-21.11-decomposition-plan.md`. S-21.24 is the capstone — the DAG's Phase 5
> convergence point — and depends on ALL FIVE other split seams (S-21.19, S-21.20, S-21.21,
> S-21.22, S-21.23). It owns no acceptance criterion exclusively; it confirms the final-state
> assertions of ACs owned elsewhere (S-21.19's AC-009/AC-012; S-21.23's AC-022) and executes
> the remaining flip commits, full regression, and CHANGELOG delivery. S-21.11's v2.11 body is
> frozen as the historical source-of-truth this story's tasks are extracted from verbatim; no
> task text or gate discipline was weakened, dropped, or reinterpreted during partitioning.

# S-21.24: Validator exhaustion fail-closed capstone — gated flip completion + full regression + CHANGELOG (ADR-039 Phase 4c completion + Phase 5; split seam 6 of 6, STRICTLY LAST)

## Narrative

- **As a** factory pipeline operator who needs the full six-plugin CWE-636 closure to reach a
  provably complete, regression-tested final state
- **I want** the three remaining non-Agent bash-adapter plugins and the two self-locking Agent
  gates flipped to `failure_policy = "fail-closed"` using S-21.21's calibrated values —
  the Agent gates gated on S-21.23's break-glass code already being merged — with S-21.19's
  AC-009/AC-012 and S-21.23's AC-022 confirmed at their final GREEN/LIVE-TREE state, a full
  `cargo test --workspace --all-targets` pass, and a CHANGELOG entry documenting the complete
  closure
- **So that** the split's six sub-stories converge to exactly the same final state the
  originally-unified S-21.11 v2.11 would have produced, with no functionality lost or silently
  weakened by the partitioning

## Context / Finding Summary

This story executes the remaining Phase 4c flip commits — the three non-Agent bash-adapter
plugins (`validate-factory-path-root`, `validate-input-hash`, `validate-template-compliance`)
using S-21.21's calibrated values, and the two self-locking `PreToolUse` `^Agent$` gates
(`validate-wave-gate-prerequisite`, `validate-pr-merge-prerequisites`) using S-21.21's
calibrated values gated on S-21.23's break-glass code already being merged (AC-022's mechanical
ordering gate). It confirms S-21.19's AC-009 (all six plugins fail-closed — or the reduced
five-plugin set if S-21.22's EC-004 escape valve fired), confirms S-21.19's AC-012's fourth
control (LIVE-TREE-CONTROL, against the real `executor.rs`/`hooks-registry.toml`), and
confirms S-21.23's AC-022 LIVE-TREE-CONTROL against the live tree post-flip. It runs
`cargo test --workspace --all-targets`, `cargo fmt --check --all && cargo clippy --workspace
--all-targets -- -D warnings`, and adds the `[Unreleased] > Security` CHANGELOG entry
documenting the complete six-seam closure. It is STRICTLY LAST — it depends on all five other
seams having merged.

**Task-ownership note (per `.factory/planning/S-21.11-decomposition-plan.md` §6):** the
original DAG's Task #26 (the native-WASM flip) is owned by **S-21.22**, not this story — that
seam has no dependency on S-21.21/S-21.23 and lands its own flip as soon as S-21.19 merges.
This story owns Tasks #27–#32 (the three-plugin flip, the two-Agent-gate flip, the final-state
confirmations, regression, lint, and CHANGELOG) **plus, per
`.factory/planning/S-21.11-decomposition-plan.md` §8 (F-S2119-P1-001 remediation, ADR-044), a
NEW Task 0** — the enforcement-active executor wiring commit, formerly (incorrectly) placed in
S-21.19 — **and two tasks moved here from S-21.19**: AC-002's bats/integration leg and
AC-011's `TC-12` integration-mirror leg, both of which require Task 0's wiring to exist before
they can be authored meaningfully.

**EC-004 conditional carry-through:** if S-21.22's calibration triggered EC-004 (Case A —
`validate-cross-site-correspondence`'s required `fuel_cap` exceeds `PRACTICAL_FUEL_CEILING`),
S-21.19's AC-009 test was renamed/parameterized to the reduced five-plugin set by S-21.22, per
that AC's own EC-004 conditional mechanism. This story's confirmation task (Task 5 below)
asserts against whatever set S-21.19's test currently enumerates — it does not hardcode "six"
independently of that upstream state.

## Position in the Split DAG

```
S-21.19 ──┬──▶ S-21.20 ──┐
          ├──▶ S-21.21 ──┤
          ├──▶ S-21.22 ──┼──▶ S-21.24 (this story, STRICTLY LAST) ──▶ S-21.16 (recommended
          └──▶ S-21.23 ──┘                                            redirect)
```

The full six-seam dependency graph and wave schedule live in
`.factory/planning/S-21.11-decomposition-plan.md` §2. This story is the sole wave-8 gate for
the split (alongside S-21.13, which runs in parallel, gated only by S-21.22).

## Acceptance Criteria

**Revised 2026-08-20 (F-S2119-P1-001 remediation, ADR-044,
`.factory/planning/S-21.11-decomposition-plan.md` §8):** this story now owns two AC legs
exclusively — AC-002's bats/integration leg and AC-011's `TC-12` integration-mirror leg, both
moved here from S-21.19 because both require this story's own Task 0 wiring to exist before
they can be authored meaningfully. Beyond those two legs, this story owns **no acceptance
criterion exclusively** — see `.factory/planning/S-21.11-decomposition-plan.md` §1 (S-21.24
detail) and §3 (AC partition table, verification arithmetic). It confirms the final-state
assertions of three ACs owned elsewhere, as gate conditions for its own Tasks 5–6.

### AC-002 (bats/integration leg only — SPLIT, moved from S-21.19; unit leg owned by S-21.19) (traces to BC-1.03.017 PC1; BC-1.01.016 PC1/PC5; ADR-039 §Decision 6 test case 1)

A bats test MUST verify that a real dispatch — through the now-wired
`execute_tiers`/`execute_tier` block-decision call site (Task 0 above) — with a plugin
configured `failure_policy = "fail-closed"` and a deliberately tiny `fuel_cap = 100` produces
dispatcher exit code 2 (BLOCK). The `fuel_cap = 100` + `failure_policy = "fail-closed"` entry
MUST live in a dedicated TEST-FIXTURE registry (NOT
`plugins/vsdd-factory/hooks-registry.toml`), so that S-21.19's AC-001 gate scan does not trip
on this fixture. The `.bats` test MUST be registered in the Bats harness at
`plugins/vsdd-factory/tests/`; no new `.sh` file may be introduced per POLICY 21.

- **Test:** bats integration test (TEST-FIXTURE registry only, NOT production registry) —
  dispatch with `fuel_cap = 100` + `failure_policy = "fail-closed"` on a budget-exhausting
  payload → observed dispatcher exit code 2 asserted. See Task 3 below.

### AC-011 (`TC-12` integration-mirror leg only — SPLIT, moved from S-21.19; unit leg owned by S-21.19) (traces to BC-1.03.017 v1.18 PC10; TD-VSDD-059)

The existing E2E integration test
`test_e2e_BC_7_06_001_sync_hook_timeout_fail_closed_on_error_block` (TC-12) in
`crates/factory-dispatcher/tests/full_stack_plugin_invocation.rs` currently constructs
`on_error=Block + failure_policy=FailOpen (registry default) + Timeout{Epoch}` and asserts
`exit_code == 2`. Under the axes-independent semantics S-21.19 mandates (AC-005(b) / EC-009:
`Timeout{Fuel|Epoch} + on_error=Block + failure_policy=FailOpen → NOT block → exit 0`), that
assertion becomes FALSE — TC-12 MUST be deliberately revised (TD-VSDD-059, non-deletion) to
assert the new semantics, meaningfully assertable only once Task 0's wiring exists:

- **TC-12 arm (a) [MUST]:** `on_error=Block + failure_policy=FailOpen + Timeout{Fuel|Epoch} → exit 0`
- **TC-12 arm (b) [SHOULD]:** `on_error=Block + failure_policy=FailClosed + Timeout{Fuel|Epoch} → exit 2`

Deletion of TC-12 without an equivalent replacement is a TD-VSDD-059 (paper-fix) violation.
The revision MUST appear in the PR diff; correct the stale `BC-7.06.001 Invariant 1`
doc-comment mis-citation to `BC-1.03.017 PC5`.

- **Test:** revised TC-12 — arm (a) [MUST], arm (b) [SHOULD]; MUST exist in the PR diff as a
  deliberate revision, not a deletion. See Task 4 below.

### Confirmation-only ACs (owned elsewhere, confirmed here as gate conditions for Tasks 5–6)

- **AC-009** (owned by S-21.19, `test_all_six_validator_class_plugins_are_fail_closed`):
  authored RED-first by S-21.19; this story's Tasks 1–2 (the flip commits) are what turn it
  GREEN. Task 5 below confirms the GREEN state — it does not re-author or re-claim ownership
  of the test.
- **AC-012** (owned by S-21.19, `test_no_on_error_block_without_fail_closed_when_3arg_executor`):
  authored with three pure-function controls by S-21.19; this story's Task 5 confirms the
  fourth control, LIVE-TREE-CONTROL, against the actual post-flip tree — genuinely meaningful
  for the first time now that Task 0's wiring exists.
- **AC-022** (owned by S-21.23, `test_no_agent_gate_fail_closed_flip_without_break_glass`):
  authored with three pure-function controls by S-21.23; this story's Task 5 confirms the
  fourth control, LIVE-TREE-CONTROL, against the actual post-flip tree (both Agent gates now
  carrying `failure_policy="fail-closed"` with the break-glass code already merged).

## Behavioral Contracts

| BC ID | Version | Title | Clause |
|-------|---------|-------|--------|
| BC-1.03.017 | v1.18 | factory-dispatcher::executor::failure_policy enforcement — exhaustion-outcome dispatch (fail-closed→block; fail-open→advisory), on_error axes independence, crash-versus-exhaustion distinct paths, Phase-3-before-Phase-4 structural half-state gate, migration-window on_error=block completeness gate, and legacy-bash-adapter runtime-timeout-wiring verification | PC1–PC6/PC10 (integration legs via AC-002/AC-011), PC9 (AC-009 final-state confirmation), PC11 (AC-012 LIVE-TREE-CONTROL confirmation) |
| BC-1.03.018 | v1.1 | factory-dispatcher `execute_tiers` break-glass override — `VSDD_BREAK_GLASS_GATE_BYPASS` environment-variable escape hatch for the two self-locking PreToolUse `^Agent$` gates (`validate-wave-gate-prerequisite`, `validate-pr-merge-prerequisites`), with mandatory `break_glass.activated` JSONL audit, intra-S-21.11 pre-flip-ordering gate, and human-operator-only Agent-tool-cannot-arm threat-boundary control | PC9 (AC-022 LIVE-TREE-CONTROL confirmation) |
<!-- POLICY 7: BC-table Title cell MUST remain a verbatim SUBSET of the corresponding BC's H1. -->

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| `hooks-registry.toml` annotations (five remaining entries: three non-Agent bash-adapter plugins + two Agent gates) | `plugins/vsdd-factory/hooks-registry.toml` | Config (calibrated `fuel_cap`/`timeout_ms` + `failure_policy = "fail-closed"`, applied using S-21.21's calibration evidence) |

## Purity Classification

N/A — this story performs no new production-code changes; it applies configuration
(registry annotations using upstream-calibrated values) and runs confirmation/regression
gates. No new Pure-core or Effectful module is introduced.

## Edge Cases

N/A for this story specifically. If S-21.22's EC-004 fired, this story's Task 5 confirms
against the reduced five-plugin set per S-21.19's own EC-004 conditional mechanism — see
"Context / Finding Summary" above. The full 14-row Edge Cases table lives in S-21.11 v2.11
(frozen historical source-of-truth), partitioned across S-21.19/S-21.21/S-21.22/S-21.23.

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | 5,200 |
| BC-1.03.017 v1.18 (PC1–PC6, PC9–PC11 — Task 0 wiring context + AC-002/AC-011 integration legs + final-state clauses) | 2,200 |
| BC-1.03.018 v1.1 (PC9 — final-state clause only) | 500 |
| `crates/factory-dispatcher/src/executor.rs` (Task 0 wiring: `execute_tiers`/`execute_tier` block-decision call site) | 4,000 |
| `crates/factory-dispatcher/tests/full_stack_plugin_invocation.rs` (TC-12 revision, Task 4) | 1,500 |
| `plugins/vsdd-factory/hooks-registry.toml` (five remaining entries + calibration evidence from S-21.21) | 1,200 |
| Bats fixture registry + fixture payload (Task 3, AC-002 leg) | 800 |
| Full `cargo test --workspace --all-targets` output (regression confirmation, not source code) | 2,000 |
| **Total** | **~17,400** |
| Agent context window | 200K (Sonnet) |
| **Budget usage** | **~8.7%** |

**Budget note:** revised 2026-08-20 (F-S2119-P1-001 remediation, ADR-044) — this story now
performs the Task 0 wiring commit and authors AC-002's bats leg and AC-011's `TC-12` leg
(moved from S-21.19), so its context footprint grows from ~8,200 to ~17,400 tokens. Still well
under the 20–30% ceiling: it applies pre-computed calibration values from S-21.21 and confirms
pre-authored tests owned elsewhere; it does not re-derive calibration or re-author S-21.19's
own decision-function logic.

## Tasks (MANDATORY)

This story's tasks are the Phase 4c completion + Phase 5 subset of S-21.11 v2.11's original
DAG, renumbered self-contained **#0–#8** (source: S-21.11 v2.11 Tasks #27, #28, #29, #30, #31,
#32, plus a NEW Task 0 and two tasks moved here from S-21.19, per ADR-044 and
`.factory/planning/S-21.11-decomposition-plan.md` §8, F-S2119-P1-001 remediation). Task #26
(the native-WASM flip) is owned by S-21.22, not this story. **Task 0** is the
enforcement-active executor wiring commit — formerly (incorrectly) assumed to already exist by
the time this story starts, per the original S-21.19 placement; this story now performs that
wiring itself, strictly after Tasks 1–2's annotation commits. **Tasks 3 and 4** (AC-002's bats
leg and AC-011's `TC-12` integration-mirror leg) are moved here from S-21.19 for the same
reason — both require Task 0's wiring to exist before they are meaningful.

0. [ ] **[NEW — F-S2119-P1-001 remediation, ADR-044]** Wire `execute_tiers`/`execute_tier`'s
       real block-decision call site to invoke S-21.19's 3-arg `plugin_fail_closed` using
       `PluginOutcome.failure_policy`, replacing the prior 2-arg call. This is the
       enforcement-active flip — perform it only after confirming (a) S-21.21's calibration
       evidence for the three non-Agent bash-adapter plugins is present, and (b) S-21.23's
       break-glass code is merged. This task and Tasks 1–2 (the registry annotation commits)
       together satisfy annotate-before-flip: the wiring in this task MUST NOT land in a
       commit tree where any of the five targeted plugins lacks its annotation — sequence
       this task's commit strictly after (or atomically with) Tasks 1–2's annotation commits,
       never before. This is the single commit that makes PC11's design-flow-independent
       detector observe "enforcement-active" (BC-1.03.017 v1.18 Invariant 7 + PC11; ADR-044).
1. [ ] Flip `validate-factory-path-root`, `validate-input-hash`, `validate-template-compliance`:
       for each, annotate `plugins/vsdd-factory/hooks-registry.toml` with S-21.21's calibrated
       `fuel_cap` AND `timeout_ms` + `failure_policy = "fail-closed"`. Per ADR-044/§8, S-21.19
       does NOT wire the extended decision function into the real block-decision chain — that
       wiring is THIS story's own Task 0 above. These annotation commits (Tasks 1–2) MUST land
       BEFORE (or atomically with) Task 0's wiring commit — never after — so that
       annotate-before-flip holds.
2. [ ] Flip `validate-wave-gate-prerequisite`, `validate-pr-merge-prerequisites`: for EACH,
       confirm S-21.23's `test_no_agent_gate_fail_closed_flip_without_break_glass` is GREEN
       against the LIVE tree (i.e. S-21.23's break-glass code is already merged) BEFORE this
       commit lands. Annotate `plugins/vsdd-factory/hooks-registry.toml` with S-21.21's
       calibrated `fuel_cap` AND `timeout_ms` + `failure_policy = "fail-closed"`. Per
       ADR-044/§8, S-21.19 does NOT wire the extended decision function into the real
       block-decision chain — that wiring is THIS story's own Task 0 above. This is the DAG's
       hardest ordering constraint — do not flip either of these two plugins without first
       confirming S-21.23's AC-022 gate passes, and do not land Task 0's wiring commit until
       both this task's and Task 1's annotation commits are already present.
3. [ ] **[MOVED FROM S-21.19, F-S2119-P1-001/ADR-044]** Author and confirm GREEN AC-002's
       bats/integration leg: create a fuel-exhausting fixture in
       `plugins/vsdd-factory/tests/fixtures/` and a TEST-FIXTURE registry (NOT the production
       `hooks-registry.toml`); add a `.bats` test registered in
       `plugins/vsdd-factory/tests/run-all.sh` verifying a real dispatch with
       `failure_policy = "fail-closed"` and `fuel_cap = 100` produces dispatcher exit code 2;
       confirm NO new `.sh` file is introduced (POLICY 21; `.bats` files and
       `tests/fixtures/` entries are exempt). This leg requires Task 0's wiring to already be
       landed — do not attempt to author it before Task 0.
4. [ ] **[MOVED FROM S-21.19, F-S2119-P1-001/ADR-044]** Author and confirm GREEN AC-011's
       `TC-12` integration-mirror revision: revise
       `test_e2e_BC_7_06_001_sync_hook_timeout_fail_closed_on_error_block` in
       `crates/factory-dispatcher/tests/full_stack_plugin_invocation.rs` — arm (a) [MUST]
       asserts `on_error=Block + failure_policy=FailOpen + Timeout{Epoch} → exit 0`; arm (b)
       [SHOULD] asserts `on_error=Block + failure_policy=FailClosed + Timeout{Epoch} → exit 2`;
       MUST be deliberately revised, not deleted (TD-VSDD-059); correct the stale
       `BC-7.06.001 Invariant 1` doc-comment mis-citation to `BC-1.03.017 PC5`. This leg
       requires Task 0's wiring to already be landed — do not attempt to author it before
       Task 0. Without this revision, `cargo test --workspace --all-targets` (Task 6 below)
       will be RED post-flip (TC-12's stale `exit_code == 2` assertion fails under the
       axes-independent semantics).
5. [ ] Confirm `test_all_six_validator_class_plugins_are_fail_closed` (S-21.19's AC-009,
       authored RED-first there) is now GREEN following this story's flip commits (Tasks 1–2)
       and S-21.22's own flip (or S-21.13 redirect, if EC-004 fired). Confirm
       `test_no_on_error_block_without_fail_closed_when_3arg_executor` (S-21.19's AC-012,
       authored there with three pure-function controls) now passes its fourth control,
       LIVE-TREE-CONTROL: run the detector against the ACTUAL
       `crates/factory-dispatcher/src/executor.rs` and the ACTUAL
       `plugins/vsdd-factory/hooks-registry.toml` and assert `enforcement_active = true` AND
       all five `on_error="block"` targeted plugins carry `failure_policy = "fail-closed"` —
       now genuinely meaningful for the first time, since Task 0's wiring commit is what makes
       enforcement-active in the real tree.
       Confirm `test_no_agent_gate_fail_closed_flip_without_break_glass` (S-21.23's AC-022,
       authored there with three pure-function controls) now passes its fourth control,
       LIVE-TREE-CONTROL: run the detector against the ACTUAL `executor.rs` and
       `hooks-registry.toml` and assert both named `^Agent$` gates report the correct ordering
       state (break-glass code present, both gates flipped). Do NOT scan for
       `fn plugin_fail_closed(` by name for AC-012's/AC-022's detection (name-coupled
       false-green risk); do NOT bare-grep `failure_policy:` across all of `executor.rs` — a
       pre-existing `failure_policy: crate::registry::FailurePolicy::FailOpen` literal exists
       inside the test function `emit_lifecycle_timeout_carries_fuel_cap_and_consumed`, which
       would false-match a bare grep; scope detection to `.failure_policy` DOT-ACCESS within
       the block-decision chain.
6. [ ] Run `cargo test --workspace --all-targets`; confirm all new and existing tests pass
       across all six split seams (S-21.19 through S-21.23's tests plus this story's own Task
       0–5 work). **NOTE (revised, F-S2119-P1-001/ADR-044):** TC-12's revision is now THIS
       story's own Task 4, not S-21.19's — this precondition is satisfied by construction of
       this story's own task ordering (Task 4 runs before Task 6), not by an upstream
       dependency. Pre-revision, TC-12's stale `exit_code == 2` assertion would be FALSE under
       the axes-independent semantics, making the test suite RED.
7. [ ] Run `cargo fmt --check --all && cargo clippy --workspace --all-targets -- -D warnings`
8. [ ] Add CHANGELOG entry under [Unreleased] > Security:
       "Validator-class WASM plugins (validate-factory-path-root, validate-input-hash,
       validate-template-compliance, validate-wave-gate-prerequisite,
       validate-pr-merge-prerequisites, validate-cross-site-correspondence) now fail closed
       on fuel/timeout exhaustion — CWE-636 closed (ADR-039 Decision 2+3+4+6; per-plugin
       calibrated fuel_cap >= 50M and, for legacy-bash-adapter.wasm-hosted plugins,
       timeout_ms >= 30_000; failure_policy = fail-closed). legacy-bash-adapter now honors the
       registry's calibrated timeout_ms at the actual subprocess-kill deadline instead of a
       hardcoded 60s constant (ADR-039 §AMD-002). The two self-locking PreToolUse ^Agent$
       gates carry a human-operator-only, audited break-glass override
       (VSDD_BREAK_GLASS_GATE_BYPASS) to prevent an unrecoverable dispatch wedge (ADR-039
       §Decision 3). Delivered across six sub-stories (S-21.19 through S-21.24) per the
       2026-08-20 operator-directed split of the originally-unified S-21.11."
       **If S-21.22's EC-004 fired**, adjust the plugin list in this entry to the five
       flippable plugins and add a note that `validate-cross-site-correspondence` is deferred
       to S-21.13.

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-21.19 (W6) | Authors AC-009 RED-first, AC-012's three pure-function controls, and (per ADR-044/§8 F-S2119-P1-001 remediation) the standalone 3-arg `plugin_fail_closed` extension as a pure function — NOT wired into the real block-decision chain. This story's Tasks 1–2 (registry annotations) turn AC-009 GREEN; this story's OWN Task 0 (the wiring, no longer S-21.19's) plus Tasks 1–2 together enable AC-012's LIVE-TREE-CONTROL. S-21.19 also hands off AC-002's bats leg and AC-011's `TC-12` leg to this story (Tasks 3–4) — S-21.19 delivers only their unit legs. | Do not re-author AC-009/AC-012's core tests here — Task 5 is a confirmation-only step for those. DO author AC-002's bats leg and AC-011's `TC-12` leg fresh here (Tasks 3–4) — S-21.19 deliberately left them unauthored, pending this story's Task 0 wiring. | If S-21.19's AC-009 test was renamed/parameterized by S-21.22 due to EC-004, Task 5 must assert against that renamed test, not assume "six" plugins unconditionally. Do not assume S-21.19 already wired the extended `plugin_fail_closed` function — verify Task 0 is this story's own first action, not an inheritance from S-21.19. |
| S-21.21 (W7) | Produces the calibration evidence (per-plugin `fuel_cap`/`timeout_ms`) this story's Task 1–2 apply to the registry; does NOT itself annotate the registry. | This story is the sole consumer of S-21.21's calibration log — verify the PR description/calibration artifact is present and current (post-wiring-fix) before applying values. | If S-21.21's calibration data predates its own AMD-002 wiring fix, it is invalid evidence (BC-1.03.017 v1.18 Precondition 5) — S-21.21's own Task 8 gate should have caught this, but this story's implementer should sanity-check the calibration log's stated methodology before applying values. |
| S-21.22 (W7) | Owns and lands the native-WASM flip itself (not this story) — see the plan's §6 task-ownership resolution. | This story's Task 5 confirmation of AC-009 depends on S-21.22's flip (or S-21.13 redirect) having already landed — this story's own `depends_on` includes S-21.22 for exactly this reason. | Do not attempt to flip `validate-cross-site-correspondence` in this story — that is out of scope (S-21.22's responsibility) and would violate AC-008's atomic-commit discipline by duplicating an annotation. |
| S-21.23 (W7) | Authors AC-022's three pure-function controls and the break-glass mechanism this story's Task 2 gates on. | Task 2's ordering check (confirm AC-022 GREEN against the live tree BEFORE flipping either Agent gate) is the single hardest ordering constraint in the whole six-seam split — do not relax it; Task 0's wiring commit must ALSO wait until Task 2 (and Task 1) land. | None new beyond what S-21.23's own story documents. |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| **The executor wiring flip (Task 0) MUST NOT land in a commit tree where any of the five `on_error="block"` targeted plugins lacks `failure_policy="fail-closed"`** | BC-1.03.017 v1.18 Invariant 7 + PC11; ADR-044 | AC-012 CI gate (LIVE-TREE-CONTROL); PR reviewer verifies Task 0's commit sequencing against Tasks 1–2 |
| The two Agent-gate flips (Task 2) MUST NOT land before S-21.23's break-glass code is confirmed merged and its AC-022 gate is GREEN | BC-1.03.018 PC9 + Invariant 6; ADR-039 §Decision 3 v1.10 amendment | Task 2 explicit precondition check; PR reviewer verifies commit ordering |
| `cargo test --workspace --all-targets` MUST be run only after Tasks 0–4 (wiring, annotations, AC-002/AC-011 legs) land | BC-1.03.017 v1.18 PC10; this story's own task ordering | Task 6's explicit note; this story's own task sequencing enforces it by construction (no longer an upstream S-21.19 dependency, per ADR-044) |
| This story does NOT annotate `validate-cross-site-correspondence` | Split scope boundary; S-21.22 owns that flip | PR reviewer verifies no edit to that entry appears in this story's diff (unless correcting a defect surfaced by S-21.22) |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| Rust toolchain (workspace pinned) | `rust-toolchain.toml` | Running the full regression suite |
| serde / toml (workspace) | workspace version | Registry annotation edits (Tasks 1–2) |

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| `crates/factory-dispatcher/src/executor.rs` | modify | Task 0: wire `execute_tiers`/`execute_tier`'s real block-decision call site to invoke the 3-arg `plugin_fail_closed` using `PluginOutcome.failure_policy`, replacing the prior 2-arg call |
| `plugins/vsdd-factory/hooks-registry.toml` | modify (five entries) | Add calibrated `fuel_cap` + `timeout_ms` (from S-21.21) + `failure_policy = "fail-closed"` to `validate-factory-path-root`, `validate-input-hash`, `validate-template-compliance`, `validate-wave-gate-prerequisite`, `validate-pr-merge-prerequisites` |
| `crates/factory-dispatcher/tests/full_stack_plugin_invocation.rs` | modify | Task 4: revise TC-12 (`test_e2e_BC_7_06_001_sync_hook_timeout_fail_closed_on_error_block`) — new arm (a) MUST, arm (b) SHOULD; correct stale BC-7.06.001 Invariant 1 trace to BC-1.03.017 PC5 (moved from S-21.19) |
| `plugins/vsdd-factory/tests/fixtures/` (new fuel-exhausting fixture + TEST-FIXTURE registry) | create | Task 3: AC-002's bats/integration leg fixture (moved from S-21.19) |
| `plugins/vsdd-factory/tests/run-all.sh` | modify (register new `.bats` test) | Task 3: register the AC-002 bats test in the harness |
| `CHANGELOG.md` | modify (Security section) | Mandatory CHANGELOG delivery (Task 8) — the single combined entry for the full six-seam closure |

## Changelog

| Version | Date | Description |
|---------|------|--------------|
| 1.1 | 2026-08-20 | F-S2119-P1-001 remediation (BLOCKER) per ADR-044 (`.factory/specs/architecture/decisions/ADR-044-split-topology-enforcement-flip-capstone-ownership.md`) and `.factory/planning/S-21.11-decomposition-plan.md` §8: this story now owns the enforcement-active executor wiring commit itself — NEW Task 0, wiring `execute_tiers`/`execute_tier`'s real block-decision call site to the 3-arg `plugin_fail_closed` S-21.19 authored (standalone, unit-tested, previously left unwired), landed strictly after Tasks 1–2's five-plugin annotation commits (annotate-before-flip by wave-schedule construction). Receives AC-002 (bats/integration leg) and AC-011 (`TC-12` integration-mirror leg) as new owned ACs, moved here from S-21.19. New Tasks 3–4 author and confirm GREEN these two legs; existing Tasks 3/4 renumbered 5/6; existing Tasks 5/6 renumbered 7/8. `points:` 3 -> 5 (combined S-21.19+S-21.24 point total unchanged at 12). `inputs:` adds ADR-044. |
| 1.0 | 2026-08-20 | Created by story-writer via the operator-directed 6-way split of CONVERGED S-21.11 (v2.11, 16-pass 3-CLEAN BC-5.39.001), per `.factory/planning/S-21.11-decomposition-plan.md` §1/§3/§6. Owns zero ACs exclusively — confirms S-21.19's AC-009/AC-012 and S-21.23's AC-022 final-state assertions, and delivers the remaining flip commits, full regression, lint, and the single combined CHANGELOG entry for the six-seam closure. Tasks renumbered #1–#6 (self-contained) from S-21.11 v2.11 Tasks #27, #28, #29, #30, #31, #32. STRICTLY LAST — depends on S-21.19, S-21.20, S-21.21, S-21.22, S-21.23 (all five other split seams). |
