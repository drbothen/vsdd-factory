---
document_type: story
level: ops
story_id: "S-8.09"
epic_id: "E-8"
version: "1.3"
status: ready
producer: story-writer
timestamp: 2026-04-30T00:00:00Z
phase: 2
inputs:
  - .factory/stories/epics/E-8-native-wasm-migration.md
  - .factory/stories/S-8.00-perf-baseline-bc-anchor-verification.md
  - plugins/vsdd-factory/hooks/regression-gate.sh
  - plugins/vsdd-factory/hooks-registry.toml
  - .factory/cycles/v1.0-brownfield-backfill/adv-s8.09-p1.md
input-hash: "e0882ac"
traces_to: .factory/stories/epics/E-8-native-wasm-migration.md
functional_requirements: []
cycle: v1.0-brownfield-backfill
points: "5"
depends_on: ["S-8.01", "S-8.02", "S-8.03", "S-8.04", "S-8.05", "S-8.06", "S-8.07", "S-8.08", "S-8.10"]
blocks: ["S-8.11", "S-8.12", "S-8.13", "S-8.14", "S-8.15", "S-8.16", "S-8.17",
         "S-8.18", "S-8.19", "S-8.20", "S-8.21", "S-8.22", "S-8.23", "S-8.24", "S-8.25",
         "S-8.26", "S-8.27", "S-8.28", "S-8.29"]
behavioral_contracts: ["BC-7.03.071", "BC-7.03.072", "BC-7.03.073", "BC-7.03.074", "BC-7.03.075"]
# BC status: anchored — regression-gate has five existing BCs:
# BC-7.03.071: identity & registry binding (PostToolUse ungated, priority=230, on_error=continue)
# BC-7.03.072: matches 9 test runners; other commands exit 0 immediately
# BC-7.03.073: pass/fail derivation: prefers exit_code, falls back to interrupted field
# BC-7.03.074: writes state file {status, timestamp, command} to .factory/regression-state.json
# BC-7.03.075: warns on pass->fail transition: hook.block severity=warn + stderr + exit 0
# All five confirmed by BC-INDEX grep (lines 1598-1602) on regression-gate.sh path.
# Additionally BC-7.01.003 (PostToolUse fail when bash interrupted) was found at line 1514
# but is a cross-cutting PostToolUse contract, not regression-gate specific — not included here.
# Per D-2 Option C: existing BCs reused; no new BC-7.xx migration family.
# OQ-6 gate: security-reviewer must audit regression-gate.sh subprocess capability profile
# before WASM crate implementation begins. This story's AC-010 is blocked on that audit.
# assumption_validations: [OQ-6] — validates the open question per E-8 epic D-2 / OQ-6.
# Status: draft (behavioral_contracts non-empty; security-review gate via AC-010 required
# before implementation of WASM crate; audit can proceed in parallel with W-15 porter stories).
verification_properties: []
priority: "P2"
wave: 15
target_module: "crates/hook-plugins/regression-gate/"
subsystems: ["SS-01", "SS-02", "SS-04", "SS-07"]
capabilities: ["CAP-022"]
estimated_days: 3
assumption_validations: ["OQ-6"]
# OQ-6: security-reviewer must audit regression-gate.sh capability profile before WASM implementation.
# NOTE: OQ-write_file was removed (F-S809-P2-003) — it is not registered in E-8 OQ-1..OQ-8.
# The host::write_file absence concern is a D-6 dependency item, not a named OQ.
# S-8.09 IS BLOCKED pending S-8.10 (SDK extension PR adding host::write_file to vsdd-hook-sdk)
# per E-8 D-6 Option A; shared blocker with S-8.04. See depends_on: S-8.10 and AC-005.
risk_mitigations: ["R-8.01", "R-8.05", "R-8.06", "R-8.07", "R-8.10"]
---

<!-- input-hash: 5015917 = commit "chore(stories): S-8.00 v1.5 status=ready CONVERGENCE_REACHED".
Verify via: git log --all --oneline -- .factory/stories/epics/E-8-native-wasm-migration.md -->

# S-8.09: Native port: regression-gate + adapter retirement prep (W-15 closer)

**Epic:** E-8 — Native WASM Migration Completion
**Phase:** Phase 2 — Story Decomposition (W-15 Tier 1 closer)
**Milestone:** `v1.1`
**Tier:** Tier 1 — PostToolUse lifecycle hooks (W-15 closing story)
**Blocks:** S-8.11 through S-8.29 (all W-16 + W-17 stories)
**Blocked by:** S-8.01, S-8.02, S-8.03, S-8.04, S-8.05, S-8.06, S-8.07, S-8.08
**Estimated effort:** M (3 days, 5 pts)

## Scope Summary

S-8.09 has three deliverables:

1. **Native port of `regression-gate.sh`** — PostToolUse hook with 5 behavioral
   contracts (BC-7.03.071..075). Medium complexity: 9 test-runner pattern matches,
   pass/fail state derivation, JSON state file write, pass-to-fail regression warning.
2. **OQ-6 security-reviewer audit** — Per E-8 epic OQ-6, regression-gate's subprocess
   capability profile must be audited BEFORE the WASM crate is implemented. The bash
   source reads stdin JSON with jq, reads/writes `.factory/regression-state.json`, and
   emits events via `bin/emit-event`. It does NOT invoke test runners itself — it only
   OBSERVES the PostToolUse hook envelope containing the Bash tool's command and
   exit/interrupted fields. The security-reviewer must confirm this read-only
   observation profile and propose the final capability declaration.
3. **Tier 1 adapter pre-retirement audit** — Confirm all 9 Tier 1 entries are now
   native (no `legacy-bash-adapter.wasm` references for Tier 1 hooks). Document
   adapter status for W-16/W-17. Adapter NOT deleted here (retired at S-8.29).

## Capability Anchor Justification

**Primary anchor:** CAP-022 ("Port hook plugins from bash to native WASM") per
`domain-spec/capabilities.md` §CAP-022. S-8.09 is the final Tier 1 port instance
of CAP-022, plus the W-15 wave-close audit deliverable.

**Stretch-Anchor Disclosure:** S-8.09 anchors to CAP-022 but spans three subsystems:
- **SS-07 Hook Bash Layer** owns `regression-gate.sh` and its BC-7.03.071..075
  behavioral contracts. SS-07 is the source-of-truth for the hook's behavior spec.
- **SS-01 Hook Dispatcher Core** owns the hooks-registry.toml routing and the host fn
  surface. Registry migration is a dispatcher-scope change.
- **SS-02 Hook SDK and Plugin ABI** (primary SDK-surface anchor) owns the vsdd-hook-sdk
  crate surface including host fn declarations (`crates/hook-sdk/`, `crates/hook-sdk-macros/`).
  The D-6 Option A blocker (host::write_file missing) is an SS-02 scope gap; S-8.10
  (the unblocker) is anchored to SS-02.
- **SS-04 Plugin Ecosystem** owns the plugin crate location (`crates/hook-plugins/regression-gate/`).
  SS-04 canonical name per ARCH-INDEX: "Plugin Ecosystem" (NOT "Hook SDK" — that is SS-02).

All four subsystem anchors verified against ARCH-INDEX Subsystem Registry:
- SS-01 Hook Dispatcher Core: canonical name confirmed.
- SS-02 Hook SDK and Plugin ABI: canonical name confirmed (F-S809-P2-001 fix).
- SS-04 Plugin Ecosystem: canonical name confirmed (NOT "Hook SDK"; corrected from pass-1 mis-anchor).
- SS-07 Hook Bash Layer: canonical name confirmed.
(Per POLICY 5 anchor justification requirement + POLICY 6 architecture-is-source-of-truth.)

**Dependency anchor justification:**
- S-8.09 depends_on=[S-8.01..S-8.08] because the W-15 Tier 1 closure audit (AC-011)
  requires all 8 prior Tier 1 ports to be complete before the audit can verify that
  0 Tier 1 entries remain on legacy-bash-adapter. Without all 8 prior ports merged,
  the audit would produce a false pass.
- S-8.09 blocks=[S-8.11..S-8.29] because W-16 and W-17 stories depend on W-15 Tier 1
  closure as a prerequisite gate (per E-8 epic wave schedule). S-8.09 is the explicit
  wave boundary marker — until S-8.09 merges, W-16/W-17 stories must not begin.
  NOTE: S-8.10 (SDK extension host::write_file) is now a depends_on of S-8.09,
  not a blocked story — S-8.10 must land BEFORE S-8.09 can proceed (D-6 Option A).
  **Blocked by S-8.10 (vsdd-hook-sdk gains host::write_file). Implementation cannot proceed until S-8.10 merges.**

**Complexity note (Medium — 5 pts):**
- regression-gate port: 3 pts (9 test-runner patterns, pass/fail state machine,
  JSON state file write, pass-to-fail warning + emit_event)
- OQ-6 audit coordination: 1 pt (security-reviewer must run; story cannot close without
  audit completion per AC-010)
- Tier 1 pre-retirement audit: 1 pt (registry scan + documentation)
- Total: 5 pts (confirmed per E-8 epic v1.7 changelog S-8.09 note)

**OQ-6 observation re: subprocess profile:** Reading regression-gate.sh carefully,
the hook does NOT execute test runners — it only reads the PostToolUse JSON envelope
(which contains the Bash tool's command string and output metadata). The hook uses
jq to parse stdin and date for timestamp, both of which are binary_allow candidates.
The state file write uses `jq -n ... > file` (jq subprocess). The preliminary profile
is: `binary_allow = ["jq"]` plus file write capability. The security-reviewer must
confirm this is correct and whether additional capabilities are needed.

## Behavioral Contracts

| BC ID | Title | Trace |
|-------|-------|-------|
| BC-7.03.071 | regression-gate: identity & registry binding | **Source: `.factory/specs/behavioral-contracts/ss-07/BC-7.03.071.md` read directly (anti-fabrication: third instance).** Postcondition 1 (verbatim): "Records test pass/fail to `.factory/regression-state.json`. Warn-only on regression. on_error=continue." Postcondition 2 (verbatim): "Exit codes: 0 always." Postcondition 3 (verbatim): "Error policy: continue." Invariant 1 (verbatim): "Hook script identity (script path) and registry binding remain stable across the contract lifetime." Invariant 2 (verbatim): "Exit-code semantics conform to the dispatcher contract: 0 = allow / advisory, 2 = block, 1 = jq-missing-fail-closed (where applicable)." → AC-001 (Postcondition 1/2/3 + Invariant 1: registry binding, PostToolUse ungated, priority=230, on_error=continue, exit 0); AC-002 (Invariant 1: script path identity — .sh deleted on migration, hooks.json entry deleted); AC-008 (Invariant 2: exit-code semantics; emit_event replaces bin/emit-event but bin/emit-event NOT removed until S-8.29); AC-009 (Postcondition 1: self-contained WASM port); AC-011 (Invariant 1: registry binding stable — W-15 wave closure audit); AC-012 (Invariant 2: exit-code + D-10 deferral) |
| BC-7.03.072 | regression-gate: matches 9 test runners | Postcondition 1: WASM port exits 0 immediately for non-test commands; applies 9-pattern match → AC-003, AC-007 |
| BC-7.03.073 | regression-gate: pass/fail derivation prefers exit_code; falls back to interrupted | Postcondition 1: STATUS logic: exit_code=0 → pass; non-zero → fail; interrupted=true → fail; interrupted=false → pass; neither → unknown (skip) → AC-004, AC-007 |
| BC-7.03.074 | regression-gate: writes state file with status, timestamp, command | Postcondition 1: WASM port writes .factory/regression-state.json with {status, timestamp, command}; ISO-8601 UTC timestamp → AC-005, AC-007 |
| BC-7.03.075 | regression-gate: warns on pass→fail transition | Postcondition 1: WASM port emits hook.block severity=warn (regression_gate_pass_to_fail) + stderr warn + exit 0 → AC-006, AC-007 |

## Verification Properties

| VP ID | Purpose |
|-------|---------|
| (none assigned) | No VP currently contracts regression-gate behavior. See v1.1 BC/VP Candidates below. |

## v1.1 BC/VP Candidates

> Registered per F-206 5-col format.

| Candidate ID | Type | Title | Rationale | Blocking? |
|---|---|---|---|---|
| [process-gap] VP-regression-gate-parity | VP | WASM regression-gate must produce identical state-file and stderr behavior as bash version for all 9 test runner fixture inputs | No VP currently verifies parity; AC-007 bats parity test is the enforcement mechanism | No |
| [process-gap] VP-tier1-all-native | VP | After S-8.09 merge, 0 Tier 1 hooks reference legacy-bash-adapter.wasm in hooks-registry.toml | Validates W-15 wave closure; AC-011 is the enforcement check | No |

## Narrative

- **As a** vsdd-factory operator running test suites during implementation sessions
- **I want** regression-gate to execute as a native WASM plugin instead of through the legacy-bash-adapter
- **So that** test pass/fail state tracking and regression warnings run on all platforms
  (including Windows without jq), with lower latency and no adapter indirection;
  and so that all 9 Tier 1 hooks are confirmed native, closing the W-15 wave

## Goal

**Deliverable 1 — WASM port:** Port `plugins/vsdd-factory/hooks/regression-gate.sh` to
a native Rust WASM crate at `crates/hook-plugins/regression-gate/`. The hook fires on
PostToolUse (no tool filter in registry; body filters to Bash commands). It reads stdin
JSON (PostToolUse envelope), checks if the Bash command matches one of 9 test-runner
patterns, derives pass/fail status from exit_code/interrupted fields, reads prior status
from `.factory/regression-state.json`, writes new status, and emits a warning on
pass-to-fail transition.

**Deliverable 2 — OQ-6 audit:** Security-reviewer audits regression-gate.sh to produce
a confirmed capability profile (`binary_allow` and file access declarations) for the
WASM crate. WASM implementation of regression-gate MUST NOT begin until audit is complete
(AC-010 gate). The audit result is recorded in
`.factory/cycles/v1.0-brownfield-backfill/E-8-oq6-capability-profile.md`.

**Deliverable 3 — Tier 1 pre-retirement audit:** Scan `hooks-registry.toml` and confirm
all 9 Tier 1 hook entries now reference native `.wasm` plugins (not
`legacy-bash-adapter.wasm`). Record result in
`.factory/cycles/v1.0-brownfield-backfill/E-8-tier1-native-audit.md`.
Adapter crate and `bin/emit-event` are NOT deleted in this story (deferred to S-8.29).

## OQ-6 Capability Anchor Justification

**OQ-6 (from E-8 epic):** "regression-gate.sh appears to invoke external test runners
(bats, cargo test). The WASM sandbox restricts subprocess calls to `binary_allow` list.
If the native port needs to exec arbitrary test runners, this may require a more
permissive capability declaration than current hooks use."

**Resolution (from bash source analysis):** regression-gate does NOT invoke test runners.
It is a PostToolUse OBSERVER hook — it receives the output of already-completed Bash
tool calls via the stdin JSON envelope. The hook's subprocess usage is limited to:
- `jq` for stdin JSON parsing (same as other Tier 1 hooks)
- `jq -n ... > file` for writing the state JSON file
- `date -u` for UTC timestamp generation
- `bin/emit-event` for telemetry (replaced by host::emit_event in WASM port)

Preliminary capability profile: `binary_allow = ["jq"]` (or empty if the WASM port
uses serde_json + chrono instead, which is preferred). Security-reviewer must confirm.

**OQ-6 validation method (per `assumption_validations: [OQ-6]`):** AC-010 requires
a security-reviewer to produce a signed-off capability profile document
before S-8.09 implementation begins. The bats test AC-007(e) then validates the
capability profile is correctly declared in the final registry entry.

## Acceptance Criteria

### AC-001 (traces to BC-7.03.071 postcondition 1)
WASM crate `crates/hook-plugins/regression-gate/` exists with `Cargo.toml` targeting
`wasm32-wasip1`, implements the `vsdd_hook_sdk` hook interface, and builds successfully
via `cargo build --target wasm32-wasip1`. Registry entry in `hooks-registry.toml` is
updated: `plugin = "hook-plugins/regression-gate.wasm"`, `script_path` removed,
`shell_bypass_acknowledged` removed. Event binding preserved: `event = "PostToolUse"`
(no `tool` filter field), `priority = 230`, `on_error = "continue"`.
Capability declarations updated per OQ-6 audit result (see AC-010).

### AC-002 (traces to BC-7.03.071 invariant 1)
`hooks.json` command entry for `regression-gate` is deleted from all platform-specific
`hooks.json.*` files and the template.
`plugins/vsdd-factory/hooks/regression-gate.sh` is deleted from the repository.

**Pre-deletion impact analysis (required before T-8 deletion):**
Run: `grep -r "regression-gate" plugins/vsdd-factory/hooks/ --include="hooks.json*" -l`
Confirm the grep output matches exactly the hooks.json files listed in File Structure
Requirements. If any additional files surface, investigate before deleting.

**Post-deletion blast-radius validation:**
After AC-002 execution, each `hooks.json.*` file must contain exactly N PostToolUse
entries (where N matches the dispatcher-routing count established in S-3.04 / D-7), and
the string `regression-gate` must not appear in any `hooks.json.*` file. Validate via:
```
grep -r "regression-gate" plugins/vsdd-factory/hooks/ --include="hooks.json*"
```
Must return zero matches. If any match, the deletion is incomplete — do not merge.

**Rollback:** If the deletion step (T-8) accidentally clobbers dispatcher routing entries
(i.e., PostToolUse entry count drops below expected N), restore from git before opening PR.

### AC-003 (traces to BC-7.03.072 postcondition 1)
WASM plugin checks `tool_name` field from stdin; if not `"Bash"`, exits 0 immediately.
For Bash tool calls, extracts the `tool_input.command` string and matches against the
9 test-runner patterns:
`cargo test`, `cargo nextest`, `pytest`, `pnpm test`, `npm test`, `go test`,
`just test`, `just ci`, `yarn test`.
If no pattern matches, exits 0 immediately (no state file write, no event).
Pattern matching uses substring contains (same as bash `case` glob `*"cargo test"*`).

### AC-004 (traces to BC-7.03.073 postcondition 1)
Pass/fail status derivation follows the exact cascade from the bash source:
1. If `tool_response.exit_code == 0` (or `tool_response.returncode == 0`): STATUS = pass
2. If `tool_response.exit_code` is non-zero and non-null: STATUS = fail
3. Else if `tool_response.interrupted == true`: STATUS = fail
4. Else if `tool_response.interrupted == false`: STATUS = pass
5. Else: STATUS = unknown → exit 0 without writing state file
Bats test covers all 5 derivation paths.

### AC-005 (traces to BC-7.03.074 postcondition 1)
When STATUS is pass or fail: WASM plugin writes `.factory/regression-state.json` with
JSON structure `{"status": "<pass|fail>", "timestamp": "<ISO8601-UTC>", "command": "<cmd>"}`.
Timestamp format: `%Y-%m-%dT%H:%M:%SZ`. State file is atomically overwritten (not
appended) on each matching invocation. Exits 0.
When `.factory/` directory does not exist: exits 0 without writing (same as bash guard).

### AC-006 (traces to BC-7.03.075 postcondition 1)
When prior status (read from `.factory/regression-state.json`) was `"pass"` and new
STATUS is `"fail"`: WASM plugin calls `host::emit_event` with:
`type=hook.block hook=regression-gate matcher=Bash reason=regression_gate_pass_to_fail
severity=warn command=<cmd>`,
then writes to stderr:
`"regression-gate: suite transitioned pass → fail.\n  command: <cmd>\n  recorded: .factory/regression-state.json\n"`.
Newlines in the stderr message are rendered as actual newline characters (0x0A).
In bats: use `$'...\n...'` quoting syntax to match rendered newlines exactly.
Exits 0. Does NOT block execution (exit 0, on_error=continue).

### AC-007 (traces to BC-7.03.071 postcondition 1 through BC-7.03.075 postcondition 1)
Bats parity test at `tests/integration/hooks/regression-gate.bats` covers all 9 scenarios.
Each scenario specifies the bats test name and fixture inputs.

**9-pattern coverage table** (each row is a dedicated bats `@test` block):

| Scenario | Pattern Exercised | Bats test name | Fixture stdin key fields |
|----------|------------------|-----------------|-----------------------------|
| (a) | `cargo test` | `regression_gate_cargo_test_pass_no_prior` | tool=Bash, command="cargo test", exit_code=0, no prior state file |
| (b) | `cargo test` | `regression_gate_cargo_test_pass_interrupted_false` | tool=Bash, command="cargo test", interrupted=false, no prior state file |
| (c) | `cargo test` | `regression_gate_cargo_test_fail_regression_warning` | tool=Bash, command="cargo test", exit_code=1, prior state={"status":"pass"} |
| (d) | `cargo test` | `regression_gate_cargo_test_interrupted_regression_warning` | tool=Bash, command="cargo test", interrupted=true, prior state={"status":"pass"} |
| (e) | `cargo test` | `regression_gate_status_unknown_no_state_write` | tool=Bash, command="cargo test", no exit_code, no interrupted field |
| (f) | (non-test) | `regression_gate_non_test_command_exits_0` | tool=Bash, command="git commit -m msg"; must exit 0, no state write |
| (g) | `cargo test` | `regression_gate_factory_dir_absent_exits_0` | tool=Bash, command="cargo test", exit_code=0; .factory/ not present |
| (h) | `cargo test` | `regression_gate_fail_to_fail_no_warning` | tool=Bash, command="cargo test", exit_code=1, prior state={"status":"fail"}; no regression warning |
| (i) | `pytest` | `regression_gate_pytest_pass` | tool=Bash, command="pytest tests/", exit_code=0 (verifies non-cargo pattern coverage) |

**Additional pattern-coverage verification** (not bats, but required in PR):
For each of the 9 test-runner substrings (`cargo test`, `cargo nextest`, `pytest`,
`pnpm test`, `npm test`, `go test`, `just test`, `just ci`, `yarn test`), the PR must
include a unit test or inline fixture comment demonstrating the substring match triggers
the hook body (not the early-exit path). These may be Rust `#[test]` unit tests in
`src/lib.rs` for the pattern-matching function.

All bats tests pass. All pattern-coverage unit tests pass.

### AC-008 (traces to BC-7.03.071 invariant 2)
`bin/emit-event` calls from the bash `_emit()` function are replaced with
`host::emit_event` Rust calls in the WASM crate. Verify: no reference to
`bin/emit-event` remains in `crates/hook-plugins/regression-gate/`.
`bin/emit-event` binary is NOT removed (E-8 D-10 — deferred to S-8.29).
`bin/emit-event` binary NOT removed note is confirmed in Architecture Compliance Rules.

### AC-009 (traces to BC-7.03.071 postcondition 1 — self-contained WASM; no jq dependency)
The WASM port is self-contained: stdin JSON is parsed via `serde_json` natively; state
file is serialized via `serde_json::to_string`; timestamp is generated via `chrono` or
`std::time`. No `jq` binary reference exists in `crates/hook-plugins/regression-gate/src/`.

**Contingency pinned:** If the OQ-6 audit doc (AC-010) concludes that `jq` IS required
(e.g., for a subprocess path), the implementer must:
1. Declare `binary_allow = ["jq"]` in the registry capability declaration (not serde_json).
2. Re-anchor this AC to a new BC drafted per D-2 Option C exception.
3. File a BC authoring request BEFORE implementing T-4.
4. The story cannot ship with `jq` in use but this AC passing — the AC and BC must be
   updated atomically before the PR is opened.

**The "may be revised" language is removed.** This AC is now pinned to the preferred
(self-contained) profile. Any deviation from this profile requires AC + BC update, not
a silent revision post-implementation.

### AC-010 (validates OQ-6 — pre-implementation security-reviewer gate)
BEFORE the WASM crate implementation (T-3 through T-5) begins, a security-reviewer
agent must complete an audit of `plugins/vsdd-factory/hooks/regression-gate.sh` and
produce `.factory/cycles/v1.0-brownfield-backfill/E-8-oq6-capability-profile.md`.

**Required audit doc frontmatter schema** (gate is not satisfied without ALL fields):
```yaml
signoff_agent_id: "<agent-id>"          # non-empty; identifies the reviewing agent
signoff_timestamp: "<ISO8601-UTC>"      # non-empty; time of signoff
confirmed_subprocesses:                 # list of all subprocesses invoked
  - "<subprocess-1>"
  - "<subprocess-2>"
recommended_binary_allow:               # list of binaries for binary_allow declaration
  - "<binary-1>"                        # may be empty list [] if no subprocess needed
recommended_capabilities:              # list of host capability declarations
  - "<capability-1>"                    # e.g. read_file, write_file
```

**Reconciliation rule:** If the audit profile differs from the story's preliminary
profile (lines 117-123 of this story), the audit takes precedence. The preliminary
profile is the implementer's pre-audit hypothesis, not a foregone conclusion. The
implementer must update AC-009 and registry capability declarations to match the audit.

**Falsifiable check:** AC-010 is satisfied if and only if:
1. The file `.factory/cycles/v1.0-brownfield-backfill/E-8-oq6-capability-profile.md` exists, AND
2. Frontmatter field `signoff_agent_id` is non-empty, AND
3. Frontmatter field `recommended_binary_allow` is explicitly present (even if `[]`).

The implementing agent must read this document before writing any Rust code for the
regression-gate crate. If the document is absent or the frontmatter is incomplete,
the implementer must STOP and request the security-reviewer audit via orchestrator.

### AC-011 (traces to BC-7.03.071 invariant 1 — W-15 wave closure audit)
**Sequencing clarification (F-S809-P1-007, F-S809-P2-005):** S-8.09 depends on S-8.01..S-8.08 already
being merged into the integration branch that this story builds on. AC-011 is verified
BY T-7 — it runs as part of T-7, immediately after the registry edit, before any further
tasks or PR merge. It is NOT a post-T-7 separate check; it is the final step WITHIN T-7
(registry edit + audit in one commit). It is NOT run post-S-8.09 merge.
S-8.09 rebases from the integration branch where S-8.01..S-8.08 are already merged.

After T-7 (registry updated in this story's branch), run:
```
grep -n "legacy-bash-adapter" plugins/vsdd-factory/hooks-registry.toml
```
Verify: zero matches for any of the 9 Tier 1 hook names
(handoff-validator, pr-manager-completion-guard, track-agent-stop, update-wave-state-on-merge,
validate-pr-review-posted, session-learning, warn-pending-wave-gate, track-agent-start,
regression-gate). Record result in
`.factory/cycles/v1.0-brownfield-backfill/E-8-tier1-native-audit.md` with:
- List of all 9 Tier 1 hooks and their new WASM plugin paths
- Confirmation that `legacy-bash-adapter.wasm` still referenced by Tier 2/3 hooks
  (adapter NOT retired — deferred to S-8.29)
- Statement: "W-15 Tier 1 migration complete. S-8.11 wave gate unblocked."

The audit doc is produced as part of S-8.09's PR (committed to the same branch as the
registry edit and the regression-gate crate). It is NOT a post-merge artifact.

### AC-012 (traces to BC-7.03.071 postcondition 1 — bin/emit-event deferral confirmation)
Verify `bin/emit-event` binary still present in the repository after S-8.09 merge.
Use the deterministic bounded check:
```
test -f plugins/vsdd-factory/bin/emit-event && echo "PRESENT" || echo "MISSING"
```
Must print `PRESENT`. If `MISSING`, the deletion was premature — restore from git
before merging S-8.09. This confirms E-8 D-10 constraint is respected — `bin/emit-event`
is used by Tier 2/3 bash hooks throughout W-16/W-17 and must not be removed until S-8.29.

**Exact path:** `plugins/vsdd-factory/bin/emit-event` (repo-root-relative). If the binary
lives at a different path, update this AC and the Architecture Compliance Rules row to
match the actual path before merging.

## Acceptance Criteria with BC Traces

| AC | Statement | BC Trace |
|----|-----------|----------|
| AC-001 | WASM crate exists, builds for wasm32-wasip1, registry updated; PostToolUse ungated binding, priority=230, on_error=continue preserved | traces to BC-7.03.071 postcondition 1 (identity & registry binding) |
| AC-002 | hooks.json entry deleted; .sh file deleted; pre-deletion impact analysis + post-deletion blast-radius validation; rollback step documented | traces to BC-7.03.071 invariant 1 (hook path lifecycle) |
| AC-003 | non-Bash tool: exit 0; Bash non-test command: exit 0; 9-pattern match applied | traces to BC-7.03.072 postcondition 1 (9 test runners matched) |
| AC-004 | Pass/fail derivation: exit_code priority over interrupted; unknown → skip | traces to BC-7.03.073 postcondition 1 (status derivation cascade) |
| AC-005 | State file written with {status, timestamp, command}; .factory/ absent guard | traces to BC-7.03.074 postcondition 1 (state file write) |
| AC-006 | Pass-to-fail: emit hook.block severity=warn + stderr; exit 0 | traces to BC-7.03.075 postcondition 1 (regression warning) |
| AC-007 | Bats parity tests pass (9 scenarios: 8 behavioral + 1 pytest-pattern); all 9 test-runner substrings verified via unit tests | traces to BC-7.03.071..075 postconditions (full parity) |
| AC-008 | host::emit_event replaces bin/emit-event; bin/emit-event binary not removed | traces to BC-7.03.071 invariant 2 (emit_event host fn deferral) |
| AC-009 | No jq dependency in WASM crate; serde_json native parse + serialize; contingency deviation requires AC+BC update before PR | traces to BC-7.03.071 postcondition 1 (self-contained WASM; any jq deviation requires BC amendment) |
| AC-010 | OQ-6 security-reviewer audit complete before WASM implementation; profile doc produced with required frontmatter schema (signoff_agent_id, signoff_timestamp, confirmed_subprocesses, recommended_binary_allow, recommended_capabilities) | validates OQ-6 (assumption_validations); falsifiable check: file exists AND signoff_agent_id non-empty AND recommended_binary_allow present |
| AC-011 | All 9 Tier 1 entries native; tier1-native-audit.md produced as part of S-8.09 PR branch (not post-merge) | traces to BC-7.03.071 postcondition 1 (W-15 wave closure) |
| AC-012 | bin/emit-event present at plugins/vsdd-factory/bin/emit-event; deterministic bounded check | traces to BC-7.03.071 invariant 2 (D-10 deferral respect) |

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| regression-gate WASM crate | `crates/hook-plugins/regression-gate/` | Effectful (reads stdin JSON, reads+writes .factory/regression-state.json, writes stderr, calls host::emit_event) |
| hooks-registry.toml (modified) | `plugins/vsdd-factory/hooks-registry.toml` | Pure (declarative config; updated once) |
| hooks.json (deleted entry) | `plugins/vsdd-factory/hooks/hooks.json.*` | Pure (deleted entry) |
| regression-gate.sh (deleted) | `plugins/vsdd-factory/hooks/regression-gate.sh` | N/A (deleted) |
| bats parity tests | `tests/integration/hooks/regression-gate.bats` | Effectful (creates fixture .factory/ dirs, regression-state.json; captures event emission) |
| OQ-6 capability profile doc | `.factory/cycles/v1.0-brownfield-backfill/E-8-oq6-capability-profile.md` | Pure (document artifact; produced by security-reviewer) |
| Tier 1 native audit doc | `.factory/cycles/v1.0-brownfield-backfill/E-8-tier1-native-audit.md` | Pure (document artifact; produced as part of S-8.09 PR branch) |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | PostToolUse envelope for non-Bash tool (e.g., Read, Edit) | Check tool_name; if not Bash, exit 0 immediately; no state write |
| EC-002 | Bash command matches pattern but STATUS = unknown (no exit_code or interrupted) | Skip state write entirely; exit 0 with no output (same as bash: `[[ "$STATUS" == "unknown" ]] && exit 0`) |
| EC-003 | `.factory/regression-state.json` exists but is malformed JSON | **Preserves bash `2>/dev/null` semantics:** silently ignore parse error; treat prior status as "unknown"; do not trigger regression warning; overwrite with new valid state. No stderr log. WASM port must match this silent-suppression behavior exactly. If an implementer adds a `log_warn` call for malformed state, that is a behavioral deviation requiring a new BC/AC — it must not be added silently. |
| EC-004 | Prior status = "unknown" in state file, new STATUS = "fail" | No regression warning (warning only on pass-to-fail, not unknown-to-fail) |
| EC-005 | Rust serde_json deserialization fails on stdin JSON OR host::write_file fails on state file write | **WASM-context reframe (F-S809-P2-008):** jq subprocess is NOT applicable to the WASM port — it is eliminated and replaced by serde_json. If `serde_json` deserialization fails, return `HookResult::Error`. If `host::write_file` fails (requires S-8.10 to be merged first), return `HookResult::Error` with the SDK error message. Neither failure triggers a regression warning (cannot compare states). The bash-era "jq subprocess failure OR state-file write failure" framing is removed — these are now distinct WASM failure modes. |
| EC-006 | State file write fails (disk full or .factory/ not writable) | **Behavioral deviation from bash (intentional):** WASM port reports write failure via `host::log_error` with message "regression-gate: failed to write state file: <path>: <error>" before exiting 0. No regression warning is emitted (cannot compare states without write). This adds observability absent from the bash source (bash silently exits 0). Document deviation in PR description. If this deviation is rejected, add T-4 comment "swallow write error to match bash source" and update EC-006 to "exit 0 silently." Either choice must be made explicitly — silent divergence is not allowed. |
| EC-007 | Multiple test runner patterns match the same command (e.g., "just ci" also contains "ci") | Use the `case` pattern order from bash source: first matching arm wins; patterns are independent substrings, not exclusive |

## OQ-6 Deferral and Security-Reviewer Gate

### Deferral Rationale

OQ-6 was deferred from the E-8 epic planning phase (v1.1 changelog) because the full
capability profile of regression-gate required reading the bash source carefully. This
story spec resolves the primary concern (regression-gate does NOT invoke test runners)
but defers final profile confirmation to a security-reviewer to avoid incorrect capability
declarations being committed to hooks-registry.toml.

### Sequencing

The security-reviewer audit (AC-010) must complete BEFORE the WASM crate implementation
(T-3). Suggested orchestration:

```
T-1: Implementer reads bash source + BCs; prepares preliminary capability summary
T-2: Implementer terminates; commits T-2-blocked status note; orchestrator detects blocked state
T-2a: Orchestrator dispatches security-reviewer with bash source + preliminary profile as inputs
T-2b: Security-reviewer produces E-8-oq6-capability-profile.md; commits to factory-artifacts branch
T-2c: Orchestrator detects file presence (AC-010 falsifiable check); re-dispatches implementer
T-3: Implementer loads audit doc as Tier-1 input; implements WASM crate per confirmed profile
T-4+: Normal port + test cycle
```

The security-reviewer does NOT need to run before the adversarial-review pass on this
story spec — only before the implementing agent writes Rust code. The adversary can
review this story spec in parallel with the security audit.

### Adversarial Review Approach

S-8.09 should undergo a standard adversarial review pass on this story spec (same as
S-8.01..S-8.08). The OQ-6 deferral does NOT require a special adversarial-review
approach — the adversary reviews the spec; the security-reviewer audits the capability
profile; these are sequential but independent steps. AC-010 is the hard gate.

## Adapter Pre-Retirement Audit Scope

The adapter pre-retirement audit (AC-011) is a registry scan, not a code change.
It confirms the expected state after all 9 Tier 1 ports are merged. It is NOT:
- Deletion of the adapter crate (deferred to S-8.29)
- Deletion of `bin/emit-event` (deferred to S-8.29, per E-8 D-10)
- Modification of any Tier 2/3 hooks-registry.toml entries

The audit doc is a factual record for the W-16 wave gate. It enables the W-15 → W-16
transition check to be mechanical: "does E-8-tier1-native-audit.md exist and contain
the expected 9-hook native confirmation?"

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| regression-gate WASM crate | Effectful | Reads stdin JSON; reads+writes .factory/regression-state.json; writes to stderr; calls host::emit_event |
| bats parity tests | Effectful | Creates fixture .factory/ directories + regression-state.json files; captures event emission and stderr |
| OQ-6 audit doc | Pure | Document artifact; no runtime side effects |
| Tier 1 native audit doc | Pure | Document artifact; grep-based verification |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens | Phase |
|---------------|-----------------|-------|
| This story spec | ~4,500 | T-1 onward |
| E-8 epic spec (relevant sections: D-2, D-6, D-10, OQ-6, Goals, Wave Schedule) | ~3,000 | T-1 onward |
| regression-gate.sh bash source | ~700 | T-1 onward |
| BC files (5 BCs: BC-7.03.071..075 — ~400 tokens each) | ~2,000 | T-1 onward |
| hooks-registry.toml (PostToolUse section, lines 130-148) | ~250 | T-1 onward |
| vsdd-hook-sdk Rust API reference | ~800 | T-3 onward |
| serde_json + serde API reference | ~600 | T-3 onward |
| Bats test scaffolding + fixture JSONs (9 test cases) | ~1,200 | T-6 onward |
| S-8.01..S-8.08 pattern reference (Previous Story Intelligence) | ~1,000 | T-1 onward |
| Tool outputs overhead | ~500 | all phases |
| **Subtotal (T-1 base — before audit doc exists)** | **~14,550** | |
| OQ-6 audit doc (conditional — loaded at T-3 after security-reviewer produces it) | ~400 | T-3 onward |
| **Total (T-3 onward, including audit doc)** | **~14,950** | |
| Agent context window | 200K for Sonnet | |
| **Budget usage** | **~7.5%** | |

> S-8.09 is the largest story in W-15 but well under the 20-30% limit. The
> security-reviewer audit doc (AC-010) adds ~400 tokens of context that must be loaded
> before T-3. The implementer should NOT load all 8 sibling story specs — read only
> S-8.03 (track-agent-stop) for emit_event pattern reference and S-8.04
> (update-wave-state-on-merge) for read_file + write_file file I/O pattern reference.

## Tasks

- [ ] T-1: Read `plugins/vsdd-factory/hooks/regression-gate.sh` and BC-7.03.071..075 in
        full. Verify BC-anchor table from S-8.00 confirms Spec-Current = Y for all 5 BCs.
        Extract preliminary subprocess list: jq (stdin parse), jq (state file write), date,
        bin/emit-event. Prepare a one-page capability profile summary for security-reviewer.
- [ ] T-2: Hand off capability profile summary to security-reviewer agent (via orchestrator).
        **Implementer terminates after T-2** with a structured status note in the cycle
        directory (e.g., `E-8-s8.09-t2-blocked.md`) stating: "Blocked on OQ-6 audit;
        awaiting E-8-oq6-capability-profile.md from security-reviewer."
        **Orchestrator signal mechanism:** Orchestrator monitors for file existence at
        `.factory/cycles/v1.0-brownfield-backfill/E-8-oq6-capability-profile.md`.
        When the file appears AND has non-empty `signoff_agent_id` frontmatter (AC-010
        falsifiable check), orchestrator re-dispatches the implementer at T-3 with the
        audit doc loaded as a Tier-1 input.
        **Security-reviewer dispatch:** Orchestrator dispatches security-reviewer agent
        with bash source (`plugins/vsdd-factory/hooks/regression-gate.sh`) + the
        preliminary capability profile (OQ-6 Capability Anchor Justification section
        of this story) as inputs. Security-reviewer commits audit doc to factory-artifacts
        branch. Orchestrator detects file presence and re-dispatches implementer.
        DO NOT proceed to T-3 until audit doc exists with complete frontmatter (AC-010).
- [ ] T-3: Read OQ-6 audit doc. Create `crates/hook-plugins/regression-gate/` crate skeleton:
        `Cargo.toml` (target wasm32-wasip1, `vsdd-hook-sdk`, `serde_json`, `serde`,
        `chrono` or `std::time` — per audit recommendation). Follow capability declarations
        from audit doc for `[hooks.capabilities]` section.
- [ ] T-4: Implement regression-gate logic in Rust per BC-7.03.071..075:
        - Read stdin JSON (PostToolUse envelope); parse with serde_json.
        - Extract tool_name; if not "Bash", return 0.
        - Extract tool_input.command; apply 9-pattern substring match; if no match, return 0.
        - Extract exit_code (prefer) and interrupted (fallback) from tool_response.
        - Derive STATUS per 5-step cascade (AC-004). If unknown, return 0.
        - Check .factory/ dir exists; if not, return 0.
        - Read .factory/regression-state.json for PRIOR status; default "unknown" if absent/malformed.
        - Write new state JSON with {status, timestamp, command} using serde_json serialization.
        - If PRIOR == "pass" and STATUS == "fail": call host::emit_event + write stderr.
        - Return 0.
- [ ] T-5: Build and test: `cargo build --target wasm32-wasip1 -p regression-gate`.
        Verify `.wasm` artifact produced.
- [ ] T-6: Write bats parity tests at `tests/integration/hooks/regression-gate.bats`
        covering all 9 scenarios per AC-007. Run all — all pass.
- [ ] T-7: Update `plugins/vsdd-factory/hooks-registry.toml`:
        Replace regression-gate entry with native WASM plugin path. Update capability
        declarations per OQ-6 audit result. Remove script_path, shell_bypass_acknowledged.
        Then run AC-011 registry audit (grep for legacy-bash-adapter in Tier 1 entries)
        and produce E-8-tier1-native-audit.md in the same commit.
- [ ] T-8: Delete `plugins/vsdd-factory/hooks/regression-gate.sh` and corresponding
        command entry from all `hooks.json.*` files and template.
        Run pre-deletion impact analysis and post-deletion blast-radius validation per AC-002.
- [ ] T-9: Run full bats test suite. Verify all 9 bats scenarios pass. Run Rust unit tests
        for the 9-pattern match function (cargo test --target wasm32-wasip1 -p regression-gate).
        NOTE: S-8.00 perf baseline does NOT include regression-gate; no perf comparison is
        required here. Perf measurement is scoped to Tier-2-only hooks per E-8 AC-7.
        If CI runs slower than 30s for the hook, file a perf finding — but do not block merge
        on a specific S-8.00 baseline comparison.
- [ ] T-10: Run the bin/emit-event presence check (AC-012):
        `test -f plugins/vsdd-factory/bin/emit-event && echo "PRESENT" || echo "MISSING"`
        Must print PRESENT. Confirm E-8 D-10 deferral is respected before opening PR.

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-8.00 | BC-anchor table gates port stories; S-8.09 requires all 5 regression-gate BCs Spec-Current = Y | S-8.09 MUST read the BC-anchor table before beginning (T-1); if any of BC-7.03.071..075 are stale, file BC update BEFORE porting | OQ-6 was deferred from planning — audit must complete before WASM implementation |
| S-8.03 (track-agent-stop) | Established emit_event pattern for PostToolUse-style hooks; similar stdin parse structure | Read S-8.03 for the serde_json stdin parse + host::emit_event pattern; regression-gate follows same shape but adds state file I/O | track-agent-stop exits 0 always (advisory); regression-gate also exits 0 always — the "warning" is advisory severity, not a block |
| S-8.04 (update-wave-state-on-merge) | Established file read + conditional write pattern; .factory/ guard; BLOCKED on host::write_file (D-6 Option A shared blocker) | See S-8.04 for the read_file + write_file + .factory/ directory guard pattern; regression-gate uses the same guard. **[NOTE: S-8.04 is BLOCKED on the same SDK extension (D-6 Option A) and has NOT converged (still at pass-2 fix burst; no CONVERGENCE_REACHED). Treat as TENTATIVE reference pending D-6 resolution — S-8.04's spec may change substantially before it converges. Verify patterns against converged S-8.04 before implementing S-8.09.]** | S-8.04 and S-8.09 share the D-6 Option A blocker: host::write_file does not exist in vsdd-hook-sdk. Neither story can implement the state-file write path until S-8.10 merges. |
| S-8.01..S-8.08 | Full port+delete+test cycle confirmed; bin/emit-event → host::emit_event throughout; wasm32-wasip1 is the correct target (not wasm32-wasi) | The 9-hook Tier 1 suite is now a proven pattern; S-8.09 is the closing instance | S-8.09 is unique: it is the ONLY story that also produces audit documents (OQ-6 + Tier 1 native audit). These documents are NOT code — do not put them in `crates/`; they belong in `.factory/cycles/v1.0-brownfield-backfill/` |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| HOST_ABI_VERSION = 1 unchanged throughout E-8 | E-8 Goal #5; BC-2.01.003; ADR-006 | regression-gate uses stdin read + read_file + write_file + emit_event — all should be existing host fns. If write_file is absent from the SDK surface, trigger D-6 Option A procedure before implementing T-4 |
| host::write_file does NOT exist in vsdd-hook-sdk — S-8.09 BLOCKED on S-8.10 (D-6 Option A SDK extension PR) | E-8 D-6 Option A; shared blocker with S-8.04; unblocker is S-8.10 | **Blocked by S-8.10 (vsdd-hook-sdk gains host::write_file). Implementation cannot proceed until S-8.10 merges.** S-8.10 is now in depends_on. Do not implement T-4 state-file write path until S-8.10 merges. See Library & Framework Requirements for the host::write_file row. |
| OQ-6 gate: security-reviewer audit MUST complete before WASM crate implementation | E-8 epic OQ-6; S-8.09 AC-010 | AC-010 is an explicit hard gate. Implementer must stop at T-2 until audit doc exists with complete frontmatter. No exception |
| bin/emit-event must NOT be removed until S-8.29 | E-8 D-10; R-8.07 | AC-012 and T-10 verify bin/emit-event is still present post-merge. Tier 2/3 bash hooks in W-16/W-17 require it via hooks.json direct path |
| BC-anchor strategy is Option C — reuse existing BCs; no new BC-7.02.x migration family | E-8 D-2 | BC-7.03.071..075 are the anchors. No new BC unless S-8.00 audit table shows Spec-Current = N |
| Dispatcher routing: native WASM plugins do NOT register in hooks.json | E-8 D-7; DRIFT-004 | AC-002: hooks.json entry deleted. hooks-registry.toml sole registration path |
| regression-gate has no tool filter in registry (fires on ALL PostToolUse) | BC-7.03.071 Registry entry; hooks-registry.toml lines 130-136 | The bash source body-filters to Bash via `[[ "$TOOL" != "Bash" ]] && exit 0`. The WASM port MUST preserve this body filter — do NOT add a `tool = "Bash"` registry filter (would change the hook's binding semantics). **OQ (F-S809-P1-013): Deliberately rejected.** Adding a registry-level tool filter would change BC-7.03.071's binding semantics. The body-filter approach preserves backward compatibility with the bash source. If a future story proposes a registry-level tool filter optimization, it must update BC-7.03.071 first. |
| All commits must follow Conventional Commits | BC-8.22.001 | feat(hook-plugins) for Rust crate; chore(registry) for registry migration; chore(cleanup) for .sh deletion; docs(.factory) for audit docs |
| Forbidden dependency: regression-gate WASM crate must NOT depend on legacy-bash-adapter | E-8 D-10 | Build-time enforcement rule |
| S-8.09 blocks W-16 wave gate — do not merge S-8.09 until AC-010, AC-011, AC-012 all pass | E-8 wave schedule; S-8.09 blocks field | W-16 stories (S-8.11+) must not be dispatched until S-8.09 is fully merged and all three audit ACs are confirmed. Legacy-bash-adapter retirement is S-8.29 (renumbered from S-8.28 post S-8.10 insertion). |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| Rust toolchain | stable (existing CI dependency) | Compile WASM crate |
| wasm32-wasip1 target | (via rustup, existing) | WASM compilation target (wasm32-wasi is deprecated; wasm32-wasip1 is correct per F-S809-P1-001) |
| vsdd-hook-sdk | (workspace version, existing) | Hook entry point macro, host::emit_event, host::read_file (existing fns) |
| host::write_file | TBD — pending D-6 Option A SDK extension PR | Required for AC-005 state file write; does NOT exist in vsdd-hook-sdk as of v1.0. **S-8.09 BLOCKED** on this SDK extension (shared blocker with S-8.04). Do not implement T-4 state-file write until extension PR merges. |
| serde_json | >=1.0 (workspace pin, existing) | Parse PostToolUse JSON from stdin; serialize state file JSON |
| serde | >=1.0 (workspace pin, existing) | Deserialization derive macros |
| chrono | >=0.4 (workspace pin — check existing) | UTC timestamp formatted as `%Y-%m-%dT%H:%M:%SZ`; replaces `date -u` subprocess |
| bats-core | >=1.10 (existing CI dependency) | Parity test harness |
| hyperfine | N/A — not required for S-8.09 | S-8.00 perf baseline does not include regression-gate; perf measurement is Tier-2-only scope per E-8 AC-7 (F-S809-P1-012). |

> NOTE: jq is NOT listed as a library dependency because the preferred WASM port uses
> serde_json for stdin parse AND serde_json::to_string for state file write. If the OQ-6
> audit determines jq subprocess is required (unlikely), add `binary_allow = ["jq"]` to
> the registry capability declaration per audit result.

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| `crates/hook-plugins/regression-gate/Cargo.toml` | create | Crate manifest; wasm32-wasip1 target; vsdd-hook-sdk, serde_json, serde, chrono deps |
| `crates/hook-plugins/regression-gate/src/lib.rs` | create | Hook implementation: stdin parse, Bash guard, 9-pattern match, status derivation, state file read/write, regression warning |
| `tests/integration/hooks/regression-gate.bats` | create | Bats parity tests (9 scenarios per AC-007) |
| `plugins/vsdd-factory/hooks-registry.toml` | modify | Replace legacy-bash-adapter entry with native regression-gate.wasm; capabilities per OQ-6 audit |
| `plugins/vsdd-factory/hooks/hooks.json.template` | modify | Delete regression-gate command entry |
| `plugins/vsdd-factory/hooks/hooks.json.darwin-arm64` | modify | Delete regression-gate command entry |
| `plugins/vsdd-factory/hooks/hooks.json.darwin-x64` | modify | Delete regression-gate command entry |
| `plugins/vsdd-factory/hooks/hooks.json.linux-arm64` | modify | Delete regression-gate command entry |
| `plugins/vsdd-factory/hooks/hooks.json.linux-x64` | modify | Delete regression-gate command entry |
| `plugins/vsdd-factory/hooks/hooks.json.windows-x64` | modify | Delete regression-gate command entry |
| `plugins/vsdd-factory/hooks/regression-gate.sh` | delete | Replaced by native WASM crate |
| `.factory/cycles/v1.0-brownfield-backfill/E-8-oq6-capability-profile.md` | create (security-reviewer) | OQ-6 audit result: confirmed subprocess list + capability declarations for registry |
| `.factory/cycles/v1.0-brownfield-backfill/E-8-tier1-native-audit.md` | create (implementer, in S-8.09 PR branch at T-7) | W-15 wave closure: all 9 Tier 1 hooks native; adapter status; bin/emit-event deferral confirmation |

## Changelog

| Version | Date | Author | Summary |
|---------|------|--------|---------|
| 1.3 | 2026-05-01 | -- | CONVERGENCE_REACHED at pass-5 (3/3 NITPICK_ONLY per ADR-013). Trajectory: 16 → 9 → 3 → 6 → 4 (substantive: 13 → 8 → 1 → 0 → 0). **5th anti-fabrication HARD GATE on BC-7.03.071 PASSED** — character-exact verbatim verification of all 5 quoted strings (3 postconditions + 2 invariants) against `.factory/specs/behavioral-contracts/ss-07/BC-7.03.071.md` lines 45-52. The 3-instance fabrication history (D-175 closure) remains permanently closed across 5 consecutive verifications. D-6 dependency wiring on S-8.10 verified correct (depends_on includes S-8.10; blocks excludes S-8.10; S-8.10 mirrors blocks=S-8.09; body references write_file at capability level — decoupled from S-8.10 v1.1 4-param signature drift). Universal-patch anchors all PASS. Status: draft → ready. Pass-5 review: adv-s8.09-p5.md. |
| 1.2 | 2026-05-01 | story-writer | Pass-2 fix burst (adv-s8.09-p2.md, 9 findings). F-S809-P2-001: SS-04 re-anchored to "Plugin Ecosystem" (canonical ARCH-INDEX name); SS-02 "Hook SDK and Plugin ABI" added as primary SDK-surface anchor. F-S809-P2-002: BC-7.03.071 invariants re-anchored to VERBATIM content from direct file read at `.factory/specs/behavioral-contracts/ss-07/BC-7.03.071.md` — THIRD fabrication instance fixed; fabricated "adapter binary present in registry" text removed; actual invariants quoted verbatim (invariant 1: "Hook script identity (script path) and registry binding remain stable across the contract lifetime." invariant 2: "Exit-code semantics conform to the dispatcher contract: 0 = allow / advisory, 2 = block, 1 = jq-missing-fail-closed (where applicable)."). F-S809-P2-003: OQ-write_file removed from assumption_validations (unregistered OQ — not in OQ-1..OQ-8 per E-8 epic); concern documented as D-6 dependency item. F-S809-P2-004: depends_on updated to include S-8.10 (SDK extension story, D-6 Option A unblocker); blocks updated S-8.28→S-8.29. F-S809-P2-005: AC-011 sequencing fixed — "runs as T-7" contradiction removed; audit "runs as part of T-7" (after registry edit, before PR merge). F-S809-P2-006: BC-7.03.075 title corrected to Unicode "pass→fail" in BC Trace table. F-S809-P2-007: S-8.04 cross-reference annotated as tentative (blocked + unconverged). F-S809-P2-008: EC-005 reframed for WASM context (jq subprocess eliminated; serde_json + host::write_file). F-S809-P2-009: AC-007 newline rendering pinned to 0x0A rendered newlines with bats $'...' quoting note. Tier 2/3 renumber: S-8.28→S-8.29 applied throughout (blocks field, adapter retirement references, legacy bridge close). |
| 1.1 | 2026-04-30 | story-writer | Pass-1 fix burst (adv-s8.09-p1.md, 16 findings). F-S809-P1-001: wasm32-wasi → wasm32-wasip1 throughout. F-S809-P1-002: host::write_file does not exist in SDK; added D-6 Option A blocker note to Library table, assumption_validations, Architecture Compliance Rules, Previous Story Intelligence; subsystems += SS-04. F-S809-P1-003: AC-010 now specifies required audit doc frontmatter schema (signoff_agent_id, signoff_timestamp, confirmed_subprocesses, recommended_binary_allow, recommended_capabilities), reconciliation rule, and falsifiable check. F-S809-P1-004: AC-009 "may be revised" language removed; contingency pinned with explicit AC+BC update requirement. F-S809-P1-005: AC-007 rewritten with 9-scenario bats fixture table (test names + stdin fields) and additional pattern-coverage unit test requirement for all 9 substrings. F-S809-P1-006: AC-002 expanded with pre-deletion impact analysis, post-deletion blast-radius validation, and rollback step. F-S809-P1-007: AC-011 sequencing clarified — audit runs inside S-8.09's PR branch (after T-7), not post-merge; S-8.09 rebases from integration branch with S-8.01..S-8.08 already merged. F-S809-P1-008: AC-012 unbounded find replaced with deterministic `test -f plugins/vsdd-factory/bin/emit-event` bounded check with exact path. F-S809-P1-009: BC table expanded with BC-7.03.071 invariant 1 and invariant 2 clause descriptions for AC-002 and AC-008 traceability. F-S809-P1-010: EC-006 updated — WASM port intentionally deviates from bash silent exit by logging write failure via host::log_error; deviation must be explicit. F-S809-P1-011: EC-003 explicitly pins bash `2>/dev/null` semantics; any log_warn addition requires new BC/AC. F-S809-P1-012: AC-007(i) perf scenario removed; replaced with pytest pattern coverage scenario; T-9 updated; hyperfine row marked N/A. F-S809-P1-013: OQ on tool filter explicitly rejected in Architecture Compliance Rules with rationale. F-S809-P1-014: T-2 rewritten with explicit orchestrator signal mechanism and security-reviewer dispatch protocol. F-S809-P1-015/016: Token Budget table restructured with Phase column; OQ-6 audit doc marked as conditional cost loaded at T-3. F-S809-P1-016-NIT: Story heading aligned to canonical title "adapter retirement prep (W-15 closer)". D-6 Option A blocker disclosed. |
| 1.0 | 2026-04-30 | story-writer | Initial draft. W-15 closing story: regression-gate port (PostToolUse, on_error=continue, 5 pts total). BC anchors: BC-7.03.071..075 confirmed via BC-INDEX grep (lines 1598-1602). 12 ACs covering WASM crate creation, Bash tool_name guard, 9-pattern test runner match, pass/fail derivation cascade (5 paths), state file write, pass-to-fail regression warning, bats parity (9 scenarios), OQ-6 security-reviewer gate (pre-implementation), Tier 1 native audit, bin/emit-event deferral check. OQ-6 resolved as read-only observer profile: preliminary binary_allow=["jq"] or empty (serde_json preferred). Security-reviewer audit doc required before T-3. assumption_validations: [OQ-6]. Adapter NOT deleted (S-8.29 — renumbered post S-8.10 insertion). bin/emit-event NOT deleted (S-8.29). Status: draft. |
