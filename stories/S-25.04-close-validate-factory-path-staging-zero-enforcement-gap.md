---
document_type: story
level: implementation
story_id: "S-25.04"
epic_id: "E-25"
version: "2.0"
last_amended: "2026-09-04 (v2.0) — F1/F2 activation + BC anchoring + status promotion (story-writer, single-commit TD-VSDD-053). Product-owner's F1 delta analysis (.factory/cycles/v1.0-brownfield-backfill/S-25.04-f1-delta-analysis.md) ground-truthed the ADR-047 §D8a \"artifact-write side\" phrasing against validate-factory-path-staging's actual authoritative contract (BC-4.16.001) and corrected the premise: this validator is a narrow git-staging exclusivity guard (INV-E21-001, CAP-034), NOT an artifact-write-content validator. The architect's F2 architecture decisions (.factory/cycles/v1.0-brownfield-backfill/S-25.04-f2-architecture-decisions.md) resolved the companion validator's identity (validate-factory-path-staged, priority 161, native-WASM fuel-axis-only, PostToolUse ^Bash$) and the trigger-scope tradeoff (BROAD — unconditional check, no command-text pre-filter), flagged PENDING-HUMAN-RATIFICATION; the human ratified BROAD 2026-09-04. Product-owner then authored BC-4.16.002 v1.0 (new, SS-04/CAP-034 — the companion validator's own contract) and amended BC-1.18.001 (v1.6→v1.7, invariant 4 clarification: PostToolUse-only marker-write trigger is a property of event type, not a closed plugin set) and BC-1.18.004 (v1.3→v1.4, postcondition 4 companion count statement: Layer-1 effective fail-closed count at S-25.04 merge becomes N≥1, additive to — not overwriting — the ZERO-at-S-25.01-merge historical anchor). This burst: (1) corrects every \"artifact write content\"/content-validator framing in this story's Narrative and Context to the git-staging-exclusivity / post-hoc detective-mirror premise throughout; (2) sets behavioral_contracts: [BC-4.16.002, BC-1.18.001, BC-1.18.004] and propagates the array change atomically to the body BC table + all six ACs + Token Budget per POLICY 8; (3) re-anchors AC-001..AC-006 from BC-TBD to real BC-S.SS.NNN PC/invariant/EC numbers; (4) corrects Architecture Mapping / File Structure / Tasks to the finalized crate name validate-factory-path-staged (crates/hook-plugins/validate-factory-path-staged/), registry priority 161, and removes all \"naming TBD at F2\" / PRELIMINARY / provisional markers; (5) removes the PENDING-BC-AUTHORSHIP blockquote and BC-status-pending comments; (6) promotes status: draft -> ready (Spec-First Gate S-7.01 satisfied — behavioral_contracts non-empty, canonical BC-S.SS.NNN IDs, bidirectional AC<->BC traces present). [Prior: 2026-09-03 (v1.0) — Initial authoring (story-writer; single-commit TD-VSDD-053). Authored per ADR-047 v1.4 §Decision 8a \"Known Gap\" note (20bf81dd) and BC-1.18.004 v1.2 postcondition 4 (df3219ea), both routed from pr-reviewer's fresh-eyes MAJOR finding on PR #807 (human-directed factual correction, NOT a POLICY 22 design change): validate-factory-path-staging's Cohort A-immediate `failure_policy = \"fail-closed\"` assignment (S-25.01) currently produces ZERO observable production enforcement effect. Registered as a PENDING-BC story; technical options and recommendation carried forward verbatim from ADR-047 §Decision 8a \"Known Gap\" note (architect adjudication).]"
status: merged
producer: story-writer
timestamp: 2026-09-03T00:00:00Z
phase: 3
inputs:
  - .factory/specs/architecture/decisions/ADR-047-indeterminate-outcome-model-durable-mutation-marker-next-advance-gate.md
  - .factory/specs/architecture/decisions/ADR-039-validator-failure-policy-resource-exhaustion-fail-closed.md
  - .factory/specs/architecture/decisions/ADR-048-fail-closed-but-recoverable-gate-block-if-marker-crash-policy-marker-ttl-deadman-and-ungated-escape-invariant.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.001.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.004.md
  - .factory/specs/behavioral-contracts/ss-04/BC-4.16.001.md
  - .factory/specs/behavioral-contracts/ss-04/BC-4.16.002.md
  - .factory/cycles/v1.0-brownfield-backfill/S-25.04-f1-delta-analysis.md
  - .factory/cycles/v1.0-brownfield-backfill/S-25.04-f2-architecture-decisions.md
  - .factory/stories/S-25.01-dispatcher-indeterminate-outcome-layer1.md
  - .factory/stories/S-21.24-capstone-gated-flip-completion-regression.md
input-hash: "fbcfb13"
# POLICY 18: story-writer computed via `compute-input-hash ... --update` in this same burst
# (2026-09-04, F1/F2 activation + promotion). Frontmatter value is authoritative for this file;
# the STORY-INDEX catalog-row + blockquote three-way-parity sync is state-manager's to perform
# next, in the burst that commits this file (per dispatch scope — story-writer edits only this
# file).
traces_to: .factory/stories/epics/E-25-validation-integrity.md
points: "8"
depends_on: [S-25.01]
blocks: []
behavioral_contracts: [BC-4.16.002, BC-1.18.001, BC-1.18.004]
verification_properties: []
# VP-TBD — Phase-6 formal-verifier anchor (POLICY 9 sanctioned VP-TBD deferral), consistent with
# BC-4.16.002's own VP row (Phase-6 deferral; see BC-4.16.002 Verification Properties section and
# its architect-directed calibration-corpus note: synthetic >=500-staged-path fixture, not the
# six-member large-artifact corpus).
priority: "P1"
cycle: v1.0-brownfield-backfill
wave: null
target_module: crates/factory-dispatcher
subsystems: [SS-01, SS-04, SS-07]
estimated_days: 3
assumption_validations: []
risk_mitigations: []
tdd_mode: strict
---

> **tdd_mode: strict** — Full TDD Iron Law enforced: all non-trivial function bodies use `todo!()`;
> Red Gate density check ≥0.5 required before Step 4 dispatch.

# S-25.04: Close validate-factory-path-staging Zero-Enforcement Gap — Real Layer-1 Production Trigger

## Narrative

- **As a** factory pipeline operator who was told (S-25.01) that `validate-factory-path-staging`'s
  fail-closed assignment closes the CWE-754 "cannot-complete-treated-as-PASS" hole for the
  `.factory/` **git-staging exclusivity guard** (INV-E21-001, CAP-034 — `validate-factory-path-staging`
  blocks `git add`/`git stage` of `.factory/` paths on product branches; it has no role in validating
  the *content* of a `.factory/` artifact write)
- **I want** that fail-closed assignment to have an ACTUAL production enforcement effect — a real
  trigger path that reaches `write_indeterminate_marker` and the next-advance gate when a
  fail-closed validator's WASM execution cannot complete (fuel exhaustion, epoch timeout, or
  OutputTooLarge)
- **So that** the gap identified in ADR-047 v1.4 / BC-1.18.004 v1.2 (the assignment is safely SET
  but currently ENFORCES NOTHING — identical runtime effect to `failure_policy = "fail-open"`) is
  genuinely closed, not just correctly documented — via a new **post-hoc git-staging detective
  mirror** validator (`validate-factory-path-staged`, BC-4.16.002) that inspects the actual git
  index/branch state after a Bash command completes, giving the existing marker/gate mechanism
  (S-25.01) its first structurally-reachable PostToolUse trigger

## Context / Feature Summary

**Follow-up to S-25.01 (Layer 1), anchored per ADR-047 v1.4 §Decision 8a "Known Gap" note
(20bf81dd) and BC-1.18.004 v1.2 postcondition 4 (df3219ea) — both human-directed factual
corrections, routed from pr-reviewer's fresh-eyes MAJOR finding on PR #807, NOT a POLICY 22
design/security-model change. Activated at F1/F2 (2026-09-04); BCs anchored; status promoted to
`ready` in this same burst.**

**The gap, precisely stated:** `validate-factory-path-staging` (BC-4.16.001) is registered in
`hooks-registry.toml` as `failure_policy = "fail-closed"`, PreToolUse `^Bash$`. The Layer-1
durable-marker write path (`write_indeterminate_marker`, invoked from the dispatcher's PostToolUse
handling per BC-1.18.001 invariant 4) fires **ONLY on a PostToolUse dispatch**. Because this
validator is registered PreToolUse, it structurally can never reach that write path — no matter
how many times its WASM execution fuel-exhausts, epoch-times-out, or trips OutputTooLarge, no
marker is ever written and no gate is ever triggered. Combined with `on_error = "continue"`
(which never blocks the *current* dispatch either, present or future) and this validator's
absence from the ADR-039 §Decision 2 exhaustion-leg roadmap prior to this story, an INDETERMINATE
outcome on this validator degrades to the identical advisory-only `plugin.indeterminate` event a
`failure_policy = "fail-open"` plugin would produce. The fail-closed config bit is real and
correctly set; it currently enforces nothing.

**PREMISE CORRECTION (F1, 2026-09-04 — supersedes v1.0's provisional framing):** the original
v1.0 authoring of this story described option (a)'s companion validator as mirroring
`validate-factory-path-staging`'s check "on the artifact-write side," and provisionally guessed a
new BC would live at `BC-1.18.005`/SS-01. Ground-truthing `validate-factory-path-staging` against
its own authoritative behavioral contract (**BC-4.16.001**, SS-04) showed this framing was
imprecise in a way that mattered for BC authorship: `validate-factory-path-staging` does **not**
validate the *content* of a `.factory/` artifact write. It is a narrow **git-staging exclusivity
guard** (INV-E21-001, CAP-034, issue #342): a `PreToolUse ^Bash$` plugin that inspects a Bash
payload for a `git add`/`git stage` command and, if any argument resolves to a `.factory/`-rooted
path **and** the current branch is a product branch (not `factory-artifacts`), blocks the command
before it runs. There is no "artifact write" for a companion validator to validate — the thing
being protected is exclusive git-tracking of `.factory/` content between the `factory-artifacts`
orphan branch and product branches. The companion validator that actually closes the enforcement
gap is therefore **not** a generic Edit/Write/MultiEdit `.factory/`-write validator; it is the
natural **post-hoc detective mirror** of the same git-staging-exclusivity concern: a
`PostToolUse ^Bash$` plugin that, after a Bash command completes, inspects the *actual* git
index/branch state (`git diff --cached --name-only` + `git branch --show-current`, the same
`host::exec_subprocess` pattern `validate-factory-path-staging` itself already uses) to detect
whether a `.factory/` path ended up staged on a product branch. This closes the residual-risk
class BC-4.16.001's own text-based command-parsing heuristics cannot see — staging via git
plumbing, aliases, wrapper scripts, or any invocation whose completed command text never contains
a recognizable `git add`/`git stage` substring (BC-4.16.001 Invariant 6 Accepted Residuals) —
because the check runs against actual index state, not payload text. This correction relocated
the new BC's home from the guessed `BC-1.18.005`/SS-01 to the correct **BC-4.16.002**/SS-04 (new
sibling of BC-4.16.001, same capability CAP-034) — the SS-01 BCs (BC-1.18.001/BC-1.18.004) needed
only narrow, non-mechanism-changing amendments, since they define the *generic* INDETERMINATE/
marker mechanism that already applies automatically to any qualifying PostToolUse fail-closed
plugin, including this new one. See `.factory/cycles/v1.0-brownfield-backfill/
S-25.04-f1-delta-analysis.md` §0 for the full correction record and `.factory/cycles/
v1.0-brownfield-backfill/S-25.04-f2-architecture-decisions.md` §4 for the ADR-047 §D8a
erratum-class clarification note (ready for a subsequent architect/state-manager commit; not
applied by this story file).

**Why this matters:** `validate-factory-path-staging` was the ONE Cohort A validator originally
advertised (S-25.01 v1.1–v1.20, ADR-047 v1.0–v1.3) as immediately, unconditionally effective — no
S-21.24 calibration dependency, unlike the two `^Agent$` Cohort A-deferred validators. Discovering
that it, too, currently delivers zero enforcement meant Layer 1's fail-LOUD guarantee had **no
live production trigger at all** as of S-25.01 merge — both effective-enforcement paths (this one
structurally inert, the other two S-21.24-gated) were dormant. This story delivers the first one.

**Confirmed closure design (F1 §2 + F2, architect/product-owner, 2026-09-04):**

- **New PostToolUse companion validator: `validate-factory-path-staged`** (SS-04, BC-4.16.002) —
  the git-staging-exclusivity **detective mirror** of BC-4.16.001. Reuses S-25.01's marker/gate
  machinery (`write_indeterminate_marker`, `should_write_marker`, `classify_outcome`,
  `validate-unvalidated-mutation-marker` both gate arms) verbatim — no new marker mechanism, no
  new WASM-tier-loop branching. Only a new WASM plugin crate + one new `hooks-registry.toml` entry
  are needed. Crate: `crates/hook-plugins/validate-factory-path-staged/`. Registration:
  `event = "PostToolUse"`, `tool = "^Bash$"`, `priority = 161`, `failure_policy = "fail-closed"`,
  `on_error = "continue"`, `timeout_ms = 5000`, native-WASM fuel-axis-only calibration
  (`fuel_cap = max(observed_max × 1.5, 50_000_000)`).
- **Trigger scope: BROAD — unconditional, no command-text pre-filter (human-ratified 2026-09-04,
  FINAL, not provisional).** The companion's internal `git diff --cached --name-only` /
  `git branch --show-current` check runs on **every** completed `PostToolUse ^Bash$` dispatch,
  regardless of whether the triggering command's text looked git-related. The architect
  recommended BROAD over a narrower text-gated alternative because a text-gated internal filter
  would make the detective mirror a near-tautological confirmation of BC-4.16.001's own successes
  and functionally blind to BC-4.16.001's own documented failure modes (Invariant 6 Accepted
  Residuals) — the check being performed is against actual index state, not payload text, so any
  internal text-based pre-filter reintroduces the same under-match risk class the mirror exists to
  close. Cost is bounded: both `exec_subprocess` calls read only git's index metadata and current-
  branch pointer, governed by staged-path cardinality and repository ref state — not by
  `.factory/` cycle-artifact size or triggering payload size, the same "bounded input"
  classification `validate-factory-path-staging` itself received for Cohort A-immediate treatment.
- **ADR-039 §Decision 2 roadmap extension confirmed** — `validate-factory-path-staged` is the
  seventh member, placed in the native-WASM sub-list (alongside `validate-cross-site-
  correspondence`), fuel-axis-only calibration; a validator-specific calibration corpus (synthetic
  ≥500-simultaneously-staged-paths fixture, not the six-member large-`.factory/`-artifact corpus)
  is directed for the eventual formal-verifier/test-writer calibration pass. This registration act
  does NOT require S-21.24 to have merged (S-21.24 governs the separate ADR-039 Phase-4
  CURRENT-DISPATCH-blocking executor wiring for the full seven-validator set).
- **Option (b) (changing `validate-factory-path-staging`'s own `on_error` from `"continue"` to a
  blocking variant) remains REJECTED as primary** — F1 found no infeasibility in option (a), so no
  escalation to option (b) is warranted. `validate-factory-path-staging`'s own registration is
  byte-identical/unchanged by this story (AC-003).

**Subsystem anchor justifications:**
- SS-01 owns the amendment scope for BC-1.18.001/BC-1.18.004 because SS-01 is the Hook Dispatcher
  Core (executor.rs, indeterminate_marker.rs) per ARCH-INDEX Subsystem Registry; the generic
  marker-write mechanism this story's companion validator activates lives there, unchanged.
- SS-04 owns the new companion WASM plugin crate (`crates/hook-plugins/validate-factory-path-staged/`)
  and BC-4.16.002 per ARCH-INDEX Subsystem Registry (SS-04 = Plugin Ecosystem / Hook Plugin SDK and
  WASM plugin development) — confirmed by the architect (F2 §5.1): SS-04 already owns
  `crates/hook-plugins/validate-factory-path-staging/` (BC-4.16.001's crate); the new companion
  crate is the same kind of artifact built by the same subsystem.
- SS-07 owns `hooks-registry.toml` configuration per ARCH-INDEX Subsystem Registry (SS-07 = Hook
  Registry Configuration); the new companion validator's registry entry (priority 161) and the
  ADR-039 §Decision 2 roadmap extension are both registry-config-adjacent changes.

**Dependency anchor justification:**
- S-25.04 depends_on [S-25.01] because S-25.01 delivers the entire mechanism this story activates:
  `write_indeterminate_marker`, `should_write_marker`, `classify_outcome`, the
  `validate-unvalidated-mutation-marker` two-arm gate plugin, and the Cohort A
  `failure_policy = "fail-closed"` registry assignment for `validate-factory-path-staging` itself
  (whose zero-enforcement gap this story closes). Without S-25.01 merged, there is no marker-write
  path for `validate-factory-path-staged` to trigger, and no gate for it to activate. S-25.04 does
  NOT depend on S-25.02/S-25.03 (Layers 2/3, different artifact-sharding and bounded-window
  concerns) or on S-21.24 for its own implementation scope — only the ADR-039 roadmap extension's
  eventual CURRENT-DISPATCH-blocking activation (via ADR-039 Phase 4 executor wiring) depends on
  S-21.24; the roadmap-registration act itself and the marker-write trigger (AC-001) do not.

## Acceptance Criteria

### AC-001: `validate-factory-path-staged` reaches the existing marker-write path (traces to BC-4.16.002 Postcondition 3 — INDETERMINATE trigger/AC-001 closure criterion; and BC-1.18.001 Postcondition 4 — durable marker write, generic mechanism invoked)

The new PostToolUse WASM validator `validate-factory-path-staged` MUST be registered in
`hooks-registry.toml` as `event = "PostToolUse"`, `tool = "^Bash$"`, `priority = 161`,
`failure_policy = "fail-closed"`. When this plugin's own WASM execution cannot complete during
either of its `exec_subprocess` calls (fuel exhaustion, epoch timeout, or OutputTooLarge), the
dispatcher MUST classify the outcome as `DispatchOutcome::Indeterminate` (BC-1.18.001 Postcondition
1) and MUST reach `write_indeterminate_marker` (BC-1.18.001 Postcondition 4) — this is the first
Layer-1 validator whose fail-closed assignment has a structurally-reachable PostToolUse trigger
(BC-4.16.002 Postcondition 3).

### AC-002: Existing marker + gate mechanism is reused, not re-implemented (traces to BC-4.16.002 Invariant 5 — reuse-not-reimplementation of S-25.01 marker/gate machinery; and BC-1.18.001 Postcondition 4 / Invariant 4 — generic mechanism, REUSE-UNCHANGED)

`validate-factory-path-staged` MUST NOT introduce a second marker file, a second gate mechanism, or
duplicate `classify_outcome` / `should_write_marker` logic, and MUST NOT introduce any
plugin-specific branching in the tier-execution loop. It reuses `.factory/unvalidated-mutation.marker`,
`write_indeterminate_marker`, `validate-unvalidated-mutation-marker` (both existing gate arms), and
the existing pure-core classification functions from S-25.01 verbatim.

### AC-003: No behavioral change to `validate-factory-path-staging` itself (traces to BC-4.16.002 Invariant 1 — second enforcement instantiation, additive not replacement; and Edge Case EC-003 — sibling's own PreToolUse registration unaffected)

`validate-factory-path-staging`'s own registration (PreToolUse `^Bash$`, `on_error = "continue"`,
`failure_policy = "fail-closed"`, BC-4.16.001) is UNCHANGED by this story. This story adds a
companion validator (`validate-factory-path-staged`); it does not modify or replace the existing
one. Option (b) (changing `on_error`) remains explicitly OUT OF SCOPE for this delivery (see
Context §Confirmed closure design).

### AC-004: ADR-039 §Decision 2 roadmap extension (traces to BC-4.16.002 Traceability — ADR-039 §Decision 2 seventh-member roadmap entry, native-WASM/fuel-axis-only sub-list; architect-owned ADR content per F1 §6 / F2 §3, no independent BC postcondition for the ADR document edit itself)

ADR-039 §Decision 2's exhaustion-leg roadmap MUST be extended to name `validate-factory-path-staged`
(from AC-001) as a seventh member, in the native-WASM sub-list (alongside
`validate-cross-site-correspondence`), fuel-axis-only calibration (`fuel_cap =
max(observed_max × 1.5, 50_000_000)`; calibration corpus: synthetic ≥500-simultaneously-staged-paths
fixture per architect F2 §3.2, not the six-member large-`.factory/`-artifact corpus). This
registration act does NOT require S-21.24 to have merged; it is independent of the ADR-039 Phase 4
CURRENT-DISPATCH-blocking executor wiring that S-21.24 delivers for the full seven-validator set.
The exact ADR-039 §Decision 2 text is finalized in F2 §3.1 and is routed to architect/state-manager
to apply verbatim (not this story's implementer to edit directly).

### AC-005: Layer-1 effective fail-closed count increases from ZERO (traces to BC-1.18.004 Postcondition 4 — companion "Layer-1 effective fail-closed count at S-25.04 merge: N≥1" statement)

After this story merges, BC-1.18.004 Postcondition 4's new companion statement — "Layer-1 effective
fail-closed count at S-25.04 merge: N ≥ 1," attributable to `validate-factory-path-staged`'s live
enforcement — becomes the observable, measurable closure of the gap ADR-047 v1.4 / BC-1.18.004 v1.2
identified. `validate-factory-path-staging` itself remains at ZERO per its own unchanged
registration (AC-003); the original "ZERO at S-25.01 merge" statement is a dated historical anchor
and is NOT overwritten by this amendment (BC-1.18.004 v1.4, already authored — additive-only,
no further story-side edit needed to BC-1.18.004 itself).

### AC-006: Full regression suite stays green (traces to BC-1.18.004 Postcondition 5 — backward-compat guard test preservation, DO-NOT-DELETE)

`cargo test --workspace --all-targets` and `plugins/vsdd-factory/tests/run-all.sh` MUST stay
green after this story lands. `test_BC_1_18_004_fail_open_default_preserves_advisory_behavior`
(the DO-NOT-DELETE backward-compat guard test from S-25.01, BC-1.18.004 Postcondition 5) MUST NOT
be deleted or modified.

## Behavioral Contracts

| BC ID | Title | Version | Subsystem | Role in this story |
|-------|-------|---------|-----------|---------------------|
| BC-4.16.002 | `validate-factory-path-staged` WASM PostToolUse Guard MUST Detect Any `.factory/` Path Staged on a Product Branch After a Bash Command Completes — Unconditional Broad-Trigger Check on Every `^Bash$` Dispatch, and MUST Reach the INDETERMINATE Marker-Write Path When It Cannot Complete | 1.0 | SS-04 | **NEW** — the companion validator's own check semantics (post-hoc git-staging detection, BROAD trigger scope) and its INDETERMINATE-trigger postcondition (AC-001 closure criterion) |
| BC-1.18.001 | When a fail-closed Validator Cannot Complete, Dispatcher Classifies Outcome as INDETERMINATE, Emits `plugin.indeterminate` Event, and Writes Unvalidated-Mutation Marker | 1.7 | SS-01 | **AMENDED (clarification-only)** — invariant 4 gains a forward-citing clause: "PostToolUse-only" marker-write trigger is a property of event type, not a closed plugin set; `validate-factory-path-staged` cited as the second concrete PostToolUse fail-closed instantiation. No postcondition/wire-format/mechanism change; the generic marker-write mechanism this story's companion validator invokes is otherwise REUSE-UNCHANGED |
| BC-1.18.004 | `failure_policy = fail-open` INDETERMINATE Is Advisory-Only — No Marker Written, No Gate Triggered (Backward-Compatibility Anchor) | 1.4 | SS-01 | **AMENDED (additive)** — postcondition 4 gains a new, separately-dated companion statement: "Layer-1 effective fail-closed count at S-25.04 merge: N ≥ 1," attributable to `validate-factory-path-staged`'s live enforcement; the "ZERO at S-25.01 merge" statement is unchanged and not overwritten (anti-volatile-pin discipline) |

`BC-4.16.001` (`validate-factory-path-staging`'s own contract) is read as ground-truth for the
companion's mirrored path-matching predicate but is REUSE-UNCHANGED and is not itself amended by
this story (AC-003) — it is not a member of this story's `behavioral_contracts:` array for that
reason, consistent with POLICY 8 (only BCs this story actually creates or amends are listed).

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| `validate-factory-path-staged` WASM PostToolUse companion plugin | `crates/hook-plugins/validate-factory-path-staged/src/lib.rs` (entry point); `src/main.rs` (WASI `_start` wrapper); `src/tests.rs`; `tests/proptest_*.rs` | Effectful (issues `host::exec_subprocess` for `git diff --cached --name-only` and `git branch --show-current`; on cannot-complete, invokes the same INDETERMINATE-classification path S-25.01 established) |
| `.factory/` path-matching predicate (reused verbatim from BC-4.16.001) | Same pure function BC-4.16.001's own plugin uses — no re-derived predicate | Pure-core |
| `write_indeterminate_marker`, `should_write_marker`, `classify_outcome` (REUSED, not modified) | `crates/factory-dispatcher/src/indeterminate_marker.rs`, `crates/factory-dispatcher/src/executor.rs` | Effectful / Pure-core (unchanged from S-25.01) |
| `validate-unvalidated-mutation-marker` two-arm gate (REUSED, not modified) | `crates/hook-plugins/validate-unvalidated-mutation-marker/src/lib.rs` | Effectful (unchanged from S-25.01) |
| `validate-factory-path-staged` registry entry | `plugins/vsdd-factory/hooks-registry.toml` (`priority = 161`, `event = "PostToolUse"`, `tool = "^Bash$"`, `failure_policy = "fail-closed"`, `on_error = "continue"`, `timeout_ms = 5000`, `[hooks.capabilities.exec_subprocess]` `binary_allow = ["git"]`) | Configuration |
| ADR-039 §Decision 2 roadmap extension (seventh member, native-WASM sub-list) | `.factory/specs/architecture/decisions/ADR-039-validator-failure-policy-resource-exhaustion-fail-closed.md` §Decision 2 | Documentation (architect/state-manager-owned; text finalized in F2 §3.1, ready to apply verbatim — not this story's implementer to edit directly) |
| ADR-047 §Decision 8a clarification note (erratum-class) | `.factory/specs/architecture/decisions/ADR-047-indeterminate-outcome-model-durable-mutation-marker-next-advance-gate.md` §Decision 8a | Documentation (architect/state-manager-owned; text finalized in F2 §4, ready to apply verbatim) |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|----------------|
| `.factory/` path-staging git-index/branch-inspection predicate | Pure-core (path-matching logic) / Effectful (the two `exec_subprocess` calls that produce its inputs) | Reuses BC-4.16.001's own pure path-matching predicate verbatim (BC-4.16.002 Invariant 4); the git-index/branch reads themselves are effectful, consistent with S-25.01's classification of the sibling validator's own `host::exec_subprocess` dependency. |
| `validate-factory-path-staged` WASM entry point | Effectful (read) | Reads completed-Bash-dispatch payload plus live git index/branch state; on cannot-complete, the dispatcher-side INDETERMINATE classification and marker write are effectful (unchanged S-25.01 machinery). |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | `validate-factory-path-staged` itself fuel-exhausts during its `exec_subprocess` calls for a completed Bash dispatch | Dispatcher classifies INDETERMINATE per `classify_outcome` (S-25.01, unchanged); `write_indeterminate_marker` fires because this validator IS registered PostToolUse (unlike `validate-factory-path-staging`) — this is the exact behavior AC-001 requires (BC-4.16.002 PC3 / EC-001). |
| EC-002 | A Bash command stages/writes files entirely outside `.factory/` | The unconditional BROAD-scope check still runs (`git diff --cached --name-only`, `git branch --show-current`); finds no `.factory/` path staged → passes (BC-4.16.002 PC2 / EC-002). |
| EC-003 | `validate-factory-path-staging` (PreToolUse, BC-4.16.001) still fuel-exhausts independently | Unchanged S-25.01 behavior: no marker written via THIS validator (PreToolUse-only per BC-1.18.001 invariant 4, AC-003); `validate-factory-path-staged` (PostToolUse, this story) is the one that can reach the marker for the corresponding completed write (BC-4.16.002 EC-003). |
| EC-004 | Both `validate-factory-path-staging` (PreToolUse, blocks a `git add` on one dispatch) and `validate-factory-path-staged` (PostToolUse, INDETERMINATE fail-closed on a later dispatch) are involved | Two `plugin.indeterminate` events emitted at most (one per plugin, per BC-3.08.001 Event 8 semantics — unchanged); `(plugin_name, artifact_path)` pairs distinguish the two validators' own INDETERMINATE events by construction; single-marker last-writer-wins policy (BC-1.18.001 invariant 3) applies exactly as it would for any two distinct fail-closed plugins (BC-4.16.002 EC-004). |
| EC-005 | Branch detection fails (`git branch --show-current` non-zero exit, empty output, detached HEAD) during the post-hoc check | Fail-open (BC-4.16.002 PC4 / Invariant 3): exit code 0, advisory warning only — mirrors BC-4.16.001 Invariant 3 exactly; an uncertain branch state is not, on its own, evidence of a violation. |
| EC-006 | `.factory/` path appears staged and current branch is `factory-artifacts` (legitimate state-manager commit) | Passes unconditionally (BC-4.16.002 PC2 / EC-006), mirroring BC-4.16.001 PC3. |
| EC-007 | Triggering Bash command was not git-related at all (e.g. `npm test`) | The check still executes unconditionally per BROAD scope (Precondition 2) — NOT a text-based fast-pass skip; resolves via PC2 because `git diff --cached --name-only` returns nothing relevant. An EARLIER git-plumbing/script/alias action that already staged a `.factory/` path before this non-git command ran would still be caught — the exact residual-risk class BROAD scope exists to close (BC-4.16.002 EC-007). |
| EC-008 | `validate-factory-path-staged` crashes for a non-resource-exhaustion reason (`Trap::UnreachableCodeReached`, host-ABI mismatch) | `on_error = "continue"` applies: advisory only, no block, no marker (BC-4.16.002 PC5 / EC-008). |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| Story spec (S-25.04, this file) | 3,500 |
| BC files (3 BCs: BC-4.16.002 new, BC-1.18.001 amended, BC-1.18.004 amended — full read) | 9,000 |
| BC-4.16.001 (sibling — ground-truth for the reused path-matching predicate) | 2,000 |
| ADR-039 §Decision 2/3/4 (roadmap extension + calibration formulas) | 2,000 |
| ADR-047 §Decision 8a (Known Gap + clarification note) | 1,000 |
| S-25.01 (predecessor story — marker/gate machinery being reused) | 4,000 |
| Existing crate: `crates/hook-plugins/validate-factory-path-staging/` (structure to mirror) | 2,500 |
| Affected code: new `validate-factory-path-staged` WASM plugin crate + tests (est.) | 4,000 |
| `hooks-registry.toml` (new entry + priority-161 collision check) | 500 |
| **Total (estimated)** | **~28,500** |
| Agent context window | 200K (Sonnet) |
| **Budget usage (est.)** | **~14.25%** |

## Tasks (MANDATORY)

1. [ ] (Routed, not this story's implementer) Architect or state-manager: apply the ADR-047
       §Decision 8a clarification-note erratum (exact wording finalized in S-25.04 F2 architecture
       decisions §4) and the ADR-039 §Decision 2 seventh-member roadmap-extension text (F2 §3.1) to
       the respective ADR files — both fully specified, ready to apply verbatim.
2. [ ] Create `crates/hook-plugins/validate-factory-path-staged/` crate mirroring
       `crates/hook-plugins/validate-factory-path-staging/` structure (`src/lib.rs`, `src/main.rs`
       WASI `_start` wrapper, `src/tests.rs`, `tests/proptest_*.rs`); reuse BC-4.16.001's
       case-insensitive `.factory/` path-matching predicate verbatim (BC-4.16.002 Invariant 4).
3. [ ] Wire the PC3 INDETERMINATE trigger to `write_indeterminate_marker` / `should_write_marker` /
       `classify_outcome` verbatim (S-25.01 machinery, zero mechanism changes — BC-4.16.002
       Invariant 5, AC-002).
4. [ ] Register `validate-factory-path-staged` in `hooks-registry.toml`: `event = "PostToolUse"`,
       `tool = "^Bash$"`, `priority = 161`, `failure_policy = "fail-closed"`,
       `on_error = "continue"`, `timeout_ms = 5000`, `[hooks.capabilities.exec_subprocess]`
       `binary_allow = ["git"]`, `env_allow = ["HOME", "GIT_CONFIG_GLOBAL", "XDG_CONFIG_HOME"]`
       (BC-4.16.002 Architecture Anchors; F2 §2.3).
5. [ ] Write RED Gate test stubs (failing `todo!()` bodies) covering BC-4.16.002's T-1..T-9
       canonical test vectors.
6. [ ] Verify EC-001..EC-008 (BC-4.16.002 Edge Cases) — marker-write trigger path, BROAD-scope
       unconditional coverage, PreToolUse/PostToolUse coexistence, branch-detection fail-open,
       crash-advisory, single-marker cross-pair policy.
7. [ ] Run `cargo test --workspace --all-targets` and `plugins/vsdd-factory/tests/run-all.sh`;
       confirm `test_BC_1_18_004_fail_open_default_preserves_advisory_behavior` (BC-1.18.004 PC5)
       stays green and unmodified (AC-006).
8. [ ] Amend S-25.01's Traceability documentation: "Layer-1 effective fail-closed count" statement
       updated to reflect the new live enforcement (AC-005 / BC-1.18.004 Postcondition 4 companion
       statement) — coordinate with story-writer/state-manager.
9. [ ] Add CHANGELOG entry under [Unreleased] > Added: "Layer-1 validation-integrity: real
       production enforcement trigger for validate-factory-path-staging via new PostToolUse
       companion validator validate-factory-path-staged (closes S-25.01/ADR-047 zero-enforcement
       gap)" — before PR creation, per CHANGELOG delivery discipline.

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-25.01 (Layer 1 — predecessor) | INDETERMINATE outcome classification + durable marker + next-advance gate shipped. Cohort A (3 validators) assigned fail-closed; `validate-factory-path-staging`'s assignment was originally believed effective ("EFFECTIVE-NOW") but corrected (ADR-047 v1.4 / BC-1.18.004 v1.2, human-directed, PR #807) to ZERO observable enforcement — the exact gap this story closes. | `indeterminate_marker.rs` atomic write pattern (temp + rename); `validate-unvalidated-mutation-marker` WASM plugin crate structure (single binary, two `[[hook]]` registry entries for the two arms); `classify_outcome` / `should_write_marker` pure-core split. | **The critical lesson this story exists to fix:** a validator's `failure_policy = "fail-closed"` registry assignment is NOT sufficient for enforcement — the event/tool registration (PreToolUse vs. PostToolUse) determines whether the marker-write path is structurally reachable at all. Always verify a fail-closed validator is registered on the event type where the durable-marker write actually fires (PostToolUse, per BC-1.18.001 invariant 4) before claiming its assignment is "effective." Do NOT repeat the EFFECTIVE-NOW overclaim pattern for `validate-factory-path-staged` — verify its PostToolUse registration reaches `write_indeterminate_marker` with a real test, not narrative assertion alone (TD-VSDD-059 paper-fix detection). |
| S-25.04 F1/F2 (this story's own activation — predecessor lesson within the same story) | The original v1.0 authoring's "artifact-write side" phrasing (loosely echoing how BC-1.18.001 talks about `artifact_path`) invited a companion-validator design that didn't correspond to anything BC-4.16.001 actually protects, and a wrong BC placement guess (BC-1.18.005/SS-01 instead of BC-4.16.002/SS-04). | Ground-truth every "mirror the sibling validator" design against the sibling's own authoritative BC (BC-4.16.001), not against loose ADR shorthand. Capability Anchor Justification (S-7.01): a second enforcement instantiation of an EXISTING capability (CAP-034) is not a new capability — do not create a new BC family (BC-1.18.00x) just because the closure story's predecessor mechanism (BC-1.18.001, CAP-041) lives there; the mechanism a plugin's failure triggers and what the plugin itself checks are two separate BC concerns, kept in separate subsystems here exactly as they already are for BC-4.16.001/BC-1.18.001. | The trigger-scope completeness-vs-cost tradeoff (BROAD vs. narrow text-gated internal check) was a genuine no-dominant-option design fork; the architect correctly routed it to explicit human ratification (POLICY-22-adjacent discipline, mirroring ADR-047's own Cohort A ratification precedent) rather than resolving it by architect fiat — do not silently pick an option on a new detective control's coverage model without surfacing the tradeoff, especially when the story itself exists because an earlier under-scrutinized registration choice silently produced zero enforcement. |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| `validate-factory-path-staged` MUST be registered PostToolUse — a PreToolUse registration would repeat the exact structural gap this story closes | BC-4.16.002 Precondition 4; BC-1.18.001 invariant 4 (v1.7) | AC-001; PR review MUST verify the `hooks-registry.toml` event field before merge |
| No second marker file or duplicate gate mechanism — reuse S-25.01's `write_indeterminate_marker` / `validate-unvalidated-mutation-marker` verbatim | BC-4.16.002 Invariant 5; TD-VSDD-053 spirit (single source of truth for the marker mechanism) | AC-002; architecture review |
| `validate-factory-path-staging`'s own registration is UNCHANGED — this story adds, does not modify | BC-4.16.002 Invariant 1; AC-003 | PR diff review: zero changes to the existing `validate-factory-path-staging` `[[hook]]` entry |
| Trigger scope is BROAD (unconditional check, no command-text pre-filter) — FINAL, human-ratified 2026-09-04, not provisional | BC-4.16.002 Precondition 2 / Invariant 7 | AC-001, EC-002, EC-007; PR review MUST verify no internal text-based pre-filter was added to the `exec_subprocess` call site |
| Branch-detection failure fails OPEN, mirroring BC-4.16.001 Invariant 3 exactly | BC-4.16.002 Postcondition 4 / Invariant 3 | EC-005 |
| `.factory/` path-matching predicate MUST be the SAME pure function BC-4.16.001 uses, not a re-derived one | BC-4.16.002 Invariant 4 | Code review: import/reuse, not reimplement |
| Option (b) (`on_error` blocking flip on the existing PreToolUse validator) is OUT OF SCOPE for this delivery | ADR-047 §D8a "Why the partition?" self-lock-avoidance rationale | Architecture review; any option-(b) fallback requires its own fresh self-lock-risk re-analysis and POLICY 22 justification |
| ADR-039 §Decision 2 roadmap extension does not gate this story's implementation on S-21.24 | BC-4.16.002 Traceability; ADR-039 §Decision 2 seventh-member entry (F2 §3.3) | AC-004; implementation may proceed independent of S-21.24's own completion |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| wasmtime (workspace) | 46.0.2 (per S-21.12) | WASM runtime for the new companion validator plugin |
| serde / serde_json (workspace) | workspace versions | Plugin payload (de)serialization, matching the sibling crate |
| proptest (workspace, dev-dependency) | "1.6" (matches `validate-factory-path-staging`'s own pin) | Property-based coverage for the reused path-matching predicate (`tests/proptest_*.rs`) |
| vsdd-hook-sdk (workspace path dependency) | workspace version | Hook plugin SDK; same dependency the sibling crate uses |
| toml (workspace) | workspace version | `hooks-registry.toml` entry; marker TOML fields (unchanged, reused from S-25.01) |

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| `crates/hook-plugins/validate-factory-path-staged/` | create | New PostToolUse companion WASM plugin crate (`src/lib.rs`, `src/main.rs`, `src/tests.rs`, `tests/proptest_*.rs`, `Cargo.toml`) |
| `crates/factory-dispatcher/src/indeterminate_marker.rs` | no change (reuse) | Existing marker mechanism from S-25.01 — referenced, not modified |
| `crates/factory-dispatcher/src/executor.rs` | no change (reuse) | Tier-execution loop already generically invokes the marker-write callsite for any PostToolUse fail-closed INDETERMINATE outcome |
| `plugins/vsdd-factory/hooks-registry.toml` | modify | New `validate-factory-path-staged` `[[hooks]]` entry: `priority = 161`, PostToolUse, `failure_policy = "fail-closed"`, `[hooks.capabilities.exec_subprocess]` |
| `.factory/specs/architecture/decisions/ADR-039-validator-failure-policy-resource-exhaustion-fail-closed.md` | modify (architect/state-manager-owned) | §Decision 2 seventh-member roadmap entry — text finalized F2 §3.1; routed, not edited by this story's implementer directly |
| `.factory/specs/architecture/decisions/ADR-047-indeterminate-outcome-model-durable-mutation-marker-next-advance-gate.md` | modify (architect/state-manager-owned) | §Decision 8a clarification-note erratum — text finalized F2 §4; routed, not edited by this story's implementer directly |
| `CHANGELOG.md` | modify | Mandatory CHANGELOG delivery |

## Changelog

| Version | Date | Description |
|---------|------|--------------|
| 2.0 | 2026-09-04 | F1/F2 activation + BC anchoring + promotion to `ready` (story-writer, single-commit TD-VSDD-053). Product-owner's F1 delta analysis corrected the premise: `validate-factory-path-staging` is a narrow git-staging exclusivity guard (BC-4.16.001, INV-E21-001/CAP-034), not an artifact-write-content validator — every "artifact-write side" framing in this story is corrected to the post-hoc git-staging detective-mirror premise. Architect's F2 architecture decisions finalized the companion validator's identity (`validate-factory-path-staged`, `crates/hook-plugins/validate-factory-path-staged/`, priority 161, native-WASM fuel-axis-only, PostToolUse `^Bash$`) and flagged the trigger-scope tradeoff PENDING-HUMAN-RATIFICATION; human ratified BROAD (unconditional, no command-text pre-filter) 2026-09-04. Product-owner then authored BC-4.16.002 v1.0 (new, SS-04/CAP-034) and amended BC-1.18.001 (v1.7, invariant 4 clarification) and BC-1.18.004 (v1.4, postcondition 4 companion count statement). `behavioral_contracts:` set to `[BC-4.16.002, BC-1.18.001, BC-1.18.004]`; body BC table, all six ACs (re-anchored from `BC-TBD` to real BC-S.SS.NNN PC/invariant/EC numbers), and Token Budget updated atomically per POLICY 8. Architecture Mapping / File Structure / Tasks corrected to the finalized crate name and registry priority; all "naming TBD at F2" / PRELIMINARY / provisional markers removed. PENDING-BC-AUTHORSHIP blockquote and BC-status-pending comments removed. `status: draft` -> `status: ready` (Spec-First Gate S-7.01 satisfied). |
| 1.0 | 2026-09-03 | Initial authoring as a PENDING-BC follow-up story (story-writer; single-commit TD-VSDD-053). Source: ADR-047 v1.4 §Decision 8a "Known Gap" note (20bf81dd, human-directed factual correction, PR #807) and BC-1.18.004 v1.2 postcondition 4 (df3219ea). Registers the closure scope for validate-factory-path-staging's ZERO-observable-enforcement gap: three technical options carried forward from the ADR's "Known Gap" note — (a) new PostToolUse companion validator (RECOMMENDED), (b) on_error continue→blocking flip (REJECTED as primary), (c) ADR-039 §Decision 2 roadmap extension (RECOMMENDED companion to (a)). Preliminary ACs (AC-001..AC-006), tasks, architecture anchors, edge cases. Status: draft. BCs pending PO authorship. Preconditions: depends_on [S-25.01] only. Epic anchor: E-25 (Validation Integrity and Large-Artifact Resilience). |
