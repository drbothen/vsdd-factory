---
document_type: story
level: implementation
story_id: S-21.04
epic_id: "E-21"
version: "1.14"
title: "story-worktree factory artifact write-path discipline and teardown preflight"
status: ready
producer: story-writer
timestamp: "2026-07-24T00:00:00Z"
last_amended: "2026-07-25 (v1.14) — pass-10 F-S2104-P10-004/P10-011 Gate-column + Tasks-inventory: (P10-004) AC-002 gate rewritten bats T-001/T-002 (§G.1 doc-parity + harness) + T-005 (EC-007 file-at-path) + T-006 (EC-008 symlink-at-path) + manual-fallback only for prose review; AC-003/004/005 cites T-001/T-002/T-003 added; AC-007 gate T-001/T-008 sub-clause notation: (a)-(c) gated inside T-001, (d) T-008; (P10-011) T-005 authoring task added (pass-4 fix wave 60f0d2d6); T-006 authoring task added (pass-5 93ec340a); T-007/T-008/T-009 propagation-gate authoring task added; input-hash updated. [Prior: 2026-07-25 (v1.13) — pass-9 F-S2104-P9-003/P9-006 fixes: (P9-003) Token Budget BC-6.26.001 pin corrected v1.9→v1.10 — v1.12 last_amended + modified[v1.12] falsely attested 'BC table + Token Budget v1.9→v1.10'; the BC table row was correctly bumped at v1.12 but the Token Budget row was not updated; v1.12 entries preserved unmodified (error-acknowledgment discipline); (P9-006) AC-007 Gate cell: 'bats T-008' cite added (bats T-008: F-S2104-P4-009 propagation-gate test, sibling-parity with T-007/T-009 neighbors); input-hash updated. [Prior: 2026-07-25 (v1.12) — pass-8 T-008/T-009+RG-008/RG-009 registration, AC-009 awareness-surface anchor, BC v1.10 pins. [Prior: 2026-07-25 (v1.11) — RG-006/RG-007 red-gate completeness (T-005/T-007): RG-006 (AC-002/EC-007 regular-file-at-path pre-impl baseline; observed RED 60f0d2d6 → GREEN 73c2bade) + RG-007 (AC-008 devops-engineer preflight-mandate pre-impl baseline; observed RED 60f0d2d6 → GREEN 0c0922e1; retro-dated pass-4; AC-008 retro-mapped v1.9) appended to §Red Gate Test Plan (POLICY 1 append-only); AC-002 Gate updated with RG-005+RG-006 non-directory PC2b baseline cites; AC-008 Gate updated with RG-007 cite; input-hash updated. [Prior: 2026-07-25 (v1.10) — BC v1.9 pin sync (count-free PC2 case labels; no semantic delta): BC table + Token Budget v1.8→v1.9; input-hash updated. [Prior: 2026-07-25 (v1.9) — pass-6 F-S2104-P6-009 test-inventory sync + BC v1.8 Precondition-3 propagation (story-writer): F-009(a) §Architecture Mapping + §File Structure Requirements bats row: T-001..T-004 covering AC-003..AC-006 → T-001..T-006 covering AC-002..AC-006 (+EC-007/EC-008 vectors) plus 3 propagation-gate tests (AC-007/AC-008 surfaces). F-009(b) AC-007 Gate: manual-grep → bats gate cite (F-S2104-P4-009 propagation-gate suite; manual-fallback retained). F-009(c) AC-008 Gate: T-007 cite added (F-S2104-P4-003 devops-engineer §Worktree Cleanup doc-parity gate, bats test 9); T-007 row appended §Test Plan (POLICY 1 append-only; maps to AC-008). BC v1.8 sync: BC table + Token Budget v1.7→v1.8; AC-008 BC trace Precondition 3 added; other cites versionless per class-death convention. [Prior: 2026-07-25 (v1.8) — pass-5 legs F-S2104-P5-004/F-P5-008 + BC v1.7 propagation (story-writer): F-004 §Narrative teardown-preflight paragraph rewritten to BC v1.7 4-step discrimination chain (existence pre-test first; `[ -L ]` symlink guard before `[ -d ]`; trailing-slash find for real dirs; proceed only on PASS; retired phrases: find-first ordering, non-directory/symlink branch absent, 'Only proceed with teardown after an empty result'). F-008 AC-008 added (executor-side defensive obligation — before executing `git worktree remove` on a story worktree the executing agent MUST verify a PASS §G.1 preflight; BC-6.26.001 Invariant 2 + PC2; bats gate); §File Structure Requirements devops-engineer.md row updated with AC-008 cite. BC v1.7 propagation: BC table + Token Budget v1.6→v1.7; AC-002(a) `[ -L ]` symlink step added + trailing-slash find form; EC-008 added (symlink-at-path any target → PC2b BLOCKED; mirrors BC-6.26.001 EC-008); T-006 added (symlink-to-dir → PC2b BLOCKED; find NOT invoked; remove NOT called; BC T-7); RG-005 added (symlink guard absent → RED). TD-VSDD-091 v1.6 prose sweep: 2 live pins normalized to versionless stable cites. [Prior: 2026-07-25 (v1.7) — pass-4 story legs F-S2104-P4-003/F-004/F-008 + BC v1.6 EC-008 propagation (story-writer): F-008 §File Structure Requirements: worktree-identity-preflight.bats 'modify' row added (test (e) re-anchored from stale-snapshot to corrected-model positive anchors; pass-3 F-007; BC-6.26.001 Invariant 5). F-003(a) AC-007(d): category broadened to 'skills, workflows, rules files, and agent files'. F-003(b) §File Structure Requirements: devops-engineer.md 'modify' row added (executor-side defensive preflight mandate; ADR-031 caller-side primary ruling); step-d5-adversary-convergence.md 'modify' row added (retract stale-snapshot; corrected live-shadow model). BC v1.6 propagation: BC table + Token Budget + narrative cites v1.5→v1.6; AC-002(a) existence-predicate semantics (`[ ! -e ]` + non-directory→PC2b BLOCKED without find); EC-007 added (regular file at .factory/ path → PC2b BLOCKED; mirrors BC EC-008); T-005 added (bats file-at-path vector; BC EC-008/T-6); TD-VSDD-060 v1.5 cite sweep complete. [Prior: 2026-07-24 (v1.6) — pass-3 F-P3-013/F-P3-016/F-P3-017 story legs (story-writer): F-013 §File Structure Requirements: adversary.md + adversarial-review/SKILL.md 'modify' rows added (retract stale-snapshot premise; live shadow-write evidence per issue #523; BC-6.26.001 Invariant 5). F-016 AC-007(d): 'sibling teardown skills and workflows' broadened to 'sibling teardown surfaces (skills, workflows, and rules files)'. F-017 Token Budget ADR-031 v1.3 pin removed; TD-VSDD-060 ADR-031 sweep complete (one active version pin normalized; historical records in last_amended/modified untouched). [Prior: 2026-07-24 (v1.5) — pass-2 F-P2-007 five-table AC-006 propagation + file-structure completeness + AC-007 + BC-6.26.001 v1.4→v1.5 cites (story-writer): T-004 (AC-006 bats) added to Test Plan; RG-004 added to Red Gate Test Plan; bats suite range AC-003..AC-006 in Architecture Mapping; 10 delivered-but-unlisted files added to File Structure Requirements; bats row T-001..T-004; AC-006 Gate manual→bats T-004; AC-007 propagation AC added; Tasks 5+9 (AC-006 test-writer + propagation sweep) inserted; BC-6.26.001 v1.4→v1.5 (BC table + Token Budget + narrative cites); input-hash 510a89a→be1fd41. [Prior: 2026-07-24 (v1.4) — S-21.04 adv pass-1 story legs F-007/F-010/F-012/F-006 + BC v1.4 PC2c propagation (story-writer): F-007 Narrative shadow-directory provenance corrected (git worktree add checks out nothing for gitignored .factory/; shadow created by errant write). F-010 AC-002(c) --force rationale corrected (BC mandate not guard). F-006 AC-002(a) 2>/dev/null removed; PC2c fail-closed added; AC-006 added (BC-6.26.001 PC2c); EC-005 updated; Architecture Mapping updated. F-012 CHANGELOG task added. BC-6.26.001 v1.3→v1.4 (BC table + Token Budget + narrative cites). [Prior: 2026-07-25 (v1.3) — D-894-E21-W2-STATUS-PROMOTION-2026-07-24 (state-manager): status draft→ready; W2 dispatch authorized D-862+D-893. [Prior: 2026-07-19 (v1.2) — adv pass-4 fix burst (O-4): Token Budget ADR-031 v1.0→v1.3 (consistent version form across all E-21 stories per pass-4 O-4 sweep). [Prior: 2026-07-19 (v1.1) — adv pass-3 fix burst (F-P3-001): retract '--force is the proximate cause' framing; adopt gitignored-shadow false-negative mechanism per BC-6.26.001 v1.3 Invariant 5; AC-002(c) corrected to plain git worktree remove (no --force per BC v1.3 PC2a); RG-001 --force occurrence corrected; secondary --force note added; BC-6.26.001 → v1.3 (BC table + Token Budget); TD-VSDD-060 full --force sweep.]]]]]]]]]]"
phase: brownfield-backfill
cycle: v1.0-brownfield-backfill
inputs:
  - .factory/cycles/v1.0-brownfield-backfill/e-21-arch-delta-analysis.md
  - .factory/specs/behavioral-contracts/ss-06/BC-6.26.001.md
  - .factory/specs/architecture/decisions/ADR-031-e21-factory-state-data-loss-hardening.md
  - plugins/vsdd-factory/skills/deliver-story/steps/_shared-context.md
  - plugins/vsdd-factory/skills/deliver-story/steps/step-g-cleanup.md
input-hash: "1165b1f"
traces_to:
  - .factory/stories/epics/E-21-factory-state-data-loss-hardening.md
wave: 2
priority: P1
points: 5
track: feature
effort_class: single-day
depends_on: []
blocks: []
behavioral_contracts: [BC-6.26.001]
verification_properties: []
domain_invariants: []
tdd_mode: strict
target_module: "plugins/vsdd-factory/skills/deliver-story/steps/_shared-context.md"
subsystems:
  - "SS-06"
estimated_days: 1.5
assumption_validations: []
risk_mitigations: []
anchored_adrs: [ADR-031]
closes:
  - "INV-E21-002 (issue #523): all .factory/** writes within deliver-story must use canonical absolute paths anchored to main-checkout root"
  - "INV-E21-004 (issue #523): git worktree remove on story worktree must be preceded by .factory/ inventory preflight"
feature: "e21-factory-state-hardening"
modified:
  - "2026-07-25 v1.14: pass-10 F-S2104-P10-004/011 — Gate-column completion (AC-002 bats T-ID rewrite; AC-003/004/005 T-001/T-002/T-003 cites; AC-007 T-001/T-008 sub-clause notation) + Tasks inventory (T-005/T-006 authoring rows + T-007/T-008/T-009 propagation-gate row); input-hash updated"
  - "2026-07-25 v1.13: pass-9 F-S2104-P9-003/P9-006 — Token Budget BC-6.26.001 pin corrected v1.9→v1.10 (v1.12 attestation error: modified[v1.12] + last_amended falsely attested 'BC table + Token Budget v1.9→v1.10'; BC table was correct but Token Budget row was not updated; v1.12 entries preserved unmodified); AC-007 Gate cell 'bats T-008' cite added (F-S2104-P4-009 propagation-gate test, sibling T-007/T-009 parity); input-hash updated"
  - "2026-07-25 v1.12: pass-8 T-008/T-009+RG-008/RG-009 registration + AC-009 awareness-surface anchor + BC v1.10 pin sync — T-008/T-009 appended §Test Plan (POLICY 1 append-only; T-008→AC-007, T-009→AC-009); RG-008/RG-009 appended §Red Gate Test Plan (POLICY 1 append-only); AC-009 added §Acceptance Criteria; adversary.md + adversarial-review/SKILL.md §File Structure rows updated with AC-009 cite; bats row '3 propagation-gate tests' → '(T-007/T-008/T-009)' in §Architecture Mapping + §File Structure; BC table + Token Budget v1.9→v1.10; input-hash updated"
  - "2026-07-25 v1.11: RG-006/RG-007 red-gate completeness (T-005/T-007) — RG-006 (AC-002/EC-007 regular-file-at-path baseline; RED 60f0d2d6→GREEN 73c2bade) + RG-007 (AC-008 devops-engineer preflight-mandate baseline; RED 60f0d2d6→GREEN 0c0922e1; retro pass-4; AC-008 retro-mapped v1.9) appended §Red Gate Test Plan (POLICY 1 append-only); AC-002 + AC-008 Gate cells updated with RG cites; input-hash updated"
  - "2026-07-25 v1.10: BC v1.9 pin sync (count-free PC2 case labels; no semantic delta) — BC table + Token Budget v1.8→v1.9; input-hash updated"
  - "2026-07-25 v1.9: pass-6 F-S2104-P6-009 test-inventory sync + BC v1.8 Precondition-3 propagation — §Architecture Mapping + §File Structure Requirements bats row: T-001..T-006 (+EC-007/EC-008 vectors) + 3 propagation-gate tests (AC-007/AC-008 surfaces); AC-007 Gate: bats gate cite (F-S2104-P4-009; manual-fallback retained); AC-008 Gate: T-007 cite (F-S2104-P4-003 devops-engineer §Worktree Cleanup doc-parity, bats test 9); T-007 row added §Test Plan (POLICY 1 append-only; AC-008); BC table + Token Budget v1.7→v1.8; AC-008 BC trace: Precondition 3 added; input-hash updated"
  - "2026-07-25 v1.8: pass-5 legs F-S2104-P5-004/F-P5-008 + BC v1.7 propagation — Narrative teardown-preflight paragraph rewritten to 4-step discrimination chain (existence pre-test → [ -L ] symlink guard → trailing-slash find → proceed-on-PASS); AC-002(a) [ -L ] symlink step + trailing-slash find; AC-006 trailing-slash find; AC-008 added (executor-side defensive obligation; BC-6.26.001 Invariant 2 + PC2); File Structure devops-engineer.md row updated (AC-008 cite); BC table + Token Budget v1.6→v1.7; EC-008/T-006/RG-005 added (symlink-at-path→PC2b; BC T-7); TD-VSDD-091 prose sweep: 2 live v1.6 pins normalized"
  - "2026-07-25 v1.7: pass-4 story legs F-S2104-P4-003/F-004/F-008 + BC v1.6 EC-008 propagation — §File Structure Requirements: worktree-identity-preflight.bats modify row (test (e) re-anchoring; pass-3 F-007; BC-6.26.001 Invariant 5); devops-engineer.md modify row (executor-side defensive preflight; ADR-031 caller-side primary); step-d5-adversary-convergence.md modify row (retract stale-snapshot); AC-007(d) broadened to 'skills, workflows, rules files, and agent files'; AC-002(a) existence-predicate; EC-007/T-005 added (BC-6.26.001 EC-008/T-6); BC cites v1.5→v1.6; TD-VSDD-060 sweep"
  - "2026-07-24 v1.6: pass-3 F-P3-013/F-P3-016/F-P3-017 story legs — §File Structure Requirements: adversary.md + adversarial-review/SKILL.md 'modify' rows added (retract stale-snapshot premise; live shadow-write evidence; BC-6.26.001 Invariant 5); AC-007(d) category broadened to skills/workflows/rules files; Token Budget ADR-031 v1.3 pin normalized to stable form; TD-VSDD-060 ADR-031 sweep complete"
  - "2026-07-24 v1.5: pass-2 F-P2-007 five-table AC-006 propagation + file-structure completeness + AC-007 added + BC-6.26.001 v1.4→v1.5 cites; T-004/RG-004 added; 10 file-structure rows added; bats row T-001..T-004; AC-006 Gate manual→bats; Tasks 5+9 inserted; input-hash 510a89a→be1fd41"
  - "2026-07-24 v1.4: adv pass-1 story legs F-007/F-010/F-012/F-006 + BC v1.4 PC2c propagation — shadow-directory provenance corrected; AC-002(c) --force rationale corrected (BC mandate); AC-002(a) 2>/dev/null removed; PC2c fail-closed + AC-006 added; EC-005 updated; CHANGELOG task added; BC cites v1.3→v1.4; input-hash updated 709f937→510a89a"
  - "2026-07-25 v1.3: D-894 status-promotion draft→ready (W2 dispatch authorization D-862+D-893)"
  - "2026-07-19 v1.0: Initial authoring"
  - "2026-07-19 v1.1: adv pass-3 fix burst (F-P3-001) — gitignored-shadow false-negative mechanism adopted; --force false-premise retracted; AC-002(c) corrected to plain git worktree remove; RG-001 corrected; BC-6.26.001 → v1.3 (BC table + Token Budget); TD-VSDD-060 full --force sweep."
  - "2026-07-19 v1.2: adv pass-4 fix burst (O-4) — Token Budget ADR-031 v1.0→v1.3 (consistent version form)"
---

# S-21.04 — story-worktree factory artifact write-path discipline and teardown preflight

**Epic:** E-21 — Factory State Data-Loss Hardening
**Wave:** 2 (independent of Wave 1 stories; benefits from S-21.01 `validate-factory-path-staging` WASM guard being active as defense-in-depth, but is independently viable without it — no formal `depends_on` block)
**Depends on:** none (Wave 2 placement reflects shared-mutable-worktree theme adjacency with S-21.05; no hard prerequisite in E-21)
**Priority:** P1 (confirmed-live pilot-fleet data-loss with three confirmed instances; story-worktree teardown silently destroys factory artifacts written via CWD-relative paths)
**Subsystem anchor:** SS-06 (Skill Catalog) owns the `deliver-story` skill steps.

## Narrative

Issue #523 confirms three pilot-fleet instances where factory artifacts (DELIVERY ledger, engine-config file, pr-reviewer pr-review.md) were silently destroyed because story agents wrote to `.factory/**` using CWD-relative paths from the story-worktree root rather than canonical absolute paths. The story-worktree's shadow `.factory/` directory does not exist at checkout time — `.factory/` is gitignored on the product branch, so `git worktree add` checks out nothing there. The shadow directory is created by the errant CWD-relative write itself, which is why the misdirected write goes undetected at write time. When step G then ran plain `git worktree remove`, the shadow tree and all its contents were permanently deleted with no warning.

The **gitignored-shadow false-negative** mechanism is the proximate cause (BC-6.26.001 Invariant 5): `.factory/` is listed in `.gitignore` on the product branch, so the shadow `.factory/` content inside the story worktree is **gitignored** (not untracked). Stock git's clean-state check inside `git worktree remove` gates on *untracked* files only — gitignored files are explicitly excluded. The check therefore passes silently as a false negative even when the shadow tree contains stray factory artifacts, and the underlying `rm -rf <worktree-path>` destroys the gitignored shadow content with no warning. The `find`-based preflight is the correct fix because `find` reads the filesystem without gitignore filtering — it is the only mechanism that surfaces this category of stray content before destruction.

**Secondary note:** `--force` is prohibited by BC-6.26.001 as a BC mandate (it strips git's built-in unclean-worktree protection for non-gitignored untracked files; the prohibition is a BC mandate, not a guard-enforced constraint). If `--force` were used despite the prohibition, the preflight would still catch `.factory/` shadow content. The primary failure mode for the current codebase is the gitignored mechanism above; step-g-cleanup.md G.1 MUST use plain `git worktree remove` (no `--force`). BC-6.26.001 establishes the protocol requirements that close this loss window.

Two complementary changes close the loss window:

**Write-path discipline (INV-E21-002 via BC-6.26.001 PC1):** Extend `_shared-context.md` §Spec-Path Discipline to explicitly cover factory artifact *writes*. Add a "Write Discipline" clause: all `.factory/**` artifact writes (DELIVERY ledger, story-frontmatter files, pr-review.md, and any factory-side record) MUST use absolute paths anchored to the canonical main-checkout root. Canonical root is determined via `git -C <story-worktree-path> rev-parse --show-toplevel` on the main worktree, or from the orchestrator-provided `CANONICAL_FACTORY_ROOT` variable. CWD-relative paths are forbidden.

**Teardown preflight (INV-E21-004 via BC-6.26.001 PC2):** Add a mandatory sub-step to `step-g-cleanup.md` before every `git worktree remove` dispatch. The preflight applies a fail-closed four-step discrimination chain: (1) if nothing exists at `<worktree-path>/.factory` (`[ ! -e ]` is true) → PC2a sub-case (a), teardown authorized immediately; (2) if a symlink exists at that path (`[ -L ]`) OR if something exists but is NOT a real directory (regular file, device node, or any other non-directory inode) → PC2b BLOCKED — list the path, do NOT run `find`, do NOT proceed with `git worktree remove`; the `[ -L ]` test MUST precede any `[ -d ]` test because POSIX `test -d` follows symlinks and a symlink-to-directory satisfies `[ -d ]`, which would otherwise fall through to the `find` branch where POSIX `find` without `-H`/`-L` returns empty output (non-descent), falsely authorizing teardown and allowing `rm -rf` to destroy the symlink target; (3) if `.factory/` exists and IS a real directory (not a symlink), run `find "<worktree-path>/.factory/" -type f` (trailing-slash form; no blanket error suppression) — if `find` exits non-zero for any reason, HALT teardown and surface the exit code and stderr (PC2c fail-closed); (4) proceed with plain `git worktree remove` (no `--force`) only when `find` succeeds with empty output.

This story is entirely skill-doc. POLICY 21 satisfied (no new WASM or shell script).

## Acceptance Criteria

| ID | Criterion | Gate | BC Trace |
|----|-----------|------|----------|
| AC-001 | `plugins/vsdd-factory/skills/deliver-story/steps/_shared-context.md` §Spec-Path Discipline section contains an explicit "Write Discipline" clause covering `.factory/**` writes. The clause MUST: (a) forbid relative paths (`".factory/..."` from story-worktree CWD); (b) mandate canonical absolute paths via `git -C <worktree> rev-parse --show-toplevel` on the MAIN worktree or `CANONICAL_FACTORY_ROOT`; (c) name the DELIVERY ledger (`*-DELIVERY.md`) and `pr-review.md` as load-bearing cases. | manual: confirm Write Discipline clause present in `_shared-context.md`; grep for `CANONICAL_FACTORY_ROOT` and `DELIVERY` in the clause. | BC-6.26.001 PC1, Invariant 1 |
| AC-002 | `plugins/vsdd-factory/skills/deliver-story/steps/step-g-cleanup.md` contains a mandatory teardown preflight sub-step before any `git worktree remove` dispatch. The preflight: (a) first checks path existence — if nothing exists at `<worktree-path>/.factory` (`[ ! -e "<worktree-path>/.factory" ]` is true) → PC2a sub-case (a), teardown authorized immediately; next checks for a symlink (`[ -L "<worktree-path>/.factory" ]`) before any directory test: any symlink at that path (symlink-to-file, symlink-to-directory, or dangling) → PC2b BLOCKED immediately (list the path; do NOT run `find`; do NOT proceed with `git worktree remove`); the `[ -L ]` test MUST precede any `[ -d ]` test because POSIX `test -d` follows symlinks and a symlink-to-directory satisfies `[ -d ]`, causing it to fall through to `find`; POSIX `find` without `-H`/`-L` does not descend symlinks and returns empty output, which would falsely authorize teardown; if something exists but is NOT a symlink and NOT a real directory (regular file, device node) → PC2b BLOCKED; (b) if `.factory/` exists and IS a real directory (confirmed: not a symlink, existence verified), runs `find "<worktree-path>/.factory/" -type f` (trailing-slash form; without blanket error suppression); if the result is non-empty, logs each stray file path with `PREFLIGHT BLOCKED` message and halts (does NOT invoke `git worktree remove`); (c) proceeds with plain `git worktree remove` (no `--force`; `--force` is prohibited by BC-6.26.001 as a BC mandate because it strips git's built-in unclean-worktree protection for non-gitignored untracked files) only when `find` succeeds with empty output. PC2c error handling (non-path-absent `find` errors → HALT) is specified in AC-006. | bats: T-001/T-002 (§G.1 doc-parity + harness), T-005 (EC-007 file-at-path), T-006 (EC-008 symlink-at-path); manual-fallback only for prose review | BC-6.26.001 PC2, Invariant 2 |
| AC-003 | A bats integration test creates a stray `.factory/` file inside a story-worktree fixture, then invokes the teardown preflight step. The test asserts: (a) the preflight exits non-zero and reports the stray file path; (b) `git worktree remove` is NOT invoked. | bats T-001: create fixture worktree with stray `.factory/stories/S-021-DELIVERY.md` file; invoke teardown; assert exit non-zero + `PREFLIGHT BLOCKED` message + `git worktree remove` NOT called. | BC-6.26.001 PC2b, Invariant 2 |
| AC-004 | A bats integration test confirms that when the story-worktree shadow `.factory/` directory is empty (or absent), teardown proceeds normally. | bats T-002: fixture worktree with no `.factory/` content; invoke teardown; assert exit 0 + `git worktree remove` IS called. | BC-6.26.001 PC2a |
| AC-005 | A bats integration test confirms that after stray files are relocated to the canonical `.factory/` mount (making the shadow tree empty), retry teardown proceeds normally. | bats T-003: fixture with stray file; relocate file to canonical mount; re-run preflight; assert teardown proceeds. | BC-6.26.001 PC2b → PC2a retry path |
| AC-006 | `step-g-cleanup.md` teardown preflight is fail-closed on `find` errors: if `find "<worktree-path>/.factory/" -type f` exits non-zero for a reason other than the `.factory/` directory being absent (e.g., permission denial, path error), step G MUST HALT teardown and surface the exact `find` exit code and stderr output to the operator; `git worktree remove` is NOT executed. | bats: T-004 — non-path-absent `find` error → PC2c HALT; exit code+stderr surfaced; `git worktree remove` NOT called. | BC-6.26.001 PC2c |
| AC-007 | Every caller-side teardown dispatch path that invokes `git worktree remove` on a story worktree MUST mandate the §G.1 preflight sub-step. Specifically: (a) `plugins/vsdd-factory/skills/deliver-story/SKILL.md` Step 8 MUST reference the mandatory §G.1 preflight as a precondition before teardown dispatch; (b) `plugins/vsdd-factory/agents/orchestrator/per-story-delivery.md` step (g) AND Story Split Recovery teardown path MUST mandate the §G.1 preflight; (c) `plugins/vsdd-factory/workflows/phases/per-story-delivery.md` Step 8 (the precedence-winning playbook per §Architectural Authority) MUST mandate the §G.1 preflight; (d) sibling teardown surfaces (skills, workflows, rules files, and agent files) listed in §File Structure Requirements that invoke `git worktree remove` MUST mandate the preflight before dispatch. (Note: adversary.md and adversarial-review/SKILL.md are awareness-only surfaces that do not invoke `git worktree remove`; their awareness-clause obligations are covered by AC-009.) | bats T-001/T-008: sub-clauses (a)–(c) primary-surface §G.1 mandate gated inside T-001; T-008 (F-S2104-P4-009 propagation-gate, `story-worktree-write-path-discipline.bats`) covers (d)'s six sibling surfaces; manual-fallback: grep for §G.1 preflight mandate on any caller-side teardown path not covered by bats suite. | BC-6.26.001 PC2, Invariant 2 |
| AC-008 | Before executing `git worktree remove` on a story worktree, the devops-engineer agent MUST verify that a PASS §G.1 preflight result is available — running the full §G.1 discrimination chain itself when not evident from the dispatch context. This is an executor-side defensive obligation that complements ADR-031's caller-side primary gate: even when the dispatch preamble does not explicitly confirm §G.1 completion, the executing agent MUST NOT proceed with `git worktree remove` without first confirming a PASS preflight result. | bats T-007: F-S2104-P4-003 devops-engineer §Worktree Cleanup doc-parity gate (`story-worktree-write-path-discipline.bats` test 9) — confirm devops-engineer.md §Worktree Cleanup mandates §G.1 preflight; teardown gated on preflight result regardless of dispatch preamble state; RG-007 documents pre-implementation RED baseline (pass-4 gate-8; retro-mapped to AC-008 at v1.9). | BC-6.26.001 Invariant 2, PC2, Precondition 3 |
| AC-009 | Awareness surfaces: `plugins/vsdd-factory/agents/adversary.md` and `plugins/vsdd-factory/skills/adversarial-review/SKILL.md` MUST state the corrected shadow-write model (no `.factory/` content exists at worktree-add; any worktree `.factory/` content is live shadow-write evidence to be REPORTED as a defect signal) and reference the `step-g-cleanup.md` §G.1 teardown preflight as the enforcement chain that catches this class. | bats T-009. | BC-6.26.001 Invariant 5 |

## Architecture Mapping

This story amends two skill-doc files and adds one bats suite:

- `plugins/vsdd-factory/skills/deliver-story/steps/_shared-context.md` — extend §Spec-Path Discipline with explicit "Write Discipline" clause covering `.factory/**` writes; name DELIVERY ledger and pr-review.md as load-bearing cases; mandate `CANONICAL_FACTORY_ROOT` or `git rev-parse --show-toplevel` resolution (BC-6.26.001 PC1 + Invariants 1, 3, 4)
- `plugins/vsdd-factory/skills/deliver-story/steps/step-g-cleanup.md` — add mandatory teardown preflight sub-step (4-step discrimination chain: `[ ! -e ]` existence pre-test → `[ -L ]` symlink guard before any `[ -d ]` test → `find "<worktree>/.factory/" -type f` trailing-slash form for real directories) before `git worktree remove` dispatch; PC2a/PC2b/PC2c logic with `PREFLIGHT BLOCKED` message (PC2b; covers both find-returns-files and symlink/non-directory-at-path cases) and fail-closed HALT (PC2c) (BC-6.26.001 PC2 + Invariant 2)
- `plugins/vsdd-factory/tests/story-worktree-write-path-discipline.bats` — T-001..T-006 covering AC-002..AC-006 (+EC-007/EC-008 vectors) plus 3 propagation-gate tests (T-007/T-008/T-009)

**ADR-031** §Decision 4 governs: INV-E21-002 (write-path anchoring) + INV-E21-004 (teardown preflight) enforced via skill-doc. No new WASM or shell script.

## Behavioral Contracts

| BC ID | Title | Version | Story ACs |
|-------|-------|---------|-----------|
| BC-6.26.001 | deliver-story step agents MUST write all `.factory/**` artifacts using absolute paths anchored to the canonical main-checkout `.factory/` mount, and step-G cleanup MUST run a worktree `.factory/` inventory preflight before `git worktree remove` | v1.10 | AC-001 (PC1, Invariant 1), AC-002 (PC2a/PC2b/symlink, Invariant 2), AC-003 (PC2b, Invariant 2), AC-004 (PC2a), AC-005 (PC2b→PC2a retry), AC-006 (PC2c), AC-007 (propagation to all caller-side teardown paths), AC-008 (executor-side defensive obligation, Precondition 3), AC-009 (awareness surfaces, Invariant 5) |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | Agent writes `Write(".factory/stories/S-021-DELIVERY.md")` with CWD = `.worktrees/S-021/` | FORBIDDEN: relative path resolves to shadow tree; violates PC1 (caught by adversarial review) |
| EC-002 | Agent writes `Write("/abs/repo/.factory/stories/S-021-DELIVERY.md")` | CORRECT: absolute path to canonical root; PC1 compliant |
| EC-003 | `find .worktrees/S-021/.factory -type f` returns empty | Teardown proceeds (AC-004; PC2a) |
| EC-004 | `find .worktrees/S-021/.factory -type f` returns stray file | BLOCKED: AC-003 (PC2b); relocate or discard then retry |
| EC-005 | Story worktree has no `.factory/` directory at all (clean worktree) | `.factory/` absent → PC2a sub-case (a): no stray files, teardown authorized. Path-absent is not a PC2c error; it is the expected clean state. Distinguished from PC2c (non-path-absent `find` failures). |
| EC-006 | `git -C <worktree> rev-parse --show-toplevel` returns story-worktree root | Agent MUST use the main worktree root, NOT the story worktree root — canonical root is determined from the MAIN worktree, not the story worktree CWD |
| EC-007 | Story worktree has `.factory` as a regular file (not a directory) at the worktree root | Non-directory inode at path → PC2b BLOCKED: file is stray shadow content subject to `rm -rf` destruction; list the path; do NOT run `find`; do NOT proceed with `git worktree remove`. `[ ! -d ".factory" ]` would be true (wrong: authorizes teardown); `[ ! -e ".factory" ]` correctly identifies the path as occupied (mirrors BC-6.26.001 EC-008). |
| EC-008 | Story worktree has `.factory` as any symlink (symlink-to-file, symlink-to-directory, or dangling) at the worktree root | Symlink at path → PC2b BLOCKED: `[ -L "<worktree-path>/.factory" ]` is true → step 2 routes to PC2b immediately; `find` NOT invoked; `git worktree remove` NOT called. Critical new case: symlink-to-directory — without the `[ -L ]` guard, POSIX `test -d` follows the symlink (satisfies `[ -d ]`) and the `find` branch would execute; POSIX `find` without `-H`/`-L` does not descend symlinks and returns empty output, falsely authorizing teardown and allowing `rm -rf` to destroy the symlink target. (Mirrors BC-6.26.001 EC-008.) |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| `plugins/vsdd-factory/skills/deliver-story/steps/_shared-context.md` | N/A (skill-doc) | Skill step instruction document |
| `plugins/vsdd-factory/skills/deliver-story/steps/step-g-cleanup.md` | N/A (skill-doc) | Skill step instruction document |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~3,500 |
| BC-6.26.001 v1.10 | ~3,500 |
| ADR-031 (§Decision 4) | ~1,500 |
| e-21-arch-delta-analysis.md (issue #523 section) | ~2,000 |
| `plugins/vsdd-factory/skills/deliver-story/steps/_shared-context.md` | ~2,000 |
| `plugins/vsdd-factory/skills/deliver-story/steps/step-g-cleanup.md` | ~1,500 |
| Test fixtures (bats suite with worktree fixture) | ~2,500 |
| Tool outputs overhead | ~1,000 |
| **Total** | **~17,500** |
| Agent context window | 200K (Sonnet) |
| **Budget usage** | **~9%** |

## Tasks (MANDATORY)

1. [ ] Capture `cargo test --workspace --all-targets` baseline pass count pre-implementation; emit `pre-implementation cargo-test baseline: <N> pass` marker.
2. [ ] Write failing bats test for AC-003 (stray `.factory/` file triggers `PREFLIGHT BLOCKED`; `git worktree remove` NOT called) — test-writer
3. [ ] Write failing bats test for AC-004 (empty shadow tree → teardown proceeds) — test-writer
4. [ ] Write failing bats test for AC-005 (stray file relocated → retry teardown proceeds) — test-writer
5. [ ] Write failing bats test for AC-006 (T-004: non-path-absent `find` error → PC2c HALT; exit code+stderr surfaced; `git worktree remove` NOT called) — test-writer
6. [ ] Verify Red Gate: all tests fail (preflight not yet in step-g-cleanup.md)
7. [ ] Amend `plugins/vsdd-factory/skills/deliver-story/steps/_shared-context.md`: add "Write Discipline" clause under §Spec-Path Discipline covering `.factory/**` writes; name DELIVERY ledger + pr-review.md; mandate canonical absolute path resolution via `CANONICAL_FACTORY_ROOT` or `git -C <main-worktree> rev-parse --show-toplevel` (BC-6.26.001 PC1 + Invariants 1, 3, 4) — implementer
8. [ ] Amend `plugins/vsdd-factory/skills/deliver-story/steps/step-g-cleanup.md`: add mandatory teardown preflight sub-step before `git worktree remove` dispatch; implement PC2a/PC2b/PC2c logic with `PREFLIGHT BLOCKED` message (PC2b) and fail-closed HALT (PC2c) (BC-6.26.001 PC2 + Invariant 2 + Invariant 5) — implementer
9. [ ] Propagation sweep: mandate §G.1 teardown preflight on every caller-side teardown dispatch path per AC-007 — update `plugins/vsdd-factory/skills/deliver-story/SKILL.md` Step 8, both `per-story-delivery.md` playbooks (agents/orchestrator + workflows/phases), and sibling teardown skills/workflows listed in §File Structure Requirements (BC-6.26.001 PC2 + Invariant 2) — implementer
10. [ ] Verify all tests pass; confirm bats suite green
11. [ ] Add a CHANGELOG entry under [Unreleased] > Fixed/Changed/Added describing the shipped behavior, before creating the PR
12. [x] Write failing bats test for T-005 (AC-002/EC-007: regular file at `<worktree>/.factory` path → PC2b BLOCKED; `find` NOT invoked; `git worktree remove` NOT called) — test-writer (authored pass-4 fix wave, commit 60f0d2d6)
13. [x] Write failing bats test for T-006 (AC-002/EC-008: symlink-to-directory at `<worktree>/.factory` path → PC2b BLOCKED; `find` NOT invoked; `git worktree remove` NOT called) — test-writer (authored pass-5, commit 93ec340a)
14. [x] Write failing bats tests for propagation-gate T-007 (AC-008: devops-engineer §Worktree Cleanup doc-parity), T-008 (AC-007: 6-surface §G.1 mandate regression), and T-009 (AC-009: awareness-clause doc-parity) — test-writer (authored pass-4 fix wave, commit 60f0d2d6)

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-21.01 (same epic W1) | `CANONICAL_FACTORY_ROOT` concept introduced in context of guard analysis | Agents should use orchestrator-provided canonical root; not independently resolve from CWD | `git -C <story-worktree> rev-parse --show-toplevel` returns the story worktree root, NOT the main checkout root — must use the MAIN worktree path or the pre-provided absolute |
| E-18 S-18.01 (HANDOFF.md) | wave-handoff skill write-path discipline established for HANDOFF.md | Write-tool calls in skill steps use absolute paths; similar discipline | HANDOFF.md write-path fix (S-18.13) is a direct precedent for this story's write-discipline extension |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| `_shared-context.md` Write Discipline clause must explicitly name DELIVERY ledger and pr-review.md as load-bearing cases | BC-6.26.001 Invariant 4 | AC-001 checks clause content |
| Teardown preflight is mandatory — not optional even when agent is "confident" no shadow writes occurred | BC-6.26.001 Invariant 2 | AC-002 checks step-g-cleanup.md; AC-003 confirms halt on stray files |
| `git -C <story-worktree> rev-parse --show-toplevel` returns the story worktree root, not the main checkout root | BC-6.26.001 Invariant 3 | Clause explicitly states: use main worktree path, not CWD |
| No new `.sh` files | POLICY 21, ADR-031 §Decision 4 | Implementer adds skill-doc steps only |
| No AI attribution in commits | CLAUDE.md Git Workflow | No `Co-Authored-By: Claude` in any commit |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| find (POSIX) | system | Teardown preflight — 4-step discrimination chain: `[ ! -e ]` existence pre-test; `[ -L ]` symlink guard (MUST precede any `[ -d ]` test; catches symlink-to-directory which POSIX `test -d` follows); `find "<worktree>/.factory/" -type f` (trailing-slash form; no blanket error suppression) for real directories only; non-path-absent `find` error → PC2c HALT |
| git | current | `git -C <path> rev-parse --show-toplevel` for canonical root resolution |
| Bats | per `plugins/vsdd-factory/tests/run-all.sh` | Integration test runner |

## Red Gate Test Plan

| Test ID | Description | Expected Outcome (pre-implementation = RED) |
|---------|-------------|---------------------------------------------|
| RG-001 | Create stray `.factory/` file in fixture worktree; invoke teardown → should BLOCK | FAIL: no preflight in step-g-cleanup.md; plain `git worktree remove` proceeds (gitignored shadow content passes clean-state check as false negative) and the underlying `rm -rf` destroys the file |
| RG-002 | Empty shadow `.factory/`; invoke teardown → should PROCEED | FAIL: preflight not present (trivially passes but test verifies preflight is invoked) |
| RG-003 | Stray file relocated; retry teardown → should PROCEED | FAIL: preflight not present |
| RG-004 | Force `find "<worktree>/.factory/" -type f` to return a non-path-absent exit code (e.g., permission denied on `.factory/`); invoke teardown → should HALT with `find` exit code+stderr surfaced | FAIL: PC2c HALT block absent from step-g-cleanup.md §G.1; teardown continues or exits silently without surfacing `find` exit code+stderr; `git worktree remove` may be invoked |
| RG-005 | Create symlink-to-directory at `<worktree>/.factory`; invoke teardown → should BLOCK at PC2b | FAIL: `[ -L ]` symlink guard absent from §G.1; POSIX `test -d` follows the symlink (symlink-to-directory satisfies `[ -d ]`) and `find` branch executes; POSIX `find` without `-H`/`-L` returns empty (non-descent); teardown proceeds and `rm -rf` destroys the symlink target |
| RG-006 | T-005 (AC-002/EC-007): create regular file at `<worktree>/.factory` (not a directory); invoke teardown → should BLOCK at PC2b | FAIL: `[ ! -e ]` existence predicate and non-directory→PC2b clause absent from step-g-cleanup.md §G.1; a regular file at the path occupies the inode (not path-absent) but fails the directory test — without the non-directory branch, execution falls through without a PC2b block and teardown proceeds, allowing `rm -rf` to destroy the file (observed RED at worktree commit 60f0d2d6; GREEN at 73c2bade) |
| RG-007 | T-007 (AC-008): devops-engineer.md §Worktree Cleanup preflight-verification mandate absent → executor-side defensive §G.1 obligation should be documented | FAIL: executor-side defensive preflight-verification mandate absent from devops-engineer.md §Worktree Cleanup section; devops-engineer may execute `git worktree remove` without independently verifying a PASS §G.1 result (observed RED at 60f0d2d6 as the pass-4 gate-8 baseline; GREEN at 0c0922e1). Note: T-007's red-gate history predates AC-008's authoring — T-007 was authored at pass-4 (v1.7) as a doc-parity gate; AC-008 was retro-mapped at v1.9 |
| RG-008 | T-008 (AC-007): F-S2104-P4-009 propagation-gate — 5 of 6 §G.1 mandate surfaces carried inline-find anti-pattern or lacked qualified §G.1 refs; bats gate should fail pre-implementation | FAIL: 5 of 6 surfaces (worktree-manage/SKILL.md, code-delivery/SKILL.md, fix-pr-delivery/SKILL.md, code-delivery.lobster, greenfield.lobster) contained inline `find` anti-pattern or lacked qualified §G.1 references (observed RED at worktree commit 60f0d2d6; GREEN at a317fd77 with later quote-tolerance strengthening at 052620dc) |
| RG-009 | T-009 (AC-009): F-S2104-P4-002 awareness-clause gate — neither agents/adversary.md nor skills/adversarial-review/SKILL.md contained §G.1/BC-6.26.001 awareness; bats gate should fail pre-implementation | FAIL: neither file contained the corrected shadow-write model statement or reference to §G.1 preflight as the enforcement chain that catches this class (observed RED at 60f0d2d6; GREEN at 4265c96c) |

## Test Plan

| Test ID | Story AC | Test File | Description |
|---------|----------|-----------|-------------|
| T-001 | AC-003 | plugins/vsdd-factory/tests/story-worktree-write-path-discipline.bats | Stray `.factory/` file → `PREFLIGHT BLOCKED` + `git worktree remove` NOT called |
| T-002 | AC-004 | plugins/vsdd-factory/tests/story-worktree-write-path-discipline.bats | Empty shadow tree → teardown proceeds |
| T-003 | AC-005 | plugins/vsdd-factory/tests/story-worktree-write-path-discipline.bats | Stray file relocated → retry teardown proceeds |
| T-004 | AC-006 | plugins/vsdd-factory/tests/story-worktree-write-path-discipline.bats | `find` error (non-path-absent) → PC2c HALT; exit code+stderr surfaced; `git worktree remove` NOT called |
| T-005 | AC-002 (EC-007) | plugins/vsdd-factory/tests/story-worktree-write-path-discipline.bats | Regular file at `<worktree>/.factory` path → PC2b BLOCKED; `find` NOT invoked; `git worktree remove` NOT called (BC-6.26.001 EC-008/T-6) |
| T-006 | AC-002 (EC-008) | plugins/vsdd-factory/tests/story-worktree-write-path-discipline.bats | Symlink-to-directory at `<worktree>/.factory` → PC2b BLOCKED; `find` NOT invoked; `git worktree remove` NOT called (BC-6.26.001 T-7) |
| T-007 | AC-008 | plugins/vsdd-factory/tests/story-worktree-write-path-discipline.bats | F-S2104-P4-003 devops-engineer §Worktree Cleanup doc-parity gate (bats test 9) — devops-engineer.md §Worktree Cleanup mandates §G.1 preflight before `git worktree remove` |
| T-008 | AC-007 | plugins/vsdd-factory/tests/story-worktree-write-path-discipline.bats | F-S2104-P4-009 propagation-gate: 6-surface §G.1 mandate regression tests — anti-inline-find + qualified §G.1 refs across worktree-manage/SKILL.md, code-delivery/SKILL.md, fix-pr-delivery/SKILL.md, code-delivery.lobster, greenfield.lobster, and worktree-protocol rules |
| T-009 | AC-009 | plugins/vsdd-factory/tests/story-worktree-write-path-discipline.bats | F-S2104-P4-002 propagation-gate: awareness-clause doc-parity — agents/adversary.md + skills/adversarial-review/SKILL.md MUST state corrected shadow-write model and reference §G.1 preflight as enforcement chain |

## File Structure Requirements (MANDATORY)

| File | Action | Notes |
|------|--------|-------|
| `plugins/vsdd-factory/skills/deliver-story/steps/_shared-context.md` | modify | Add "Write Discipline" clause under §Spec-Path Discipline (BC-6.26.001 PC1 + Invariants 1/3/4) |
| `plugins/vsdd-factory/skills/deliver-story/steps/step-g-cleanup.md` | modify | Add mandatory teardown preflight sub-step before `git worktree remove` (BC-6.26.001 PC2 + Invariant 2 + 5) |
| `plugins/vsdd-factory/tests/story-worktree-write-path-discipline.bats` | create | New bats suite: T-001..T-006 covering AC-002..AC-006 (+EC-007/EC-008 vectors) plus 3 propagation-gate tests (T-007/T-008/T-009) |
| `plugins/vsdd-factory/tests/fixtures/story-worktree/` | create | Fixture worktree directory for bats tests (stray `.factory/` file scenarios) |
| `plugins/vsdd-factory/skills/deliver-story/SKILL.md` | modify | Step 8 preflight mandate — reference §G.1 teardown preflight as mandatory precondition before `git worktree remove` dispatch (BC-6.26.001 PC2 + AC-007) |
| `plugins/vsdd-factory/agents/orchestrator/per-story-delivery.md` | modify | step (g) + Story Split Recovery teardown — mandate §G.1 preflight before `git worktree remove` dispatch (BC-6.26.001 PC2 + AC-007) |
| `plugins/vsdd-factory/workflows/phases/per-story-delivery.md` | modify | Step 8 precedence-winning playbook — mandate §G.1 preflight before `git worktree remove` dispatch (BC-6.26.001 PC2 + AC-007) |
| `plugins/vsdd-factory/skills/worktree-manage/SKILL.md` | modify | Mandate §G.1 preflight before `git worktree remove` dispatch on story worktrees (BC-6.26.001 PC2 + AC-007) |
| `plugins/vsdd-factory/skills/code-delivery/SKILL.md` | modify | Mandate §G.1 preflight before `git worktree remove` dispatch on story worktrees (BC-6.26.001 PC2 + AC-007) |
| `plugins/vsdd-factory/skills/fix-pr-delivery/SKILL.md` | modify | Mandate §G.1 preflight before `git worktree remove` dispatch on story worktrees (BC-6.26.001 PC2 + AC-007) |
| `plugins/vsdd-factory/workflows/code-delivery.lobster` | modify | Mandate §G.1 preflight before `git worktree remove` dispatch on story worktrees (BC-6.26.001 PC2 + AC-007) |
| `plugins/vsdd-factory/workflows/greenfield.lobster` | modify | Mandate §G.1 preflight before `git worktree remove` dispatch on story worktrees (BC-6.26.001 PC2 + AC-007) |
| `CHANGELOG.md` | modify | Add entry under [Unreleased] describing AC-006 bats coverage + propagation sweep to all caller-side teardown paths |
| `plugins/vsdd-factory/rules/worktree-protocol.md` | modify | False-safety-claim correction: remove any claim that plain `git worktree remove` safely detects gitignored shadow `.factory/` content; mandate §G.1 preflight (BC-6.26.001 PC2 + Invariant 2) |
| `plugins/vsdd-factory/agents/adversary.md` | modify | Retract stale-snapshot premise; state corrected model — worktree `.factory/` content is live (issue #523 shadow-write evidence confirms gitignored-shadow files exist at teardown time); mandate §G.1 preflight awareness (BC-6.26.001 Invariant 5; AC-009) |
| `plugins/vsdd-factory/skills/adversarial-review/SKILL.md` | modify | Retract stale-snapshot premise; state corrected model — worktree `.factory/` content is live (issue #523 shadow-write evidence confirms gitignored-shadow files exist at teardown time); mandate §G.1 preflight awareness (BC-6.26.001 Invariant 5; AC-009) |
| `plugins/vsdd-factory/tests/worktree-identity-preflight.bats` | modify | Re-anchor test (e) from retracted stale-snapshot premise to corrected-model positive anchors (pass-3 F-007; BC-6.26.001 Invariant 5) |
| `plugins/vsdd-factory/agents/devops-engineer.md` | modify | Executor-side defensive preflight mandate — verify caller ran §G.1 preflight before `git worktree remove`; if not evident from dispatch context, run §G.1 preflight itself before executing removal; BC-6.26.001 Invariant 2, PC2; AC-008; defense-in-depth consistent with ADR-031 caller-side primary ruling |
| `plugins/vsdd-factory/skills/deliver-story/steps/step-d5-adversary-convergence.md` | modify | Retract residual stale-snapshot phrasing; corrected live-shadow model |
