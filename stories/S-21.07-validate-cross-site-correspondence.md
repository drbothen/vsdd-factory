---
document_type: story
level: implementation
story_id: S-21.07
epic_id: "E-21"
version: "1.20"
title: "validate-cross-site-correspondence WASM hook — six-arm PostToolUse cross-site value-correspondence gate (BC-5.39.010 v1.22 Classes A/B/E; Class D deferred pending convention standardization)"
status: draft
producer: story-writer
timestamp: "2026-07-30T00:00:00Z"
last_amended: |-
  2026-08-14 (v1.20) — F-S2107-RECON-002 close (adversary recon; MEDIUM; TD-VSDD-091
  anti-volatile-pin correction): Task 10's Phase 1 explanatory example ("covers the isolated
  Version column of `## Behavioral Contracts` body-table rows (e.g. this story's own `1.20`
  cell in the Behavioral Contracts table below)") pinned a hard-coded version number that had
  already drifted — the referenced `## Behavioral Contracts` table Version cell reads `1.22`,
  not `1.20`, having been advanced by the v1.15/v1.17/v1.18 BC-version-cite sweeps while this
  illustrative example was missed. Fixed PIN-FREE (not 1.20→1.22, which would decay again on
  the next BC bump): reworded to "this story's own governing-BC-version cell in the
  Behavioral Contracts table below" — the example now points at the CELL as source of truth
  without asserting a specific number. Comprehensive story-wide sweep performed for the same
  defect class (TD-VSDD-091): the symmetric Phase 2 explanatory example three lines below —
  "covers `## Token Budget` rows where BC ID and version appear inline in one field (e.g.
  `BC-5.39.010 v1.22 (full text...)`)" — carries the identical illustrative-pointer-to-this-
  story's-own-live-cell risk (it happened to read the CURRENT value only because it was swept
  in lockstep with the Token Budget row itself at v1.18; nothing prevented it decaying the
  same way Task 10's Phase 1 example just did) and was likewise de-versioned to "this story's
  own governing-BC-version citation in the Token Budget row below". No other hard-coded
  governing-BC-version token in an illustrative/explanatory/pointer context was found
  story-wide: every other live `v1.2x`/`1.2x` site is either (a) a normative "Under
  BC-5.39.010 v1.22 PCxx..." anchor that this story's own established full-sweep precedent
  (v1.15/v1.17/v1.18) already advances on every BC bump, (b) worked-example/fixture DATA for
  a DIFFERENT BC (BC-6.26.001 AC-005/EC-026 fixtures; BC-9.01.001/BC-9.01.002 AC-027 S-4.08
  corpus row) which TD-VSDD-091 explicitly excepts, or (c) an amendment-provenance label
  naming WHEN a clause was introduced ("v1.14 PC2...", "the v1.21 same-field scan-stop",
  "BC-5.39.010 v1.20 precondition 13..." Task 21/22 headings, "the BC-INDEX row has since
  been synced to BC-5.39.010 v1.20") — all already established as exempt historical markers
  by this story's own v1.15/v1.16/v1.17/v1.18 sweep entries below. The `## Behavioral
  Contracts` table Version cell and `## Token Budget` BC row (citation-of-record cells) were
  independently confirmed to already read `1.22` — unchanged by this fix. No AC scope, count,
  cap VALUE, or normative behavior changed — two illustrative example rewordings only.
  input-hash unchanged (no `inputs:` file touched by this fix). STORY-INDEX re-sync owed to
  state-manager (S-21.07 v1.19→v1.20). (story-writer; closes F-S2107-RECON-002.)
  [Prior: 2026-08-14 (v1.19) — F-S2107-RECON-001 close (adversary recon; MEDIUM; POLICY 4
  semantic-anchoring correction): AC-019 §Build constraints `max_bytes` cap enumeration
  mislabeled the STORY-INDEX.md secondary-read cap (`max_bytes = 1048576`,
  `timeout_ms = 3000`) as "(B2)" — WRONG; this value matches `STORY_INDEX_B1_MAX_BYTES` /
  `STORY_INDEX_B1_TIMEOUT_MS` in `arm_b.rs` lines 39-40 (Arm B1 secondary-index read), not
  Arm B2. The adjacent row (`max_bytes = 2097152`, `timeout_ms = 5000`, correctly labeled
  "B2 — Arm B2 write trigger") already matches `STORY_INDEX_B2_MAX_BYTES` /
  `STORY_INDEX_B2_TIMEOUT_MS` (`arm_b.rs` lines 44-45) and needed no change. Fixed: the
  mislabeled row's arm citation corrected from "(B2)" to "(B1 — Arm B1 secondary read)";
  cap VALUE unchanged. Sibling-sweep (TD-VSDD-060) of the full six-row `max_bytes` cap
  enumeration (BC file primary, BC-INDEX.md, Story file primary, STORY-INDEX.md B1,
  STORY-INDEX.md B2, Cycle artifacts D-arm) against `arm_b.rs` lines 39-45 and `lib.rs`
  lines 458-461 confirmed the other five rows are correctly labeled and valued — no
  further mislabels found. No AC scope, count, cap VALUE, or BC-version cite changed —
  arm-label correction only. input-hash unchanged (no `inputs:` file touched by this fix).
  STORY-INDEX re-sync owed to state-manager (S-21.07 v1.18→v1.19). (story-writer; closes
  F-S2107-RECON-001.)
  [Prior: 2026-08-14 (v1.18) — ADV-RECON11-001 close (product-owner BC-5.39.010 v1.21→v1.22
  architect-adjudicated amendment; story-writer BC-version-cite propagation + AC-028 close):
  BC-5.39.010 v1.22 adds precondition 15b / postcondition 26 ("Secondary Index-File UTF-8
  Decode Failure") — DISTINCT from precondition 15a / postcondition 25 (primary-target decode
  failure, BLOCK). Scope: Arm A1's BC-INDEX.md secondary read and Arm B1's STORY-INDEX.md
  secondary read only (Arm B2 explicitly excluded — STORY-INDEX.md is Arm B2's primary target,
  already governed by 15a/25). A non-UTF-8 secondary index-file read (bytes decode OK, UTF-8
  decode fails) MUST emit a DISTINCT advisory naming the index-file path and stating the
  row/hash state for `<id>` is INDETERMINATE, not confirmed-absent, + `HookResult::Continue` —
  MUST NOT fall through into `RowAbsent` (Arm A1; would trigger postcondition 4's misleading
  "dropped registration" BLOCK for any cited BC with `version:` > "1.0") or `(None, None)`
  (Arm B1; would trigger postcondition 12's generic fail-open "not yet registered" advisory,
  silently disabling three-way hash checking with no disclosure of the decode failure).
  Disposition is ADVISORY (Continue), not block — deliberately lower severity than precondition
  15a / postcondition 25's primary-target BLOCK, per the LOW/near-zero-reachability
  architect adjudication. New EC-040 (combined Arm A1 + Arm B1 scenario). Per POLICY 8
  canonical scope (CLAUDE.md §Architectural Authority item 7), the mechanically-checked
  minimum cite sites are the `## Behavioral Contracts` table row and the `## Token Budget`
  row — both updated v1.21→v1.22; BC table ACs cell extended to enumerate AC-028; Token
  Budget EC count 39→40 (BC-5.39.010 v1.22 added EC-040). Per this story's own established
  full-sweep precedent (v1.15/v1.17), every other live governing-BC-version cite was also
  advanced v1.21→v1.22 this same burst: title, H1 heading, Wave Placement Justification, BC
  Status governing-BC statement and active-AC-range summary, the "restates the governing
  algorithm text" clause, the PC2a/PC2b/PC13a/PC13b/PC13c "Under BC..." anchors, the Fixture
  rationale provenance boundary, all four AC-020 §Gate Spec anchors, and the AC-025/AC-026/
  AC-027 heading + body "Under BC..." anchors. Left as historical (NOT swept, by design):
  the `[Prior: ...]` chain, historical `## Changelog` rows, and amendment-provenance labels
  that name WHEN a clause was introduced rather than the current governing version (e.g. "the
  v1.21 same-field scan-stop", "A pre-v1.21 field-scoped-only implementation...", Task 21/22's
  "BC-5.39.010 v1.20 precondition 13/15a..." headings). A story-wide grep confirms zero live
  (non-historical, non-provenance-labeled) `v1.21` residuals remain in the body. **New AC-028**
  (ADV-RECON11-001, precondition 15b / postcondition 26 / EC-040) — asserts a non-UTF-8
  secondary INDEX read (BC-INDEX.md for Arm A1; STORY-INDEX.md for Arm B1) emits the distinct
  INDETERMINATE advisory naming the index file, not a misleading RowAbsent block (Arm A1) or a
  generic "not yet registered" advisory (Arm B1). Cites mutant tests
  `test_BC_5_39_010_arm_a1_non_utf8_bc_index_indeterminate_advisory_not_block` (`arm_a1.rs`) and
  `test_BC_5_39_010_arm_b1_non_utf8_story_index_indeterminate_advisory` (`arm_b.rs`) — both
  confirmed present in the `feature/S-21.07-validate-cross-site-correspondence` worktree; and
  pre-existing control tests `test_BC_5_39_010_arm_a1_new_v1_0_not_in_index_advisory` (AC-002,
  `arm_a1.rs`) and `test_BC_5_39_010_arm_b1_absent_index_sites_advisory` (AC-010, `arm_b.rs`),
  confirming a genuinely-absent row/site in a DECODABLE index file still produces the normal
  advisory disposition, unaffected by the v1.22 amendment. No AC renumbering (POLICY 1
  append-only). input-hash: 397e06d→3885444 (BC-5.39.010.md v1.21→v1.22;
  `compute-input-hash S-21.07-validate-cross-site-correspondence.md --update`). STORY-INDEX
  re-sync owed to state-manager (S-21.07 v1.17→v1.18, BC v1.22, 40 ECs). (story-writer;
  closes ADV-RECON11-001.)
  [Prior: 2026-08-14 (v1.17) — ADV-RECON5-003 close (product-owner BC-5.39.010 v1.20→v1.21 adjudication
  amendment, human-approved "Amend now (v1.21)"; story-writer BC-version-cite propagation +
  AC-027 close): BC-5.39.010 v1.21 adds a PC13 Phase 2 same-field scan-stop — within the Phase 2
  anchor field, the forward `\bv([0-9]+\.[0-9]+)\b` scan after the BC ID's position MUST
  terminate, without producing a version, the moment it encounters a DIFFERENT `BC-S.SS.NNN`
  word-boundary token before any qualifying v-token; the field then yields NO citation and the
  caller proceeds to the next pipe-delimited field. Corpus-confirmed reachable (not hypothetical):
  `.factory/stories/S-4.08-rc1-release-gate.md`'s `## Behavioral Contracts` row for
  `BC-9.01.002` — Trace cell mentions `BC-9.01.001` then, later in the SAME field past an
  intervening `BC-9.01.002` mention, an unrelated "v1.1 BC candidate" phrase — the v1.20
  field-scoped-only algorithm still extracted "v1.1" as `BC-9.01.001`'s citation despite its
  real frontmatter version being "1.0", a live false BLOCK. New EC-039 + new Canonical Test
  Vector row ("A Arm2 — Phase 2 same-field scan-stop (v1.21 / ADV-RECON5-003)") added to
  BC-5.39.010 v1.21. **Per POLICY 8 canonical scope** (CLAUDE.md §Architectural Authority item 7
  — bcs frontmatter array changes propagate atomically to body BC table + ACs + Token Budget;
  this story's own vsdd-factory:story-writer agent definition, §BC Array Propagation Policy),
  the mechanically-checked minimum cite sites are exactly the `## Behavioral Contracts` table
  row and the `## Token Budget` row — both updated v1.20→v1.21; BC table ACs cell extended to
  enumerate AC-027; Token Budget EC count 38→39 (BC-5.39.010 v1.21 added EC-039). This burst
  elected the broader ADV-RECON3-004-style full sweep (the same precedent established at v1.15)
  rather than stopping at the POLICY-8 minimum: every other live governing-BC-version citation
  was also advanced v1.20→v1.21 — title, H1 heading, this section's own governing-BC statement,
  the PC2a/PC2b/PC13a/PC13b/PC13c "Under BC..." anchors, the Fixture rationale provenance
  boundary, and all three AC-020 §Gate Spec anchors. Only genuinely-historical append-only
  content is left as historical version markers: this field's own `[Prior: ...]` chain, the
  body `## Changelog` table's historical rows, amendment-identity provenance labels that name
  WHEN a clause was introduced rather than the current governing version (e.g. Task 21/22's
  "BC-5.39.010 v1.20 precondition 13..." headings, which correctly document that PC13 Phase 1
  and postcondition 25 were added by the v1.20 amendment), and dated facts such as "BC-INDEX.md
  has since been synced to v1.20" (a historical event record, not a live version claim). A
  story-wide grep confirms zero live (non-append-only, non-provenance-labeled) `v1.20` residuals
  remain in the body (ADV-RECON6-002 close). **New AC-027** (ADV-RECON5-003, PC13
  Phase 2 same-field scan-stop / EC-039 / new Canonical Test Vector "A Arm2 — Phase 2 same-field
  scan-stop") — asserts a `## Behavioral Contracts` row whose first field names a DIFFERENT BC,
  whose anchor field mentions the target BC followed by a DIFFERENT BC token before an unrelated
  v-token, yields NO citation (Phase 2 scan-stops → no false block) — the S-4.08 BC-9.01.002
  shape. Cites the actual Rust unit tests in
  `crates/hook-plugins/validate-cross-site-correspondence/src/arm_a2.rs`: mutant
  `test_BC_5_39_010_arm_a2_phase2_same_field_scan_stop_different_bc_no_false_citation`
  (verified present in the `feature/S-21.07-validate-cross-site-correspondence` worktree, RED
  against the pre-scan-stop implementation per its own doc comment); control
  `test_BC_5_39_010_arm_a2_pc13_phase2_anchor_field_with_v_token_produces_citation` (pre-existing
  ADR-038 §Decision 5 Fixture 2 control test — the OWN BC's anchor field precedes its own version
  token ("BC-9.01.002 v1.1") with no intervening different-BC-ID token, still resolves to "1.1";
  confirms the scan-stop fix does not weaken legitimate same-field resolution). No AC renumbering
  (POLICY 1 append-only). input-hash: fd73ba8→397e06d (BC-5.39.010.md v1.20→v1.21;
  `compute-input-hash S-21.07-validate-cross-site-correspondence.md --update`). STORY-INDEX
  re-sync owed to state-manager (S-21.07 v1.16→v1.17, per this version bump). (story-writer;
  closes ADV-RECON5-003.) In-place v1.17 correction (same burst, pre-merge, finalizing v1.17;
  no version bump; `inputs:`/input-hash unchanged at 397e06d): two coherence defects fixed.
  (1) ADV-RECON6-001 (stale §BC Status active-AC enumeration) — the preamble's `ACs 001–011,
  015–024 are active` had not been advanced when AC-025/026 (v1.15) and AC-027 (v1.17, this
  burst) were added; corrected to `015–027`, with the parenthetical extended to record
  AC-025/AC-026/AC-027 provenance. Story-wide grep confirmed this was the only live
  active-AC-range summary sentence in the body — the `## Behavioral Contracts` table ACs cell
  already correctly read "AC-001 through AC-027" before this fix; no other site needed
  reconciling. (2) ADV-RECON6-002 (v1.17 provenance narrative contradicted the body) — the
  scope sentence above previously claimed only the `## Behavioral Contracts` table row and
  `## Token Budget` row were swept v1.20→v1.21 this burst, with title/H1/narrative/BC
  Status/AC-020 anchors "intentionally left" at v1.20; a story-wide grep proved this false —
  every one of those sites had in fact already been swept to v1.21 in the same burst (matching
  the v1.15 ADV-RECON3-004 full-sweep precedent). The scope sentence is rewritten above to
  describe the sweep that was actually performed, so the narrative now matches the body.
  (story-writer; closes ADV-RECON6-001, ADV-RECON6-002.)
  [Prior: 2026-08-14 (v1.16) — ADV-RECON4-002 close (adversary recon-pass-4, MEDIUM; POLICY 17
  frontmatter-parity violation; story-writer). The v1.15 `last_amended` narrative below (now
  wrapped as `[Prior: ... (v1.15) ...]`) was frozen at initial v1.15 authoring time and
  described a PRE-FIX snapshot of Tasks 21/22 that the body's own Task list and `## Changelog`
  v1.15 row had already superseded via in-place corrections during the same burst — this is
  precisely the frontmatter-vs-body contradiction ADV-RECON4-002 flags (an earlier
  minimize-churn / "leave frontmatter frozen" instruction caused the drift; that guidance is
  retracted for this class of defect — frontmatter and body MUST tell one truth, so this is a
  real version bump, not an in-place correction). Reconciled to the delivered truth confirmed
  in the `feature/S-21.07-validate-cross-site-correspondence` worktree (branch, not a pinned
  HEAD SHA — see de-pinning note below): **Task 21** (BC-5.39.010 v1.20 precondition 13 Phase 1
  BC-ID-anchoring, ADV-RECON-003) IS implemented — `arm_a2.rs`'s
  `row_first_field_matches_bc_id` gates Phase 1 pure-version extraction on the row's own
  first-field BC-ID match (delegating to `arm_a1::first_cell_matches_bc_id`), including the
  compound-supersession-cell case; the v1.15 frontmatter's "confirmed NOT yet implemented ...
  still scans all pipe fields" claim is stale and superseded. **Task 22** (BC-5.39.010 v1.20
  precondition 15a / postcondition 25, non-UTF-8 primary-target block, ADV-RECON-007) IS
  implemented — `lib.rs`'s `std::str::from_utf8` decode step on the primary-target read now
  returns `HookResult::block_with_fix(...)` citing invariant 4 (extended, v1.20) on decode
  failure; the fail-OPEN `Continue`-under-invariant-9 misattribution the v1.15 frontmatter
  described is removed (`is_char_boundary()` guards, invariant 9, slicing safety only, remain
  correctly scoped and unaffected). Test evidence corrected: `cargo test -p
  validate-cross-site-correspondence` — **173 passed, 0 failed** (not the v1.15 frontmatter's
  stale mid-burst snapshot of "168 passed, 1 failed"); `bats
  validate-cross-site-correspondence.bats` — **49/49 passed** (not the stale "47/47"). BC-INDEX
  sync status corrected: BC-INDEX.md has ALREADY been synced to BC-5.39.010 v1.20 by a separate
  state-manager burst on `factory-artifacts` — this is no longer "owed to state-manager" as a
  pending action; the `test_BC_corpus_version_sync_all_indexed_bcs_match_frontmatter` test that
  was expected-red immediately after the v1.19→v1.20 bump is now green. **SHA-drift
  de-pinning (TD-VSDD-091):** the v1.15 frontmatter cited evidence HEAD `2b323c05`; the body
  Task list (Tasks 17, 21, 22) separately cited a later HEAD `68c928b6`; the adversary correctly
  identified these as three different SHAs for what should be one coherent delivery state, and
  any SHA pinned here would be stale again by the next commit on the branch (the branch keeps
  advancing). Both this narrative and the body Task-list prose now cite the DELIVERY as
  "implemented on branch `feature/S-21.07-validate-cross-site-correspondence` (pending merge)"
  instead of a specific HEAD SHA; the historical `## Changelog` v1.15 row retains its
  originally-recorded SHAs (`2b323c05`, `68c928b6`, `2aecf52f`, etc.) unchanged, per POLICY 1
  append-only — those are dated historical verification records, not live current-state claims,
  and are exempt from de-pinning. No AC scope, count, BC-version, or normative-behavior change
  in this burst — BC-5.39.010 remains v1.20 (no BC amendment this burst); this is a
  frontmatter/body coherence correction only. input-hash unchanged: fd73ba8 (no `inputs:` file
  changed by this burst). (story-writer; closes ADV-RECON4-002.)
  [Prior: 2026-08-14 (v1.15) — BC-5.39.010 v1.19→v1.20 propagation (POLICY 8; story-writer). Scope:
  per POLICY 8's canonical definition (CLAUDE.md §Architectural Authority item 7 — bcs
  frontmatter array changes propagate atomically to body BC table + ACs + Token Budget), the
  mechanically-checked cite sites are exactly the `## Behavioral Contracts` table row and the
  `## Token Budget` row — the two sections BC-5.39.010 precondition 13 itself confines Arm
  A2's scan to. Both updated to v1.20; BC table ACs cell extended to enumerate AC-025/AC-026;
  Token Budget EC count 36→38 (BC-5.39.010 v1.20 added EC-037 + EC-038). Other v1.19 prose
  cites (title, H1, narrative, BC Status governing-BC statement, existing AC "Under BC..."
  anchors, Task 1/Task 10) sit outside the two scanned sections and are intentionally left as
  historical-version markers per this narrower POLICY-8 scope — a deliberate scope decision
  for this burst, not an incomplete sweep (contrast F-S2107-P18-001/P19-001, both failures to
  complete a FULL sweep the burst had committed to). Two new ACs added from BC-5.39.010 v1.20's
  two amendments: **AC-025** (ADV-RECON-003, precondition 13 Phase 1 BC-ID-anchoring / EC-037 /
  new Canonical Test Vector "A Arm2 — Phase 1 cross-BC-row anchoring") — asserts a cross-BC
  `## Behavioral Contracts` table row does NOT produce a false block/pass when Phase 1 resolves
  a DIFFERENT BC's Version cell; **AC-026** (ADV-RECON-007, precondition 15a / postcondition 25
  / invariant 4 extension / EC-038) — asserts a primary target that reads successfully as bytes
  but fails UTF-8 decoding MUST block, not silently Continue. OBS-P23-A fixed: Token Budget
  Estimated-Tokens rows sum to ~47,500 (9000+8000+4500+2500+6000+4000+3000+5000+3500+2000), not
  the stated ~47,000; Total corrected to ~47,500 and Budget usage corrected to ~23.75%
  (47,500/200,000). AC-019 heading citation corrected: it cited "invariant 10" (POLICY 21
  compliance) for the UTF-8-safety clause the body text and invariant list both correctly
  attribute to invariant 9 (`is_char_boundary()` guard) — heading corrected to invariant 9; no
  other AC-019 content changed. Task checkboxes updated against the actual build state verified
  in the `feature/S-21.07-validate-cross-site-correspondence` worktree (HEAD `2b323c05`, not yet
  merged): `cargo test -p validate-cross-site-correspondence` — 168 passed, 1 failed (the 1
  failure is `test_BC_corpus_version_sync_all_indexed_bcs_match_frontmatter`, an EXPECTED
  consequence of this very burst's BC-5.39.010 v1.19→v1.20 bump — BC-INDEX.md sync is owed to
  state-manager, not this story); `bats validate-cross-site-correspondence.bats` — 47/47 passed;
  release WASM build (`cargo build --release --target wasm32-wasip1 -p
  validate-cross-site-correspondence`) succeeds; registry entry, workspace-members entry, and
  CHANGELOG.md entry all present in the worktree. Tasks 1-11, 13-17, 19-20 checked `[x]` DONE on
  this evidence. Task 12 (Class D / `arm_d.rs`) left unchecked and annotated "DEFERRED v1.6 →
  S-21.08 (Class D not built)" — confirmed: `arm_d.rs` does not exist in the crate's `src/`
  directory. Task 18 (smoke test + PR evidence) left unchecked — `gh pr list` confirms no open
  or merged PR exists for this branch; the build-and-run portion succeeds but the AC-021
  PR-description evidence step has not occurred. Two new unchecked tasks added reflecting the
  v1.20 amendment scope: **Task 21** — implement the PC13 Phase 1 BC-ID-anchoring fix in
  `arm_a2.rs` (confirmed NOT yet implemented: the current Phase-1 pass scans all pipe fields
  right-to-left with no check that the row's own first field names the target BC, exactly the
  un-anchored pattern BC-5.39.010 v1.20 precondition 13 declares NON-CONFORMING); **Task 22** —
  implement the non-UTF-8 primary-target block in `lib.rs` (confirmed NOT yet implemented: the
  current `run` entrypoint's `std::str::from_utf8` decode step fail-opens to
  `HookResult::Continue` citing "invariant 9" in an inline comment, precisely the misattribution
  BC-5.39.010 v1.20 postcondition 25 names NON-CONFORMING — invariant 9 governs slicing safety
  only). input-hash: 93c4a89→fd73ba8 (BC-5.39.010.md v1.19→v1.20; `compute-input-hash
  S-21.07-validate-cross-site-correspondence.md --update`). (story-writer.)
  [Prior: 2026-08-13 (v1.14) — F-S2107-P21-001 close (adversary pass-21, HIGH; POLICY 4
  spec-implementation contradiction + TD-VSDD-060 algorithm-content propagation gap):
  Task 10's `extract_story_bc_version_citations` build directive specified the single-pass
  bare-optional-v regex `\bv?([0-9]+\.[0-9]+)\b` — labeled "per BC-5.39.010 v1.19 PC13
  (unchanged since v1.14)" — which is the EXACT form BC-5.39.010 PC13 declares
  NON-CONFORMING (three collision classes: story-ID collision 29 rows/6 stories,
  BC-section-number collision on Token Budget rows, and an ACs-column collision on this
  story's own governing citation — see BC-5.39.010 v1.19 "Why the prior bare form
  (v1.3–v1.7) is NON-CONFORMING" clause). §BC Status (this story, lines ~197-227) already
  correctly documented the two-phase algorithm since v1.5 (pass-5 fix burst); Task 10 was
  the stale outlier. ROOT CAUSE: the regex was set correctly at story v1.2 against BC v1.3
  PC13 (which then used the bare form); BC v1.8 replaced PC13 with the two-phase algorithm
  and declared the bare form NON-CONFORMING, but subsequent BC-version-cite propagation
  sweeps (v1.4 through v1.13) advanced Task 10's cited VERSION NUMBER on every pass without
  re-deriving the REGEX CONTENT from the BC text each time — the false "(unchanged since
  v1.14)" annotation on the stale bare form was itself evidence of the gap (the regex
  content had in fact changed at v1.8, five story-versions earlier). Task 10 rewritten to
  the two-phase algorithm verbatim per BC-5.39.010 v1.19 PC13: Phase 1 pure-version field
  `^v?([0-9]+\.[0-9]+)$` (isolated Version-column cells in `## Behavioral Contracts` body
  tables); Phase 2 BC-ID-anchored mandatory-v inline token `\bv([0-9]+\.[0-9]+)\b`
  (`## Token Budget` rows); collision rationale (story-ID/BC-section/ACs-column) restated
  inline so the implementer cannot silently regress to the bare form. Class-complete
  algorithm-content sweep performed story-wide (TD-VSDD-060): `grep -nE` for the bare
  regex literal and for "rightmost"/"two-phase"/"Phase 1"/"Phase 2" tokens across the full
  file confirmed exactly one other hit — the append-only Changelog v1.2 row (2026-07-30),
  which correctly documents that at v1.2 Task 10's regex WAS the bare optional-v form (true
  at that historical point, pre-dating BC v1.8's two-phase replacement) — left unchanged
  per POLICY 1 append-only. §BC Status's own PC5/PC13 narrative (lines ~197-227) was
  independently verified already conformant with BC-5.39.010 v1.19 (two-phase PC13,
  first-token-of-last-chain-entry PC5) — no correction needed there. No Red-Gate-anchored
  AC or Test Plan assertion is keyed to the bare-vs-two-phase distinction: the existing
  Arm A2 fixtures (AC-005 through AC-008) all use Token Budget-style v-prefixed citations
  that both the bare and two-phase algorithms extract identically — this is a build-directive
  and documentary correction, not a test-behavior change. Flagged to test-writer (not
  actioned in this burst — out of this finding's scope): the story has no corpus-style
  regression test analogous to BC-5.39.010's `corpus_arm_a1` requirement that would catch a
  future regression to the bare form via the story-ID-collision or ACs-column-collision
  hazards (this story's own BC-table ACs cell `"DEFERRED v1.6"` is a live instance of the
  ACs-column hazard); recommend a `test_BC_5_39_010_arm_a2_no_false_block_on_acs_column`
  or equivalent negative test in a future story-writer/test-writer pass. No AC scope, count,
  or other test assertion changed. input-hash unchanged: 93c4a89 (the `inputs:` file list
  was not modified by this body-only fix). (story-writer; closes F-S2107-P21-001.)
  [Prior: 2026-08-13 (v1.13) — F-S2107-P19-001 close (adversary pass-19, MEDIUM; POLICY 8
  BC-version-cite propagation completeness correction): the v1.12 F-S2107-P18-001 sweep
  was INCOMPLETE — its grep predicate (`grep -niE "BC-5\.39\.010 v1\.18"`, single literal
  space, prefix-required) does not match whitespace-tolerant or line-wrapped variants, and
  missed two live residuals. (1) AC-019 §Build constraints: a line-wrapped cite —
  `BC-5.39.010` at the end of one line, `v1.18 §Gate Spec, F-S2107-P10-004` at the start of
  the next — the un-swept third member of a triplet whose two siblings in AC-020 Notes were
  already correctly advanced; corrected to `v1.19`. (2) AC-018 §Fixture rationale: the
  provenance upper-bound "PC2b/E1 content unchanged through v1.18" did not carry forward;
  corrected to "through v1.19" (BC-5.39.010 v1.19 last_amended confirms §Postconditions
  incl. PC2b and §Edge Cases are unchanged at v1.19, so v1.19 is the accurate boundary).
  The v1.12 entry's claim "Post-sweep verification ... confirmed zero live residuals" was
  FALSE: both sites were live body prose, not append-only/Changelog material — the narrow
  predicate simply could not see them. Corrected verification uses a whitespace-tolerant +
  bare-token predicate: `grep -niE "v1\.1[0-8]"` (catches every v1.10–v1.18 token
  regardless of prefix or line-wrap), plus line-wrap-specific checks `grep -nA1
  "BC-5\.39\.010$"` and `grep -niE "BC-5\.39\.010[[:space:]]"`. Every hit was individually
  triaged; the surviving `v1.1[0-8]` occurrences are exclusively: (a) this field's own
  append-only `[Prior: ...]` chain; (b) body Changelog historical rows (v1.5–v1.12); (c)
  legitimately-historical §BC Status provenance annotations ("v1.15–v1.18 amended only...",
  "unchanged since v1.14", "v1.10 PC5...") documenting WHEN a clause last changed, not live
  current-version claims; (d) fixture/example version strings belonging to the unrelated
  example BC `BC-6.26.001` in AC-005/AC-006 and the EC-002/EC-026 edge-case rows
  (illustrative test data, not citations of governing BC-5.39.010's version). Zero live
  residuals of BC-5.39.010's governing version remain after this triage — this claim is
  made only because the full triage above actually supports it, not by default. No AC
  scope, test behavior, or count number changed — version-cite and provenance-boundary
  correction only. input-hash unchanged: 93c4a89 (the `inputs:` file list was not modified
  by this body-only fix; per POLICY 8/TD-VSDD-059 this is a body-prose correction, not an
  input-content change). (story-writer; closes F-S2107-P19-001.) [Prior: 2026-08-13
  (v1.12) — F-S2107-P18-001 close (POLICY 8 BC-version-cite propagation;
  TD-VSDD-060 class-complete sweep): BC-5.39.010 v1.18→v1.19 propagation (product-owner
  reconciled §VP Anchors reserved-range count 17→19 to match its own 19-entry VP table;
  no count VALUE changed in either the BC or this story — 19 VPs/36 ECs/24 ACs were
  already correct in both). Story-wide grep sweep (`grep -niE "BC-5\.39\.010 v1\.18"`)
  performed for every live cite pattern (title, H1, narrative, BC Status governing-BC
  statement, AC "Under BC..." anchors, AC-020 §Gate Spec traces (x3), BC table version
  cell, Token Budget row, Tasks 1/10). All 16 live sites advanced v1.18→v1.19: title
  frontmatter; H1 heading; narrative "authors this gate" clause; BC Status governing-BC
  statement; AC-001 PC2b anchor; AC-009 PC13b anchor; AC-020 heading trace; AC-020 tool-field
  §Gate Spec trace; AC-020 Notes "current cap figure" §Gate Spec trace; AC-020 Notes "Global
  fuel cap" §Gate Spec trace; AC-022 PC2a anchor; AC-023 PC13a anchor; AC-024 PC13c anchor;
  BC table version cell (`| 1.18 |` → `| 1.19 |`); Token Budget BC row; Task 1; Task 10.
  Post-sweep verification (`grep -n "BC-5.39.010 v1.18"`) confirmed zero live residuals —
  the only surviving `v1.18` hits are this field's own append-only `[Prior: ...]` chain and
  the body Changelog v1.10 row, both historical version-of-record citations documenting
  WHEN the v1.14→v1.18 propagation happened (POLICY 1 append-only; not live cites). No AC
  scope, test behavior, or count number changed — version-cite propagation only.
  input-hash: 7bc1850→93c4a89 (BC-5.39.010.md v1.18→v1.19; `compute-input-hash
  S-21.07-validate-cross-site-correspondence.md --update`). (story-writer; closes
  F-S2107-P18-001.)] [Prior: 2026-08-13 (v1.11) — F-S2107-P13-001 close (adversary pass-13, class-complete sibling
  sweep per TD-VSDD-060/POLICY 8; the v1.10 fix corrected only AC-020 Notes and left
  sibling sites carrying the same retracted premise, which is exactly why this finding
  recurred): two flagged sites fixed. (1) Out-of-Scope table "Per-plugin `fuel_cap`
  registry field extension" row: retracted premise "this hook uses `on_error =
  \"continue\"` and needs no `fuel_cap` override" removed (contradicted AC-020's own
  SHOULD-set-once-ADR-039-Phase-1-ships language); reason rewritten to "out of THIS
  story's scope because it depends on ADR-039 Phase 1 / S-21.10"; anchor re-pointed
  ADR-035 §Decision 5 → ADR-042 §Decision 2 (ADR-035 v1.1 Correction 3 itself confirms
  its "no registry change needed for this new hook" note is superseded). (2) AC-019
  §Build constraints: flat "10M-instruction fuel cap" claim (no source-vs-operator
  qualifier, violating the rule AC-020 already establishes) corrected to state both the
  operator-effective 10,000,000 cap (through v1.0.0-rc.23) and the source-HEAD
  20,000,000 cap (ADR-042 §Decision 1 / PR #774) explicitly. Story-wide grep sweep
  performed for the full defect class (RETRACTED-false "calibrated" claims, active
  fuel_cap-prohibition phrasing, on_error=continue-implies-no-fuel_cap-needed rationale,
  unqualified flat 10M/20M cap figures, live BC-version cites below v1.18, and
  fuel_cap-related ADR-035 §Decision 5 anchors) — all remaining hits verified to be
  either already-correct v1.10-fixed text (AC-020 Notes), correctly-qualified historical
  measurement statements ("then-operative 10,000,000-instruction cap", "former 10M
  cap"), legitimate non-fuel-cap ADR-035 §Decision 5 anchors (release-build/on_error
  general rationale — ADR-035 v1.1 §Decision 5 retains that scope), historical
  version-of-record citations ("BC-5.39.010 v1.6 §Deferred Scope", "BC-5.39.010 v1.1/
  v1.2/v1.16 §Gate Spec had/corrected/lifted..." — documenting WHEN a change happened,
  not claiming current version), or append-only modified[]/Changelog/[Prior:] chain
  entries (POLICY 1) — no further live sibling sites of this defect class survive. Full
  grep-sweep stdout reported in the closing burst record. No AC scope or Red-Gate-
  anchored test assertion changed (AC-019 has no test keyed to the specific cap figure;
  Out-of-Scope table is non-normative). input-hash unchanged (7bc1850) — the `inputs:`
  files (BC-5.39.010.md, ADR-035, etc.) were not themselves modified by this sweep; only
  this story's own documentary prose was corrected. (story-writer; closes
  F-S2107-P13-001.) [Prior: 2026-08-13 (v1.10) — F-S2107-P12-001 close (adversary pass-12, MEDIUM; POLICY 8 bc-array/version propagation + POLICY 4 semantic-anchoring): BC-5.39.010 v1.14→v1.18 propagation (story-writer). Live BC-version cites advanced to v1.18 (title/H1, narrative, BC Status governing-BC statement, AC-001/009/018/022/023/024/020 body "Under BC..." anchors, AC-020 §Gate Spec traces, BC table version cell, Token Budget, Task 1/Task 10); PC2/PC13/PC5 substance is unchanged since v1.14 — only the §Gate Spec fuel-cap subsection was amended v1.15→v1.18 — so those AC-body cites are annotated "unchanged since v1.14" rather than silently re-pinned. Three downstream-propagation drifts fixed in §AC-020 Notes on field choices: (1) RETRACTED-FALSE claim removed — "the `max_bytes` caps are calibrated to keep reads within the 10M fuel budget" is retracted per BC-5.39.010 v1.15 (F-S2107-P9-002); replaced with the measured-exhaustion reality (fuel_consumed=9,920,913/10,000,000, 99.21% consumed, at the production-scale BC-INDEX fixture). (2) LIFTED PROHIBITION corrected — the v1.14-era "this hook MUST NOT include a `fuel_cap` registry field" rule is retracted per BC-5.39.010 v1.16 (ADR-042 §Decision 2; corroborated ARCH-INDEX ADR-042 §Decision 2, ADR-035 v1.1); rewritten to state a per-plugin `fuel_cap` SHOULD be set once ADR-039 Phase 1 (S-21.10) ships; the registry entry in AC-020 still omits `fuel_cap` today (ADR-039 Phase 1 unshipped at operator level), but the omission is now a "not yet" state, not a spec prohibition. (3) STALE CAP FIGURE corrected — the flat "10M-instruction fuel budget" claim (attributed to ADR-035 §Decision 5) is replaced with the source-HEAD-vs-operator-effective framing per BC-5.39.010 v1.18 (F-S2107-P10-004): `DEFAULT_FUEL_CAP = 20,000,000` at source-HEAD (PR #774 / commit `62fbcf1a`, ADR-042 §Decision 1) but the marketplace-cached dispatcher binary this project's own hooks actually run remains pinned to 10,000,000 through v1.0.0-rc.23 until v1.0.0-rc.24 is consumed. No AC scope change: all edits are factual-claim and version-cite propagation only; no Red-Gate-anchored AC test assertion changed. input-hash: d366da1→7bc1850 (BC-5.39.010.md v1.14→v1.18 propagation). [Prior: 2026-08-07 (v1.9) — F-S2107-P8-009 close (active-AC enumeration corrected: §BC Status ACs 001–011, 015–023→015–024; AC-024 provenance recorded); BC-5.39.010 v1.13→v1.14 propagation (POLICY 8; story-writer): BC version cites advanced to v1.14 (AC-001/009/018/022/023/024/020 body, AC-020 §Gate Spec traces, BC table, Token Budget, Task 1/Task 10, BC Status PC2/PC13/PC5 subclauses); ADR-038 §Empirical Measurement corrected three→two of four corpus rows per v1.14; Test Plan rows T-P8A/T-P8B/T-P8C added for AC-024 PC13c Rust unit tests; Task 3 arm_b.rs extended with AC-023/AC-024; Task 4 bats range extended to AC-001 through AC-023. [Prior: 2026-08-06 (v1.8) — BC-5.39.010 v1.12→v1.13 propagation (POLICY 8; story-writer): version cites advanced to v1.13 (title/H1, Priority, BC Status preamble/PC2/PC13/PC5 subclauses, AC-001/009/018/022/023/020 body, BC table, Token Budget, Task 1); PC13c half-present sub-case (ADR-038 §Decision 3): AC-024 added (advisory+Continue; traces to BC-5.39.010 PC13c; test names arm_b1_half_present_catalog_mismatch_is_advisory_not_block and arm_b1_half_present_blockquote_mismatch_is_advisory_not_block, GREEN control arm_b1_half_present_catalog_match_is_advisory); EC-036 mirrored from BC v1.13; BC table ACs cell updated to include AC-024; EC count 35→36 per BC-5.39.010 v1.13. modified[] v1.5/v1.6 ordering defect fixed (F-S2107-P7-016; both on 2026-08-05; E2 date-only predicate blind to same-day version-token decrease). input-hash: 1bc3197→c0ab6a3 (BC-5.39.010.md v1.13). [Prior: 2026-08-05 (v1.7) — BC-5.39.010 v1.11→v1.12 propagation (POLICY 8; story-writer): version cites advanced to v1.12 (title/H1, Priority, BC Status preamble/PC2/PC13/PC5, AC-001/009/018/022/023/020 body, BC table, Token Budget, Task 1/Task 10); EC count 33→35, VP count 17→19 per BC-5.39.010 v1.12. input-hash: 19454c3→1bc3197 (BC-5.39.010.md v1.12). [Prior: 2026-08-05 (v1.6) — BC-5.39.010 v1.10→v1.11 propagation (POLICY 8; story-writer; pass-6 fix burst): PC2 directional carve-out (F-P6-001 Option 1): AC-001 rewritten to test PC2b (index-newer-than-primary → block; new fixture BC v1.5 / INDEX v1.6); new AC-022 added for PC2a advisory path (primary-newer-than-index); PC13 two sub-cases (F-P6-001): AC-009 rewritten to test PC13b (B2≠B3 → block; new fixture B1=47a65c9, B2=4be9d21, B3=c3f9811); new AC-023 added for PC13a advisory path (B2==B3, B1≠B2 → advisory); AC-018 fixture updated (index-newer + E1 mismatch exercises combined-block path under v1.11; prior primary-newer fixture was advisory only); EC-002 corrected (primary-newer → advisory per PC2a normative postconditions; BC-5.39.010 EC table is stale relative to its own postconditions — flagged to product-owner); sweep confirmed no other ACs trace to PC2 or PC13; all v1.10 BC cites advanced to v1.11 (title/H1, BC Status, BC table, Token Budget, Tasks, AC-020); input-hash: dd5c9d2→19454c3 (BC-5.39.010.md v1.10→v1.11). [Prior: 2026-08-05 (v1.5) — BC-5.39.010 v1.7→v1.10 propagation (POLICY 8; story-writer; pass-5 fix burst): title/H1 updated to v1.10; BC Status updated (v1.10 is governing BC; v1.10 PC5 locator/body-table two-level predicates — locator predicate (conditions (1)+(2)) distinct from validity predicate (condition (3)); candidate-selection full-file scan must prefer first (1)+(2)+(3)-satisfying line (first-match-wins NON-CONFORMING per PC5); RowMalformed redefined as "locator-matched line with <5 fields after escape-aware split" (F-S2107-P4-022 prose contradiction resolved); PC4a normative advisory text pinned (MUST-verbatim; "Registration status cannot be determined" and "Verify BC-INDEX body-table registration manually" MUST NOT be omitted); PC13 prescribed message text MUST cite POLICY 14 leg 3 for modified[] violations; PC36 block-scalar MUST — extract_frontmatter_field MUST handle `|-` block scalars; returning "|-" is NON-CONFORMING (corpus: 2 occurrences — BC-5.39.010 + S-21.07); PC40 updated — S-21.07 absent from ADR-037 §Context 19-story table (corrected to 78 stories in ADR-037 v1.1); story-side fix removes ARCH-INDEX.md from inputs:; Story Anchor TBD→S-21.07; v1.9 RowMalformed fourth state (locator-matched line found but <5 fields → advisory+Continue; NEVER reaches postcondition 4 blocking path; postcondition 4a added); v1.8 PC5 column-anchored locator — escape-aware column count (5 fields → RowPresentNoVersion; 6+ → Version(v)); two-phase PC13 (Phase 1 pure-version `^v?[0-9]+\.[0-9]+$`; Phase 2 mandatory-v inline `\bv([0-9]+\.[0-9]+)\b`; optional-v excluded — story-ID collision 194/1943 rows + BC-section-number collision + ACs-column collision per corpus check)); arm count in BC Status corrected 4→6 blocking arms (A1,A2,B1,B2,E1,E2) per F-S2107-P4-009; BC table version cell updated to 1.10; Token Budget BC row updated to v1.10; AC-020/Task 1/Task 10 §Gate Spec cites advanced to v1.10 (F-S2107-P4-002). ARCH-INDEX.md removed from inputs: per ADR-037 §Decision 5 (F-S2107-P4-013 story-side remediation; ARCH-INDEX.md is volatile per ADR-037 §Decision 2 — growing architecture catalog; removal ends PC40 Class B suppression for S-21.07; ADR-037 v1.1 §Context now names S-21.07 in corrected 78-story roster). input-hash: 25c7324→dd5c9d2 (single-file compute-input-hash --update; ARCH-INDEX.md removed from inputs: plus BC-5.39.010.md advanced to v1.10 since prior hash). [Prior: 2026-08-04 (v1.4) — BC-5.39.010 v1.6→v1.7 propagation (POLICY 8; story-writer): title/H1 updated to v1.7; BC Status updated (BC-5.39.010 v1.7 is governing BC; v1.7 corrected PC5 — `extract_bc_index_version` rearchitected to three-state `RowAbsent`/`RowPresentNoVersion`/`Version(v)`: prior two-state `Option<String>` conflating absent-row and no-version-cell into a single `None` produced ≥1,712 false BLOCKs per F-S2107-P3-001 corpus analysis; `RowPresentNoVersion` is canonical normal state for ~98% of BC-INDEX rows; postcondition 4 expanded to silent-continue on `RowPresentNoVersion`; invariant 11 updated to three-category taxonomy stale/fabricated/algorithm-divergent; `1acf3c6` reclassified ALGORITHM-DIVERGENT per ADR-036 §Decision 4 — Pass-30 M02 POLICY 18 violation claim retracted; Gate Spec `run_part_a_arm1` pseudocode updated to three-state match); AC-009 invariant 11 reference updated to three-category taxonomy; AC-019/AC-020 §Gate Spec cites advanced to v1.7; Behavioral Contracts table BC version cell updated to v1.7; Token Budget BC row updated to v1.7; Task 1 cite updated to v1.7. Pass-4 fix burst. [Prior: 2026-08-04 (v1.3) — Class D (AC-012/013/014) descoped and marked [DEFERRED v1.6 — Class D] per BC-5.39.010 v1.6 human-approved scope decision (2026-08-04); BC-5.39.010 v1.3→v1.6 propagation (POLICY 8; story-writer): title/H1 updated (six-arm; Classes A/B/E; Class D deferred); BC Status section updated to v1.6 as governing BC; Behavioral Contracts table BC H1 title and version updated to v1.6; arm_d.rs Architecture Mapping/Purity Classification/Tasks/Test Plan/File Structure entries marked DEFERRED v1.6; EC-010/011/012/013/024 marked DEFERRED v1.6 in Edge Cases table; EC-033 added from BC v1.6; ADR-035 v1.0 volatile pin corrected per TD-VSDD-091 (Token Budget row: ADR-035 v1.0 → ADR-035); AC-020 path_allow updated (.factory/cycles/ commented out DEFERRED v1.6); Token Budget BC version updated to v1.6. Adversary pass-2 fix-burst (F-S2107-P2-014 + descope items (a)/(b)/(c)). [Prior: 2026-07-30 (v1.2) — BC-5.39.010 v1.2→v1.3 propagation (POLICY 8; story-writer): BC H1 title restored verbatim (trigger clauses 'after a BC frontmatter bump'/'after a story edit' added; 'non-monotonicity' → 'date-decrease' per v1.3); title/H1 arm count corrected five-arm → seven-arm (7 arms across 4 classes — 6 blocking A1/A2/B1/B2/E1/E2 + 1 advisory D); AC-016 E2 semantics updated non-ascending/non-monotonic → non-decreasing/strict-decrease per BC v1.3 postconditions 21-22; EC-030/EC-031 mirrored from BC v1.3 EC table; Task 10 arm_a2 regex updated optional-v form; Changelog section added (POLICY 14 leg 2 / POLICY 17). Adversary pass-1 fix-burst (F-S2107-P1C-006/008/013/015). [Prior: 2026-07-30 (v1.1) — BC-5.39.010 v1.1→v1.2 propagation (POLICY 8; story-writer): all 7 live-site BC-version citations updated to v1.2; AC-020 notes updated (MultiEdit gap closed in BC v1.2; fuel_cap guard added per v1.2 §Gate Spec); datum-copy ruling for Arm A2 confirmed in BC Status. [Prior: 2026-07-30 (v1.0) — Initial authoring (story-writer; human-authorized ahead of pass-30 fix burst; E-21 cross-site correspondence gate; BC-5.39.010 v1.1; ADR-035 v1.0; 21 ACs; 11 pts; W4).]]]]]]]]]]]]]]]]
phase: brownfield-backfill
cycle: v1.0-feature-engine-discipline-pass-1
inputs:
  - .factory/specs/behavioral-contracts/ss-05/BC-5.39.010.md
  - .factory/specs/architecture/decisions/ADR-035-cross-site-correspondence-validation-three-tier-architecture.md
  - .factory/specs/architecture/SS-04-plugin-ecosystem.md
  - .factory/specs/architecture/SS-05-orchestration.md
  - plugins/vsdd-factory/hooks-registry.toml
  - crates/hook-sdk/src/host.rs
  - crates/hook-sdk/src/result.rs
input-hash: "3885444"
traces_to:
  - .factory/stories/epics/E-21-factory-state-data-loss-hardening.md
wave: 4
priority: P1
points: 11
track: feature
effort_class: multi-day
depends_on: []
blocks: []
behavioral_contracts: [BC-5.39.010]
verification_properties: []
# VP IDs pending VP-INDEX allocation by state-manager at post-merge burst (BC-5.39.010 §VP Anchors)
domain_invariants: []
tdd_mode: strict
target_module: "crates/hook-plugins/validate-cross-site-correspondence/"
subsystems:
  - "SS-04"
  - "SS-05"
estimated_days: 3.0
assumption_validations: []
risk_mitigations: []
anchored_adrs: [ADR-035]
# ADR-038 (§Decision 1/5 — first-token-of-last-chain-entry, two-phase PC13) and ADR-042
# (fuel-cap framing) are intentionally NOT separately anchored here: their decisions are
# restated verbatim in governing BC-5.39.010's own normative body (§Preconditions PC13,
# §Gate Specifications), which IS an inputs: member and already drives input-hash. See
# §BC Status below (ADV-RECON10-001 close) for the full rationale.
feature: "e21-factory-state-hardening"
modified:
  - "2026-07-30 v1.0: Initial authoring"
  - "2026-07-30 v1.1: BC-5.39.010 v1.2 propagation (POLICY 8)"
  - "2026-07-30 v1.2: BC-5.39.010 v1.3 propagation (POLICY 8; adversary pass-1 fix-burst)"
  - "2026-08-04 v1.3: Class D descoped DEFERRED v1.6; BC-5.39.010 v1.3→v1.6 propagation; ADR-035 volatile pin fixed; adversary pass-2 fix-burst"
  - "2026-08-04 v1.4: BC-5.39.010 v1.6→v1.7 propagation (POLICY 8; pass-4 fix burst)"
  - "2026-08-05 v1.5: BC-5.39.010 v1.7→v1.10 propagation; ARCH-INDEX.md removed from inputs:; arm count 4→6; pass-5 fix burst"
  - "2026-08-05 v1.6: BC-5.39.010 v1.10→v1.11 propagation (POLICY 8; pass-6 fix burst); AC-001/009/018 rewritten for PC2b/PC13b; AC-022/023 added for advisory paths"
  - "2026-08-05 v1.7: BC-5.39.010 v1.11→v1.12 propagation (POLICY 8; story-writer)"
  - "2026-08-06 v1.8: BC-5.39.010 v1.12→v1.13 propagation (POLICY 8; story-writer); AC-024 added for PC13c half-present; EC-036 mirrored; modified[] v1.5/v1.6 ordering fixed (F-S2107-P7-016)"
  - "2026-08-07 v1.9: F-S2107-P8-009 close (active-AC enumeration corrected: 015–023→015–024); BC-5.39.010 v1.13→v1.14 propagation (POLICY 8; story-writer); Test Plan T-P8A/B/C added for AC-024"
  - "2026-08-13 v1.10: F-S2107-P12-001 close (adversary pass-12, MEDIUM); BC-5.39.010 v1.14→v1.18 propagation (POLICY 8; story-writer); fixed retracted false fuel-calibration claim, lifted fuel_cap prohibition, and stale flat 10M cap figure (source-HEAD-vs-operator-effective framing) in AC-020 Notes; no AC scope change"
  - "2026-08-13 v1.11: F-S2107-P13-001 close (adversary pass-13, class-complete sibling sweep, TD-VSDD-060); fixed Out-of-Scope table fuel_cap deferral reason (retracted premise removed; re-anchored ADR-035→ADR-042 §Decision 2) and AC-019 flat 10M fuel-cap figure (added source-HEAD-vs-operator-effective qualifier); story-wide grep sweep confirmed no other live sibling sites of the defect class survive; no AC scope change; input-hash unchanged (7bc1850)"
  - "2026-08-13 v1.12: F-S2107-P18-001 close (POLICY 8 BC-version-cite propagation, TD-VSDD-060 class-complete sweep); BC-5.39.010 v1.18→v1.19 propagation (product-owner reconciled §VP Anchors reserved-range count 17→19 to match its 19-entry VP table; no count VALUE changed in either the BC or this story — 19 VPs/36 ECs/24 ACs were already correct). All 16 live BC-version-cite sites advanced v1.18→v1.19 (title/H1, narrative, BC Status governing-BC statement, AC-001/009/020(x3)/022/023/024 body \"Under BC...\"/§Gate Spec anchors, BC table version cell, Token Budget, Task 1/Task 10); zero live v1.18 residuals confirmed by story-wide grep sweep (only append-only modified[]/Changelog historical entries retain the v1.18 token, per POLICY 1). No count/scope/test-behavior change. input-hash: 7bc1850→93c4a89 (BC-5.39.010.md v1.18→v1.19)"
  - "2026-08-13 v1.13: F-S2107-P19-001 close (adversary pass-19, MEDIUM); v1.12's zero-live-residuals attestation was FALSE — its grep predicate (single-space, prefix-required) missed a line-wrapped BC-5.39.010 v1.18 cite (AC-019 §Build constraints) and a bare v1.18 provenance boundary (AC-018 §Fixture rationale); both corrected to v1.19. Corrected verification used a whitespace-tolerant + bare-token predicate (`grep -niE \"v1\\.1[0-8]\"` plus line-wrap-specific checks); full triage confirmed zero live residuals of BC-5.39.010's governing version remain. No AC scope/count/test-behavior change; input-hash unchanged (93c4a89 — inputs: list not modified, body-only fix)"
  - "2026-08-13 v1.14: F-S2107-P21-001 close (adversary pass-21, HIGH; POLICY 4 spec-implementation contradiction + TD-VSDD-060); Task 10's `extract_story_bc_version_citations` directive specified the bare-optional-v regex `\\bv?([0-9]+\\.[0-9]+)\\b` that BC-5.39.010 v1.19 PC13 declares NON-CONFORMING (story-ID/BC-section/ACs-column collision hazards); rewritten to the two-phase algorithm (Phase 1 pure-version field `^v?([0-9]+\\.[0-9]+)$`; Phase 2 BC-ID-anchored mandatory-v `\\bv([0-9]+\\.[0-9]+)\\b`) matching §BC Status, which was already conformant. Class-complete story-wide sweep found no other live bare-regex sites. No AC scope/count/test-behavior change; input-hash unchanged (93c4a89 — inputs: list not modified, body-only fix)"
  - "2026-08-14 v1.15: BC-5.39.010 v1.19→v1.20 propagation (POLICY 8 canonical scope — BC table + Token Budget row only, per CLAUDE.md §Architectural Authority item 7; ADV-RECON-003/ADV-RECON-007 amendments); AC-025 added (PC13 Phase 1 BC-ID-anchoring / EC-037); AC-026 added (precondition 15a / postcondition 25 / invariant 4 extension / EC-038, non-UTF-8 primary target → block); OBS-P23-A Token Budget Total/usage arithmetic corrected; AC-019 heading invariant-citation corrected (10→9); Tasks 1/3/4/5/6/7/8/9/10/11/13/14/15/16/17/19/20 checked off DONE (crate built/wired/registered/tested in feature/S-21.07-validate-cross-site-correspondence worktree: 171 unit tests + 47 bats tests green except the one test expectedly RED against the still-unsynced BC-INDEX row this very burst's BC bump produces, owed to state-manager); Task 2 checked (baseline capture historical); Task 12 (Class D/arm_d) left unchecked, annotated DEFERRED v1.6 → S-21.08 (Class D not built — confirmed absent from crate); Task 18 left unchecked (WASM release build succeeds but AC-021 live-session smoke-test evidence has not yet been captured in an open PR description — no PR exists for the branch as of this burst); new Task 21 added (PC13 Phase 1 BC-ID anchoring implementation — confirmed NOT yet implemented: `arm_a2.rs` still scans each table row right-to-left for any pure-version field with no row-first-field BC-ID anchor check) and Task 22 added (non-UTF-8 primary-target block implementation — confirmed NOT yet implemented: `lib.rs` line ~183 still does `std::str::from_utf8` fail-OPEN with an explicit `// invariant 9` comment, the exact misattribution v1.20 postcondition 25 declares NON-CONFORMING), both unchecked. input-hash: 93c4a89→fd73ba8 (BC-5.39.010.md v1.19→v1.20; `compute-input-hash S-21.07-validate-cross-site-correspondence.md --update`). (story-writer.) [NOTE added at v1.16: this v1.15 bullet is a historical append-only record of the PRE-FIX snapshot as originally authored; it was superseded same-burst by in-place body/Changelog corrections that this frontmatter bullet never received (frozen-at-authoring per then-current guidance) — see the v1.16 bullet below for the reconciliation. Left unedited per POLICY 1 append-only.]"
  - "2026-08-14 v1.16: ADV-RECON4-002 close (adversary recon-pass-4, MEDIUM; POLICY 17 frontmatter-parity violation) — the v1.15 `last_amended` narrative and this array's own v1.15 bullet above described a PRE-FIX snapshot (Tasks 21/22 \"confirmed NOT yet implemented\"; evidence HEAD `2b323c05`; 168 passed/1 failed; 47/47 bats; BC-INDEX sync \"owed to state-manager\") that contradicted the already-corrected body (Task list, `## Changelog` v1.15 row): Tasks 21 and 22 are DONE (PC13 Phase 1 BC-ID anchoring in `arm_a2.rs`; non-UTF-8 primary-target fail-CLOSED block in `lib.rs` citing invariant 4 extended, not the invariant-9 fail-open misattribution); unit tests 173 passed / 0 failed; bats 49/49 passed; BC-INDEX already synced to BC-5.39.010 v1.20 (no longer owed). `last_amended:` rewritten with the v1.16 reconciliation narrative on top; the old v1.15 text preserved verbatim as a nested `[Prior: ...]` historical entry per POLICY 1 append-only. SHA-drift de-pinned (TD-VSDD-091): live delivery-state prose (frontmatter narrative + body Task 17/21/22) no longer pins a volatile HEAD SHA (`2b323c05` / `68c928b6`) — both now cite \"implemented on branch `feature/S-21.07-validate-cross-site-correspondence` (pending merge)\"; the historical `## Changelog` v1.15 row's originally-recorded SHAs are left unchanged (dated historical record, exempt). New `## Changelog` v1.16 row added. No AC scope, count, or BC-version change — BC-5.39.010 remains v1.20; input-hash unchanged: fd73ba8 (no `inputs:` file changed). (story-writer; closes ADV-RECON4-002.)"
  - "2026-08-14 v1.17: ADV-RECON5-003 close (product-owner BC-5.39.010 v1.20→v1.21 adjudication amendment; story-writer BC-version-cite propagation): BC-5.39.010 v1.21 adds the PC13 Phase 2 same-field scan-stop (forward v-token scan after the anchor BC ID MUST terminate, yielding no citation, on encountering a DIFFERENT BC-S.SS.NNN token first — closes a live false BLOCK corpus-confirmed against `.factory/stories/S-4.08-rc1-release-gate.md`'s BC-9.01.002 row); new EC-039 + new Canonical Test Vector row. Per POLICY 8 canonical scope (CLAUDE.md §Architectural Authority item 7), the mechanically-checked minimum is the `## Behavioral Contracts` table row and `## Token Budget` row (BC table ACs cell extended to AC-027; Token Budget EC count 38→39); this burst elected the broader ADV-RECON3-004-style full sweep beyond that minimum — every other live governing-BC-version cite (title, H1, BC Status, PC anchors, AC-020 §Gate Spec) was also advanced v1.20→v1.21, leaving only genuinely-historical append-only content (Changelog rows, [Prior:] chain, amendment-provenance labels) at v1.20. New AC-027 added (traces to BC-5.39.010 v1.21 PC13 Phase 2 same-field scan-stop / EC-039 / the new Canonical Test Vector): asserts the S-4.08 BC-9.01.002 row shape yields no false citation; cites mutant `test_BC_5_39_010_arm_a2_phase2_same_field_scan_stop_different_bc_no_false_citation` and control `test_BC_5_39_010_arm_a2_pc13_phase2_anchor_field_with_v_token_produces_citation` (both in `arm_a2.rs`). No AC renumbering. input-hash: fd73ba8→397e06d (BC-5.39.010.md v1.20→v1.21; `compute-input-hash S-21.07-validate-cross-site-correspondence.md --update`). STORY-INDEX re-sync owed to state-manager (S-21.07 v1.16→v1.17). (story-writer; closes ADV-RECON5-003.) In-place v1.17 correction (same burst, pre-merge, finalizing v1.17; no version bump; input-hash unchanged 397e06d): ADV-RECON6-001 (stale §BC Status active-AC enumeration `015–024`→`015–027`, reconciling to AC-027; story-wide grep confirmed this was the only live active-AC-range summary needing the fix) and ADV-RECON6-002 (this v1.17 bullet's own scope sentence, and the matching `last_amended:` sentence, falsely claimed only the BC table + Token Budget row were swept v1.20→v1.21 while title/H1/BC Status/AC-020 were 'intentionally left' at v1.20; a story-wide grep proved the body had in fact already been fully swept to v1.21 in the same burst — the narrative above is corrected to describe the sweep actually performed) both closed. (story-writer; closes ADV-RECON6-001, ADV-RECON6-002.)"
  - "2026-08-14 v1.18: ADV-RECON11-001 close (product-owner BC-5.39.010 v1.21→v1.22 architect-adjudicated amendment; story-writer BC-version-cite propagation): BC-5.39.010 v1.22 adds precondition 15b / postcondition 26 (Secondary Index-File UTF-8 Decode Failure, DISTINCT from precondition 15a/postcondition 25's primary-target BLOCK) — non-UTF-8 secondary index-file read (Arm A1 BC-INDEX.md; Arm B1 STORY-INDEX.md only, NOT Arm B2) MUST emit a distinct INDETERMINATE advisory + Continue, MUST NOT fall through into RowAbsent (Arm A1) or (None, None) (Arm B1); new EC-040. Per POLICY 8 canonical scope, BC table + Token Budget row updated v1.21→v1.22 (BC table ACs cell extended to AC-028; Token Budget EC count 39→40); per this story's established full-sweep precedent (v1.15/v1.17), every other live governing-BC-version cite also advanced v1.21→v1.22 (title, H1, Wave Placement Justification, BC Status governing statement + active-AC range, restates-algorithm-text clause, PC2a/PC2b/PC13a/PC13b/PC13c anchors, Fixture rationale boundary, all four AC-020 §Gate Spec anchors, AC-025/026/027 heading + body anchors); only genuinely-historical append-only content (Changelog rows, [Prior:] chain, amendment-identity labels like \"the v1.21 same-field scan-stop\") left at v1.21. New AC-028 added (traces to BC-5.39.010 v1.22 precondition 15b / postcondition 26 / EC-040): asserts a non-UTF-8 secondary INDEX read emits the distinct INDETERMINATE advisory, not a misleading RowAbsent block (Arm A1) or generic \"not yet registered\" advisory (Arm B1); cites mutants `test_BC_5_39_010_arm_a1_non_utf8_bc_index_indeterminate_advisory_not_block` (arm_a1.rs) + `test_BC_5_39_010_arm_b1_non_utf8_story_index_indeterminate_advisory` (arm_b.rs), and pre-existing controls `test_BC_5_39_010_arm_a1_new_v1_0_not_in_index_advisory` (AC-002) + `test_BC_5_39_010_arm_b1_absent_index_sites_advisory` (AC-010). No AC renumbering. input-hash: 397e06d→3885444 (BC-5.39.010.md v1.21→v1.22). STORY-INDEX re-sync owed to state-manager (S-21.07 v1.17→v1.18, BC v1.22, 40 ECs). (story-writer; closes ADV-RECON11-001.)"
  - "2026-08-14 v1.19: F-S2107-RECON-001 close (adversary recon, MEDIUM; POLICY 4 semantic-anchoring correction): AC-019 §Build constraints `max_bytes` cap enumeration mislabeled the STORY-INDEX.md secondary-read cap (`max_bytes = 1048576`, `timeout_ms = 3000`) as \"(B2)\" — this value matches `STORY_INDEX_B1_MAX_BYTES`/`STORY_INDEX_B1_TIMEOUT_MS` in `arm_b.rs` lines 39-40 (Arm B1 secondary-index read), not Arm B2; the adjacent B2 row (`max_bytes = 2097152`, `timeout_ms = 5000`) was already correctly labeled and matches `STORY_INDEX_B2_MAX_BYTES`/`STORY_INDEX_B2_TIMEOUT_MS` (`arm_b.rs` lines 44-45). Fixed the mislabeled row's arm citation to \"(B1 — Arm B1 secondary read)\"; cap VALUE unchanged. Sibling-sweep (TD-VSDD-060) of the full six-row cap enumeration against `arm_b.rs` lines 39-45 and `lib.rs` lines 458-461 confirmed the other five rows are correctly labeled/valued. No AC scope, count, cap VALUE, or BC-version cite changed. input-hash unchanged (no `inputs:` file touched). STORY-INDEX re-sync owed to state-manager (S-21.07 v1.18→v1.19). (story-writer; closes F-S2107-RECON-001.)"
  - "2026-08-14 v1.20: F-S2107-RECON-002 close (adversary recon, MEDIUM; TD-VSDD-091 anti-volatile-pin correction): Task 10's Phase 1 explanatory example cited a stale hard-coded `1.20` for \"this story's own\" Behavioral Contracts table Version cell (which actually reads `1.22`); de-versioned PIN-FREE to \"this story's own governing-BC-version cell\" so the example points at the cell as source of truth instead of asserting a specific number. Comprehensive story-wide sweep of the same defect class found and likewise de-versioned the symmetric Phase 2 Token Budget explanatory example (`BC-5.39.010 v1.22 (full text...)` → \"this story's own governing-BC-version citation in the Token Budget row below\"); no other live illustrative-pointer version pins found (all other `v1.2x` sites are normative BC-anchors already covered by the established full-sweep precedent, worked-example fixture data for other BCs, or amendment-provenance historical labels). BC table Version cell and Token Budget BC row (citation-of-record) confirmed unchanged at `1.22`. No AC scope, count, or normative behavior changed. input-hash unchanged (no `inputs:` file touched). STORY-INDEX re-sync owed to state-manager (S-21.07 v1.19→v1.20). (story-writer; closes F-S2107-RECON-002.)"
---

> **tdd_mode: strict** — Full TDD Iron Law enforced. All non-trivial function bodies use `todo!()`. Red Gate density check >= 0.5 required before Step 4 dispatch (BC-8.30.001).

> **Execute:** `/vsdd-factory:deliver-story S-21.07`

# S-21.07 — validate-cross-site-correspondence WASM hook — six-arm PostToolUse cross-site value-correspondence gate (BC-5.39.010 v1.22 Classes A/B/E; Class D deferred pending convention standardization)

**Epic:** E-21 — Factory State Data-Loss Hardening
**Wave:** 4 (new wave; see Wave Placement Justification below)
**Depends on:** none (`depends_on: []` — fully independent; no E-21 predecessor produces
  artifacts this story consumes)
**Priority:** P1 (recurring adversarial-cascade root cause; 6 consecutive passes 28-30
  showed the same stale-secondary-site failure shape; BC-5.39.010 v1.22 authors this gate)
**Subsystem anchors:**
  - SS-04 (Plugin Ecosystem) owns the new WASM crate
    (`crates/hook-plugins/validate-cross-site-correspondence/`) — SS-04 is the canonical
    home for all hook plugin crates per ARCH-INDEX.
  - SS-05 (Pipeline Orchestration) is the enforcement context — the hook fires inside
    the PostToolUse pipeline that governs factory artifact writes.
**POLICY 21 note:** One new WASM crate. No `.sh` files. POLICY 21 satisfied.

## Wave Placement Justification

Wave 4 is the correct placement, not W2. Three reasons:

1. **W2 is in-flight (mid-cascade).** S-21.04 is currently undergoing adversarial review;
   S-21.05 is unstarted but queued. Adding S-21.07 to W2 requires reopening the W2
   wave-gate scope definition mid-cascade — a process-disruption with no correctness
   benefit, because S-21.07 has no technical dependency on S-21.04 or S-21.05.

2. **W3 resource conflict.** S-21.06 (W3) creates a new WASM crate AND modifies
   `plugins/vsdd-factory/hooks-registry.toml`. S-21.07 also modifies `hooks-registry.toml`.
   Running them in parallel creates a merge conflict on that file even with no formal
   code dependency. W4 (after W3) eliminates this conflict.

3. **W4 provides clean entry.** `depends_on: []` means S-21.07 can begin immediately
   after the W3 wave gate passes. The topological sort places it in W4 with a clean
   in-edge from W3's gate. No story in W4 is yet defined, so the slot is uncontested.

## BC Status

`behavioral_contracts: [BC-5.39.010]` — BC-5.39.010 v1.22 is the governing BC (product-owner;
v1.15–v1.18 amended only the §Gate Spec fuel-cap subsection — see §AC-020 Notes below for the
propagated fuel-cap corrections (F-S2107-P12-001); the PC2/PC13/PC5 clauses summarized in this
section are unchanged since v1.14. v1.14 PC2 directional carve-out (F-P6-001 Option 1): primary-newer-than-index → advisory +
Continue (POLICY 3 burst-ordering artefact; state-manager writes BC-INDEX after BC write);
index-newer-than-primary → retains BLOCK (anomalous; no POLICY 3 explanation). v1.14 PC13
three sub-cases (F-P6-001/ADR-038): B2==B3 AND B1≠B2 → advisory + Continue (PC13a; POLICY 3
artefact; STORY-INDEX internally consistent but not yet propagated); B2≠B3 → retains BLOCK
(PC13b; STORY-INDEX internal inconsistency; no burst-ordering explanation); exactly one of
{B2, B3} present AND differing from B1, other absent → advisory + Continue (PC13c; ADR-038
§Decision 3; half-present case; implementation MUST NOT block for `(Some(b2), None)` or
`(None, Some(b3))`; live instances S-18.11/S-18.12). All three carve-outs mirror PC3/PC12
POLICY 3 ordering rationale. v1.14 PC5 ≥6-field/no-v-token state normatively defined as
RowPresentNoVersion (F-P6-018); PC5 Version(v) extraction uses first-token-of-last-chain-entry
algorithm (ADR-038 §Decision 1): join fields[5..] with `|`, split on `\x00` escape sentinel,
take last non-empty entry, extract first v-token; rightmost-of-field[5] is NON-CONFORMING
(ADR-038 §Empirical Measurement: two of four corpus rows produce incorrect results per v1.14 corpus correction). v1.10 PC5 locator/body-table two-level predicates: locator predicate (conditions (1)+(2)) distinct
from validity predicate (condition (3)); candidate-selection full-file scan must prefer first
(1)+(2)+(3)-satisfying line — first-match-wins implementation is NON-CONFORMING per PC5
(F-S2107-P4-005 fix); RowMalformed redefined as "locator-matched line (conditions (1)+(2)
satisfied) with <5 fields after escape-aware split" — prose contradiction with three-condition
candidacy clause resolved (F-S2107-P4-022); PC4a normative advisory text pinned (MUST-verbatim
with <id>/<N> as only substitutions; omitting "Registration status cannot be determined" or
"Verify BC-INDEX body-table registration manually" is NON-CONFORMING; F-S2107-P4-025 fix);
PC13 prescribed block message MUST cite POLICY 14 leg 3 for modified[] violations
(F-S2107-P4-008 sibling sweep; postconditions 2 and 7 cite leg 5 correctly; postcondition 20
cites leg 4 correctly; gap was postcondition 22 only); PC36 block-scalar MUST:
`extract_frontmatter_field` MUST handle `|-` block scalars; returning `"|-"` is NON-CONFORMING
(corpus: 2 occurrences — BC-5.39.010 itself + S-21.07 story; F-S2107-P4-004 coupling);
PC40 transitional clause updated: S-21.07 absent from ADR-037 §Context 19-story table (corrected
to 78 stories in ADR-037 v1.1; F-S2107-P4-013 BC-side fix); story-side fix removes ARCH-INDEX.md
from inputs:; vacuous condition depends on exhaustive §Context enumeration; Story Anchor and
§Traceability Stories TBD→S-21.07; Gate Spec pseudocode updated for two-level predicates;
Architecture Anchor `extract_bc_index_version` terminology updated; `extract_frontmatter_field`
block-scalar requirement added to Architecture Anchors; v1.9 PC5 fourth state `RowMalformed`:
a locator-matched line was found but has <5 non-empty fields after escape-aware splitting —
NOT a valid body-table row (likely a Changelog entry or notes table that incidentally carries
the BC ID link); disposition: advisory+Continue; NEVER reaches postcondition 4 blocking path
(distinct from `RowAbsent` which means no locator-matched line at all; postcondition 4a added
prescribing advisory message text); v1.8 PC5 column-anchored locator: escape-aware column count
(5 fields → `RowPresentNoVersion` unconditionally; 6+ → `Version(v)` from 6th column) + two-phase
PC13 (Phase 1 pure-version `^v?[0-9]+\.[0-9]+$`; Phase 2 mandatory-v inline
`\bv([0-9]+\.[0-9]+)\b`; optional-v excluded — story-ID collision 194/1943 rows + BC-section
collision + ACs-column collision per corpus check 2026-08-04); v1.7 corrected PC5 —
`extract_bc_index_version` rearchitected to three-state `RowAbsent`/`RowPresentNoVersion`/
`Version(v)`: prior two-state `Option<String>` conflating absent-row and no-version-cell into
a single `None` produced ≥1,712 false BLOCKs per F-S2107-P3-001 corpus analysis;
`RowPresentNoVersion` is canonical normal state for ~98% of BC-INDEX rows; postcondition 4
expanded to silent-continue on `RowPresentNoVersion`; invariant 11 updated to three-category
taxonomy stale/fabricated/algorithm-divergent; `1acf3c6` reclassified ALGORITHM-DIVERGENT per
ADR-036 §Decision 4 — Pass-30 M02 POLICY 18 violation claim retracted; Gate Spec
`run_part_a_arm1` pseudocode updated to three-state match; v1.6 descoped Class D entirely —
active gated classes are A, B, E only; `is_cycle_artifact` dispatch branch marked DEFERRED;
`.factory/cycles/` removed from registry `path_allow` pending Closes/Refs convention
standardization (follow-up story: S-21.08); v1.5 corrected PC31 Closes/Refs regex and added
PC40 volatile-input precondition (EC-032); v1.4 corrected PC13 bounding-section predicates to
prefix-with-word-boundary; v1.3 added PC13 scan confinement + dual version-token regex + LAST
rightmost pipe-field algorithm + non-decreasing E2 relation + EC-030/031; v1.2 corrected the
§Gate Spec `tool` field to include MultiEdit per POLICY 13 ESCAPE-SCOPE-PARITY).
ACs 001–011, 015–028 are active (AC-022 and AC-023 added in v1.6 to cover PC2a and PC13a
advisory paths; AC-024 added in v1.8 for PC13c half-present; enumeration corrected to 015–024 at v1.9; AC-025/AC-026 added in v1.15 for PC13 Phase 1 BC-ID anchoring and the non-UTF-8 primary-target block; AC-027 added in v1.17 for the PC13 Phase 2 same-field scan-stop; enumeration corrected to 015–027 at v1.17 (ADV-RECON6-001); AC-028 added in v1.18 for precondition 15b / postcondition 26, the secondary index-file UTF-8 decode advisory; enumeration corrected to 015–028 at v1.18); AC-012/013/014 (Class D) are
DEFERRED v1.6 — see §Deferred Scope in BC-5.39.010. All ACs trace explicitly to BC-5.39.010 preconditions, postconditions, or
invariants by ID.
**Datum-copy ruling (BC-5.39.010 §Postconditions Part A Arm2):** BC-version cells in
story bodies carry datum-copy semantics — they must match the BC's current frontmatter
`version:` field at the time of any story edit. Arm A2's blocking of present-but-stale
citations is correct as specified; no AC amendment is warranted by this ruling. Per
S-7.01, status remains `draft` because VP IDs are pending VP-INDEX allocation by
state-manager at post-merge burst. Status may transition to `ready` once: (1) BC-5.39.010
is committed and indexed; (2) VP IDs are allocated and written into
`verification_properties:` frontmatter; (3) every AC↔BC bidirectional trace is present
(verified: each AC below cites a BC-5.39.010 clause; each of the 6 blocking arms
(A1, A2, B1, B2, E1, E2) is covered by at least one blocking AC).

**`anchored_adrs:` scope rationale (ADV-RECON10-001 close, LOW).** `anchored_adrs:
[ADR-035]` and `inputs:` list only ADR-035 and BC-5.39.010 (plus SS-04/SS-05 architecture
shards and the hook-sdk source files) — not ADR-038 or ADR-042, even though the
implemented algorithm depends substantively on ADR-038 §Decision 1/5
(first-token-of-last-chain-entry; two-phase PC13 BC-ID-anchored extraction) and ADR-042
(20,000,000 fuel-cap framing, `fuel_exhausted` signaling contract). This is intentional,
not an omission, because BC-5.39.010 v1.22 **restates the governing algorithm text
itself** rather than merely pointing at the ADRs:
- §Preconditions PC13 spells out the full
  two-phase algorithm in the BC's own normative prose — Phase 1 BC-ID-anchored
  pure-version field, Phase 2 BC-ID-anchored inline v-token with the v1.21 same-field
  scan-stop, and the `first-token-of-last-chain-entry` join-fields[5..]/split-on-`\x00`
  rule — with ADR-038 §Decision 1/5 cited only as *provenance* for why the rule is
  correct, not as the sole source of the rule's text.
- §Gate Specifications (BC-5.39.010 §Fuel-cap subsection) spells out the 20,000,000
  source-HEAD cap, the operator-effective-vs-source-HEAD distinction, the
  `fuel_exhausted=true fuel_cap=<N>` stderr/JSON signaling contract, and the
  `fuel_consumed ≤ 12,000,000` margin-gate assertion directly in the BC's own text, again
  citing ADR-042 §Decision 1/2 as rationale/provenance rather than as the normative
  source.
- Confirmed against the implementation: `crates/hook-plugins/validate-cross-site-correspondence/src/{lib.rs,arm_a2.rs,arm_b.rs,arm_e.rs}`
  anchor every doc-comment and test name to `BC-5.39.010` clause IDs (preconditions,
  postconditions, invariants, PC13/PC5/PC26/PC34/PC40, etc.); ADR-038/ADR-042 appear only
  as secondary rationale annotations alongside a BC clause cite (e.g. `arm_b.rs`: "ADR-038
  §Decision 3: half-present is ALWAYS advisory + Continue per PC12"), never as the sole
  citation for a behavioral requirement.

Because the algorithm text and the fuel-cap framing are both fully self-contained in
BC-5.39.010's own body, a future change to what the code must DO requires a BC-5.39.010
version bump (§Preconditions PC13 or §Gate Specifications edit) — and BC-5.39.010 is
already an `inputs:` member driving this story's `input-hash`. A change to ADR-038 or
ADR-042 alone, without a corresponding BC-5.39.010 edit, would not change what
BC-5.39.010 requires or what the implementation must conform to (per this project's
spec-wins-over-code / BC-is-governing-contract convention — the code cites "BC-5.39.010
§PC13/§PC5", never "ADR-038" or "ADR-042", as its behavioral authority) — so the
input-hash coverage gap the adversary flagged does not create a real drift risk. ADR-035
remains the sole `anchored_adrs:` entry because it is the primary architectural anchor
(three-tier validation architecture, pure-core/effectful-shell module split, Class
A/B/E arm boundaries) that this story's file/module structure is organized around;
ADR-038 and ADR-042 are narrower algorithm-refinement and fuel-budget ADRs whose
decisions are fully absorbed into the governing BC and therefore do not need a separate
`anchored_adrs:` entry. **No `inputs:`/`input-hash` change and no version bump accompany
this clarification** (Option B per the ADV-RECON10-001 resolution criteria) — this is
an in-place v1.17 documentation addition.

## Narrative

- **As a** factory pipeline agent writing BC, story, or cycle-artifact files
- **I want** a PostToolUse hook to fire after each Edit/Write on governed paths and
  compare cross-site value pairs (BC frontmatter version vs BC-INDEX row, story BC-citation
  version vs BC frontmatter, story input-hash vs STORY-INDEX catalog and blockquote,
  cycle-artifact finding-ID namespace, frontmatter version vs last_amended text-prefix,
  and modified[] date monotonicity)
- **So that** stale secondary sites — the root cause of every Class A/B/E finding in
  adversarial passes 28-30 — are caught before the burst is committed, not in the
  adversary's next review pass

## Acceptance Criteria

### AC-001: Class A Arm1 — index-newer-than-primary blocks (traces to BC-5.39.010 postcondition 2b)

When a BC file is written with `version: "1.5"` and the BC-INDEX.md body table row for
that BC ID says `v1.6` (index newer than primary), the hook MUST return
`HookResult::block_with_fix(...)` with message text matching
`"validate-cross-site-correspondence [Class A Arm1]:"`, citing the index version, the
frontmatter version, the phrase `"index is newer than primary"`, and `"POLICY 14 leg 5"`.

Under BC-5.39.010 v1.22 PC2b (unchanged since v1.14): when index_version > fm_version (numerically, comparing
major.minor as decimal integers), the direction is anomalous — no POLICY 3 burst-ordering
argument explains the INDEX carrying a higher version than the BC file itself. The hook
MUST block. (The opposite direction — primary newer than index — is PC2a; see AC-022.)

- **Mutant test (RED before impl):** `test_BC_5_39_010_arm_a1_index_newer_blocks` —
  fixture BC file `version: "1.5"`; INDEX row `v1.6` → assert exit code 2, message
  contains `[Class A Arm1]`, `v1.6`, `1.5`, `"index is newer than primary"`,
  `POLICY 14 leg 5`.
- **Control test (GREEN before impl):** `test_BC_5_39_010_arm_a1_current_index_passes` —
  same fixture but INDEX row `v1.5` (matches frontmatter) → assert exit code 0, no block.

### AC-002: Class A Arm1 — new v1.0 BC not yet in INDEX is advisory-only (traces to BC-5.39.010 postcondition 3)

When a BC file is written with `version: "1.0"` and BC-INDEX.md has no row for that BC ID,
the hook MUST emit `host::log_warn` advisory and return `HookResult::Continue` (exit 0).
When the same BC has `version: "1.1"` (> 1.0) and its ID is absent from BC-INDEX.md,
the hook MUST block (postcondition 4 — v > 1.0 means previous registration existed).

- **Test:** `test_BC_5_39_010_arm_a1_new_v1_0_not_in_index_advisory` — `version: "1.0"`,
  no INDEX row → exit 0, warn emitted. `test_BC_5_39_010_arm_a1_v1_1_not_in_index_blocks` —
  `version: "1.1"`, no INDEX row → exit 2 (postcondition 4).

### AC-003: Class A Arm1 — BC-INDEX.md CapabilityDenied blocks (traces to BC-5.39.010 precondition 8, invariant 5)

When `host::read_file` for BC-INDEX.md returns `HostError::CapabilityDenied`, the hook
MUST block: sandbox misconfiguration on a secondary target is never a legitimate state
(BC-5.39.010 invariant 5). `HostError::NotFound` on BC-INDEX.md is advisory + Continue
(bootstrap ordering; precondition 8 second clause).

- **Test:** `test_BC_5_39_010_arm_a1_bc_index_capability_denied_blocks` — mock
  `CapabilityDenied` on BC-INDEX path → exit 2.
  `test_BC_5_39_010_arm_a1_bc_index_not_found_advisory` — mock `NotFound` → exit 0, warn.

### AC-004: Class A Arm1 — primary target (BC file) read failure blocks (traces to BC-5.39.010 precondition 7, invariant 4)

When `host::read_file` for the BC file itself (the PostToolUse trigger file) returns any
`HostError`, the hook MUST block (`HookResult::block_with_fix(...)`). No fail-open path
exists for the primary target (BC-5.39.010 invariant 4; BC-5.39.008 v1.6 fail-closed
ruling).

- **Test:** `test_BC_5_39_010_arm_a1_primary_target_capability_denied_blocks` — trigger
  file `HostError::CapabilityDenied` → exit 2.

### AC-005: Class A Arm2 — stale story BC-table version citation blocks (traces to BC-5.39.010 precondition 9-13, postcondition 7)

When a story file is written containing `behavioral_contracts: [BC-6.26.001]` and a
Token Budget table row that contains `BC-6.26.001` and `v1.17`, but BC-6.26.001's
frontmatter has `version: "1.18"`, the hook MUST block with message text matching
`"validate-cross-site-correspondence [Class A Arm2]:"` and citing the story ID, the BC ID,
the stale-cited version, and the current BC frontmatter version.

The BC path is derived deterministically: `BC-6.26.001` → section `6` → `ss-06` →
`.factory/specs/behavioral-contracts/ss-06/BC-6.26.001.md`. No `list_dir` or filesystem
enumeration. (BC-5.39.010 precondition 11 `derive_bc_path` algorithm.)

- **Mutant test:** `test_BC_5_39_010_arm_a2_stale_token_budget_row_blocks` — story cites
  `v1.17` in Token Budget row; BC frontmatter `1.18` → exit 2, `[Class A Arm2]`,
  `v1.17`, `1.18`.
- **Control test:** `test_BC_5_39_010_arm_a2_current_citation_passes` — both `v1.18` →
  exit 0.

### AC-006: Class A Arm2 — cascade: two stale BCs produce single combined block (traces to BC-5.39.010 postcondition 7 last sentence)

When a story cites two BCs in `behavioral_contracts:` and both have stale version tokens
in table rows, all violations MUST be reported in ONE combined `block_with_fix` message.
The hook does NOT stop on the first stale BC (postcondition 7 "cascade").

- **Test:** `test_BC_5_39_010_arm_a2_two_stale_bcs_combined_block` — two BCs, each with
  a stale cite → exit 2, message references both BC IDs.

### AC-007: Class A Arm2 — empty behavioral_contracts skips Arm2 entirely (traces to BC-5.39.010 precondition 10, postcondition 9)

When a story file has `behavioral_contracts:` absent or empty, Arm A2 MUST skip
(`HookResult::Continue`) without attempting any BC file reads.

- **Test:** `test_BC_5_39_010_arm_a2_empty_bcs_skips` — story frontmatter `behavioral_contracts: []`
  → exit 0, no BC file reads attempted.

### AC-008: Class A Arm2 — no version-citing table row for a BC: skip that BC (traces to BC-5.39.010 postcondition 8)

When a story's `behavioral_contracts: [BC-X.YY.ZZZ]` list includes a BC whose ID appears
in the story body only in prose text (no table row containing both the BC ID and a
`v\d+\.\d+` version token), the hook MUST skip that BC and return `HookResult::Continue`
for it. The hook does NOT block on missing citations; only on present-but-stale citations.

- **Test:** `test_BC_5_39_010_arm_a2_no_version_row_skips` — BC ID present in narrative
  only, no `| … BC-X.YY.ZZZ … v1.N …|` row → exit 0.
- **Test (advisory for NotFound BC):** `test_BC_5_39_010_arm_a2_bc_not_found_advisory` —
  BC path resolves to `HostError::NotFound` → advisory warn, exit 0 (postcondition 10
  first clause).

### AC-009: Class B Arm1 — STORY-INDEX internal inconsistency blocks (traces to BC-5.39.010 preconditions 16-21, postcondition 13b)

When a story file is written with `input-hash: "47a65c9"` in frontmatter (B1), the
STORY-INDEX.md catalog row for that story contains `input-hash 4be9d21` (B2), and the
STORY-INDEX.md blockquote contains `S-21.07=c3f9811` (B3), where B2 ≠ B3, the hook MUST
block with message text matching `"validate-cross-site-correspondence [Class B]:"`,
citing the three site values and `"POLICY 18 (D-923)"`. The block message MUST enumerate
all three provenance categories per BC-5.39.010 invariant 11 WITHOUT asserting which
applies (classifying one is NON-CONFORMING per F-S2107-P4-006):
stale (previously valid computed value; inputs changed after authoring; remedy:
`compute-input-hash --update`); fabricated (never the output of `compute-input-hash --update`
at any revision; remedy: PROVENANCE-BREAK acknowledgment in burst-log before recompute);
algorithm-divergent (legitimately computed by a prior binary version per ADR-036 §Decision 4;
NOT fabricated; remedy: recompute with current authoritative binary, no PROVENANCE-BREAK
annotation required).

Under BC-5.39.010 v1.22 PC13b (unchanged since v1.14): B2 ≠ B3 → STORY-INDEX catalog and blockquote disagree
internally → anomalous (no POLICY 3 burst-ordering explanation) → BLOCK, regardless of B1.
(B2 == B3 AND B1 ≠ B2 is PC13a — advisory only; see AC-023.)

- **Mutant test:** `test_BC_5_39_010_arm_b1_b2_ne_b3_blocks` — B1=47a65c9,
  B2=4be9d21, B3=c3f9811 (B2 ≠ B3) → exit 2, `[Class B]`, `POLICY 18 (D-923)`,
  provenance category enumeration (all three; no classification assertion).
- **Control test:** `test_BC_5_39_010_arm_b1_hash_match_passes` — all three sites agree
  → exit 0.

### AC-010: Class B Arm1 — absent secondary sites are advisory-only (traces to BC-5.39.010 postcondition 12)

When a story has `input-hash:` populated but B2 (STORY-INDEX.md catalog row token) or B3
(STORY-INDEX.md aggregation blockquote entry) are absent, the hook MUST emit
`host::log_warn` advisory and return `HookResult::Continue`. Absence is the expected
state when a story is newly authored before STORY-INDEX.md is updated in the same burst.

- **Test:** `test_BC_5_39_010_arm_b1_absent_index_sites_advisory` — B1=47a65c9, B2 absent,
  B3 absent → exit 0, warn.
- **Test:** `test_BC_5_39_010_arm_b1_no_input_hash_skips` — story frontmatter has no
  `input-hash:` field → exit 0, Arm B1 skips entirely (precondition 18 second clause).

### AC-011: Class B Arm2 — STORY-INDEX.md internal catalog vs blockquote mismatch blocks (traces to BC-5.39.010 preconditions 22-25, postcondition 15)

When STORY-INDEX.md is written and the aggregation blockquote contains
`S-21.07=47a65c9` but the catalog row for S-21.07 has `input-hash 4be9d21`, the hook
MUST block reporting ALL mismatching stories in ONE combined message (cascade — postcondition
15). `STORY-INDEX.md` IS the primary target for Arm B2; any `HostError` on its read blocks.

- **Mutant test:** `test_BC_5_39_010_arm_b2_catalog_blockquote_mismatch_blocks` —
  blockquote `47a65c9`, catalog `4be9d21` → exit 2, `[Class B]`.
- **Control test:** `test_BC_5_39_010_arm_b2_catalog_blockquote_agree_passes` — both same
  hash → exit 0.

### AC-012: [DEFERRED v1.6 — Class D] Known-safe namespace tokens do not trigger advisory (traces to BC-5.39.010 precondition 32, postcondition 16)

> **[DEFERRED v1.6 — Class D]** This AC is deferred pending Closes/Refs convention
> standardization (BC-5.39.010 v1.6 §Deferred Scope). The `is_cycle_artifact` dispatch
> branch MUST NOT be compiled into the v1.6 hook. AC ID preserved per POLICY 1 append-only.
> Implementation target: S-21.08.

On a `burst-log.md`, `lessons.md`, or `INDEX.md` write (under `cycles/` component), tokens
from the known-safe namespace exclusion list MUST NOT produce advisories. Excluded prefixes
per BC-5.39.010 precondition 32: `D-`, `S-`, `BC-`, `VP-`, `R-`, `L-`, `ADR-`, `EC-`,
`NFR-`, `ASM-`, `FM-`. Example: `"Closes: D-944, S-21.03"` → Continue, no advisory.

- **Test:** `test_BC_5_39_010_class_d_excluded_namespace_d944_passes` — `Closes: D-944` in
  last H2 of burst-log → exit 0, no advisory.
- **Test:** `test_BC_5_39_010_class_d_excluded_namespace_s_bc_vp_passes` — tokens starting
  with S-, BC-, VP- in Refs → exit 0.

### AC-013: [DEFERRED v1.6 — Class D] Non-F- finding-like token triggers advisory, not block (traces to BC-5.39.010 postcondition 17; invariant 6)

> **[DEFERRED v1.6 — Class D]** This AC is deferred pending Closes/Refs convention
> standardization (BC-5.39.010 v1.6 §Deferred Scope). AC ID preserved per POLICY 1.
> Implementation target: S-21.08.

A token that (a) matches shape `[A-Za-z][A-Za-z0-9-]*[0-9]+` (starts with letter, ends
with digit), (b) is NOT in the exclusion list, and (c) does NOT start with `F-` MUST
produce `host::log_warn` advisory per token and `HookResult::Continue` (NOT block).
Class D is advisory-only, never blocking (BC-5.39.010 invariant 6).

The advisory message MUST match: `"validate-cross-site-correspondence [Class D] advisory:
non-canonical finding-ID token '<token>' on line '<line>' in <section> of <file>. Finding
IDs must start with 'F-'. Verify this is not a phantom ID or retracted reference."`.

**Gap — Class D format-only limitation (EXPLICITLY OUT OF SCOPE):** Class D Arm checks
syntactic format (does the token start with `F-`?) only. It does NOT check whether a
well-formed `F-<prefix>-<seq>` token actually names an existing finding in any adversary
pass record. A fabricated ID such as `F-S2104-P99-H99` passes Class D cleanly. This is
the exact failure shape from passes 29-30 (e.g., `F-P4-002` in burst-log Closes lines
referencing a re-labeled finding — the format was correct, the semantic existence was not
verified). Semantic existence validation requires corpus enumeration and is routed to a
Rust workspace integration test per BC-5.39.010 §Honest Gap + ADR-035 §Decision 4. This
story does NOT implement that check and MUST NOT be read as having done so.

- **Mutant test:** `test_BC_5_39_010_class_d_non_f_token_b01_advisory` — `Closes: B01,
  F-S2104-P29-H01` in scoped region → advisory for `B01`, exit 0 (not exit 2).
- **Control test:** `test_BC_5_39_010_class_d_all_f_prefix_passes` — only `F-` tokens in
  Closes/Refs → exit 0, no advisory.

### AC-014: [DEFERRED v1.6 — Class D] Scope-limited extraction excludes frozen-provenance regions (traces to BC-5.39.010 precondition 30)

> **[DEFERRED v1.6 — Class D]** This AC is deferred pending Closes/Refs convention
> standardization (BC-5.39.010 v1.6 §Deferred Scope). AC ID preserved per POLICY 1.
> Implementation target: S-21.08.

The hook MUST extract ONLY the scope-limited region of each cycle artifact:
- `burst-log.md`: last H2 section (from last `^## ` heading through EOF)
- `lessons.md`: last `^L-EDP1-[0-9]+-[0-9]+:` anchor block; if absent, last 200 lines
- `INDEX.md`: `## Adversarial Reviews` section only

Historical content in earlier sections MUST NOT be scanned. This exclusion is by
STRUCTURAL POSITION (last section only), not by pattern-matching. An over-broad exclusion
pattern is an escape channel — using, for example, a `## Changelog` section pattern instead
of positional anchoring would allow non-F- tokens in the scoped region to evade the check.
(BC-5.39.010 precondition 30; lessons from pass-29 `F-S2104-P29-H02`.)

- **Test:** `test_BC_5_39_010_class_d_historical_section_excluded` — fixture `lessons.md`
  with old `L-EDP1-001` block containing `Closes: P45-001` (non-F- shape) and a NEW
  `L-EDP1-062` block with clean `Closes: F-xxx` lines — only the new block scanned;
  `P45-001` NOT flagged → exit 0.

### AC-015: Class E1 — frontmatter version vs last_amended text-prefix mismatch blocks (traces to BC-5.39.010 preconditions 34-38, postcondition 20)

When a BC, VP, or story file is written with `version: "1.33"` and
`last_amended: "2026-07-29 (v1.31) — ..."`, the hook MUST block with message text matching
`"validate-cross-site-correspondence [Class E1]:"` citing both version values and
`"POLICY 14 leg 4 / POLICY 17"`. The outermost version is extracted by regex
`^\d{4}-\d{2}-\d{2}\s+\(v([0-9]+(?:\.[0-9]+)*)\)` anchored at CHARACTER POSITION 0
of the field value (BC-5.39.010 precondition 37).

If the regex fails to match (e.g., `last_amended:` has an unparseable format), the hook
MUST emit `host::log_warn` advisory and return `HookResult::Continue` — do NOT block on
unparseable format (precondition 37 last sentence).

- **Mutant test:** `test_BC_5_39_010_class_e1_version_mismatch_blocks` — `version: "1.33"`,
  `last_amended: "2026-07-29 (v1.31) — ..."` → exit 2, `[Class E1]`.
- **Control test:** `test_BC_5_39_010_class_e1_version_match_passes` — `version: "1.6"`,
  `last_amended: "2026-07-29 (v1.6) — ..."` → exit 0.
- **Test:** `test_BC_5_39_010_class_e1_unparseable_last_amended_advisory` — `last_amended:`
  does not match regex → exit 0, warn (not block).

### AC-016: Class E2 — strict-decrease in modified[] blocks; equal consecutive dates pass (traces to BC-5.39.010 precondition 38, postconditions 21-22)

When a BC file is written with `modified: ["2026-05-14", "2026-05-18 (v1.1)", "2026-05-15"]`,
the hook MUST block on the first pair where `date[i] > date[i+1]` after stripping annotation
suffixes. `"2026-05-15"` follows `"2026-05-18 (v1.1)"` → `"2026-05-15"` < `"2026-05-18"` →
strict-decrease → block. Equal consecutive dates (e.g., `["2026-07-30", "2026-07-30 (v1.1)"]`)
satisfy `date[i] ≤ date[i+1]` and MUST NOT block (BC-5.39.010 postcondition 21, EC-030).

- **Mutant test:** `test_BC_5_39_010_class_e2_non_monotonic_blocks` — modified[] with
  strict-decrease entry → exit 2, `[Class E2]`.
- **Control test:** `test_BC_5_39_010_class_e2_ascending_passes` — dates non-decreasing
  after suffix strip (including equal consecutive dates) → exit 0.

### AC-017: Class E — Prior-chain versions in last_amended excluded by positional anchor (traces to BC-5.39.010 precondition 37, EC-018)

When `last_amended:` is `"2026-07-30 (v1.6) — Active text. [Prior: 2026-07-01 (v1.5) — ...]"`,
the hook MUST match ONLY the outermost `(v1.6)` at CHARACTER POSITION 0 of the field
value. The nested `(v1.5)` inside the `[Prior:` chain is excluded STRUCTURALLY by the
positional anchor (character position 0 of the field), NOT by a `[Prior:` pattern match.

- **Test:** `test_BC_5_39_010_class_e1_prior_chain_excluded` — `version: "1.6"`;
  `last_amended: "2026-07-30 (v1.6) — ... [Prior: 2026-07-01 (v1.5) — ...]"` → exit 0
  (outermost v1.6 matches version 1.6; prior chain ignored).

### AC-018: Combined violations across arms produce a single block (traces to BC-5.39.010 invariant 7, postcondition 23)

When a single file write triggers violations in multiple arms simultaneously — specifically
a BC file written with `version: "1.5"`, INDEX row `v1.6` (index-newer-than-primary per
PC2b → A1 block), AND `last_amended: "2026-07-29 (v1.33)"` (E1 mismatch: version 1.5 ≠
last_amended v1.33 → E1 block) — both Class A Arm1 and Class E1 fire, and the hook MUST
return ONE combined `HookResult::block_with_fix(...)` enumerating all violations. Part D
advisories are logged via `host::log_warn` regardless of block state. Arms MUST NOT
suppress each other (invariant 7).

**Fixture rationale (v1.14; PC2b/E1 content unchanged through v1.22):** the A1 component uses index-newer-than-primary (PC2b →
block). The prior v1.10 fixture (BC v1.33, INDEX v1.5 — primary-newer direction) would
produce a PC2a advisory from A1 and an E1 block; the combined-violations path requires
both arms to independently produce blocks to confirm Arms do not suppress each other.
PC2b (index-newer) + E1 mismatch ensures both arms block.

- **Test:** `test_BC_5_39_010_combined_a1_and_e1_single_block` — BC file `version: "1.5"`,
  INDEX row `v1.6`, `last_amended: "2026-07-29 (v1.33)"` → exit 2, single block message
  references both `[Class A Arm1]` and `[Class E1]`.

### AC-019: Build constraints — release WASM only; serde_norway; max_bytes caps; UTF-8 safety (traces to BC-5.39.010 invariant 9; ADR-035 §Decision 5)

**Release build requirement:** The hook MUST be compiled with `cargo build --release
--target wasm32-wasip1 -p validate-cross-site-correspondence`. Debug builds exhaust the
operator-effective fuel cap (10,000,000 instructions through v1.0.0-rc.23; source-HEAD
raised `DEFAULT_FUEL_CAP` to 20,000,000 per ADR-042 §Decision 1 / PR #774, but that raise
is not yet operator-effective for this hook's own live PostToolUse enforcement — neither
"10M" nor "20M" alone is accurate without this source-vs-operator qualifier; BC-5.39.010
v1.22 §Gate Spec, F-S2107-P10-004) during `serde_json` deserialization of the dispatch
payload — `on_error = "continue"` means the hook silently produces no findings,
indistinguishable from a clean pass. A debug WASM binary is a broken gate regardless of
which cap governs. The CI step that stages hook binaries MUST use the `--release` flag.
(ADR-035 §Decision 5.)

**`serde_norway` not `serde_yaml`:** The workspace dependency is `serde_norway = "0.9"`.
`serde_yaml` was archived and deprecated in 2024; the workspace migrated in 2026-05 via
TD #72. Any `serde_yaml` import MUST fail to compile via `#[cfg(not)]` deny or absence
from `Cargo.toml`. The crate MUST use `serde_norway` for any YAML deserialization needs.
(YAML is not required for this hook — frontmatter is parsed via line-scanning regex as in
existing hook plugins — but the constraint is recorded to block inadvertent reintroduction.)

**`max_bytes` caps:** `host::read_file` calls MUST use the exact caps from
BC-5.39.010 preconditions:
- BC files (primary and BC cited in story): `max_bytes = 524288`, `timeout_ms = 3000`
- BC-INDEX.md: `max_bytes = 1048576`, `timeout_ms = 3000`
- Story files: `max_bytes = 524288`, `timeout_ms = 3000`
- STORY-INDEX.md (B1 — Arm B1 secondary read): `max_bytes = 1048576`, `timeout_ms = 3000`
- STORY-INDEX.md (B2 — Arm B2 write trigger): `max_bytes = 2097152`, `timeout_ms = 5000`
- Cycle artifacts (D arm): `max_bytes = 2097152`, `timeout_ms = 5000` — **[DEFERRED v1.6 — Class D]** Class D arm not compiled in v1.6 hook

**UTF-8 safety:** All byte-index slicing on strings extracted from file content MUST use
`str::is_char_boundary()` checks where multi-byte UTF-8 is possible
(BC-5.39.010 invariant 9; BC-5.39.008 v1.6 inv-11).

- **Test:** `test_BC_5_39_010_multibyte_utf8_no_panic` — fixture content with multi-byte
  UTF-8 characters in BC title or frontmatter values → no panic; clean exit.

### AC-020: Registry entry with correct fields (traces to BC-5.39.010 v1.22 §Gate Spec)

The hook MUST be registered in `plugins/vsdd-factory/hooks-registry.toml` with:

```toml
[[hooks]]
name = "validate-cross-site-correspondence"
event = "PostToolUse"
tool = "^(Edit|Write|MultiEdit)$"
plugin = "hook-plugins/validate-cross-site-correspondence.wasm"
priority = 460
timeout_ms = 8000
on_error = "continue"
async = false

[hooks.capabilities.read_file]
path_allow = [
  ".factory/specs/behavioral-contracts/",
  ".factory/specs/verification-properties/",
  ".factory/stories/",
  # ".factory/cycles/" — DEFERRED v1.6: Class D (cycle artifact arm) deferred pending
  # Closes/Refs convention standardization. Re-add when S-21.08 implements Class D.
]
```

Notes on field choices:
- `priority = 460`: next available slot after `validate-wave-handoff-completeness` (450);
  PostToolUse tier ordered ascending.
- `on_error = "continue"`: fuel exhaustion (ADR-035 §Decision 5) and crash → silent
  non-finding, not a spurious block. **The prior claim that the `max_bytes` caps "are
  calibrated to keep reads within the fuel budget" is RETRACTED as FALSE (BC-5.39.010
  v1.15, F-S2107-P9-002; propagated here per F-S2107-P12-001).** Measured fuel
  consumption against a production-scale BC-INDEX fixture (576,396 bytes / 1,985 rows)
  was `fuel_consumed = 9,920,913` against the then-operative 10,000,000-instruction cap —
  99.21% consumed, 79,087 fuel of headroom (0.79%). The 1 MiB `max_bytes` cap on PC4
  permits a file of 1,048,576 bytes, which is ~76% *above* the ~594 KB point at which the
  former 10M cap actually exhausted — the caps bounded nothing; they simply had not yet
  been exercised by a large-enough corpus. See the `fuel_cap` bullet below for the
  current cap figure and its source-HEAD-vs-operator-effective distinction (BC-5.39.010
  v1.22 §Gate Spec, F-S2107-P10-004).
- `tool = "^(Edit|Write|MultiEdit)$"`: aligns with all 41 Edit/Write hooks in the live
  registry. BC-5.39.010 v1.1 §Gate Spec had `tools = ["Edit", "Write"]` (array, field
  name plural, MultiEdit omitted) — a POLICY 13 ESCAPE-SCOPE-PARITY gap: a MultiEdit on
  any governed path would have bypassed every arm. BC-5.39.010 v1.2 corrected this to the
  regex `tool = "^(Edit|Write|MultiEdit)$"`. This AC traces to the BC-5.39.010 v1.22 §Gate Spec.
- `fuel_cap` field — **prohibition LIFTED (BC-5.39.010 v1.16 §Gate Spec, ADR-042
  §Decision 2; corroborated ARCH-INDEX ADR-042 §Decision 2 and ADR-035 v1.1).** The prior
  v1.14-era rule that this hook "MUST NOT include a `fuel_cap` registry field" is
  retracted: its premise — that the existing `max_bytes` caps keep reads inside the fuel
  budget — is measurement-falsified (see the `on_error` bullet above). The registry entry
  in this AC still omits `fuel_cap` today, but omission is no longer a spec prohibition —
  it is a "not yet" state: a per-plugin `fuel_cap` **SHOULD** be set for this hook once
  ADR-039 Phase 1 (registry `fuel_cap` schema extension, story S-21.10) ships, calibrated
  to p99×1.5 per ADR-039 §Decision 4 Option A (minimum 50M for Phase 3 fail-closed
  annotation). ADR-039 Phase 1 is unshipped at the operator level as of this propagation
  (`grep -c 'fuel_cap|failure_policy' hooks-registry.toml` → 0); until then the only
  available lever is the global cap (next bullet).
- Global fuel cap (BC-5.39.010 v1.22 §Gate Spec, F-S2107-P10-004): `DEFAULT_FUEL_CAP` is
  **20,000,000 at source-HEAD** — raised from 10,000,000 by PR #774 (commit `62fbcf1a`,
  ADR-042 §Decision 1; `crates/factory-dispatcher/src/invoke.rs`). **This is not yet the
  operator-effective cap** for this hook's own live PostToolUse enforcement: the
  marketplace-cached `factory-dispatcher` binary this project's hooks actually run
  remains pinned to the pre-raise **10,000,000** cap through v1.0.0-rc.23, and only
  becomes 20,000,000-effective once v1.0.0-rc.24 is cut and the marketplace cache is
  refreshed (CLAUDE.md fuel-exhaustion diagnostics row). Do not cite a single flat cap
  figure for this hook without this source-vs-operator qualifier — neither "10M" nor
  "20M" alone is accurate; state both with which one governs at build-time vs. at
  live-operator-enforcement-time.
- `path_allow`: covers all four directory subtrees the hook reads from.

Verify with: `grep -A 20 'name = "validate-cross-site-correspondence"'
plugins/vsdd-factory/hooks-registry.toml` — all 8 fields must be present.

### AC-021: Pre-release live-session smoke test (practical gate; cites BC-5.39.010 invariant 10 + POLICY 21)

Because a new WASM hook plugin is inert at the operator level until an rc ships (the
marketplace-tarball cache `~/.claude/plugins/cache/claude-mp/vsdd-factory/1.0.0-rc.23/`
holds 73 compiled plugins while 75 are in source — verified by S-19.04 precedent), the
implementer MUST perform a live-session smoke test before the PR is filed:

1. **Build release WASM:**
   ```
   cargo build --release --target wasm32-wasip1 -p validate-cross-site-correspondence
   ```
   Artifact: `target/wasm32-wasip1/release/validate_cross_site_correspondence.wasm`

2. **Stage into operator cache:**
   ```
   cp target/wasm32-wasip1/release/validate_cross_site_correspondence.wasm \
     ~/.claude/plugins/cache/claude-mp/vsdd-factory/1.0.0-rc.23/hook-plugins/validate-cross-site-correspondence.wasm
   ```

3. **Add registry entry** to
   `~/.claude/plugins/cache/claude-mp/vsdd-factory/1.0.0-rc.23/hooks-registry.toml`
   with the fields from AC-020. The cache registry is writable; no plugin integrity gate
   exists (the "signatures" in the dispatcher are ABI function signatures, not
   cryptographic hashes).

4. **Fire a deliberately-stale BC write** in a live session: write a test BC file with
   `version: "1.7"` but leave the BC-INDEX row at `v1.6`. Confirm exit code 2 and
   `[Class A Arm1]` in the error output.

5. **Fire a clean BC write**: update the BC-INDEX row to `v1.7`. Confirm exit code 0.

6. **Document cache/source divergence** in the PR description:
   - Source: `hooks-registry.toml` has the entry at its canonical location
   - Cache: `~/.claude/plugins/cache/...rc.23/` has the staged binary + registry entry
   - Divergence: ONLY visible in this developer's session until rc.24 ships
   - Teardown plan: when rc.24 is released, delete the manually-staged binary and
     registry entry from the rc.23 cache to restore clean state. Document this
     as a required step in the PR checklist.

**The smoke-test evidence (steps 4-5 output) MUST be included verbatim in the PR
description.** This is the only way to confirm the hook fires correctly before a
real release candidate is cut.

### AC-022: Class A Arm1 — primary-newer-than-index is advisory only (traces to BC-5.39.010 postcondition 2a)

When a BC file is written with `version: "1.6"` and the BC-INDEX.md body table row for
that BC ID still says `v1.5` (primary newer than index), the hook MUST emit
`host::log_warn` advisory and return `HookResult::Continue` (exit 0). This direction
reflects a POLICY 3 burst-ordering artefact: state-manager writes the BC-INDEX row AFTER
the product-owner writes the BC file; "index behind primary" is the guaranteed intermediate
state of every conforming burst and must not block correct authoring.

Under BC-5.39.010 v1.22 PC2a (unchanged since v1.14): primary-newer-than-index → advisory + Continue.
The advisory message MUST match (verbatim, with `<id>`, `<index_version>`, `<fm_version>`
as the only substitutions):
`"validate-cross-site-correspondence [Class A Arm1] advisory: BC-INDEX.md body-table row
for <id> cites v<index_version> but frontmatter version: is \"<fm_version>\" — primary
newer than index; state-manager index update pending; Class A BLOCK suspended."`

- **Advisory test (T-P6A):** `test_BC_5_39_010_arm_a1_primary_newer_advisory` —
  fixture BC file `version: "1.6"`; INDEX row `v1.5` → assert exit code 0 (Continue),
  `host::log_warn` advisory emitted containing `"[Class A Arm1] advisory:"`,
  `"primary newer than index"`, and `"Class A BLOCK suspended"`.
- **Contrast with AC-001:** the same fixture direction (primary 1.6 > index 1.5) that
  prior v1.10 AC-001 asserted must BLOCK now produces advisory under PC2a. AC-001 tests
  the opposite direction (index 1.6 > primary 1.5 → block per PC2b). Both ACs together
  confirm the directional split fires correctly.

### AC-023: Class B Arm1 — STORY-INDEX-consistent frontmatter-mismatch is advisory only (traces to BC-5.39.010 postcondition 13a)

When a story file is written with `input-hash: "47a65c9"` in frontmatter (B1), and both
STORY-INDEX.md catalog row (B2) and blockquote (B3) contain `4be9d21` (B2 == B3, but
B1 ≠ B2), the hook MUST emit `host::log_warn` advisory and return `HookResult::Continue`
(exit 0). This direction reflects a POLICY 3 burst-ordering artefact: state-manager writes
STORY-INDEX (both catalog row and blockquote) AFTER the story write; both STORY-INDEX sites
agreeing with each other while differing from the freshly-written story is the guaranteed
intermediate state of every conforming story-authoring burst.

Under BC-5.39.010 v1.22 PC13a (unchanged since v1.14): B2 == B3 AND B1 ≠ B2 → advisory + Continue.
The advisory message MUST match (verbatim, with `<id>`, `<h1>`, `<h2>`, `<h3>` as the
only substitutions):
`"validate-cross-site-correspondence [Class B] advisory: Story <id> input-hash mismatch —
frontmatter=<h1>; STORY-INDEX-catalog=<h2>; STORY-INDEX-blockquote=<h3>. STORY-INDEX
sites agree with each other; story frontmatter differs. State-manager STORY-INDEX update
pending; Class B BLOCK suspended."`

- **Advisory test (T-P6C):** `test_BC_5_39_010_arm_b1_b2_eq_b3_b1_ne_advisory` —
  B1=47a65c9, B2=4be9d21, B3=4be9d21 (B2==B3, B1≠B2) → assert exit code 0 (Continue),
  `host::log_warn` advisory emitted containing `"[Class B] advisory:"`,
  `"STORY-INDEX sites agree with each other"`, and
  `"State-manager STORY-INDEX update pending; Class B BLOCK suspended"`.
- **Contrast with AC-009:** the fixture that prior v1.10 AC-009 asserted must BLOCK
  (B1≠B2, B2==B3) now produces advisory under PC13a. AC-009 tests B2≠B3 (→ block per
  PC13b). Both ACs together confirm the directional split fires correctly.

### AC-024: Class B Arm1 — half-present case is advisory only (traces to BC-5.39.010 postcondition 13c)

When a story file is written with `input-hash: "d4f8a12"` in frontmatter (B1), and exactly
one of the two STORY-INDEX.md sites is present and differs from B1 — either the catalog row
(B2) is `"47a65c9"` (present and differs from B1) while the blockquote (B3) is absent, OR
B3 is present and differs from B1 while B2 is absent — the hook MUST emit `host::log_warn`
advisory and return `HookResult::Continue` (exit 0). The implementation MUST NOT block for
`(Some(b2), None)` or `(None, Some(b3))` cases.

Under BC-5.39.010 v1.22 PC13c (ADR-038 §Decision 3; unchanged since v1.14): the half-present case is an extension
of PC12's inclusive-or ("B2 or B3 absent"). The absent site cannot be verified at PostToolUse
trigger time; the present-but-differing site may reflect a mid-burst state where only one
STORY-INDEX update has completed. Blocking produces a self-lock on stories with permanently
missing blockquote entries (live instances: S-18.11, S-18.12) that have no burst-ordering
escape path. The long-term remedy is state-manager adding the missing blockquote entries, not
a hook block. This carve-out extends PC12's existing absent-site advisory to the
mixed-present/absent case.

- **Mutant test (B2 present and differs, B3 absent):**
  `test_BC_5_39_010_arm_b1_half_present_catalog_mismatch_is_advisory_not_block` —
  B1=d4f8a12, B2=47a65c9 (present, differs from B1), B3=absent → assert exit code 0
  (Continue), `host::log_warn` advisory emitted. The test name asserts advisory-not-block;
  a blocking implementation makes this test RED.
- **Mutant test (B3 present and differs, B2 absent):**
  `test_BC_5_39_010_arm_b1_half_present_blockquote_mismatch_is_advisory_not_block` —
  B1=d4f8a12, B2=absent, B3=47a65c9 (present, differs from B1) → assert exit code 0
  (Continue), `host::log_warn` advisory emitted. Symmetric to the B2 case; confirms both
  `(Some(b2), None)` and `(None, Some(b3))` are advisory.
- **Control test (B2 present and matches B1, B3 absent):**
  `test_BC_5_39_010_arm_b1_half_present_catalog_match_is_advisory` —
  B1=d4f8a12, B2=d4f8a12 (present, matches B1), B3=absent → assert exit code 0 (Continue),
  advisory emitted. Confirms that even when the present site agrees with B1, the absent-B3
  still triggers PC12 advisory (not the PC13c mismatch path, but the PC12 absent path).

### AC-025: Class A Arm2 — Phase 1 pure-version extraction is BC-ID-anchored; cross-BC table row does not produce a false block or false pass (traces to BC-5.39.010 v1.22 precondition 13 Phase 1 / EC-037)

When a story's `## Behavioral Contracts` table has a row whose FIRST non-empty
pipe-delimited field names BC-X (the row's own governing BC) with an isolated pure-version
Version cell (e.g. `1.7`), and a DIFFERENT row in the same table merely mentions BC-Y in a
Notes/Trace/VP-mapping cell (not as that row's own first field), and the story's
`behavioral_contracts: [BC-Y]` frontmatter cites BC-Y whose current frontmatter version
differs from BC-X's Version cell (e.g. BC-Y frontmatter `version: "2.0"`), the hook MUST NOT
attribute BC-X's Version cell to BC-Y. Phase 1 extraction for BC-Y MUST require BC-Y to be
named in the row's own first non-empty field (the same word-boundary locator-predicate test
already normative for Part A Arm1 and Phase 2) before extracting that row's pure-version
field. Because BC-Y then has no version-citing row of its own, the hook MUST skip BC-Y
(`HookResult::Continue` for that BC) per postcondition 8 — it MUST NOT false-BLOCK using
BC-X's unrelated Version cell, and it MUST NOT false-PASS by coincidental match.

Under BC-5.39.010 v1.22 precondition 13 Phase 1 (ADV-RECON-003): un-anchored Phase 1 (scanning
the entire row for the rightmost pure-version field with no requirement that the row's own
subject is the target BC) is corpus-confirmed reachable — 14 of 480 examined
`## Behavioral Contracts` body-table rows across `.factory/stories/*.md` name more than one
distinct BC ID within a single row (supersession/refinement/VP-mapping notes). Row-first-field
anchoring closes both the false-BLOCK and false-PASS directions.

- **Mutant test (Rust unit, `crates/hook-plugins/validate-cross-site-correspondence/src/arm_a2.rs`):**
  `test_BC_5_39_010_arm_a2_phase1_bc_id_anchored_no_cross_bc_row_false_block` — Row 1: first
  field `BC-X`, Version cell `1.7`; Row 2 (same table) mentions `BC-Y` only in a Notes cell;
  `behavioral_contracts: [BC-Y]`; BC-Y frontmatter `version: "2.0"` → assert exit code 0
  (Continue for BC-Y — no version-citing row found for BC-Y; no false block from BC-X's
  Version cell). An un-anchored (full-row-scan) Phase 1 implementation resolves `1.7` for
  BC-Y and false-blocks against `2.0` — this test is RED for that implementation.
- **Extraction-layer pin (Rust unit, `arm_a2.rs`; mutant):**
  `test_BC_5_39_010_arm_a2_phase1_bc_id_anchored_cross_bc_row_excluded_from_citations` — pins
  the fix at the extraction layer itself: asserts the Phase 1 pure-version-extraction routine
  excludes Row 1's `BC-X`-anchored Version cell from the citation set it returns for `BC-Y`
  (`BC-Y` yields zero extracted citations from this fixture), closing the false-block/
  false-pass gap at its source rather than only at the outward dispatch outcome.
- **Control test (Rust unit, `arm_a2.rs`):**
  `test_BC_5_39_010_arm_a2_phase1_bc_id_anchored_own_row_stale_still_blocks` — the row's own
  first field names BC-Y itself, with a Version cell that is genuinely stale (differs from
  BC-Y's current frontmatter version) → the hook still blocks. Confirms the BC-ID-anchoring
  fix narrows Phase 1 to the row's own subject WITHOUT weakening its legitimate
  stale-citation detection — anchoring must not become a new way to suppress a true block.
- **Mutant test — compound/supersession first-cell, strong-predicate case (Rust unit,
  `arm_a2.rs`):**
  `test_BC_5_39_010_arm_a2_phase1_supersession_first_cell_excluded_from_phase1` — Row 1's
  first field is the compound form `BC-X (supersedes BC-Y)` (e.g.
  `BC-9.99.010 (supersedes BC-9.99.011)`), not an exact-equals or `[BC-Y]` link-form match
  for BC-Y, with an isolated Version cell (e.g. `1.7`) that belongs to BC-X, not BC-Y;
  `behavioral_contracts: [BC-Y]` → asserts ZERO Phase-1 (and zero overall) citations for
  BC-Y, and confirms `run_arm_a2_for_bc_with_result` returns no violations (skip-not-block
  per postcondition 8) for that empty-citations case. This is a harder shape than the
  Notes-cell tests above: `BC-Y` appears at a word boundary INSIDE the row's OWN first cell
  (not a separate Notes/Trace cell), so it further pins the strong Arm-A1
  locator-predicate (exact-equals or link-form only) as the anchor test, ruling out a
  weaker substring/word-boundary match against the first cell's raw text.

### AC-026: Primary-target read succeeds as bytes but fails UTF-8 decoding — MUST block, not silently Continue (traces to BC-5.39.010 v1.22 precondition 15a, postcondition 25, invariant 4 extension / EC-038)

When `host::read_file` on the PostToolUse **primary target** (the BC file for Arm1/Part E,
the story file for Arm2/Arm B1/Part E, or STORY-INDEX.md for Arm B2) succeeds at the byte
level but the returned bytes fail UTF-8 decoding, the hook MUST return
`HookResult::block_with_fix(...)` naming the file path and the decode error, under the same
fail-closed posture as invariant 4. The hook MUST NEVER return `HookResult::Continue` for
this case, regardless of which arm(s) the primary target would otherwise dispatch to. Citing
invariant 9 (`is_char_boundary()` slicing safety on already-decoded strings) as authorization
for `Continue` on a decode failure is NON-CONFORMING — invariant 9's scope is slicing safety
only, not read-decode disposition.

Under BC-5.39.010 v1.22 invariant 4 extension (ADV-RECON-007): the shipped implementation's
primary-target UTF-8 decode step (post-primary-read, pre-arm-dispatch) had filled this
previously-undefined case by citing invariant 9 as authorization for `HookResult::Continue`
on decode failure; invariant 9 does not grant this. New precondition 15a and postcondition 25
close the gap and name the invariant-9 misattribution NON-CONFORMING to prevent recurrence.

These are bats integration tests (not Rust unit tests) in
`plugins/vsdd-factory/tests/validate-cross-site-correspondence.bats`:

- **Mutant test:** `"ADV-RECON-007: primary target UTF-8 decode failure produces exit code 2
  (block), not Continue"` — fixture: primary target (BC file) `host::read_file` succeeds but
  returns bytes that are not valid UTF-8 (e.g. a raw non-UTF-8 byte sequence) → assert exit
  code 2 (block), not `HookResult::Continue`. A `Continue`-on-decode-failure implementation
  (citing invariant 9) makes this test RED — this is exactly the shipped implementation's
  NON-CONFORMING pattern this AC exists to close (see Task 22).
- **Control test:** `"ADV-RECON-007 CONTROL: primary target valid UTF-8 dispatches normally
  (exit code 0)"` — same fixture with valid UTF-8 bytes → normal arm dispatch proceeds; exit
  code 0, no decode-failure block.

### AC-027: Class A Arm2 — Phase 2 same-field scan-stop; an intervening different-BC token before the qualifying v-token yields no citation (traces to BC-5.39.010 v1.22 precondition 13 Phase 2 same-field scan-stop / EC-039)

When, within a story's `## Behavioral Contracts` table, a row's own first non-empty
pipe-delimited field names a DIFFERENT BC (so Phase 1 is ineligible for the target BC), and the
Phase 2 anchor field for the target BC — a field that mentions the target BC ID at a
word-boundary — ALSO mentions, later in that SAME field, a DIFFERENT `BC-S.SS.NNN` token BEFORE
any qualifying `\bv([0-9]+\.[0-9]+)\b` token is reached, the hook MUST NOT attribute the
eventual v-token (wherever it later appears in that field) to the target BC. The forward scan
MUST terminate at the intervening different-BC-ID token, without producing a version, and the
anchor field MUST yield NO citation for the target BC. Per the existing per-field fallback, the
caller then proceeds to the next pipe-delimited field; if no other field in the row names the
target BC, the row produces zero citations for it. Because the target BC then has no
version-citing row, the hook MUST skip it (`HookResult::Continue` for that BC) per postcondition
8 — it MUST NOT false-BLOCK using a version token that plausibly belongs to the intervening
different BC, not the target BC.

Under BC-5.39.010 v1.22 precondition 13 Phase 2 same-field scan-stop (ADV-RECON5-003):
field-scoping alone (v1.19 / ADR-038 §Decision 5) does NOT fully resolve this case, because the
spurious v-token and the intervening different-BC-ID mention both reside in the SAME field —
scoping the scan to that one field does not exclude the false match; only a same-field scan-stop
that halts at the intervening different-BC-ID token does. Corpus-confirmed reachable, not
hypothetical (2026-08-14): `.factory/stories/S-4.08-rc1-release-gate.md`'s `## Behavioral
Contracts` row for `BC-9.01.002` — the row's own first field names `BC-9.01.002` (Phase 1
ineligible for target `BC-9.01.001`, which IS in that story's `behavioral_contracts:`
frontmatter array); the Trace cell mentions `BC-9.01.001`, then, later in the SAME cell — past
an intervening `BC-9.01.002` mention — the unrelated text "v1.1 BC candidate ...". The v1.20
field-scoped-only algorithm still extracted "v1.1" as `BC-9.01.001`'s citation despite its real
frontmatter version being "1.0" — a live false BLOCK, not a hypothetical one (EC-039).

- **Mutant test (Rust unit, `crates/hook-plugins/validate-cross-site-correspondence/src/arm_a2.rs`):**
  `test_BC_5_39_010_arm_a2_phase2_same_field_scan_stop_different_bc_no_false_citation` — faithful
  transcription of the S-4.08 corpus row: `## Behavioral Contracts` row with first field
  `BC-9.01.002`, Trace cell mentioning `BC-9.01.001` then, past an intervening `BC-9.01.002`
  mention in the same cell, "v1.1 BC candidate ..."; resolving `BC-9.01.001` against this row
  MUST produce zero citations (`extract_story_bc_version_citations(content, "BC-9.01.001")` is
  empty), and the full `run_arm_a2_for_bc_with_result` pipeline against BC-9.01.001's real
  frontmatter `version: "1.0"` MUST produce zero violations and zero advisories. A pre-v1.21
  field-scoped-only implementation (no same-field-scan-stop check) is RED against this test — it
  extracts "1.1" and produces a false-BLOCK violation ("1.1" != "1.0").
- **Control test (Rust unit, `arm_a2.rs`; pre-existing ADR-038 §Decision 5 Fixture 2 control,
  unaffected by and consistent with the v1.21 scan-stop):**
  `test_BC_5_39_010_arm_a2_pc13_phase2_anchor_field_with_v_token_produces_citation` — anchor
  field `"BC-9.01.002 v1.1 (gating story)"`: the row's OWN BC ID precedes its own version token
  in the SAME field with NO intervening different-BC-ID token → Phase 2 still correctly resolves
  `"1.1"` as the citation. This test predates v1.21 (it exercises the ADR-038 §Decision 5
  BC-ID-anchored-first-v-token base algorithm) but is the genuine "own-BC-resolves,
  Token-Budget-inline-style" control for this AC: the scan-stop condition never triggers here
  (no intervening different-BC token exists before the qualifying v-token), so it confirms the
  v1.21 scan-stop addition does not regress legitimate same-field resolution.

### AC-028: Class A Arm1 / Class B Arm1 — non-UTF-8 secondary index-file read yields a distinct INDETERMINATE advisory, not a misleading RowAbsent block or a generic "not yet registered" advisory (traces to BC-5.39.010 v1.22 precondition 15b, postcondition 26 / EC-040)

When the **secondary** index-file read — BC-INDEX.md at Arm A1, or STORY-INDEX.md at Arm B1
(**NOT** Arm B2, where STORY-INDEX.md is the *primary* target and is already governed by
precondition 15a / postcondition 25's fail-closed block) — succeeds at the byte level
(`Ok(...)`) but the returned bytes fail UTF-8 decoding, the hook MUST NOT silently conflate
the decode failure with a genuinely-absent or not-yet-registered row/site:

- **Arm A1 (BC-INDEX.md):** the hook MUST NOT fall through into `RowAbsent` — which, for any
  cited BC with frontmatter `version:` > "1.0", would trigger postcondition 4's BLOCK with the
  misleading message "previous registration appears to have been dropped" (the true root cause
  is index-file corruption, not a dropped registration). Instead the hook MUST emit a DISTINCT
  advisory naming `BC-INDEX.md`, stating the row state for `<id>` is INDETERMINATE, not
  confirmed-absent, and MUST return `HookResult::Continue` (no block).
- **Arm B1 (STORY-INDEX.md, non-primary-target case only):** the hook MUST NOT fall through
  into `(None, None)` — which would trigger postcondition 12's generic fail-open "not yet
  registered" advisory, silently disabling three-way hash checking with no disclosure that the
  actual root cause is an undecodable STORY-INDEX.md, not legitimate bootstrap absence. Instead
  the hook MUST emit a DISTINCT advisory naming `STORY-INDEX.md`, stating the hash state for
  `<id>` is INDETERMINATE, not confirmed-absent, and MUST return `HookResult::Continue`.

Prescribed verbatim advisory message (postcondition 26), with `<index-file>` and `<id>` as the
only substitutions: "validate-cross-site-correspondence: `<index-file>` failed UTF-8 decode —
row/hash state for '`<id>`' is INDETERMINATE, not confirmed-absent. Fix: verify the index
file's encoding and re-save as UTF-8."

Under BC-5.39.010 v1.22 precondition 15b / postcondition 26 (ADV-RECON11-001, LOW /
near-zero-reachability, architect-adjudicated): this clause is DISTINCT from precondition 15a
/ postcondition 25, which govern PRIMARY-target decode failure only (fail-closed BLOCK). Gap:
BC-INDEX.md (Arm A1 secondary read) and STORY-INDEX.md (Arm B1 secondary read only) had no
specified UTF-8 decode disposition prior to v1.22 — the same diagnostic-accuracy defect class
as F-S2107-P1B-016 (fixed for Arm A2's secondary BC-FILE read), applied asymmetrically to Arm
A1/B1's secondary INDEX-FILE reads. Disposition is deliberately ADVISORY (Continue), not
block — lower severity than precondition 15a / postcondition 25's primary-target BLOCK,
appropriate to this LOW/near-zero-reachability gap. See EC-040 (combined Arm A1 + Arm B1
scenario).

- **Mutant test (Rust unit, `crates/hook-plugins/validate-cross-site-correspondence/src/arm_a1.rs`):**
  `test_BC_5_39_010_arm_a1_non_utf8_bc_index_indeterminate_advisory_not_block` — BC-INDEX.md
  "read" succeeds as bytes but the bytes are not valid UTF-8; asserts zero violations (no
  `RowAbsent`-derived block) and a distinct advisory containing "BC-INDEX.md", "failed UTF-8
  decode", and "INDETERMINATE, not confirmed-absent", and asserts the advisory does NOT
  contain "previous registration appears to have been dropped".
- **Mutant test (Rust unit, `crates/hook-plugins/validate-cross-site-correspondence/src/arm_b.rs`):**
  `test_BC_5_39_010_arm_b1_non_utf8_story_index_indeterminate_advisory` — STORY-INDEX.md
  "read" succeeds as bytes but the bytes are not valid UTF-8; asserts zero violations and a
  distinct advisory containing "STORY-INDEX.md", "failed UTF-8 decode", and "INDETERMINATE,
  not confirmed-absent", and asserts the advisory does NOT contain "not yet registered".
- **Control test (Rust unit, `arm_a1.rs`; pre-existing AC-002 test):**
  `test_BC_5_39_010_arm_a1_new_v1_0_not_in_index_advisory` — a genuinely-absent row for a v1.0
  BC in a DECODABLE (valid-UTF-8) BC-INDEX.md still produces the normal postcondition-3
  advisory + Continue, unaffected by the v1.22 amendment; confirms the new INDETERMINATE
  disposition does not regress the legitimate genuinely-absent-row path.
- **Control test (Rust unit, `arm_b.rs`; pre-existing AC-010 test):**
  `test_BC_5_39_010_arm_b1_absent_index_sites_advisory` — genuinely-absent secondary sites in
  a DECODABLE (valid-UTF-8) STORY-INDEX.md still produce the normal postcondition-12 advisory
  + Continue, unaffected by the v1.22 amendment.

## Architecture Mapping

This story introduces one new WASM crate and one registry entry:

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| Hook entry point | `crates/hook-plugins/validate-cross-site-correspondence/src/lib.rs` | effectful-shell (reads files via `host::read_file`) |
| Dispatch router (arm selection) | `crates/hook-plugins/validate-cross-site-correspondence/src/dispatch.rs` | pure-core (path matching only) |
| Class A Arm1 (BC→INDEX check) | `crates/hook-plugins/validate-cross-site-correspondence/src/arm_a1.rs` | effectful-shell (reads BC-INDEX.md) |
| Class A Arm2 (story→BC check) | `crates/hook-plugins/validate-cross-site-correspondence/src/arm_a2.rs` | effectful-shell (reads per-BC files via deterministic path derivation) |
| Class B (input-hash three-way) | `crates/hook-plugins/validate-cross-site-correspondence/src/arm_b.rs` | effectful-shell (reads STORY-INDEX.md) |
| Class D (finding-ID namespace) — **[DEFERRED v1.6]** | `crates/hook-plugins/validate-cross-site-correspondence/src/arm_d.rs` — **[DEFERRED v1.6 — Class D; do not create in v1.6]** | pure-core (regex on already-read content) |
| Class E (frontmatter parity) | `crates/hook-plugins/validate-cross-site-correspondence/src/arm_e.rs` | pure-core (regex on already-read content) |
| Frontmatter parser utilities | `crates/hook-plugins/validate-cross-site-correspondence/src/frontmatter.rs` | pure-core |
| Registry entry | `plugins/vsdd-factory/hooks-registry.toml` | config (non-code) |

**Architecture anchor functions to implement** (named in BC-5.39.010 §Architecture
Anchors — these are functions to create, not existing functions to verify):

- `derive_bc_path(bc_id: &str) -> String` — deterministic path from BC ID; no `read_dir`
- `extract_story_bc_version_citations(content: &str, bc_id: &str) -> Vec<(String, String)>` —
  finds all table rows containing bc_id + version token; returns `(location, version)` pairs
- `extract_frontmatter_sequence(content: &str, field: &str) -> Vec<String>` —
  parses YAML sequence from frontmatter block

**ADR-035 §Decision 1** (Tier 2A) governs this hook. It is a read-only PostToolUse
validator — no writes, no directory enumeration (BC-5.39.010 invariant 1 + invariant 3).

**SDK anchors (existing, grep-verified):**
- `host::log_warn` — `crates/hook-sdk/src/host.rs:40: pub fn log_warn(msg: &str)`
- `host::read_file` — `crates/hook-sdk/src/host.rs:203: pub fn read_file(path: &str, max_bytes: u32, timeout_ms: u32) -> Result<Vec<u8>, HostError>`
- `HookResult::Continue` — `crates/hook-sdk/src/result.rs:20: Continue,`
- `HookResult::block_with_fix` — `crates/hook-sdk/src/result.rs:50: pub fn block_with_fix(`
- `HostError::CapabilityDenied` — `crates/hook-sdk/src/host.rs:92: CapabilityDenied,`
- `HostError::NotFound` — `crates/hook-sdk/src/host.rs:105: NotFound,`

## Behavioral Contracts

| BC ID | Title | Version | Story ACs |
|-------|-------|---------|-----------|
| BC-5.39.010 | validate-cross-site-correspondence WASM hook MUST block on stale BC-INDEX version-cite after a BC frontmatter bump (Class A Arm1), stale story body BC-table and Token Budget citations after a story edit (Class A Arm2), STORY-INDEX three-way input-hash inequality (Class B), and frontmatter version↔last_amended text-prefix mismatch and modified[] date-decrease (Class E); Class C count/enumeration parity is not mechanically checkable in WASM; Class D (finding-ID namespace advisory in Closes/Refs lines) deferred pending Closes/Refs convention standardization | 1.22 | AC-001 through AC-028 (AC-012/013/014 DEFERRED v1.6 — Class D; AC-022/023 added v1.6 for PC2a/PC13a advisory paths; AC-024 added v1.8 for PC13c half-present; enumeration extended to include AC-024 at v1.9; AC-025/026 added v1.15 for v1.20 ADV-RECON-003/007 — PC13 Phase 1 BC-ID anchoring and non-UTF-8 primary-target block; AC-027 added v1.17 for v1.21 ADV-RECON5-003 — PC13 Phase 2 same-field scan-stop; AC-028 added v1.18 for v1.22 ADV-RECON11-001 — secondary index-file UTF-8 decode advisory) |

## Out-of-Scope (Explicitly Excluded)

The following are NOT covered by this story. A reviewer who sees no test for these items
should NOT file a finding — these are declared non-scope, not missed coverage.

| Item | Reason Not In Scope | Route |
|------|---------------------|-------|
| Class C (count/enumeration parity) | Declared ungatable by generic WASM hook in BC-5.39.010 §Honest Gap — English cardinal parsing + structural anchor identification requires per-file fixtures; not a generic hook concern. ADR-035 §Decision 4 routes to Rust workspace integration test in bats-full-suite | story-writer (TBD story ID per ADR-035 §Downstream Routing) |
| Class D semantic existence (does F-NNN actually exist?) | WASM hook cannot enumerate `adv-cycle-pass-*.md` corpus without unbounded `list_dir` enumeration. **Class D here is format-only.** A fabricated `F-S2104-P99-H99` passes Class D cleanly because its format is correct — the hook only checks the `F-` prefix. This was the pass-29/30 failure shape: well-formed IDs naming stale/retracted findings passed undetected. Semantic existence routed to bats Tier 3 per ADR-035 §Decision 4. | story-writer (TBD story ID) |
| Tier 2B generator (`generate-index-rows` binary) | ADR-035 §Decision 3 — future Rust binary for BC-INDEX + STORY-INDEX derived cell generation; complements Tier 2A but is a separate story | story-writer (TBD story ID per ADR-035 §Downstream Routing) |
| wasmtime 44→47 bump | ADR-035 §Decision 6 — separate maintenance story; not an architectural change for this crate | story-writer (maintenance story TBD) |
| Fuel exhaustion advisory emission in dispatcher | ADR-035 §Decision 5 point 2 — requires modifying `handle_plugin_err` in `crates/factory-dispatcher/src/invoke.rs`; separate implementer task | implementer (separate dispatch) |
| `on_error = "block"` audit for existing hooks | ADR-035 §Decision 5 point 1 — separate audit of `validate-factory-path-root` and `validate-input-hash`; this story does not touch those hooks | implementer (separate dispatch) |
| Per-plugin `fuel_cap` registry field extension | Out of THIS story's scope because it depends on ADR-039 Phase 1 (registry `fuel_cap` schema extension, story S-21.10) shipping first — per ADR-042 §Decision 2 (re-anchored from the superseded ADR-035 §Decision 5 framing; ADR-035 v1.1 Correction 3 confirms its own "no registry change needed for this new hook" note is superseded). The premise that `on_error = "continue"` makes a `fuel_cap` override unnecessary is RETRACTED (BC-5.39.010 v1.15/v1.16, F-S2107-P9-002/F-S2107-P12-001) — fuel exhaustion under `on_error = "continue"` is a silent non-finding, not a safe no-op. A per-plugin `fuel_cap` SHOULD be set for this hook once S-21.10 ships (see AC-020 Notes); this story's registry entry correctly omits `fuel_cap` today as a "not yet" state, not a permanent non-need | implementer (separate dispatch, blocked on S-21.10) |

## Edge Cases

| ID | Scenario | Expected Behavior | BC Reference |
|----|----------|-------------------|--------------|
| EC-001 | BC v1.0 written, no BC-INDEX row | Advisory + Continue | BC-5.39.010 EC-001 |
| EC-002 | BC bumped v1.17→v1.18; INDEX says v1.17 (primary v1.18 > index v1.17) | Advisory + Continue: PC2a (primary newer than index; POLICY 3 burst-ordering artefact; Class A BLOCK suspended) | BC-5.39.010 postcondition 2a |
| EC-003 | BC-INDEX.md returns NotFound | Advisory + Continue | BC-5.39.010 EC-003 |
| EC-004 | Primary BC file CapabilityDenied | Block (fail-closed) | BC-5.39.010 EC-004 |
| EC-005 | Story hash "47a65c9"; STORY-INDEX catalog "4be9d21"; blockquote "47a65c9" | Block: B2 ≠ B1 | BC-5.39.010 EC-005 |
| EC-006 | Story file has no input-hash: field | Continue — Arm B1 skips | BC-5.39.010 EC-006 |
| EC-007 | STORY-INDEX.md written; blockquote and catalog agree | Continue — B2 == B3 | BC-5.39.010 EC-007 |
| EC-008 | STORY-INDEX.md written; blockquote ≠ catalog | Block: Class B Arm B2 | BC-5.39.010 EC-008 |
| EC-009 | Story write; STORY-INDEX.md CapabilityDenied | Block: capability-class error on secondary | BC-5.39.010 EC-009 |
| EC-010 | burst-log last H2: "Closes: F-S2104-P29-H01, F-S2104-P29-H02" | **(DEFERRED v1.6 — Class D)** Continue (no advisory) | BC-5.39.010 EC-010 |
| EC-011 | burst-log last H2: "Closes: B01, F-S2104-P29-H02" | **(DEFERRED v1.6 — Class D)** Advisory for B01; Continue | BC-5.39.010 EC-011 |
| EC-012 | lessons.md; latest L-EDP1 entry "Closes: 001" (bare numeric) | **(DEFERRED v1.6 — Class D)** Not flagged — starts with digit, not letter → not finding-like | BC-5.39.010 EC-012 |
| EC-013 | lessons.md; historical Changelog section has "P45-001" | **(DEFERRED v1.6 — Class D)** Continue — outside scope window | BC-5.39.010 EC-013 |
| EC-014 | BC written `version: "1.6"`, `last_amended: "2026-07-29 (v1.6) — ..."` | Part E passes | BC-5.39.010 EC-014 |
| EC-015 | BC written `version: "1.33"`, `last_amended: "... (v1.31) ..."` | Block: Class E1 | BC-5.39.010 EC-015 |
| EC-016 | modified: ["2026-05-14", "2026-05-18 (v1.1)", "2026-05-15"] | Block: Class E2 | BC-5.39.010 EC-016 |
| EC-017 | modified: ["2026-05-14", "2026-05-18", "2026-05-20 (v1.3)"] | Part E passes (ascending) | BC-5.39.010 EC-017 |
| EC-018 | last_amended with [Prior: ... (v1.5) ...]; version "1.6"; outermost prefix "(v1.6)" | Part E passes: positional anchor matches v1.6 | BC-5.39.010 EC-018 |
| EC-019 | BC write triggers both Class A Arm1 stale + Class E1 mismatch | Single combined block | BC-5.39.010 EC-019 |
| EC-020 | VP file written `version: "2.4"`, `last_amended: "... (v2.4)"` | Part E passes — VP files in scope (PC34) | BC-5.39.010 EC-020 |
| EC-021 | Story S-21.07 written; B1 populated; B2/B3 absent (new story) | Advisory + Continue | BC-5.39.010 EC-021 |
| EC-022 | last_amended: does not match regex | Advisory + Continue (no block on unparseable format) | BC-5.39.010 EC-022 |
| EC-023 | STORY-INDEX.md written | Only Arm B2 fires; Arm A2/B1/E do not apply | BC-5.39.010 EC-023 |
| EC-024 | burst-log "Refs: D-944" | **(DEFERRED v1.6 — Class D)** D- prefix excluded → Continue, no advisory | BC-5.39.010 EC-024 |
| EC-025 | Both BC file AND BC-INDEX CapabilityDenied | Combined block citing both | BC-5.39.010 EC-025 |
| EC-026 | Story cites BC-6.26.001; Token Budget row says "v1.17"; BC frontmatter "1.18" | Block: Class A Arm2 | BC-5.39.010 EC-026 |
| EC-027 | Story cites BC-5.39.010; BC-5.39.010.md returns NotFound | Advisory for NotFound BC; Continue | BC-5.39.010 EC-027 |
| EC-028 | Story cites BC-6.26.001 and BC-5.39.008; both stale | Single combined block (cascade) | BC-5.39.010 EC-028 |
| EC-029 | Story cites BC-6.26.001; BC mentioned only in prose, no version token in table row | Arm A2 skips — no version-citing rows | BC-5.39.010 EC-029 |
| EC-030 | BC written with `modified: ["2026-07-30", "2026-07-30 (v1.1)"]` — two entries with equal date after suffix-strip | Part E passes. Equal consecutive dates satisfy `date[i] ≤ date[i+1]`; same-day multi-burst cadence is permitted (this BC's own v1.0/v1.1 modified[] is exactly this shape). | BC-5.39.010 EC-030 |
| EC-031 | BC written with `modified: ["2026-07-30", "2026-07-29 (v1.1)"]` — date decreases after suffix-strip | Block: Class E2. `2026-07-30 > 2026-07-29` — strict decrease violates the non-decreasing requirement. | BC-5.39.010 EC-031 |
| EC-032 | Story S-19.01 written; `inputs:` contains `.factory/cycles/v1.0-brownfield-backfill/lessons.md`; input-hash present; B2 and B3 absent | Advisory (volatile input detected per ADR-037 §Decision 2; Class B BLOCK suspended; story-writer must remove volatile input) + Continue. PC40 fires at Arm B1 before reading STORY-INDEX.md. | BC-5.39.010 EC-032 |
| EC-033 | `burst-log.md` last H2 section contains no lines matching `^\*\*Closes\b` or `^\*\*Refs\b` | **(DEFERRED v1.6 — Class D)** Scope-count advisory "0 Closes/Refs line(s) scanned" emitted (postcondition 24) + Continue. N=0 makes inert scope observable rather than vacuously passing. | BC-5.39.010 EC-033 |
| EC-036 | Story S-18.11 written; B1="d4f8a12" (new hash); STORY-INDEX catalog B2="47a65c9" (stale, pre-existing); blockquote for S-18.11 absent (B3 absent) | Advisory + Continue: Class B Arm1, sub-case PC13c (half-present case per ADR-038 §Decision 3: B2 present and differs from B1; B3 absent; PC12 inclusive-or "B2 or B3 absent" governs; implementation MUST NOT block for `(Some(b2), None)`). | BC-5.39.010 EC-036 |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| `crates/hook-plugins/validate-cross-site-correspondence/src/lib.rs` | effectful-shell | Hook entry point; orchestrates reads via `host::read_file` and emits via `host::log_warn` / `HookResult` |
| `src/dispatch.rs` | pure-core | File path matching (is_bc_file, is_story_file, is_story_index, is_cycle_artifact) — string/path ops only; no I/O |
| `src/arm_a1.rs`, `src/arm_a2.rs`, `src/arm_b.rs` | effectful-shell | Read secondary files (BC-INDEX, BC files, STORY-INDEX) via host::read_file; return violation lists |
| `src/arm_d.rs` — **[DEFERRED v1.6 — Class D; do not create in v1.6]**, `src/arm_e.rs` | pure-core | Regex scan on already-read byte slices; no further I/O |
| `src/frontmatter.rs` | pure-core | YAML frontmatter field extraction via line scan; no I/O |

## Forbidden Dependencies

The following MUST NOT appear in `crates/hook-plugins/validate-cross-site-correspondence/Cargo.toml`.
If any appears, the build MUST fail (CI enforces this via `cargo deny` or similar):

| Forbidden Crate | Reason |
|-----------------|--------|
| `serde_yaml` | Deprecated, archived 2024; workspace migrated to `serde_norway = "0.9"` via TD #72. Any import is a regression. |
| `wasmtime` | Hook plugins are WASM binaries; they must NOT link against wasmtime. Only the dispatcher binary links wasmtime. A hook crate depending on wasmtime creates a circular compile dependency. |
| `factory-dispatcher` | Same reason — binary crate; hook plugins must not depend on the dispatcher binary. |

Note: `serde_norway` is permitted (YAML deserialization if needed) but YAML parsing is
not required for this hook — frontmatter is extracted via line-scanning regex as in all
existing hook plugins. `serde` + `serde_json` (for payload deserialization) are permitted.

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~9,000 |
| BC-5.39.010 v1.22 (full text, 40 ECs, 19 VPs, Gate Spec pseudocode) | ~8,000 |
| ADR-035 (§Decisions 1-5 + Rationale) | ~4,500 |
| `crates/hook-sdk/src/host.rs` + `src/result.rs` (SDK surface) | ~2,500 |
| `crates/hook-plugins/validate-closes-completeness/src/lib.rs` (reference pattern for multi-arm hook) | ~6,000 |
| `crates/hook-plugins/validate-index-cite-refresh/src/lib.rs` (reference pattern for BC version extraction) | ~4,000 |
| `plugins/vsdd-factory/hooks-registry.toml` (priority context + existing entry patterns) | ~3,000 |
| BC files (N≈2 for context: BC-5.39.010 + one cited BC like BC-6.26.001) | ~5,000 |
| Test fixture files (bats + Rust unit test stubs) | ~3,500 |
| Tool outputs overhead | ~2,000 |
| **Total** | **~47,500** |
| Agent context window | 200K (Sonnet) |
| **Budget usage** | **~23.75%** |

Within the 20-30% target. If the reference crate reads push context over 25%, load only
the relevant sub-sections (the extraction + version-comparison logic) rather than full
source files.

## Tasks (MANDATORY)

1. [x] Read BC-5.39.010 v1.22 fully before writing any test — do not work from AC summaries
   alone; the BC contains exact message text strings and regex anchors (test-writer) — DONE
   (evidenced by exact-message-text-conformant test/impl code in the feature worktree)
2. [x] Capture `cargo test --workspace --all-targets` baseline pass count; emit
   `pre-implementation baseline: <N> pass` marker (implementer) — DONE (historical
   pre-implementation gate satisfied; crate now exists and passes)
3. [x] Write failing Rust unit tests in `crates/hook-plugins/validate-cross-site-correspondence/src/` for:
   - `dispatch.rs`: path-component-strict guards (is_bc_file, is_story_file, is_story_index, is_cycle_artifact)
   - `frontmatter.rs`: `extract_frontmatter_sequence`, `extract_frontmatter_field` (version)
   - `arm_a1.rs`: stale INDEX block (AC-001 mutant), v1.0 advisory (AC-002), CapabilityDenied (AC-003)
   - `arm_a2.rs`: stale citation block (AC-005), cascade (AC-006), empty bcs skip (AC-007), no-version-row skip (AC-008)
   - `arm_b.rs`: B1 three-way mismatch (AC-009), B1 absent sites (AC-010), B2 cascade (AC-011), B1 advisory PC13a (AC-023), B1 half-present advisory PC13c (AC-024)
   - `arm_d.rs`: **[DEFERRED v1.6 — Class D; omit from v1.6 test-writer dispatch]** excluded namespaces pass (AC-012), non-F- advisory (AC-013), scope-limited extraction (AC-014)
   - `arm_e.rs`: E1 mismatch (AC-015), E2 strict-decrease (AC-016), Prior-chain excluded (AC-017)
   - `lib.rs`: combined A+E single block (AC-018), UTF-8 no-panic (AC-019 last item)
   (test-writer)
4. [x] Write failing bats integration tests in `plugins/vsdd-factory/tests/validate-cross-site-correspondence.bats`
   for full payload-delivery scenarios (AC-001 through AC-018, AC-022, AC-023 via fixture files; AC-024 Rust unit tests only — three half-present tests in arm_b.rs, see T-P8A/T-P8B/T-P8C; AC-026 via the ADV-RECON-007 mutant/control pair) (test-writer) — DONE (49/49 bats tests green)
5. [x] Verify Red Gate: `cargo test --workspace --all-targets` shows all new tests failing
   (crate does not exist yet) — DONE (historical gate satisfied; superseded by current 173/173 Rust unit tests + 49/49 bats green)
6. [x] Create `crates/hook-plugins/validate-cross-site-correspondence/` crate: `Cargo.toml`
   (with hook-sdk dep, NO serde_yaml, NO wasmtime); add to workspace `Cargo.toml` members
   (implementer) — DONE
7. [x] Implement `dispatch.rs`: path-component-strict guards per BC-5.39.010 PC1/9/16/22/28/34
   using `Path::new(file_path).components()` — raw `ends_with` string matching FORBIDDEN
   (BC-5.39.010 invariant 3) (implementer) — DONE
8. [x] Implement `frontmatter.rs`: `extract_frontmatter_sequence` + `extract_frontmatter_field`
   using `serde_norway`-free line scanning; handle quoted and unquoted `version:` values
   (implementer) — DONE
9. [x] Implement `arm_a1.rs`: Arm A1 logic including `derive_bc_path` (deterministic, no
   list_dir), version cell extraction from BC-INDEX body table, normalization, block/advisory
   message strings matching BC-5.39.010 postconditions 1-5 exactly (implementer) — DONE
10. [x] Implement `arm_a2.rs`: Arm A2 logic including `extract_story_bc_version_citations`,
    using the TWO-PHASE algorithm per BC-5.39.010 v1.22 PC13 (see §BC Status above) — NOT
    a single bare-optional-v regex. **Phase 1 (pure-version field, BC-ID-anchored):** the
    row's own first non-empty pipe-delimited field MUST name the target BC (word-boundary
    match) before any field of that row is scanned for a pure-version token; a row whose
    first field names a DIFFERENT BC is excluded from Phase 1 for this BC entirely (v1.20
    PC13 Phase 1 BC-ID-anchoring, ADV-RECON-003 — see Task 21 below for the anchoring
    implementation itself). Within an anchored row, if any field's stripped content matches
    `^v?([0-9]+\.[0-9]+)$` exactly (the entire field is exclusively a version number,
    optionally v-prefixed), use it, stripping the leading `v` — covers the isolated Version
    column of `## Behavioral Contracts` body-table rows (e.g. this story's own
    governing-BC-version cell in the Behavioral Contracts table below). **Phase 2
    (fallback — BC-ID-anchored mandatory-v inline token):** only when Phase 1 finds no
    pure-version field, locate the field containing the BC ID at a word-boundary match;
    within that field return the FIRST `\bv([0-9]+\.[0-9]+)\b` token appearing AFTER the BC
    ID position (mandatory `v` prefix; `None` if no field carries both the BC ID and a
    subsequent v-prefixed token) — covers `## Token Budget` rows where BC ID and version
    appear inline in one field (e.g. this story's own governing-BC-version citation in the
    Token Budget row below). If neither phase matches, the row is not a version
    citation — skip it (`HookResult::Continue` for that BC). The single-pass bare-optional-v
    form `\bv?([0-9]+\.[0-9]+)\b` is NON-CONFORMING (BC-5.39.010 v1.22 PC13 "Why the prior
    `\bv?([0-9]+\.[0-9]+)\b` bare form (v1.3–v1.7) is NON-CONFORMING" clause): it produces a
    story-ID collision (29 rows / 6 stories — a Trace-column story ID like `S-1.03` reads as
    version `1.03`), a BC-section-number collision (Token Budget rows — `5.39` from
    `BC-5.39.010` matches before the real `v1.7`), and an ACs-column collision (1 row — this
    very story: the ACs column's `DEFERRED v1.6` text would match before the real
    Version-column cell, producing a false BLOCK on S-21.07's own governing citation). All
    read failure semantics per BC-5.39.010 PC14-15; cascade per PC-7 (implementer) — DONE,
    including the v1.20 precondition 13 Phase 1 BC-ID-anchoring amendment (ADV-RECON-003):
    `arm_a2.rs` now requires the row's own first non-empty pipe-delimited field to name the
    target BC (delegating to `arm_a1::first_cell_matches_bc_id`) before attempting Phase 1
    extraction on that row, closing the cross-BC-row false-block/false-pass gap; verified by
    the AC-025 mutant/control/supersession tests listed under Task 21 below, all green in
    the `feature/S-21.07-validate-cross-site-correspondence` worktree.
11. [x] Implement `arm_b.rs`: B1 and B2 logic; three-way comparison; provenance note in block
    message (stale vs fabricated distinction per invariant 11); STORY-INDEX NotFound advisory
    vs CapabilityDenied block (implementer) — DONE
12. [ ] **[DEFERRED v1.6 — Class D; do not implement in v1.6]** Implement `arm_d.rs`: scope-limited extraction per PC30; finding-like shape check;
    exclusion list; advisory message matching BC-5.39.010 postcondition 17 exactly;
    `HookResult::Continue` always (invariant 6) — target: S-21.08 (implementer) — DEFERRED v1.6
    → S-21.08 (Class D not built; confirmed absent from `crates/hook-plugins/validate-cross-site-correspondence/src/`)
13. [x] Implement `arm_e.rs`: E1 positional regex at character position 0; E2 suffix-strip
    then lexicographic comparison; combined E1+E2 single block (postcondition 23) (implementer) — DONE
14. [x] Implement `lib.rs`: dispatch router; read_primary helper; combine violations into
    single block; log advisories independently; UTF-8 `is_char_boundary()` guards (implementer) — DONE,
    including the v1.20 postcondition 25 non-UTF-8-primary-block amendment (ADV-RECON-007,
    see Task 22 below): `lib.rs` now returns `HookResult::block_with_fix(...)` on primary-target
    UTF-8 decode failure, citing invariant 4 (extended, v1.20) — the fail-open
    `Continue`-under-invariant-9 misattribution the amendment declared NON-CONFORMING has
    been removed; `is_char_boundary()` guards (invariant 9, slicing safety only) remain
    unchanged and unaffected by this fix.
15. [x] Add registry entry to `plugins/vsdd-factory/hooks-registry.toml` per AC-020 fields
    (priority 460, PostToolUse, Edit|Write|MultiEdit, on_error=continue) (implementer) — DONE
16. [x] Register bats suite in `plugins/vsdd-factory/tests/run-all.sh` (implementer) — DONE
    (covered by the existing `tests/*.bats` glob in `run-all.sh`; no explicit per-file
    listing required for flat `tests/` suites)
17. [x] Verify all tests pass: `cargo test --workspace --all-targets` green; bats suite green
    — DONE, re-verified against the implementation on branch
    `feature/S-21.07-validate-cross-site-correspondence` (pending merge) in this reconciliation burst:
    `cargo test -p validate-cross-site-correspondence` — 173 passed, 0 failed (the
    `test_BC_corpus_version_sync_all_indexed_bcs_match_frontmatter` regression noted as
    expected-red in the prior burst is now GREEN — the BC-INDEX row has since been synced
    to BC-5.39.010 v1.20); `bats validate-cross-site-correspondence.bats` — 49/49 green;
    `cargo test --workspace --all-targets` — green, no FAILED lines in the full run.
18. [ ] Build release WASM and run smoke test per AC-021; include evidence in PR description
    — release WASM build succeeds (`cargo build --release --target wasm32-wasip1`), but no
    open or merged PR exists for this story's branch as of this burst (`gh pr list` empty) —
    the AC-021 smoke-test-evidence-in-PR-description step has not occurred; left unchecked
19. [x] Verify per-arm POLICY 15 (D-889) mutant + control coverage: each blocking arm (A Arm1,
    A Arm2, B Arm1, B Arm2, E1, E2) has a mutant test confirming the block fires AND a control
    test confirming the guard does not over-block — DONE (MUTANT/CONTROL-suffixed test names
    confirmed present for every blocking arm in the bats suite and unit tests)
20. [x] Add a CHANGELOG entry under [Unreleased] > Added describing the shipped behavior
    before creating the PR — DONE (entry present in the feature worktree's CHANGELOG.md)
21. [x] Implement the BC-5.39.010 v1.20 precondition 13 Phase 1 BC-ID-anchoring amendment
    (ADV-RECON-003) in `arm_a2.rs`: require the row's own first non-empty pipe-delimited
    field to name the target BC (same word-boundary locator-predicate test already used for
    Part A Arm1 and Phase 2) before extracting that row's pure-version field; a cross-BC-row
    match MUST NOT be attributed to a different cited BC (AC-025; Rust unit tests in
    `arm_a2.rs` — mutant
    `test_BC_5_39_010_arm_a2_phase1_bc_id_anchored_no_cross_bc_row_false_block`;
    extraction-layer pin (mutant)
    `test_BC_5_39_010_arm_a2_phase1_bc_id_anchored_cross_bc_row_excluded_from_citations`;
    control `test_BC_5_39_010_arm_a2_phase1_bc_id_anchored_own_row_stale_still_blocks`;
    strong-predicate supersession mutant
    `test_BC_5_39_010_arm_a2_phase1_supersession_first_cell_excluded_from_phase1`)
    (implementer) — DONE. Implemented on branch
    `feature/S-21.07-validate-cross-site-correspondence` (pending merge), in the worktree:
    `arm_a2.rs`
    `row_first_field_matches_bc_id` gates Phase 1 pure-version extraction on the row's own
    first-field BC-ID match (delegating to `arm_a1::first_cell_matches_bc_id` per the strong
    Phase-1-anchor refactor, ADV-RECON2-001), including the compound-supersession-cell case
    (`BC-X (supersedes BC-Y)` excludes BC-Y from Phase 1). All four listed tests present in
    `arm_a2.rs` and pass under `cargo test -p validate-cross-site-correspondence` (173
    passed, 0 failed as of this reconciliation burst).
22. [x] Implement the BC-5.39.010 v1.20 precondition 15a / postcondition 25 non-UTF-8
    primary-target block amendment (ADV-RECON-007) in `lib.rs`: replace the current
    fail-open `Continue`-on-UTF-8-decode-failure path (which mis-cites invariant 9) with
    `HookResult::block_with_fix(...)` per postcondition 25's prescribed message text, under
    the same fail-closed posture as invariant 4 (AC-026; bats tests in
    `plugins/vsdd-factory/tests/validate-cross-site-correspondence.bats` — mutant
    `"ADV-RECON-007: primary target UTF-8 decode failure produces exit code 2 (block), not
    Continue"`; control `"ADV-RECON-007 CONTROL: primary target valid UTF-8 dispatches
    normally (exit code 0)"`) (implementer) — DONE. Implemented on branch
    `feature/S-21.07-validate-cross-site-correspondence` (pending merge): `lib.rs`'s
    `std::str::from_utf8` decode step on the primary-target read now returns
    `HookResult::block_with_fix(...)` citing invariant 4 (extended, v1.20) on decode failure
    — the fail-open `Continue`-under-invariant-9 misattribution has been removed;
    `is_char_boundary()` guards (invariant 9, slicing safety only) are unaffected. Both bats
    tests present in `plugins/vsdd-factory/tests/validate-cross-site-correspondence.bats`
    (`ADV-RECON-007` mutant + `ADV-RECON-007 CONTROL`) and pass under
    `bats validate-cross-site-correspondence.bats` (49/49 green).

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-21.01 (E-21 W1, MERGED) | WASM crate pattern: `src/lib.rs` (entry), `src/classifier.rs` (command detection); POLICY 21 WASM only; on_error=continue fail-open; new crate added to workspace | Path-component-strict matching using `Path::new(p).components()` — not `ends_with` | Crate name must be DISTINCT and DESCRIPTIVE — avoid generic `validate-path` class names; registry entry placement matters (ascending priority order) |
| S-21.06 (E-21 W3, draft) | Also creates new WASM crate + registry entry; registry entry after priority 140; `exec_subprocess` capability for git calls; on_error=continue | PreToolUse pattern differs from PostToolUse: block fires BEFORE tool executes | S-21.06 modifies hooks-registry.toml in W3 — S-21.07 must be in W4 to avoid merge conflict; same file, different entries |
| S-15.07 (validate-index-cite-refresh, MERGED) | First BC-version extraction hook; extracts version strings from BC frontmatter + index rows; line-scanning regex for YAML frontmatter | `test_BC_5_39_003_` naming prefix for BC-specific tests | Do NOT use `serde_yaml` for frontmatter parsing — line scanning is sufficient and avoids the deprecated crate |
| S-15.12 (validate-closes-completeness, MERGED) | Multi-arm PostToolUse hook with 22 ACs; scope-limited extraction for cycle artifacts; advisory-vs-block distinction well-tested | `src/lib.rs` multi-arm dispatch pattern; `test_is_<file>_target()` tests for path guards | Advisory arms MUST be advisory in tests too — do not accidentally assert exit code 2 for Class D scenarios |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| Crate name MUST be `validate-cross-site-correspondence` — matches registry `name` field exactly | BC-5.39.010 §Gate Spec; ARCH-INDEX naming convention | AC-020 registry grep; workspace `cargo test` would fail on rename |
| Path matching MUST use `Path::new(file_path).components().any(...)` — NOT `contains()` or `ends_with()` on the raw string | BC-5.39.010 invariant 3 (path-component-strict per BC-5.39.008 precedent) | AC code review; `grep -n 'ends_with\|contains' src/*.rs` must return 0 matches for path guards |
| BC path derivation MUST use the `derive_bc_path` algorithm (section integer zero-padded to 2 digits) — no `read_dir`, no `glob` | BC-5.39.010 precondition 11; ADR-035 §Alternatives (no list_dir) | `test_BC_5_39_010_arm_a2_bc_path_derivation_correct` unit test; `grep -rn 'read_dir\|list_dir' src/` must return 0 |
| Hook fires PostToolUse ONLY — never PreToolUse | BC-5.39.010 invariant 2 | Registry entry `event = "PostToolUse"`; test suite confirms hook sees content after write |
| Hook NEVER writes to any file | BC-5.39.010 invariant 1 | Crate has no `write_file` capability declaration; code review confirms no `host::write_file` calls |
| `on_error = "continue"` is MANDATORY | ADR-035 §Decision 5 (fuel exhaustion = silent skip, not spurious block); BC-5.39.010 §Gate Spec | AC-020 registry grep |
| `is_char_boundary()` check REQUIRED on all byte-index slicing of UTF-8 content | BC-5.39.010 invariant 9; BC-5.39.008 v1.6 inv-11 | `test_BC_5_39_010_multibyte_utf8_no_panic`; adversary will check for raw byte slices |
| No AI attribution in commits | CLAUDE.md Git Workflow | No `Co-Authored-By: Claude` in any commit |
| `--release --target wasm32-wasip1` build required | ADR-035 §Decision 5 (debug builds exhaust fuel) | CI build step; AC-020 smoke test |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| Rust | workspace MSRV (single-workspace pin via `rust-toolchain.toml`) | WASM plugin crate |
| vsdd-hook-sdk | workspace path dep (`crates/hook-sdk`) | Entry point, `HookResult`, `host::read_file`, `host::log_warn`, `HostError` |
| serde | `1.0` (workspace) | JSON deserialization of dispatch payload |
| serde_json | `1.0` (workspace) | Parse `HookPayload` JSON from dispatcher |
| serde_norway | `0.9` (workspace) | YAML deserialization if any YAML parsing is needed (preferred over serde_yaml) |
| wasm32-wasip1 | target via rustup | WASM compilation target |
| Bats | per `plugins/vsdd-factory/tests/run-all.sh` | Integration test runner |
| wasmtime | `44.0` (workspace — bump to 47 is a separate maintenance story, ADR-035 §Decision 6) | Dispatcher runtime (NOT a hook crate dependency) |

**Note on wasmtime version:** Root `Cargo.toml` workspace has `wasmtime = "44.0"`. ADR-035
§Decision 6 directs a bump to `"47"`. This bump is a SEPARATE maintenance story and does
NOT block S-21.07. This story's crate MUST NOT add a direct `wasmtime` dependency.

## Red Gate Test Plan

| Test ID | Description | Expected Outcome (pre-implementation = RED) |
|---------|-------------|---------------------------------------------|
| RG-001 | Write BC-5.39.010.md with v1.1 and BC-INDEX row at v1.0; hook invoked | FAIL: crate does not exist; block NOT emitted; CI fails with "no such crate" |
| RG-002 | Write story S-21.07 with BC-5.39.010 cited at v1.0 in Token Budget; BC frontmatter v1.1; hook invoked | FAIL: crate does not exist; stale cite passes through |
| RG-003 | Write STORY-INDEX.md with S-21.07 blockquote hash ≠ catalog hash | FAIL: crate does not exist; mismatch passes through |
| RG-004 | **[DEFERRED v1.6 — Class D]** Write burst-log.md last H2 with "Closes: B01, F-S2104-P29-H01" | DEFERRED — Class D arm not compiled in v1.6 hook; target: S-21.08 |
| RG-005 | Write BC-5.39.010.md with version "1.33" and last_amended "(v1.31)" | FAIL: crate does not exist; E1 mismatch passes through |

## Test Plan

| Test ID | Story AC | Test File | Description |
|---------|----------|-----------|-------------|
| T-001 | AC-001 | `crates/.../src/arm_a1.rs` | Arm A1: INDEX row newer than primary (PC2b) → block with [Class A Arm1] / POLICY 14 leg 5 message |
| T-002 | AC-001 | `crates/.../src/arm_a1.rs` | Arm A1 CONTROL: INDEX row current → Continue |
| T-003 | AC-002 | `crates/.../src/arm_a1.rs` | Arm A1: v1.0 not in INDEX → advisory |
| T-004 | AC-002 | `crates/.../src/arm_a1.rs` | Arm A1: v1.1 not in INDEX → block (postcondition 4) |
| T-005 | AC-003 | `crates/.../src/arm_a1.rs` | Arm A1: BC-INDEX CapabilityDenied → block |
| T-006 | AC-003 | `crates/.../src/arm_a1.rs` | Arm A1: BC-INDEX NotFound → advisory + Continue |
| T-007 | AC-004 | `crates/.../src/lib.rs` | Arm A1: primary target CapabilityDenied → block |
| T-008 | AC-005 | `crates/.../src/arm_a2.rs` | Arm A2: stale Token Budget row → block [Class A Arm2] |
| T-009 | AC-005 | `crates/.../src/arm_a2.rs` | Arm A2 CONTROL: current Token Budget row → Continue |
| T-010 | AC-006 | `crates/.../src/arm_a2.rs` | Arm A2: two stale BCs → single combined block |
| T-011 | AC-007 | `crates/.../src/arm_a2.rs` | Arm A2: empty behavioral_contracts → skip |
| T-012 | AC-008 | `crates/.../src/arm_a2.rs` | Arm A2: no version-citing rows → skip that BC |
| T-013 | AC-008 | `crates/.../src/arm_a2.rs` | Arm A2: BC NotFound → advisory for that BC; Continue |
| T-014 | AC-009 | `crates/.../src/arm_b.rs` | B Arm1: B2≠B3 (PC13b; STORY-INDEX catalog hash ≠ blockquote hash) → block with POLICY 18 provenance note |
| T-015 | AC-009 | `crates/.../src/arm_b.rs` | B Arm1 CONTROL: three-way match → Continue |
| T-016 | AC-010 | `crates/.../src/arm_b.rs` | B Arm1: B2/B3 absent → advisory + Continue |
| T-017 | AC-010 | `crates/.../src/arm_b.rs` | B Arm1: no input-hash field → skip entirely |
| T-018 | AC-011 | `crates/.../src/arm_b.rs` | B Arm2: catalog ≠ blockquote → block (cascade) |
| T-019 | AC-011 | `crates/.../src/arm_b.rs` | B Arm2 CONTROL: all agree → Continue |
| T-020 | AC-012 — **[DEFERRED v1.6 — Class D]** | `crates/.../src/arm_d.rs` (S-21.08) | Class D: D-944 excluded → Continue (EC-024) |
| T-021 | AC-012 — **[DEFERRED v1.6 — Class D]** | `crates/.../src/arm_d.rs` (S-21.08) | Class D: S-, BC-, VP-, L-, ADR-, FM- excluded → Continue |
| T-022 | AC-013 — **[DEFERRED v1.6 — Class D]** | `crates/.../src/arm_d.rs` (S-21.08) | Class D ADVISORY: B01 → advisory, NOT block |
| T-023 | AC-013 — **[DEFERRED v1.6 — Class D]** | `crates/.../src/arm_d.rs` (S-21.08) | Class D CONTROL: only F- tokens → Continue, no advisory |
| T-024 | AC-014 — **[DEFERRED v1.6 — Class D]** | `crates/.../src/arm_d.rs` (S-21.08) | Class D: historical section content outside scope → NOT scanned |
| T-025 | AC-015 | `crates/.../src/arm_e.rs` | Class E1: version 1.33 vs last_amended (v1.31) → block |
| T-026 | AC-015 | `crates/.../src/arm_e.rs` | Class E1 CONTROL: version 1.6 vs last_amended (v1.6) → Continue |
| T-027 | AC-015 | `crates/.../src/arm_e.rs` | Class E1: unparseable last_amended → advisory, not block |
| T-028 | AC-016 | `crates/.../src/arm_e.rs` | Class E2: strict-decrease modified[] (date[i] > date[i+1]) → block |
| T-029 | AC-016 | `crates/.../src/arm_e.rs` | Class E2 CONTROL: non-decreasing modified[] (equal consecutive dates allowed) → Continue |
| T-030 | AC-017 | `crates/.../src/arm_e.rs` | Class E1: Prior-chain version positionally excluded → Continue |
| T-031 | AC-018 | `crates/.../src/lib.rs` | Combined A1(PC2b: index-newer)+E1(version≠last_amended) → single block, both classes cited |
| T-032 | AC-019 | `crates/.../src/lib.rs` | Multi-byte UTF-8 content → no panic |
| T-033 | AC-020 | `plugins/vsdd-factory/tests/validate-cross-site-correspondence.bats` | Registry entry present with all required fields |
| T-034 | AC-021 | Manual / PR description | Smoke test: live session block fires on stale BC write |
| T-P6A | AC-022 | `crates/.../src/arm_a1.rs` | Arm A1: primary newer than INDEX (PC2a; POLICY 3 burst-ordering artefact) → exit 0, advisory message |
| T-P6C | AC-023 | `crates/.../src/arm_b.rs` | B Arm1: B2==B3 AND B1≠B2 (PC13a; POLICY 3 burst-ordering artefact) → exit 0, advisory message |
| T-P8A | AC-024 | `crates/.../src/arm_b.rs` | B Arm1: B2 present and differs from B1, B3 absent (PC13c half-present mutant) → exit 0, advisory (`arm_b1_half_present_catalog_mismatch_is_advisory_not_block`) |
| T-P8B | AC-024 | `crates/.../src/arm_b.rs` | B Arm1: B2 absent, B3 present and differs from B1 (PC13c symmetric mutant) → exit 0, advisory (`arm_b1_half_present_blockquote_mismatch_is_advisory_not_block`) |
| T-P8C | AC-024 | `crates/.../src/arm_b.rs` | B Arm1 CONTROL: B2 present and matches B1, B3 absent (PC12 absent-site path) → exit 0, advisory (`arm_b1_half_present_catalog_match_is_advisory`) |

## File Structure Requirements (MANDATORY)

| File | Action | Notes |
|------|--------|-------|
| `crates/hook-plugins/validate-cross-site-correspondence/Cargo.toml` | create | New crate; workspace dep on hook-sdk; NO serde_yaml/wasmtime direct dep; add to workspace `Cargo.toml` members |
| `crates/hook-plugins/validate-cross-site-correspondence/src/lib.rs` | create | Hook entry point; dispatch router; combine_violations helper; UTF-8 guards |
| `crates/hook-plugins/validate-cross-site-correspondence/src/dispatch.rs` | create | `is_bc_file`, `is_story_file`, `is_story_index`, `is_cycle_artifact` — path-component-strict |
| `crates/hook-plugins/validate-cross-site-correspondence/src/frontmatter.rs` | create | `extract_frontmatter_field`, `extract_frontmatter_sequence` — line-scanning; no serde_norway needed |
| `crates/hook-plugins/validate-cross-site-correspondence/src/arm_a1.rs` | create | Arm A1 impl: BC→INDEX version check; `derive_bc_path` |
| `crates/hook-plugins/validate-cross-site-correspondence/src/arm_a2.rs` | create | Arm A2 impl: story→BC citation check; `extract_story_bc_version_citations`; cascade |
| `crates/hook-plugins/validate-cross-site-correspondence/src/arm_b.rs` | create | B Arm1 + B Arm2 impl; three-way comparison; provenance note |
| `crates/hook-plugins/validate-cross-site-correspondence/src/arm_d.rs` | **DEFERRED v1.6 — Class D; do NOT create** | Class D impl: scope-limited extraction; exclusion list; advisory-only — implementation target: S-21.08 |
| `crates/hook-plugins/validate-cross-site-correspondence/src/arm_e.rs` | create | Class E1 + E2 impl; positional regex anchor; suffix-strip for E2 |
| `plugins/vsdd-factory/hook-plugins/validate-cross-site-correspondence.wasm` | create (build artifact) | Release build; staged from `target/wasm32-wasip1/release/` |
| `plugins/vsdd-factory/hooks-registry.toml` | modify | Add entry at priority 460 per AC-020 fields |
| `plugins/vsdd-factory/tests/validate-cross-site-correspondence.bats` | create | Bats suite: T-033 fixture scenarios + payload delivery; follows `validate-closes-completeness.bats` pattern |
| `plugins/vsdd-factory/tests/fixtures/validate-cross-site-correspondence/` | create | Fixture files: mock BC files, BC-INDEX stubs, story stubs, STORY-INDEX stubs, cycle artifact stubs |
| `plugins/vsdd-factory/tests/run-all.sh` | modify | Add `validate-cross-site-correspondence.bats` to suite list |
| `Cargo.toml` (workspace root) | modify | Add `crates/hook-plugins/validate-cross-site-correspondence` to `[workspace] members` |
| `CHANGELOG.md` | modify | Add entry under [Unreleased] > Added before PR creation |

## Changelog

| Version | Date | Change |
|---------|------|--------|
| 1.20 | 2026-08-14 | F-S2107-RECON-002 close (adversary recon, MEDIUM; TD-VSDD-091 anti-volatile-pin correction): Task 10's Phase 1 explanatory example — "covers the isolated Version column of `## Behavioral Contracts` body-table rows (e.g. this story's own `1.20` cell in the Behavioral Contracts table below)" — pinned a hard-coded version number (`1.20`) that had already drifted from the live governing-BC-version cell, which correctly reads `1.22` (advanced by the v1.15/v1.17/v1.18 sweeps while this illustrative example was missed). Fixed PIN-FREE, not by re-pinning 1.20→1.22 (which would only decay again on the next BC bump): reworded to "this story's own governing-BC-version cell in the Behavioral Contracts table below" — the example points at the cell as source of truth without asserting a specific number. Comprehensive story-wide sweep for the same defect class (TD-VSDD-091) found and likewise de-versioned the symmetric Phase 2 explanatory example three lines below — "covers `## Token Budget` rows where BC ID and version appear inline in one field (e.g. `BC-5.39.010 v1.22 (full text...)`)" — reworded to "this story's own governing-BC-version citation in the Token Budget row below" (it carried the identical illustrative-pointer-to-a-live-cell risk; it only happened to still read the current value because it was swept in lockstep with the Token Budget row itself at v1.18, with nothing preventing it decaying the same way Task 10's Phase 1 example just did). Every other live `v1.2x`/`1.2x` site in the story was triaged and left unchanged: (a) normative "Under BC-5.39.010 v1.22 PCxx..." anchors, which this story's own established full-sweep precedent (v1.15/v1.17/v1.18) already advances on every BC bump; (b) worked-example/fixture DATA for a DIFFERENT BC (BC-6.26.001 AC-005/EC-026 fixtures; BC-9.01.001/BC-9.01.002 AC-027 S-4.08 corpus row), which TD-VSDD-091 explicitly excepts; (c) amendment-provenance labels naming WHEN a clause was introduced (e.g. "v1.14 PC2...", "the v1.21 same-field scan-stop", Task 21/22's "BC-5.39.010 v1.20 precondition 13/15a..." headings, "the BC-INDEX row has since been synced to BC-5.39.010 v1.20"), already established as exempt historical markers by this story's own prior sweep entries. The `## Behavioral Contracts` table Version cell and `## Token Budget` BC row (citation-of-record cells, POLICY 8 canonical scope) were independently confirmed to already read `1.22` — unchanged by this fix. No AC scope, count, cap VALUE, or normative behavior changed — two illustrative-example rewordings only. input-hash unchanged (no `inputs:` file touched by this fix). STORY-INDEX re-sync owed to state-manager (S-21.07 v1.19→v1.20). (story-writer; closes F-S2107-RECON-002.) |
| 1.19 | 2026-08-14 | F-S2107-RECON-001 close (adversary recon, MEDIUM; POLICY 4 semantic-anchoring correction): AC-019 §Build constraints `max_bytes` cap enumeration mislabeled the STORY-INDEX.md secondary-read cap (`max_bytes = 1048576`, `timeout_ms = 3000`) as "(B2)" — WRONG; this value matches `STORY_INDEX_B1_MAX_BYTES`/`STORY_INDEX_B1_TIMEOUT_MS` in `crates/hook-plugins/validate-cross-site-correspondence/src/arm_b.rs` lines 39-40 (Arm B1 secondary-index read), not Arm B2. The adjacent row (`max_bytes = 2097152`, `timeout_ms = 5000`, correctly labeled "B2 — Arm B2 write trigger") already matches `STORY_INDEX_B2_MAX_BYTES`/`STORY_INDEX_B2_TIMEOUT_MS` (`arm_b.rs` lines 44-45) and needed no change. Fixed: the mislabeled row's arm citation corrected from "(B2)" to "(B1 — Arm B1 secondary read)"; cap VALUE unchanged. Sibling-sweep (TD-VSDD-060) of the full six-row `max_bytes` cap enumeration (BC file primary, BC-INDEX.md, Story file primary, STORY-INDEX.md B1, STORY-INDEX.md B2, Cycle artifacts D-arm) against `arm_b.rs` lines 39-45 and `lib.rs` lines 458-461 confirmed the other five rows are correctly labeled and valued — no further mislabels found. No AC scope, count, cap VALUE, or BC-version cite changed — arm-label correction only. input-hash unchanged: this fix touches only this story's own documentary prose, not any `inputs:` file. STORY-INDEX re-sync owed to state-manager (S-21.07 v1.18→v1.19). (story-writer; closes F-S2107-RECON-001.) |
| 1.18 | 2026-08-14 | ADV-RECON11-001 close (product-owner BC-5.39.010 v1.21→v1.22 architect-adjudicated amendment; story-writer BC-version-cite propagation + AC-028 close): BC-5.39.010 v1.22 adds precondition 15b / postcondition 26 ("Secondary Index-File UTF-8 Decode Failure") — DISTINCT from precondition 15a / postcondition 25 (primary-target decode failure, BLOCK). Scope: Arm A1's BC-INDEX.md secondary read and Arm B1's STORY-INDEX.md secondary read only (Arm B2 explicitly excluded — STORY-INDEX.md is Arm B2's primary target, already governed by 15a/25). A non-UTF-8 secondary index-file read (bytes decode OK, UTF-8 decode fails) MUST emit a DISTINCT advisory naming the index-file path and stating the row/hash state for `<id>` is INDETERMINATE, not confirmed-absent, + `HookResult::Continue` — MUST NOT fall through into `RowAbsent` (Arm A1; would trigger postcondition 4's misleading "dropped registration" BLOCK for any cited BC with `version:` > "1.0") or `(None, None)` (Arm B1; would trigger postcondition 12's generic fail-open "not yet registered" advisory, silently disabling three-way hash checking with no disclosure of the decode failure). Disposition is ADVISORY (Continue), not block — deliberately lower severity than precondition 15a / postcondition 25's primary-target BLOCK, per the LOW/near-zero-reachability architect adjudication. New EC-040 (combined Arm A1 + Arm B1 scenario). Per POLICY 8 canonical scope (CLAUDE.md §Architectural Authority item 7), the mechanically-checked minimum cite sites are the `## Behavioral Contracts` table row and the `## Token Budget` row — both updated v1.21→v1.22; BC table ACs cell extended to enumerate AC-028; Token Budget EC count 39→40 (BC-5.39.010 v1.22 added EC-040). Per this story's own established full-sweep precedent (v1.15/v1.17), every other live governing-BC-version cite was also advanced v1.21→v1.22 this same burst: title, H1 heading, Wave Placement Justification, BC Status governing-BC statement and active-AC-range summary, the "restates the governing algorithm text" clause, the PC2a/PC2b/PC13a/PC13b/PC13c "Under BC..." anchors, the Fixture rationale provenance boundary, all four AC-020 §Gate Spec anchors, and the AC-025/AC-026/AC-027 heading + body "Under BC..." anchors. Left as historical (NOT swept, by design): the `[Prior: ...]` chain, historical `## Changelog` rows, and amendment-provenance labels that name WHEN a clause was introduced rather than the current governing version. A story-wide grep confirmed zero live (non-historical, non-provenance-labeled) `v1.21` residuals remained in the body. **New AC-028** (ADV-RECON11-001, precondition 15b / postcondition 26 / EC-040) — asserts a non-UTF-8 secondary INDEX read (BC-INDEX.md for Arm A1; STORY-INDEX.md for Arm B1) emits the distinct INDETERMINATE advisory naming the index file, not a misleading RowAbsent block (Arm A1) or a generic "not yet registered" advisory (Arm B1). Cites mutant tests `test_BC_5_39_010_arm_a1_non_utf8_bc_index_indeterminate_advisory_not_block` (`arm_a1.rs`) and `test_BC_5_39_010_arm_b1_non_utf8_story_index_indeterminate_advisory` (`arm_b.rs`) — both confirmed present in the `feature/S-21.07-validate-cross-site-correspondence` worktree; and pre-existing control tests `test_BC_5_39_010_arm_a1_new_v1_0_not_in_index_advisory` (AC-002, `arm_a1.rs`) and `test_BC_5_39_010_arm_b1_absent_index_sites_advisory` (AC-010, `arm_b.rs`), confirming a genuinely-absent row/site in a DECODABLE index file still produces the normal advisory disposition, unaffected by the v1.22 amendment. No AC renumbering (POLICY 1 append-only). input-hash: 397e06d→3885444 (BC-5.39.010.md v1.21→v1.22; `compute-input-hash S-21.07-validate-cross-site-correspondence.md --update`). STORY-INDEX re-sync owed to state-manager (S-21.07 v1.17→v1.18, BC v1.22, 40 ECs). (story-writer; closes ADV-RECON11-001.) [Row backfilled 2026-08-14 by story-writer, closing F-S2107-RECON11-BACKFILL-001 (ADV-RECON11-001 changelog-table parity gap): frontmatter `last_amended:`/`modified:` already documented this v1.18 change in full; this table row was missing, a POLICY 14 frontmatter↔changelog parity violation flagged in the now-superseded v1.19 row NOTE. Content transcribed verbatim from the `last_amended:` `[Prior: ... (v1.18) ...]` block; no new content invented. Version NOT bumped for this backfill (historical-parity correction to an already-past version's row, not a new content change to the current version); `version:`/`last_amended:`/`modified:` frontmatter unchanged.] |
| 1.17 | 2026-08-14 | ADV-RECON5-003 close (product-owner BC-5.39.010 v1.20→v1.21 adjudication amendment, human-approved "Amend now (v1.21)"; story-writer BC-version-cite propagation + AC-027 close): BC-5.39.010 v1.21 adds a PC13 Phase 2 same-field scan-stop — within the Phase 2 anchor field, the forward `\bv([0-9]+\.[0-9]+)\b` scan after the BC ID's position MUST terminate, without producing a version, the moment it encounters a DIFFERENT `BC-S.SS.NNN` word-boundary token before any qualifying v-token; the field then yields NO citation and the caller proceeds to the next pipe-delimited field per the existing per-field fallback. Corpus-confirmed reachable, not hypothetical (2026-08-14): `.factory/stories/S-4.08-rc1-release-gate.md`'s `## Behavioral Contracts` row for `BC-9.01.002` — Trace cell mentions `BC-9.01.001` then, later in the SAME field past an intervening `BC-9.01.002` mention, an unrelated "v1.1 BC candidate" phrase — the v1.20 field-scoped-only algorithm still extracted "v1.1" as `BC-9.01.001`'s citation despite its real frontmatter version being "1.0", a live false BLOCK. New EC-039 + new Canonical Test Vector row ("A Arm2 — Phase 2 same-field scan-stop (v1.21 / ADV-RECON5-003)") added to BC-5.39.010 v1.21. **Per POLICY 8 canonical scope** (CLAUDE.md §Architectural Authority item 7 — bcs frontmatter array changes propagate atomically to body BC table + ACs + Token Budget), the mechanically-checked minimum cite sites are the `## Behavioral Contracts` table row and the `## Token Budget` row — both updated v1.20→v1.21; BC table ACs cell extended to enumerate AC-027; Token Budget EC count 38→39 (BC-5.39.010 v1.21 added EC-039). This burst elected the broader ADV-RECON3-004-style full sweep (the same precedent established at v1.15) rather than stopping at the POLICY-8 minimum: every other live governing-BC-version cite — title, H1, narrative, BC Status governing-BC statement, the PC2a/PC2b/PC13a/PC13b/PC13c anchors, the Fixture rationale provenance boundary, and all three AC-020 §Gate Spec anchors — was also advanced v1.20→v1.21 this same burst. Only genuinely-historical append-only content (the `[Prior: ...]` chain, historical `## Changelog` rows below this one, and amendment-provenance labels that document WHEN a clause was introduced, e.g. Task 21/22's "BC-5.39.010 v1.20 precondition 13..." headings) remain at v1.20. **New AC-027** (ADV-RECON5-003, PC13 Phase 2 same-field scan-stop / EC-039 / new Canonical Test Vector "A Arm2 — Phase 2 same-field scan-stop") — asserts a `## Behavioral Contracts` row whose first field names a DIFFERENT BC, whose anchor field mentions the target BC followed by a DIFFERENT BC token before an unrelated v-token, yields NO citation (Phase 2 scan-stops → no false block) — the S-4.08 BC-9.01.002 shape. Cites the actual Rust unit tests in `crates/hook-plugins/validate-cross-site-correspondence/src/arm_a2.rs`: mutant `test_BC_5_39_010_arm_a2_phase2_same_field_scan_stop_different_bc_no_false_citation` (verified present in the `feature/S-21.07-validate-cross-site-correspondence` worktree); control `test_BC_5_39_010_arm_a2_pc13_phase2_anchor_field_with_v_token_produces_citation` (pre-existing ADR-038 §Decision 5 Fixture 2 control test — the OWN BC's anchor field precedes its own version token with no intervening different-BC-ID token, still resolves correctly; confirms the scan-stop fix does not weaken legitimate same-field resolution). No AC renumbering (POLICY 1 append-only). input-hash: fd73ba8→397e06d (BC-5.39.010.md v1.20→v1.21; `compute-input-hash S-21.07-validate-cross-site-correspondence.md --update`). STORY-INDEX re-sync owed to state-manager (S-21.07 v1.16→v1.17). (story-writer; closes ADV-RECON5-003.) In-place v1.17 correction (same burst, pre-merge, finalizing v1.17; no version bump; input-hash unchanged 397e06d): (1) ADV-RECON6-001 — the §BC Status preamble's active-AC enumeration `ACs 001–011, 015–024 are active` had not been advanced when AC-025/AC-026 (v1.15) and AC-027 (this burst) were added; corrected to `015–027` with provenance recorded for all three; a story-wide grep confirmed this was the only live active-AC-range summary sentence in the body (the `## Behavioral Contracts` table ACs cell already correctly read "AC-001 through AC-027"). (2) ADV-RECON6-002 — the scope sentence above, and the matching `last_amended:`/`modified:` frontmatter narratives, previously claimed only the `## Behavioral Contracts` table row and `## Token Budget` row were swept v1.20→v1.21 this burst, with title/H1/BC Status/AC-020 anchors "intentionally left" as historical markers; a story-wide grep proved this false — those sites had already been fully swept to v1.21 in the same burst (per the v1.15 ADV-RECON3-004 full-sweep precedent). The scope sentence above is rewritten to describe the sweep actually performed; the frontmatter narratives are corrected identically. (story-writer; closes ADV-RECON6-001, ADV-RECON6-002.) Further in-place v1.17 addition (same burst, pre-merge, finalizing v1.17; no version bump; input-hash unchanged 397e06d): ADV-RECON10-001 close (adversary recon-pass-10, LOW; completeness/intent question) — `anchored_adrs: [ADR-035]` and `inputs:` list only ADR-035, not ADR-038 or ADR-042, despite the implemented algorithm depending substantively on ADR-038 §Decision 1/5 (first-token-of-last-chain-entry; two-phase PC13 BC-ID-anchored extraction) and ADR-042 (fuel-cap framing). Resolved as Option B (rationale documentation, no `inputs`/`input-hash`/version change): confirmed by inspection that BC-5.39.010 v1.21 §Preconditions PC13 and §Gate Specifications restate the ADR-038/ADR-042 algorithm and fuel-cap decisions verbatim in the BC's own normative prose (not merely a pointer to the ADRs), and confirmed the implementation (`crates/hook-plugins/validate-cross-site-correspondence/src/{lib.rs,arm_a2.rs,arm_b.rs,arm_e.rs}`) anchors every behavioral doc-comment/test name to BC-5.39.010 clause IDs, citing ADR-038/ADR-042 only as secondary rationale never as sole authority. Because the algorithm and fuel-cap text are fully self-contained in BC-5.39.010 (an existing `inputs:` member), a required-behavior change without a BC-5.39.010 edit is not possible — the input-hash coverage gap the adversary flagged does not create real drift risk. New rationale paragraph added to §BC Status ("`anchored_adrs:` scope rationale (ADV-RECON10-001 close, LOW)") plus a frontmatter comment above `anchored_adrs: [ADR-035]` pointing to it. (story-writer; closes ADV-RECON10-001.) |
| 1.16 | 2026-08-14 | ADV-RECON4-002 close (adversary recon-pass-4, MEDIUM; POLICY 17 frontmatter-parity violation): the v1.15 frontmatter `last_amended` narrative (and its mirror `modified:` bullet) described a PRE-FIX snapshot — Task 21 (PC13 Phase 1 BC-ID anchoring, `arm_a2.rs`) and Task 22 (non-UTF-8 primary-target block, `lib.rs`) "confirmed NOT yet implemented"; evidence HEAD `2b323c05`; `cargo test` 168 passed/1 failed; `bats` 47/47 passed; BC-INDEX sync "owed to state-manager" — that directly contradicted the already-corrected body (Task list Tasks 21/22 `[x]` DONE; the v1.15 `## Changelog` row below, amended in-place same burst) which correctly stated Tasks 21/22 implemented, 173 passed/0 failed, 49/49 bats, and BC-INDEX already synced to v1.20. Root cause: an earlier minimize-churn instruction froze the frontmatter narrative at authoring time on the theory that in-place body corrections did not need frontmatter back-porting; the adversary correctly identified that theory as producing a live frontmatter-vs-body contradiction (POLICY 17), so it is retracted for this defect class. Fix: real version bump (not another in-place correction). `last_amended:` rewritten with a v1.16 head narrative confirming the delivered truth (Tasks 21/22 DONE; 173 unit tests / 0 failed; 49/49 bats; BC-INDEX synced v1.20; primary UTF-8 decode fail-CLOSED under invariant 4 extended, not the old invariant-9 fail-open); the original v1.15 text preserved verbatim as a nested `[Prior: ...]` historical entry (POLICY 1 append-only — not rewritten, since it is a legitimate as-of-that-moment authoring record). A new `modified:` v1.16 bullet added alongside the (unedited) v1.15 bullet. SHA-drift de-pinned per TD-VSDD-091: the adversary found three different HEAD SHAs cited for one delivery (`2b323c05` frontmatter, `68c928b6` body Tasks 17/21/22, and the branch has since advanced further) — every LIVE (non-Changelog) delivery-state citation now reads "implemented on branch `feature/S-21.07-validate-cross-site-correspondence` (pending merge)" instead of a pinned HEAD SHA, in the new `last_amended:` head narrative and in body Tasks 17, 21, and 22. The `## Changelog` v1.15 row's own originally-recorded SHAs (`2b323c05`, `68f7f40b`, `68c928b6`, `bd6a4a7e`, `6f92702e`, `05a9ff3a`, `2aecf52f`) are left unchanged — those are dated historical verification records made at the time they were true, exempt from de-pinning per POLICY 1 append-only, not live current-state claims. Count/status consistency sweep (class-complete): `grep -nE "(47/47\|168\|169\|171 unit\|owed to state-manager\|NOT yet implemented\|fail-OPEN\|invariant 9)"` run across the whole story; every live (non-append-only) hit reconciled — the only surviving instances of the stale counts/status are inside this field's own historical `[Prior: ...]` chain, the `modified:` array's per-version historical bullets, and the historical `## Changelog` rows below this one, all exempt under POLICY 1; body Task list, BC Status, and BC/Token-Budget tables were already correct pre-burst and needed no change beyond the three SHA de-pins above. No AC scope, count, or BC-version change; BC-5.39.010 remains v1.20 (no BC amendment this burst). input-hash unchanged: fd73ba8 (no `inputs:` file changed — frontmatter/body coherence correction only). STORY-INDEX re-sync owed to state-manager (S-21.07 v1.15→v1.16, per this version bump). (story-writer; closes ADV-RECON4-002.) |
| 1.15 | 2026-08-14 | BC-5.39.010 v1.19→v1.20 propagation (POLICY 8 canonical scope — `## Behavioral Contracts` table row + `## Token Budget` row only, per CLAUDE.md §Architectural Authority item 7; the other v1.19 prose cites sit outside Arm A2's own scan-confined sections and are intentionally left as historical-version markers, not an incomplete sweep). Two new ACs from BC-5.39.010 v1.20's amendments: AC-025 (ADV-RECON-003, precondition 13 Phase 1 BC-ID-anchoring / EC-037) — cross-BC `## Behavioral Contracts` table row does NOT produce a false block/pass (Rust unit tests in `arm_a2.rs`: mutant `test_BC_5_39_010_arm_a2_phase1_bc_id_anchored_no_cross_bc_row_false_block`, extraction-layer pin (mutant) `test_BC_5_39_010_arm_a2_phase1_bc_id_anchored_cross_bc_row_excluded_from_citations`, control `test_BC_5_39_010_arm_a2_phase1_bc_id_anchored_own_row_stale_still_blocks`, strong-predicate supersession mutant `test_BC_5_39_010_arm_a2_phase1_supersession_first_cell_excluded_from_phase1`); AC-026 (ADV-RECON-007, precondition 15a / postcondition 25 / invariant 4 extension / EC-038) — non-UTF-8 primary target MUST block, not silently Continue (bats tests in `plugins/vsdd-factory/tests/validate-cross-site-correspondence.bats`: mutant `"ADV-RECON-007: primary target UTF-8 decode failure produces exit code 2 (block), not Continue"`, control `"ADV-RECON-007 CONTROL: primary target valid UTF-8 dispatches normally (exit code 0)"`). In-place v1.15 correction (same burst, pre-merge): the AC-025/AC-026 test-citation bullets and Task 21/22 test citations originally named placeholder identifiers that did not match the tests the test-writer actually implemented (commit `68f7f40b`); corrected to the exact identifiers above — no AC scope, count, or normative behavior changed; input-hash unchanged. BC table ACs cell extended to AC-001–AC-026; Token Budget EC count 36→38. OBS-P23-A: Token Budget Estimated-Tokens rows sum to ~47,500, not the stated ~47,000 — Total and Budget usage corrected to ~47,500 / ~23.75%. AC-019 heading citation corrected (cited invariant 10; body and invariant list both correctly cite invariant 9 for UTF-8 safety — heading corrected to invariant 9). Task checkboxes reconciled against the actual build state verified in the `feature/S-21.07-validate-cross-site-correspondence` worktree (HEAD `2b323c05`): 171 unit tests (168 pass / 1 expected-red pending state-manager's BC-INDEX sync for this very v1.20 bump) + 47/47 bats tests green; crate built, wired into the workspace, registered in `hooks-registry.toml`, and covered by CHANGELOG.md. Tasks 1-11, 13-17, 19-20 checked `[x]`. Task 12 (Class D) left unchecked, annotated "DEFERRED v1.6 → S-21.08 (Class D not built)" — confirmed absent from the crate. Task 18 (smoke test + PR evidence) left unchecked — no PR exists yet for this branch. Two new unchecked tasks added for the v1.20 amendment scope: Task 21 (PC13 Phase 1 BC-ID anchoring in `arm_a2.rs` — confirmed not yet implemented) and Task 22 (non-UTF-8 primary-target block in `lib.rs` — confirmed not yet implemented; current code fail-opens citing invariant 9, the exact misattribution the amendment declares NON-CONFORMING). input-hash: 93c4a89→fd73ba8 (BC-5.39.010.md v1.19→v1.20). Further in-place v1.15 addition (same burst, pre-merge, finalizing v1.15): AC-025's and Task 21's test-citation lists extended with a fourth test, `test_BC_5_39_010_arm_a2_phase1_supersession_first_cell_excluded_from_phase1` (Rust unit test, `arm_a2.rs`) — an additional mutant / strong-predicate regression pin proving a compound first cell of the form `BC-X (supersedes BC-Y)` excludes BC-Y from Phase-1 extraction, a harder shape than the existing Notes-cell tests since BC-Y's word-boundary match occurs inside the row's own first cell rather than a separate Notes/Trace cell; no AC scope, count, BC citation, or normative behavior changed; no version bump; input-hash unchanged (fd73ba8). Further in-place v1.15 correction (ADV-RECON3-002/ADV-RECON3-004 close, same burst, pre-merge, finalizing v1.15): (1) ADV-RECON3-002 (MEDIUM) — Tasks 21 and 22, and the trailing status notes on Tasks 10 and 14 that referenced them, described a PRE-FIX snapshot ("confirmed NOT yet implemented") that no longer matched the delivered worktree; verified against HEAD `68c928b6` (commits `bd6a4a7e`, `6f92702e`, `05a9ff3a`, `68c928b6`) that both the PC13 Phase 1 BC-ID-anchoring fix (`arm_a2.rs::row_first_field_matches_bc_id`, delegating to `arm_a1::first_cell_matches_bc_id`) and the non-UTF-8 primary-target block (`lib.rs`, `HookResult::block_with_fix` citing invariant 4 extended, replacing the invariant-9 fail-open misattribution) are implemented and green; Tasks 21/22 checked `[x]` and their bodies, plus Task 10's and Task 14's trailing notes, rewritten to the DONE state; the stale `47/47 bats` cite (Tasks 4/17) corrected to `49/49` (confirmed via `bats plugins/vsdd-factory/tests/validate-cross-site-correspondence.bats` — 49/49 green) and Task 17's stale `168 passed, 1 failed` cite corrected to `173 passed, 0 failed` (the previously-expected-red `test_BC_corpus_version_sync_all_indexed_bcs_match_frontmatter` is now green — BC-INDEX.md was synced to BC-5.39.010 v1.20 by a separate state-manager burst, commit `2aecf52f`). (2) ADV-RECON3-004 (LOW) — the title, H1, and every other live "governing BC version" cite outside the `## Behavioral Contracts` table row / `## Token Budget` row (narrative, BC Status governing-BC statement, AC-001/009/019/020(x3)/022/023/024 anchors, Fixture rationale provenance boundary, Task 1) still read `v1.19`; per re-review, the narrower POLICY-8 scope recorded earlier in this same v1.15 entry is superseded — these are live current-governing-BC-version cites, not historical markers, and the substantive claims they anchor (Classes A/B/E; Class D deferred; PC2/PC13(postcondition)/PC5 unchanged since v1.14; PC2b/E1 content unchanged; §Gate Spec content last amended v1.18) are all still accurate at v1.20, so the sweep is a pure version-token correction with no substantive rewrite; all swept to `v1.20`. Left AS historical (NOT swept, by design): the frontmatter `last_amended:` v1.15 narrative and the `modified:` array v1.15 bullet (both frozen-at-authoring per this story's own established precedent — the two prior in-place v1.15 corrections above were likewise narrated only in this Changelog row, never back-ported into `last_amended:`/`modified:`); Task 10's internal algorithm-provenance mechanics were rewritten (not merely swept) since the DONE/NOT-yet-implemented boundary they marked no longer exists now that Task 21 is complete; the append-only `[Prior: ...]` chain and every historical `## Changelog` row below this one (POLICY 1). No AC scope, count, or normative behavior changed by either fix; input-hash unchanged (fd73ba8) — no `inputs:` file changed, this is a body-only coherence correction. In-place at v1.15 (no version bump) per explicit instruction. |
| 1.14 | 2026-08-13 | F-S2107-P21-001 close (adversary pass-21, HIGH; POLICY 4 spec-implementation contradiction + TD-VSDD-060 algorithm-content propagation gap): Task 10's `extract_story_bc_version_citations` build directive specified the single-pass bare-optional-v regex `\bv?([0-9]+\.[0-9]+)\b`, labeled "per BC-5.39.010 v1.19 PC13 (unchanged since v1.14)" — the EXACT form BC-5.39.010 v1.19 PC13 declares NON-CONFORMING (story-ID collision 29 rows/6 stories; BC-section-number collision on Token Budget rows; ACs-column collision on this story's own governing citation — see BC-5.39.010 v1.19 "Why the prior bare form is NON-CONFORMING" clause). §BC Status (this story) already correctly documented the two-phase algorithm since v1.5; Task 10 was the stale outlier. ROOT CAUSE: the regex was correct at story v1.2 against BC v1.3 PC13 (then bare-form); BC v1.8 replaced PC13 with the two-phase algorithm and declared the bare form NON-CONFORMING, but subsequent BC-version-cite propagation sweeps (v1.4–v1.13) advanced Task 10's cited VERSION NUMBER without re-deriving the REGEX CONTENT — the false "(unchanged since v1.14)" annotation was itself evidence of the gap. Task 10 rewritten to the two-phase algorithm verbatim (Phase 1 pure-version field `^v?([0-9]+\.[0-9]+)$` for `## Behavioral Contracts` body-table Version cells; Phase 2 BC-ID-anchored mandatory-v inline token `\bv([0-9]+\.[0-9]+)\b` for `## Token Budget` rows), with collision rationale restated inline. Class-complete algorithm-content sweep (TD-VSDD-060) performed story-wide: `grep -nE` for the bare-regex literal and for "rightmost"/"Phase 1"/"Phase 2"/"two-phase" tokens confirmed exactly one other hit — the append-only Changelog v1.2 row (2026-07-30), correctly documenting that Task 10's regex WAS the bare form at that historical point (true then, pre-dating BC v1.8) — left unchanged per POLICY 1. §BC Status's own PC5/PC13 narrative independently verified already conformant with BC-5.39.010 v1.19 (two-phase PC13; first-token-of-last-chain-entry PC5) — no correction needed there. No Red-Gate-anchored AC or Test Plan assertion is keyed to the bare-vs-two-phase distinction: existing Arm A2 fixtures (AC-005 through AC-008) all use Token Budget-style v-prefixed citations that both algorithms extract identically — documentary/build-directive correction only, not a test-behavior change. Flagged to test-writer (not actioned in this burst, out of this finding's scope): no corpus-style regression test analogous to BC-5.39.010's `corpus_arm_a1` requirement exists for Arm A2's collision-avoidance behavior (this story's own BC-table ACs cell "DEFERRED v1.6" is a live instance of the ACs-column hazard); recommend a negative test (e.g. `test_BC_5_39_010_arm_a2_no_false_block_on_acs_column`) in a future pass. No AC scope, count, or other test assertion changed. input-hash unchanged: 93c4a89 (`inputs:` list not modified — body-only fix). (story-writer; closes F-S2107-P21-001.) |
| 1.13 | 2026-08-13 | F-S2107-P19-001 close (adversary pass-19, MEDIUM; POLICY 8 BC-version-cite propagation completeness correction): the v1.12 F-S2107-P18-001 sweep was INCOMPLETE — its grep predicate (`grep -niE "BC-5\.39\.010 v1\.18"`, single literal space, prefix-required) missed two live residuals invisible to a prefix-required single-space match. (1) AC-019 §Build constraints: a line-wrapped cite (`BC-5.39.010` at end of line, `v1.18 §Gate Spec, F-S2107-P10-004` at start of next — the un-swept third member of a triplet whose two siblings in AC-020 Notes were already correctly advanced) corrected to `v1.19`. (2) AC-018 §Fixture rationale: provenance upper-bound "PC2b/E1 content unchanged through v1.18" corrected to "through v1.19" (BC-5.39.010 v1.19 last_amended confirms §Postconditions incl. PC2b and §Edge Cases are unchanged at v1.19). The v1.12 entry's "Post-sweep verification ... confirmed zero live residuals" claim was FALSE — both sites were live body prose, not append-only/Changelog material. Corrected verification used a whitespace-tolerant + bare-token predicate (`grep -niE "v1\.1[0-8]"` plus line-wrap-specific `grep -nA1 "BC-5\.39\.010$"` and `grep -niE "BC-5\.39\.010[[:space:]]"`); every hit individually triaged — all surviving `v1.1[0-8]` occurrences are this field's own append-only `[Prior: ...]` chain, body Changelog historical rows (v1.5–v1.12), legitimately-historical §BC Status provenance annotations ("unchanged since v1.14" etc.), or fixture/example version strings for the unrelated example BC `BC-6.26.001` (AC-005/AC-006, EC-002/EC-026) — zero live residuals of BC-5.39.010's governing version remain. No AC scope, test behavior, or count number changed — version-cite and provenance-boundary correction only. input-hash unchanged: 93c4a89 (`inputs:` list not modified — body-only fix). (story-writer; closes F-S2107-P19-001.) |
| 1.12 | 2026-08-13 | F-S2107-P18-001 close (POLICY 8 BC-version-cite propagation; TD-VSDD-060 class-complete sweep): BC-5.39.010 v1.18→v1.19 propagation (product-owner reconciled §VP Anchors reserved-range count 17→19 to match its own 19-entry VP table; no count VALUE changed in either the BC or this story — 19 VPs/36 ECs/24 ACs were already correct in both, so this is a pure version-cite propagation, not a count sweep). Story-wide grep sweep (`grep -niE "BC-5\.39\.010 v1\.18"`) performed for every live cite pattern (title, H1, narrative, BC Status governing-BC statement, AC "Under BC..." anchors, AC-020 §Gate Spec traces ×3, BC table version cell, Token Budget row, Tasks 1/10). All 16 live sites advanced v1.18→v1.19: title frontmatter; H1 heading; narrative "authors this gate" clause; BC Status governing-BC statement; AC-001 PC2b anchor; AC-009 PC13b anchor; AC-020 heading trace; AC-020 tool-field §Gate Spec trace; AC-020 Notes "current cap figure" §Gate Spec trace; AC-020 Notes "Global fuel cap" §Gate Spec trace; AC-022 PC2a anchor; AC-023 PC13a anchor; AC-024 PC13c anchor; BC table version cell (`| 1.18 |` → `| 1.19 |`); Token Budget BC row; Task 1; Task 10. Post-sweep verification confirmed zero live `v1.18` residuals — the only surviving hits are this Changelog's own historical v1.10 row and the frontmatter `last_amended:` append-only `[Prior: ...]` chain, both documenting WHEN the v1.14→v1.18 propagation happened (POLICY 1 append-only; not live cites). No AC scope, test behavior, or count number changed. input-hash: 7bc1850→93c4a89 (BC-5.39.010.md v1.18→v1.19). |
| 1.11 | 2026-08-13 | F-S2107-P13-001 close (adversary pass-13, class-complete sibling sweep per TD-VSDD-060/POLICY 8 — the v1.10 fix corrected only AC-020 Notes and left sibling sites carrying the same retracted claims, which is exactly why this finding recurred): two flagged sites fixed story-wide, not site-scoped. (1) Out-of-Scope table "Per-plugin `fuel_cap` registry field extension" row: removed the retracted premise "this hook uses `on_error = \"continue\"` and needs no `fuel_cap` override" (contradicted AC-020's own SHOULD-set-once-ADR-039-Phase-1-ships language, and the underlying `on_error=continue`-implies-safe-omission premise is measurement-falsified per BC-5.39.010 v1.15/v1.16); reason rewritten to "out of THIS story's scope because it depends on ADR-039 Phase 1 / S-21.10"; anchor re-pointed ADR-035 §Decision 5 → ADR-042 §Decision 2 (ADR-035 v1.1 Correction 3 itself confirms its "no registry change needed for this new hook" note is superseded). (2) AC-019 §Build constraints: flat "10M-instruction fuel cap" claim (no source-vs-operator qualifier, violating the rule AC-020 already establishes) corrected to state both the operator-effective 10,000,000 cap (through v1.0.0-rc.23) and the source-HEAD 20,000,000 cap (ADR-042 §Decision 1 / PR #774) explicitly; retained the general ADR-035 §Decision 5 citation there (release-build/on_error-continue rationale is legitimately still that ADR's scope, not the fuel_cap-value-specific ADR-042 §Decision 2 scope). Class-complete sibling sweep performed across the full story for every member of the defect class (RETRACTED-false "calibrated" claims; active fuel_cap-prohibition phrasing; on_error=continue-implies-no-fuel_cap-needed rationale; unqualified flat 10M/20M cap figures; live BC-version cites below v1.18; fuel_cap-related ADR-035 §Decision 5 anchors) via `grep -niE` over 6 patterns; all remaining hits verified to be either already-correct v1.10-fixed text (AC-020 Notes), correctly-qualified historical measurement statements ("then-operative 10,000,000-instruction cap", "former 10M cap"), legitimate non-fuel-cap-value ADR-035 §Decision 5 anchors (release-build/on_error-continue general rationale), historical version-of-record citations ("BC-5.39.010 v1.6 §Deferred Scope", "v1.1/v1.2/v1.16 §Gate Spec had/corrected/lifted..." — documenting WHEN a change happened, not a live current-version claim), or append-only modified[]/Changelog/[Prior:] chain entries (POLICY 1) — zero further live sibling sites of this defect class remain. No AC scope or Red-Gate-anchored test assertion changed (AC-019 has no test keyed to the specific cap figure; the Out-of-Scope table is non-normative). input-hash unchanged (7bc1850) — the `inputs:` files were not modified by this sweep, only this story's own documentary prose. (story-writer; closes F-S2107-P13-001.) |
| 1.10 | 2026-08-13 | F-S2107-P12-001 close (adversary pass-12, MEDIUM; POLICY 8 bc-array/version propagation + POLICY 4 semantic-anchoring): BC-5.39.010 v1.14→v1.18 propagation (story-writer). All live BC-version cites advanced to v1.18 (title/H1, narrative, BC Status governing-BC statement, AC-001/009/018/022/023/024/020 body "Under BC..." anchors, AC-020 §Gate Spec traces, BC table version cell, Token Budget, Task 1/Task 10); PC2/PC13/PC5 substance unchanged since v1.14 (only §Gate Spec fuel-cap subsection amended v1.15→v1.18), so AC-body cites annotated "unchanged since v1.14" rather than silently re-pinned. Three drifts fixed in §AC-020 Notes on field choices: (1) retracted the false claim that `max_bytes` caps "are calibrated to keep reads within the 10M fuel budget" (BC-5.39.010 v1.15, F-S2107-P9-002) — replaced with the measured-exhaustion figure (fuel_consumed=9,920,913/10,000,000, 99.21%); (2) corrected the "this hook MUST NOT include a `fuel_cap` field" prohibition, which BC-5.39.010 v1.16 lifted (ADR-042 §Decision 2) — rewritten as a SHOULD once ADR-039 Phase 1 (S-21.10) ships, hook still omits `fuel_cap` today as a "not yet" state; (3) replaced the flat "10M-instruction fuel budget" figure with the source-HEAD (20,000,000 per PR #774/commit `62fbcf1a`, ADR-042 §Decision 1) vs. operator-effective (10,000,000 through v1.0.0-rc.23) framing per BC-5.39.010 v1.18 (F-S2107-P10-004). No AC scope change; purely factual-claim and version-cite propagation — no Red-Gate-anchored AC test assertion changed. input-hash: d366da1→7bc1850. |
| 1.9 | 2026-08-07 | BC-5.39.010 v1.13→v1.14 propagation (POLICY 8; story-writer): all live BC version cites advanced to v1.14 (title/H1, Priority, BC Status governing-BC statement and PC2/PC13/PC5 subclauses, AC-001/009/018/022/023/024 body, AC-020 §Gate Spec traces, BC table version cell, Token Budget, Task 1/Task 10). Active-AC enumeration corrected (F-S2107-P8-009; story-writer): §BC Status preamble `ACs 001–011, 015–023 are active` → `ACs 001–011, 015–024 are active`; parenthetical updated to record AC-024/v1.9 provenance. Test Plan rows T-P8A/T-P8B/T-P8C added for AC-024 (PC13c half-present) Rust unit tests. Task 3 arm_b.rs bullet extended with AC-023/AC-024. Task 4 bats range extended to include AC-022/AC-023. input-hash: c0ab6a3→d366da1 (BC-5.39.010.md v1.14). |
| 1.8 | 2026-08-06 | BC-5.39.010 v1.12→v1.13 propagation (POLICY 8; story-writer): version cites advanced to v1.13 (title/H1, Priority, BC Status preamble/PC2/PC13/PC5 subclauses, AC-001/009/018/022/023/020 body, BC table, Token Budget, Task 1); PC13c half-present sub-case (ADR-038 §Decision 3) — AC-024 added (advisory+Continue; test names `arm_b1_half_present_catalog_mismatch_is_advisory_not_block`, `arm_b1_half_present_blockquote_mismatch_is_advisory_not_block`, GREEN control `arm_b1_half_present_catalog_match_is_advisory`); EC-036 mirrored from BC v1.13; BC table ACs cell updated to include AC-024; EC count 35→36 per BC-5.39.010 v1.13. modified[] v1.5/v1.6 ordering defect fixed (F-S2107-P7-016): v1.5 entry moved to precede v1.6 entry (both 2026-08-05; E2 date-only predicate blind to this class). input-hash: 1bc3197→c0ab6a3 (BC-5.39.010.md v1.13). |
| 1.7 | 2026-08-05 | BC-5.39.010 v1.11→v1.12 propagation (POLICY 8; story-writer): version cites advanced to v1.12 (title, H1, Priority, BC Status governing-BC statement and PC2/PC13/PC5 subclauses, AC-001/009 "Under BC..." anchors, AC-018 fixture rationale, AC-020 (×3 §Gate Spec traces), AC-022/023 "Under BC..." anchors, BC table version cell, Token Budget); EC count 33→35, VP count 17→19 per BC-5.39.010 v1.12. input-hash: 19454c3→1bc3197 (BC-5.39.010.md v1.12). |
| 1.6 | 2026-08-05 | BC-5.39.010 v1.10→v1.11 propagation (POLICY 8; story-writer; pass-6 fix burst): PC2 directional carve-out (F-P6-001 Option 1): AC-001 rewritten to test PC2b (index-newer-than-primary → block; BC-5.39.010 PC2b); new AC-022 added for PC2a advisory path (primary-newer-than-index); AC-009 rewritten to test PC13b (B2≠B3 → block); new AC-023 added for PC13a advisory path (B2==B3, B1≠B2 → advisory); AC-018 fixture updated to use PC2b (index-newer) + E1 to exercise combined-block path under v1.11; EC-002 corrected (primary-newer → advisory per PC2a normative postconditions); sweep confirmed no other ACs trace to PC2 or PC13; all v1.10 BC cites advanced to v1.11 (title, H1, BC Status, BC table, Token Budget, Tasks, AC-020); input-hash: dd5c9d2→19454c3 (BC-5.39.010.md v1.11). |
| 1.5 | 2026-08-05 | BC-5.39.010 v1.7→v1.10 propagation (POLICY 8; story-writer; pass-5 fix burst): title/H1/BC Status/BC table/Token Budget/AC-020/Task 1/Task 10 updated to v1.10 (F-S2107-P4-002); BC Status arm count corrected 4→6 blocking arms (A1,A2,B1,B2,E1,E2) (F-S2107-P4-009); BC Status updated with v1.8/v1.9/v1.10 amendment summaries. ARCH-INDEX.md removed from inputs: per ADR-037 §Decision 5 (F-S2107-P4-013 story-side; ARCH-INDEX.md volatile per ADR-037 §Decision 2; removal ends PC40 Class B suppression for S-21.07). input-hash: 25c7324→dd5c9d2 (ARCH-INDEX.md removal + BC-5.39.010.md v1.10). |
| 1.4 | 2026-08-04 | BC-5.39.010 v1.6→v1.7 propagation (POLICY 8; story-writer): title/H1 updated to v1.7; BC Status updated (BC-5.39.010 v1.7 is governing BC; v1.7 corrected PC5 — `extract_bc_index_version` rearchitected to three-state `RowAbsent`/`RowPresentNoVersion`/`Version(v)`: prior two-state `Option<String>` conflating absent-row and no-version-cell into a single `None` produced ≥1,712 false BLOCKs per F-S2107-P3-001 corpus analysis; `RowPresentNoVersion` is canonical normal state for ~98% of BC-INDEX rows; postcondition 4 expanded to silent-continue on `RowPresentNoVersion`; invariant 11 updated to three-category taxonomy stale/fabricated/algorithm-divergent; `1acf3c6` reclassified ALGORITHM-DIVERGENT per ADR-036 §Decision 4 — Pass-30 M02 POLICY 18 violation claim retracted; Gate Spec `run_part_a_arm1` pseudocode updated to three-state match); AC-009 invariant 11 reference updated to three-category taxonomy; AC-019/AC-020 §Gate Spec cites advanced to v1.7; Behavioral Contracts table BC version cell updated to v1.7; Token Budget BC row updated to v1.7; Task 1 cite updated to v1.7. Pass-4 fix burst. |
| 1.3 | 2026-08-04 | Class D (AC-012/013/014) descoped and marked [DEFERRED v1.6 — Class D] per BC-5.39.010 v1.6 human-approved scope decision (2026-08-04); BC-5.39.010 v1.3→v1.6 propagation (POLICY 8; story-writer): title/H1 updated (six-arm; Classes A/B/E; Class D deferred); BC Status section updated to reflect v1.6 as governing; Behavioral Contracts table BC H1 title and version updated to v1.6; arm_d.rs Architecture Mapping/Purity Classification/Tasks/Test Plan/File Structure entries marked DEFERRED v1.6; EC-010/011/012/013/024 marked DEFERRED v1.6 in Edge Cases table; EC-033 added from BC v1.6; ADR-035 v1.0 volatile pin corrected per TD-VSDD-091 (Token Budget row); AC-020 path_allow .factory/cycles/ commented-out DEFERRED v1.6; Token Budget BC version updated to v1.6. Adversary pass-2 fix-burst (F-S2107-P2-014 + descope items (a)/(b)/(c)). |
| 1.2 | 2026-07-30 | BC-5.39.010 v1.2→v1.3 propagation (POLICY 8; story-writer). BC H1 title restored verbatim per POLICY 7: trigger clauses 'after a BC frontmatter bump' (Class A Arm1) and 'after a story edit' (Class A Arm2) restored; 'non-monotonicity' → 'date-decrease' per v1.3 H1. Title/H1 arm count corrected five-arm → seven-arm (7 arms across 4 classes — 6 blocking A1/A2/B1/B2/E1/E2 + 1 advisory D) per F-S2107-P1C-006. AC-016 E2 semantics updated: non-ascending/non-monotonic → non-decreasing/strict-decrease per BC v1.3 postconditions 21-22 (equal consecutive dates permitted; F-S2107-P1C-013). EC-030/EC-031 mirrored from BC v1.3 (POLICY 8 EC-mirror). Task 10 arm_a2 regex updated to optional-v form `\bv?([0-9]+\.[0-9]+)\b` per BC v1.3 PC13. Changelog section added per POLICY 14 leg 2 / POLICY 17 (F-S2107-P1C-008). Adversary pass-1 fix-burst (F-S2107-P1C-006/008/013/015). |
| 1.1 | 2026-07-30 | BC-5.39.010 v1.1→v1.2 propagation (POLICY 8; story-writer): all 7 live-site BC-version citations updated to v1.2; AC-020 notes updated (MultiEdit gap closed in BC v1.2; fuel_cap guard added per v1.2 §Gate Spec); datum-copy ruling for Arm A2 confirmed in BC Status. |
| 1.0 | 2026-07-30 | Initial authoring (story-writer; human-authorized ahead of pass-30 fix burst). E-21 cross-site correspondence gate; BC-5.39.010 v1.1; ADR-035 v1.0; 21 ACs; 11 pts; Wave 4. |
