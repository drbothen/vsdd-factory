---
document_type: story
level: ops
story_id: "S-17.03"
epic_id: "E-17"
version: "1.2"
title: "S-17.03: /factory-lock + /factory-unlock skills + /factory-health and /factory-worktree-health lock status (D4 + D5 + D7 + D8 + D9 skill bats)"
status: merged
merged_commit: "60fd0233"
merged_pr: 183
producer: story-writer
timestamp: 2026-06-10T00:00:00Z
last_amended: "2026-06-11 (v1.2) — S-17.03 DELIVERED/MERGED: PR #183 squash-merged 60fd0233 to develop 2026-06-11; status draft→merged; BC-6.23.001 POL-14 draft→active; 26 bats + 5-platform CI run 27343001859 all-green; LOCAL adversary BC-5.39.001 3-CLEAN (cascade caught+fixed: refusal-msg guard-parity, CRLF cross-component parity, subshell-scoped CRLF temp-file leak); security 0-findings; pr-reviewer APPROVE; /factory-lock + /factory-unlock skills + 3-state lock-status in factory-health/factory-worktree-health via shared factory-lock-status.sh; 3 new bats-tested bin helpers; issue #170 CLOSED; E-17 3/3 COMPLETE. [Prior: 2026-06-11 (v1.1) — Executable-helper refinement (story-writer; delivery-prep Red-Gate-feasibility defect; L-issue-169-176-worktree-identity(b) precedent; S-17.01 executable-helper model; issue #170). Three new bin/ helpers extracted (factory-lock-status.sh, factory-lock-acquire-precheck.sh, factory-unlock-decide.sh) + bats coverage; SKILL.md files become thin orchestrators; File Structure, Tasks, Red Gate Test Table, Token Budget updated. All 14 ACs + BC traces UNCHANGED. [Prior: 2026-06-10 (v1.0) — Initial authoring (story-writer; brownfield-backfill issue #170; ADR-025 v1.2 D4/D5/D7/D8/D9; BC-6.23.001 v1.0). 14 ACs tracing PC1–PC8 + Pre-2/Pre-3 + EC-001/EC-005/EC-010 + D9 bats T-1..T-10. Wave 3; points 8; tdd_mode strict; depends_on [S-17.01, S-17.02]; blocks [].]]"
phase: brownfield-backfill
cycle: v1.0-brownfield-backfill
inputs:
  - .factory/specs/architecture/decisions/ADR-025-single-writer-factory-locklease-prevent-concurrent-session-races-on-factory-artifacts-orphan-branch.md
  - .factory/specs/behavioral-contracts/ss-06/BC-6.23.001.md
  - .factory/specs/behavioral-contracts/ss-05/BC-5.40.001.md
  - .factory/specs/behavioral-contracts/ss-04/BC-4.13.001.md
  - .factory/specs/architecture/ARCH-INDEX.md
  - plugins/vsdd-factory/skills/factory-health/SKILL.md
  - plugins/vsdd-factory/skills/factory-worktree-health/SKILL.md
input-hash: "f6f36bb"
traces_to:
  - .factory/specs/behavioral-contracts/ss-06/BC-6.23.001.md
  - .factory/specs/architecture/decisions/ADR-025-single-writer-factory-locklease-prevent-concurrent-session-races-on-factory-artifacts-orphan-branch.md
wave: 3
priority: P1
points: 8
track: factory-state-durability
effort_class: multi-day-with-research
depends_on:
  - "S-17.01"
  - "S-17.02"
blocks: []
behavioral_contracts:
  - "BC-6.23.001"
verification_properties: []
# BC status: VPs to be allocated during verification scoping per TD-VSDD-063 lagging-VP precedent
domain_invariants: []
tdd_mode: strict
target_module: "plugins/vsdd-factory/skills"
subsystems:
  - "SS-06"
estimated_days: 2.5
assumption_validations: []
risk_mitigations: []
anchored_adrs:
  - ADR-025
closes: []
modified: []
---

# S-17.03: /factory-lock + /factory-unlock Skills + Health Status Surfacing

**Epic:** E-17 — Factory State Durability and Concurrency (brownfield-backfill #170)
**Wave:** 3 (depends on S-17.01 schema + S-17.02 guard)
**Depends on:** S-17.01 (schema + bin/ helpers); S-17.02 (guard active before skills delivered)
**Blocks:** none
**Priority:** P1 (user-facing entry points for CAP-031 lock management)
**BC gate:** BC-6.23.001 v1.0 (draft; POL-14 auto-promotion on PR merge)

## Narrative

- **As a** developer managing concurrent factory sessions
- **I want** explicit `/factory-lock` and `/factory-unlock` skills that acquire and
  release the cross-session factory lock with CAS-protected pushes, plus three-state
  lock status surfacing in `/factory-health` and `/factory-worktree-health`
- **So that** I can deliberately take ownership of the factory-artifacts branch before
  beginning pipeline work, release it cleanly when done, break-glass force-release a
  stale lock with an audit trail, and always know who holds the lock and when it expires
  from any health check

## Behavioral Contracts

| BC ID | Title | Version | PCs Exercised |
|-------|-------|---------|---------------|
| BC-6.23.001 | /factory-lock + /factory-unlock + health status | 1.0 | PC1, PC2, PC3, PC4, PC5, PC6, PC7, PC8 |

## Acceptance Criteria

### AC-001 (traces to BC-6.23.001 postcondition PC1 — /factory-lock success)

When `/factory-lock` is run and the factory is unlocked (or the lock is expired), the
acquire succeeds:
1. `state-manager` writes `factory_lock = { holder: <current_email>, locked_at: <now_iso8601>, expires_at: <now_iso8601 + 2700s> }` into STATE.md frontmatter.
2. `state-manager` commits and pushes using the CAS primitive from S-17.01 (D6):
   `git -C .factory fetch origin factory-artifacts && EXPECTED_SHA=... && git -C .factory push --force-with-lease=factory-artifacts:"${EXPECTED_SHA}" origin factory-artifacts`
3. A `factory.lock.acquired` event is emitted to the SS-03 event pipeline with fields:
   `holder` (email), `locked_at` (ISO-8601), `expires_at` (ISO-8601).
4. User sees: `Factory lock acquired by <email>. Expires at <expires_at>.`

**Error variant:** `AcquireRaceRejected` (on CAS push rejection)
**Test:** `test_BC_6_23_001_factory_lock_acquire_success_schema_event_message()`

### AC-002 (traces to BC-6.23.001 postcondition PC2 — /factory-lock CAS rejection)

When the CAS push is rejected (non-zero exit from `git push --force-with-lease`), the
acquire MUST fail with:
```
Acquire failed — concurrent lock write detected. Fetch and retry.
(TOCTOU acquire-race CWE-367: another session wrote the lock between your fetch and your push.)
```
STATE.md MUST NOT be in a partial written state (the push rejection means the write was
rejected by git — no local STATE.md commit should be present). No audit event emitted
for a rejected acquire.

**Error variant:** `AcquireRaceRejected`
**Test:** `test_BC_6_23_001_factory_lock_cas_rejection_error_message()`

### AC-003 (traces to BC-6.23.001 postcondition PC3 — /factory-lock foreign lock held: refuse)

When the fetch reveals `factory_lock.holder` is set, `now ≤ expires_at`, and
`holder != current_git_email`, the `/factory-lock` skill MUST exit without attempting
the CAS push and MUST display the refusal message containing ALL FIVE fields (matching
BC-4.13.001 PC1 format):
- Holder email
- `locked_at` timestamp
- `expires_at` timestamp
- `time_remaining` (human-readable)
- `/factory-unlock --force` command

**Error variant:** `ForeignLockHeld`
**Test:** `test_BC_6_23_001_factory_lock_foreign_lock_refusal_message_all_five_fields()`

### AC-004 (traces to BC-6.23.001 postcondition PC4 — /factory-unlock self-release)

When `factory_lock.holder == current_git_email` and the user runs `/factory-unlock`:
1. `state-manager` removes the `factory_lock` key entirely from STATE.md frontmatter
   (BC-5.40.001 PC2: not null — key must be absent).
2. `state-manager` commits and pushes (CAS push per S-17.01 D6).
3. A `factory.lock.released` event emitted with: `holder` (email), `locked_at`,
   `released_at` (ISO-8601 of the release instant).
4. User sees: `Factory lock released.`

**Error variant:** `UnlockCASRejected` (if CAS push rejected)
**Test:** `test_BC_6_23_001_factory_unlock_self_release_clears_key_emits_event()`

### AC-005 (traces to BC-6.23.001 postcondition PC5 — /factory-unlock non-holder rejection)

When `factory_lock.holder != current_git_email` and the user runs plain `/factory-unlock`
(no `--force`):
1. Skill exits non-zero.
2. Error displayed: `Cannot unlock — factory is held by <holder_email>. Use /factory-unlock --force to force-release.`
3. STATE.md MUST NOT be modified.
4. No event emitted.

**Error variant:** `NotLockHolder`
**Test:** `test_BC_6_23_001_factory_unlock_non_holder_rejected_no_state_change()`

### AC-006 (traces to BC-6.23.001 postcondition PC6 — /factory-unlock --force break-glass)

When the user runs `/factory-unlock --force` (regardless of current holder):
1. `state-manager` removes the `factory_lock` key entirely from STATE.md.
2. `state-manager` commits and pushes (CAS push per S-17.01 D6).
3. A `factory.lock.stolen` audit event MUST be emitted with ALL FOUR required fields:
   - `stolen_by`: `current_git_email`
   - `stolen_from`: `factory_lock.holder` at force-release time
   - `holder_locked_at`: original `locked_at` from the lock block
   - `stolen_at`: ISO-8601 of the force-release instant
4. The event is PERMANENT — if SS-03 sink is unavailable, proceed with the lock clear
   but log the emission failure as `log_warn` (BC-6.23.001 Invariant 2 + EC-008).
5. User sees: `Factory lock force-released. Audit event 'factory.lock.stolen' emitted.`

Special case (EC-010): If `/factory-unlock --force` is run against a self-held lock,
emit `factory.lock.released` (NOT `factory.lock.stolen` — `stolen_by == stolen_from`
is not a meaningful audit event).

**Error variant:** `ForceStealAudited`
**Test:** `test_BC_6_23_001_factory_unlock_force_emits_stolen_event_all_four_fields()`

### AC-007 (traces to BC-6.23.001 postcondition PC7 — /factory-health three-state display)

The `/factory-health` skill MUST display one of three lock status lines based on the
local STATE.md `factory_lock` block. Both `/factory-health` and `/factory-worktree-health`
invoke the shared `factory-lock-status.sh` helper — they CANNOT diverge on display strings:
- `Factory lock: FREE` — `factory_lock` absent or expired
- `Factory lock: HELD by this session (expires <expires_at>)` — holder is current email AND unexpired
- `Factory lock: HELD by <holder_email> since <locked_at> (expires <expires_at>)` — holder is another email AND unexpired

Malformed block renders as: `Factory lock: FREE (malformed block — treated as unlocked)`.

The status is read from the LOCAL STATE.md (no fetch required — local view is what matters
for informational display).

**Test:** `test_BC_6_23_001_factory_lock_status_sh_three_state_display()` (three fixture
variants → assert three display strings, run against `factory-lock-status.sh` directly)

### AC-008 (traces to BC-6.23.001 postcondition PC8 — /factory-worktree-health three-state display)

The `/factory-worktree-health` skill MUST display the same three-state lock status as
AC-007, using the same display strings and the same local STATE.md read logic. The
implementation MUST use `factory-lock-status.sh` (the shared helper — not duplicated
inline logic) so that the two skills cannot diverge on display format.

**Test:** `test_BC_6_23_001_factory_lock_status_sh_shared_by_both_health_skills()` —
assert both `factory-health/SKILL.md` and `factory-worktree-health/SKILL.md` contain
an invocation of `factory-lock-status.sh` (grep-based; structural test).

### AC-009 (traces to BC-6.23.001 precondition 2 — fetch-before-check mandatory on acquire)

The `/factory-lock` acquire MUST perform `git fetch origin factory-artifacts` BEFORE
reading STATE.md for the lock check. The fetch completes before any lock state is read
or written. If the fetch fails (non-zero exit), the skill exits non-zero:
`"Fetch failed before lock check. Cannot acquire safely."` No lock is written.

This guard lives in `factory-lock-acquire-precheck.sh` (EC-006 path).

**Error variant (from EC-006):** `FetchFailed`
**Test:** `test_BC_6_23_001_acquire_precheck_fetch_failure_aborts()`

### AC-010 (traces to BC-6.23.001 precondition 3 — git user.email required for acquire)

If `git config user.email` returns empty output during `/factory-lock`, the skill exits
non-zero: `"git user.email not configured — cannot acquire factory lock."` No lock is
written with an empty holder.

This guard lives in `factory-lock-acquire-precheck.sh` (EC-007 path).

**Test:** `test_BC_6_23_001_acquire_precheck_empty_email_rejected()`

### AC-011 (traces to BC-6.23.001 invariant 5 — state-manager delegation on all writes)

Neither `/factory-lock` nor `/factory-unlock` writes STATE.md directly. Both MUST
delegate to `state-manager` for the write and push. The bin/ helpers perform DECISION
and DISPLAY logic only; the STATE.md write itself is delegated to state-manager which
invokes `factory-lock-write.sh` (acquire/clear) + `factory-cas-push.sh` from S-17.01.

**Test:** `test_BC_6_23_001_skill_mds_contain_no_direct_state_write()` — structural grep
on both new SKILL.md files confirming no direct `Write STATE.md` instruction; confirms
the delegation delegation path via state-manager is documented.

### AC-012 (traces to BC-6.23.001 edge case EC-001 — re-lock by self is no-op)

When `/factory-lock` is run and the factory is already locked BY THE SAME developer
(self-held, unexpired), the `factory-lock-acquire-precheck.sh` helper MUST return the
self-held decision and the skill treats this as already acquired:
- Display: `Factory lock already held by this session.`
- No STATE.md write.
- No new event emitted.
- Exit 0.

**Test:** `test_BC_6_23_001_acquire_precheck_self_held_noop()`

### AC-013 (traces to BC-6.23.001 canonical test vector T-4 — concurrent acquire CAS race)

When two concurrent `/factory-lock` invocations (from `dev-a@x.com` and `dev-b@x.com`)
both see an unlocked STATE.md and both attempt acquire simultaneously, the CAS push
is the tiebreaker. One CAS push succeeds; the other receives a non-fast-forward
rejection. The loser MUST receive `AcquireRaceRejected` error and may retry. The winner
holds the lock. This is the D9 T-10 bats scenario (BC-4.13.001 canonical test vectors).

**Test:** `test_BC_6_23_001_concurrent_acquire_cas_race_one_wins_one_rejected()` (bats T-10)

### AC-014 (traces to BC-6.23.001 edge case EC-005 — force-release of absent lock is no-op)

When `/factory-unlock --force` is run and no `factory_lock` block is present (factory
already unlocked), `factory-unlock-decide.sh` MUST return the absent-lock decision and
the skill succeeds silently. Exit 0. No event emitted (there is no holder to name in
`stolen_from`).

**Test:** `test_BC_6_23_001_unlock_decide_force_on_absent_lock_noop()`

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| `factory-lock-status.sh` | `plugins/vsdd-factory/bin/factory-lock-status.sh` (new) | Pure-core (reads STATE.md + git email → returns display string; no writes) |
| `factory-lock-acquire-precheck.sh` | `plugins/vsdd-factory/bin/factory-lock-acquire-precheck.sh` (new) | Effectful (git fetch, git email read → acquire decision; no STATE.md write) |
| `factory-unlock-decide.sh` | `plugins/vsdd-factory/bin/factory-unlock-decide.sh` (new) | Pure-core (inspect lock block + identity → unlock decision + audit event fields; no writes) |
| `/factory-lock` skill | `plugins/vsdd-factory/skills/factory-lock/SKILL.md` (new) | Thin orchestrator: invokes precheck helper → delegates write to state-manager → emits event |
| `/factory-unlock` skill | `plugins/vsdd-factory/skills/factory-unlock/SKILL.md` (new) | Thin orchestrator: invokes decide helper → delegates write to state-manager → emits event |
| `/factory-health` lock status | `plugins/vsdd-factory/skills/factory-health/SKILL.md` (amend) | Invokes `factory-lock-status.sh`; additive to existing health output |
| `/factory-worktree-health` lock status | `plugins/vsdd-factory/skills/factory-worktree-health/SKILL.md` (amend) | Invokes same `factory-lock-status.sh`; additive to existing output |
| `factory-lock-write.sh` | `plugins/vsdd-factory/bin/factory-lock-write.sh` (REUSE from S-17.01) | State-manager-invoked writer; NOT invoked by skills directly |
| `factory-cas-push.sh` | `plugins/vsdd-factory/bin/factory-cas-push.sh` (REUSE from S-17.01) | State-manager-invoked CAS pusher; NOT invoked by skills directly |
| SS-03 event pipeline | `plugins/vsdd-factory/bin/emit-event` (REUSE existing) | `factory.lock.acquired`, `factory.lock.released`, `factory.lock.stolen` events |

**Subsystem anchor justification:** SS-06 is "Skill Catalog" per ARCH-INDEX Subsystem
Registry — it owns all user-facing skills under `plugins/vsdd-factory/skills/`. The
four deliverables for this story (two new skills: factory-lock, factory-unlock; two
amended skills: factory-health, factory-worktree-health) are all skill-catalog artifacts.
The three new bin/ helpers are support code for those skills. No other subsystem owns
skill SPEC authoring.

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | `/factory-lock` when already locked by self (re-lock) | `factory-lock-acquire-precheck.sh` returns self-held; skill displays "Already held by this session"; no write; exit 0 |
| EC-002 | `/factory-lock` when lock is expired (held by other but expired) | Treat as unlocked; proceed with acquire (CAS push safety net) |
| EC-003 | `/factory-unlock` when factory already unlocked | Exit 0 silently; no error; no event |
| EC-004 | `/factory-lock` CAS push rejected (concurrent acquire race) | `AcquireRaceRejected`; user retries; no partial STATE.md write |
| EC-005 | `/factory-unlock --force` when no lock held | `factory-unlock-decide.sh` returns absent-lock noop; exit 0; no event |
| EC-006 | `git fetch origin factory-artifacts` fails during `/factory-lock` | `factory-lock-acquire-precheck.sh` exits non-zero: "Fetch failed before lock check. Cannot acquire safely." No lock written |
| EC-007 | `git config user.email` not set during `/factory-lock` | `factory-lock-acquire-precheck.sh` exits non-zero: "git user.email not configured." No lock written |
| EC-008 | `factory.lock.stolen` event emission fails (SS-03 sink down) | Force-release proceeds (lock cleared); `emit-event` is failure-tolerant (always exits 0); `log_warn` emitted |
| EC-009 | Locked STATE.md pushed from `/factory-lock`; concurrent blocked session's guard sees stale local STATE.md | Guard reads local STATE.md; stale view resolved on next fetch (state-burst fetch cycle); cooperative convergence window |
| EC-010 | `/factory-unlock --force` run against a self-held lock | `factory-unlock-decide.sh` detects self-force; emits `factory.lock.released` (NOT `stolen` — `stolen_by == stolen_from`); proceeds as plain unlock |

## Tasks

- [ ] **T-1 — Read existing factory-health and factory-worktree-health SKILL.md** to understand
  current structure before amending (T-6/T-7). Note the exact location where new lock status
  invocation of `factory-lock-status.sh` should be inserted.

- [ ] **T-2 — Create `factory-lock-status.sh`** (`plugins/vsdd-factory/bin/factory-lock-status.sh`):
  Pure-core: reads local STATE.md path + current git email as arguments → parses
  `factory_lock` frontmatter block → emits one of four display strings to stdout:
  - `Factory lock: FREE` (absent or expired)
  - `Factory lock: HELD by this session (expires <expires_at>)` (self-held, unexpired)
  - `Factory lock: HELD by <holder_email> since <locked_at> (expires <expires_at>)` (foreign, unexpired)
  - `Factory lock: FREE (malformed block — treated as unlocked)` (parse failure)
  No git ops — caller passes the email; script only does YAML parsing + string output.
  `set -euo pipefail`. BSD/GNU date compat (same pattern as `factory-lock-write.sh`).

- [ ] **T-3 — Create `factory-lock-acquire-precheck.sh`**
  (`plugins/vsdd-factory/bin/factory-lock-acquire-precheck.sh`):
  Effectful: given STATE.md path as argument:
  - Step 1: run `git fetch origin factory-artifacts` — if non-zero exit: print
    "Fetch failed before lock check. Cannot acquire safely." to stderr; exit 2 (EC-006).
  - Step 2: read `git config user.email` — if empty: print
    "git user.email not configured — cannot acquire factory lock." to stderr; exit 2 (EC-007).
  - Step 3: read local STATE.md `factory_lock` block.
  - Step 4: if self-held (holder == email, unexpired): print "Already held by this session."
    to stdout; exit 0 with decision code "NOOP_SELF_HELD" on stdout.
  - Step 5: if foreign unexpired (holder != email, unexpired): print the 5-field refusal
    message to stderr (matching BC-4.13.001 PC1 format exactly); exit 1 with
    decision code "REFUSED_FOREIGN_LOCK".
  - Step 6: otherwise (absent or expired): print decision code "PROCEED_ACQUIRE" to
    stdout; exit 0.
  This helper determines the DECISION. The skill then acts on that decision.

- [ ] **T-4 — Create `factory-unlock-decide.sh`** (`plugins/vsdd-factory/bin/factory-unlock-decide.sh`):
  Pure-core: given STATE.md path + `--force` flag as arguments:
  - If `factory_lock` absent: print "NOOP_ABSENT" to stdout; exit 0 (EC-003/EC-005).
  - If plain unlock (no `--force`) and `holder != current_email`: print
    "Cannot unlock — factory is held by <holder_email>. Use /factory-unlock --force to force-release."
    to stderr; exit 1 with "REFUSED_NOT_HOLDER".
  - If plain unlock and `holder == current_email`: print "PROCEED_RELEASE" + emit field
    block (holder, locked_at, released_at=now ISO-8601) to stdout; exit 0.
  - If `--force` and `holder == current_email` (EC-010): print "PROCEED_RELEASE_SELF_FORCE"
    + same field block to stdout; exit 0 (emits `factory.lock.released` not `stolen`).
  - If `--force` and `holder != current_email`: print "PROCEED_FORCE_STEAL" + audit
    event field block (stolen_by, stolen_from, holder_locked_at, stolen_at=now) to
    stdout; exit 0.
  Note: This helper only does DECISION and FIELD CONSTRUCTION. No STATE.md writes.
  State-manager receives the decision and fields from the skill.

- [ ] **T-5 — Write bats tests for all three helpers** (all FAIL before implementation — Red Gate):
  - `factory-lock-status.bats`: tests per AC-007/AC-008 (three fixture variants + malformed).
  - `factory-lock-acquire-precheck.bats`: tests per AC-009/AC-010/AC-012/AC-003 (fetch failure,
    empty email, self-held noop, foreign-lock refusal 5-field message).
  - `factory-unlock-decide.bats`: tests per AC-004/AC-005/AC-006/AC-014/EC-010 (self-release
    decision, non-holder rejection, force-steal 4-field event block, absent-lock noop, self-force
    emits released not stolen).

- [ ] **T-6 — Create `/factory-lock` skill** (`plugins/vsdd-factory/skills/factory-lock/SKILL.md`):
  Thin orchestrator documenting:
  1. Invoke `factory-lock-acquire-precheck.sh <STATE_MD_PATH>`.
  2. On exit 0 + "PROCEED_ACQUIRE": delegate to `state-manager` to run `factory-lock-write.sh acquire`
     + `factory-cas-push.sh` (from S-17.01 D6). On CAS rejection: display `AcquireRaceRejected` message.
  3. On exit 0 + "NOOP_SELF_HELD": display "Factory lock already held by this session." Exit 0.
  4. On exit 1: precheck already printed the refusal message; propagate exit 1.
  5. On success: invoke `emit-event type=factory.lock.acquired holder=<email> locked_at=<ts> expires_at=<ts>`.
  6. Display: `Factory lock acquired by <email>. Expires at <expires_at>.`
  The SKILL.md documents that NO direct STATE.md write occurs in the skill — all writes
  go through state-manager (AC-011/Invariant 5).

- [ ] **T-7 — Create `/factory-unlock` skill** (`plugins/vsdd-factory/skills/factory-unlock/SKILL.md`):
  Thin orchestrator documenting:
  1. Invoke `factory-unlock-decide.sh <STATE_MD_PATH> [--force]`.
  2. On "NOOP_ABSENT": exit 0 silently (EC-003/EC-005).
  3. On "REFUSED_NOT_HOLDER": helper already printed error; propagate exit 1.
  4. On "PROCEED_RELEASE" / "PROCEED_RELEASE_SELF_FORCE": delegate to state-manager to run
     `factory-lock-write.sh clear` + `factory-cas-push.sh`. Emit
     `emit-event type=factory.lock.released holder=... locked_at=... released_at=...`.
     Display: `Factory lock released.`
  5. On "PROCEED_FORCE_STEAL": delegate same write+push. Emit
     `emit-event type=factory.lock.stolen stolen_by=... stolen_from=... holder_locked_at=... stolen_at=...`.
     (EC-008: `emit-event` is failure-tolerant; SS-03 down cannot abort the release.)
     Display: `Factory lock force-released. Audit event 'factory.lock.stolen' emitted.`
  The SKILL.md documents that NO direct STATE.md write occurs in the skill.

- [ ] **T-8 — Amend `/factory-health` skill** (`plugins/vsdd-factory/skills/factory-health/SKILL.md`):
  Add lock status display section that invokes:
  `factory-lock-status.sh <STATE_MD_PATH> "$(git config user.email)"`
  and appends the output line to the health report. Existing skill behavior MUST remain
  unchanged; the lock status line is additive. Malformed block handled by the helper.

- [ ] **T-9 — Amend `/factory-worktree-health` skill**
  (`plugins/vsdd-factory/skills/factory-worktree-health/SKILL.md`):
  Add lock status display using the SAME helper as T-8:
  `factory-lock-status.sh <STATE_MD_PATH> "$(git config user.email)"`
  Same invocation, same display strings (AC-008 shared-helper mandate).

- [ ] **T-10 — Write integration bats tests** (D9 skill test vectors — BC-6.23.001 Canonical
  Test Vectors T-1..T-10; test against helpers + structural SKILL.md grep):
  See Red Gate Test Table for full test names.

- [ ] **T-11 — SDK Grounding Evidence from BC-6.23.001**: Run canonical greps with captured stdout:
  1. `ls plugins/vsdd-factory/skills/factory-health/SKILL.md` — confirm D7 target exists
  2. `ls plugins/vsdd-factory/skills/factory-worktree-health/SKILL.md` — confirm D8 target exists
  3. `ls plugins/vsdd-factory/bin/factory-lock-write.sh` — confirm S-17.01 helper present
  4. `ls plugins/vsdd-factory/bin/factory-cas-push.sh` — confirm S-17.01 helper present
  5. `ls plugins/vsdd-factory/bin/emit-event` — confirm SS-03 event emitter present
  6. `grep -rn "factory\.lock\." plugins/vsdd-factory/ crates/` — confirm no existing events
  Record all stdout in the Red Gate log.

- [ ] **T-12 — Run bats regression suite** for existing factory-health and factory-worktree-health
  tests (if any) — confirm zero regressions from T-8/T-9 amendments.

## Previous Story Intelligence

**Predecessors:** S-17.01 and S-17.02.

**From S-17.01 (MERGED PR #181, c64b46d2):** The S-17.01 precedent is the direct model
for this story's executable-helper refinement (L-issue-169-176-worktree-identity(b)).
S-17.01 extracted `factory-lock-write.sh` and `factory-cas-push.sh` from prose-only
SKILL.md targets, enabling bats coverage of mechanical logic. This story applies the
SAME pattern: extract decision and display logic into `factory-lock-status.sh`,
`factory-lock-acquire-precheck.sh`, and `factory-unlock-decide.sh`.

**CRITICAL — Reuse S-17.01 helpers directly:**
- `plugins/vsdd-factory/bin/factory-lock-write.sh` — acquire/renew/clear modes; the
  STATE.md WRITE primitive. State-manager invokes this; skills do NOT.
- `plugins/vsdd-factory/bin/factory-cas-push.sh` — fetch-then-`--force-with-lease` CAS push.
  State-manager invokes this; skills do NOT.
- `plugins/vsdd-factory/bin/emit-event` — failure-tolerant SS-03 event emitter (always exit 0).
  Skills invoke this for `factory.lock.acquired/released/stolen` events.

**DO NOT recreate these helpers.** Do NOT write a new CAS push or lock write helper —
use the S-17.01 implementations. The division of responsibilities is:
- S-17.01 helpers: WRITE + PUSH (state-manager's domain)
- S-17.03 new helpers: DECIDE + DISPLAY (skill domain, bats-testable without git operations)

**From S-17.02:** The guard (BC-4.13.001) is active before this story ships (Wave 3
comes after Wave 2). The refusal message format in `/factory-lock` (AC-003) MUST
match the guard's block message format (BC-4.13.001 PC1) exactly — same 5 fields, same
display format. Both share the same mental model: the blocked developer sees identical
information whether blocked by the guard (Edit/Write/Agent) or by the skill (attempt to
acquire while locked).

**Red Gate feasibility (L-issue-169-176-worktree-identity(b)):** v1.0 listed prose-only
targets (SKILL.md) as the bats test surface. This is the SAME defect S-17.01 had: you
cannot write deterministic FAILING executable tests against markdown an LLM interprets.
The fix (S-17.01 precedent): extract mechanical logic into bin/ helpers with bats coverage;
SKILL.md files become thin orchestrators that invoke the helpers + document state-manager
delegation. The helpers are the bats test target; the SKILL.md files are thin wrappers.

## Architecture Compliance Rules

From ADR-025 v1.2, BC-6.23.001, and BC-5.40.001:

1. **state-manager delegation for ALL writes.** Neither `/factory-lock` nor
   `/factory-unlock` writes STATE.md directly. They delegate to `state-manager`.
   `state-manager` invokes `factory-lock-write.sh` (acquire/clear) and
   `factory-cas-push.sh` from S-17.01. The skills invoke DECISION helpers only.
   TD-VSDD-053 single-writer discipline applies.

2. **`factory.lock.stolen` is mandatory on force-release (non-self).** The event MUST be
   emitted. `emit-event` is failure-tolerant (always exits 0); SS-03 unavailability does
   NOT abort the force-release. The skill emits via `emit-event type=factory.lock.stolen`.

3. **`factory_lock` key absent on unlock (not null).** Per BC-5.40.001 PC2, state-manager
   runs `factory-lock-write.sh clear` which performs key deletion (not null assignment).
   Skills MUST specify key removal, not null assignment, in their delegation to state-manager.

4. **Fetch-before-check is mandatory for acquire.** `factory-lock-acquire-precheck.sh`
   performs the fetch as Step 1 before reading STATE.md (BC-6.23.001 Invariant 6).
   A stale local STATE.md must not be the basis for the acquire decision.

5. **Shared helper for health display.** factory-health and factory-worktree-health MUST
   both invoke `factory-lock-status.sh`. Divergence on display strings between the two
   skills is a production-grade defect (BC-6.23.001 PC8 mandates "same helper, cannot
   diverge"). AC-008 structural test enforces this via grep.

6. **No auto-acquire.** The `/factory-lock` skill is the ONLY acquisition path. No
   other code auto-acquires on first write (BC-6.23.001 Invariant 1 + ADR-025 Decision 6).

7. **Do NOT add new CAS push or lock-write logic.** Reuse S-17.01 helpers verbatim.
   Creating a second implementation of the CAS push would create dual-implementation drift.
   Forbidden: any new `.sh` file that re-implements `git push --force-with-lease` or
   `factory_lock` YAML frontmatter write.

## Library and Framework Requirements

| Dependency | Version | Usage |
|------------|---------|-------|
| bash | system shell | bin/ helpers; set -euo pipefail; same BSD/GNU date compat as S-17.01 |
| git | ≥2.6 | fetch (in precheck helper); `--force-with-lease` used via S-17.01 `factory-cas-push.sh` |
| `factory-lock-write.sh` | S-17.01 (MERGED) | acquire/clear writes; state-manager invokes |
| `factory-cas-push.sh` | S-17.01 (MERGED) | CAS push; state-manager invokes |
| `emit-event` | existing SS-03 | `factory.lock.acquired`, `factory.lock.released`, `factory.lock.stolen` events; always exit 0 |
| bats | ≥1.7 | test runner for `*.bats` files; same version as used by S-17.01/S-17.02 |

**No new library dependencies.** The three new bin/ helpers are pure bash.

**Forbidden dependencies:**
- Any new shell helper that re-implements the CAS push (reuse `factory-cas-push.sh`)
- Any new shell helper that re-implements YAML frontmatter write (reuse `factory-lock-write.sh`)
- Direct STATE.md write calls in skill SPEC (must delegate to state-manager)
- Hardcoded 45-minute TTL in skill SPEC body — the TTL constant 2700s lives in
  `factory-lock-write.sh`; skill passes "acquire with default TTL"

## File Structure Requirements

| File | Action | Purpose |
|------|--------|---------|
| `plugins/vsdd-factory/bin/factory-lock-status.sh` | CREATE | Pure-core shared display helper: reads STATE.md + git email → three-state lock status string. Invoked by both health skills. Bats test target. |
| `plugins/vsdd-factory/bin/factory-lock-acquire-precheck.sh` | CREATE | Effectful decision helper: git fetch + email check + lock-state inspection → acquire decision (PROCEED/NOOP_SELF_HELD/REFUSED_FOREIGN_LOCK) + 5-field refusal. Bats test target. |
| `plugins/vsdd-factory/bin/factory-unlock-decide.sh` | CREATE | Pure-core decision helper: inspect lock block + identity + --force flag → unlock decision + audit event field construction. Bats test target. |
| `plugins/vsdd-factory/tests/factory-lock-status.bats` | CREATE | Bats coverage for `factory-lock-status.sh` — AC-007/AC-008 three-state display (4+ fixture variants including malformed) |
| `plugins/vsdd-factory/tests/factory-lock-acquire-precheck.bats` | CREATE | Bats coverage for `factory-lock-acquire-precheck.sh` — AC-003/AC-009/AC-010/AC-012 (fetch failure, empty email, self-held noop, foreign-lock 5-field refusal) |
| `plugins/vsdd-factory/tests/factory-unlock-decide.bats` | CREATE | Bats coverage for `factory-unlock-decide.sh` — AC-004/AC-005/AC-006/AC-014/EC-010 (self-release, non-holder rejection, force-steal 4-field audit, absent-lock noop, self-force releases not steals) |
| `plugins/vsdd-factory/skills/factory-lock/SKILL.md` | CREATE | New skill: thin orchestrator invoking precheck helper + documenting state-manager delegation + event emission (D4) |
| `plugins/vsdd-factory/skills/factory-unlock/SKILL.md` | CREATE | New skill: thin orchestrator invoking decide helper + documenting state-manager delegation + event emission (D5) |
| `plugins/vsdd-factory/skills/factory-health/SKILL.md` | MODIFY | Add invocation of `factory-lock-status.sh` for three-state lock status display (D7); existing behavior unchanged |
| `plugins/vsdd-factory/skills/factory-worktree-health/SKILL.md` | MODIFY | Add invocation of same `factory-lock-status.sh` for lock status display (D8); existing behavior unchanged |
| `plugins/vsdd-factory/bin/factory-lock-write.sh` | REUSE (S-17.01) | State-manager-invoked writer — DO NOT modify |
| `plugins/vsdd-factory/bin/factory-cas-push.sh` | REUSE (S-17.01) | State-manager-invoked CAS pusher — DO NOT modify |
| `plugins/vsdd-factory/bin/emit-event` | REUSE (existing) | SS-03 event emitter — DO NOT modify |

**Pre-write verification:** Run T-11 SDK Grounding Evidence greps before any writes to
confirm S-17.01 helpers + existing skill targets are present.

## Token Budget Estimate

| Component | Estimated Tokens |
|-----------|-----------------|
| Story spec (this file) | ~9,500 |
| BC-6.23.001 (skills + health BC) | ~8,500 |
| BC-5.40.001 (schema/CAS reference for delegation model) | ~5,500 |
| BC-4.13.001 (refusal message format alignment) | ~6,000 |
| ADR-025 v1.2 (Decisions 5 Path B, 6, 8) | ~8,000 |
| `plugins/vsdd-factory/skills/factory-health/SKILL.md` | ~4,000 |
| `plugins/vsdd-factory/skills/factory-worktree-health/SKILL.md` | ~4,000 |
| BC files (3: BC-6.23.001, BC-5.40.001, BC-4.13.001) | ~5,500 |
| New bin/ helpers (3 × ~2,000) | ~6,000 |
| New bats test files (3 × ~4,000) | ~12,000 |
| New SKILL.md files (2 new, 2 amend) | ~6,000 |
| Tool outputs (greps, bats run) | ~3,000 |
| **Total** | **~83,500** |

**Budget usage:** ~83,500 / ~200,000 ≈ 42%. Exceeds the 20–30% ceiling slightly due to
3 bats test files with comprehensive fixture coverage. If the implementer is constrained,
split delivery: T-2/T-3/T-4/T-5 (helpers + bats) in sub-burst A; T-6..T-10 (skills +
integration tests) in sub-burst B. Each sub-burst is under 30% independently.

## Red Gate Test Table

All tests are bats (bash executable tests) against the three new bin/ helpers per the
executable-helper model (L-issue-169-176-worktree-identity(b) + S-17.01 precedent).
Test names are bats `@test` names.

### `factory-lock-status.bats` (AC-007, AC-008)

| Bats Test Name | BC Clause | Pass Condition | Initial State (Red Gate) |
|----------------|-----------|---------------|--------------------------|
| `test_BC_6_23_001_factory_lock_status_sh_free_when_absent` | PC7 | Fixture STATE.md with no `factory_lock` key → stdout is `Factory lock: FREE` | FAIL: script does not exist |
| `test_BC_6_23_001_factory_lock_status_sh_free_when_expired` | PC7 | Fixture with `expires_at` in the past → stdout is `Factory lock: FREE` | FAIL: script does not exist |
| `test_BC_6_23_001_factory_lock_status_sh_self_held` | PC7 | Fixture with `holder == test_email`, unexpired → stdout is `Factory lock: HELD by this session (expires <expires_at>)` | FAIL: script does not exist |
| `test_BC_6_23_001_factory_lock_status_sh_foreign_held` | PC7 | Fixture with `holder != test_email`, unexpired → stdout is `Factory lock: HELD by other@x.com since <locked_at> (expires <expires_at>)` | FAIL: script does not exist |
| `test_BC_6_23_001_factory_lock_status_sh_malformed_block` | PC7 | Fixture with malformed `factory_lock:` block (missing fields) → stdout is `Factory lock: FREE (malformed block — treated as unlocked)` | FAIL: script does not exist |
| `test_BC_6_23_001_factory_lock_status_sh_shared_by_both_health_skills` | PC8 | Both `factory-health/SKILL.md` and `factory-worktree-health/SKILL.md` contain `factory-lock-status.sh` (grep assertion) | FAIL: SKILL.md files not yet amended |

### `factory-lock-acquire-precheck.bats` (AC-003, AC-009, AC-010, AC-012)

| Bats Test Name | BC Clause | Pass Condition | Initial State (Red Gate) |
|----------------|-----------|---------------|--------------------------|
| `test_BC_6_23_001_acquire_precheck_fetch_failure_aborts` | Pre-2 (EC-006) | Stub git returning non-zero on `fetch`; script exits non-zero; stderr contains "Fetch failed before lock check" | FAIL: script does not exist |
| `test_BC_6_23_001_acquire_precheck_empty_email_rejected` | Pre-3 (EC-007) | Stub `git config user.email` returning empty; script exits non-zero; stderr contains "git user.email not configured" | FAIL: script does not exist |
| `test_BC_6_23_001_acquire_precheck_self_held_noop` | EC-001 | Fixture with self-held unexpired lock; script exits 0; stdout contains "NOOP_SELF_HELD" | FAIL: script does not exist |
| `test_BC_6_23_001_acquire_precheck_foreign_lock_refusal_all_five_fields` | PC3 | Fixture with foreign unexpired lock; script exits 1; stderr contains holder email, locked_at, expires_at, time_remaining, `/factory-unlock --force` | FAIL: script does not exist |
| `test_BC_6_23_001_acquire_precheck_proceed_when_absent` | PC1 | Fixture with absent lock; script exits 0; stdout contains "PROCEED_ACQUIRE" | FAIL: script does not exist |
| `test_BC_6_23_001_acquire_precheck_proceed_when_expired` | EC-002 | Fixture with expired lock (any holder); script exits 0; stdout contains "PROCEED_ACQUIRE" | FAIL: script does not exist |

### `factory-unlock-decide.bats` (AC-004, AC-005, AC-006, AC-014, EC-010)

| Bats Test Name | BC Clause | Pass Condition | Initial State (Red Gate) |
|----------------|-----------|---------------|--------------------------|
| `test_BC_6_23_001_unlock_decide_self_release_proceed` | PC4 | Fixture with self-held lock, no `--force`; exits 0; stdout contains "PROCEED_RELEASE" + holder, locked_at, released_at fields | FAIL: script does not exist |
| `test_BC_6_23_001_unlock_decide_non_holder_rejected` | PC5 | Fixture with foreign lock, no `--force`; exits 1; stderr contains "Cannot unlock — factory is held by"; "Use /factory-unlock --force" | FAIL: script does not exist |
| `test_BC_6_23_001_unlock_decide_force_steal_four_fields` | PC6 | Fixture with foreign lock + `--force`; exits 0; stdout contains "PROCEED_FORCE_STEAL" + stolen_by, stolen_from, holder_locked_at, stolen_at | FAIL: script does not exist |
| `test_BC_6_23_001_unlock_decide_force_on_absent_lock_noop` | EC-005 | Fixture with absent lock + `--force`; exits 0; stdout contains "NOOP_ABSENT" | FAIL: script does not exist |
| `test_BC_6_23_001_unlock_decide_already_unlocked_noop` | EC-003 | Fixture with absent lock, no `--force`; exits 0; stdout contains "NOOP_ABSENT" | FAIL: script does not exist |
| `test_BC_6_23_001_unlock_decide_self_force_emits_released_not_stolen` | EC-010 | Fixture with self-held lock + `--force`; exits 0; stdout contains "PROCEED_RELEASE_SELF_FORCE" (NOT "PROCEED_FORCE_STEAL") | FAIL: script does not exist |

### Integration bats (AC-001, AC-013 — require real git repo fixture)

| Bats Test Name | BC Clause | Pass Condition | Initial State (Red Gate) |
|----------------|-----------|---------------|--------------------------|
| `test_BC_6_23_001_factory_lock_acquire_success_schema_event_message` | PC1 | Full acquire: precheck PROCEED → state-manager write → CAS push → event emitted → success message | FAIL: skill SPEC + helpers don't exist |
| `test_BC_6_23_001_factory_lock_cas_rejection_error_message` | PC2 | Stub CAS push rejection → `AcquireRaceRejected` message displayed | FAIL: no CAS rejection path in skill |
| `test_BC_6_23_001_factory_unlock_self_release_clears_key_emits_event` | PC4 | Decide PROCEED_RELEASE → state-manager clear → event emitted → "Factory lock released." | FAIL: skill doesn't exist |
| `test_BC_6_23_001_factory_unlock_force_emits_stolen_event_all_four_fields` | PC6 | Decide PROCEED_FORCE_STEAL → state-manager clear → `factory.lock.stolen` with 4 fields | FAIL: no force-release skill |
| `test_BC_6_23_001_factory_health_three_state_display` | PC7 | factory-health invokes `factory-lock-status.sh`; output contains the status line | FAIL: SKILL.md not amended |
| `test_BC_6_23_001_factory_worktree_health_same_display_as_factory_health` | PC8 | factory-worktree-health invokes same helper; same status string for same fixture | FAIL: SKILL.md not amended |
| `test_BC_6_23_001_concurrent_acquire_cas_race_one_wins_one_rejected` | T-4/T-10 | Two concurrent acquire attempts; one PROCEED wins CAS; one `AcquireRaceRejected` | FAIL: skill doesn't exist |
| `test_BC_6_23_001_factory_unlock_non_holder_rejected_no_state_change` | PC5 | Decide REFUSED_NOT_HOLDER; no STATE.md write occurs | FAIL: skill doesn't exist |
| `test_BC_6_23_001_skill_mds_contain_no_direct_state_write` | Inv 5/AC-011 | grep on factory-lock/SKILL.md + factory-unlock/SKILL.md: no direct `Write STATE.md` instruction | FAIL: files don't exist yet |

## Demo Plan

| AC | Demo Command | Expected Output |
|----|-------------|-----------------|
| AC-007/AC-008 | `bash plugins/vsdd-factory/bin/factory-lock-status.sh /path/fixture-free.md "dev@x.com"` | `Factory lock: FREE` |
| AC-007 (self-held) | `bash plugins/vsdd-factory/bin/factory-lock-status.sh /path/fixture-self.md "dev@x.com"` | `Factory lock: HELD by this session (expires 2026-06-11T02:00:00Z)` |
| AC-007 (foreign) | `bash plugins/vsdd-factory/bin/factory-lock-status.sh /path/fixture-foreign.md "dev@x.com"` | `Factory lock: HELD by other@x.com since 2026-06-11T00:15:00Z (expires 2026-06-11T01:00:00Z)` |
| AC-009 | `bash plugins/vsdd-factory/bin/factory-lock-acquire-precheck.sh /path/fixture.md` (with git fetch stubbed to fail) | Exit non-zero + "Fetch failed before lock check." in stderr |
| AC-003 | `bash plugins/vsdd-factory/bin/factory-lock-acquire-precheck.sh /path/fixture-foreign.md` | Exit 1 + 5-field refusal in stderr |
| AC-006 | `bash plugins/vsdd-factory/bin/factory-unlock-decide.sh /path/fixture-foreign.md --force` | Exit 0 + "PROCEED_FORCE_STEAL" + stolen_by/stolen_from/holder_locked_at/stolen_at in stdout |
| AC-011 | `grep -c "factory-lock-status.sh" plugins/vsdd-factory/skills/factory-health/SKILL.md plugins/vsdd-factory/skills/factory-worktree-health/SKILL.md` | 2 (one per file) |

## Risk

| Risk | Severity | Mitigation |
|------|----------|-----------|
| `factory.lock.stolen` not emitted when SS-03 sink is down | HIGH | `emit-event` is failure-tolerant (always exits 0); AC-006 / Invariant 2: force-release unconditional |
| Refusal message format in `factory-lock-acquire-precheck.sh` diverges from guard block message (BC-4.13.001 PC1) | MEDIUM | AC-003 bats test checks the 5-field structure matches guard T-2 output format exactly |
| factory-health and factory-worktree-health drift on display format | MEDIUM | AC-008 structural bats test greps both SKILL.md files for `factory-lock-status.sh` invocation |
| Implementer re-creates CAS push logic instead of reusing `factory-cas-push.sh` | HIGH | Architecture Compliance Rule 7 + Forbidden Dependencies: new `--force-with-lease` implementations are forbidden |
| `factory_lock: null` written instead of key removal on unlock | HIGH | state-manager uses `factory-lock-write.sh clear` from S-17.01; AC-004 tests this path |
| Token budget overflow (42%) causes context degradation | MEDIUM | Burst split procedure: helpers+bats in sub-burst A (~26%); skills+integration in sub-burst B (~22%) |

## Changelog

| Version | Date | Description |
|---------|------|-------------|
| 1.2 | 2026-06-11 | S-17.03 DELIVERED/MERGED (state-manager; D-547; issue #170). PR #183 squash-merged 60fd0233 to develop 2026-06-11; status draft→merged; BC-6.23.001 POL-14 draft→active; 26 bats + 5-platform CI run 27343001859 all-green; LOCAL adversary BC-5.39.001 3-CLEAN (refusal-msg guard-parity, CRLF cross-component parity, subshell-scoped CRLF temp-file leak); security 0-findings; pr-reviewer APPROVE; issue #170 CLOSED; E-17 3/3 COMPLETE; BC-INDEX v2.71→v2.72; STORY-INDEX v3.91→v3.92. |
| 1.1 | 2026-06-11 | Executable-helper refinement (story-writer; delivery-prep Red-Gate-feasibility defect; L-issue-169-176-worktree-identity(b) precedent; S-17.01 executable-helper model; issue #170). Three new bin/ helpers extracted: `factory-lock-status.sh` (shared three-state display, AC-007/AC-008), `factory-lock-acquire-precheck.sh` (fetch + email + acquire decision, AC-003/AC-009/AC-010/AC-012), `factory-unlock-decide.sh` (unlock decision + audit field construction, AC-004/AC-005/AC-006/AC-014/EC-010). Three new bats files added (`factory-lock-status.bats`, `factory-lock-acquire-precheck.bats`, `factory-unlock-decide.bats`). SKILL.md files (factory-lock, factory-unlock, factory-health, factory-worktree-health) become thin orchestrators invoking helpers + documenting state-manager delegation; they are no longer the bats test targets. File Structure Requirements updated (6 new files: 3 helpers + 3 bats; 4 SKILL.md create/amend; 3 REUSE anchors). Red Gate Test Table rewritten as bats @test names against helpers. Tasks rewritten to reflect helper-first TDD. Architecture Mapping updated with helper components + REUSE anchors. Token Budget updated (~58,000 → ~83,500; 42%; burst-split guidance added). Previous Story Intelligence section updated with S-17.01 MERGED status + explicit REUSE instructions. All 14 ACs + their BC-6.23.001 PC/EC traces UNCHANGED. BC-6.23.001 confirmed mechanism-agnostic (prescribes WHAT, not HOW to implement helpers). |
| 1.0 | 2026-06-10 | Initial authoring (story-writer; brownfield-backfill issue #170; ADR-025 v1.2 D4/D5/D7/D8/D9; BC-6.23.001 v1.0). 14 ACs tracing PC1–PC8 + Pre-2/Pre-3 + EC-001/EC-005/EC-010 + D9 bats T-1..T-10. Wave 3; points 8; tdd_mode strict; depends_on [S-17.01, S-17.02]; blocks []. |
