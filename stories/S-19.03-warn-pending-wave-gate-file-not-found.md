---
document_type: story
level: implementation
story_id: S-19.03
epic_id: "E-19"
version: "1.4"
title: "warn-pending-wave-gate FINDING-2: read_file file_not_found semantics + graceful absent-file handling"
status: draft
producer: story-writer
timestamp: "2026-07-04T00:00:00Z"
last_amended: "2026-07-06 (v1.4) — E-19 pass-3 fix burst (story-writer): F-P3-005: subsystems: SS-02 added → [SS-04, SS-01, SS-02]; F-P3-014: behavioral_contracts: BC-2.02.011 added → [BC-2.07.001, BC-2.02.011]; POLICY 8 BC table row for BC-2.02.011 v1.4 added; AC-001 BC Trace updated to include BC-2.02.011 EC-001. F-P3-010: blocks: [] → [S-19.06]. O-P3-003: AC-001 gate extended with path_resolution_failed reason token assertion per BC-2.07.001 EC-007. O-P3-006: F-P2-012 baseline task updated to emit machine-parseable marker. [Prior: 2026-07-06 (v1.3) — E-19 pass-2 fix burst (story-writer): behavioral_contracts: [BC-2.07.001] wired; verification_properties: [VP-097, VP-098] wired; POLICY 8 full treatment: Behavioral Contracts section (BC-2.07.001 v1.0 table) + BC Trace column in AC table (with VP-097 Kani traversal-defense cite in AC-001, VP-098 in AC-002/AC-003). F-P2-007: Architecture Mapping + File Structure concrete module site — replaced vague 'codes.rs (or equivalent)' with `pub mod codes` block in `crates/factory-dispatcher/src/host/mod.rs`. O-P2-002: task added for HostError::NotFound named-variant in hook-sdk host.rs (no #[non_exhaustive] — prevents exhaustive pattern matching in plugin code). F-P2-012 cargo test baseline task added. [Prior: 2026-07-06 (v1.2) — E-19 pass-1 fix burst (story-writer): F-P1-011: AC-001 broken ancestor-fallback rewritten to rejoin algorithm (canonicalize deepest existing ancestor, rejoin non-existent tail, then starts_with canonical prefix — mirrors resolve_path_for_allowlist in write_file.rs; hoist to shared host module instructed). F-P1-001: ADR-025 Decision 13 cite added to AC-003. Missing template sections added (Edge Cases, Purity Classification, Token Budget Estimate, Tasks, Previous Story Intelligence, Architecture Compliance Rules, Library & Framework Requirements). [Prior: 2026-07-06 (v1.1) — F-P1-001 architect fix burst: corrected codes::NOT_FOUND value -4→-5 (collision with INVALID_ARGUMENT=-4 verified; correct allocation is -5 per ADR-025 Decision 13); AC-003 grep gate updated -4→-5; Architecture Mapping updated -4→-5; T-005 description updated. [Prior: 2026-07-04 (v1.0) — initial draft authored by story-writer; post-rc.22 smoke gate authorization (2026-07-04); closes rc.22 smoke FINDING-2 (warn-pending-wave-gate capability_denied path_not_allowed on every Stop event when wave-state.yaml absent; dispatcher trace bc687a0f).]]]]"
phase: F3
cycle: v1.0-feature-engine-discipline-pass-1
inputs:
  - crates/hook-plugins/warn-pending-wave-gate/src/lib.rs
  - crates/factory-dispatcher/src/host/read_file.rs
  - plugins/vsdd-factory/hooks-registry.toml
input-hash: "8d1225d"
traces_to:
  - .factory/stories/epics/E-19-post-rc22-operator-hardening.md
wave: 1
priority: P1
points: 5
track: feature
effort_class: single-day
depends_on: []
blocks: [S-19.06]
behavioral_contracts: [BC-2.07.001, BC-2.02.011]
verification_properties: [VP-097, VP-098]
domain_invariants: []
tdd_mode: strict
target_module: "crates/hook-plugins/warn-pending-wave-gate"
subsystems:
  - "SS-04"
  - "SS-01"
  - "SS-02"
estimated_days: 1.5
assumption_validations: []
risk_mitigations: []
anchored_adrs: [ADR-025]
closes:
  - "rc.22 smoke FINDING-2: warn-pending-wave-gate emits false-positive capability_denied path_not_allowed on every Stop event when .factory/wave-state.yaml is absent (dispatcher trace bc687a0f; verified root cause: read_file.rs path_allowed() calls canonicalize() which fails for non-existent files, returning path_not_allowed even when path IS in the allowlist)"
feature: "post-rc.22-smoke"
modified:
  - "2026-07-06 v1.4: F-P3-005 SS-02 added to subsystems; F-P3-014 BC-2.02.011 wired (behavioral_contracts + POLICY 8 BC table + AC-001 BC Trace); F-P3-010 blocks: [S-19.06]; O-P3-003 path_resolution_failed assertion in AC-001 gate; O-P3-006 F-P2-012 baseline marker"
  - "2026-07-06 v1.3: behavioral_contracts wired [BC-2.07.001]; verification_properties wired [VP-097, VP-098]; POLICY 8 BC table + BC Trace column; F-P2-007 concrete module site; O-P2-002 task; F-P2-012 task"
  - "2026-07-06 v1.2: F-P1-011 AC-001 rejoin algorithm rewrite; F-P1-001 ADR-025 Decision 13 cite in AC-003; missing template sections added"
  - "2026-07-06 v1.1: F-P1-001 fix — codes::NOT_FOUND corrected -4→-5 (collision with INVALID_ARGUMENT=-4; ADR-025 Decision 13 allocates -5); AC-003, Architecture Mapping, T-005, File Structure updated"
  - "2026-07-04 v1.0: initial draft"
---

# S-19.03 — warn-pending-wave-gate FINDING-2: read_file file_not_found semantics + graceful absent-file handling

**Epic:** E-19 — Post-rc.22 Operator Hardening
**Wave:** 1 (no hard predecessor in E-19; can begin in parallel with S-19.02)
**Depends on:** none
**Priority:** P1 (functional defect — silently incorrect telemetry on every Stop event in fresh installs; masks real capability misconfiguration)
**Subsystem anchor:** SS-04 (Plugin Ecosystem) owns the WASM plugin. SS-01 (Hook Dispatcher Core) owns the `read_file.rs` host function being corrected.

## Narrative

The `warn-pending-wave-gate` Stop-event plugin warns operators when a wave gate check is pending (i.e., `.factory/wave-state.yaml` indicates `pending_gate: true`). In a fresh install — or any install where no wave has been handed off yet — `wave-state.yaml` does not exist.

**The defect:** `read_file.rs::path_allowed()` calls `resolved.canonicalize()` to resolve symlinks before checking the path against the allowlist. `canonicalize()` returns `Err` if the file does not exist. On error, `path_allowed()` returns `false`, which causes the dispatcher to emit `internal.capability_denied reason=path_not_allowed` and return `CAPABILITY_DENIED (-1)` — even though `.factory/wave-state.yaml` IS in the allowlist.

The consequence is twofold:
1. The plugin can never detect a pending wave gate in an install where `wave-state.yaml` does not yet exist (it degrades gracefully to Continue, but the detection fails silently).
2. Every Stop event emits a false-positive `capability_denied` telemetry event that misleads operators into thinking there is a capability misconfiguration when there is none. The event type `path_not_allowed` is indistinguishable from a genuine allowlist violation.

**Diagnosis note:** The `path_allow = [".factory/wave-state.yaml"]` entry IS present in both the development `plugins/vsdd-factory/hooks-registry.toml` and the rc.22 operator cache registry. The root cause is entirely in `read_file.rs::path_allowed()` conflating "file does not exist" with "path not allowed".

**Fix direction (two-layer, production-grade):**

1. **Dispatcher fix (SS-01):** Refactor `read_file.rs::path_allowed()` to use the rejoin algorithm for target paths that do not yet exist. If `resolved.canonicalize()` fails (file absent), walk ancestors until one canonicalizes successfully (the deepest existing ancestor), then rejoin the non-existent tail path components back onto the canonical ancestor path, producing a synthesized canonical path. Check that this synthesized path starts_with an allowed prefix. This mirrors the `resolve_path_for_allowlist` helper in `write_file.rs` (cite by function name, not line number — TD-VSDD-091); that helper MUST be extracted to a shared host module (`crates/factory-dispatcher/src/host/path_util.rs`) rather than duplicated in `read_file.rs`. If path is allowed but file absent, `read_bounded()` fails to open → return a distinct `ReadErr::NotFound` variant that emits a new telemetry event `internal.file_not_found` (not `capability_denied`) and returns a new `codes::NOT_FOUND (-5)`. This distinguishes the two failure modes for downstream consumers and operators.

2. **Plugin fix (SS-04):** Update `warn-pending-wave-gate` to explicitly handle `host::read_file` returning `NOT_FOUND (-5)`: treat this as "wave-state.yaml absent = no pending gate → Continue, no WARN log." Also ensure the plugin handles `CAPABILITY_DENIED (-1)` for genuine allowlist violations with an appropriate WARN (not the same silent path as file-not-found).

## Acceptance Criteria

| ID | Criterion | Gate | BC Trace |
|----|-----------|------|----------|
| AC-001 | `read_file.rs::path_allowed()` uses the rejoin algorithm for target paths that do not yet exist: if `resolved.canonicalize()` fails (file absent), walk ancestors until one canonicalizes (deepest existing ancestor), then rejoin the non-existent tail components to produce a synthesized canonical path, then check starts_with an allowed prefix. Implementation MUST reuse `resolve_path_for_allowlist` extracted to a shared host module (`crates/factory-dispatcher/src/host/path_util.rs`) rather than duplicating logic from `write_file.rs`. If the ancestor check passes, `path_allowed()` returns `true` (file is reachable under an allowed prefix, even though it does not yet exist). | Unit test: allowlisted path for a non-existent file under an allowed parent → `path_allowed()` returns true. Exit non-zero if false. Negative control A: path outside all allowed prefixes → `path_allowed()` returns false. Negative control B (O-P3-003 per BC-2.07.001 EC-007): path with NO existing ancestor (every ancestor component is absent) → `path_allowed()` returns false AND the dispatcher emits `internal.capability_denied` with `reason=path_resolution_failed` (NOT `path_not_allowed`); assert exact reason token in telemetry; exit non-zero if reason token is absent or is `path_not_allowed`. | BC-2.07.001 part b (rejoin algorithm); BC-2.02.011 EC-001 (path traversal → CAPABILITY\_DENIED via `resolve_path_for_allowlist` in `path_util.rs` — `write_file.rs` depends on the same function; VP-097 Kani proof anchors both); VP-097 (Kani traversal-defense: synthesized canonical path is traversal-safe; no `..` injection possible) |
| AC-002 | When `path_allowed()` returns true but the file does not exist, `read_file.rs::prepare()` emits `internal.file_not_found` (not `internal.capability_denied`) and returns `codes::NOT_FOUND`. | Unit test: path allowed, file absent → `prepare()` returns `Err(codes::NOT_FOUND)`; the emitted event has `type=internal.file_not_found` and `reason=file_not_found`. Captured event stream contains zero `capability_denied` events. Exit non-zero on deviation. | BC-2.07.001 part c (NOT_FOUND for absent allowlisted); VP-098 (functional: NOT_FOUND returned, zero false-positive capability_denied) |
| AC-003 | `codes::NOT_FOUND` is defined as `-5` (next available negative integer after `INVALID_ARGUMENT = -4` per ADR-025 Decision 13; occupied codes: 0/-1/-2/-3/-4/-99; -5 is the next free code in the compact negative sequence) in the `pub mod codes` block in `crates/factory-dispatcher/src/host/mod.rs` and exported from `hook-sdk` for plugin use. | `grep -rq "NOT_FOUND.*=.*-5\|NOT_FOUND.*-5" crates/` exits 0. | BC-2.07.001 (NOT_FOUND = -5); VP-098 |
| AC-004 | `warn-pending-wave-gate` plugin handles `NOT_FOUND` return from `host::read_file` as "wave-state.yaml absent → no pending gate → Continue" without emitting a WARN-level internal log message. | Unit/integration test: fixture with no `wave-state.yaml` → plugin exit code 0 with zero plugin.log entries at warn level. Exit non-zero if WARN log entries present or exit code non-zero. | BC-2.07.001 part c (plugin behavior on NOT_FOUND) |
| AC-005 | Positive-control test: when `wave-state.yaml` exists and contains `pending_gate: true`, the plugin emits a user-visible warning to stderr and exits 0 (advisory; on_error=continue). | Integration test with fixture `wave-state.yaml` containing `pending_gate: true` → assert stderr contains warning text; assert exit code 0. Exit non-zero if warning absent or exit code non-zero. | BC-2.07.001 (positive-path operation) |
| AC-006 | No `internal.capability_denied reason=path_not_allowed` events for `plugin_name=warn-pending-wave-gate` in any operator install where `wave-state.yaml` is absent after this fix ships. | Integration test: no `wave-state.yaml` → zero `capability_denied` events in captured stream for this plugin. Exit non-zero if any present. | BC-2.07.001 part c (zero false-positive capability_denied); VP-098 |

## Architecture Mapping

**SS-01 changes (crates/factory-dispatcher/src/host/read_file.rs):**
- `path_allowed()`: replace broken `canonicalize()`-only check with the rejoin algorithm for absent files; extract shared helper `resolve_path_for_allowlist` (from `write_file.rs`) into `crates/factory-dispatcher/src/host/path_util.rs`; both `read_file.rs` and `write_file.rs` import from `path_util`
- `prepare()`: add `ReadErr::NotFound` variant; emit `internal.file_not_found` event; return `codes::NOT_FOUND`
- `read_file.rs` unit tests: add AC-001 and AC-002 coverage

**SS-01 changes (`pub mod codes` block in `crates/factory-dispatcher/src/host/mod.rs`):**
- Add `pub const NOT_FOUND: i32 = -5;` in the `pub mod codes` block (ADR-025 Decision 13)
- This is the concrete site for `codes::NOT_FOUND` (F-P2-007); "codes.rs or equivalent" language retired

**SS-01 changes (crates/factory-dispatcher/src/host/path_util.rs — new shared module):**
- Extract `resolve_path_for_allowlist` from `write_file.rs` into this shared module; import in both `read_file.rs` and `write_file.rs`

**SS-02 changes (crates/hook-sdk/src/host.rs):**
- Export `NOT_FOUND` code constant and add `HostError::NotFound` named variant so WASM plugins can pattern-match on it (O-P2-002: do NOT add `#[non_exhaustive]` — this attribute would prevent exhaustive pattern matching in plugin code, which is required for correct abandoned-path handling)

**SS-04 changes (crates/hook-plugins/warn-pending-wave-gate/src/lib.rs):**
- Handle `NOT_FOUND` return: return Continue, suppress WARN log
- Handle `CAPABILITY_DENIED`: emit WARN "capability configuration error"
- Add AC-004 / AC-005 / AC-006 tests

## Behavioral Contracts

| BC ID | Title | Version | Story ACs |
|-------|-------|---------|-----------|
| BC-2.07.001 | host::read\_file absent-file semantics — codes::NOT\_FOUND (-5) additive error code, HostError::NotFound SDK variant, rejoin path-allowed resolution, zero false-positive capability\_denied | v1.1 | AC-001 (part b rejoin algorithm), AC-002 (part c NOT\_FOUND + file\_not\_found), AC-003 (NOT\_FOUND = -5), AC-004 (plugin NOT\_FOUND handling), AC-005 (positive-path operation), AC-006 (zero false-positive capability\_denied) |
| BC-2.02.011 | host::write\_file: bounded write capability with allowlist enforcement | v1.4 | AC-001 (path traversal prevention via `resolve_path_for_allowlist` in `path_util.rs`; EC-001 traversal → CAPABILITY\_DENIED) |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | `wave-state.yaml` exists but is malformed (not valid YAML) | Plugin emits WARN "wave-state.yaml parse error" and exits 0 (advisory; fail-open). No block. |
| EC-002 | `wave-state.yaml` exists and `pending_gate: false` or field absent | Plugin exits 0 without warning (no pending gate). |
| EC-003 | Genuine path-not-allowed violation (path outside all allowed prefixes) | Continues to emit `internal.capability_denied reason=path_not_allowed` (unchanged behavior for this case). |
| EC-004 | `wave-state.yaml` path resolves through a symlink | Rejoin algorithm follows symlink resolution on the ancestor correctly (canonicalize on the existing ancestor succeeds; tail is rejoined). |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| `crates/factory-dispatcher/src/host/read_file.rs` | effectful-shell | File I/O (open, read), telemetry emission via event sink |
| `crates/factory-dispatcher/src/host/path_util.rs` | pure-core | Path manipulation only (ancestor walk, rejoin, starts_with); no I/O |
| `pub mod codes` in `crates/factory-dispatcher/src/host/mod.rs` | pure-core | Constant definitions only |
| `crates/hook-sdk/src/host.rs` | pure-core | Type definitions and re-exports; no I/O |
| `crates/hook-plugins/warn-pending-wave-gate/src/lib.rs` | effectful-shell | Calls `host::read_file` (host ABI boundary, I/O side effect), emits WARN to stderr |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~5,000 |
| `crates/factory-dispatcher/src/host/read_file.rs` | ~2,000 |
| `crates/factory-dispatcher/src/host/write_file.rs` (resolve_path_for_allowlist reference) | ~1,500 |
| `crates/hook-plugins/warn-pending-wave-gate/src/lib.rs` | ~1,500 |
| `crates/hook-sdk/src/host.rs` | ~1,000 |
| BC-2.07.001 v1.0 | ~2,500 |
| ADR-025 (Decision 13) | ~1,000 |
| Test files (new) | ~2,000 |
| Tool outputs overhead | ~1,500 |
| **Total** | **~18,000** |
| Agent context window | 200K (Sonnet) |
| **Budget usage** | **~9%** |

## Tasks (MANDATORY)

1. [ ] Capture `cargo test --workspace --all-targets` baseline pass count pre-implementation; emit machine-parseable marker line `pre-implementation cargo-test baseline: <N> pass` to stdout (replace `<N>` with actual count) and record in PR description (F-P2-012)
2. [ ] Extract `resolve_path_for_allowlist` from `write_file.rs` into `path_util.rs` shared module (test-writer writes unit tests first)
3. [ ] Write failing tests for `path_allowed()` rejoin algorithm (AC-001) — test-writer
4. [ ] Write failing tests for `prepare()` `NOT_FOUND` return + `file_not_found` event (AC-002) — test-writer
5. [ ] Write failing test for `codes::NOT_FOUND == -5` constant in `pub mod codes` block in `host/mod.rs` (AC-003) — test-writer
6. [ ] Write failing tests for `warn-pending-wave-gate` graceful absent-file handling (AC-004, AC-006) — test-writer
7. [ ] Write positive-control test for pending_gate: true flow (AC-005) — test-writer
8. [ ] Write Kani harness for traversal-defense property of `resolve_path_for_allowlist` (VP-097): assert that for any input path, the synthesized canonical path starts_with an allowed prefix and contains no `..` components — formal-verifier
9. [ ] Verify Red Gate: all tests fail with stubs
10. [ ] Implement `path_util::resolve_path_for_allowlist` + `read_file::path_allowed()` rejoin branch (implementer)
11. [ ] Implement `ReadErr::NotFound` + `pub const NOT_FOUND: i32 = -5;` in `pub mod codes` in `host/mod.rs` + `file_not_found` event emission (implementer); O-P2-002: add `HostError::NotFound` named variant to `crates/hook-sdk/src/host.rs` WITHOUT `#[non_exhaustive]`
12. [ ] Update `hook-sdk` to export `NOT_FOUND` code + `HostError::NotFound` (implementer)
13. [ ] Update `warn-pending-wave-gate` to handle `NOT_FOUND` and `CAPABILITY_DENIED` (implementer)
14. [ ] Verify all tests pass; confirm zero `capability_denied` events for absent-file path; assert baseline preserved + new tests added (post-implementation F-P2-012)

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-19.02 (same wave) | verify-factory-lock raised byte cap to 262144 via STATE_MD_MAX_BYTES constant; `extract_frontmatter()` added for frontmatter-only parse | WASM plugin source changes require host ABI compatibility check; constants live in plugin `lib.rs`, not dispatcher `codes.rs` | None affecting S-19.03 |
| S-17.02 (verify-factory-lock impl) | `host::read_file` capability block requires explicit `path_allow`; deny-by-default silent-no-op footgun class documented | Hook plugins pattern-match on `codes::*` constants from `hook-sdk` | `exec_subprocess` env_allow silent-no-op: same class as this story's `canonicalize` silent-no-op |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| `codes::NOT_FOUND = -5` declared in `pub mod codes` block in `crates/factory-dispatcher/src/host/mod.rs` (F-P2-007) | ADR-025 Decision 13; BC-2.07.001 | `grep -rq "NOT_FOUND.*=.*-5" crates/` exits 0; vague "codes.rs or equivalent" language retired |
| `HostError::NotFound` named variant in `hook-sdk/src/host.rs` MUST NOT use `#[non_exhaustive]` | O-P2-002 | Code review; `grep -rq "non_exhaustive" crates/hook-sdk/src/host.rs` exits non-zero |
| No file:line volatile pins in spec artifacts | TD-VSDD-091 | Implementer uses function/symbol anchors only; story spec cites `resolve_path_for_allowlist` by name |
| Shared path helpers must not be duplicated across host modules | Architecture Mapping (this story) | `path_util.rs` extraction; both read_file.rs and write_file.rs import from it |
| No `println!` in production Rust | CLAUDE.md Conventions | CI clippy + grep in integration tests |
| Error taxonomy: named error variants, no `unwrap()` in critical paths | CLAUDE.md Conventions | `ReadErr::NotFound` variant; Rust compiler enforces exhaustive match |
| PostToolUse hooks fire after write; writes are not reverted | CLAUDE.md Hook Diagnostics | Implementer proceeds; advisory blocks are non-fatal |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| Rust toolchain | per `rust-toolchain.toml` at repo root | Single-workspace MSRV enforcement |
| `wasmtime` (via factory-dispatcher) | workspace-pinned | WASM plugin sandbox execution |
| `serde` / `serde_json` | workspace-pinned | JSON event serialization |
| `tokio` | workspace-pinned | Async runtime for dispatcher |
| Kani model checker | workspace-pinned (if present) | VP-097 traversal-defense formal proof |
| Bats (Bash Automated Testing System) | per `plugins/vsdd-factory/tests/run-all.sh` | Integration test runner for bats test suites |

## Red Gate Test Plan

| Test ID | Description | Expected Outcome |
|---------|-------------|------------------|
| RG-001 | path_allowed(): allowed path, file absent → returns true | true |
| RG-002 | path_allowed(): path outside all prefixes, file absent → returns false | false |
| RG-003 | prepare(): allowed path, file absent → Err(NOT_FOUND), internal.file_not_found emitted | Err(NOT_FOUND); zero capability_denied events |
| RG-004 | Plugin: no wave-state.yaml → exit 0, zero WARN log entries | exit 0; zero warn entries |
| RG-005 | Plugin: wave-state.yaml with pending_gate: true → stderr warning, exit 0 | stderr non-empty; exit 0 |

## Test Plan

| Test ID | Story AC | Test File | Description |
|---------|----------|-----------|-------------|
| T-001 | AC-001 | crates/factory-dispatcher/src/host/read_file.rs (unit) | path_allowed rejoin algorithm: allowed absent file |
| T-002 | AC-001 | crates/factory-dispatcher/src/host/read_file.rs (unit) | path_allowed: genuinely outside allowlist |
| T-003 | AC-002 | crates/factory-dispatcher/src/host/read_file.rs (unit) | prepare(): NOT_FOUND return + file_not_found event |
| T-004 | AC-002 | crates/factory-dispatcher/src/host/read_file.rs (unit) | prepare(): zero capability_denied events for absent-but-allowed file |
| T-005 | AC-003 | crates/factory-dispatcher/src/host/mod.rs (unit) | NOT_FOUND == -5 assertion in pub mod codes block |
| T-006 | AC-004 | crates/hook-plugins/warn-pending-wave-gate/src/lib.rs (unit) | NOT_FOUND → Continue, zero WARN |
| T-007 | AC-005 | plugins/vsdd-factory/tests/warn-pending-wave-gate.bats | Positive control: pending_gate: true → warning + exit 0 |
| T-008 | AC-006 | plugins/vsdd-factory/tests/warn-pending-wave-gate.bats | Absent file → zero capability_denied events |

## File Structure Requirements (MANDATORY)

| File | Action | Notes |
|------|--------|-------|
| `crates/factory-dispatcher/src/host/read_file.rs` | modify | Replace `canonicalize()`-only check with rejoin algorithm in `path_allowed()`; add `ReadErr::NotFound`; emit `internal.file_not_found`; import `path_util::resolve_path_for_allowlist`; add T-001..T-004 unit tests |
| `crates/factory-dispatcher/src/host/path_util.rs` | create | Extract `resolve_path_for_allowlist` from `write_file.rs`; shared ancestor-based path resolution helper for both read and write; Kani harness for VP-097 traversal-defense proof |
| `crates/factory-dispatcher/src/host/write_file.rs` | modify | Import `resolve_path_for_allowlist` from `path_util` instead of defining locally (sibling-site sweep per TD-VSDD-060) |
| `crates/factory-dispatcher/src/host/mod.rs` | modify | Add `pub const NOT_FOUND: i32 = -5;` in `pub mod codes` block (F-P2-007 concrete site); add T-005 unit test |
| `crates/hook-sdk/src/host.rs` | modify | Export NOT_FOUND constant; add `HostError::NotFound` named variant WITHOUT `#[non_exhaustive]` (O-P2-002) |
| `crates/hook-plugins/warn-pending-wave-gate/src/lib.rs` | modify | Handle NOT_FOUND gracefully; add T-006 unit test |
| `plugins/vsdd-factory/tests/warn-pending-wave-gate.bats` | create or modify | T-007 and T-008 integration tests |
