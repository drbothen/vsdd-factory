---
document_type: story
level: ops
story_id: "S-4.05"
epic_id: "E-4"
version: "1.36"
status: draft
producer: product-owner
timestamp: 2026-04-28T17:00:00
phase: 1.8
inputs:
  - .factory/stories/v1.0-legacy/S-4.5-dead-letter-queue.md
  - .factory/specs/architecture/ARCH-INDEX.md
input-hash: ""
traces_to: .factory/specs/prd.md#FR-044
cycle: v1.0.0-greenfield
points: "5"
depends_on: ["S-4.04"]
blocks: ["S-4.07", "S-4.08"]
behavioral_contracts:
  - BC-3.07.003
  - BC-3.07.004
  - BC-1.06.011
verification_properties:
  - VP-012
capabilities:
  - CAP-024
functional_requirements:
  - FR-044
priority: "P1"
wave: 13
target_module: crates/sink-core/src/dead_letter.rs
subsystems: ["SS-03", "SS-01"]
estimated_days: 3
assumption_validations: []
risk_mitigations: []
---

# S-4.05: Dead letter queue implementation

**Epic:** E-4 — Observability Sinks and RC Release
**Phase:** Phase 4 — Observability sinks
**Milestone:** `1.0.0-rc.1`
**Tier:** E
**Blocks:** S-4.07, S-4.08
**Blocked by:** S-4.04 (retries need to exhaust before DLQ)
**Estimated effort:** M (bumped from S due to path_template extraction, sink-http wiring, configuration AC, and new edge case coverage)

## Capability Anchor Justification

Anchoring to **CAP-024: "Per-sink retry, circuit breaker, and dead-letter queue"** per capabilities.md §CAP-024.

CAP-024 states: "S-4.4/4.5: each sink driver retries failed sends with exponential backoff, trips a circuit breaker on sustained failure, and routes dropped events to a `dead-letter-<sink>-<date>.jsonl`." S-4.05 is explicitly cited as S-4.5 in CAP-024 and delivers the dead-letter queue portion. The outcome CAP-024 promises — "a Datadog outage doesn't lose events — they land in the DLQ" — is directly what this story implements.

**Filename pattern authority:** CAP-024 is the single source of truth for the DLQ filename pattern, extending CAP-024's outcome description (`dead-letter-<sink>-<date>.jsonl`) into the canonical implementation form using BC-3.02.001's `{date}` template placeholder syntax. The semantic mapping is: `<sink>` → `<sink-name>` (operator-assigned sink name); `<date>` → `{date}` (template placeholder substituting to YYYY-MM-DD per BC-3.02.001). All ACs and implementation code use the BC-3.02.001-aligned form: `dead-letter-<sink-name>-{date}.jsonl`.

## Narrative
- **As a** factory operator
- **I want to** have events that exhaust their retries written to a dead letter file
- **So that** I can inspect and replay dropped events rather than losing them silently

## Goal

When events are dropped — because the sink's retry attempts exhausted,
or the outbound queue overflowed — write them to a per-sink dead letter
file. This prevents silent data loss.

## Acceptance Criteria

- [ ] **AC-001** — Per-sink dead letter file at `.factory/logs/dlq/dead-letter-<sink-name>-{date}.jsonl`
  where `{date}` substitutes to `YYYY-MM-DD` at write time (daily rotation via `{date}` placeholder,
  in BC-3.02.001-aligned form (see Capability Anchor Justification for the explicit `<sink>` → `<sink-name>` and `<date>` → `{date}` semantic mapping), reusing the `resolve_path_template` function extracted into
  `sink-core::path_template` per Task 0 below).
  Canonical filename pattern: `dead-letter-<sink-name>-{date}[-<seq>].jsonl` where `<seq>` is an
  optional 3-digit incrementing counter (e.g., `001`, `002`) present ONLY after a within-day size-cap
  rotation (100 MB cap). The first daily file has NO `<seq>` suffix. Size-cap rotation files use
  the same UTC date as the base file opened at that rotation event.
  (Uncontracted — v1.1 BC candidate `BC-3.NN.NNN-dlq-write-on-retry-exhaustion` covers the trigger.
  Note: BC-3.02.013 — `take_failures()` returns SinkFailure — is listed as Related (informational)
  below; it does not contract file-path or write behavior.)

- [ ] **AC-002** — Events written to DLQ when all retries exhausted
  (DLQ write on retry exhaustion is uncontracted — v1.1 BC candidate
  `BC-3.NN.NNN-dlq-write-on-retry-exhaustion`. See v1.1 BC Candidates table.)

- [ ] **AC-003** — Events written to DLQ when outbound queue overflows (queue full)
  (traces to VP-012: Sink Failure Affects Only That Sink — queue overflow failure is per-sink;
  DLQ write on overflow is uncontracted — v1.1 BC candidate
  `BC-3.NN.NNN-dlq-write-on-queue-overflow`. See v1.1 BC Candidates table.)

- [ ] **AC-004** — DLQ file has daily rotation: a new file is created each UTC day via the
  `{date}` placeholder in the filename pattern `dead-letter-<sink-name>-{date}.jsonl`.
  `{date}` resolves to the current UTC date formatted as `YYYY-MM-DD` at write time, identical
  to BC-3.02.001's date-rotation semantics. Default size cap: 100 MB per file; file is rotated
  (new date-stamped file opened) at midnight UTC regardless of size.
  (Traces to BC-3.02.001 for the `{date}` → YYYY-MM-DD contract.
  The `{date}` substitution is provided by `sink-core::path_template::resolve_path_template`
  extracted in Task 0. DLQ-specific composition is uncontracted — v1.1 BC candidate
  `BC-3.NN.NNN-dlq-daily-rotation`. See v1.1 BC Candidates table.)

- [ ] **AC-005** — `internal.sink_dlq_write` event emitted for each DLQ write, subject to channel back-pressure (see BC-3.07.003 PC1 "at most one" semantic under try_send drop).
  Event field schema contracted by **BC-3.07.003** (allocated this wave):
  `type: "internal.sink_dlq_write"`, `sink_name`, `sink_type`, `event_type`, `reason`, `ts` (ISO 8601 with offset),
  `ts_epoch` (i64 seconds), `dispatcher_trace_id`, `schema_version` (u32).
  `schema_version` (u32) is a per-event field on the canonical envelope, populated by `INTERNAL_EVENT_SCHEMA_VERSION`
  (currently 1) per PRD §3.1. Every event MUST emit this field with the constant's current value.
  (Aligns with existing `InternalEvent` struct serialization in `crates/factory-dispatcher/src/internal_log.rs:89-90,131`.)
  (Traces to BC-3.07.003 — see BCs to Create section.)

- [ ] **AC-006a** — Unit test in `sink-core/src/dead_letter.rs`: inject simulated
  retry-exhaustion error into the DLQ writer; verify file contents match expected JSONL
  with all contracted fields present and correct.
  (Traces to BC-3.07.003 for event schema correctness.
  Note: BC-3.02.013 is NOT the anchor here — this tests file write behavior, not
  `take_failures()` return value.)

- [ ] **AC-006b** — Integration test in `sink-http/tests/integration.rs`: mock 5xx server
  forces all retries to exhaust; verify DLQ file at
  `.factory/logs/dlq/dead-letter-<sink-name>-{date}.jsonl` contains the dropped events
  in valid JSONL with correct field schema.
  (Traces to BC-3.07.003 for event schema; the HTTP failure path is sink-http-scoped and
  requires the integration test to live in sink-http, not sink-core.)

- [ ] **AC-007** — DLQ directory auto-created if missing.
  (Traces to BC-3.02.007 — `sink-file::auto_creates_parent_directory` — for the mkdir-p
  auto-create contract. BC-3.02.013 is NOT the anchor here — that BC contracts
  `take_failures()` semantics, not directory creation behavior.)

- [ ] **AC-008** — DLQ is on-by-default for all sinks. Operators may opt-out per-sink
  with `dlq_enabled = false` on the per-sink `[[sinks]]` stanza in `observability-config.toml`
  (where the stanza identifies itself via `name = '<sink-name>'`).
  Default size cap is 100 MB per file; rotation occurs at midnight UTC. When `dlq_enabled = false`,
  retry-exhaustion events are logged to stderr and dropped (not written to disk).
  (Uncontracted — v1.1 BC candidate `BC-3.NN.NNN-dlq-config-toggle-and-size-cap`: DLQ
  on-by-default; per-sink dlq_enabled toggle; size cap and rotation timing; stderr-on-disabled
  drop. See v1.1 BC Candidates table.)

- [ ] **AC-009** — `prune_old` (BC-1.06.004) is extended to cover DLQ files (matching pattern `dead-letter-*.jsonl` per BC-1.06.011 PC2; this prune scope intentionally excludes other unrelated `.jsonl` files that may appear in the dlq directory in v1.1+, e.g., replay-tool logs) with an independent default retention (`INTERNAL_DLQ_DEFAULT_RETENTION_DAYS = 7`, defined in `crates/factory-dispatcher/src/internal_log.rs` per Task 5b). The 7-day value is the named constant; tests assert against the constant, not a literal '7'. Note: dispatcher log retention (`DEFAULT_RETENTION_DAYS = 30`) is independent — DLQ does NOT inherit the 30-day default. (Traces to BC-1.06.011 PC1.)

- [ ] **AC-010** — When a DLQ file write fails (disk full, permission denied), the dispatcher
  emits `internal.sink_dlq_failure` to the internal channel, logs a fallback message to stderr,
  and continues processing without crashing.
  (Traces to BC-3.07.004 — `sink-dlq::dlq_write_failure_observable_via_internal_event` —
  allocated this wave. See BCs to Create section.)

- [ ] **AC-011** — On sink load, `sink_name` is rejected with `SinkConfigError::InvalidSinkName`
  if it contains `/`, `\`, or `..` path tokens. The DLQ file is never created for an
  invalid sink name.
  (Traces to v1.1 BC candidate `BC-3.NN.NNN-sink-name-sanitization` per EC-003.
  See v1.1 BC Candidates table.)

## Behavioral Contracts

| BC ID | Title | Reason for Scope |
|-------|-------|-----------------|
| BC-3.07.003 | sink-dlq::internal_event_emission_on_dlq_write | Contracts the `internal.sink_dlq_write` event shape (9 fields per canonical TV; conforming to `INTERNAL_EVENT_SCHEMA_VERSION = 1` per PRD §3.1; allocated this wave — see BCs to Create) |
| BC-3.07.004 | sink-dlq::dlq_write_failure_observable_via_internal_event | Contracts that DLQ write failures emit `internal.sink_dlq_failure` to the internal channel, not silently dropped (allocated this wave — see BCs to Create) |
| BC-1.06.011 | prune_old::extends_to_dlq_files | Contracts that prune_old covers files in the `dlq/` directory matching the `dead-letter-*.jsonl` prefix-scope with an independent 7-day default retention (INTERNAL_DLQ_DEFAULT_RETENTION_DAYS = 7, distinct from DEFAULT_RETENTION_DAYS = 30 for dispatcher logs; allocated this wave — see BCs to Create) |

## Related Contracts (informational, not authoritative for this story's ACs)

> Per the sanctioned 'spirit-inherited but not semantically anchored' convention (POLICY 8 exception): BCs listed in this section fall into two categories, neither of which is in the story's `behavioral_contracts:` frontmatter array:
> 1. **No AC anchor** (e.g., BC-3.02.015): the BC contracts a behavior (sink-file shutdown drain) that no AC body asserts. Pass-21 F-2101 adjudicated this case via Option A (informational; future story may promote a sink-core-scoped sibling).
> 2. **Scope-extraction inheritance** (e.g., BC-3.02.001, BC-3.02.007): AC-004 / AC-007 do test the contracted behavior ({date} rotation, mkdir-p auto-create), but via the Task 0 extraction (`sink-core::path_template` handles the `{date}` interpolation; mkdir-p is inherited by-replication — DlqWriter duplicates the `fs::create_dir_all` idiom from sink-file::append_jsonl, NOT shared code-path) — the BC's "sink-file" scope label is stale relative to the inherited code path. The "Traces to BC-3.02.NNN" language in AC-004 / AC-007 is intentional: it cites the upstream specification this AC inherits semantics from, without re-anchoring the BC to a sink-core scope. Pass-22 F-2201 adjudicated this case via Option C (status quo + documentation).

### BC-3.02.013 — previously anchored, now informational only

BC-3.02.013 (`sink-file::read_only_path_records_failure_without_panic`) establishes the
invariant that filesystem errors record a SinkFailure without crashing. This story inherits
that spirit but does NOT use BC-3.02.013 as the load-bearing anchor for any AC, because:
- BC-3.02.013 contracts `take_failures()` returning a SinkFailure (in-memory recording)
- S-4.05's ACs are about file-write behavior, HTTP failure paths, and mkdir-p — none of
  which are contracted by BC-3.02.013

| BC | Title | Relationship |
|---|---|---|
| BC-3.02.013 | sink-file::read_only_path_records_failure_without_panic | Informational — spirit inherited (fail without panic), but not semantically anchored to any AC |

### otel-grpc-scoped sibling patterns

These BCs describe otel-grpc-scoped error-path behavior. S-4.05 wires DLQ
at the sink-core layer (sink-agnostic), so the sink-core-layer DLQ contracts
are v1.1 BC candidates; otel-grpc BCs are listed here for reference only.

| BC | Title | Relationship |
|---|---|---|
| BC-3.03.002 | otel-grpc::self-healing reconnect-on-error | otel-grpc-scoped sibling pattern (S-4.05's DLQ is the next-level fallback when retries exhausted) |

### file-sink-scoped sibling patterns

These BCs describe file-sink-scoped behaviors that DLQ inherits transitively
via shared crate code. Listed for reference only; the DLQ-specific
composition is captured in the v1.1 BC Candidates table.

| BC | Title | Relationship |
|---|---|---|
| BC-3.02.001 | sink-file::template_date_only ({date} → YYYY-MM-DD) | DLQ inherits daily rotation by reusing `sink-core::path_template::resolve_path_template` (extracted from sink-file in Task 0) |
| BC-3.02.007 | sink-file::auto_creates_parent_directory | AC-007 directory auto-create traces here (mkdir-p on first write; authoritative per BC-INDEX) |
| BC-3.02.015 | sink-file::shutdown_drains_queued_events | Informational — the upstream sink-file's drain reduces partial-write window for events flowing to DLQ via EC-007, but DLQ's own shutdown semantics are NOT contracted by this story (sink-file scoped; DLQ is sink-core scoped with separate `Mutex<Option<(PathBuf, File, u64)>>` cache field per Task 1; deferred — future story may promote a sink-core-scoped BC-3.NN.NNN-dlq-shutdown-drains-pending-writes if DLQ shutdown semantics are explicitly contracted) |

## Verification Properties

| VP ID | Title | Proof Method |
|-------|-------|-------------|
| VP-012 | Sink Failure Affects Only That Sink | unit-test |

> **VP-011 rejected:** VP-011 (Sink submit Must Not Block the Dispatcher) was considered
> but rejected for this story. DLQ writes occur on the worker thread AFTER `submit()` has
> already returned; VP-011's invariant (try_send on the dispatcher hot path must not block)
> is neither strengthened nor exercised by DLQ write behavior. VP-011 remains anchored to
> the stories that govern the hot-path submit mechanics.

## VP-INDEX Updates Required

State-manager must apply the following VP-INDEX anchor-back updates in the next
state-manager burst (per POLICY 9):

- **VP-012** → add `S-4.05` to Story Anchors column in VP-INDEX.md line for VP-012

> Note: VP-011 is explicitly NOT added to S-4.05 anchors (see VP-011 rejection note above).

## PRD FR-044 Updates Required (state-manager same-burst)

State-manager must update `/Users/jmagady/Dev/vsdd-factory/.factory/specs/prd.md` FR-044 PROSE NARRATIVE (lines 330-346, NOT a table) in same commit as story implementation:

- **"Source BCs (currently anchored):" line:** APPEND `BC-3.07.003, BC-3.07.004, BC-1.06.011` (3 newly-anchored BCs from S-4.05 wave). Note: BC-3.07.003 + BC-3.07.004 are PROMOTIONS from PRD's v1.1 candidate list (corresponding to "DLQ event shape" and "DLQ disk-full no-crash" entries). BC-1.06.011 is a NEW SS-01 BC, never previously in any v1.1 candidate list. (BC-3.02.015 is informational-only for this story — sink-file scoped, not load-bearing for any S-4.05 AC; see Pass-21 F-2101 adjudication.)

- **'Subsystem(s):' line:** ADD a new `**Subsystem(s):** SS-03, SS-01` line to FR-044 prose (parallel to FR-045's structure at PRD line 354). FR-044 currently lacks an explicit subsystem line — this addition matches the canonical FR prose pattern. (Alternative: insert subsystem ID inline in the description paragraph if FR table column structure is preferred.)

- **"v1.1 BC candidates pending: 8" line:** RECOMPUTE at commit time. The PRD line currently shows '8' but the parenthetical enumeration in PRD lists 4 (S-4.04) + 5 (S-4.05) = 9 items — this is a pre-existing PRD baseline arithmetic gap that S-4.05 does NOT take responsibility for fixing (tracked as TD-XXX consistency-validator sweep candidate). For this S-4.05 wave specifically: REMOVE 2 promoted entries (dlq-event-shape → BC-3.07.003; dlq-disk-full-no-crash → BC-3.07.004) AND ADD 4 new entries (sink-name-sanitization, dlq-config-toggle-and-size-cap, sink-name-collision-detection, internal-event-rfc3339-strict). State-manager must audit the actual PRD-stated count at commit time and apply (PRD-current - 2 + 4) = (PRD-current + 2). If PRD says '8' at commit time, new value = 10. If PRD has been independently corrected to '9', new value = 11. Note: After this S-4.05 wave applies (PRD-current + 2), the resulting count value (10 if PRD says 8; 11 if PRD already corrected to 9) may STILL diverge from the parenthetical enumeration if the upstream baseline gap is not also resolved. This residual gap is the SAME TD-XXX acknowledged above; S-4.05 does NOT close it. State-manager: log a separate TD entry if not already tracked.

- **"Status: partial" line:** No change unless rc.1 is pending; `partial` remains correct until S-4.07 + S-4.08 complete the per-sink resilience deliverable end-to-end.

Bundle with state-manager's BC creation commit per the BCs to Create section.

## BC-INDEX Updates Required (state-manager same-burst)

State-manager must update `/Users/jmagady/Dev/vsdd-factory/.factory/specs/behavioral-contracts/BC-INDEX.md` in same commit as BC file creation:

- **SS-03 row (line ~26):** count `51` → `53` (+2: BC-3.07.003, BC-3.07.004). Audit at commit time per defensive language.
- **SS-01 row (line ~24):** count `99` → `100` (+1: BC-1.06.011). Audit at commit time.
- **Summary Total (line ~34):** `1893` → `1896` (+3 net). Audit at commit time per defensive language; if other stories concurrently affected the catalog, increment from current value.
- **Per-BC index rows:** ADD index rows for BC-3.07.003, BC-3.07.004 (under SS-03 group) and BC-1.06.011 (under SS-01 group).

Bundle with the 3 new BC file creates per the BCs to Create section.

## BCs to Create

State-manager must create the following BC files in `.factory/specs/behavioral-contracts/`
after this story is committed. These BCs are allocated and referenced as load-bearing
anchors in this story's ACs.

### BC-3.07.003 — sink-dlq::internal_event_emission_on_dlq_write

**Subsystem:** SS-03 (Observability Sinks)
**Capability:** CAP-024
**Summary:** On each successful DLQ file write, the sink driver emits an
`internal.sink_dlq_write` event to the internal event channel.

**Canonical Test Vector table** (aligned with BC-3.07.002 sibling field set + DLQ-specific additions; `schema_version` is a per-event field populated by the `INTERNAL_EVENT_SCHEMA_VERSION` constant; every event emits the constant's current value (1 in v1.0). 9-field canonical TV including envelope-shared `schema_version`; matches BC-3.07.002 sibling structure):

| Field | Value | Notes |
|-------|-------|-------|
| `type` | `"internal.sink_dlq_write"` | Literal string; DLQ-specific addition over BC-3.07.002 base |
| `sink_name` | `"<sink-name>"` | Name of the failing sink (matches BC-3.07.002 field) |
| `sink_type` | `"<sink-type>"` | Sink driver type (e.g., `"http"`, `"file"`; matches BC-3.07.002 field) |
| `event_type` | `"<event-type>"` | Type of the dropped event; DLQ-specific addition |
| `reason` | `"retry_exhausted" \| "queue_overflow"` | Trigger reason; DLQ-specific addition |
| `ts` | string | ISO 8601 with offset (chrono `%Y-%m-%dT%H:%M:%S%z` form, e.g. `2026-04-28T12:00:00-0500`). Note: RFC 3339 strict-mode requires a colon in the offset (`-05:00`); the canonical envelope uses ISO 8601 basic-form offset for sibling parity with BC-3.07.002. Downstream consumers requiring strict RFC 3339 must apply colon-insertion. v1.1 candidate to migrate to RFC 3339 strict (`%:z`) tracked in v1.1 BC Candidates table. |
| `ts_epoch` | i64 seconds | Same instant as `ts` in Unix epoch seconds (populated via `ts.timestamp()` returning seconds per chrono; matches `InternalEvent.ts_epoch` envelope semantic; same as BC-3.07.002 sibling envelope) |
| `dispatcher_trace_id` | UUID string (optional envelope field; omitted from JSON when absent via `Option<String>` + `#[serde(skip_serializing_if = "Option::is_none")]`; populated via canonical envelope; matches BC-3.07.002 sibling pattern) | Optional — propagated from dispatched event when present (matches `InternalEvent.dispatcher_trace_id` semantics at `crates/factory-dispatcher/src/internal_log.rs:91-93`) |
| `schema_version` | u32 | Equals `INTERNAL_EVENT_SCHEMA_VERSION` = 1 per PRD §3.1; populated by canonical envelope; matches BC-3.07.002 sibling pattern |

**Envelope-optional fields footnote:** Additional envelope-optional fields supported by `InternalEvent` (`session_id`, `plugin_name`, `plugin_version` — all `Option<String>` with `skip_serializing_if = Option::is_none`) MAY appear in serialized DLQ events when populated by upstream emitters but are NOT required for DLQ contract conformance and are NOT asserted by AC-006a/006b. Test assertions verify only the 9 in-scope fields listed in the canonical TV table above.

> PC4: Every emitted `internal.sink_dlq_write` event MUST include the `schema_version` field with value `INTERNAL_EVENT_SCHEMA_VERSION` (currently 1; defined in `crates/factory-dispatcher/src/internal_log.rs`). This matches the canonical envelope semantic shared with BC-3.07.002 (`internal.sink_error`).

**Postconditions:**
1. Every DLQ disk write produces AT MOST one `internal.sink_dlq_write` event with all fields above populated; under channel back-pressure (`try_send` full), the observability event MAY be dropped, but the on-disk DLQ entry is the load-bearing durability path. The 'exactly one' invariant from earlier drafts is loosened to 'at most one' per F-3207 adjudication (try_send semantics).
2. The event is routed to the internal channel only — NOT through SinkRegistry (mirroring BC-3.07.002 invariant).
3. The `type` literal `internal.sink_dlq_write` is reserved in the SS-01 internal-event-type registry (`crates/factory-dispatcher/src/internal_log.rs` event-type constants). The constant `INTERNAL_SINK_DLQ_WRITE` MUST be added in the same commit that ships this BC's emitter.
4. Every emitted `internal.sink_dlq_write` event MUST include the `schema_version` field with value `INTERNAL_EVENT_SCHEMA_VERSION` (currently 1; defined in `crates/factory-dispatcher/src/internal_log.rs`). This matches the canonical envelope semantic shared with BC-3.07.002 (`internal.sink_error`).

### BC-3.07.004 — sink-dlq::dlq_write_failure_observable_via_internal_event

**Subsystem:** SS-03 (Observability Sinks)
**Capability:** CAP-024
**Summary:** When a DLQ file write itself fails (disk full, permission error, etc.), the
failure is emitted as `internal.sink_dlq_failure` to the internal channel, logged to stderr
as fallback, and the dispatcher continues without crashing.

**Postconditions:**
1. `internal.sink_dlq_failure` event emitted to internal channel (NOT SinkRegistry)
2. Error logged to stderr as fallback when the internal channel `try_send` returns Full (back-pressure drop per F-3203 fire-and-forget semantics) OR if the boundary task is not yet spawned during early dispatcher init. The stderr fallback is the load-bearing observability path for both back-pressure and pre-boundary-task failure modes.
3. Dispatcher continues processing; no panic
4. The `type` literal `internal.sink_dlq_failure` is reserved in the SS-01 internal-event-type registry (`crates/factory-dispatcher/src/internal_log.rs` event-type constants). The constant `INTERNAL_SINK_DLQ_FAILURE` MUST be added in the same commit that ships this BC's emitter.

**Canonical Test Vector (8-field internal.sink_dlq_failure event):**

| Field | Type | Source | Notes |
|-------|------|--------|-------|
| `type` | string | constant | Always `"internal.sink_dlq_failure"` (envelope) |
| `sink_name` | string | DlqWriter | Name of the sink whose DLQ write failed |
| `sink_type` | string | DlqWriter | Type discriminator (e.g., `"file"`, `"http"`) |
| `error` | string | std::io::Error | `format!("{}", err)` of the captured filesystem error |
| `ts` | string | clock_fn | RFC3339 ISO-8601 timestamp |
| `ts_epoch` | i64 | with_ts | Seconds since UNIX epoch (envelope; populated by `InternalEvent::with_ts`) |
| `dispatcher_trace_id` | Option\<string\> | upstream event | Optional; propagated from dispatched event when present; `skip_serializing_if = Option::is_none` |
| `schema_version` | u32 | constant | Envelope-shared `INTERNAL_EVENT_SCHEMA_VERSION` |

Field count: 8 fields per canonical TV (one fewer than BC-3.07.003's 9-field TV — BC-3.07.004 omits the dropped event's `event_type` and `reason` because the failure path doesn't necessarily have an `event` payload context; the failure could be on the FIRST write attempt before the event is even serialized).

**Why this is a BC and not just EC-002:** EC-002 previously said "log to stderr; do not crash".
That is lossy under hooks-driven invocation (AGENT-SOUL.md #4: no silent failures). Promoting
to a BC ensures the failure is observable in fan-out, not just in terminal output.

### BC-1.06.011 — prune_old::extends_to_dlq_files

**Subsystem:** SS-01 (Hook Dispatcher Core)
**Capability:** CAP-024 (DLQ lifecycle) / CAP-006 (log retention)
**Summary:** `prune_old` (BC-1.06.004) is extended to cover files in the `dlq/` directory matching the `dead-letter-*.jsonl` prefix-scope (per AC-009 and PC2; intentionally excludes other unrelated `.jsonl` files that may appear in dlq/ in v1.1+)
with an independent 7-day default retention (`INTERNAL_DLQ_DEFAULT_RETENTION_DAYS = 7`), distinct from the 30-day default applied to dispatcher logs (`DEFAULT_RETENTION_DAYS = 30`).

**Postconditions:**
1. `prune_old` deletes DLQ files older than `dlq_retention_days` (distinct from `retention_days` for dispatcher logs which defaults to 30). When `dlq_retention_days` is unset in config, the default `INTERNAL_DLQ_DEFAULT_RETENTION_DAYS` (= 7, defined in `crates/factory-dispatcher/src/internal_log.rs`) is used (NOT a fallback to `retention_days`).
2. DLQ files matching `dead-letter-<sink-name>-{date}[-<seq>].jsonl` (where `{date}` resolves to YYYY-MM-DD per BC-3.02.001 and `<seq>` is an optional 3-digit suffix from size-cap rotation) are included in the prune scan.
3. Retention for DLQ files is controlled by `dlq_retention_days` in `[prune]` section, with default `INTERNAL_DLQ_DEFAULT_RETENTION_DAYS` (= 7). Dispatcher log retention (`retention_days`, default `DEFAULT_RETENTION_DAYS` = 30) is independent.

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| sink-core/src/path_template.rs | crates/sink-core/ | pure-core (path interpolation, no I/O) |
| sink-core/src/dead_letter.rs | crates/sink-core/ | effectful-shell (filesystem append, internal event emit via SinkDlqEvent channel) |
| sink-core/src/events.rs (extend) | crates/sink-core/ | pure-core (adds `SinkDlqEvent`, `SinkDlqWriteEvent`, `SinkDlqFailureEvent` types mirroring SinkErrorEvent precedent; no I/O) |
| sink-http/src/lib.rs | crates/sink-http/ | effectful-shell (DLQ wiring on retry exhaustion) |
| sink-http/tests/integration.rs | crates/sink-http/ | test (mock 5xx → DLQ verification) |
| crates/factory-dispatcher/src/lib.rs (modify) | crates/factory-dispatcher/ | effectful-shell (spawn site for DLQ boundary adapter task; creates shared (dlq_event_tx, dlq_event_rx) channel at init; drops original tx at shutdown to terminate the boundary task) |
| crates/factory-dispatcher/src/internal_log.rs (modify) | crates/factory-dispatcher/ | effectful-shell (conversion logic: `dispatch_to_internal_event(event: SinkDlqEvent, writer: &InternalLog)` — synchronous, shared reference; matches `InternalLog::write(&self, event: &InternalEvent)` at internal_log.rs:207; converts SinkDlqEvent variants to InternalEvent envelopes; called from boundary adapter task loop; new INTERNAL_SINK_DLQ_WRITE / INTERNAL_SINK_DLQ_FAILURE / INTERNAL_DLQ_DEFAULT_RETENTION_DAYS constants; extend prune_old via scan_files_with_prefix tuples for dlq/ directory) |
| crates/factory-dispatcher/src/sinks/mod.rs (modify) | crates/factory-dispatcher/ | effectful-shell (extend ObservabilityConfig with [prune] section + SinkStanza with dlq_enabled toggle and sink_name validation per Tasks 6/7/10) |

**Dependency direction (no cycles):**
```
sink-file  -->  sink-core::path_template  (sink-file imports; path_template is pure-core)
sink-core  -->  sink-core::path_template  (dead_letter.rs imports; same crate, no cross-crate cycle)
sink-http  -->  sink-core                 (sink-http depends on sink-core; existing direction)
```

Task 0 (path_template extraction) resolves the circular dependency risk identified in F-002:
sink-core's `dead_letter.rs` no longer needs to import from `sink-file`. The extraction
moves the pure interpolation logic DOWN into sink-core where it can be used by both sink-file
and sink-core itself.

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | DLQ directory missing | Auto-create via mkdir-p (traces to BC-3.02.007) |
| EC-002 | DLQ disk full or permission error on DLQ write | Emit `internal.sink_dlq_failure` event to internal channel (NOT SinkRegistry); log to stderr as fallback; continue without crash (contracted by BC-3.07.004) |
| EC-003 | Path traversal in `sink_name` (e.g., `sink_name="../etc/passwd"` or `sink_name="a/b"`) | Sink fails to start with a `SinkConfigError::InvalidSinkName` error; sanitization rejects any `sink_name` containing path separator characters (`/`, `\`) or relative-path tokens (`..`); DLQ file is never created |
| EC-004 | Two sinks configured with identical `sink_name` | Second sink fails to start with `SinkConfigError::NameCollision`; each sink must have a globally unique name; DLQ files are not shared between sinks |
| EC-005 | Concurrent DLQ writes from worker thread | Single-writer-per-DlqWriter via `Mutex<Option<(PathBuf, File, u64)>>` cache field on the DlqWriter struct (PathBuf, File, and byte-count u64 held under one lock; atomically rotates path + handle pair during cross-day or size-cap transitions); lock held continuously across clock eval + rotate + write_all + flush — workers acquiring the lock serialize writes; no interleaved JSONL lines |
| EC-006 | Recursive DLQ feedback — `internal.sink_dlq_write` event itself fails and would be routed through a failing sink | `internal.sink_dlq_write` events are routed to the internal channel ONLY, never through SinkRegistry (mirroring BC-3.07.002 invariant); no recursive DLQ write is possible |
| EC-007 | Process crash mid-JSONL-line (partial write) | On next start, partial lines at end of DLQ file are tolerated by replay tools (documented contract); BC-3.02.015 shutdown drain reduces (but does not eliminate) the partial-write window; no data corruption of prior complete lines |

## Configuration

### DLQ Configuration in `observability-config.toml`

```toml
[[sinks]]
name = "my-http-sink"
type = "http"
# DLQ is on-by-default. Set to false to disable for this sink.
dlq_enabled = true  # default = true (hard-coded for v1.0; promoted to INTERNAL_DLQ_DEFAULT_ENABLED constant if future config requires runtime introspection)

[[sinks]]
name = "my-otel-sink"
type = "otel-grpc"
# DLQ explicitly disabled for this sink
dlq_enabled = false

# DLQ retention is independent of dispatcher-log retention_days (default DEFAULT_RETENTION_DAYS = 30; defined in crates/factory-dispatcher/src/internal_log.rs).
[prune]
dlq_retention_days = 7  # default; defined as INTERNAL_DLQ_DEFAULT_RETENTION_DAYS in internal_log.rs
```

When `dlq_enabled = false` for a sink, retry-exhaustion events for that sink are logged
to stderr and dropped — they are NOT written to any DLQ file. The `internal.sink_dlq_write`
event is also suppressed for that sink.

**Note on `[prune]` section parser:**
The `[prune]` block in `observability-config.toml` is parsed by extending `ObservabilityConfig`
in `crates/factory-dispatcher/src/sinks/mod.rs` with a new `prune: Option<PruneConfig>` field,
where `PruneConfig` is a new struct with field `dlq_retention_days: Option<u32>`. If `[prune]`
is absent or `dlq_retention_days` is unset, the default `INTERNAL_DLQ_DEFAULT_RETENTION_DAYS = 7`
is used (per BC-1.06.011 PC1 + Task 5b). Validation: `dlq_retention_days >= 1` (zero or negative
values are rejected with `SinkConfigError::InvalidPruneConfig`).

> **Size cap:** The 100 MB per-file size cap is a hard-coded constant in v1.0 (not exposed as a TOML knob). Configurability is part of the v1.1 BC candidate `BC-3.NN.NNN-dlq-config-toggle-and-size-cap`.

**Default size cap:** 100 MB per DLQ file. When a file reaches 100 MB before midnight UTC,
a new rotation is triggered immediately (file closed, new `dead-letter-<sink-name>-{date}-<seq>.jsonl`
opened where `<seq>` is a 3-digit incrementing counter starting at 001).

## Subsystem Scope Note

CAP-024 maps to SS-01 + SS-03 + SS-10 per capabilities.md §CAP-024. This story scopes to:
- SS-03 (Observability Sinks) — primary: BC-3.07.003 + BC-3.07.004 sink-dlq events; `sink-core/src/dead_letter.rs`
- SS-01 (Hook Dispatcher Core) — secondary: BC-1.06.011 prune_old extension; `crates/factory-dispatcher/src/internal_log.rs`

SS-10 (CLI Tools and Bin) is intentionally OUT OF SCOPE for v1.0. No bin tool or commands/ surface is touched in this story. If a bin/ tool is needed for DLQ inspection or replay, that work is deferred to v1.1 as a follow-up story.

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| sink-core/src/path_template.rs | pure-core | No I/O; pure string interpolation |
| sink-core/src/dead_letter.rs | effectful-shell | Writes to filesystem; emits internal events |
| sink-http/src/lib.rs | effectful-shell | Existing effectful module; DLQ wiring is additional effectful path |
| internal_log.rs (modified by Tasks 5b/7) | effectful-shell | Filesystem prune_old extension; constant declarations are static — no runtime I/O for the constants themselves, but the file is part of the effectful-shell module |

## v1.1 BC Candidates

| Candidate ID (proposed) | Description | Source |
|---|---|---|
| BC-3.NN.NNN-dlq-write-on-retry-exhaustion | When a sink's RetryPolicy exhausts all retries for a batch, each event in the batch is written to the DLQ file before being discarded from memory | Pre-emptive (Wave 2 AC scan — no BC contracts the DLQ write trigger; BC-3.01.008 contracts in-memory failure recording only) |
| BC-3.NN.NNN-dlq-write-on-queue-overflow | When `submit()` returns `try_send` overflow (queue full), the event is written to the DLQ instead of being silently dropped | Pre-emptive (Wave 2 AC scan — queue overflow path writes to DLQ is a new behavior not contracted in BC-3.01.007 or BC-3.01.008) |
| BC-3.NN.NNN-dlq-daily-rotation | DLQ daily rotation as the composition of sink-core path-template (BC-3.02.001) + DLQ-specific naming convention `dead-letter-<sink-name>-{date}.jsonl` | F-501 (pass-5) |
| BC-3.NN.NNN-sink-name-sanitization | sink_name must not contain path separators or relative-path tokens; config validation rejects at startup | F-006 EC-003 (this wave) |
| BC-3.NN.NNN-dlq-config-toggle-and-size-cap | DLQ on-by-default for all sinks; per-sink `dlq_enabled` toggle on the `[[sinks]]` stanza in observability-config.toml; size cap (100 MB default); rotation timing (midnight UTC); stderr-on-disabled drop. v1.1 candidate per F-305 (pass-3); confirmed schema alignment with `[[sinks]]` array-of-tables per F-1101 (pass-11). | F-305 (pass-3) — AC-008 uncontracted (added this wave) |
| BC-3.NN.NNN-sink-name-collision-detection | When two sinks are configured with identical `sink_name`, the second fails to start with `SinkConfigError::NameCollision`; each sink has a globally unique name | F-307 (pass-3) — EC-004 untraced (added this wave) |
| BC-3.NN.NNN-internal-event-rfc3339-strict | Migrate `InternalEvent.ts` from chrono `%z` (no colon) to `%:z` (colon-included) for strict RFC 3339 compliance across all internal.* event types. Requires update to BC-3.07.002 + BC-3.07.003 + BC-3.07.004 TV rows. Source: F-O1401 (pass-14) | O-1401 (pass-14) — chrono `%z` emits `-0500`; strict RFC 3339 requires `-05:00`; migration deferred to v1.1 |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~1,400 |
| sink-core/src/path_template.rs | ~300 |
| sink-core/src/dead_letter.rs | ~700 |
| sink-http/src/lib.rs (DLQ wiring) | ~400 |
| Integration test (sink-http/tests/) | ~800 |
| prune_old extension | ~300 |
| Tool outputs overhead | ~400 |
| **Total** | **~4,300** |
| Agent context window | 200K for Sonnet |
| **Budget usage** | **~2.2%** |

## Tasks (MANDATORY)

- [ ] **Task 0 (path_template extraction — prerequisite for all other tasks):**
  Extract `resolve_path_template` (and any pure helpers it depends on) from
  `crates/sink-file/src/lib.rs` into a new `crates/sink-core/src/path_template.rs` module.
  **Scope: PURE string interpolation only** — `{date}`, `{name}`, etc. NO filesystem I/O.
  NO mkdir-p (mkdir-p stays inline at each caller's effectful-shell layer: sink-file::append_jsonl
  keeps its existing `fs::create_dir_all` call; DLQ's `dead_letter.rs` adds its own
  `fs::create_dir_all(parent)?;` call inline — BC-3.02.007 inheritance is by-replication,
  not code-path reuse).
  - Update sink-file to import `resolve_path_template` from `sink-core::path_template`; remove the duplicated function body.
  - Verify zero behavioral change via existing sink-file unit tests (all must pass unchanged).
  - The extracted signature MUST be generic over timezone and take `DateTime<Tz>` by value: `pub fn resolve_path_template<Tz: TimeZone>(template: &str, date: DateTime<Tz>, name: &str, project: Option<&str>) -> Result<PathBuf, sink_core::PathTemplateError>`. (Verify against actual sink-file source signature during extraction; the listed parameters match Task 1 line 464's call site `resolve_path_template(&self.config.template, (self.clock_fn)(), sink_name, self.config.project.as_deref())`.) Note: by-value `DateTime<Tz>` matches sink-file's existing `DateTime<Local>` calling convention (both `Local` and `Utc` are `Copy`). All existing sink-file caller sites continue to compile unchanged — type inference resolves `DateTime<Local>` to the generic bound with no `&` mismatch.
  - **Signature mirrors existing sink-file shape exactly.** The signature MIRRORS sink-file's existing `pub fn resolve_path_template(template: &str, date: DateTime<Local>, name: &str, project: Option<&str>) -> Result<PathBuf, FileSinkError>` shape exactly except for (a) generic `Tz: TimeZone` bound (`Local` was concrete) and (b) error type (`FileSinkError` → `PathTemplateError`). All existing sink-file caller sites compile unchanged after the import switch. The `{name}` placeholder in templates maps to the `name: &str` parameter, preserving backward compatibility with user TOML configs that use `{name}` placeholder syntax.
  - Define a new error type in sink-core: `pub enum PathTemplateError { UnknownPlaceholder { placeholder: String }, /* additional variants as discovered during extraction */ }` — struct-variant form matches sink-file's existing `FileSinkError::UnknownPlaceholder { placeholder: String }`. The `From<PathTemplateError> for FileSinkError` impl is a structural rename: each variant name + field set maps 1:1. sink-file's existing `FileSinkError::UnknownPlaceholder` arm becomes a `From<PathTemplateError>` mapping at the sink-file caller site (no behavior change for sink-file consumers). The full variant set will be enumerated when Task 0 executes the extraction; for the From conversion to FileSinkError, every variant must have a 1:1 named-field counterpart in FileSinkError so the From impl is a mechanical rename.
  - Variant shape choice: PathTemplateError variants MUST use struct-variant form (named fields) to mirror FileSinkError exactly; this allows the From impl to be a mechanical rename without field-shape adjustment.
  - Files modified: `crates/sink-core/src/path_template.rs` (create — pure string interpolation only, no I/O), `crates/sink-core/src/lib.rs` (expose module), `crates/sink-file/src/lib.rs` (replace function body with import from sink-core)

- [ ] **Task 1:** Create `crates/sink-core/src/dead_letter.rs`
  - `DlqWriter` struct with `std::sync::Mutex<Option<(PathBuf, File, u64)>>` cache field per DlqWriter instance (PathBuf, File, and u64 byte-count held under one lock to atomically rotate during cross-day or size-cap transitions; the u64 is the cumulative byte count of the currently-open file, initialized via `f.metadata()?.len()` on file open to handle process-restart mid-day correctly; `std::sync::Mutex` matches sink-file convention at `sink-file/src/lib.rs:43,216`; tests handle `LockResult` via `unwrap()` — the lock contention window is tiny, so poisoning is unlikely)

    **Cache shape:** `Mutex<Option<(PathBuf, File, u64)>>` where the third `u64` element is the cumulative byte count of the currently-open file. Initialize on file open via `f.metadata()?.len()` (handles process-restart mid-day correctly when the file already exists). Increment by `line.len() + 1` (newline) per write within the lock guard. On size-cap exceedance, advance the seq suffix and rotate (close current File, open new `(path, fresh File, 0u64)` tuple).
  - `write_event(&self, sink_name: &str, sink_type: &str, event: &SinkEvent, reason: DlqReason) -> Result<(), DlqError>` method using `resolve_path_template` from `sink-core::path_template`, where:
    - `sink_type` parameter is the sink's static type discriminator. Existing drivers emit one of: `"file"` (sink-file), `"http"` (sink-http; ALSO covers Datadog/Honeycomb which wrap sink-http and emit `"http"`), `"otel-grpc"` (sink-otel-grpc). Matches SinkErrorEvent precedent at sink-core/events.rs:34.
    - `enum DlqReason { RetryExhausted, QueueOverflow }` with an explicit `as_str()` method:
      ```rust
      impl DlqReason {
          pub fn as_str(&self) -> &'static str {
              match self {
                  Self::RetryExhausted => "retry_exhausted",
                  Self::QueueOverflow => "queue_overflow",
              }
          }
      }
      ```
      `DlqReason::as_str()` returns the snake_case literal that matches BC-3.07.003 TV's `reason` discriminator. The boundary adapter (Task 5b) MUST use `e.reason.as_str()` (NOT `to_string()` which would emit the PascalCase variant name). (matches BC-3.07.003 TV `reason` discriminator)
    - `enum DlqError { MkdirFailed(std::io::Error), OpenFailed(std::io::Error), WriteFailed(std::io::Error) }` (3 filesystem-error variants only; `Serialization` is intentionally absent — `SinkEvent` is `serde_json::Map<String, Value>` and `serde_json::to_string` of a `Map` is infallible barring OOM which would abort the process; every `DlqError` variant maps to a filesystem failure that triggers `internal.sink_dlq_failure` emission per F-2706 Option (c) scope)
    - `SinkEvent` is the existing event envelope type from sink-core
  - Filename pattern: `dead-letter-<sink-name>-{date}.jsonl` (BC-3.02.001-aligned form per Capability Anchor Justification — extends CAP-024's outcome description into the canonical implementation form using the `{date}` template placeholder; note: in template strings this is expressed as `dead-letter-{name}-{date}.jsonl` using the `{name}` placeholder matching sink-file's existing placeholder vocabulary)
  - DlqWriter performs its own `fs::create_dir_all(parent)?;` inline before opening the DLQ file (same idiom as sink-file::append_jsonl). BC-3.02.007 inheritance is by-replication, not by-code-path-reuse.
  - **DlqWriter constructor configuration:** Define `pub struct DlqWriterConfig { pub template: String, pub size_cap_bytes: u64, pub project: Option<String> }` in `crates/sink-core/src/dead_letter.rs`. Fields:
    - `template`: filename template (default `"dead-letter-{name}-{date}.jsonl"`); BC-3.02.001-aligned form per Capability Anchor Justification. Uses `{name}` placeholder (matching sink-file's existing placeholder vocabulary) for backward compatibility with user TOML configs.
    - `size_cap_bytes`: per-file size limit triggering seq-based rotation within a day (default 100 * 1024 * 1024 = 100 MiB per AC-004).
    - `project`: project basename string passed through to `resolve_path_template`'s `project` parameter; defaults to `std::env::var("CLAUDE_PROJECT_DIR").ok()` at construction time per existing sink-file convention.

    Note: pruning is NOT a DlqWriter concern — `factory-dispatcher::internal_log::prune_old` reads `PruneConfig.dlq_retention_days` directly per Task 7. `DlqWriterConfig` carries only fields DlqWriter actively reads.

    Canonical constructor: `pub fn new(config: DlqWriterConfig, internal_tx: tokio::sync::mpsc::Sender<SinkDlqEvent>) -> Self` (clock_fn defaults to `Arc::new(|| Utc::now()) as Arc<dyn Fn() -> DateTime<Utc> + Send + Sync>` — closure literal required because `Utc::now` is a fn-item type that does NOT auto-coerce to `Arc<dyn Fn(...) + Send + Sync>`; the closure wrap forces the coercion per F-3105). Test constructor: `pub fn with_clock_fn(config: DlqWriterConfig, internal_tx: tokio::sync::mpsc::Sender<SinkDlqEvent>, clock_fn: Arc<dyn Fn() -> DateTime<Utc> + Send + Sync>) -> Self`.

  - **sink-core event types (mirror SinkErrorEvent precedent at sink-core/src/events.rs):** Define in `crates/sink-core/src/events.rs` (or a new `dlq_events.rs` module if the file is large):
    - `pub enum SinkDlqEvent { Write(SinkDlqWriteEvent), Failure(SinkDlqFailureEvent) }`
    - `pub struct SinkDlqWriteEvent { pub sink_name: String, pub sink_type: String, pub event_type: String, pub reason: DlqReason, pub ts: DateTime<Utc>, pub dispatcher_trace_id: Option<String> }` — fields match BC-3.07.003 TV (excluding the canonical envelope fields `type` and `schema_version`, which are added at the dispatcher boundary).
    - `pub struct SinkDlqFailureEvent { pub sink_name: String, pub sink_type: String, pub error: String, pub ts: DateTime<Utc>, pub dispatcher_trace_id: Option<String> }` — fields match BC-3.07.004 TV similarly.

    Conversion to `InternalEvent` happens at the factory-dispatcher boundary adapter (Task 5b — see below). This design ensures sink-core has NO import of factory-dispatcher types, preserving the existing dependency direction (`factory-dispatcher → sink-core`).
  - **Clock injection seam:** `DlqWriter` holds a field `clock_fn: Arc<dyn Fn() -> DateTime<Utc> + Send + Sync>`, defaulted to `Arc::new(|| Utc::now()) as Arc<dyn Fn() -> DateTime<Utc> + Send + Sync>` in the canonical constructor (closure literal is required because `Utc::now` is a fn-item type that does NOT auto-coerce to `Arc<dyn Fn(...) + Send + Sync>` — the closure wrap forces the coercion). A test-only constructor `DlqWriter::with_clock_fn(config: DlqWriterConfig, internal_tx: tokio::sync::mpsc::Sender<SinkDlqEvent>, clock_fn: Arc<dyn Fn() -> DateTime<Utc> + Send + Sync>)` accepts an arbitrary `clock_fn` so Task 8b's rotation test can advance simulated time across UTC midnight without OS-clock manipulation. Inside `write_event`, replace `Utc::now()` with `(self.clock_fn)()`.
  - **Test-only path accessor:** DlqWriter exposes `pub(crate) fn current_path(&self) -> Option<PathBuf>` returning a clone of the currently-cached PathBuf. Use `pub(crate)` (NOT `#[cfg(test)]`): `pub(crate)` keeps the accessor available for in-crate test modules and any future internal metric collector, while preventing external production callers from depending on it (which would invite contention). The accessor `.lock()`s the cache Mutex briefly, clones the cached PathBuf if present, and returns the owned value (no lifetime tied to the lock). The accessor is observable-state-only; production callers do not rely on it.
  - **DlqWriter rotation semantics:** on every `write_event` invocation, the writer (1) computes the target path via `resolve_path_template(&self.config.template, (self.clock_fn)(), sink_name, self.config.project.as_deref())`; (2) compares the resolved path to the cached file path; (3) if the path changed (UTC date crossed OR seq advanced via size cap), closes the prior `File` handle and opens a new one for the new path before writing. The cache is `Mutex<Option<(PathBuf, File, u64)>>`. This ensures AC-004's midnight UTC rotation is exercised on every cross-midnight write event — not just that `resolve_path_template` returns different paths, but that the open file handle is actually closed and reopened.

  - **Lock scope (EC-005 invariant):** The `Mutex<Option<(PathBuf, File, u64)>>` guard is held CONTINUOUSLY across (1) `(self.clock_fn)()` evaluation, (2) path comparison + rotation if needed, (3) `f.write_all(line.as_bytes())`, AND (4) the subsequent flush. The guard is released ONLY after the line is fully written and flushed. This guarantees EC-005's "no interleaved JSONL lines" — releasing the guard before write would let two workers each obtain `&mut File` sequentially but write concurrently using OS-level append, subject to PIPE_BUF interleave on >4096-byte lines (matches the existing internal_log.rs concurrency note).
  - Files created: `crates/sink-core/src/dead_letter.rs`

- [ ] **Task 2:** Implement DLQ daily rotation
  - DLQ rotation uses `(self.clock_fn)()` (which defaults to `Utc::now()` in production; NOT `Local::now()`) and passes the resulting `DateTime<Utc>` to `resolve_path_template`. AC-004's "midnight UTC" requirement is thus satisfied by-construction. The clock seam (Task 1 F-2701 fix) allows tests in Task 8b.3 to drive this path without OS-clock manipulation.
  - `{date}` resolves to UTC `YYYY-MM-DD` at write time (distinguishing DLQ from sink-file's `DateTime<Local>` resolution; the generic signature of `resolve_path_template<Tz: TimeZone>` accommodates both)
  - 100 MB size cap with sequential suffix (`-001`, `-002`, ...) on overflow
  - Files modified: `crates/sink-core/src/dead_letter.rs`

- [ ] **Task 3 — Wire DLQ on queue-overflow at each driver's `Sink::submit` try_send failure path.**

  Each sink driver's `submit` method calls `try_send` on its bounded internal queue (sink-http, sink-file, sink-otel-grpc all use bounded mpsc per their respective lib.rs files). On `try_send` returning `Err(TrySendError::Full)`, the submit path MUST also call:

  ```rust
  self.dlq_writer.write_event(sink_name, sink_type, event, DlqReason::QueueOverflow);
  ```

  where `sink_type` is the driver's static type literal:
  - sink-http passes `"http"` (covers Datadog/Honeycomb which wrap sink-http)
  - sink-file passes `"file"`
  - sink-otel-grpc passes `"otel-grpc"`

  Note: Retry-exhaustion wiring is OWNED BY Task 4 (sink-http only — the only driver with retries today). Task 3 covers ONLY queue-overflow which applies to all bounded-queue drivers.

  Files modified:
  - `crates/sink-http/src/lib.rs` (submit overflow path)
  - `crates/sink-file/src/lib.rs` (submit overflow path)
  - `crates/sink-otel-grpc/src/lib.rs` (submit overflow path)

- [ ] **Task 4:** Wire DLQ into sink-http retry-exhaustion error path
  - `sink-http` calls `DlqWriter::write_event` on retry exhaustion (HTTP-specific path)
  - Call: `dlq_writer.write_event(sink_name, "http", event, DlqReason::RetryExhausted)` — sink-http passes its own static `sink_type` string `"http"`.
  - Files modified: `crates/sink-http/src/lib.rs`

- [ ] **Task 5:** Emit DLQ events from sink-core (BC-3.07.003, BC-3.07.004)

  - **DLQ event emission (sink-core side):** On ANY filesystem error emerging from `DlqWriter::write_event` — including `fs::create_dir_all` failure (mkdir-p), `OpenOptions::open` failure, AND `f.write_all` / flush failure — construct `SinkDlqFailureEvent` and send via `self.internal_tx.try_send(SinkDlqEvent::Failure(failure_event))`, then log to stderr as fallback. F-2706 adjudication: Option (c) — all-filesystem-error scope (NOT just write-after-open). The `now` value is the SAME `DateTime<Utc>` returned by `(self.clock_fn)()` for the path-resolution call within the same `write_event` invocation, guaranteeing the event `ts` and DLQ filename `{date}` reflect the same UTC moment.

  - **Lock-release ordering for BOTH success and failure paths (F-3303 + F-3401 unified):** The MutexGuard is held across (clock + rotate + write_all + flush) per F-3103. After the guard is released, route to either success-emission or failure-emission OUTSIDE the critical section. Use a tuple-capturing inner block to propagate the clock value alongside the disk-write result:

    ```rust
    // `sink_type: &str` parameter is supplied by the caller (sink driver's retry layer)
    let (now, result): (DateTime<Utc>, Result<(), std::io::Error>) = {
        let mut guard = self.cache.lock().unwrap();
        let now = (self.clock_fn)();
        // 1. resolve target path via resolve_path_template(template, now, name, project)
        // 2. compare to cached path; rotate if needed (close old File; open new (path, fresh File, 0u64))
        // 3. f.write_all(line.as_bytes())?  // disk write inside guard
        // 4. f.flush()?
        // 5. update byte counter in cache: cached.2 += line.len() as u64 + 1
        (now, Ok(()))  // or (now, Err(io_err)) on any disk failure
    }; // guard dropped here — `now` and `result` are now accessible outside the critical section

    match result {
        Ok(()) => {
            let write_event = SinkDlqWriteEvent {
                sink_name: sink_name.to_string(),
                sink_type: sink_type.to_string(),
                event_type: event.event_type().unwrap_or("<unknown>").to_string(),
                reason,
                ts: now,
                dispatcher_trace_id: event.fields.get("dispatcher_trace_id").and_then(serde_json::Value::as_str).map(str::to_owned),
            };
            let _ = self.internal_tx.try_send(SinkDlqEvent::Write(write_event));
        }
        Err(err) => {
            let failure_event = SinkDlqFailureEvent {
                sink_name: sink_name.to_string(),
                sink_type: sink_type.to_string(),
                error: format!("{}", err),
                ts: now,
                dispatcher_trace_id: event.fields.get("dispatcher_trace_id").and_then(serde_json::Value::as_str).map(str::to_owned),
            };
            let _ = self.internal_tx.try_send(SinkDlqEvent::Failure(failure_event));
            // stderr fallback per BC-3.07.004 PC2 / F-2706 emission scope:
            eprintln!("DLQ write failed for sink={}: {}", sink_name, err);
        }
    }
    ```

    Both success-emission and failure-emission happen AFTER the guard is dropped — minimal critical section per F-3303 rationale (avoid extending lock across `try_send`; avoid deadlock if boundary task ever needed sink locks).

  - **event_type source:** `SinkEvent::event_type()` returns `Option<&str>` (sink-core/lib.rs:82-84). When None (producer bug per the lib.rs note — drivers can't route what they can't classify), default to `"<unknown>"` matching SinkErrorEvent::new convention at sink-core/events.rs:55-63 (`'<unnamed>'` for missing sink_name).

  - **SinkEvent does not expose a `dispatcher_trace_id()` accessor** — use the fields-map lookup pattern: `event.fields.get("dispatcher_trace_id").and_then(serde_json::Value::as_str).map(str::to_owned)`. Future cleanup may extend SinkEvent with the accessor (sink-core API change), but this story does NOT modify sink-core's public surface beyond adding SinkDlqEvent / DlqWriter types.

  - **Channel send semantics: use `try_send` (NOT `.send().await`).** Rationale: (1) matches SinkErrorEvent precedent at sink-core/events.rs:85-91 — fire-and-forget per BC-3.07.002 PC3; (2) DlqWriter::write_event runs in sync context (Sink::submit is sync per sink-core/lib.rs:209); `.send().await` would panic at runtime if no tokio runtime is active in the calling thread; (3) under burst load, `.send().await` would stall DLQ disk writes on consumer back-pressure. With `try_send`, channel-full results in dropped observability event, but disk write succeeds — disk is the load-bearing durability path.

  - **Disk-vs-channel atomicity:** Disk write is the load-bearing durability path; SinkDlqEvent channel emission is best-effort observability. A successful disk write followed by a failed/full channel send results in a DLQ entry that exists on disk but generates no `internal.sink_dlq_write` event — consistent with BC-3.07.002 fire-and-forget semantics. Operators relying on the DLQ for replay tooling should use the on-disk JSONL files as the canonical record; the internal channel is for live observability only.

  - **sink-core has NO InternalEvent import.** DlqWriter emits `SinkDlqEvent` (defined in `sink-core::events`) only. Conversion to `InternalEvent` is the sole responsibility of the boundary adapter (Task 5b below). The dependency direction `factory-dispatcher → sink-core` is preserved — sink-core has zero project-internal deps.

  - Files modified: `crates/sink-core/src/dead_letter.rs`, `crates/sink-core/src/events.rs`

- [ ] **Task 5b:** DLQ event boundary adapter + constants (factory-dispatcher side). This task must be committed in the same commit that ships the BC-3.07.003 and BC-3.07.004 emitters.

  - **Channel architecture (multiplexed):** factory-dispatcher creates ONE shared `(dlq_event_tx, dlq_event_rx) = mpsc::channel::<SinkDlqEvent>(1024)` at dispatcher initialization (in `crates/factory-dispatcher/src/lib.rs` or wherever the dispatcher main task spawns). All per-sink `DlqWriter` instances receive a `dlq_event_tx.clone()` at construction time. The boundary adapter task is spawned ONCE via:
    ```rust
    tokio::spawn(async move {
        while let Some(event) = dlq_event_rx.recv().await {
            dispatch_to_internal_event(event, &internal_log_writer);
        }
    });
    ```
    The `internal_log_writer` is a `Clone` of the dispatcher's shared `InternalLog` (per `InternalLog: Clone` impl at internal_log.rs:193); `move` captures it into the spawned task; per-iteration calls pass `&internal_log_writer` (shared ref). Only `rx.recv().await` is async — the conversion fn is synchronous because `InternalLog::write` is sync. The task drains `dlq_event_rx` until all senders are dropped (dispatcher shutdown), at which point `recv()` returns `None` and the loop exits cleanly.
  - **Spawn location:** factory-dispatcher's main initialization function (`crates/factory-dispatcher/src/lib.rs`); the boundary adapter is independent of any specific sink — it is a process-level service spawned alongside the existing dispatcher subsystems.
  - **Shutdown:** When the dispatcher tears down, ALL `dlq_event_tx` Senders MUST be dropped for the boundary task's `rx.recv()` to return `None`. This requires:
    1. Each sink's `dlq_event_tx.clone()` is dropped as the sink is deconstructed (per-sink cleanup).
    2. The ORIGINAL `dlq_event_tx` returned from `mpsc::channel(...)` at the init site MUST also be dropped — either by (a) consuming it immediately into the SinkRegistry registration (no retained handle at init scope), OR (b) explicitly calling `drop(dlq_event_tx)` in the dispatcher shutdown sequence AFTER all sinks are torn down.

    Without (1) AND (2), the boundary task's `recv().await` never returns None and the task hangs at shutdown — a silent process-leak failure mode forbidden by AGENT-SOUL.md #4.

    The boundary task itself drains remaining queued events from rx (best-effort durability mirroring sink-file shutdown drain), then exits when rx returns None. Queued events at shutdown are converted to InternalEvents and forwarded to internal_log_writer.

  **Boundary adapter (factory-dispatcher):** factory-dispatcher owns a task that receives `SinkDlqEvent` from the `dlq_event_rx` channel (paired with the `dlq_event_tx` passed to `DlqWriter::new`) and converts each variant into the canonical `InternalEvent` envelope:
  - `SinkDlqEvent::Write(e)` →
    ```rust
    let mut ev = InternalEvent::with_ts(INTERNAL_SINK_DLQ_WRITE, e.ts)
        .with_field("sink_name", e.sink_name)
        .with_field("sink_type", e.sink_type)
        .with_field("event_type", e.event_type)
        .with_field("reason", e.reason.as_str());
    if let Some(tid) = e.dispatcher_trace_id {
        ev = ev.with_trace_id(tid);
    }
    // (omit ts_epoch — populated by with_ts; F-3204)
    // (omit schema_version — populated by InternalEvent::with_ts via INTERNAL_EVENT_SCHEMA_VERSION constant at internal_log.rs:47)
    writer.write(&ev);
    ```
    Note: `ts_epoch` is populated automatically by `InternalEvent::with_ts(type_, ts)` (internal_log.rs:120-138 sets `ts_epoch = ts.timestamp()` on the envelope). Do NOT re-add via `with_field` — that would duplicate the key in the flattened JSON output. After constructing the InternalEvent envelope, the boundary adapter invokes `writer.write(&ev)` to flush it through the InternalLog (sync `&self` API per internal_log.rs:207); this is the single point where InternalEvent shape is materialized AND emitted from DLQ events.
  - `SinkDlqEvent::Failure(e)` →
    ```rust
    let mut ev = InternalEvent::with_ts(INTERNAL_SINK_DLQ_FAILURE, e.ts)
        .with_field("sink_name", e.sink_name)
        .with_field("sink_type", e.sink_type)
        .with_field("error", e.error);
    if let Some(tid) = e.dispatcher_trace_id {
        ev = ev.with_trace_id(tid);
    }
    // (omit ts_epoch — populated by with_ts; F-3204)
    writer.write(&ev);
    ```
  Note: The boundary adapter consumes `e: SinkDlqWriteEvent` field-by-field (`e.sink_name`, `e.sink_type`, `e.event_type`, `e.reason.as_str()`, `e.dispatcher_trace_id`). Rust's partial-move semantics permit this without a final binding to `e`. If implementers prefer defensive style, replace each field move with `e.field.clone()` — both compile.

  - `dispatcher_trace_id` propagation: ONLY chain `.with_trace_id(tid)` when the SinkDlqEvent's `dispatcher_trace_id` is `Some`. When `None`, do nothing — `InternalEvent`'s `dispatcher_trace_id: Option<String>` field with `skip_serializing_if = Option::is_none` will omit it from JSON output (per BC-3.07.003 TV's optional-field semantic).

  The boundary adapter is the SINGLE point where `InternalEvent` shape is constructed from DLQ events. `InternalEvent::with_ts` takes two arguments: `(type_: &str, ts: DateTime<Utc>)` — NOT the single-arg form `with_ts(now)`. All usages must follow the two-arg form.

  Add `INTERNAL_SINK_DLQ_WRITE` and `INTERNAL_SINK_DLQ_FAILURE` constants to `crates/factory-dispatcher/src/internal_log.rs` event-type constants block, matching the existing `INTERNAL_*` naming convention:

  Constant declarations to add:
  ```rust
  pub const INTERNAL_SINK_DLQ_WRITE: &str = "internal.sink_dlq_write";
  pub const INTERNAL_SINK_DLQ_FAILURE: &str = "internal.sink_dlq_failure";
  ```

  **Implementation ordering for SS-03 count audit:** (1) Add `INTERNAL_SINK_DLQ_WRITE` and `INTERNAL_SINK_DLQ_FAILURE` constants to `internal_log.rs` (constants block at lines 57-71) FIRST. (2) Then grep `crates/factory-dispatcher/src/internal_log.rs` for `pub const INTERNAL_*` to count the new total. (3) Update SS-03 architecture doc count enumeration with the new value. State-manager processes this update during the post-implementation handoff.

  Update `/Users/jmagady/Dev/vsdd-factory/.factory/specs/architecture/SS-03-observability-sinks.md` constants enumeration: synchronize the listed names + count to match the actual `internal_log.rs` constants block at the time of commit. Pre-existing baseline drift (SS-03 currently lists 17 names; code has 15 — drift exists from pre-S-4.05 spec evolution). Do NOT pre-commit to a specific final count; instead audit the actual file at commit time and produce an accurate count.

  Note: this story does NOT take responsibility for fixing all pre-existing drift names (`DISPATCHER_COMPLETED`, `DISPATCHER_ERROR`, `EVENT_EMITTED`, `EVENT_EMIT_ERROR` not in code; missing `INTERNAL_HOST_FUNCTION_PANIC`); that is tracked as TD-XXX (consistency-validator sweep candidate). S-4.05 only adds the 2 new DLQ constants and updates the count to reflect ACTUAL code state at commit time.

  This story also does NOT take responsibility for fixing the SS-03 architecture doc's pre-existing TOML schema drift (lines 64-77 still show `[sinks.<name>]` table-of-tables; canonical schema per code is `[[sinks]]` array-of-tables per F-1101 pass-11 fix). Tracked as separate TD-XXX (consistency-validator sweep candidate) — out of scope for S-4.05.

  Also add the DLQ retention constant alongside `DEFAULT_RETENTION_DAYS`:
  ```rust
  pub const INTERNAL_DLQ_DEFAULT_RETENTION_DAYS: u32 = 7;
  ```
  This ensures the 7-day DLQ default is a named constant (distinct from `DEFAULT_RETENTION_DAYS = 30` for dispatcher logs).

  - Files modified: `crates/factory-dispatcher/src/lib.rs` (channel + spawn site), `crates/factory-dispatcher/src/internal_log.rs` (boundary adapter conversion logic + 2 new constants), `.factory/specs/architecture/SS-03-observability-sinks.md` (count audit)

- [ ] **Task 6:** Implement `dlq_enabled` config toggle and size cap
  - **SinkStanza schema extension:** Add `dlq_enabled: Option<bool>` as a SIBLING field on `SinkStanza` (NOT in the `extra: toml::value::Table` flatten capture), with `#[serde(default)]` so absence parses as `None`. The downstream `FileSinkConfig` / `OtelGrpcConfig` deserialization (line 144-152 of `sinks/mod.rs`) is unaffected because `dlq_enabled` is captured at `SinkStanza` level before `extra` is passed through.
  - **Default-true semantics:** When `stanza.dlq_enabled` is `Some(false)`, DLQ is disabled for that sink. When `None` (absent in TOML) OR `Some(true)`, DLQ is enabled (AC-008 default-on).
  - **Strict-mode check:** Verify `FileSinkConfig` (`sink-file/src/lib.rs` lines 62-90) and other driver configs do NOT use `#[serde(deny_unknown_fields)]`. If any does, the `dlq_enabled` capture must happen via `SinkStanza` sibling extraction (current design) — flatten escape would fail. As of 2026-04-28, no driver config uses `deny_unknown_fields` per repo grep.
  - Read `dlq_enabled` from the `SinkStanza` struct in `crates/factory-dispatcher/src/sinks/mod.rs` (add `dlq_enabled: Option<bool>` field with `#[serde(default)]`; if absent, default to true per AC-008), populated from the per-sink `[[sinks]]` stanza (identified by `name = '<sink-name>'`) in `observability-config.toml`
  - Enforce 100 MB per-file cap with sequential suffix rotation
  - **DlqWriterConfig construction site:** When SinkRegistry loader sees a `[[sinks]]` stanza with `dlq_enabled = true` (or absent — defaults to true per AC-008), construct DlqWriter as:
    ```rust
    // At dispatcher init (factory-dispatcher::lib.rs or similar):
    // let (dlq_event_tx, dlq_event_rx) = tokio::sync::mpsc::channel::<SinkDlqEvent>(1024);
    // tokio::spawn(boundary_adapter(dlq_event_rx, internal_log_writer));
    //
    // At per-sink config time (SinkRegistry loader):
    let dlq_config = DlqWriterConfig {
        template: "dead-letter-{name}-{date}.jsonl".to_string(),  // BC-3.02.001-aligned form, hard-coded per AC-001; {name} placeholder matches sink-file's existing vocabulary
        size_cap_bytes: 100 * 1024 * 1024,  // 100 MiB per AC-004
        project: std::env::var("CLAUDE_PROJECT_DIR").ok(),  // project basename string per existing sink-file convention
    };
    let dlq_writer = DlqWriter::new(dlq_config, dlq_event_tx.clone());
    // Channel buffer = 1024: bounded for memory safety; sized to absorb typical
    // burst-of-failure scenarios (1k DLQ events) before back-pressure. With try_send
    // semantics (F-3203), channel-full results in DROPPED observability events —
    // the on-disk DLQ entry remains intact and is the load-bearing durability path.
    ```
    Note: `retention_days` is intentionally NOT on DlqWriterConfig (per F-2903 adjudication) — pruning lives in `factory-dispatcher::internal_log::prune_old` reading `PruneConfig.dlq_retention_days` independently.

    Field name clarifier: `DlqWriterConfig.project` (matching `resolve_path_template`'s parameter name); sink-file's `FileSink::new_with_error_channel` uses `project_dir` for the same value at the constructor parameter level — semantically identical, named differently due to scoping (O-3101).
  - Files modified: `crates/sink-core/src/dead_letter.rs`, `crates/factory-dispatcher/src/sinks/mod.rs`

- [ ] **Task 7:** Extend `prune_old` to cover DLQ files (BC-1.06.011)
  - Extend `crates/factory-dispatcher/src/sinks/mod.rs` `ObservabilityConfig` with `prune: Option<PruneConfig>` field. Add new `PruneConfig` struct with `dlq_retention_days: Option<u32>` field. Read from `[prune]` section of `observability-config.toml`. Default to `INTERNAL_DLQ_DEFAULT_RETENTION_DAYS` (= 7, defined in `crates/factory-dispatcher/src/internal_log.rs`) when absent (NOT a fallback to `retention_days`).
  - Modify `crates/factory-dispatcher/src/internal_log.rs` — extend `prune_old` to include DLQ file pattern `dead-letter-<sink-name>-{date}[-<seq>].jsonl` per BC-1.06.011. (No new crate needed; prune_old is in factory-dispatcher per BC-1.06.004 architecture.)
  - Read `dlq_retention_days` from `PruneConfig` (default `INTERNAL_DLQ_DEFAULT_RETENTION_DAYS` = 7, independent of `retention_days` which defaults to `DEFAULT_RETENTION_DAYS` = 30; do NOT fall back to `retention_days`).
  - **Implementation approach (REQUIRED):** Task 7 introduces `scan_files_with_prefix(dir: &Path, prefix: &str, retention_days: u32) -> Result<usize, std::io::Error>` as a NEW helper in `internal_log.rs` (returns count of files pruned) — it does NOT exist today; `prune_old`'s current inline `fs::read_dir` walk is refactored to call it. Refactor `internal_log.rs::prune_old` using the `scan_files_with_prefix(dir: &Path, prefix: &str, retention_days: u32) -> Result<usize, std::io::Error>` helper pattern:
    - Refactored call (extract `scan_files_with_prefix` from current inline `fs::read_dir` walk in `prune_old`): `prune_old()` invokes `scan_files_with_prefix(&log_root, "dispatcher-internal-", DEFAULT_RETENTION_DAYS)` (default 30 days)
    - New call (added in this story for DLQ retention): `prune_old()` ALSO invokes `scan_files_with_prefix(&log_root.join("dlq"), "dead-letter-", INTERNAL_DLQ_DEFAULT_RETENTION_DAYS)` (default 7 days)

  This per-(dir, prefix, retention) tuple approach is REQUIRED because:
  1. Different prefixes need DIFFERENT retentions: dispatcher logs = 30 days; DLQ = 7 days
  2. Different prefixes live in DIFFERENT directories: dispatcher logs in `.factory/logs/`; DLQ in `.factory/logs/dlq/`

  Per BC-1.06.011 PC1/PC3 (independence), a single-prefix-multi-tuple approach is the only architecturally feasible path. The earlier-considered single-directory `prefix_filter: &[&str]` approach is REJECTED because it cannot enforce per-prefix retention semantics.

  For pure-core testability, `scan_files_with_prefix` returns `Result<usize, Error>` for the count of files pruned (callers do not need to know specific filenames; only count + total).
  - Files modified: `crates/factory-dispatcher/src/internal_log.rs`

- [ ] **Task 8 (AC-006a unit test):** Unit test in `crates/sink-core/src/dead_letter.rs`
  - Inject simulated retry-exhaustion error into DLQ writer
  - Verify file contents match expected JSONL with all BC-3.07.003 fields present
  - Files modified: `crates/sink-core/src/dead_letter.rs` (test module)

- [ ] **Task 8b — DLQ failure-mode + auto-create + rotation unit tests** (split into 3 sub-tests; unit tests in `crates/sink-core/src/dead_letter.rs`)

  - **8b.1 — AC-010 DLQ write-failure path** (fixture: tmpdir with read-only `dlq/` permissions OR injected I/O error)
    - Simulate DLQ write failure (read-only filesystem or permission error injection)
    - Verify `internal.sink_dlq_failure` event is emitted to internal channel
    - Verify stderr log fallback occurs and dispatcher does not panic

  - **8b.2 — AC-007 DLQ mkdir-p auto-create** (fixture: writable tmpdir; `dlq/` subdirectory does NOT pre-exist)
    - Invoke `DlqWriter::write_event`; assert (a) `dlq/` subdirectory was created; (b) DLQ file exists inside it; (c) JSONL line appended successfully
    - Exercises by-replication `fs::create_dir_all(parent)?;` in dead_letter.rs (NOT shared with sink-file's mkdir-p test path)

  - **8b.3 — AC-004 midnight UTC rotation** (fixture: writable tmpdir + clock injection via `DlqWriter::with_clock_fn`)
    - Construct `DlqWriter` via the test-only `DlqWriter::with_clock_fn(config: DlqWriterConfig, internal_tx: tokio::sync::mpsc::Sender<SinkDlqEvent>, clock_fn: Arc<dyn Fn() -> DateTime<Utc> + Send + Sync>)` constructor with a mock `clock_fn` that returns controllable `DateTime<Utc>` values. Test fixture creates a synthetic `(test_internal_tx, test_internal_rx) = mpsc::channel(64)` channel; pass `test_internal_tx` to `with_clock_fn`. `test_internal_rx` is held in the test for assertion-side observation of emitted `SinkDlqEvent` items (alternative to disk-only assertions). Test sequence: (1) `clock_fn` returns `2026-04-27T23:59:59Z`; call `write_event` → assert (a) file `dead-letter-<sink>-2026-04-27.jsonl` exists with 1 JSONL line, (b) `DlqWriter::current_path()` returns `Some(day-N path)` as an owned `PathBuf`. (2) `clock_fn` returns `2026-04-28T00:00:01Z`; call `write_event` → assert (a) file `dead-letter-<sink>-2026-04-28.jsonl` exists with 1 JSONL line, (b) `DlqWriter::current_path()` returns `Some(day-N+1 path)` DIFFERENT from step 1's owned PathBuf — proves the cache rotated, (c) day-1 file is unchanged at 1 line (proves no continued writing to day-1 handle). PathBuf comparison is by-value (no `&` borrow language). The day-1-file-unchanged assertion is the operational proxy for 'prior File handle is closed' — if the prior handle were still being used, day-2's append would land in day-1's file.
    - Uses the clock-injection seam from Task 1 (F-2701 fix): `DlqWriter::with_clock_fn(config: DlqWriterConfig, internal_tx: tokio::sync::mpsc::Sender<SinkDlqEvent>, clock_fn: Arc<dyn Fn() -> DateTime<Utc> + Send + Sync>)` constructor.
    - Note: test mock `clock_fn` closures that produce a sequence of distinct values (e.g., `2026-04-27T23:59:59Z` then `2026-04-28T00:00:01Z`) MUST use interior synchronization (e.g., `Arc<AtomicUsize>` index or `Mutex<usize>`) to satisfy the `Send + Sync` bound on the closure type; trivial `Cell`-based mocks will fail to compile.

  - Files modified: `crates/sink-core/src/dead_letter.rs` (test module)

- [ ] **Task 9 (AC-006b integration test):** Integration test in `crates/sink-http/tests/integration.rs`
  - Mock 5xx server forces all retries to exhaust
  - Verify DLQ file at correct path contains dropped events in valid JSONL
  - Verify `internal.sink_dlq_write` events emitted with correct schema
  - Files created/modified: `crates/sink-http/tests/integration.rs`

- [ ] **Task 10 (AC-011 sink_name validation):** Implement sink_name sanitization at config load time
  - Reject `sink_name` values containing `/`, `\`, or `..` path tokens with `SinkConfigError::InvalidSinkName`; implement as `SinkStanza::validate()` method in `crates/factory-dispatcher/src/sinks/mod.rs` (returns `SinkConfigError::InvalidSinkName` if name contains `/`, `\`, or `..` per EC-003 + AC-011)
  - Validate at sink startup before any DLQ file creation
  - Unit tests: valid names pass; `"../etc/passwd"`, `"a/b"`, `"a\\b"`, `"a..b"` all rejected
  - Files modified: `crates/factory-dispatcher/src/sinks/mod.rs`

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-4.04 | Retry exhaustion is the trigger | DLQ receives post-retry events | Queue overflow also writes to DLQ |
| S-1.07 | JSONL + daily rotation pattern | Reuse same pattern | |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| DLQ filename matches `dead-letter-<sink-name>-{date}[-<seq>].jsonl` — base form (no `<seq>`) for first daily file; `<seq>` is 3-digit counter only present after within-day size-cap rotation | CAP-024 (filename authority), AC-001 | AC-001, unit test |
| No circular dependency: sink-core must not import sink-file | Architecture layering (F-002) | Task 0 extracts path_template to sink-core |
| DLQ write emits `internal.sink_dlq_write` | BC-3.07.003 (this wave) | AC-005, Task 5 |
| DLQ write failure emits `internal.sink_dlq_failure` | BC-3.07.004 (this wave) | EC-002, Task 5 |
| Internal events NOT routed through SinkRegistry | BC-3.07.002 invariant | EC-006, Task 5 |
| `prune_old` covers DLQ files | BC-1.06.011 (this wave) | AC-009, Task 7 |
| sink_name must be sanitized (no path separators or `..` tokens) | EC-003 (this story), AC-011 | AC-011, Task 10 |
| EC-004 (sink_name collision): second sink with duplicate name fails at startup with `SinkConfigError::NameCollision`; DLQ files not shared | v1.1 BC candidate `BC-3.NN.NNN-sink-name-collision-detection` (see v1.1 table) | Config validation at startup |
| EC-005 (concurrent DLQ writes): single-writer-per-DlqWriter via `Mutex<Option<(PathBuf, File, u64)>>` cache field on the DlqWriter struct (atomic rotation of path + handle pair; lock held continuously across clock eval + rotate + write_all + flush; serializes writes — no interleaved JSONL lines) | Enforced architecturally by `Mutex<Option<(PathBuf, File, u64)>>` cache field per DlqWriter instance + explicit lock-scope directive (Task 1 Lock Scope EC-005 invariant bullet); no AC needed | Task 1 (DlqWriter struct design + Lock Scope bullet) |
| EC-006 (recursive DLQ feedback): `internal.sink_dlq_write` events never routed through SinkRegistry — no recursive DLQ write possible | BC-3.07.002 invariant (internal events not routed through SinkRegistry); applies analogously to sink_dlq_write | EC-006, Task 5 |
| EC-007 (process crash mid-JSONL-line): prior complete lines remain uncorrupted; replay tools must tolerate partial trailing lines | Best-effort tolerance — documented contract; not formally contracted | BC-3.02.015 shutdown drain reduces (not eliminates) the partial-write window |
| internal-log event-type registry — INTERNAL_SINK_DLQ_WRITE + INTERNAL_SINK_DLQ_FAILURE constants | BC-3.07.003 + BC-3.07.004 reservation postconditions — both constants must be added to `crates/factory-dispatcher/src/internal_log.rs` constants block in the same commit as their respective emitters; naming must match existing `INTERNAL_*` convention (verified by grepping `internal_log.rs:57-71`) | Task 5b + grep on `internal_log.rs` constants block |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| serde_json | workspace-pinned | JSONL serialization |
| chrono | workspace-pinned | UTC date formatting for `{date}` placeholder |

## File Structure Requirements (MANDATORY)

**Scope:** This table lists IMPLEMENTATION files (code + tests) only. Spec document updates (PRD, BC-INDEX, SS-03 architecture, BC-3.NN.NNN files) are tracked separately in the 'PRD FR-044 Updates Required', 'BC-INDEX Updates Required', and 'BCs to Create' state-manager handoff sections above.

| File | Action | Purpose |
|------|--------|---------|
| crates/sink-core/src/path_template.rs | create | Pure path interpolation (extracted from sink-file; resolves F-002 cycle) |
| crates/sink-core/src/lib.rs | modify | Expose `path_template` module |
| crates/sink-file/src/lib.rs | modify | Import `resolve_path_template` from `sink-core::path_template` (remove local definition) |
| crates/sink-core/src/dead_letter.rs | create | DLQ implementation (DlqWriter, write_event, rotation, event emission) |
| crates/sink-core/src/events.rs | modify | Add SinkDlqEvent / SinkDlqWriteEvent / SinkDlqFailureEvent types per Task 5 (F-3101 architectural cycle resolution); mirrors existing SinkErrorEvent precedent. |
| crates/sink-http/src/lib.rs | modify | Wire DLQ on queue-overflow (Task 3) + retry-exhaustion (Task 4; HTTP-specific path; AC-006b host) |
| crates/sink-file/src/lib.rs | modify | Wire DLQ on queue-overflow (Task 3) |
| crates/sink-otel-grpc/src/lib.rs | modify | Wire DLQ on queue-overflow (Task 3) |
| crates/sink-http/tests/integration.rs | create/modify | Mock 5xx integration test (AC-006b) |
| crates/factory-dispatcher/src/lib.rs | modify | Create shared `(dlq_event_tx, dlq_event_rx) = mpsc::channel::<SinkDlqEvent>(1024)` at dispatcher init; spawn boundary adapter task via `tokio::spawn`; ensure original `dlq_event_tx` is dropped at shutdown so boundary task exits cleanly. |
| crates/factory-dispatcher/src/internal_log.rs | modify | Extend prune_old to cover `dead-letter-*.jsonl` files (BC-1.06.011); add INTERNAL_SINK_DLQ_WRITE + INTERNAL_SINK_DLQ_FAILURE + INTERNAL_DLQ_DEFAULT_RETENTION_DAYS constants (Task 5b); boundary adapter conversion logic (`dispatch_to_internal_event`) |
| crates/factory-dispatcher/src/sinks/mod.rs | modify | Extend ObservabilityConfig (add prune: Option<PruneConfig> field) + SinkStanza (add dlq_enabled: Option<bool> field) per Tasks 6, 7, 10; add sink_name path-token validation per AC-011 |

## Version History

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 1.0 | 2026-04-27 | story-writer | Initial draft |
| 1.1 | 2026-04-27 | story-writer | Pass-1 fix burst |
| 1.2 | 2026-04-27 | story-writer | Pass-2 initial fixes |
| 1.3 | 2026-04-27 | story-writer | Pass-2 additional fixes |
| 1.4 | 2026-04-28 | story-writer | Pass-2 fix burst — frontmatter coherence, filename pattern authority, AC traceability cleanups |
| 1.5 | 2026-04-28 | story-writer | Pass-3 fix burst — F-301..F-308: subsystem label canonicalization (3 sites); BC-1.06.011 PC2 size-cap propagation; AC-002 spurious BC-3.03.002 trace dropped; AC-006a BC-3.02.015 mis-anchor removed; AC-007 BC-3.02.007 anchor fixed via Task 0 mkdir-p extraction; AC-008 v1.1 BC candidate added; EC-004/005/006/007 compliance rules added; subsystems frontmatter expanded to SS-03+SS-01 |
| 1.6 | 2026-04-28 | product-owner | Pass-4 fix burst — addressed F-401..F-408; Task 7 file path correction (crates/cli/src/prune_old.rs → crates/factory-dispatcher/src/internal_log.rs); sibling BC-3.07.003/004 registry reservation postconditions + BC-3.07.003 TV aligned with BC-3.07.002 (Option a, schema_version as conformance constant not per-event field); new Task 5b (SINK_DLQ_WRITE + SINK_DLQ_FAILURE constants + SS-03 count update); CAP-024 verbatim-claim correction in Capability Anchor Justification; Subsystem Scope Note added (SS-10 explicitly out of scope); size cap TOML note added; Related Contracts POLICY 8 exception note added; Architecture Compliance Rule for registry reservation added |
| 1.7 | 2026-04-28 | product-owner | Pass-5 fix burst — addressed F-501..F-508; constant naming canonicalized at 5 sites (INTERNAL_SINK_DLQ_WRITE + INTERNAL_SINK_DLQ_FAILURE replacing bare SINK_DLQ_* prefix); AC-005 field list aligned with BC-3.07.003 TV (added sink_type, removed per-event schema_version, schema_version is conformance constant per PC4); AC-001 verbatim claim replaced with BC-3.02.001-aligned form note; BC-1.06.011 retention default disambiguated (dlq_retention_days default 7 independent of retention_days default 30; no fallback); INTERNAL_DLQ_DEFAULT_RETENTION_DAYS constant added to Task 5b; SS-03 baseline-drift acknowledged as TD-XXX (pre-existing, consistency-validator sweep); Task 5b rewritten defensive (audit-at-commit-time, no hard-coded count); Task 7 updated with prefix_filter architectural guidance; F-507 {date} placeholder normalized in PC2; Pass-5 fix burst incorporated naming-convention grep step against internal_log.rs to prevent recurrence of the F-501 drift class — future story-writer/product-owner edits should grep source for established conventions before adopting new constant names |
| 1.8 | 2026-04-28 | product-owner | Pass-6 fix burst — addressed F-601..F-603 (only 3 findings; trajectory decreasing 11→5→8→8→8→3); Configuration TOML block aligned with BC-1.06.011 PC1 (no retention_days fallback; replaced "same retention as main logs unless overridden" + "falls back to retention_days if unset" with "independent default 7" language anchored to INTERNAL_DLQ_DEFAULT_RETENTION_DAYS); AC-009 and BC-1.06.011 PC1/PC3 now cite INTERNAL_DLQ_DEFAULT_RETENTION_DAYS constant (not literal '7'); Architecture Mapping table completed with internal_log.rs row (Tasks 5b/7); Purity Classification table completed with internal_log.rs row |
| 1.9 | 2026-04-28 | product-owner | Pass-7 NITPICK fix burst — addressed F-701..F-705 (broken inputs path; constant citations consistency; Mutex wording standardization). NITPICK_ONLY pass-7 = 1 of 3 toward convergence per ADR-013. |
| 1.10 | 2026-04-28 | product-owner | Pass-8 fix burst — addressed F-801..F-805 (HIGH=1 MED=2 LOW=2); BC-1.06.011 Summary aligned with PC1/PC3 (no parity claim with main logs); AC-009 "same default retention" → "independent default retention"; Task 7 architecturally rewritten to use scan_files_with_prefix(dir, prefix, retention) tuples; literal "30" cites DEFAULT_RETENTION_DAYS; AC-009 glob matches BC-1.06.011 PC2 pattern. NITPICK clock resets per ADR-013 (was 1/3 NITPICK; now back to 0/3). |
| 1.11 | 2026-04-28 | product-owner | Pass-9 fix burst — addressed F-901 (LOW partial-fix carryover from pass-8 F-805); BC-1.06.011 Summary glob aligned with AC-009/PC2 prefix-scope. Single-line edit. |
| 1.12 | 2026-04-28 | product-owner | Pass-10 NITPICK fix — addressed F-1001 (LOW BC-3.07.003 description schema_version drift); F-1002 (LOW AC-011 cross-ref BCs to Create → v1.1 Candidates only). NITPICK_ONLY pass-10 = 1 of 3 toward convergence per ADR-013. |
| 1.13 | 2026-04-28 | product-owner | Pass-11 fix burst — addressed F-1101 (HIGH TOML schema conflict); `[sink.<name>]` → `[[sinks]]` array-of-tables alignment with `crates/factory-dispatcher/src/sinks/mod.rs` ObservabilityConfig parser model. 4 sites updated: AC-008, Configuration TOML block, v1.1 BC Candidates table, Task 6. NITPICK clock resets to 0/3 per ADR-013. |
| 1.14 | 2026-04-28 | product-owner | Pass-12 fix burst — addressed F-1201 (MED config parsing module unidentified) + F-1202 (MED [prune] section parser home unspecified). Architecture Mapping + File Structure Requirements now declare `crates/factory-dispatcher/src/sinks/mod.rs` as modified. Tasks 6+7+10 disambiguated. Sibling sweep complete on F-1101's structural propagation. |
| 1.15 | 2026-04-28 | product-owner | Pass-14 fix burst — addressed F-1401/F-1402 (HIGH BC TV ↔ runtime envelope reconciliation) + O-1401 (MED RFC3339 strict-mode v1.1 candidate) + O-1402 (LOW SS-03 TOML drift disclaimer extension). Path 1 chosen: align BC TV with existing canonical envelope semantics. NITPICK clock resets to 0/3. |
| 1.16 | 2026-04-28 | product-owner | Pass-15 fix burst — addressed F-1501 (MED dispatcher_trace_id TV omission semantics) + F-1502 (LOW envelope-optional fields footnote). Aligns BC-3.07.003 TV with runtime InternalEvent serde semantics. NITPICK clock holds at 0/3. |
| 1.17 | 2026-04-28 | product-owner | Pass-17 fix — F-1701 (MED PRD FR-044 propagation gap); added 'PRD FR-044 Updates Required' state-manager handoff section. Closes anchor-back gap surfaced by fresh-context audit of FR-level traceability matrix. |
| 1.18 | 2026-04-28 | product-owner | Pass-18 fix burst — F-1801 PRD FR-044 prose-not-table directive correction; F-1803 BC-1.06.011 promotion claim corrected (only 2 actual promotions); F-1804 BC-INDEX handoff section added; F-1802 v1.1 count math corrected to 10. Sibling-fix discipline applied per S-7.01. |
| 1.19 | 2026-04-28 | product-owner | Pass-19 fix burst — F-1901 PRD FR-044 directive now ADDs Subsystem(s) line (parallel to FR-045; FR-044 had no existing line to UPDATE); F-1902 v1.1 count baseline anchor 'after pass-12 cleanup' replaced with audit-at-commit-time framing acknowledging PRD's 8-vs-9 baseline gap as separate TD; O-1901 BC-INDEX line ~32→~26 drift fix. |
| 1.20 | 2026-04-28 | product-owner | Pass-20 fix burst — F-2001 MED BC-3.02.015 anchor classification corrected (load-bearing via AC-004, not informational); F-2002 LOW pending-intent residual TD acknowledgment strengthened. Pass-19 introduced classification drift; pass-20 closes. |
| 1.21 | 2026-04-27 | product-owner | Pass-21 fixes: F-2101 (HIGH) BC-3.02.015 returned to informational-only — Option A (AC-004 has no shutdown-drain assertion; sink-file vs sink-core scope mismatch); F-2102 (MED) BC-3.07.003 field-count narrative aligned to 9 (line 158, 259 sibling-fixed). Adjudication note: Pass-21 returned BC-3.02.015 to informational-only (Option A). AC-004 body had no shutdown-drain assertion; BC-3.02.015 is sink-file scoped while DLQ is sink-core scoped. Future story may promote a sink-core-scoped BC-3.NN.NNN-dlq-shutdown-drains-pending-writes if DLQ shutdown semantics are explicitly contracted. |
| 1.22 | 2026-04-27 | product-owner | Pass-22 fixes (minimal, Option C adjudication): F-2201 (LOW pending-intent) extended POLICY 8 exception note to distinguish 'no AC anchor' (BC-3.02.015 / F-2101) from 'scope-extraction inheritance' (BC-3.02.001/007); F-2202 (LOW NITPICK) BC-INDEX SS-01 row line number 25 → 24. |
| 1.23 | 2026-04-27 | product-owner | Pass-23 fix: F-2301 (LOW pending-intent) — POLICY 8 sub-case (2) module reference corrected; `sink-core::mkdir_parent` was a phantom module name; mkdir-p logic lives in `sink-core::path_template` per Task 0. |
| 1.24 | 2026-04-27 | product-owner | Pass-24 fix: F-2401 (HIGH) — Task 7 line 499 orphan directive struck. The directive ("Update Task 0 path-template helper module to consume the new retention") contradicted Architecture Mapping (path_template.rs is pure-core no-I/O; scan_files_with_prefix belongs in internal_log.rs effectful-shell) and contradicted Task 7's own canonical wiring at lines 500-504 + Files-modified declaration. Net effect: removes ambiguity; canonical wiring in lines 500-504 unchanged. |
| 1.25 | 2026-04-27 | product-owner | Pass-25 fixes: F-2501 (HIGH) path_template.rs purity preserved — mkdir-p extraction REVOKED from Task 0; DlqWriter does inline mkdir-p (BC-3.02.007 by-replication, not code-path reuse); F-2502 (HIGH) resolve_path_template signature generalized to DateTime<Tz: TimeZone> — sink-file passes Local (unchanged), DLQ passes Utc (AC-004 midnight UTC by-construction); F-2503 (LOW) phantom INTERNAL_DLQ_DEFAULT_ENABLED constant name removed from Configuration comment — note: pass-25 burst report wording 'removed' was imprecise; line 358 TOML comment retained the constant name as a forward-looking v1.1 promotion placeholder (not a dangling current-version reference), which F-2601 (pass-26) formally adjudicates as intentional; F-2504 (LOW) phantom prepare_target_path function name removed via Task 0 rewrite. |
| 1.26 | 2026-04-27 | product-owner | Pass-26 fixes: F-2602 (HIGH) resolve_path_template signature corrected to by-value `DateTime<Tz>` (was incorrectly `&DateTime<Tz>` by-reference); return type defined as `Result<PathBuf, sink_core::PathTemplateError>`; both `Local` and `Utc` are Copy so by-value matches existing sink-file calling convention with no caller breakage; F-2605 (LOW) PathTemplateError type definition directive added to Task 0 (enum + From mapping at sink-file caller site); F-2603 (MED) AC-007 by-replication mkdir-p test added to Task 8b (tmpdir without dlq/ subdir, assert directory created + file written); F-2604 (MED) DlqWriter recompute-on-each-write design directive added to Task 1 (Option<(PathBuf, File)> cache, close+reopen on path change); AC-004 midnight UTC rotation clock-injection test added to Task 8b (mock clock, verify prior-day File handle closed and new file opened); F-2601 (LOW) adjudicated as intentional — INTERNAL_DLQ_DEFAULT_ENABLED in line 358 TOML comment is a forward-looking v1.1 promotion placeholder, not a dangling current-version reference; no content edit to line 358. |
| 1.27 | 2026-04-27 | product-owner | Pass-27 fixes (6 findings): F-2701 (HIGH) DlqWriter clock_fn injection seam added (Arc<dyn Fn() -> DateTime<Utc> + Send + Sync>; default Utc::now; test constructor with_clock_fn); Task 8b clock-injection test rewritten to use seam. F-2702 (MED) Task 7 'Existing call' / 'New call' wording corrected — both calls are NEW (existing inline fs::read_dir refactored); preamble bullet added clarifying scan_files_with_prefix does not exist today. F-2703 (MED) PathTemplateError variant shape specified as struct-variant matching FileSinkError (UnknownPlaceholder { placeholder: String }); variant shape choice bullet added. F-2704 (LOW) Task 8b restructured into 8b.1/8b.2/8b.3 with separate fixtures (read-only fs for write-failure; writable tmpdir for mkdir-p; writable tmpdir + clock injection for rotation). F-2705 (LOW) CAP-024 'verbatim' claim corrected to BC-3.02.001-aligned form per Capability Anchor Justification. F-2706 (LOW) emission boundary adjudicated Option (c) — ALL filesystem errors in write_event emit internal.sink_dlq_failure (mkdir-p, open, write_all/flush). Task 2 Utc::now() updated to (self.clock_fn)() via sibling sweep. |
| 1.28 | 2026-04-27 | product-owner | Pass-28 fixes (2 MED + 2 LOW): F-2801 (MED) Task 8b.3 EBADF assertion replaced with feasible Rust assertions — `DlqWriter::current_path()` test accessor + day-1-file-unchanged operational proxy; F-2802 (MED) `DlqWriterConfig` struct defined explicitly (template, size_cap_bytes, project_root, retention_days fields) + canonical and test constructor signatures fully typed; O-2801 (LOW) multi-threaded clock_fn synchronization note added; O-2802 (LOW) Files-modified table scope clarifier added (implementation files only; spec docs tracked in handoff sections). |
| 1.29 | 2026-04-27 | product-owner | Pass-29 fixes (2H + 5M + 1L): F-2901 (HIGH) current_path() returns Option<PathBuf> (clone-on-call) — borrow-checker-feasible; F-2902 (HIGH) Mutex shape unified to Mutex<Option<(PathBuf, File)>> across lines 451/464/585 (also EC-005 edge-cases table + BC-3.02.015 informational entry); F-2903 (MED) DlqWriterConfig.retention_days REMOVED — canonical source PruneConfig.dlq_retention_days; F-2904 (MED) write_event signature typed (DlqReason enum + DlqError enum); F-2905 (MED) resolve_path_template '...' replaced with explicit (sink_name: &str, project_root: &Path) params; F-2906 (MED) scan_files_with_prefix signature typed (dir: &Path, prefix: &str, retention_days: u32, return std::io::Error, returns count pruned); F-2907 (MED) Task 6 SinkStanza → DlqWriterConfig construction site directive added with concrete Rust snippet; O-2901 (LOW) PathTemplateError variant guidance clarified — From mechanical-rename rule: every variant must have 1:1 named-field counterpart in FileSinkError. |
| 1.30 | 2026-04-27 | product-owner | Pass-30 fixes (1H + 3M + 2L): F-3001 (HIGH) resolve_path_template signature aligned EXACTLY with existing sink-file shape (name: &str + Option<&str> project + {name} placeholder) — preserves 'zero behavioral change' for sink-file consumers; DLQ default template updated to dead-letter-{name}-{date}.jsonl; DlqWriterConfig.project_root → project: Option<String>; clarification bullet added stating signature mirrors sink-file shape exactly. F-3002 (MED) DlqError::Serialization variant removed (SinkEvent serialization is infallible for Map<String,Value>); 3 filesystem-error variants only. F-3003 (MED) SinkStanza dlq_enabled directive expanded — #[serde(default)], sibling-field capture (not extra), strict-mode check. F-3004 (MED) internal.sink_dlq_write/_failure event ts MUST use InternalEvent::with_ts(self.clock_fn()) — same UTC value as path resolution; avoids local-vs-UTC drift. F-3005 (LOW) Mutex specified as std::sync::Mutex (matches sink-file convention at sink-file/src/lib.rs:43,216). O-3001 (LOW) current_path gating specified as pub(crate) — NOT #[cfg(test)]; keeps accessor available for in-crate test modules and future internal metric collectors. |
| 1.31 | 2026-04-27 | product-owner | Pass-31 fixes (2H + 4M + 2L): F-3101 (HIGH) + F-3106 (MED) sink-core architectural cycle resolved via SinkErrorEvent precedent — added SinkDlqEvent/SinkDlqWriteEvent/SinkDlqFailureEvent types in sink-core::events; DlqWriter constructors accept mpsc::Sender<SinkDlqEvent>; conversion to InternalEvent happens at factory-dispatcher boundary adapter (Task 5b). F-3102 (HIGH) InternalEvent::with_ts arity corrected to (type_, ts) two-arg form in boundary adapter directive; old single-arg form removed from Task 5 entirely. F-3103 (MED) EC-005 lock scope made explicit — guard held continuously across clock+rotate+write_all+flush (Lock Scope EC-005 invariant bullet added to Task 1 rotation semantics). F-3104 (MED) size-cap tracking specified — cache extended to Mutex<Option<(PathBuf, File, u64)>>; init via metadata().len(); increment per write; 5 sites updated (Task 1 struct def, Task 1 rotation semantics, EC-005 edge cases table, Architecture Compliance Rule EC-005, BC-3.02.015 informational entry). F-3105 (MED) Arc<dyn Fn> coercion footgun — Arc::new(\|\| Utc::now()) closure form required in constructor and clock_fn bullet; bare Arc::new(Utc::now) removed. O-3101 (LOW) project vs project_dir naming clarifier added to Task 6 construction site. O-3102 (LOW) let now = (self.clock_fn)() binding made explicit in Task 5 emission snippet. |
| 1.32 | 2026-04-27 | product-owner | Pass-32 fixes (3H + 2M + 3L): F-3201 (HIGH) phantom event.dispatcher_trace_id() replaced with fields.get("dispatcher_trace_id").and_then(Value::as_str).map(str::to_owned) — applied at both success-path SinkDlqWriteEvent construction and failure-path SinkDlqFailureEvent construction. F-3202 (HIGH) DlqReason::as_str() impl added returning snake_case literals matching BC-3.07.003 TV; boundary adapter uses as_str() not to_string(). F-3203 (HIGH) channel send specified as try_send (matches SinkErrorEvent precedent / BC-3.07.002 fire-and-forget / sync-safe); applied to both Write and Failure emission. F-3204 (MED) ts_epoch double-population removed from boundary adapter — with_ts handles it; clarifying note added. F-3205 (MED) dispatcher_trace_id Option propagation made explicit via if-let-with_trace_id pattern in both Write and Failure boundary adapter conversions. F-3206 (LOW) event_type uses event.event_type().unwrap_or("<unknown>") with source note citing sink-core/lib.rs:82-84. F-3207 (LOW) channel buffer 1024 documented with back-pressure rationale; BC-3.07.003 PC1 loosened from 'exactly one' to 'at most one' under try_send semantics. F-3208 (LOW) disk-vs-channel atomicity disclosure added as bullet at end of Task 5. |
| 1.33 | 2026-04-27 | product-owner | Pass-33 fixes (3M + 3L; 1 finding self-withdrew on Rust partial-move analysis): F-3302 (MED) channel architecture specified as MULTIPLEXED — single shared (dlq_event_tx, dlq_event_rx) at dispatcher init; sinks receive dlq_event_tx.clone(); boundary task spawned once via tokio::spawn; shutdown drains rx until None. F-3303 (MED) lock-release ordering on filesystem error made explicit — drop guard FIRST, then construct/emit SinkDlqFailureEvent (skeleton snippet provided). F-3304 (MED) BC-3.07.004 8-field TV table added (drops event_type+reason; adds error). O-3301 (LOW) implementation ordering for SS-03 count audit specified. O-3302 (LOW) AC-005 wording references BC-3.07.003 PC1 'at most one' semantic. O-3303 (LOW) comment correctly references INTERNAL_EVENT_SCHEMA_VERSION constant (not phantom InternalEvent::SCHEMA_VERSION method). |
| 1.34 | 2026-04-27 | product-owner | Pass-34 fixes (1H + 3M + 1L): F-3402 (HIGH) + F-3401 (MED) unified success+failure skeleton with tuple capture (now, result) — fixes unbound `now` variable AND propagates lock-release ordering to both paths. F-3403 (MED) Files modified updated — added crates/factory-dispatcher/src/lib.rs to File Structure Requirements and Task 5b; Architecture Mapping disambiguated (lib.rs = spawn site, internal_log.rs = conversion logic). F-3404 (MED) shutdown sequence requires BOTH per-sink clone drop AND original-tx drop to avoid boundary-task hang. O-3401 (LOW) BC-3.07.004 PC2 wording updated to reference try_send Full + boundary-task-not-yet-spawned. |
| 1.35 | 2026-04-27 | product-owner | Pass-35 fixes (2H + 1M + 1L + 1 process obs): F-3501 (HIGH) phantom `sink_type` resolved — added `sink_type: &str` parameter to write_event signature; Task 3/4 directives updated to pass sink driver's static type; unified skeleton opening comment added. F-3502 (HIGH) + F-3503 (MED) boundary adapter signature unified — sync `dispatch_to_internal_event(event: SinkDlqEvent, writer: &InternalLog)`; spawn snippet updated to drop spurious .await on conversion + use shared ref; Architecture Mapping updated. F-3504 (LOW) partial-move clarifier added after boundary adapter conversion snippet. O-3502 (LOW) speculative 'internal-log-flush task' reference removed; replaced with neutral 'spawned alongside existing dispatcher subsystems' language. |
| 1.36 | 2026-04-27 | product-owner | Pass-36 fixes (3M + 2L): F-3601 (MED) Task 8b.3 with_clock_fn arity corrected to 3 args matching Task 1 canonical definition; test fixture mpsc::channel directive added. F-3602 (MED) File Structure Requirements gains crates/sink-core/src/events.rs row for SinkDlqEvent types. F-3603 (MED) Task 3 phantom crates/sink-core/src/retry.rs removed; Task 3 re-scoped to queue-overflow per-driver (sink-http/sink-file/sink-otel-grpc lib.rs); Task 4 retains retry-exhaustion ownership (sink-http only). F-3604 (LOW) boundary adapter snippets now invoke writer.write(&ev). F-3605 (LOW NITPICK) sink_type candidate list qualified — Datadog/Honeycomb wrap sink-http and emit 'http'. |
