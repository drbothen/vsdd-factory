---
document_type: story
level: ops
story_id: "S-10.05"
epic_id: "E-10"
version: "1.4"
status: draft
producer: story-writer
timestamp: 2026-05-06T00:00:00Z
phase: 2
inputs:
  - .factory/specs/architecture/decisions/ADR-015-single-stream-otel-schema.md
  - .factory/stories/epics/E-10-single-stream-otel-event-emission.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.11.003.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.12.009.md
input-hash: ""
traces_to: .factory/stories/epics/E-10-single-stream-otel-event-emission.md
functional_requirements: []
cycle: v1.0-brownfield-backfill
points: "8"
wave: 17
depends_on: ["S-10.02", "S-10.03", "S-10.04"]
blocks: ["S-10.06"]
behavioral_contracts:
  - BC-1.11.003
  - BC-1.12.009
  - BC-2.06.001
  - BC-4.09.001
verification_properties: []
priority: "P1"
tdd_mode: strict
target_module: "crates/hook-plugins"
subsystems: ["SS-01", "SS-02", "SS-04"]
estimated_days: 3
assumption_validations: []
risk_mitigations: []
anchored_adr: ADR-015
---

> **Note on wave terminology:** `wave: 17` in frontmatter refers to the project-cycle wave (orchestrator scheduling). The "Wave 0/1/2/3/4/5" references in this story body refer to ADR-015 migration waves (per ADR-015 Migration Plan section).

# S-10.05: ADR-015 Wave 2 — Plugin schema migration, dual-emit shims, and bug-fix bundle

**Epic:** E-10 — Single-stream OTel-aligned event emission (ADR-015)
**Wave:** Wave 2
**Depends on:** S-10.02, S-10.03, S-10.04
**Blocks:** S-10.06
**Estimated effort:** L (8 pts, ~3 days)

## Narrative

- **As a** vsdd-factory plugin author
- **I want** all native WASM plugins to emit events using reverse-DNS versioned
  `event.name` values alongside their old names (dual-emit), with bug fixes for
  the three known content defects
- **So that** Grafana dashboards continue matching during Wave 3 development (no
  dashboard regression), and the `pr.opened` / `plugin_version` / `open_to_merge_seconds`
  bugs are permanently resolved

## Acceptance Criteria

### AC-001: All plugins emit event.name in reverse-DNS .v1 format (traces to ADR-015 D-15.2 §event.name + Wave 2) (traces: BC-4.09.001 postcondition 1)

Every native WASM plugin's `emit_event` call uses an `event.name` value in
reverse-DNS + `.v1` format. Examples: `vsdd.commit.made.v1`, `vsdd.pr.created.v1`,
`vsdd.session.started.v1`. Plugin-side Resource-level field stamps are REMOVED
(host stamps these; they are no longer needed in plugin code).

**Falsifiable test:** Static analysis (`grep -r "emit_event" crates/hook-plugins/`)
shows no plugin emitting an `event.name` that doesn't match
`^vsdd\.[a-z.]+\.v[0-9]+$` pattern.

### AC-002: Dual-emit shims installed for all migrated event names (traces to ADR-015 Wave 2 §dual-emit strategy + D-15.2.e) (traces: BC-1.12.009 postcondition 1)

For each event family being renamed, BOTH the old and new names are emitted in the
same dispatcher invocation. The two paired emissions share:
- `event.correlation_id`: same UUID (shared across the pair)
- New-name emission: `event.replaces_deprecated_alias` = `event.id` of the old-name emission
- Old-name emission: `event.deprecated_by` = `event.id` of the new-name emission

This implements the D-15.2.e five-state identity contract.

**Falsifiable test:** An integration test for a renamed event family asserts that
both the old and new names appear in `events-*.jsonl` with a matching
`event.correlation_id` and symmetric cross-reference fields.

### AC-003: vsdd.internal.event_name_deprecated.v1 emitted once per old name per invocation (traces to ADR-015 Wave 2 §dual-emit strategy)

Each deprecated old event name triggers exactly ONE `vsdd.internal.event_name_deprecated.v1`
lifecycle event per dispatcher invocation (rate-limited; same mechanism as the
host-field override rate-limiter from S-10.04). The event carries the old-to-new
name mapping.

**Falsifiable test:** An invocation that triggers the same deprecated event name
three times produces exactly one `vsdd.internal.event_name_deprecated.v1` event.

### AC-004: pr.opened → vsdd.pr.created.v1 bug fixed (traces to ADR-015 Context §Naming inconsistencies)

The `capture-pr-activity` plugin (or whichever plugin currently emits `pr.created`)
is updated to emit both `pr.created` (old name, dual-emit) AND `vsdd.pr.created.v1`
(new name). The old `pr.opened` Grafana query mismatch is documented as a known
content defect in the S-10.01 audit; this story fixes the plugin side.

**Falsifiable test:** `events-*.jsonl` after a PR-activity plugin run contains
`vsdd.pr.created.v1` with a non-empty `vcs.ref.head.revision` field.

### AC-005: plugin_version bug fixed — plugin's own version stamped (traces to ADR-015 Context §Identity fields present but wrong + main.rs:143) (traces: BC-4.09.001 postcondition 7)

Every plugin's `plugin.version` field now reflects the plugin's own
`CARGO_PKG_VERSION` (read from plugin metadata at load time or from the WASM
binary's embedded version string), NOT the dispatcher's version. The old
`main.rs:143` assignment `plugin_version = env!("CARGO_PKG_VERSION")` is removed
or replaced.

**Falsifiable test:** An integration test loads two plugins with different versions
and asserts that each plugin's emitted events carry that plugin's version string,
not the dispatcher's version.

### AC-006: open_to_merge_seconds emitted by capture-pr-activity plugin (traces to ADR-015 Context §open_to_merge_seconds queried but never emitted) (traces: BC-4.09.001 postcondition 8)

The `capture-pr-activity` plugin (or its equivalent) now emits
`open_to_merge_seconds` as a domain field when a PR merge event occurs. The value
is the elapsed seconds from PR creation timestamp to merge timestamp.

**Falsifiable test:** A test PR-merge event results in an event record in
`events-*.jsonl` containing a numeric `open_to_merge_seconds` field with value > 0.

### AC-007: No dashboard regression during Wave 2 (traces to ADR-015 Wave 2 §Wave 2 ships independently)

Old event names continue to be emitted (dual-emit). Grafana queries that were
previously matching (even if few) continue to match after Wave 2 merges.
Aggregation queries MUST filter to one event-name namespace (old OR new) to
avoid double-counting — not enforced by code, but documented in test comments.

**Falsifiable test:** The Grafana panel querying `pr.created` (old name) returns
at least one row within 24 hours of Wave 2 merging, confirming backward compat.

### AC-008: SDK changelog notes MAJOR version bump (traces to ADR-015 §SDK semver impact / OQ-6 resolved) (traces: BC-2.06.001 postcondition 1)

The `vsdd-hook-sdk` CHANGELOG and version number reflect a MAJOR semver bump
(e.g., 1.0.0 → 2.0.0 or 0.2.0 → 1.0.0) because the behavioral contract changes:
host-stamped fields no longer pass through from plugin values. The `emit_event`
ABI signature is unchanged (`HOST_ABI_VERSION` stays at 1); only the behavioral
contract changes.

**Falsifiable test:** `grep "version" crates/hook-sdk/Cargo.toml` shows a
major-version increment relative to the pre-Wave-2 version.

## Behavioral Contracts

| BC ID | Title | Subsystem | Capability | Notes |
|-------|-------|-----------|------------|-------|
| [BC-1.11.003](../specs/behavioral-contracts/ss-01/BC-1.11.003.md) | factory-dispatcher::host::emit_pair — atomic dual-emit host helper for Wave 2 migration window | SS-01 | CAP-009 | Wave 2 — emit_pair host helper for atomic dual-emit; optional migration path for plugins (AC-009) |
| [BC-1.12.009](../specs/behavioral-contracts/ss-01/BC-1.12.009.md) | factory-dispatcher::dual_emit::pair_identity_contract — event.correlation_id / event.deprecated_by / event.replaces_deprecated_alias field semantics; five-state event classification (paired-current / paired-deprecated / orphaned-deprecated-half / orphaned-current-half / non-paired); malformed → orphaned-half downgrade rule; consumer degradation rule for orphaned halves (ADR-015 D-15.2.e v1.5) | SS-01 | CAP-029 | Wave 2 — dual-emit pair identity contract; consumer classification rules (AC-002, AC-010) |
| [BC-2.06.001](../specs/behavioral-contracts/ss-02/BC-2.06.001.md) | vsdd-hook-sdk::versioning::wave2_major_semver_bump_for_d_15_3_host_field_precedence — SDK MAJOR version increment signals host-field-precedence semantics change per ADR-015 D-15.3 with migration guidance for plugin authors | SS-02 | CAP-009 | Wave 2 — SDK MAJOR semver bump for D-15.3 behavioral contract change; CHANGELOG "Breaking Changes" section required (AC-008) |
| [BC-4.09.001](../specs/behavioral-contracts/ss-04/BC-4.09.001.md) | hook-plugins::event_naming::wave2_reverse_dns_event_name_migration_with_dual_emit — all native WASM plugins under crates/hook-plugins/ migrate event-name strings from legacy short-form to reverse-DNS .v1 canonical form; dual-emit shim active during Wave 2→Wave 3 window; legacy emission removed post-Wave-3 | SS-04 | CAP-009 | Wave 2 — plugin-side reverse-DNS event name migration + dual-emit shim; plugin_version fix; open_to_merge_seconds addition (AC-001, AC-004, AC-005, AC-006); CAP-029 secondary reference (plugin emissions appear on single stream) |

### AC-009: emit_pair host helper available for Wave 2 plugin adoption (traces: BC-1.11.003 postcondition 1)

The `vsdd_hook_sdk::host::emit_pair(old_event, new_event)` host helper is available
during Wave 2 as an optional migration path. When used:
1. Both `old_event` and `new_event` are written to `events-*.jsonl` as complete JSONL lines.
2. The host automatically assigns a shared `event.correlation_id` UUIDv4 to both events,
   `old_event.event.deprecated_by = new_event.event.id`, and
   `new_event.event.replaces_deprecated_alias = old_event.event.id`.
3. Plugin code cannot set `event.correlation_id`, `event.deprecated_by`, or
   `event.replaces_deprecated_alias` — these are host-assigned inside `emit_pair`
   (per BC-1.11.003 invariant 1).

Plugins that have NOT yet migrated to `emit_pair` continue to perform two sequential
`emit_event` calls. Both paths produce valid dual-emit pairs for BC-1.12.009
classification purposes.

**Falsifiable test:** A test plugin that calls `emit_pair("old.name.v0", "vsdd.pr.created.v1")`
produces two events in `events-*.jsonl` with matching `event.correlation_id` and
symmetric `event.deprecated_by` / `event.replaces_deprecated_alias` cross-references;
neither event has a plugin-supplied `event.correlation_id` in the payload.

### AC-010: consumers classify dual-emit pairs using five-state taxonomy (traces: BC-1.12.009 postcondition 4)

All factory-query and Grafana dashboard consumers classify events from `events-*.jsonl`
using the BC-1.12.009 five-state taxonomy (paired-current, paired-deprecated, orphaned-deprecated-half, orphaned-current-half, non-paired):
- State 1 — Paired-Current: `event.replaces_deprecated_alias` set + target present in same trace scope
- State 2 — Paired-Deprecated: `event.deprecated_by` set + target present in same trace scope
- State 3 — Orphaned-Deprecated-Half: `event.deprecated_by` set + target absent
- State 4 — Orphaned-Current-Half: `event.replaces_deprecated_alias` set + target absent
- State 5 — Non-paired: all three correlation fields absent (event has no `correlation_id`, no `deprecated_by`, no `replaces_deprecated_alias`); this is the baseline state and the post-Wave-3 default after dual-emit shim removal

Orphaned halves (State 3/4) are treated as single non-paired events for dedup purposes —
they are NOT excluded from aggregation. Aggregation queries during Wave 2 MUST filter to
a single event-name namespace (old OR new) to avoid double-counting (BC-1.12.009 postcondition 7).

**Falsifiable test:** A unit test with one event per state variant asserts correct
classification; an orphaned-half event IS included in single-event accounting (not excluded).

## Architecture Mapping

| Component | Module | Pure/Effectful | ADR-015 Reference |
|-----------|--------|---------------|-------------------|
| All native WASM plugins | `crates/hook-plugins/*/` | Effectful (emit_event calls) | Wave 2 plugin schema migration |
| `capture-pr-activity` plugin | `crates/hook-plugins/capture-pr-activity/` | Effectful | Bug fixes: AC-004, AC-006 |
| Dual-emit shim logic | Plugin side (each plugin's `emit_event` calls) | Effectful | D-15.2.e |
| `vsdd-hook-sdk` | `crates/hook-sdk/` | Mixed | SDK semver major bump |
| `vsdd-hook-sdk` (SS-02: Hook SDK and Plugin ABI) | `crates/hook-sdk/` | Effectful (MAJOR semver bump — breaking behavioral contract change) | T-7 MAJOR semver bump; crates/hook-sdk/Cargo.toml + CHANGELOG.md modified |

**Plugin inventory (count TBD — to be enumerated from S-10.01 Wave 0 audit output):**
Per ADR-015 Context "every plugin-emitted domain event," the following plugins are
in scope. Final list from S-10.01 `adr015-wave0-event-field-snapshot.md`:
capture-commit-activity, capture-pr-activity, block-ai-attribution, handoff-validator,
pr-manager-completion-guard, track-agent-stop, update-wave-state-on-merge,
validate-pr-review-posted, session-learning, warn-pending-wave-gate, track-agent-start.
(Use S-10.01 Wave 0 audit output as the authoritative plugin count.)

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | FileSink failure between old-name and new-name emission in a dual-emit pair | Orphaned-half condition per D-15.2.e. Consumer degrades to single-event accounting. OQ-8 (atomic dual-emit) is deferred. |
| EC-002 | Plugin emits `event.name` without reverse-DNS prefix | `event.category = "unknown"` per D-15.2.b; deprecated-name dual-emit warning emitted |
| EC-003 | PR with no merge timestamp (open PR) | `open_to_merge_seconds` is OMITTED from the event (field is absent, not zero) for non-merge events |
| EC-004 | Aggregation query double-counts during Wave 2 window | Known artifact documented in ADR-015 Negative consequences; mitigation is consumer-side single-namespace filter |
| EC-005 | Additional plugins discovered in S-10.01 audit beyond the initial enumeration | All discovered plugins are included in this story's scope; the authoritative plugin count comes from the S-10.01 Wave 0 audit output |

## Tasks

- [ ] T-0: STOP CHECK — confirm S-10.02, S-10.03, S-10.04 all merged before plugin-side changes
- [ ] T-1: Remove plugin-side Resource-level field stamps from all 11 WASM plugins
- [ ] T-2: Update `event.name` in all 11 plugins to reverse-DNS `.v1` format
- [ ] T-3: Implement dual-emit shim in each plugin (old name + new name + correlation fields)
- [ ] T-4: Fix `pr.opened` → `pr.created` → `vsdd.pr.created.v1` reconciliation in capture-pr-activity
- [ ] T-5: Fix `plugin_version` at main.rs:143 — source from plugin metadata, not dispatcher version
- [ ] T-6: Add `open_to_merge_seconds` emission to capture-pr-activity for merge events
- [ ] T-7: Bump vsdd-hook-sdk to MAJOR semver version; update CHANGELOG
- [ ] T-8: Write integration test: dual-emit pair has matching correlation_id + symmetric cross-refs (AC-002)
- [ ] T-9: Write test: single deprecated-event-name produces exactly one deprecated lifecycle event (AC-003)
- [ ] T-10: Write test: two plugins with different versions → correct per-plugin version in events (AC-005)
- [ ] T-11: Write test: PR merge event contains open_to_merge_seconds > 0 (AC-006)
- [ ] T-12: Run full test suite; confirm no regressions

## Previous Story Intelligence

S-10.03 fixed the `plugin_version` bug at the host side (host now stamps the correct
value). This story removes the wrong plugin-side stamp. Coordinate: if S-10.03 already
eliminated the plugin-side stamp from all plugins during T-9, T-5 here may be a
verification task rather than a new fix.

S-10.04 added the `event.category` registry entries for `vsdd.internal.event_name_deprecated.v1`.
The rate-limiter pattern from S-10.04 (per-invocation HashMap) is reused here for
the deprecated-name rate-limit.

## Architecture Compliance Rules

Per ADR-015 D-15.2.e and Wave 2:
- Dual-emit is PLUGIN-ASSERTED for Wave 2. Plugin authors are responsible for
  emitting matched UUIDs across the pair. The host does NOT validate cross-event refs.
- Plugin unit tests SHOULD assert symmetric-pair emission (matched UUIDs).
- `HOST_ABI_VERSION` stays at 1. The `emit_event` ABI signature is unchanged.
- `schema_version` field in JSONL records is REMOVED (per D-15.3 §Schema versioning).

## Library and Framework Requirements

| Library | Version | Notes |
|---------|---------|-------|
| `uuid` | workspace | Dual-emit correlation_id and cross-reference UUIDs |
| `vsdd-hook-sdk` | MAJOR bump | SDK major semver bump per OQ-6 resolution |

## File Structure Requirements

| File | Action | Notes |
|------|--------|-------|
| `crates/hook-plugins/*/src/lib.rs` (11 plugins) | MODIFY | Remove Resource stamps; update event.name; add dual-emit shims |
| `crates/hook-plugins/capture-pr-activity/src/lib.rs` | MODIFY | pr.opened→pr.created reconciliation; open_to_merge_seconds |
| `crates/factory-dispatcher/src/main.rs` | MODIFY | Remove old plugin_version = env!("CARGO_PKG_VERSION") at line 143 |
| `crates/hook-sdk/Cargo.toml` | MODIFY | MAJOR semver version bump |
| `crates/hook-sdk/CHANGELOG.md` | MODIFY | Document breaking behavioral change |
| `crates/hook-plugins/*/tests/` | CREATE/MODIFY | Dual-emit pair tests per plugin |

## Token Budget Estimate

| Context Source | Estimated Tokens |
|----------------|-----------------|
| This story spec | ~8,000 |
| ADR-015 Wave 2 + D-15.2.e sections | ~6,000 |
| BC files (4 BCs) | ~6,000 |
| 11 plugin source files (partial reads) | ~12,000 |
| S-10.01 measurement outputs | ~3,000 |
| Test file context | ~4,000 |
| **Total** | **~39,000** |

~19% of a 200k context window. Within budget but approaching split threshold for a
complex story. If plugin count is significantly higher than 11, consider splitting
into two stories (one per half of the plugin set).

## CHANGELOG

| Version | Date | Change |
|---------|------|--------|
| v1.0 | 2026-05-04 | Initial authoring from ADR-015 Wave 2 decomposition. |
| v1.1 | 2026-05-06 | D-313 Phase 1c POLICY 8 propagation: behavioral_contracts array populated with 2 BCs (BC-1.11.003, BC-1.12.009). BC table added. AC-002 traced to BC-1.12.009 postcondition 1. New ACs AC-009→BC-1.11.003 and AC-010→BC-1.12.009 added. Phase 1b (D-313) BCs propagated; BC-1.11.003 (architect-authored 2026-05-04) deferral healed. |
| v1.2 | 2026-05-06 | D-316: F-5 SS-02 added to subsystems (Hook SDK and Plugin ABI — crates/hook-sdk/ modified in T-7; BC-1.11.003 Architecture Module is half-SS-02); SS-02 Architecture Mapping row added. F-16 plugin count claim corrected: "11 affected WASM plugins" → "count TBD (S-10.01 audit)"; EC-005 updated. F-14 wave-terminology note added. POLICY 8 sweep: BC-1.11.003 CAP-TBD → CAP-009 (D-314 resolve); BC-1.12.009 CAP-003 → CAP-029 + H1 updated to five-state taxonomy (D-315 F-12). |
| v1.3 | 2026-05-06 | D-320 — F-7 fix: BC-1.12.009 body BC table row state-order corrected to match H1 verbatim (orphaned-deprecated-half BEFORE orphaned-current-half per POLICY 7 byte-for-byte). POLICY 8 sweep: BC-2.06.001 (SS-02 SDK semver, CAP-009) + BC-4.09.001 (SS-04 plugin migration, CAP-009) added to behavioral_contracts (architect-authored in D-318 with S-10.05 as Story Anchor); body BC table expanded with both rows; AC-001 += traces: BC-4.09.001 postcondition 1; AC-005 += traces: BC-4.09.001 postcondition 7; AC-006 += traces: BC-4.09.001 postcondition 8; AC-008 += traces: BC-2.06.001 postcondition 1; Token Budget BC count 2→4 BCs (~37k → ~39k). BC-1.11.003 + BC-1.12.009 H1/cap-anchor freshness verified (no changes needed). |
| v1.4 | 2026-05-06 | D-323 — F-3 fix: AC-002, AC-010 title + body + enumeration swept "four-state" → "five-state" to match BC-1.12.009 H1; AC-010 enumeration extended with 5th row (non-paired: all three correlation fields absent; baseline state and post-Wave-3 default after dual-emit shim removal). D-320 BC-table-row sweep was incomplete; this completes the AC-body propagation. |
