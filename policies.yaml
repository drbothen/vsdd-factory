---
document_type: governance-policy-registry
version: "1.4.12"
last_amended: "2026-07-26 (v1.4.12) — POLICY 5 HARNESS-EMITTED LINE-NUMBER CARVE-OUT (D-914 per F-S2104-P15-004): bats-emitted line numbers in verbatim stdout blocks are exempt from the stable-anchor prohibition; authored prose line-number pins remain fully bound. POLICY 13 NORMALIZED-DOMAIN MANDATE (D-914 per F-S2104-P15-001 fifth-generation class): semantic gates over prose MUST be evaluated on normalized domain (reflow+sentence-split); per-physical-line over soft-wrapped paragraphs forbidden; LINE-REWRAP GREEN proof required. POLICY 15 SAME-AC GATE AUDIT (D-914 per F-S2104-P15-002 HIGH class): gate-hardening wave MUST audit all same-AC sibling gates; per-gate audit table recorded in red-gate-log attestation. All three extensions per D-497 parsimony. [Prior: 2026-07-26 (v1.4.11) — POLICY 13 mutant-derived-gate alternation mandate extension (D-912; D-497 parsimony — extension, not new policy): when a gate predicate is derived from a specific mutant token, the predicate MUST be a POLICY-13 alternation over the syntactic-form class of that token; attestation MUST record at least one synonym-substituted mutant proving the alternation fires RED on the synonym. POLICY 15 attestation-location gate extension (D-912): a fix wave adding/strengthening a bats assertion site MUST NOT be pushed until the matching red-gate-log.md attestation section exists at that commit; literal shell check `grep -c 'assertion-site attestation (<HEAD>)' red-gate-log.md` → 1. Both extensions cite D-912 + F-S2104-P14R-001/002/004 fourth-generation recurrence class. [Prior: 2026-07-24 (v1.4.10) — POLICY 15 ll_n_verbatim_stdout_discipline per-guard-mutant-verification mandate extension (D-889; human-directed 2026-07-24): bats test guards of the form `<cmd> && { false; } || true` (&&{false;}||true inert-guard class) MUST include at least one mutant-proving test vector demonstrating the guard actually fires when triggered. A guard not backed by a mutant-proving test vector is a POLICY 15 HIGH finding. Detection: for each bats guard expression matching `&&[[:space:]]*{[[:space:]]*false[[:space:]]*;[[:space:]]*}[[:space:]]*||[[:space:]]*true`, verify at least one test in the same file asserts the failure path is reached when the trigger condition is true. S-7.14 registered as follow-up for automated CI detection. codified_at updated to D-889. [Prior: 2026-07-24 (v1.4.9) — POLICY 21 no_new_shell_scripts test-double-shim exemption (D-889; human-directed 2026-07-24): test-double shims under `plugins/vsdd-factory/tests/fixtures/` are exempt from the no_new_shell_scripts prohibition by category (not by individual file enumeration). Test-double shims are testing infrastructure that do not ship in release bundles; POLICY 21's platform-agnostic motivation applies to operational tooling, not test harness stubs that exercise bash-version-branching logic in bats. amended_at updated to D-889. [Prior: 2026-07-15 (v1.4.8) — POLICY 21 no_new_shell_scripts grandfather clause (D-846; human-directed 2026-07-15): 5 S-19.01 files explicitly grandfathered — plugins/vsdd-factory/bin/check-stale-verdict.sh; plugins/vsdd-factory/bin/enforce-merge-strategy.sh; plugins/vsdd-factory/tests/fixtures/pr-manager/darwin-leg-preflight.sh; plugins/vsdd-factory/tests/fixtures/pr-manager/stub-bash-version.sh; plugins/vsdd-factory/tests/fixtures/pr-manager/stub-gh.sh. Rationale: S-19.01 spec frozen v1.18 2026-07-11 PRE-POLICY (POLICY 21 codified D-836 2026-07-13); architect-approved ADR-030 §Decision 2/3; merged PR #613 2026-07-13 post-policy — timing collision surfaced F-WG-001 E-19 W1+W2 wave-gate D-845; human-directed grandfather 2026-07-15; migration anchored E-20 factory-tools class migration alongside all other grandfathered .sh. [Prior: 2026-07-13 (v1.4.7) — POLICY 21 no_new_shell_scripts registered (D-836; human-directed 2026-07-13): No new .sh scripts may be added to the repository going forward; new tooling MUST be platform-agnostic (Rust workspace tests, Rust binaries, or WASM plugins). Existing .sh files grandfathered until class-migration (deferred factory-tools program, E-20 anchor). Informed by Rust migration research 2026-07-13. [Prior: 2026-07-11 (v1.4.6) — POLICY 4 ADR-ANCHOR-FIELD PARITY verification_steps extension (v1.4.6; D-817 per F-P58-001 class): VP §Source Contract and §Traceability structured `**ADR:**` anchor fields MUST name the governing ADR (e.g., `ADR-030 §Decision 1/2/3`) consistent with the VP's own §Property Statement ADR cites and sibling-VP convention. A claim of 'not an ADR-documented decision' is forbidden when an ADR §Decision governs a postcondition in the same VP. §Traceability MUST include a `**ADR:**` bullet parallel to the §Source Contract bullet. Detection: grep VP §Source Contract `**ADR:**` line and §Traceability `**ADR:**` line; verify both name the same ADR as cited in §Property Statement PS-* clauses; verify no 'not an ADR-documented decision' claim when an ADR §Decision is load-bearing. Extends standing gate roster to 12. [Prior: 2026-07-11 (v1.4.5) — POLICY 4 INVOCATION-SIGNATURE-FORM PARITY verification_steps extension (v1.4.5; D-815 per F-P57-001 class): VP §Proof Harness Skeleton script-invoking @test blocks MUST invoke scripts with the ADR §Decision canonical invocation form (positional vs named-flag, required positional arguments). The invocation signature is a load-bearing test-fidelity anchor — a harness using the wrong invocation form cannot test the correct behavior. ADR-030 §Decision 2/3 positional/gh-fetch forms are SoT; named-flag `--covered-sha`/`--live-sha`/`--branch`/`--strategy` signatures are superseded. Detection: grep VP §Proof Harness script invocation lines for `--` named-flag patterns vs ADR §Decision canonical form; zero unexpected named-flags expected. When an ADR adjudicates an invocation-form migration, propagation directives MUST enumerate VP §Proof Harness invocation signatures as sweep sites. Extends standing gate roster to 11. [Prior: 2026-07-10 (v1.4.4) — POLICY 4 DESCRIPTION-BEARING ANCHOR-PROSE PARITY verification_steps extension (v1.4.4; D-809 per F-P53-001+F-P53-002 class): traceability-table row descriptions, VP frontmatter module: fields, VP §Traceability Function-anchor/Subsystem/Anchor-story bullets, and VP §Feasibility Artifact cells are ALL description-bearing anchor prose and MUST derive from the target artifact's SoT (BC H1/§Architecture Anchors, story §File Structure/body BC-table). Detection: per-field grep of the described path/semantics against the target SoT; zero-match or contradiction = POLICY 4 mis-anchor MEDIUM+. Supersedes-extends D-808 8th-gate scope (D-808 was epic-traceability description class; this extends to all VP spec-level anchor-prose fields per F-P53-001/002 adjacent-axis). [Prior: 2026-07-10 (v1.4.3) — POLICY 5 v1.3.8 SIBLING-SWEEP CATEGORY EXTENSION (j) registered (D-799 per F-P43-004): body §Property Statement / §Description / §Postcondition inline parenthetical cites (e.g. `(BC-X PCn)`) at same-file semantic-parallel sites extended as category (j). Any anchor migration or PC renumbering MUST sweep these inline cites same-burst with per-cite ground-truth verification. Failure is MEDIUM with [regression] tag. Extension per D-497 parsimony (single-policy-extension, no new POLICY domain). [Prior: 2026-07-09 (v1.4.2) — POLICY 5 v1.3.7 SIBLING-SWEEP CATEGORY EXTENSION (i) registered (D-791 per F-P36-001 META-33 sub-route): same-file aggregation cells that duplicate per-row values (delivery summaries, 'All N distinct' attests, wave-summary Input-hashes lines, index aggregation paragraphs) added as category (i). When any per-row cell is bumped, all same-file aggregation cells duplicating that value MUST be swept same-burst. Failure is MEDIUM with [regression] tag. Extension per D-497 parsimony (single-policy-extension, no new POLICY domain). [Prior: 2026-06-19 (v1.4.1) — POLICY 7 verification_steps editorial-abbreviation-exception clause added (C-P3-001 adjudication; F-SP3-006 re-confirmed S-18.02 LOCAL pass-3): clarifies that the story-body Behavioral Contracts table Title column is an editorial view where subset-of-H1 abbreviation is permitted provided BC ID + version cite are verbatim; POLICY 7 enforcement targets are (i) BC-INDEX ↔ H1 verbatim parity and (ii) downstream-only enrichment prohibition; abbreviation-without-enrichment is at most ADVISORY for adversary. Prevents systematic re-flagging across ~112 stories that use editorial abbreviations. [Prior: 2026-06-14 (v1.4.0) — POLICY 19 adr_version_cite_volatile_pin_prohibition registered (D-567 F2 pass-6 fix burst; state-manager; closes recurring stale-ADR-cite class): BC Traceability rows citing any ADR MUST use stable section-anchor form `ADR-NNN §Decision N` without a load-bearing version number (TD-VSDD-091). Inline ADR version tokens in Traceability cell prose are forbidden as load-bearing identifiers; an informational `(as of vX.Y)` parenthetical is permitted only if explicitly non-load-bearing and ungated. Severity: HIGH. Scope: behavioral-contracts traceability rows. Codified at D-567. [Prior: v1.3.6 (2026-05-29) — POLICY 5 Part B HEAD-reproducibility-or-structural-form mandate + Part D snapshot-rescue-pattern detection per F-SP8-006/F-SP8-PG-001 META-LEVEL-36 cure-extension. Closes META-36 snapshot-annotation-rescue-pattern (rule-Y-of-rule-X admits cure-form defeating rule-X's reproducibility guarantee through fresh-context-loop-asymmetry — adversary works at HEAD, never historical SHA, so snapshot-annotated stdout is non-reproducible in cycle loop). Cure-of-cure-of-cure-OF-cure-OF-cure recursion now at level 6 in POLICY 5 evolution v1.3 → v1.3.1 → v1.3.3 → v1.3.4 → v1.3.5 → v1.3.6. Extension per D-497 parsimony (single-policy-extension, no new POLICY domain). [Prior: v1.3.5 — POLICY 5 META-LEVEL-35 codification per F-SP7-PG-001 (BC-5.39.009 pass-7 fix-burst; D-520). Three-part extension: (Part A) historical-by-construction enumeration for v1.3.4 verification gate — 'historical' sites are EXCLUSIVELY: (i) YAML frontmatter modified[] array entries; (ii) body ## Changelog table rows (between header and section end); (iii) [Prior: ...] nested clauses inside the last_amended: field; (iv) body ## Adversary Pass Coverage entries; (v) cycle lessons.md [codified]/[process-gap] cross-references. ALL OTHER body narrative referencing prior BC/story versions (T-N pseudocode comments, BC Table cells, Token Budget rows, Risk tables, Parity Audit notes, LOCAL Adversary Cascade Plan, AC trace cells, EC table notes, §Architecture Anchors) MUST cite the CURRENT version. Provenance-attribution phrasing ('introduced at BC vX, carried forward in BC vY') acceptable IFF it includes carry-forward-to-current annotation (Y = current version). (Part B) adversary-replay-reproducibility mandate — when capturing literal-shell verification gate stdout, fix-burst MUST: (i) cite parent-commit-SHA at which stdout was captured; (ii) ensure adversary fresh-context replay against the SAME SHA yields IDENTICAL stdout; (iii) if gate predicate yields non-empty stdout at any other SHA (including the committed SHA itself), this is a META-35 violation and the cure is non-reproducible. The captured stdout MUST be reproducible by running the exact predicate against the committed factory-artifacts SHA. (Part C) sibling-sweep category extension — extends v1.3.3 categories (a)-(e) to (a)-(h): (f) NEW — Risk-Mitigation table cells (titles + mitigations + traces); (g) NEW — §Bidirectional Parity Audit Note version-cite cells; (h) NEW — §LOCAL Adversary Cascade Plan prerequisite refs + adversary-MUST-verify-notes. Closes META-LEVEL-35 (cure-claim-of-self-application-asserts-empty-stdout-but-replay-by-fresh-context-adversary-yields-non-empty-stdout) + L-S-15.17-SP7-META-35-replay-reproducibility lesson. Extension chosen per D-497 parsimony (extends POLICY 5 scope). [Prior: v1.3.4 — POLICY 5 sibling-sweep literal-shell VERIFICATION GATE per F-SP6-PG-001 META-LEVEL-34 cure-extension (BC-5.39.009 pass-6 fix-burst). Adds to v1.3.3 sibling-sweep mandate: after enumerating sibling sites, the fix-burst MUST execute literal-shell verification (e.g., `grep -n 'BC v[0-9.]+' <file>`) capturing stdout that proves zero stale references remain (or only historical Changelog rows). The captured stdout MUST be included in the commit message body or burst-log Dim-2/Dim-6 evidence. Sweep claims without literal-shell verification stdout are now MEDIUM-severity findings. Closes META-LEVEL-34 sweep-claim-without-execution recurrence first observed at F-SP6-002 + F-SP6-003 + F-SP6-007. Extension chosen per D-497 parsimony (literal-shell verification == verification-step extension already in POLICY 5 scope). [Prior: v1.3.3 — POLICY 5 sibling-sweep-inside-policy-cure sub-clause per F-SP5-001/004/009/012 META-LEVEL-33 cure-extension (BC-5.39.009 pass-5 fix-burst). Adds to SDK-GROUNDING MANDATE + stable-anchor sub-clause: when a stable-anchor or evidence-form policy is applied to a primary cure site, ALL sibling sites with the same evidence pattern (extractor NOTES in stories, code comments, parallel BC tables, body Changelog cells, Token Budget descriptions) MUST be swept same-burst. Sibling sites are enumerated explicitly in the fix-burst before commit. Extension chosen per D-497 parsimony (sibling-sweep discipline == verification-step extension already in POLICY 5 scope; META-LEVEL-33 is a sub-route of META-LEVEL-32 stable-anchor recurrence). [Prior: v1.3.2 — POLICY 8 EC-mirror routing-rule extension per D-517 F-SP4-016 process-gap closure. When a story adds an EC row referencing a BC anchor, orchestrator MUST dispatch same-burst PO mirror so equivalent EC appears in the BC. Extension chosen per D-497 parsimony (EC propagation == AC propagation already in POLICY 8 scope). [Prior: v1.3.1 — POLICY 5 stable-anchor sub-clause per F-SP4-002 META-LEVEL-32 cure-extension (BC-5.39.009 pass-4 fix-burst). Adds to SDK-GROUNDING MANDATE: all grep captures MUST use stable anchors only (heading prefix `^## `, function signature `^pub fn`, etc.); line numbers are FORBIDDEN in captured stdout; if line numbers appear in a grep capture (from -n flag or file:NNN: format), re-execute with stable-anchor pattern before persisting. Extension does NOT introduce a new POLICY (per D-497 parsimony) — extends existing POLICY 5 v1.3 SDK-grounding mandate because stable-anchor discipline is a necessary sub-constraint of the 'literal-shell grep of actual source' mandate. [Prior: v1.3 — POLICY 5 extension SDK-grounding mandate per D-516 L-S-15.17-SP3-SDK-grounding-mandate lesson. Adds: every BC narrative claim about external artifact (SDK symbol, file path, cycle name, type signature, registry priority, sibling pattern) MUST include literal-shell grep stdout of actual source artifact at BC §SDK Grounding Evidence (or equivalent named section). [Prior: v1.2 — POLICY 8 audit-block-exclusion v1.1→v1.2 per D-515/D-516 META-LEVEL-31 cure-of-cure F-SP3-014. Adds: audit grep target MUST exclude the audit block itself using sed strip or AC-per-PC explicit listing to prevent self-counting false-positives. POLICY 8 extension chosen (not new POLICY 19) because this is a verification-step procedure extension, not a new governance policy domain. POLICY 19 slot reserved.] [Prior: v1.1 — POLICY 8 verification_steps extended with bidirectional AC↔PC parity check requirement per D-515 + META-LEVEL-31 cascade-propagation-gap-from-PC-insertion cure (D-497 parsimony extension).] [Prior: v1.0 — Part A structural remediation; F-PASS14-004 closure]]]]]]]]"
---
# .factory/policies.yaml — Declarative governance policy registry
#
# Each project maintains its own policy list. The adversarial-review skill
# auto-loads this file into the adversary's dispatch rubric. Lint hooks
# read this file to determine which validators to run.
#
# Initialize with: /vsdd-factory:policy-registry init
# Add policies with: /vsdd-factory:policy-add <name>
# Validate with: /vsdd-factory:policy-registry validate
#
# Schema per entry:
#   id: <integer>              # Sequential policy number
#   name: <string>             # snake_case policy flag name
#   description: <string>      # One-line human-readable description
#   adopted: <string>          # Context where adopted (burst, pass, or date)
#   severity: <HIGH|MEDIUM>    # Severity floor for violations
#   enforced_by: [<string>]    # Enforcement mechanisms
#   scope: [<string>]          # Artifact types this policy covers
#   lint_hook: <string|null>   # Path to automated hook (relative to plugin root)
#   verification_steps: [<string>]  # Step-by-step check procedure for the adversary
#                                   # Baseline policies (1-9) have steps baked into agent
#                                   # prompts. Custom policies (10+) MUST include steps
#                                   # here so the adversary knows HOW to verify them.
#   codified_at: <D-NNN|null>  # Decision-log D-NNN at which this policy was formally
#                              # registered as a D-NNN-class governance event. Baseline
#                              # policies (1-12) predate the codified_at convention
#                              # (established by D-472 / S-15.06 F-PASS14-003 closure
#                              # 2026-05-15); their codified_at is null. POLICY 13-18
#                              # were registered at commit b8909832 (2026-05-14) and
#                              # retroactively assigned D-472 per S-15.06.

policies:
  - id: 1
    name: append_only_numbering
    description: "All VSDD identifiers are never renumbered or reused. Retired IDs stay in indexes. Filename slugs are immutable."
    adopted: baseline
    severity: HIGH
    enforced_by: [adversary-prompt, consistency-validator]
    scope: [bc, vp, di, cap, story, hs]
    lint_hook: null
    codified_at: null
    verification_steps:
      - "Scan all index files for retired/removed IDs — verify no active artifact reuses a retired ID"
      - "Verify retired entries have replaced_by or removal justification"
      - "Check that filename slugs match original creation (compare git log if available)"

  - id: 2
    name: lift_invariants_to_bcs
    description: "Every domain invariant (DI-NNN) must be cited by at least one BC's Traceability L2 Invariants field. Orphan invariants are drift."
    adopted: baseline
    severity: MEDIUM
    enforced_by: [adversary-prompt, consistency-validator]
    scope: [di, bc]
    lint_hook: null
    codified_at: null
    verification_steps:
      - "Read domain-spec/invariants.md — extract all DI-NNN IDs"
      - "For each DI-NNN, search BC files for citations in Traceability/L2 Invariants fields"
      - "Report orphan invariants (DI declared but no BC enforces it)"
      - "Verify invariant Scope/enforcer column names BCs that cite it back (bidirectional)"

  - id: 3
    name: state_manager_runs_last
    description: "State-manager agent must be the last to commit in every burst, preventing version-race regressions."
    adopted: baseline
    severity: HIGH
    enforced_by: [orchestrator-rule]
    scope: [story, bc, vp, architecture]
    lint_hook: null
    codified_at: null
    verification_steps:
      - "Review burst commit history — state-manager commits must come after all other agents"
      - "Check for citation staleness — STORY-INDEX/BC-INDEX versions must match latest story-writer/PO output"

  - id: 4
    name: semantic_anchoring_integrity
    description: "Every anchor claim must be semantically correct, not just syntactically valid. Mis-anchoring always blocks convergence."
    adopted: baseline
    severity: MEDIUM
    enforced_by: [adversary-prompt, consistency-validator]
    scope: [bc, vp, story, architecture]
    lint_hook: null
    codified_at: null
    verification_steps:
      - "For each BC, verify declared capability actually describes the BC's purpose per capabilities.md"
      - "For each story, verify subsystems: field references subsystems that own the story's scope per ARCH-INDEX"
      - "For each VP, verify anchor_story builds the test vehicle (not an architectural ancestor)"
      - "For traceability table rows, verify descriptions match target artifact's actual title"
      - "DESCRIPTION-BEARING ANCHOR-PROSE PARITY (v1.4.4; D-809 per F-P52-001+F-P53-001/002 class): traceability-table row descriptions, VP frontmatter module: fields, VP §Traceability Function-anchor/Subsystem/Anchor-story bullets, and VP §Feasibility Artifact cells are ALL description-bearing anchor prose and MUST derive from the target artifact's SoT (BC H1/§Architecture Anchors, story §File Structure/body BC-table). Detection: per-field grep of the described path/semantics against the target SoT; zero-match or contradiction = POLICY 4 mis-anchor MEDIUM+."

  - id: 5
    name: creators_justify_anchors
    description: "Agents creating anchors must justify each choice against the source-of-truth artifact. Stop and ask rather than guess."
    adopted: baseline
    severity: MEDIUM
    enforced_by: [adversary-prompt]
    scope: [bc, vp, story, cap, architecture]
    lint_hook: null
    codified_at: null
    verification_steps:
      - "Check that BC→CAP anchors include justification citing capabilities.md"
      - "Check that story→subsystem anchors include justification citing ARCH-INDEX"
      - "Check that VP→anchor_story assignments justify why that story builds the test vehicle"
      - "Flag mechanical citations without body substantiation"
      - "SDK-GROUNDING MANDATE (v1.3; D-516 per L-S-15.17-SP3-SDK-grounding-mandate): For every BC narrative claim about external artifact (SDK symbol, file path, cycle name, type signature, registry priority, sibling pattern), require literal-shell grep of actual source artifact at BC §SDK Grounding Evidence section (or named equivalent) with captured stdout per POLICY 15. A BC section making claims about external state (e.g., 'HostError::OutputTooLarge variant', 'current_cycle: field in STATE.md', 'Vec<u8> return type in sibling hooks') without corresponding grep evidence in §SDK Grounding Evidence is a MEDIUM-severity finding. This closes the BC-authoring-without-SDK-grounding root cause confirmed at pass-3 (F-SP3-002 + F-SP3-003 + F-SP3-006 share this root). Codified D-516 per cure-extension-parsimony (D-497): extends POLICY 5 'creators justify anchors' rather than introducing new policy."
      - "STABLE-ANCHOR SUB-CLAUSE (v1.3.1; F-SP4-002 META-LEVEL-32 cure-extension): All grep captures in §SDK Grounding Evidence MUST use stable anchors only — heading prefix `^## `, function signature `^pub fn `, regex pattern `^key:`, etc. Line numbers are FORBIDDEN in captured stdout. Specifically: (a) do NOT use grep -n (which prefixes output with file:NNN:); (b) do NOT use sed -n 'NNN,MMMp' as the SOLE capture (acceptable as a secondary confirmation when the primary anchor grep identifies the target, but must not be the only evidence); (c) if captured stdout contains file:NNN: format, re-execute with a stable-anchor pattern before persisting. Rationale: line numbers shift with every edit (F-SP4-002 confirmed `## Phase Progress` drifted from line 61 to 64 in one burst); stable anchors (heading prefix, fn signature) are invariant to line drift."
      - "SIBLING-SWEEP MANDATE (v1.3.3; D-{TBD-state-manager-close} per L-S-15.17-SP5-META-33-sibling-sweep-inside-policy-cure): When applying any stable-anchor (v1.3.1) or SDK-grounding (v1.3) discipline to a primary cure site (e.g., a BC §SDK Grounding Evidence section), the fix-burst MUST enumerate and sweep ALL sibling sites where the same evidence pattern appears. Sibling sites include: (a) extractor NOTES in story T-N task pseudocode comments, (b) code-comment line-pin examples in implementation guides, (c) parallel BC body tables that cite the primary BC (e.g., Behavioral Contracts Table version cells), (d) Token Budget row descriptions, (e) ADR architecture-anchor citations. Failure to sweep a sibling is a MEDIUM-severity finding (with [regression] tag if the cure had been previously applied to the primary). Closes META-LEVEL-33 sibling-sweep-inside-policy-cure recurrence first observed at F-SP4-002 + F-SP5-004 + F-SP5-009 + F-SP5-012. Extension chosen per D-497 parsimony (sibling-sweep discipline == verification-step extension already in POLICY 5 scope; META-LEVEL-33 is a sub-route of META-LEVEL-32 stable-anchor recurrence). Story-writer next burst sweeps F-SP5-004/009/010/011/012 sibling sites (T-5 NOTES grep -n strip, BC Table cell v1.3→v1.5, Token Budget row description, PC11/PC12 cite)."
      - "SIBLING-SWEEP LITERAL-SHELL VERIFICATION GATE (v1.3.4; D-519 per L-S-15.17-SP6-META-34-sibling-sweep-verification-gate): After applying sibling-sweep per v1.3.3 mandate, fix-burst MUST execute literal-shell verification command (e.g., `grep -n 'BC v[0-9.]+' <swept-file>` or `grep -nE '<stale-pattern>' <swept-file>`) and capture verbatim stdout. The captured stdout MUST demonstrate zero non-historical stale references (or only references inside Changelog table rows, which are historical by construction). The stdout MUST be included in the fix-burst commit message body OR in burst-log Dim-2/Dim-6 evidence. Sweep claims without captured-stdout verification are MEDIUM-severity findings (META-34 sub-route). Cure-of-cure-of-cure recursion: applied to POLICY 5 v1.3.3 sibling-sweep mandate. Closes META-LEVEL-34 sweep-claim-without-execution recurrence first observed at F-SP6-002 + F-SP6-003 + F-SP6-007."
      - "HISTORICAL-BY-CONSTRUCTION ENUMERATION (v1.3.5 Part A; D-520 per L-S-15.17-SP7-META-35-replay-reproducibility): For purposes of the v1.3.4 verification gate, 'historical-by-construction' references are EXCLUSIVELY: (i) YAML frontmatter modified: array entries; (ii) body ## Changelog table rows (between the '| Version | Date | Description |' header and section end); (iii) [Prior: ...] nested clauses inside the last_amended: field; (iv) body ## Adversary Pass Coverage entries; (v) cycle lessons.md [codified]/[process-gap] cross-references. ALL OTHER body narrative referencing prior BC/story versions (T-N pseudocode comments, BC Table cells, Token Budget rows, Risk tables, Parity Audit notes, LOCAL Adversary Cascade Plan, AC trace cells, EC table notes, §Architecture Anchors) MUST cite the CURRENT version of the artifact-under-review. Provenance-attribution phrasing ('introduced at BC vX, carried forward in BC vY') is acceptable IFF it includes the carry-forward-to-current annotation (Y = current version). Adversary: when executing the v1.3.4 literal-shell gate, exclude ONLY these 5 historical-by-construction site classes from the 'stale reference' determination. All other stale version cites are violations regardless of prose framing."
      - "HEAD-REPRODUCIBILITY-OR-STRUCTURAL-FORM MANDATE (v1.3.6 Part B; per F-SP8-006/F-SP8-PG-001 META-LEVEL-36 cure-extension): Captured stdout in §SDK Grounding Evidence MUST EITHER (a) be reproducible at HEAD against the current factory-artifacts SHA (the adversary fresh-context loop executes at HEAD; the gate predicate must yield identical stdout when run on current artifacts), OR (b) capture STRUCTURAL-FORM invariants only (regex form, presence counts, header occurrences, structural patterns) that survive monotonic content evolution and remain identical across cycle SHAs. **Snapshot-annotation-only cures (e.g., 'Captured at SHA X — historically valid') are FORBIDDEN** — they admit cure-forms that satisfy the literal letter of (i)/(ii)/(iii) but defeat the reproducibility guarantee via fresh-context-loop-asymmetry. This supersedes the v1.3.5 Part B mandate (which allowed SHA-pinned snapshot cures) by closing the snapshot-annotation-rescue-pattern: any cure that works at SHA X but fails at current HEAD is a META-36 violation. Adversary: when verifying §SDK Grounding Evidence greps, execute them at HEAD — if a grep fails or yields different output, flag as META-36 recurrence even if the BC annotates a historical SHA."
      - "SNAPSHOT-RESCUE-PATTERN DETECTION (v1.3.6 Part D; per F-SP8-006/F-SP8-PG-001 META-LEVEL-36): Adversary fresh-context loop executes captured-stdout gate predicates at HEAD. If gate stdout differs from the BC-attested capture (even when the BC annotates a historical SHA), this is a META-36 violation. The cure MUST be: (a) re-capture at HEAD with reproducible structural-form-only invariants, OR (b) replace the body citation with a re-execute-at-burst-time instruction (e.g., 'see live grep against HEAD STATE.md' rather than pasted stdout). The intent: structural invariants (e.g., 'every state-manager Commit E template includes the trajectory-tail marker') are evergreen; specific stdout snapshots are not. Adversary: for each §SDK Grounding Evidence block that cites a snapshot SHA or contains 'Snapshot: captured at', verify the block also includes HEAD-reproducible structural predicates. If snapshot-only without structural-form predicates, flag as META-36 violation."
      - "SIBLING-SWEEP CATEGORY EXTENSION (a)-(h) (v1.3.5 Part C; D-520 per F-SP7-003 category gap): The v1.3.3 sibling-sweep categories (a)-(e) are extended to (a)-(h). New categories: (f) Risk-Mitigation table cells (titles + mitigations + traces in story §Risk section); (g) §Bidirectional Parity Audit Note version-cite cells (any version reference in the parity audit table or prose); (h) §LOCAL Adversary Cascade Plan prerequisite refs + adversary-MUST-verify-notes (any BC version cited in the cascade plan's prerequisites or verification instructions). When sweeping sibling sites per v1.3.3 mandate, include all 8 categories (a)-(h) in the enumeration. Failure to sweep a category-(f)/(g)/(h) site is a MEDIUM-severity finding with [regression] tag (META-33/META-34 sub-route). Adversary: when verifying sibling-sweep completeness, check categories (f)/(g)/(h) explicitly."
      - "SIBLING-SWEEP CATEGORY EXTENSION (i) (v1.3.7; D-791 per F-P36-001 META-33 sub-route): categories (a)-(h) extended with (i) same-file aggregation cells that duplicate per-row values (delivery summaries, 'All N distinct' attests, wave-summary Input-hashes lines, index aggregation paragraphs). When any per-row cell is bumped, all same-file aggregation cells duplicating that value MUST be swept same-burst. Failure is MEDIUM with [regression] tag."
      - "SIBLING-SWEEP CATEGORY EXTENSION (j) (v1.3.8; D-799 per F-P43-004): categories (a)-(i) extended with (j) body §Property Statement / §Description / §Postcondition inline parenthetical cites (e.g. `(BC-X PCn)`) at same-file semantic-parallel sites. Any anchor migration or PC renumbering MUST sweep these inline cites same-burst with per-cite ground-truth verification. Failure is MEDIUM with [regression] tag."
      - "INVOCATION-SIGNATURE-FORM PARITY (v1.4.5; D-815 per F-P57-001 class): VP §Proof Harness Skeleton script-invoking @test blocks MUST invoke scripts with the ADR §Decision canonical invocation form (positional vs named-flag, including required positional arguments such as `<pr_number>`). The invocation signature is a load-bearing test-fidelity anchor because a mismatched form defeats the script's ability to test the correct behavior (e.g., `--live-sha` bypasses the `gh pr view` fetch that is the load-bearing behavior of check-stale-verdict.sh). Detection: grep VP §Proof Harness script invocation lines for `run bash` and `result=$(bash` patterns; extract argument form; compare against ADR §Decision `**Invocation:**` clause; zero unexpected `--named-flag` arguments expected vs ADR canonical positional form. When an ADR adjudicates an invocation-form migration (e.g., named-flag → positional), propagation directives MUST enumerate VP §Proof Harness invocation signatures as sweep sites alongside BC §Architecture Anchors and story §File Structure. 11th standing gate."
      - "ADR-ANCHOR-FIELD PARITY (v1.4.6; D-817 per F-P58-001 class): VP §Source Contract `**ADR:**` field and §Traceability `**ADR:**` field MUST both name the governing ADR consistent with the VP's own §Property Statement ADR cites. A 'not an ADR-documented decision' claim is forbidden when an ADR §Decision governs a postcondition in the same VP's §Property Statement. §Traceability MUST include a parallel `**ADR:**` bullet when §Source Contract names an ADR (sibling-VP convention: VP-095, VP-097, VP-098 as SoT). Detection: grep VP `\\*\\*ADR:\\*\\*` lines in §Source Contract and §Traceability; verify both cite same ADR as §Property Statement PS-* clauses; verify no 'not an ADR-documented decision' denial when §Property Statement cites that ADR. When an ADR propagation directive enumerates BC §Architecture Anchors and S-NN stories, it MUST also enumerate VP §Source Contract/§Traceability `**ADR:**` anchor fields for VPs whose §Property Statement cites that ADR. 12th standing gate."
      - "HARNESS-EMITTED LINE-NUMBER CARVE-OUT (D-914 per F-S2104-P15-004 pass-15 bare-pin class): line numbers that appear inside verbatim harness-emitted stdout blocks (e.g., bats `# (in test file ..., line NNN)` annotations, `line NNN` failure markers automatically emitted by the bats test framework into fenced code blocks) are EXEMPT from the stable-anchor prohibition — redacting or replacing them would falsify the verbatim record that POLICY 15 (ll_n_verbatim_stdout_discipline) requires. Authored prose and manual comments remain fully bound: bare line-number pins of the form `line ~NNN`, `~:NNN`, `:NNN`, `lines NNN/MMM` in manually-written comment text, docblocks, or narrative prose are forbidden under TD-VSDD-091. The carve-out is tightly scoped to automatically-emitted harness output only; it authorizes no authored line-number pin. Detection: when auditing for TD-VSDD-091 line-pin violations, strip bats-emitted verbatim-stdout fenced blocks (identified by surrounding `Exit code:` / bats output structure) before grepping for bare line-number patterns in manually-authored text."

  - id: 6
    name: architecture_is_subsystem_name_source_of_truth
    description: "ARCH-INDEX Subsystem Registry is the canonical source for subsystem names. All references must match verbatim."
    adopted: baseline
    severity: HIGH
    enforced_by: [adversary-prompt, consistency-validator]
    scope: [bc, story, prd, architecture]
    lint_hook: null
    codified_at: null
    verification_steps:
      - "Read ARCH-INDEX Subsystem Registry for canonical subsystem names"
      - "For sampled BCs, verify subsystem: frontmatter matches registry exactly"
      - "For sampled stories, verify subsystems: field uses registry names"
      - "Check BC-INDEX subsystem column matches ARCH-INDEX"

  - id: 7
    name: bc_h1_is_title_source_of_truth
    description: "BC file H1 heading is the authoritative BC title. All downstream references must match. Enrichment goes INTO the H1."
    adopted: baseline
    severity: HIGH
    enforced_by: [adversary-prompt, consistency-validator]
    scope: [bc, story, prd]
    lint_hook: null
    codified_at: null
    verification_steps:
      - "Sample 10+ BCs — read H1 heading and compare to BC-INDEX title column"
      - "Check for downstream-only enrichment not present in H1"
      - "Verify H1 accurately describes what the BC's postconditions specify"
      - "EDITORIAL-ABBREVIATION EXCEPTION (C-P3-001 adjudication; F-SP3-006 re-confirmed S-18.02 LOCAL pass-3 2026-06-19): The story body `## Behavioral Contracts` TABLE TITLE COLUMN is an EDITORIAL VIEW. Subset-of-H1 abbreviation is PERMITTED in that column provided: (a) the BC ID in the table row is verbatim correct, (b) the version cite (vX.Y) in the table row is verbatim current, and (c) the PC/clause list is present. POLICY 7's enforcement target is (i) BC-INDEX Title column ↔ BC file H1 verbatim parity, and (ii) prohibition on downstream-only ENRICHMENT that is NOT present in the H1 (e.g., adding qualifiers that go beyond the H1's scope). POLICY 7 does NOT require verbatim H1 reproduction in story-body Behavioral Contracts table descriptive title cells. An abbreviation that omits non-essential words (e.g., omitting 'WASM gate blocks HandoffIncomplete on PostToolUse HANDOFF.md writes' down to 'WASM gate') is editorial economy, not a POLICY 7 violation — provided the abbreviation does not introduce text not in the H1 (enrichment) and does not change the meaning. Adversary: when checking story body BC tables, flag ENRICHMENT (title cell text not derived from H1) as POLICY 7 HIGH. Flag ABBREVIATION (title cell is a strict substring/paraphrase of H1) as POLICY 7 at most ADVISORY — do NOT flag as HIGH or MEDIUM absent actual enrichment or an incorrect BC ID/version cite."

  - id: 8
    name: bc_array_changes_propagate_to_body_and_acs
    description: "When a story bcs: frontmatter changes, the same commit must update body BC table, ACs, and Token Budget count."
    adopted: baseline
    severity: HIGH
    enforced_by: [adversary-prompt, consistency-validator]
    scope: [story, bc]
    lint_hook: null
    codified_at: null
    verification_steps:
      - "Sample 5+ stories — for each BC in behavioral_contracts: frontmatter (canonical field; alias: bcs: for S-15.03 only), verify it appears in body BC table"
      - "For each BC in behavioral_contracts: frontmatter, verify at least one AC traces to it"
      - "For each BC in body BC table or AC traces, verify it appears in behavioral_contracts: frontmatter"
      - "Verify Token Budget BC count matches len(behavioral_contracts)"
      - "After any PC insertion/deletion/renumbering in BC, story-writer MUST run literal-shell bidirectional AC↔PC parity check (grep PC cites in story per PC in BC; grep BC for each AC PC cite) with captured stdout per POLICY 15. Captured stdout MUST appear in fix-burst commit body or story §Bidirectional Parity Audit Note. Prevents cascade-propagation-gap-from-PC-insertion (META-LEVEL-31 CANDIDATE; D-515)."
      - "AUDIT-BLOCK EXCLUSION MANDATE (v1.2; F-SP3-014 META-LEVEL-31 cure-of-cure): The parity check grep MUST exclude the audit block itself from the grep target to prevent self-counting false-positives. Implement via ONE of: (a) sed strip before grep: `sed '/^## Bidirectional Parity Audit Note/,/^## /d' story.md | grep -c \"BC-5.39.009 PC6\"` (strip audit section before counting); OR (b) explicit AC-per-PC listing: audit MUST list AC numbers per PC explicitly (e.g., 'PC6: AC-7' instead of 'PC6: 1 cite') — so self-counting produces no false-positive (the audit line says 'AC-7' not a count that would increment). A parity audit whose grep target includes the audit block's own stdout is structurally self-counting and constitutes a MEDIUM-severity finding per META-LEVEL-31."
      - "EC-MIRROR ROUTING-RULE (v1.3; D-517 F-SP4-016 process-gap closure): When a story adds an Edge Case (EC) row that references a BC anchor (PC, invariant, AC-class, or §Architecture Anchors), the orchestrator MUST dispatch a same-burst PO mirror task. The PO mirror MUST add an equivalent EC row in the BC with consistent ID and trigger/expected-behavior/test-vector form. The story-writer MUST NOT leave `[needs-po]` placeholder text in the story for EC mirrors deferred across bursts (closes F-SP4-016). The story-writer is responsible for flagging new ECs with `[needs-po-mirror]` to signal the orchestrator. Adversary MUST verify: for any EC in the story body that names a BC PC, an equivalent EC exists in the BC body with matching semantics."

  - id: 9
    name: vp_index_is_vp_catalog_source_of_truth
    description: "VP-INDEX.md is authoritative. Changes must propagate same-burst to verification-architecture.md and verification-coverage-matrix.md."
    adopted: baseline
    severity: HIGH
    enforced_by: [adversary-prompt, lint-hook, consistency-validator]
    scope: [vp, architecture]
    lint_hook: hooks/validate-vp-consistency.sh
    codified_at: "D-472"
    verification_steps:
      - "Verify VP-INDEX arithmetic: total = sum of per-tool counts = row count"
      - "For each VP in VP-INDEX, verify it appears in verification-architecture.md Provable Properties Catalog"
      - "For each VP in VP-INDEX, verify it appears in verification-coverage-matrix.md VP-to-Module table"
      - "Sum coverage matrix per-tool columns — must equal VP-INDEX per-tool totals"
      - "Reverse check: every VP in arch docs must exist in VP-INDEX"

  - id: 10
    name: demo_evidence_story_scoped
    description: "All demo-recorder output MUST live under docs/demo-evidence/<STORY-ID>/. No flat files at docs/demo-evidence/*.md."
    adopted: baseline
    severity: HIGH
    enforced_by: [adversary-prompt, consistency-validator, pr-reviewer, lint-hook]
    scope: [story, demo-evidence, pr]
    lint_hook: hooks/validate-demo-evidence-story-scoped.sh
    codified_at: "D-472"
    verification_steps:
      - "For each story's feature branch, verify docs/demo-evidence/<STORY-ID>/ exists"
      - "Verify NO files exist at docs/demo-evidence/*.md — only subfolders"
      - "Cross-check: subfolder name matches the story's frontmatter story_id"

  - id: 11
    name: no_test_tautologies
    description: "test_BC_* and test_TV_* functions must call a production fn (emit_*, process_*, validate_*, etc.) — they cannot only assert on data they themselves constructed. Promotes Check 8 of validate-consistency from advisory to gating."
    adopted: 2026-04-27
    severity: MEDIUM
    enforced_by: [validate-consistency, adversary-prompt]
    scope: [test, story]
    lint_hook: null
    codified_at: null
    verification_steps:
      - "Run validate-consistency Check 8 (Test Tautology Detector)"
      - "For each FLAGGED row in the Advisories section, treat as a blocking finding"
      - "Recognized opt-outs: function carries `/// data-shape pin` doc-comment, OR file-level `//! tautology-allowed: <reason>` declaration"
      - "Reference fixtures: ${CLAUDE_PLUGIN_ROOT}/skills/validate-consistency/fixtures/tautology/"

  - id: 12
    name: bc_tv_emitter_consistency
    description: "When a BC's Canonical Test Vectors table marks a field excluded (e.g., 'Field X in Entry? = No'), the production struct must not unconditionally serialize that field. Promotes Check 9 of validate-consistency from advisory to gating."
    adopted: 2026-04-27
    severity: HIGH
    enforced_by: [validate-consistency, adversary-prompt]
    scope: [bc, code]
    lint_hook: null
    codified_at: null
    verification_steps:
      - "Run validate-consistency Check 9 (BC Canonical TV ↔ Emitter Field Consistency)"
      - "For each FLAGGED row in the Advisories section, treat as a blocking finding"
      - "Recognized opt-outs: BC frontmatter `tv_emitter_check: skip` or `tv_emitter_check: manual`, OR row notes column says 'field tolerated when present'"
      - "Reference fixtures: ${CLAUDE_PLUGIN_ROOT}/skills/validate-consistency/fixtures/bc-tv-consistency/"

  - id: 13
    name: hh_n_regex_alternation_predicates
    description: "Pre-fix scope-determination grep predicates MUST use regex-alternation (-E flag with | alternation) covering all syntactic forms of canonical-violation pattern, not single literal strings. Each predicate covers all permutations (table-cell vs heading vs prose vs lowercase descriptive)."
    adopted: "2026-05-14 (E-10 pass-13 fix burst D-468; codifies HH-4 structural resolution from pass-13 §7)"
    severity: HIGH
    enforced_by: [adversary-prompt, architect-prompt, state-manager-prompt]
    scope: [bc, vp, story, epic, architecture, all-spec]
    lint_hook: null
    codified_at: D-472
    verification_steps:
      - "Adversary: when surfacing a canonical-violation finding, predicate must use regex-alternation grep (-E flag with alternation), not single literal string"
      - "Architect: pre-fix scope-determination grep at fix-burst time must use HH-N predicate with regex-alternation covering all syntactic forms"
      - "State-manager: post-fix verification grep must use the same regex-alternation form; verbatim stdout captured per POLICY 15 (LL-N)"
      - "MUTANT-DERIVED-GATE ALTERNATION MANDATE (v1.4.11; D-912 + F-S2104-P14R-001 third-generation class; D-497 parsimony extension): when a bats gate predicate is added in response to a specific mutant token T (e.g., a gate keyed on 'CWD-relative' in response to the M-P14-A polarity-inversion vector), the predicate MUST be a POLICY-13 alternation over the syntactic-form CLASS of T — all tokens a doc writer might use to express the same concept (e.g., 'CWD-relative|worktree-relative|relative[[:space:]]+path' for the class of 'prohibited-path-form subjects'). The attestation MUST record at least one synonym-substituted mutant (a vector substituting a class-member synonym for T) proving the alternation fires RED on the synonym AND GREEN on the original text. A gate whose attestation records only the originating vector T but no synonym-substituted vector is structurally incomplete — it proves coverage of T but not the class. Detection: for each bats gate that is labeled as 'added in response to mutant X', grep the attestation for a synonym-substituted vector beyond X; if none present, flag as POLICY 13 HIGH (mutant-derived-gate alternation gap). A claim of 'polarity-COMPLETE' or 'class-complete' is forbidden until a synonym-substituted mutant is RED-proven."
      - "NORMALIZED-DOMAIN MANDATE (D-914 per F-S2104-P15-001 fifth-generation class; D-497 parsimony extension): semantic gates over prose MUST be evaluated on normalized domain — reflow the target paragraph into one joined block before applying predicates (e.g., `joined_block=$(echo \"$section\" | tr '\\n' ' ')`), then optionally sentence-split (e.g., `sentences=$(echo \"$joined_block\" | sed 's/\\. /\\n/g')`). Per-physical-line grep over a soft-wrapped markdown paragraph is FORBIDDEN as a polarity gate because line-wrap position is not semantic content and any physical-line predicate can be bypassed by reformatting the same content across different line boundaries. A LINE-REWRAP mutant (same semantic content, different word-wrap position) MUST be attested GREEN in the attestation to confirm the gate is wrap-invariant before it is shipped. Detection: for each bats gate that operates over a multi-line markdown paragraph (section extracted via awk or sed), verify the implementation normalizes first with `tr '\\n' ' '` before applying grep predicates; a bare `grep 'pattern' <section>` or `echo \"$section\" | grep` without tr normalization over a known-wrapped paragraph is a POLICY 13 HIGH finding (per-physical-line gate over wrapped prose). A gate whose attestation lacks a LINE-REWRAP GREEN proof is structurally incomplete — the LINE-REWRAP proof is a necessary attestation element parallel to the deletion-mutant proof."

  - id: 14
    name: kk_n_tripartite_parity_gate
    description: "When a body changelog row is added with version v(N+1), the same burst MUST sync ALL 5 parity legs: (1) version: frontmatter, (2) body Changelog row, (3) frontmatter modified[] array, (4) frontmatter last_amended: text-prefix, (5) upstream-index body-table cells citing the bumped artifact. Codified at D-468 (3-leg); extended at D-490 to 5-leg quintuple parity per INV-020 CONFIRMED."
    adopted: "2026-05-14 (D-468; codifies KK-2 tripartite-parity from E-10 pass-11/12 discipline); extended 2026-05-20 (D-490; codifies INV-020 5-leg quintuple parity per pass-5 F-BC006P5-001 + F-BC006P5-002)"
    severity: HIGH
    enforced_by: [state-manager-prompt, consistency-validator, pre-commit-hook]
    scope: [bc, vp, story, epic]
    lint_hook: null
    codified_at: D-472
    extended_at: D-494
    verification_steps:
      - "For each file with body Changelog section, verify latest row date matches frontmatter last_amended: date prefix"
      - "Verify frontmatter modified: array's last entry corresponds to latest body Changelog row"
      - "Verify frontmatter version: matches body Changelog row's version cell"
      - "Verify frontmatter last_amended: TEXT PREFIX (e.g., '(v1.6) — ...') matches body Changelog top row's version (5-leg leg-4; codified D-490)"
      - "Verify upstream-index body-table cells citing the bumped artifact (e.g., BC-INDEX body table for BC bumps; STORY-INDEX body table for story bumps) reflect the new version same-burst (5-leg leg-5; codified D-490)"
      - "All five legs must be updated in the same burst — no deferred-frontmatter pattern; no deferred-upstream-index pattern"
      - "State-manager codification bursts that bump any of the 4 indexes (BC/VP/STORY/ARCH) MUST run a literal-shell self-application gate BEFORE commit: for each of 4 indexes, grep ^version: and ^last_amended:, extract the v-prefix, assert match. Template: `for IDX_PATH in BC-INDEX.md VP-INDEX.md STORY-INDEX.md ARCH-INDEX.md; do V=$(grep -E '^version:' \"$IDX_PATH\" | grep -oE '\"[0-9]+\\.[0-9]+\"' | tr -d '\"'); LA=$(grep -E '^last_amended:' \"$IDX_PATH\" | grep -oE '\\(v[0-9]+\\.[0-9]+\\)' | head -1 | tr -d '()v'); [ \"$V\" = \"$LA\" ] && echo PASS || echo FAIL; done`. Gate produces zero FAIL output before commit (5-leg leg-4 self-application; codified D-494 per INV-020 RECURRENCE at the 4-index codifying-burst level)."

  - id: 15
    name: ll_n_verbatim_stdout_discipline
    description: "Literal-shell evidence at persistence layer (changelog rows, decision-log rows, commit bodies) MUST include both command verbatim AND captured file:line: stdout verbatim, OR sentinel '(zero output)' / '(EXIT:1; no matches)'. Narrative paraphrase of grep outcomes is NOT compliant. Extended at D-889 to include per-guard mutant-verification mandate for bats test guards."
    adopted: "2026-05-14 (D-468; codifies LL-3 strict-form elevating LL-2 hybrid-command+narrative-output to full file:line: requirement); extended 2026-07-24 (D-889 PER-GUARD-MUTANT-VERIFICATION-MANDATE; human-directed; &&{false;}||true inert-guard class)"
    severity: HIGH
    enforced_by: [adversary-prompt, state-manager-prompt, consistency-validator]
    scope: [arch-index, bc-index, vp-index, story-index, cycle-decision-log, cycle-burst-log, bats-test-guards]
    lint_hook: null
    codified_at: D-472
    extended_at: D-889
    verification_steps:
      - "Adversary: when verifying a closure claim, check the changelog/decision-log row for verbatim file:line: stdout content (not just command text)"
      - "State-manager: when writing changelog/decision-log rows asserting grep verification, include verbatim stdout (file:line: matched-text) or sentinel (zero output) or (EXIT:1; no matches)"
      - "Narrative paraphrase like 'all rows are legitimate exclusions' or 'zero live violations' is forbidden — must show verbatim file:line: evidence"
      - "LL-2 hybrid form (verbatim command + narrative output) is NOT compliant; LL-3 requires both command AND verbatim output"
      - "PER-GUARD MUTANT-VERIFICATION MANDATE (D-889; human-directed 2026-07-24): bats test guards of the form `<cmd> && { false; } || true` (inert-guard class: &&{false;}||true pattern) MUST include at least one mutant-proving test vector in the same test file demonstrating the guard ACTUALLY FIRES when the trigger condition is true. A guard matching this pattern that has no mutant-proving test is a POLICY 15 HIGH finding. Detection: grep test files for the pattern `&&[[:space:]]*{[[:space:]]*false[[:space:]]*;` (or equivalent inert-guard signature); for each match, verify at least one @test block asserts the failure path is exercised when the trigger condition holds. S-7.14 (bats inert-guard lint, D-889) registered for automated CI detection of this class."
      - "ATTESTATION-LOCATION GATE (v1.4.11; D-912 + F-S2104-P14R-002/004 fourth-generation class): a fix wave that adds or strengthens any bats assertion site MUST NOT be pushed until the matching red-gate-log.md attestation section EXISTS at that commit. Literal shell check: `grep -c 'assertion-site attestation (<HEAD-SHA>)' red-gate-log.md` → 1 (where <HEAD-SHA> is the actual SHA being pushed). If the count is 0, the push is BLOCKED until the state-manager appends the attestation section and bundles it in the same commit per TD-VSDD-053. The attestation section heading MUST be `### <Pass-N> assertion-site attestation (<HEAD-SHA>)` so the check is SHA-bound and cannot be satisfied by a prior pass's section. Detection: adversary verifies that for the reviewed_head SHA, a matching `### *attestation (reviewed_head)` section exists in red-gate-log.md; if absent, flag as POLICY 15 HIGH (attestation-location violation). Root cause of the 4th-generation recurrence: test-writer wrote attestation content directly into the shipped bats file (wrong owner) instead of coordinating with state-manager to update red-gate-log.md — POLICY 15 now explicitly governs location, not just content."
      - "SAME-AC GATE AUDIT (D-914 per F-S2104-P15-002 HIGH class; D-497 parsimony extension): a gate-hardening wave that adds or strengthens any gate for AC-NNN MUST audit ALL existing same-AC sibling gates to the same hardening standard (polarity-complete: both affirmative and negative legs; domain-normalized: sentence-scoped per POLICY 13 NORMALIZED-DOMAIN MANDATE; mutant-proven: at least one deletion mutant RED). The audit result MUST be recorded in the red-gate-log attestation section as a per-gate same-AC audit table with columns: Gate, Domain shape, Polarity-asserting, Mutant coverage. A gate-hardening wave whose attestation does not include this table for the hardened AC is incomplete; the next adversary pass may reject the closure as partial. Detection: adversary verifies that for any gate-hardening wave touching AC-NNN, the red-gate-log attestation section contains a same-AC audit table covering all AC-NNN gates; if absent or incomplete, flag as POLICY 15 HIGH (same-AC gate audit absent). Root cause of F-S2104-P15-002: pass-14R added Gate 6 for AC-001(a)(ii) without auditing it against the polarity-blind class being fixed in Gate 1 (AC-001(a)(i)) in the same wave; the new gate re-seeded the defect class one pass later in the sibling clause."

  - id: 16
    name: mm_n_cross_cycle_namespace_gate
    description: "D-NNN identifiers are GLOBAL namespace across all cycles. State-manager MUST allocate next-D-NNN > max(all existing D-NNN across all cycle decision-logs). Any body content citing a D-NNN must be verifiable at citation-authoring time. Planning-stage placeholder D-NNNs must use sentinel form D-{TBD-pass-N-fix-burst} until allocated."
    adopted: "2026-05-14 (D-468; codifies MM allocation gate + MM-2 citation-authoring scope extension from pass-13 §7)"
    severity: HIGH
    enforced_by: [state-manager-prompt, architect-prompt, pre-commit-hook]
    scope: [all-cycles, decision-log, spec, story]
    lint_hook: null
    codified_at: D-472
    verification_steps:
      - "State-manager: pre-burst, run grep across all cycle decision-logs to find global D-NNN max; assert next-allocation is global-max+1"
      - "Architect: when authoring body changelog rows citing D-NNN, verify D-NNN exists in appropriate cycle's decision-log AT THAT TIME (citation-authoring scope)"
      - "Planning-stage placeholders use sentinel form like D-{TBD-pass-N-fix-burst}; must NOT use bare D-NNN before allocation"
      - "Cross-cycle: D-NNN from F5 cycle must not be reused in brownfield cycle and vice versa; global namespace is shared"

  - id: 17
    name: nn_n_frontmatter_parity_full_file_type_scope
    description: "KK-N tripartite-parity discipline (POLICY 14) applies to all 4 file-type layers: BC, epic, story, VP. Each file type has the same body Changelog, frontmatter last_amended:, and frontmatter modified: requirement at fix-burst time."
    adopted: "2026-05-14 (D-468; codifies NN-2 epic+story+VP scope extension from pass-13 §7 F-PASS13-002)"
    severity: HIGH
    enforced_by: [state-manager-prompt, architect-prompt, consistency-validator]
    scope: [bc, epic, story, vp]
    lint_hook: null
    codified_at: D-472
    verification_steps:
      - "For each spec/story/epic/VP file with a body Changelog section, verify NN-2 frontmatter parity (last_amended + modified[] + version) — same predicates as POLICY 14 (KK-N) but applied to all 4 file types"
      - "Adversary: when a fix burst touches an epic or VP, verify NN-2 parity was applied (not just BC-level KK-N)"
      - "State-manager: at seal-time, grep for last_amended and modified: in all files touched by architect in the burst"

  - id: 18
    name: oo_input_hash_mechanical_verification
    description: "Any narrative claim of input-hash: change MUST be backed by literal bin/compute-input-hash invocation with captured stdout in burst evidence. inputs: array must include all files whose modifications would trigger the input-hash change."
    adopted: "2026-05-14 (D-468; codifies OO discipline from pass-13 §9 F-PASS13-004 — narrative-vs-content drift class)"
    severity: MEDIUM
    enforced_by: [state-manager-prompt, architect-prompt, pre-commit-hook]
    scope: [bc, vp, story, architecture]
    lint_hook: null
    codified_at: D-472
    verification_steps:
      - "When a file's input-hash: field changes in a burst, verify the burst evidence contains literal bin/compute-input-hash invocation + captured stdout matching the new hash"
      - "Verify the file's inputs: array lists every file whose modification triggered the hash change"
      - "If bin/compute-input-hash is not available, document this explicitly and note hash was verified against prior burst narrative; flag for automation in S-15.03 PRIORITY-A"
      - "Reject narrative-attestation-only input-hash drift claims without tool invocation evidence"

  - id: 19
    name: adr_version_cite_volatile_pin_prohibition
    description: "BC Traceability rows citing any ADR MUST use stable section-anchor form `ADR-NNN §Decision N` without a load-bearing version number (TD-VSDD-091). Inline ADR version tokens in Traceability cell prose are forbidden as load-bearing identifiers; an informational `(as of vX.Y)` parenthetical is permitted only if explicitly non-load-bearing and ungated."
    adopted: "2026-06-14 (D-567 F2 E-18 pass-6 fix burst — closes recurring stale-ADR-cite class; architect codification confirmed in ADR-026 v1.6 §BC Traceability Cite Convention)"
    severity: HIGH
    scope: behavioral-contracts traceability rows
    enforced_by: [adversary-prompt, consistency-validator]
    lint_hook: null
    codified_at: D-567
    verification_steps:
      - "For each sampled BC Traceability table row that cites an ADR (e.g., '| ADR | ADR-026 v1.5 Decision 10 ... |'), verify the cite does NOT include a load-bearing version token (vX.Y) as a gating identifier"
      - "The correct stable form is `ADR-NNN §Decision N` or `ADR-NNN §<section-name>` (e.g., 'ADR-026 §Decision 10 (PreCompact flush lifecycle...)')"
      - "An informational parenthetical `(as of vX.Y)` is acceptable ONLY if: (a) it is syntactically parenthetical (not the primary reference), AND (b) it is explicitly annotated as non-load-bearing, AND (c) the gate condition does not depend on the version token being current"
      - "Any Traceability row that would fail if the ADR were amended (because the version token would be stale) is a POLICY 19 violation — flag as HIGH"
      - "When a fix burst migrates a stale version cite, run tree-wide grep to enumerate all BC Traceability rows that still cite the ADR with version tokens; complete the migration in the same burst (sibling-sweep per POLICY 5 v1.3.3)"

  - id: 20
    name: release_bundle_no_dev_samples
    description: "Release bundles MUST NOT include development sample artifacts (e.g. hello-hook.wasm) or WASMs unreferenced by both hooks-registry.toml and resolvers-registry.toml. Orphan status requires the dual-registry check."
    adopted: "2026-07-06 (D-753 E-19 pass-2 fix burst; architect O-P2-001 adjudication + S-19.04 + L-BB-orphan-status-requires-dual-registry-check)"
    severity: HIGH
    enforced_by: [adversary-prompt, lint-hook]
    scope: [release, bundle]
    lint_hook: null
    codified_at: D-753
    verification_steps:
      - "Check release bundle WASM contents against hooks-registry.toml + resolvers-registry.toml union set"
      - "Flag any WASM file present in the bundle but absent from BOTH registries — this is an orphan artifact"
      - "Apply dual-registry check: orphan status requires absence from BOTH hooks-registry.toml AND resolvers-registry.toml; presence in either registry means NOT orphan"
      - "Development sample artifacts (e.g. hello-hook.wasm) are NEVER release artifacts regardless of registry presence; verify no hello-hook.wasm or analogous dev-sample names are included"
      - "Verify release bundle contains zero WASMs whose name or content indicates a development/testing origin (stub size ≤103B, 'hello' prefix, 'sample' prefix, underscore-prefixed duplicates of production WASMs)"

  - id: 21
    name: no_new_shell_scripts
    description: "No new .sh scripts may be added to the repository going forward — new tooling MUST be platform-agnostic (Rust workspace tests, Rust binaries, or WASM plugins). FULL SCOPE: operational bin/ tools, hooks, CI/test helpers. TEST-DOUBLE-SHIM EXEMPTION (D-889 2026-07-24; human-directed): test-double shim scripts under plugins/vsdd-factory/tests/fixtures/ are exempt by category (not by individual file enumeration) — they are testing infrastructure, do not ship in release bundles, and POLICY 21's platform-agnostic motivation applies to operational tooling, not test harness stubs that exercise bash-version-branching logic in bats. Existing .sh files are grandfathered until class-migration (deferred factory-tools program, E-20 anchor). Human-directed 2026-07-13. 5 S-19.01 files EXPLICITLY GRANDFATHERED at D-846 2026-07-15 (timing collision; human-directed; see grandfather_clause field)."
    adopted: "2026-07-13 (D-836 POST-W1-REGISTRATION burst; human-directed; informed by Rust migration research 2026-07-13); amended 2026-07-15 (D-846 POLICY-21-GRANDFATHER-CLAUSE; human-directed; F-WG-001 grandfather for 5 S-19.01 files: timing collision — spec frozen pre-POLICY, merged post-POLICY; ADR-030 §Decision 2/3 architect-approved; E-20 migration anchor); amended 2026-07-24 (D-889 TEST-DOUBLE-SHIM-EXEMPTION; human-directed; plugins/vsdd-factory/tests/fixtures/ exempt by category)"
    severity: blocking
    enforced_by: [adversary-prompt, state-manager-prompt]
    scope: [repository-wide]
    lint_hook: null
    codified_at: D-836
    amended_at: D-889
    grandfather_clause:
      decision: D-846
      rationale: "S-19.01 spec frozen v1.18 2026-07-11 PRE-POLICY (POLICY 21 codified D-836 2026-07-13); architect-approved ADR-030 §Decision 2/3; merged PR #613 2026-07-13 post-policy — timing collision surfaced at E-19 W1+W2 wave-gate (F-WG-001, D-845); human-directed grandfather 2026-07-15; migration anchored to the E-20 factory-tools class migration alongside all other grandfathered .sh"
      files:
        - plugins/vsdd-factory/bin/check-stale-verdict.sh
        - plugins/vsdd-factory/bin/enforce-merge-strategy.sh
        - plugins/vsdd-factory/tests/fixtures/pr-manager/darwin-leg-preflight.sh
        - plugins/vsdd-factory/tests/fixtures/pr-manager/stub-bash-version.sh
        - plugins/vsdd-factory/tests/fixtures/pr-manager/stub-gh.sh
    test_double_shim_exemption:
      decision: D-889
      rationale: "Test-double shims in plugins/vsdd-factory/tests/fixtures/ are bats testing infrastructure that exercise bash-version-branching logic; they do not ship in release bundles; POLICY 21's platform-agnostic motivation applies to operational tooling not test harness stubs; human-directed 2026-07-24"
      scope: "plugins/vsdd-factory/tests/fixtures/"
    verification_steps:
      - "Verify no new .sh files were added to bin/, plugins/vsdd-factory/hooks/, .github/workflows/, or any other repository location (excluding plugins/vsdd-factory/tests/fixtures/ — see TEST-DOUBLE-SHIM EXEMPTION) in this burst or PR"
      - "If a new shell script is found outside the exempt path: flag as POLICY 21 BLOCKER — route to implementer for Rust/WASM replacement"
      - "Existing .sh files (grandfathered): verify they are on the pre-D-836 list OR explicitly listed in grandfather_clause.files (D-846); no modifications that expand their scope are allowed without human approval"
      - "GRANDFATHER CLAUSE GATE (D-846): exactly 5 files are explicitly grandfathered — plugins/vsdd-factory/bin/check-stale-verdict.sh; plugins/vsdd-factory/bin/enforce-merge-strategy.sh; plugins/vsdd-factory/tests/fixtures/pr-manager/darwin-leg-preflight.sh; plugins/vsdd-factory/tests/fixtures/pr-manager/stub-bash-version.sh; plugins/vsdd-factory/tests/fixtures/pr-manager/stub-gh.sh. These MAY remain as .sh through E-20 factory-tools migration. Any PR adding NEW .sh files beyond this explicit list (and outside the test_double_shim_exemption scope) remains a POLICY 21 BLOCKER. Adversary: verify grandfather_clause.files contains exactly these 5 entries; any addition to the list without a D-NNN + human direction is itself a POLICY 21 violation."
      - "TEST-DOUBLE-SHIM EXEMPTION GATE (D-889): new .sh files under plugins/vsdd-factory/tests/fixtures/ are EXEMPT from POLICY 21 — they are test-double shims (bats testing infrastructure). Adversary: when a new .sh file is found under that path, confirm it is a test-double shim (stub or fixture for bats testing) before applying the exemption; operational scripts masquerading as fixtures are still POLICY 21 violations."
      - "New tooling that would previously have been a .sh script MUST be implemented as: (a) a Rust workspace test, (b) a Rust binary in crates/, or (c) a WASM plugin in hook-plugins/"
      - "Cross-platform compatibility is the primary motivation — Rust/WASM alternatives compile and run identically on Darwin-arm64, Darwin-x86_64, Linux-x86_64, Linux-musl, and Windows-x86_64"
